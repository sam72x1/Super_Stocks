# 🕵️📈🌙 `T-OPHOLD-2` — تسجيلٌ مسبقٌ **حاكم**: الحملُ إلى **أوّلِ إغلاقٍ نظاميٍّ بعد المِرساة** — فيغطّي المجتمعَ كلَّه لا ‏64% منه

> **مدفوعٌ ومدموجٌ قبل أيّ سطرِ أداةٍ وقبل أيّ رقمٍ من موضوعه.** أمرُ المالك **«سجّل
> الحمل 2»** (‏2026-09-16) بعد أن أخرجت **جدوى `T-OPHOLD`** (التشغيلة `35155775931`)
> أن الذراعَ الحاكمةَ هناك **لا تُغطّي خُمسَ مجتمعها** فالأرضيّةُ ساقطةٌ والحكمُ
> «لا حكم» محتومٌ حسابيًّا.
> **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · لا عتبةَ تتحرّك · لا
> أوامرَ تداولٍ ولا وسيطَ تداول · ولا وكلاء.**

---

## ⓪ إعلانُ التلوّث — كلُّ رقمٍ رأيتُه من جدوى `T-OPHOLD`، وما يُقيّده

**رأيتُ من التشغيلة `35155775931` (وضعُ جدوى · صفرُ `R` مُثبَتٌ بعدّاد نداءات):**

| ما رأيتُه | القيمة |
|---|---|
| المجتمعُ بعد `V-H1` | **‏481** صفًّا · `H1` **‏289** · `H2` **‏192** |
| قابلٌ للتداول تحت `HC` (إغلاقُ نظاميِّ اليوم نفسِه) | **‏310** · `H1` **‏184** · `H2` **‏126** |
| غيرُ قابلٍ بنيويًّا (`e5 ≤ alow`) | **‏89** |
| أزواجُ `C-MOM` | `D0` **‏120** · `D1` **‏120** · بلا بديلة **‏70** |
| **حصّةُ الوقف تحت `HC`** | `H1` **‏70.1%** · `H2` **‏54.0%** |
| حرّاسٌ عبرت | `V-H1` مطابقةٌ بت-بت · `V-H2` = `H0` يطابق `P0` في 481 صفًّا |

🔑 **والاستنتاجُ البنيويّ الذي وَلَّد هذا العقد:** `HC` تُعرَّف «إغلاقُ الجلسة
النظاميّة **من يوم المِرساة**» — و**مِرساةُ الافتر لا جلسةَ نظاميّةً بعدها في يومها**
⇒ `simulate` تُرجع `None` فتسقط **‏92 مِرساةَ افتر** (‏19% من المجتمع) بلا رقم.
‏310 = 481 − 89 − (الافترُ وما لا شمعةَ بعده). **وهذا تطبيقٌ أمينٌ لعقدٍ مدموج، لا عطبُ
كود** — والعقدُ الأوّل **لا يُعدَّل**، فيُسجَّل محورٌ جديد.

**وستّةُ قيودٍ تنشأ عن التلوّث وتُكتب الآن:**

1. 🔑 **الكمّيّاتُ الحاكمةُ ما زالت عمياء:** لم أرَ `R` واحدة · ولا عائدًا بعد تكلفة ·
   ولا فاصلًا · ولا فرقًا مقترنًا · ولا رقمَ ضبطِ زخم — **مُثبَتٌ بعدّاد نداءاتٍ
   حقيقيّ** (‏`OHA10`: صفرٌ لـ`trade_r` وصفرٌ لـ`ci_of` في وضع الجدوى).
2. **وحصّةُ الوقف معلومةٌ سلفًا** (‏70.1% · 54.0%) ⇒ **تُنقَل إلى «حقائقُ مُعلَنة»
   (§⑦) ولا تُكتب تنبّؤًا** — ولا يُقال بعد التشغيل «تنبّأتُ بها».
3. **لا تُلمَس أرضيّةُ العيّنة:** تبقى **‏≥150** لكلّ نصفٍ في الذراع **و**في الأزواج
   كما في `T-OPHOLD §⑤` حرفيًّا. ⛔ **وخفضُها بعد رؤية `D0` 120 و`D1` 120 مطاردةُ
   نتيجة** — وعلاجُ نقصِ الأزواج **تصميميٌّ مُعلَنٌ لا عتبيّ** (`D2` في §⑤).
4. **لا يُحصَر المجتمعُ ولا يُوسَّع بعد أيّ رقم** — هو عينُ مجتمع `T-OPCURVE`
   (‏481) بحارس `V-N1`.
5. **لا تُضاف ذراعٌ ولا ضبطٌ بعد قراءة أيّ رقمٍ من هذا العقد** — مُغلَقان في §④/§⑤.
6. **والفرعُ 1 لا يُرقّى إلى دليلٍ أوّليّ** إلّا بحصادٍ أماميّ `N-FWD` (§⑧).

---

## ① السؤالُ القابل للتكذيب

هل سياسةُ حَمْلٍ إلى **أوّلِ إغلاقٍ نظاميٍّ يقع عند المِرساة أو بعدها** — بوقفٍ = قاعُ
المِرساة، وبلا هدفٍ ثابت — تُحقّق **الثلاثةَ معًا**:
- (أ) توقّعًا **موجبًا** بـ`R` بعد التكلفة المقيسة،
- (ب) **وتتفوّق مقترنةً** على سياسة `T-OPTRADE` (‏+10% في 120 دقيقة) على الصفوف نفسِها،
- (ج) **وتتفوّق مقترنةً** على ضبطِ زخمٍ صرف؟

⚖️ **و(ب) هي سؤالُ المالك بعينه** («‏RETO وDLXY ترتفع نسبًا جنونيّةً بعد المِرساة»)،
و(أ) و(ج) يمنعان أن يكون الجوابُ «نعم لأن المجتمعَ يصعد» أو «نعم لأن الزخمَ يفعلها».

---

## ② المجتمع — عينُ مجتمع `T-OPCURVE` بحارسه

- **المصدرُ والطبقاتُ والترتيب** كما في `optrade_prereg.md §②` حرفيًّا.
- **`V-N1` = `V-T1`/`V-H1` نفسُه:** على النافذة المجمَّدة (`until = 2026-09-15`) يطابق
  مُتعدَّدُ `(date, sym)` صفوفَ `opcurve_rows.tsv` (‏481 · `H1` 289 · `H2` 192)
  **وإلّا خروج 5 قبل أيّ رقم**. ونافذةٌ غيرُ مجمَّدةٍ ⇒ الحكمُ يُجبَر على «لا حكم».
- **النصفان** `H1`/`H2` كما هما (`H2_FROM = 2026-09-01`).
- **غيرُ القابل للتداول** يبقى كما هو: `e5 ≤ alow` (‏89 صفًّا) — **لا يُمَسّ**.

---

## ③ السياسةُ والقياس — بالأسماء لا بالشبيه

| العنصر | التعريف | المصدر بالاسم |
|---|---|---|
| الدخول | سعرُ الكرت `e5` عند `t0 = anchor_ms + 4·60_000` | `tier_days_report.true_e5` · `opcurve_probe.resolve_anchor` |
| التكلفة | `k(c) = (1−c/2)/(1+c/2)` · `c = C-MED` · `s = S-MED` · وشبكةُ `c × s` تُطبَع | `fcost_arms.ret_at` · `optrade_arms.read_costs` |
| الوقف | قاعُ المِرساة `alow` · أوّلُ **إغلاقِ دقيقةٍ** تحته **في أيّ جلسةٍ داخل نافذة الحمل** | `sym_day_probe.exit_point` |
| `R` | `r_fixed(ret, e5, e5, alow)` بمقامٍ ثابت · **الترتيب:** `ret_at` ⟶ `r_fixed` ⟶ المتوسّط | `tranche_arms.r_fixed` |
| الفاصل | بوتستراب عنقوديٌّ بالرمز | `exitmgmt_arms.boot_ci` عبر `optrade_arms.ci_of` |

🔑 **«أوّلُ إغلاقٍ نظاميٍّ عند المِرساة أو بعدها» مُعرَّفٌ صراحةً:**
- مِرساةُ **بريماركت** أو **جلسةٍ نظاميّة** في يوم `d` ⇒ إغلاقُ نظاميِّ `d` نفسِه.
- مِرساةُ **افتر** في يوم `d` ⇒ إغلاقُ نظاميِّ **يوم التداول التالي** `d+1`
  (حملٌ ليليٌّ صريحٌ يُعلَن، لا التفافٌ على `P4`).
- **والوقفُ نافذٌ في كلّ دقيقةٍ بين الطرفين** — بريماركتًا كان أو افترًا أو نظاميًّا:
  فمَن يُكسَر قاعُه ليلًا يخرج ليلًا، ولا يُنتظَر به الجرس.

**لا إعادةَ دخول · ولا وقفَ يتحرّك في الذراع الحاكمة.**

---

## ④ الأذرع — قائمةٌ مُغلَقة

| الذراع | الخروج | الدور |
|---|---|---|
| **`HN`** 🥇 | **أوّلُ إغلاقٍ نظاميٍّ عند المِرساة أو بعدها** أو الوقف — أيُّهما أوّلًا | **الحاكمةُ الوحيدة** |
| **`H0`** | = `P0` من `T-OPTRADE` **بالحرف** (‏120 دقيقة · هدف ‏+10%) | مرجعُ الاقتران — و`V-N2` يشترط تطابقَه مع `P0` حقلًا حقلًا |
| `HC0` | `HC` من `T-OPHOLD` **بالحرف** (إغلاقُ نظاميِّ اليوم نفسِه) | وصفيّة — **تُقيس ما اشتراه إعادةُ التعريف**، و`HN ≡ HC0` على غير الافتر (مقفولٌ سلوكيًّا) |
| `HD` | `HN` مقصورةً على مراسي **الافتر** وحدَها | وصفيّة (تُقارَن **بمُكمِّلها** `C-SUB` لا بالمجتمع) |
| `HX2` | آخرُ شمعةٍ في الجلسة الممتدّة **لليوم الذي يقع فيه إغلاقُ `HN`** أو الوقف | وصفيّة — `C-MED` مقيسةٌ على النظاميّ فسبريدُ الافتر خارجَها |

**والضوابط:**
- **`C-P0`** 🥇 **حاكم:** الفرقُ المقترن `HN − H0` لكلّ صفٍّ (نفسُ المجتمع بالبناء).
- **`C-MOM`** 🥇 **حاكم:** بديلةٌ حتميّةٌ بالسلّم **`D0 → D1 → D2`** (§⑤) · التطابقُ على
  **عائد الدقيقة** · وفضُّ التعادل **الأقدمُ طابعًا** · والسياسةُ المطبَّقة عليها
  **`HN` نفسُها** بوقفٍ **مشتقٍّ بنسبة `alow/e5`** (كما في `T-OPHOLD §④` — والفارقُ عن
  `T-OPTRADE` مقصودٌ ومكتوبٌ هناك).
- `C-SUB` لـ`HD` (الجزئيّةُ بمُكمِّلها).
- ⚠️ **`C-MOM-15` لا يُعاد** (منحازٌ نحو الصفر بالبناء).

---

## ⑤ سلّمُ البديلة `D0 → D1 → D2` — **علاجٌ تصميميٌّ لا عتبيّ**

قِيس في `T-OPHOLD` أن ‏**70 صفًّا من 310 (‏22.6%) بلا بديلةٍ إطلاقًا** بالسلّم
`D0 → D1`. ⇒ يُضاف **مستوًى ثالث** بنصّ هذا العقد **قبل أيّ رقمٍ من موضوعه**:

| المستوى | البركة |
|---|---|
| `D0` | اليومُ نفسُه · الجلسةُ نفسُها (`tod`) · **بصفرِ تداخل** مع نافذة الحمل |
| `D1` | يومُ التداول السابق · الجلسةُ نفسُها |
| **`D2`** | **اليومُ الذي قبله** · الجلسةُ نفسُها |

⚖️ **ولماذا هذا ليس مطاردةَ نتيجة:** الأرضيّةُ **لم تُخفَّض** (‏150 لكلّ نصفٍ كما هي)،
والمغيَّرُ **تغطيةُ الشاهد** لا بارُ النجاح — والشاهدُ كلّما اتّسعت بركتُه صار
**أقربَ مطابقةً وأصعبَ على الذراع** لا أسهل. 🔴 **وحدُّ صدقٍ معه:** `D2` يبعد يومين
فيزيد خطرَ اختلاف نظام السوق — **وحصّةُ كلّ مستوًى تُطبَع**، وتُطبَع معها قراءةٌ
ثانيةٌ لـ`OH3` **على `D0 ∪ D1` وحدَها** وصفيّةً بجانب الحاكمة.

---

## ⑥ المعاييرُ والفروع — مكتوبةٌ قبل أيّ رقم

| المعيار | النصّ |
|---|---|
| **`NH1`** | متوسّطُ `R` لـ`HN` بعد التكلفة **موجبٌ في النصفين** والفاصلُ المجمَّع لا يلمس الصفر |
| **`NH2`** | `HN − H0` مقترنًا **موجبٌ في النصفين** والفاصلُ المجمَّع لا يلمس الصفر |
| **`NH3`** | `HN − C-MOM` مقترنًا **موجبٌ في النصفين** والفاصلُ المجمَّع لا يلمس الصفر |
| **الأرضيّة** | **‏≥150** صفًّا قابلًا للتداول في كلّ نصف · **و‏≥150** زوجَ `C-MOM` في كلّ نصف — وسقوطُها **يمنع الفرعَ 1** |

**الفروع** (ثلاثةٌ كلٌّ في سطره):
1. **تُوصى** — `NH1` و`NH2` و`NH3` معًا والأرضيّتان مستوفاتان ⇒ **ثمّ** `N-FWD` (§⑧).
2. **فشلت** — أرضيّتان قائمتان وسقط معيارٌ.
3. **لا حكم** — أرضيّةٌ ساقطة.

**رموزُ الخروج:** ‏0 حكمٌ صدر · 2 لا مفتاح · 3 تغطيةٌ دون 80% · 4 صفرُ مراسٍ ·
**‏5 المجتمعُ يخالف `V-N1`** · **‏6 حارسٌ ساقط** (`V-N2`/التكلفة/`h*`/القراءة-فقط) ·
**‏9 «لا حكم»**.

---

## ⑦ حقائقُ مُعلَنةٌ سلفًا · وتنبّؤاتٌ عمياء

**🔒 مُعلَنةٌ لا تُحسَب تنبّؤًا** (رُئيت في جدوى `T-OPHOLD`):
- حصّةُ الوقف تحت الحمل **فوق 50% في النصفين** (‏70.1% · 54.0%).
- ‏89 صفًّا غيرُ قابلٍ بنيويًّا · و`D0`/`D1` ‏120/120 بالسلّم القديم.

**التنبّؤاتُ العمياءُ السبعة** (تُنشَر مع الحكم مؤكَّدةً أو مكذَّبة):

| # | التنبّؤ |
|---|---|
| `NP1` | متوسّطُ `R` لـ`HN` بعد التكلفة **سالب** (‏`close_reg` المنشور فاصلُه [−4.2, −1.9] والليلُ أدنى) |
| `NP2` | **`HN − HC0` سالبٌ** على مُشتركِهما — أي أن الليلَ يضرّ (متّسقٌ مع «لا `P4`» في `T-OPCURVE`) |
| `NP3` | `HN − H0` **موجبٌ في `H1`** · وإشارتُه في `H2` **مجهولةٌ لي وأُعلنها** لا أتنبّأ بها |
| `NP4` | أعلى ‏10% من الصفوف تحمل **أكثرَ من نصف** مجموع `R` الموجب لـ`HN` |
| `NP5` | `C-MOM` يأكل **نصفَ** فرقِ `HN` عن الصفر فأكثر (‏`1 − d/a ≥ 0.5`) |
| `NP6` | **بالسلّم الثلاثيّ تعبر الأزواجُ ‏150 في كلّ نصف** — تنبّؤُ جدوًى قابلٌ للتكذيب |
| `NP7` | `HD` (الافتر) **أدنى** من مُكمِّلها متوسّطًا (تحمل ليلًا كاملًا ويومًا) |

---

## ⑧ حدودُ الصدق · والحصادُ الأماميّ

**حدودُ الصدق:**
1. **مجتمعٌ مقروء** (§⓪) ⇒ الفرعُ 1 **لا يُرقّى** بلا `N-FWD`.
2. **لمسٌ لا تنفيذ** للوقف والإغلاق (إغلاقُ الدقيقة لا تعبئةٌ فعليّة).
3. `C-MED`/`S-MED` **مستعارتان** من مجتمع المحرّك ⇒ الشبكةُ تُطبَع، والخليّةُ
   `(C-MED, 0)` ملاصقةً لأن `S-MED` سالبٌ يُجمّل الخاسر.
4. **الحملُ الليليُّ بلا تكلفةٍ مقيسة له** — الفجوةُ الافتتاحيّة تُنفَّذ كسعرٍ لا
   كانزلاق، وذلك **يُجمّل `HN`** ⇒ يُقال ولا يُصحَّح بهذا العقد.
5. **نظامُ سوقٍ واحد** (‏08-18 ⟶ 09-15) وقد بَرَد بين النصفين.
6. **‏64% من الانفجارات بلا مِرساةٍ أصلًا** ⇒ سقفُ أيّ سياسةٍ ‏≈36% من الفرص.
7. `D2` يبعد يومين (§⑤) ⇒ قراءةٌ ثانيةٌ على `D0 ∪ D1` تُطبَع.
8. **تعدّدُ المقارنات** في الوصفيّات الثلاث ⇒ لا تُرقّى واحدةٌ منها بلا تسجيلٍ جديد.

**`N-FWD` (الحصادُ الأماميّ):** مراسي «هنا الدخول» **بعد `2026-09-15`** بالدوالّ
نفسِها · القراءةُ الحاكمةُ الأولى عند **‏≥150 صفًّا قابلًا للتداول** أو في
**‏2026-11-15** أيُّهما أوّلًا · بالمعايير `NH1`-`NH3` نفسِها · وأيُّ قراءةٍ قبلها
**وصفيّةٌ تُطبَع بوسمٍ صريح**. ⏳ **ولا وعدَ يتعفّن:** الأداةُ تطبع في كلّ تشغيلةٍ
عددَ الصفوف الأماميّة وتاريخَ الأرضيّة.

---

## ⑨ سقفُ النجاح — والمحظورات

**سقفُ النجاح:** **اقتراحٌ للمالك** بسطرِ تسليمٍ على كرت المِرساة (مثلًا: «لا تسقف
الهدفَ بـ‏+10% — وقفُك القاع») **بقراره وحدَه**.
**المحظورات:** ① لا تنفيذَ أوامر · ② لا وسيطَ تداول · ③ لا مسَّ جذرٍ من الاثني عشر ·
④ لا مؤشّرَ اختيارٍ ولا ترتيب · ⑤ لا عتبةَ إنتاجٍ تتحرّك بهذي التجربة.

---

## ⑩ الأداة — مواصفةٌ ملزِمة (تُبنى **بعد** دمج هذا العقد)

- `ophold2_arms.py` + `.github/workflows/ophold2.yml` (يدويٌّ بلا كرون · **وبلا سرِّ
  تلغرام** فالإرسالُ مستحيلٌ بنيويًّا).
- **إعادةُ استعمالٍ بالاسم لا نسخ:** `ophold_arms.attach_hold`/`arm_R`/`paired`/
  `trail_trade` حيث تنطبق · و`optrade_arms.simulate`/`trade_r`/`placebo_pool`/
  `pick_placebo`/`read_costs`/`read_hstar`/`build_rows`/`attach_trades`/`ci_of`/
  `crit_halves`/`tod_of`/`tsv_population`/`selfcheck_readonly`/`no_config_assign` —
  **وأيُّ منطقِ حسمٍ مكرَّرٍ عيبٌ** (يُقفَل بـAST).
- **`V-N1`** مطابقةُ `opcurve_rows.tsv` (خروج 5) · **`V-N2`** `H0` يطابق `P0` حقلًا
  حقلًا (خروج 6) · **`V-N3`** `HN ≡ HC0` على كلّ صفٍّ **غيرِ افتريّ** (سلوكيّ) ·
  والتكلفةُ و`h*` بالنصّ لا باليد (خروج 6).
- **وضعُ جدوى `OPHOLD2_DRY=1`:** يطبع **أربعةَ أعدادٍ فقط** (القابلُ للتداول لكلّ نصف ·
  أزواجُ `C-MOM` لكلّ نصفٍ وحصّةُ `D0`/`D1`/`D2` · عددُ صفوف الافتر التي صارت قابلةً ·
  وتطابقُ `V-N3`) **ويعود قبل أيّ نداءِ تكلفةٍ أو `R`** — مقفولٌ بعدّاد نداءات.
- **الحتميّة:** المدخلاتُ نفسُها ⇒ مُخرَجٌ بت-بت · والمُخرَج `ophold2_result.md`
  بجانب هذا العقد لا فوقه، وجدولُ `⟦TSV⟧` لكلّ صفٍّ بأذرعه الخمسة.

---

## ⑪ الأقفال (تُكتب في السويّة مع هذا العقد، وكلٌّ يسقط بطفرةٍ مسمّاة)

- **`HNK0`** — بنيويّ: فروعُ §⑥ ثلاثةٌ كلٌّ في رقمه · «لا حكم» في الثالث وحدَه ·
  ورمزا الخروج 5 و9 منصوصان · والأرضيّةُ **‏150 في كلّ نصفٍ للذراع وللأزواج** وتمنع الفرعَ 1.
- **`HNK1`** — §④: ذراعٌ حاكمةٌ **واحدة** (`HN`) · ضابطان حاكمان (`C-P0` · `C-MOM`) ·
  و`HC0`/`HD`/`HX2` وصفيّةٌ نصًّا · و`C-MOM-15` لا يُعاد.
- **`HNK2`** — §⓪ يُعلن التلوّثَ بأرقامه (‏481 · 310 · 184 · 126 · 89 · 120/120 ·
  70.1% · 54.0%) **ويُقرّ أن حصّةَ الوقف حقيقةٌ مُعلَنةٌ لا تنبّؤ** · و§⑤ يُصرّح أن
  الأرضيّةَ لم تُخفَّض وأن العلاجَ تصميميّ.
- **`HNK3`** — §③ يُعرّف «أوّلَ إغلاقٍ نظاميٍّ» للحالات الثلاث بالنصّ · وينصّ أن
  **الوقفَ نافذٌ في كلّ دقيقةٍ بين الطرفين** · و§⑧ يُسمّي أرضيّةَ `N-FWD` وتاريخَها.
