# 🥇⑦➡️ T-TIE-FWD — تسجيلٌ مسبق للحصاد الأماميّ (يُدفَع **قبل** أوّل صفٍّ يُسجَّل)

> **قرارُ المالك (2026-08-06):** بعد أن فشلت `T-RANKER-TIE` على الباكتيست **وتبيّن أن
> الأداة لا تصل إلى الظاهرة** (‏1-2 مرشّح/جلسة مقابل 109 حيًّا)، اختار المالكُ
> **الطريق ①: الحصادُ الأماميّ** — «‏1».
>
> **وهذا ملفٌّ جديد لا إلحاقٌ بـ`ranker_tie_prereg.md`** — فذاك مدفوعٌ قبل أرقامه
> ولم يُمَسّ بعدها، وإلحاقُ شيءٍ به الآن يهدم تلك الخاصّية.

---

## ① السؤال — كما هو بلا توسيع

على الجلسة الحيّة 2026-08-05 تعادل **‏16 سهمًا عند جاهزية 70** والسعة 10 ⇒ **‏10 من 10
خاناتٍ قرّرها كاسرُ التعادل** (`score` ثم `rr`). والسؤال:

> **هل مَن أخذهم كاسرُ التعادل أفضلُ ممّن قصّهم — بوحدة المخاطرة؟**

**ولا يُوسَّع السؤال:** لا نقيس المُرتِّب كلَّه، ولا البوّابات، ولا الخروج.

## ② لماذا أماميًّا — والعيبُ الذي يعالجه مقيسٌ لا مُفترَض

`backtest_symbol` يقفز `i += fwd` بعد كلّ صفقة و`BACKTEST_STEP=5` بينها ⇒ **يستحيل
بنيويًّا أن يظهر رمزٌ في جلستين متتاليتين**، فكثافةُ المرشّحين **‏1.03/جلسة (2023) و
2.08 (2025)** بينما الفرزُ الحيّ يُعيد إدراج الاسم كلَّ يومٍ ما دام في وضعه فيبلغ **109**.
⇒ الصفقاتُ المتأثّرة في الباكتيست **‏4 · 11 · 13** (دون حدّ الثلاثين) **بالبناء**.

**والحصادُ الأماميّ يقيس الظاهرة نفسَها في مكانها:** كلَّ يومٍ يقع فيه حدُّ القطع داخل
تعادل، نسجّل **الكوهورت كاملًا** — المأخوذين **والمقصوصين** — بخططهم كما كانت لحظتها،
ثم نحسم الجميع بعد النافذة. **والمقصوصون هم الشاهدُ المضادّ الذي لا نراه اليوم إطلاقًا.**

## ③ ما يُسجَّل — لحظةَ القرار لا بعده

عند كلّ اختيار (التجديد الأسبوعيّ · الإضافة اليومية)، وبعد `select_top` مباشرةً:

- `date` (جلسة القرار) · `source` (`renew` / `daily`) · `level` (الجاهزية عند حدّ القطع)
- ولكلّ عضوٍ في الكوهورت: `symbol` · `rank` (موضعُه في الترتيب) · `readiness` · `score`
  · `rr` · `entry` (**متوسط الدفعات** — نفسُ ما يستعمله محرّك الباكتيست) · `stop` · `t1`
  · `taken` (أُخذ أم قُصّ).

🔒 **ولا يُسجَّل شيءٌ إن لم يقع حدُّ القطع داخل تعادل** — فالصمتُ هنا صدقٌ لا نقص.

## ④ الحسم — بمحرّك الإنتاج نفسِه لا بنسخةٍ منه

- التعبئة: **أوّل جلسةٍ ينزل فيها `low` إلى متوسط الدفعات** (`filled`) — نصُّ المحرّك.
- الحسم: **`_resolve_arm` نفسُها** (ذراع الذيل: الوقف بلمسة `low<=stop`، الهدف `t1`).
- `R` = العائد ÷ `((دخول − وقف) ÷ دخول)` ⇒ الوقفُ **‏−1R** بالضبط. **وغيرُ المُعبَّأة
  ‏0.0R** (نفسُ عُرف `replay10.r_unit`).
- النافذة: `BACKTEST_FORWARD_DAYS` — **نفسُ ثابت الباكتيست**، فلا يتفرّق أفقان.

## ⑤ المعيار — مثبَّتٌ قبل أيّ رقم

**يُعتمَد «كاسرُ التعادل يضيف» إن استوفى الثلاثة معًا:**

1. **الفرقُ المزدوج داخل الكوهورت** (‏متوسط `R` للمأخوذين − متوسط `R` للمقصوصين، ثم
   المتوسط عبر الكوهورتات) **موجب**.
2. الفرق **‏≥ +0.10R** — نفسُ عتبة `ranker_tie_prereg.md` §④-2 بلا تغيير.
3. الفاصل 95% (‏cluster bootstrap **بالرمز**) **لا يلمس الصفر**.

**وحدُّ العيّنة: ‏≥ 30 مقصوصًا محسومًا** (‏`TIE_HARVEST_MIN`) — دونه **«لا حكم»** ولا
يُطبَع فرقٌ يُقرأ نتيجة. **والقياسُ داخل الكوهورت** فهو مضبوطٌ بالبناء على اليوم
والجاهزية والسعة ⇒ لا يحمل ضجيجَ السوق (نفسُ منطق §④-1).

⚖️ **ولا يُوقَف الحصادُ عند بلوغ العتبة** — يستمرّ، والحكمُ يُكتب مرّةً واحدة عند بلوغها
أوّل مرّة **ولا يُعاد كلَّ أسبوع** (وإلّا صار «شغّل حتى تعجبني النتيجة»).

## ⑥ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر ولو كُذِّبت)

- **`F1`** التعادلُ يقع في **‏≥ 60%** من أيام الاختيار (لأن الجاهزيةَ مضاعفاتُ 5
  و`h4_confirm`=0 حيًّا). *(لو كُذِّب فالمادّة أرقُّ ممّا قِيس على جلسةٍ واحدة.)*
- **`F2`** تبلغ العيّنةُ ثلاثين مقصوصًا محسومًا **خلال ‏4-6 أشهر** (‏≈6 مقصوصين/يوم
  اختيار، ونافذةُ الحسم `BACKTEST_FORWARD_DAYS`).
- **`F3`** الفرقُ **أصغرُ من 0.10R** — أتوقّع سقوطَ الشرط 2، لأن `T-RANKER-TIE` قاسته
  **سالبًا في الثلاث** والباكتيستُ وإن كان رقيقًا فإشارتُه لم تكن موجبةً مرّة.
- **`F4`** المستوى المطلق لـ`R` **سالبٌ في المجموعتين** (المأخوذين والمقصوصين) — اتّساقًا
  مع كلّ ما قِيس (‏`T-EXIT` · `T-STOP` · الأذرع الأربعة).

## ⑦ سقفُ النجاح — محدَّدٌ سلفًا

نجاحٌ كامل ⇒ **اقتراحٌ للمالك** بمراجعة كاسر التعادل، مع `LOGIC_VERSION` مرفوع.
**ولا شيءَ يُغيَّر تلقائيًّا.** وفشلٌ أو «لا حكم» ⇒ يبقى الحالُ ويُنشَر الرقم.

## ⑧ الأثرُ الإنتاجيّ — صفر بالبناء

التسجيلُ يقع **بعد** `select_top` ويقرأ مُخرَجَه ⇒ **لا يمسّ الاختيار ولا الترتيب ولا
العضوية**. `rank_key` · `select_top` · `classify_tier` · `analyze_ticker` وبقيّةُ الجذور
**byte-identical**. فاشلٌ-آمنٌ مطلق (‏انهيارُ التسجيل لا يُسقط الفرز) · لا `LOGIC_VERSION`
· ولا قناةَ تلغرام رابعة (القسمُ داخل **تقرير التطوير الأسبوعيّ** بأمر المالك: «أي شي
يحتاج وقت ضفه مع اداة التطوير»).

## ⑨ حدودُ صدقٍ تُقرأ مع أيّ نتيجة

- **المقصوصُ لم يُشترَ فعلًا** — حسمُه **محاكاةٌ على نفس القواعد** (تعبئةٌ عند متوسط
  الدفعات · وقفٌ بلمسة الذيل) ⇒ لا انزلاقَ ولا سيولةَ تنفيذ. وهو **نفسُ عُرف الباكتيست**
  فالمقارنةُ بين المأخوذ والمقصوص عادلةٌ رغم ذلك.
- **الخطّةُ مجمَّدةٌ على يوم القرار** بينما الإنتاج يجدّد الرقم الحرج يوميًّا — والتجميدُ
  **مشترَكٌ بين المجموعتين** فلا يحيز المقارنة.
- **بلا افتر** (الشمعةُ اليومية لا تشمله) ⇒ الأرقام **أرضيّةٌ لا سقف**.
- **‏`t2`/`t3` غير مقيسة** — الحسمُ عند `t1` كما في كلّ قياساتنا.
- **الكوهورتُ يتغيّر حجمُه يومًا بيوم** فالمتوسطُ عبر الكوهورتات يزن كلَّ يومٍ **بالتساوي**
  لا بعدد أعضائه — مثبَّتٌ هنا قبل الأرقام.

---

## ⑩ ملحقٌ مؤرَّخ **‏2026-08-12 — «سجّل 4س» (`T-TIE-4H`)** · يُدفَع **قبل أوّل صفٍّ يُسجَّل**

بأمر المالك «سجّل 4س». **تسجيلٌ لا قرار**: يُضاف حقلٌ واحدٌ لكلّ عضوٍ في الكوهورت
(المأخوذ **والمقصوص**) هو **`h4_confirm`** — درجةُ تأكيد الأربع ساعات (0-3) بدالّة
الإنتاج `h4_confirm_score` نفسِها. ولا يُستعمَل في أيّ قرار الآن.

### ⑩-1 السؤال — واحدٌ فقط، مثبَّتٌ قبل أيّ رقم
> **هل كان `h4_confirm` كاسرَ تعادلٍ أفضلَ من كاسر الإنتاج الحاليّ (`score` ثم `rr`)؟**

المقياسُ **هو نفسُه المسجَّل في §⑤ بلا تغيير**: `R` لكلّ عضوٍ محسوم بمحرّك الإنتاج،
ويُقارَن **متوسطُ العشرة الذين كان 4س سيأخذهم** بمتوسط **العشرة الذين أخذهم الإنتاج
فعلًا** — على **نفس الكوهورت ونفس اليوم ونفس السعة** (مقارنةٌ مزدوجةٌ داخل الكوهورت
فلا تحمل ضجيجَ السوق). و**تعادلُ 4س نفسُه يُكسَر بكاسر الإنتاج** (`score` ثم `rr`)
فلا يُخترَع كاسرٌ ثالث.

### ⑩-2 🔒 «تسجيلٌ لا قرار» — مقفولٌ بالبناء لا بالوعد
🔴 **والخطرُ حقيقيٌّ ومحدَّد:** `fill_picks` ينادي `select_top` **في كلّ جولة**، فلو
كُتبت الدرجةُ على صفوف النتائج (`r["h4_confirm"]`) لقرأها `select_top` في **الجولة
الثانية** — و`h4_confirm` مفتاحٌ **ثالثٌ** في `rank_key` ⇒ **التسجيلُ يُبدّل الاختيار**.
⇒ **الدرجةُ تُحسَب في قاموسٍ محلّيٍّ بمفتاح الرمز، ولا تُكتَب على أيّ صفٍّ من النتائج
أبدًا**، ويُقفَل ذلك **سلوكيًّا**: مُخرَجُ `select_top` **مطابقٌ بت-بت** بالتسجيل
وبدونه.

### ⑩-3 القياسُ لا يُخمَّن — و«تعذّر» ليس صفرًا
- الجلبُ بدالّة الإنتاج `fetch_4h` (مُحقَنةٌ للاختبار).
- **تعذّرُ الجلب ⇒ `h4_confirm = None`** ويُعَدّ ويُعلَن — **وليس صفرًا**، لأن الصفرَ
  درجةٌ حقيقيّةٌ تعني «لا تأكيد» (قاعدةُ «تعذّرٌ ≠ صفر» المدوَّنة).
- **وكوهورتٌ فيه أيُّ `None` يُستبعَد من الحكم** ويُعَدّ: المقارنةُ بين مُرتِّبَين
  تستلزم درجةً لكلّ عضو، وإلّا صار الفارقُ أثرَ تغطيةٍ لا أثرَ إشارة.
- **سقفٌ مُعلَن `TIE_H4_CAP` = 25 عضوًا/كوهورت** · وأيُّ قصٍّ **يُطبَع بعدده**
  والكوهورتُ المقصوصُ **يُستبعَد من الحكم** (كوهورتٌ ناقصٌ لا يُقارَن).

### ⑩-4 المعيار — **هو معيارُ §⑤ نفسُه بلا حرفٍ زائد**
‏≥30 كوهورتًا حاكمًا (كاملَ التغطية) · وفرقُ متوسطِ `R` المزدوج داخل الكوهورت مع
فاصلِ ثقةٍ لا يلمس الصفر · **ولا انقلابَ إشارةٍ عبر الأرباع**. وسقفُ النجاح **اقتراحٌ
للمالك** (‏§⑦) — **ولا تفعيلَ تلقائيّ لـ`h4_confirm` مهما كانت النتيجة.**

### ⑩-5 التنبّؤات — تُنشَر لتُكذَّب
- **`H1`** التغطيةُ الكاملة ستكون **‏<100%** (‏`fetch_4h` يعتمد ساعةً من ياهو، وكوننا
  رقيق) — وأتوقّع **‏70-95%** من الكوهورتات صالحةً للحكم.
- **`H2`** توزيعُ `h4_confirm` سيغلب عليه **‏0** (فيصل يشترط تأكيدًا نادرًا) ⇒ **وإن
  كان ‏≥80% أصفارًا فالمفتاحُ عاجزٌ بنيويًّا عن كسر تعادل** ويُعلَن ذلك **نتيجةً** لا
  عيبًا — وهو نظيرُ ما وقع لـ`TF_MIN_REVERSALS` (حدُّه صفرٌ من الكاتالوج).
- **`H3`** الفرقُ لن يستوفي المعيار (سابقتُه `R-ENV` سقطت ولم تتفوّق على العشوائيّ).

### ⑩-6 حدودٌ تُقرأ مع النتيجة
حدودُ §⑨ كلُّها سارية · **ويُضاف:** الدرجةُ تُقاس **يوم القرار** بشموع 4س المتاحة
لحظتَها، والإنتاج يحسبها في `enrich` **بعد** الاختيار ⇒ **قيمتان لنفس اليوم قد تختلفان
بدقائق** (‏`fetch_4h` بـ`prepost=True`) — وهذا **مشترَكٌ بين المأخوذ والمقصوص** فلا
يحيز المقارنة، لكنه يُصرَّح به.
