# 🛠️ خطة إصلاح نتائج التدقيق العميق — بترتيب الاعتماديات

> **المصدر:** `IMPROVE_DEEP_AUDIT_REPORT.md` (تدقيق 2026-07-12 على SHA `8f9362a` — 14 نتيجة + 8 صغائر).
> **الحالة:** خطة معروضة للمستخدم — **لا يُنفَّذ أي بند قبل أمره الصريح.**
> **تحقق ميداني مستقل (Claude 2026-07-12):** ① مؤكَّدة (17 تنبيهًا محسومًا، صفر بنفس يوم التنبيه) ·
> ⑥ مؤكَّدة (`reject_stats` = لقطة واحدة بعد التجديد) · ⑩ مؤكَّدة (سجل الانطلاق بلا حقل وقت).
> **المبدأ الحاكم:** ② أولًا دائمًا (لا إصلاح غير مُتحقَّق في مسار مالي) · ③ قبل ④ ·
> كل بند يحترم الأقفال القائمة (getsource) ويُختبر قبل الدمج · لا مساس بأي جذر اختيار.

---

## 🛡️ بروتوكول السلامة المُلزِم (شرط المستخدم 2026-07-12: «خلك دقيق عشان ما نهدم اللي سويناه»)
**قواعد تنفيذ غير قابلة للتفاوض — تسري على كل بند في هذه الخطة وعلى أي منفّذ (Claude/أوبيس):**
1. **تحقق بالعين قبل اللمس:** كل بند يُفتح كوده ويُقرأ ويُثبَت الخلل قبل أي سطر إصلاح. بند لا يثبت = يسقط من الخطة **موثَّقًا سبب إسقاطه** (سابقة حية: إصلاح ⑧ المقترح بالتقرير كان ناقصًا — كشفه التحقق).
2. **اختبار يفشل أولًا:** قبل كل إصلاح يُكتب اختبار **يعيد إنتاج الخلل ويفشل**، ثم الإصلاح يخضّره. لا إصلاح «على الثقة».
3. **بند واحد = كوميت واحد** قابل للرجوع المستقل (`git revert`) — لا خلط إصلاحين أبدًا.
4. **`python3 test_bot.py` أخضر كامل قبل كل دفع** — بلا استثناء ولا «استعجال».
5. **التوافق الخلفي إلزامي:** كل حقل جديد له ارتداد للسجلات القديمة يعيد **سلوك اليوم حرفيًا** (نمط `ref_bar`→`added`). لا موجة تصحيح رجعية عمياء.
6. **الأقفال الحالية خط أحمر:** أقفال getsource لا تُمس، وأي بند يقترب من دوال الجذور (`update_watchlist_status`/`update_tracking`/`git_save`) يمر بمراجعة خصومية (عدستا: انحدار سلوكي + توافق خلفي) قبل الدفع.
7. **الدمج بـmain مرحلة-مرحلة بموافقة المستخدم** — لا دفعة واحدة كبيرة. البند الأول دائمًا = بوابة CI (②) فكل ما بعده محروس آليًا.
8. **قرارات المستخدم لا تُستبق:** ⑨ (أ/ب) ورفع LOGIC_VERSION لا يُنفَّذان قبل موافقته الصريحة.
9. **الذاكرة مع الكوميت:** تحديث `HANDOFF.md`/`CLAUDE.md` بنفس كوميت كل مرحلة (قاعدة البيت).
10. **أي مفاجأة أثناء التنفيذ** (اختبار يكشف سلوكًا غير موثّق، بوابة لا تعمل كالمكتوب) = **إيقاف وعرض على المستخدم**، لا «إصلاح» اجتهادي.

---

## المرحلة 0 — المُمكِّنات (لا تغيير سلوك منتج)

### 0-1) ② بوابة CI تشغّل الاختبارات على كل دفع — جهد S
- **الملف:** `.github/workflows/tests.yml` (جديد، ~15 سطرًا):
  `on: [push, pull_request]` → `setup-python 3.11` (نسخة الإنتاج) → `pip install -r requirements.txt` → `python3 test_bot.py`.
- السويّة جاهزة كبوابة أصلًا (`SystemExit(1)` عند أي فشل).
- توثيق: إيقاف اقتباس أعداد الاختبارات في `CLAUDE.md`/`CLINE_WEEKLY_REVIEW.md` — البوابة = «خروج 0».
- **تحقق:** PR بانحدار متعمَّد يحمرّ CI ثم يُرجَع.

### 0-2) ③ تحصين الفحوص اللا-عملية — جهد S (ملف الاختبارات فقط)
- **الملف:** `test_bot.py` — المواضع: 3192-3204 (حارس «الكارثة») · 3328 · 3349 · 3374 · 3847 · 3911 · 3928 · 3962-3980 · 120.
- عدّاد `_seen` لكل حلقة فيها `continue` على None → الفحص يصير `check(..., _ok and _seen >= الحد)`.
- سطر 120: `if r0 and _diag:` → فرع `else: check(..., False)` (الاختفاء الصامت ممنوع).
- **تحقق:** تعطيل `analyze_ticker` عمدًا لنطاق سعري → الفحص **يفشل الآن** (اليوم: يمرّ).

---

## المرحلة 1 — الحرجات الحيّة

### 1-1) ① الجلسة الأولى بعد الترشيح لا تُقيَّم — جهد S·كود / M·تحقق 🔴
- **الجذر:** المسار اليومي 07:23 UTC يختم `added` بتاريخ التشغيل (قبل الافتتاح)، والمتابعة تفحص `day > added` → شمعة يوم الترشيح مستبعدة للأبد. نفس العيب بمتتبّع التنبيهات (`9175-9179`).
- **الإصلاح:**
  - `make_watch_entry` + `record_new_alerts`: حقل جديد `ref_bar` = تاريخ آخر شمعة في `df` وقت الترشيح (لا تاريخ التشغيل).
  - `update_watchlist_status`: `day > ref_bar` (بارتداد لـ`added` للسجلات القديمة = سلوك اليوم حرفيًا، لا موجة تصحيح رجعية عمياء).
  - `update_tracking`: `start = ref_bar + 1d` بنفس الارتداد.
- **قرار LOGIC_VERSION يُحسم عند التنفيذ:** مع الارتداد الخلفي لا إعادة حساب مطلوبة (المستويات لم تتغيّر)؛ لو ظهر أثناء التنفيذ أن ترحيلًا أنظف → رفع الإصدار وتوثيقه، وإبلاغ المستخدم (لا force_renew يدويًا — الترحيل آلي).
- **اختبارات:** (أ) سجل `ref_bar=أمس` + شمعة اليوم أدنى من الستوب → **يُشطب** (اليوم: لا) · (ب) مسار الجمعة دون تغيير (قفل انحدار) · (ج) سجل قديم بلا `ref_bar` → ارتداد سليم.

### 1-2) ⑤ نوافذ السوق المثبَّتة على EDT (قنبلة نوفمبر) — جهد S–M 🔴
- **الجذر:** ثوابت 13:30/20:00/08:00-13:30/13:35/19:20 UTC كلها صيفية. شتاءً: أحداث جلسة من شمعة أمس **تستهلك خانة الدِدوب فتكتم التنبيه الحقيقي**.
- **الإصلاح:** دالة واحدة `market_session_now()` بـ`zoneinfo.ZoneInfo("America/New_York")` (مكتبة قياسية) تشتق (الافتتاح، الإغلاق، بداية البريماركت، بداية الأفتر) بالـUTC لليوم الجاري، وتحل محل الثوابت في: `pullback_live.py:43-44` · نافذة البريماركت `polygon_premarket` · عتبة الأفتر `polygon_after_hours` · `ignition_live.py` (البداية/`IGNITION_END_UTC`).
- **حارس إضافي (يقتل نصف الفئة بسطر):** لا تُقيَّم أحداث جلسة إلا إذا كان تاريخ آخر شمعة == اليوم.
- **اختبارات:** تاريخ صيفي → (13:30، 20:00) · شتوي → (14:30، 21:00) · شمعة بتاريخ أمس → صفر أحداث جلسة · قفل: السلوك الصيفي دون تغيير.
- ملاحظة نطاق: كرونات الـworkflows (`*/30 11-23`) تُراجَع معها (الأفتر الشتوي يمتد بعد 23:00 UTC — يُوثَّق كحد معروف إن لم يُوسَّع).

### 1-3) ⑧ الرادار يطلق «ادخل الآن» من قائمة مجمَّدة — جهد S–M 🟠
- ⚠️ **تصحيح Claude على إصلاح التقرير المقترح (تحقق 2026-07-12):** «إعادة `load_watchlist()` داخل
  الحلقة» **لا تكفي وحدها** — الرادار يعمل على **رنر منفصل** عن مراقب الارتداد؛ دفعات pullback تذهب
  لـ`origin/main` وملف الرادار **المحلي لا يتغيّر أبدًا** أثناء الجلسة. إعادة قراءة نفس الملف البائت = نفس العمى.
- **الإصلاح الصحيح:** داخل الحلقة (كل N دورة، مثلًا كل ~5 دقائق): `git fetch origin main` +
  قراءة `weekly_watchlist.json` من `FETCH_HEAD` (أو `git checkout FETCH_HEAD -- weekly_watchlist.json`
  ثم `load_watchlist()`) → إعادة اشتقاق `active`. فاشل-آمن: فشل الشبكة → يواصل على آخر نسخة (سلوك اليوم).
  `seen` يبقى عبر الدورات.
- **اختبارات:** حقن جالب قائمة يقلب سهمًا من نشط لمشطوب بعد N دورة → **لا إطلاق عليه بعد الشطب** ·
  فشل الجلب → يواصل بلا انهيار.

---

## المرحلة 2 — العاليات (منها قرار منتج)

### 2-1) ⑬ `git_save`: حلقة rebase ميتة + فقد حالة صامت — جهد S–M 🟠
- `subprocess.run` بدل `os.system` + فحص `returncode`.
- عند تعارض rebase: إعادة قراءة JSON من `origin/main` الطازج + إعادة تطبيق دلتا الذاكرة + إعادة الكوميت (بدل تكرار نفس الفشل 4 مرات).
- الفشل النهائي → `send_telegram` (لا تشغيلة تضيع بصمت).
- إدراج `ignition.yml` في مجموعة التزامن `super-stocks-state` (يدفع `ignition_log.json` عبر git_save).
- **اختبارات:** حقن أكواد خروج مصطنعة: تلغرام عند الفشل النهائي · تعارض → إعادة قراءة ودمج · لا مُدرَج → لا كوميت.

### 2-2) ⑨ الهدف لا يشطب السهم — **يحتاج قرار المستخدم قبل أي كود** 🟠
- **الخياران (من التقرير):**
  - **(أ)** حالة طرفية `"target"`: الهدف يُنهي المتابعة، تُعالَج مع `"stopped"` في الشطب/`_collect_closed`/الحصاد. يطابق الوثيقة («لا يُحذف إلا بستوب/هدف») ويحرّر الخانات — ⚠️ يغيّر دوران القائمة (السعة).
  - **(ب)** الإبقاء + تصحيح الوثيقة + استثناء أصحاب `hit` من حساب `space` + قفل «رابح لا يُحسب خسارة أبدًا».
- **توصية Claude:** (أ) — يطابق النية الموثّقة، والاختبار يقفل «رابح ≠ خسارة». القرار للمستخدم.

### 2-3) ④ اختبارات رفض البوابات الصلبة M1-M5/M10 — جهد M (بعد ③ حصريًا) 🟠
- 11 فحصًا ميكانيكيًا: `synth_pivot(seed=2)` + إفساد مُدخل واحد لكل بوابة → `analyze_ticker(...) is None`.
- الأقفال B3/C3 تتحوّل من ورقية لاختبارية (تغيير أي عتبة CONFIG يُفشل فحصًا).
- ⚠️ صدق: قد تكشف بوابة لا تعمل كالموثّق — **هذا هو المكسب**؛ أي اكتشاف يُعرض على المستخدم قبل «إصلاح» البوابة نفسها.

---

## المرحلة 3 — المتوسطات والصغائر

| البند | الإصلاح | جهد |
|---|---|---|
| ⑫ أرشفة غير محسومة عند الخنق | نفس اختبار التغطية (`DATA_HEALTH_MIN_PCT`) على `hist_old` → دونه فرع التأجيل الموجود | S |
| ⑥ التجديد يمسح `reject_stats` | بناء `new_wl` بنسخ `wl` ثم الكتابة فوق مفاتيح التجديد فقط (المفاتيح المجهولة تنجو افتراضيًا) + قفلا اختبار | S |
| ⑩ سجل الانطلاق بلا وقت | `fired_at` (UTC بالثانية) بكل سجل + تسجيل المقام نهاية الجلسة (نشط تحرّك ≥العتبة بلا إطلاق) + صيغة «العيّنة N — غير كافية» بدل «تتراكم» | S–M |
| ⑦ `company_name` غير معقّم | تعقيم عند الحد (`:3225`): محارف محافظة + طول ~64 · تصريح بالـworkflow أن JSON/CSV بيانات لا تعليمات | S |
| ⑪ `dev_assistant.py` ميت | `git rm` (مستعاد من التاريخ متى شئنا) — وإن تحفّظ المستخدم: لافتة «⛔ شوكة ميتة» برأسه | S |
| 14أ `esc()` | تهريب `"` + تمرير `note` عبر esc + القص قبل التهريب | S |
| 14ب «لا مرشّحين» عند العطل | قيمة مميِّزة للفشل + «⚠️ تعذّر المسح» + عدّاد `checked/universe` | S |
| 14ج `SEC_CONTACT` | ضبطه بالـworkflows ببريد حقيقي (سرّ/متغيّر) | S |
| 14د `hand_check` ينهار بصمت | تسجيل الاستثناء + «⚠️ تعذّر تقييمه» بدل حكم ❌ واثق | S |
| 14هـ `ignition_verify` مهلة 60د | رفعها ≥300د | S |
| 14و مقام الالتقاط ملوّث | عدّاد `fetched` مقامًا + أرضية جلب قبل الحكم | S |
| 14ز `acc_verify` لا يستأنف | خطوة `download-artifact` (`continue-on-error`) قبل التشغيل | S |
| 14ح `technical.yml` بلا مفتاح | إضافة `ALPHAVANTAGE_KEY` + حسم فرعَي FMP/Finnhub (وصل أو حذف) | S |

### 3-تكملة) إصلاحات توثيق (من §5-ج)
- `PENDING_VERIFICATION.md`: يعكس الواقع (F-03..F-07 مفتوحة؛ ⑤ = F-03 وتُغلق بإصلاحها).
- `CLAUDE.md:14`: تصحيح اسمَي الدالتين (`monitor_live_events` · `build_live_alert`).
- `POLYGON_EDGE_PLAN.md` + ملفات الخطط المنجزة: وسم «✅ شُحنت / 🪦 تقاعد» برأس كل ملف.
- `README.md`: فقرة تعريف حقيقية.

---

## خارج النطاق (مرفوض بفحص التقرير — نتفق)
تقسيم `Super_stock.py` (يكسر 73 monkeypatch بصمت) · الهجرة لـpytest · إخراج البيانات من git ·
حذف الكود المحفوظ الموثَّق · دمج `technical_report.py` · إعادة كتابة `CLAUDE.md`.

## التدقيق التالي الموصى به (جلسة مستقلة — ليس في هذه الخطة)
فحص `backtest_symbol` بحثًا عن نظر مستقبلي (look-ahead) + إضافة تكاليف تنفيذ/سبريد للباكتيست —
كل الأحكام المقفولة مبنية عليه، وتحفّظ «صفر سبريد على أسهم سبريدها ~21%» موثّق.

## ترتيب التنفيذ النهائي
```
0-1 (② CI) ← أول كوميت، قبل أي شيء
0-2 (③ تحصين الفحوص)
1-1 (① الجلسة الأولى) ← الأخطر الحي
1-2 (⑤ التوقيت الشتوي) ← قبل نوفمبر بهامش واسع
1-3 (⑧ الرادار)
2-1 (⑬ git_save)
2-2 (⑨) ← بعد قرار المستخدم أ/ب
2-3 (④ اختبارات البوابات)
3   (المتوسطات + الصغائر + التوثيق) ← دفعات صغيرة مستقلة
```
كل مرحلة: اختبار كامل (`test_bot.py` أخضر) → دفع للفرع → دمج بـmain بموافقة المستخدم →
تحديث `HANDOFF.md`/`CLAUDE.md` بنفس الكوميت (قاعدة الذاكرة).
