# 🕵️📈 `T-OPCURVE` — تسجيلٌ مسبقٌ وصفيّ: **«الفترةُ البسيطة» رقمًا — منحنى ما بعد مِرساة «هنا الدخول»**

> **مدفوعٌ ومدموجٌ قبل أيّ سطرِ أداةٍ وقبل أيّ رقم.** أمرُ المالك «سجل المنحنى»
> (‏2026-09-15) بعد إعادة قراءة الرابط على النافذة الممتدّة وحزمة
> `OPUS_OPERATOR_PACKAGE.md §②`.
> **قراءةٌ/بحثٌ فقط · وصفيٌّ بنصّه (يُنشَر جدولٌ لا حكم) · صفرُ مسٍّ بالإنتاج · لا
> `LOGIC_VERSION` · لا عتبةَ تتحرّك · ولا وكلاء.**

---

## ⓪ إعلانٌ يسبق كلَّ شيء — ما رأيتُه قبل أن أكتب هذا العقد

قبل كتابة هذا الملفّ قرأتُ نتيجةَ إعادة قراءة الرابط (التشغيلة `35029466583` ·
‏498 مِرساةً منذ 2026-08-18 · قِيس منها 482) ورأيتُ فيها: **أساسُ `exploded50`
‏10.8%** · **وسيطُ `mg_day` ‏+8.5%** · **وسيطُ `close_ret` ‏−4.4%** · **وسيطُ `mg_5d`
‏+5.2%** · وأن `after` أسوأُ الأوقات (‏4.3% · وسيطُ إغلاقٍ ‏−11.8%) · وأن `J1` سقط
على معيار التكرار وأن `قوي`/`gap≥30%`/`pre` تعبر الرباعيَّ وتتحلّل بين النصفين.

**وهذا يُعلَن ولا يُطوى**، وأربعةُ قيودٍ عليه:
1. **كلُّ ما رأيتُه أرقامُ يومٍ كامل** (قمّةٌ وإغلاقٌ وخمسُ جلسات) — **وموضوعُ هذا
   العقد هو المسارُ بين المِرساة والإغلاق** (‏+5 · +15 · +30 · +60 دقيقة · زمنُ
   القمّة · الهبوطُ قبلها · وبريماركت الغد) **وهو ما لم يُحسَب في أيّ أداةٍ عندنا**
   (بحثُ الأرشيفين: `grep -n "OPCURVE\|منحنى ما بعد"` صفرُ مطابقة · و`T-PM-CURVE`
   المُغلَقة منحنى **البريماركت قبل** الجلسة لا ما بعد المِرساة).
2. **التقاطعُ `pre ∧ gap≥30%` مُطَّلَعٌ عليه** (اختير في `OPUS_OPERATOR_PACKAGE.md
   §⓪-ب` بعد رؤية أرقام إعادة القراءة) ⇒ يُطبَع سلّةً **وصفيّةً موسومةً «بعديّة»** ولا
   يُبنى عليه شيءٌ في هذا العقد.
3. **صفرُ رقمِ مسارٍ رأيتُه** — لا وسيطَ ساعةٍ ولا زمنَ قمّةٍ ولا هبوطًا قبلها ⇒
   التنبّؤاتُ في §⑥ **عمياءُ** عن مادّتها.
4. **المجتمعُ عُدّ من التشغيلة لا من نسختي المحليّة** (‏النسخةُ ضحلةُ التاريخ منذ
   09-04 · والملفُّ الحيُّ يحمل يومَين فقط) ⇒ رقمُ ‏498/482 **منقولٌ** والأداةُ
   تُعيد العدَّ على تاريخٍ كامل (`fetch-depth: 0`).

---

## ① السؤال بصيغةٍ قابلةٍ للتكذيب

المالكُ: «أسهمٌ كثيرة جدًّا تجيني *هنا دخل المضارب* وبعدها **بفترةٍ بسيطة** تنفجر».
`T-TIERLINK` أجابت **مَن** ينفجر (‏≈11%) ولم تُجب **متى** ولا **كم يهبط قبل أن يصعد**.
**السؤالان هنا:**
- **(أ) الزمن:** بعد كرت `M5`، أين تقع القمّةُ في الوقت؟ وكم من الصاعدين يبلغ
  **‏+10%** (هدفُ `T-TARGET10` المعتمَد) خلال 5 · 15 · 30 · 60 دقيقة · وحتى الإغلاق؟
- **(ب) الثمن:** كم يهبط السهمُ **قبل** قمّته وكم يكسر قاعَ المِرساة في كلّ أفق؟ —
  وهذا الهبوطُ هو ما يقتل المضاربَ لا غيابُ الانفجار.

**وسقفُ الجواب مكتوبٌ سلفًا:** جدولُ مساراتٍ بالسلال ‏+ **نافذةُ خروجٍ تُستخرَج بقاعدةٍ
مثبَّتةٍ هنا (§⑦)** تُغذّي عقدَ `T-OPTRADE` — **ولا حكمَ على ربحيّةٍ ولا `R` ولا سياسة**.

---

## ② المجتمع — ثلاثُ طبقاتٍ مُسمّاة، لا انتقاء

- **A — المراسي الحيّة:** `tierlink_probe.anchor_history()` **بالاسم** = كلُّ `LIQ:*`
  بـ`anchor_ms` من **آخر لقطةِ `op_entry_state.json` لكلّ يومٍ** في تاريخ git، منذ
  `OPCURVE_SINCE` = **2026-08-18** حتى يوم التشغيل. ⚠️ بالبناء: المِرساةُ المقروءة
  = **آخرُ مِرساةٍ للرمز في يومه** (بعد أيّ إعادةِ رسوٍّ `A1`) — كما في `T-TIERLINK`.
- **B — كروتُ `M5` المُسلَّمة:** `tier_fwd_report.load_ledger()` **بالاسم** (أوّلُ صفٍّ
  لكلّ (تاريخ، رمز)) — مصدرُ `e5` · `prev_close` · `tier` · `j1` · `cls`.
- **C — وسمُ الزناد:** حقلُ `trig` في صفّ المِرساة (‏`None` = `R1` · `"T-C"` = الزنادُ
  الموازي) **معتبرٌ فقط للمراسي بعد `liq_trig_read.SHIP_ISO`** (‏2026-09-09T15:26:30Z ·
  بالاسم) — قبله لا زنادَ موازيًا فتُوسَم «قبل الشحن» ولا تدخل سلّةَ الزناد.

**لا يُستبعَد صفٌّ إلّا لتعذّر الأساس أو الجلب — ويُعَدّ ويُطبَع.**

---

## ③ الأساسُ والمقاييس — كلُّها من الشموع نفسِها (`adjusted=false` · صفرُ نظرٍ مستقبليّ في الميزات)

**الأساس = سعرُ كرت `M5`** (`e5` من B، وإلّا `tier_days_report.true_e5` **بالاسم**) —
السعرُ الذي يملكه المالكُ فعلًا (درسُ `HUIZ`). و**`t0` = بدايةُ شمعة الكرت** =
`anchor_ms + 4·60,000` (الشمعةُ التي إغلاقُها `e5`؛ نفسُ `card_ms` في `tierlink_probe.measure`).
والشموعُ: `tier_fwd_report.fetch_day(sym, day, key)` **بالاسم** (يومٌ كاملٌ بريماركت +
نظاميّ + أفتر). **النوافذُ بالساعة الجداريّة لا بعدّ الشموع** (الدقيقةُ بلا صفقةٍ لا
تُطيل النافذة): `W_h` = الشموعُ ذاتُ `t0 < t ≤ t0 + h·60,000`.

| رمز | التعريف | مصدرُ الحساب |
|---|---|---|
| **`up_h`** لكلّ `h ∈ {5, 15, 30, 60}` | أقصى `high` في `W_h` ÷ `e5` − 1 | مباشر |
| **`dn_h`** | أدنى `low` في `W_h` ÷ `e5` − 1 (**الهبوطُ الأقصى** في الأفق) | مباشر |
| **`ret_h`** | إغلاقُ آخر شمعةٍ في `W_h` ÷ `e5` − 1 | مباشر |
| **`up_reg`** / **`dn_reg`** / **`close_reg`** | حتى **إغلاق النظاميّ** (آخر شمعةٍ ساعتُها بنيويورك دون 16) | مباشر · `kasih_scan.NY` |
| **`up_ext`** | أقصى `high` بعد `t0` **حتى آخر شمعةٍ في اليوم** (يشمل الأفتر) = `mg_day` المنشور | `sym_day_probe.full_day_max` بالاسم |
| **`t_peak`** | دقائقُ من `t0` إلى شمعة `up_ext` | `full_day_max` تُعيد `(best, at)` |
| **`dd_pre_peak`** | أدنى `low` في `(t0, at_peak]` ÷ `e5` − 1 (**كم هبط قبل قمّته**) | مباشر |
| **`t_hit10`** | دقائقُ من `t0` إلى **أوّل** شمعةٍ `high ≥ 1.10·e5` (لمسٌ) · `None` إن لم تُلمَس | مباشر |
| **`t_stop`** | دقائقُ من `t0` إلى **أوّل إغلاقِ دقيقةٍ دون `anchor_low`** · `None` إن لم يُكسَر | `sym_day_probe.exit_point` بالاسم |
| **`pm_up`** / **`pm_dn`** / **`pm_last`** | في **بريماركت يوم التداول التالي** (شموعُ `fetch_day(sym, next)` ذاتُ ساعةِ نيويورك دون 9.5): أقصى/أدنى/آخرُ إغلاق ÷ `e5` − 1 · `None` إن لم تُتداوَل | `next` = أوّلُ تاريخٍ بعد اليوم في `tierlink_probe.daily_range` بالاسم |

**والمُشتقّان:** `hit10_by_h` = حصّةُ المراسي التي `t_hit10 ≤ h` · `stop_by_h` =
حصّةُ التي `t_stop ≤ h` — **كلٌّ على حدة**، **ولا يُحسَب «‏+10% قبل الوقف» مركَّبًا**
ولا `R` ولا تكلفة: **ذاك موضوعُ `T-OPTRADE` وحكمُه يجب أن يبقى غيرَ مُطَّلَعٍ عليه.**

---

## ④ السلال — قائمةٌ مُغلَقة، لا تُضاف سلّةٌ بعد الأرقام

`الكلّ` · `trig` (‏`R1` / `T-C` — بعد الشحن فقط · و«قبل الشحن» تُعَدّ) · `tod`
(‏`pre` / `reg` / `after` — تعريفُ `tierlink_probe.features` حرفيًّا) · `tier` (قوي /
متوسط / ضعيف) · `j1` · `gap` (‏<10% / 10-30% / ≥30%) · **`pre ∧ gap≥30%`** (بعديّةٌ
— §⓪-2) · **`exploded50`** (نعم/لا — **اختيارٌ على النتيجة**: يصف **شكلَ** الانفجار
في ساعته الأولى ولا يُبنى عليه اختيار).
**والنصفان:** `H1` = قبل **2026-09-01** · `H2` = منه فصاعدًا (يُطبَع كلُّ رقمٍ للنصفين).

**لكلّ سلّةٍ وأفق:** `n` · الوسيط · الرُّبيعان (‏p25/p75) · و**فاصلُ بوتستراب ‏95%
عنقوديٌّ بالرمز** للوسيط (`btcost_arms.boot_ci_median` **بالاسم** · بذرتُه الثابتة)
**على ثلاثة أرقامٍ رئيسة فقط** (‏`up_60` · `close_reg` · `pm_last`) لئلّا يغرق التقرير.

---

## ⑤ الصلاحية — حرّاسٌ يُطبَع كلٌّ منها في كلّ تقرير

| الحارس | الشرط | إن سقط |
|---|---|---|
| `V-C1` | `POLYGON_API_KEY` حاضر | خروج **2** بلا رقم |
| `V-C2` | صفرُ مِرساةٍ في التاريخ = بصمةُ `no-op` | خروج **4** |
| `V-C3` | **التغطية** (قِيس ÷ المجتمع) **‏≥ 80%** | خروج **3** — لا يُفسَّر رقم |
| `V-C4` | `adjusted=false` في كلّ جلبة (بالاسم من `fetch_day`/`daily_range`) | بنيويٌّ — مقفول |
| `V-C5` | **قراءةٌ فقط:** صفرُ إرسالٍ · صفرُ كتابةِ حالة · والإنتاجُ لا يستورد الأداة | AST مقفول |
| `V-C6` | **الحتميّة:** المدخلاتُ نفسُها ⇒ المُخرَجُ نفسُه بت-بت (بذرةُ البوتستراب ثابتة · لا `random` عارٍ) | مقفول |
| `V-C7` | `e5` = سعرُ الكرت نفسُه الذي أُنتجت به أرقامُ `T-TIERLINK` (`true_e5`/`e5` من B) | بالاسم |
| `V-C8` | بريماركتُ الغد: يُعَدّ **الغائبُ** (لا شموعَ) ولا يُخمَّن صفرًا · وتُطبَع حصّةُ الغياب | مقفول |
| `V-C9` | سلّةُ الزناد **بعد `SHIP_ISO` فقط** وعددُها يُطبَع — دون **20** ⇒ توصَف «صغيرة» في السطر نفسِه | مقفول |

---

## ⑥ التنبّؤاتُ القابلة للكسر — تُكتب الآن وتُنشَر مكذَّبةً أو مؤكَّدة

- **`CP1`** بين `exploded50` وسيطُ `t_peak` **‏≤ 60 دقيقة** (الانفجارُ يبلغ قمّتَه في ساعته الأولى).
- **`CP2`** على `الكلّ` وسيطُ `dn_60` **‏≤ −5%** (الهبوطُ الأقصى في الساعة الأولى ليس تافهًا).
- **`CP3`** `hit10_by_60` **أكبرُ من** (`hit10_by_reg` − `hit10_by_60`): مَن يبلغ ‏+10% يبلغه في الساعة الأولى أكثرَ ممّا بعدها.
- **`CP4`** وسيطُ `pm_last` **أدنى من** وسيط `close_reg` (الحملُ ليلًا يخسر على الوسيط).
- **`CP5`** وسيطُ `up_60` لسلّة `T-C` **أدنى منه** لسلّة `R1` (الزنادُ التراكميّ يلتقط الزاحفَ لا المنفجر).
- **`CP6`** وسيطُ `up_reg` لـ`pre` **أعلى منه** لـ`reg` (امتدادُ فصل `pre` المنشور إلى المقدار).
- **`CP7`** قاعدةُ النافذة §⑦ تُخرج **`h*` = 60 أو 120 دقيقة** — لا الإغلاق.

---

## ⑦ سقفُ النجاح — قاعدتان مثبَّتتان الآن تُغذّيان `T-OPTRADE` ولا تُختاران بعد الأرقام

- **قاعدةُ النافذة `W`:** `hits` = المراسي التي `t_hit10` معرَّفٌ **وداخلَ النظاميّ**.
  إن كان `|hits| < 30` ⇒ **`h* = EOD`** (إغلاقُ النظاميّ). وإلّا `p75` = الرُّبيعُ الأعلى
  لـ`t_hit10` بين `hits` (رتبةٌ أقرب — `btcost_arms.pctile` **بالاسم**) و**`h*` = أصغرُ
  عنصرٍ في `{15, 30, 60, 120, 240}` ‏≥ `p75`**، وإن لم يوجد ⇒ `EOD`. (‏`p50`/`p90`
  تُطبَعان للاطّلاع ولا تحكمان.)
- **قاعدةُ الليل `O`:** إن كان وسيطُ `pm_last` **أعلى من** وسيط `close_reg` **في النصفين
  معًا** (بين مَن له بريماركتُ غد) ⇒ يحمل عقدُ `T-OPTRADE` ذراعًا **وصفيّةً** رابعة `P4`
  (حملٌ إلى بريماركت الغد) — وإلّا لا ذراع. **وصفيّةٌ لا حاكمة** بنصّ هذا السطر.
- **وما لا يخرج من هذا العقد مهما كانت الأرقام:** لا سطرُ تسليمٍ · لا عتبةٌ · لا
  بوّابةٌ · لا وزنُ ترتيب · لا `LOGIC_VERSION` · ولا «أفضلُ سلّة» تُختار بعد الأرقام.

---

## ⑧ حدودُ صدقٍ مكتوبةٌ قبل النتيجة

- **لمسٌ لا تنفيذ:** `up_h`/`dn_h`/`t_hit10` من قممٍ وقيعانٍ لُمست بلا انزلاقٍ ولا سبريد
  (سقفٌ متفائل) — والتكلفةُ المقيسة (`C-MED` ‏1.80%) **موضوعُ `T-OPTRADE`** لا هنا.
- **‏≈4 أسابيعَ من سوقٍ واحد** (‏08-18 ⟶ يوم التشغيل) وإعادةُ قراءة الرابط أظهرت أن
  **كلَّ ميزةٍ تتحلّل من `H1` إلى `H2`** ⇒ المنحنى يُقرأ **بنصفَيه** لا بمجمَّعه.
- **المجتمعُ = المراسي التي بقيت في آخر لقطةِ يومها**: ما كُتم ببوّابة المضارب ثمّ زال
  من الحالة **ليس فيه** ⇒ منحنى **ما وصل المالكَ أو صمد**، لا كلِّ ما رسا.
- **`e5` قد يغيب** (كسرُ القاع قبل اكتمال الخمس ⇒ `true_e5` تُعيد `None`) ⇒ يُعَدّ
  «بلا أساس» ويُطبَع، ولا يُنسَب.
- **سلّةُ الزناد قصيرة:** `T-C` مشحونٌ منذ 09-09 ⇒ **‏≈4-5 جلساتٍ** ⇒ وصفيّةٌ صغيرةٌ
  بالتعريف (‏`V-C9`).
- **`exploded50` سلّةُ نتيجة** — تصف ولا تختار (§④).
- **بريماركتُ الغد رقيقُ السيولة** (شموعٌ قليلة · سبريد واسع) ⇒ `pm_last` قد يكون
  إغلاقَ صفقةٍ يتيمة؛ يُطبَع عددُ شموعه.
- **الرُّبيعاتُ والفواصلُ على الوسيط** — لا متوسّطاتٍ (الذيولُ الطويلة تُشوّه المتوسّط).
- **‏٪ من `e5` لا من المِرساة** ⇒ لا تُقارَن هذي الأرقامُ بأرقام `kasih` (المِرساة) إلّا
  عبر `up_ext` = `mg_day` الذي يشترك مع `T-TIERLINK` بالبناء.
- **الحصّادُ الأماميّ** (`T-J1PM` · `T-TC-YIELD`) لا يُمَسّ ولا يُقرأ منه رقمٌ هنا.

---

## ⑨ الأداةُ حين تُبنى — بعد دمج هذا العقد لا قبله

- `opcurve_probe.py` + `.github/workflows/opcurve.yml` (**يدويٌّ بلا كرون** ·
  `contents: read` · `fetch-depth: 0` · سرُّ `POLYGON_API_KEY` وحدَه · مدخلان
  `since` و`h2_from` موصولان بـ`OPCURVE_SINCE`/`OPCURVE_H2_FROM`).
- تستورد **بالاسم**: `tierlink_probe.anchor_history/features/daily_range` ·
  `tier_fwd_report.fetch_day/load_ledger` · `tier_days_report.true_e5` ·
  `sym_day_probe.full_day_max/exit_point` · `kasih_scan.NY/wilson` ·
  `btcost_arms.boot_ci_median/pctile` · `liq_trig_read.SHIP_ISO`. **صفرُ منطقِ
  حسمٍ مكرَّر.**
- **رموزُ الخروج:** 0 صدر · 2 لا مفتاح · 3 تغطيةٌ دون 80% · 4 صفرُ مِرساة.
- **المُخرَج:** جدولُ §③ × سلال §④ × النصفين · `hit10_by_h`/`stop_by_h` · `h*` و`O`
  بنصّ §⑦ · التنبّؤاتُ السبعةُ مؤكَّدةً/مكذَّبةً · وحدودُ الصدق تُطبَع في ذيل التقرير.
  والصفوفُ كاملةً بلا قصّ (وما قُصّ يُعلَن بعدّاده). النتيجةُ في `opcurve_result.md`
  **بجانب هذا العقد لا فوقه**.

---

## ⑩ الأقفالُ المطلوبة قبل الدفع (والطفراتُ التي تُسقطها)

- **`OCK0`** — **بنيويّ**: جدولُ §③ يحمل المقاييسَ المسمّاة (‏`up_h` · `dn_h` ·
  `ret_h` · `t_peak` · `dd_pre_peak` · `t_hit10` · `t_stop` · `pm_last`) كلًّا في صفّه ·
  وقاعدتا §⑦ (`W` و`O`) كلٌّ في بندها · و«الحرّاسُ» `V-C1`-`V-C9` التسعةُ في جدولهم.
- **`OCK1`** — §⓪ يُعلن التلوّثَ **بأرقامه** (‏10.8% · +8.5% · −4.4% · +5.2%) ويُسمّي
  التقاطعَ البعديَّ · و§③ يُصرّح **بعدم** حساب «‏+10% قبل الوقف» مركَّبًا.
- **`OCK2`** — **الآليّةُ من الكود الحيّ لا من نصّ العقد**: `anchor_history`/`features`/
  `daily_range` موجودةٌ في `tierlink_probe` · `full_day_max` تُعيد **زمنَ** القمّة
  (سلوكيًّا على فِكستشر) · `exit_point` تُعيد زمنَ الكسر · `true_e5` تُعيد `None` بلا
  شمعةِ خمس · `liq_trig_read.SHIP_ISO` معرَّفٌ ونصُّه يطابق §② · و`btcost_arms.pctile`
  رتبةٌ أقرب (‏`p75` على فِكستشرٍ معلوم).
- **`OCK3`** — التنبّؤاتُ `CP1`-`CP7` **سبعةٌ كلٌّ في سطره** · والقائمةُ المُغلَقة في
  §④ تحمل السلالَ الثماني بأسمائها · و`H2` = 2026-09-01 مكتوبٌ.
- وأقفالُ الأداة (`OCA*`) تُكتب مع الأداة: صفرُ نداءٍ محظور (AST) · الاستيرادُ بالاسم ·
  الحتميّة · رموزُ الخروج · وصلةُ الـworkflow.
