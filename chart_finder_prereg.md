# 🔎 `T-CHARTID` — مُعرِّف الشارت: العقدُ المسبق للقبول

> **مدفوعٌ قبل أيّ تشغيلٍ على المجموعة الحقيقيّة (§④) وقبل أيّ رقمٍ منها.** أمرُ المالك
> (‏2026-09-24) بنصّه في §⓪-ب. **الأداةُ ليست تجربةَ حافّة** بل أداةُ تعرّف — والعقدُ يحدّد
> متى يُقال عنها «جاهزة» ومتى تُوسَم كلُّ إجابةٍ منها «تجريبيّة»، **ولا يُرخى بعد أيّ رقم**.

---

## ⓪ إفصاحٌ كامل — ما رأيتُه قبل كتابة هذا العقد

| رُئي | ما هو | هل يمسّ التقييم؟ |
|---|---|---|
| صورتا المالك A (منشورُ @kisar_ · موموو) وB (تريدنج فيو) | **تطبيقٌ حيّ لا تقييم**: التشغيلة `36051868290` ⟵ **SXTC** في الاثنتين (A: مرساةٌ لحظيّة وحيدة ‏+ النافذة · B: تحقّقٌ متقاطع) | لا — ليستا من المجموعتين |
| بطاقة B **وحدَها** (بلا تحقّقٍ متقاطع) | التشغيلة `36052993616` (مِجَسٌّ مؤقّت على الفرع): لوحةُ 754 يومًا ⟵ **383 رمزًا** عبرت القيدَين الخامَّين ⟵ فُحص أوّلُ 60 بالتقسيم الفعليّ ⟵ **SXTC عابرٌ وحيد «واثق»** · 🔴 **ورأيتُ هذه النتيجة قبل دفع العقد** | نعم على `CP1` وحدَه: لم يعد تنبّؤًا مسبقًا (§⑥) |
| مِجَسُّ الجلسة (التشغيلة نفسُها) | يوميُّ Polygon = **الجلسة النظاميّة**: ‏29 يومًا فاصلًا (SXTC 14 · SMTK 15) كلُّها نظاميّة وصفرٌ ممتدّ · وفي 3 أيامٍ من SXTC فرقُ سنتٍ في الأدنى بين اليوميّ والنظاميّ (‏3.46/3.47 · 3.00/3.01 · 2.14/2.1409) | يُبنى عليه §① (اليوميّ نظاميّ) — ولا يُضبط عليه ثابت |
| قراءةُ البكسل على A وB | B: ارتباطُ الإغلاقات بشموع SXTC الفعليّة ‏≈1.0 (37 شمعة) · **A: فشلت** (‏47 كشفًا زائفًا من الخطوط والتسميات والعلامات) | تُعلَن حدًّا ولا تُضبط عليها |
| `TG_1977` و`TG_2051` | رأيتُهما بالعين عند كتابة البرومبت · **لم يُبحث عنهما** | هما الاختبارُ الحيّ (§④-ج) |
| صورُ المجموعة الحقيقيّة | **لا صورةَ منها كُتبت بطاقتُها أو بُحث عنها** | — |

## ⓪-ب أمرُ المالك بنصّه (2026-09-24)

```text
ابنِ أداة «🔎 مُعرِّف الشارت»: أرسل لك صورة شارت لأي سهم أمريكي، وترجع لي رمزه مع الدليل، أو تقول «لا أعرف» بصراحة.
نفّذ كاملًا بالتفويض، وقواعد CLAUDE.md أعلى من هذا النص عند التعارض.

① النطاق
- المدخل: لقطة أرسلها لك في المحادثة (تريدنج فيو أو موموو أو ويبل، فاتح أو داكن، وقد يكون عليها خطوط فيصل ومؤشرات).
- الكون: كل الأسهم الأمريكية المدرجة، بما فيها المشطوبة لاحقًا. وOTC جولة ثانية إذا ما وُجد تطابق.
- النسخة الأولى: اليومي والأسبوعي إذا ظهر محور السعر، واللحظي إذا ظهر تاريخه. اللحظي بلا تاريخ يُحاوَل ولا يُخمَّن.
- إذا الرمز ظاهر في الصورة: اقرأه، ثم تحقّق منه بالمطابقة. لا تعتمد على القراءة وحدها.

② الطريقة
أ) بطاقة الشارت (أنت، داخل الجلسة): اقرأ كل نص في الصورة واكتبه JSON بحقول ثابتة:
   - التاريخ والوقت ومنطقته الزمنية، والفريم.
   - نوع الشارت (شموع أو هايكن آشي أو خط أو مساحة)، والمقياس (خطي أو لوغاريتمي).
   - أرقام OHLC في رأس الشارت وعلى أي شمعة تنطبق، ونسبة التغيّر، والحجم.
   - آخر سعر، وأعلى وأدنى الشاشة، وتسميات المحورين.
   - المؤشرات ببارامتراتها وقيمها، وهل الجلسة الممتدة ظاهرة.
   كل رقم يُوسَم «نص مقروء» (دقيق) أو «من البكسل» (تقريبي)، والتسامح يتبع الوسم. والبطاقة لا تحمل الرمز.
ب) قراءة البكسل (احتياط فقط): دالة نقيّة تستخرج سلسلة الشموع من الصورة، وتحذف الخطوط الأفقية المرسومة، وتطبع صورة تحقّق فوق الأصل.
ج) البحث (على Actions، workflow يدوي اسمه chart_finder.yml، مدخله البطاقة):
   - السوق كله يومًا بيوم من Polygon. القيود النصية تصفّي أولًا، ثم الشكل يرتّب، ثم المؤشرات تؤكّد.
   - طابق على الأسعار كما تداولت فعلًا، ثم بعوامل التقسيم الفعلية للمرشّحين، لأن الشارت يعرض تعديل التقسيم حتى لحظة التقاطه فقط.
   - اللحظي: قائمة قصيرة من اليومي، ثم شموع الدقيقة للمرشّحين.
   - جرّب الوقت بتوقيت نيويورك ثم بتوقيت الجهاز. وأيام التداول من market_calendar.
   - إذا ما ظهر تاريخ: امسح آخر 3 سنوات.
   - أعد استعمال الموجود، واكتب قبل كل واحد «بُنيت لـ…»: link100_probe.grouped_day · presession_radar.polygon_grouped وpolygon_minutes · faisal_indicator_anchor · faisal_price_anchor. كلها بُنيت لسهم معروف أو لغرض آخر، لا للبحث في السوق كله.
   - قِس بتجربة قصيرة قبل الاعتماد عليه: هل الشمعة اليومية في Polygon تشمل الجلسة الممتدة؟
د) التحقّق: السجلّ يطبع سلسلة أعلى 3 مرشّحين. ارسم منها شارت بنفس النافذة والفريم، وقارنه بالأصل بعينك قبل ما تجاوبني، وأرسل لي صورة المقارنة.

③ الثقة
- «واثق» بثلاثة شروط معًا: كل قيد نصي دقيق طابق ضمن تسامحه، والمرشّح الثاني يسقط على قيد دقيق واحد على الأقل، والمقارنة بالعين متطابقة.
- وما دون ذلك يُوسَم «مرجّح» أو «غير محسوم» أو «لا تطابق». لا تخمين صامت.

④ الإثبات قبل ما تقول «جاهزة»
- مجموعة مصنوعة بتوليد ثابت: نوافذ حقيقية من 2023 إلى 2025 تُرسم بأشكال مختلفة (فاتح، داكن، لوغاريتمي، مع خطوط فيصل، بلا رمز). ضبط العتبات عليها وحدها.
- مجموعة حقيقية: 30 صورة على الأقل من faisal_images رمزها معروف من الكاتالوج. بطاقاتها تُكتب وتُجمَّد قبل أي بحث.
- معيار القبول يُدمج قبل أول تشغيل على المجموعة الحقيقية، ولا يُرخى بعده. المقترح:
  - إذا قالت الأداة «واثق»، تكون صحيحة في 95% من المرات على الأقل.
  - الرمز الصحيح ضمن أول 3 في 80% على الأقل من الشارتات اليومية اللي فيها محور سعر.
- اختبار حي، والنتيجة تُنشر أيًّا كانت، ولو «لا أعرف»:
  - TG_1977: لقطة لحظية بتاريخ 04/15/2026، السهم فيها من 1.425 إلى 5.050.
  - TG_2051: شارت 4 ساعات بلا تاريخ.

⑤ القيود
- قراءة فقط، خارج الجذور والفرز، بلا تلغرام، بلا وكلاء.
- لا تُرفع صوري إلى المستودع (عام)، ولا بيانات Polygon الخام.
- كل قفل تُسقطه طفرة، وتشغيل الاختبارات في worktree بخروج 0، وCI أخضر على الـPR وعلى main، والدمج عبر PR، وحدّث الذاكرة.
- الهدف: جواب خلال نحو 10 دقائق من الصورة. هدف لا وعد.

⑥ شكل الجواب لكل شارت
🎯 الرمز · الثقة · النافذة أو اللحظة · عامل التقسيم إن وُجد · الرمز الحالي إن تغيّر · أي القيود طابقت وبأي فرق · البدائل الثلاثة · رابط التشغيل · صورة المقارنة.
```

---

## ① الأداةُ كما تُدمج (الحالةُ التي يُحكم عليها)

**الملفّات:** `chart_finder.py` (البطاقة · البحث · الحكم) · `chart_pixels.py` (البكسل) ·
`chart_render.py` (الرسمُ للمقارنة وللمجموعة المصنوعة) · `.github/workflows/chart_finder.yml` (يدويّ).

**البطاقة** (`validate_card`): ‏`v=1` · `id` · `timeframe` من قائمةٍ مغلقة · **وحقلٌ باسم الرمز يُرفض**
(`ticker`/`symbol`/`sym`/`رمز`/`الرمز` في أيّ عمق). **كلُّ رقمٍ نصٌّ كما قُرئ** ⇒ التسامحُ
`نصفُ آخر خانة + FEED_TOL_PCT%` من القيمة (‏`tol_near`) · والحجمُ `VOL_TOL_PCT` **ترتيبًا لا بوّابة**.

**المسارات:**
| البطاقة | المسار |
|---|---|
| مرساةٌ مؤرَّخة (رأسُ الشارت O/H/L/C بتاريخ) | ملفُّ دقائق اليوم **لكلّ الرموز** ⟵ كلُّ تعريفٍ للشمعة (`SPANS` × البداية/النهاية × نيويورك ثم `DEVICE_TZ`) ⟵ عابرو الأسعار الأربعة ⟵ **قيودُ النافذة** لكلٍّ من دقائقه الخام مُسوّاةً بالتقسيم |
| نافذةٌ مؤرَّخة بلا مرساة (تواريخُ المحور) | لوحةُ أيام النافذة ‏±10 ⟵ الطرفان وآخرُ سعر على كلّ رمز ⟵ القائمةُ القصيرة بالتقسيم الفعليّ حتى نهاية النافذة (`stage_market_dated` ⟵ `stage_dated_exact`) |
| بلا تاريخ | لوحةُ السوق لآخر `UNDATED_YEARS` سنوات (ملفّاتُ S3 اليوميّة · REST احتياطًا) ⟵ قيدان لا يكسرهما التقسيم (آخرُ سعر · أحدُ الطرفين خامًا) ⟵ ترتيبٌ حتميّ (الخطأ ثم الرمز) ⟵ أوّلُ `SHORTLIST_MAX` رمزًا (**والمقصوصُ يُعلَن بعدده**) ⟵ `undated_scan` بالتقسيم الفعليّ **على أيام النهاية التي عبرت خامًا وحدَها** (`_LAST_ENDS`) · والتقسيماتُ **جماعيّةٌ بنداءٍ واحد** من أوّل أيام اللوحة (`load_all_splits`) ونداءُ الرمز خارج تغطيتها وحدَها |
| `cross_with` | **تحقّقٌ متقاطع** على مرشّحي بطاقةٍ أخرى — يُوسَم `mode=cross` ولا يُعَدّ بحثًا مستقلًّا |

**والترتيبُ حتميٌّ في كلّ المسارات** (الرمزُ يحسم كلَّ تعادلٍ في الخطأ) — فالحكمُ نفسُه في كلّ تشغيلة.

**قاعدةُ التقسيم:** السعرُ الخامّ يومَ `d` يُعرض على شارتٍ التُقط يومَ `as_of` مضروبًا في جداء
`from/to` لكلّ تقسيمٍ `d < تنفيذه ≤ as_of` (‏`split_factor`).

**الشمعةُ اليوميّة في Polygon = الجلسة النظاميّة (مقيس لا مفترض — §⓪):** فالمسارُ اليوميّ يُطابق
النظاميّةَ وحدَها، والمرساةُ اللحظيّة تُبنى من الدقائق **بالجلسة الممتدّة** (ملفُّ الدقائق يحملها).

**الثوابت (هندسيّة · وسمُها `engineering`):**
`FEED_TOL_PCT=0.5` · `VOL_TOL_PCT=5` · `DECISIVE_RATIO=3` · `WINDOW_SLACK_DAYS=3` ·
`SPANS=(1,2,3,5,10,15,30,45,60,90,120,180,240,390,960)` · `DEVICE_TZ=Asia/Riyadh` ·
`UNDATED_YEARS=3` · `SHORTLIST_MAX=1000` · `ANCHOR_MAX=25` · `PANEL_WORKERS=8`.

**طولُ النافذة بالأيام** (`window_days`): عددُ الشموع الظاهرة ‏±(20%-25%) × أيامِ الشمعة — اليوميّ 1 ·
الأسبوعيّ 5 · الشهريّ 21 · واللحظيُّ بلا تاريخ بين الجلسة الممتدّة (16 ساعة) والنظاميّة (6.5) **تقريبًا
بشموعٍ يوميّةٍ نظاميّة** (محاولةٌ لا تخمين: الحكمُ يُسقط ما لا يطابق) · وبلا عددٍ ⟵ 20-120 يومًا.

## ② قاعدةُ الثقة (نصُّ المالك §③ مُفصَّلًا)

- **آليًّا** (`verdict`): **واثق** = مرشّحٌ **وحيد** يعبر كلَّ القيود ضمن تسامحها **وقيدان مستقلّان
  على الأقل** · **مرجّح** = عابرٌ وحيدٌ بقيدٍ واحد، أو عابرون وخطأُ الثاني ‏≥ `DECISIVE_RATIO`× الأوّل ·
  **غير محسوم** = عابرون لا يفصلهم شيء · **لا تطابق** = لا عابر.
- **والتفرّدُ يُتحقَّق على كلّ العابرين لا على عيّنة:** إن قُصّ عابرٌ خامٌّ واحدٌ بلا فحصٍ كامل
  (فوق `SHORTLIST_MAX` في المسارين اليوميَّين، أو فوق `ANCHOR_MAX` في المرساة) ⟵ **«واثق» يُحجب إلى
  «مرجّح»** ويُعلَن عددُ المقصوص. (🔴 كُشف قبل الدمج: التشغيلةُ `36052993616` فحصت 60 من 383 وقالت
  «وحيد» — والوحدةُ لم تكن مُتحقَّقةً على الباقي ⇒ رُفع السقفُ بعد تجميع التقسيمات، وصار القصُّ يحجب.)
- **نهائيًّا للمالك:** «واثق» لا تُقال إلّا بعد **المقارنة بالعين** بنفس النافذة والفريم وإرسال صورتها
  (الشرطُ الثالث) — وإن لم تتطابق ⟵ تُخفَّض إلى «مرجّح» وتُذكر المخالفة.

## ③ المجموعةُ المصنوعة — للضبط وحدَه

- **التوليد:** بذرةٌ `20260924` · ‏150 نافذةً **تنتهي بين 2023-10-01 و2025-12-31** (‏`SYNTH_FROM`/`SYNTH_TO`:
  داخل لوحة «آخر 3 سنوات» يومَ التقييم، لأنّ البحثَ بلا تاريخ لا يرى أبعدَ منها) من لوحة السوق نفسِها
  (رمزٌ وطولُ نافذةٍ 20-90 شمعة ويومُ نهايةٍ بالسحب المنتظم · سعرٌ بين 0.3 و50 · بلا فجوة) · والشموعُ
  **كما يعرضها شارتٌ التُقط يومَ النهاية** (خامٌ × معاملُ التقسيم حتى ذلك اليوم) · تُرسم بـ`render_candles` بأنماطٍ مسحوبة:
  فاتح/داكن · خطّيّ 80% ولوغاريتميّ 20% · 0-4 خطوطٍ أفقيّةٍ بألوان فيصل · تسميتا أعلى/أدنى الشاشة 80% ·
  وسمُ آخر سعر 90% · تواريخُ المحور 50%.
- **البطاقةُ الآليّة** من النصّ المرسوم وحدَه (كما يقرؤه إنسان) — لا من البيانات · **وما لا نصَّ له**
  (بلا تسميتَي أعلى/أدنى · بلا وسمِ آخر سعر) **يُكمَل من قراءة البكسل بتسامحها المصرَّح** (احتياطُ البرومبت ②-ب).
- **يُقاس:** الأوّلُ الصحيح · ضمن أوّل 3 · دقّةُ «واثق» · وللبكسل متوسّطُ الخطأ النسبيّ لـH/L/C وارتباطُ الإغلاقات.
- **ما يجوز ضبطُه عليها وحدها:** `DECISIVE_RATIO` · `SHORTLIST_MAX` · ثوابتُ البكسل (`LINE_MIN_FRAC` ·
  `BODY_COVER` · `SAT_MIN` · `VAL_MIN`). **ولا يُضبط `FEED_TOL_PCT`** (المصنوعةُ بلا ضجيج مصدر فلا تقيسه).
  **والقيمُ النهائيّة تُكتب في ملحق §⑦ ويُدمج قبل أيّ تشغيلٍ على الحقيقيّة** — ثمّ تُجمَّد.
- **وإصلاحُ عيبٍ هندسيٍّ** يكشفه التشغيلُ المصنوع (خطأُ كودٍ لا عتبة) يجوز **قبل الحقيقيّة وحدَها**
  بإعلانه في §⑦: ما هو · ودليلُه من سطور `SYNTH_ROW` · وتشغيلةُ المصنوعة بعده. وبعد أوّل رقمٍ حقيقيّ
  **لا إصلاحَ يُحتسب** إلّا بملحقٍ جديد ومجموعةٍ حقيقيّةٍ جديدة (§⑤ فرع 3).

## ④ المجموعةُ الحقيقيّة — للقبول

- **الاختيارُ الحتميّ:** صفوفُ `FAISAL_IMAGE_PASS2.md` و`FAISAL_IMAGE_PASS3.md` التي خليّتُها الأولى
  «معرّفُ صورة + رمزٌ واحد بحروفٍ كبيرة (2-5)» ووسمُها 🎯 بلا 🔁 وملفُّها موجود (‏189 صفًّا يوم كتابة العقد)
  ⟵ مرتَّبةً بـ`sha256(المعرّف + "20260924")` ⟵ **أوّلُ 40 صورةً تعرض شارتَ أسعارٍ برقمين مقروءين
  على الأقل**. وكلُّ مستبعَدٍ يُسجَّل بسببٍ من ثلاثة لا غير: «ليست شارتًا» · «بلا رقمين مقروءين» ·
  «الرمزُ غيرُ مؤكَّد في الكاتالوج». (`TG_1977` و`TG_2051` **ليسا في المعين أصلًا**: خليّتُهما الأولى بلا رمز.)
- **البطاقاتُ** تُكتب بقراءتي للصورة **بلا رمز** ⟵ تُدفع مع بصمة `sha256` لكلّ ملفّ (`chart_cards/real/MANIFEST.sha256`)
  **قبل أيّ بحث** · ومفتاحُ الأجوبة (`KEY.tsv`) من الكاتالوج **لا يقرؤه البحث** بل المُقيِّم وحدَه.
- **ج) الاختبارُ الحيّ:** `TG_1977` و`TG_2051` — يُنشران أيًّا كانت النتيجة ولو «لا أعرف».

## ⑤ معيارُ القبول (لا يُرخى بعد أيّ رقم)

| الرمز | المعيار | الحدّ الأدنى للعيّنة |
|---|---|---|
| `K1` | من بطاقات الحقيقيّة الموسومة **«واثق» آليًّا**: الأوّلُ = رمزُ الكاتالوج في **95% فأكثر** (ويُطبع حدُّ Wilson الأدنى) | 10 بطاقاتٍ «واثق» |
| `K2` | من بطاقات الحقيقيّة **اليوميّة/الأسبوعيّة التي فيها رقمان مقروءان على محور السعر**: رمزُ الكاتالوج ضمن أوّل 3 في **80% فأكثر** | 20 بطاقة |
| `K3` | كلُّ بطاقةٍ صالحةٍ تنتهي بسطر `CHART_FINDER` وبواحدٍ من الأوسمة الأربعة · صفرُ انهيار | الكلّ |

**الفروع:**
1. `K1` و`K2` و`K3` ⟵ **«جاهزة»**.
2. `K1` و`K3` وسقط `K2` ⟵ **«جاهزة للواثق وحدَه»** — «واثق» منها يُعتمد والتغطيةُ تُنشر بعددها.
3. سقط `K1` أو `K3` ⟵ **«غير جاهزة»** — كلُّ جوابٍ منها يُوسَم «تجريبيّ» · والإصلاحُ بملحقٍ جديد
   **ومجموعةٍ حقيقيّةٍ جديدة** (المستعملةُ احترقت للضبط).
4. عيّنةٌ دون الحدّ ⟵ **«لا حكم»** لذلك المعيار وحدَه.

## ⑥ بطاقةُ التنبّؤ (تُنشر مهما كانت)

| | التنبّؤ | يكذّبه |
|---|---|---|
| `CP1` | بطاقةُ المالك B **وحدَها** (بلا تحقّقٍ متقاطع) ⟵ **SXTC «واثق»** — 🔴 **مكشوفٌ قبل الدفع:** كُتب قبل التشغيلة `36052993616` ورأيتُ نتيجتَها (‏SXTC «واثق») قبل دفع هذا العقد ⇒ **يُنشر ولا يُعدّ تنبّؤًا مسبقًا** | أيُّ وسمٍ آخر أو رمزٍ آخر |
| `CP2` | `TG_1977` ⟵ **«واثق»** برمزٍ واحد | «مرجّح» أو دونه |
| `CP3` | `TG_2051` ⟵ **ليس «واثق»** | «واثق» |
| `CP4` | `K1` يعبر | سقوطُه |
| `CP5` | `K2` يعبر (أتوقّع نحو 85%) | سقوطُه |
| `CP6` | أكثرُ إخفاقات الحقيقيّة: شارتاتٌ بلا تسمياتٍ مقروءة أو لحظيّةٌ بلا تاريخ | غيرُ ذلك |
| `CP7` | المصنوعةُ ببطاقاتٍ نصّيّة: الأوّلُ الصحيح في **90% فأكثر** | دونه |

## ⑦ ملحقُ الضبط (يُكتب بعد المصنوعة وقبل الحقيقيّة)

*(فارغٌ عند الدمج — يُملأ بالقيم النهائيّة وتاريخها ورقمِ تشغيلة المصنوعة.)*

## ⑧ حدودُ الصدق

1. **تسرّبٌ ممكن:** أنا أكتب بطاقات الحقيقيّة **وأرى الرمز في الصورة** ⇒ الاختبارُ الأعمى الأقوى
   بيد المالك (يرسل شارتاتٍ يعرف رموزها ويخفيها) — يُنشر منفصلًا.
2. **اختلافُ المصدر:** التطبيق (تريدنج فيو/موموو) غيرُ Polygon ⇒ `FEED_TOL_PCT` لا يُقاس على المصنوعة.
3. **التغطية:** ملفّاتُ الدقائق (SIP) للمدرجة وحدَها · وOTC خارج هذه النسخة ⇒ شارتُ OTC ⟵ «لا تطابق».
4. **اللحظيّ بلا تاريخ** خارج النسخة الأولى · **وهايكن آشي** لا يُطابَق إلّا إن وُسم في البطاقة.
5. **البكسل احتياط:** يفشل على شارتٍ مزدحمٍ بالخطوط والتسميات والعلامات (صورة A) — يُعلَن ولا يُخمَّن.
6. **نافذةٌ فيها تقسيمٌ وطرفا الشاشة كلاهما قبله** لا يلتقطها المرشِّحُ الخامّ في المسار بلا تاريخ (يشترط
   طرفًا خامًا واحدًا على الأقل) ⇒ تُعدّ إخفاقًا إن وقعت، ويُطبع عددُها من المصنوعة.
7. **الشمعةُ اليوميّة نظاميّة (مقيس):** شارتٌ يوميٌّ يعرض الجلسةَ الممتدّة (نادر) يُطابَق بتسامحٍ لا يسعه ⇒
   قد يسقط الصحيح — يُعلَن ولا يُوسَّع التسامحُ له.

## ⑨ ترتيبُ التنفيذ

1. دمجُ الأداة وهذا العقد ⟵ 2. تشغيلُ المصنوعة ⟵ ملحق §⑦ يُدمج ⟵ 3. بطاقاتُ الحقيقيّة وبصمتُها تُدمج ⟵
4. تشغيلُ الحقيقيّة والاختبار الحيّ على main ⟵ 5. `chart_finder_result.md` بالحكم والتنبّؤات ⟵ 6. الذاكرة.
