# 🌅📡 `T-PMFWD` — تسجيلٌ مسبقٌ **أماميّ**: هل يُسلِّم البريماركت على **مرشّحينا**، وبأيّ كلفة؟

> **مدفوعٌ ومدموجٌ قبل أيّ سطرِ أداةٍ وقبل أوّل صفٍّ يُحصَد.** أمرُ المالك
> **«سجّل الأماميّ»** (‏2026-09-17) بعد حكم `T-PMUSD` (الفرعُ 1 «تُوصى»).
> **حصادٌ/قراءةٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · ولا رسالةٌ تُرسَل.**

---

## ⓪ إعلانُ التلوّث — ما أعرفه، وما يُقيّده

**أعرف من `T-PMUSD`** (مقيسًا على السوق كلِّه رجعيًّا): `pm_usd > 1M` صباحَ يوم
الحدث تفصل المنفجرين ‏**20.90× · 12.53× · 8.92×** مقابل الشاهد المقطعيّ و‏**17.73× ·
13.98× · 14.22×** مقابل الذاتيّ، **وتضيف** فوق `vol_x ≥ 3` و`ret5 > +20%`
(‏`PU3` ‏20.38× · 15.43× · 12.63×) · **وسيولةُ الأمس لا تفصل شيئًا** (‏≈1.0×).

⚠️ **فاتّجاهُ الإشارة معروفٌ لي — والمجهولُ هنا شيئان لم يُقَسا قطّ:**
**(أ) هل تنطبق على مجتمعٍ مفلترٍ سلفًا** (مرشّحو الارتكاز عند قيعانهم، وهم
**ليسوا** عيّنةَ السوق) **(ب) وبأيّ كلفةِ تسليمٍ يوميّة.**

**وثلاثةُ قيودٍ تنشأ عن التلوّث وتُكتب الآن:**
1. **لا تُعاير العتبةُ بعد أيّ رقم** — `PM_FWD_USD = 1,000,000` هي عتبةُ `T-PMUSD`
   حرفيًّا (سلّةُ `pm_feats` العليا)، ولا تُحرَّك صعودًا ولا نزولًا.
2. **لا تُضاف ذراعٌ ولا معيارٌ بعد أوّل صفٍّ يُقرأ** — القائمةُ مُغلَقةٌ في §④.
3. **ولا يُحصَر المجتمعُ ولا يُوسَّع** بعد الدفع — هو المنصوصُ في §②.

---

## ① السؤالُ القابل للتكذيب

على **قائمتنا الحيّة**: هل `pm_usd > 1M` في صباحِ يومٍ ما تفصل الرموزَ التي
**تتحرّك** عن التي لا تتحرّك — **وهل تضيف على طبقة السيولة التي نُشغّلها أصلًا**
(`R1 ∪ T-C`) — **وكم تكلّف رسائلَ لو صارت قاعدةَ إشعار؟**

🔑 **والشقُّ الأوسطُ هو الحاكمُ الحقيقيّ.** درسُ `T-PMGATE` و`T-WAIT-23W` مقفول:
**أيُّ مرشِّحٍ يفصل بعضَ الشيء آليًّا** لأنه يقسم مجتمعًا متحرّكًا — والسؤالُ
النافعُ الوحيد: **هل يضيف على ما نُسلّمه اليوم؟**

---

## ② المجتمع — **أماميٌّ حصريًّا** ومُغلَق

- **كلُّ رمزٍ نشطٍ في `weekly_watchlist.json`** (‏`stocks` بحالة `active`، ويشمل
  المحمولين `cont_status`) **‏+ كلُّ رمزٍ في `pullback`** — في **كلّ يومِ تداولٍ**
  من أوّل تشغيلةٍ بعد الدمج.
- **الصفُّ = (يوم، رمز)**، ويُسجَّل **حتى لو لم يتجاوز العتبة** (فالمُكمِّل شاهد).
- 🔴 **ولا باكتيست:** السؤالُ عن **مجتمعِنا نحن وعن التسليم**، وكلاهما لا يُعاد
  بناؤه رجعيًّا (القائمةُ تتغيّر أسبوعيًّا وسجلُّها الكاملُ ليس لقطاتٍ يوميّة).
- **`V-W1`:** أيُّ يومٍ تعذّر فيه تحميلُ القائمة **يُسجَّل غيابًا صريحًا** ولا
  يُطوى · **`V-W2`:** تغطيةُ الجلبِ الدقيقيّ ‏≥95% لصفوف اليوم وإلّا **يُوسَم
  اليومُ ناقصًا ويُقصى من الحكم ويُعَدّ**.

---

## ③ التعريفاتُ — بالأسماء لا بالشبيه

| العنصر | التعريف | المصدر بالاسم |
|---|---|---|
| `pm_usd` | مجموعُ `close × volume` لشموع الدقيقة **قبل 09:30 نيويورك** · السلّةُ العليا `>1M` | `link100_probe.pm_feats` |
| شموعُ اليوم | دقائقُ اليوم كاملةً `adjusted=false` | `tier_fwd_report.fetch_day` |
| ساعةُ نيويورك | لفصل ما قبل الجرس عمّا بعده | `opcurve_probe.ny_hour` |
| **`mv`** | **(أعلى الجلسة النظاميّة ÷ أوّلِ إغلاقِ دقيقةٍ عند 09:30 أو بعدها) − 1** | يُشتقّ من الشموع نفسِها |
| **`fired`** | هل أطلقت طبقةُ السيولة (`R1 ∪ T-C`) لهذا الرمز في هذا اليوم | `op_entry_state.json` بتاريخه |
| الإصابة | `mv ≥ +20%` | عتبةُ `T-C-TRIGGER` المقيسة، لا رقمٌ جديد |

🔑 **والحصادُ بعد الإغلاق في جوبٍ واحد** — الشمعةُ اليوميّةُ الدقيقيّةُ تحمل
البريماركتَ والجلسةَ معًا ⇒ **القياسُ حتميٌّ ويُعاد بالأمر نفسِه**، ولا خطرَ
توقيتٍ حيٍّ ولا سباقَ حالة.

---

## ④ الأذرعُ والشواهد — قائمةٌ **مُغلَقة**

| # | التعريف | الدور |
|---|---|---|
| **`F-PM`** 🥇 | صفوفُ اليوم التي `pm_usd > 1M` | **حاكمة** |
| **`F-NONE`** 🥇 | **مُكمِّلُها في اليوم نفسِه** (‏`pm_usd ≤ 1M` أو فارغ) | **الشاهدُ الحاكم** |
| **`C-LIQ`** 🥇 | الفرقُ نفسُه **داخلَ مُكمِّل `fired`** (أيّامٌ لم تُطلق فيها طبقتُنا) | **الشاهدُ الحاكمُ الثاني** |
| `F-IN` | الفرقُ داخلَ `fired` نفسِه | وصفيّة |
| `F-COST` | عددُ صفوف `F-PM` في اليوم = **الرسائلُ الإضافيّةُ لو صارت قاعدةَ إشعار** | وصفيّةٌ تُطبَع |

⛔ **ولا ذراعَ سادسة ولا شاهدَ رابع بعد أيّ رقم.**

---

## ⑤ المعاييرُ والفروع — مكتوبةٌ قبل أوّل صفّ

| المعيار | النصّ |
|---|---|
| **`PF1`** | نسبةُ إصابة `F-PM` ناقص `F-NONE` ‏**≥15 نقطة** · وفاصلا Wilson ‏95% **منفصلان** |
| **`PF2`** 🥇 | داخلَ **مُكمِّل `fired`**: الفرقُ نفسُه ‏**≥10 نقاط** وفاصلان منفصلان |
| **`PF3`** | **الكلفة:** وسيطُ `F-COST` ‏**≤3 صفوفٍ في اليوم** |
| **الأرضيّة** | `n ≥ 50` في `F-PM` **و**`n ≥ 50` في `F-PM ∩ ¬fired` — وسقوطُها **يمنع الفرعَ 1** |

**الفروع** (ثلاثةٌ كلٌّ في سطره):
1. **تُوصى** — `PF1` **و**`PF2` **و**`PF3` والأرضيّةُ مستوفاة ⇒ **يُقترَح سطرُ
   تسليمٍ على المالك** (§⑧) ولا يُشحَن بهذا العقد.
2. **فشلت** — الأرضيّةُ قائمةٌ وسقط معيارٌ ⇒ لا تُوصى، ويُنشَر الرقم.
3. **لا حكم** — أرضيّةٌ ساقطةٌ عند **تاريخ الحسم**.

**تاريخُ الحسم:** أوّلُ الأمرين — بلوغُ الأرضيّة، **أو 2026-12-31**. وعندها
**يُنشَر معدّلُ التراكم المقيس** حتى لو سقطت الأرضيّة، فهو بذاته معلومة.

**رموزُ الخروج:** ‏0 حكمٌ صدر · 2 لا مفتاح · 3 تغطيةٌ دون الحدّ · 4 صفرُ صفّ ·
**‏6 حارسٌ ساقط** · **‏9 «لا حكم»**.

---

## ⑥ التنبّؤاتُ العمياء — تُنشَر مؤكَّدةً أو مكذَّبة

| # | التنبّؤ |
|---|---|
| `W1` | **الأرضيّةُ تُبلَغ قبل 2026-12-31** — ‏47 رمزًا/يوم × ‏≈70 يومًا ‏≈3,300 صفًّا، وبمعدّل 2-5% ⇒ ‏65-165 عبورًا |
| `W2` | **معدّلُ العبور على مرشّحينا أدنى من السوق** (‏<2%) لأنهم مفلترون عند قيعانٍ خامدة |
| `W3` | **`PF1` يعبر** (‏الفرقُ ‏≥15 نقطة) |
| `W4` | **`PF2` هو الساقط** — أي أن أكثرَ ما تلتقطه `F-PM` تلتقطه طبقةُ السيولة أصلًا |
| `W5` | **`PF3` يعبر** (‏وسيطُ الكلفة ‏≤3) لأن القائمة صغيرة |
| `W6` | **`F-IN` أقوى من `C-LIQ`** — الإشارتان تتضافران حيث تجتمعان |

⚠️ **والاحتمالُ الأسبق:** `T-PMUSD` قاست **الاختيارَ رجعيًّا**، وهذي تقيس
**التسليمَ أماميًّا على مجتمعٍ آخر** — والتاريخُ عندنا أن التسليمَ يسقط حيث
ينجح الاختيار.

---

## ⑦ حدودُ الصدق

1. **مجتمعُنا مفلترٌ سلفًا** فمعدّلُ الأساس فيه **ليس** معدّلَ السوق ⇒ لا تُنقَل
   نسبةُ `T-PMUSD` إليه ولا العكس.
2. **`mv` ليست عائدًا:** بلا وقفٍ ولا تكلفةٍ ولا حجمِ مركزٍ ولا لحظةِ دخول ⇒
   **لا `R` ولا توقّع.**
3. **التراكمُ بطيء** (‏≈47 صفًّا/يوم) ⇒ الفرعُ 3 وارد، ويُعلَن الآن لا بعد الحكم.
4. **`fired` تُقرأ من حالةٍ مدفوعة** وقد تغيب لعطلٍ في العامل ⇒ اليومُ الغائب
   **يُوسَم ويُقصى من `C-LIQ` ويُعَدّ**.
5. **القائمةُ تتجدّد أسبوعيًّا** فتركيبُ المجتمع يتغيّر تحت القياس — يُطبَع عددُ
   الرموز الفريدة وعددُ الأيّام لكلّ رمز.
6. **تعدّدُ المقارنات** في الوصفيّتين ⇒ لا تُرقّى واحدةٌ منهما بلا تسجيلٍ جديد.

---

## ⑧ سقفُ النجاح — والمحظورات

**سقفُ النجاح:** اقتراحُ **شارةٍ على الكرت القائم** (مثل شارة «🥇 توليفة») تقول
إن سيولةَ بريماركت السهم تجاوزت المليون هذا الصباح — **بقرار المالك وحدَه**.
**المحظورات:** ① **لا بوّابةَ فرزٍ ولا مفتاحَ ترتيب** ② **ولا قناةَ رسائلَ
رابعة** (القاعدةُ المُلزِمة: لا تُضاف قناةٌ بلا طلب المالك) ③ لا تنفيذَ أوامر
④ لا مسَّ جذرٍ من الاثني عشر ⑤ ولا عتبةَ إنتاجٍ تتحرّك.

---

## ⑨ الأداة — مواصفةٌ ملزِمة (تُبنى **بعد** دمج هذا العقد)

- `pmfwd_harvest.py` + `.github/workflows/pmfwd.yml` — **كرونٌ يوميٌّ بعد إغلاق
  الافتر** (‏ولا يزاحم الأدواتِ القائمة) ‏+ `workflow_dispatch` · **وبلا سرِّ
  تلغرام فالإرسالُ مستحيلٌ بنيويًّا** · `contents: write` **للسجلّ وحدَه**.
- **السجلُّ `pmfwd_log.jsonl`** — صفٌّ لكلّ (يوم، رمز) يحمل: `pm_usd` والسلّة ·
  `mv` · `fired` · `open930` · وسمَ نقصِ التغطية. **يُضاف ولا يُعاد كتابتُه**،
  و**الدِدوب بمفتاح (يوم، رمز)** فإعادةُ التشغيل لا تُضاعف صفًّا.
- **إعادةُ استعمالٍ بالاسم:** `link100_probe.pm_feats` · `tier_fwd_report.fetch_day`
  · `opcurve_probe.ny_hour` · `optrade_arms.selfcheck_readonly`/`no_config_assign`.
- **`pmfwd_report.py`** (أو وضعٌ في الأداة نفسِها) يقرأ السجلَّ ويُصدر الحكم
  بمعايير §⑤ **حرفيًّا** — ولا يُصدره قبل تاريخ الحسم أو بلوغ الأرضيّة.
- **وضعُ جدوى `PMFWD_DRY=1`:** يطبع **ثلاثةَ أعدادٍ فقط** (صفوفُ اليوم · العابرون ·
  تغطيةُ الجلب) **ويعود قبل أيّ كتابةٍ للسجلّ وقبل أيّ نسبة** — مقفولٌ بعدّاد.
- **والحتميّة:** المدخلاتُ نفسُها ⇒ مُخرَجٌ بت-بت · والنتيجةُ في `pmfwd_result.md`
  بجانب هذا العقد لا فوقه.

---

## ⑩ الأقفال (تُكتب في السويّة مع هذا العقد، وكلٌّ يسقط بطفرةٍ مسمّاة)

- **`PWK0`** — بنيويّ: فروعُ §⑤ ثلاثةٌ كلٌّ في رقمه · «لا حكم» في الثالث وحدَه ·
  ورموزُ الخروج 6 و9 منصوصة · والأرضيّةُ **‏50 في الشقَّين** وتمنع الفرعَ 1 ·
  وتاريخُ الحسم منصوص.
- **`PWK1`** — §④: ثلاثُ حاكماتٍ (`F-PM` · `F-NONE` · `C-LIQ`) ووصفيّتان ·
  و`C-LIQ` منصوصٌ أنه **شاهدٌ حاكم** · والقائمةُ مُغلَقةٌ نصًّا.
- **`PWK2`** — §⓪ يُعلن التلوّثَ بأرقام `T-PMUSD` ويقيّد ثلاثةً (‏لا معايرةَ عتبة ·
  لا ذراعَ جديدة · لا حصرَ مجتمع) · و§⑧ يمنع البوّابةَ ومفتاحَ الترتيب والقناةَ الرابعة.
- **`PWK3`** — §③ يُعرّف `mv` و`fired` بالنصّ · و§② يُعلن أنه **أماميٌّ بلا باكتيست**
  · و§⑨ يُسمّي السجلَّ والدِدوبَ ووضعَ الجدوى · و§⑥ تنبّؤاتُه ستّةٌ كلٌّ في سطره.
