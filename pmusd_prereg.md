# 🌅💧 `T-PMUSD` — تسجيلٌ مسبقٌ **حاكم**: هل سيولةُ البريماركت رابطٌ **قبل** الانفجار، أم أنها الانفجارُ نفسُه؟

> **مدفوعٌ ومدموجٌ قبل أيّ سطرِ أداةٍ وقبل أيّ رقمٍ من موضوعه.** أمرُ المالك
> **«سجّل البريماركت»** (‏2026-09-17) بعد حكم `T-LINK100` الذي أخرج `pm_usd > 1M`
> **أعلى رقمٍ في التشغيلة كلِّها ولم يعبر** لغياب الشاهد الذاتيّ.
> **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · لا عتبةَ تتحرّك · ولا وكلاء.**

---

## ⓪ إعلانُ التلوّث — ما رأيتُه، وما يُقيّده

**رأيتُ من التشغيلة `35163391888` (حكمُ `T-LINK100`):**

| ما رأيتُه | القيمة |
|---|---|
| `pm_usd > 1M` — حصّةُ الأحداث | ‏65.8% · 69.1% · 70.3% |
| ‏ونسبتُها إلى الشاهد المقطعيّ `CX` | **‏34.72× · 21.69× · 14.03×** |
| الشاهدُ الذاتيّ `CC` لها | **«‏—» غيرُ محسوب** (الطبقةُ الدقيقةُ لا تُجلَب له) |
| `pm_gap 10-30%` — `CX` | ‏4.54× · 8.61× · 9.50× |
| الأحداثُ المقيسة | ‏481 · 655 · 773 |

⚠️ **فاتّجاهُ `CX` معروفٌ لي، و`CC` مجهولٌ تمامًا** — وهو المحورُ الحاكم هنا.
**ولا يُقرأ ما سبق حكمًا:** `LK1` يشترط **الشاهدين معًا**، وسقوطُ `pm_usd` كان
**قصورَ قياسٍ مُعلَنًا لا ضعفَ إشارة** (‏`link100_result.md §②`).

**وثلاثةُ قيودٍ تنشأ عن التلوّث وتُكتب الآن:**
1. **لا تُخفَّض عتبةُ `LK1`** (‏≥2× مقابل الشاهدين · فواصلُ Wilson منفصلة · `n ≥ 50` ·
   بونفيروني · **وفي كلّ سنة**) — هي عتبةُ `T-LINK100` حرفيًّا.
2. **لا تُضاف ميزةٌ ولا ذراعٌ بعد أيّ رقم** — القائمةُ مُغلَقةٌ في §④.
3. **ولا يُحصَر المجتمعُ ولا يُوسَّع** — هو عينُ مجتمع `T-LINK100` بحارس `V-U1`.

---

## ① السؤالُ القابل للتكذيب

`pm_usd` تُحسَب من **شموع الدقيقة قبل 09:30 من يوم الحدث `d` نفسِه**، والحدثُ
مُعرَّفٌ بـ`high(d)/close(d−1) ≥ 2` على الشمعة اليوميّة **وهي تشمل الجلسةَ الممتدّة**.
⇒ **إن وقع الانفجارُ في البريماركت فـ`pm_usd` تقيس الانفجارَ نفسَه لا ما يسبقه** —
وهذا وحدَه كافٍ لتفسير `CX` ‏34.7×.

**فالسؤال:**
هل سيولةُ البريماركت تفصل المنفجرَ عن شاهدَيه **قبل** انفجاره —
- (أ) على الأحداث التي **لم** يقع انفجارُها في البريماركت (قراءةُ اليوم نفسِه خاليةً من التلوّث)، **أو**
- (ب) بقراءةٍ **سابقةٍ بالكامل** (بريماركتُ اليوم السابق `d−1`)،
- **(ج) وهل تضيف شيئًا فوق ما نعرفه سلفًا** (`vol_x ≥ 3` و`ret5 > +20%` العابرتان)؟

⚖️ **و(ج) شرطٌ حاكمٌ لا تزيين** — بنصّ الدرس المدوَّن من `T-PMGATE` و`T-WAIT-23W`:
**أيُّ مقياسِ «حرارةٍ» جديد يفصل بعضَ الشيء آليًّا**، فالسؤالُ النافعُ الوحيد:
**هل يضيف على المعروف؟**

---

## ② المجتمع — عينُ مجتمع `T-LINK100` بحارسه

- السنواتُ **‏2023 · 2024 · 2025** · الحاكمُ `high(d)/close(d−1) ≥ 2.0` · إغلاقُ الأمس
  في `[PRICE_LO, 20]$` · طيُّ 20 جلسة · حارسُ تقسيمٍ ‏±3 أيّام — **كلُّها بالاسم من
  `link100_probe`، لا يُعاد بناءُ أيٍّ منها**.
- **`V-U1`:** عددُ الأحداث المقيسة لكلّ سنةٍ يطابق المنشورَ في `link100_result.md §①`
  (**‏481 · 655 · 773**) **وإلّا خروج 5 قبل أيّ رقم**. وأيُّ سنةٍ غيرِ الثلاث ⇒ وصفيّةٌ
  بلا حكم.
- **`V-U2`:** تغطيةُ أيّام التداول ‏≥95% لكلّ سنة (‏`MIN_DAYS_COVER` بالاسم) وإلّا خروج 3.
- **`V-U3`:** مطابقةُ الشاهد المقطعيّ ‏≥90% (‏`MIN_MATCH` بالاسم) وإلّا خروج 3.

---

## ③ التعريفاتُ — بالأسماء لا بالشبيه

| العنصر | التعريف | المصدر بالاسم |
|---|---|---|
| `pm_usd` | مجموعُ `close × volume` لشموع الدقيقة **قبل 09:30 نيويورك** · سلالُ `<100k` / `100k-1M` / `>1M` | `link100_probe.pm_feats` |
| `pm_gap` | `آخرُ إغلاقِ دقيقةٍ قبل 09:30 ÷ إغلاق الأمس − 1` · سلالُ `<10%` / `10-30%` / `≥30%` | `link100_probe.pm_feats` |
| **`pm_high(d)`** | **أعلى `high` بين شموع الدقيقة قبل 09:30 من يوم `d`** | يُشتقّ من الشموع نفسِها — **بلا نداءٍ إضافيّ** |
| **حدثٌ بريماركتيّ `E-PM`** | `pm_high(d) / close(d−1) ≥ 2.0` ⇒ **الانفجارُ وقع قبل الجرس** | **التعريفُ الحاكم للعزل** |
| **حدثٌ نظاميّ `E-REG`** | مُكمِّلُ `E-PM` بين الأحداث المقيسة | — |
| الشاهدُ المقطعيّ `CX` | أوّلُ رمزٍ أبجديًّا في خليّة (خانةُ السعر × عُشرِ الدولار) لم يتحرّك ‏≥30% في `d` | `link100_probe.pick_cx` |
| الشاهدُ الذاتيّ `CC` | **الرمزُ نفسُه** عند `d−21` بشرط خلوّ ما بينهما من حدث | `link100_probe.enrich_rows` |
| الحكم | `≥2×` مقابل الشاهدين · فواصلُ Wilson منفصلة · `n ≥ 50` · `z` بونفيروني | `link100_probe.enrich`/`z_bonf` |

🔑 **والجديدُ الوحيدُ بنيويًّا: الطبقةُ الدقيقةُ تُجلَب للشاهدَين وليومِ `d−1`** —
وهو ما لم يُفعَل في `T-LINK100` فسقطت `pm_*` على «‏—».

---

## ④ الأذرعُ والشواهد — قائمةٌ **مُغلَقة**

| # | القراءة | النطاق | الدور |
|---|---|---|---|
| **`U-NOW`** 🥇 | `pm_usd(d)` | **`E-REG` وحدَها** | **حاكمةٌ (أ)** — خاليةٌ من التلوّث بالبناء |
| **`U-PREV`** 🥇 | `pm_usd(d−1)` | كلُّ الأحداث | **حاكمةٌ (ب)** — سابقةٌ بالكامل |
| `U-RAW` | `pm_usd(d)` | كلُّ الأحداث | **وصفيّةٌ** — «الإصلاحُ الساذج»، تُطبَع لتُرى المغالطة |
| `G-PREV` | `pm_gap(d−1)` | كلُّ الأحداث | وصفيّة |
| `G-NOW` | `pm_gap(d)` | `E-REG` وحدَها | وصفيّة |

**والشواهدُ الثلاثة:**
- **`CX`** مقطعيٌّ — بطبقةٍ دقيقةٍ لليوم `d` **ولليوم `d−1`**.
- **`CC`** ذاتيٌّ عند `d−21` — **بطبقةٍ دقيقةٍ لِيومه** (‏هذا هو سدُّ الثغرة).
- **`C-KNOWN`** 🥇 **حاكم** — القراءةُ **مشروطةً**: تُعاد الحاكمةُ العابرةُ داخلَ
  **مُكمِّل** `vol_x ≥ 3 ∪ ret5 > +20%` (أي الأحداثُ التي **لا** تلتقطها العابرتان)
  وداخلَ ذلك الاتّحاد نفسِه — **فيُعرَف: هل تضيف أم تُعيد قياسَ المعروف؟**

⛔ **ولا ذراعَ سادسة ولا شاهدَ رابع بعد أيّ رقم.**

---

## ⑤ الخطرُ الأوّل: التلوّثُ الزمنيّ — وكيف يُعزَل

1. **يُحسَب `pm_high(d)` لكلّ حدثٍ** من الشموع المجلوبة أصلًا (‏صفرُ نداءٍ إضافيّ).
2. **تُطبَع حصّةُ `E-PM` لكلّ سنة** — وهي رقمٌ بذاته يجيب «كم من منفجري ‏+100%
   انفجروا قبل الجرس؟».
3. **الحاكمةُ (أ) تُقاس على `E-REG` وحدَها**، فما تقيسه **يسبق** الانفجارَ بالبناء.
4. **`U-RAW` تبقى وتُطبَع** — فلو عبرت وحدَها وسقطت `U-NOW` كان ذلك **برهانَ التلوّث
   لا دليلَ إشارة**، ويُقال صراحةً في النتيجة.

🔴 **ولا يُحذَف `E-PM` من المجتمع** — يُقصى من الحاكمة (أ) **ويُعَدّ ويُطبَع**.

---

## ⑥ المعاييرُ والفروع — مكتوبةٌ قبل أيّ رقم

| المعيار | النصّ |
|---|---|
| **`PU1`** | `U-NOW` تعبر `LK1` (‏≥2× مقابل `CX` **و**`CC` · فواصلُ Wilson منفصلة · `n ≥ 50` · بونفيروني) **في السنوات الثلاث** |
| **`PU2`** | `U-PREV` تعبر `LK1` بالشروط نفسِها **في السنوات الثلاث** |
| **`PU3`** 🥇 | **الإضافةُ فوق المعروف:** الذراعُ العابرةُ تُبقي نسبةً **‏≥1.5×** مقابل شاهدها **داخلَ مُكمِّل** `vol_x ≥ 3 ∪ ret5 > +20%` **في السنوات الثلاث** |
| **الأرضيّة** | `n ≥ 50` في السلّة الحاكمة **لكلّ سنة** · و`n ≥ 50` في **كلّ** شقَّي `C-KNOWN` — وسقوطُها **يمنع الفرعَ 1** |

**الفروع** (ثلاثةٌ كلٌّ في سطره):
1. **تُوصى** — (`PU1` أو `PU2`) **و**`PU3` معًا والأرضيّةُ مستوفاة ⇒ **ثمّ** تسجيلٌ أماميٌّ جديد (§⑨).
2. **فشلت** — الأرضيّةُ قائمةٌ وسقط معيارٌ ⇒ لا تُوصى، ويُنشَر الرقم.
3. **لا حكم** — أرضيّةٌ ساقطة.

**رموزُ الخروج:** ‏0 حكمٌ صدر · 2 لا مفتاح · 3 تغطيةٌ/مطابقةٌ دون الحدّ ·
4 صفرُ حدث · **‏5 المجتمعُ يخالف `V-U1`** · **‏6 حارسٌ ساقط** · **‏9 «لا حكم»**.

---

## ⑦ التنبّؤاتُ العمياء — تُنشَر مؤكَّدةً أو مكذَّبة

| # | التنبّؤ |
|---|---|
| `Q1` | **`U-RAW` تعبر `LK1` بسهولة بعد حساب `CC`** — **وعبورُها لا يعني شيئًا**: الشاهدُ الذاتيُّ يومٌ بلا انفجار فبريماركتُه هادئٌ بالضرورة ⇒ النسبةُ تتضخّم بالتعريف |
| `Q2` | **حصّةُ `E-PM` ‏≥40%** من الأحداث المقيسة في كلّ سنة |
| `Q3` | **`U-NOW` لا تعبر في السنوات الثلاث** (‏أتوقّع سقوطَها على `CC` أو على سنةٍ واحدة) |
| `Q4` | **`U-PREV` أضعفُ من `U-NOW`** بفارقٍ واضح على `CX` |
| `Q5` | **`PU3` ساقط**: داخلَ مُكمِّل العابرتين تنكمش النسبةُ دون ‏1.5× — أي أن البريماركت **يُعيد قياسَ الحرارة المعروفة** |
| `Q6` | `pm_gap` (‏`G-NOW`/`G-PREV`) **أضعفُ** من `pm_usd` مقابل `CC` |

⚠️ **والاحتمالُ الأسبقُ الفشل:** ثلاثةَ عشرَ ميزةً قِيست في `T-LINK100` فعبرت **اثنتان**،
وكلتاهما **ليست** بريماركتيّة.

---

## ⑧ حدودُ الصدق

1. **إثراءٌ قبل الحدث لا تنبّؤٌ بالمقدار** — تقول «المنفجرون كانوا كذا» لا «مَن كان كذا ينفجر».
2. **ولا حافّةَ تداولٍ:** بلا وقفٍ ولا تكلفةٍ ولا حجمِ مركزٍ ولا لحظةِ دخول.
3. **`CX` لا يضبط القطاع** (يضبط خانةَ السعر وعُشرَ الدولار فقط).
4. **`CC` مرجعُه `d−21`** ⇒ يضبط الرمزَ ولا يضبط نظامَ السوق في ذلك اليوم.
5. **الطبقةُ الدقيقةُ مسقوفةٌ** بـ`PMUSD_CAP` ويُطبَع المستعمَل — وأيُّ قصٍّ يُعلَن.
6. **`pm_high(d)` من شموع الدقيقة** بينما الحدثُ من الشمعة اليوميّة المجمَّعة ⇒ قد
   يختلفان قليلًا (مصدران)، **فالعزلُ متحفّظٌ في اتّجاه إبقاء المشكوك داخل `E-PM`**.
7. **تعدّدُ المقارنات** في الوصفيّات ⇒ لا تُرقّى واحدةٌ منها بلا تسجيلٍ جديد.

---

## ⑨ سقفُ النجاح — والمحظورات

**سقفُ النجاح:** **تسجيلٌ أماميٌّ جديدٌ** يُقترَح على المالك (رصدُ `pm_usd` حيًّا على
مرشّحينا وقياسُ تسليمه) — **بقراره وحدَه**، ولا يُشحَن شيءٌ بهذا العقد.
**المحظورات:** ① **لا تُضاف بوّابةً ولا مفتاحَ ترتيبٍ للفارز** (ذلك ينقله إلى مضاربة
زخمٍ — نظامٌ آخر بنصّ قاعدتنا المُلزِمة) ② لا تنفيذَ أوامر ③ لا مسَّ جذرٍ من الاثني عشر
④ لا عتبةَ إنتاجٍ تتحرّك ⑤ **ولا تُعدَّل عتبةُ `LK1`**.

---

## ⑩ الأداة — مواصفةٌ ملزِمة (تُبنى **بعد** دمج هذا العقد)

- `pmusd_probe.py` + `.github/workflows/pmusd.yml` (يدويٌّ بلا كرون · **وبلا سرِّ
  تلغرام** فالإرسالُ مستحيلٌ بنيويًّا · `contents: read`).
- **إعادةُ استعمالٍ بالاسم لا نسخ:** `link100_probe.year_days`/`grouped_day`/
  `day_events`/`fold_events`/`pick_cx`/`ticker_daily`/`splits_of`/`daily_feats`/
  `pm_feats`/`enrich`/`z_bonf`/`wilson`/`px_bucket`/`decile_of`/`dollar_deciles`/
  `fetch_day`/`selfcheck_readonly`/`no_config_assign` — **وأيُّ منطقِ حسمٍ مكرَّرٍ
  عيبٌ** (يُقفَل بالـAST: صفرُ دالّةٍ هنا تحمل اسمَ دالّةِ حسمٍ مستورَدة).
- **`V-U1`** مطابقةُ أعداد `link100_result.md` (خروج 5) · **`V-U2`/`V-U3`** التغطيةُ
  والمطابقة (خروج 3) · **`V-U4`** الطبقةُ الدقيقةُ تُجلَب فعلًا لـ`CC` (نسبةُ
  الصفوف ذاتِ `pm_usd` غيرِ `None` **‏≥95%** في `CC` و`CX` والأحداث، وإلّا خروج 6) ·
  **`V-U5`** `E-PM ⊎ E-REG` يساوي المجتمعَ بت-بت (خروج 6).
- **وضعُ جدوى `PMUSD_DRY=1`:** يطبع **أربعةَ أعدادٍ فقط** (الأحداثُ لكلّ سنة ·
  حصّةُ `E-PM` · عددُ صفوف `CC` ذاتِ طبقةٍ دقيقة · المستعمَلُ من `PMUSD_CAP`)
  **ويعود قبل أيّ نسبةٍ أو فاصلٍ أو حكم** — مقفولٌ بعدّاد نداءات.
- **الحتميّة:** المدخلاتُ نفسُها ⇒ مُخرَجٌ بت-بت · والمُخرَج `pmusd_result.md`
  بجانب هذا العقد لا فوقه.

---

## ⑪ الأقفال (تُكتب في السويّة مع هذا العقد، وكلٌّ يسقط بطفرةٍ مسمّاة)

- **`PUK0`** — بنيويّ: فروعُ §⑥ ثلاثةٌ كلٌّ في رقمه · «لا حكم» في الثالث وحدَه ·
  ورموزُ الخروج 5 و6 و9 منصوصة · والأرضيّةُ **‏50 لكلّ سنةٍ ولكلّ شقٍّ** وتمنع الفرعَ 1.
- **`PUK1`** — §④: حاكمتان (`U-NOW` · `U-PREV`) وثلاثُ وصفيّات · وشاهدٌ حاكمٌ ثالث
  (`C-KNOWN`) منصوصٌ أنه **حاكمٌ لا وصفيّ** · والقائمةُ مُغلَقةٌ نصًّا.
- **`PUK2`** — §⓪ يُعلن التلوّثَ بأرقامه (‏34.72 · 21.69 · 14.03 · و«`CC` غيرُ محسوب»)
  · و§⑤ يُعرّف `E-PM` ويشترط **عدمَ حذفها** من المجتمع · و§⑨ يمنع البوّابةَ ومفتاحَ الترتيب.
- **`PUK3`** — §③ يُعرّف `pm_high(d)` و`E-PM`/`E-REG` بالنصّ · و§⑩ يُسمّي `V-U1`-`V-U5`
  ووضعَ الجدوى · و§⑦ تنبّؤاتُه ستّةٌ كلٌّ في سطره.
