# 🔔 T-WAKE — «سجّل الإيقاظ» · تسجيلٌ مسبقٌ يُدفَع **قبل أيّ سطرِ كودِ أداة**

> **أمرُ المالك (‏2026-08-28): «سجّل الإيقاظ».** جاء جوابًا لخيارٍ عرضتُه بنصّه:
> «تسجيلٌ مسبقٌ جديد على المحور الذي أثبته القياس: **ما يوقظ الكرت** (نافذةُ
> إعادة التنبيه) لا ترتيبُه».
> **بحث/قياسٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION`.**

---

## ⓪ لماذا الآن — **السندُ مقيسٌ ومنشورٌ لا حدس**

`T-HOLD-KEY-2` (‏`hold_key2_result.md`) قاست قناةَ الضغط **عند كثافتها الحيّة**
لأوّل مرّة فأخرجت رقمين يقلبان ترتيبَ الأولويات:

| | 2023 | 2024 | 2025 |
|---|---|---|---|
| وسيطُ الجاهزين لكلّ جلسة (`HV4`) | ‏32.0 | ‏38.0 | ‏37.0 |
| **بندُ سقف الكروت** (`ALERT_CAP`=8) | **‏18.8%** | **‏29.8%** | **‏24.0%** |
| الكروتُ المُسلَّمة فعلًا (`B0`) | ‏1323 | ‏1508 | ‏1451 |
| ⇒ كروتٌ لكلّ جلسة | ‏5.29 | ‏5.98 | ‏5.80 |

⇒ **السقفُ ليس القيد** (يبنّد في خُمسٍ إلى ثلثِ الليالي فقط)، **ومع ذلك يُسلَّم
‏≈6 كروتٍ من ‏≈35 جاهزًا.** ⇒ **الخانقُ الحقيقيُّ بينهما**، والمرشَّحُ الوحيد
في كود الإنتاج هو **بوّابةُ الإيقاظ**.

🔍 **وقرأتُ الميكانيكا من الإنتاج نفسِه قبل كتابة هذا العقد** (درسُ `T-HOLD-KEY`:
وصفتُ المصدرَ بما **يُنتجه** لا بما **يقرؤه** الإنتاج، فسقطت التجربةُ على بوّابةِ
كثافة). **والمقروءُ من `press_radar.py` حرفيًّا شقّان لا شقٌّ واحد:**

1. **الدِدوب `should_alert`** (‏`:452-458` · `REALERT_DAYS`=5): رمزٌ نُبِّه عليه
   خلال خمس جلسات **يُسقَط من `rows` كليًّا** — قبل الترتيب وقبل السقف.
2. **حصرُ الكرت بالمستيقظين** (‏`:604,618,622` · `base = fire if fire else xs`):
   إن وُجد **مستيقظٌ واحد** («صحوةُ آخر جلستين» — قفزةُ حجم أو شمعةٌ انعكاسية أو
   حركةُ افتر) صار **الكروتُ هم المستيقظون حصرًا بلا إكمالٍ من الهادئين**.

🔑 **و`awake` ليس مفتاحَ ترتيب** — `alert_rank` مفاتيحُه الستّة لا تحويه (فُحص
بالمصدر) ⇒ أثرُه **حصرٌ وتقديمٌ في `build_alert` وحدَه** ⇒ **قابلٌ للعزل نظيفًا.**

⚖️ **ومحورٌ لم يُنقَّب فيه قطّ — ولا دائريّةَ فيه:** ثوابتُه الثلاثة
(`REALERT_DAYS` · `ALERT_CAP` · `MEMORY_DAYS`) **من هندستنا** ولا رقمَ لفيصل
فيها، **ولم يُقَس أيٌّ منها ولا مرّة**. **ولا يُمَسّ أيُّ رقمٍ لفيصل** في هذي
التجربة (‏`READY_HOLD`=3 مسنودٌ بنصّه ومقيسٌ `+0.174R` — **ثابتٌ في الأذرع
الأربع**).

---

## ① المتغيّرُ الوحيد — **بوّابةُ الإيقاظ بشقَّيها، ولا ثالث**

| | الإنتاج (`W0`) | ما يتغيّر في الأذرع |
|---|---|---|
| نافذةُ إعادة التنبيه | `REALERT_DAYS`=5 | ‏1 (`W1`) · 10 (`W2`) |
| حصرُ الكرت بالمستيقظين | نعم (بلا إكمال) | يُلغى الحصرُ ويبقى التقديم (`W3`) |

⛔ **وما لا يُمَسّ في أيّ ذراع:** `ALERT_CAP`=8 · `ALERT_W`=40 · `READY_HOLD`=3 ·
`MEMORY_DAYS`=45 · `POOL_CAP`=250 · `alert_rank` بمفاتيحه الستّة · بناءُ البِركة
· `press_read` · خطّةُ الدخول والوقف · و`kasih_scan.resolve`.

---

## ② المجتمع — **`D_live2` نفسُه بت-بت** (لا مجتمعَ جديد)

البِركةُ المُعادُ بناؤها بـ`select_top` بسعة `WATCHLIST_SIZE` — **وهي التي عبرت
بوّابةَ الكثافة في السنوات الثلاث** (‏32.0 · 38.0 · 37.0 داخل ‏[8، 40]).
**تُستورَد بالاسم ولا يُعاد بناءُ صفٍّ واحد** — والأسماءُ **مقروءةٌ من مصدرها
لا مُخترَعة**: من `hold_key2_arms` ⟵ `grid_dates` · `q5_grid_cal` · `top_by_date`
· `enrich2` · `hw1_gate` · ومن `hold_key_arms` ⟵ `live_pool` · `mover_days` ·
`order_b0` · `hv8_agree` · ومن `press_rank_arms` ⟵ `ready_rows` · `r_of` ·
`boot_ci` · `dedupe_violations` · `pv0_gate`.

🐞 **وأوّلُ صياغةٍ لهذا السطر كتبت `grid_rows` و`live_rows` — واسمان لا وجود
لهما**؛ أمسكهما جردُ `ast` **قبل الدفع**. وهو «الاسمُ المتخيَّل» بعينه في ثوبِ
اسمِ دالّةٍ لا مفتاحِ قاموس — يُدوَّن لأنه الصنفُ الذي أسقط `T-HOLD-KEY`.

⇒ **بوّابةُ التكامل `WV0` تُثبت ذلك بت-بت** (‏§⑤).

---

## ③ الأذرع — **أربعٌ ولا خامسة**

- **`W0` المرجع** — الإنتاج حرفيًّا: دِدوبُ 5 جلسات · حصرٌ بالمستيقظين · سقفُ 8 ·
  `alert_rank`. **‏= `B0` من `T-HOLD-KEY-2` بالضبط.**
- 🥇 **`W1` الحاكمة** — `REALERT_DAYS`=1 (لا كتمَ إلّا داخل الجلسة نفسِها) ·
  وكلُّ ما عداه كما هو.
- **`W2`** — `REALERT_DAYS`=10 (الاتجاهُ المعاكس: كتمٌ أشدّ).
- **`W3`** — **إلغاءُ الحصر وحدَه**: المستيقظُ يبقى **متقدّمًا** في ترتيب الكروت
  **ويُكمَل من الهادئين حتى السقف** (‏`base = xs` بترتيبِ «المستيقظُ أوّلًا»).

⛔ **ولا تُضاف ذراعٌ بعد الأرقام ولا يُبدَّل تعريفُ ذراع.**

---

## ④ المقياسُ والمعايير — **مثبَّتةٌ قبل أيّ رقم**

🔴 **وأوّلُ ما يُثبَّت: الحاكمُ ليس `R` لكلّ جلسة.** الأذرعُ هنا **تغيّر عددَ
الكروت** (بخلاف `T-PRESS-RANK`/`T-HOLD-KEY-2` حيث ثبّته السقف) ⇒ صافي `R`/جلسة
**يكافئ الذراعَ الأكثرَ تسليمًا** حين يكون التوقّعُ موجبًا — وهو **عينُ الفخّ
المقيس في `T-RANK-DENSE`** («الأذرعُ تفترق في عدد الصفقات لا في جودة الاختيار»).

- 🥇 **الحاكم: `R` لكلّ كرت** = مجموعُ `R` عبر الجلسات ÷ عددِ الكروت.
- **ورفيقان يُطبَعان دائمًا:** `R` لكلّ جلسة · وعددُ الكروت.
- **وعددُ الفائزين** = الكروتُ التي `oc == "win"` (بلغت الهدف).

**تُعتمَد ذراعٌ إن استوفت الثلاثةَ معًا:**

1. **`R` لكلّ كرت لا يسوء عن `W0` بأكثر من ‏0.02R في كلّ سنة** — الحدُّ **مُعادٌ
   حرفيًّا** من `T-STABILITY §(ج)` و`T-BASE-475 §(ج)` بلا تحريك.
2. **عددُ الفائزين أعلى من `W0` في السنوات الثلاث بلا انقلاب** — مقياسُ التسليم
   نفسُه المستعمَل في `T-CAP` و`T-BASE-475`.
3. **كلفةُ الإشعار: عددُ الكروت لا يزيد على ‏+50% عن `W0`** — الحدُّ **مُعادٌ
   حرفيًّا** من `T-CUMRISE §④` («إشعاراتٌ ‏+50% فأقلّ»).

**والأرضية (④):** أربعون جلسةً فأكثر **و150 كرتًا** فأكثر في كلّ سنة — مُعادةٌ
حرفيًّا. **والحكمُ يُكتب مرّةً واحدة** عند اكتمال السنوات الثلاث. ⛔ **ولا تُعاد
تشغيلةٌ بمعيارٍ مبدَّل، ولا يُحرَّك حدُّ 0.02R ولا حدُّ ‏+50% مهما جاء الرقم.**

---

## ⑤ بوّاباتُ الصلاحية — **تُنفَّذ في الكود برموزِ خروجها**

| | الشرط | الإخفاق |
|---|---|---|
| `WW1` | بصماتُ `hold_key2_arms` و`hold_key_arms` و`press_rank_arms` مطابقةٌ للمثبَّت | خروج 3 |
| `WV7` | التغطية تبلغ `COVERAGE_MIN` | خروج 3 |
| `WV0` | **`W0` يُعيد `B0` من `T-HOLD-KEY-2` بت-بت** (صافي `R`/جلسة · الكروت · `bind`) وبتطابقٍ حقلًا حقلًا مع `hold_key_arms.deliver_prod` | خروج 3 |
| `WV4` | وسيطُ الجاهزين/جلسة داخل ‏[8، 40] (مُعادٌ) | خروج 4 |
| `WV5` | **الدِدوبُ يكتم فعلًا**: نسبةُ الصفوف التي يُسقطها `should_alert` في `W0` أكبرُ من صفر وأصغرُ من مئة — **وتُطبَع** | خروج 4 |
| `WV2` | **الأذرعُ تتفرّق**: عددُ كروت `W1` و`W2` و`W3` ليس متطابقًا مع `W0` | خروج 4 |
| `WV6` | صفرُ خرقِ دِدوبٍ **داخلَ نافذة الذراع نفسِها** | خروج 3 |
| `WV3` | الأرضيتان (‏40 جلسة · 150 كرتًا) | خروج 4 |

---

## ⑥ التنبّؤات — تُنشَر مكذَّبةً كما تُنشَر محقَّقة

- **`W-P1`** (الأثقل — يراهن على تشخيصي): **الدِدوبُ هو الخانقُ الأكبر** —
  أرجّح أن **‏70% فأكثر** من الصفوف الجاهزة يُسقطها `should_alert` في `W0`.
  🔴 **وإن جاء دون 50% فتشخيصي في `hold_key2_result §④` ناقصٌ ويُعلَن كذلك.**
- **`W-P2`**: `W1` ترفع عددَ الكروت بين ‏+30% و+50% (فتمرّ ③ بشعرةٍ أو تسقط).
- **`W-P3`**: `W1` تسلّم فائزين أكثرَ في الثلاث (②) **وتسقط على ①** لأن الكروتِ
  المضافةَ إعاداتٌ لأسماءٍ سبق تنبيهُها.
- **`W-P4`**: `W3` **لا يغيّر شيئًا يُذكَر** (فارقُ كروتٍ دون ‏+10%) لأن السقفَ
  لا يبنّد إلّا في ‏18.8-29.8% من الجلسات — **وهو اختبارُ ميكانيكاي المنشورة.**
- **`W-P5`**: `W2` يرفع `R`/كرت ويُنقص الفائزين ⇒ يسقط على ②.
- **`W-P6`**: نصيبُ الجلسات التي فيها مستيقظٌ واحدٌ فأكثر بين ‏20% و60%.

---

## ⑦ سقفُ النجاح — مثبَّتٌ سلفًا

**أقصى ما يُنتجه النجاحُ الكامل: اقتراحٌ للمالك ‏+ سطرُ عرضٍ تشخيصيّ.**
⛔ **وممنوعٌ مهما كانت الأرقام:** تغييرُ `REALERT_DAYS` أو `ALERT_CAP` أو
`ALERT_W` أو `READY_HOLD` أو `MEMORY_DAYS` أو `POOL_CAP` أو `alert_rank` أو
`WATCHLIST_SIZE` أو أيّ بوّابةٍ من `M1`-`M14` أو الدخول/الوقف/الأهداف أو
`kasih_scan.resolve` أو صيّاد المقسّم. **وأيُّ اعتمادٍ قرارُ مالكٍ صريح.**

---

## ⑧ حدودُ صدقٍ — **تُكتَب قبل الأرقام وتُقرأ معها**

1. 🔴 **الأذرعُ تغيّر عددَ الكروت** ⇒ `R`/جلسة **مُضلِّل هنا**، والحاكمُ `R`/كرت
   **مثبَّتٌ قبل أيّ رقم** (‏§④) — درسُ `T-RANK-DENSE`.
2. 🔴 **حدُّ ‏0.02R مُعادٌ حرفيًّا وشدّتُه النسبيّة تتفاوت بالسنة:** على أرقام
   `T-HOLD-KEY-2` يساوي ‏≈7% من `R`/كرت في 2024 و**‏≈66%** في 2023 (حيث الرقمُ
   ‏+0.0302) ⇒ **المعيارُ أشدُّ في السنوات القويّة وأرخى في الضعيفة** — يُعلَن
   ولا يُحرَّك بعد الأرقام.
3. 🔴 **الافترُ غائبٌ عن `wake_read` في المِشية التاريخية** (‏`ah_pct=None` —
   أرضيّةٌ مُعلَنةٌ في `press_rank_prereg §⑧-2`) ⇒ مجموعةُ المستيقظين **أضيقُ من
   الحيّة** ⇒ **قراءةٌ متحفّظة تجاه `W3`** (أثرُ الحصر مبخوسٌ لا منفوخ).
4. **`plan` خامدٌ بنيويًّا** (ذاكرةُ البِركة لا وجود لها تاريخيًّا) · و`prev_q`
   محسوبٌ بصدقٍ ‏39.9-43.0%.
5. **إعادةُ `D_live2` تطابق المقدارَ والاختيار لا الهويّة** (مُعادٌ من
   `hold_key2_prereg §⑧-2`) · و`M13`/`M14` غيرُ قابلتين للإرجاع.
6. **لمسُ هدفٍ لا تنفيذ** — كلُّ أرقام `R` بلا انزلاق (سابقةُ `T-SLIP`) ·
   **وانحيازُ البقاء** قائمٌ في اللقطات الثلاث.
7. **عددُ الفائزين مقياسُ تسليمٍ لا ربحيّة** — كرتٌ بلغ الهدف ليس صفقةً منفَّذة.
8. 🔴 **وفجوةٌ في دفتر المصادر تُعلَن:** `REALERT_DAYS` و`ALERT_CAP` و
   `MEMORY_DAYS` **عتباتُ قرارٍ بلا صفٍّ في `FAISAL_SOURCE_LEDGER.md`** — قفلُ
   الدفتر يحرس وسمَ الصفوف الموجودة ولا يكشف الغائبة. **يُبلَّغ ولا يُسدّ هنا**
   (خارج أمر المالك).
9. **ولا دائريّةَ في هذا المحور** (لم يُنقَّب فيه قطّ) — بخلاف `T-HOLD-KEY-2`.

---

## ⑨ ملحقٌ مؤرَّخ — إقراراتٌ قبل أيّ رقم (2026-08-28)

1. **ثلاثُ أدواتٍ مجمَّدة (‏CAP15) تُستورَد بالاسم ولا تُعدَّل:**
   `hold_key2_arms.py` = `6c8f7d2a2d8c24f0` · `hold_key_arms.py` = `39643875f649a42f`
   · `press_rank_arms.py` = `6bb28144c1742840` (أوائلُ بصماتها، والمثبَّتُ كاملًا
   في الأداة) — أرقامُها منشورةٌ فتبديلُها يُبطل المقارنة.
2. **`deliver_wake(..., realert_days, fill_from_quiet)` نسخةٌ مُوسَّعة** لأن
   `hold_key_arms.deliver_prod` مجمَّدةٌ بثوابتها ⇒ **و`WV0` يُثبت التطابقَ
   بت-بت** عند `(5, False)` — سابقةُ `V0`/`RV0`/`BV0`.
3. **الأرقامُ المنشورة في `hold_key2_result.md` و`press_rank_result.md` لا
   تُعدَّل** بنتيجة هذي التجربة.
4. **وإن سقطت `WV0` فهو عطبُ أداةٍ لا نتيجة** ولا يُفسَّر جدولُ أذرع.
5. **وإن خرجت الأذرعُ متطابقةً فهو `no-op`** يُعلَن بخروج 4 ولا يُفسَّر.
6. **وإن سقطت `W-P1` تحت 50% فالتشخيصُ المنشورُ في `hold_key2_result §④` ناقصٌ**
   ⇒ **يُصحَّح صراحةً** ولا يُترَك.
7. **والسنواتُ الثلاثُ على اللقطات نفسِها:** 2023 `31574165523` · 2024
   `31199493957` · 2025 `31191327731` — **بلا تبديلٍ ولا سنةٍ رابعة.**
