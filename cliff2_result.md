# ⛰️②🔴 T-CLIFF-2 — النتيجة: **فشلت** · و`EHGO` **يُلتقَط فعلًا** لكنّ ثمنَه ‏×5.36

> **التسجيلُ المسبق** `cliff2_prereg.md` مدفوعٌ **قبل أيّ رقم** ولم يُمَسّ بعده.
> **اللقطة:** `30597787554` (نفسُ لقطة `T-CLIFF`) · 125 يومًا · `float_mode=yahoo`.
> **التشغيلات:** `A0`+`A1` = `31151315697` · `A2` = `31152421400` ·
> `A3` = `31152428182`. (وتشغيلتا `31151321977`/`31151328941` **سُحبتا** — §③.)
> 🔒 قياس فقط · الجذورُ الاثنا عشر byte-identical · لا `LOGIC_VERSION`.

---

## ① الأرقام — والنسبةُ إلى **الأساس الإنتاجيّ** لا إلى أساس كلّ تشغيلة

| الذراع | الترتيب | السقف | مطابق/يوم | المجموع | **‏×A0** | من الستة |
|---|---|---|---|---|---|---|
| **A0** (الإنتاج، `c0`) | `cliff` | 80 | **1.16** | 145 | **×1.00** | — |
| **A1** (`c3`، ذراعُ T-CLIFF) | `cliff` | 80 | 1.20 | 150 | **×1.03** | ❌ |
| **A2** (`c3`) | **`cum`** | 80 | 3.53 | 441 | **×3.04** | ❌ |
| **A3** (`c3`) | **`cum`** | **240** | 6.22 | 777 | **×5.36** | ✅ **`EHGO`=8 أيام** |

🔴 **والنسبةُ أعلاه محسوبةٌ إلى `A0` وحده — لا إلى `×C0` المطبوع في السجلّ.** فالأداة
تطبع نسبةَ كلِّ ذراعٍ إلى `c0` **الخاصّ بتشغيلته**، و`c0` في تشغيلتَي `cum` **مرتَّبٌ
تراكميًّا فليس الأساس الإنتاجيّ** (‏300 و360 مطابقًا بدل 145). **وهذا القيدُ مسجَّلٌ
حرفيًّا في §④ قبل التشغيل** — ولولاه لقُرئت `A3` «×2.16 ⇒ داخل السقف» وهي **×5.36**.

## ② البوّابةُ الثنائية (‏§⑤ — منقولةٌ من `cliff_prereg.md` §④ بلا تعديل)

| | (أ) استرجاعٌ حقيقيّ | (ب) كلفةٌ ‏≤3× من `A0` | الحكم |
|---|---|---|---|
| `A1` | 🔴 صفر | ✅ ×1.03 | **فشل** |
| `A2` | 🔴 صفر | 🔴 ×3.04 | **فشل** |
| `A3` | ✅ **`EHGO`** | 🔴 **×5.36** | **فشل** |

⇒ **لا ذراعَ تستوفي الشرطين ⇒ `T-CLIFF-2` فشلت.** `SPLIT_RADAR_ORDER` و
`SPLIT_RADAR_PROBE_CAP` **يبقيان على قيمتَي الإنتاج** (`cliff` · 80).

## ③ 🐞 وذراعُ الترتيب خرجت أوّلًا **no-op** — والعدّاداتُ كشفَتها لا القراءة

أوّلُ تشغيلةٍ بـ`order=cum` خرجت **مطابقةً بت-بت** لتشغيلة `cliff`: ‏145 و150 مطابقًا ·
نفسُ الرموز · نفسُ إحصاء `probe`. **وهي بصمةُ الـno-op** التي تمنع قاعدتُنا تفسيرَ
نتيجةٍ عليها. **السبب عيبٌ في كودي:** وضعتُ الخطّاف في `scan_split_radar` بينما أدواتُ
`T-CLIFF` تقيس **`scan_split_hunter`** ⇒ المفتاحُ لم يُقرأ أصلًا.

✅ **نُقل الخطّاف إلى مُرشّح التكلفة داخل `scan_split_hunter`** (بإذن المالك «سوها لو ما
تاثر على الفحص و الفرز نفسه»)، **والافتراضُ `cliff` = السلوكُ السابق حرفيًّا** — مقفولٌ
**سلوكيًّا** بأربعة أقفال (‏CL3a-d) ومنها «بسقفٍ يسع الجميع ⇒ **نفسُ المفحوصين**
بالترتيبين» ⇒ **الشروطُ الخمسة والحكمُ لم تُمَسّ**. وأُعيدت `A2`/`A3`، **والتشغيلتان
الأوليان مسحوبتان**.
🧭 **الدرس: «الخطّافُ موجود» ليس «الخطّافُ في المسار المقيس» — والعدّادُ المتطابق
بت-بت هو الشاهد الوحيد الذي يفرّق بينهما.**

## ④ التنبّؤات (‏§⑥) — **‏2 مؤكَّدان · 2 مكذَّبان**، والمكذَّبان أنفعُ

| | التنبّؤ | النتيجة |
|---|---|---|
| `G1` | `A2` (الترتيبُ وحده) يلتقط `EHGO` | 🔴 **مكذَّب** — الترتيبُ وحده لا يكفي؛ **السقفُ شرطٌ لازم** |
| `G2` | كلفةُ `A2` ‏≈ كلفةُ `A1` (‏±20%) | 🔴 **مكذَّب بشدّة** — ‏441 مقابل 150 = **×2.94** |
| `G3` | `A3` يتجاوز ‏3× | ✅ **مؤكَّد** (‏×5.36) |
| `G4` | `A1` يُعيد إنتاج رقم `T-CLIFF` | ✅ **مؤكَّد حرفيًّا** (‏145/150 · ×1.03) ⇒ التشغيلةُ صالحة |

🔑 **وسقوطُ `G2` هو الاكتشافُ الحقيقيّ:** ظننتُ الترتيبَ **محايدًا للحصيلة** («لا يغيّر
كم يدخل بل أيَّهم») — **والأرقام تنفيه**: بالسقف نفسِه (80) قفز المطابقون **‏150 ⟶ 441**،
و`c0` نفسُه قفز **‏145 ⟶ 300**. ⇒ **الترتيبُ التراكميّ لا يُعيد ترتيب البِركة نفسِها —
بل يُدخل مجتمعًا آخر**، أغنى بكثيرٍ بمن يجتاز شروط فيصل الخمسة (منهارون فعليًّا لا
أصحابُ شمعةٍ حادّةٍ واحدة). **لكنّ «أكثر» ليس «أفضل»:** عند السقف 80 ظلّ **صفرٌ** من
الستة رغم ثلاثة أضعاف المطابقين.

## ⑤ 🎯 و`EHGO` **قابلٌ للالتقاط** — وهذا يُثبت تشخيص `T-CLIFF` ويُسعّره

`T-CLIFF` قال: «العلّةُ مفتاحُ الترتيب والسقف لا العتبة». **والقياسُ يؤكّده:** `EHGO`
ظهر مطابقًا كاملًا **‏8 أيام** — لكن **فقط** حين اجتمع الاثنان (`cum` + سقف 240)،
**وبثمنٍ مقيس: ‏×5.36 مطابقًا يوميًّا** أي **‏≈6 أسهم كلّ ليلة بدل ‏≈1**.

⇒ **السؤالُ لم يعد «هل يُمكن؟» بل «بأيّ ثمن؟»** — والثمنُ يكسر عقد المالك «لا إشعار
إلا بالجاهز»، **والمعيارُ المسجَّل رفضه قبل أن نراه**.

⚠️ **وحدٌّ يمنع المبالغة:** حتى عند السقف 240 بقي `قُصّ` = **‏118 من 125 يومًا** لـ`c3`
⇒ **أعدادُ الالتقاط أرضيّةٌ لا سقف**، وسقفٌ أعلى قد يلتقط المزيد **بثمنٍ أعلى بعد**.
وباقي الخمسة (`AZI`·`DSY`·`ZCMD`·`JZ`·`SPRC`) **غائبون في كلّ الأذرع**.

## ⑥ الأقفالُ التشغيلية — كلُّها خضراء في الأربع

«الكلفة من الإنتاج» (عدّادُ `probe` من داخل `scan_split_hunter`): **اختلاف 0 من 125**
في كلّ ذراع · «تكافؤ الميكانيكا»: **اختلاف 0** · و`CONFIG` بعد كلّ تشغيلة
**`SPLIT_CLIFF_PCT=30 · SPLIT_RADAR_PROBE_CAP=80` = الإنتاجية بلا تلوّث**.

## ⑦ الأثرُ الإنتاجيّ — صفر

لا عتبةَ تغيّرت · ولا شرطٌ من شروط فيصل الخمسة · و`scan_split_hunter` تغيّرت **بخطّافٍ
مطفأٍ افتراضيًّا فقط** (بإقرارٍ مؤرَّخ في الختمين) · ولا اقتراحَ للمالك.
⏸️ **وإعادةُ الفتح قرارُ مالكٍ حصريّ** — ولها ثمنٌ معلومٌ الآن بالرقم.

## ⑧ حدودُ صدق (‏§⑧ كما سُجِّلت)

انحيازُ البقاء · الفلوت **ليس point-in-time** (حدٌّ **مشترَك** بين الأذرع فلا يحيز
المقارنة) · بلا افتر · اللقطةُ كونُ ناسداك **اليوم** · و**ثبوتُ الالتقاط ليس ثبوتَ
الربحية** — المقيس «هل يظهر مطابقًا قبل انفجاره؟» لا «هل يربح؟».
