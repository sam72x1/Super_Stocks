# 💥🔗② `T-LINK100-2` — **الحكم: الفرعُ 2 «لا تعبر»**
### (‏والنتيجةُ الأولى **الفرعُ 3 «لا حكم»** بسقوط `V-N1` — تبقى منشورةً بحرفها في `§⓪`-`§④`)

> **العقد:** `link100_2_prereg.md` (مدموجٌ `48d1bfe5` ⟶ `e1775452` **قبل أيّ سطرِ
> أداةٍ وقبل أيّ رقم**) · **الأداة:** ميزةٌ خلف علمٍ مطفأ داخل `link100_probe.py`
> (`LINK100_NEWLOW`) · مدموجةٌ `ee3dd457`.
> **التشغيلة:** [`35487721499`](https://github.com/sam72x1/Super_Stocks/actions/runs/35487721499)
> على `main` (‏8.1 دقيقة · خروج **‏7**).
> **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · ولم يُشحَن شيء.**

---

## ⓪ الحكم في سطر

**`V-N1` سقط في 2023 فأوقف التشغيلةَ قبل أيّ رقمِ ميزة** — الأحداثُ **‏480**
والمنشورُ **‏481**. وبنصّ `§⑤`-3 هذا **الفرعُ 3 «لا حكم»، يُعلَن بعدده ولا
يُفسَّر**. 🔑 **والسببُ مُشخَّصٌ تمامًا ولا علاقةَ له بالميزة الجديدة:
مرجعُ التقسيمات الحيُّ عند المزوّد تغيّر بين التشغيلتين.**

---

## ① التشخيص — كلُّ ما قبل فلتر التقسيم **مطابقٌ بت-بت**

| ‏2023 | المنشور (‏`35163391888` · 09-17) | هذي (‏`35487721499` · 09-20) | الفرق |
|---|---|---|---|
| أيّامُ تداول | ‏250 | ‏250 | **‏0** |
| جُلبت | ‏250 (‏100.0%) | ‏250 (‏100.0%) | **‏0** |
| **أحداثٌ خام** | **‏869** | **‏869** | **‏0** |
| **بعد الطيّ (‏20 جلسة)** | **‏700** | **‏700** | **‏0** |
| **مُستبعَدو التقسيم (‏±3ي)** | **‏160** | **‏161** | **‏+1** ⬅️ |
| بلا تاريخٍ كافٍ | ‏59 | ‏59 | **‏0** |
| بلا شاهدٍ ذاتيّ | ‏84 | ‏84 | **‏0** |
| **تُقاس** | **‏481** | **‏480** | **‏−1** |
| شاهدٌ مقطعيّ `CX` | ‏473 (‏98.3%) | ‏472 (‏98.3%) | ‏−1 |
| شاهدٌ ذاتيّ `CC` | ‏397 | ‏396 | ‏−1 |
| `vol_x ≥ 3` | ‏38.7% | ‏38.8% | ‏+0.1 نقطة |

**وقانونُ البقاء يُغلق التشخيص حسابًا لا ظنًّا** (‏`len(ev) = بعد الطيّ −
مُستبعَدو التقسيم − بلا تاريخٍ كافٍ`):
- المنشور: ‏700 − 160 − 59 = **‏481** ✓
- هذي: ‏700 − **161** − 59 = **‏480** ✓

⇒ **الفرقُ كلُّه يُفسَّر بصفٍّ واحدٍ إضافيٍّ سقط على حارس التقسيم `V-L2`**،
وكلُّ ما قبله — `year_days` · `grouped_day` · `day_events` · `fold_events` —
**أعاد رقمَه المنشورَ بت-بت**. ⇒ **الآلةُ هي الآلةُ نفسُها، والمتغيّرُ مرجعُ
تقسيماتٍ حيٌّ يُنقَّح رجعيًّا عند المزوّد** (‏`splits_of` تقرأ `/v3/reference/splits`).

🔒 **وأن الميزةَ الجديدة ليست السبب ثابتٌ بالبناء لا بالدعوى:** `newlow60_5`
تُضاف **مفاتيحَ** إلى قاموس `daily_feats` ولا تُسقط صفًّا ولا تُغيّر شرطَ
بقاءٍ واحدًا — و`LNA0` يقفل أن العلمَ المُطفَأ يعطي القاموسَ نفسَه بت-بت.

---

## ② ما لم يُقَس — والحارسُ هو الذي حفظه

التشغيلةُ توقّفت **قبل** `n_cells` و`z` و`enrich` ⇒ **صفرُ رقمٍ عن
`newlow60_5` قُرئ**، ولا عن الشبكة التسع. ⇒ **المحورُ ما زال عذراء**،
والتسجيلُ المسبقُ سليمٌ لم يُطَّلَع على شيءٍ منه.

---

## ③ 🔴🔴 والعيبُ في **اختياري أنا للمرساة** — يُنشَر ولا يُطوى

`§③` من العقد **يرفض `rsplit180` مرساةً** ويُعلّل: «**يقرأ تقسيماتٍ حيّةً قد
تتغيّر**» — **ثمّ اختار مرساةً (عددُ الأحداث) تمرّ هي نفسُها عبر فلتر
التقسيمات الحيّ**. ⇒ **سمّيتُ العلّةَ ثمّ وقعتُ فيها في الفقرة نفسِها.**

🧭 **والدرسُ يُعمَّم على المستودع كلِّه: أيُّ مرساةِ هُويّةٍ يجب أن تقع
«فوق» كلّ نداءٍ لمرجعٍ حيّ** — لا تحتَه. **وينطبق أثرًا رجعيًّا على
`U-RAW` في `T-PMUSD`** (‏34.72× · 21.69× · 14.03×): وهي محسوبةٌ على `ev`
**بعد** الفلتر نفسِه ⇒ **معرَّضةٌ للانزياح ذاته**، وقد عبرت يوم 09-17
**ولا ضمانَ أن تعبر غدًا**. (‏لم تُمَسّ بهذا العقد — يُدوَّن فقط.)

---

## ④ ما لم يتغيّر — ولا حرف

- **لم يُخفَّض حدٌّ ولم يُرخَ حارسٌ** بعد رؤية الرقم.
- **العقدُ مدموجٌ فلا يُعدَّل** — والعلاجُ **ملحقٌ مؤرَّخ `§⑬`** كما في
  `T-SUPDEF §⑩`-`§⑫` و`T-PMUSD §⑫` و`T-OPHOLD-2 §⑫`.
- **صفرُ شحنٍ · لا `LOGIC_VERSION` · والجذورُ الاثنا عشر بصمتُها مطابقة.**

---

*التشغيلة: <https://github.com/sam72x1/Super_Stocks/actions/runs/35487721499>*

---
---

# ⑤ التشغيلةُ الحاكمة (‏2026-09-20) — **الفرعُ 2 «لا تعبر»**

> **التشغيلة:** [`35500939991`](https://github.com/sam72x1/Super_Stocks/actions/runs/35500939991)
> على `main` (رأس `d0e4396e`) · `newlow=1` · **‏36.6 دقيقة · خروج `0`**.
> **والعقدُ لم يُمَسّ بحرف** — لا `link100_2_prereg.md` ولا ملحقُه `§⑬`
> (كلاهما مدموجٌ **قبل** هذي التشغيلة) · **وصفرُ شحن** بنصّ `§⑨`.

## ⑤-أ الحكم في سطر

**`newlow60_5` لا تعبر `LK1` في أيّ سنةٍ من الثلاث — وتسقط على الشاهدَين
معًا لا على أحدهما.** أعلى نسبةٍ بلغتها مقابل `CX` هي **‏1.45×** ومقابل `CC`
**‏1.12×**، والعتبةُ **‏2×** لكليهما ⇒ **الفرعُ 2** بنصّ `§⑤`-2.

**والسلّةُ العابرةُ في السنوات الثلاث بقيت كما هي بحرفها:**
`['ret5:>+20%', 'vol_x:≥3']` — **أي أن إضافةَ الميزة لم تُحرّك حكمَ
`T-LINK100` المنشور**، وهو شاهدُ هُويّةٍ ثانٍ فوق `V-N2`.

## ⑤-ب `V-N1-b` (‏`§⑬`) — عبرت في السنوات الثلاث · **والقديمةُ كانت ستسقط ثانيةً**

| السنة | الرباعيُّ فوق المرجع الحيّ | المنشور | الحكم | ميزانٌ مُغلَق |
|---|---|---|---|---|
| ‏2023 | (‏250, 250, **869**, **700**) | مطابق | ✅ | ✅ |
| ‏2024 | (‏252, 252, **1088**, **912**) | مطابق | ✅ | ✅ |
| ‏2025 | (‏250, 250, **1248**, **1087**) | مطابق | ✅ | ✅ |

**والانزياحُ مطبوعٌ كما اشترط `§⑬`-ⓒ-3:**

| السنة | مُستبعَدو التقسيم | أحداثٌ تُقاس | `vol_x ≥ 3` |
|---|---|---|---|
| ‏2023 | ‏161 (المنشور ‏160 · **‏+1**) | ‏480 (المنشور ‏481 · **‏−1**) | ‏38.8% (المنشور ‏38.7%) |
| ‏2024 | ‏206 (‏**+0**) | ‏655 (‏**+0**) | ‏47.3% (مطابق) |
| ‏2025 | ‏263 (‏**+0**) | ‏773 (‏**+0**) | ‏48.6% (مطابق) |

🔑 **والدلالةُ أثقلُ من كونها «عبرت»: انزياحُ 2023 لم يرجع** — ‏161 كما كان
يوم 09-20 لا ‏160 المنشور ⇒ **`V-N1` بصيغته الأولى كانت ستوقف هذي التشغيلةَ
أيضًا بخروج ‏7**. فالملحقُ `§⑬` لم يكن تجميلًا: **هو الفرقُ بين حكمٍ
وصمت**، وقد كُتب **قبل** أن يُعرَف أن الانزياحَ دائم.
⚖️ وبالمقابل يُقال ما خسرناه كما نصّ `§⑬`-ⓔ: النسبُ المنشورة (‏38.7% ·
47.3% · 48.6%) صارت **وصفيّةً لا مرساة** — وقد تحرّكت ‏0.1 نقطةٍ فعلًا.

## ⑤-ج الأرقامُ الحاكمة — الميزةُ الرابعةَ عشرة

**الخلايا ‏41** (كانت ‏39 قبل الإضافة) · **`z` بونفيروني ‏3.237** (‏α=0.05/خليّة)
· **الأرضيّة ‏3.222** ⇒ `V-N4` ✅ **والمستعمَلةُ أعلى من المنشورة** (زيادةُ
الخلايا ترفع `z`) ⇒ **تشديدٌ لا إرخاء** بنصّ `§④`.

| السنة | `نعم` | `n` | `CX` | `CC` | `LK1` |
|---|---|---|---|---|---|
| ‏2023 | **‏27.9%** | ‏126 | **‏1.19×** | **‏0.93×** | ❌ |
| ‏2024 | **‏26.3%** | ‏163 | **‏1.10×** | **‏0.96×** | ❌ |
| ‏2025 | **‏23.7%** | ‏174 | **‏1.45×** | **‏1.12×** | ❌ |

**وسلّةُ `لا` للمقارنة:** ‏2023 ‏72.1% (‏0.94× · 1.03× · n=326) · ‏2024 ‏73.7%
(‏0.97× · 1.01× · n=456) · ‏2025 ‏76.3% (‏0.91× · 0.97× · n=560).

🔴 **وليست عيّنةً قليلة:** `n` في سلّة الحدث ‏126 · 163 · 174 — **كلُّها فوق
`MIN_BUCKET_N = 50`** ⇒ هذا **سالبٌ حقيقيٌّ لا «لا حكم»**.
ℹ️ **والمُقصَون بنصّ `§②`-2** (‏`len(lows) < 60` ⇒ `None` تخرج من مقام هذي
الميزة وحدَها): ‏28 من 480 (‏5.8%) · ‏36 من 655 (‏5.5%) · ‏39 من 773 (‏5.0%).

## ⑤-د الشبكةُ الوصفيّة — **والسالبُ ليس أثرًا لاختيارنا `W=60`/`K=5`**

`CX` · `CC` لكلّ خليّةٍ من التسع (‏لا تدخل الحكمَ ولا عدَّ بونفيروني · `§②`):

| `W_K` | ‏2023 | ‏2024 | ‏2025 |
|---|---|---|---|
| `20_3` | ‏33.1% (‏1.17 · 0.98) | ‏33.3% (‏1.17 · 0.96) | ‏32.3% (‏1.38 · 1.09) |
| `20_5` | ‏45.6% (‏1.24 · 0.99) | ‏42.7% (‏1.13 · 0.95) | ‏40.6% (‏1.26 · 1.07) |
| `20_10` | ‏62.9% (‏1.15 · 0.97) | ‏64.1% (‏1.16 · 0.97) | ‏57.6% (‏1.15 · 1.01) |
| `60_3` | ‏19.7% (‏1.07 · 0.85) | ‏20.5% (‏1.12 · 0.99) | ‏20.2% (**‏1.68** · **‏1.18**) |
| **`60_5`** ⬅️ | **‏27.9%** (‏1.19 · 0.93) | **‏26.3%** (‏1.10 · 0.96) | **‏23.7%** (‏1.45 · 1.12) |
| `60_10` | ‏40.0% (‏1.14 · 0.95) | ‏38.9% (‏1.16 · 1.04) | ‏33.5% (‏1.34 · 1.08) |
| `120_3` | ‏15.5% (‏1.22 · 0.91) | ‏13.9% (‏1.11 · 0.91) | ‏11.9% (‏1.41 · 1.17) |
| `120_5` | ‏21.5% (‏1.35 · 0.95) | ‏18.0% (‏1.06 · 0.86) | ‏14.2% (‏1.23 · 1.06) |
| `120_10` | ‏32.7% (‏1.38 · 1.04) | ‏28.4% (‏1.16 · 1.03) | ‏20.9% (‏1.21 · 1.05) |

🔑 **من ‏27 خليّةً: صفرٌ تبلغ ‏2× على أيّ شاهد.** الأقصى `CX` = **‏1.68×**
والأقصى `CC` = **‏1.18×**، وكلاهما في الخليّة نفسِها (‏2025 · `60_3`).
⇒ **الحكمُ لا يعلّق على الرقمين اللذين اخترناهما** — وهو بالضبط ما خِيفَ منه
في `§⑦`-2، **وقد جاء الجوابُ في صالح الصدق لا في صالح الرقم**.

⚠️ **ورتابةُ الشبكة ليست اكتشافًا بل شاهدُ سلامةٍ للتنفيذ:** صاعدةٌ في `K`
في **‏9 من 9** وهابطةٌ في `W` في **‏9 من 9** — وكلتاهما **مفروضةٌ بالتعريف**
(`K` أكبر ⇒ فرصٌ أكثر · `W` أكبر ⇒ الأدنى أدنى فيصعب بلوغُه). **تُقرأ إثباتًا
أن الدالّة تفعل ما تقوله، لا سِمةً في السوق.**

## ⑤-هـ تنبّؤاتي — **اثنان مكذَّبان · واحدٌ غيرُ قابلٍ للحسم بعيبٍ منّي**

| # | النصّ المسجَّل | الحكم |
|---|---|---|
| `NP1` | تعبر مقابل **`CX`** في الثلاث | 🔴 **مكذَّب** — أقصى `CX` ‏1.45× (‏1.68× في الشبكة كلِّها) ولم تبلغ ‏2× قطّ |
| `NP2` | **وتسقط على `CC`** ⇒ الفرعُ 2 | 🟡 **الحكمُ صحيحٌ والآليّةُ خطأ** — سقطت على `CC` نعم، **لكنّها سقطت على `CX` أيضًا** ⇒ تشبيهي بـ`rsplit180` (‏`CX` تعبر و`CC` تسقط) **باطل** |
| `NP3` | حصّةُ `نعم` بين ‏15% و45% كلَّ سنة | ✅ **مؤكَّد** (‏27.9 · 26.3 · 23.7) |
| `NP4` | رتيبةٌ في `K` · وغيرُ رتيبةٍ بالضرورة في `W` | ✅ **مؤكَّد في `K`** (‏9/9) — **والواقعُ أشدُّ انتظامًا ممّا تنبّأتُ به** (‏رتيبةٌ في `W` أيضًا ‏9/9)، وكلتاهما بنيويّةٌ كما في `⑤-د` |
| `NP5` | التقاطعُ مع `ret5 < −20%` **دون ‏50%** | ⛔ **غيرُ قابلٍ للحسم — والعيبُ في أداتي** |

### 🔴 `NP5` — أداتي لم تطبع ما اشترطه عقدي

`§⑦`-4 نصّ على أن **«يُطبَع تقاطعُها الوصفيّ»** مع `ret5 < −20%` و
`dd52 < −90%` — **و`link100_probe.py` لا يحسبه ولا يطبعه** (بحثُ الملفّ عن
التقاطع: **صفرُ مطابقة**). ⇒ **حدُّ صدقٍ كتبتُه ثمّ لم أنفّذه.**

والهوامشُ المنشورةُ تعطي **سقفًا لا قيمة**: التقاطعُ لا يتجاوز
`ret5<−20% ÷ حصّةِ نعم` = **‏50.2%** (‏2023) · **‏66.9%** (‏2024) ·
**‏65.0%** (‏2025) من سلّة `نعم` ⇒ **لا 2023 يُحسَم ولا غيرُه.**

⚖️ **وأثرُه على الحكم: صفر** — `LK1` يُحسَم على `CX` و`CC` وحدَهما وقد سقطا
في الثلاث. **لكنّه عيبُ أداةٍ يُنشَر ولا يُطوى**، وعلاجُه سطرٌ واحدٌ في
تشغيلةٍ لاحقة **لا يُكتَب الآن** (‏العقدُ مدموج، والتقاطعُ وصفيٌّ لا يغيّر فرعًا).

## ⑤-و القراءةُ الموضوعيّة — **الدعوى صادقةٌ وصفًا وعاجزةٌ فصلًا**

فيصل قال: «دائمًا السهم قبل يدبل **يُضغط لأدنى قاع**». والمقيس:

1. **ربعُ المنفجرين فعلوا ذلك** — ‏27.9% · 26.3% · 23.7% سجّلوا قاعَ ‏60 جلسةً
   داخلَ آخر خمس. ⇒ **الوصفُ ليس خطأً.**
2. **وأقرانُهم فعلوه بالقدر نفسِه** — `CC` ‏0.93× · 0.96× · 1.12× ⇒ **السهمُ
   نفسُه في أيّامٍ أخرى يسجّل قاعًا جديدًا بالمعدّل ذاته تقريبًا**.
   ⇒ **السِّمةُ لا تفصل «أيَّ يومٍ» ينفجر.**
3. **ولا تفصل «أيَّ سهمٍ» إلّا بالكاد** — `CX` ‏1.19× · 1.10× · 1.45×.

🔎 **والمقارنةُ تُوضّح الفرق:** `rsplit180` أعطت `CX` **‏6.94× · 8.84× · 3.47×**
بـ`CC` ‏1.24× · 1.20× · 1.25× ⇒ قرأناها «**أيُّ الأسهم** لا **أيُّ الأيّام**».
و`newlow60_5` **ليست أيًّا منهما**.

🧭 **والسببُ البنيويُّ مقيسٌ لا مُخمَّن، وهو عينُ ما خِيفَ منه في `§⓪`:**
المجتمعُ **مُنتقًى سلفًا** ليكون عند قاعه — `dd52` دون ‏−50% تغطّي **‏76.4%**
و**‏75.1%** و**‏73.3%** من الأحداث. **ففي مجتمعٍ ثلاثةُ أرباعه منهارٌ أصلًا،
«سجّل قاعَه للتوّ» قريبٌ من معدّل الأساس.** ⇒ العقدُ سأل: هل تضيف حداثةُ
القاع على `dd52`؟ **والجوابُ المقيس: الإضافةُ عند هذي الدقّة تكاد تكون صفرًا.**

⚠️ **وما لا يُعمَّم عليه هذا الحكم** (‏`§⑦`-1): الشقُّ الثاني من الدعوى —
**«وسحب سيولة المشترين بالأعلى»** — **لم يُقَس أصلًا** (نيّةٌ لا عمودٌ في
`grouped`) ⇒ **سكوتٌ لا نفي.**

## ⑤-ز حرّاسٌ وحدودٌ أخرى تُطبَع

- **`V-L4`** (شاهدُ التكامل): استُعيد **‏0 من 91 = 0.0%** — **وصفيٌّ هنا
  بنصّه**، يلزمه تشغيلُ ‏2026 ولا يحكم.
- **`V-N5`** الأداةُ لا ترسل ولا تكتب حالة · **`V-N6`** الجذورُ الاثنا عشر
  بصمتُها مطابقة · **`V-N3`** صفرُ تسريب (الميزةُ من `hist[:i]` وحدَها).
- **حدودُ الصدق `§⑥` من الأداة نفسِها:** إثراءٌ قبل الحدث **لا تنبّؤٌ
  بالمقدار** · **ولا حافّةَ تداولٍ** (بلا وقفٍ ولا تكلفة) · و`CX` **لا يضبط
  القطاع**.
- **`§⑦`-3 تعدُّد المقارنات يتراكم عبر التجارب** — ولا أثرَ له هنا لأن
  النتيجةَ **سالبة**: بونفيروني يحمي من الإيجابيّ الكاذب لا من السالب.

## ⑤-ح ما لم يتغيّر — ولا حرف

- **لم يُخفَّض حدٌّ ولم يُرخَ حارسٌ** بعد رؤية الرقم — `LK1` بحاله و`z` أعلى.
- **صفرُ شحنٍ** بنصّ `§⑨` («ولا تُضاف الميزةُ إلى الفارز ولا إلى أيّ رسالةٍ
  تُرسَل — ولو عبرت») · **والعلمُ `LINK100_NEWLOW` يبقى مطفأً افتراضًا**.
- **لا `LOGIC_VERSION`** · والجذورُ الاثنا عشر بصمتُها مطابقة.
- **و`link100_result.md` لم يُعَد كتابتُه** — قُرئ مرساةً كما نصّ `§⑨`.

---

*التشغيلةُ الحاكمة: <https://github.com/sam72x1/Super_Stocks/actions/runs/35500939991>*
