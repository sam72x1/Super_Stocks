# 💥🔗② `T-LINK100-2` — النتيجة الأولى: **الفرعُ 3 «لا حكم»** (سقوط `V-N1`)

> **العقد:** `link100_2_prereg.md` (مدموجٌ `48d1bfe5` ⟶ `e1775452` **قبل أيّ سطرِ
> أداةٍ وقبل أيّ رقم**) · **الأداة:** ميزةٌ خلف علمٍ مطفأ داخل `link100_probe.py`
> (`LINK100_NEWLOW`) · مدموجةٌ `ee3dd457`.
> **التشغيلة:** [`35487721499`](https://github.com/sam72x1/Super_Stocks/actions/runs/35487721499)
> على `main` (‏8.1 دقيقة · خروج **‏7**).
> **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · ولم يُشحَن شيء.**

---

## ⓪ الحكم في سطر

**`V-N1` سقط في 2023 فأوقف التشغيلةَ قبل أيّ رقمِ ميزة** — الأحداثُ **‏480**
والمنشورُ **‏481**. وبنصّ `§⑤`-3 هذا **الفرعُ 3 «لا حكم»، يُعلَن بعدده ولا
يُفسَّر**. 🔑 **والسببُ مُشخَّصٌ تمامًا ولا علاقةَ له بالميزة الجديدة:
مرجعُ التقسيمات الحيُّ عند المزوّد تغيّر بين التشغيلتين.**

---

## ① التشخيص — كلُّ ما قبل فلتر التقسيم **مطابقٌ بت-بت**

| ‏2023 | المنشور (‏`35163391888` · 09-17) | هذي (‏`35487721499` · 09-20) | الفرق |
|---|---|---|---|
| أيّامُ تداول | ‏250 | ‏250 | **‏0** |
| جُلبت | ‏250 (‏100.0%) | ‏250 (‏100.0%) | **‏0** |
| **أحداثٌ خام** | **‏869** | **‏869** | **‏0** |
| **بعد الطيّ (‏20 جلسة)** | **‏700** | **‏700** | **‏0** |
| **مُستبعَدو التقسيم (‏±3ي)** | **‏160** | **‏161** | **‏+1** ⬅️ |
| بلا تاريخٍ كافٍ | ‏59 | ‏59 | **‏0** |
| بلا شاهدٍ ذاتيّ | ‏84 | ‏84 | **‏0** |
| **تُقاس** | **‏481** | **‏480** | **‏−1** |
| شاهدٌ مقطعيّ `CX` | ‏473 (‏98.3%) | ‏472 (‏98.3%) | ‏−1 |
| شاهدٌ ذاتيّ `CC` | ‏397 | ‏396 | ‏−1 |
| `vol_x ≥ 3` | ‏38.7% | ‏38.8% | ‏+0.1 نقطة |

**وقانونُ البقاء يُغلق التشخيص حسابًا لا ظنًّا** (‏`len(ev) = بعد الطيّ −
مُستبعَدو التقسيم − بلا تاريخٍ كافٍ`):
- المنشور: ‏700 − 160 − 59 = **‏481** ✓
- هذي: ‏700 − **161** − 59 = **‏480** ✓

⇒ **الفرقُ كلُّه يُفسَّر بصفٍّ واحدٍ إضافيٍّ سقط على حارس التقسيم `V-L2`**،
وكلُّ ما قبله — `year_days` · `grouped_day` · `day_events` · `fold_events` —
**أعاد رقمَه المنشورَ بت-بت**. ⇒ **الآلةُ هي الآلةُ نفسُها، والمتغيّرُ مرجعُ
تقسيماتٍ حيٌّ يُنقَّح رجعيًّا عند المزوّد** (‏`splits_of` تقرأ `/v3/reference/splits`).

🔒 **وأن الميزةَ الجديدة ليست السبب ثابتٌ بالبناء لا بالدعوى:** `newlow60_5`
تُضاف **مفاتيحَ** إلى قاموس `daily_feats` ولا تُسقط صفًّا ولا تُغيّر شرطَ
بقاءٍ واحدًا — و`LNA0` يقفل أن العلمَ المُطفَأ يعطي القاموسَ نفسَه بت-بت.

---

## ② ما لم يُقَس — والحارسُ هو الذي حفظه

التشغيلةُ توقّفت **قبل** `n_cells` و`z` و`enrich` ⇒ **صفرُ رقمٍ عن
`newlow60_5` قُرئ**، ولا عن الشبكة التسع. ⇒ **المحورُ ما زال عذراء**،
والتسجيلُ المسبقُ سليمٌ لم يُطَّلَع على شيءٍ منه.

---

## ③ 🔴🔴 والعيبُ في **اختياري أنا للمرساة** — يُنشَر ولا يُطوى

`§③` من العقد **يرفض `rsplit180` مرساةً** ويُعلّل: «**يقرأ تقسيماتٍ حيّةً قد
تتغيّر**» — **ثمّ اختار مرساةً (عددُ الأحداث) تمرّ هي نفسُها عبر فلتر
التقسيمات الحيّ**. ⇒ **سمّيتُ العلّةَ ثمّ وقعتُ فيها في الفقرة نفسِها.**

🧭 **والدرسُ يُعمَّم على المستودع كلِّه: أيُّ مرساةِ هُويّةٍ يجب أن تقع
«فوق» كلّ نداءٍ لمرجعٍ حيّ** — لا تحتَه. **وينطبق أثرًا رجعيًّا على
`U-RAW` في `T-PMUSD`** (‏34.72× · 21.69× · 14.03×): وهي محسوبةٌ على `ev`
**بعد** الفلتر نفسِه ⇒ **معرَّضةٌ للانزياح ذاته**، وقد عبرت يوم 09-17
**ولا ضمانَ أن تعبر غدًا**. (‏لم تُمَسّ بهذا العقد — يُدوَّن فقط.)

---

## ④ ما لم يتغيّر — ولا حرف

- **لم يُخفَّض حدٌّ ولم يُرخَ حارسٌ** بعد رؤية الرقم.
- **العقدُ مدموجٌ فلا يُعدَّل** — والعلاجُ **ملحقٌ مؤرَّخ `§⑬`** كما في
  `T-SUPDEF §⑩`-`§⑫` و`T-PMUSD §⑫` و`T-OPHOLD-2 §⑫`.
- **صفرُ شحنٍ · لا `LOGIC_VERSION` · والجذورُ الاثنا عشر بصمتُها مطابقة.**

---

*التشغيلة: <https://github.com/sam72x1/Super_Stocks/actions/runs/35487721499>*
