# 🚦💵 `T-TC-YIELD` — تسجيلٌ مسبق: **هل الزنادُ الموازي `T-C` يُسلِّم، وهل ما يُسلِّمه يربح؟**

> **مدفوعٌ ومدموجٌ قبل أيّ سطرِ أداةٍ وقبل أيّ رقمٍ حاكم.** أمرُ المالك «سجل المحور»
> (‏2026-09-12) بعد ترشيحي محورَ **التوقيت والتسليم**.
> **بحث/قياس فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · ولا يتحرّك
> `LIQ_TC_ON` ولا `LIQ_TC_USD` ولا `LIQ_TC_PCT` بهذي التجربة.**

---

## ⓪ إعلانٌ يسبق كلَّ شيء — ما رأيتُه قبل أن أكتب هذا العقد

قبل كتابة هذا الملفّ قرأتُ **آخرَ لقطةٍ من `op_entry_state.json`** لأتحقّق أن المادّة
موجودة، ورأيتُ فيها: **‏36 مِرساةً — 19 بـ`R1` و17 موسومةً `T-C`، وكلُّها تحمل `sent`.**

**وهذا يُعلَن ولا يُطوى**، وثلاثةُ قيودٍ عليه:
1. **عدُّ لقطةٍ واحدة**، لا سلسلةً ولا مجتمعًا ⇒ **وصفيٌّ لا حاكم**.
2. **صفرُ رقمِ حصيلةٍ رأيتُه** — لا `exploded50` ولا `R` ولا أيَّ مخرجٍ أماميّ.
   والمعاييرُ أدناه كلُّها على الحصيلة، وهي **غيرُ مُطَّلَعٍ عليها**.
3. **وهو ما استدعى المحورَ أصلًا**، لأنه **يناقض ما نشرناه**: المدوَّن أن الطبقةَ
   كلفتُها «‏≈+4 رسائل/يوم **سقفٌ لا وقوع**» وأن أوّلَ قراءةٍ حيّة وجدت **رمزين
   كُتما كلاهما وصفرَ تسليم**. فإن كان `T-C` اليومَ **نصفَ المراسي وكلُّها تُسلَّم**،
   فأحدُ السطرين بائت — **وهذا بعينه ما يجب أن يُقاس لا أن يُفترَض**.

---

## ① الدعوى والمحور

**المشحونُ حيًّا منذ 2026-09-09:** المِرساةُ تقع بأبكرِ العابرَين من ذراعين —
`R1` (‏3× حجمًا · ‏$30k في ثلاث دقائق · تجاوبٌ 5% وإغلاقٌ في النصف الأعلى)
**أو** `T-C` (سيولةٌ تراكميّةٌ `LIQ_TC_USD` مع إغلاقٍ `LIQ_TC_PCT`% فوق إغلاق الأمس).

**والمحورُ هنا ليس الزنادَ بل ما بعده — شقّان مفصولان عمدًا:**

- **(أ) التسليمُ والكلفة:** كم مِرساةً يضيف `T-C` فعلًا، وكم منها **يَعبُر بوّابةَ
  المضارب فيصل المالكَ**، وكم رسالةً/يوم يُكلّف؟
- **(ب) الحصيلة:** هل ما يُسلِّمه `T-C` **يربح** — أم يضيف مراسيَ أضعفَ فيُخفِّف
  جودةَ ما يصل؟

## ② لماذا محورٌ جديدٌ لا إعادةَ فتح — بحثُ الأرشيفين

`T-C-TRIGGER` (‏2026-09-07، `tc_trigger_prereg.md`) **استوفى عقدَه** وقاس:
التكرارَ خارج العيّنة · التكامليّةَ (`R1` و`T-C` **متقاطعتان لا متداخلتان`) ·
الالتقاطَ `TC5` ‏×1.40/×1.27 و`TC6` **‏+10.5/+7.9 نقطة** · و`C-NULL` (نزعُ شرط
الارتفاع يضرب الرسائلَ ‏×13.8).

🔑 **وكلُّ ذلك «رسائلُ في باكتيست» — والمُعلَن بنصّه في `CLAUDE.md`:**
«**والمقيسُ رسائلُ لا تسليم** و**لا ربحيّةَ قِيست**».
⇒ **التسليمُ (بوّابةُ المضارب) والحصيلةُ (`R`/الانفجار) صفرُ قياسٍ حتى اليوم.**

⚠️ **وسببٌ ثانٍ يجعل إعادةَ القياس واجبةً لا تكرارًا:** الالتقاطُ قِيس على تراكمٍ
**من فتح البريماركت**، والحيُّ يتراكم **من أوّل رؤيةٍ للسهم** ⇒ المدوَّن نفسُه يقول
**«لا وعدَ بتكرار ‏+10.5/+7.9»** ⇒ الرقمُ الحيُّ **غيرُ معروف**.

⛔ **وما لا يُعاد فتحُه هنا:** تعريفُ الزناد · عتباتُه · وأفضليّةُ `T-C` على `R1`
في **الالتقاط** (‏محكومةٌ في `T-C-TRIGGER`). المقيسُ **ما بعد المِرساة فقط**.

---

## ③ الأذرع — كلُّها إعادةٌ على المادّة نفسِها

| الذراع | التعريف | الدور |
|---|---|---|
| `A0` | `R1` وحدَه | الأساس (ما قبل 09-09) |
| `A1` | `R1 ∪ T-C` | 🥇 **الحاكمة** (المشحون) |
| `A-TC` | `T-C` وحدَه | الشريحةُ الإضافية |

## ④ الضبطُ الحاكم — **لا وصفيٌّ** (تطبيقُ درسَي 2026-09-11)

🔴 **درسُ `T-PMGATE` نصًّا:** «**لا تُقاس بوّابةُ توقيتٍ بعد اليوم بلا ضبطِ زخمٍ صرفٍ
حاكمًا**» — لأن أيَّ بوّابةٍ تقسم سهمًا متحرّكًا **تفصل بعضَ الشيء آليًّا**.
🔴 **ودرسُ `T-WAIT-23W`:** كلُّ ذراعٍ تُغيّر **عدّةَ** ما تلتقطه **تربح أو تخسر آليًّا**
ما لم يُطابَق العدد.

⇒ **ثلاثةُ ضبطٍ حاكمة، تُحسَب من المادّة نفسِها:**

| الضبط | التعريف | السؤال الذي يحسمه |
|---|---|---|
| `C-MOM` | الارتفاعُ `LIQ_TC_PCT`% وحدَه — **بلا** شرط السيولة التراكميّة | هل مرجعُ الـ$100k **يضيف** على الزخم المجرَّد؟ |
| `C-USD` | السيولةُ التراكميّة وحدَها — **بلا** شرط الارتفاع | أيُّ نصفَي الشرط يحمل الأثر؟ |
| `C-CNT` | **مطابَقُ العدد**: يأخذ من غير مراسي `R1` **العددَ نفسَه** الذي أضافه `T-C`، مختارًا **بالأسبقيّة الزمنيّة وحدَها** (أعمى عن شرط `T-C`) | هل الحافّةُ **قاعدةُ `T-C`** أم مجرّدُ «مراسٍ أكثر»؟ |

**والثلاثةُ حاكمةٌ بنصّ هذا العقد** — لا تُقرأ «وصفيّة» بعد النتيجة.

---

## ⑤ المقياس

**يُعاد استعمالُ المنشور بالاسم لا يُبنى ثانيةً** (قاعدةُ المشروع): تسميةُ المُخرَج
من `tierlink_probe` — **`exploded50`** حاكمًا، و`exploded100` و`kasih30_cut` وصفيَّين.
والمرجعُ **`anchor_price`** (سعرُ كرت المِرساة) لا إغلاقُ الأمس.

- **شقُّ (أ):** المِراسي/اليوم · نسبةُ **`sent`** لكلّ ذراع · الرسائلُ المُسلَّمة/اليوم.
- **شقُّ (ب):** `exploded50` على **المُسلَّم فقط** (‏فما لم يصل المالكَ لا يُحتسَب له
  ولا عليه) ‏+ فاصلُ بوتستراب **عنقوديٍّ بالرمز** (‏5,000 إعادة · بذرة `20260912`).

---

## ⑥ المعايير والأرضيّة — **تُثبَّت الآن**

- **`TY1`** (حاكم): `A1 − A0` في `exploded50` **موجب** وفاصلُه لا يلمس الصفر.
- **`TY2`** (حاكم): `A-TC − C-MOM` **موجب** وفاصلُه لا يلمس الصفر.
- **`TY3`** (حاكم): `A-TC − C-CNT` **موجب** وفاصلُه لا يلمس الصفر.
- **`TY4`** (كلفة، يُطبَع ولا يُسقِط وحدَه): الرسائلُ المُسلَّمة/اليوم لـ`A1` مقابل `A0`.
- **الأرضيّة `V-Y1`:** **‏120 مِرساةً مُسلَّمةً محسومة** بينها **‏40 على الأقلّ** من
  `A-TC` — ودونها **الفرعُ 3 «لا حكم»** ولا يُقرأ شيءٌ من `TY1`-`TY3`.

## ⑦ الفروعُ الثلاثة

1. **`TY1` و`TY2` و`TY3` الثلاثةُ تعبر** ⇒ الطبقةُ **مُوصًى بإبقائها** بالدليل.
2. **واحدٌ منها يسقط** (والأرضيّةُ مبلوغة) ⇒ **لا تُوصى**، والقرارُ للمالك:
   الإبقاء أو `LIQ_TC_ON=False`. **وسقوطُ سنةٍ/شريحةٍ واحدةٍ ليس «لا حكم» بل الفرعُ 2.**
3. **الأرضيّةُ لم تُبلَغ** ⇒ **«لا حكم»** بخروجٍ مميَّز، **وتاريخُ إعادةِ قراءةٍ
   مكتوبٌ في النتيجة** — ولا يُترَك وعدًا بلا موعد (‏درسُ «حدُّ صدقٍ يَعِد ولا يُنفَّذ
   يتحوّل إلى سطرٍ يكذب»).

## ⑧ الحرّاس

- **`V-Y2`:** الأداةُ **قراءةٌ فقط** — صفرُ كتابةِ حالة · صفرُ إرسال · ولا سرَّ تلغرام
  في الـworkflow (‏فالإرسالُ مستحيلٌ بنيويًّا لا ممنوعٌ بالنيّة).
- **`V-Y3`:** الأذرعُ والضبطُ **من المادّة نفسِها وبالنافذة نفسِها** — لا جلبةَ
  إضافيّة لذراعٍ دون أخرى (‏درسُ `T-CLIFF`: الميزانيّةُ الفارقة تُفبرك أثرًا).
- **`V-Y4`:** `A0` يجب أن يُعيد **عددَ مراسي `R1` المقروءَ من الوسم** بت-بت
  ⇒ تحقّقٌ لا دعوى.
- **`V-Y5`:** حتميّة — التشغيلةُ نفسُها تُعطي الرقمَ نفسَه (‏بذرةٌ مثبَّتة · وترتيبٌ
  ثانويٌّ صريحٌ `(رمز·وقت)` فلا يسقط الفرزُ إلى مقارنةِ قواميس — **عطبُ `T-PMGATE`
  حرفيًّا**).
- **`V-Y6`:** `LIQ_TC_ON`/`LIQ_TC_USD`/`LIQ_TC_PCT` **لا تُضبَط في الذاكرة** داخل
  الأداة (‏بالـAST: صفرُ إسنادٍ إلى `CONFIG` ولا إلى ثوابت الوحدة) — **درسُ `RKA10`**.
- **`V-Y7`:** `Super_stock.py` **بت-بت** مع `origin/main`، وإلّا خروجٌ قبل أيّ قياس.

## ⑨ التنبّؤات — **تُنشَر مكذَّبةً ولا تُحذَف**

- **`Q1`:** `TY2` هو الساقط — الزخمُ المجرَّد يأكل مرجعَ الـ$100k (‏نفسُ ما وقع
  في `T-PMGATE` و`T-WAIT-23W`).
- **`Q2`:** نسبةُ `sent` **فوق 90% للذراعين** ⇒ **بوّابةُ المضارب لا تكتم عند
  المِرساة** — وهذا **يُكذّب قراءتي أنا** في فحص الدخان («كُتما كلاهما»).
- **`Q3`:** `A-TC` **أضعفُ** من `A0` في `exploded50` (‏شرطُه أرخى فيلتقط أكثر).
- **`Q4`:** الأرضيّةُ **لا تُبلَغ** في القراءة الأولى ⇒ الفرعُ 3 مرجَّح.
- **`Q5`:** `C-USD` أضعفُ من `C-MOM` (‏شرطُ الارتفاع يحمل العمل — اتّساقًا مع `C-NULL`).

## ⑩ حدودُ صدقٍ مكتوبةٌ قبل أيّ رقم

1. **المدى ثلاثُ جلساتٍ فقط** (‏09-09 ⟶ 09-11) ⇒ **الأرجحُ سلفًا «لا حكم»**،
   والمعيارُ **لا يُرخى** لبلوغه.
2. **الاحتمالُ الأسبقُ الفشل:** ثلاثُ تجاربِ مُرتِّبٍ وأربعُ تجاربِ دخولٍ وبوّابةُ
   توقيتٍ — **كلُّها لم تُوصَ**.
3. **التاريخُ يحمل ما كتبه العاملُ الحيّ فقط** — جلسةٌ سقط فيها الجوب لا أثرَ لها
   ⇒ **أرضيّةٌ لا سقف**.
4. **`exploded50` مقياسُ حركةٍ لا ربحيّةَ محفظة** — لا وقفَ ولا حجمَ مركزٍ فيه
   ⇒ **لا يُقرأ توقّعًا ماليًّا**.
5. **الشقُّ (أ) وصفيٌّ بنصّ العقد** (‏ثلاثُ جلسات) — يُطبَع ويُصحِّح سطرًا بائتًا
   إن خالفه، **ولا يدخل أيَّ معيار**.

## ⑪ النطاق

بحث/قياس · **صفرُ مسٍّ بالإنتاج** · لا `LOGIC_VERSION` · لا `force_renew` ·
ولا تتحرّك عتبةٌ واحدة. النتيجةُ في **`tc_yield_result.md`** بجانب هذا العقد لا فوقه.

## ⑫ شرطُ إغلاقٍ مكتوبٌ قبل النتيجة

**سقوطُ `TY2`** (‏أي: مرجعُ السيولة التراكميّة **لا يضيف** على الزخم المجرَّد)
⇒ **يُغلَق محورُ «شرطُ السيولة التراكميّة مرجعًا للمِرساة» إغلاقًا مُنفَّذًا لا
مكتوبًا** — ولا يُعاد فتحُه إلّا بمصدرٍ ثانٍ للدعوى ‏+ تسجيلٍ جديد ‏+ إذنِ المالك.
