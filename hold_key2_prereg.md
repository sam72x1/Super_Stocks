# 🕰️🥇② `T-HOLD-KEY-2` — «سجّل 2» · **والبِركةُ تُبنى بـ`select_top` لا بكون المرشّحين**

> **تسجيلٌ مسبقٌ يُدفَع قبل أيّ سطرِ كودِ أداة وقبل أيّ رقمٍ من `D_full`.**
> أمرُ المالك (2026-08-27، بعد حكم `T-HOLD-KEY`): «**سجّل 2 · طبع الكامل**».
> 🔒 بحثٌ/قياسٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · ولا مسَّ
> `alert_rank` ولا أرقامِ المالك (‏`WATCHLIST_SIZE`=15 · `READY_HOLD`=3 ·
> `ALERT_CAP`=8 · `ALERT_W`=40) — تُقرأ **بالاسم** ولا تُبدَّل.

---

## ⓪ لماذا الآن — ولماذا هي **تثبيتٌ لا اكتشاف**

`T-HOLD-KEY` (‏`33110555754`) عبرت ستَّ بوّاباتٍ ثم سقطت على **`HV4`**: وسيطُ
الجاهزين/جلسة في `D_live` **‏44.0** والنطاقُ المسجَّل **‏[8، 40]**. والسببُ
**سطرٌ في عقدي أنا** (‏`hold_key_prereg §③-1`): وصفتُ المصدرَ الأوّل بما
**يُنتجه** `analyze_ticker` — وهو **كونُ المرشّحين** المقيسُ حيًّا عند
**‏≈109/يوم** — بينما `press_radar.build_pool` يقرأ **`wl["stocks"]`** = **الخمسةَ
عشرَ الناجين من `select_top`**. ⇒ نسبةُ 109 إلى 15 هي نفسُها نسبةُ 44 إلى 15.9.

🔴 **والدائريّةُ مُعادةٌ صريحًا ولا تُطوى:** مفتاحُ `hold_sessions` اختير **بعد**
رؤية أرقام `A3` في `T-PRESS-RANK`، وعلى **السنوات الثلاث نفسِها** ⇒ **هذي تجربةُ
تثبيتٍ لا اكتشاف**، ونتيجتُها **لا تُسمّى «خارج العيّنة» أبدًا** مهما كان رقمُها.

⚖️ **وما لا يُعاد النقاشُ فيه:** حكمُ `T-HOLD-KEY` **«لا حكم»** قائمٌ ومنشور،
و`hold_key_result.md` **لا يُعدَّل بأرقام هذي التجربة** — تُنشَر نتيجةُ `2`
بملفٍّ مستقلّ.

---

## ① المتغيّرُ الوحيد — **سطرٌ في بناء البِركة** (وثانٍ يلزمه بنيويًّا، مُعلَنٌ الآن)

**التغييرُ المقصود:**

| | `T-HOLD-KEY` (ساقطة) | 🥇 `T-HOLD-KEY-2` |
|---|---|---|
| المصدرُ ① | رمزٌ **اجتازه `analyze_ticker`** خلال `MEMORY_DAYS` | رمزٌ **يُسلّمه `select_top`** بسعة `WATCHLIST_SIZE` عند إحدى جلسات الشبكة، خلال `MEMORY_DAYS` |

**والترتيبُ بـ`Super_stock.rank_key` والاختيارُ بـ`Super_stock.select_top`
كلاهما بالاسم** — لا نسخةَ ولا مكافئ.

🔑 **وإعادتُنا أمينةٌ في موضعٍ لم يكن متوقَّعًا:** الإنتاج يرتّب ويختار **قبل**
`enrich` (‏`Super_stock.py:11711` ثم `select_top`) ⇒ `rank_key` يعمل على مُخرَج
`analyze_ticker` **الخام** كما نفعل بالضبط، **و`h4_confirm` صفرٌ للجميع وقتَ
القرار في الإنتاج أيضًا** (قرارٌ موثَّق `D7`) ⇒ **صفرُ انحرافٍ في هذا الشقّ.**

🔴 **والتغييرُ الثاني اللازمُ بنيويًّا — يُعلَن الآن لا بعد الأرقام:** شبكةُ `Q5`
تصير **بتقويمٍ مشترك** (كلُّ `Q5_STRIDE` جلسةٍ من تقويم الكون داخل النافذة) بدل
**خطوةٍ موضعيّةٍ لكلّ رمز**. والسببُ إلزاميّ: **`select_top` مقطعيّ** (يقارن
رموزًا في **اليوم نفسِه**) ويستحيل تطبيقُه على رموزٍ تُعايَن كلٌّ في تواريخَ
أخرى. **وحارسُه هو هو:** `HV8` يشترط توافقَ `prev_q` المشتقّ مع
`press_radar.prev_qualified` **الحقيقيّة** بنسبة **‏90% فأعلى** وإلّا **خروج 3**.

🔒 **وما عداهما بت-بت** (يُستورَد **بالاسم** من `hold_key_arms` المجمَّدة ومن
الإنتاج): الأذرعُ الأربع · `deliver_prod` · `r_of` · `boot_ci` · `ready_rows` ·
`pv0_gate` · `order_b0/b1/b2/b3` · `hv0_bridge` · `hv8_agree` · `budget_probe` ·
`mover_days` · `MEMORY_DAYS` · `POOL_CAP` · `ALERT_W` · `READY_HOLD` ·
`ALERT_CAP` · `EXPLOSION_PCT` · `LIVE_MED_MIN/MAX`.

---

## ② المجتمعان — والحاكمُ واحد

- 🥇 **`D_live2`** (الحاكم) — الصفوفُ الجاهزةُ المقصورةُ على البِركة المُعادِ
  بنائها: **(‏① `select_top` خلال `MEMORY_DAYS`) أو (② متحرّكٌ بعتبة
  `EXPLOSION_PCT` خلال `MEMORY_DAYS`)** ثم **سقفُ `POOL_CAP`**.
- 📎 **`D_full`** — كونُ `T-PRESS-RANK` نفسُه. **يُطبَع دائمًا و«قبل» بوّابة
  الكثافة** (أمرُ المالك «طبع الكامل») بأذرعه الأربع وفواصلِ ثقتها.

🔴 **و`D_full` وصفيٌّ لا يحكم — مُعادٌ حرفيًّا من `hold_key_prereg §⑤`:**
⛔ **لا يُنقَل الحكمُ إلى `D_full` إن سقط على `D_live2`**، ولا يُعتمَد منه شيء،
ولا يُقاس بمعايير `§④`. وهو يسدّ فجوةً **أعلنتُها بنفسي** في
`press_rank_result §④` («‏`A3` تتفوّق **وبلا فاصلِ ثقة**») — **سدُّ فجوةِ نشرٍ
لا اكتشافُ حافة.**

---

## ③ الأذرع — أربعٌ ولا خامسة (‏مُعادةٌ من `hold_key_prereg §④`)

- **`B0` المرجع** — `press_radar.alert_rank` بالاسم و**`prev_q` محيًى** ‏+ **قاعدةُ
  عددِ كروت الإنتاج حرفيًّا** (المستيقظون حصرًا بلا إكمالٍ من الهادئين).
- 🥇 **`B1` الحاكمة** — `hold_sessions` تنازليًّا **مفتاحًا أوّلَ** ثم ترتيبُ `B0`
  فاصلَ تعادلٍ (فرزٌ مستقرّ).
- **`B2`** شاهدُ الصدفة — 200 بذرة.
- **`B3`** حفظٌ صرف (‏= `A3`) — **وصفيٌّ لا يُرقّى** (بنصّ `press_rank_prereg §⑦`:
  لا تُرقّى شاهدةُ ضبط).

⛔ **ولا تُضاف ذراعٌ بعد الأرقام** ولا يُبدَّل تعريفُ ذراعٍ.

---

## ④ المقياسُ والمعايير — **مُعادةٌ حرفيًّا بلا تحريكِ رقم**

**المقياسُ الحاكم:** صافي `R` المُسلَّم لكلّ جلسة **على `D_live2`**
(‏فائزة ‏+`r_win_value()` · خاسرة ‏−1 · `no_fill`/`open` صفرٌ **وتستهلك كرتًا**).

**تُعتمَد `B1` إن استوفت الأربعةَ معًا:**

1. **`B1 − B0` موجبٌ في السنوات الثلاث** بلا انقلاب.
2. **الفاصلُ 95% للفرق المجمَّع لا يلمس الصفر** (بوتستراب عنقودُه الجلسة ·
   ‏10,000 سحبة · بذرة **54321**).
3. **`B1` فوق المئين 95 لشاهد `B2` في سنتين من ثلاث.**
4. **الأرضية:** أربعون جلسةً فأكثر **و150 كرتًا مُسلَّمًا** فأكثر في كلّ سنة.

**والحكمُ يُكتب مرّةً واحدة** عند اكتمال السنوات الثلاث. ⛔ ولا تُعاد تشغيلةٌ
بمعيارٍ مبدَّل، **ولا يُحرَّك النطاقُ `[8، 40]` مهما جاء الرقم**.

---

## ⑤ بوّاباتُ الصلاحية — **تُنفَّذ في الكود برموزِ خروجها**

| البوّابة | الشرط | عند السقوط |
|---|---|---|
| **`HW2`** التجميد | `hold_key_arms.py` و`press_rank_arms.py` **byte-identical** لِما نُشرت به أرقامُهما (‏CAP15) | **خروج 3** |
| **`HV1`** تكامل | `HOLD3` محسومةً **‏1660/1857/1854** وفائزةً **‏351/333/341** | **خروج 3** |
| **`HV0`** جسرٌ | `A0` بقاعدةِ كروت الأمس تُعيد **‏+0.3102 · +0.7353 · +1.9946** | **خروج 3** |
| **`HV8`** الشبكة | توافقُ `prev_q` مع `prev_qualified` الحقيقيّة **‏90% فأعلى** | **خروج 3** |
| **`HV7`** التغطية | **‏95% فأعلى** · والمفقودُ يُسمّى بعدده | **خروج 3** |
| **`HV2`** الإحياء | نصيبُ `prev_q` الصادق **بين صفرٍ ومئةٍ حصرًا** | **خروج 4** |
| **`HV3`** الميزانية | إسقاطٌ من عيّنةٍ مقيسة **قبل** المشي · تجاوزُ السقف يقف | **خروج 6** |
| 🆕 **`HW1`** أثرُ `select_top` | نصيبُ `live_q` **ينخفض ‏30% فأكثر** عن نظيره في `T-HOLD-KEY` (‏2023: **‏9181** صفًّا) — وإلّا فالتغييرُ **`no-op`** | **خروج 4** |
| **`HV4`** الكثافة | وسيطُ الجاهزين/جلسة في `D_live2` **بين 8 و40** | **خروج 4** |
| **`HV5`** التفرّق | `B1` تُسلّم كرتًا لا تُسلّمه `B0` في كلّ سنة | **خروج 4** |
| **`HV6`** الدِدوب | صفرُ رمزٍ يُسلَّم مرّتين داخل `REALERT_DAYS` في أيّ ذراع | **خروج 3** |

🔒 **وحارسُ العلم الميّت:** جدولُ أذرعٍ متطابقٌ بت-بت ⇒ بصمةُ `no-op` **تُعلَن
ولا تُفسَّر** (سابقةُ `BT_CANDLE`).

🔴 **و`D_full` يُطبَع قبل `HV4`** — فطبعُه أمرُ المالك، وسقوطُ الكثافة **يجب
ألّا يكتمَه** كما كتمه في التشغيلة السابقة.

---

## ⑥ التنبّؤات — تُنشَر مكذَّبةً كما تُنشَر محقَّقة

- **`H2-P1`** (الأثقل، ويراهن **ضدّ** الإصلاح): أرجّح أن تعبر `D_live2` بوّابةَ
  `HV4` بوسيطٍ **بين 10 و20** — لأن نسبةَ 15 إلى 109 تُنزل الـ44 إلى ‏≈6،
  **ومصدرُ المتحرّكين يرفعها** إلى جوار الرقم الحيّ 15.9.
- **`H2-P2`**: `HW1` تعبر بانخفاضٍ **‏70% فأكثر** في `live_q` (لا 30% فقط).
- **`H2-P3`**: `B1 − B0` على `D_live2` **موجبٌ في الثلاث** (المعيار ①) **ويسقط
  في ②** — لأن السقفَ يبنّد أقلَّ عند الكثافة الحيّة فيضيق أثرُ الترتيب.
- **`H2-P4`**: على `D_full` تتفوّق `B3` على `B0` بفارقٍ **يزيد على ‏+0.5R/جلسة**
  (استمرارًا لِما قِيس في `T-PRESS-RANK`: ‏+0.817R لـ`A3`).
- **`H2-P5`**: `B0` على `D_live2` **فوق** وسيط `B2` — بعكس ما وقع على `D_full`.
- **`H2-P6`**: نصيبُ `prev_q` الصادق يبقى **بين 10% و60%** (كما قِيس: 41.4%).
- **`H2-P7`**: `HV8` تعبر بنسبةٍ **‏90% فأعلى** رغم تبدّل الشبكة إلى التقويم
  المشترك (وإن سقطت فالتشغيلةُ تقف ولا تُفسَّر نتيجتُها).

---

## ⑦ سقفُ النجاح — مثبَّتٌ سلفًا

**أقصى ما يُنتجه النجاحُ الكامل: اقتراحٌ للمالك ‏+ سطرُ عرضٍ تشخيصيّ.**
⛔ **وممنوعٌ مهما كانت الأرقام:** مسُّ `press_radar.alert_rank` · أو `READY_HOLD`
· أو `ALERT_CAP` · أو `ALERT_W` · أو `WATCHLIST_SIZE` · أو أيّ بوّابةٍ من
`M1`-`M14` · أو الدخول/الوقف/الأهداف · أو `kasih_scan.resolve` · أو صيّاد
المقسّم. **وأيُّ اعتمادٍ قرارُ مالكٍ صريح.**

---

## ⑧ حدودُ صدقٍ — **تُكتَب قبل الأرقام وتُقرأ معها**

1. 🔴 **الدائريّة** (‏⓪): تثبيتٌ لا اكتشاف.
2. 🔴 **`wl["stocks"]` الحيّة قائمةٌ مستمرّة** (تجديدٌ أسبوعيّ ‏+ تعبئةٌ يوميّة
   بـ`fill_picks`) لا `select_top` يوميّ مستقلّ ⇒ إعادتي **تطابق المقدارَ
   والاختيارَ لا الهويّة**.
3. 🔴 **`M13`/`M14` غيرُ قابلتين للإرجاع** (شورت FINRA اليوميّ · الفلوت) ⇒
   الخمسةَ عشرَ عندي **أوسعُ قليلًا** من الحيّة ⇒ الكثافةُ المقيسة **أعلى لا
   أدنى** ⇒ **قراءةٌ متحفّظة تجاه `HV4`**.
4. **المصدران غيرُ القابلَين للإرجاع** (خططُ الارتداد · «شُطب حديثًا»)
   **يُنقصان** البِركة — بالاتّجاه المعاكس لـ(3).
5. **الشبكةُ خطوتُها 5** ⇒ يومُ تأهّلٍ بين نقطتَي شبكةٍ قد يفوت ⇒ **نقصانٌ
   محافظ** (وهو نفسُ ما يفعله `prev_qualified` الإنتاجيّ).
6. **إسقاطُ `HV3` قِيس متفائلًا بـ‏≈35%** في التشغيلة السابقة (‏38.6 مقابل ‏≈52
   دقيقة) ⇒ السقفُ يُقرأ بهامش، وعيّنةُ 40 رمزًا لا تمثّل ذيلَ الثقال.
7. **لمسُ هدفٍ لا تنفيذٌ** — كلُّ أرقام `R` هنا بلا انزلاق (‏سابقةُ `T-SLIP`).
8. **انحيازُ البقاء** قائمٌ في اللقطات الثلاث كما في كلّ تجاربنا.
9. **‏`D_full` وصفيّ** — ولو جاء رقمُه أقوى، **لا يُعتمَد ولا يُقاس بمعايير `§④`**.

---

## ⑨ ملحقٌ مؤرَّخ — إقراراتٌ قبل أيّ رقم (2026-08-27)

1. **`hold_key_arms.py` تُجمَّد** (‏CAP15): أرقامُها المنشورة (‏44.0 · 41.4% ·
   96.0% · 612ms · 1660/351 · +0.3102) محسوبةٌ عليها، فتبديلُها يُبطل المقارنة ⇒
   الأداةُ الجديدة **ملفٌّ مستقلّ يستوردها بالاسم**.
2. **`Q5` بالتقويم المشترك** — أُعلن في `§①` **قبل** أيّ رقم، وسببُه بنيويٌّ
   (مقطعيّةُ `select_top`) لا تحسينيّ.
3. **صفوفُ الشبكة تحفظ الحقولَ التي يقرؤها `rank_key` وحدَها** (‏`symbol` ·
   `readiness` · `score` · `rr` · `price` · `tranches` · `h4_confirm`) — لا
   نسخةَ منطقٍ ولا مفتاحَ مخترَع، والترتيبُ بـ`rank_key` بالاسم.
4. **`classify_tier` لا يُعاد تطبيقُه** لأن `analyze_ticker` يطبّق حدَّ النواقص
   بنفسه (`Super_stock.py:3420`) ⇒ مُخرَجُه **هو** مجموعةُ المؤهَّلين.
5. **الأرقامُ المنشورةُ في `press_rank_result.md` و`hold_key_result.md` لا
   تُعدَّل** بنتيجة هذي التجربة — تُنشَر في `hold_key2_result.md`.
6. **وإن سقطت `HW1`** (الإصلاحُ بلا أثر) فالنتيجةُ **«عطبُ وصفةٍ لا نتيجة»**
   وتُعلَن كذلك، ولا يُفسَّر جدولُ أذرعٍ متطابق.
7. **وإن سقطت `HV4` ثانيةً** فالمحورُ **يُغلَق** ولا يُفتَح بتسجيلٍ ثالث إلّا
   بأمرِ مالكٍ صريحٍ ومحورٍ مختلفٍ كلّيًّا — **لا بتحريك النطاق.**
