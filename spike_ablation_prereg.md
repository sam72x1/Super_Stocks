# 🧪 تسجيلٌ مسبق — `T-SPIKE-ABLATION` (المرحلة صفر)

> **مدفوعٌ قبل أيّ رقم · ‏2026-08-05 · بأمر المالك «شغّل المرحلة صفر · سوّ كل شي
> يعطينا أفضل نتيجة حتى لو ياخذ وقتًا طويلًا جدًّا وقدرة».**
> 🔒 بحث/قياس فقط · لقطةٌ مجمَّدة إلزامية · صفر مسّ إنتاج · الجذور التسعة byte-identical.

---

## ① السؤال — واحدٌ فقط

**هل ظرفُ الكاتالوج أحدَ عشر معيارًا، أم معيارٌ واحد يلبس أحدَ عشر اسمًا؟**

سببُه مقيسٌ سلفًا (`envelope_causality_verdict.md` §②، وتحقّقتُ من أربعِ حقائقه
بنفسي): حافّةُ `best_spike ≥ 98.94` مقابل **‏60** إنتاجًا · و`spike_info` تعريفُها
«أكبرُ مكسبٍ في **≤20 جلسة عبر كلّ التاريخ**» ⇒ الشرطُ حرفيًّا «**تضاعف مرّةً في ≤20
جلسة**» · **وقاعدةُ عضويّة الكاتالوج (`explosion_index`) هي الحدثُ نفسُه بموضعٍ زمنيّ
آخر** ⇒ الظرفُ يُعاير على الحدث ثم يشترطه · والسجلُّ الحيّ: وسيط `best_spike`
**‏312.7%** و**‏71 من 109 فوق 200%**.

---

## ② لماذا هذي المرحلة **محصَّنةٌ** من كل ما أسقط التصاميم الأربعة

لا وسمَ مستقبليًّا · لا نافذةَ أماميّة (‏`fwd`) · لا تعبئةَ ولا صفقة · **ولا سببيّةَ
مطلوبة أصلًا** — فهي **وصفٌ للقاعدة لا تنبّؤٌ بها**. ⇒ لا يمسُّها: استحالةُ السببيّة
الزمنيّة · انحيازُ البقاء · تشويهُ التقسيمات · ولا انعدامُ القوّة (‏`MDE`).
⚠️ **ولذلك مُخرَجُها ليس دليلَ ربحيةٍ ولا يُقرأ كذلك** — هو **بنيةُ القاعدة**.

---

## ③ الأذرع — مجموعاتٌ جزئيّة، بصفر منطقٍ جديد

`envelope_scan.decide` يشترط وجودَ **معيارٍ واحد** على الأقلّ، و`inside_envelope`
**تتخطّى كلَّ مفتاحٍ غائب** ⇒ الذراعُ = مجموعةٌ جزئيّة من **ملفّ الحوافّ نفسه**،
ويُحكَم عليها بـ`inside_envelope` **الإنتاجيّة نفسها**.

**الرئيسة (مُثبَّتةٌ قبل الرقم):**
- `S1` = `{best_spike}` — المحورُ المشتبَه به وحده.
- `S2` = `{price, best_spike}` — «معيارٌ واحد + أرضيةُ سعر».
- `S5` = `S2 ∪ {drop_pct, base_range}` — يضيف **فقط** المحورَين الحاملَين بصمةَ تشويه
  التقسيم ⇒ يفصل «حافّة سوق» عن «فلترةِ أثرِ تقسيم».
- `S12` = الظرف الكامل (‏11 معيارًا) = ذراع `E1` المسجَّلة.

**والشاملة (لأن المالك أذِن بأقصى جهد — وكلفتُها صفرُ نداءٍ إضافيّ):**
- **`ALONE[c]`** لكلّ معيارٍ من الأحد عشر: الذراع `{c}` وحدها ⇒ **قوّةُ الإقصاء
  المنفردة** لكلّ محور.
- **`LOO[c]`** لكلّ معيار: `S12 ∖ {c}` ⇒ **الإسهامُ الحدّيّ** — كم يُقصي هذا المحور
  **فوق** العشرة الباقية. وهو المقياسُ الذي يفرّق «معيارًا يعمل» عن «معيارٍ مكرّر».

🔑 **والكلفةُ صفرٌ إضافيّة:** الغالي هو `measure` (نداءُ `analyze_ticker` المُرخى)،
ويُنادى **مرّةً واحدة لكل (رمز، جلسة)**؛ وكلُّ الأذرع تُختبَر على **الصفّ نفسه**.

🔒 **و`chase_ok` (‏D11) و`M13`/`M14` مُستبعَدةٌ من المرحلة صفر ويُصرَّح بذلك:** تُطبَّق
**بالتساوي** على كل الأذرع فتُختصر من النسبة `w` رياضيًّا، وإدخالُها يعني نداءَ شبكةٍ
لكل رمز (‏محرَّمٌ على الكون الكامل). ⇒ **`w` دقيقٌ بلا نقصان.**

---

## ④ المُخرَج والقراءة — **مثبَّتةٌ قبل الرقم ولا تُبدَّل بعده**

`w = |S12| / |S2|` = «النسبةُ الباقية بعد أن يُقصي العشرةُ الباقية ممّا يقبله المحورُ
الواحد + أرضيةُ السعر».

| `w` | القراءة **الملزِمة** |
|---|---|
| **‏≥ 0.90** | العشرةُ **زينة** ⇒ الظرفُ **معيارٌ واحد يلبس أحدَ عشر اسمًا** ⇒ **يُغلَق محورُ الظرف**، والقرارُ الحقيقيّ هو موقفُنا من «المضاعِف المتسلسل» |
| **‏≤ 0.50** | العشرةُ تُقصي **نصفَ** ما يقبله المحورُ الواحد ⇒ للظرف **مضمونٌ مستقلّ** ⇒ تُقترَح المرحلةُ الأولى (بمقياس `D` لا `Δ_P`) |
| بينهما | **«لا حكم»** — يُنشَر الرقمُ وصفيًّا ولا يُبنى عليه قرار |

**ومقياسٌ مساند مُثبَّتٌ الآن:** `w1 = |S12| / |S1|` (بلا أرضية السعر) — يُنشَر ليُعرَف
هل الأرضيةُ هي الفاعلة أم `best_spike`.

---

## ⑤ حرّاسُ الصلاحية — كلٌّ منها **يُوقِف** لا يُطبَع فقط

| # | البطلان | الكشف |
|---|---|---|
| **A1** | **تداخلٌ منقوض**: `|S12| > |S5|` أو `|S5| > |S2|` أو `|S2| > |S1|` — والتداخل محتومٌ بالبناء (`inside_envelope` عطفٌ منطقيّ) ⇒ نقضُه = عطلُ أداة | خروج **4** |
| **A2** | **علمٌ خامل**: `|S1| == |S12|` **و**‏`|ALONE[c]|` متساوية للأحد عشر ⇒ الأذرعُ لا تُطبَّق فعلًا | خروج **2** |
| **A3** | **حوافٌّ غيرُ آليّة**: وسمُ `envelope_p90.json` ≠ «مُخرَجٌ آليّ» أو بصمتُه ≠ `2dc8c7afb0db` | خروج **5** |
| **A4** | **تسرّبُ `CONFIG`**: بصمةُ `RELAX_ALL` قبل/بعد لا تتطابق | خروج **7** |
| **A5** | **تغطيةٌ ناقصة**: قِيست أقلُّ من **‏60%** من رموز اللقطة بلا سببٍ مسمّى | خروج **6** |
| **A6** | **مقامٌ صفريّ**: `|S2| == 0` ⇒ لا نسبة | خروج **3** |

---

## ⑥ ثوابتُ التشغيل — مجمَّدةٌ الآن

| | |
|---|---|
| اللقطة | `frozen-dataset` لتشغيلة **`30598885456`** (as-of 2026-07-31) |
| الحواف | `envelope_p90.json` · بصمة **`2dc8c7afb0db`** · `n_symbols=28` · `excluded={HTZ,YYAI,XHLD}` |
| الشبكة | **آخرُ 20 جلسة** من اللقطة (نفسُ مقام `denominator_sessions`) × **الكونُ الكامل** |
| الرموز | **بلا سقف** (‏3,383) — المالكُ أذِن بالوقت، والسقفُ كان مصدرَ انحيازِ موضعٍ سابقًا |
| المُستبعَدون | رموزُ الكاتالوج الثمانية والعشرون **تُستبعَد** من الحساب (الظرفُ مُفصَّلٌ عليها) — ويُنشَر عددُها |
| الأعلام | `SCREENER_MODE=BACKTEST` **قبل الاستيراد** · وكلُّ `BT_*` مطفأ |

---

## ⑦ سقفُ النجاح — مُعلَنٌ سلفًا

**أقصى ما يُنتجه هذا التشغيل: رقمٌ وتقريرٌ واقتراحٌ للمالك.** وممنوعٌ مهما كان الرقم:
مسُّ `M1-M14` أو أيّ عتبةٍ في `CONFIG` · رفعُ `LOGIC_VERSION` · مسُّ المُرتِّب أو
السعة · تغييرُ `envelope_p90.json` · تشغيلُ الصيّاد بتلغرام · وأيُّ تغييرٍ إنتاجيّ.

---

## ⑧ تنبّؤاتي — تُكتَب الآن لتُكذَّب

- **P1**: `w ≥ 0.90` (العشرةُ زينة) — **ثقةٌ متوسطة**. سندُه أن `best_spike ≥ 98.94`
  شرطٌ نادرٌ جدًّا في السوق، فمَن يجتازه يكون سلفًا سهمًا متطرّفًا يجتاز غيرَه.
- **P2**: `ALONE[best_spike]` **أقلُّ** من `ALONE` لكل معيارٍ آخر (أي أشدُّها إقصاءً).
- **P3**: `LOO[best_spike]` **أكبرُ** إسهامٍ حدّيّ من بين الأحد عشر.
- **P4**: `LOO[rsi_now]` و`LOO[readiness]` **قريبةٌ من الصفر** (مكرّرةٌ بنيويًّا).
- **P5**: التغطيةُ **‏≥90%** من الكون.

⚖️ **وإن كُذِّب `P1`** (‏`w ≤ 0.50`) فذلك **نتيجةٌ لصالح الظرف** وأُعلنها كذلك بلا
تلطيف — فالتسجيلُ يُكتب ليُكذَّب لا ليُصادَق.
