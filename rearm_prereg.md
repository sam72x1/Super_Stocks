# 🔁⚓ T-REARM — «إعادةُ المِرساة بعد الخروج البنيويّ»: هل تستحقّ المِرساةُ الثانيةُ كرتًا؟

**تسجيلٌ مسبق — يُدفَع قبل أيّ رقم، ولا معيارَ يُحرَّك بعده.**
أمرُ المالك (2026-09-02): «**سجّل إعادة المِرساة**» — بعد مِجَسّ `CHAI` ليوم 08-28
(`sym_day.yml` تشغيلة `33649154963`) وبعد إثباتٍ من الكود لا من المِجَسّ.

---

## ⓪ الدافعُ والسؤال

**الحقيقةُ البنيويّة (مُثبَتةٌ من `Super_stock.liq_stage_events`):** الدالّةُ تبحث عن
مِرساةٍ **فقط حين لا `anchor_ms` في الحالة**؛ وعند الخروج البنيويّ (أوّلُ إغلاقِ دقيقةٍ
دون قاع شمعة المِرساة) تُبقي `anchor_ms` وتضع `exit_ms` **وتعود فورًا في كلّ مسحةٍ
بعده**، والحالةُ لا تُصفَّر إلّا بتبدّل التاريخ في `scan_liq_stages` ⇒ **مِرساةٌ واحدة
لكلّ سهمٍ في اليوم**.

**الحالةُ التي أخرجتها:** `CHAI` يوم 2026-08-28 — البوّابةُ الإنتاجية تُمسكه 07:52
نيويورك (بريماركت) عند 0.29، كرتُ `M5` 0.3131، خروجٌ بنيويّ 08:05 عند 0.2384، **ثم
الانفجارُ الحقيقيّ 12:00–12:51** (قمّةُ اليوم 0.52 = ‏+105.5% عن إغلاق الأمس). حتى لو
كان العاملُ حيًّا من الفجر لكان الوهمُ الصباحيُّ قد استهلك مِرساةَ اليوم الوحيدة وصمت
البوتُ عن الانفجار **بالبناء**.

**السؤالُ الحاكم:** لو سمحنا — بعد الخروج البنيويّ للمِرساة الأولى — بمِرساةٍ ثانيةٍ
**بالبوّابات الثلاث نفسِها حرفيًّا** (قفزةُ الحجم ‏3× على سوابقها في النافذة · الأرضيةُ
التراكمية $30,000 على 3 دقائق · الاتجاهُ الداخل برفعة 5%): **كم مرّةً تقع؟ وهل هي
بجودة الأولى؟ وكم حركةً كاسحةً تسترجع؟ وبأيّ كلفةٍ من الكروت؟**

**ما ليس السؤال:** تخفيفُ أيّ عتبة (‏5% · $30,000 · 3× · النبض 10% · الهدف 10%) —
كلُّها كما هي بلا مساس · ولا «الزاحفُ بلا دقيقة 5%» (مقيسٌ ومُغلَق: `T-GATE` ·
`T-CUMRISE` · `T-VOLBASE`).

---

## ① البياناتُ والكون — مقياسٌ واحدٌ لا اثنان

- المصدر: ملفّات Polygon المجمّعة `minute_aggs` (تشمل المشطوبين = PIT بالبناء).
- **السنواتُ الثلاث: 2023 · 2024 · 2025** — والحكمُ عبرها لا عبر سنة.
- الأنبوبةُ من `kasih_scan` **بالاسم** (‏`weekdays` · `parse_day` · `prescreen` ·
  `first_anchor` [= `liq_stage_events` الإنتاجية بالنمط القانونيّ] · `resolve` ·
  `f2_usd5` · `GAP_CAP`=500 · كون [`PRICE_LO`,`PRICE_HI`] · `coverage_verdict`).
- سعرُ الدخول `e5` من `tier_days_report.true_e5` **بالاسم** (سعرُ كرت M5 بلا قطع).
- خروجُ `T0`/`T10` من `target10_arms.arm_exit` **بالاسم** (الوقفُ أوّلًا داخل
  الدقيقة · هدفُ ‏+10% رقمُ المالك المقيس في `T-TARGET10`).
- **المِرساةُ الثانية بالدالّة الإنتاجية نفسِها:** إعادةُ تشغيلٍ تدريجية على شرائح
  `bars[max(0, k-LIQ_WINDOW_MIN):k]` **بحالةٍ مصفَّرة علامتُها `last_eval_ms` = زمنُ شمعة
  الخروج** ⇒ تُقيَّم كلُّ شمعةٍ مغلقةٍ بعد الخروج، الأقدمُ أوّلًا، بـ`_bar_gates` نفسِها
  (قفزةُ الحجم على سوابقها **في النافذة** كما يراها العاملُ الحيّ) — **صفرُ عتبةٍ جديدة
  وصفرُ منطقِ كشفٍ مكرَّر**. وخروجُها البنيويّ = قاعُ شمعتها هي (تعريفُ `resolve`).
- وضعُ الرموز (`REARM_DAY` + `REARM_SYMS`، بلا فلتر سعر — سابقةُ `sym_day_probe`)
  لشاهد الحالة؛ وإغلاقُ الأمس من بذرة الأيام السابقة كما في `kasih_scan`.

## ② تعريفاتُ الأذرع — ثلاثٌ ولا رابعة

- **A0 (الإنتاج):** المِرساةُ الأولى وحدها — يجب أن تُعيد أرقامَ `T-KASIH` بت-بت.
- **A1 (الحاكمة): إعادةُ مِرساةٍ واحدة** بعد الخروج البنيويّ للأولى (إن لم تخرج الأولى
  بنيويًّا — `eod` — فلا إعادة، كما لا يُصفَّر شيءٌ في الإنتاج بلا `Xs`).
- **A2 (وصفيّة): إعادةٌ بلا حدّ** — كلُّ خروجٍ بنيويّ يُعيد التسليحَ حتى نهاية اليوم
  (سقفٌ هندسيّ 20 مِرساةً/يوم يُعَدّ ولا يُخفى) — لمنحنى الكلفة وجودةِ الثالثة فأكثر.

**الاستبعاداتُ المُعلَنة (تُعَدّ وتُسمّى ولا تُطوى):** `GAP_CAP` على المِرساة الثانية
بالقاعدة نفسِها · `broke5` (كُسر القاعُ قبل اكتمال الخمس) يخرج من تجميع `R` وحده ·
`R` غيرُ المُعرَّف (‏`e5 − anchor_low` ليس موجبًا) يخرج من `R` وحده.

## ③ المقاييس

لكلّ مِرساة (أولى/ثانية/ثالثة فأكثر): `kasih30` (من سعر المِرساة — المقياسُ المنشور) ·
**`kasih30_from5` (الحاكم — درسُ `HUIZ`: سعرُ المِرساة لا يملكه المالك، وسعرُ الكرت
يملكه)** · `mg_after` · نوعُ الخروج · `e5` · `R(T0)` و`R(T10)` بوحدة
`R₀ = e5 − anchor_low` · سلّةُ الفجوة عن إغلاق الأمس · الزمن.
لكلّ يوم-رمز: عددُ المراسي · الفاصلُ الزمنيّ (خروجُ الأولى ⟶ الثانية) · **«مسترجَع»** =
الأولى **لم** تبلغ ‏+30% من `e5` (أو كُسرت قبل الخمس) **و**الثانية بلغته.
**الكلفة:** نسبةُ المراسي الثانية إلى الأولى (كلُّ مِرساةٍ ‏≈ كرتا `M1`+`M5` وربما
`M30`/`Tg`/`Xs`) — تُقاس بالمراسي لا بالرسائل (الفلترُ طبقةُ تسليمٍ مستقلّة).

## ④ بوّاباتُ الصلاحية (قبل أيّ تفسير)

- **`V0` (تكامل):** عددُ مراسي `A0` وعددُ كاسحيها (‏`kasih30`) يطابقان المنشورَ في
  `kasih_result.md` **بت-بت** لكلّ سنة: 2023 ‏10,851/1,049 · 2024 ‏14,312/1,676 · 2025
  ‏19,037/2,128 — تفرّقٌ ⇒ خروج 3 «عطبُ أداةٍ لا نتيجة».
- **`V1` (لا-no-op):** صفرُ مِرساةٍ ثانيةٍ في سنةٍ كاملة ⇒ خروج 4 «ذراعٌ خامدة».
- **`V2` (بنيويّة):** كلُّ مِرساةٍ لاحقة زمنُها **بعد** شمعة الخروج البنيويّ لسابقتها —
  تفرّقٌ واحدٌ ⇒ خروج 3.
- **`V3` (شاهدُ الحالة):** في وضع الرموز على `CHAI` يوم 2026-08-28 تُوجد مِرساةٌ ثانية
  بين **11:50 و12:15 نيويورك** — وإلّا خروج 3 (الأداةُ لا تُعيد إنتاج الحالة التي
  وُلدت لها = عطبُ أداةٍ أو فرضيةٌ باطلة، وكلاهما يُعلَن **قبل** السنوات).
- **`V4`:** حارسُ التغطية `kasih_scan.coverage_verdict` أخضرُ في كلّ سنة.
- **`V5`:** شمعةُ الخروج التي تُبنى عليها الإعادة موجودةٌ **⟺** `resolve.exit == "break"`
  لكلّ صفّ — تفرّقٌ ⇒ خروج 3 (مقياسُ خروجٍ واحدٌ لا اثنان).
- **الأرضية:** 150 مِرساةً ثانيةً (`A1`) لكلّ سنة، وإلّا «لا حكم» لتلك السنة.

## ⑤ معاييرُ الحكم (كلُّها معًا) — تُقرأ كما كُتبت

1. **الكلفة:** مراسي `A1` الثانية **‏≤ 50% من الأولى** في كلّ سنة (الحدُّ مُعادٌ من
   `T-GATE`/`T-CUMRISE`: «إشعاراتٌ ‏≤+50%»).
2. **الجودة:** `kasih30_from5(الثانية) ≥ kasih30_from5(الأولى) − 3.6 نقطة` في كلّ سنة
   **ومجمَّعًا** (‏3.6 = `JITTER_PP` المقيس في `T-GATE`)، والرفيقُ `kasih30` (من سعر
   المِرساة) **لا يناقض** إشارةَ الفرق مجمَّعًا.
3. **الاسترجاع:** «مسترجَع» **‏≥ 1.0% من أيام المِرساة الأولى** مجمَّعًا وموجبٌ في
   كلّ سنة.

استيفاءُ الثلاثة ⇒ «استوفت». سقوطُ أحدها ⇒ **فشلت** (وتُنشَر الأرقامُ كما هي).
`A2` **وصفيّةٌ لا تدخل الحكم** مهما كانت أرقامُها.

## ⑥ سقفُ النجاح — مُثبَّتٌ سلفًا

إن استوفت: **اقتراحٌ للمالك بالأرقام**. تنفيذُه يمسّ `liq_stage_events` (وسيطٌ
اختياريّ افتراضُه بت-بت — سابقةُ `now_ms` — فتبقى أدواتُ الإعادة المجمَّدة و
`kasih_scan.resolve` وكلُّ رقمٍ منشورٍ كما هو) ⇒ **بأمره الصريح فقط**، ولا تغييرَ قبله.
ولا مساسَ بأرقامه ولا بالفلتر ولا بأيّ بوّابةِ إطلاقٍ أو كتم.

## ⑦ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر نتائجُها كما تقع)

- **RA-P1:** مراسي `A1` الثانية بين **‏10% و30%** من أيام المِرساة الأولى في كلّ سنة.
- **RA-P2:** `kasih30_from5` للثانية **أدنى** من الأولى بـ0 إلى 5 نقاط مجمَّعًا
  (إعادةُ وهمٍ غالبًا) — يُكذّبه أن تكون أعلى، أو أدنى بأكثر من 5.
- **RA-P3:** `CHAI` 2026-08-28 تحصل على مِرساةٍ ثانية بين 11:50 و12:15 نيويورك.
- **RA-P4:** الثالثةُ فأكثر (`A2`) في **أقلّ من 5%** من أيام المِرساة الأولى.
- **RA-P5:** وسيطُ الفاصل (خروجُ الأولى ⟶ الثانية) **فوق 60 دقيقة**.
- **RA-P6:** «مسترجَع» بين **‏0.5% و2%** من أيام المِرساة الأولى مجمَّعًا.

## ⑧ حدودُ صدقٍ تُقرأ مع الأرقام لا بعدها

1. لمسٌ لا تنفيذ (‏`MEASURE_CUT_NOTE` يسري) — كلُّ نسبةٍ سقفٌ متفائل.
2. يومُ المِرساة وحده ‏04:00–20:00 نيويورك؛ لا مِرساةَ ثانيةً بعد خروج `eod` بالتعريف.
3. الإعادةُ تُحاكى بالقاعدة المجمَّدة (`rows[:-1]` بلا ساعة الحائط) كسائر أدوات
   الإعادة — والحيُّ أسرعُ بدقيقةٍ تقريبًا (مقيس في `provider_gap_result.md`).
4. الفرقُ بين الثانية والأولى **ليس سببيًّا**: أيامُ الثانية مجتمعٌ اختير بكسر الأولى
   ⇒ تُقارَن أيضًا **بأوائل الأيام نفسِها** وصفيًّا.
5. الكلفةُ مقيسةٌ بالمراسي؛ ما يصل المالكَ فعلًا يحكمه `alert_filter.json`.
6. لا انحيازَ بقاء (الملفّات تشمل المشطوبين) · وتشويهُ التقسيم يُستبعَد بـ`GAP_CAP`.

## ⑨ الأداةُ والتشغيل

`rearm_arms.py` + `.github/workflows/rearm.yml` (يدويٌّ حصرًا · بلا كرون · قراءةٌ
فقط: لا تلغرام ولا كتابةَ حالةٍ والإنتاجُ لا يستورده). الأذرعُ الثلاث تُحسَب على
الصفوف نفسِها في تمريرةٍ واحدة ⇒ ميزانيةٌ واحدةٌ بالبناء (`faisal-gates §⑤`).
المخرَج: `rearm_rows_{السنة}.jsonl` + جداول. **الترتيب الملزِم:** هذا العقدُ يُدفَع
⟶ الأداةُ وأقفالُها وطفراتُها ⟶ شاهدُ `V3` على `CHAI` ⟶ السنواتُ الثلاث ⟶ الحكم.
