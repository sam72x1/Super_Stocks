# 🎯✂️ `T-HEADCUT` — تسجيلٌ مسبق: **«بِع قبل رأس شمعة الهبوط»**

> **أمرُ المالك (2026-09-19): «نفذ جميع النواقص معك الصلاحية كاملة».**
> **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · ولا يُشحَن شيءٌ
> مهما كانت النتيجة.** مدفوعٌ ومدموجٌ **قبل أيّ سطرِ أداةٍ وقبل أيّ رقم**.

---

## §⓪ الدعوى ومصدرُها — حرفيًّا لا مُستنتَجًا

**الكاتالوج `L251`** — تغريدةُ فيصل ‏6817 على `AUUD` (فريم ‏4س):
> «صعد فوق ما تطرده خلّه يولّي · الأهداف ع ٤س · **شمعة الهبوط 2.18 = مقاومة/هدف،
> بِع قبلها**»

الوسم **`faisal_verbatim`**.

⛔ **ولا يُستشهَد بـ`L886`** («رأس شمعة الهبوط جني الربح عند ارتداده لها») — فهي
من قسم القناة التعليميّة ونظامُها **مضاربةُ زخمٍ على خمس دقائق** ⇒ `third_party`
(قاعدةُ `faisal-gates §②`: نظامٌ مختلفٌ يلزمه بوّابةُ سياق).

**النظام:** إدارةُ الهدف على **مجتمع الارتكاز** — نظامُنا ⇒ **لا بوّابةَ سياقٍ لازمة**.

### والمحورُ جديدٌ بالبحث لا بالتسمية
`grep` في الأرشيفين: **صفرُ حكمٍ** على `T-HEADCUT` (ذكرُه الوحيدُ وصفُه
مقترَحًا في حزمة 2026-09-18). و**`T-EXIT`** قاس أهدافًا **فوق** `t1`
(‏+20% ⟶ ‏−0.05/−0.16/−0.25R · ‏+100% ⟶ ‏−0.62/−0.80/−0.90R · **وأفضلُ هدفٍ =
المرجعُ نفسُه ‏+0.00R**) ⇒ **ما دون `t1` لم يُقَس قطّ**، وحكمُ `T-EXIT` يجعله
السؤالَ الطبيعيَّ التالي: إن كان الأعلى أسوأَ، فهل الأدنى أفضل؟

### 🔒🔴 وهو **داخلَ محورٍ مُغلَق** — فتُستوفى شروطُ فتحه الثلاثةُ صراحةً
حكمُ `T-TRAIL` (‏2026-09-18) أغلق **«محورَ إدارةِ الخروج بعد الدخول»** إغلاقًا
مُنفَّذًا، **وقصُّ الهدف إدارةُ خروجٍ بعد الدخول** ⇒ هذا العقدُ لا يمرّ إلّا
باستيفاء الثلاثة، **وتُثبَت واحدةً واحدة:**

| # | الشرط (نصُّ `trail_arms.closure_notice`) | الاستيفاء |
|---|---|---|
| ① | **مصدرٌ جديدٌ للدعوى** | ✅ **`L251` `faisal_verbatim`** — ولم يُستشهَد به في أيٍّ من السّتّ الساقطة (`T-EXIT` قاس أهدافًا **فوق** `t1` · `T-EXITMGMT` خروجًا جزئيًّا · `T-T1MOVE` هدفًا يتحرّك مع الدخول · `T-MANAGE-25` · `T-OPHOLD-2` إلغاءَ السقف · `T-TRAIL` وقفًا يترقّى) ⇒ **«هدفٌ دون `t1`» لم يُقَس قطّ** |
| ② | **تسجيلٌ مسبقٌ جديد بدقّةٍ تُخرج الفرقَ من نطاق `±0.08R`** ‏+ **ضبطٌ يعزل** | ✅ **`HC2` صارت ‏≥ +0.08R** (‏`§⑤`) — **رفعٌ لا خفض** · والضبطُ الذي يعزل هنا **`C-HEAD`** (الرأسُ مقابل غيره) و**`C-BACK`** (القصُّ في الاتّجاه المعاكس)، **يقومان مقامَ «عزل الترقّي عن طول الأفق»** لأن تلك الصياغةَ تخصّ الوقفَ المترقّي لا الهدف |
| ③ | **إذنُ المالك** | ✅ أمرُ ‏2026-09-19 |

⚠️ **ولا يُمَسّ إغلاقُ `T-TRAIL` نفسُه:** `trail_arms.py`/`trail_op_arms.py`
تبقيان خارجتَين بـ‏8، **و`TRAIL_REOPEN` لا يُضبَط بهذا العقد إطلاقًا** —
وأداةُ `T-HEADCUT` **مستقلّةٌ** ولا تستورد أيًّا منهما.

---

## §① ما هو قائمٌ وما هو غائب — مقيسٌ لا مُدَّعًى

- **قائم:** `_red_candle_heads` (`Super_stock.py:2629`) تبني رؤوسَ الحمرا في
  سلّم المقاومات · **وإصابةُ `t1` في الباكتيست لمسةٌ**: `if k >= t1_from and
  hi[k] >= t1` (‏`Super_stock.py:19909`) ⇒ **نبيع على الرأس لا قبله** ·
  وإعادةُ تمثيل الخروج بلا مسّ الجذر قائمةٌ في `exitmgmt_arms.resolve_exit`
  (‏استُعملت في `T-TRAIL` وأُثبت أنها تُعيد الجذرَ **بت-بت**).
- **غائب:** أيُّ قياسٍ لهدفٍ **دون** `t1`.

---

## §② التعريفُ المُقاس — واحدٌ ومكتوبٌ بحرفه

```
t1_eff(h) = t1 × (1 − h)
```

والخروجُ يُحسَم بـ`exitmgmt_arms.resolve_exit(..., t1=t1_eff, ...)` **بلا إدارة**
(`take=0`, `level=None`, `move_be=False`) ⇒ عند `h = 0` **يطابق الجذرَ بت-بت**.

**وأربعةُ قيودٍ تُكتَب الآن لأنها تغيّر النتيجة:**
1. **المقامُ ثابتٌ للأذرع كلِّها**: `r_fixed(ret, e, e, stop)` بالاسم من
   `tranche_arms` — **والوقفُ والدخولُ لا يُمَسّان** (عزلُ أثر الهدف شرطُ صلاحية).
2. **`no_fill` = صفرُ عائدٍ يدخل المقام** (اصطلاحُ `T-EXITMGMT` المنشور) ⇒
   **المقامُ واحدٌ** والتعبئةُ واحدةٌ للأذرع كلِّها ⇒ **ضبطُ التعبئة بلا موضوعٍ
   هنا ويُعلَن** (لا يُطوى — درسُ `T-OPTRADE`).
3. **التكميمُ خانةٌ واحدةٌ مجمَّدة** كما في `T-TRAIL`/`T-EXITMGMT` وإلّا استحالت
   هُويّةُ `V-H1`.
4. **الهدفُ المقصوص يُقارَن على نفس الشموع ونفس الدخول ونفس الوقف** —
   المتغيّرُ **الهدفُ وحدَه**.

---

## §③ المجتمع — عينُ `E-BT` بحارسِ هُويّة

اللقطاتُ الثلاث المجمَّدة (‏2023/2024/2025) عبر `trail_arms.load_frozen_dataset`
ودوالِّه **بالاسم** ⇒ **‏4,817 زوجًا** كما في `T-TRAIL`.

🔒 **`V-H1` هُويّةٌ لا دعوى:** ذراعُ `H0` (‏`h = 0`) يجب أن تُعيد **أرقامَ `Y0`
المنشورة في `trail_result.md` بت-بت**:

| السنة | `R₀` | صفوف | تعبئة |
|---|---|---|---|
| ‏2023 | **‏−0.2602** | ‏1620 | ‏1399 |
| ‏2024 | **‏−0.1807** | ‏1591 | — |
| ‏2025 | **‏−0.2172** | ‏1606 | — |

**وأيُّ انزياحٍ يوقف القياسَ بخروجٍ مميَّز** ولا يُغيَّر صامتًا.

---

## §④ الأذرع

| الذراع | `h` | الدور |
|---|---|---|
| `H0` | ‏0% | **الأساس = الإنتاج** (وهُويّةُ `V-H1`) |
| **`H1`** 🥇 | **‏1%** | **الحاكمة** — أقربُ قصّةٍ لا تُغيّر الهدفَ جوهريًّا |
| `Hh` | ‏0.5% | وصفيّة |
| `H2` | ‏2% | وصفيّة |
| `H3` | ‏3% | وصفيّة |
| **`C-BACK`** | **‏−1%** | **شاهدٌ حاكم** — قصٌّ **بعد** الرأس (هدفٌ أعلى) |

**والأرقامُ `engineering` مُعلَنة** (فيصل قال «قبلها» **بلا نسبة**) ⇒ **لا تُشحَن
عتبةً بأيّ حكم**، والحساسيّةُ تُطبَع كاملةً.

### 🔬 والشاهدُ الحاكمُ الثاني — `C-HEAD` (وهو تعديلٌ مُعلَنٌ على مقترَح الحزمة)
الحزمةُ اقترحت `C-RAND` «قصًّا عشوائيًّا حتميًّا». **وأرفضه شاهدًا لأنه ليس
شاهدًا:** القصُّ العشوائيُّ **هو العمليّةُ نفسُها** بقيمةٍ أخرى، فلا يعزل شيئًا.

**والشاهدُ الذي يعزل فعلًا:** تقسيمُ الصفوف بحسب **هل `t1` رأسُ شمعةٍ حمراء
أصلًا؟** (`t1` يقع ضمن تسامح `TARGET_HEAD_TOL_PCT` = **‏0.5%** `engineering`
من أحد مُخرَجات `_red_candle_heads` **بالاسم**) ⇒
- `Δ_head` = ‏`R(H1) − R(H0)` على صفوف **الرأس**.
- `Δ_other` = نفسُه على **مُكمِّلها**.

**فإن كان الأثرُ في الاثنين سواءً فهو «الهدفُ الأدنى أفضل عمومًا» لا قاعدةَ
فيصل** — وهو بعينه ما كشفه `C-MOM` في `T-PMGATE` و`C-DEPTH` في `T-RSI-RANK`.

⚠️ **وإن كانت نسبةُ صفوف الرأس ‏100% أو ‏0% فالشاهدُ منحلٌّ ويُعلَن** ولا يُستبدَل
بعد رؤية الرقم.

---

## §⑤ معيارُ القبول — مسجَّلٌ قبل أيّ رقم

- **`HC1`** ‏`R(H1) − R(H0)` **موجبٌ في السنوات الثلاث** وفاصلُ البوتستراب
  المجمَّع (`exitmgmt_arms.boot_ci` بالاسم · عنقوديٌّ بالرمز) **لا يلمس الصفر**.
- **`HC2`** ماديّةٌ: المجمَّعُ **‏≥ +0.08R** — **وهو شرطُ فتح المحور**
  (`trail_arms.closure_notice` ②: «دقّةٌ تُخرج الفرقَ من نطاق `±0.08R`»)
  ⇒ **رفعٌ للحدّ لا خفض**، ووسمُه `engineering` مُعلَن.
- **`HC3`** يتفوّق على **`C-BACK`**: ‏`R(H1) − R(C-BACK)` موجبٌ ومجمَّعُه
  لا يلمس الصفر.
- **`HC4`** يتفوّق على **`C-HEAD`**: ‏`Δ_head − Δ_other` **موجب** ⇒ الأثرُ
  يخصّ الرأسَ لا القصَّ عمومًا.

---

## §⑥ فروعُ الحكم — ثلاثةٌ · ولا فرعَ رابع

1. **الفرعُ 1 — «تُوصى»:** `HC1` و`HC2` و`HC3` و`HC4` **كلُّها** ✅ ⇒ تُقترَح
   على المالك **اقتراحًا لا تنفيذًا**، **ولا يُشحَن شيءٌ بهذا العقد**.
2. **الفرعُ 2 — «لا تُوصى»:** سقوطُ **أيٍّ** منها.
3. **الفرعُ 3 — «لا حكم»:** أرضيّةُ **‏≥150 صفًّا محسومًا في كلّ سنة** لم تُبلَغ ·
   أو سقوطُ حارس · أو تعذّرُ اللقطة — **تُعلَن بعددها ولا تُفسَّر**.

---

## §⑦ حرّاسٌ تُطبَع في كلّ تقرير — والسقوطُ يوقف

- **`V-H1`** هُويّةُ `H0` ≡ `Y0` المنشور **بت-بت** (‏`§③`).
- **`V-H2`** `t1_eff(0) == t1` بالضبط · و`t1_eff(h) < t1` لكلّ `h > 0`
  و`> t1` لـ`C-BACK` — **سلوكيًّا**.
- **`V-H3`** **الوقفُ والدخولُ والمقامُ متطابقةٌ في كلّ الأذرع** على كلّ صفّ.
- **`V-H4`** عدُّ صفوف «`t1` رأسُ حمرا» يُطبَع **قبل** أيّ `Δ`، ومعه إعلانُ
  الانحلال إن كان ‏0% أو ‏100%.
- **`V-H5`** الأداةُ **لا ترسل ولا تكتب حالةً** (‏`selfcheck_readonly` بالـAST
  المُشدَّد: ما لا يُثبَت أنه قراءةٌ يُعَدّ كتابة) · **وبلا سرِّ تلغرام في الـworkflow**.
- **`V-H6`** الجذورُ الاثنا عشر **بصمتُها مطابقة** قبل التشغيل وبعده.

---

## §⑧ حدودُ صدقٍ تُقال قبل النتيجة

1. **الأثرُ صغيرٌ متوقَّعًا:** القصُّ ‏1% يحرّك هدفًا واحدًا بنسبةٍ ضئيلة ⇒
   **الفرقُ في نطاق الضجيج** الذي شُوهد في `T-TRAIL` (‏±0.08R) ⇒ `HC2`
   **مرشَّحٌ بقوّةٍ للسقوط حتى لو كان الاتّجاه صحيحًا** — وهذا مقصودٌ لا عيب:
   شرطُ فتح المحور يطلب دقّةً تتجاوز الضجيجَ، **فمن لم يتجاوزه لا يُوصى به**.
2. **الباكتيست ينفّذ بسعر الهدف نفسِه بلا انزلاق** — والانزلاقُ مقيسٌ في
   `T-SLIP` (‏+0.689 ⟶ +0.566R) ⇒ **يُطبَع أثرُ التكلفة** عبر
   `fcost_arms.ret_at` بالاسم عند `C-MED` **وصفيًّا لا حكمًا**.
3. **`t1` ليس دائمًا رأسَ حمرا** — سلّمُ المقاومات يضمّ فجواتٍ وفيبو وقممَ
   سوينغ ⇒ **`C-HEAD` قد ينحلّ** ويُعلَن.
4. **مصدرُ الدعوى فريمُ ‏4س** والقياسُ على **اليوميّ** (مجتمعُ الباكتيست) ⇒
   **نقلُ إطارٍ مُعلَن** (`faisal_adopted` في التطبيق لا في النصّ).
5. **وسجلُّ هذا المحور سالب:** خمسُ تجاربِ إدارةِ خروجٍ سقطت (`T-EXIT` ·
   `T-EXITMGMT` · `T-T1MOVE` · `T-MANAGE-25` · `T-OPHOLD-2` · `T-TRAIL`)
   ⇒ **الاحتمالُ الأسبقُ الفشل.**

---

## §⑨ تنبّؤاتي — تُنشَر مكذَّبةً أو مؤكَّدة

| # | التنبّؤ | ثقتي |
|---|---|---|
| `HP1` | `HC1` يعبر (الاتّجاه صحيح: اللمسةُ تفوّت صفقاتٍ تصل ‏99% من الهدف) | متوسّطة |
| `HP2` | **`HC2` يسقط** (المجمَّع دون ‏+0.08R) ⇒ **الفرعُ 2** | **عاليةٌ جدًّا** |
| `HP3` | `C-BACK` أسوأُ من `H0` (الهدفُ الأعلى يضرّ — متّسقٌ مع `T-EXIT`) | عالية |
| `HP4` | **`C-HEAD` ينحلّ أو `HC4` يسقط** — الأثرُ «هدفٌ أدنى» لا «رأسٌ بعينه» | **عالية** |
| `HP5` | حصّةُ صفوف «`t1` رأسُ حمرا» بين ‏40% و80% | منخفضة |

---

## §⑩ ما لا يُمَسّ بهذي التجربة

- ⛔ **الجذورُ الاثنا عشر** · **و`_red_candle_heads` تُقرأ ولا تُعدَّل** ·
  و`MIN_TARGET_GAP_PCT` و`TARGET_MAJOR_GAP_PCT` **لا تتحرّكان**.
- ⛔ **لا `LOGIC_VERSION`** · **ولا شحنَ حقلٍ أو مفتاحٍ أو سطرِ عرض** مهما كانت
  النتيجة — **والفرعُ 1 اقتراحٌ على المالك لا تنفيذ**.
- ⛔ **ولا تُعاير `h` بعد رؤية النتيجة** — الحاكمةُ ‏1% مكتوبةٌ هنا،
  والباقي حساسيّةٌ **وصفيّة** تُطبَع كاملةً ولا يُنتقى منها الأمرح.
