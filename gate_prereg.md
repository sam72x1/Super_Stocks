# 🚪🔬 GATE-PREREG — تسجيلٌ مسبق: **أيُّ شرطٍ يُؤخّر أوّلَ تنبيه؟**

> **مدفوعٌ قبل أيّ رقم.** أمرُ المالك 2026-08-18: «**قس شروط البوابة**» — بعد أن
> أثبت `m0_result.md` أن `M0` يشتري **23 ثانيةً فقط**، وأن **التأخّرَ من شروط
> البوّابة نفسِها** لا من انتظارِ اكتمالِ الشمعة.

## ① الشكوى بنصّها — وهي المقياسُ الحاكم

«التنبية وصلني **بعد ما ارتفع السهم فوق 20٪** وهذا شي مستحيل يصير خلال دقيقة وحده».
⇒ **المقياسُ الأوّلُ: «التأخّر» = ارتفاعُ السهم عن إغلاق الأمس لحظةَ أوّلِ تنبيه.**
وسيطًا على الأسهم المُنبَّه عليها. **أقلُّ = أفضل.**

## ② البوّابةُ الحاليّةُ ثلاثةُ شروطٍ مجتمعة (‏`liq_stage_events`)

| # | الشرط | القيمة | مصدرُه |
|---|---|---|---|
| ① | قفزةُ حجمِ الدقيقة عن متوسّطِ ما قبلها | `IGNITION_VOL_MULT` = 3.0× | `engineering` (زنادُ الرادار) |
| ② | أرضيةٌ **تراكميّة** على آخر `LIQ_CUM_MINUTES` | `LIQ_MIN_USD` = $30,000 على **3** دقائق | 🔴 **نصُّ المالك** («فوق ٣٠ الف دولار») · والنافذةُ 3 **مقيسةٌ** (`liq_move_result`) |
| ③ | «سيولةٌ داخلة»: خضراء **و** رفعةٌ فتحًا⟶إغلاقًا **و** إغلاقٌ في النصف الأعلى | `LIQ_MIN_MOVE_PCT` = 5.0% · `LIQ_CLOSE_POS_MIN` = 0.5 | 🔴 **الرفعةُ نصُّ المالك** («يرتفع خلال دقيقة وحده نسبة فوق 5٪») · وموضعُ الإغلاق `engineering` |

🔴🔴 **وقيدٌ يُعلَن قبل أيّ رقم: رقمان من الثلاثة نصُّ المالك حرفيًّا** (‏$30 ألفًا
و5%). ⇒ **تُقاس كلفتُهما ولا يُوصى بتغييرهما** — قرارُهما له وحدَه، وأنا أُسلّم
**منحنى الكلفة** لا توصية. **والقابلُ للتوصية هو `engineering` وحدَه**
(‏`LIQ_CLOSE_POS_MIN` · `IGNITION_VOL_MULT` · `LIQ_CUM_MINUTES`).

## ③ المجتمعان — مثبَّتان

- **`A` المُنبَّه عليهم:** كلُّ سهمٍ تُطلِق عليه بوّابةُ الإنتاج (`G0`) ⇒ يقيس
  **«كم تأخّر التنبيهُ الذي وصلني»**.
- **`B` المتحرّكون:** كلُّ سهمٍ بلغت قمّتُه ‏≥`MOVER_PCT` فوق إغلاق الأمس ⇒ يقيس
  **«هل التُقِط أصلًا، وبأيّ تأخّر»**. و`MOVER_PCT`=30 **منقولٌ حرفيًّا** من
  `liq_noise_prereg` ولا يُحرَّك.
- والكونُ **كونُ المُشغِّل نفسُه** (`live_watch_universe` بلا سقف) — لا كونٌ أوسع.

## ④ الأذرعُ السبع — مثبَّتةٌ ولا تُزاد ذراعٌ بعد الأرقام

| الذراع | التغيير | نوعُه |
|---|---|---|
| **`G0`** | الإنتاجُ كما هو | الأساس |
| `G1` | `LIQ_CLOSE_POS_MIN` = 0 (إلغاءُ موضع الإغلاق) | `engineering` ⇒ **قابلٌ للتوصية** |
| `G2` | `IGNITION_VOL_MULT` = 2.0 | `engineering` ⇒ **قابلٌ للتوصية** |
| `G3` | `LIQ_CUM_MINUTES` = 5 (نافذةٌ أوسعُ بنفس الأرضية) | `engineering` ⇒ **قابلٌ للتوصية** |
| `G4` | `LIQ_MIN_MOVE_PCT` = 3.0 | 🔴 **رقمُ المالك — كلفةٌ تُعرَض لا تُوصى** |
| `G5` | `LIQ_MIN_USD` = 15,000 | 🔴 **رقمُ المالك — كلفةٌ تُعرَض لا تُوصى** |
| `G6` | **بلا شرط رفعةٍ إطلاقًا** (خضراء فقط) | ⛔ **حدٌّ أعلى للأسبقيّة الممكنة — ليس اقتراحًا** |

**وذراعٌ واحدةٌ في كلّ مرّة** (لا تركيب) — فالتركيبُ يخلط الأثرَ ولا يُعرَف صاحبُه.

## ⑤ المقاييس — مثبَّتةٌ قبل الأرقام

1. 🥇 **التأخّر**: `(سعرُ المِرساة ÷ إغلاقِ الأمس − 1) × 100` — **وسيطًا**.
2. **الأسبقيّةُ بالدقائق**: `دقيقةُ مِرساةِ G0 − دقيقةُ مِرساةِ الذراع` لنفس السهم.
3. **الإثمار**: **نفسُ تعريف `liq_move_prereg §④-ب` حرفيًّا** (بلوغُ ‏+3% خلال
   ‏15 دقيقةً بعد التنبيه) — ويُعاد استعمالُ الدالّة نفسِها لا نسخةٌ منها.
4. **الكلفة**: عددُ الإشعارات وعددُ الأسهم المُنبَّه عليها.
5. **التغطية**: كم من `B` التُقِط (وأيُّها ضاع).

## ⑥ 🧱 جدارُ البداية — التشخيصُ الذي لا يحتاج ذراعًا

لكلّ سهمٍ في `B`: تُحدَّد **`t10`** = أوّلُ دقيقةٍ إغلاقُها ‏≥10% فوق إغلاق الأمس
(**علامةٌ مثبَّتةٌ لا قانون** — اختيرت لأن شكوى المالك عند 20% فما دونها يسبقها).
ثم يُسجَّل **أوّلُ شرطٍ ساقطٍ** عند `t10` وفي كلّ دقيقةٍ حتى المِرساة ⇒ **توزيعُ
«مَن يحجب عند بدء الحركة»**. وهو نظيرُ `wall_stack` المُثبَتِ نفعُه في هذا المستودع.

## ⑦ قاعدةُ القرار — مكتوبةٌ قبل أيّ رقم

ذراعٌ **`engineering`** تُوصى **إن وإن فقط** استوفت الأربعة:
1. **التأخّرُ الوسيط ينخفض ‏5 نقاطٍ مئويّةٍ فأكثر** عن `G0`.
2. **والإثمارُ لا ينخفض بأكثرَ من 3.6 نقطة** (تقلّبُ القياس المقيس).
3. **وعددُ الإشعارات لا يزيد أكثرَ من 50%**.
4. **ولا تفقد سهمًا التقطه `G0`** (لا تراجعَ في التغطية).

⇒ **وإن لم تستوفِ ذراعٌ `engineering` فالحكمُ: «التأخّرُ ثمنُ أرقامِ المالك نفسِها»**
ويُسلَّم **منحنى الكلفة** لـ`G4`/`G5` **قرارًا له**. ⛔ **وسقفُ النجاح مثبَّتٌ:
اقتراحٌ على المالك ‏+ تغييرُ ثابتٍ `engineering` واحدٍ عند موافقته — ولا مسَّ
بـ`M1`-`M14` ولا بالجذور ولا بالدخول/الوقف/الأهداف.**

## ⑧ تنبّؤاتي — قابلةٌ للكسر

- **`P1`: الجدارُ الغالبُ عند `t10` هو رفعةُ الدقيقة 5%** — لأن السهمَ يصعد 10%
  على دقائقَ صغيرةٍ متتاليةٍ بلا دقيقةٍ واحدةٍ تبلغ 5%.
- **`P2`: `G1` (موضعُ الإغلاق) يحرّك التأخّرَ أقلَّ من نقطتين** — أضعفُ الشروط.
- **`P3`: تأخّرُ `G0` الوسيط ‏≥15%** (متّسقًا مع «فوق 20%»).
- **`P4`: `G6` يخفض التأخّرَ ‏≥10 نقاطٍ ويضاعف الإشعارات ‏≥3×.**
- **`P5`: لا ذراعَ `engineering` تستوفي الأربعة** ⇒ التأخّرُ من أرقام المالك.

## ⑨ حدودُ صدقٍ تُقرأ مع النتيجة لا بعدها

1. **جلسةٌ واحدة** ⇒ تشخيصٌ لا تجربةَ عائد · **ولا صفقةَ تُقاس**.
2. **«الإثمار» لمسُ ‏+3% لا ربحٌ محقَّق** ⇒ سقفٌ متفائل.
3. **الإعادةُ ترى كلَّ دقيقة** والحيُّ يلفّ كلَّ ‏≈60 ثانية ⇒ **الأسبقيّةُ سقف**.
4. **إغلاقُ الأمس معدَّلٌ بالتقسيمات** ⇒ مقسَّمٌ حديثًا قد يُشوّه «التأخّر» —
   والحالاتُ الشاذّة (تأخّرٌ فوق 500%) **تُعَدّ وتُستبعَد وتُسمّى**.
5. **`t10` علامةٌ مختارةٌ لا قانون** — تُغيّرها تُغيّر توزيعَ الجدار.
6. **الكونُ رقيق** (أسهمُ فيصل) ⇒ لا يُعمَّم على كونٍ سائل.
7. **وعددُ الإشعارات عبورُ دقائقَ لا رسائل** (المِرساةُ رسالةٌ والباقي تحديثات) —
   ويُعرَض العدّادان معًا.
