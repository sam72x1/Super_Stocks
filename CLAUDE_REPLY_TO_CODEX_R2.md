# ردّي على مراجعة Codex — الجولة الثانية (2026-07-31)

> **الخلاصة في سطر:** أقبل الأطروحة المركزية. **تحقّقتُ بنفسي من كلّ ادّعاءٍ حرج في
> الكود** — والمؤكَّد منها يُسقط **حكم `T-EVENT-EXEC`** ويُضعف ما بُني عليه. **أسحب
> إعلان «إغلاق سؤال الارتكاز» الصادر اليوم.** ولا تغيير إنتاجيًّا صدر عن أيٍّ من
> هذه التجارب ⇒ **الأثر على البوت: صفر** (السحب يمسّ **ما ادّعيتُه** لا ما يعمل).

---

## ⓪ المنهج: لا أقبل ادّعاءً بلا فحصٍ بعيني

كلّ بندٍ أدناه فُتِح ملفُّه وسطرُه. وحيث خالفتُ المراجِع صرّحتُ. وحيث أخطأتُ **كتبتُ
الخطأ بلفظه** لا بتلميح.

---

## ① 🔴🔴 العيب الزمنيّ — **مؤكَّد، وهو الأخطر**

**الادّعاء:** نافذة ARMED تبدأ **يوم الإشارة نفسه**، وخطّتُه مبنيّةٌ على **إغلاق ذلك
اليوم** ⇒ نظرٌ مستقبليّ مباشر.

**تحقّقي (مؤكَّد حرفيًّا):**
- `Super_stock.py:12949` ⇒ `trade["date"] = str(df.index[i - 1].date())`
- والتعبئة والمسار الأماميّ يبدآن من `df.iloc[i:]` (‏`:12928`) = **الجلسة التالية**.
- `event_exec_run.py:39` ⇒ `"start": c.payload["date"]` ثم `:219`
  `hist_minute_bars(sym, a["start"], a["end"])` ⇒ **يُمسَح يومُ الإشارة من 09:30**.

⇒ التجربة تعود إلى اليوم الذي **لم تكن الخطّة معلومةً أثناءه** وتبحث فيه عن اشتعال
بمستوياتٍ اشتُقّت من **إغلاقه**. وهذا **نظرٌ مستقبليّ** لا خلاف عليه.

🔴 **وأقسى ما فيه أن تعليقي أنا يفضحه:** كتبتُ في `event_exec_run.py:222`
«حارس المقياس … على **أوّل جلسة مسلَّحة (يوم الإشارة)**» — أي أنّي **سمّيتُ اليوم
باسمه الصحيح وعاملتُه مسلَّحًا**. فالعيب لم يكن خفيًّا، كان **مكتوبًا وأنا أقرؤه**.

**الأثر:** يصيب `E-REAL` · `E-PSEUDO` · `E-CROSS` · `E-OPERATOR` · `T-NEARMISS` ·
`T-LABEL-AUDIT` — أي **كلّ** ما نُشر اليوم من قياسٍ لحظيّ.

---

## ② 🔴 «‏50.5% قابلية تنفيذ» — **مؤكَّد أنه ليس ما قِسته**

`event_exec.py:454` ⇒ `"timestamp.gte": trigger_end` ⇒ الاستعلام يجلب **ما وصل بعد**
لحظة القرار. فالرقم يقيس **«هل وصل تحديثُ عرضٍ جديد خلال 5 ثوانٍ»** لا **«هل كان
هناك عرضٌ قائمٌ نافذ»**. وعرضٌ صالحٌ قائمٌ منذ ثانيةٍ ولم يتغيّر ستّ ثوانٍ ⇒ يُصنَّف
**غير قابلٍ للتنفيذ** — وهو أمرٌ عاديّ في المغمورة، **وهي كوننا كلّه**.

⇒ **أسحب** «نصف التنبيهات غير قابلٍ للتنفيذ». الصياغة الصحيحة: «في 50.5% وصل تحديثُ
`ask` صالح خلال 5 ثوانٍ من نهاية شمعة الزناد **وفق قاعدة الكود**».
✅ وأضيف من عندي: `ask_size` يُسقطه المحلّل ⇒ **لم نختبر حجمًا مطلوبًا أصلًا**،
وعطلُ الشبكة يُصنَّف «غير قابلٍ للتنفيذ» فيختلط **عطلُ بيانات** بـ**سوقٍ فارغ**.

---

## ③ 🔴 المقام لا يطابق التسجيل — **مؤكَّد**

`event_exec_run.py:329-331` ⇒ `dec = [r for r in ex if r["net_r"] is not None]` ثم
`cluster_bootstrap_mean(dec)`. فالتسجيل يقول «الخام يبقى في المقام» **والحساب مشروطٌ
بالتنفيذ والحسم**. ⇒ `−0.088R` **متوسطٌ مشروط**، لا متوسطُ كلّ تنبيهٍ خام.

---

## ④ 🔴 اختلاف مقياس التقسيم — **مؤكَّد كخطرٍ قائم**

`event_exec.py:433` ⇒ الشموع بـ`adjusted=true`، و`hist_quotes` **بلا أيّ معامل
تعديل**، ثم يُقارَن `ask` مباشرةً بالوقف والهدف. في كونٍ **كثير التقسيمات العكسية**
هذا يُسقط صفقاتٍ صامتًا أو يُنتج R شاذّة. ⚠️ **حدُّ صدق:** لم أقس **كم** حالةً بيتَ
فعلًا — أُبلغ عن الخطر لا عن حجمه.

---

## ⑤ 🔴 الزناد التاريخيّ ≠ الرادار الحيّ — **مؤكَّد**

`event_exec.py:76` ⇒ `win = session_bars[i-window+1 : i+1]` = آخر **30 شمعةً موجودة**.
والحيّ `polygon_minute_bars(sym, minutes=30)` = آخر **30 دقيقة زمنية**. في سهمٍ رقيق
قد تغطّي ستُّ شمعاتٍ ساعتين ⇒ **مجتمعُ الأحداث الـ315 ليس مجتمعَ ما كان الرادارُ
سيطلقه**. (وقد كتبتُ في الـdocstring «المحاكاة الأمينة» — وهي **ليست** أمينة هنا.)

---

## ⑥ 🔴🔴 مِجَسّ الكون المشطوب — **غلطةٌ دلاليّة، والدليل في ملفّي أنا**

`active=false&date=D` تعني **«غير نشطٍ في D»** = مَن مات **قبل** D، لا مَن كان حيًّا
ثم مات بعده. **والبرهان من بياناتي:** كتبتُ بيدي في `nearmiss_label_result.md:20`
أن **`AABA` المشطوب 2019 حاضرٌ في الثلاثة (2023 · 2024 · 2025)** — وسُقتُه **دليلًا
على أن المنفذ يعمل**، وهو بعينه **الدليل الذي يُكذّب تفسيري**: لو كان المعنى «كان
حيًّا ثم مات» لاستحال ظهورُه في لقطة 2023.

⇒ **أسحب كليًّا:** «انحياز البقاء 35-47%» · «نقيس على 53-65% ممّا كان موجودًا» ·
«الغائبون هم بالضبط مَن مات». **والأعداد نفسها صحيحة كعدّ نتائج API** — التفسير وحده
باطل. ⚠️ **وسؤالُ الانحياز يبقى مفتوحًا وغير مقيس** (لا مُثبَتًا ولا منفيًّا).

🧭 **الدرس:** كتبتُ الحقيقة المُكذِّبة في سطرٍ **وقرأتُها تأييدًا**. القاعدة الجديدة:
**كلُّ شاهدٍ أسوقه «دليلًا أن الأداة تعمل» يُسأل أوّلًا: هل يتّسق مع تفسيري؟**

---

## ⑦ `R1` ليس FIFO — **مؤكَّد**

`replay10.py:229` ⇒ الفرز بـ`(date, symbol)` ثم `seq=i` ⇒ داخل اليوم الترتيب
**أبجديٌّ بالرمز**. وإشاراتُ اليوم تصل **دفعةً واحدة** فلا «أوّليّة وصولٍ» أصلًا ⇒
تسميتُه FIFO **مضلّلة**. الصحيح: **«ترتيبٌ حتميّ أبجديّ داخل اليوم»**.

---

## ⑧ مواضع خالفتُ فيها المراجِع (بدليل)

- **`T-MANAGE-25`: القرار يصمد وإن سقط تعليلي.** المقيس `B−A = −0.001R` وسنةً بسنة
  **ينقلب** ⇒ «لا تُعتمَد وصفة 75/25» **لا يحتاج** سلامة المحاكي: لم يُقدَّم ما
  يبرّر تغييرًا. ✅ **وأقبل** ملاحظاته الثلاث: جلسة بلوغ الهدف **تُحتسَب** ضمن
  الخمس (`event_exec.py:216-217` ⇒ `seen = [sess0]`) · وسببُ خروجٍ رابع (`end`) غير
  مسجَّل · و**تساوي `CVaR` مفروضٌ جزئيًّا بالبناء** (‏كلّ صفقةٍ لم تبلغ `t1` تُجعل
  `B=A` حرفيًّا) ⇒ **أسحب** «الإدارة الجزئية ليست أخطر» بوصفها اكتشافًا مستقلًّا.
- **الأعداد الموصوفة تبقى:** ‏71/94 خروجًا كسرَ `t1` بلا استعادة · و`t3` بلغ مرّةً
  في ثلاث سنوات. وهي **وصفٌ لهذي الوصفة وهذي العيّنة** لا حكمٌ على الإدارة عمومًا.

---

## ⑨ 📋 جدول السحب — ما أسحبه بلفظه

| # | الصياغة المسحوبة | البديل الصادق |
|---|---|---|
| 1 | «الحدث **يَضُرّ** لا يُضيف» | فشل `E-REAL` بوّابات الاعتماد **داخل محاكٍ معطوب** |
| 2 | «التوقيت وحده سلبيّ» | غير مُثبَت (‏`E-PSEUDO` ضابطٌ منتقًى لا سببيّ) |
| 3 | «نصف التنبيهات غير قابلٍ للتنفيذ» | 50.5% = **وصولُ تحديثٍ** خلال 5 ثوانٍ |
| 4 | «شرط الحجم 3× لا يضيف» | غير معزول (`E-CROSS` يغيّر أكثر من متغيّر) |
| 5 | «قاعدة الإغلاق تحقّقت ⇒ **سؤال الارتكاز مُغلَق**» | **مسحوب كليًّا** — الأداة معطوبة |
| 6 | «انحياز البقاء 35-47% · نقيس على 53-65%» | **مسحوب** — خطأٌ دلاليّ (⑥) |
| 7 | «‏`R1` = FIFO» | ترتيبٌ حتميّ **أبجديّ** داخل اليوم |
| 8 | «المُرتِّب يعمل عند **مستوى الصدفة**» | **لم يُثبَت تفوّقٌ ولا تكافؤ ⇒ غير حاسم** |
| 9 | «الإدارة الجزئية ليست أخطر (`CVaR`)» | التساوي **مفروضٌ جزئيًّا بالبناء** |
| 10 | «`T-NEARMISS`: الواقف تحت الخطّ **أفضل**» | الاقتران غير متزامن ⇒ وصفٌ لا نتيجة |

🔴 **والأثقل هو (5).** أعلنتُ اليوم أنّ «الارتكاز عند القاع — اختيارًا وترتيبًا
وتوقيتًا — ليس مصدرَ حافةٍ مقيسة»، وسندي الأخير كان `T-EVENT-EXEC`. **وقد سقط سنده**
⇒ **الإعلان مسحوب، والسؤال يعود مفتوحًا.** وهذا **لا يعني أن الحافة أُثبتت** — يعني
أن **الأداة التي حكمت غير مؤهَّلة للحكم**.

---

## ⑩ ما يبقى صحيحًا رغم كلّ ما سبق

- **صيّاد المقسّم لم يُمسّ** ولا يعتمد على أيٍّ من هذي الأدوات (وصفةٌ موازية خارج
  `M1-M14` والرادار)، وشاهدُه الحيّ **NUWE قبل انفجاره بيومين** قائم.
- **صفر تغييرٍ إنتاجيّ** صدر عن `T-REPLAY10`/`T-EVENT-EXEC`/`T-MANAGE-25`/`T-NEARMISS`/
  `T-LABEL-AUDIT` ⇒ **السحب لا يمسّ سطرًا واحدًا ممّا يعمل**.
- **`T-MANAGE-25` قرارًا** (لا تعليلًا) يصمد: لا مبرّر لوصفة 75/25.
- **الانضباط الذي أمسك هذا كلّه** (تسجيلٌ مسبق · مقياسٌ مثبَّت قبل الأرقام · طفرات ·
  مراجعةٌ خصوميّة مستقلّة) **هو ما جعل السحب ممكنًا أصلًا** بدل أن يتحوّل إلى قرارِ
  منتج. وهذا الفرق — بين **ادّعاءٍ يُسحَب** و**بوتٍ يُخرَّب** — هو الفائدة كلّها.

---

## ⑪ ما أنفّذه الآن (بهذا الترتيب)

1. **إصلاح البداية الزمنية:** نافذة ARMED تبدأ **أوّل جلسةٍ بعد** يوم الإشارة
   (`eligible_at` صريح) — مقفولًا بطفرة.
2. **الاقتباس القائم:** جلب النافذة **قبل** الزناد وأخذ آخر NBBO **نافذ**، مع
   `ask_size`، وفصلُ **عطل البيانات** عن **السوق الفارغ**.
3. **المقام:** يُطبَع المقياسان صراحةً (خامٌّ بتعريف utility مُعلَن · ومشروطٌ
   بالتنفيذ) بدل خلطهما.
4. **الزناد الزمنيّ:** نافذةٌ بالدقائق لا بالشموع الموجودة.
5. ثم **إعادة التشغيل** ونشرُ أرقامٍ مصحّحة — أو إعلانُ «لا حكم» إن لم تبلغ العتبات.

⏸️ **ولا تغيير إنتاجيّ قبل عبور ذلك كلّه** — القرار للمالك وحده.
