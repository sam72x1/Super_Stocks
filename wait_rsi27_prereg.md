# `T-WAIT-RSI27` — «سحب السيولة المتوقّع = RSI 27» سياسةَ دخول (تسجيلٌ مسبقٌ **قبل أيّ رقم**)

> **المصدر — بيانات فيصل حرفيًّا (أمرُ المالك 2026-09-09: «اشتغل حسب بيانات فيصل لانها ادق و اصح»):**
> - GWAV (دليلُ طريقة فيصل ص91 · شارتٌ **أسبوعيّ** · لقطةٌ بعيني): «سحب السيوله المتوقع من 2.70
>   **يمثل هدف الشورت = RSI 27** · يتم تحليله بعد سحب السيوله» — و«GWAV: لم يصل RSI 27» =
>   **شرطُ انتظارٍ** حرفيًّا (`FAISAL_GUIDE_NOTES.md` سطر 138).
> - `TG_2043` (فيصل): «ضغط الارتكاز لابد قبل ينفجر يكون **RSI بين 23-27**» ⇒ حدُّه الأدنى **23**.
> - ص54: «اذا حللنا سهم ارتكاز **ممنوع الدخول** … انتظر اقل سعر 10% او 20% تحت» (قِيست في `T-WAIT-LOWER`).
> - وقفُ فيصل للارتكاز **بنيويٌّ** (5-7% تحت الدعم · وقفُ القاع المشحون `PIVOT_STOP_AT_LOW`) — لا يتحرّك مع الدخول.
> **أمرُ المالك:** صلاحيةٌ كاملة بشرط بيانات فيصل ⇒ **كلُّ رقمٍ في هذا العقد من نصّه** (‏27 · 23 · الأسبوعيّ ·
> وقفُه البنيويّ)، والرقمان الهندسيّان الوحيدان مُعلَنان (حدُّ الأرضية 100 من `T-EXIT` · تعبئةُ 30% من `T-WAIT-LOWER`).

## ⓪ لماذا التجربةُ مشروعة — وما حدُّ صدقها المسبق (يُكتب قبل الأرقام)
- النظامُ **ارتكاز** بنصّ فيصل («سهم ارتكاز … انتظر») ⇒ لا استيرادَ نظام. والهدفُ `t1` نفسُه (لا هدفَ مستورد).
- 🔴🔴 **حدُّ صدقٍ أوّل — داخلَ العيّنة:** ذراعُ `W27` في `T-WAIT-LOWER` قِيست **وصفيًّا** على اللقطات الثلاث
  نفسِها وأعطت **+0.0715 · +0.1058 · +0.0861** (‏≈ +0.088R) فعبرت `WL1`+`WL2` «مرشَّحًا لتسجيلٍ لاحق لا حكمًا».
  ⇒ **إعادةُ قياس `R27` على 2023-2025 ليست تأكيدًا مستقلًّا**: الأداةُ حتميّةٌ فستُعيد الأرقامَ نفسَها بت-بت
  (وهذا **بوّابةُ صلاحية `V-R4`** لا نتيجة). **الجديدُ الذي يشتريه هذا العقد خمسة:** ① فاصلُ بوتستراب مزدوج
  (أزواجُ `W27` لم تُصدَّر) ② تفكيكُ الحافة على الصفوف (لم يُجرَ) ③ قراءةُ **شارت فيصل الأسبوعيّ** كما قرأه هو
  (`R27w`) ④ حدُّه الأدنى **23** (`R23`) ⑤ **سنةٌ خارج العيّنة 2026 حتى تاريخه** على لقطةٍ **جُمِّدت اليوم**
  (تشغيلةُ `freeze.yml` `34322427155` · as-of 2026-09-09 · بدءُ التاريخ 2023-10-01 · **جُمِّدت قبل أيّ رقم**).
- 🔴 **حدُّ صدقٍ ثانٍ — الاحتمالُ الأسبق:** خمسُ تجاربِ توقيتِ دخولٍ سابقة: أربعٌ ضرّت (‏`T-LIBVOL` −0.879 ·
  `T-LIBVOL-2` −0.820 · `T-TRANCHE` −0.199 · `T-RECLAIM-INTRADAY` −0.130) وواحدةٌ غيرُ سالبةٍ فشلت على عقدها
  (‏`T-WAIT-LOWER` +0.063 بفاصلٍ يلامس الصفر). **فالاحتمالُ الأسبق أن يسقط الفاصلُ أو السنةُ الخارجية.**
- ⚖️ **انحيازٌ بنيويٌّ مُعلَن ضدّ الفرضية:** نافذةٌ أماميّةٌ واحدة (`BACKTEST_FORWARD_DAYS`) ⇒ التعبئةُ المتأخّرة
  وقتُها لبلوغ `t1` أقلّ — كما في الإنتاج، فالمقارنةُ عادلةٌ ومحافِظة.
- ⛔ **ما لا يُعاد فتحُه:** «سحبٌ ثم فشلُ الهبوط واستعادة» (جاهزيةُ GWAV عند فيصل) قِيس مرّتين وضرّ
  (‏`T-SWEEP-RECLAIM` −0.041R · `T-RECLAIM-INTRADAY` −0.130R) — لا ذراعَ استعادةٍ هنا.

## ① السؤال
> على صفقات الفارز الإنتاجية نفسِها، هل **أمرُ حدٍّ أدنى عند سعر RSI 27** (سحبُ السيولة المتوقّع عند فيصل) —
> بالهدف `t1` نفسِه ووقفٍ **بنفس النسبة** تحت التعبئة الجديدة — يُحسّن التوقّعَ بوحدة `R₀` الثابتة، **مع احتساب
> ما لم يُعبَّأ صفرًا في المقام**، **وبفاصلِ ثقةٍ لا يلامس الصفر**، **وبإشارةٍ تصمد خارج العيّنة (2026)**؟

## ② المجتمعُ والمحرّك — صفرُ مسٍّ بالإنتاج
- **سنواتُ الحكم:** لقطاتُ PIT الثلاث (‏2023 `31574165523` · 2024 `31199493957` · 2025 `31191327731`) بإعدادات
  الإنتاج (`FAISAL_ONLY=1`) **ونفسُ أعلام `T-TRANCHE`/`T-WAIT-LOWER`** (`BT_REPLAY10=1` · `BT_ENVVALS=1`) ⇒
  المجتمعُ نفسُه (‏1620/1591/1606 صفقة).
- **سنةٌ خارج العيّنة (وصفيّةٌ بحدّها):** 2026 حتى تاريخه على لقطة `freeze.yml` `34322427155` (كونُ اليوم —
  **انحيازُ بقاءٍ مُعلَن**: المشطوبُ خلال 2026 غائب · والصفقاتُ حتى ‏≈40 جلسةً قبل as-of لأن `backtest_symbol`
  لا يُصدر صفقةً بلا نافذةٍ كاملة) — **تُطبَع بمداها الفعليّ (أوّلُ صفقةٍ وآخرُها) ولا تدخل حكمَ الثلاث.**
- صفقاتُ `backtest_symbol` الإنتاجية · إعادةُ الخطّة بـ`tranche_arms.plan_at` (تنادي `analyze_ticker` عند
  الفهرس نفسِه) · الحسمُ بـ`_resolve_arm` الإنتاجية بالاسم · سعرُ RSI بـ`rsi_target_price` الإنتاجية بالاسم
  (عكسُ Wilder · **تقريبُ شمعةٍ واحدة = الحدُّ الأعمق** — يُعلَن) · و`R₀` بـ`tranche_arms.r_fixed` بالاسم.
- **سعرُ التحليل** = إغلاقُ شمعة الإشارة. `Super_stock.py` **لا يُمَسّ بحرف** · الأداةُ قراءةٌ فقط (قفل AST) ·
  الإنتاجُ لا يستوردها · لا `LOGIC_VERSION`.

## ③ الأذرع — خمسٌ ولا سادسة (تُثبَّت هنا؛ إضافةُ ذراعٍ بعد الأرقام ممنوعة)
| الذراع | الدخول | الوقف | الهدف | المصدر |
|---|---|---|---|---|
| `R0` | متوسّطُ دفعات الإنتاج `entry0` | `stop0` حرفيًّا | `t1` | الإنتاج |
| `R27` 🥇 **الحاكمة** | `min(rsi_target_price(الإغلاقاتُ اليوميّة حتى الإشارة, 27), entry0)` · وإن كان RSI عند 27 أو تحته (`None`) فالسحبُ وقع ⇒ `entry0` | `entry × (stop0/entry0)` — **نفسُ النسبة** (= «يتم تحليله بعد سحب السيوله»: الوقفُ يُعاد رسمُه تحت الدخول الجديد) | `t1` | GWAV «هدف الشورت = RSI 27» |
| `R23` | كذلك بـ**23** | كذلك | `t1` | `TG_2043` «RSI بين 23-27» — حدُّه الأدنى |
| `R27w` | `min(rsi_target_price(الإغلاقاتُ **الأسبوعيّة**, 27), entry0)` — الأسبوعيّ = إغلاقاتُ الأسابيع (نهايةُ الجمعة) حتى الإشارة؛ إن لم تكن شمعةُ الإشارة جمعةً **تُسقَط شمعةُ الأسبوع الناقص** فيكون إغلاقُ هذا الأسبوع هو المجهول، وإلّا فالأسبوعُ التالي · `None` ⇒ `entry0` | كذلك | `t1` | شارتُ GWAV عند فيصل **أسبوعيّ** (الدفتر: «حالةٌ واحدة · أسبوعيّ») |
| `R27abs` | دخولُ `R27` | **`stop0` المطلق** (وقفُ فيصل البنيويّ لا يتحرّك) · وإن كان الدخول ‏≤ `stop0` ⇒ `skip` يُعدّ ولا يُخفى | `t1` | وقفُ فيصل «تحت الدعم» |

- **القيدُ الواحد `min(·, entry0)`:** لا ذراعَ تدخل فوق خطّة الإنتاج (النصُّ «ممنوع الدخول … انتظر» منعٌ لا
  ملاحقة). ويُطبَع معدّلُ «التبنيد» لكلّ ذراع.
- التعبئة = أوّلُ شمعةٍ `Low ≤ الدخول`. **`no_fill` = صفرُ عائدٍ ويدخل المقام** (المقامُ واحدٌ للأذرع).
- `R23` و`R27w` و`R27abs` **وصفيّةٌ** تُطبَع بنفس الجدول ولا تحكم — إلّا أن تعبر إحداها `WR1`+`WR2` وحدَها
  فتُذكر **مرشَّحةً لتسجيلٍ لاحق** لا حكمًا (القاعدةُ نفسُها التي أوصلت `W27` إلى هنا).

## ④ الحُكمُ — قبل أيّ رقم
- **المقياسُ الحاكم:** متوسّطُ `R₀` (وحدةُ وقفِ الإنتاج الثابتة `entry0 − stop0`).
- **`WR1` (الحاكم — أشدُّ من `WL1` لأن الذراعَ داخلَ العيّنة):** `R27 − R0` **موجبٌ في 2023 و2024 و2025 كلٍّ على
  حدة** **و**مجمَّعًا (موزونًا بالصفوف) ‏≥ **+0.05R** **و**الحدُّ الأدنى لفاصل **بوتستراب مزدوج 95%** للفرق المجمَّع
  (إعادةُ معايَنةِ الصفوف بأزواجها · 2000 عيّنة · بذرة 27 · `no_fill`=0) **أكبرُ من صفر**.
- **`WR2`:** تعبئةُ `R27` ‏≥ **‏30%** من تعبئة `R0` في كلّ سنة.
- **الأرضية:** ‏≥ **100** صفقةٍ مُعبَّأة في `R0` **و**الحاكمة لكلّ سنةِ حكم (وإلّا «لا حكم»).
- **`WR3` (خارج العيّنة — شرطٌ لازمٌ لوسم «مرشَّحٌ للشحن» لا لحكم الثلاث):** على 2026 حتى تاريخه `R27 − R0`
  **موجب** بتعبئةِ `R27` ‏≥ **50** صفقة — يُطبَع بمداه؛ وإن تعذّرت اللقطةُ أو قلّت التعبئة **حُجبت التوصيةُ**
  («لا توصية حتى يُقاس») وبقي حكمُ `WR1`/`WR2` وحدَه.
- **④-ب تفكيكُ الحافة (يُطبَع لكلّ سنة — تفسيرٌ لا حكم):** صفوفُ `R27` مقابل `R0` أربعُ سلال: `B0` لا تبنيد
  (`R27 ≡ R0` ⇒ فرقٌ صفر) · `B1` تبنيدٌ وكلتاهما مُعبَّأة (فرقُ `R₀` + مصفوفةُ الانتقال ربح/خسارة/عالق) · `B2`
  تبنيدٌ و`R27` لم تُعبَّأ و`R0` مُعبَّأة (الفرقُ = −`R₀(R0)` = **ما تجنّبته**) · `B3` `R0` لم تُعبَّأ (صفر).
  ومجموعُ السلال = الفرقُ الكلّيّ **(هويّةٌ تُفحَص)**.
- **بوّاباتُ الصلاحية (عطبُ أداة لا نتيجة):** `V-R0` حسمُ `R0` = حسمُ الإنتاج في ‏≥99% (وإلّا خروج 3) ·
  `V-R1` تعبئةُ `R27` ‏≤ تعبئة `R0` بالبناء (وإلّا 3) · `V-R2` الحاكمةُ تتفرّق عن `R0` (وإلّا `no-op` خروج 4؛
  الوصفيُّ المتطابق يُطبَع ولا يُسقط) · `V-R3` سنةُ اللقطة = سنةُ القياس (وإلّا 4) · **`V-R4` (سنواتُ الحكم
  فقط):** `R0` و`R27` يُعيدان أرقامَ `T-WAIT-LOWER` المنشورة **بت-بت بأربع خانات** (‏`R0` −0.2602/−0.1807/−0.2172
  · `R27` −0.1887/−0.0748/−0.1312 · الصفوفُ 1620/1591/1606 · تعبئةُ `R27` 779/680/678) وإلّا خروج 3 —
  **فالتكرارُ إثباتُ سلامة الأداة لا دليلًا جديدًا.**

## ⑤ التنبّؤات (تُنشَر مكذَّبةً أو محقَّقة — لا تُحذف)
- **`WR-P1`:** الحدُّ الأدنى لفاصل البوتستراب المجمَّع **بين 0 و+0.05R** (يعبر `WR1` بهامشٍ ضيّق).
- **`WR-P2` (آليّةُ الحافة):** الحافةُ **تجنُّبٌ لا تعبئةٌ أفضل**: سلّةُ `B2` (ما تجنّبته) تسهم بأكثرَ من **‏100%**
  من الفرق الكلّيّ، وسلّةُ `B1` (كلتاهما مُعبَّأة) **سالبةٌ أو صفر** في **سنتين على الأقلّ** من الثلاث.
- **`WR-P3`:** `R23` تتعبّأ **أقلَّ** من `R27` في الثلاث، وفرقُها المجمَّع **أدنى** من فرق `R27` (الانتظارُ الأعمق
  يخسر تعبئاتٍ أكثرَ ممّا يتجنّب).
- **`WR-P4`:** `R27w` (الأسبوعيّ) تبنّد في **أقلَّ من نصف** الصفوف (RSI الأسبوعيّ لسهمٍ انهار ‏≥50% غالبًا عند 27
  أو تحته أصلًا ⇒ `None` ⇒ `R0`)، وفرقُها المجمَّع **بين 0 وفرق `R27`**.
- **`WR-P5` (خارج العيّنة):** على 2026 حتى تاريخه `R27 − R0` **موجبٌ بين +0.03 و+0.15R** بتعبئةٍ ‏≥ 50.
- **`WR-P6`:** `R27abs` تُسقِط (`skip`) **‏≥ 40%** من الصفوف (سعرُ RSI 27 كثيرًا ما يقع تحت وقف فيصل البنيويّ)
  وفرقُها عن `R0` **سالبٌ أو دون +0.02R** ⇒ «الوقفُ البنيويّ» و«الانتظارُ حتى RSI 27» لا يجتمعان على الجاهز.

## ⑥ سقفُ النجاح
قياسٌ وتوصية. **لا يتحرّك `ENTRY_*` ولا وقفٌ ولا `LOGIC_VERSION` بهذي التجربة.** إن عبرت `WR1`+`WR2`+`WR3`
فالاقتراحُ للمالك بكلفته: **أمرُ حدٍّ اختياريّ عند سعر RSI 27 لأسهم «🟢 جاهز»** (السطرُ ⏳ العرضيّ قائمٌ سلفًا) —
والشحنُ كلمتُه بعد رؤية الجدول. والنتيجةُ تُدوَّن في `wait_rsi27_result.md` بجانب هذا العقد لا فوقه.
