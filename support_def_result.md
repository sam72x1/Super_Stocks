# 🧱🔬🔴 `T-SUPDEF` — النتيجة: **الفرعُ 3 · «لا قياس»**

> **العقد:** `support_def_prereg.md` ‏+ الملحق `§⑨` — **مدموجان قبل الأداة**.
> **التشغيلة:** `35397958641` على `main` (`369b1ea3`) · خروج **‏3** = *حكمٌ* لا عطب.
> **قراءةٌ فقط · صفرُ مسٍّ بالإنتاج · ولم يُشحَن شيءٌ ولن يُشحَن من هذا العقد.**

---

## ① الجدول — `V-S3` صفًّا صفًّا (تغطية: طُلبت ‏6 · نجحت ‏4 · استُبعدت ‏2)

| الرمز | التاريخ | الفريم | إغلاق | 🔴 فيصل | `A0` | `A1` | `A2` | `A-FAR` | خطأ `A0` |
|---|---|---|---|---|---|---|---|---|---|
| **PPBT** | 2026-09-05 | يوميّ | 1.750 | 1.682 | **1.16** | 1.41 | 1.34 | 1.16 | **‏−31.03%** |
| NUWE | 2026-09-18 | 4س\* | 0.897 | 0.712 | 0.71 | 0.73 | 0.71 | 0.71 | −0.27% |
| DAIC | 2026-09-18 | 4س\* | 3.550 | 1.986 | 0.37 | 0.42 | 0.41 | 0.37 | −81.37% |
| **SXTC** | 2026-09-18 | يوميّ | 2.400 | 2.064 | **1.78** | 1.93 | 2.12 | 1.78 | **‏−13.76%** |

\* وصفيٌّ بنصّ `§⑨-ⓐ` — **خارج كلّ معيار** (خلطُ فريمَين).

**والمستبعَدان بقاعدة `§②`** (كلُّ مستوياتهما **فوق** الإغلاق ⇒ لا «أقربَ تحته»):
`DCOY` (إغلاق 2.690) · `ELPW` (إغلاق 2.490).

## ② الحكم — ساقطٌ من بابين

- **`SD0` 🔴**: ثلاثيّاتٌ حاكمة **‏2** من **‏2** رمزًا · والأرضيّة **‏5/3**.
- **`SD3` 🔴**: وسيطُ \|خطأ `A-FAR`\| = **‏31.03%** = وسيطُ \|خطأ `A0`\| **بالضبط**
  ⇒ **شاهدُ الضبط لا يفرّق** ⇒ **المقياسُ غيرُ مُثبَتِ الصلاحية على هذي العيّنة.**

⇒ **الفرعُ 3 بنصّ `§④`: «لا قياس» — يُعلَن بعدَده ولا يُفسَّر · ولا يُرفَع العدُّ
بتأريخٍ مُستنتَج · ولا تُخفَّض الأرضيّة.**

## 🔴 ③ وعيبُ تصميمٍ في **شاهدي أنا** يُدوَّن

`A-FAR` (نافذة ‏120 جلسة) اختُير ليكون **معلومَ الخطأ**، لكنّ رموزَ هذي العيّنة
**صنعت قاعَها حديثًا** ⇒ أدنى قاعٍ في ‏120 جلسة = أدنى قاعٍ في ‏25 ⇒ **`A-FAR` ≡
`A0` بت-بت في الصفَّين الحاكمَين**. فالشاهدُ **خامدٌ بالبناء على هذا المجتمع**،
وهو عيبُ اختيارِ شاهدٍ لا عيبُ كود. ⚖️ **ولا يُبدَّل الآن** — تبديلُ شاهدٍ بعد
رؤية النتيجة **مطاردةُ نتيجة**؛ يُدوَّن لتسجيلٍ لاحق.

## ④ أرقامٌ وصفيّةٌ تُنشَر ولا تُفسَّر (‏`§④` الفرع 3)

- `A0` **أدنى** من مستوى فيصل في **‏4 من 4** صفوفٍ مقروءة (والحاكمُ منها 2).
- `A1` (أدنى إغلاق) **أقربُ** من `A0` في **‏4 من 4**.
- ‏`p` لاختبار الإشارة عند n=2 = **‏0.5** ⇒ **لا قوّةَ إحصائيّةَ أصلًا** عند هذا الحجم.

## ⑤ التنبّؤات — تُنشَر مكذَّبةً أو مؤكَّدة

| # | التنبّؤ | النتيجة |
|---|---|---|
| `P1` | `SD1` تعبر | ⚪ **بلا موضوع** — `SD0` تسقط قبلها (والاتّجاه 100% لكن p=0.5) |
| `P2` | `SD2` تعبر (‏≥10%) | ⚪ بلا موضوع — والقيمةُ ‏31.03% وصفيًّا |
| `P3` | `A1` أقربُ من `A0` | ✅ **مؤكَّدٌ وصفيًّا** (‏4/4) |
| `P4` | `SD0` تعبر بشقّ الأنفس (‏5-8) | 🔴 **مكذَّب** — بلغت **‏2** فقط |
| `P5` | `A-FAR` يسقط | 🔴 **مكذَّب** — **تعادل** مع `A0`، وهو عيبُ شاهدي |

🔴 **وتقديري في الملحق كان أدقَّ من تنبّؤي في العقد:** كتبتُ قبل التشغيل أن
«الفرعَ 3 مرجَّح» **فوقع**، لكنّي قدّرتُ الحاكمَ **‏3 صفوف** والواقعُ **‏2**
(‏`ELPW` سقط بقاعدة القراءة لأن إغلاقه ‏2.490 **تحت** مستواه ‏2.500 بفارق ‏0.4%).

## ⑥ تصحيحٌ على قراءةٍ سابقة

في تقرير `§⑦` قلتُ إن مستوى `PPBT` «كان مكسورًا طوالَ الشهر». **الأدقُّ:** ‏20 من
‏25 جلسةً أغلقت تحته، **لكنّ إغلاقَ يوم القصّ نفسِه ‏1.750 — أي فوقه**. الوصفُ
الصحيح: **مستوًى كُسر ثمّ استُعيد لتوّه**، وهو ما يجعل كلامَ فيصل («فشل السهم
تحت الرقم هذا مباشره») **تحذيرًا قائمًا لا وصفًا لماضٍ**.

## ⑦ ما يفتح المحورَ — ثلاثةٌ معًا

1. **عيّنةٌ تبلغ الأرضيّة**: تلزمها **تواريخُ** صور `IMG_xxxx` (‏≈34 صفًّا بمستوًى
   أحمر) — وهي **ليست في المستودع**، ولا تُستنتَج من رقم الصورة.
2. **شاهدُ ضبطٍ يفرّق فعلًا** على مجتمعٍ صنع قاعَه حديثًا (‏`A-FAR` لا يصلح).
3. **إذنُ المالك.** ⛔ **ولا يُمَسّ `pivot_stability` بأيّ حال من هذا العقد.**
