# 🧱🔬🔴 `T-SUPDEF` — النتيجة: **الفرعُ 3 · «لا قياس»**

> **العقد:** `support_def_prereg.md` ‏+ الملحق `§⑨` — **مدموجان قبل الأداة**.
> **التشغيلة:** `35397958641` على `main` (`369b1ea3`) · خروج **‏3** = *حكمٌ* لا عطب.
> **قراءةٌ فقط · صفرُ مسٍّ بالإنتاج · ولم يُشحَن شيءٌ ولن يُشحَن من هذا العقد.**

---

## ① الجدول — `V-S3` صفًّا صفًّا (تغطية: طُلبت ‏6 · نجحت ‏4 · استُبعدت ‏2)

| الرمز | التاريخ | الفريم | إغلاق | 🔴 فيصل | `A0` | `A1` | `A2` | `A-FAR` | خطأ `A0` |
|---|---|---|---|---|---|---|---|---|---|
| **PPBT** | 2026-09-05 | يوميّ | 1.750 | 1.682 | **1.16** | 1.41 | 1.34 | 1.16 | **‏−31.03%** |
| NUWE | 2026-09-18 | 4س\* | 0.897 | 0.712 | 0.71 | 0.73 | 0.71 | 0.71 | −0.27% |
| DAIC | 2026-09-18 | 4س\* | 3.550 | 1.986 | 0.37 | 0.42 | 0.41 | 0.37 | −81.37% |
| **SXTC** | 2026-09-18 | يوميّ | 2.400 | 2.064 | **1.78** | 1.93 | 2.12 | 1.78 | **‏−13.76%** |

\* وصفيٌّ بنصّ `§⑨-ⓐ` — **خارج كلّ معيار** (خلطُ فريمَين).

**والمستبعَدان بقاعدة `§②`** (كلُّ مستوياتهما **فوق** الإغلاق ⇒ لا «أقربَ تحته»):
`DCOY` (إغلاق 2.690) · `ELPW` (إغلاق 2.490).

## ② الحكم — ساقطٌ من بابين

- **`SD0` 🔴**: ثلاثيّاتٌ حاكمة **‏2** من **‏2** رمزًا · والأرضيّة **‏5/3**.
- **`SD3` 🔴**: وسيطُ \|خطأ `A-FAR`\| = **‏31.03%** = وسيطُ \|خطأ `A0`\| **بالضبط**
  ⇒ **شاهدُ الضبط لا يفرّق** ⇒ **المقياسُ غيرُ مُثبَتِ الصلاحية على هذي العيّنة.**

⇒ **الفرعُ 3 بنصّ `§④`: «لا قياس» — يُعلَن بعدَده ولا يُفسَّر · ولا يُرفَع العدُّ
بتأريخٍ مُستنتَج · ولا تُخفَّض الأرضيّة.**

## 🔴 ③ وعيبُ تصميمٍ في **شاهدي أنا** يُدوَّن

`A-FAR` (نافذة ‏120 جلسة) اختُير ليكون **معلومَ الخطأ**، لكنّ رموزَ هذي العيّنة
**صنعت قاعَها حديثًا** ⇒ أدنى قاعٍ في ‏120 جلسة = أدنى قاعٍ في ‏25 ⇒ **`A-FAR` ≡
`A0` بت-بت في الصفَّين الحاكمَين**. فالشاهدُ **خامدٌ بالبناء على هذا المجتمع**،
وهو عيبُ اختيارِ شاهدٍ لا عيبُ كود. ⚖️ **ولا يُبدَّل الآن** — تبديلُ شاهدٍ بعد
رؤية النتيجة **مطاردةُ نتيجة**؛ يُدوَّن لتسجيلٍ لاحق.

## ④ أرقامٌ وصفيّةٌ تُنشَر ولا تُفسَّر (‏`§④` الفرع 3)

- `A0` **أدنى** من مستوى فيصل في **‏4 من 4** صفوفٍ مقروءة (والحاكمُ منها 2).
- `A1` (أدنى إغلاق) **أقربُ** من `A0` في **‏4 من 4**.
- ‏`p` لاختبار الإشارة عند n=2 = **‏0.5** ⇒ **لا قوّةَ إحصائيّةَ أصلًا** عند هذا الحجم.

## ⑤ التنبّؤات — تُنشَر مكذَّبةً أو مؤكَّدة

| # | التنبّؤ | النتيجة |
|---|---|---|
| `P1` | `SD1` تعبر | ⚪ **بلا موضوع** — `SD0` تسقط قبلها (والاتّجاه 100% لكن p=0.5) |
| `P2` | `SD2` تعبر (‏≥10%) | ⚪ بلا موضوع — والقيمةُ ‏31.03% وصفيًّا |
| `P3` | `A1` أقربُ من `A0` | ✅ **مؤكَّدٌ وصفيًّا** (‏4/4) |
| `P4` | `SD0` تعبر بشقّ الأنفس (‏5-8) | 🔴 **مكذَّب** — بلغت **‏2** فقط |
| `P5` | `A-FAR` يسقط | 🔴 **مكذَّب** — **تعادل** مع `A0`، وهو عيبُ شاهدي |

🔴 **وتقديري في الملحق كان أدقَّ من تنبّؤي في العقد:** كتبتُ قبل التشغيل أن
«الفرعَ 3 مرجَّح» **فوقع**، لكنّي قدّرتُ الحاكمَ **‏3 صفوف** والواقعُ **‏2**
(‏`ELPW` سقط بقاعدة القراءة لأن إغلاقه ‏2.490 **تحت** مستواه ‏2.500 بفارق ‏0.4%).

## ⑥ تصحيحٌ على قراءةٍ سابقة

في تقرير `§⑦` قلتُ إن مستوى `PPBT` «كان مكسورًا طوالَ الشهر». **الأدقُّ:** ‏20 من
‏25 جلسةً أغلقت تحته، **لكنّ إغلاقَ يوم القصّ نفسِه ‏1.750 — أي فوقه**. الوصفُ
الصحيح: **مستوًى كُسر ثمّ استُعيد لتوّه**، وهو ما يجعل كلامَ فيصل («فشل السهم
تحت الرقم هذا مباشره») **تحذيرًا قائمًا لا وصفًا لماضٍ**.

## ⑦ ما يفتح المحورَ — ثلاثةٌ معًا

1. **عيّنةٌ تبلغ الأرضيّة**: تلزمها **تواريخُ** صور `IMG_xxxx` (‏≈34 صفًّا بمستوًى
   أحمر) — وهي **ليست في المستودع**، ولا تُستنتَج من رقم الصورة.
2. **شاهدُ ضبطٍ يفرّق فعلًا** على مجتمعٍ صنع قاعَه حديثًا (‏`A-FAR` لا يصلح).
3. **إذنُ المالك.** ⛔ **ولا يُمَسّ `pivot_stability` بأيّ حال من هذا العقد.**

---

# 🧱🔬📎 `T-SUPDEF` بالملحق `§⑩` — النتيجةُ الثانية: **الفرعُ 3 · «لا قياس»** (‏2026-09-19)

> **قسمٌ مؤرَّخٌ يُلحَق ولا يُبدّل ما فوقه** — النتيجةُ الأولى تبقى بحرفها.
> **العقد:** `support_def_prereg.md` ‏+ الملحق `§⑩` — **مدموجان قبل الأداة وقبل أيّ رقم**.
> **التشغيلتان:** `35425104529` على `main` (`638a27c2`) ثمّ `35425979255` (`37c6c35a`) —
> **الأرقامُ الحاكمةُ متطابقةٌ بت-بت في الاثنتين** (التشغيلةُ الثانية أضافت تشخيصًا
> يُطبَع ولا يُستعمَل). خروج **‏3** = *حكمٌ* لا عطب.
> **قراءةٌ فقط · صفرُ مسٍّ بالإنتاج · ولم يُشحَن شيءٌ ولن يُشحَن من هذا العقد.**

---

## ① الحرّاسُ أوّلًا — قبل أيّ رقم

| الحارس | النتيجة |
|---|---|
| `V-S1` قراءةٌ فقط | ✅ |
| `V-S2` `PIVOT_LOOKBACK`=25 · التسامح 2.0% | ✅ |
| **`V-S7`** شاهدُ الضبط يفارق على فِكستشر `ⓕ` | ✅ **قبل أيّ جلب** — `A-FAR` ≡ `A0` (‏2.8) و`A-RAND` يفارق بـ**‏80.92 نقطة** |
| **`V-S6`** هُويّةُ المجتمع | ✅ الأربعةُ أُعيد اشتقاقُها **بتواريخها بالضبط** من `faisal_price_anchor` |
| `V-S5` التغطية | طُلبت ‏10 · نجحت ‏8 · استُبعدت ‏2 |

**والمُستبعَدان بقاعدة `§②`** (كلُّ مستوياتهما فوق سعر المرجع): `DCOY` (‏2.690) ·
`ELPW` (‏2.490) — نفسُ سببِ استبعادهما في النتيجة الأولى.

## ② الجدول — `V-S3` صفًّا صفًّا

| الرمز | as-of | الفريم | سعرُ المرجع | 🔴 فيصل | `A0` | خطأ `A0` | خطأ `A-RAND` |
|---|---|---|---|---|---|---|---|
| **PPBT** | 2026-09-05 | يوميّ | 1.750 | 1.682 | 1.16 | **‏−31.03%** | 20.45% |
| **SXTC** | 2026-09-18 | يوميّ | 2.400 | 2.064 | 1.78 | **‏−13.76%** | 76.89% |
| **TRUG** | 2026-03-27 | يوميّ | 4.470 | 3.967 | 3.50 | **‏−11.77%** | 47.53% |
| **TURB** | 2026-04-01 | يوميّ | 3.105 | 2.819 | 0.657 | **‏−76.69%** | 50.83% |
| **GRI** | 2026-04-10 | يوميّ | 2.145 | 2.100 | 2.115 | **‏+0.71%** 🔴 | 20.52% |
| **VEEE** | 2026-05-13 | يوميّ | 6.210 | 5.990 | 5.00 | **‏−16.53%** | 38.36% |
| NUWE | 2026-09-18 | 4س\* | 0.900 | 0.712 | 0.71 | −0.27% | 50.88% |
| DAIC | 2026-09-18 | 4س\* | 3.550 | 1.986 | 0.37 | −81.37% | 93.89% |

\* وصفيٌّ بنصّ `§⑨-ⓐ` — **خارج كلّ معيار**. و`A1`/`A2`/`A-FAR`/`A-MID`/`A-HIGHLOW`
طُبعت كلُّها وصفيّةً بنصّ `§⑩-ⓖ`.

🔑 **و`as-of` هو `match_date` بنصّ `ⓒ`:** الأنواعُ `spot` قُصَّت عند **اليوم السابق**
لتاريخ الشارت (‏`TURB` 04-01 لشارت 04-02 · `GRI` 04-10 لشارت 04-13 · `VEEE` 05-13
لشارت 05-14) — فلا تدخل الحسابَ شمعةٌ لم تكتمل على شاشته.

## ③ الحكم — **ثلاثةٌ من أربعةٍ تعبر، والرابعُ يسقط بصفٍّ واحد**

| المعيار | القيمة | الحدّ | الحكم |
|---|---|---|---|
| **`SD0`** | **‏6** ثلاثيّات من **‏6** رموز | ‏≥5 من ≥3 | ✅ **تعبر — لأوّل مرّة** |
| **`SD1`** | `A0` أدنى في **‏5/6 = 83.3%** · `p = 0.2188` | ‏≥80% **و**`p ≤ 0.10` | 🔴 **تسقط على `p`** |
| **`SD2`** | وسيطُ \|خطأ `A0`\| = **‏16.53%** | ‏≥10% | ✅ تعبر |
| **`SD3`** | `A-RAND` **‏47.53%** مقابل `A0` ‏16.53% | الشاهدُ أسوأ | ✅ تعبر |

⇒ **`V-S8` سببُ الفرع 3 مفصولًا: «`SD1` لم تُستوفَ (الشاهدُ عبر)»** — وهو **سببٌ
ثالثٌ مختلفٌ** عن سببَي النتيجة الأولى (الأرضيّة · والشاهدُ الخامد).

🔑 **وهذا بالضبط ما حسبه `§⑩-ⓘ-1` قبل التشغيل:** «عند `n = 6` ⟵ ‏6/6 ⟶ `p = 0.0312` ✓
و‏5/6 ⟶ `p = 0.2188` ✗ ⇒ **صفٌّ واحدٌ مخالفٌ يُسقط `SD1` مهما بلغت النسبة**». فالنتيجةُ
**سقفُ عيّنةٍ لا نفيُ أثر** — ولا تُقرأ دليلًا على غياب الانحياز، ولا دليلًا على وجوده.

⛔ **ولا يُخفَّض حدُّ `p` بعد رؤيته** (`§⑩-ⓚ`) — وهو أوّلُ ما يخطر، وأوّلُ ما يُرفَض.

## ④ الصفُّ المخالف — `GRI`، وما يكشفه

`GRI`: مستوى فيصل **‏2.100** و`A0` **‏2.115** ⇒ `A0` **أعلى** بـ‏0.71%. فالمستوى الذي
رسمه فيصل يقع **تحتَ أدنى قاعٍ في نافذة `A0` كلِّها (‏25 جلسة)** ⇒ **رسمه من بنيةٍ
أقدمَ من النافذة**.

🔴 **وهذا يُكذّب `Q2` وتعليلَه معًا:** كتبتُ «ثقةٌ عالية — `A0` أدنى قاعٍ **بالتعريف**»،
والتعليلُ ناقص: `A0` أدنى قاعٍ **داخلَ نافذته**، ومستوى فيصل ليس ملزَمًا بأن يقع فيها.

📏 **والفجوةُ ليست ثابتةً بل طيفٌ عريض:** من **‏+0.71%** (‏`GRI`) إلى **‏−76.69%**
(‏`TURB`). وأقصاها `TURB` لأن نافذةَ الـ25 جلسةً **تشقّ انفجارًا**: السعرُ جاء من
‏0.657 إلى ‏3.105، فـ`A0` يرسو على **أرضيّة ما قبل الانفجار** بينما فيصلٌ يرسم داخلَ
البنية الحديثة. **وصفٌ لا حكم** — العيّنةُ ستّة.

## ⑤ الحساسيّاتُ الثلاث — `V-S9` (وصفيّةٌ، لا تدخل معيارًا)

| الحساسيّة | حاكمة | `SD1` | `SD2` | `SD3` | الفرع |
|---|---|---|---|---|---|
| **القاعدةُ المسجَّلة** | 6/6 | 83.3% · p=0.2188 | 16.53% | 47.53% ✅ | **3** |
| `ⓒ` القصُّ عند `date` | 6/6 | 83.3% · p=0.2188 | 16.53% | 47.53% ✅ | 3 |
| `ⓓ` المرجعُ = إغلاقُ `match_date` | **5/5** | 80.0% · p=0.375 | 13.76% | 38.36% ✅ | 3 |
| `ⓔ` بلا وسمِ مؤشّر السعر | 6/6 | 83.3% · p=0.2188 | 16.53% | 47.53% ✅ | 3 |

✅ **و`ⓓ` تُثبت أن الإفصاحَ المسبق كان صادقًا:** الملحقُ أعلن قبل التشغيل أن `TURB`
**هو الصفُّ الوحيد** الذي تُبدّله قاعدةُ سعر المرجع — **وسقط وحدَه** (‏6 ⟶ 5). **والفرعُ
لا يتغيّر بأيّ حساسيّة** ⇒ القاعدةُ المختارة لم تصنع النتيجة.

🔴 **واكتشافٌ يُدوَّن: قاعدةُ `ⓔ` فيّالةٌ على هذي العيّنة — أطلقت ولم تُغيّر شيئًا.**
العدّادُ يقول «‏1 مستوًى استُبعد» (‏`TRUG` 4.470)، لكنّ حساسيّةَ `ⓔ` أعادت **الأرقامَ
الثلاثةَ بت-بت** — ولو تبدّل مرجعُ `TRUG` لتحرّك الوسيطان (‏`SD2` لأن خطأه يصير
‏−21.70% · و`SD3` لأن **قيمتَه هي الوسيط** ‏47.53%) ⇒ **الإغلاقُ المطابَق ليس فوق
‏4.47**، فقاعدةُ `§②` كانت ستستبعده أصلًا. ⇒ **`ⓔ` مكرّرةٌ مع `§②` هنا**، وتعليلي في
الملحق (الإغلاقُ المُشتقّ ‏4.4701 **فوق** المستوى) **صحيحٌ عن الرقم المُشتقّ وخاطئٌ عن
الرقم المطابَق**. ولا تُحذَف القاعدةُ بهذا (‏`ⓚ`) — تُدوَّن خموديّتُها المقيسة.

## ⑥ شواهدُ هُويّةٍ لم تُطلَب ومرّت

1. **ضدّ منشورٍ سابق:** `PPBT` (‏`A0`=1.16 · ‏−31.03%) و`SXTC` (‏1.78 · ‏−13.76%)
   **أُعيدا بت-بت** كما في `§①` من النتيجة الأولى ⇒ الآلةُ المُعادُ بناؤها
   **متحقَّقٌ منها ضدّ قياسٍ منشور** لا «تعمل» فقط.
2. **حتميّةُ `A-RAND`:** تشغيلتان مختلفتان (`638a27c2` و`37c6c35a`) أعطتا
   **‏47.53% نفسَها** ⇒ البذرةُ تعمل فعلًا عبر التشغيلات لا في الذاكرة فقط.
3. **`A-FAR` ما زال خامدًا:** وسيطُ \|خطأ `A-FAR`\| = **‏16.53% = وسيطُ `A0` بالضبط**
   ⇒ عيبُ النتيجة الأولى **مُثبَتٌ مرّةً ثانية**، وتقاعدُه كان صوابًا.
   و`A-RAND` يفصل بـ**‏31 نقطة** — **وليس ختمًا مطاطيًّا**: `V-S7` يُسقطه على فِكستشرٍ
   منهار، و`SDY8` يُثبت أن `SD3` تسقط حين يتفوّق الشاهد.

## ⑦ حدُّ صدقٍ **اكتُشف بعد الحكم** — فقِيس ولم يُصلَح صامتًا

مستوياتُ فيصل والسعرُ المسجَّل بمقياس **الشاشة**، و`A0` بمقياس المزوّد **المعدَّل
بالتقسيمات** ⇒ صفٌّ بعامل تقسيمٍ غيرِ الواحد **غيرُ قابلٍ للمقارنة**. الملحقُ لم ينصّ
عليه، **والعقدُ مدموجٌ لا يُعدَّل** ⇒ أُضيف **تشخيصٌ يُطبَع ولا يُستعمَل** (‏`SDY10`
يقفل أن صفرَ دالّةٍ حاكمةٍ تقرؤه).

📊 **والمقيس: العاملُ = ‏1 في الأربعة كلِّها** ⇒ **الحدُّ لا يَعَضّ على هذي العيّنة**،
ويبقى شرطًا واجبَ الفحص في أيّ توسيعٍ لاحق.

## ⑧ التنبّؤات — تُنشَر مكذَّبةً أو مؤكَّدة

| # | التنبّؤ (بثقته المعلنة) | النتيجة |
|---|---|---|
| `Q1` | `SD0` تعبر — **متوسّطة** | ✅ **مؤكَّد** (‏6 من 6 رموز) |
| `Q2` | `SD1` تعبر بالإجماع — **عالية** | 🔴 **مكذَّب** بصفٍّ واحد (`GRI`) — **وتعليلي كان ناقصًا** |
| `Q3` | `SD2` تعبر — **منخفضة** | ✅ **مؤكَّد** (‏16.53%) |
| `Q4` | `SD3` تعبر (`A-RAND` أسوأ) — **متوسّطة** | ✅ **مؤكَّد** (‏47.53 مقابل 16.53) |
| `Q5` | صفٌّ من الأربعة يسقط — **عالية** | 🔴 **مكذَّب** — دخلت الأربعةُ كلُّها |
| `Q6` | الأرجحُ الفرعُ 1 أو 3 | ✅ **مؤكَّد** (الفرعُ 3) |

🔴 **والمكذَّبان هما وحدَهما اللذان وسمتُهما «ثقةٌ عالية»** — والمنخفضةُ ثقةً مؤكَّدة.
⇒ **درسٌ يُدوَّن: ثقتي المعلنةُ في هذي التجربة كانت مقلوبةَ الترتيب.**

## ⑨ ما يعنيه هذا — وما لا يعنيه

- **يعني:** المجتمعُ بلغ أرضيّتَه لأوّل مرّة (بفضل التأريخ بمرساة السعر) ·
  وشاهدَ الضبطِ صار يفرّق · والفجوةُ بين `A0` ومستوى فيصل **موجودةٌ ومادّيّةٌ
  وصفيًّا** (وسيطُ ‏16.53%، وطيفٌ من ‏+0.7% إلى ‏−76.7%).
- **لا يعني** أن الانحيازَ «مُثبَت»: `SD1` هي المعيارُ المسجَّل، وقد سقطت.
  **«لا قياس» تُعلَن بعدَدها ولا تُفسَّر** (‏`§④`).
- **ولا يُشحَن شيء** (‏`§⑥-6` و`§⑩` رأسُه) · **و`pivot_stability` لم تُمَسّ.**

## ⑩ ما يفتح المحورَ ثالثةً — ثلاثةٌ معًا

1. **عيّنةٌ أكبر**: التأريخُ استنفد `unique` ذاتَ النسبة (‏4 من 11) · والعشرةُ
   `ambiguous`/`none` **لا تُؤرَّخ بالتسامح الضيّق** (‏`ⓐ`) · و‏≈243 مستوًى في
   الكاتالوج بلا تاريخ. ⇒ يلزمه **مصدرُ تأريخٍ جديد** لا تخفيفُ الموجود.
2. **معيارٌ مسجَّلٌ يناسب العيّنةَ الصغيرة** — **في ملحقٍ مؤرَّخٍ جديد** (العقدُ
   مدموجٌ لا يُعدَّل)، **ولا يكون تخفيضًا لحدٍّ رأيتُ قيمتَه** (‏`p = 0.2188`).
3. **إذنُ المالك.** ⛔ **ولا يُمَسّ `pivot_stability` بأيّ حال من هذا العقد.**

## ⑪ 🔒 الإغلاق — **مُنفَّذٌ لا مكتوب** (‏2026-09-19، أمرُ المالك «اقفل الدعم»)

`support_def_probe.py` يخرج **‏8** **قبل** أن يقرأ مدخلًا أو يفتح لقطةً أو يُطلق
جلبةً أو يحسب حرفًا — لا سطرَ تحذيرٍ يُتجاوَز بإعادة تشغيل. **ورمزُ ‏8 مميَّزٌ عمدًا**
عن كلّ ما تُخرجه الأداة (‏0/2/3 حكمًا · 5/6/7 حرّاسًا) فلا يُقرأ الإغلاقُ خللًا في
السجلّ. والمفتاح `SUPDEF_REOPEN=1` **إقرارٌ بالشروط الثلاثة لا التفافٌ عليها**.

🔑 **والسببُ أن الباقيَ دقّةٌ لا اتّجاه:** ثلاثةُ معاييرَ من أربعةٍ تعبر، والرابعُ
يسقط على **قوّةِ اختبارِ الإشارة عند `n = 6`** — وهو ما حسبه `§⑩-ⓘ-1` **قبل**
التشغيل. ورفعُ العيّنة يلزمه **مصدرُ تأريخٍ لا وجودَ له اليوم**: `unique` ذاتُ
النسبة استُنفدت (‏4 من 11) والعشرةُ الباقية `ambiguous`/`none` **لا تُؤرَّخ
بالتسامح الضيّق** (`ⓐ`).

⛔ **ولم تُخفَّض عتبةُ `p` ولا الأرضيّة** — والعقدُ وملحقُه مدموجان لا يُعدَّلان،
وأيُّ فتحٍ يلزمه **ملحقٌ مؤرَّخٌ جديد**. **و`pivot_stability` لم تُمَسّ.**

🔒 مقفولٌ سلوكيًّا بـ`SDY11` من أربعة أشقّ: خروج ‏8 **بصفرِ عمليّة** (عدّادٌ على
الجلب والاشتقاق والتقسيم وبوّابة الفِكستشر) · والإقرارُ **يرفع الحارسَ فعلًا**
(الآلةُ تعمل وتصل حارسَها التالي) · و‏8 **شاغرٌ** بشاهدَي ضبطٍ موجبٍ وسالب ·
ونقطةُ دخولٍ **واحدة** بلا فرعٍ يلتفّ عليها.
