# 🔥📐 RADAR-FRAMES — تسجيلٌ مسبق: الرادارُ يقرأ فريمات فيصل الثلاثة

> **مدفوعٌ قبل أيّ رقم.** أمرُ المالك 2026-08-17 «**صلّح الرادار**» بعد أن أبلغتُه
> بالفجوة المقيسة. ⚠️ **وهذا تصحيحُ تنفيذٍ لقاعدةٍ منصوصة لا فرضيةٌ جديدة** — ومع
> ذلك يُسجَّل لأنه **يمسّ قرارَ كتمِ تنبيه** ويُغيّر بصمةَ جذرٍ مدروع (`scan_ignition`).

## ① العيبُ المقيس

نصُّ فيصل (`faisal_images/IMG_1066.png`، 2026-08-17): «**حط فريم دقيقه و 5 و 30** ·
اضغط ع الشمعه مطوله ع كل فريم **شف كم السيوله** · المضارب … يشلع السهم 30٪ مباشره
**فوق 100 الف دولار**».
**والرادارُ يقرأ دقيقةً واحدة:** `_ignition_signal` يُرجع `usd = إغلاق × حجم` لشمعةٍ
واحدة (`Super_stock.py:12752`)، وعليها يُبنى: **(أ)** كتمُ «القروب» في مسار الاحتياط
(`:12871`) · **(ب)** `candle_class` **المخزَّن** في سجلّ الإطلاقات (`:12992`) الذي
تُبنى عليه جداولُ قياسنا · **(ج)** سطرُ العرض في التنبيه (`:12907`).
⇒ **بصمةُ مضاربٍ تظهر على الثلاثين والدقيقةُ دونها تُقرأ «قروبًا»** — والعيّنةُ في
السويّة تُظهره بالرقم: **دقيقة $1,000 مقابل ثلاثين $299,000** للحركة نفسِها.

## ② ثلاثةُ قيودٍ بنيوية تحكم التصميم — مقيسةٌ لا مفترَضة

1. 🔴 **نافذةُ جلب الرادار ‏30 دقيقة** (`fb = … polygon_minute_bars(sym, minutes=30)`)
   ⇒ **فريمُ الثلاثين غيرُ قابلٍ للقراءة أصلًا** (يلزمه بُكيتان مكتملتان ‏≈65د).
2. 🔴 **وتوسيعُ نافذة الجلب يُغيّر قرارَ الإطلاق** — لأن `_ignition_signal` يحسب
   `avg` على **كلّ** الشموع السابقة في النافذة ⇒ نافذةٌ أوسع = متوسطٌ آخر = بوّابةُ
   `vol_mult` تتغيّر. **فالتوسيعُ ممنوعٌ على مسار الزناد.**
3. 🔴 **وقصُّ «آخر 30 شمعة» عدديًّا لا يكفي بديلًا** — Polygon يُسقط الدقائق الفارغة،
   فالسهمُ الرقيقُ يعطي 8 شموعٍ في 30 دقيقة ⇒ «آخر 30 شمعة» من نافذةٍ أوسع تمسح
   **‏65 دقيقة** فتغيّر المتوسط. (‏درسُ «نافذةٌ زمنيّة لا عدديّة» — مراجعةُ Codex.)

## ③ التصميمُ المُلزِم — والزنادُ لا يُمَسّ

- **الجلبُ الأصليّ كما هو حرفيًّا**، و`_ignition_signal` يقرأ **نفسَ الشموع** ⇒
  **قرارُ الإطلاق byte-identical** (مقفولٌ سلوكيًّا).
- **نداءٌ ثانٍ أوسعُ يقع بعد الاشتعال حصرًا** (`LIQ_WINDOW_MIN`) ⇒ الاشتعالُ نادر
  (‏37 في 37 يومًا) ⇒ **صفرُ كلفةٍ على غير المشتعل**.
- **العتبةُ تُقاس على `max(usd الدقيقة، أقصى الفريمات الثلاثة)`** ⇒ 🔒 **رتيبٌ لا
  يُشدِّد أبدًا**: القيمةُ لا تنقص عن اليوم فالصنفُ لا ينزل درجةً ⇒ **الكتمةُ تُخفَّف
  ولا تُزاد**، والخطأُ الممكنُ **في جهة عدم تفويت تنبيه**.
- **فاشلٌ-آمن:** تعذّرَ النداءُ الثاني ⇒ **سلوكُ اليوم حرفيًّا** (الدقيقةُ وحدها).
- **ولا عتبةَ تتغيّر:** `IGNITION_USD_OPERATOR/STRONG/GROUP` و`IGNITION_VOL_MULT`
  و`IGNITION_CONFIRM_PCT` **كما هي** — المتغيّرُ **مدى القراءة** لا الرقم.
- **وحمولاتُ `_emit_trace` لا تُمَسّ بحرف** (طبقةُ قياس E2): الأثرُ يسجّل **الإشارةَ
  الخامّة** (دقيقةً واحدة) والقرارُ يستعمل **القراءةَ الفعّالة** — ويُصرَّح بذلك في
  الكود كي لا يُقرأ الأثرُ قرارًا.

## ④ ما يُتوقَّع — وأقوله قبل الرقم

- **`F1`: أثرُ الكتم ‏≈صفر.** مسارُ الاحتياط يُفعَّل عند **فشل النداء** لا عند رقّة
  السيولة، و`T-OPFIRE` قاسه **صفرًا من 257**. ⇒ **مَن يقرأ هذا «فتحُ تنبيهاتٍ كثيرة»
  فقد أخطأ**: المكسبُ الحقيقيُّ **صدقُ العرض وصدقُ سجلّ القياس** لا عددُ التنبيهات.
- **`F2`: جداولُ قياسنا الحالية مشكوكةٌ لا باطلة.** `radar_read_result §③` تقول
  **‏21 من 31 محسومًا صنفُها `group`** — وبعضُها قد يكون مضاربًا على فريمٍ أعلى ⇒
  **النسبةُ 81% لصنف `group` تُقرأ بحذرٍ من الآن**، ولا تُعاد بأثرٍ رجعيّ.
- **`F3`: لا يُعاد تصنيفُ الـ37 الماضية** — `polygon_minute_bars` يجلب **آخر N
  دقيقة من الآن** فقط، فشموعُ لحظةِ إطلاقٍ ماضية غيرُ متاحةٍ بهذي الأداة ⇒ **الأثرُ
  يتراكم للأمام**، ومَن أراد الرجعيَّ فليبنِ جالبًا بمدًى زمنيّ (خارج هذا التسجيل).

## ⑤ سقفُ النجاح — مثبَّتٌ سلفًا

**المسموح:** مدى القراءة (فريمان إضافيّان) · وسطرُ عرضٍ يطبع الفريمات الثلاثة ·
وحقلٌ جديدٌ في سجلّ الإطلاق. ⛔ **الممنوعُ صريحًا:** أيُّ تغييرِ عتبة · أيُّ كتمٍ
**جديد** · أيُّ مسٍّ بـ`_ignition_signal` أو بقرار `vol_mult` · وأيُّ دخولٍ في الفرز.

## ⑥ عيبٌ ثانٍ **يُبلَّغ ولا يُصلَح** (خارج أمر المالك)

`_ignition_signal` يقرأ `bars[-1]` — و`polygon_minute_bars` يطلب حتى `now` فتكون
**آخرُ شمعةٍ قيد التكوين**؛ والحلقةُ تلفّ كلَّ ‏≈45ث فوسيطُ عمرِها ‏≈30ث ⇒ **حجمُها
جزئيٌّ بنيويًّا** ⇒ **‏(أ)** بوّابةُ `vol_mult` أشدُّ ممّا يبدو (جزئيٌّ مقابل متوسطِ
كوامل) و**(ب)** `usd` **منقوصٌ فينحدر الصنفُ نحو «قروب»**.
⛔ **ولا يُمَسّ في هذا العمل:** تغييرُ الشمعة المقروءة **يُغيّر قرارَ الإطلاق نفسَه**
وهو أوسعُ من أمرِ المالك («صلّح الرادار» في سياق الفريمات) ⇒ **يُسجَّل بندًا مستقلًّا
بإذنٍ صريح**. ⚖️ ومكسبُ هذا العمل يُخفّف أثرَه جزئيًّا (‏`max` مع فريماتٍ **مكتملة**).
