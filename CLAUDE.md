# CLAUDE.md — دليل المشروع (يُقرأ تلقائيًا في أي جلسة جديدة)

> الغرض: تكملة العمل في **أي محادثة جديدة** من حيث توقّفنا، بدون شرح من الصفر.
> اقرأ هذا الملف أولًا، ثم `FAISAL_METHODOLOGY_NOTES.md` و`Super_stock.py`.

## ما هو المشروع
بوت تيليجرام يفرز **أسهم الارتكاز** في ناسداك حسب منهجية فيصل (@kisar_).
سهم الارتكاز = انفجر ≥100% ثم انهار ≥50% → فلوت صغير + شورت منخفض + تقسيم
عكسي غالبًا → الآن يتجمّع عند قاع متشبّع بفجوات فوقه = مرشّح انفجار.

## الملفات المهمة
- `Super_stock.py` — البوت الكامل (الفرز + الرسائل + التتبع).
- `analyze_one.py` — تحليل سهم واحد عند الطلب (يفوّض لـ`analyze_ticker`).
- `pullback_live.py` — مراقبة الارتداد اللحظية (كل 30د بالسوق).
- `technical_report.py` — **أداة مستقلة**: `TICKER=رمز` → تقرير فني كلاسيكي · `SCAN_EARNINGS=1` → أداة الأرباح (مسح ناسداك قوي فنيًا + أرباح قريبة).
- `test_bot.py` — 147 اختبار (يشتغل بلا إنترنت). **شغّله قبل أي دفع:** `python3 test_bot.py`
- `FAISAL_METHODOLOGY_NOTES.md` / `FAISAL_IMAGES_CATALOG.md` — توثيق منهجية فيصل من الصور.
- `.github/workflows/` — daily_screener.yml (10ص السعودية) · pullback_monitor.yml (كل 30د) · scan_earnings.yml (أداة الأرباح، يومي 06:00 UTC) · backtest.yml · analyze.yml/technical.yml (يدوي).

## الفروع (مهم)
- التطوير على `claude/faisal-images-methodology-r6gfhq`، ثم **دمج في `main`** (الأكشن يشتغل على main).
  ⚠️ git push لـ`main` متعطّل (503) في بيئة الويب → الدمج عبر GitHub API (PR مؤقت + دمج فوري).
- بعد كل تعديل: اختبار → commit → push للفرعين.
- **لا تلمس «جذور» البوت بدون سؤال المستخدم.** المستخدم لا يتقبّل أي خطأ — تأكّد وطابق منهجية فيصل الموثّقة، لا تضف اجتهادات شخصية.

## النظام الحالي (مكتمل)
1. **فرز يومي** 10ص السعودية → قائمة **A/B**:
   - 14 بوابة (M1-M14). **الأساسية (الهوية، صارمة): M1-M5** + أرضية RSI.
   - A = صفر نواقص تأكيد · B = ≤3 نواقص (`WATCH_MAX_FAILS=3`) · أكثر = رفض.
2. **قائمة ثابتة دائمة**: تُضاف لها الجديد، **ولا تُحذف إلا بستوب/هدف** (لا ترفرف).
   التجديد الكامل = الجمعة فقط (`should_renew`).
3. **قائمة مراقبة الارتداد** (مستقلة): أسهم ارتكاز ارتفعت فوق دخولها →
   نراقبها يوميًا + تنبيه لحظي أول ما تنزل للدعم. مرتّبة بالجودة، حد 15.
   **تتخرّج تلقائيًا لقائمة A/B** عند إعادة تأهّلها بالفرز اليومي (تنكمش قاعدتها)
   وتُحذف من المراقبة (`prune_graduated_pullback` — لا ازدواج بين القائمتين).
4. **ترقية B→A** تلقائية عند اكتمال النموذج · **ونزول A→B** لو خبر تخفيف
   ظهر له تأثير سلبي فعلي (كسر الدعم/قاع أدنى) — يرجع A عند الاستقرار.
5. **مؤشر صحة البيانات** + retry للتحميل (تغطية ثابتة، لا اختفاء عشوائي).
6. **منظومة الـ4 ساعات** (`four_hour_levels`، طبقة مساندة): دعوم/أهداف من
   رؤوس وذيول الشموع الحمرا + «مقاومة↗دعم» + ذيل المسح (يشمل خارج السوق).
   تُعرض «🕓 4س» — لا تمسّ الدعم/الوقف اليومي المقفول.
7. **مساعد التطوير** (`build_dev_assistant_report`، تقرير مستقل الجمعة):
   يحلّل الصفقات المحسومة المتراكمة → نسبة النجاح بالشرائح (A/B·قطاع·RSI·فلوت·
   شورت·RR·إشارة) + أنماط الفشل + **الفرص الفائتة** (مرفوض صعد ≥`MISSED_RISE_PCT`)
   + **💥 كاشف الانفجارات** (`scan_explosions`/`accumulate_explosions`: قفزة يوم
   ≥`EXPLOSION_PCT`=50% → يصنّف «كان ارتكازًا فاتنا» مقابل «عشوائي» + سبب عدم
   الترشيح، يتراكم بـ`wl["explosions"]`) + اقتراحات ضبط (اقتراح فقط).
   + **تصدير CSV للمشرف** كل جمعة (`export_weekly_csvs`: trades/signals/missed).
8. **ضمانات ضد البيانات القديمة**: `LOGIC_VERSION`+`migrate_watchlist` (ترحيل
   آلي عند تغيّر المنطق) · ختم «🧾 إصدار SHA» بكل رسالة · Action `force_renew=1`.

## قرارات مطابِقة لفيصل (محسومة — لا تغيّرها بدون سؤال)
- الوقف **~7% تحت الدعم** (`STOP_BELOW_LOW_PCT=(5,7)`, `USE_ATR_STOP=False`).
  ⚠️ ATR للوقف كان خطأ (يعمّقه) — أُلغي. فيصل: 5-7% (ENPH/VMAR).
- MACD **12/26/9** · المتوسط الأسي **EMA 30/50** · Fibonacci أهداف.
- **الأهداف متعددة الفريمات** (`USE_MULTIFRAME_TARGETS`): يومي + أسبوعي + **4س**
  (فيصل: «الأهداف ع يومي + 4 ساعات») → t2/t3 أدق. **t1 وRR محفوظان دائمًا**.
  (`refine_targets_4h` يدمج رؤوس 4س في t2/t3 فقط، بنفس بيانات 4س المجلوبة.)
- **مقاومات/أهداف من رؤوس الشموع الحمرا** (`_red_candle_heads`، قاعدة فيصل
  «رأس الحمرا = مقاومة/هدف»): تُضاف على **اليومي** لـ`resistance_levels` فلا
  يتخطّى مناطق العرض المتوسطة (مثل EZRA 4.00/4.38 = منشأ هبوط لا قمة سوينغ).
  إضافة فقط بترتيب تصاعدي — لا حذف ولا تخطّي. **الأسبوعي بلا رؤوس حمرا**
  (`include_red_heads=False`) حفاظًا على ثبات t1.
- **لا تجاوز في الأهداف** (`MIN_TARGET_GAP_PCT=3` = تسامح التجميع): الأهداف =
  أقرب 3 مقاومات حقيقية بالترتيب، فلا يُتخطّى مستوى قريب (كان 8% يقفز فوقه).
- **ترتيب بتأكيد الـ4س** (`h4_confirm_score` 0-3 في `rank_key`): الإعدادات
  المحاذية لكل الفريمات تطلع أعلى القائمة — **عرض/ترتيب فقط، لا يمسّ الاختيار**.
- **دعوم/مقاومات أساسية وفرعية** (`key_levels`، مفهوم فيصل NAMM «$2 أساسي ·
  2.20 فرعي»): الدعم الأساسي=الأرضية(pivot) · الفرعي=أقرب قاع سوينغ فوقها ·
  المقاومة الأساسية=منشأ أكبر شمعة حمرا · الفرعية=أقرب قمة. سطر 🧱 — معلومة فقط.
- RSI: قاع ≤27 مثالي (≤32 أرضية صلبة، 27-32 نقص) · الآن ≤40 (40-50 نقص، >50 رفض).
- الدخول **دفعات** (أسلوب فيصل @kisar_ الموثّق بالصور): 3 أوامر عند الدعم
  وصعوداً بخطوة ~3% (`ENTRY_TRANCHES=3`, `ENTRY_STEP_PCT=3`). مثال فيصل:
  1.70/1.75/1.80 وقف 1.50. أدنى دفعة=الدعم=أفضل تعبئة (KHALID 2.55/2.60 يؤكّد).
- العائد/المخاطرة من **متوسط الدفعات** (فيصل يمتّع → تعبئته الفعلية ≈ المتوسط)
  لا السعر الحالي. موحّد في الكرت والتقرير (لا تناقض).
- فلوت <50م · شورت **≤40ألف** (`SHORT_GATE_MAX`؛ ≤20 مثالي · SPPL 35ألف مقبول)
  · ثبات الدعم 3-8 جلسات.
- **فحص أخبار آلي** (`scan_news_risk`): طرح/تخفيف/تقسيم عكسي/شطب من عناوين
  ياهو → تحذير بالبطاقة (يكمّل مرصد SEC). TipRanks ⭐ أفضل مصدر متابعة (رابط).
- **حجم المركز** (`position_size`): كم سهم بحيث الخسارة عند الوقف = `RISK_PER_TRADE_PCT`%
  من `ACCOUNT_SIZE` (env). يُحسب من متوسط الدفعات. سطر 📐 بالكرت.
- **تنبيه الدخول** (التقرير اليومي): «🎯 في منطقة الدفعات الآن» لما ينزل السعر
  داخل [أدنى دفعة، أعلى دفعة] = اللحظة العملية للتنفيذ.
- **باكتيست تاريخي** (`backtest_symbol`/`run_backtest`، وضع `SCREENER_MODE=BACKTEST`
  + `.github/workflows/backtest.yml`): مشي للأمام بلا نظر للمستقبل (ننتظر تعبئة
  منطقة الدخول ثم t1 قبل الوقف). ⚠️ انحياز الناجين: لازم عيّنة واسعة عشوائية.
- تحذير الصين/هونغ كونغ (تتجاهل التحليل) — تحذير فقط.

## شكل البطاقة/التقرير (مصطلحات المستخدم)
- **نسبة الدخول/الجاهزية X/100** (قرب الدخول) + **النسبة العامة %** (الجودة).
- سطر: `💵 السعر | دخول من–إلى | الدعم | ستوب` + الأهداف + القاب (من–إلى) + التحرر.
- القطاع/الدولة **بالعربي** (`ar_sector`/`ar_country`).

## التشغيل والاختبار
- اختبار: `python3 test_bot.py` (لازم 147/147).
- `analyze_one.py` (الفحص اليدوي) **مطابق تمامًا** لـ`analyze_ticker` (نفس الدرجة/
  الأهداف/الوقف/RR/المؤشرات) — مقفول باختبار «الفحص اليدوي = الأساسي».
- **جلسات Claude Code ويب**: `.claude/hooks/session-start.sh` (SessionStart hook،
  مُسجَّل في `.claude/settings.json`) يثبّت `requirements.txt` + `ruff`/`pyflakes`
  تلقائيًا عند بدء كل جلسة ويب (متزامن · ويب فقط · آمن للتكرار).
- البوت لا يعمل محليًا (Yahoo محجوب بالبيئة) — يعمل على GitHub Actions.
- الأسرار: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` (يقبل عدة أرقام بفاصلة).

## قاعدة الذاكرة الدائمة (مهمة — لكل جلسة)
بعد **كل قرار أو تعديل مهم**، حدّث `HANDOFF.md` و`CLAUDE.md` (قسم الحالة/القرارات)
وادفعهما مع نفس الـcommit — حتى تبقى الذاكرة حيّة ولا يضيع شي لو توقفت المحادثة
فجأة. للاستمرار في محادثة جديدة: المستخدم ينسخ «نص التشغيل» من أعلى `HANDOFF.md`.

## ضوابط الـ commit
- رسائل commit تنتهي بـ:
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
  `Claude-Session: https://claude.ai/code/session_01LBuXeYuQbmaEjVK9gwExgJ`
- لا تنشئ PR إلا بطلب صريح. لا تضع معرّف النموذج في أي ملف مدفوع.
