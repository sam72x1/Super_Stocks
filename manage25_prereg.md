# 🎚️ T-MANAGE-25 — التسجيل المسبق (يُكتب ويُدفع **قبل أيّ رقم**)

> **السؤال:** هل **الإدارة الجزئية** — بيعُ الأغلبية عند الهدف الأوّل و**تمتيعُ** ربعٍ
> باقٍ — تضيف توقّعًا فوق الخروج الكامل؟
>
> 🔑 **وهي التي تختبر «فيصل يمتّع» فعلًا** — بخلاف `T-EXIT` التي اختبرت **عتبات قمّة
> ثابتة** فسقطت (‏`exit_policy_prereg.md`: كلُّ هدفٍ أطول أسوأ). الفارق جوهريّ:
> `T-EXIT` تسأل «أيّ هدفٍ ثابتٍ أفضل؟» — وهذي تسأل «هل **الإدارة** أفضل من الخروج
> الكامل؟»، وهو سؤالٌ مختلف لم يُختبَر قطّ.
>
> 🔒 **بحث/قياس — صفر مسّ إنتاج · لا `LOGIC_VERSION` · سقف النجاح: أرقامٌ واقتراح.**
> **الحالة عند الكتابة: صفر رقمٍ من أيّ سياسة.**

---

## ⓪ 🔴 تصحيحٌ يسبق التجربة — **أفق الحسم**

راجعتُ مسار `T-EVENT-EXEC` فوجدتُ أن الكود كان يحسم **داخل جلسة الزناد وحدها** بينما
التسجيل ينصّ على الحسم **«من يوم الزناد»** (‏أفقُ نافذة الخطة). ⇒ **الأرقام المنشورة
سُحبت** (`event_exec_result.md` بترويسة السحب)، و**الأفق يُصحَّح الآن**:

**الحسم يمتدّ من دقيقة الدخول حتى نهاية نافذة ARMED** (‏`exit_date` للخطة) على **شموع
الدقيقة**. ⚖️ وهذا **ليس تخفيفًا**: كشفُ الوقف بلمسة الذيل على الدقيقة **مطابقٌ** له
على الشمعة اليومية (أدنى اليوم = أدنى دقائقه)، والفرق أن التوقيت يصير أدقّ — وهو
**شرطٌ لازم** لهذي التجربة أصلًا (السياسة ب تحتاج مسار السعر بعد الهدف).

⇒ **تُعاد تشغيلات `T-EVENT-EXEC` بالأفق الصحيح**، وتُقاس `T-MANAGE-25` في **نفس
التشغيلة** — فالمجتمع واحدٌ **بالبناء** لا بالتوفيق.

---

## ① المجتمع

**نفس الصفقات القابلة للتنفيذ من `T-EVENT-EXEC`** (‏`E-REAL` المُنفَّذة) — كما نصّ
المراجِع حرفيًّا. لا مجتمعَ آخر، ولا انتقاءَ بعد رؤية الأرقام.

⚠️ **وحدٌّ يُعلَن الآن:** توقّعُ هذا المجتمع قد يكون **سالبًا** (‏`T-EVENT-EXEC`).
وهذا **لا يُبطل** التجربة: السؤال **فرقٌ مزدوجٌ على الصفقة نفسها** (ب مقابل أ)، وهو
معرَّفٌ ومقيسٌ سواء كان الأساس موجبًا أم سالبًا. **لكنه يقيّد التعميم**: نتيجةٌ موجبة
هنا تعني «الإدارة تحسّن **هذي** الصفقات»، لا «الإدارة تصنع حافةً من عدم».

---

## ② السياستان — مثبَّتتان الآن، لا ثالثة لهما

| | التعريف |
|---|---|
| **A** (الأساس) | **خروجٌ كامل** عند `t1` أو الوقف — سياسة الإنتاج الحالية |
| **B** (الإدارة) | **‏75% عند `t1`** · و**‏25% تُمتَّع** ثم تخرج بأوّل ما يقع: **①** كسرُ آخر مستوًى بنيويّ بلا استعادةٍ خلال **15 دقيقة** · **②** بلوغ `t3` · **③** نهاية **5 جلسات** بعد الهدف |

**تعريفاتٌ تُثبَّت الآن لئلّا تُختار بعد الأرقام:**
- **«آخر مستوًى بنيويّ» = `t1` نفسه.** الحاجز الذي كُسر صاعدًا يصير الدعمَ المدافَع
  عنه — أوضحُ قراءةٍ وأقلُّها اجتهادًا. **الكسر بإغلاق دقيقة تحت `t1`**، والاستعادة
  **بإغلاق دقيقةٍ فوقه خلال 15 دقيقة**.
- **`t3` من الخطة المخزَّنة** (`interp_in`) — لا هدفٌ مُخترَع.
- **الوقف على الربع الباقي = `t1`** (لا الوقف الأصليّ): بعد بيع ‏75% عند الهدف، الدفاع
  يكون عن المستوى المكسوب. ⚠️ **وهذا اختيارٌ مسجَّل** لا مقيس، ويُصرَّح به.
- **بلا وقفٍ متحرّك** ولا إضافةٍ ولا إعادة دخول — خارج نطاق التجربة.
- **صفقةٌ لم تبلغ `t1`** ⇒ **ب = أ حرفيًّا** (فرقُها صفر) وتبقى **في المقام**.

---

## ③ المقياس — مثبَّت الآن

- **الأساسيّ:** **الفرق المزدوج `B − A` بصافي R بعد التكاليف، على الصفقة نفسها** —
  **متوسطًا على كلّ الصفقات المحسومة** (لا على الرابحات وحدها).
- **يُنشَر ولا يحكم:** الوسيط · الفرق **مشروطًا ببلوغ `t1`** (الميكانيكا) · **CVaR‏5%**
  وخسارة الذيل · **حصّة أعلى اسمَين من الربح** · وعدد الصفقات التي اختلفت فيها ب عن أ.
- **التكاليف:** كلُّ خروجٍ يدفع **نصف السبريد المقيس عند الدخول** — والربع الباقي
  يدفعه **مرّةً ثانية** عند خروجه (خروجان = تكلفتان). **لا مجّانية للإدارة.**

## ④ الاستدلال — مثبَّت الآن

**‏`cluster bootstrap` بالرمز** على الفرق المزدوج · **‏10,000 عيّنة** · وفاصل ‏95%.
**لا اختبارٌ يفترض استقلال الصفقات.**

## ⑤ معيار الاعتماد — **حرفيًّا كما اقترحه المراجِع**

1. **تحسّنٌ لا يقلّ عن ‏0.15R** (متوسط الفرق المزدوج).
2. **الحدّ السفليّ لـ`cluster bootstrap` ‏95% فوق الصفر مجمَّعًا.**
3. **اتجاهٌ موجب في كلّ سنة.**
4. **لا زيادةَ غير مقبولة في `CVaR`** — تُثبَّت الآن: **`CVaR‏5%` لا يسوء بأكثر من
   ‏0.10R** عن السياسة أ.

⇒ **الأربعة معًا.** وسقوط أيّها = **لا اعتماد**، ويُكتب السبب صراحةً.

---

## ⑥ التنبّؤات القابلة للكسر

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| **M1** | **الفرق لن يبلغ ‏0.15R** — لأنه **مخفَّفٌ بنيويًّا**: يقع على ‏25% فقط ومن الرابحات وحدها، والرابحات أقلية | بلوغُه ⇒ الإدارة أقوى ممّا يسمح به الحساب |
| **M2** | **الاتجاه سيكون موجبًا لكن صغيرًا** (‏0.00–0.05R) — «يمتّع» يلتقط ذيلًا نادرًا | سالبٌ ⇒ الدفاع عن `t1` يخرج قبل الارتداد فيخسر أكثر ممّا يكسب |
| **M3** | **`CVaR` لن يسوء بوضوح** — الربع الباقي مدافَعٌ عنه عند `t1` لا عند الوقف الأصليّ | سوءُه ⇒ الدفاع أضعف ممّا افترضت |
| **M4** | **الفرق يحمله ذيلُ قِلّة**: أعلى اسمَين ‏>40% من الربح الإضافيّ | توزّعُه ⇒ الإدارة تفيد عمومًا لا استثناءً |
| **M5** | **حكمُ `T-EVENT-EXEC` المصحَّح سيبقى سالبًا** رغم تصحيح الأفق | انقلابُه موجبًا ⇒ **الأفق كان هو العلّة**، ويُعاد كلُّ حكم الأمس |

🔴 **سجلّي اليوم: سحبتُ حكمًا كاملًا، وأصلحتُ ثلاثة عيوب قياس، ونجت أربع طفرات من
أقفالي.** فاحتمال كسر هذي عالٍ، ويُكتب المكسور كما هو.

---

## ⑦ حدود الصدق (تُعلَن قبل النتيجة)

1. **المجتمع صغيرٌ وسالبُ التوقّع** ⇒ التعميم مقيَّد كما في ‏§①.
2. **«‏15 دقيقة» و«‏5 جلسات» و«‏75/25» أرقامُ المراجِع** لا أرقامُ فيصل — ولم تُعاير
   على بياناتنا. ⇒ **نتيجةٌ سالبة تنفي هذي الوصفة بعينها، لا «الإدارة» عمومًا.**
3. **الدفاع عن `t1` اختيارٌ مسجَّل** (‏§②) — بديلُه (الوقف الأصليّ) **غير مقيس**.
4. **بلا افتر** · **انحياز بقاء** · **تشويه تقسيمات** · **`NBBO` قمّةُ دفتر**.
5. **‏`t3` قد يكون غائبًا** لبعض الخطط ⇒ تُعدّ وتُعلَن، وتخرج بالشرطَين الباقيَين.
6. **لا تُقارَن أرقام هذي التجربة بأرقام `T-EXIT`** — المجتمع والمحرّك والسؤال مختلفة.

## ⑧ سقف النجاح

**أرقامٌ + اقتراحٌ واحد للمالك.** **لا تغيير إنتاجيّ · لا مسّ بالجذور · لا
`LOGIC_VERSION` · ولا تعديل على سلّم الأهداف.** وأيّ تفعيلٍ = **قرار مالكٍ حصريّ**.
