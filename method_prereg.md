# 🔬 T-METHOD — تسجيلٌ مسبق (يُدفَع **قبل** أيّ رقم · 2026-08-01)

> **الطلب:** المالك — «توليفة تجمع شروط الصور الستّ **مع شروط الارتكاز الأساسية**…
> ولو ما قدرنا فنستخدم هذي التوليفة **وحدها** لأنها واضحة بشكل كبير».
> **المصدر:** `FAISAL_SCIENTIFIC_METHOD.md` (‏6 صور UPC، مقروءةٌ بالعين).
> 🔒 بحث/قياس · **صفر مسّ إنتاج** · لا `LOGIC_VERSION` · وأيّ تغييرٍ لاحق **قرار مالك**.
> ⚠️ كلُّ ما دون مثبَّتٌ **قبل** رؤية أيّ رقم.

---

## ⓪ اكتشافٌ يسبق التصميم — ويجب أن يُقرأ أوّلًا

**التسلسل الرباعيّ في الصور الستّ ليس جديدًا علينا: هو `trigger_state` المبنيّة سلفًا**
(`Super_stock.py:12729`)، وسندُها المدوَّن `TG_1870` حرفيًّا:
> «قاع ⟶ اختبار أوّل مقاومة فوقه ⟶ رجوعٌ يختبر القاع **مع سحب سيولة** ⟶ **هنا يكون
> rsi جاهز للانفجار**»

ومطابقتُها لصور UPC **بندًا ببند**:

| صور UPC | `trigger_state` |
|---|---|
| «قبل صعوده كان 3.10» (قاع) | ① قاعٌ مكوَّن |
| «صعد 17٪ · صعود اختبار مقاومه» | ② ارتفاعٌ ثم رَدّ |
| «يرجع يهبط يختبر الدعم» | ③ مسحٌ ثم استعادة |
| «هبوط اختبار دعم **ثبات** = انفجار» | ④ RSI جاهز [20, 30] |

⇒ **الصور الستّ = القاعدة نفسها بأرقامٍ صريحة** (‏10-15-17% · 20-30 يومًا · دخول 3.20
· وقف 3 · هدف 8). **والجديد فيها عنصرٌ واحد: تعريفُ المجتمع** (صعودٌ حديث عالٍ ثم
هبوطٌ 20-30 يومًا) — وهو **غير مبنيّ** عندنا.

## ⓵ 🔴 وجوابُ «هل تصلح التوليفة؟» **مقيسٌ سلفًا — قبل هذي التجربة**

`candle_readiness_prereg.md` §④ (‏2026-07-30، قياس قدرةٍ على **300 سلسلة عشوائية**):
> «تتحقّق «حالة الزناد» في **‏3.7%** فقط. وبإشاراتنا (‏168 · 110 · 147 محسومة للسنوات
> الثلاث) يعني ذلك شريحةً بحجم **‏4-8 صفقات/سنة** — أدنى بكثير من عتبة العيّنة
> المسجَّلة (‏≥30/شريحة/سنة).»

⇒ **التقاطع (ارتكاز ∩ التسلسل الرباعيّ) = 4-8 إشارات في السنة ⇒ غير قابلٍ للحكم
بنيويًّا.** ويُضاف إليه ما قِسناه أمس: **مجتمع الارتكاز كلُّه يُسلّم 0-3 منفجرين
سنويًّا** ⇒ **التقاطع — وهو جزءٌ منه — لا يستطيع رفع العدد، أقصى ما يفعله رفعُ الدقّة.**

🎯 **فحدسُ المالك «لو ما قدرنا نستخدمها وحدها» هو الصحيح بالأرقام**، ولذلك تُقاس
الأذرع الثلاث معًا ولا يُختار قبل النتائج.

## ② الأذرع — مثبَّتةٌ الآن، ولا رابعة بعد الأرقام

| الذراع | التعريف | ماذا تُجيب |
|---|---|---|
| **M0** | الارتكاز وحده (‏`M1-M14` كما هي) | خطُّ الأساس = ما يصلك اليوم |
| **M1** | **التوليفة**: الارتكاز **∩** `trigger_state` | طلبُ المالك حرفيًّا (متوقَّع: «لا حكم») |
| **M2** | **النهج العلمي وحده**: مجتمعٌ جديد **بلا `M1-M14`** | **الوحيدة التي ترفع السقف** |

**تعريف مجتمع M2** (‏من الصور، بلا اختراع):
1. **صعودٌ حديث عالٍ**: `spike_info` ‏≥ `EXPLOSION_PCT` (‏**50%، ثابتٌ قائم لا مُبتكَر**)
   داخل `PRIOR_SPIKE_WINDOW` (‏20 جلسة ≈ «قبل شهر بالضبط»).
2. **هبوطٌ 20-30 يومًا منذ القمّة** — **حرفيّ** من `IMG_0489`.
3. **`trigger_state.ok`** — بعتباتها الإنتاجية **بلا ضبط** (أدناه §④).
4. **لا إعلان طرح**: `_offering_event(...) is None`.
5. **خالٍ من قروب**: `group_pump_scar(...) is None`.

## ③ ما **لا** يُنفَّذ — ويُصرَّح به بدل تخمينه

- **«لازم يرجع يدبل»** — لفظٌ **غامض** (يتضاعف؟ ينخفض للنصف؟). **لا يُنفَّذ**،
  ويُوسَم `ambiguous` في دفتر المصادر. (القاعدة: لا اجتهاد على نصٍّ غير قاطع.)
- **«شرط الشورت قليل ونسبة اقتراض عالية»** — **بلا تاريخٍ عندنا** (المتاح/الرسوم
  لحظيّان، مُثبَتٌ مرارًا) ⇒ **يستحيل باكتيستُه**؛ يبقى **شرطًا حيًّا** يُطبَّق عند
  التنفيذ لا في القياس. **وهذا يجعل M2 المقيسة أوسع من وصفة فيصل الكاملة** — حدُّ
  صدقٍ يُعلَن، والاتجاه: **نتيجةُ M2 أرضيّةٌ لا سقف**.
- **«شموع مضارب سهل قراءتها»** — وصفٌ لا معيار؛ **مُنفَّذٌ جزئيًّا** عبر ⑤ (خلوّ
  القروب) وهو ما قصده فيصل صراحةً («دخول سيولة قروبات ⇒ لا أحد يستطيع قراءة الشموع»).
- **نطاق الارتداد 10-15-17%** — `trigger_state` تستعمل `SPLIT_SWEEP_MIN_PCT=7%` لشرطَي
  ② و③ معًا. **لا أضبطه**: تغييرُه بعد معرفة أن الصور تقول 10-15% = ضبطٌ بأثرٍ رجعيّ.
  تُشغَّل الذراع **بعتباتها الإنتاجية حصرًا**، ويُدوَّن الفارق فرضيةً لتجربةٍ مستقلّة.

## ④ صفر ضبطٍ للعتبات

كلُّ رقمٍ مستعمَل إمّا **حرفيّ من الصور** (‏20-30 يومًا) أو **ثابتٌ إنتاجيّ قائم**
(`EXPLOSION_PCT` · `PRIOR_SPIKE_WINDOW` · `CANDLE_*` · `SPLIT_SWEEP_*`). **ولا رقم
جديد يُبتكَر.** أيّ تعديلٍ بعد رؤية نتيجة = `p-hacking` صريح.

## ⑤ المقاييس — مثبَّتة قبل الأرقام

- **الأساسيّ:** **عدد المنفجرين المُسلَّمين** (بلغوا ‏+100% قبل الوقف) — نفس مقياس
  `T-RANKER` (سؤال المالك: «كم منفجر يوصلني»).
- **المساند:** صافي `R` · ونسبة بلوغ ‏+50%.
- **والأوّل قبلهما: حجمُ العيّنة** — لأن هذا بالذات ما أعدم `T-CANDLE`.

## ⑥ بوّابة الحكم — الخمسة معًا

1. **العيّنة:** ‏≥30 إشارة محسومة **لكلّ ذراعٍ في كلّ سنة**، والسنوات الثلاث.
   ⚠️ **دون ذلك ⇒ «لا حكم» لا «فشل»** (تمييزٌ ألزمَتنا به `T-SHORT`).
2. **الاتجاه:** M2 تتفوّق على M0 في المنفجرين المُسلَّمين في **الثلاث بلا انقلاب**.
3. **الدلالة:** الحدّ السفليّ 95% لفرق `R` **فوق الصفر** مجمَّعًا (bootstrap عنقوديّ).
4. **لا يحملها اسمٌ واحد:** أكبر رمزٍ يسهم بأقلّ من **30%** من الفارق.
5. **لقطةٌ مجمَّدة** إلزامية (`frozen_run_id`) — فأيّ تقسيمٍ داخل النافذة يشوّه الحكم.

## ⑦ ما يُكذِّب كلَّ ذراع (يُكتَب الآن)

- **M1 مكذَّبة عمليًّا** لو جاءت عيّنتُها دون 30/سنة — **وهو المتوقَّع بقياس 3.7%**.
- **M2 مكذَّبة** لو لم تتجاوز منفجريها **صفرًا إلى ثلاثة** سنويًّا (سقف M0 المقيس)،
  أو تفوّقت في سنةٍ وانقلبت في أخرى.
- **والعدميّة الشاملة** لو لم يتجاوز أيٌّ منهما M0 ⇒ **لا مجتمع أفضل في متناولنا
  بهذي القواعد**، ويُغلَق المحور بالدليل.

## ⑧ سقف النجاح — مُعلَنٌ سلفًا

**أقصى ما يترتّب: اقتراحٌ للمالك.** ولا تغيير إنتاجيّ إلا بإذنه، ومعه `LOGIC_VERSION`
+ `force_renew=1`. **ولا يُمَسّ:** `M13`/`M14` (خارج البحث بقراره) · أرضية `M5` ·
قفل `C3` · الوقف/الأهداف/الدخول · **وصيّاد المقسّم** (قرار المالك: يُترَك جانبًا،
**وقد فحصتُ الصور الستّ فلا شيء فيها يخصّه**).

## ⑧-مكرّر 🔴 تعديلاتٌ إلزامية بعد **مراجعةٍ خصومية** (‏2026-08-01، قبل أيّ رقم)

طُعِن التصميم بـ28 اعتراضًا. **أقبل الآتي ويُدمَج في التسجيل قبل التشغيل:**

**① 🔴🔴 الأخطر — البيانات المعدَّلة للتقسيم تُصنّع الحدث المؤسِّس نفسه.**
بصمة الوصفة («وصل 17 قبل شهر ثم 3.10») هي **حرفيًّا بصمة التقسيم العكسيّ** في
السلسلة المعدَّلة (‏عيب `C1` المدوَّن؛ NUWE يقرأ 908.25 لسهمٍ بـ$20.65). ⇒ **M2
ستنتقي مقسّمين عكسيًّا وتُسمّيهم «صاعدين قبل شهر»**. **لم أكن أرى هذا.**
⇒ **إلزاميّ:** لقطةٌ مجمّدة (`frozen_run_id` → `_BT_SPLITS_CTX`) · **واستبعادٌ
وعدّ** لكل مرشّح يقع تقسيمٌ داخل نافذة «الصعود ثم الهبوط» أو داخل النافذة الأمامية
· وسطر «**العلم فعّال: قرأ لقطة splits لـN رمزًا**» — وصفرٌ ⇒ **no-op لا تُفسَّر**.

**② 🔴 بوّابة «لا إعلان طرح» ميتةٌ في المشي التاريخيّ.** `sec_recent_filings` يقصّ
عند `today − SEC_FILING_DAYS` و`_offering_event` يرفض القديم بـ`_fresh` ⇒ **يُرجع
`None` دائمًا** فيُقرأ الشرط **مستوفًى** والاختبارات خضراء. ⇒ **يُعلَن الشرط ①
«غير مُختبَر» لا مستوفًى**، ويُطبَع عدّاد تغطية؛ ولا يُدَّعى تنفيذُه.
⚠️ **وانعكاس قطبيّة:** نفس الدالّة **حدثٌ مؤسِّس إيجابيّ** عند المقسّم و**مانعٌ
سالب** هنا ⇒ اختبار قطبيّة صريح إلزاميّ.

**③ 🔴 الرمز المصدر داخل عيّنة الحكم = `in-sample`.** UPC هو **مصدر** الوصفة،
والصور وصلت **بعد** ‏+99.76%. ⇒ **UPC وكلُّ رمزٍ ورد في الصور الستّ يُستبعَد من
عيّنة الحكم** ويُستعمل **شاهد توصيلٍ فقط** (سابقة: استبعاد GEOS في `E2`). ونجاحُ
الأداة عليه يُعلَن «**الأداة موصولة**» لا «الوصفة تعمل».

**④ نسبة الارتداد — تصريحٌ مكتوب قبل الأرقام.** النصّ فيه **ثلاثة أرقام** لمرحلةٍ
واحدة (‏«10 < 15٪» · «صعد 17٪» · «يصعد 15٪»). و`trigger_state` تستعمل **7%**
(`SPLIT_SWEEP_MIN_PCT`) — **وهو رقمٌ رابع لا يطابق أيًّا منها**. ⇒ **أُثبّت
استعمال 7% الإنتاجيّ** (‏صفر ضبط)، **وأُصرّح** بأنه **أوسع** من نصّ فيصل ⇒
**عددُ الإشارات سقفٌ لا مطابقة**، والفرق يُدوَّن فرضيةً لتجربةٍ مستقلّة.

**⑤ ✅ تنبّؤ «80٪ هذا نهج الاسهم» يُقاس ويُنشَر** — وهو **أرخص اختبارٍ للمصدر في
الحزمة كلّها**. والمقيس عندنا سلفًا **3.7%** على 300 سلسلة ⇒ **فجوة 20×**.
⇒ يُنشَر بوصفه **إمّا** أن تشغيلنا للنصّ **أشدّ بكثير** من مراد فيصل، **أو** أن
التعميم غير دقيق — **والاثنان نتيجةٌ معتبرة**، ولا يُستعمل عتبةً أبدًا.

**⑥ الحسم بمحرّكٍ واحد حصرًا** (`_resolve_arm`) — الهدف من `filled+1` والوقف من
`filled` (مخالفة `F-L1` وقعتُ فيها مرّتين في يوم). + **حالة F-L1 التمييزية**
إلزامية في السويّة قبل أي تشغيل. **ولا يُبنى محرّك حسمٍ ثانٍ.**

**⑦ اختبار سببية (`prefix-causality`) إلزاميّ:** ناتجُ أيّ دالّة عند `i` **مطابقٌ
حرفيًّا** سواء بُتِر الإطار عند `i` أو مُرِّر كاملًا — لأن «**آخر** اختبار» في نصّ
الوصفة لا يُعرَف إلا بأثرٍ رجعيّ، **وهو تسريبٌ لا تكشفه اختبارات الوحدة**.
(`trigger_state` تجتازه **بنيويًّا** لأنها تقرأ `df.tail(win)` وحدها.)

**⑧ ‏`recency_bars` مُزاحة 15 جلسة** (`c[:n-BASE_WINDOW]`) ⇒ **يُمنَع** استعمالها
لقياس «20-30 يومًا منذ القمّة»؛ تُبنى دالّةٌ نقيّة جديدة باختبارٍ على سلسلة
اصطناعية معلومة القمّة.

**⑨ «حقق متوسط 30» و«الثبات»** — الأولى غامضة (قد تكون EMA30) والثانية بلا رقمٍ
في هذي الوصفة ⇒ **كلتاهما `inferred` أو تُسقَط**، ولا تُترجَم بالحدس.

## ⑨ التوقّع المُعلَن (لئلّا يُسوَّق النفي مفاجأةً)

**M1: «لا حكم» شبه مؤكَّد** (‏4-8/سنة). **M2: غير معروف — وهي التجربة الحقيقية**؛
فهي أوّل مرّةٍ نختبر فيها **مجتمعًا مختلفًا** بدل معايرةِ مجتمعنا. وإن سقطت أيضًا،
فالحكم: **قواعد فيصل المنصوصة لا تُنتج آليًّا مجتمعًا أكثر انفجارًا من مجتمعنا** —
ويبقى ما أصاب فعلًا (صيّاد المقسّم) هو النموذج الذي يُبنى عليه.
