# 🧱⛰️ T-CEILING — تسجيلٌ مسبق: **كم يُكلّف رفعُ سقف الانهيار `MAX_DROP_PCT`؟**

> **مدفوعٌ قبل أيّ رقم.** أمرُ المالك 2026-08-16: «**قِس السقف**» — بعد أن أثبت
> `rubi_case.md` أن `RUBI` (سهمٌ يملكه الآن) يسقط على `M2_هبوط_فوق_97` في سبعةِ
> أيامِ فرزٍ متتالية، وأن الجدارَ يصطاد **عائلةَ فيصل** (‏`HUBC`·`PAVS`·`VMAR`·
> `WOK`·`YYGH` في قائمة اليوم نفسِها).
>
> 🔒 **بحث/قياس فقط.** لا تُشغَّل أيُّ ذراعٍ قبل دفع هذا الملفّ. **صفرُ مسٍّ
> بالإنتاج**: العلمُ يُضبَط **بعد** الاستيراد في عمليةِ الذراع وحدها، والفارزُ
> الحيُّ لا يمرّ من هنا. ولا `LOGIC_VERSION` (‏لا تغييرَ إنتاجيًّا في هذي التجربة).

---

## ① السؤال — بصيغةٍ قابلةٍ للتكذيب

بوّابةُ `M2` تحسب `drop = (1 − price / hi52) × 100` حيث `hi52 = high.tail(252).max()`
**على سلسلةٍ معدَّلةٍ بالتقسيمات**، ثم:

```python
if drop_pct > CONFIG["MAX_DROP_PCT"]:
    return _reject("M2_هبوط_فوق_97")      # Super_stock.py:3069-3070 (مقارنةٌ صارمة >)
```

والقيمةُ النافذة **‏99.94998260261913** (من ظرف الكاتالوج P100، لا من قرارٍ هندسيّ).

**السؤال:** ما كلفةُ رفع هذا السقف — بالمنفجرين المُسلَّمين وبالعائد — وماذا
يدخل مقابله؟

📐 **وترجمةُ السقف إلى كمّيةٍ مفهومة:** `drop > X` ⟺ `hi52 / price > 1/(1−X/100)`.
فالسقفُ النافذ يعني **رفضَ كلّ سهمٍ قمّتُه المعدَّلة أعلى من سعره بأكثر من
‏×1,998**. والأذرعُ أدناه سلّمٌ على هذي النسبة.

---

## ② الأذرع — **أربعٌ ولا خامسة** (مثبَّتةٌ الآن)

| الذراع | `MAX_DROP_PCT` | يعني: يرفض إذا كانت `hi52/price` أعلى من | القراءة |
|---|---:|---:|---|
| **`C0`** | **99.94998260261913** | ‏×1,998 | **الأساس = الإنتاج الحاليّ** |
| `C1` | 99.99 | ‏×10,000 | فتحٌ محدود |
| `C2` | 99.999 | ‏×100,000 | فتحٌ واسع |
| `C3` | **100.0** | — | **بلا سقفٍ إطلاقًا** (المقارنةُ `>` صارمة و`drop < 100` دائمًا لأن `price > 0`) |

**وفي تشغيلةٍ واحدةٍ بميزانيةٍ ثابتة** لكلّ سنة: نفسُ اللقطة · نفسُ الكون ·
نفسُ السعة · نفسُ المُرتِّب · نفسُ النافذة. (‏درسُ `T-CLIFF`: ذراعان بميزانيتين
تقيسان الميزانيةَ لا الذراع.)

⛔ **ولا ذراعَ تُضاف بعد رؤية الأرقام** — إضافةُ ذراعٍ لاحقًا `p-hacking`.
⛔ **ولا يُمَسّ في هذي التجربة**: أرضيةُ `M2` (‏`MIN_DROP_FLOOR`) · `M3` ·
`M4` · `M5` · مِرساةُ الدخول · الوقفُ · الأهدافُ · السعة · المُرتِّب.

---

## ③ المقياسُ الحاكم — مثبَّتٌ سلفًا

**`d50` = المنفجرون المُسلَّمون** (‏`mg_pre_stop ≥ EXPLOSION_PCT`=50% قبل الوقف)
**داخل خانات المحفظة** (سعة **15** الحيّة عبر `replay10` و`rank_live`) — أي **ما
يصل المالكَ فعلًا** لا ما تقبله البوّابة.

**والمساندة (تُنشَر ولا تحكم وحدها):** `d100` · `R`/صفقة · الإشارات · المحسومة ·
الدقّة · المرفوض بالسعة · **وعدّادُ جدار السقف** من توزيع رفض الإنتاج نفسِه.

---

## ④ السنواتُ واللقطات — **مثبَّتةٌ الآن**

| السنة | لقطة PIT (`frozen_run_id`) |
|---|---|
| 2024 | `31199493957` |
| 2025 | `31191327731` |
| 2026 | `31199510868` |

**والحكمُ يلزمه الثلاث** — سنةٌ واحدة لا تحكم (درسُ `T-SHORT`: الإشارةُ انقلبت
بالسنة الثالثة).

---

## ⑤ بوّاباتُ الصلاحية — **تُقرأ قبل أيّ تفسير** (منفَّذةٌ في الكود لا موصوفة)

> 🔴 درسُ `envelope_prereg §⑦` المدوَّن: **حارسُ صلاحيةٍ مكتوبٌ وغيرُ منفَّذٍ
> في الكود ليس حارسًا — هو نيّة.** فلكلّ شرطٍ أدناه سطرُ كودٍ يُوقِف، وقفلٌ
> في السويّة يُثبت أنه يُوقِف.

| # | الشرط | رمزُ الخروج عند السقوط |
|---|---|---|
| `V1` | **القيمُ النافذة أربعٌ متمايزة** وتُطبَع (`ARM_EFFECTIVE`) | 3 |
| `V2` | **العلمُ فعّال — لا `no-op`**: عدّادُ `M2_هبوط_فوق_97` **يتناقص رتيبًا** ‏C0 ≥ C1 ≥ C2 ≥ C3، **و`C3` = صفرٌ بالضبط** | 3 |
| `V3` | **فحصُ تكاملٍ بت-بت**: `C0` يعيد أرقامَ `anchor_prereg §⑫` للذراع `B2` (الإنتاج الحاليّ) — **2024=32 · 2025=41 · 2026=15** | 3 |
| `V4` | **المقياسُ الحاكم محسوبٌ لا غائب** (‏`d50`/`taken` ليسا `None`) — درسُ `T-CHASE`: مقياسٌ غائبٌ يُطبَع صفرًا فيُقرأ «لا فرق» | 3 |
| `V5` | **الإشاراتُ رتيبةٌ تصاعديًّا** ‏C0 ≤ C1 ≤ C2 ≤ C3 (فتحُ السقف لا يُنقص إشارة) | 3 |
| `V6` | ذراعٌ سقطت (‏`rc≠0`) أو بلا مُخرَجٍ | 2 |

🔒 **و`V3` هو الأقوى**: السعةُ الحيّة **15** = سعةُ النشر ⇒ **يُفرَض بت-بت بلا
تأجيل** (بخلاف `base2_arms` حيث اختلفت السعتان). خرقُه ⇒ **عطبُ أداةٍ لا نتيجة**.

---

## ⑥ معاييرُ الحكم — مثبَّتةٌ قبل الأرقام

**تُعتمَد ذراعٌ للعرض على المالك إن استوفت الثلاثة:**

| # | المعيار | الحدّ |
|---|---|---|
| (أ) | `d50` مجمَّعًا لا ينقص عن `C0` | ‏≥ 0 |
| (ب) | ولا سنةً بأكثر من | **‏20%** |
| (ج) | `R`/صفقة **مجمَّعًا** لا يسوء بأكثر من | **‏0.02R** |

📌 **والقراءةُ «مجمَّعًا» مكتوبةٌ الآن صراحةً** — تصحيحًا لقصورِ `T-BASE-475`
حيث تركتُ (ج) مبهمةً فصار الحكمُ «غير حاسم» **بسببي**.

**(د) مِجَسُّ `RUBI` — تشخيصٌ مُعلَنٌ لا بوّابةُ حكم:** يُشغَّل
`_diagnose_symbol` **الإنتاجيّ** على `RUBI` تحت كلّ ذراع، ويُطبَع: هل قُبِل؟
وإن رُفِض فبأيّ جدارٍ جديد؟ ومعه `hi52/price` المقيسة.

🔴🔴 **وإعلانٌ صريحٌ ضدّ تحريك الهدف: «يقبل RUBI» ليس معيارَ نجاح ولن يصير.**
السببان: ① `RUBI` **مختارٌ بعد الحدث** (المالكُ يملكه) ⇒ معايرةُ عتبةٍ لتقبله
هي ملاءمةُ عيّنةٍ واحدة ② والمعاييرُ (أ)(ب)(ج) وحدها تحكم. فإن قبلته ذراعٌ
ساقطةٌ بالمعايير ⇒ **تبقى ساقطة**، وإن رفضته ذراعٌ عابرة ⇒ **تبقى عابرة**.

**سقفُ النجاح — مثبَّتٌ سلفًا:** **منحنى كلفةٍ + توصيةٌ للمالك.** ولا تغييرَ
إنتاجيّ بأيّ حال إلّا بأمرٍ صريحٍ منه (يمسّ العضوية ⇒ `LOGIC_VERSION` +
`force_renew=1` مرّة).

---

## ⑦ تنبّؤاتٌ قابلةٌ للكسر — مكتوبةٌ قبل أيّ رقم

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| `P1` | **`RUBI` لا يصير إشارةً في أيّ ذراع** — `M3` ترفضه (‏`best_spike`=65% مقابل أرضية **78.27**) عند النقاط الحديثة | ظهورُه مقبولًا في أيّ ذراع |
| `P2` | **الأثر صغير**: زيادةُ الإشارات في `C3` **أقلُّ من 5%** عن `C0` في كلّ سنة | تجاوزُ 5% في أيّ سنة |
| `P3` | `d50` مجمَّعًا **لا يتحسّن بأكثر من ‏+3** (‏±3 = ضجيجُ العدد الصحيح الصغير المدوَّن) | تحسّنٌ ‏≥ +4 |
| `P4` | `R`/صفقة **يسوء** اتجاهًا (الفتحُ يُدخل المقسَّم المتسلسل) | تحسّنٌ مجمَّع |
| `P5` | عدّادُ جدار السقف عند `C3` = **صفر** في الثلاث | أيُّ رقمٍ غير صفر ⇒ **عطبُ أداة** |
| `P6` | **الرتابة**: `C1`/`C2` بينهما فرقٌ ضئيلٌ جدًّا (‏الكتلةُ تتركّز فوق ×100,000) | فرقٌ كبيرٌ بين `C1` و`C2` |

---

## ⑧ عيبٌ بنيويٌّ اكتُشف **قبل** كتابة الأداة — ويُدوَّن لأنه يمسّ أدواتٍ أخرى

🔴🔴 **`BT_MAX_DROP_PCT` — ومعه خمسةُ أعلامٍ أخرى — ميّتٌ بنيويًّا عند
`FAISAL_ONLY=1`** (وهو الافتراض في `CONFIG` وفي `backtest.yml`).

**البرهانُ من ترتيب التنفيذ على مستوى الوحدة:**

```
Super_stock.py:769   _BT_OVERRIDES = _apply_backtest_overrides(MODE)   # يقرأ BT_* من البيئة ⟶ CONFIG
Super_stock.py:945   _FAISAL_ONLY_APPLIED = apply_faisal_only()        # cfg.update(ov) ⟵ يدهسها
```

و`ov` من ظرف الكاتالوج يحوي **‏20 مفتاحًا** منها `MAX_DROP_PCT` بالضبط
(مطبوعٌ في سجلّ كلّ تشغيلة) ⇒ **الظرفُ يدهس تجاوزَ الباكتيست.**

**والأعلامُ الستّة المدهوسة:** `BT_MAX_DROP_PCT` · `BT_MIN_DROP_FLOOR` ·
`BT_BASE_RANGE_MAX` · `BT_MIN_PRICE` · `BT_MIN_DOLLAR_VOL` · `BT_WATCH_MAX_FAILS`.

🐞 **وقفلُ `OGB6` القائم لا يمسكه** — لأنه ينادي `_apply_backtest_overrides`
**بعد** اكتمال الاستيراد (فبعد `apply_faisal_only`)، فيمرّ أخضرَ على مسارٍ
لا يقع في الإنتاج. **قفلٌ يحرس ترتيبًا لا يقع = قفلٌ على لا شيء** (النمطُ
المدوَّن نفسُه).

✅ **وأثرُه على تجاربَ منشورة: صفرٌ فيما فُحص** — الأذرعُ التي ركضت بعد
2026-08-06 تضبط `CONFIG` **بعد الاستيراد** (‏`base2_arms:139`) أو تستعمل
مفاتيحَ **خارج** الظرف (‏`BT_ANCHOR` · `BT_STABILITY_GATE`) ⇒ لم يُبنَ حكمٌ
على علمٍ ميّت. **وهذا يُفحَص ولا يُفترَض** (بندٌ في النتيجة).

⚙️ **وعليه فأداةُ T-CEILING تضبط `S.CONFIG["MAX_DROP_PCT"]` بعد الاستيراد**
(نمطُ `base2_arms`) **ولا تعتمد على البيئة إطلاقًا** — و`V1` يطبع القيمةَ
النافذة فيستحيل أن تمرّ ذراعٌ ميّتة.

---

## ⑨ حدودُ الصدق — تُقرأ **مع** الأرقام لا بعدها

1. 🔴 **الأثقل: هذي التجربة تقيس «كم يُكلّف *قبولُ* أثر التقسيم» لا «كم يُكلّف
   *تصحيحُه*».** فتحُ السقف يُدخل السهمَ **بقراءةِ هبوطٍ مشوّهة (‏100%)** لا
   بقراءةٍ صادقة. **والتصحيحُ هو `R1`** (مرجعُ ما بعد التقسيم) **وقد أغلقه
   المالك** 2026-08-08 ⇒ **الذراعان يجيبان سؤالين مختلفين ولا يُقارَنان.**
2. **انحيازُ بقاء** — لقطاتُ PIT تخفّفه ولا تمحوه.
3. **تشويهُ التقسيمات** قائمٌ في كلّ الأسعار (وهو موضوعُ التجربة نفسِه).
4. **بلا الجلسة الممتدّة** — الشموعُ اليومية لا تشمل الافتر ⇒ أرقامُ `d50` أرضيّة.
5. **السعةُ 15 والمُرتِّبُ مقيسٌ عند الصدفة** (‏`T-REPLAY10`) ⇒ **‏±3 داخل الضجيج**،
   وأيُّ فرقٍ في `d50` أصغرَ من ذلك **لا يُدَّعى**.
6. **خطوةُ المشي 5 جلسات** ⇒ عيّنةٌ متّسقةٌ بين الأذرع لا حصرٌ كامل.
7. **«يُقبَل مرشّحًا» ≠ «يصل المالك»** — ولهذا المقياسُ الحاكم `d50` المحفظيّ.
8. **`RUBI` حالةٌ تشخيصية لا اختبار** (‏§⑥-د).
9. **ولا يُدَّعى عائدٌ**: `d50` تسليمُ منفجرين لا ربحية؛ و`R` بهدفٍ ثابت.

---

## ⑩ ما يُنتَج

`ceiling_arms.py` (‏+ `ceiling.yml` يدويّ بلا كرون) ⟶ ثلاثُ تشغيلاتٍ (سنةً سنة)
⟶ `ceiling_result.md` **بكلّ الأرقام والتنبّؤات وبطاقتِها** (تحقّق/كُذِّب)
**ومنحنى الكلفة** ⟶ عرضٌ على المالك.

**وإن سقطت بوّابةُ الصلاحية في أيّ سنة: تُنشَر «لا حكم» ولا تُفسَّر النتيجة.**
