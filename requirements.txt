# 📌 **مثبَّتة عمدًا** (تدقيق 2026-07-28، خطة `plans/007`).
#
# لماذا: كل workflow إنتاجي يثبّت من هنا **وقت التشغيل**، و`yfinance` مكتبة تكشط بيانات
# بلا عقد API مستقرّ — تكسر شكل مُخرَج `download`/`Ticker().info` بين الإصدارات بلا إنذار،
# فقد يختلف سلوك تشغيلتين متتاليتين لنفس الكوميت.
#
# 🔴 **والفجوة الأخطر:** سويّة `test_bot.py` تعمل **بلا إنترنت بتصميمها** (كل الجالبات
# محقونة/مقلَّدة) ⇒ كسرُ yfinance **لا يُسقط أي اختبار**: تبقى `tests.yml` خضراء بينما
# الإنتاج يقرأ صفر بيانات. حراس التغطية (`DATA_HEALTH_MIN_PCT`=85) تمنع الضرر — لكنها
# **لا تشخّص السبب**: يرى المالك «تغطية ضعيفة» يوميًّا بلا معرفة أنها ترقية مكتبة.
#
# 🔧 **بروتوكول الترقية (لا تُرقِّ بلا هذا):**
#   1) غيّر النسخة على فرع  2) `python3 test_bot.py` (خروج 0 · صفر فشل)
#   3) شغّل `deps_smoke.yml` يدويًّا من Actions (فحص العقود الحيّة)  4) ادمج عند ✅ فقط.
#
# ⚠️ الاعتماديات المتعدّية غير مقفولة عمدًا (لا `pip-compile`/hashes) — قرار معماري للمالك.
# 🔴 **‏`PyYAML` أُضيفت 2026-08-07 — لأن غيابَها أبقى بوّابة CI حمراء بصمت.**
# السبب: أقفال `test_bot.py` صارت تقرأ **ملفّات الـworkflows نفسَها** (مجموعةُ التزامن ·
# مدخلاتُ الأذرع · قفلُ «كل مفتاح BT_* له صفّ») — وهي أقفالٌ **بنيويّة لا تُستغنى عنها**.
# وكانت المكتبة حاضرةً محلّيًّا وغائبةً عن الرنر ⇒ `python3 test_bot.py` ينجح عندي
# و`tests.yml` يسقط بـ`ModuleNotFoundError` على **كلّ دفعة**.
# ⚠️ **ولا يُحَلّ بـ`try/except ImportError`** — ذاك يجعل الأقفال **تختفي في CI بالذات**
# وهو أسوأُ من الفشل: بوّابةٌ خضراء بلا حراسة. ⇒ التصريحُ بالاعتمادية هو الحلّ.
# 🔒 ومقفولٌ باختبار: **كلُّ استيرادٍ غيرِ قياسيّ في السويّة له سطرٌ هنا**.
PyYAML==6.0.1
yfinance==1.5.2
pandas==3.0.5
numpy==2.4.6
requests==2.34.2
