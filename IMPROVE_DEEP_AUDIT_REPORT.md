# 🔍 تقرير التدقيق العميق — `/improve deep`

> **التاريخ:** 2026-07-12
> **الـSHA المُدقَّق:** `8f9362a` («bot data 2026-07-11»)
> **الطبيعة:** قراءة/تحليل فقط — **صفر تغيير كود**. هذا الملف هو الأثر الوحيد للجلسة.
> **مصدر المحتوى:** نتائج جلسة `/improve deep` التي اكتملت قبل كتابته. لم يُعد تشغيل أي فحص.

## 🏷️ اصطلاح التوثيق (مهم — اقرأه قبل أي بند)

كل ادّعاء في هذا الملف موسوم بأحد ثلاثة:

| الوسم | المعنى |
|---|---|
| **✅ حقيقة مؤكَّدة** | قرأتُ الكود/البيانات بنفسي وأثبتُّه. لا اجتهاد. |
| **🔸 استنتاج** | مبني على حقائق مؤكَّدة + منطق صريح مذكور. قابل للنقض بدليل. |
| **⚪ احتمال / غير محقَّق** | لم أستطع إثباته في هذه البيئة (لا شبكة/لا اعتماديات/لا سجلّ Actions). يحتاج تحققًا. |

**لم أفبرك أي رقم.** كل ما لم أستطع قياسه قلتُ عنه ذلك صراحةً.

---

# 1) الملخّص التنفيذي

بوت `Super_Stocks` **ليس مشروعًا مهملًا** — بل العكس: ثقافة الأدلة فيه (تجارب مسجَّلة مسبقًا، مراجعات خصومية، أحكام تصمد) أقوى من أغلب ما أراه. `CLAUDE.md` (579 سطرًا مؤرَّخة ومربوطة بـPRs) نجا من كل فحوص التناقض التي أجريتها. والأقفال المعمارية (`getsource` تمنع طبقات العرض من التسرّب إلى الاختيار) فكرة صحيحة ونادرة.

**الخلل ليس في المنهجية — بل في ثلاث فجوات بنيوية:**

1. **فجوة زمنية:** المسار اليومي يعمل **قبل افتتاح السوق بـ6 ساعات** ويختم السجلات بتاريخ التشغيل، بينما المتابعة تفحص `day > added` — فـ**جلسة الترشيح الأولى لا تُقيَّم أبدًا**. ستوب في اليوم الأول = غير مرئي، للأبد. (النتيجة ①)
2. **فجوة توقيت صيفي/شتوي:** كل نوافذ السوق مثبَّتة على EDT. **من نوفمبر** ستقرأ المراقبةُ اللحظية شمعة **أمس** كأنها حيّة، **وتستهلك خانة الدِدوب** فيُكتَم التنبيه الحقيقي بقية اليوم. (النتيجة ⑤)
3. **فجوة تحقّق:** **لا workflow يشغّل الـ638 اختبارًا**. 433 كوميت في 8 أسابيع إلى `main` بلا بوابة. وبوابات M1–M14 — قلب المنتج — **بلا اختبار رفض واحد**، فأقفال B3/C3 «الموثّقة» أقفال **ورقية لا اختبارية**. (النتيجتان ② و④)

**عن الباكتيست، بصراحة:** من الـ14 نتيجة **لا واحدة تُفسد محرّك `backtest_symbol`**. لم أجد فيه خطأً. القيدان الحقيقيان على مصداقيته موثّقان عندك ومؤجَّلان **بقرارك**: صفر تكاليف تنفيذ/سبريد (`FAISAL1_VIDEO_PLAN.md:66-67`) وانحياز البقاء. وهما يحرّكان أحكامًا فُصِلت بهوامش ضيقة (T1b سقط بفارق 0.4 · تجربة M4/M2 على 4-5 صفقات محسومة) — **قرار منهجي مؤجَّل، لا عطل.**

**رُفض في التحقّق:** ادّعاء أن `fintel_short` عنق أداء يكلّف «حتى 88 دقيقة». البيانات المخزَّنة كذّبته (~5 مرشّحين فقط يصلون البوابة → ~8 ثوانٍ). تفاصيله في §5.

---

# 2) مدّة التدقيق · المراحل · الوكلاء

## المراحل
| المرحلة | ما جرى |
|---|---|
| **الاستطلاع** | بنية الريبو · `requirements.txt` · 14 workflow · churn عبر git · محاولة تشغيل السويّة · قراءة `CLAUDE.md`/`PENDING_VERIFICATION.md`/`ai_debate/round3` |
| **التدقيق (متوازٍ)** | **8 وكلاء `Explore` للقراءة فقط**، أُطلقوا معًا، واحد لكل فئة/عنقود |
| **التحقّق (بنفسي)** | فتحتُ كل موضع مُستشهَد به وقرأته. أُسقط ادّعاء رئيسي وصُحِّح آخر — التفاصيل في §5 |
| **الترتيب** | بالرافعة (الأثر ÷ الجهد، مخصومًا بالثقة وخطر الإصلاح) |

## الوكلاء الثمانية (مُدد فعلية من إشعارات الإنجاز)
| الوكيل | المدّة | استدعاءات أدوات | التوكنات |
|---|---|---|---|
| صحّة/أخطاء — النواة (`Super_stock.py`) | ~909 ث | 39 | 141,990 |
| صحّة/أخطاء — السكربتات التابعة + الـworkflows | ~807 ث | 30 | 126,683 |
| الأمان | ~623 ث | 20 | 60,827 |
| الأداء | ~583 ث | 23 | 90,583 |
| تغطية الاختبارات | ~601 ث | 20 | 56,413 |
| الدَّين التقني والمعمار | ~738 ث | 23 | 76,080 |
| الاعتماديات وتجربة المطوّر | ~477 ث | 16 | 43,309 |
| التوثيق والاتجاه | ~704 ث | 25 | 117,895 |

**الزمن الجداري للتدقيق المتوازي ≈ 15 دقيقة** (أطول وكيل ~15.1 د). مجموع عمل الوكلاء ≈ 5,442 ثانية (~91 دقيقة معالجة متوازية) · 196 استدعاء أداة · ~713 ألف توكن.

**نطاق التغطية:** `deep` = الريبو كامل، كل الفئات التسع.
**ما لم يُغطَّ (بصراحة):** لم أشغّل السويّة ولا البوت ولا الباكتيست (مستحيل بيئيًّا — انظر §6) · لم أرَ سجلّ تشغيلات GitHub Actions · لم أدقّق صحّة المؤشرات الرياضية (`rsi`/`macd`/`atr`…) بندًا بندًا · لم أفحص `backtest_symbol` بحثًا عن نظرٍ مستقبلي (وهو **أهمّ مكان متبقٍّ للنظر فيه**، لأن كل العتبات «مقفولة بالباكتيست»).

---

# 3-4) النتائج الأربع عشرة (بالترقيم الأصلي)

---

## ① الجلسة الأولى بعد الترشيح لا تُقيَّم أبدًا

- **النوع:** خطأ منطقي (آلة الحالة / التتبّع)
- **الخطورة:** 🔴 **حرجة** — أثر مالي مباشر
- **الثقة:** ✅ **عالية جدًا** (قرأت المسارين والكرون بنفسي)
- **الملف والأسطر:** `Super_stock.py:7876` · `Super_stock.py:4748` · `Super_stock.py:5571-5578` · `Super_stock.py:9175-9179` · `.github/workflows/daily_screener.yml:15`

### الدليل من الكود — ✅ حقيقة مؤكَّدة
```python
# Super_stock.py:7876 — run_daily_watchlist
today_iso = dt.date.today().isoformat()          # ← تاريخ التشغيل

# Super_stock.py:4748 — make_watch_entry
"symbol": r["symbol"], "added": today_iso,

# Super_stock.py:5571-5578 — update_watchlist_status
# شموع ما بعد يوم الإضافة فقط (التنبيه صدر بعد إغلاق يومه)   ← الافتراض المكتوب
if day > added:
    rows.append((day, float(df["High"].iloc[i]), float(df["Low"].iloc[i])))
```
```yaml
# .github/workflows/daily_screener.yml:15
- cron: "23 7 * * 2-5"      # 07:23 UTC = قبل افتتاح ناسداك بـ6 ساعات
```

### شرح المشكلة
الكرون يعمل 07:23 UTC، أي **قبل** افتتاح السوق (13:30 UTC صيفًا). فآخر شمعة في `df` هي إغلاق **أمس**، بينما `added` يُختم بتاريخ **اليوم**. لاحقًا تفحص المتابعةُ الشموعَ بشرط `day > added` — فشمعة يوم الترشيح نفسه (التي تتكوّن بعد ساعات) شرطها `اليوم > اليوم` = `False` → **مُستبعَدة في كل تشغيل لاحق، إلى الأبد**.

**التعليق في السطر 5571 يوثّق الافتراض صراحةً** («التنبيه صدر بعد إغلاق يومه») — وهو **صحيح لتجديد الجمعة 22:07** (بعد الإغلاق) و**خاطئ للمسار اليومي 07:23** (قبل الافتتاح). أي أن المسار اليومي يخرق عقدًا كُتب لأجل مسار آخر.

نفس العيب في متتبّع التنبيهات: `Super_stock.py:9175-9179` (`if age < 1: continue` ثم `start = date + 1d`).

### السيناريو
الثلاثاء 07:23 UTC: يُرشَّح سهم على إغلاق الاثنين، `added = 2026-07-07`. تُفتح جلسة الثلاثاء 13:30، السهم يهوي ويخترق ستوبه.
الأربعاء 07:23: تعمل المتابعة، شمعة الثلاثاء موجودة في `df` لكن `day == added` → **مُتخطّاة**.
النتيجة: السهم يبقى `status: active`، الستوب **لم يُسجَّل**، و`max_gain_pct` لا يرى أعلى/أدنى ذلك اليوم. ولا تشغيلة لاحقة تعيد فحصها.

### الأثر
- **إشارات التداول:** 🔴 سهم مشطوب فعليًا يبقى معروضًا كـ«نشط» في قائمتك.
- **استقرار/تضليل:** 🔴 إحصاءات النجاح/الخسارة التي تغذّي تقرير التطوير **منحازة بحذف اليوم الأول** — وهو أخطر الأيام. «سجلّه الحي 80%» الذي تعتمد عليه محسوب على عيّنة ناقصة اليوم الأول.
- **الباكتيست:** لا أثر (مسار مستقل).
- **Telegram:** غير مباشر (تنبيه شطب لا يُرسَل).

### التحقّق / إعادة الإنتاج (بلا تعديل كود)
```bash
python3 -c "import json;w=json.load(open('weekly_watchlist.json'));\
print([(s['symbol'],s['added'],s.get('hit_date')) for s in w['stocks']+w['removed']])"
```
ابحث عن سجلّ واحد `hit_date == added`. **غيابه التام عبر كل السجلات = بصمة الخطأ.**
(⚠️ قارِن `hit_date` تحديدًا — `removed_date` يُكتب بتاريخ التشغيل لا بتاريخ الشمعة، فلا يصلح دليلًا.)

### الإصلاح المقترح — 🔸 استنتاج (لم يُنفَّذ)
خزّن **تاريخ شمعة الترشيح** لا تاريخ التشغيل: أضف حقلًا `ref_bar = df.index[-1].date()` في `make_watch_entry` و`record_new_alerts`، وقارن `day > ref_bar` في `update_watchlist_status`، و`start = ref_bar + 1d` في `update_tracking`. أبقِ ارتدادًا لـ`added` للسجلات القديمة.
**⚠️ يستوجب رفع `LOGIC_VERSION` + ترحيلًا** — لأن سجلات مفتوحة قد تُحسم فورًا كمشطوبة (موجة تصحيح لمرة واحدة، متوقَّعة وصحيحة).

### الاختبارات المطلوبة بعد الإصلاح
1. سجل `added=2026-07-07` + إطار فيه شمعة `2026-07-07` أدناها تحت الستوب → **يجب أن يُشطب**. (اليوم: لا يُشطب.)
2. مسار الجمعة (`added` = يوم شمعة حقيقية) → السلوك **دون تغيير** (قفل انحدار).
3. سجل قديم بلا `ref_bar` → الارتداد لـ`added` يعمل، بلا انهيار.

---

## ② لا بوابة CI تشغّل الاختبارات الـ638

- **النوع:** بنية تحقّق (تمكينية — تسبق كل إصلاح آخر)
- **الخطورة:** 🟠 **عالية**
- **الثقة:** ✅ **عالية جدًا** (grep مباشر)
- **الملف والأسطر:** `.github/workflows/` (14 ملفًا — لا واحد فيه `on: push` أو `pull_request`) · `test_bot.py:3985-3988` · `CLAUDE.md:561`

### الدليل — ✅ حقيقة مؤكَّدة
- `grep -rln "test_bot" .github/workflows/` → يطابق **`cline_weekly_review.yml` فقط** (كرون الجمعة 08:17).
- العدد الفعلي للفحوص: `grep -c "check(" test_bot.py` → **638** (قِسته بنفسي).
- بينما `CLAUDE.md:561` يقول: «لازم **191+/191+**» و`CLINE_WEEKLY_REVIEW.md:57` يقول «**181/181**». والوثائق تقتبس ستة أعداد متضاربة عبر الملفات (191 · 181 · 277 · 325 · 498 · 649).
- السويّة **صالحة كبوابة كما هي**:
```python
# test_bot.py:3985-3988
print(f"النتيجة: {len(PASS)} نجح · {len(FAIL)} فشل")
if FAIL:
    print("الفاشل: " + " | ".join(FAIL))
    raise SystemExit(1)          # ← خروج ≠ 0 عند أي فشل
```

### شرح المشكلة
أكبر أصل سلامة في المشروع (638 تأكيدًا تحرس البوابات والمؤشرات وآلة الحالة) **لا يشغّله أي شيء آليًّا**. القاعدة «شغّله قبل أي دفع» شرف شخصي، لا بوابة. و**العدد المقتبس في بوابتين حقيقيتين (قاعدة الدفع + دستور Cline) أقلّ من الواقع بـ3.4×** — أي أن حذف 450 تأكيدًا يظلّ «مطابقًا للبوابة» حرفيًّا.

**🔸 استنتاج:** 433 كوميت في 8 أسابيع، كلها إلى `main`، بلا حارس آلي.

### السيناريو
انحدار في `Super_stock.py` يُدمج في `main` → يصل إلى تلغرام في كرون 07:23 التالي، بلا أي شيء بينهما.

### الأثر
🟠 يضاعف خطورة **كل** نتيجة أخرى في هذا التقرير: أي إصلاح تنفّذه لن يكون **مُتحقَّقًا منه** بلا هذه البوابة.

### التحقّق (بلا تعديل)
```bash
grep -rn "on:" -A3 .github/workflows/*.yml | grep -E "push|pull_request"   # → صفر نتائج
grep -c "check(" test_bot.py                                              # → 638
```

### الإصلاح المقترح — 🔸 استنتاج
ملف workflow واحد (~15 سطرًا): `on: [push, pull_request]` → `setup-python 3.11` (نفس نسخة الإنتاج) → `pip install -r requirements.txt` → `python3 test_bot.py`.
وأوقِف اقتباس الأعداد نهائيًّا في الوثائق — البوابة الصحيحة هي **«خروج 0 · صفر فشل»**، لأن أي عدد يتقادم.
**⚪ غير محقَّق:** لا أعرف إن كانت السويّة **خضراء الآن** (تعذّر تشغيلها — §6). لو كانت حمراء يصير الإصلاح «أصلحها ثم بوّبها».

### الاختبارات المطلوبة
لا اختبارات جديدة — الملف **هو** الاختبار. تحقّق: PR بانحدار متعمَّد (اقلب `>` إلى `<` في بوابة) **يجب أن يُحوِّل CI أحمر**.

---

## ③ فحوص تتحوّل بصمت إلى «لا-عمليات» (بما فيها حارس «الكارثة»)

- **النوع:** جودة اختبارات (سلامة زائفة)
- **الخطورة:** 🟠 **عالية**
- **الثقة:** ✅ **عالية** على الكود · **مع تصحيح مهم أدناه**
- **الملف والأسطر:** `test_bot.py:3192-3204` (الكارثة) · `3328` · `3349` · `3374` · `3847` · `3911` · `3928` · `3962-3980` · و`test_bot.py:120`

### ⚠️ تصحيح ذاتي (مهم — كنتُ بالغت)
في ملخّصي الشفهي الأول قلت إن السويّة «تصير خضراء كلها لو مات البوت». **هذا خطأ مني وأصحّحه:** `test_bot.py:106` يحوي `check("سهم الارتكاز يُحلَّل (ليس None)", r0 is not None)` — فانحدار يجعل `analyze_ticker` يرجّع `None` **للجميع** **سيُلتقَط**. الخلل الحقيقي **أضيق وأدقّ**، وهو ما أصفه الآن.

### الدليل — ✅ حقيقة مؤكَّدة
```python
# test_bot.py:3192-3204 — «(أ) الكارثة: الوقف لازم يكون دائمًا تحت أدنى منطقة الدخول»
_stop_ok = True
for sd in range(6):
    for cur, cl, ph in [(3.6, 3.0, 20.0), (1.6, 1.3, 9.0), (12.0, 9.0, 60.0)]:
        rr = S.analyze_ticker("X", synth_pivot(current=cur, crash_low=cl,
                                               prior_high=ph, seed=sd))
        if rr is None:
            continue                     # ← التكرار يختفي بلا أثر ولا عدّاد
        lo = rr["entry"][0]
        if not (rr["stop"][0] < lo and rr["stop"][1] < lo):
            _stop_ok = False
check("الوقف دائمًا تحت أدنى الدخول (لا كارثة)", _stop_ok)   # ✅ حتى لو صفر تكرار فعلي
```

### شرح المشكلة (الحدّ الدقيق)
انحدار **جزئي** — بوابة ترفض خطأً نطاقًا سعريًا معيّنًا (مثلًا `cur=1.6` أو `cur=12.0`) أو بذورًا معيّنة — يجعل تلك التكرارات **تُتخطّى بصمت**، فلا يُفحَص حارس «الوقف تحت الدخول» **لتلك المدخلات إطلاقًا**، والفحص يبقى ✅. **لا يوجد أي عدّاد يثبت أن الحلقة اشتغلت ولو مرة واحدة.**

نفس النمط في `test_bot.py:120` (`if r0 and _diag:`) — لو كان `r0` فارغًا، الفحص لا يُسجَّل نجاحًا **ولا فشلًا**، بل **يختفي**.

### السيناريو
تعديل يجعل بوابة ترفض الأسهم تحت $2. تكرارات `cur=1.6` (ثلثا العيّنة عمليًّا) تصير `None` → تُتخطّى. الحارس الوحيد الذي يمنع «وقف فوق الدخول» (= خسارة فورية مضمونة عند التنفيذ) **معطَّل لتلك النطاقات وأنت تراه أخضر**.

### الأثر
🟠 **إشارات التداول:** الحارس المسمّى «الكارثة» في الكود نفسه قد يكون غير فعّال دون أي إشارة.

### التحقّق (بلا تعديل)
لا يمكن إثبات «هل هو معطَّل الآن؟» بلا تشغيل السويّة (§6). **القصور المؤكَّد هو غياب الضمانة**، لا بالضرورة عطل حالي.
الفحص المستقبلي: أضف `_seen = 0` / `_seen += 1` داخل الحلقة واطبعه — لو `_seen == 18` فالحلقة سليمة اليوم.

### الإصلاح المقترح — 🔸 استنتاج
لكل حلقة تحوي `continue` على `None`: عدّاد `_seen`، والفحص يصير `check(..., _ok and _seen >= 1)`. وحوّل `if r0 and _diag:` (سطر 120) إلى `else: check(..., False)`.
تعديل ميكانيكي على **ملف الاختبارات فقط** — لا يمسّ كود الإنتاج.

### الاختبارات المطلوبة
الفحوص الثمانية نفسها بعد التحصين: عطّل `analyze_ticker` عمدًا لنطاق سعري واحد → **يجب أن يفشل الفحص الآن** (اليوم: ينجح).

---

## ④ صفر اختبار رفض للبوابات الصلبة M1–M5 / M10

- **النوع:** فجوة تغطية على قلب المنتج
- **الخطورة:** 🟠 **عالية**
- **الثقة:** ✅ **عالية** (بحث مباشر في `test_bot.py`)
- **الملف والأسطر:** البوابات في `Super_stock.py:1704-1805` · العتبات في `Super_stock.py:120-156`

### الدليل — ✅ حقيقة مؤكَّدة
رموز الرفض الصلبة في `analyze_ticker`: `M1_سعر` · `M2_hi52` · `M2_هبوط_فوق_97` · `M2_هبوط_تحت_40` · `M3_انفجار_تحت_60` · `M4_base_lo` · `M4_base_واسعة` · `M4_انفجر_فعلاً` · `M5_سيولة` · `M10_RSI_ما_تشبّع` · `M10_RSI_فات_القطار`.

بحثتُ عن أي فحص يُطعم إطارًا يكسر بوابة **صلبة** ويؤكّد `analyze_ticker(...) is None`. **لا يوجد.** الموجود فقط:
- `test_bot.py:161` — «4 نواقص → يُرفض None» = عتبة **النواقص الليّنة**، لا بوابة صلبة.
- `test_bot.py:163` — تصنيف الفئة.
- `test_bot.py:2568-2574` — regex على **نصّ المصدر** يتأكّد أن رموز الرفض بلا `<`/`>` = حارس HTML لتلغرام، **ليس اختبار بوابة**.

لا شيء يثبت أن سهمًا بسعر $1.40 يُرفض، ولا أن هبوطًا 39% يُرفض، ولا أن سيولة $190K تُرفض.

### شرح المشكلة
كل عتبة في `CONFIG` (`MIN_PRICE=1.5` · `MIN_DROP_FLOOR=40` · `MAX_DROP_PCT=97` · `PRIOR_SPIKE_FLOOR=60` · `BASE_RANGE_MAX_PCT=40` · `MIN_DOLLAR_VOL=200K` · RSI 32/50) يمكن تغييرها لأي قيمة — أو **عكس عامل مقارنة واحد** — والسويّة تبقى خضراء ما دام `synth_pivot` ينفذ.

**🔸 استنتاج حادّ:** الأقفال B3 (عتبتا M4/M2) وC3 (السقف 97 · النافذة 20ج · السيولة 200K) موصوفة في `CLAUDE.md` بأنها «مقفولة باختبار». **هي مقفولة وثائقيًّا لا اختباريًّا** — لا اختبار يمنع تغييرها.

### السيناريو
وكيل (أو أنت في جلسة متعبة) يعدّل `MIN_DROP_FLOOR` من 40 إلى 35 «تجريبيًّا» وينسى. السويّة خضراء. البوت يبدأ ترشيح أسهم لا تطابق منهجية فيصل، وتكتشفه بعد أسابيع من نتائج سيئة.

### الأثر
🟠 **إشارات التداول + الباكتيست:** هذه هي العتبات التي «حُسمت بالتجربة» (50%→40% عند التخفيف). لا شيء يحرسها آليًّا.

### التحقّق (بلا تعديل)
```bash
grep -n 'check("' test_bot.py | grep -E "يُرفض|رفض|None"
```
→ 10 نتائج، **لا واحدة منها بوابة صلبة** (شغّلته وراجعت كل نتيجة).

### الإصلاح المقترح — 🔸 استنتاج
11 فحصًا ميكانيكيًّا: خذ `synth_pivot(seed=2)` (العيّنة المعروفة بالنجاح)، اعبث **بمُدخل واحد** حتى يتجاوز عتبة واحدة، وأكّد `analyze_ticker(...) is None`.
**⚠️ تحذير صادق:** كتابة هذه الاختبارات **قد تكشف أن بوابة لا تعمل كما هو موثّق**. هذا **هو المكسب**، لا انحدار.
**يعتمد على ③** — لا تُضِف اختبارات إلى سويّة تكذب على نفسها.

### الاختبارات المطلوبة
هي نفسها المنتج: 11 فحص رفض، واحد لكل رمز. + قفل: تغيير أي عتبة في `CONFIG` **يجب** أن يُفشل فحصًا واحدًا على الأقل.

---

## ⑤ كل نوافذ السوق مثبَّتة على التوقيت الصيفي (EDT) — تُسقِط التنبيه الحقيقي شتاءً

- **النوع:** خطأ منطقي (توقيت) — قنبلة موقوتة بتاريخ معلوم
- **الخطورة:** 🔴 **حرجة (مؤجَّلة — تنفجر ~نوفمبر 2026)**
- **الثقة:** ✅ **عالية جدًا**
- **الملف والأسطر:** `pullback_live.py:43-44` · `Super_stock.py:6013-6081` · `Super_stock.py:6141-6145` · `Super_stock.py:5947` · `Super_stock.py:5976` · `Super_stock.py:6102-6113` · `ignition_live.py:28-37` · `.github/workflows/ignition.yml:14,43` · `.github/workflows/pullback_monitor.yml:8`

### الدليل — ✅ حقيقة مؤكَّدة
```python
# pullback_live.py:43-44
_now = bot.dt.datetime.utcnow()
_pm_only = (_now.hour * 60 + _now.minute) < 13 * 60 + 30      # ← 13:30 UTC ثابتة (EDT)

# Super_stock.py:6141-6145 — دِدوب: خانة واحدة لكل (سهم، نوع) في اليوم
s["live_alert"][kind] = today_iso
```
```yaml
# pullback_monitor.yml:8
- cron: "*/30 11-23 * * 1-5"     # يشمل 13:30 و14:00
# ignition.yml:14,43
- cron: "35 13 * * 1-5"
  IGNITION_END_UTC: "19:20"
```

### شرح المشكلة
شتاءً (EST) افتتاح ناسداك = **14:30 UTC** لا 13:30. فتشغيلتا 13:30 و14:00 تمرّران `premarket_only=False` **والسوق مقفل**، فيقرأ `monitor_live_events` قيمة `df["Close"].iloc[-1]` = **إغلاق أمس** ويحكم عليها كأنها لحظية.

**والقاتل ليس التنبيه الكاذب — بل الدِدوب:** الحدث الكاذب **يستهلك الخانة الوحيدة** لذلك (سهم، نوع) في ذلك اليوم، فيُكتَم الحدث **الحقيقي** حين يقع فعلًا أثناء الجلسة. **تخسر التنبيه، لا تستقبل واحدًا زائدًا فقط.**

وإضافةً — ✅ مؤكَّد:
- `afterdump` يبدأ 20:00 UTC = **آخر ساعة من الجلسة النظامية** شتاءً → هبوط نظامي يُوسَم «تصريف أفتر».
- الرادار يبدأ 13:35 = **ساعة قبل الافتتاح** (يلفّ على العدم) وينتهي 19:20 = **1:40 قبل الإغلاق**.

### السيناريو (نوفمبر)
13:30 UTC: تشغيلة pullback. السوق **لم يفتح**. `_pm_only=False` → يُطلق حدث `buyzone` من شمعة أمس ويكتب `live_alert["buyzone"] = اليوم`.
16:00 UTC: السوق مفتوح، السهم **ينزل لدعمه حقيقةً** → الحدث **مكتوم** (الخانة مستهلكة).
**التنبيه الذي بُني البوت كلّه لأجله: لم يصل.**

### الأثر
- **Telegram / إرسال التنبيهات:** 🔴 كتم التنبيه الحقيقي.
- **إشارات التداول:** 🔴 أحداث جلسة محسوبة على شمعة ميتة.
- **حافة التوقيت:** 🔴 الرادار — حافتك الوحيدة المُثبتة — يفقد أثمن نافذة (الافتتاح) ويلفّ ساعة على سوق مغلق.
- **الباكتيست:** لا أثر (يومي).

### التحقّق (بلا تعديل — لا تنتظر نوفمبر)
```bash
python3 -c "
from zoneinfo import ZoneInfo; import datetime as dt
for d in ['2026-07-15','2026-11-15']:
    x=dt.datetime.fromisoformat(d+'T09:30').replace(tzinfo=ZoneInfo('America/New_York'))
    print(d,'الافتتاح UTC =',x.astimezone(dt.timezone.utc).strftime('%H:%M'))"
```
→ يوليو **13:30** · نوفمبر **14:30**. قارِن بالثابت `13*60+30` في `pullback_live.py:44`.

### الإصلاح المقترح — 🔸 استنتاج
دالّة واحدة `market_session_now()` بـ`zoneinfo.ZoneInfo("America/New_York")` (**مكتبة قياسية — بلا اعتمادية جديدة**) تشتقّ الافتتاح/الإغلاق بالـUTC لكل يوم، وتحلّ محلّ الثوابت الستة و`premarket_only`. طبقة **نوافذ زمنية** بحتة — خارج كل أقفال الاختيار.
حارس إضافي: **لا تقيّم أحداث جلسة إلا إذا كان تاريخ آخر شمعة == اليوم.**

### الاختبارات المطلوبة
1. `market_session_now()` بتاريخ صيفي → (13:30, 20:00) · بتاريخ شتوي → (14:30, 21:00).
2. `monitor_live_events` بشمعة تاريخها **أمس** → **صفر أحداث جلسة** (اليوم: يولّدها).
3. قفل انحدار: السلوك الصيفي **دون تغيير**.

---

## ⑥ التجديد الأسبوعي يمسح `reject_stats` — نافذة الـ56 يومًا وهمية

- **النوع:** خطأ منطقي (فقد حالة) + لغم بنيوي
- **الخطورة:** 🟡 **متوسطة** (قياس/تعلّم — لا يمسّ تنبيهًا حيًّا)
- **الثقة:** ✅ **عالية جدًا** (كود **وبيانات**)
- **الملف والأسطر:** `Super_stock.py:7788-7794` · `Super_stock.py:5113-5128` · `Super_stock.py:7768`

### الدليل — ✅ حقيقة مؤكَّدة
```python
# Super_stock.py:7788-7794 — new_wl مبني كـ«قائمة مفاتيح بيضاء»
new_wl = {"week_start": today_iso, "created": today_iso,
          "logic_version": LOGIC_VERSION,
          "stocks": [make_watch_entry(r, today_iso) for r in picks],
          "removed": [], "replacements_log": [], "notes": [],
          "pullback": pull_entries,
          "history": wl.get("history", []),
          "explosions": wl.get("explosions", [])}      # ← لا reject_stats
save_watchlist(new_wl)

# Super_stock.py:5127-5128 — بينما الدالة تَعِد صراحةً بـ56 يومًا
cutoff = (dt.date.today() - dt.timedelta(days=56)).isoformat()
wl["reject_stats"] = [e for e in log_rs if e.get("date", "") >= cutoff][-60:]
```
`record_reject_stats(wl)` يُستدعى في السطر **7768** فيكتب في `wl` **القديم** — الذي يُرمى بعده بعشرين سطرًا.

**الدليل الميداني (أقوى من الكود) — ✅ مؤكَّد:** `weekly_watchlist.json` يحوي **لقطة واحدة** (`2026-07-11`)، لا 56 يومًا.

### السيناريو
كل جمعة 22:07 UTC: التجديد يبني `new_wl` من الصفر → كل تاريخ الرفض المتراكم **يتبخّر**. المقام يبدأ من الصفر كل أسبوع.

### الأثر
- **تضليل:** 🟡 المقام الذي يجيب «هل بوابة M4/M2 متشدّدة؟» (الفائتة ÷ المقام) **لا يتراكم أبدًا**. تقرير التطوير يحسب نسبًا على أسبوع واحد **ويظنّها 56 يومًا**. و`CLAUDE.md:301` يذكر الرقم كأنه حقيقة.
- **لغم بنيوي:** 🔸 **أي مفتاح حالة جديد** تضيفه مستقبلًا سيختفي كل جمعة بصمت.

### التحقّق (بلا تعديل)
```bash
python3 -c "import json;print([e['date'] for e in json.load(open('weekly_watchlist.json'))['reject_stats']])"
```
→ قائمة بعنصر واحد.

### الإصلاح المقترح — 🔸 استنتاج
ابنِ `new_wl` بنسخ `wl` ثم **الكتابة فوق المفاتيح التي يملكها التجديد فقط** — فتنجو المفاتيح المجهولة افتراضيًّا.

### الاختبارات المطلوبة
1. `wl` فيه `reject_stats` بـ10 لقطات → بعد `run_weekly_renewal` **العشر باقية**.
2. مفتاح مخترَع (`wl["زائد"] = 1`) → **ينجو** التجديد (قفل ضد اللغم البنيوي).

---

## ⑦ `company_name` غير معقّم يصل إلى وكيل Cline (contents: write)

- **النوع:** أمان (سطح حقن موجَّه لوكيل)
- **الخطورة:** 🟡 **متوسطة** (أثر عالٍ · احتمال منخفض)
- **الثقة:** ✅ **عالية على مسار البيانات** (تتبّعت كل قفزة) · ⚪ **متوسطة على قابلية الاستغلال**
- **الملف والأسطر:** `Super_stock.py:3225-3227` → `Super_stock.py:4765` → `weekly_watchlist.json` → `.github/workflows/cline_weekly_review.yml:18-20,52`

### الدليل — ✅ حقيقة مؤكَّدة
```python
# Super_stock.py:3225 — نصّ حرّ من ياهو: بلا تحقّق، بلا طول أقصى، بلا محارف مسموحة
r["company_name"] = (info.get("shortName") or info.get("longName") or ...)
# Super_stock.py:4765 — يُخزَّن كما هو ثم يُكوَّت في الريبو
"company_name": r.get("company_name"),
```
و`cline_weekly_review.yml` يمنح `contents: write` + `pull-requests: write`، ويشغّل `cline -y` (**بلا تأكيد بشري**)، **ويأمر الوكيل صراحةً بقراءة `weekly_watchlist.json`**.

### شرح المشكلة
حقل نصّي حرّ من مصدر خارجي (اسم الشركة كما يورّده المُصدِر/البورصة عبر ياهو) يُكوَّت في ملف **يقرأه وكيل ذكاء اصطناعي مستقلّ يملك صلاحية الكتابة على الريبو وفتح PR**. بالنسبة لنموذج لغوي، النصّ في هذا الحقل **لا يتمايز عن التعليمات المحيطة به**.

**⚪ بصراحة عن الاحتمال:** يتطلب التأثير على حقل اسم شركة مدرجة في ناسداك — **ليس هجومًا عابرًا**. لكن **حدّ الثقة غير موجود أصلًا**، والإصلاح سطر واحد.

### الأثر
- **إشارات/باكتيست:** ❌ لا شيء (طبقة إثراء/عرض).
- **استقرار:** 🟡 المسار الوحيد الذي فشله «شيء آخر يكتب في ريبوك» لا «تنبيه لا يصل».

### التحقّق (بلا تعديل)
```bash
grep -n company_name weekly_watchlist.json | head    # → قيم حقيقية مخزَّنة اليوم
```

### الإصلاح المقترح — 🔸 استنتاج
عقّم **عند الحدّ** (`Super_stock.py:3225`) لا عند كل موضع عرض: قصّ إلى محارف محافظة (حروف/أرقام/مسافة/`.,&-`) وطول ~64. وفي الـworkflow: صرِّح للوكيل أن محتوى JSON/CSV **بيانات لا تعليمات**.

### الاختبارات المطلوبة
`company_name` يحوي محارف توجيهية/وسوم → **يُقصّ ويُعقَّم** قبل التخزين · اسم طبيعي («Cardlytics, Inc.») → **يمرّ دون تشويه**.

---

## ⑧ رادار الانطلاق يعمل ~6 ساعات على لقطة قائمة مجمَّدة

- **النوع:** خطأ منطقي (حالة بائتة / سباق)
- **الخطورة:** 🟠 **عالية**
- **الثقة:** ✅ **عالية جدًا** (قرأت `ignition_live.py` كاملًا — 91 سطرًا)
- **الملف والأسطر:** `ignition_live.py:44-45` مقابل `57-61` · `pullback_live.py:61-63` · `.github/workflows/ignition.yml:19` (مجموعة `super-stocks-ignition`) مقابل `daily_screener.yml:4-8` (`super-stocks-state`)

### الدليل — ✅ حقيقة مؤكَّدة
```python
# ignition_live.py:44-45 — تُقرأ مرة واحدة، قبل الحلقة
wl = bot.load_watchlist()
active = [s for s in wl.get("stocks", []) if s.get("status") == "active"]
...
# ignition_live.py:57-61 — ثم تُستعمل ~6 ساعات بلا إعادة قراءة
while bot.dt.datetime.utcnow() < deadline and loops < max_loops:
    rows = bot.scan_ignition(wl, today)          # ← نفس wl البائت
    time.sleep(interval)
```
```python
# pullback_live.py:61-63 — وفي الأثناء: عملية أخرى تُعدّل نفس الملف وتدفعه كل 30 دقيقة
bot.save_watchlist(wl)
bot.git_save([bot.WATCH_FILE])
```
والـworkflowان في **مجموعتَي تزامن مختلفتين** → التداخل **بالتصميم**.

### السيناريو
13:35 يبدأ الرادار بـ12 سهمًا نشطًا.
15:00 يضرب `ACON` ستوبه → `pullback_live` يشطبه (`status="stopped"`) ويدفع الملف.
17:20 يقفز `ACON` بحجم 3× فوق رقمه الحرج → الرادار (الذي ما زال يراه نشطًا في نسخته المجمَّدة) يرسل **«🔥 ادخل الآن»** لسهم **خرج من القائمة قبل ساعتين ونصف بضرب ستوبه**. ودِدوب الجلسة (`seen`) يحجزه فلا يُصحَّح بقية اليوم.

### الأثر
- **Telegram / إشارات:** 🟠 تنبيه دخول عالي الإلحاح في سهم مشطوب — أخطر أنواع التنبيه الخاطئ (يدعوك للتنفيذ فورًا).

### التحقّق (بلا تعديل)
```bash
python3 -c "import json;w=json.load(open('weekly_watchlist.json'));\
print([(s['symbol'],s.get('removed_date')) for s in w['removed']][-10:])"
cat ignition_log.json
```
أي إطلاق لسهم في **نفس يوم** `removed_date` = الخطأ محقَّق حيًّا.
**⚪ اليوم لم يظهر** لأن السجلّ فيه **إطلاق واحد فقط** (GEOS 2026-07-09) — وهذا **حظّ لا حماية**.

### الإصلاح المقترح — 🔸 استنتاج
أعِد `load_watchlist()` داخل الحلقة (قراءة ملف محلي — بلا كلفة شبكة) وأعِد اشتقاق `active` منها، مع إبقاء `seen` عبر الدورات.

### الاختبارات المطلوبة
حقن `load_watchlist` يرجّع قائمة فيها سهم نشط ثم (بعد N دورة) مشطوبًا → **لا إطلاق عليه بعد الشطب**.

---

## ⑨ الهدف لا يشطب السهم — يخنق ترشيح الجديد ويلوّث الإحصاء

- **النوع:** انحراف عقد (الكود ≠ الوثيقة) + سعة القائمة
- **الخطورة:** 🟠 **عالية**
- **الثقة:** ✅ **عالية على الكود** · ⚪ **السلوك المقصود غامض — قرار منتج لك**
- **الملف والأسطر:** `Super_stock.py:5586-5606` · `5628-5630` · `7922` · مقابل `7874-7875` و`CLAUDE.md:31`

### الدليل — ✅ حقيقة مؤكَّدة
```python
# 5594-5599 — الهدف يُسجَّل ولا يشطب
if hi >= _t3_c and s["hit"] != "t3":
    s["hit"], s["hit_date"] = "t3", str(day)

# 5601-5606 — والستوب بعد الهدف يُسجَّل كـ«stopped»
if lo <= _stop_c:
    s["status"] = "stopped"
    s["removal_reason"] = (f"لمس الستوب يوم {day} بعد تحقيق هدف{s['hit'][-1]} (خروج رابح مفترض)")

# 5628-5630 — والشطب لا يعرف إلا «غير active»
newly = [s for s in wl["stocks"] if s["status"] != "active"]

# 7922 — والسعة تُحسب على الأحياء
space = CONFIG["WATCHLIST_SIZE"] - len(wl["stocks"])
```
الإسنادان الوحيدان لـ`s["status"]` في الملف كله هما `"stopped"` (5587 و5602).
بينما docstring السطر 7874 و`CLAUDE.md:31` يقولان: «لا يُحذف منها سهم إلا **بستوب/هدف**».

### السيناريو
أسبوع جيّد: 6 أسهم من 10 حقّقت t2/t3. كلها تبقى `active` وتحتلّ خاناتها → `space = 4` فقط → **البوت يتوقف عن قبول مرشّحين جدد لأن الرابحين لا يغادرون**، حتى تمسح جمعةُ التجديد القائمة.
وأسوأ: الرابح الذي حقّق t3 يظل يُفحَص ضد ستوبه يوميًا؛ فإن ارتدّ ولمسه يُسجَّل في `removed` **كحدث ستوب** → يدخل مقام الخسائر في تقرير التطوير **رغم أنه صفقة رابحة**.

### الأثر
- **إشارات التداول:** 🟠 سعة القائمة تحجب مرشّحين حقيقيين في أفضل الأسابيع.
- **تضليل:** 🟠 رابحون مسجَّلون كأحداث ستوب → نسبة النجاح المعروضة **أقلّ من الحقيقة**.

### التحقّق (بلا تعديل)
```bash
python3 -c "import json;w=json.load(open('weekly_watchlist.json'));\
print('أحياء حقّقوا هدفًا:',[(s['symbol'],s['hit'],s['status']) for s in w['stocks'] if s.get('hit')]);\
print('مشطوبون بعد هدف:',[(s['symbol'],s.get('removal_reason')) for s in w['removed'] if 'بعد تحقيق' in (s.get('removal_reason') or '')])"
```
القائمة الأولى غير فارغة = خانات محجوزة برابحين. الثانية غير فارغة = رابحون مسجَّلون كستوب.

### الإصلاح المقترح — ⚪ **قرار منتج، ليس إصلاحًا تقنيًّا**
خياران، اختر أنت:
- **(أ)** الهدف يُنهي المتابعة: حالة طرفية `"target"` تُعالَج مع `"stopped"` في 5628 وفي `_collect_closed`/`build_wrapup_message`. **⚠️ يغيّر دوران القائمة وبالتالي مَن يُرشَّح — يمسّ السعة.**
- **(ب)** الهدف لا يُنهيها: **صحّح الوثيقة** (`CLAUDE.md:31` + docstring 7874) واستثنِ أصحاب `hit` من حساب `space`.
**لا تنفّذ أيًّا منهما بلا قرارك الصريح.**

### الاختبارات المطلوبة
حسب الخيار: (أ) إصابة t3 → `status="target"` + يُشطب + **لا يُحسب خسارة**. (ب) `space` يتجاهل أصحاب `hit`. وفي الحالتين: قفل «رابح لا يُعدّ خسارة في تقرير التطوير».

---

## ⑩ سجلّ الانطلاق بلا طابع زمني — مقياسان من ثلاثة مستحيلان بنيويًّا

- **النوع:** حلقة قياس مفتوحة (على الحافة الوحيدة المُثبتة)
- **الخطورة:** 🟡 **متوسطة**
- **الثقة:** ✅ **عالية جدًا** (قرأت الدالة **والسجلّ**)
- **الملف والأسطر:** `Super_stock.py:6694-6697` · `ignition_log.json` · `IGNITION_PLAN.md:38-44` · `CONFIG: IGNITION_OUTCOME_MIN=8`

### الدليل — ✅ حقيقة مؤكَّدة
```python
# Super_stock.py:6694-6697 — record_ignition_fires
log_data.append({"symbol": sym, "date": today_iso,
                 "break_level": _ignition_break_level(s),
                 "price": sig.get("price"), "vol_x": sig.get("vol_x"),
                 "usd": usd, "candle_class": _ignition_candle_class(usd)[0]})
```
`date` = اليوم فقط. **لا حقل وقت إطلاقًا.**

ومحتوى `ignition_log.json` بالكامل (قرأته):
```json
[{"symbol":"GEOS","date":"2026-07-09","break_level":7.0,"price":7.21,
  "vol_x":17.4,"usd":47449,"candle_class":"group"}]
```

### شرح المشكلة
`IGNITION_PLAN.md:38-44` سجّل **مسبقًا** ثلاثة مقاييس:
1. **الالتقاط** — يحتاج **مقامًا** (أي أسهم القائمة انفجرت ولم يُطلِق عليها) → **لا يُسجَّل** ❌
2. **الأبكرية** — «وسيط الدقائق قبل المسار اليومي» → يحتاج **وقتًا** → **غير قابل للحساب بنيويًّا** ❌
3. **الإنذار الكاذب** — قابل للحساب ✅ (عبر `_ignition_outcome`)

وواقعيًّا حتى الثالث لا يُحسب: السجلّ فيه **إطلاق واحد**، و`_ignition_log_block` يرفض التقرير تحت `IGNITION_OUTCOME_MIN = 8` → تقرير الجمعة يطبع «⏳ تتراكم» منذ إطلاق الحلقة، وسيظلّ **شهورًا** بهذا المعدّل.

**تفصيلة لافتة — ✅ مؤكَّدة:** الإطلاق الوحيد `usd: 47449` → تصنيفه `"group"` = ما يصفه `CLAUDE.md:543` بـ«**قروب يُصرَّف**» (تحذير هبوط). أي أن العيّنة الوحيدة التي عندك هي من الصنف الذي يحذّر منه نظامك.

### الأثر
🟡 **تضليل استراتيجي:** `MARKET_BACKTEST_FINDINGS §0-ك` هو **النتيجة الإيجابية الوحيدة** من اشتراك Polygon، ويحمل تحفّظًا صريحًا: «نسبة الإنذار الكاذب لم تُقَس تاريخيًّا → **تُقاس حيًّا**». الحلقة التي كان يُفترض أن تُفرِغ هذا التحفّظ **لا تُغلَق كما هي مبنية**. أنت تدفع لـPolygon وتشغّل جوبًا 6 ساعات يوميًّا على وعد قياس **لا يمكن قياسه**.

### التحقّق (بلا تعديل)
```bash
cat ignition_log.json      # → سجلّ واحد، صفر حقول وقت
```

### الإصلاح المقترح — 🔸 استنتاج
- **صغير (S):** أضف `fired_at` (UTC، بالثانية) إلى كل سجلّ → تصير الأبكرية قابلة للقياس. طبقة **إلحاق فقط**، خارج كل الأقفال.
- **متوسط (M):** عند نهاية الجلسة، سجّل **المقام**: أي أسهم نشطة تحرّكت ≥`EXPLOSION_PCT` اليوم ولم يُطلَق عليها → يكتمل «الالتقاط».
- **صدق:** حتى يبلغ العدد 8، اجعل تقرير الجمعة يقول **«العيّنة N — غير كافية»** بالرقم، لا «⏳ تتراكم» المبهمة.

### الاختبارات المطلوبة
`record_ignition_fires` يخزّن `fired_at` · الدِدوب (مرة/سهم/يوم) **يبقى كما هو** · سجلّ قديم بلا `fired_at` **لا يُسقِط** التقرير (توافق خلفي).

---

## ⑪ `dev_assistant.py` — 616 سطرًا ميتة ومنحرفة

- **النوع:** دَين تقني (فخّ لوكيل مستقبلي)
- **الخطورة:** 🟡 **متوسطة**
- **الثقة:** ✅ **عالية جدًا** (grep قاطع)
- **الملف:** `dev_assistant.py` بكامله (616 سطرًا)

### الدليل — ✅ حقيقة مؤكَّدة
`grep -rn "dev_assistant" *.py .github/workflows/*.yml` → **صفر مستورِد · صفر workflow**. المطابقتان الوحيدتان تعليقان في `Super_stock.py:7211,7271` يشيران إلى `dev_assistant_standalone` كمصدر **اقتباس**.

- النسخة الحيّة: `Super_stock.py:7367` — `build_dev_assistant_report(wl, alert_data=None)`
- الشوكة الميتة: `dev_assistant.py:210` — `build_dev_assistant_report(wl, missed=None, ...)` — **توقيع مختلف**

**والانحراف واقع فعلًا:** الشوكة تفتقد كل عمل «المقاييس الصادقة» (`_wilson_ci` · `_median` · `_realized_r` · `_weekly_compare_block` · `_honest_metrics_block`) وتسوية التقسيم §6 — وما زالت تحمل تصنيف **A/B المتقاعد**.

### الأثر
- **تشغيليًّا:** ❌ **لا شيء** (لا يعمل، لا يستورده أحد).
- **الخطر الحقيقي — 🔸 استنتاج:** وكيل مستقبلي (أو مراجِع Cline الأسبوعي) يقرأه كمرجع موثوق و«يُزامنه» مع `Super_stock.py` → **يعيد استيراد منطق A/B المتقاعد إلى الجذور**. هذا بالضبط الفخّ الذي حذّر منه `CLAUDE.md`.

### التحقّق (بلا تعديل)
```bash
grep -rn "dev_assistant" *.py .github/workflows/*.yml | grep -v "^dev_assistant.py"
```

### الإصلاح المقترح — 🔸 استنتاج
`git rm dev_assistant.py` — الملف مستعاد بالكامل من التاريخ متى شئت (`git show f775390:dev_assistant.py`). **بديل أخفّ:** لافتة سطر واحد في رأسه: «⛔ شوكة ميتة ومنحرفة — النسخة الحيّة في `Super_stock.py:7367`. لا تُزامِنها.»

### الاختبارات المطلوبة
لا شيء (صفر مستورِد). فقط: السويّة تبقى 638/638 بعد الحذف.

---

## ⑫ التجديد يؤرشف نتائج غير محسومة عند خنق Yahoo

- **النوع:** خطأ منطقي (حارس ناقص)
- **الخطورة:** 🟡 **متوسطة**
- **الثقة:** ✅ **عالية** (قرأت الكتلة والحارس)
- **الملف والأسطر:** `Super_stock.py:7707-7712` · الحارس في `7726-7742` · `Super_stock.py:5516-5519` · `7719-7721`

### الدليل — ✅ حقيقة مؤكَّدة
```python
# 7707-7712 — تحديث الأسبوع المنتهي
if old_syms and yf is not None:
    try:
        hist_old = download_history(old_syms)
        update_watchlist_status(wl, hist_old)      # ← رموز مفقودة تُتخطّى بصمت
    except Exception as e:
        log(f"⚠️ تحديث الأسبوع المنتهي: {e}")      # ← يسجّل فقط، ثم **يكمل**

# 7726-7731 — الحارس يُحسب من الفرز الجديد، لا من hist_old
_uni = _SCAN_STATS.get("universe") or 0
_val = _SCAN_STATS.get("valid") or 0
_cov = (_val / _uni * 100.0) if _uni else 0.0
if not results or _cov < CONFIG["DATA_HEALTH_MIN_PCT"] or _fallback:
    ...   # تأجيل التجديد
```

### شرح المشكلة
حارس صحّة البيانات **موجود وممتاز** — لكنه يحرس **الفرز الجديد** فقط. لا شيء يحرس `hist_old` (تحديث الأسبوع المنتهي). ففرز جديد صحّي يسمح للتجديد بالمضي **حتى لو فشل تحديث الأسبوع المنتهي كليًّا**.

### السيناريو
جمعة 22:07: `download_history(old_syms)` يُخنَق من ياهو (جزئي/فارغ) → `update_watchlist_status` يتخطّى الرموز المفقودة بصمت. الفرز الجديد صحّي (تغطية 95%) → الحارس يمرّر. أسهم **ضربت الستوب خلال الأسبوع** تُؤرشَف بـ`status: active` و`removal_reason: None` وأسعار الأسبوع الماضي.

### الأثر
- **تضليل:** 🟡 تلوّث `history` (أرشيف التعلّم) ومقامات الربح/الخسارة في تقرير التطوير.
- **إشارات:** 🟡 تفلت من `EXCLUDE_STOPPED_FROM_RENEWAL` (7719) → **يمكن إعادة ترشيحها في نفس المساء**.

### التحقّق (بلا تعديل)
🔸 يحتاج حقن `download_history` يرجّع `{}` في مسار التجديد → ستُلاحظ أن الحصاد يُرسَل والأرشفة تمضي والحارس لا يمنعها. (لم أستطع تشغيله — §6.)

### الإصلاح المقترح — 🔸 استنتاج
طبّق **نفس** اختبار التغطية على `hist_old`: `len(hist_old) / len(old_syms)` مقابل `DATA_HEALTH_MIN_PCT`؛ ودونها خذ **فرع التأجيل الموجود أصلًا** بدل الأرشفة.

### الاختبارات المطلوبة
`hist_old` فارغ/جزئي → **التجديد يُؤجَّل** ولا يُؤرشَف شيء · تغطية كافية → السلوك دون تغيير.

---

## ⑬ `git_save`: تعارض rebase = حلقة ميتة + فقد حالة **صامت**

- **النوع:** خطأ منطقي (مسار فشل) + فقد بيانات
- **الخطورة:** 🟠 **عالية**
- **الثقة:** ✅ **عالية على الكود** · ⚪ **متوسطة على التواتر** (لا أرى سجلّ Actions)
- **الملف والأسطر:** `Super_stock.py:9092-9106` · النداءات: `Super_stock.py:9354` · `pullback_live.py:63` · `ignition_live.py:83`

### الدليل — ✅ حقيقة مؤكَّدة
```python
# Super_stock.py:9092-9106
for attempt in range(4):
    os.system("git fetch origin main >/dev/null 2>&1")
    if os.system("git rebase FETCH_HEAD >/dev/null 2>&1") != 0:
        os.system("git rebase --abort >/dev/null 2>&1")    # ← الحالة رجعت كما كانت
    if os.system("git push origin HEAD:main") == 0:         # ← non-fast-forward حتمًا
        pushed = True
        break
    wait = 2 ** (attempt + 1)
    time.sleep(wait)
if pushed:
    log("✅ حُفظت بيانات التتبع في الـ repo")
else:
    log("⛔ git push فشل نهائيًا — البيانات محفوظة محليًا فقط. ...")   # ← لوق فقط، بلا تلغرام
```

### شرح المشكلة
عند تعارض rebase: `--abort` يعيد الحالة **كما كانت تمامًا**، فالدفع مرفوض حتمًا. والمحاولات الأربع **بمدخلات متطابقة** → فشل مؤكَّد (~30 ثانية) ثم استسلام. والفشل النهائي يُبلَّغ **باللوق فقط، بلا تلغرام** — خلافًا لكل مسارات الفشل الأخرى في الملف.

**والرنر مؤقّت** → ما لم يُدفَع، **ضاع**.

### السيناريو
تشغيلة pullback ترصد ستوبًا وتكتب `status="stopped"` + طوابع دِدوب `live_alert`. يتعارض الدفع (مثلًا مع دفعة `ignition.yml` — **وهي خارج مجموعة `super-stocks-state`**). الأربع محاولات تفشل. **الجوب أخضر.** الحالة تتبخّر مع الرنر.
التشغيلة التالية تقرأ الملف القديم: السهم المشطوب **يعود نشطًا**، وطوابع الدِدوب اختفت → **تُعاد إطلاق تنبيهات أُطلقت أصلًا**، والستوب المرصود ضاع.

### الأثر
- **Telegram:** 🟠 تنبيهات مكرّرة.
- **استقرار/تضليل:** 🟠 فقد حالة صامت مع **جوب أخضر**.

### التحقّق (بلا تعديل)
⚪ يحتاج سجلّ Actions (لا أراه). ابحث في مخرجات آخر أسبوعين عن السلسلة:
```
⛔ git push فشل نهائيًا
```
كل ظهور = تشغيلة ضاعت حالتها بصمت. وقارِن عدد كوميتات البيانات:
```bash
git log --oneline --since="2 weeks ago" -- weekly_watchlist.json | wc -l
```

### الإصلاح المقترح — 🔸 استنتاج
`subprocess.run(..., check=False)` بدل `os.system` وافحص `returncode`. **وعند تعارض rebase:** أعِد قراءة الـJSON من `origin/main` الطازج، أعِد تطبيق دلتا الذاكرة، ثم أعِد الكوميت — بدل إعادة نفس الـrebase الفاشل. **وعند الفشل النهائي: `send_telegram`** فلا تضيع تشغيلة بصمت أبدًا.
**وأدرِج `ignition.yml` في `super-stocks-state`** أو اجعله لا يدفع إطلاقًا.

### الاختبارات المطلوبة
حقن `os.system`/`subprocess` بأكواد خروج مُصطنعة: (أ) 4 محاولات ثم تلغرام عند الفشل النهائي · (ب) تعارض rebase → **إعادة قراءة ودمج**، لا حلقة ميتة · (ج) لا شيء مُدرَج → لا كوميت.

---

## ⑭ صغائر مؤكَّدة (كلها جهد S)

كلها ✅ **مؤكَّدة بقراءة الكود**.

| # | البند | الملف:السطر | المشكلة والأثر | الإصلاح المقترح |
|---|---|---|---|---|
| **14أ** | `esc()` لا يهرّب `"` | `Super_stock.py:3509-3512`، مستعملة داخل خاصية HTML في `:3743` | `esc()` يهرّب `&`/`<`/`>` فقط. مستعملة في `href="{esc(link)}"` والرابط من ياهو. اقتباس في الرابط يكسر الخاصية → تلغرام يرفض بـ**400** و`send_telegram` **يسجّل ويمضي** (`:4512`) = **تنبيه تداول لا يصل**. ونفس الخطر: `e['note']` **بلا تهريب إطلاقًا** في `:2764` و`:2782`. **⚪ المحفِّز نادر** (اقتباس في رابط) لكنه حقيقي | أضف `.replace('"', "&quot;")` · مرّر `e['note']` عبر `esc()` · **واقصص قبل التهريب لا بعده** (`:3737` يقصّ بعد التهريب فيشطر كيانًا مثل `&am`) |
| **14ب** | «لا مرشّحين اليوم» عند العطل التام | `technical_report.py:844-854`, `949-951` | فشل جلب الكون أو التحميل → `return []` → `render_scan([])` → رسالة **مطمئنة**: «لا توجد أسهم تطابق الشروط اليوم» + خروج **0** (workflow أخضر). **لا تفرّق بين «السوق فاضي» و«المسح لم يعمل»** | أرجِع قيمة مميِّزة عند الفشل وأرسل «⚠️ تعذّر المسح» + اطبع `checked/len(universe)` في رسالة النتيجة الفارغة |
| **14ج** | `SEC_CONTACT` وهمي | `Super_stock.py:429-430` — ولا workflow يضبطه | كل طلبات SEC تخرج بـUA فيه `contact@example.com`. سياسة SEC للوصول العادل قد تُبطئ أو **تحجب** | اضبط `SEC_CONTACT` في الـworkflows ببريد حقيقي |
| **14د** | `hand_check` يقول «ليس ارتكازًا» عند **انهيار** التحليل | `hand_check.py:199-213` (`except Exception: pass` بلا لوق) | استثناء داخل `analyze_ticker` يُعرَض للمستخدم كـ**حكم سلبي واثق** على السهم. `analyze_one.py:565` يعالجها صحيحًا (`bot.log(...)`) — **المساران انحرفا** | سجّل الاستثناء واضبط `r["analysis_error"]` → «⚠️ تعذّر تقييمه» بدل ❌ |
| **14هـ** | `ignition_verify` تايم‌آوت 60د لجوب يحمّل الكون كاملًا | `.github/workflows/ignition_verify.yml:19` مقابل `backtest.yml:58` (300د) و`acc_verify.yml:20` (340د) | يُقتَل قبل إرسال الحكم (`ignition_verify.py:146` آخر سطر) → سؤال «هل أفادنا اشتراك Polygon؟» **لا يُجاب أبدًا** | ارفع إلى ≥300د |
| **14و** | مقام الالتقاط ملوَّث بإخفاقات البيانات | `ignition_verify.py:35-43`, `114-126` | `_day_minutes` يرجّع `None` عند 429/عدم استحقاق **بلا عدّاد**، لكن `rate = caught / len(exps)`. تشغيلة مخنوقة تُنتج **حكمًا سلبيًّا كاذبًا على حافتك**، لا يتمايز عن حكم حقيقي | عُدّ `fetched` منفصلًا واستعمله مقامًا · أوقِف الحكم تحت أرضية جلب |
| **14ز** | `acc_verify` يَعِد باستئناف CSV ولا يستعيده | `acc_verify.yml` (لا خطوة `download-artifact`) · `acc_verify.py:94` | كل إعادة تشغيل تبدأ من **الصفر** في جوب 340 دقيقة → التجربة **لا يمكن أن تكتمل** لو تجاوزت النافذة مرة | أضف `download-artifact` (بـ`continue-on-error`) قبل خطوة التشغيل |
| **14ح** | `technical.yml` بلا `ALPHAVANTAGE_KEY` | `.github/workflows/technical.yml:28-31` مقابل `scan_earnings.yml:28` | التقرير الفني اليدوي يتدهور لمسار أرباح ياهو فقط **بلا إشارة**، والسرّ موجود أصلًا في الريبو. و`FMP_API_KEY`/`FINNHUB_API_KEY` (`technical_report.py:124,141`) **كود ميت** — لا workflow يضبطهما | أضف السرّ · واحذف فرعَي FMP/Finnhub أو صِلهما |

---

# 5) ملاحظات إضافية خارج جدول النتائج

## 5-أ) ⛔ ادّعاء أُسقط في التحقّق (مهم — لا تُعِد فتحه)
**«`fintel_short` عنق أداء يكلّف حتى 88 دقيقة»** — **مرفوض.**
مدقّق الأداء افترض ~200 مرشّح يصلون `apply_short_gate`. الأرقام **المخزَّنة في `weekly_watchlist.json`** تكذّبه — ✅ حقيقة مؤكَّدة (حسبتها بنفسي):

| القياس | القيمة (لقطة 2026-07-11) |
|---|---|
| الكون | 3,562 |
| الصالح | 3,364 |
| مجموع رموز الرفض | **3,359** |
| **المرشّحون الواصلون للبوابة** | **≈ 5** |

الكلفة الحقيقية ≈ **8 ثوانٍ**، لا 88 دقيقة. **درس:** أرقام الوكلاء **قرائن لا حقائق** — كل بند في هذا التقرير فُتح وقُرئ بنفسي قبل إدراجه.

**🔸 استنتاج:** عنق الأداء الحقيقي (إن وُجد) هو `download_history` — 3,500 رمز، 14 دفعة **تسلسلية**، `CHUNK_SLEEP=2.0`، وإعادة تحميل أبدية للرموز التي تحت `MIN_BARS` (تُعدّ «مفقودة» كل تشغيلة، للأبد). **⚪ لم أقِس زمن التشغيل الفعلي** — يحتاج سجلّ Actions.

## 5-ب) قرارات معمارية: **لا تفعلها** (فحصتُها ورفضتها)
| المقترح | لماذا **لا** |
|---|---|
| **تقسيم `Super_stock.py`** | المقطع النظيف الوحيد (المؤشرات، `447-620`) = **1.9%** من الملف. والمقاطع المجدية (الجالبات ~1105 سطر · التنسيق ~1097) تكسر **73 موقع monkeypatch** في `test_bot.py` **بصمت**: نمط `S.f = fake` يتوقف عن العمل لو انتقل النادي والمنادى معًا → **سويّة خضراء لم تعد تعزل الشبكة** = أسوأ من الدَّين نفسه. **الملف كبير لكنه مسطّح ومقسَّم بلافتات — ليس مرضًا.** |
| **الهجرة لـpytest** | ~638 فحصًا تحتاج إعادة كتابة + fixtures لكل حالة عامة. السويّة **تخرج 1 عند الفشل أصلًا** = صالحة كبوابة كما هي. أنفِق الجهد على ② و④ لا على الإطار. |
| **إخراج بيانات البوت من git** | `size-pack: 2.12 MiB` · 75 كوميت بيانات في 60 يومًا. **سليم تمامًا**، وgit يعطيك أثرًا تدقيقيًّا كاملًا مجانًا. |
| **حذف الكود «المحفوظ»** | ~50 سطرًا (`key_levels_block` · `h4_levels_block` · `position_size_line`)، **كلها موثّقة في `CLAUDE.md` بقرار صريح**. مخزون، لا عفن. |
| **دمج `technical_report.py`** | ليس تكرارًا — **مرآة**: `Super_stock` يصطاد أنماطًا **صاعدة** للشراء، و`technical_report` أنماطًا **هابطة** لمن يملك السهم. دمجهما يضع كود أداة استشارية داخل نطاق انفجار محرّك الفرز. |
| **إعادة كتابة `CLAUDE.md`** | 579 سطرًا مؤرَّخة ومربوطة بـPRs. **نجا من كل فحوص التناقض** التي أجريتها. **أسلم ملف في الريبو.** |

## 5-ج) فجوات توثيق مؤكَّدة (خارج الـ14)
- ✅ **`PENDING_VERIFICATION.md:3` يقول «لا بنود معلّقة»** بينما `ai_debate/round2` يؤكّد أن **F-03…F-07 ما زالت مفتوحة** على أحدث SHA (F-01 و F-02 فقط أُصلحتا). **الملف الوحيد المكلَّف بتتبّع المفتوح يقول إن كل شيء نظيف.** ونتيجتي ⑤ هنا = F-03 نفسها.
- ✅ `CLAUDE.md:14` يذكر دالّتين **غير موجودتين**: `monitor_sweeps` و`build_sweep_alert`. الحقيقيتان: `monitor_live_events` (`Super_stock.py:5997`) و`build_live_alert` (`:6365`).
- ✅ `POLYGON_EDGE_PLAN.md:3` ما زال يقول «**جاهزة للتنفيذ**» رغم أن أجزاءها الثلاثة **شُحنت** والجزء «أ» **تقاعد** (`CLAUDE.md:493`). 🔸 **خطر:** وكيل يقرأه سيعيد وصل `silent_accumulation` بـ`enrich` — أي يعيد إحياء إشارة **سقطت في اختبارها المسجَّل مسبقًا**.
- 🔸 **12 من 15** ملف خطة يبدو **منجزًا** بلا أي علامة إتمام → خطة مشحونة تُقرأ كخطة مفتوحة.
- ✅ `README.md` = **19 بايت**.

## 5-د) اتجاه (خيارات — لا أخطاء)
- **`sweep_verify.py`:** أثر «المسح ثم الاستعادة» **موجب في السنتين** (`MARKET_BACKTEST_FINDINGS §0-و`) ورُفض *فقط* كبوابة دخول **لندرته على الشمعة اليومية**. 🔸 والندرة قد تكون **أثر دقّة قياس** لا حقيقة — التنبيه حيّ الآن بدقّة **الدقيقة** وبلا أي قياس. يعيد استخدام `ignition_verify` + نمط استئناف `acc_verify`. **يستوجب تسجيلًا مسبقًا للمعيار** (كعادة البيت) لأن رغبتك في نجاحه عالية.
- **تكاليف التنفيذ في الباكتيست:** كل أحكامك صادرة من محرّك **صفر سبريد** على أسهم سبريدها ~21% موثّق بصورة فيصل نفسها. **يرفع جودة كل قرار قادم، ولا يغيّر أي سلوك إنتاجي.**
- **مراجِع Cline:** ✅ `reports/` فيه **ملفان فقط** (آخرهما 2026-07-01) رغم كرون **كل جمعة** → جمعتان بلا تقرير، **وهو يستهلك رصيدًا**. ⚪ إمّا يفشل صامتًا أو PRs لا تُدمَج — **لم أستطع التمييز** (لا أرى سجلّ Actions). **الغموض نفسه هو الخلل.**

---

# 6) حالة الاختبارات و git أثناء التدقيق

## نتيجة تشغيل السويّة — ⚪ **تعذّر**
```
$ python3 test_bot.py
ModuleNotFoundError: No module named 'numpy'
EXIT=1
```
**السبب:** بايثون النظام = **3.9.6**، و`pip3 list` يُظهر **صفر** من اعتماديات المشروع (لا numpy · لا pandas · لا yfinance).
**لذلك: لم أستطع تشغيل السويّة، ولا أعرف إن كانت خضراء الآن.** كل نتائج هذا التقرير **من قراءة الكود، لا من تشغيله.**
🔸 **هذا نفسه ملاحظة:** حلقة التغذية الراجعة الوحيدة للمشروع هي **تشغيلة Actions إنتاجية** — لا يوجد مسار تحقّق محلي.

## حقائق مقيسة عن السويّة — ✅ مؤكَّدة
| القياس | القيمة |
|---|---|
| أسطر `test_bot.py` | **3,989** |
| عدد فحوص `check(` | **638** |
| سلوك الفشل | `raise SystemExit(1)` (`:3987`) → **صالح كبوابة CI** |
| مَن يشغّلها آليًّا | **لا أحد** (فقط `cline_weekly_review.yml`، كرون الجمعة) |

## حالة git أثناء التدقيق — ✅ مؤكَّدة
```
$ git status --short
?? .claude/skills/          ← سابق لجلستي (ملفات مهارة /improve نفسها)

$ git diff --name-status            → (فارغ)
$ git diff --name-status --staged   → (فارغ)

$ git rev-parse --short HEAD
8f9362a                     ← لم يتغيّر طوال الجلسة
```

**الملفات التي أنشأتُها أو عدّلتُها داخل الريبو أثناء التدقيق: لا شيء.**
الملف الوحيد المكتوب في النظام كان **خارج الريبو**: `/private/tmp/.../scratchpad/tests.log` (ناتج محاولة تشغيل السويّة الفاشلة).
**هذا الملف (`IMPROVE_DEEP_AUDIT_REPORT.md`) هو أول وآخر ما أضفته للريبو، وبطلبك الصريح.**

---

# 7) الترتيب النهائي — من الأخطر إلى الأقلّ

| المرتبة | # | المشكلة | الخطورة | الثقة | المعيار المتأثّر | الجهد |
|---|---|---|---|---|---|---|
| **1** | ① | جلسة الترشيح الأولى لا تُقيَّم أبدًا | 🔴 حرجة | ✅ عالية جدًا | إشارات + تضليل | S |
| **2** | ⑤ | نوافذ السوق مثبَّتة على EDT (تكتم التنبيه الحقيقي) | 🔴 حرجة (نوفمبر) | ✅ عالية جدًا | Telegram + إشارات | S–M |
| **3** | ② | لا بوابة CI تشغّل الـ638 اختبارًا | 🟠 عالية | ✅ عالية جدًا | **تُمكّن كل ما سبق** | S |
| **4** | ⑧ | الرادار يطلق «ادخل الآن» من قائمة مجمَّدة | 🟠 عالية | ✅ عالية جدًا | Telegram + إشارات | S |
| **5** | ⑨ | الهدف لا يشطب السهم (سعة + إحصاء) | 🟠 عالية | ✅ عالية (قرار منتج) | إشارات + تضليل | S |
| **6** | ⑬ | `git_save`: حلقة ميتة + فقد حالة صامت | 🟠 عالية | ✅ كود · ⚪ تواتر | Telegram + استقرار | S–M |
| **7** | ③ | فحوص تتحوّل بصمت إلى لا-عمليات | 🟠 عالية | ✅ عالية | سلامة زائفة | S |
| **8** | ④ | صفر اختبار رفض للبوابات M1–M5/M10 | 🟠 عالية | ✅ عالية | إشارات + باكتيست | M |
| **9** | ⑫ | التجديد يؤرشف نتائج غير محسومة عند الخنق | 🟡 متوسطة | ✅ عالية | تضليل + إشارات | S |
| **10** | ⑩ | سجلّ الانطلاق بلا وقت (حلقة قياس مفتوحة) | 🟡 متوسطة | ✅ عالية جدًا | تضليل استراتيجي | S–M |
| **11** | ⑥ | التجديد يمسح `reject_stats` | 🟡 متوسطة | ✅ عالية جدًا | تعلّم + لغم بنيوي | S |
| **12** | ⑦ | `company_name` غير معقّم → وكيل Cline | 🟡 متوسطة | ✅ مسار · ⚪ استغلال | أمان | S |
| **13** | ⑪ | `dev_assistant.py` — 616 سطرًا ميتة | 🟡 متوسطة | ✅ عالية جدًا | فخّ وكيل | S |
| **14** | ⑭ | الصغائر الثماني (14أ–14ح) | 🟢–🟡 | ✅ عالية | متفرّق | S |

## 🔗 الاعتماديات (اقرأها قبل أي تنفيذ)
```
②  (بوابة CI)  ─┬─► كل إصلاح آخر   ← بلا بوابة، أي إصلاح غير مُتحقَّق منه
                 │
③  (تحصين الفحوص) ──► ④ (اختبارات البوابات)   ← لا تُضِف اختبارات لسويّة تكذب على نفسها
                 │
⑬  (git_save) ──► إدراج ignition.yml في مجموعة الحالة
```
- **② أولًا، دائمًا.** إصلاح ① أو ⑤ بلا بوابة = تغيير غير مُتحقَّق منه في مسار مالي.
- **③ قبل ④.**
- **⑨ يحتاج قرارك** (أ أم ب) قبل أي كود.
- **① يستوجب رفع `LOGIC_VERSION` + ترحيلًا** — وحده بين الأربعة عشر.

---

## ⚠️ إخلاء مسؤولية أخير
- **لم يُنفَّذ أي إصلاح.** كل «الإصلاح المقترح» أعلاه **اقتراح غير مُنفَّذ وغير مُختبَر**.
- كل النتائج **من قراءة الكود**، لا من تشغيله (تعذّر التشغيل — §6).
- **لم أرَ سجلّ GitHub Actions** — لذلك التواتر الفعلي لـ⑬، وزمن تشغيل الفرز، ومصير مراجِع Cline: **كلها ⚪ غير محقَّقة**.
- **أهمّ مكان لم يُدقَّق:** `backtest_symbol` بحثًا عن **نظرٍ مستقبلي** (look-ahead). كل عتبات البوت «مقفولة بالباكتيست» — فلو كان المحرّك نفسه معيبًا، فكل حكم مبنيّ عليه محلّ شكّ. **هذا هو التدقيق التالي الذي أنصح به.**
