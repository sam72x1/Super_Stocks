# `T-PIVOT-BOTTOM-2` — «قس الوقف من جديد» (تسجيلٌ مسبقٌ **قبل أيّ رقم**)

> **أمرُ المالك (2026-08-29):** «قس الوقف من جديد» — بعد أن أبلغتُه أن
> ‏+0.0644R **حدٌّ أعلى لِما شُحن لا وصفٌ له**.
> **وأمرُه «قس» لا «اعتمد»** ⇒ سقفُ النجاح **وصفٌ وتوصية**، والوقفُ المشحونُ
> يبقى حتى كلمةٍ منه (‏§⑧).

---

## ⓪ لماذا تسجيلٌ جديدٌ لا إعادةُ تشغيل

`T-PIVOT-BOTTOM` عبرت بوّاباتِها وصدر حكمُها («لا تُوصى» ‏3 من 4). **وإعادةُ
تشغيلها بتعريفِ ذراعٍ مختلفٍ بعد رؤية أرقامها = تحريكُ هدف.** فالسابقةُ
المدوَّنة (`T-HOLD-KEY` ⟶ `T-HOLD-KEY-2`) تُلزم **عقدًا جديدًا**:
- **حكمُ السابقة لا يُمَسّ** ويبقى منشورًا كما صدر.
- **والبارُ ‏+0.15R مُعادٌ حرفيًّا** ولا يُحرَّك.
- **والجديدُ سؤالٌ مختلف**: لا «هل يُوصى بوقف القاع؟» بل **«ما الرقمُ الصادق
  لِما يعمل عند المالك الآن؟»**

---

## ① العيبُ المُصلَح — مذكورٌ بحرفه

`r["pivot"]` **ليس مِرساةَ الدخول**:
- `pivot = min(lows[-PIVOT_LOOKBACK:])` بنافذة **‏25** (‏`pivot_stability`).
- ومِرساةُ الدفعات والوقف `_anchor = tested_level(df)["level"]` بنافذة **‏30**.
⇒ **‏`tested ≤ pivot` بالبناء** (أدنى ثلاثين ≤ أدنى خمسٍ وعشرين).

**والعقدُ السابق `§①` نصَّ «الوقفُ عند مِرساة الدخول نفسِها» — وأداتُه نفّذت
`r["pivot"]`** (صنفُ «المفتاح المتخيَّل» · `wire-check §①`).
🔒 **والمشحونُ حيًّا هو الصحيحُ بالعقد:** `stop = round(_anchor, 2)` **=
`tranches[0]` بالضبط** (مقفولٌ في الإنتاج بمساواةٍ تامّة).

---

## ② السؤال

> بالدفعات نفسِها والشموع نفسِها والتعبئة نفسِها، ما أثرُ **الوقفِ المشحون
> فعلًا** (‏= أدنى دفعةٍ = المِرساة) مقابل **‏7% تحت المِرساة** — بوحدةِ
> مخاطرةٍ **واحدة**؟ **وكم يبعد عن الرقم المنشور؟**

---

## ③ الأذرعُ ثلاثٌ ولا رابعة

| الذراع | الوقف | الدور |
|---|---|---|
| **`C0`** | `stop[0]` بالعلم **مُطفأً** = ‏7% تحت المِرساة | المرجع — **ويجب أن يُعيد `B0` بت-بت** |
| **`C1`** 🥇 | **`tranches[0]`** = `round(_anchor,2)` = **المشحونُ حيًّا** | **الحاكمة** |
| **`C2`** | `r["pivot"]` (وقفُ الأداة السابقة) | وصفيّة — **ويجب أن تُعيد `B1` بت-بت** |

⚖️ **ولماذا `C2` باقية:** ليست ذراعًا تُقترَح — هي **بوّابةُ صلاحيةٍ حيّة**:
إعادتُها لأرقام `B1` المنشورة تُثبت أن **المحرّك هو هو** وأن المتغيّرَ الوحيد
**تعريفُ الوقف** ⇒ الفرقُ `C1 − C2` **يُحجّم الانحراف بالرقم** بدل الوصف.

🔒 **والدخولُ واحدٌ في الثلاث** (متوسّطُ دفعات الإنتاج) ⇒ **فهرسُ التعبئة
متطابق** ⇒ المقارنةُ مزدوجةٌ نظيفة، وصفوفُ `no_fill` **هي هي** في الثلاث.

---

## ④ المقياس — **منقولٌ حرفيًّا** من العقد السابق بلا تغيير

> **`R₀ = entry − stop(C0)`** — وحدةُ مخاطرةِ الإنتاج **القديم**، تُقسَم عليها
> الأذرعُ الثلاث (‏= `r0_of` في الأداة السابقة حرفيًّا).

- **المقياسُ الحاكم:** متوسّطُ `R₀` لكلّ صفقةٍ محسومة.
- **و`no_fill` يُستبعَد** — **بالضبط كما استُبعد في المنشور** (وهو محايدٌ هنا
  لأن الصفوفَ متطابقةٌ عبر الأذرع الثلاث).
- **المساند (يُنشَر ولا يحكم):** نسبةُ الربح · العائدُ المحقَّق ٪ · عددُ
  المستبعَدين بـ«الوقفُ فوق الدخول» لكلّ ذراع.

🔴 **ومقامان مُعلَنان قبل أيّ رقم — لئلّا يُخلطا:**
1. **مقامُ البوّابتين `RV0`/`RV1`** = **الصفوفُ التي قاستها الأداةُ السابقة
   حرفيًّا** (‏`pivot` حاضرٌ **و**`entry > pivot`) ⇒ الإعادةُ بت-بت ممكنة.
2. **ومقامُ الحكم `C1 − C0`** = **الصفوفُ كلُّها** (كلُّ صفٍّ له خطّة) —
   لأن قصرَه على مقام (1) **يفلتر بشرطٍ خاصٍّ بـ`C2`** فيلوّث الحكم.
   و`stop(C1) < entry` **بالبناء** فلا يُقصى منه شيء.
⇒ يُطبَع **`C0` بالمقامَين** صراحةً (‏`C0@use` للبوّابة · `C0` للحكم) فلا
يُقرأ رقمٌ في غير مقامه.

**تُوصف `C1` «ذاتَ أثرٍ يبلغ البار» إن استوفت الأربعةَ معًا:**
1. `C1 − C0` بـ`R₀` يبلغ **‏+0.15** فأكثر مجمَّعًا (**مُعادٌ حرفيًّا** —
   ولا يُحرَّك مهما قارَبه).
2. **موجبٌ في السنوات الثلاث** بلا انقلاب.
3. الفاصلُ 95% للفرق المجمَّع **لا يلمس الصفر** (بوتستراب عنقودُه **الرمز**).
4. **الأرضية:** ‏100 صفقةٍ محسومةٍ فأكثر في كلّ سنة (‏مُعادٌ من `T-EXIT`).

---

## ⑤ بوّاباتُ الصلاحية — **منفَّذةٌ في الكود** لا موصوفة

| البوّابة | الشرط | عند السقوط |
|---|---|---|
| **`RV0`** | `C0` يُعيد **`B0` المنشور** (‏−0.2010 · −0.1243 · −0.0912) ضمن ‏1e-3 | **خروج 3** |
| **`RV1`** | `C2` يُعيد **`B1` المنشور** (‏−0.0906 · −0.0621 · −0.0715) ضمن ‏1e-3 | **خروج 3** |
| **`RV2`** | `C1` **يتفرّق** عن `C0` **وعن `C2`** (وإلّا `no-op`) | **خروج 4** |
| **`RV3`** | `stop(C1) == tranches[0]` **مساواةً تامّة** في ‏100% من الصفوف | **خروج 3** |
| **`RV4`** | **`stop(C1) ≤ stop(C2)` في ‏100%** — اختبارُ دعوى «`tested ≤ pivot`» | **خروج 3** ويُعلَن |
| **`RV5`** | `pivot` حاضرٌ في ‏95% فأكثر | **خروج 3** |
| **`RV6`** | قراءةٌ فقط (بالـAST) · والإنتاجُ لا يستوردها | **خروج 3** |
| **`RV7`** | إعادةُ حسم `C0` تُطابق `outcome`/`ret_a` المخزَّنَين **بت-بت** | **خروج 3** |

🔴 **و`RV4` تُختبَر لا تُفترَض:** هي **دعوايَ أنا** في ملحق `§⑨`، وسقوطُها
يعني أن استدلالي البنيويَّ خاطئ ⇒ **يُنشَر مكذَّبًا لا يُطوى.**

---

## ⑥ التنبّؤات (قابلةٌ للكسر — وأوّلُها يراهن على أن انحرافي كان **صغيرًا**)

- **`RP1`** (الأثقل): أرجّح `C1 − C0` في **‏[+0.03 · +0.0644]** — أي **موجبٌ
  وأدنى من المنشور**. ⇒ **إن تجاوز ‏+0.0644 فاستدلالي في `§⑨` مكذَّبٌ في
  اتّجاهه**، وإن نزل تحت الصفر فالمشحونُ أسوأُ من القديم وأُبلغ فورًا.
- **`RP2`**: `pivot ≠ _anchor` في **أقلَّ من 25%** من الصفقات.
- **`RP3`**: `C1` **يبقى دون البار ‏+0.15** ⇒ قرارُ المالك يبقى **قرارًا** لا
  استنتاجَ قياس.
- **`RP4`**: نسبةُ ربح `C1` **بين** ‏≈29% (`C0`) و≈11% (`C2`) وأقربُ إلى `C2`.
- **`RP5`**: `RV4` تصمد ‏100% (‏`tested ≤ pivot` بنيويّ).
- **`RP6`**: المستبعَدون بـ«الوقفُ فوق الدخول» = **صفرٌ في `C1`** (بالبناء)
  مقابل ‏2.45-2.80% في `C2` ⇒ الشحنُ **يُلغي صنفَ استبعادٍ كاملًا**.

---

## ⑦ حدودُ صدقٍ مكتوبةٌ قبل أيّ رقم

1. 🔴 **الأذرعُ الثلاثُ سالبةٌ متوقَّعًا** — المقيسُ **حجمُ النزيف** لا ربحيّة.
   ولا يُقرأ أيُّ رقمٍ هنا وعدَ عائد.
2. الحسمُ **بلمسةِ ذيلٍ بلا انزلاق** ⇒ الأوقافُ الأضيقُ **متفائلةٌ أكثر**
   (تُلمَس أكثر والانزلاقُ عندها أرجح) ⇒ **الفرقُ الحقيقيُّ أصغرُ ممّا يُظهره
   الجدول** — وهذا يشدّ الحكمَ نحو التحفّظ لا ضدَّه.
3. **الوقفُ المشحون داخل منطقة حصاد السيولة بالتعريف** (‏7-13% بلفظ فيصل).
4. `t1` والدفعاتُ والعضويّةُ **لا تتغيّر** ⇒ المقيسُ **الوقفُ وحده**
   (‏`rr` يبقى على الأساس القديم في الإنتاج — مقفولٌ `PBS2`).
5. انحيازُ البقاء مخفَّفٌ باللقطة **وغيرُ منفيّ** · تشويهُ التقسيمات قائم ·
   وعمى الجلسة الممتدّة قائم (`T-AH`).
6. **والدائريّةُ منتفية** (الوقفُ ليس مشتقًّا من نتيجة).
7. 🔴 **وأداةُ `pivot_stop_arms.py` لا تُمَسّ بحرف** (سابقةُ `CAP15`): أرقامُها
   منشورةٌ وقابلةٌ للإعادة، والأداةُ الجديدة **ملفٌّ مستقلّ**.

---

## ⑧ سقفُ النجاح — مثبَّتٌ سلفًا

**أمرُه «قس» ⇒ المُخرَجُ رقمٌ صادقٌ وتوصيةٌ مكتوبة**، و`pivot_stop_result.md`
يُذيَّل ببانرٍ ثانٍ يحمل **الرقم المصحَّح** بدل «حدٍّ أعلى».
- **إن كان `C1 − C0` موجبًا ودون البار** ⇒ الوصفُ «أثرٌ حقيقيٌّ صغيرٌ دون
  البار» و**الوقفُ المشحون يبقى** (إرجاعُه يزيد الخسارة).
- 🔴 **وإن كان صفرًا أو سالبًا** ⇒ **أُبلغ فورًا وأعرض الإرجاع** — فالشحنُ
  حينها بلا سندٍ مقيس.

⛔ **وممنوعٌ مهما كانت الأرقام:** مسُّ `M1`-`M14` أو الأهداف أو الدفعات أو
`ENTRY_*` أو أوقافِ الأنظمة الأربعة الأخرى · أو تغييرُ الوقف المشحون بلا
كلمةٍ منه · أو تحريكُ البار · أو إضافةُ ذراعٍ رابعة.

---

## ⑩ ملحقٌ مؤرَّخ (2026-08-29) — 🔴 **`RV4` كُذِّبت · واستدلالُ §① خاطئ**

**الوقائع:** التشغيلاتُ الثلاث (`33271123537` · `33271127069` · `33271131013`)
على `59698abe` خرجت **بخروج 3** على `RV4` وحدَها. سطرُ 2024 حرفيًّا:

```
🔒 `RV7`: 1591 من 1591 مطابقةٌ بت-بت لمحرّك الإنتاج
🔒 `RV5`: `pivot` حاضرٌ في 100.0%
🔒 `RV3`: `C1` = `tranches[0]` بالبناء · صفوفٌ قابلةٌ للقياس 1591 من 1591
📏 `RP6`: «الوقفُ فوق الدخول» — `C1` 0 (0.00%) · `C2` 39 (2.45%)
🔒 `RV4`: `stop(C1) ≤ stop(C2)` في 1446 من 1591 · مخالفات 145
📏 `RP2`: `pivot` يخالف المِرساة في 549 من 1591 (34.5%) · وسيطُ الفجوة 0.18%
⛔ `RV4` مكذَّبة — ABLV/2024-09-26: tr0=0.84 pivot=0.8351
```

⇒ **دعواي في §① «`tested ≤ pivot` بالبناء» باطلةٌ بالرقم: ‏145 من 1591 (‏9.1%)
وقفُ المشحون فيها **فوق** وقف `B1` المنشور** ⇒ **الانحرافُ ذو اتجاهين لا اتجاهٍ
واحد**، وكلُّ استدلالٍ بنيويٍّ بنيتُه عليه يسقط معه.

### التعديل — `RV4` تُقاس وتُطبَع ولا توقف

⚖️ **والتعليل مكتوبٌ قبل أيّ رقمِ ذراع** (خروجُ 3 وقع **قبل** جدول `C0/C1/C2`
⇒ لم يُنتَج رقمُ ذراعٍ ولم يُرَ، فليس هذا تحريكَ بار):

`RV4` — بنصّ العقد نفسِه — **«تختبر دعوايَ أنا لا تفترضها»** ⇒ هي **دعوًى
تُكذَّب** لا **بوّابةَ صلاحيةِ أداة**. وأذرعُ `C0`/`C1`/`C2` تُحسب **مستقلّةً
تمامًا** عنها (‏`RV3`/`RV7` هما بوّابتا الأداة، وقد عبرتا 1591/1591) ⇒ إيقافُ
التشغيلة على دعوًى مكذَّبة **يمنع القياسَ ولا يحمي منه شيئًا**، والتكذيبُ نفسُه
**نتيجةٌ تُنشَر**.

⛔ **وما لم يُمَسّ:** `RV0`/`RV1` (إعادةُ المنشور بت-بت) · `RV2` · `RV3` ·
`RV5` · `RV6` · `RV7` — **كلُّها تبقى قاتلةً كما سُجِّلت**، والمقامان كما هما،
والأذرعُ الثلاث بتعريفها، ومعايير §⑤ بحروفها.

### 🥇 وثلاثةُ أرقامٍ صدرت قبل التوقّف وتُنشَر

| الرقم | المقيس | ما يعنيه |
|---|---|---|
| `RV7` | ‏1591/1591 بت-بت | **المحرّكُ هو هو** — الأداةُ سليمة |
| `RP6` | `C1` **0.00%** مقابل `C2` **2.45%** | الوقفُ المشحون **لا يعلو الدخولَ أبدًا** (أثرُ التدوير بدقّة الدفعات) ⇒ يُقاس على **كلّ** الصفوف بينما `B1` استبعد 2.45% |
| `RP2` | ‏549/1591 (‏34.5%) · وسيط **0.18%** · أقصى **169.44%** | `pivot` يخالف المِرساةَ في **ثلث** الصفوف — والفجوةُ النمطيّة ضئيلةٌ والذيلُ هائل |

### 🧭 الدرس

> **بوّابةُ صلاحيةٍ تحرس الأداة · ودعوًى تُكذَّب تُنشَر.** وخلطُهما يجعل
> **بطلانَ استدلالي** سببًا في ألّا أقيس شيئًا — وهو أسوأُ العقوبتين.
