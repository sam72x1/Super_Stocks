# 🔬🔥 T-OPFIRE — تسجيلٌ مسبق: هل بصمةُ المضارب تفصل الاشتعالَ الحقيقيّ عن الكاذب؟

> **مدفوعٌ قبل أيّ رقم.** أمرُ المالك 2026-08-11 («سجّل التجربة»).
> **لا تُشغَّل ولا يُكتَب رقمٌ واحد قبل موافقة المالك على هذا الملفّ.**
> الأداةُ المزمَعة: `opfire_scan.py` + `.github/workflows/opfire.yml` · النتيجة:
> `opfire_result.md`.

---

## 🔴 تعديل ① (‏2026-08-11، بعد مراجعةٍ خصوميّةٍ على النسخة الأولى — مؤرَّخٌ ولا يُطوى)

النسخةُ الأولى دُفعت في `6008546`، ثم أُخضعت لمراجعةٍ خصوميّةٍ حكمُها **`FIX_FIRST`**
بسبعةِ قواتل. **تحقّقتُ من كلٍّ منها في الكود بنفسي** فصحّت، وهذي هي التغييراتُ —
**وأثقلُها يُسقط دعوى النسخة الأولى الأساسية:**

| # | ما سقط | التصحيح |
|---|---|---|
| ① | **المجتمعُ كان غيرَ معرَّف**: `pit_universe` يناقض إعادةَ بناء `break_level` — و`_ignition_break_level` ترجع `None` بلا `interp`/`pivot` (`Super_stock.py:12292-12307`) ⇒ ‏99% من الرموز **صفرُ اشتعال** = `no-op` بنيويّ | المجتمعُ **نوافذُ ARMED** حصرًا · **ودعوى «يعالج انحيازَ البقاء» مسحوبة** |
| ② | **خلطُ مقياسين**: شموعُ الدقيقة `adjusted=true` (`:11161`) والحكمُ على شموعٍ معدَّلة و`break_level` من سلسلةٍ معدَّلة — وعلاجي كان على **النافذة الخطأ** | **خامٌّ في الثلاثة معًا** + استبعادُ كلّ رمزٍ له تقسيمٌ **بين الاشتعال وتاريخ الجلب** |
| ③ | **`V5` تُسمّي حقلًا لم يُرَ قطّ**: `REQ_COLS` ثلاثةُ أعمدة، و`reduce_trades` **تُجمِّع اليومَ كلَّه بلا تقسيمٍ دقيقيّ** (`flatfiles_probe.py:179-188`) ⇒ «شموعُ دقيقةٍ من الملفّات» **مستحيلة اليوم** | مصدرُ الدقائق `polygon_minute_bars` والصفقاتُ `/v3/trades` ⇒ **الملفّاتُ المجمَّعة ليست ضروريّةً لهذي التجربة** (§⑩) |
| ④ | **`F1` ليست بوّابةَ الإنتاج**: القرارُ **ثلاثيٌّ مركَّب** (`:12440-12456`) · و`_operator_blocks` ترجع `None` تحت 20 صفقة (`:12121`) ⇒ `None["has_operator"]` **انهيار** | الشريحةُ = **قرارُ الكتم المركَّب** بثلاث قيم، و`None` **يُعَدّ ولا يُسقَط** |
| ⑤ | **نافذةُ البصمة «ستّ دقائق» رقمٌ من عندي**، والحيُّ **آخر 250 صفقة** بلا زمن (`:12146-12153`) — وهو **العيبُ ذاته** الذي أُجبر `T-EVENT-EXEC` على إصلاحه | ‏**250 صفقةً قبل نهاية دقيقة الاشتعال** · والكشفُ **30 دقيقة** (`:12369`) |
| ⑥ | **`P2` استند إلى ‏−0.168R** وهو داخل وثيقةٍ «للسجلّ **لا للاستشهاد**» | أساسُ `P2` صار **ثمانيةَ اختبارات `T-ACC`** وحدها |
| ⑦ | **اشتعالُ ما قبل السوق وسمُه مدمَّر**: `_ignition_outcome` تتخطّى **يومَ الاشتعال كلَّه** (`:12629`) ⇒ صعودُ D لا يُرى | **الجلسةُ النظاميّةُ حصرًا · و`P5` مسحوب** |

**‏+ خمسةُ إصلاحاتٍ منهجيّة:** دِدوب اشتعالٍ واحدٍ لكلّ (رمز، جلسة) · `acc_components`
**وصفيّةٌ لا تدخل حكمًا** (وإلّا فأربعةُ محاورَ حرّةٍ بعديًّا) · `F3` **حتميُّ الاختيار
بنافذته هو** · قاعدةُ «‏≥النصف» صارت **قابلةً للتكذيب** بأرضيةٍ وإشارة · وأرضيّةُ
العيّنة **صارت مسنودةً بالقياس** (‏§⑥).

🔴 **وتصحيحٌ ذاتيٌّ ثالث يُقال صريحًا:** بما أن الملفّاتَ المجمَّعة **لم تبقَ ضروريّةً
لهذي التجربة**، فحجّةُ «الآلةُ الجديدة (صفقاتٌ بلا اقتطاع)» **تسقط عن هذا التصميم**
وتبقى لنسخةٍ مستقبليّةٍ على الكون الكامل. **والمُبقى للمشروعية: المقياسُ الجديد
(نسبةُ الإنذار الكاذب) + أن الاختبارَ السابقَ الوحيد مسحوبٌ ولم يُعَد.**

⚖️ **وتصحيحٌ للمراجِع:** ادّعى أن `CLAUDE.md:2594` يستشهد بـ‏−0.168 **بلا وسمِ سحب** —
**غيرُ صحيح**: الكتلةُ موسومةٌ في رأسها «🔴 مسحوبة (أعلاه)». فلا تعديلَ هناك.

---

## ① السؤال — ومحورُه الجديد

**السؤال:** في **لحظة اشتعالٍ** يرصدها كاشفُ الإنتاج (`_ignition_signal`)، هل بصمةُ
المضارب في **الصفقات الخام** تفصل:

- **`real`** — بلغ ‏≥`IGNITION_CONFIRM_PCT`(=12%) صعودًا بعد الاشتعال، عن
- **`fakeout`** — أغلق تحت مستوى الكسر قبل بلوغ التأكيد،

بتعريفَي `_ignition_outcome` الإنتاجيَّين **حرفيًّا**؟

### 🔴 ولماذا ليس إعادةَ تشغيلٍ لملفٍّ مُغلَق — بفارقين صامدين

**(أ) مقابل `T-ACC` (مُغلَق 2026-07-08):** ذاك سأل سؤالَ **اختيار** — «هل التجميعُ
**عند القاع قبل الإشارة** يتنبّأ بانفجارٍ لاحق؟» — وأُغلق بنصّه: «أُغلق ملف
**«التجميع/الصفقات كمؤشّر اختيار»** (لا T-ACC-3 بلا **محور جديد + تسجيل + موافقة**)»
(`CLAUDE.md:1558`). وهذا سؤالُ **تأكيدٍ وتوقيت** على حدثٍ **بدأ فعلًا** — والتمييزُ
**منصوصٌ في المستودع قبل هذا التسجيل**: «**فشل T-ACC لا ينطبق: الحجم/السعر واقعان،
التدفق يؤكّد لا يتنبّأ**» (`CLAUDE.md`)، ويسنده السطرُ التالي لقاعدة الإغلاق: «**حافة
Polygon = التوقيت لا الاختيار**». والثلاثةُ مستوفاةٌ هنا (محورٌ + تسجيلٌ + موافقةٌ
مطلوبة قبل التشغيل).

**(ب) مقابل `E-OPERATOR` (ذراعٌ ثانوية في `T-EVENT-EXEC`) — وهذا أقربُ ما يُقال عنه
«مكرَّر»، فيُواجَه صريحًا:** تلك الذراعُ قاست **توقّعَ العائد (R)** فأعطت ‏−0.168R.
🔴🔴 **وذلك الرقمُ مسحوبٌ ولم يُعَد قياسُه قطّ:** `event_exec_result.md:28` يسحب صريحًا
أرقامَ `E-REAL`/`E-PSEUDO`/`E-CROSS`/**`E-OPERATOR`** بنصّ «**ما قِيس ليس ما سُجِّل**»
(أفقُ حسمٍ خاطئ = يومٌ واحد)، وترويستُه «الأرقامُ أدناه … **للسجلّ لا للاستشهاد**»،
**والملفُّ المصحَّح يحوي `E-REAL` و`E-PSEUDO` فقط — و`E-OPERATOR` غائبٌ بصفر ذِكر**
(مُتحقَّقٌ بالعدّ). ⇒ **بوّابةُ الكتم النافذةُ في الإنتاج الآن بلا أيّ قياسٍ صالحٍ على
الإطلاق.**
**والمقياسُ هنا مختلفٌ أصلًا: نسبةُ الإنذار الكاذب** — ويُصرّح الكودُ الإنتاجيّ بأنها
«**لا تُقاس إلّا حيًّا**» (`Super_stock.py:12502`) ⇒ وهذي التجربةُ تُبطل تلك الجملةَ
على مجتمعٍ تاريخيّ.
✅ **ووعدُ إعادة بناء مستوى الكسر ليس سرابًا:** `session_levels` موجودةٌ
(`event_exec_run.py:117`) ومقفولةٌ (`test_bot.py:13830`).

### 🎯 وقيمةُ القرار — الطرفان مُفيدان

قرارُ الكتم **نافذٌ في الإنتاج الآن** (`Super_stock.py:12440-12456`) و**لم يُصادَق
قطّ**: السجلُّ الحيّ **‏26 إطلاقًا** (‏18 `group` · 4 `mid` · 4 `operator`) وحدُّ العرض
`IGNITION_OUTCOME_MIN`=8 محسومة.
⚠️ **وحدُّ صدقٍ على هذا العدّ:** السجلُّ **بعد-البوّابة** (المكتومُ لا يُسجَّل — الدورةُ
تنتهي بـ`continue` قبل التسجيل) ⇒ **‏«18 من 26» لا يقدّر توزيعًا قبل البوّابة**،
ولذلك سُحب `P1` من صياغته الأولى (‏§⑧).

- **نجحت** ⇒ الكتمُ مُستحقٌّ وقابلٌ للمعايرة (اقتراحٌ للمالك).
- **فشلت** ⇒ **نكتم بلا مقابل** ⇒ يُعرَض **رفعُ الكتم** (قرارُه حصرًا).

---

## ② المجتمع والتعريفات — بدوالّ الإنتاج بأسمائها وبأرقامه

1. **المجتمع = نوافذُ ARMED على مرشّحي الارتكاز** (نمطُ `T-EVENT-EXEC`): كلُّ
   (رمز، جلسة) له `interp_in` مبنيٌّ **بإغلاق الجلسة السابقة** حصرًا، فيوجد له
   `break_level`. **المقيسُ سلفًا: ‏4,223 جلسةً مسلَّحة · 315 حدثًا خامًّا (7.5%) ·
   بتوزيعٍ سنويٍّ 86 / 116 / 113** (`event_exec_result.md:66`).
   🔴 **وانحيازُ البقاء غيرُ معالَج** — الكونُ كونُ الباكتيست الناجي، **و`pit_universe`
   لا يُستعمَل في هذي التجربة** (استعمالُه يلزمه `analyze_ticker` على كلّ رمز-جلسة =
   خارج الميزانية). **ودعوى النسخة الأولى بمعالجته مسحوبةٌ صريحًا.**
2. **شموعُ الدقيقة** = `polygon_minute_bars` **الإنتاجيّة** بـ**‏`adjusted=false`**
   (خامٌّ — §⑨-4)، **والدقائقُ الفارغة تُحذَف ولا تُملأ بأصفار** (سلوكُ تجميع
   Polygon نفسُه).
3. **مستوى الكسر** = `_ignition_break_level` **الإنتاجيّ** على حالةٍ مُعادةِ البناء
   بـ`session_levels` (‏`build_interpretation` بإغلاق الجلسة السابقة). **ولا رقمَ
   جديد**: عند غياب الرقم الحرج يعمل الاحتياطُ الإنتاجيّ (`الأرضية × 1.05`)، ويُعَدّ
   عددُ الحالتين ويُطبَع.
4. **الاشتعال** = `_ignition_signal(bars, break_level, vol_mult=CONFIG["IGNITION_VOL_MULT"])`
   **بدالّة الإنتاج نفسِها**، على نافذةٍ **‏30 دقيقةً زاحفة** بـ`min_bars=6`
   الافتراضيّ (‏= الحيّ حرفيًّا، `Super_stock.py:12369`). وتُطبَع نسبةُ ما سقط دون
   `min_bars`.
   🔒 **ودِدوب: اشتعالٌ واحدٌ لكلّ (رمز، جلسة) — الأوّل** (مطابقةً للإنتاج ولسجلّ
   الإطلاق) وإلّا انكسر استقلالُ الأرصاد وبطلت فواصلُ Wilson.
5. **بصمةُ المضارب** = `_operator_blocks(trades, CONFIG["OPERATOR_MIN_SHARES"])`
   **الإنتاجيّة** على **آخر 250 صفقةً قبل نهاية دقيقة الاشتعال** — ‏**= نافذةُ
   `operator_flow` الحيّة حرفيًّا** (`limit=250, order=desc`، `Super_stock.py:12147`)
   **لا ستّ دقائق**. والصفقاتُ من `/v3/trades` لذلك (الرمز، اليوم).
   ⚠️ **وضمُّ دقيقة الاشتعال نفسِها مقصودٌ ومُصرَّحٌ به:** قرارُ الكاشف يُتَّخذ عند
   **إغلاق** تلك الدقيقة فكلُّ صفقاتها وقعت قبل نهايتها (والإنتاجُ يرى **أكثر**: ينادي
   `operator_flow` بعد الإغلاق) ⇒ **ليس تسريبًا، بل متحفّظ**. **لكنّه تداخلٌ انتقائيّ**:
   الدقيقةُ مُنتقاةٌ لأن حجمَها ‏≥3× المتوسط، ودقيقةُ الحجم الشاذّ أرجحُ ميكانيكيًّا أن
   تحوي طبعةً ‏≥1000 سهم ⇒ **`F3` هو الحارسُ الوحيد لهذا** ⇒ صار **شرطَ صلاحيةٍ لا
   تحسينًا**.
6. **الحكم** = `_ignition_outcome(fire, df)` **الإنتاجيّة**، على **‏40 جلسةً بعد يوم
   الاشتعال حصرًا** (لا سلسلةً مفتوحة، وإلّا استحال `pending`). و`pending` **تُستبعَد
   من المقام ويُعلَن عددُها**.
   🔴 **والاشتعالاتُ مقصورةٌ على الجلسة النظاميّة (‏09:30-16:00 ET)** لأن
   `_ignition_outcome` تتخطّى **يومَ الاشتعال كلَّه** (`Super_stock.py:12629`) فتُهدر
   جلسةَ اشتعالِ ما قبل السوق ⇒ **`P5` مسحوب**، وضمُّ الافتر يُترَك لتجربةٍ أخرى.
7. **`acc_components`** تُحسَب وتُطبَع **وصفًا فقط** ولا تدخل أيّ ذراعٍ ولا أيّ حكم —
   **ومحاورُها الأربعة مُغلقةٌ بحكم `T-ACC`** (فلا يُنتقى محورٌ منها بعد الأرقام).

---

## ③ المقياسُ الحاكم — مثبَّتٌ قبل أيّ رقم

> **`FAKE(g)` = نسبةُ `fakeout` من المحسوم (`real`+`fakeout`) داخل الشريحة `g`.**
> والفرقُ المحكوم: **`FAKE(المكتوم) − FAKE(الممرَّر)`** بالنقاط المئوية،
> **بقرار الكتم الإنتاجيّ المركَّب** لا بشريحةٍ مبتكرة.

**وقرارُ الكتم يُعاد بناؤه كما في `Super_stock.py:12440-12456` حرفيًّا، بثلاث قيم:**

| القيمة | التعريف | قرارُ الإنتاج |
|---|---|---|
| `pass_operator` | البصمةُ مقيسة **و**`has_operator` | **يمرّ** |
| `mute_operator` | البصمةُ مقيسة **و**`¬has_operator` | **يُكتَم** |
| `fallback` | `_operator_blocks` أرجعت **`None`** (‏<20 صفقة) | يُشرَّح بصنف الشمعة: `group` ⇒ **يُكتَم** · غيرُه ⇒ **يمرّ** |

⇒ **المكتومُ = `mute_operator` ∪ (`fallback` ∧ `group`)** · **والممرَّر = الباقي**.
🔒 **و`fallback` يُعلَن بعدَده ولا يُسقَط** (السيولةُ الرقيقة تُنتج `None` وتُنتج
`group` معًا ⇒ إسقاطُها يحذف الشريحةَ المتحيّزة بعينها).
- **ومقياسٌ مساندٌ واحدٌ فقط** (يُعلَن ولا يحكم): نسبةُ `real` — لكشف أن الكتم قد
  يُقلّل الكاذبَ **ويُقلّل الحقيقيَّ معه** (كتمٌ بلا ربح).
- 🔒 **ولا مقياسَ ثالث، ولا مقياسٌ يُضاف بعد رؤية الأرقام. ولا `R`** (‏`T-EVENT-EXEC`
  ملفُّه، وإعادتُه هنا خلطُ ملفَّين).

---

## ④ الأذرع — مثبَّتةٌ أربعًا لا خامسة

| الذراع | التعريف |
|---|---|
| **`F0`** | كلُّ الاشتعالات (الأساس، بلا شريحة) — **المقام** |
| **`F1`** | **قرارُ الكتم المركَّب** (§③): المكتومُ مقابل الممرَّر |
| **`F2`** | `_ignition_candle_class` الثلاثية (`group` ≤50 ألف · `mid` · `operator`/`strong` ≥100 ألف) — **بعتبات الإنتاج بلا تغيير** |
| **`F3`** | **شاهدُ ضبطٍ حتميّ**: لكلّ اشتعالٍ **دقيقةٌ واحدةٌ غيرُ مشتعلة من نفس الرمز ونفس الجلسة، مطابقةٌ في دقيقة الساعة ‏±5** (نمطُ `E-PSEUDO`)، تُختار بـ`sha256("YYYY-MM-DD:SYM")` (نمطُ `control_panel` الحتميّ) · **وتُحسَب لها البصمةُ من نافذتها هي** (لا وسمَ الاشتعال منسوخًا) · وتُوسَم بـ`_ignition_outcome` بنفس `break_level` الجلسة |

🔒 **إضافةُ ذراعٍ خامسة بعد الأرقام = `p-hacking` ممنوع بنصّ هذا السطر.**

---

## ⑤ المعيار — حاسمٌ غيرُ قابلٍ للتفاوض بعديًّا

تُعتمَد النتيجةُ **فقط** إذا تحقّقت **الخمسةُ معًا**:

1. **ثلاثُ سنواتٍ** (‏2023 · 2024 · 2025) — ولا حكمَ من سنتين.
2. **الفرقُ ‏≥10 نقاط مئوية** في **كلّ** سنة.
3. **فاصلا Wilson ‏95% منفصلان** — **واختبارٌ ثنائيُّ الطرف** (`lo_a > hi_b` **أو**
   `lo_b > hi_a`) 🔴 **تصحيحًا صريحًا لعيب `T-ACC` الأحاديّ الطرف** (`Super_stock.py:16692`)
   الذي كان يستحيل عليه أن يرى علاقةً عكسية — **والنمطُ الثنائيُّ موجودٌ في الملفّ
   نفسِه** (`:16307`) فأحاديّتُه تعارضٌ داخليٌّ لا سهو.
4. **لا انقلابَ إشارة** بين السنوات الثلاث.
5. **‏≥150 محسومًا مجمَّعًا · ≥40 في كلّ سنة · ≥25 في كلٍّ من شريحتَي `F1`.**

**وإن سقط أيُّ شرط: يُبلَّغ الرقمُ بلا توصية.** و**«فشلت الفرضية» ≠ «العيّنة لا تكفي»**
— ويُصرَّح بأيِّهما وقع.

🔴 **والشرطُ السالبُ الحاكم (شاهدُ الضبط) — بصيغةٍ قابلةٍ للتكذيب:**
إن كان **`|Δ(F3)| ≥ 0.5 × |Δ(F1)|`** **و**‏`|Δ(F3)| ≥ 4 نقاط` ⇒ **تُلغى النتيجةُ**
(أثرُ آلةٍ لا أثرُ اشتعال). **وإن اختلفت إشارةُ `F3` عن إشارة `F1` فلا إلغاء** بل
يُطبَع الطرفان. **وإن كان `|Δ(F1)| < 10` فالمعيارُ ساقطٌ أصلًا** ولا يُطبَّق شرطُ
الإلغاء (فلا يُلغى ما هو ساقط).

---

## ⑥ أرضيّةُ العيّنة — مسنودةٌ بالقياس لا بالأمل

من المقيس (‏`event_exec_result.md:66`): **‏315 حدثًا خامًّا** بتوزيع 86/116/113.
عند `pending` ‏≈20% ⇒ **‏≈252 محسومًا مجمَّعًا و≈69/93/90 سنويًّا** ⇒ **شرطا «‏≥150»
و«‏≥40/سنة» مستوفيان بهامش.**
🔴 **والخطرُ الوحيدُ الباقي: شرطُ «‏≥25 لكلّ شريحة»** — رهنٌ بنسبة
`_operator_blocks → None` (‏<20 صفقة) على كوننا الرقيق، **وهي غيرُ مقيسة** ⇒
**تُطبَع في يوم الجدوى الواحد قبل السوق الكامل** (‏§⑩)، وإن تعذّرت الأرضيةُ أُبلِغ
**«لا حكم» قبل التشغيل الكامل** لا بعده.

---

## ⑦ بوّاباتُ الصلاحية — تكشف الـ`no-op` وتسقط بصوتٍ عالٍ

- **`V1` شاهدُ الضبط:** رمزٌ سائلٌ معروف يُخرج سماتٍ غيرَ تافهة — **والصفرُ عطبُ أداةٍ
  حتى يُنفى** (درسُ PIT).
- **`V2` الدوالُّ الإنتاجيةُ تُنادى فعلًا:** قفلٌ **نحويٌّ (AST)** على نداء
  `_ignition_signal` و`_operator_blocks` و`_ignition_outcome` و`_ignition_candle_class`
  **بالاسم** — **والقفلُ النصّيُّ ممنوع** (يسقط على التعليق).
- **`V3` العلمُ فعّال:** يُطبَع «قرأ N (رمز، جلسة)»، **وصفرٌ ⇒ خروجٌ غيرُ صفريّ**
  بنصّ «‏no-op لا تُفسَّر نتيجتُها».
- **`V4` عدّاداتٌ لا تكذب:** الاشتعالات · `real`/`fakeout`/`pending` · الشرائحُ الثلاث
  (`pass_operator`/`mute_operator`/`fallback`) · حالتا `break_level` (حرجٌ مقابل
  احتياط) · وكلُّ قصٍّ **بعدَده**.
- **`V5` بلا تسريبٍ مستقبليّ — وبعمودٍ **مُثبَتٍ** لا مُسمًّى تخمينًا:** كلُّ صفقةٍ
  تدخل البصمة زمنُها **أصغرُ من** نهاية دقيقة الاشتعال (`<` لا `<=`)، والوسمُ من
  شموعٍ **بعد يوم الاشتعال حصرًا**. **واسمُ حقل الزمن يُثبَّت من مُخرَج `/v3/trades`
  الفعليّ ويُكتَب في `opfire_result.md` قبل أيّ رقم** — وغيابُه ⇒ **خروجٌ غيرُ صفريّ**.
- **`V6` طباعةٌ حيّة** (`PYTHONUNBUFFERED=1`) — تشغيلةٌ تُقتَل لا تضيع بكاملها.
- **`V7` معامِلُ التوسيع مطبوعٌ** مع كلّ إسقاطِ كلفةٍ من عيّنة.
- **`V8` `break_level` مبنيٌّ فعلًا:** يُطبَع عددُ (رمز، جلسة) التي بُني لها،
  **وصفرٌ ⇒ خروجٌ غيرُ صفريّ** (يقتل قاتلَ ① قبل استهلاك الميزانية).
- **`V9` مقياسٌ واحدٌ للسعر:** يُطبَع أن **‏100%** من الشموع والمستويات **خامّة**،
  وعددُ الرموز المستبعَدة للتقسيم لكلّ سنة — **وأيُّ خلطٍ ⇒ خروجٌ غيرُ صفريّ**.
- **`V10` `exchange`:** إن استُعمل `acc_components` فغيابُ العمود ⇒ `dark_share_pct`
  **يُعلَن «غيرُ مقيس» لا صفرًا** (لا صفرٌ صامتٌ يُقرأ قياسًا).

---

## ⑧ التنبّؤات — تُنشَر لتُكذَّب

- **`P1`** (‏**مُعاد الصياغة**): توزيعُ `_ignition_candle_class` **قبل البوّابة** سيغلب
  عليه `group` — ⚠️ **ولا يُستشهَد بـ«18 من 26» الحيّة** لأنها **بعد-البوّابة**
  فلا تقدّر التوزيعَ غيرَ المشروط.
- **`P2`** الفرقُ في `F1` **سيقع بين ‏0 و10 نقاط** (أي **يفشل** المعيار) — **بناءً على
  ثمانيةِ اختباراتٍ ساقطةٍ سابقةٍ على الصفقات (`T-ACC`، `CLAUDE.md:1556`) وحدها**، ولا
  أستند إلى ‏−0.168R المسحوب.
- **`P3`** `pending` كبيرةٌ ماديًّا (‏>20%).
- **`P4`** حالةُ **الاحتياط** في `break_level` تغلب حالةَ الرقم الحرج تاريخيًّا.
- **`P5`** 🔴 **مسحوبٌ** (كان عن ضمّ ما قبل السوق — والوسمُ يستحيل هناك، §②-6).
- **`P6`** (جديد) نسبةُ `fallback` (‏`None`) **‏≥15%** من الاشتعالات على كوننا الرقيق
  — وهي التي تحكم أرضيّةَ «‏≥25 لكلّ شريحة».

---

## ⑨ حدودُ الصدق — تُقرأ مع النتيجة لا بعدها

1. **انحيازُ البقاء غيرُ معالَج** (كونُ الباكتيست الناجي) — **مُعلَنٌ لا مُدَّعى عكسُه.**
2. **بلا `NBBO`/طلبٍ وعرض** ⇒ قرائنُ «تفريغ العروض» (N5) **غيرُ مقيسة**،
   **و«قابليةُ التنفيذ» لا تُقاس هنا** (وهي مأخذُ `T-EVENT-EXEC`).
3. **ولا فلوت ولا شورت FINRA ولا متاحَ اقتراض ولا قروبات ولا SEC** ⇒ لا ضبطَ لها.
4. **مقياسُ السعر موحَّدٌ خامًّا في الثلاثة** (شموعُ الدقيقة · `break_level` · شموعُ
   الحكم) — 🔴 **لأن `auto_adjust` يعيد تسعيرَ التاريخ كلِّه حتى تاريخ الجلب** فتقسيمُ
   2026 يُفسد اشتعالَ 2023 ⇒ **ويُستبعَد كلُّ رمزٍ له تقسيمٌ بين تاريخ الاشتعال وتاريخ
   الجلب** (لا «داخل نافذة الحكم» كما كتبتُ أوّلًا)، ويُعَدّ المستبعَدون.
   ⚠️ وهو عيبٌ **مُعلَنٌ غيرُ مُصلَحٍ** في المستودع (`event_exec_result_corrected.md:75`).
5. **وسمُ `_ignition_outcome` متفائلٌ داخل اليوم** (القمّةُ تُفحَص قبل الإغلاق) —
   **مقصودٌ ومُعلَنٌ في الـdocstring**، وليس تسريبًا (القمّةُ بعد الاشتعال فعلًا).
6. **ضمُّ دقيقة الاشتعال تداخلٌ انتقائيّ** لا تسريب (§②-5) — و`F3` حارسُه.
7. **النتيجةُ عن مجتمع الاشتعالات لا عن السوق** ولا تُعمَّم.

---

## ⑩ الكلفة والتجزئة — بعد التصحيح: **الملفّاتُ المجمَّعة ليست ضروريّةً هنا**

مجتمعُ ARMED **‏4,223 (رمز، جلسة)** فقط ⇒ يُجلَب بـ`/v3/trades` **لكلّ رمز-يوم** كما
فعل `T-ACC`، **بلا ملفّاتٍ مجمَّعةٍ وبلا 630 ساعة**.
📌 **وللسجلّ، كلفةُ الملفّات المقيسة** (`flatfiles_access_verdict.md`): يومٌ ‏≈50 دقيقة
· سنة ‏≈210 ساعة · ثلاثُ سنواتٍ ‏≈630 ساعة (‏≈79 بالتوازي 8) — **وتلزم لنسخةٍ
مستقبليّةٍ على الكون الكامل، لا لهذي.**
**والتشغيل:** يومُ جدوى **واحد** أوّلًا (يُطبَع فيه: الترويسةُ/حقلُ الزمن · نسبةُ
`fallback` · حالتا `break_level`) ⇒ **ولا سنةٌ كاملةٌ قبل عبوره ببوّاباته العشر.**
وكلُّ جوبٍ **يرفع مُخرَجَه `artifact` قبل أيّ دفع** (درسُ `ignition.yml`: الدفعُ
العاري أضاع عشرَ جلسات).

---

## ⑩-ب نتيجةُ **يوم الجدوى** (‏2026-08-11 · تشغيلة `31534739418` · **وقائعُ لا حكم**)

عبرت **البوّاباتُ الستُّ** كلُّها على يوم **‏2026-08-10** بـ12 رمزًا من القائمة الحيّة
(‏1,321 شمعة · 12,394 صفقة · **‏2.8 ثانية**):

| البوّابة | المقيس |
|---|---|
| **`V5`** ✅ | **حقلُ الزمن مُثبَتٌ من المزوّد: `sip_timestamp`** — وحقولُه كاملةً: `conditions, decimal_size, exchange, id, participant_timestamp, price, sequence_number, sip_timestamp, size, tape` ⇒ **قاتلُ التسريب انحلّ بالإثبات لا بالتخمين** |
| **`V8`** ✅ | `break_level` مبنيٌّ **‏12 من 12** — **وكلُّها «رقمٌ حرج» وصفرُ احتياط** 🔴 **⇒ `P4` مكذَّبٌ على هذي العيّنة** (توقّعتُ غلبةَ الاحتياط) — عيّنةٌ حيّةٌ لا تاريخيّة، والتنبّؤُ يبقى للمشي التاريخيّ |
| **`V10`** ✅ | `exchange` حاضر ⇒ `dark_share_pct` قابلٌ للقياس |
| **`V1`** ✅ | شاهدُ الضبط `AAPL` = **‏100,000 صفقة** |
| **`V9`** ✅ | خامٌّ (`adjusted=false`) في الشموع والمستويات |
| **`V3`** ✅ | صفرُ `no-op` |

🔴 **وأوّلُ قراءةٍ للخطر المسجَّل في §⑥ (شرطُ «‏≥25 لكلّ شريحة»): اشتعالان — وكلاهما
`pass_operator` · صفرٌ مكتوم.** وكلتا الشمعتين **`group`** (‏$13,537 و$10,025) ومع ذلك
**مرّتا** — لأن قرارَ الإنتاج يمرّ من الفرع الأوّل (`مقيسٌ ∧ has_operator`) **وكتمُ
`group` لا يعمل إلّا في فرع `fallback`**.
⇒ **فرضيةٌ بنيويّةٌ تُسجَّل الآن قبل قياسها: كتمُ `IGNITION_USD_GROUP`(=50 ألف) شبهُ
ميّتٍ عمليًّا** — لأن طبعةَ ‏1000 سهم على سهمِ ‏$1.06 = **‏$1,060** فتتحقّق بسهولة
فيُقاس `has_operator=True`. ⚠️ **و`n=2` لا يحكم بشيء** — لكنه يُنذر بأن شريحةَ المكتوم
قد لا تبلغ ‏25 ⇒ **تُقاس على مجتمع ARMED قبل أيّ سنةٍ كاملة**، وإن لم تبلغ فالحكمُ
**«لا حكم» يُعلَن قبل التشغيل لا بعده** (‏§⑥).

---

## ⑪ الأداةُ المزمَعة — عقدُها قبل كتابتها

`opfire_scan.py`: **قارئٌ محض** · لا يكتب حالةً · **لا يُستورَد في `Super_stock`** ·
يستورد دوالَّ الإنتاج **ولا ينسخها** · خروجٌ غيرُ صفريّ عند سقوط أيّ بوّابة ·
وأقفالٌ في `test_bot.py` بأسماء (`OPF1`…) **وطفراتٌ تُثبت سقوطَ كلٍّ منها**.

---

## ⑫ ما لا تحلّه هذي التجربةُ ولو نجحت

لا تقول شيئًا عن **الاختيار** (‏M1-M14) ولا **الترتيب** ولا **الربحية** ولا **المتاح
للاقتراض**. **وأقصى ما تقوله: هل الكتمُ بقرار المضارب المركَّب يستحقّ أن يبقى.**

---

## ⑬ سقفُ النجاح — مثبَّتٌ قبل الأرقام

- **الأقصى المسموح: اقتراحٌ للمالك بمعايرة عتبةٍ قائمة** (`IGNITION_USD_*` أو
  `OPERATOR_MIN_SHARES`) **+ سطرُ عرضٍ في التنبيه** — ولا شيءَ غيرَه.
- 🔒 **ممنوعٌ بنصّ هذا التسجيل:** أيُّ مسٍّ بـ`M1`-`M14` أو `rank_key`/`select_top`/
  `classify_tier`/`analyze_ticker`/`entry_status` · أيُّ تغييرٍ في الدخول/الوقف/الأهداف
  · أيُّ وزنِ ترتيب · وأيُّ رقمٍ يُخترَع.
- **ولا يُنفَّذ شيءٌ تلقائيًّا:** `OPERATOR_MIN_SHARES`=1000 **رقمُ فيصل الموثَّق**
  وتغييرُه قرارُ مالك.

---

## ⑭ ملحقٌ — خمسةُ عيوبِ قياسٍ في آلة `T-ACC` (تُدوَّن لأن حكمَه ما زال يُستشهَد به)

1. 🔴 **اختبارُ الفصل أحاديُّ الطرف بنيويًّا** (`Super_stock.py:16692`) ⇒ يستحيل عليه
   وسمُ علاقةٍ **عكسية**، فكلُّ الفروق السالبة (‏−7 · −4 · −14 · −6) وُسمت «متداخلة»
   **بالبناء لا بالقياس** — **والنمطُ الثنائيّ موجودٌ في الملفّ نفسِه** (`:16307`).
2. 🔴 **اقتطاعٌ صامتٌ عند 150 ألف صفقة** بترتيبٍ **تصاعديّ** (`:12019`) ⇒ للسائل تُقاس
   **أقدمُ** الصفقات لا كلُّها، بلا وسم.
3. 🔴 **شرطُ السنتين غيرُ مُنفَّذٍ في الكود** — قراءةٌ يدويّة من تقريرَين.
4. 🔴 **انحيازُ بقاءٍ غيرُ مُعلَن**، **و`acc_verify.py:64` يدّعي العكسَ حرفيًّا:
   «السوق الكامل بلا انحياز»**.
5. 🔴 **ذراعُ 2026 سنةٌ ناقصة** (المشيُ يتوقّف 40 شمعةً قبل نهاية البيانات) بينما
   الـworkflow يسمّيها «السنة المكتملة» · **+ وسمُ `exploded` يحتسب صعودًا بعد ضربِ
   الوقف** ⇒ ليس ربحًا قابلًا للتنفيذ.

⚖️ **ولا يُبنى على هذا أن الأثرَ موجود** — بل أن الاختبارَ كان أضعفَ مما ادّعى،
**وأن أيَّ استشهادٍ بـ`T-ACC` بعد اليوم يُقرَن بهذي الحدود.**

---

## ⑩-ج تعديلٌ مؤرَّخ **‏2026-08-11 · قبل أيّ رقمٍ من كون ARMED** — وضعُ «قياسِ المكتوم»

بأمر المالك («‏1» = «قِس المكتوم») يُنفَّذ **شرطُ الأرضيّة المكتوبُ في §⑥** قبل أيّ
سنةٍ كاملة: يُقاس على مجتمع ARMED **حجمُ الشريحتين وحدهما**. وثلاثةُ إقراراتٍ تُكتَب
**الآن** لا بعد الأرقام:

1. 🔴 **`V9` تُقرأ «مقياسٌ واحد» لا «خامٌّ حتمًا».** في وضع الجدوى كانت المستوياتُ من
   القائمة الحيّة فأمكن الخامّ في الطرفين. وفي ARMED **المستوياتُ مشتقّةٌ من اللقطة
   المجمَّدة** (`plan_levels` تعيد بناء الأرضية من `stop` المخزَّن، و`auto_adjust=True`)
   ⇒ **جلبُ شموعٍ خامّةٍ يخلط مقياسَين** وهو ما تمنعه `V9` أصلًا. فيُستعمَل
   `EX.hist_minute_bars` (‏`adjusted=true`) **في الطرفين**، وحارسُه `EX.scale_mismatch`
   بتسامح ‏0.15 (نفسُ `T-EVENT-EXEC` — المجتمعُ واحدٌ فيُقاس بمسطرته)، **وعددُ
   المستبعَدِ للتقسيم مطبوعٌ**. ⚖️ فالتعديلُ **تنفيذٌ لروح `V9`** لا إرخاءٌ لها،
   ولا يمسّ معيارَ الحكم (‏§⑤) بحرف.
2. **البصمةُ تُجلَب بشكل نداء الإنتاج حرفيًّا** (`order=desc&limit=250` ثم عكسٌ
   للتصاعديّ) مع `timestamp.lt` وحده — **وبلا `timestamp.gte` عمدًا** لأن الإنتاج بلا
   نافذةٍ زمنية، **ومدى البصمة يُقاس ويُطبَع** بدل أن يُحَدّ.
3. 🔒 **هذا الوضعُ يَعُدّ ولا يحكم:** لا `_ignition_outcome` · لا فرقَ نقاطٍ · لا
   `Wilson` · لا `R` ⇒ **يستحيل عليه بنيويًّا أن يحرّك المعيار**. وإن لم تُبلَغ
   الأرضيةُ فالخروجُ **‏5** ونصُّه «لا حكم» — **قبل** التشغيل الكامل لا بعده.
4. **المجتمعُ `FAISAL_ONLY=0`** ليطابق التشغيلةَ التي منها الرقمُ المستشهَدُ به في §⑥
   (‏`event_exec_result.md` — وكانت قبل اعتماد بوّابات فيصل في 08-06) ⇒ **العددُ قابلٌ
   للمقارنة**، و**النسبُ** هي الكمّيةُ المنقولة للمجتمع الحيّ اليوم.

🔴 **وعيبان في أداتي كشفهما هذا الوصلُ فصُحِّحا (كلاهما من صنفٍ مدوَّنٍ سلفًا):**
نافذةُ الكشف كانت **عدديّة** (‏30 شمعةً موجودة) وهو ما أصلحته مراجعةُ Codex الثانية في
`replay_trigger` («زمنيّة لا عدديّة» — في كوننا الرقيق تغطّي ستُّ شمعاتٍ ساعتين فيرى
التاريخيُّ سياقًا لا يراه الحيُّ أبدًا) · وكانت تمشي **ما قبل السوق وما بعده**
**والرادارُ الحيُّ لا يعمل خارج الجلسة النظامية** ⇒ اشتعالٌ يُعَدّ ولا يمكن أن يقع.
⇒ الكشفُ صار `group_sessions` + `replay_trigger` **نفسَهما في الوضعين**.
⚠️ **وعليه: «‏2 اشتعال» في يوم الجدوى رقمٌ قابلٌ للتغيّر** — ولم يكن حكمًا (‏«صفرُ حكم»)
لكن يُصرَّح به.

---

## ⑩-د نتيجةُ بوّابة الأرضيّة — مقيسةٌ ‏2026-08-11 (تشغيلة `31537955017` · وضعُ ARMED)

المجتمع: ARMED · 2025 · لقطة `30598885456` · `FAISAL_ONLY=0` · 1.3 دقيقة قياسًا.
**‏97 نافذة** (من 191 إشارة · **‏92 مرفوضًا بالسعة**) · **‏1,401 جلسة** ·
**‏93 اشتعالًا**.

| الشريحة | العدد |
|---|---|
| `pass_operator` | ‏83 |
| `mute_operator` | **‏10** |
| `fallback` (‏`_operator_blocks → None`) | **‏0** |
| ⇒ **مكتوم** (`mute_operator` + `fallback`∧`group`) | **‏10 = 10.8%** |
| ⇒ **ممرَّر** | ‏83 |

**والبوّاباتُ الأربع خضراء:** `V3` (‏1,401 جلسة) · `V8` (**‏97/97 بالرقم الحرج**) ·
`V9` (مستبعَدٌ للتقسيم ‏20 من 97) · `V4` (‏93 = 83+10+0). ومدى البصمة وسيطًا **‏13.8
دقيقة** · وتعذّرُ جلبِ الصفقات **صفر**.

🔴 **الأرضيةُ لم تُبلَغ: ‏10 × 3 × 0.8 ‏≈ 24 مقابل الحدّ 25 ⇒ خروجُ `5` ⇒ «لا حكم»**
بنصّ §⑥ — **قبل** تشغيل السنوات الثلاث، والميزانيةُ توفَّرت.
⚠️ **وهذا إسقاطٌ لا قياس:** معامِلُ ×3 يفترض تشابهَ السنوات، و×0.8 هو تنبّؤ `P3`
**غيرُ المقيس** في هذا الوضع ⇒ **القياسُ المباشر لـ2023 و2024 يستبدل الإسقاطَ برقم**،
وهو **لا يغيّر المعيار** (‏≥25 كما هو) ⇒ **قرارُ المالك**.

🔑🔑 **والاكتشافُ البنيويّ أهمُّ من الحكم — و`P6` مكذَّبٌ بعنف:** توقّعتُ `fallback`
‏**≥15%** والمقيسُ **صفرٌ من 93**. **والسببُ بنيويّ لا عارض:** الاشتعالُ **يشترط
قفزةَ حجمٍ ×3**، فالدقيقةُ التي يقع فيها سائلةٌ بالضرورة، ونافذةُ 250 صفقةً السابقةَ
لها تتجاوز حدَّ العشرين دائمًا ⇒ **`_operator_blocks` لا تُرجع `None` في لحظة اشتعالٍ
قطّ**. ⇒ 🔴 **وكتمُ «القروب» بعتبة `IGNITION_USD_GROUP` = 50 ألفًا ميّتٌ بنيويًّا**:
لا يعمل إلّا في فرع `fallback` (‏`Super_stock.py:12447` — `elif` تحت `of is None`)،
**و‏48 من 93 اشتعالًا (‏52%) شمعتُها `group` ومرّت كلُّها**.
📌 **وبدقّةٍ أكبر: الفرعُ ليس مستحيلًا — بل يُفعَّل عمليًّا عند فشلِ النداء لا عند
رقّةِ السيولة** (‏`operator_flow` ترجع `None` أيضًا على 401/403/429/شبكة) ⇒ **عتبةُ
«القروب» تكتم حين يسقط Polygon، لا حين تكون الشمعةُ قروبًا.**
📌 وهو **تصديقٌ للفرضية المسجَّلة قبل قياسها** من اشتعالَي يوم الجدوى (‏`n=2`)، وقد
صارت مقيسةً على **‏93**.

🎯 **و`P1` تحقّق:** توزيعُ الشمعة **قبل** البوّابة يغلب عليه `group` (‏48 · `mid` 13 ·
`operator` 19 · `strong` 13).
🔴 **و`P4` مكذَّبٌ ثانيةً — تاريخيًّا هذي المرّة:** ‏**‏97/97** بالرقم الحرج. وكنتُ
كتبتُ بعد يوم الجدوى «عيّنةٌ حيّةٌ لا تاريخيّة، والتنبّؤُ يبقى للمشي التاريخيّ» —
**فسقط هناك أيضًا**.

⚠️ **وفحصُ اتّساقٍ يُنشَر:** ‏93 اشتعالًا مقابل **‏113** المستشهَد بها لـ2025 في
`event_exec_result.md` = **‏−18%**، وهو **الاتّجاهُ المتوقَّع من إصلاحَي الكشف**
(إسقاطُ ما قبل السوق وما بعده · ونافذةٌ زمنيّةٌ بدل عدديّة) — فكلاهما **يُزيل**
اشتعالاتٍ لا يراها الحيُّ ولا يُضيف واحدًا.
⚠️ **وحدود:** ‏20 نافذةً أُقصيت بحارس التقسيم ⇒ الأعدادُ **أرضيّةٌ لا سقف** ·
والسعةُ رفضت **‏92 من 191** إشارة ⇒ المجتمعُ **ما وصل المالكَ فعلًا** لا كلَّ إشاراته
· ولا وسمَ نتيجةٍ هنا فـ`pending` غيرُ مقيسة.

## ⑩-هـ الأرضيّةُ **مقيسةً** في السنوات الثلاث — ‏2026-08-12، أمرُ المالك «قِس السنتين»

بدل الإسقاط ×3 من سنةٍ واحدة، قِيست **‏2023** (`31556325800`) و**‏2024**
(`31556332865`) على **نفس** اللقطة `30598885456` ونفس الوضع — والهويّةُ مؤكَّدةٌ من
سجلّ الباكتيست نفسِه (‏2023 = 120 إشارة/24.2% · 2024 = 117/33.7% · 2025 = 191/19.7%،
مطابقةً للأرقام المنشورة سلفًا = **فحصُ اتّساقٍ عابر**).

| السنة | إشارات | نوافذ | مرفوضٌ بالسعة | جلسات | اشتعالات | **مكتوم** | ممرَّر | نسبةُ الكتم | `fallback` | مستبعَدٌ للتقسيم |
|---|---|---|---|---|---|---|---|---|---|---|
| 2023 | 120 | 85 | 33 | 1,002 | 63 | **17** | 46 | **27.0%** | 0 | 23/85 |
| 2024 | 117 | 82 | 35 | 1,269 | 101 | **13** | 88 | **12.9%** | 0 | 17/82 |
| 2025 | 191 | 97 | 92 | 1,401 | 93 | **10** | 83 | **10.8%** | 0 | 20/97 |
| **مجمَّع** | 428 | **264** | 160 | **3,672** | **257** | **40** | **217** | **15.6%** | **0** | 60/264 |

✅ **والأرضيةُ تُبلَغ بالمقيس:** ‏40 × 0.8 = **‏32 مقابل الحدّ 25** · والممرَّر 174 ·
والمجمَّعُ المحسوم ‏≈206 (الحدّ 150) · وكلُّ سنةٍ ‏≈50/81/74 (الحدّ 40) ⇒ **الشروطُ
العدديّةُ الأربعة مستوفاة**، وبوّاباتُ الصلاحية خضراء في الثلاث (‏`V8` = **‏264/264
بالرقم الحرج**).
📌 **والقراءةُ «مجمَّعًا» ليست اختيارًا بعديًّا:** §⑥ حسبت الأرضيةَ على المجمَّع
**قبل أيّ رقم** («‏≈252 محسومًا مجمَّعًا»). ⚠️ **ولو قُرئت «لكلّ سنة» لسقطت** (‏17 ·
13 · 10 — كلُّها دون 25) ⇒ **الطرفان مُعلَنان.**

🔴🔴 **وتصحيحٌ ذاتيّ: إسقاطي ×3 من 2025 كان مُضلِّلًا في اتّجاهه.** ‏2025 هي **أدنى**
السنوات كتمًا (‏10.8% مقابل 27.0% في 2023) ⇒ نسبةُ الكتم تتفاوت **‏2.5×** بين
السنوات، **والحكمُ ينقلب بحسب السنة التي تُسقِط منها**: من 2023 وحدها ‏≈41 ✅ · من
2024 ‏≈31 ✅ · **ومن 2025 ‏≈24 🔴**. 🧭 **الدرس: إسقاطٌ ×N من سنةٍ واحدة لا قيمة له
حين تتفاوت الكمّيةُ 2.5× بين السنوات — وقد نشرتُ «لا حكم» على إسقاطٍ كهذا، ونقضه
القياسُ في عشرين دقيقة.** ⇒ اختيارُ المالك «قِس السنتين» كان الصحيح.

🔑 **و`P6` مكذَّبٌ في السنوات الثلاث لا في واحدة: `fallback` = صفرٌ من 257.** ⇒ حكمُ
«كتمُ القروب ميّتٌ بنيويًّا» صار مقيسًا على **‏257 اشتعالًا**: توزيعُ `F2` المجمَّع
**`group` 126 (‏49.0%)** · `mid` 43 · `operator` 47 · `strong` 41 ⇒ **‏126 اشتعالًا
شمعتُها قروبٌ (‏≤50 ألف دولار) ووصلت كلُّها المالكَ في ثلاث سنوات، وصفرٌ كُتم.**

⚠️⚠️ **وإعلانٌ إحصائيٌّ يُكتَب الآن قبل أيّ رقمِ حكم (فلا يُقرأ لاحقًا اعتذارًا):
بلوغُ الأرضية ليس قوّةً إحصائية.** شريحةُ المكتوم **‏17 · 13 · 10** سنويًّا ⇒ فاصلُ
Wilson لنسبةٍ عند `n=17` عرضُه ‏≈±23 نقطة ⇒ **الشرطُ ②(‏≥10 نقاط في كلّ سنة) مع
③(فاصلان منفصلان) يستلزم فرقًا هائلًا (‏~25-30 نقطة)** لكي يُعتمَد ⇒ **الاحتمالُ
الأرجحُ سلفًا: «لا حكم» أو «فشلت» لأسبابٍ إحصائية**، والمعيارُ لا يُرخى بحرف.

---

*الحكمُ يُكتَب في `opfire_result.md` **مرّةً واحدة** عند اكتمال السنوات الثلاث — ولا
يُعاد التشغيلُ حتى تعجب النتيجة.*
