# 🔬🔥 T-OPFIRE — تسجيلٌ مسبق: هل بصمةُ المضارب في الصفقات تفصل الاشتعالَ الحقيقيّ عن الكاذب؟

> **مدفوعٌ قبل أيّ رقم.** أمرُ المالك 2026-08-11 («سجّل التجربة») بعد أن فُتح وصولُ
> الملفّات المجمَّعة (`flatfiles_access_verdict.md`) وثبت أن **تاريخ الصفقات من 2003**.
> **لا تُشغَّل ولا يُكتَب رقمٌ واحد قبل موافقة المالك على هذا الملفّ.**
> الأداةُ المزمَعة: `opfire_scan.py` + `.github/workflows/opfire.yml` · النتيجة:
> `opfire_result.md`.

---

## ① السؤال — ومحورُه الجديد

**السؤال:** في **لحظة اشتعالٍ** يرصدها كاشفُ الإنتاج (`_ignition_signal`)، هل بصمةُ
المضارب في **الصفقات الخام** لتلك الدقيقة وما قبلها تفصل:

- **`real`** — بلغ ‏≥`IGNITION_CONFIRM_PCT`(=12%) صعودًا بعد الاشتعال، عن
- **`fakeout`** — أغلق تحت مستوى الكسر قبل بلوغ التأكيد،

بتعريفَي `_ignition_outcome` الإنتاجيَّين **حرفيًّا**؟

### 🔴 ولماذا هذا **ليس** إعادةَ تشغيلٍ لملفٍّ مُغلَق — بثلاثة فوارقَ مكتوبة

**(أ) مقابل `T-ACC` (مُغلَق 2026-07-08):** ذاك سأل سؤالَ **اختيار** — «هل التجميعُ
**عند القاع قبل الإشارة** يتنبّأ بانفجارٍ لاحق؟» — وأُغلق بنصّه: «أُغلق ملف
**«التجميع/الصفقات كمؤشّر اختيار»**». وهذا سؤالُ **تأكيدٍ وتوقيت** على حدثٍ **قد بدأ
فعلًا**. والتمييزُ **منصوصٌ في المستودع قبل هذا التسجيل**، في تأسيس رادار الانطلاق:
«**فشل T-ACC لا ينطبق: الحجم/السعر واقعان، التدفق يؤكّد لا يتنبّأ**» (`CLAUDE.md`).
فهذا التسجيلُ **يُشغّل** تمييزًا قائمًا لا يخترعه.

**(ب) مقابل `E-OPERATOR` (ذراعٌ ثانوية في `T-EVENT-EXEC`):** ⚠️ **وهذا أقربُ ما يُقال
عنه «مكرَّر»، فيُواجَه صريحًا — وتحقّقي أخرج ما هو أقوى.** تلك الذراعُ قاست **توقّعَ
العائد (R)** للدخول على الاشتعال المشروط بالمضارب فأعطت ‏**−0.168R «لا تُنقذ»**.
🔴🔴 **لكنّ ذلك الرقمَ مسحوبٌ ولم يُعَد قياسُه قطّ:** `event_exec_result.md` (‏السطر 28)
يسحب صريحًا أرقامَ `E-REAL`/`E-PSEUDO`/`E-CROSS`/**`E-OPERATOR`** بنصّ «**ما قِيس ليس
ما سُجِّل**» (أفقُ حسمٍ خاطئ = يومٌ واحد)، **والملفُّ المصحَّح
`event_exec_result_corrected.md` يحوي `E-REAL` و`E-PSEUDO` فقط — و`E-OPERATOR` غائبٌ
بصفر ذِكر** (‏مُتحقَّقٌ بالعدّ: صفر مطابقة).
⇒ **بوّابةُ الكتم النافذةُ في الإنتاج الآن بلا أيّ قياسٍ صالحٍ على الإطلاق** — لا
بمقياس R ولا بغيره. وهذا **يُقوّي** مشروعيةَ هذي التجربة ولا يُضعفها.
**والمقياسُ هنا مختلفٌ أصلًا: نسبةُ الإنذار الكاذب** — وهي التي وُعِد بها الرادارُ
نفسُه، **ويُصرّح الكودُ الإنتاجيّ بأنها غيرُ قابلةٍ للقياس إلّا حيًّا**: «الالتقاط
ونسبةُ الإنذار الكاذب **لا تُقاسان إلّا حيًّا**» (`Super_stock.py`، ترويسة حلقة القياس)
⇒ **والجديدُ أن الملفّاتَ تُبطل هذي الجملة** فتجعلها قابلةً للقياس تاريخيًّا لأوّل مرّة.
✅ **ووعدُ إعادة بناء مستوى الكسر ليس سرابًا:** `session_levels` **موجودةٌ فعلًا**
(`event_exec_run.py:117`) **ومقفولةٌ باختبار** (`test_bot.py:13830`).

**(ج) والآلةُ نفسُها مختلفة — وهذا الفارقُ الأثقل:** تدقيقُ اليوم أخرج **خمسةَ عيوبِ
قياسٍ في آلة `T-ACC`** (‏§⑬ أدناه)، أشدُّها أن اختبارَ الفصل كان **أحاديَّ الطرف
بنيويًّا** فيستحيل عليه أن يرى علاقةً **عكسية**، وأن جالبَ الصفقات **يقتطع صامتًا عند
‏150 ألف صفقة بترتيبٍ تصاعديّ** فيقيس أقدمَ الصفقات لا كلَّها.
⚖️ **وحدُّ صدقٍ صريح: هذي العيوبُ لا تعني أن الأثر موجود** — تعني أن الاختبارَ السابق
كان **أضعفَ مما ادّعى**، فيَحِلُّ إعادةُ الفتح **بآلةٍ مُصحَّحة على محورٍ آخر**، ولا يَحِلُّ
إعادةُ السؤال نفسِه. **وحكمُ `T-ACC` على سؤاله يبقى قائمًا.**

### 🎯 وقيمةُ القرار — الطرفان مُفيدان

بوّابةُ المضارب **نافذةٌ في الإنتاج الآن**: `scan_ignition` **لا تُطلق** «🔥 ادخل الآن»
بلا `has_operator`، وشمعةُ ‏≤`IGNITION_USD_GROUP`(=50 ألف دولار) **تُكتَم**. وهذي
البوّابةُ **لم تُصادَق قطّ**: السجلُّ الحيّ **‏26 إطلاقًا** (‏18 `group` · 4 `mid` ·
4 `operator`) وحدُّ العرض `IGNITION_OUTCOME_MIN`=8 محسومة ⇒ **نكتم تنبيهاتٍ بقاعدةٍ
بلا دليل**.

- **نجحت** ⇒ البوّابةُ مُستحقّةٌ وقابلةٌ للمعايرة بالرقم (اقتراحٌ للمالك).
- **فشلت** ⇒ **نكتم بلا مقابل** ⇒ يُعرَض على المالك **رفعُ الكتم** (قرارُه حصرًا).

---

## ② المجتمع والتعريفات — بدوالّ الإنتاج بأسمائها

1. **الكونُ point-in-time:** لقطةُ `pit_universe.py` لكلّ سنة (‏`active=true&date=D`)
   ⇒ **تشمل المشطوبين** (يعالج انحيازَ البقاء الذي عمي عنه `T-ACC`: كونُه
   `nasdaqlisted.txt` **اليوم**).
2. **شموعُ الدقيقة تُبنى من الصفقات نفسِها** (‏لا مصدرَ ثانٍ): تجميعُ
   (سعر، حجم) لكلّ دقيقة من `us_stocks_sip/trades_v1` ⇒ **يشمل ما قبل السوق وبعده**
   (يعالج «عمى الافتر»، المصيبة 8).
3. **مستوى الكسر** `_ignition_break_level` **الإنتاجيّ** على حالةٍ مُعادةِ البناء
   لتلك الجلسة بنمط `T-EVENT-EXEC` (`session_levels`: `build_interpretation`
   بإغلاق **الجلسة السابقة** حصرًا). **ولا رقمَ جديد**: عند غياب الرقم الحرج يعمل
   الاحتياطُ الإنتاجيّ (`الأرضية × 1.05`) — ويُعَدّ عدَدُ الحالتين ويُطبَع.
4. **الاشتعال** = `_ignition_signal(bars, break_level, vol_mult=CONFIG["IGNITION_VOL_MULT"])`
   ‏**بدالّة الإنتاج نفسِها بلا نسخة**، على نافذةٍ متحرّكة.
5. **بصمةُ المضارب** = `_operator_blocks(trades, CONFIG["OPERATOR_MIN_SHARES"])`
   و`acc_components(trades)` **الإنتاجيّتان**، على صفقات **الدقيقة المشتعلة والدقائق
   الخمس السابقة** (النافذةُ مثبَّتةٌ هنا ولا تُغيَّر بعد الأرقام).
6. **الحكم** = `_ignition_outcome(fire, df)` **الإنتاجيّة** بـ`confirm_pct`
   الافتراضيّ ⇒ `real` / `fakeout` / `pending`. و**`pending` تُستبعَد من المقام
   ويُعلَن عددُها**.

---

## ③ المقياسُ الحاكم — مثبَّتٌ قبل أيّ رقم

> **`FAKE(g)` = نسبةُ `fakeout` من المحسوم (‏`real`+`fakeout`) داخل الشريحة `g`.**
> والفرقُ المحكوم: **`FAKE(بلا مضارب) − FAKE(بمضارب)`** بالنقاط المئوية.

- **الشريحة تُحدَّد ببوّابة الإنتاج حرفيًّا:** `bool(_operator_blocks(...)["has_operator"])`.
- **ومقياسٌ مساندٌ واحدٌ فقط** (يُعلَن ولا يحكم): نسبةُ `real` — لكشف أن البوّابة قد
  تُقلّل الكاذبَ **وتُقلّل الحقيقيَّ معه** (كتمٌ بلا ربح).
- 🔒 **ولا مقياسَ ثالث، ولا مقياسٌ يُضاف بعد رؤية الأرقام.**
- 🔒 **ولا `R`**: `T-EVENT-EXEC` قاسه وحكمَ؛ إعادتُه هنا خلطُ ملفَّين.

---

## ④ الأذرع — مثبَّتةٌ أربعًا لا خامسة

| الذراع | التعريف |
|---|---|
| **`F0`** | كلُّ الاشتعالات (الأساس، بلا شريحة) — **المقام** |
| **`F1`** | شريحةُ `has_operator=True` مقابل `False` (**بوّابةُ الإنتاج**) |
| **`F2`** | شريحةُ `_ignition_candle_class` الثلاثية (`group` ≤50 ألف · `mid` · `operator`/`strong` ≥100 ألف) — **بعتبات الإنتاج بلا تغيير** |
| **`F3`** | **شاهدُ ضبطٍ عشوائيّ**: لكلّ اشتعالٍ دقيقةٌ **غيرُ مشتعلة** من **نفس الرمز ونفس الجلسة** تُنسَب لها الشريحةُ نفسُها ⇒ لو ظهر «فصلٌ» في `F3` أيضًا فالفصلُ **أثرُ آلةٍ لا أثرُ اشتعال** |

🔒 **إضافةُ ذراعٍ خامسة بعد الأرقام = `p-hacking` ممنوع بنصّ هذا السطر.**

---

## ⑤ المعيار — حاسمٌ غيرُ قابلٍ للتفاوض بعديًّا

تُعتمَد النتيجةُ **فقط** إذا تحقّقت **الخمسةُ معًا**:

1. **ثلاثُ سنواتٍ كاملة** (‏2023 · 2024 · 2025) — ولا حكمَ من سنتين.
2. **الفرقُ ‏≥10 نقاط مئوية** في **كلّ** سنة.
3. **فاصلا Wilson ‏95% منفصلان** — **واختبارٌ ثنائيُّ الطرف** (‏`lo_a > hi_b` **أو**
   `lo_b > hi_a`) 🔴 **تصحيحًا صريحًا لعيب `T-ACC` الأحاديّ الطرف** الذي كان يستحيل
   عليه أن يرى علاقةً عكسية.
4. **لا انقلابَ إشارة** بين السنوات الثلاث.
5. **‏≥150 اشتعالًا محسومًا مجمَّعًا و≥40 في كلّ سنة**، **و≥25 في كلّ شريحة** من
   شريحتَي `F1`.

**وإن سقط أيُّ شرط: يُبلَّغ الرقمُ بلا توصية.** و**«فشلت الفرضية» ≠ «العيّنة لا تكفي»**
— ويُصرَّح بأيِّهما وقع.
🔴 **وشرطٌ سالبٌ حاكم:** إن ظهر الفصلُ في **`F3` (شاهد الضبط)** بمقدار **‏≥نصف** ما ظهر
في `F1` ⇒ **تُلغى النتيجةُ كلُّها** ولا تُفسَّر (أثرُ آلة).

---

## ⑥ بوّاباتُ الصلاحية — تكشف الـ`no-op` وتسقط بصوتٍ عالٍ

المستودعُ احترقَ بهذا مرارًا (‏علمٌ ميّت `BT_CANDLE` · سياقُ تقسيماتٍ غائب ·
خطّافٌ في المسار الخطأ · مُخرَجٌ مخزَّنٌ ضاع بالقتل · رقمُ عيّنةٍ قُرئ رقمَ يوم) ⇒
**كلُّ واحدةٍ من هذي تُوقِف التشغيلةَ بخروجٍ غيرِ صفريّ:**

- **`V1` شاهدُ الضبط:** رمزٌ سائلٌ معروف (`AAPL`) يجب أن يُخرج سماتٍ غيرَ تافهة في
  كلّ يومٍ يُقرأ — **والصفرُ عطبُ أداةٍ حتى يُنفى** (درسُ PIT).
- **`V2` الدالّةُ الإنتاجية تُنادى فعلًا:** قفلٌ **نحويٌّ (AST)** يُثبت أن
  `_ignition_signal` و`_operator_blocks` و`_ignition_outcome` **تُنادى بالاسم** من
  الأداة (لا نسخةَ منطق) — **ونصٌّ ممنوع** (القفلُ النصّيّ يسقط على التعليق).
- **`V3` العلمُ فعّال:** يُطبَع «قرأ صفقاتِ N رمزًا × M جلسة»، **وصفرٌ ⇒ خروج غيرُ
  صفريّ** بنصّ «‏no-op لا تُفسَّر نتيجتُها».
- **`V4` عدّاداتٌ لا تكذب:** يُطبَع للمجموع: الاشتعالات · `real` · `fakeout` ·
  `pending` · وحالتا مستوى الكسر (حرجٌ مقابل احتياط) · وكلُّ قصٍّ **بعدَده**.
- **`V5` بلا تسريبٍ مستقبليّ — والشرطُ صريحٌ بالحقل:** كلُّ صفقةٍ تدخل حسابَ البصمة
  يجب أن يكون `sip_timestamp` لها **أصغرَ من** نهاية دقيقة الاشتعال (`<` لا `<=`)،
  و`_ignition_outcome` تعمل على شموعٍ **بعد يوم الاشتعال حصرًا** (‏`d <= fdate` تُتخطّى
  — سلوكُ الإنتاج نفسُه).
- **`V6` طباعةٌ حيّة:** `PYTHONUNBUFFERED=1` ⇒ تشغيلةٌ تُقتَل لا تضيع بكاملها
  (درسُ اليوم: ‏11 دقيقةً ضاعت).
- **`V7` معامِلُ التوسيع مطبوعٌ** مع كلّ إسقاطِ كلفةٍ من عيّنة (درسُ اليوم: 240ث
  قُرئت يومًا كاملًا وهي ‏8% منه).
- **`V8` الترويسةُ الحقيقيةُ تُطبَع أوّلَ شيء — ولا يُفترَض عمودٌ.** 🔴 **حدُّ صدقٍ
  أُعلنه قبل الأرقام:** أداةُ المِجَسّ الحالية تقرأ **ثلاثةَ أعمدةٍ فقط**
  (`REQ_COLS = ("ticker","price","size")` ‏+ `exchange` اختياريًّا،
  `flatfiles_probe.py:140`) ⇒ **الطابعُ الزمنيّ (`sip_timestamp`) لا يُقرأ اليوم
  إطلاقًا**، ووجودُه في الملفّ **لم أتحقّق منه** (التشغيلةُ الناجحة لم تطبع الترويسة).
  ⇒ **`V5` (منعُ التسريب) غيرُ قابلٍ للتنفيذ حتى يُوسَّع القارئ ويُثبَت العمود** ⇒
  **`V8` بوّابةٌ حاكمة: تُطبَع الترويسةُ كاملةً، وغيابُ عمودٍ زمنيٍّ صالحٍ ⇒ خروجٌ غيرُ
  صفريّ و«لا تجربة»** — لا تقديرٌ ولا افتراضُ ترتيبِ صفوف.

---

## ⑦ سقفُ النجاح — مثبَّتٌ قبل الأرقام

- **الأقصى المسموح عند النجاح: اقتراحٌ للمالك بمعايرة عتبةٍ قائمة** (`IGNITION_USD_*`
  أو `OPERATOR_MIN_SHARES`) **+ سطرُ عرضٍ في التنبيه**. ولا شيءَ غيرَ ذلك.
- 🔒 **ممنوعٌ بنصّ هذا التسجيل:** أيُّ مسٍّ بـ`M1`-`M14` أو `rank_key`/`select_top`/
  `classify_tier`/`analyze_ticker`/`entry_status` · أيُّ تغييرٍ في الدخول أو الوقف أو
  الأهداف · أيُّ وزنِ ترتيب · وأيُّ رقمٍ يُخترَع (عتباتُ فيصل من الكاتالوج حصرًا).
- **وعند النجاح لا يُنفَّذ شيءٌ تلقائيًّا:** `OPERATOR_MIN_SHARES`=1000 **رقمُ فيصل
  الموثَّق** وتغييرُه قرارُ مالك.

---

## ⑧ التنبّؤات — تُنشَر لتُكذَّب

- **`P1`** المطبوعُ الأكثر: `group` سيكون **أغلبَ** الاشتعالات (السجلُّ الحيّ 18 من 26)
  ⇒ لو خرج `operator` أغلبَ التاريخ فالسجلُّ الحيُّ منحازٌ بصِغَره.
- **`P2`** الفرقُ في `F1` **سيقع بين ‏0 و10 نقاط** (أي **يفشل** المعيار) — بناءً على
  ثمانيةِ اختباراتٍ ساقطةٍ سابقةٍ على الصفقات و`E-OPERATOR` ‏−0.168R.
- **`P3`** `pending` ستكون **كبيرةً ماديًّا** (‏>20%) لأن كثيرًا من الاشتعالات لا
  يُحسم داخل النافذة.
- **`P4`** حالةُ **الاحتياط** في مستوى الكسر ستغلب حالةَ الرقم الحرج تاريخيًّا.
- **`P5`** ضمُّ ما قبل السوق وبعده سيزيد الاشتعالات **‏≥20%** عن نظيرٍ نظاميّ-فقط.

---

## ⑨ حدودُ الصدق — تُقرأ مع النتيجة لا بعدها

1. **الملفّاتُ بلا `NBBO`/طلبٍ وعرض** ⇒ قرائنُ «تفريغ العروض» و«العرضُ الفارغ» (N5)
   **غيرُ مقيسة**، و«قابليةُ التنفيذ» **لا تُقاس** هنا (وهي مأخذُ `T-EVENT-EXEC`).
2. **ولا فلوت ولا شورت FINRA ولا متاحَ اقتراض ولا قروبات ولا إيداعاتِ SEC** ⇒ لا
   ضبطَ لهذي المتغيّرات.
3. **بناءُ شموع الدقيقة من الصفقات** قد يخالف شموعَ المزوّد بحدود التقريب ⇒ **يُقاس
   الفرقُ على يومٍ واحدٍ ويُطبَع** (لا يُفترَض صفرًا).
4. **التقسيماتُ في الملفّات خامٌ غيرُ معدَّل** بينما شموعُنا اليومية معدَّلة ⇒ الحكم
   (`_ignition_outcome`) على مقياسٍ والبصمةُ على آخر ⇒ **يُستبعَد كلُّ رمزٍ له تقسيمٌ
   داخل نافذة الحكم**، ويُعَدّ المستبعَدون.
5. **الكونُ PIT يعالج البقاءَ ولا يمحوه** (مُدرَجو أثناء السنة).
6. **النتيجةُ عن مجتمع الاشتعالات لا عن السوق** — ولا تُعمَّم.

---

## ⑩ الكلفة والتجزئة — بالحساب لا بالتقدير

المقيسُ اليوم (`flatfiles_access_verdict.md`): يومُ صفقاتٍ **‏3,201MB** · تنزيلُ عيّنةٍ
‏256MB في **‏197ث** (‏1.3MB/ث · **أرضيّةٌ لا سقف**: مدًى واحدٌ بخيطٍ واحد و`s3 cp`
متوازٍ) · اختزالُ **‏11.2 مليون صفقة في 43ث**.
⇒ **يومٌ كامل ‏≈50 دقيقة** · **سنة ‏≈210 ساعة** · وبالتوازي 8 ‏**≈26 ساعة**.
**وسقفُ جوب GitHub ‏6 ساعات** ⇒ **تجزئةٌ إلزامية**: جوبٌ لكلّ شهرٍ (‏≈18 ساعة/شهر
تسلسليًّا ⇒ يُقسَّم أسابيعَ)، **ومصفوفةٌ متوازية**، **وكلُّ جوبٍ يرفع مُخرَجَه
`artifact` قبل أيّ دفع** (درسُ ‏`ignition.yml`: الدفعُ العاري أضاع عشرَ جلسات).
⚠️ **ولا يُشغَّل السوقُ الكامل قبل يومٍ واحدٍ ناجحٍ ببوّاباته السبع.**

---

## ⑪ الأداةُ المزمَعة — عقدُها قبل كتابتها

- `opfire_scan.py`: **قارئٌ محض** · لا يكتب حالةً · **لا يُستورَد في `Super_stock`** ·
  يستورد دوالَّ الإنتاج ولا ينسخها.
- تنزيلٌ ⟶ اختزالٌ ⟶ **إسقاطُ الخام** (المساحةُ لا تتراكم).
- خروجٌ غيرُ صفريّ عند سقوط أيّ بوّابةِ صلاحية.
- أقفالٌ في `test_bot.py` بأسماءٍ (`OPF1`…) **وطفراتٌ تُثبت سقوطَ كلٍّ منها**.

---

## ⑫ ما لا تحلّه هذي التجربةُ ولو نجحت

لا تقول شيئًا عن **الاختيار** (‏M1-M14) ولا عن **الترتيب** ولا عن **الربحية**
(`T-EVENT-EXEC` حكمَ فيها) ولا عن **المتاح للاقتراض** (‏`T-BORROW-FWD` حصادٌ أماميّ).
**وأقصى ما تقوله: هل الكتمُ بالمضارب يستحقّ أن يبقى.**

---

## ⑬ ملحقٌ — خمسةُ عيوبِ قياسٍ في آلة `T-ACC` (كُشفت اليوم · تُدوَّن ولا تُطوى)

يُدوَّن **لأن حكمَ `T-ACC` ما زال يُستشهَد به**، ومن حقّ القارئ أن يعرف حدَّ قوّته:

1. 🔴 **اختبارُ الفصل أحاديُّ الطرف بنيويًّا:** `sep = rates[-1][1] > rates[0][2]`
   ⇒ **يستحيل** عليه وسمُ علاقةٍ **عكسية** بالفصل، فكلُّ الفروق السالبة (‏−7 · −4 ·
   −14 · −6) وُسمت «متداخلة» **بالبناء لا بالقياس**.
2. 🔴 **اقتطاعٌ صامتٌ عند 150 ألف صفقة** بترتيبٍ **تصاعديّ** ⇒ للرموز السائلة تُقاس
   **أقدمُ** الصفقات لا كلُّها، بلا وسمِ اقتطاع.
3. 🔴 **شرطُ السنتين غيرُ مُنفَّذٍ في الكود** — قراءةٌ يدويّة من تقريرَين.
4. 🔴 **انحيازُ بقاءٍ غيرُ مُعلَن** (كونُ `nasdaqlisted.txt` **اليوم**)،
   **و`docstring` الأداة يدّعي العكسَ حرفيًّا: «السوق الكامل بلا انحياز»**.
5. 🔴 **ذراعُ 2026 سنةٌ ناقصة** (المشيُ يتوقّف 40 شمعةً قبل نهاية البيانات ⇒ إشاراتٌ
   حتى ‏~مايو) بينما الـworkflow يسمّيها «السنة المكتملة».
   **+ وسمُ `exploded` يحتسب صعودًا وقع بعد ضرب الوقف** ⇒ ليس ربحًا قابلًا للتنفيذ.

⚖️ **ولا يُبنى على هذا أن الأثرَ موجود** — بل أن الاختبارَ كان أضعفَ مما ادّعى،
**وأن أيَّ استشهادٍ بـ`T-ACC` بعد اليوم يُقرَن بهذي الحدود.**

---

*الحكمُ يُكتَب في `opfire_result.md` **مرّةً واحدة** عند اكتمال السنوات الثلاث — ولا
يُعاد التشغيلُ حتى تعجب النتيجة.*
