# ⏱️🔥 `T-SECONDS` — تسجيلٌ مسبق: **«إذا دخل المضارب بثوانٍ معدودة يتجاوز السعر»**

> **أمرُ المالك (2026-09-20): «سجل التسوية و نفذ جميع النواقص معك الصلاحية
> كاملة تنفذ كل شي لا تجيني إلا وانت مخلص كل شي».**
> **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · ولا يُشحَن شيءٌ
> مهما كانت النتيجة.** مدفوعٌ ومدموجٌ **قبل أيّ سطرِ أداةٍ وقبل أيّ رقم**.

---

## §⓪ الدعوى ومصدرُها — حرفيًّا لا مُستنتَجًا

**الكاتالوج `L322`-`L326`** (‏صور 9424/9425/9430):
> «**اذا دخل المضارب بثواني معدوده يتجاوز السعر · ‏6 هي مقاومه سعريه**»
> «**شمعة المضارب بثواني تشوف السهم متعدي 50٪ · سيولة الشمعه عاليه جدا من
> ‏100 [ألف] دولار**»

الوسم **`faisal_verbatim`** · **والقراءةُ التشغيليّةُ `faisal_inferred`**
ومكتوبةٌ هنا صراحةً: **«ثوانٍ معدودة» = سرعةُ عبور المقاومة، لا حجمُها** ⇒
المقيسُ **زمنٌ** بين آخر صفقةٍ تحت المستوى وأوّلِ صفقةٍ فوقه.

### 🔑 والمحورُ جديدٌ لأن المبنيَّ من هذي الصور نفسِها **هو الدولاراتُ لا السرعة**
`IGNITION_USD_OPERATOR` = ‏100,000 · `_STRONG` = 300,000 · `_GROUP` = 50,000 —
**كلُّها من الجملة الثانية** (‏«سيولة الشمعه … من 100 ألف دولار»)، **والجملةُ
الأولى («بثواني معدوده») لم تُقَس قطّ**: `_ignition_signal` يقرأ **شموعَ
الدقيقة** فهو **أعمى عن الثواني بالبناء**.

---

## §① الحقائقُ المقيسةُ قبل أيّ تصميم — **وواحدةٌ منها تُصحّح الحزمة**

مقروءةً من `ignition_log.json` **اليوم** (‏لا من الذاكرة):
1. **‏96 إطلاقًا** · المدى **‏2026-07-09 ⟶ 09-18**.
2. الأصناف: **`group` 73 · `mid` 12 · `operator` 8 · `strong` 3**.
3. **`fired_ts_ms` غيرُ محفوظ** — صفرٌ من 96. ✅ (كما وصفت الحزمة)
4. 🔴🔴 **وتصحيحٌ للحزمة يُنشَر: حقلُ `fired_at` موجودٌ في ‏95 صفًّا ولا يصلح**
   — فهو **لحظةُ كتابة السجلّ لا لحظةُ الإطلاق**: مقيسٌ أن صفوفَ اليوم الواحد
   تتجمّع عند **‏20:00-20:01 UTC** (نهايةُ الجلسة، حين يكتب الـassembler)
   **بوسيطِ مدًى داخلَ اليوم = ثانيةٌ واحدة** على **‏28 يومًا** متعدّدَ الصفوف.
   ⇒ **عقدٌ يبني على `fired_at` يقيس شيئًا آخر تمامًا** — ولذلك يُقاس **أوّلُ
   عبورٍ للمستوى في اليوم** لا لحظةَ الإطلاق، **ويُعلَن هذا نقلَ تعريف**.
5. **`polygon_base_trades` تُرجع `price`/`size`/`exchange` فقط** — **بلا طابعٍ
   زمنيّ** ⇒ لا تصلح، **ولا تُعدَّل** (تُنادى من مسارات الإنتاج).

---

## §② المبنيُّ لهذي التجربة — جالبٌ **جديد** لا تعديلُ قائم

**`polygon_trades_ts(sym, day)`** — يُرجع `[(sip_ts_ns, price, size), …]` ليومٍ
واحدٍ بترقيم صفحات. **قراءةٌ فقط · فاشلٌ-آمن مطلق** (بلا مفتاح/401/403/429/
شبكة ⇒ `None` فورًا) · **وسقفُ صفحاتٍ مُعلَن** · **ولا يُنادى من أيّ مسارِ
إنتاج** (‏يُقفَل بالـAST: صفرُ مرجعٍ له خارج أداة التجربة).

🔒 **و`polygon_base_trades` لا تُمَسّ بحرف** (‏`V-N7`).

---

## §③ التعريفُ المُقاس — واحدٌ ومكتوبٌ بحرفه

لكلّ إطلاقٍ `(sym, date, break_level = L)`:
1. تُجلَب صفقاتُ **اليوم النظاميّ** (‏09:30-16:00 ET) بطوابعها.
2. **أوّلُ عبور** = أوّلُ صفقةٍ سعرُها **‏≥ L × 1.005** ويسبقها في اليوم صفقةٌ
   **‏≤ L × 0.995**.
3. **`t_cross`** = الثواني بين **آخر صفقةٍ ‏≤ L × 0.995 قبله** وتلك الصفقة.
4. **‏0.5% `engineering`** مُعلَن · و**‏10 ثوانٍ `engineering`** («معدودة»)،
   وحساسيّتُها **‏{5, 10, 30, 60}** **وصفيّةٌ تُطبَع كاملةً ولا يُنتقى منها**.

🔴 **وحدُّ صدقٍ في قلب التعريف (‏§①-4):** **أوّلُ عبورٍ في اليوم ≠ لحظةُ
الإطلاق** — قد يسبقه بساعات. **نقلُ تعريفٍ مُعلَن**، وأثرُه معروفُ الاتّجاه:
**يُدخِل عبورًا مبكّرًا هادئًا** فيُبطئ `t_cross` ⇒ **ينحاز ضدّ فرضيّة
«السرعة»** — أي أن النتيجةَ الموجبة رغمَه أقوى، والسالبةَ **غيرُ حاسمة**.

---

## §④ الأذرع والضبطُ الحاكم

| الذراع | التعريف |
|---|---|
| **`fast`** 🥇 | `t_cross ≤ 10` ثوانٍ |
| **`slow`** 🥇 | `t_cross` أكبر |
| **`C-USD`** 🥇 | **ضبطٌ حاكم** — الفرقُ نفسُه **داخلَ صنف الدولارات الواحد** |
| `S-SENS` | حساسيّةُ العتبة ‏{5, 30, 60} — **وصفيّةٌ لا تحكم** |

⚠️ **و`C-USD` حاكمٌ لا وصفيّ — بدرسِ `T-PMGATE` بحرفه:** الصنفُ الدولاريُّ
**مشحونٌ ومعروضٌ اليوم**، والسؤالُ النافعُ الوحيد: **هل تُضيف السرعةُ على ما
نعرضه أصلًا؟** — فإن كان `fast` مجرّدَ مرادفٍ لـ`operator/strong` فهو **إعادةُ
قياسِ المعروف**.

## §⑤ المقياسُ والمعايير

- الحاكم: **نسبةُ `real`** بتعريف الإنتاج `IGNITION_CONFIRM_PCT` = ‏12%
  (‏`_ignition_outcome` **بالاسم**، لا إعادةُ بناء).
- **`SC1`** ‏`real(fast) − real(slow)` **‏≥ +15 نقطة** وفاصلا Wilson منفصلان.
- **`SC2`** الفرقُ يبقى **‏≥ +10 نقاط** داخلَ صنفٍ دولاريٍّ واحدٍ على الأقلّ
  (‏`C-USD`) — **وإلّا فالسرعةُ وجهٌ آخرُ للدولارات لا معلومةٌ زائدة**.

## §⑥ فروعُ الحكم — ثلاثةٌ · ولا فرعَ رابع
1. **الفرعُ 1 — «تُوصى»:** `SC1` **و**`SC2` ✅ ⇒ **اقتراحٌ على المالك لا
   تنفيذ**، **ولا يُشحَن شيءٌ بهذا العقد**.
2. **الفرعُ 2 — «لا تُوصى»:** سقوطُ أيٍّ منهما.
3. **الفرعُ 3 — «لا حكم»:** **دون ‏30 إطلاقًا محسومًا في أيّ ذراع** · أو
   تعذّرَ الجلبُ لأكثرَ من **‏20%** من الأيّام — **يُعلَن بعدده ومعه معدّلُ
   التراكم المقيس**، ولا يُفسَّر.

⚠️ **والفرعُ 3 مرجَّحٌ الآن ويُعلَن قبل أيّ رقم:** ‏96 إطلاقًا **كلُّها بحاجةٍ
لحسمٍ ولجلبِ صفقاتِ يومها**، و`group` وحدَه ‏73 منها ⇒ **`fast` قد لا يبلغ
‏30**. **ومعدّلُ التراكم بذاته معلومةٌ تُنشَر** (‏96 إطلاقًا في ‏71 يومًا
تقويميًّا ⇒ **‏≈1.35 إطلاقًا/يوم**).

---

## §⑦ حرّاسٌ تُطبَع في كلّ تقرير — والسقوطُ يوقف

- **`V-N1`** عددُ الإطلاقات وأصنافُها **بت-بت** مع `ignition_log.json`
  (‏96 · 73/12/8/3) — وإلّا **خروج 5** (سجلٌّ تبدّل ⇒ مجتمعٌ آخر).
- **`V-N2`** نسبةُ الأيّام التي نجح جلبُها **تُطبَع**، ودون ‏80% ⇒ **الفرعُ 3**.
- **`V-N3`** `_ignition_outcome` **بالاسم من الإنتاج** · و`IGNITION_CONFIRM_PCT`
  تُقرأ من `CONFIG` **ولا تُكتَب بيدٍ**.
- **`V-N4`** `t_cross` **لا يُحسَب من `fired_at`** — يُقفَل بالـAST: صفرُ
  قراءةٍ للحقل في مسار الحساب (‏§①-4).
- **`V-N5`** الجالبُ الجديد **صفرُ مرجعٍ له في `Super_stock.py`** وفي كلّ
  مسارِ إنتاج (‏AST) — وإلّا **خروج 5**.
- **`V-N6`** الأداةُ **لا ترسل ولا تكتب حالةً** · **وبلا سرِّ تلغرام** في
  الـworkflow.
- **`V-N7`** الجذورُ الاثنا عشر **بصمتُها مطابقة** · **و`polygon_base_trades`
  بت-بت**.

## §⑧ ما لا يُمَسّ

- ⛔ الجذورُ الاثنا عشر · `pivot_stability` · `_ignition_signal` ·
  `scan_ignition` · وعتباتُ الدولارات الثلاث — **كلُّها بت-بت**.
- ⛔ **لا `LOGIC_VERSION`** ولا شحنَ حقلٍ أو سطرِ عرض مهما كانت النتيجة.
- ⛔ **ولا تُعايَر ‏0.5% ولا ‏10 ثوانٍ ولا ‏15/10 نقطةً بعد رؤية أيّ رقم.**
- 💡 **ويُقترَح ولا يُنفَّذ** (‏سطرُ تسجيلٍ لا يمسّ قرارًا، **بإذن المالك
  وحدَه**): حفظُ `fired_ts_ms` في `record_ignition_fires` حتى تُقاس السرعةُ
  **عند لحظة الإطلاق** لا عند أوّل عبور — **وهو ما يرفع حدَّ الصدق §③**.

## §⑨ تنبّؤاتي — تُنشَر مكذَّبةً أو مؤكَّدة

| # | التنبّؤ | ثقتي |
|---|---|---|
| `NP1` | **الفرعُ 3 «لا حكم»** بسقوط الأرضيّة | **عالية** |
| `NP2` | العبورُ السريع **يتركّز في `operator`/`strong`** ⇒ `SC2` يسقط ولو عبر `SC1` | عالية |
| `NP3` | وسيطُ `t_cross` **فوق ‏60 ثانية** بسبب «أوّل عبورٍ في اليوم» (‏§③) | متوسّطة |
| `NP4` | نسبةُ الأيّام التي ينجح جلبُها **فوق ‏90%** (المفتاحُ يغطّي المدى) | متوسّطة |
| `NP5` | `real` الإجماليّ **دون ‏35%** (‏`group` ثلاثةُ أرباع المجتمع) | متوسّطة |

---

## §⑩ ملحقٌ مؤرَّخ ‏2026-09-23 — **«لحظة الإطلاق»: الإذنُ صدر والحفظُ نُفِّذ**

> **العقدُ أعلاه مدموجٌ ولا يُعدَّل** — وهذا ملحقٌ مؤرَّخٌ يُقرأ معه.
> **ويُكتَب بعد صدور الحكم**، ويُعلَن ذلك: **لا يغيّر عتبةً ولا معيارًا ولا فرعًا
> ولا رقمًا منشورًا ولا الحكمَ** — الفرعُ 2 «لا تُوصى» باقٍ بحرفه.

### ⓐ الإذن
`§⑧` نصَّ: «**يُقترَح ولا يُنفَّذ** (سطرُ تسجيلٍ لا يمسّ قرارًا، **بإذن المالك
وحدَه**): حفظُ `fired_ts_ms` في `record_ignition_fires`». **وأذن المالكُ بلفظ
«لحظة الإطلاق» (‏2026-09-23)** ردًّا على خيارٍ عُرض عليه بنصّ «احفظ لحظة الإطلاق».

### ⓑ ما نُفِّذ — وهو أضيقُ ما يحقّق الشرط
- `record_ignition_fires` **يُلحِق** بكلّ سجلٍّ جديدٍ ثلاثةَ حقولٍ عبر
  `_fired_ts_fields` (نقيّةٌ **فاشلةٌ-آمنةٌ لكلّ صفٍّ على حدة**):
  - `fired_ts_ms` — **الطابعُ نفسُه الذي يقيس منه `_fire_sustain`**، وكان يُستهلَك
    ثمّ يُرمى (‏صفرٌ من 96 · `§①`-3).
  - `fired_ts_src` — مصدرُه: `telegram_sent` (لحظةُ الإرسال الفعليّ · الأصدق) أو
    `trigger_bar_start` (بدايةُ شمعة الزناد · احتياط).
  - `trigger_bar_ms` — بدايةُ شمعة الزناد **ولو كان المصدرُ الإرسال**، فيُقاس
    العبورُ حول الشمعة لا حول لحظة إرسالٍ تتأخّر بدورة الاستطلاع.
- **ويُرفَض طابعٌ خارجَ نطاق المللي ثانية المعقول** (‏`1e12 ≤ ts < 1e13`) ⇒
  ثوانٍ مكتوبةٌ في حقل مللي **لا تُفسد القياسَ بصمت** بل تُسجَّل مجهولةً.
- `_fires_from_candidates` يُمرِّر المصدرَ وبدايةَ الشمعة من `candidates.jsonl`
  — **صفرُ جلبٍ إضافيّ**.

### ⓒ ما **لا** يغيّره هذا الملحق
⛔ لا الجذورُ الاثنا عشر (بصمتُها مطابقة) ولا `scan_ignition` ولا `_ignition_signal`
ولا نصُّ التنبيه ولا عتباتُ الدولارات الثلاث · ⛔ ولا `LOGIC_VERSION` · ⛔ ولا
الحكمُ المنشور ولا أرقامُه · ⛔ **ولا يُعاد كتابةُ الماضي**: الصفوفُ الموجودةُ قبل
الدمج (‏101 يومَ 2026-09-23) تبقى بلا الحقل — **مجهولٌ لا صفر** — والحقلُ يتراكم
**للأمام** من أوّل جلسةٍ يكتبها الـassembler بعد الدمج.

### ⓓ إعادةُ القياس — **بعقدٍ جديدٍ لا بهذا**
الحكمُ الحاليّ **لا يُعاد تشغيلُه آليًّا**. إعادةُ القياس عند لحظة الإطلاق يلزمها
**تسجيلٌ مسبقٌ جديد** يكتب تعريفَ العبور حول `trigger_bar_ms` **قبل أيّ رقم**،
وأرضيّتُه تُكتَب فيه. **وللاسترشاد لا للإلزام:** أرضيّةُ هذا العقد ‏30 إطلاقًا
محسومًا **في كلّ ذراع**، وبتوزيع الذراعين المرصود (‏39 و42 من 81 مقيسًا من 96)
يلزم **‏≈75 إطلاقًا بالحقل**، وبمعدّل التراكم المقيس (‏≈1.35/يومٍ تقويميّ · `§⑥`)
⇒ **‏≈56 يومًا ⇒ نحو 2026-11-19** — **تقديرٌ لا وعد**: المعدّلُ متقلّب.
