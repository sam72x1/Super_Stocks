# 🔬🕵️ `T-PREEXP-PROBE` — مجسُّ الصفقات الخام في اليومين قبل الانفجار (تسجيلٌ مسبق + مواصفةُ بناءٍ لأوبيس)

**التاريخ:** 2026-08-13 · **الأمر:** «سجل مجس الصفقات» · **الطبيعة: مِجَسٌّ
تشخيصيّ لا تجربةَ حكم** — سقفُ نجاحه المثبَّت سلفًا: **تقريرٌ وصفيّ للمالك،
صفرُ تغيير كودٍ أو عتبةٍ أو وزن ترتيب**. أيُّ قاعدةٍ تُشتقّ منه ⇒ تسجيلٌ مسبقٌ
جديد + اختبارٌ أماميّ + موافقة المالك.

**السؤال (سؤال المالك نصًّا):** ماذا فعل المنفجرون **على مستوى الصفقات الخام**
في اليومين قبل انفجارهم؟ — وهي الزاوية الوحيدة غير المفحوصة بعد خمس تجارب
فشلت على الشموع اليومية وصفقات ما قبل الإشارة (`Q5`-family)، وقد صارت رخيصةً
بمفاتيح S3/‏`/v3/trades` المُثبَتة.

---

## ① العيّنة — مثبَّتة قبل أيّ جلب

**المجموعة أ — كتالوج فيصل:** الرموز الـ35 بمرساة `explosion_onset`
**المصحَّحة حصرًا** (⛔ ليس `explosion_index` — عيبُ P0 الموثَّق: المِرساة القديمة
تقع داخل الانفجار 20/20). تُستورَد من `catalog_envelope` **بأسمائها** (إعادةُ
استعمالٍ لا نسخ — مقفول).

**المجموعة ب — أسماكُنا الحيّة (الأثمن):** كلُّ صفٍّ في سجلّ الانفجارات
(`wl["explosions"]`) حيث `base_reason == "مرشّح"` **و** `suspect_split == False`
— وهي اليوم **‏24 اسمًا 0 من 24 وصل المالك** (‏HYFM ‏+285% · BOXL ‏+169% ·
SGLY ‏+144% · TGHL ‏+139% · WXM ‏+98% …) **‏+ `DFSC`**. المِرساة = `expl_date`
المسجَّل. القائمة تُقرأ من السجلّ وقتَ التشغيل (تنمو ولا تُنتقى يدويًّا).

**نافذة الحدث:** الجلستان **‏[−2، −1]** قبل المِرساة (يومُ الانفجار نفسُه
**مُستبعَد** — لا تسريب). **وشاهد الضبط لكل حدث: `case-crossover`** — نفسُ
السهم في الجلستين **‏[−12، −11]** قبل المِرساة (سابقة `E-PSEUDO` المعتمَدة:
تضبط الهويّة والفلوت والقطاع تلقائيًّا، وتُنصِّف كلفة الجلب مقارنةً بشاهدٍ
خارجيّ). حدثٌ بلا بياناتٍ كافية في إحدى النافذتين ⇒ **يُستبعَد الزوج كاملًا
ويُعلَن بعدّاد** (لا مقارنةَ عرجاء).

---

## ② المقاييس — دوالُّ الإنتاج القائمة حصرًا (صفرُ منطقٍ جديد)

لكل (رمز، جلسة): جلبُ صفقات اليوم (`/v3/trades` بنمط نداء الإنتاج · العدُّ
والاقتطاعُ **يُطبَعان** — درسُ اقتطاع `T-ACC` الصامت) ثم:

| مقياس | الدالّة القائمة | يقرأ |
|---|---|---|
| شراءٌ عدوانيّ % · كتلٌ % · دارك % | `acc_components` | بصمة التجميع |
| كتلُ ‏≥1000 (شراء/على الطلب) | `_operator_blocks` | طبعات المضارب (قاعدة فيصل) |
| المشتريات الموحّدة 1·3·5·7·10·100·500 | `uniform_prints` | بصمة الخوارزمي/اليد (N8) |
| عددُ الصفقات · قيمةُ التداول $ | عدٌّ مباشر | هل «هدوء» الشموع هدوءُ صفقاتٍ فعلًا؟ |

**المُخرَج:** جدولُ كلّ حدث (النافذتان جنبًا إلى جنب) + **وسيطُ الفرق الزوجيّ**
(حدث − شاهد) لكل مقياس ولكل مجموعة (أ · ب · معًا) + **عدُّ الإشارات الموجبة/
السالبة** (كم زوجًا ارتفع فيه المقياس). **بلا اختبارات دلالةٍ حاكمة** — وصفٌ
وفواصلُ Wilson وصفيّةً حيث تنفع، والعينةُ الصغيرة تُقال بحجمها.

---

## ③ حرّاسُ الصلاحية — لكلٍّ سطرُ كودٍ يُوقِف

| # | الحارس | الخروج |
|---|---|---|
| `PX1` | **صفرُ تسريب**: `timestamp.lt` عند فتح جلسة المِرساة — ولا صفقةَ من يوم الانفجار في أيّ نافذة (قفلٌ يبدّل المِرساة فيتغيّر الجلب) | 3 |
| `PX2` | **شاهدُ ضبطٍ خارجيّ**: `AAPL` يومًا واحدًا — عددُ صفقاته وشكلُ حقوله معقولان (درسُ «الصفرُ عطبُ أداةٍ حتى يُنفى») | 3 |
| `PX3` | تعذّرُ الجلب **يُعَدّ ولا يُصنَّف** (عطبُ شبكةٍ ليس «هدوءًا») — وعدّادُه يُطبَع | يُعلَن |
| `PX4` | مرساةُ الكتالوج من `explosion_onset` **بالاسم** (قفلٌ نحويّ يمنع `explosion_index`) | قفل |
| `PX5` | الاقتطاعُ مُعلَن: لكل جلبٍ يُطبَع (المجلوب/الحدّ) — وصفرُ اقتطاعٍ صامت | قفل |
| `PX6` | أزواجٌ صالحة ‏≥15 لكل مجموعة وإلا وُسمت المجموعة «لا تكفي وصفًا» وتُنشَر خامًا | يُعلَن |

**وقيود التشغيل:** المفتاح من `POLYGON_API_KEY` (‏Secrets حصرًا · لا يُطبَع) ·
سقفُ صفقاتٍ لكل جلسةٍ مُصرَّحٌ به في الكود ويُطبَع · الجلبُ بترتيبٍ تنازليّ
(درسُ اقتطاع `T-ACC` التصاعديّ) · CSV تراكميّ قابلٌ للاستئناف (سابقة
`acc_verify`).

---

## ④ ما لا يُدَّعى — يُكتَب قبل أيّ رقم

1. **تشخيصٌ لا حكم:** لا بوّابة ولا وزن ولا سطرَ عرضٍ يتولّد من هذا المِجَسّ.
2. **العيّنة مختارةٌ على النتيجة** (منفجرون) ⇒ توصيفٌ لا معدَّلُ إصابة — ولا
   يُشتقّ منه «لو فعلنا كذا لربحنا كذا».
3. سوابقُ `T-ACC` قائمة: هذي المقاييس **انقلبت إشارتُها** كمُميِّزات اختيارٍ
   على مجتمع الإشارات — الجديدُ هنا **المجتمع** (منفجرونا نحن بالذات) و**النافذة**
   (‏يومان قبل الحدث لا نافذة القاع) — فإن خرج توصيفٌ لافت فهو **فرضيةٌ**
   لاختبارٍ أماميّ لا نتيجة.
4. صفقاتُ Polygon تشمل الجلسةَ الممتدّة — يُعلَن ولا يُفصَل في هذا المِجَسّ.
5. انحيازُ البقاء في المجموعة أ قائم؛ المجموعة ب حيّةٌ فلا يمسّها.

---

## ⑤ مواصفةُ البناء لأوبيس (بناءٌ ثم تشغيل — الأرقام من الـworkflow حصرًا)

1. **`preexp_probe.py`** (أداةٌ مستقلّة · لا يستوردها `Super_stock` ولا تُستورَد
   في مسار فرز): يقرأ العيّنتين (§①) · يجلب · يحسب (§②) · يطبع الجداول
   والعدّادات والحرّاس · CSV تراكميّ · رموز خروج: 0 قِيس · 2 مفتاحٌ غائب ·
   3 صلاحيةٌ ساقطة.
2. **`preexp_probe.yml`**: يدويّ فقط (لا كرون) · `POLYGON_API_KEY` من Secrets ·
   كلُّ مدخلٍ موصولٌ بـ`env` (قاعدة P1).
3. **أقفال:** `PX1`-`PX6` أعلاه + قفلُ عزلٍ (لا استيراد في الإنتاج) + قفلُ
   «دوالُّ الإنتاج بأسمائها» (‏`acc_components`/`_operator_blocks`/
   `uniform_prints` تُستورَد من `Super_stock` **ولا تُنسَخ**) — وطفرةٌ لكلٍّ.
4. **الترتيب:** بعد حزمتَي الفصل وصدق الدخول (ثالثةً) — وقبل التشغيل يتحقّق
   أوبيس أن هذا الملفّ مدفوعٌ في التاريخ (وهو كذلك: يُدفَع الآن قبل أيّ رقم).

---

## ⑥ ملحقٌ مؤرَّخ 2026-08-13 (قبل أيّ رقم) — طبقةُ الشموع اليومية تُضاف للمِجَسّ نفسه

**أمر المالك:** «ارجع لسلوك أسهم الكاتالوج قبل ما تنفجر بيومين وتأكّد — ممكن
فيه شي غافلين عنه». ⇒ حتى لا يبقى مستوًى غيرُ مفحوص، يقيس المِجَسّ **الطبقتين
معًا** على نفس النوافذ (‏[−2،−1] مقابل شاهد ‏[−12،−11]):

| طبقة | الدالّة القائمة | المقاييس |
|---|---|---|
| **الشموع اليومية** | `activity_features` (إنتاجية · بُنيت في `T-RANKER3`) | `vol_x` (حجم/متوسط20) · `range_x` (مدى/وسيط20) · `close_pos` (موضع الإغلاق) · `a1/a2/a3` · وفجوةُ الافتتاح % (حسابٌ مباشر من `Open` مقابل إغلاق الأمس) |
| **الصفقات الخام** | §② كما هو | بلا تغيير |

الشموعُ تُجلَب من `/v2/aggs` **المعدَّلة** (نمط `pit_history` القائم) لكلّ حدثٍ
حول نافذته فقط — كلفةٌ تافهة. **حقيقةٌ تُقرأ قبل التفسير:** الطبقةُ اليومية على
مستوى العشرين جلسةً مقيسةٌ سلفًا (الكاتالوج **هادئٌ هابط**: وسيط `gain5`
‏−8.26%) — الجديدُ هنا **عزلُ اليومين الأخيرين تحديدًا** مقابل شاهدِ نفس السهم،
فإن ظهر فيهما ما لا يظهر في العشرين فهو بالضبط «الشي المُغفَل» الذي يسأل عنه
المالك؛ وإن لم يظهر فالنفيُ صار مقيسًا على النافذة الدقيقة لا مستنتَجًا.
**قفل إضافيّ `PX7`:** طبقةُ الشموع تُحسب بـ`activity_features` **بالاسم**
(لا نسخَ منطق) وعلى شريحةٍ تنتهي عند يوم النافذة حصرًا (صفر تسريب — نفس `PX1`).

---

## ⑦ النتيجة — تشغيلة `31749403631` (‏2026-08-13 · قبلها العقدُ مدفوع)

**التغطية:** ‏53 زوجًا كاملًا من 53 (‏تُخطّي 2 · تعذّرُ جلبٍ **صفر**) — المجموعة (أ)
كتالوج فيصل **27** · المجموعة (ب) أسماكُنا الحيّة **26**. شاهدُ الضبط `AAPL` عبَر.
**والجدولُ وسيطُ الفرق الزوجيّ** (حدث ‏[−2،−1] ناقص شاهد ‏[−12،−11] لنفس السهم)
**وعدُّ إشاراته**:

| المقياس | (ب) حيّة · وسيطُ الفرق · موجب/سالب | المجموعتان · وسيطُ الفرق · موجب/سالب |
|---|---|---|
| **`vol_x`** (حجم/متوسط20) | **‏+0.276** · 20/6 | **‏+0.272** · **38/15** |
| `range_x` | ‏+0.103 · 16/10 | ‏+0.059 · 30/23 |
| `close_pos` | **‏−0.111** · 10/15 | **‏−0.162** · 18/33 |
| `gap_pct` | **‏−0.692** · 10/16 | **‏−0.529** · 21/32 |
| `n_trades` | ‏+10.75 · 13/13 | ‏+33.0 · 27/26 |
| `usd` | ‏−33,776 · 11/15 | ‏−3,336 · 25/28 |
| `aggressive_buy_pct` | ‏+1.5 · 14/11 | **‏0.000** · **25/25** |
| `block_share_pct` | ‏−1.0 · 10/16 | ‏−0.5 · 24/28 |
| `dark_share_pct` | ‏−2.25 · 10/16 | ‏−2.5 · 20/32 |
| `uniform_count` | ‏−27.25 · 12/14 | ‏−2.5 · 25/28 |
| `buy_block_shares` · `bid_block_shares` | **مقامٌ صفر** 🔴 | **مقامٌ صفر** 🔴 |

### القراءة (تشخيصٌ لا حكم — بنصّ §⑤)

🥇 **مقياسٌ واحدٌ يميل، ويميل بوضوح: `vol_x`.** ‏+0.272 وسيطًا و**‏38 موجبًا مقابل
15 سالبًا** على المجموعتين، و20/6 على أسماكنا وحدها ⇒ **اليومان قبل الانفجار
يحملان تمدّدَ حجمٍ يسبق الحركة** — وهو المقياسُ الوحيد الذي اتّجاهُه واحدٌ في
المجموعتين بعدٍّ يميل بمقدار الضِّعفين ونصف.

🔴 **وكلُّ مقاييس «المضارب» من الصفقات الخام ضجيجٌ أو سالبة:** الشراءُ العدوانيّ
**‏0.000 و‏25/25** (تعادلٌ تامّ — أنظفُ عدميّةٍ قسناها) · حصّةُ الطبعات الكبيرة
‏−0.5 · الدارك ‏−2.5 · الطبعاتُ الموحّدة ‏−2.5. ⇒ **تأكيدٌ سادسٌ لعائلة
«سلوكٌ يميّز المنفجر»** بعد `T-ACC`(×2) و`T-ACC-2` و`T-OPFIRE` و`T-RANKER3`،
وهذي المرّة على **النافذة الأدقّ التي طلبها المالك** (يومان لا عشرون جلسة).

🔑 **واتّجاهان معاكسان لحدسي يُنشَران:** `close_pos` **سالب** (يُغلق أدنى في مدى
يومه) و`gap_pct` **سالب** ⇒ اليومان قبل الانفجار **هادئان وضعيفان قليلًا** لا
«متجمّعان صاعدان». ⇒ صورةُ «تمدّدُ حجمٍ بلا تحسّنِ سعر» — وهي متّسقةٌ مع المقيس
سلفًا على العشرين جلسة (وسيط `gain5` ‏−8.26%) ⇒ **العشرون تصف الفئة واليومان
لا يشذّان عنها** ⇒ الجوابُ على «هل غفلنا عن شيء؟»: **لا شيءَ في الشموع ولا في
الصفقات يميّز — إلّا مَيلَ الحجم.**

### 🔴 عيبُ قياسٍ في المِجَسّ نفسِه — يُعلَن ولا يُطوى

`buy_block_shares`/`bid_block_shares` **لم تُحسَبا ولا مرّة**: `_operator_blocks`
الإنتاجية تفكّ `for p, s in trades` ⇒ تستقبل **أزواج (سعر، حجم)**، والمِجَسُّ كان
يمرّر **قواميسَ Polygon الخام** ⇒ ترمي داخلها فترجع `None` **صامتةً**
(وحارسي كان `except Exception: pass`). **مُستنسَخٌ محلّيًّا:** قواميسُ ⇒ `None` ·
أزواجُ ⇒ `{'n_blocks': 30, 'buy_block_shares': 30000, …}`.
🧭 **والدرسُ المكرَّر: «الصفرُ عطبُ أداةٍ حتى يُنفى»** — وقد طُبع «مقامٌ صفر» بلا
سببٍ مُسمًّى فكاد يُقرأ «لا طبعاتِ مضاربٍ في اليومين» وحقيقتُه **خطأُ شكلٍ عندي**؛
وهو **نظيرُ درسِ «قرأتُ `available` والحقلُ `shares_available`»** حرفيًّا، **ووقع
في السطر الذي يليه استعمالٌ صحيحٌ للتحويل** (`uniform_prints` تأخذ الأزواج).
✅ **أُصلح** ‏+ صار المقامُ الصفريّ **يُشخَّص بحالاتٍ مُسمّاة** (`_status_tally`)
ولا `pass` صامتٌ يبتلع الاستثناء. أقفال `PX10أ/ب` (سلوكيٌّ **مفرِّق** ‏+ نحويّ على
شكل المُمرَّر) و`PX11أ/ب`. ⚠️ **والمقياسان يبقيان بلا رقمٍ حتى إعادة التشغيل**،
ولا يُقال عنهما «لا فرق».

### حدودُ صدقٍ تُقرأ مع الأرقام لا بعدها
1. **العيّنةُ مختارةٌ على النتيجة** ⇒ توصيفٌ لا معدَّلُ إصابة، ولا تُشتقّ منه قاعدة.
2. **لا اختبارَ دلالةٍ حاكمًا** بنصّ العقد ⇒ «‏38/15» مَيلٌ لا برهان.
3. صفقاتُ Polygon تشمل الجلسةَ الممتدّة (مُعلَنٌ سلفًا §④-4).
4. المجموعة (أ) عليها انحيازُ بقاء.
5. **مقياسان من اثني عشر لم يُقاسا** (أعلاه).
⇒ **المخرَجُ الوحيد المشروع: فرضيةٌ لاختبارٍ أماميّ مسجَّل** — «هل يسبق تمدّدُ
الحجم بيومين الانفجارَ **في الأمام** بمعدّلٍ يفوق الأساس؟» — ولا سطرَ عرضٍ ولا
وزنَ ترتيبٍ قبل ذلك.

---

## ⑦-ب إعادةُ التشغيل بعد الإصلاح — المقياسان الغائبان صدرا (تشغيلة `31751661636`)

على `737bc79` بنفس العيّنتين (‏53 زوجًا · 27 + 26 · تعذّرُ جلبٍ صفر):

| المقياس | (ب) حيّة: حدث · شاهد · فرق · موجب/سالب | المجموعتان |
|---|---|---|
| **`buy_block_shares`** | ‏26,916 · 8,907 · **‏+1,444** · **14/10** | ‏30,362 · 23,930 · **‏+1,000** · **28/23** |
| **`bid_block_shares`** | ‏28,174 · 15,745 · **‏+3,068** · **14/10** | ‏30,982 · 24,565 · **‏+2,752** · **29/22** |

**القراءة:** الاتجاهُ موجبٌ في المقياسين وفي المجموعتين — **طبعاتٌ أكبر (شراءً
وعلى الطلب) في اليومين مقابل شاهد السهم نفسِه**. ⚠️ **لكنّ الميلَ أضعفُ بكثيرٍ
من `vol_x`**: ‏28/23 و29/22 (‏≈55%) مقابل **‏38/15 (‏72%)** ⇒ **قريبٌ من التعادل**،
وفجوةُ الوسيطين (‏×3.0 و×1.8 على أسماكنا) **يصنعها عددٌ قليلٌ من الأزواج الكبيرة**
لا ميلٌ عامّ — والدليلُ أنّ عدَّ الإشارات لا يتبع فجوةَ الوسيطين.
⇒ 🔑 **والخلاصةُ لا تتغيّر: `vol_x` يبقى المقياسَ الوحيد الذي يميل بوضوح**،
وطبعاتُ المضارب **تميل ميلًا ضعيفًا** — وهو أقربُ إلى «تمدّدُ حجمٍ يجرّ معه
طبعاتٍ أكبر» منه إلى بصمةٍ مستقلّة. ⚖️ **ولا يُدَّعى غيرُ ذلك**: لا اختبارَ دلالةٍ
هنا بنصّ العقد، والعيّنةُ مختارةٌ على النتيجة.

### 🔴 عيبٌ ثانٍ في المِجَسّ يُعلَن ولم يُشخَّص بعد — التشغيلتان لا تتطابقان

بين `31749403631` و`31751661636` (نفسُ العيّنتين ونفسُ الأعداد 53/27/26) تحرّكت
قيمٌ **يُفترَض أنها حتميّة**: `vol_x` الحدث ‏0.729 ⟶ **0.691** · `close_pos` الفرق
‏−0.111 ⟶ **−0.160** · `gap_pct` الشاهد ‏0.134 ⟶ **0.443** · و`aggressive_buy_pct`
عدُّه ‏25/25 ⟶ **24/26**. **وأغربُها أن `vol_x` و`usd` تغيّر وسيطُ حدثِهما وبقي
وسيطُ الفرق وعدُّ الإشارات مطابقًا بت-بت** (‏0.276 و20/6 · و‏−33,776).
⇒ **المِجَسُّ ليس قابلًا لإعادة الإنتاج حرفيًّا**، والسببُ **غيرُ مُشخَّصٍ بعد**
(مرشَّحاتٌ: نافذةُ جلب الصفقات · اختيارُ مِرساة المجموعة (أ) وقتَ الجلب · أو
اختلافُ مجموعةِ الأزواج «الكاملة» رغم تساوي عددها).
🧭 **وبالقاعدة المدوَّنة — «الصفرُ عطبُ أداةٍ حتى يُنفى» في صيغتها الأعمّ: فرقٌ
غيرُ مُفسَّرٍ عيبٌ مرشَّحٌ حتى يُشخَّص** — يُعلَن ولا يُفسَّر بالرأي.
⚠️ **وأثرُه على القراءة:** الأرقامُ **أوصافٌ ذاتُ ‏±ضجيجٍ غيرِ مُقدَّر**، والوحيدُ
الذي صمد **مطابقًا في التشغيلتين هو `vol_x`** (‏+0.276 و20/6 على أسماكنا · و+0.272
و38/15 معًا) ⇒ **الخلاصةُ تصمد، ودقّةُ ما عداها أضعفُ ممّا يوحي جدولُها.**
⏸️ **ولا تُبنى تجربةٌ أماميّةٌ على هذا المِجَسّ قبل تشخيص الفرق** — وإلّا بُنيت
على مقياسٍ يتحرّك تحتها.

---

## ⑦-ج تشخيصُ عيب التكرار — **مُسنَدٌ بالقياس** (‏2026-08-14، أمرُ المالك «شخّص الفرق»)

**السؤال:** تشغيلتان بنفس العيّنة أعطتا وسائطَ مختلفة (‏`vol_x` حدث 0.729 ⟶ 0.691 ·
`close_pos` −0.111 ⟶ −0.160) **مع بقاء وسيطِ الفرق وعدِّ الإشارات كما هما**.

### ① السببُ مقروءٌ من الكود لا مستنتَجًا
شموعُ المِجَسّ كانت من `download_history` وبدؤها `today − HISTORY_DAYS(800)` ⇒
**نافذةٌ متدحرجةٌ تنتهي «الآن»**، ومنها ثلاثةُ آثار:
1. **مِرساةُ المجموعة (أ)** تُحسب بـ`explosion_index(pick="last")` على نافذةٍ
   **تتحرّك** ⇒ «أحدثُ انفجار» قد يتبدّل ⇒ **نوافذُ السهم الأربع كلُّها تتغيّر**.
2. **الشمعةُ الأخيرة تُنقَّح بعد الإغلاق** ⇒ تتحرّك المتوسّطاتُ المشتقّة منها
   (‏`vol_x` = حجم/متوسط20).
3. 🔴 **وانفجارٌ أقدمُ من ‏≈2.2 سنة لا يُرى أصلًا** — **عينُ عيبِ `op23_prereg §⓪-أ`
   الذي أُصلح هناك وتُرك هنا.**
✅ **ونُفي المرشَّحُ المنافس بالقياس:** `weekly_watchlist.json` **لم يتغيّر** بين
التشغيلتين (‏`git log a8d119d..737bc79 -- weekly_watchlist.json` فارغ) ⇒ عيّنةُ (ب)
مطابقة ⇒ **الفرقُ ليس في العيّنة**.

### ② والإسنادُ لم يُترك للاستنتاج — بُنيت أداتُه وقيس
`row_fingerprint` (المِرساة · تواريخُ النوافذ الأربع · قيمُ المقاييس) تُطبَع مع كلّ
رمز، و**بصمةُ تشغيلة** فوقها. **أربعُ تشغيلاتٍ متتاليةٍ على نفس الـcommit `4d3cccf`:**

| التشغيلة | المصدر | **بصمةُ التشغيلة** | حجمُ الصفوف | (أ) | (ب) |
|---|---|---|---|---|---|
| `31780469530` diag_y1 | yahoo | **`fc4ee1a1d8fb772f`** | 18,739 | — | 35 |
| `31780475488` diag_y2 | yahoo | **`20d106668e06a098`** | 18,736 | — | 35 |
| `31780481696` diag_g1 | **polygon** | **`624e16cc19bcb34e`** | 18,409 | — | 33 |
| `31780488034` diag_g2 | **polygon** | **`624e16cc19bcb34e`** | 18,409 | — | 33 |

🔑 **yahoo ⇒ بصمتان مختلفتان (عيبُ حتميّةٍ مُثبَتٌ حيًّا) · polygon ⇒ بصمةٌ متطابقةٌ
حرفيًّا (العلاجُ مُثبَتٌ فعّالًا).** وفرقُ حجم صفوف yahoo **‏3 بايت فقط** ⇒ الاختلافُ
في **قيمةٍ أو قيمتين لا في العضوية** — **وهو بالضبط ما يفسّر جدولَ أمس**: تحرّكُ
وسيطِ الحدث مع ثباتِ وسيطِ الفرق وعدِّ الإشارات.

### ③ كلفةُ العلاج مُعلَنة
المصدرُ الثابت يُسقط **صفَّين** من أسماكنا (‏33 مقابل 35): رمزان بلا تاريخٍ عند
Polygon. ⇒ الاختيارُ بحسب السؤال: **للحتميّة `polygon`** · **ولأوسعِ عيّنةٍ `yahoo`
مع الإقرار بأن أرقامَه تتحرّك**. **والافتراضُ لم يتغيّر** فلا رقمَ منشورٌ يتحرّك بلا
اختيارٍ صريح.

### 🔴 وثلاثةُ عيوبٍ في أدواتي كشفتها التشغيلةُ لا القراءة
1. **`fetch_bars` سقط على 55 رمزًا من 55** بعيبين في سطرٍ واحد: `polygon_daily` لها
   وسيطٌ **إلزاميّ** `api_key` لم أُمرّره · **وتُرجع زوجًا** `(df, تشخيص)` أرجعتُه
   كما هو ⇒ طولُه 2 ⇒ **يُتخطّى الرمزُ صامتًا**. 🧭 **والنمطُ الصحيح كان في
   `op23_scan.py` نفسِه** — نظيرُ عيب الأمس حرفيًّا (قواميسُ لدالّةٍ تريد أزواجًا
   والاستعمالُ الصحيح سطرًا تحتها) ⇒ **نسخُ النمط القائم أوثقُ من كتابته من الذاكرة.**
2. 🔴 **والتشغيلةُ خرجت خضراءَ بصفر قياس** — بصمةُ الـno-op التي تمنعها قواعدُنا
   (‏«بصمةُ التشغيلة `e3b0c442…`» = هاشُ الفراغ) ⇒ **صفرُ أزواجٍ ⇒ خروج 4 مُعلَن.**
3. 🔴 **وقفلا `PX13` و`PX11ب` نصّيّان نجوا** — الأوّلُ على **نداءٍ معطوب** (يفحص وجودَ
   الاسم لا صحّةَ النداء) والثاني على **طفرةِ `pass`** (الأسماءُ باقيةٌ في فرعٍ آخر)
   ⇒ صارا **نحويًّا (‏`PX16`: أربعةُ وسائطَ وتفكيكُ الزوج)** و**سلوكيًّا (‏`PX11ب`
   بثلاث حالاتٍ متفرّقة)**. **رابعُ ظهورٍ للفخّ النصّيّ في يومين.**

🐞 **ودرسٌ تشغيليّ تكرّر ويُدوَّن:** عدّلتُ `test_bot.py` **أثناء** جولةِ طفراتٍ عاملة
⇒ قرأت البوّابةُ شجرةً مطفَّرة فسقط `PX17` **كذبًا**. ⇒ **لا تعديلَ أثناء جولة ·
والطفراتُ على شجرةٍ ملتزمةٍ حصرًا · و`git status` بعد كلّ جولة.**
✅ **والجولةُ الكاملة: 10 طفراتٍ كلُّها سقطت بأقفالها المسمّاة** (‏PX10ب · PX11ب ×2 ·
PX12 ×2 · PX15 · PX16أ · PX16ب · PX17 · O14أ). السويّة **2665 · 0 فشل**.

⚠️ **وما لا يتغيّر بهذا كلِّه:** أرقامُ §⑦ و§⑦-ب **تبقى كما نُشرت** (قِيست على
`yahoo` وهذا مُعلَنٌ الآن)، **والحكمُ الوصفيّ لم يتحرّك**: `vol_x` وحدَه يميل، ومقاييسُ
المضارب ضجيجٌ أو سالبة، و`close_pos`/`gap_pct` سالبان.
