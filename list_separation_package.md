# 📦🪑 حزمة التنفيذ لأوبيس — «فصل القائمتين» (أمر المالك 2026-08-13: «افصل القايمتين»)

> **العقد:** هذا الملف هو المواصفة الكاملة والوحيدة. أوبيس **ينفّذ منه حرفيًّا ولا
> يجتهد خارجه**. أيّ غموض ⇒ يوقف ويسأل، لا يخمّن. المخطِّط (Claude) ثبّت كلَّ
> قرار تصميمٍ هنا — التنفيذ ميكانيكيّ.
> **السند:** `ranker3_prereg.md §⑫` (البند المسجَّل) + ملحق `§⑫-أ-3` + قياس
> `T-RANKER3` (`ranker3_result.md §④`: صفر من 66 منفجرًا في المراكز الثلاثة الأولى
> عبر ثلاث سنوات — الخانات يحتلّها طويلو المكوث).

---

## ⓪ المشكلة بالأرقام (مقيسة على القائمة الحية 2026-08-13 — لا مُفترَضة)

- القائمة تحمل **22 اسمًا** · حاملو الخانات بالحساب الحالي (`Super_stock.py:15137`)
  = **20 من سعة 15** ⇒ `space = −5` ⇒ **صفر مرشّح جديد يدخل يوميًّا** حتى تُشطب
  5 أسماء بالستوب. هذي هي آلية انحباس `DFSC` حرفيًّا.
- **6 أسماء فوق نطاق دفعاتها الآن وتحجز خانات** (🔴 تصحيح مؤرَّخ 2026-08-13
  بعد تدقيقٍ خصوميّ — كانت «5» والقياسُ المعاد على القائمة الحية 6): `QTI`
  (‏2.885 مقابل سقف 2.77) · `FLYE` (‏1.742/1.70) · `FUFU` (‏1.385/1.34) ·
  **`FTCI`** (‏2.46/2.42 — كان على الحدّ بالضبط وتحرّك سعره) · `PRHI`
  (‏4.75/4.41) · `BBLG` (‏0.672/0.66). طلبات الدخول فيها **لا يمكن أن تُعبَّأ
  اليوم** (أوامر حدٍّ تحت السوق) — فحجزُها خانةً بلا معنى تنفيذيّ. والأرقامُ
  تتحرّك بالأسعار؛ الحاكمُ المنطقُ لا الأسماء.
- `DFSC` و`XAIR` معفيان أصلًا (‏`hit=True` — سابقة ⑨-ب) ⇒ الفجوة الباقية هي
  بالضبط الفئة أعلاه: **لمس نطاقه (أو عُبِّئ) ثم ارتفع فوقه ولم يبلغ الهدف بعد**.

**دلالة الخانة (التعريف الحاكم):** الخانة = حجزٌ لاسمٍ **يمكن تعبئة طلباتك فيه
الآن** (سعره داخل/تحت نطاق دفعاته) ولم يخرج من النموذج ولم يبلغ هدفه. ما فوق
النطاق = ليس منافسًا على دخولٍ جديد ⇒ لا خانة — **ويبقى مُتابَعًا بكل شيء**
(«تُشطب بالستوب فقط» لا تُمَسّ بحرف).

---

## ① التعديل 1 — دالّة نقيّة جديدة `_position_risen(s)`

تُوضَع مباشرةً **بعد** `_nf8_slot_free` (‏`Super_stock.py:~10946`). **هذي الصيغة
النهائية — تُنسَخ كما هي:**

```python
def _position_risen(s: dict) -> bool:
    """🪑 فصل القائمتين (أمر المالك 2026-08-13 «افصل القايمتين» — سند
    `ranker3_prereg.md §⑫` وقياس `ranker3_result.md §④`): هل هذا الاسم
    **مركزٌ ارتفع فوق نطاق دخوله**؟ نعم إذا (لمس نطاقَه `band_hit` أو بلغ
    هدفَه `hit`) **و** سعرُه الأخير فوق أعلى دفعةٍ الآن.
    ⇒ لا يحجز خانةً (طلباتُ الدخول أوامرُ حدٍّ تحته فلا تُعبَّأ اليوم)
    **ويبقى مُدرَجًا متابَعًا بكل شيء** — جنسُ سابقات ⑨-ب و«افتح الباب»
    وNF8؛ وقاعدة «تُشطب بالستوب فقط» لا تُمَسّ بحرف. ويعود يحجز فور رجوع
    السعر إلى النطاق (يُعاد التقييم يوميًّا من `last_price` — ذاتيّ الشفاء،
    لا ترحيل ولا حقل جديد).
    شرطُ (`band_hit` أو `hit`) يمنع تحرير مَن لم يلمس نطاقه قطّ فور إدراجه —
    تلك فئةُ NF8 القائمة (مؤقّت 8 جلسات) ولا تُبتلَع.
    ⚠️ لا يُستعمَل `in_entry_band` هنا: ترجع False عند النقص، وFalse هنا
    معناها «فوق النطاق» ⇒ الناقصُ كان سيتحرّر خطأً — فالمقارنة مكتوبة
    بنفس صيغتها حرفيًّا معكوسةً، والنقصُ يحجز (قفل `LS7` يثبت التكامل).
    **فاشل-آمن:** حقلٌ غائب/تالف ⇒ False ⇒ يحجز (سلوك ما قبل الميزة حرفيًّا)."""
    try:
        if not (s.get("band_hit") or s.get("hit")):
            return False
        tr = [float(t) for t in (s.get("tranches") or []) if t]
        px = float(s.get("last_price") or 0)
        if not tr or px <= 0:
            return False          # ناقصٌ ⇒ يحجز (فاشل-آمن)
        return px > max(tr) * (1 + float(
            CONFIG.get("ENTRY_READY_BAND_TOL_PCT", 0.0)) / 100.0)
    except (TypeError, ValueError):
        return False
```

**قيود صارمة:** صفر رقم جديد (حقول `band_hit`/`hit`/`last_price`/`tranches`
القائمة + عتبة `ENTRY_READY_BAND_TOL_PCT` المعتمَدة) · **لا يقرأ `entry_ref`**
(المرجع نطاقُ الدفعات لا سعر الترشيح، ولا ارتدادَ إليه عند غياب `last_price` —
الغائب يحجز) · المقارنة `>` الصارمة (المساوي للسقف داخل النطاق فيحجز).

---

## ①-ب التعديل ⑫-ب — المحمولُ غيرُ المُعادِ اختياره لا يحجز (أمر المالك 2026-08-13: «نفّذ ⑫-ب»)

**التخويل مؤرَّخ:** كان مؤجَّلًا صراحةً في §⑥ أدناه، **وأمر به المالك بعد شاهد
`BOXL`** (‏`HANDOFF.md`: اجتاز الفارز عند قاعه · سجّله صيّاد الظرف 4 جلسات ·
انفجر ‏+169% · وما وصل لأن الخانات محجوزة — **11 منها بمحمولين لم يُعادوا
اختيارهم**).

**القاعدة:** `cont_status == "continues"` (حُمِل عبر التجديد لأنه ما زال
ارتكازًا أو تعذّرت بياناته، **ولم يُعَدْ اختياره** في أفضل-15) ⇒ **لا يحجز
خانةً حتى وهو داخل نطاقه** — لأنه ليس اختيارَ هذا الأسبوع؛ الخانةُ لمرشّحي
الأسبوع الجاري ومَن أُضيف يوميًّا. **ويبقى مُدرَجًا متابَعًا بكل شيء** (قسم
«🔄 متابعة لمركزك» · الستوبات · الأهداف — «تُشطب بالستوب فقط» بحرفها).

**الحدود الدقيقة:** `"renewed"` (أعاد التأهّل) **يحجز** · `None` (اختيارُ هذا
الأسبوع/إضافةٌ يومية) **يحجز** · `"exited"` مُستبعَدٌ أصلًا (فتح الباب) ·
والعائد للترشيح في تجديدٍ لاحق يصير `"renewed"` فيحجز من جديد — ذاتيّ الشفاء،
**صفرُ حقلٍ جديد وصفرُ رقمٍ جديد** (شرطُ مساواةٍ على حقلٍ قائم).

## ② التعديل 2 — سطر المحاسبة الوحيد (`Super_stock.py:15137-15140`)

```python
    _slot_holders = [s for s in wl["stocks"]
                     if not s.get("hit") and s.get("cont_status") != "exited"
                     and s.get("cont_status") != "continues"
                     and not _nf8_slot_free(s) and not _position_risen(s)]
```

📊 **الأثر المقيس على قائمة اليوم (2026-08-13):** حاملون **‏20 من 15**
(‏space=−5) ⟶ بعد ⑫-أ وحده **‏14** (‏space=1) ⟶ **بعد ⑫-أ+⑫-ب معًا: ‏6
حاملين ⇒ 9 خانات مفتوحة** (محرَّرو ⑫-ب: TIL·ARBK·VHUB·CVKD·AIXI·ENGN·LPCN·
FEMY). أوبيس يعيد إنتاج هذا العدّ في بوّابة القبول ④.

- **موضع واحد فقط.** لا يوجد حسابُ خاناتٍ آخر (تجديد الجمعة يبني 15 جديدة
  والمحمولون لا يستهلكون منها — تحقّقتُ: المسار الوحيد المتأثّر هو الإضافة
  اليومية). أوبيس يتحقّق بـ`grep -n "WATCHLIST_SIZE\"] - len"` = موضع واحد.
- `fill_picks`/`select_top`/`rank_key` **لا تُمَسّ بحرف** — يصلها `space` أكبر فقط.
- تجاوزُ العدد الكلّي فوق 15 مؤقّتًا (لو رجع مرتفعٌ إلى نطاقه) سلوكٌ قائم أصلًا
  (سابقتا ⑨-ب وNF8) — لا يُعالَج، وصمّام `CONTINUITY_MAX` قائم.

---

## ③ التعديل 3 — العرض (صدق التسمية · ثلاثة مواضع مرصودة + wire-check)

الفئة المتحرّرة تضمّ رابحًا كبيرًا (`DFSC` ‏+90%) ومنجرفًا هامشيًّا (`BBLG`
‏+1.8%) ⇒ **التسمية محايدة صادقة، لا «رابح» ولا «خارج أفضل-15»**:

1. **`build_message` (‏`Super_stock.py:8629`):** لو `_position_risen(r)` صادقة
   على السجلّ، استبدل سطرَي `cont_status` بـ:
   `‏📈 فوق نطاق دخوله — لا يحجز خانة (يعود يحجز إذا رجع للنطاق)`
   وإلّا يبقى السطران القائمان حرفيًّا.
   ⚠️ **نطاق wire-check لهذا الموضع (تحديدٌ مؤرَّخ بعد تدقيق):** فرعُ
   `cont_status` في `build_message` **خاملٌ إنتاجيًّا اليوم** (‏`build_message`
   تُنادى بـ`picks` الطازجة فقط في `:14907`، والمحمول `continues/exited` لا
   يدخلها والمعادُ تأهيلُه وسمُه `renewed`) ⇒ يُعدَّل **للاتّساق الاحتياطيّ**
   ويُثبَت **بعيّنة وحدةٍ محقونة** — ولا يُطالَب ببلوغه من النداء الحيّ.
   **الموضع الحيّ الفعليّ للفئة هو §③-2** (`build_position_watch_section`) —
   وعليه يقع wire-check الحيّ.
2. **قسم «🔄 متابعة لمركزك» (`build_position_watch_section`):** نفس الاستبدال
   لنفس الفئة (السطر يوضّح الحالة الحقيقية بدل إيحاء «طُرد من القائمة»).
3. **ترويسة التقرير اليومي:** سطرٌ واحد يُضاف حين تكون الفئة غير فارغة:
   `‏📈 K فوق نطاق الدخول (لا تحجز خانات)` بجوار عدّادَي «جاهز/متابعة».
4. **تقرير المصير (`:14852`) لا يُمَسّ** — وقتَ التجديد التصنيف صحيح.

**إلزاميّ:** سكِل `wire-check` على كلّ سطر عرض — يُثبَت من نقطة النداء الحيّة
بعيّنة تُنتج السطر فعلًا (لا «الدالّة موجودة»).

---

## ④ `LOGIC_VERSION` والإبلاغ

- التعديل **يغيّر ما يصل المالك** (إضافات يومية كانت مستحيلة تصير ممكنة) ⇒
  **ارفع `LOGIC_VERSION`** بوسمٍ يحوي `listsplit` (أعلى `Super_stock.py`).
- بعد الدمج: **أبلغ المالك بسطرٍ واحد أن يشغّل `force_renew=1` مرّة** (شرط
  `HANDOFF.md` الملزم).

---

## ⑤ الأقفال والطفرات (سكِل `lock-and-mutate` إلزاميّ — عيّنات مفرِّقة)

**أقفال جديدة في `test_bot.py` (‏`LS1`-`LS8`):**

| # | القفل | العيّنة المفرِّقة |
|---|---|---|
| `LS1` | مرتفعٌ لمس نطاقه (`band_hit=True` · فوق السقف) **يتحرّر** وقائمةٌ ممتلئة تقبل مرشّحًا جديدًا | نسخة DFSC: ‏last=2.40 · سقف 1.26 |
| `LS2` | داخل النطاق يحجز | نسخة TIL: ‏7.32/7.44 |
| `LS3` | **فوق النطاق بلا لمسٍ قطّ** (`band_hit=False` · `hit=False` · `band_wait_days<8`) **يحجز** — فئة NF8 لا تُبتلَع | عيّنة تُسقط طفرة حذف شرط `band_hit` |
| `LS4` | رجع للنطاق ⇒ يحجز من جديد (إعادة تقييم يومية) | نفس السجلّ بسعرين |
| `LS5` | ناقص البيانات (`last_price=None` أو `tranches=[]`) ⇒ يحجز | يُسقط طفرة «الناقص يتحرّر» |
| `LS6` | حدّي بالضبط: `last == max(tranches)` ⇒ **يحجز** (المقارنة `>` الصارمة) | يُسقط طفرة `>=` |
| `LS7` | تكامل (🔴 صياغة مصحَّحة 2026-08-13 بعد تدقيق): لكل سجلٍّ كامل البيانات **ولامسٍ** (`band_hit` أو `hit`): `_position_risen ⟺ not in_entry_band` — التكافؤ **مشروطٌ باللمس** (غيرُ اللامس فوق النطاق: `in_entry_band=False` و`_position_risen=False` عمدًا — فئة NF8) · ⚠️ وفرقُ حقلٍ مقصود: `in_entry_band` تقرأ `price` ثم `last_price` و`_position_risen` تقرأ `last_price` وحدها | 6 حالات تشمل التخوم واللامس/غير اللامس |
| `LS8` | سلوكيّ: العرض — سجلّ مرتفع يطبع السطر الجديد ولا يطبع «خارج أفضل-» · وداخل النطاق يطبع القديم حرفيًّا | يُسقط طفرة حذف الاستبدال |
| `LS9` | ⑫-ب: `continues` **داخل نطاقه** لا يحجز — وقائمةٌ ممتلئة به تقبل مرشّحًا جديدًا | نسخة TIL (‏7.32/سقف 7.44 · داخل النطاق) |
| `LS10` | ⑫-ب حدود: `renewed` داخل النطاق **يحجز** (لا تُبتلَع فئةُ المُعادِ اختياره) | يُسقط طفرة استهداف `renewed` بدل `continues` |
| `LS11` | ⑫-ب حدود: `cont_status=None` (اختيار الأسبوع/إضافة يومية) **يحجز** | يُسقط طفرة `!= None` |

**الطفرات المطلوبة (كلٌّ تُدار على شجرة ملتزمة · `PYTHONDONTWRITEBYTECODE=1` +
`rm -rf __pycache__` قبل كل قياس · استعادة بـ`git checkout` ثم فحص `git status`):**
① حذف `not _position_risen(s)` من السطر 15137 ⟶ يسقط `LS1` ② قلب المقارنة
⟶ `LS2` ③ حذف شرط `(band_hit or hit)` ⟶ `LS3` ④ `>=` بدل `>` ⟶ `LS6`
⑤ ارتداد إلى `entry_ref` عند غياب `last_price` ⟶ `LS5` ⑥ حذف استبدال العرض
⟶ `LS8` ⑦ حذف شرط `!= "continues"` ⟶ `LS9` ⑧ استبداله بـ`!= "renewed"`
⟶ `LS10` (وعيّنة `LS11` تفرّق `None`). **كل طفرة يجب أن تُسقط قفلها المسمّى
وإلّا فالقفل أعمى — يُصلَح القفل لا تُحذف الطفرة.**

**أقفال قائمة — 🔴 تخويلٌ مؤرَّخ 2026-08-13 (أخرجه تدقيقٌ خصوميّ قبل التنفيذ):**
قفلا **`OD1`/`OD2`** (‏`test_bot.py:16453-16484`) **سيسقطان حتمًا** مع هذي الحزمة
— وسقوطُهما **متوقَّعٌ ومخوَّلُ التحديث** لا مفاجأة:
- ميكانيكيًّا: القفلان يقيّمان تعبيرَ الإنتاج بـ`eval` بغلوبلز فيها
  `_nf8_slot_free` وحدها ⇒ بعد إضافة `_position_risen` وشرط `continues` يرمي
  `NameError` ⇒ **أضِف `_position_risen` (وأيَّ اسمٍ جديد في التعبير) إلى غلوبلز
  الـ`eval` في القفلين**.
- دلاليًّا: عيّنة `OD1` الحرف `E` (‏`cont_status="continues"`) موسومة «حاملٌ»
  والمتوقَّع `["A","D","E","G"]` ⇒ **حدِّث العيّنةَ والمتوقَّع** (‏`E` يصير
  محرَّرًا بنصّ ⑫-ب) · وشاهدُ `OD2` نصُّه «‏"continues" **يحجز**» = **عكسُ ⑫-ب
  حرفيًّا** ⇒ **استبدله بشاهد «ليس إفراغًا شاملًا» الجديد**: `renewed` يحجز
  و`None` يحجز (وهما بالضبط `LS10`/`LS11`) — تحديثٌ بإقرارٍ مؤرَّخٍ وتشديدٍ
  (الشاهد الجديد أدقّ تفريقًا) لا إرخاء.
- أما دالّتا `_nf8_track`/`_nf8_slot_free` **فلا تُمسّان بحرف** وأقفالُهما
  الداخلية تبقى خضراء · وأقفال العرض القديمة تُفحَص.
**وأيُّ قفلٍ آخر يسقط خارج هذي القائمة المسمّاة ⇒ توقّف وراجع** — لا تحديث
بإرخاء؛ التحديث بإقرارٍ مؤرَّخ وتشديدٍ فقط.

---

## ⑥ ما لا يُمَسّ بحرف (بصمات قبل/بعد إلزامية)

`rank_key` · `select_top` · `fill_picks` · `in_entry_band` · `_nf8_track` ·
`_nf8_slot_free` · `classify_tier` · `entry_status` · `analyze_ticker` ·
`scan_market` · `backtest_symbol` · `apply_short_gate` · `apply_float_gate` ·
درع الصيّادين كاملًا (‏`_HUNTER_PINS`) · `replay10.py` · قاعدة «تُشطب بالستوب
فقط» · الوقف/الأهداف/الدفعات. **أوبيس يأخذ بصمة AST لهذه الدوالّ قبل أوّل سطر
ويطبع تطابقها في نهاية العمل.**

**خارج النطاق صراحةً (لا يُنفَّذ):**
- ~~⑫-ب مؤجَّل~~ — 🔴 **تحديث مؤرَّخ 2026-08-13: أمر المالك «نفّذ ⑫-ب»**
  (بعد شاهد `BOXL`) ⇒ صار **داخل** النطاق — مواصفته §①-ب أعلاه.
- أيّ مساس بالمُرتِّب أو بمقياس `d50@3` — ذاك ملفّ `T-RANKER3` المغلق.

---

## ⑦ بوّابات القبول (بالترتيب — كلّها شرط للدفع)

1. `python3 test_bot.py` ⇒ **خروج 0 وصفر فشل** (البوابة = الخروج لا عدد مقتبس).
2. الطفرات الستّ كلّها أسقطت أقفالها المسمّاة.
3. بصمات ⑥ مطابقة قبل/بعد.
4. **إعادة إنتاج قياس ①-ب/② على القائمة الحية:** سكربت تحقّق مؤقّت يطبع
   «حاملون قبل=20 · بعد ⑫-أ=14 · بعد ⑫-أ+⑫-ب=6 (‏9 خانات مفتوحة)» (بقيم يوم
   التنفيذ — قد تتغيّر بتحرّك الأسعار؛ المطلوب اتّساق المنطق لا الأرقام حرفيًّا).
5. `LOGIC_VERSION` مرفوع بوسم `listsplit`.
6. تحديث `CLAUDE.md` (بند قرار جديد) و`HANDOFF.md` (آخر عمل) بنفس الـcommit.
7. رسالة commit بالعربية تشرح الأمرَ والسندَ والقياس، وتنتهي حرفيًّا بـ:
   `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` ثم سطر
   `Claude-Session` القائم في ضوابط المشروع. **لا PR** — دفع مباشر لـ`main`
   (fetch+rebase عند الرفض). **لا معرّف نموذج في أيّ ملف مدفوع.**
8. بعد الدفع: سطر إبلاغ للمالك: «شغّل Daily Pivot Screener بـ`force_renew=1` مرّة».

---

## ⑧ ملاحظات سياق لأوبيس (اقرأ ولا تتصرّف بها)

- تجديد الجمعة (غدًا 2026-08-14 ‏22:07 UTC) سيعيد بناء القائمة — التعديل يعمل
  معه لا ضدّه: بعد التجديد يمنع تراكمَ الانحباس من جديد.
- أثر القياس على `T-RANKER3`: تحرير الخانات العليا شرطٌ مسبق لأيّ إعادة طرحٍ
  لسؤال «توب-3» — لا تُنفَّذ هنا.
- `BBLG` المتحرّر هامشيًّا (+1.8%) ليس «رابحًا» — لذلك صيغة العرض محايدة
  («فوق نطاق دخوله») لا احتفالية. لا تغيّرها.
