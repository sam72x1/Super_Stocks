# 😴 `T-DORMANT` — تسجيلٌ مسبق: **طولُ السكون** (لا الدوريّة)

> **أمرُ المالك (2026-08-28): «سجل الهدوء و كمل 1».** هذا العقدُ **مدفوعٌ قبل
> سطرِ كودِ أداةٍ واحد** ولا يُمَسّ بعد أوّل رقم.
> 📛 **والاسمُ `T-DORMANT` لا `T-QUIET`** — الأخيرُ مأخوذٌ لتجربةٍ **مسحوبةٍ قبل
> التشغيل** (`quiet_prereg.md`، أرضيةُ السيولة) ولا يُعاد استعمالُ اسمٍ محجوز.

## ⓪ لماذا — فرضيّةٌ وُلدت من تكذيبٍ لا من نجاح

`T-CADENCE` (اليومَ نفسَه · `cadence_result.md`) أعطت `C1 − C̄1` = **‏+0.019 ·
+0.145 · +0.100 ⇒ ‏+0.088R موجبًا في السنوات الثلاث بلا انقلاب** — وهي أقوى
مفتاحِ اختيارٍ قِيس على الفارز اليوميّ. **ولم تُعتمَد** (‏2 من 4).

🔴 **وتنبّؤي `CD-P2` مكذَّبٌ بمرتبة:** توقّعتُ وسيطَ إيقاعٍ ‏15-40 جلسة (فيصل
قال 20 و30) **والمقيسُ ‏113 · 104 · 99**. ⇒ عناقيدُنا بعتبة `EXPLOSION_PCT`
=50% تصف حدثًا أندرَ بكثيرٍ ممّا يعدّه فيصل بالورقة.

⇒ **الفرضيةُ المُعاد صوغُها:** ما ميّزته `due` ليس «حان موعدُه الدوريّ» بل
**«مضى عليه سكونٌ طويل»** — وهو يتّسق مع مقيسَين مستقلَّين: `T-PRESS` (الحجمُ
شكلُ U والسلّةُ الهادئة `T1` أوّلًا في الثلاث) و`T-GOLD-ENTRY` (أسهمُ فيصل قبل
انفجارها هادئة · `gain5` وسيطُه ‏−8.26%).

## ⓪-ب 🔴 الدائريّةُ مُعلَنةٌ قبل أيّ رقم — وشرطٌ خامسٌ يقاومها

**الفرضيةُ وُلدت من هذي اللقطات الثلاث نفسِها** ⇒ إعادةُ قياسها عليها
**تثبيتٌ لا اكتشاف** (سابقةُ `hold_key2_result §⑨` حرفيًّا). ولأن ذلك يحابيها،
يُضاف **الشرطُ ⑤ في §④**: أن تُضيف `D1` أثرًا **فوق** ما أعطته `due` — وإلّا
فالمقياسان واحدٌ بثوبَين، **ويُعلَن ذلك نتيجةً لا عيبًا**.

## ① المقاييس — مثبَّتةٌ ولا تُحرَّك · وكلُّها من `df.iloc[:i]` حصرًا

| المقياس | التعريف | الدور |
|---|---|---|
| **`q_range`** | `max(High آخرَ 60 جلسة) ÷ min(Low آخرَ 60 جلسة) − 1` | **الحاكم** — **مستقلٌّ تمامًا عن `EXPLOSION_PCT`** |
| `q_absret` | وسيطُ ‏\|عائد الإغلاق اليوميّ\| على 60 جلسة | وصفيّ |
| `q_since` | `since_last` من `spike_gaps` **بالاسم** | **شاهد** — يعيد إنتاج ما قاسته `T-CADENCE` |

⚖️ **والنافذةُ 60 جلسة رقمُنا لا رقمُ فيصل** — تُوسَم **`engineering`** في
`FAISAL_SOURCE_LEDGER.md`. ⛔ **ولا مسحَ نوافذ:** تُطبَع ‏20 و120 **وصفيًّا
فقط ولا تُرقَّى مهما كانت أرقامُها** (درسُ `T-CLIFF-2`: اختيارُ الأجمل بعد
الأرقام تحريكُ هدف).

## ② المجتمعُ والأذرع

المجتمع: **لقطاتُ PIT الثلاث** ونفسُ تشغيلة `arms4.yml` ⇒ **الميزانيةُ الثابتة**
(‏قاعدةُ `karpathy/autoresearch`) — كلُّ الأذرع على نفس الصفقات.

**السلالُ بالمئينات داخل كلّ سنة — لا عتبةً مخترَعة:**

| الذراع | التعريف |
|---|---|
| **`D0`** | كلُّ الصفقات (الأساس) |
| **`D1`** | **أهدأُ ثلث** بـ`q_range` (**الحاكمة**) |
| **`D̄1`** | **الثلثان الباقيان** — شاهدُ التكذيب (مُكمِّلٌ لا طرفٌ مقابل) |
| `D2` | أعنفُ ثلث — **وصفيٌّ لا يحكم** |

⚖️ **والمقارنةُ الحاكمة `D1` مقابل مُكمِّله** (لا الطرفَين) — نفسُ بنية
`T-CADENCE` فتكون قابلةً للمقارنة معها، وتستعمل كلَّ البيانات.

## ③ المقياسُ الحاكم — و**سؤالُ المالك يُجاب بالرقم هذي المرّة**

**`R` المحقَّق لكلّ صفقة** — لا نسبةُ النجاح.

📌 **والمساندُ الأهمّ عمليًّا (وصفيٌّ لا يحكم): `bars_to_50` و`soon_50`** —
الجوابُ المباشر لـ«ادخل السهم وخلال أسبوعٍ ينفجر». 🔴 **وسببُ غيابهما أمسِ
مُشخَّصٌ ومكتوبٌ هنا قبل التشغيل: يملؤهما `_candle_augment` خلف `BT_CANDLE`،
و`arms4.yml` كان يشحن `BT_POTENTIAL` وحدَه — وملاحظتي في المُخرَج سمّت العلمَ
الخطأ.** ⇒ يُشحَن `BT_CANDLE=1` ويُصحَّح النصّ، **و`DV5` تمنع تكرارَه.**

## ④ المعايير — **خمسةٌ تلزم معًا**

1. `D1 − D̄1` **‏≥ +0.15R** مجمَّعًا — **عتبةٌ مُعادةٌ** من `T-STOP`/
   `T-LIBERATION`/`T-RECLAIM-INTRADAY`/`T-PIVOT-BOTTOM` ⛔ **ولا تُحرَّك**.
2. **موجبٌ في السنوات الثلاث بلا انقلاب.**
3. **فاصلُ ثقةٍ 95% للفرق — مطبوعًا من الأداة لا مُقدَّرًا — لا يلمس الصفر.**
4. أرضية **‏150 محسومةً مجمَّعًا و30/سنة لكلّ ذراع**.
5. 🔴 **الإضافةُ فوق `due`:** داخل شريحة **`due=False` وحدَها** يبقى
   `D1 − D̄1` **موجبًا في الثلاث**. سقوطُه ⇒ **«المقياسان واحد»** يُعلَن
   نتيجةً، ولا تُعتمَد `D1` بديلًا عن `due` ولا تُضاف إليه.

## ⑤ بوّاباتُ الصلاحية — **تُنفَّذ في الكود لا تُوصَف**

> 🔴 **درسُ اليوم بعينه (`libvol_result §④`): `LV0` كُتبت حارسًا وأُخرجت سطرًا
> إخباريًّا — «حارسٌ مكتوبٌ وغيرُ منفَّذٍ ليس حارسًا، هو نيّة».** ولذلك كلُّ
> بوّابةٍ أدناه **لها رمزُ خروجٍ صريحٌ ينفُذ**، وقفلُ سويّةٍ يُثبت أنها توقف.

| البوّابة | الشرط | الإخفاق |
|---|---|---|
| **`DV0`** | `D0` يُعيد أساسَ `cadence_result.md` **بت-بت** (‏−0.198 · −0.123 · −0.094) | خروج **3** |
| **`DV1`** | السلالُ الثلاث غيرُ فارغةٍ **وتفترق** | خروج **4** |
| **`DV2`** | `q_range` محسوبٌ لـ**‏≥90%** من المحسومة | خروج **3** |
| **`DV3`** | لا نظرَ مستقبليّ: المقاييسُ من `df.iloc[:i]` حصرًا | قفلٌ سلوكيّ |
| **`DV4`** | **فاصلُ الفرق مطبوعٌ فعلًا** (لا فاصلا ذراعَين وحدَهما) | قفلُ سويّة |
| **`DV5`** | كلُّ حقلٍ يُطبَع في المُخرَج **مملوءٌ فعلًا** بعلمه المشحون — و`bars_to_50` يُسمّي `BT_CANDLE` | قفلُ سويّة |

⚖️ **و`DV0` قابلةٌ للاستيفاء بالبناء** — تُقارِن برقمٍ من **نفس الإعداد ونفس
اللقطات ونفس اليوم**، لا برقمٍ قِيس قبل ثلاثةِ قراراتٍ للمالك (‏وهو ما جعل
`LV0` مستحيلةً). **الدرسُ مطبَّقٌ لا مقتبَس.**

## ⑥ التنبّؤات — قابلةٌ للكسر · وأوّلُها يراهن على الفشل

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| `DM-P1` | **لا تبلغ +0.15R** — أرجّح الفشل (سبعُ سقطاتٍ لعائلة «سلوكٌ يميّز المنفجر») | تبلغها |
| `DM-P2` | **الشرط ⑤ يسقط** — `q_range` و`due` يقيسان الشيءَ نفسَه | يصمد ⇒ السكونُ محورٌ مستقلّ |
| `DM-P3` | وسيطُ `bars_to_50` **فوق 5 جلسات** ⇒ «خلال أسبوع» متفائل | ‏5 أو أقلّ |
| `DM-P4` | نصيبُ `soon_50` من المُعبَّئين **دون 10%** | فوقها |
| `DM-P5` | أهدأُ ثلثٍ **سعرُه أدنى** (ارتباطٌ بالسعر) ⇒ لا يُنسَب للهدوء ما هو للسعر | لا ارتباط |

## ⑦ سقفُ النجاح — مثبَّتٌ سلفًا

**اقتراحٌ للمالك + سطرُ عرضٍ («‏😴 ساكنٌ منذ N جلسة · نطاقُ ستّين X%»)** —
⛔ **ولا بوّابةَ رفضٍ ولا وزنَ ترتيبٍ ولا مسَّ بـ`M1`-`M14` ولا بالوقف ولا
بالأهداف ولا بأيّ عتبة** مهما كانت الأرقام.

## ⑧ حدودُ صدقٍ — مكتوبةٌ قبل الأرقام

1. 🔴 **الدائريّةُ قائمةٌ ولا يزيلها الشرط ⑤** — يخفّفها ولا ينفيها؛ **والنظيفُ
   الوحيد حصادٌ أماميّ**.
2. **النافذةُ 60 جلسة `engineering`** — ولا تُنسَب لفيصل، ولا يُسحَب عليها مسحٌ.
3. **السلالُ بالمئينات داخل السنة** ⇒ حجمُ كلِّ ذراعٍ ثابتٌ بالبناء (‏≈الثلث)
   فالفرقُ **جودةُ اختيارٍ لا عددُ صفقات** (درسُ `T-COUNT`).
4. **لمسٌ لا تنفيذ** وانحيازُ بقاءٍ وتشويهُ تقسيمات — **بالتساوي على الأذرع**.
5. `bars_to_50` **لمسٌ لا تنفيذ** ⇒ سقفٌ متفائل · وأفقُه `CANDLE_SOON_BARS`=10
   جلسات فـ«الأسبوع» يُقرأ داخله (‏5 جلسات) ولا يُمَدّ.
6. **`q_range` يرتفع بالتقسيمات العكسية** في السلسلة المعدَّلة ⇒ قد يَسِم
   المقسَّمَ «عنيفًا» بغير حقّ — **قيدٌ يُعلَن ولا يُصحَّح بعد الأرقام**.
