# 🏦 `T-HRT` — تسجيلٌ مسبق: **«اي سهم يتخارج منه صندوق hrt يعطي 200٪ بالراحه»**

> **أمرُ المالك (2026-09-23): «سجّل HRT».** وتعريفُه كما عرضتُه قبل الأمر: **مِجَسُّ بياناتٍ
> لإيداعات الصندوق أوّلًا، ثمّ عقد.** والمِجَسُّ جرى ثلاثَ جولاتٍ في Actions قبل هذا العقد
> (‏§①)، وحُذف قبل الدمج.
> **تسجيلٌ فقط:** بناءُ الأداة يحتاج أمرًا مستقلًّا («ابن أداة HRT»)، وتشغيلُها أمرًا ثالثًا
> («شغّل HRT»). **قراءةٌ/بحثٌ فقط:** صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · **ولا يُشحَن شيءٌ
> مهما كانت النتيجة.**

---

## §⓪ الدعوى ومصدرُها — حرفيًّا

**`TG_50826` — فيصل على X (‏2026/9/23 · الكاتالوج `§تاسع وعشرون`):**
> «اي سهم يتخارج منه صندوق hrt يعطي 200٪ بالراحه · لذلك احرص ع الاسهم خارج الصندوق»

- **وسياقُها** محادثةٌ منشورةٌ داخل الصورة نفسِها:
  - **A.Alghamdi** (طرفٌ ثالث · `third_party`): «كان متولجه صندوق hrt وباع نص كمياته يوم 10/9».
    قالها ردًّا على قول فيصل: «مثل العقاري ماله سوابق ارتفاع».
  - **وسؤالُ «كيف اعرف ؟» بلا جواب.**
- **الوسم:** قولُ فيصل **`faisal_verbatim`** · وجملةُ A.Alghamdi **`third_party`**.
- **الدعوى المقيسة:** **خروجُ هذا الحامل من سهمٍ يسبق صعودًا بنحو ‏+200%.** و«اي سهم» تعني أنها
  **شبهُ كلّيّة**.

---

## §① ما أثبته المِجَسّ — **وقائعُ من SEC لا استنتاج**

ثلاثُ تشغيلاتٍ في Actions (EDGAR وحدَه · بلا أيّ سعر):
- [`35891207060`](https://github.com/sam72x1/Super_Stocks/actions/runs/35891207060)
- [`35891549229`](https://github.com/sam72x1/Super_Stocks/actions/runs/35891549229)
- [`35892031431`](https://github.com/sam72x1/Super_Stocks/actions/runs/35892031431)

### ⓐ الهُويّة — `U-15` محسومٌ إلى «مرجَّحٌ جدًّا»
- **المالكُ المُبلِغ في نماذج 3/4 على WHLR هو `HRT FINANCIAL LP`:**
  - الرقم لدى SEC: `CIK 1475597`.
  - الاسمُ السابق: `HRT FINANCIAL LLC`.
  - العنوان: **3 World Trade Center, 175 Greenwich St, New York**، وهو مقرّ **Hudson River Trading**.
  - ⇒ **«hrt» = HRT Financial LP. الثقة: `STRONGLY_SUPPORTED`** لا `CONFIRMED`. SEC لا تكتب
    «Hudson River Trading» بالحرف، والربطُ بالعنوان.
- **وهو وسيطٌ وصانعُ سوق، لا «صندوق»:**
  - سجلُّه يحمل إيداعات `X-17A-5` و`FOCUSN`، وهي تقاريرُ الوسطاء.
  - وحواشي نماذجه تقول: **«Resulting in short sales»**.
  - ⇒ لفظُ «صندوق» في المصدر تجوّزٌ يُقال ولا يُبنى عليه.

### ⓑ WHLR (المرجَّح أنه «العقاري» — Wheeler Real Estate) — **التاريخُ مؤكَّد، و«النص» غيرُ مؤكَّد**

| الإيداع | قُبل (ET) | ما فيه |
|---|---|---|
| Form 3 | 2026-09-10 16:08:33 | يملك **15,717** سهمًا (مباشرة) |
| Form 4 | 2026-09-10 16:12:28 | شراء `P` **8,197** بـ$0.39 في 09-08 (بعدها 7,520) · شراء `P` **11,253** بـ$0.376 في 09-09 (بعدها 3,733) |
| **Form 4** | **2026-09-11 14:19:08** | **بيع `S` ‏5,048 بـ$0.369 في 2026-09-10 (بعدها 1,315)** |

- ⇒ **«يوم 10/9» مؤكَّدٌ بإيداع SEC**: بيعٌ في ‏2026-09-10، عُلن في ‏2026-09-11 14:19 ET.
- ⇒ **«نص كمياته» غيرُ مؤكَّد:**
  - النسبةُ تتغيّر بالقاعدة: ‏5,048 من 6,363 ≈ ‏79%، أو من 15,717 ≈ ‏32%.
  - والأرقامُ **لا تتّسق حسابيًّا**: شراءٌ يتبعه رصيدٌ أصغر، وبيعٌ أكبرُ من الرصيد المُعلَن قبله.
  - والحاشيةُ تقول «بيعٌ على المكشوف».
  - ⇒ **مخزونُ صانعِ سوقٍ يدور، لا تخارجُ مستثمر.**
- **والحجم:** البيعُ ‏≈ **‏1,863$**، **ومركزُ «مالك 10%» كلُّه بضعةُ آلاف دولار**.
  - صفةُ «مالك 10%» جاءت من **صِغَر عدد أسهم الشركة** بعد التقسيمات، لا من حجم الاستثمار.
- **وتوقّعُ الحزمة السابق كان خاطئًا ويُصحَّح:** إيداعاتُ `13G/A` على WHLR من **Magnetar** لا من
  HRT.
  - إشارةُ HRT في **نماذج 3/4** (‏مالك 10%) لا في 13G.

### ⓒ سجلُّ HRT مالكًا — **الظاهرةُ وُلدت في 2026**

- ‏**321** إيداعًا منذ 2011-02-28.
- **Form 4 بالسنة:** ‏2023: **1** · ‏2024: **3** · ‏2025: **4** · **‏2026: ‏167** (حتى 09-17).
- **Form 3 بالسنة:** ‏1 · ‏2 · ‏4 · **‏66**.
- **وما فيه بيعٌ `S`:** ‏1 · ‏3 · ‏3 · **‏117** إيداعًا، على **‏41 مُصدِرًا** في 2026.
- **«خروجٌ كامل»** (رصيدٌ صفرٌ بعد آخر بيع): 2026: **19** · 2024: **1**.
- **تأخّرُ الإيداع عن آخر بيع:** وسيطُه **يومان** وأقصاه **خمسة**.
- **و`13F-HR`** أربعةٌ في السنة منذ 2015 (‏47). ولماذا لا تُستعمَل: تُقرأ بعد ‏45 يومًا من نهاية
  الربع، وهي مخزونُ نهاية ربعٍ لآلاف الرموز، وربطُ CUSIP بالرمز خارجَ ما عندنا. ⇒ **ليست
  إشارةَ الدعوى** («يتخارج» يومًا بيوم).

⚖️ **الأثرُ على التصميم يُكتب الآن لا بعد الرقم:**
- **قاعدةُ «ثلاثُ سنواتٍ» مستحيلة**: ‏7 إيداعات بيعٍ فقط في 2023-2025.
- **وأرضيّةُ ‏150 مستحيلة** في 2026 وحدَها: ‏41 مُصدِرًا.
- ⇒ **لا فرعَ يُثبت الدعوى في هذا العقد.** هذي **ذراعٌ وصفيّةٌ** تُجيب سؤالًا واحدًا: **أيستحقّ
  المحورُ حصادًا أماميًّا أم يُغلَق؟**

---

## §② المجتمع والوحدة ولحظةُ الإشارة

- **المصدر:** نماذجُ Form 4 **الأصليّة** (لا `4/A`؛ التعديلاتُ تُعَدّ وتُطبَع) من `CIK 1475597`.
- **النطاق:** من 2026-01-01 حتى **2026-09-23 (يومِ هذا التسجيل)**. **وما بعده لا يدخل.**
  - وإيداعاتُ 2023-2025 السبعة **تُطبَع منفصلةً ولا تُعَدّ.**
- **الوحدة = «حلقةُ خروج»:**
  - أوّلُ Form 4 فيه بيعٌ `S` على سهمٍ عاديٍّ غيرِ مشتقّ لكلّ مُصدِر.
  - **ولا تبدأ حلقةٌ جديدة للمُصدِر نفسِه** إلّا بعد **‏20 جلسةً بلا بيع** (نافذةُ الطيّ نفسُها في
    `T-LINK100`).
  - ⇒ لا تُعَدّ سلسلةُ إيداعاتٍ متتالية حلقاتٍ مستقلّة.
- 🔒 **لحظةُ الإشارة `t0` = وقتُ قبول الإيداع في EDGAR (`ACCEPTANCE-DATETIME` · ET) لا تاريخُ
  العمليّة.**
  - التاريخُ يُعرف بعد يومين في الوسيط وخمسةٍ في الأقصى.
  - ⇒ القياسُ منه نظرٌ في المستقبل.
- **سعرُ المرجع `p0`** = افتتاحُ أوّل دقيقةٍ نظاميّةٍ **عند `t0` أو بعده**.
  - قُبل بعد ‏16:00 ET ⇒ افتتاحُ ‏09:30 في الجلسة التالية.
  - الشموع `adjusted=false`.
- **النتيجة:**
  - **`mr20`** = أعلى سعرٍ في **‏20 جلسةً** بعد `p0`، مقسومًا على `p0`، ناقص 1.
  - **`hit200`** = بلغ `mr20` **‏+200% فأكثر**.
  - وصفيًّا: `hit100` ووسيطُ `mr20`، **والصعودُ في الجلسات الخمس قبل `t0`**.
- **الحلقةُ «معلَّقة»** إن لم تكتمل جلساتُها العشرون عند التشغيل. **تُطبَع ولا تدخل نسبةً.**
- **تقسيمٌ في [‏t0 − 3, t0 + 20] جلسة ⇒ تُستبعَد الحلقة**، ويُطبَع العدد.
- 🔴 **حلقةُ WHLR (‏2026-09-10) تُطبَع منفصلةً ولا تُعَدّ:**
  - هي **المثالُ الذي وُلدت منه الدعوى**.
  - **وأعرف صعودَها** (‏+213% يوم 09-23، من `TG_50817`).

---

## §③ الذراعُ والضوابط

| الذراع | التعريف |
|---|---|
| **`S-EXIT`** 🥇 | `hit200` لحلقات الخروج |
| **`C-BUY`** 🥇 | **ضبطٌ حاكم:** «حلقاتُ الشراء». أوّلُ Form 4 **شراؤه `P` وحدَه بلا بيع** لكلّ مُصدِر، بالقواعد نفسِها: القبول، والطيّ 20 جلسة، والمرجعُ نفسُه، والنافذةُ نفسُها |
| `C-F3` | **وصفيّ:** «حلقاتُ الدخول». Form 3 (أوّلُ عبورٍ لـ10%) بالقواعد نفسِها |
| `C-SELF` | **وصفيّ:** المُصدِرُ نفسُه في **‏20 جلسةً تنتهي قبل أوّل إيداعٍ لـHRT عليه بعشرين جلسة**. هذا ميلُ السهم نفسِه إلى ‏+200% |

⚠️ **ولماذا `C-BUY` حاكم:**
- HRT لا تبلغ «10%» إلّا في **أسهمٍ عددُ أسهمها ضئيل**، وهذي **متفجّرةٌ بطبيعتها**.
- فإن تبع ‏+200% **شراءَها** بقدر ما يتبع **بيعَها**، فالميزةُ **للسهم لا للخروج**.
- **وصانعُ السوق يبيع لمن يشتري**، فبيعُه يتكتّل **أثناء** الصعود.
- ⇒ **الخروجُ وحدَه لا يُعَدّ إشارة.**

---

## §④ ما يُقاس ويُنشَر

- **`HR1`:** `hit200(خروج)` و`hit200(شراء)`، **كلٌّ بفاصل Wilson 95% ومقامِه**، والفرقُ بينهما.
- **`HR2` «بالراحه»:** هل بلغ **نصفُ** حلقات الخروج ‏+200%؟
  - **الحدُّ الأعلى لفاصل Wilson** مقارنةً بـ‏**50%**.
  - ⚠️ **‏50% قراءتي لكلمة «بالراحه» واللفظ «اي سهم»، وسمُها `engineering` لا فيصليّ.** تُكتب هنا
    قبل الرقم كي لا تُعايَر بعده.
- **وصفيًّا:** `hit100` · وسيطُ `mr20` · صعودُ الأيّام الخمسة قبل `t0` (هل تتكتّل المخارجُ بعد
  صعود؟) · `C-F3` · `C-SELF`.

🔒 **كلُّ نسبةٍ مقرونةٌ بمقامها.**

---

## §⑤ فروعُ الحكم — ثلاثةٌ لا غير، **وليس بينها فرعٌ يُثبت الدعوى**

1. **الفرعُ 1 — «إشارةٌ وصفيّةٌ موجبة»:**
   - `hit200(خروج)` **أعلى** من `hit200(شراء)`.
   - **و**الحدُّ الأعلى لفاصل Wilson لـ`hit200(خروج)` **‏50% فأكثر**.
   - ⇒ **ليس إثباتًا** (‏§①-ⓒ). **يُعرَض على المالك قرارُ حصادٍ أماميّ بعقدٍ جديد**، أو الإغلاق.
2. **الفرعُ 2 — «لا ميزة»:** القياسُ صالحٌ **ولم يستوفِ الفرعَ 1**:
   - إمّا `hit200(خروج)` **لا يزيد** على `hit200(شراء)`.
   - أو الحدُّ الأعلى لفاصله **دون ‏50%**.
   - ⇒ **إشارةٌ سالبةٌ تُنشَر، ويُوصى بإغلاق المحور.**
3. **الفرعُ 3 — «لا قياس»:** يقع في أيٍّ من الحالات التالية:
   - حلقاتُ خروجٍ مقيسةٌ **أقلُّ من 20**.
   - حلقاتُ شراءٍ مقيسةٌ **أقلُّ من 20**.
   - سقوطُ حارس.
   - ⇒ **يُعلَن بعدده ولا يُفسَّر.**

**ولا فرعَ رابع.** ⛔ **ولا يُخفَّض ‏20 ولا ‏50% ولا ‏+200% ولا نافذةُ ‏20 جلسةً بعد رؤية أيّ رقم.**

---

## §⑥ حرّاسٌ تُطبَع في كلّ تقرير

- **`V-H1` هُويّةُ المالك:** `rptOwnerCik` = **1475597** في كلّ إيداعٍ يُقرأ، والاسمُ يحمل
  `HRT FINANCIAL`. وإلّا **خروج ‏7**.
- **`V-H2` تغطيةُ EDGAR:** ما قُرئ من الإيداعات المُدرجة **‏95% فأكثر**. المِجَسّ قرأ 173 من 175.
- **`V-H3` `t0` من `ACCEPTANCE-DATETIME` وحدَه.** ويُطبَع عددُ الحلقات التي **سبق** فيها تاريخُ
  العمليّة `t0`، والمتوقَّعُ أنها كلُّها.
- **`V-H4` تغطيةُ الأسعار:** **‏90% فأكثر** من الحلقات، وإلّا الفرعُ 3.
- **`V-H5` قراءةٌ فقط** (فحصٌ بالـAST) · **بلا سرِّ تلغرام** · وSEC بهويّة `SEC_UA` نفسِها.
- **`V-H6`** الجذورُ الاثنا عشر **بصمتُها مطابقة**، والإنتاجُ **لا يستورد** الأداة.
- **`V-H7`** حلقةُ WHLR ‏2026-09-10 **خارجَ العدّ ومطبوعةٌ منفصلة**.

---

## §⑦ ما لا يُمَسّ

- ⛔ **الجذورُ الاثنا عشر** · و`sec_recent_filings` · وكلُّ قنوات SEC في البوت.
- ⛔ **لا `LOGIC_VERSION` ولا شحنَ حقلٍ أو سطرِ عرضٍ مهما كانت النتيجة.**
  - **ولا وسمَ «سهمٌ في الصندوق» في أيّ كرت** بلا عقدٍ جديدٍ وأمر.
- ⛔ **ولا يُبنى شيءٌ قبل أمر «ابن أداة HRT».**

---

## §⑧ حدودُ صدقٍ تُقال قبل النتيجة

1. **معرفتي المسبقة تُعلَن:**
   - أعدادُ المِجَسّ (‏§①).
   - وصعودُ WHLR (‏مستبعَد).
   - **وعيّنةُ الجولة الثالثة** طبعت ستّة رموزٍ من 2023-2025، وكلُّها **خارجَ العدّ**:
     `COOL` · `SMFL` · `ADTX` · `XHG` · `MULN`.
   - **وأعرف عن `MULN` و`ADTX` أنهما ممّن تكرّرت تقسيماتُهم العكسيّة وانهارا.**
   - ⇒ **ميلي المسبق أن مراكز HRT بصفة «10%» تقع في أسهمٍ منهارةٍ ضئيلةِ الأسهم.**
   - **ولم أنظر في سعرِ أيّ حلقةٍ من 2026.**
2. **سنةٌ واحدةٌ وعيّنةٌ صغيرة** ⇒ **وصفيّةٌ بنصّ قاعدة «ثلاثُ سنوات».**
3. **صانعُ سوقٍ «يبيع على المكشوف».** «الخروج» في Form 4 **ليس تخارجَ مستثمر**. الدعوى قد تصف ظاهرةً
   حقيقيّة **بآليّةٍ غيرِ التي يتخيّلها قائلُها**، والعكسُ وارد.
4. **الأرقامُ في نماذج صانع السوق لا تتّسق حسابيًّا** (‏§①-ⓑ) ⇒ **لا يُقاس «باع النصف»**. يُقاس
   **وجودُ البيع** وحدَه.
5. **‏+200% في ‏20 جلسةً تعريفي أنا للدعوى**، وسمُه `engineering`. فيصل لم يذكر نافذة.
6. **توزيعُ إيداعات 2026 على الأشهر لم يُطبَع في المِجَسّ.**
   - إن تكتّلت في أغسطس وسبتمبر **عُلِّق كثيرٌ منها عند التشغيل**.
   - آخرُ حلقةٍ في النطاق (‏09-23) تكتمل جلساتُها العشرون **نحو ‏2026-10-22**.
   - ⇒ **تشغيلٌ قبل ذلك قد ينتهي بالفرع 3** («لا قياس») **بسبب المعلَّق لا بسبب الدعوى**.
     **ويُقال ذلك في التقرير بعدده.**

---

## §⑨ تنبّؤاتي — تُنشَر مكذَّبةً أو مؤكَّدة

| # | التنبّؤ | ثقتي |
|---|---|---|
| `HP1` | `hit200(خروج)` **لا يزيد** على `hit200(شراء)` بفرقٍ ذي معنى. البيعُ والشراءُ في الحلقات نفسِها | **عالية** |
| `HP2` | الحدُّ الأعلى لفاصل `hit200(خروج)` **دون ‏50%**، أي «بالراحه» لا تصمد | **عالية** |
| `HP3` | وسيطُ صعود الأيّام الخمسة **قبل** `t0` في حلقات الخروج **فوق ‏+30%**، أي أن المخارج تتكتّل بعد صعود | متوسّطة |
| `HP4` | حلقاتُ الخروج بعد الطيّ (مقيسةً ومعلَّقةً معًا) بين **‏30 و60** | متوسّطة |

---

## §⑩ الخطوةُ التالية — بأمر المالك وحدَه

- **«ابن أداة HRT»**: الأداة `hrt_arms.py` ومعها workflow يدويّ، بلا كرون ولا سرِّ تلغرام،
  وأقفالُها وطفراتُها. **وصفرُ رقمٍ قبل دمجها.**
- ثمّ **«شغّل HRT»**: `hrt_result.md` بالحكم وتنبّؤاتي مكذَّبةً أو مؤكَّدة.
- **وجوابُ «كيف اعرف ؟»** (‏سؤالُ وليد) صار معروفًا مصدرًا:
  - **نماذج Form 3/4 لـ`CIK 1475597` في EDGAR تُعلَن خلال يومين.**
  - ومعرفةُ المصدر **ليست** دليلًا على الدعوى.

---

## §⑪ ملحقُ البناء — «ابن أداة HRT» (‏2026-09-23 · قبل أيّ رقم)

> **مضافٌ ولا يُعدّل ما قبله.** يسمّي ما احتاجه البناءُ ولم ينصّ عليه العقد. كُتب **قبل أيّ سعرٍ وقبل أيّ
> تشغيلٍ للأداة**، والتشغيلُ بأمرٍ مستقلّ («شغّل HRT»).

- **ⓐ الأداةُ والتشغيل:** `hrt_arms.py` + `.github/workflows/hrt.yml` يدويٌّ بلا كرون **وبلا سرِّ تلغرام**.
  - سرّاه: `POLYGON_API_KEY` (الأسعار) و`SEC_CONTACT` (هويّةُ `SEC_UA` **بالاسم** من `Super_stock`).
  - مُدخَلان: `dry` (جدوى EDGAR وحدَه **بلا سعرٍ واحد**) و`tsv`. والصفوفُ `hrt_rows.tsv` تُرفَع دائمًا.
- **ⓑ المصادر:**
  - قائمةُ الإيداعات من `data.sec.gov/submissions/CIK0001475597.json` (‏`recent` ثمّ `files`).
  - والإيداعُ من نصّه الكامل (`.txt`) تحت مجلّد المالك. ولا شيءَ آخر من SEC.
  - الشموعُ دقيقيّةٌ `adjusted=false`، بنداء مدًى واحدٍ لكلّ حلقة (ويُتبَع `next_url`).
  - والتقسيماتُ `link100_probe.splits_of` بالاسم (بُنيت لـ`T-LINK100`: تقسيماتُ الرمز — والغرضُ نفسُه).
- **ⓒ «سهمٌ عاديّ»:** عنوانُ الورقة في جدول **غير المشتقّ** يحمل `common` أو `ordinary` (بلا حساسيّةٍ لحالة الأحرف).
  وكلُّ عنوانٍ مستبعَدٍ يُطبَع باسمه وعدده.
- **ⓓ تصنيفُ الإيداع:**
  - خروجٌ = Form 4 أصليٌّ فيه عمليّةُ `S` واحدةٌ على الأقلّ.
  - شراءٌ = Form 4 أصليٌّ فيه `P` **وصفرُ `S`**.
  - دخولٌ = Form 3 أصليّ.
  - و`4/A`/`3/A` تُعَدّ وتُطبَع ولا تدخل ذراعًا.
- **ⓔ جلسةُ `t0` و`p0`:**
  - قبولٌ قبل إغلاق يومِ تداول (بحدّ `session_info` — فيومُ الإغلاق المبكّر 13:00) ⇒ ذلك اليوم. وإلّا يومُ التداول التالي.
  - `p0` = افتتاحُ أوّل دقيقةٍ نظاميّةٍ **عند `t0` أو بعده** في تلك الجلسة.
  - ولا دقيقةَ نظاميّةَ فيها ⇒ الجلسةُ التالية، ويُعَدّ ذلك ويُطبَع.
- **ⓕ الطيّ:** حلقةٌ جديدةٌ للمُصدِر (‏`issuerCik`) في الذراع نفسِها **فقط** إن وقعت جلستُها **بعد أكثر من 20 جلسةً**
  من جلسة **آخر إيداعٍ مؤهَّلٍ** في تلك الذراع (لا من بدء الحلقة).
- **ⓖ `mr20`:** الجلسةُ الأولى جلسةُ `p0` (من دقيقته)، ثمّ 19 جلسةً كاملة ⇒ 20 جلسة.
  - والقمّةُ من **الدقائق النظاميّة وحدَها** بحدود `session_info` لكلّ يوم.
  - جلسةٌ ناقصةُ الدقائق لا تُسقط الحلقة؛ وغيابُ كلّ دقائق النافذة ⇒ «بلا سعر».
- **ⓗ «معلَّقة»:** جلستُها العشرون بعد آخر جلسةٍ مكتملةٍ قبل يوم التشغيل (بتوقيت نيويورك).
- **ⓘ `pre5`** = آخرُ إغلاقٍ نظاميٍّ قبل `t0` ÷ الإغلاقُ النظاميّ قبله بخمس جلسات − 1.
- **ⓙ WHLR — تشديدٌ لا إرخاء:** **كلُّ** حلقات المُصدِر WHLR في **كلّ** الأذرع خارج العدّ ومطبوعةٌ منفصلة.
  العقدُ ذكر حلقةَ الخروج وحدَها، وصعودُ السهم معروفٌ فيلوّث أيَّ ذراعٍ يدخلها.
- **ⓚ `C-SELF`:** لكلّ مُصدِرٍ في المجتمع نافذةُ 20 جلسةً **تنتهي قبل أوّل إيداعٍ لـHRT عليه (أيِّ نموذج) بعشرين جلسة**.
  و`p0` فيها افتتاحُ أوّل دقيقةٍ نظاميّة.
- **ⓛ حسمُ التنبّؤات آليًّا:**
  - `HP1` مؤكَّدٌ إن **تداخل** فاصلا Wilson، أو لم يزد الخروجُ على الشراء. وإلّا مكذَّب.
  - `HP2` مؤكَّدٌ إن كان الحدُّ الأعلى لفاصل الخروج دون 50%. وإلّا مكذَّب.
  - `HP3` بوسيط `pre5` لحلقات الخروج المقيسة (فوق ‏+30% ⇒ مؤكَّد). وبلا `pre5` ⇒ **لا يُحسَم**.
  - `HP4` بعدد حلقات الخروج بعد الطيّ (مقيسةً ومعلَّقةً وبلا سعر) خارج WHLR.
- **ⓜ رموزُ الخروج:** 0 حكم (الفرع 1 أو 2) · 2 مفتاحٌ غائب · 5 حارسٌ قبل الجلب (`V-H5` · `V-H6`) ·
  7 الهُويّة (`V-H1`) · 9 «لا قياس» (الفرع 3، ومنه سقوطُ `V-H2` و`V-H4`).
- **ⓝ `V-H5`/`V-H6`:**
  - `selfcheck_readonly` و`importers` **نسختان محلّيّتان في الأداة**، مطابقتان سلوكيًّا لنظيرتيهما في
    `waves_probe` (بُنيتا لـ`V-W5` و`V-W6`-ب — والغرضُ نفسُه)، **ويُقفَل تكافؤُهما** (`HRA11`).
  - 🔴 **لماذا لا تُستوردان بالاسم — أمسكه البناءُ قبل أيّ رقم:** استيرادُهما من `waves_probe` يجعل
    `hrt_arms.py` **مستوردًا لأداة الموجات**، فيُسقط حارسَها `V-W6`-ب («لا مستوردَ لأداة الموجات»)
    ويُخرج كلَّ تشغيلٍ قادمٍ لها بخروج ‏5. أمسكه قفلُ `WVA1` في السويّة.
  - `tc_yield_arms.production_untouched` (بُنيت لـ`V-Y7`: `Super_stock.py` بت-بت مع `origin/main`).
  - `optrade_arms.no_config_assign` (بُنيت لـ`V-T4`-ب: صفرُ إسنادٍ إلى `CONFIG`).
  - و`kasih_scan.wilson` (بُنيت لفاصل Wilson 95% بالنسبة المئويّة — والغرضُ نفسُه).
- **ⓞ ⚠️ إسقاطٌ حسابيّ يُقال قبل الرقم:** آخرُ حلقةٍ في النطاق (09-23) تكتمل جلستُها العشرون **2026-10-21**
  بتقويم المستودع — والعقدُ قال «نحو 10-22». تشغيلٌ قبله يحمل معلَّقاتٍ بعددها.
- **ⓟ نافذةُ استبعاد التقسيم — العقدُ حرفيًّا، وتصحيحٌ قبل أيّ رقم:**
  - النافذةُ **[جلسة `t0` − 3، جلسة `t0` + 20]** كما في §② بالحرف (‏`split_window` · `SPLIT_POST` = 20).
  - **وتمتدّ فقط** إلى آخر جلسةٍ في نافذة `mr20` إن أُزيح `p0` (ⓔ) فتجاوزتها. **تشديدٌ لا إرخاء:**
    تقسيمٌ داخل نافذة القياس الفعليّة يُفسد `mr20` على شموعٍ غيرِ مُسوّاة.
  - 🔴 **ومسوّدةُ البناء استعملت [−3، ‏+23] ثابتًا** — أوسعَ من العقد بثلاث جلسات، فكانت ستستبعد حلقاتٍ
    يُدخلها العقد. **أمسكتها مراجعةُ ما قبل الالتزام** وصُحِّحت قبل أيّ تشغيل، ويحرسها `HRA12`.
