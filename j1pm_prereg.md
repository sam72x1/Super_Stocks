# 🌅🥇 `T-J1PM` — تسجيلٌ مسبقٌ أماميّ: **«J1 في البريماركت»** هل يصمد على حالاتٍ لم تُرَ؟

> **أمرُ المالك (2026-09-02): «الثنتين»** — الشارةُ عرضًا الآن، **والتسجيلُ الأماميّ** هذا.
> مدفوعٌ **قبل سطرِ كودِ أداةٍ واحد وقبل أيّ رقم**، ولا يُمَسّ بعد أوّل حصاد. أيُّ تعديلٍ لاحق = ملحقٌ مؤرَّخ.

## ⓪ لماذا أماميٌّ لا تاريخيّ

`T-TIERLINK` (‏`tierlink_result.md`) وجدت أن الميزات الستّ العابرة **ظاهرةٌ واحدة**: مِرساةٌ في
البريماركت بفجوةٍ ‏≥30% وسيولةِ خمسٍ قويّة = **«J1 في البريماركت»** — ‏**29.2% (‏7 من 24)** تبلغ ‏+50%
مقابل أساس **‏10.3%**، وJ1 خارج البريماركت **‏0 من 9**. 🔴 **لكنّ**: العيّنة 7 من 24 · والنصفُ الثاني
**‏1 من 9** · والرابطُ **دائريٌّ جزئيًّا** (‏J1 اعتُمدت في التصنيف على فترةٍ متداخلة) ⇒ الاختبارُ النظيف
**حالاتٌ لم تُرَ عند التسجيل**. والتاريخُ ممتنع: `k2`/`J1` وُلدتا 2026-08-20 ولا لقطاتِ دقائقَ لما قبلها
في السجلّ الأماميّ.

## ① التعريفُ الإجرائيّ — مثبَّت

- **المصدر:** `tier_fwd_ledger.jsonl` (كرتُ `M5` لكلّ مِرساةٍ **سُلِّمت** للمالك — يكتبه `record_tier_fwd`
  بعد نجاح الإرسال) عبر `tier_fwd_report.load_ledger` **بالاسم** (أوّلُ صفٍّ لكلّ (تاريخ، رمز)).
- **الشريحةُ الحاكمة `A`:** `j1 == True` **و** المِرساةُ قبل **‏09:30 نيويورك** (‏`anchor_ms` ⟶ ساعةُ
  نيويورك `< 9.5` — تعريفُ `tod == "pre"` في `tierlink_probe` حرفيًّا).
- **المُكمِّل `Ā`:** كلُّ صفٍّ آخر في السجلّ (‏J1 خارج البريماركت · وبلا J1 في أيّ وقت).
- **المقياس:** `exploded50` = أقصى قمّةِ **يوم المِرساة** ‏≥ ‏+50% من سعر كرت `M5` (`e5`) **بلا قصٍّ
  بنيويّ** — بدالّتَي `tier_fwd_report.fetch_day` (‏`adjusted=false`) و`tierlink_probe.full_day_max`
  **بالاسم** = المقياسُ الذي أُنتجت به أرقامُ `T-TIERLINK` ⇒ مقياسٌ واحد لا اثنان.
- **والرفيقُ الوصفيّ:** `mg_cut` (قبل الخروج البنيويّ) يُطبَع ولا يحكم.

## ② البدايةُ الأماميّة

**تُحسَب الصفوفُ التي تاريخُها بعد تاريخ دفع هذا الملفّ حصرًا** (‏`J1PM_SINCE` = تاريخُ الكوميت + يوم).
وما قبله **يُطبَع فقرةً مستقلّة موسومًا «داخل العيّنة — لا يحكم»** (هو مجتمعُ `T-TIERLINK`).

## ③ المعيار — الأربعةُ تلزم معًا

1. نسبةُ `A` **‏≥ ‏2×** نسبةِ `Ā` (عتبةٌ مُعادة من معيار `T-TIERLINK` الرباعيّ)
2. فاصلا Wilson 95% **منفصلان**
3. **الأرضية: ‏≥ 60 صفًّا محسومًا في `A` و‏≥ 150 مجمَّعًا** (‏60 = رقمٌ مُعاد من `T-BORROW-FWD`؛ وبتراكم
  `T-TIERLINK` ‏≈24 حالة/أسبوعين ⇒ **‏≈5-6 أسابيع** قبل أوّل حكم — يُعلَن الآن لا يُكتشَف بعده)
4. **الثبات عبر نصفَي الفترة الأمامية** (كلٌّ منهما `A` أعلى من `Ā`)

**قبل بلوغ ③ يُطبَع «لا حكم» والعدّادان وحدَهما**، والحكمُ يُكتب **مرّةً واحدة** عند أوّل بلوغٍ ولا
يُعاد فتحُه بتحريك رقم.

## ④ التنبّؤات — قبل أيّ رقم

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| `JP-P1` | نسبةُ `A` الأمامية **دون** 29.2% المنشورة (انكماشُ الدائريّة) لكن **فوق** ‏2× المُكمِّل | ‏≥29.2% أو دون 2× |
| `JP-P2` | `A` تبلغ 60 صفًّا خلال **‏4-8 أسابيع** من الدفع | أقلّ من 4 أو أكثر من 8 |
| `JP-P3` | J1 **خارج** البريماركت تبقى دون الأساس (كما 0/9) | تتجاوزه |
| `JP-P4` | الأكبرُ انفجارًا (‏≥+150%) في الفترة الأمامية **خارج** `A` (الرابطُ يلتقط التكرار لا الحجم) | داخلها |

## ⑤ سقفُ النجاح

**إن عبرت الأربعة:** اقتراحٌ للمالك بترقية الشارة إلى **مفتاح ترتيبٍ أو بوّابةِ تسليمٍ** — **بتسجيلٍ
مسبقٍ جديد** (لا يُنفَّذ من هذا العقد). ⛔ **ولا كتمَ لغير `A` مهما كانت الأرقام** (الأكبرُ انفجارًا خارجه).
**والشارةُ عرضًا** (`🌅 J1 في البريماركت` على كرت `M5`) **تُشحَن اليوم بأمره مستقلّةً عن هذا الحكم.**

## ⑥ حدودُ صدق — قبل النتيجة

1. **«انفجر» لمسُ قمّةٍ لا صفقة** (سقفٌ متفائل) — والرفيقُ `mg_cut` يقصّه.
2. **المجتمعُ ما سُلِّم للمالك** لا كونُ السوق (السجلُّ يُكتب بعد الإرسال).
3. **الشارةُ المشحونة اليوم قد تغيّر سلوكَ المالك لا السجلّ** (السجلُّ يُكتب قبل أيّ قرارِ دخول).
4. **`anchor_ms` بالتوقيت الصيفيّ/الشتويّ يُحسَم بـ`zoneinfo`** لا بإزاحةٍ ثابتة.
5. الأداةُ **قراءةٌ فقط** · بلا كرون · بلا تلغرام · والإنتاجُ لا يستوردها.

**التوقيع:** مدفوعٌ قبل أيّ رقمٍ وقبل أيّ سطرِ أداة.
