> 🔒 **قراءة فقط.** لم أعدّل حرفًا. كلُّ رقمٍ له `ملف:سطر` فتحتُه بنفسي في هذي الجلسة. وما ليس كذلك موسومٌ **【غير مُتحقَّق】** أو **【تقدير】**.
> 🔴 **وأثقلُ ما فيها: أسحب الرقم الذي بُنيت عليه v1 كلُّها.**

# 🏗️ الارتكاز v2 — الخطة النهائية بعد الطعون الثلاثة

## ⓪ ما تغيّر عن v1 — في عشرة أسطر

1. **«إثراء ‏8.6×» مسحوب.** قسمةُ مقامَين مختلفين (‏20 جلسة مقابل 5) — والدليل في الكود لا في الرأي.
2. **ويُستعاد بتشغيلةٍ واحدة بلا سطر كودٍ واحد** (‏`cost_days`/`cost_cap` مدخلان في الـworkflow) ⇒ صار البند **الأول**.
3. **م0-أ③ (تفويض `backtest_portfolio` لـ`replay10`) حُذف** — يكسر قفلًا نصّيًّا يوقف كل دفع، وهو غير لازم أصلًا.
4. **صيّاد الظرف يُولَد صامتًا** (artifact/`jsonl`) — لا تلغرام إلا بعد التقليم **وبإذنٍ صريح**؛ «الجاهز فقط» مقفولة باختبارٍ باسم القرار.
5. **M13/M14 تُطبَّقان داخل الصيّاد** — وإلّا سلّم فئةً أخرجها المالك بقرارٍ صريح.
6. **عتبة «‏≤300/يوم» أُلغيت** — تُستعمل المسجَّلة كما هي؛ تلييُنها بعد رؤية الرقم هو تعريف ما نتّهم به غيرنا.
7. **م2 لا تُنفَّذ بضبط اثني عشر مفتاحًا** — بل بمصدرٍ واحد يشترك فيه الصيّاد والباكتيست، وإلّا قاست مجتمعًا غير الذي بناه م1.
8. **م2 بذراعين إلزاميًّا** (كونٌ كامل · كونٌ يستبعد الواحد والثلاثين) — وإلّا كانت دائريةً بالبناء.
9. **م0-ب أُعيد تعريفها**: البِركة الحيّة **1-10/يوم** ⇒ تسجيلُ المؤهَّلين بلا قيمة؛ القيمة في تسجيل **المرفوضين وجدارهم**.
10. **وحكم م2 يُعلَن سلفًا «وصفيّ داخل العيّنة» لا حكمَ ربحية** — ‏27 تسجيلًا مسبقًا استنفدت المدى، ولم يبقَ نظيفٌ إلا **الأماميّ**.

---

## ① جدول الطعون — الحكم على كلٍّ منها

### أ. اعتراضات **صمدت** وعولجت

| # | الاعتراض | ما تحقّقتُ منه بنفسي | العلاج |
|---|---|---|---|
| **A1** | مقاما الالتقاط والتمييز مختلفان | الالتقاط `any(...)` على **كل** جلسات الرمز (`catalog_envelope.py:384`) والنافذة **20** (`:25`) · التمييز `if seen[k]` على **5** جلسات (`:409`،`:434`) | **م-أ**: تشغيلةٌ واحدة بـ`cost_days=20` |
| **A2** | «‏151/يوم» تقديرُ عيّنةٍ لا قياس | توسيعٌ خطّيّ `per_day * len(universe)/tested` (`:440`) على عيّنة `universe[:cap]` (`:416`، `cap=600` في `:408`) | **م-أ**: `cost_cap=4000` ⇒ **الكون كلّه، بلا عيّنة أصلًا** |
| **A3** | شاهد الضبط غير عشوائيّ | `universe[:cap]` = أوّل 600 بترتيب قاموس اللقطة (`:416`) | يزول تمامًا بـ**م-أ** (لا قصّ ⇒ لا انتقاء) |
| **A4** | م2 دائرية | `run_backtest` في وضع السوق يمسح `get_universe()` كاملًا بلا استبعاد (`Super_stock.py:15618-15622`) | **ذراعان إلزاميًّا**؛ والنافذ عند الاختلاف هو **المستبعِد** |
| **A5** | م1 لا تُصادقها م2 | `MIN_RR_T1=-1e9` في الإرخاء (`catalog_envelope.py:64`) يكتم نقصًا يُضاف في الإنتاج (`Super_stock.py:3144-3147`)، وحافّة `P90` للعائد **1.72** أشدُّ من حدّنا **1.00** (`catalog_envelope_result.md:41`) | **مصدرٌ واحد** `envelope_scan.py` يستورده الاثنان ⇒ تطابقٌ **بالبناء** |
| **A6** | العائلة الإحصائية بلا تصحيح | **‏27** ملفَّ `*_prereg.md` (عددتُها) على نفس اللقطة `30598885456` ونفس السنوات | حكم م2 **وصفيّ داخل العيّنة**؛ والمدى النظيف = **م0-ب الأماميّة** |
| **B1** | تفويض `backtest_portfolio` يكسر قفلًا | `"replay10" not in _rp_src` يقرأ **كامل** `Super_stock.py` (`test_bot.py:12610`)، و`replay10.py:19` ينصّ «لا تُستورَد في `Super_stock.py`» | **حُذف البند**؛ البديل مُشغّلٌ خارجيّ على قالب `replay10_arms.py:32` |
| **B2** | علم م2 يلزمه صفّ وإلّا أسقط القفل | قفل P1 **مُعمَّم على كل الـworkflows** (`test_bot.py:12738-12747`) | **لا علم `BT_*` إطلاقًا** — workflow مستقلّ كـ`catalog_envelope.yml` |
| **B3** | `backtest.yml` عند سقف 25 مقفول | `_wf_dispatch_inputs(...) == 25` (`test_bot.py:10685`) | نفس العلاج (workflow مستقلّ) |
| **B4** | اللقطة تنتهي بعد 90 يومًا | `retention-days: 90` (`.github/workflows/freeze.yml:44`) واللقطة as-of **2026-07-31** (`catalog_envelope_result.md:5`) | **م-أ فورًا**؛ وإعادة تجميدٍ عند التجاوز |
| **B5** | م0-ب حمولتها ≈ صفر | حسبتُ البِركة من `weekly_watchlist.json`: **1-10/يوم · وسيط 3.5** على 16 يومًا مسجَّلًا | **أُعيد تعريفها**: تسجيل **المرفوضين + الجدار الأول** |
| **C1** | خرق «الجاهز فقط» | القاعدة وحدُّها مقفولان **باختبارٍ باسم القرار** (`CLAUDE.md:355-368`)، وسابقةُ قصّ رادار التقسيم من 12 إلى المطابق وحده (`:357`) | **الصيّاد صامت**؛ والرسالة بندٌ مستقلّ بإذن المالك |
| **C2** | خرق قرار M14 الصلب | البوّابتان تُطبَّقان في `scan_market` حصرًا (`Super_stock.py:9473-9475`)، **وHTZ نفسه داخل الكاتالوج** (`catalog_envelope.py:45`) | **M13/M14 فلترٌ نهائيّ في الصيّاد** + حسم تناقض HTZ قبل البناء |
| **C3** | تليين عتبةٍ مسجَّلة | المسجَّل «فوق 50 ⇒ يلزمه مُرتِّبٌ مُثبَت» (`catalog_envelope_design.md:86-90`) | **«‏≤300» أُلغيت** |
| **C4** | C3/B3 عمياوان عن الإرخاء | تأكيدا **قيمةٍ عند الاستيراد** (`test_bot.py:9056`،`:9064`)، والإرخاء زمن التشغيل (`catalog_envelope.py:203-205`) و`NEAR_PCT` يُقرأ **داخل** `analyze_ticker` (`Super_stock.py:2790`) | **قفلٌ سلوكيّ**: `analyze_ticker` قبل/بعد الأداة يتطابق بت-بت |
| **C5** | «‏NEAR_PCT لم تُختبَر ولا مرّة» غير دقيق | `BT_CORE5` يُرخيها مع `SCORE_MIN`/`MIN_RR_T1`/`RSI_NOW_HARD` (`Super_stock.py:644-647`) | **صُحّحت العبارة**: «لم تُختبَر **منفردة**» |
| **C6** | «رسالة رابعة» أقلُّ من الواقع | المسار اليوميّ **ثلاثة** نداءات (`Super_stock.py:13425`،`:13428`،`:13446`) ‏+ ثلاثة صيّادين يرسلون حتى في يوم «لا يوجد» (`split_hunter.py:250-262`) | **صُحّحت**: «تيّارٌ سابع» — والمنعُ منصوصٌ في `OPUS_EXECUTION_PACKAGE.md:849-851` |
| **C7** | م0-ب بلا حارسٍ ولا سقف | سابقةُ سقوط التشغيلة قبل `git_save` موثّقة · ونمطُ القصّ الجاهز 56 يومًا (`Super_stock.py:9345`) · و`*.jsonl` غير مستثنى في `.gitignore` (قرأتُه) و`.git` = **154M** | `try/except` مطلق + سقف + كتابةٌ بعد كل عملٍ حرج |

### ب. اعتراضات **سقطت** — ولماذا (لا تُتجاهل بصمت)

| # | الادّعاء | لماذا سقط |
|---|---|---|
| **F1** | «نظرٌ مستقبليّ في نافذة القياس» | **الطاعن نفسه نقضه، وأكّدتُه**: `for j in range(start, idx)` ويوم الانفجار مستبعَدٌ صراحةً (`catalog_envelope.py:249`). النظر المستقبليّ **في اختيار المِرساة فقط**، وهو مُعلَنٌ في التصميم — ويصير قاتلًا **لحظة أن تُشتقّ منه م2 حكمَ ربحية على كونٍ يحوي رموزَ المِرساة** ⇒ ولهذا الذراع المستبعِد. |
| **F2** | «‏2.4× هو الرقم الصحيح» | **وسمه الطاعن [مرجَّح] ولم أستطع إعادة اشتقاقه.** ⇒ لا أستبدل رقمًا غير مقيس برقمٍ غير مقيس: الصياغة النافذة **«الإثراء غير محسوم حتى تُعاد التشغيلة»**. |
| **F3** | «‏api.github.com مفتوح لا محجوب» | **الاثنان غير دقيقين، وقِستُ بنفسي**: `rate_limit` ⟶ **200** · لكن `repos/microsoft/qlib` ⟶ **403** («‏access to this repository is not enabled for this session») · و**Actions API لمستودعنا ⟶ 200** · و**سجلّ التشغيلة نفسه ⟶ فشل** لأن الوجهة `productionresultssa17.blob.core.windows.net` محجوبة. ⇒ الوصف الصحيح: **بياناتُ مستودعنا مقروءة، وسجلّاتُ التشغيلات والمستودعات الخارجية لا.** |
| **F4** | «طبّق `inside_envelope` مرشِّحًا على مُخرَج الفارز» (توصية A5) | **سليمُ التشخيص، خاطئُ العلاج**: ذلك يقيس **تقاطعًا داخل** الفارز، والظرف مقترَحٌ **مجتمعًا بديلًا أوسع** ⇒ يجيب سؤالًا آخر. العلاج المعتمَد **أقوى**: مصدرٌ واحد يجعل المجتمعين متطابقَين **بالبناء** (وله سابقة: `cliff_scan.py` تستورده أداتان). |
| **F5** | «`fails_result` نتيجةٌ عدميّة قوية» (استشهاد v1) | **سُحبت في الملفّ نفسه** (`fails_result.md:3-8`) لغياب `TOST` ⇒ الصياغة النافذة «لا تظهر حمايةٌ كبيرة، والتكافؤ غير مُثبَت». |

---

## ② الرقم المسحوب — وثمنه بصراحة

`catalog_envelope_result.md:18` يعلن **«الإثراء خارج العيّنة ‏≈ 8.6×»** = ‏64.5% ÷ 7.5% (`:15`).
- **البسط** يُحسب بـ`any(...)` على جلسات الرمز داخل نافذة **20** (`catalog_envelope.py:384`،`:25`).
- **المقام** يُحسب بـ`seen[k]` على **5** جلسات (`:409`،`:434`).

⇒ **كسرٌ بسطُه وحدةٌ ومقامُه أخرى.** والملفّ نفسه يقرّ أن الكلفة **عيّنة** (`:66`) وأنه يفترض تمثيليّتها في التوسيع الخطّيّ (`catalog_envelope.py:440`).

**والصياغة النافذة حتى تصدر م-أ:** «الظرف **يعمّم ولا يحفظ** — والالتقاط ‏64.5% خارج العيّنة رقمٌ صالح. أمّا **الإثراء فغير محسوم**.»
✅ **وما لا يسقط بسقوط الرقم:** انهيارُ الالتقاط ‏10 نقاط فقط بين داخل العيّنة وخارجها (`catalog_envelope_result.md:19-21`) — وهو مقياسُ التعميم، ولا يعتمد على المقام إطلاقًا.

📌 **وثالثةٌ اكتشفتُها ولم يذكرها أحد:** ملفّ النتيجة نفسه يكتب «‏151 مقابل سعة **15**» (`:57`) — **والسعة الحيّة 10** (`Super_stock.py:254`). فرقمُ السعة المعطوب سرى من محاكي المحفظة إلى ملفّ النتيجة إلى عقد التصميم (`catalog_envelope_design.md:110`).

---

## ③ المبدأ المعماريّ المصحَّح — «مصدرٌ واحد» لا «أداتان متشابهتان»

```
envelope_scan.py   ← المصدر الوحيد للقرار (نقيّ · بلا شبكة · بلا حالة)
  ├── EDGES        ← حواف P90 مجمَّدة في envelope_p90.json + snapshot_id + run_id
  ├── relaxed(S)   ← مدير السياق الوحيد الذي يمسّ CONFIG (ببصمة sha256 قبل/بعد)
  ├── measure()    ← ينادي analyze_ticker الإنتاجيّ (لا يعدّله)
  └── decide()     ← inside_envelope + M13/M14 + التقليم النصّيّ
        │
        ├── envelope_hunter.py  (حيّ · عمليّة منفصلة · صامت · قالب split_hunter.py)
        └── envelope_bt.py      (بحث · لقطة مجمّدة · يستورد replay10 مباشرةً)
```

**لماذا هذا الشكل بالذات — بسندٍ لا ذوق:**
- **يقتل A5 بالبناء**: مستحيلٌ أن يختلف مجتمع م1 عن م2 لأن الدالّة واحدة.
- **يقتل B1/B2/B3**: لا استيراد `replay10` داخل `Super_stock` (القفل `test_bot.py:12610` سليم) · لا علم `BT_*` (قفل P1 `:12738` سليم) · لا مدخل في `backtest.yml` (قفل 25 `:10685` سليم). والنمط **مُثبَتٌ عمليًّا**: `replay10_arms.py:32` يستورد الاثنين معًا في مُشغّلٍ خارجيّ.
- **له سابقة في المستودع**: `cliff_scan.py` بُني مصدرًا واحدًا تستورده أداتان **لهذا السبب حرفيًّا**.
- **العزل بالعمليّة**: `catalog_envelope.py:203-205` يأخذ لقطة `CONFIG` من حالةٍ عالمية ⇒ أيُّ توازٍ يترك البوّابات مُعطَّلةً بصمت. الفصلُ بجوبٍ منفصل هو ما يمنع ذلك — **فإن اختُصر سقط الأمان كلُّه**.

⚠️ **وتصحيح صياغةٍ إلزاميّ**: «صفر مسّ بالجذور» **صحيحٌ نصًّا ومضلِّلٌ سلوكًا**. الصياغة الأمينة: **«الجذور byte-identical، وسلوكُها مُرخًى داخل الأداة وحدها، ومقفولٌ بقفلٍ سلوكيّ.»**

---

## ④ المراحل

### 🔧 م-أ · إعادة قياس الكلفة بمقامٍ موحّد — **تشغيلةٌ واحدة، صفر كود**

- **ما تحلّه:** A1 + A2 + A3 + B4 دفعةً واحدة.
- **التنفيذ:** `catalog_envelope.yml` بـ`workflow_dispatch`: نفس `frozen_run_id` · **`cost_days=20`** · **`cost_cap=4000`** (‏`universe[:4000]` = الكون كلّه ⇒ **لا عيّنة ⇒ لا انحياز انتقاء**).
- **مخرجٌ ثالثٌ مجّانيّ:** الأداة تطبع «‏انفجر {date}» لكل رمز (`catalog_envelope.py:260`) ⇒ **تُنشَر التواريخ الواحد والثلاثون** — وهي مدخل الذراع المستبعِد في م2.
- **المقياس المكتوب قبل التشغيل:** يُنشَر جدولٌ بأربعة أرقام (التقاط `LOO` · مطابق/يوم · التمييز · نسبتهما) **بمقامٍ واحد 20 جلسة**، ويُشطب `8.6×` ويحلّ محلّه الرقم الجديد أيًّا كان.
- **الحكم:** 🟢 الظرف يمضي إن بقي الإثراء **≥3×**. 🔴 يُوقَف إن نزل تحت **2×** (= لا يميّز عمليًّا). 🟡 بينهما ⇒ يمضي **بوسمٍ صريح** وقرار المالك.
- **جذور:** لا. **إذن مالك:** لا (تشغيلة قياسٍ بحتة).
- ⚠️ **قيد زمن**: مهلة الـworkflow **120 دقيقة** (`catalog_envelope.yml`). و【تقدير الطاعن، غير مُتحقَّق منّي】 ‏≈0.068 ث لنداء `analyze_ticker` المكتمل ⇒ ‏3349×20×0.068 ‏≈ 76 دقيقة. **بديلٌ آمن عند التجاوز: `cost_cap=1200`** (‏≈27 دقيقة) مع بقاء المقام موحّدًا.

---

### 🗂️ م-ب · سجلّ المرفوضين اليوميّ — **الشاهد الأماميّ الوحيد غير المُنقَّب**

- **ما تحلّه:** A6 (استنفاد المدى) + الفجوة المقيسة أن `record_reject_stats` يحفظ **أعدادًا بلا رمزٍ واحد** (`Super_stock.py:9331-9345`).
- **🔴 وتصحيحٌ لـv1:** كنت أقترح تسجيل **البِركة المؤهَّلة** — والمقيس أنها **1-10/يوم (وسيط 3.5)** مقابل سعة 10 ⇒ **المُرتِّب لا يقصّ شيئًا اليوم** ⇒ التسجيل بلا قيمة. **القيمة في المرفوضين**: الرمز + جداره الأول + قيمُه العددية.
- **موضع الكتابة:** عند **المُنادي** لا الجذر — `scan_market` يُرجع البِركة (`Super_stock.py:9490`) ومُنادياه `run_weekly_renewal`/`run_daily_watchlist` وليسا جذرين.
- **قيدٌ إلزاميّ:** **ممنوع أيُّ حقلٍ من `enrich`** — `enrich` (`:4341`) يُنفَّذ **بعد** `select_top` (`:13011-13013`) ⇒ حقولُه تسريبٌ صريح لأي نموذجٍ لاحق.
- **الحرّاس:** `try/except` مطلق · سقفٌ وتدوير على نمط 56 يومًا (`:9345`) · الكتابة **بعد** كل عملٍ حرج · واختبارٌ يثبت أن انهيارها لا يُسقط المسار اليوميّ.
- **المقياس:** بعد **20 يوم تداول**: صفوفٌ > 0 · توزيع الجدار الأول حيًّا · وتنوّع الرموز. 🔴 فشلت إن كان السجلّ فارغًا أو ثابت الرموز.
- **جذور:** لا. **إذن مالك:** نعم (كتابةٌ داخل مسارٍ إنتاجيّ).

---

### 📏 م-ج · المسطرة الأمينة — **بلا مسّ `backtest_portfolio` إطلاقًا**

- **العائق المقيس:** `backtest_portfolio` يستبعد `outcome=="no_fill"` **قبل** السعة (`Super_stock.py:13609`) و`outcome` مجهولٌ وقت الإشارة ⇒ **نظرٌ مستقبليّ** · وسعتُه **15** (`:13600` ⟵ `:332`) مقابل **10** حيًّا (`:254`) · ويحرّر بـ`toordinal()` **تقويميًّا** (`:13612`) · ويرتّب بمحورين فقط (`:13604`) مقابل أربعة في `rank_key` (`:9156-9165`) · **ووصفُه يقول «بلا نظر مستقبلي»** (`:13598`).
- **🔴 القرار الجديد: لا يُفوَّض ولا يُصلَح — بل يُعتزَل.** التفويض يكسر قفل `test_bot.py:12610`، والإصلاح في مكانه ينشئ **محرّكَين يتفرّقان بصمت** (وهو صنف العطب الذي اكتوى به المستودع مرارًا).
- **البديل:** مُشغّلٌ خارجيّ على قالب `replay10_arms.py:32` — يستورد `Super_stock` و`replay10` معًا، و`run_backtest` يُرجع الصفقات سلفًا (`Super_stock.py:15968`)، و`replay10.CAPACITY = 10` (`replay10.py:27`).
- **المقياس:** يُعاد الأساس `F0` على نفس اللقطة والسنوات، ويُنشر **قبل/بعد** لثلاثة أرقام: صافي R · المأخوذون · المرفوضون بالسعة. 🔴 **عدّاداتٌ متطابقة بت-بت ⇒ بصمة `no-op` ⇒ تُعاد لا تُفسَّر.**
- **جذور:** لا. **إذن مالك:** لا. **بندٌ اختياريّ للمالك:** سطر تحذيرٍ داخل `backtest_portfolio` يُعلن أرقامه متجاوَزة.

---

### 🎯 م-د · `envelope_scan.py` + صيّادٌ **صامت**

- **ما تحلّه:** F1/F2/F4 من التشخيص (الفارز يرفض ‏99.8% من أيام أسهم فيصل — `additions_audit.md:52`؛ والجدار الأول `engineering` **94%** والوحيد **95%** — `:41-42`).
- **الشكل:** `envelope_hunter.py` + workflow، على قالب `split_hunter.py` بحرّاسه الستّة: أرضية تغطية **60%** (`split_hunter.py:32`) · بوّابة توقيت (`:99`) · حارس افتر (`:37`) · ختمٌ **بعد** الإرسال (`:127`) · إخفاقٌ صريح لا صمت (`:149`) · تجاوزٌ يدويّ.
- **🔕 يُولَد صامتًا:** يكتب `envelope_candidates.jsonl` ويُرفع artifact — **صفر `send_telegram`**. سببه مقيس: قاعدة «ما ابي يجيني اشعار نهائيًّا إلا بالأسهم الجاهزة فقط» واستثناءاها **محصوران** ومقفولان باختبارٍ باسم القرار (`CLAUDE.md:355-368`)، وسابقةُ رادار التقسيم قُصَّت من 12 لأنها «ضجيج يدفن الجاهز» (`:357`).
- **🔴 M13/M14 فلترٌ نهائيّ داخل الصيّاد:** معايير الظرف الأحد عشر **لا تشمل** الفلوت ولا الشورت (`catalog_envelope.py:65-79`)، والبوّابتان تُطبَّقان في `scan_market` وحده (`Super_stock.py:9473-9475`) ⇒ بلا ذلك يسلّم فئةً أمر المالك بإخراجها تمامًا.
- **⚠️ وتناقضٌ يُحسم قبل البناء لا بعده:** **HTZ داخل الكاتالوج** الذي عوير عليه الظرف (`catalog_envelope.py:45`)، وهو أحد سهمَي قرار M14 الصلب. إمّا يُسقَط وتُعاد الحواف، وإمّا يُعلَن صراحةً أن الظرف مُعاير على سهمٍ مُخرَجٍ بقرار.
- **القمع من مرحلتين:** ثمانية معايير من OHLCV مباشرةً (`entry_readiness` نقيّة، `Super_stock.py:9505`) ثم `analyze_ticker` للثلاثة الباقية. **وهو ادّعاءٌ لا برهان في v1** ⇒ يصير **قفلًا سلوكيًّا**: المساران يعطيان نفس مجموعة الرموز على اللقطة، وطفرةٌ تزحزح عتبة قمعٍ واحدة بـ`1e-9` **يجب** أن تُسقط القفل.
- **🔒 أربعة أقفال سلوكية (لا نصّية — الفخّ الموثّق):**
  1. بالـAST: `Super_stock.py` **لا يستورد** الوحدة ولا تُنادى دالّتها المُرخية من أيّ مسارٍ إنتاجيّ.
  2. `analyze_ticker` على عيّنةٍ مثبَّتة **قبل وبعد** الأداة ⇒ تطابقٌ بت-بت (يكشف تسرّب `CONFIG` فعلًا — وهو ما تعجز عنه C3/B3 لأنهما تأكيدا قيمةٍ عند الاستيراد: `test_bot.py:9056`،`:9064`).
  3. لقطةٌ واستعادة لـ`_REJECT_STATS`/`_REJECT_REASONS` + اختبارٌ يثبت أن المقام لم يتلوّث (`Super_stock.py:9331-9345`).
  4. الحواف تُقرأ من `envelope_p90.json` ولا تُحسب حيًّا ⇒ تغييرُ رقمٍ يحتاج commit مرئيًّا.
- **المقياس المسجَّل قبل التشغيل:** ‏① مطابقون/يوم حيًّا على 20 جلسة **مقابل جدول العتبات المسجَّل كما هو** (`catalog_envelope_design.md:86-90`) · ② **زمن التشغيل** — 🔴 **لا زمنَ مدوَّنًا في `catalog_envelope_result.md` إطلاقًا** (قرأتُ الملفّ كاملًا) ⇒ يُقاس ويُنشر · ③ التغطية · ④ **الدوران**: كم رمزًا **جديدًا** يدخل يوميًّا (مقياسٌ رابع أضافه الطعن بحقّ — العضوية حالةٌ ممتدّة لا لحظة، فـ«‏N/يوم» مخزونٌ لا تدفّق).
- **جذور:** لا (تُنادى `analyze_ticker` ولا تُعدَّل). **إذن مالك:** نعم (أداةٌ خامسة، ولو صامتة).

---

### 📮 م-هـ · التقليم بالنصّ — **قبل أيّ رسالة**

- **العائق:** الظرف يعطي عددًا كبيرًا والسعة **10** (`Super_stock.py:254`، والحساب في `:13278-13279`).
- **ولماذا لا مُرتِّبٌ متعلّم:** `T-RANKER` فشلت **1 من 5** (`ranker_result.md:34`)، **والبِركة كلُّها تُسلّم ‏0-3 منفجرين/سنة وأفضلُ عشوائيّ ‏2·2·5** (`:41-42`) ⇒ **لا مُرتِّب — ولو كان عرّافًا — يُسلّم ما ليس في البِركة** (`:45`).
- **البديل المسنود:** تقليمٌ **بشرطٍ فيصليٍّ منصوص** (حدثٌ مؤسِّس · ثبات 3 جلسات · شمعة انعكاسية) — منفَّذٌ ومُختبَرٌ سلفًا في `split_hunter.py`. وسندُه: شروط الصيّاد **5 من 5 بسندٍ حرفيّ** مقابل **صفر** لبوّابات الهوية (`additions_audit.md:91`)، والتفسير **قِصَر المسافة إلى النصّ** (`:94-95`).
- **المقياس:** بنفس مسطرة م-و — هل يرفع المنفجرين المُسلَّمين في `top-10`؟ **والعدّ الكامل يُعلَن دائمًا** («اليوم N داخل الظرف · معروضٌ M») فالقصّ الصامت ممنوعٌ بقاعدةٍ قائمة.
- **جذور:** لا. **إذن مالك:** نعم (اختيار المُقلِّم قرارُ منتج).

---

### 🧪 م-و · `T-ENVELOPE` — الحكم، **وصفيًّا داخل العيّنة**

- **المسار:** `envelope_bt.py` يستورد **نفس** `envelope_scan.decide` ⇒ صفر منطقٍ مستنسَخ وصفر ضبطٍ لاثني عشر مفتاحًا. الحسم بـ`_resolve_arm` (`Super_stock.py:13497`، ليست جذرًا) والمحفظة بـ`replay10` بسعة **10**.
- **🔴 ذراعان إلزاميًّا:** كونٌ كامل · وكونٌ **يستبعد الواحد والثلاثين** (تواريخُها من م-أ). **والنافذ عند الاختلاف هو المستبعِد**، ويُسجَّل ذلك **قبل أيّ رقم**.
- **الوسم الصالح وحده:** `mg_pre_stop`. **ممنوع** `exploded`/`fwd_max_gain` (ما بعد الوقف) و`sector`/`country`/`days_to_earnings` (ليست point-in-time).
- **بوّابة القبول — خمسة شروطٍ معًا، مكتوبةً قبل أيّ رقم:** ① موجبٌ في الثلاث بلا انقلاب · ② الفرق المجمَّع **‏≥0.15R** · ③ **‏≥30 محسومة/سنة** · ④ **اختبار دلالة صريح (`z`)** — 🔴 مضافٌ لأن `M4-ISO` استوفت معيارها ثم سقطت بـ**`z = 0.72`** (`m4_ladder_result.md:6-7`) · ⑤ **الحكم يصمد في الذراع المستبعِد**.
- **سقفُ النجاح مثبَّتٌ سلفًا:** اقتراحٌ للمالك + إبقاء الصيّاد يعمل — **لا** مسّ بـM1-M14 ولا `LOGIC_VERSION` مهما كانت الأرقام (`OPUS_EXECUTION_PACKAGE.md:849-851`).
- **🔴 حدُّ صدقٍ يُكتب قبل التشغيل، لا بعده:** ‏27 تسجيلًا مسبقًا على **نفس** اللقطة والسنوات ⇒ **لا مدًى غير مُنقَّب**، والظرف مُفصَّلٌ على أحداث 2026 فلا تصلح 2026 مدًى محجوزًا ⇒ **هذا حكمٌ وصفيّ داخل العيّنة، والحكم الأماميّ من م-ب وحدها.**
- **جذور:** لا. **إذن مالك:** نعم (تشغيلاتٌ ثلاث + تسجيلٌ مسبق).

---

### 📖 م-ز · دفتر الوصفات · 🚪 م-ح · بوّابةٌ واحدة (آخِرًا)

- **م-ز:** لا وصفةً مخترَعة — بل سدُّ الفجوة المقيسة: **مُرشّح الكليف حجب 124 يومًا عبر ثلاثة أسهم وهو بوّابة تكلفةٍ هندسية**، و`EHGO` سقط عليه **‏51/51 يومًا ولم يُقيَّم له شرطٌ آخر قطّ** (`hunter_six_result.md:18`،`:36`). **والمقياس مزدوج**: الالتقاط **و**المطابقون الزائدون على السوق الكامل — بلا الثاني يكون الالتقاط غشًّا.
- **م-ح:** **لا يُفتح قبل صدور م-و.** فتحُ بوّابةٍ واحدة يُدخل **‏2.9%** والوسيط **أربعة جدران** (`additions_audit.md:64-65`)، وبسعة 10 ومُرتِّبٍ غير حاسم كلُّ تعديلٍ يُقاس داخل ضجيجه (`m4_ladder_result.md:6-11`).

---

## ⑤ الجدول النهائيّ

| المرحلة | ما تحلّه (مقيسًا) | مقياس النجاح المكتوب **قبلها** | تمسّ جذرًا؟ | قرار مالك؟ |
|---|---|---|---|---|
| **م-أ** إعادة القياس | مقامان مختلفان (`catalog_envelope.py:384` مقابل `:409`) + عيّنة 600 (`:416`) | إثراءٌ بمقامٍ واحد: **‏≥3× يمضي · <2× يُوقَف** · ونشر التواريخ الـ31 | **لا** | لا |
| **م-ب** سجلّ المرفوضين | `record_reject_stats` أعدادٌ بلا رموز (`Super_stock.py:9331-9345`) · والمدى مُنقَّب (27 prereg) | بعد 20 يومًا: صفوف>0 · توزيع جدارٍ حيّ · تنوّع | **لا** (عند المُنادي `:9490`) | **نعم** |
| **م-ج** المسطرة | نظرٌ مستقبليّ (`:13609`) · سعة 15≠10 (`:13600`/`:254`) · تقويم (`:13612`) | إعادة `F0`: تحرّكٌ **مع إثبات أن الأداة اشتغلت**؛ تطابقٌ بت-بت ⇒ فشل | **لا** | لا |
| **م-د** الصيّاد الصامت | ‏99.8% رفضًا (`additions_audit.md:52`) · ‏94/95% `engineering` (`:41-42`) | مطابق/يوم مقابل الجدول المسجَّل · الزمن · التغطية · **الدوران** | **لا** | **نعم** |
| **م-هـ** التقليم | ‏0-3 منفجرين/سنة (`ranker_result.md:41`) والسعة 10 | يرفع المنفجرين المُسلَّمين في top-10 · والعدّ الكامل مُعلَن | **لا** | **نعم** |
| **م-و** T-ENVELOPE | «لم تُقَس الربحية إطلاقًا» (`catalog_envelope_result.md:55`) | البوّابة الخماسية أعلاه — ومنها **`z` صريح** و**الذراع المستبعِد** | **لا** | **نعم** |
| **م-ز** الوصفات | الكليف 124 يومًا · EHGO ‏51/51 (`hunter_six_result.md:36`) | الالتقاط **و** الزائدون على السوق الكامل معًا | **لا** | **نعم** |
| **م-ح** بوّابة واحدة | الجدار الوحيد ‏2.9% (`additions_audit.md:64`) | لا يُفتح قبل م-و | **نعم** | **نعم** |

---

## ⑥ البوّابات بأولوية الاستبدال — مصحَّحة

> **المعادلة:** (أضيق من `P90`) × (وسمُها `engineering`) × (**بلا** شاهد حمايةٍ مقيس) × (كلفتها).
> **والقاعدة المُلزِمة:** «من عندنا» ≠ «خطأ» (`additions_audit.md:60-62`).

| # | البوّابة | قيمتُنا | `P90` | الكلفة | المبرّر المقيس |
|---|---|---|---|---|---|
| **1** | `NEAR_PCT` (`Super_stock.py:277`) | 50 | **15** | **3273** | أعلى كلفةٍ في المنظومة (`additions_audit.md:70`) · **🔴 وتصحيح v1: اختُبرت داخل حزمة `BT_CORE5` لا منفردة** (`Super_stock.py:645`) · والقرينة الوحيدة سالبة (`T-LIBERATION` ‏≈−0.56R، `liberation_result.md:16`) |
| **2** | `WATCH_MAX_FAILS` (`:224`) | 3 | **6** | **3222** | ثاني أكبر كلفة · يقصّ **‏72/73/70%** مقابل فرقٍ ينقلب ‏+1.8/−1.5/+2.6ن (`fails_result.md:24-34`) — **بصياغته المصحَّحة**: لا حمايةٌ كبيرة، والتكافؤ غير مُثبَت (`:3-8`) |
| **3** | `RSI_OS_HARD` (`:145`) | 32 | **55** | 1968 | «أسهمُه لم تكن متشبّعة» (`catalog_envelope_result.md:36`) ⇒ تنفي افتراضًا أساسيًّا · و**27/40 بسندٍ حرفيّ، أمّا 32/50 فمن عندنا** |
| **4** | `MAX_DROP_PCT` (`:128`) | 97 | **99.7** | — | تعليقُ الكود «فخ تقسيم» **يناقض** تصحيح فيصل الموثّق · وNUWE يقرأ **908.25** لسهمٍ بـ$20.65 (`gates_result.md:63-72`) ⇒ **يُصحَّح التعليق فورًا** (نصّ لا رقم) والرقمُ بتجربة |
| **5** | `SCORE_MIN` (`:212`) | 45 | **45.00** | — | **متطابقٌ بالضبط** (`catalog_envelope_result.md:40`) ⇒ سهمٌ من فئة كاتالوج فيصل يُقصّ **بنقطةٍ واحدة** — هشاشةٌ لا تُهمَل |
| **6** | `RSI_NOW_HARD` (`:148`) | 50 | 68 | — | أضيق وبلا سند، لكنها خارج أعلى خمس كُلفات |
| **7** | `BASE_RANGE_MAX_PCT` (`:133`) | 40 | **593** | 2638 | **الأضيق ‏15× ومع ذلك ليست الأولى**: (أ) درعٌ حقيقيّ في سياقٍ لم يُقَس — ZCMD قاعدة **7,930%** = انهيارٌ ‏−99% (`gates_result.md:130`) · (ب) **الوحيدة المُجرَّبة والحكم النافذ «لا تغيير»** (`m4_ladder_result.md:11`). ⚠️ وأقوى شاهدٍ ضدّها قائم: **SPRC حجبته M4 وحدها ثم حقّق ‏+100%** (`additions_audit.md:77-81`) |
| **8** | `MIN_DOLLAR_VOL` (`:135`) | $200,000 | $124,792 | 1662 | تستوفي «أضيق» و«`engineering`» — **ويُخرجها الشرط الثالث وحده**: البوّابة **الوحيدة بشاهد حمايةٍ مقيس** (‏80%→17% · وYYAI بسيولة $86 ألف رُفض ثم نزل ‏−68%، `additions_audit.md:60-61`) |

**✅ يُبرّئها الظرف — لا تُلمَس:** `MIN_PRICE` 1.5 < 1.64 · `MIN_DROP_FLOOR` 40 < 63 · `PRIOR_SPIKE_FLOOR` 60 < 100 · `MIN_RR_T1` 1.0 < 1.72 (`catalog_envelope_result.md:32-42`).
🔒 **و`M3` هي الوحيدة الصحيحة، وهي الأقرب لنصّ فيصل** (`:42`) — وهذا وحده يُلخّص التشخيص.
❓ **غير قابلة للترتيب:** `SHORT_GATE_MAX` (`:156`) و`FLOAT_GATE_MAX` (`:169`) — خارج معايير الظرف الأحد عشر، ولا تُطبَّقان في `analyze_ticker` أصلًا بل في `scan_market` (`:9473-9475`). ومعهما `BASE_WINDOW` · `RECENT_RISE_BLOCK_PCT` · `MIN_BARS`.

---

## ⑦ الاثنا عشر مستودعًا

> 🔴 **حدُّ صدقٍ يسبق الجدول — وهو تصحيحٌ لـv1 ولأحد الطعون معًا:** قِستُ الوصول بنفسي في هذي الجلسة: `api.github.com/rate_limit` ⟶ **200** · `api.github.com/repos/<external>` ⟶ **403** («‏access to this repository is not enabled for this session») · `api.github.com/repos/sam72x1/Super_Stocks/actions/...` ⟶ **200** · وسجلّ التشغيلة ⟶ **فشل** (الوجهة `productionresultssa17.blob.core.windows.net` محجوبة).
> ⇒ **كلُّ ما يخصّ محتوى المستودعات الاثني عشر أدناه — الرخص والأحجام والاعتماديات — 【غير مُتحقَّق في هذي الجلسة】 ويجب التحقّق منه قبل أيّ قرار.** والتوزيعُ أدناه مبنيٌّ على **الملاءمة المعمارية للعائق المقيس عندنا**، وهو ما أستطيع الحكم فيه.

### أ. تُستعمل فعلًا (3)

**1️⃣ `karpathy/autoresearch` — الشكل لا الكود.** حلقتُه (عدِّل ← درِّب ← قِس ← أبقِ أو أسقط) هي حلقتُنا يدويّةً. **موضعه:** `envelope_lab/` مِشْحَذُ أذرع حيث «`program.md`» = التسجيل المسبق و«`train.py`» = **ملفّ أعلامٍ فقط** و«`val_bpb`» = المقياس المسجَّل.
🔴 **وانقلابٌ إلزاميّ في قاعدته:** حلقتُه **تُحسّن على مقياس التحقّق** — وهذا في مجالنا `p-hacking` بعينه. ⇒ **تُولّد الأذرع ولا تحكم**، والحكم خلف تسجيلٍ مسبق وإذن المالك. **وبلا هذا القيد تصير ماكينةَ خداعٍ ذاتيّ.**

**2️⃣ `addyosmani/agent-skills` — مهاراتٌ للوكيل، لا كودًا في البوت.** أربعٌ منها تقابل إخفاقاتٍ موثّقةً عندنا: «الشكّ» ⟵ «أثبت أن التجربة اشتغلت» (بصمة `no-op`) · «المصدر» ⟵ `FAISAL_SOURCE_LEDGER.md` · «التصحيح» ⟵ «اقرأ أيّ خطوة فشلت» · «المراجعة» ⟵ الأقفال والطفرات. **القيمة: تحويلُ دروسٍ دُفع ثمنُها إلى إجراءٍ لا يُنسى.**

**3️⃣ `multica-ai/andrej-karpathy-skills` — معيارُ مراجعةٍ لا نصًّا يُضاف.** «تغييراتٌ جراحية» ≡ صفر مسّ بالجذور · «تنفيذٌ مدفوعٌ بهدف» ≡ معيارٌ مكتوبٌ قبل التشغيل.
🔴 **ولا يُضاف منها سطرٌ إلى `CLAUDE.md`:** الملفّ **315,595 بايت** و`HANDOFF.md` **317,093** (قِستُهما) ويُقرآن كل جلسة ⇒ الزيادةُ ضرر.

### ب. مشروطة (1)

**4️⃣ `shiyu-coder/Kronos` — نموذج شموع.** **لا يُستعمل في الاختيار**: لا مؤشّر مشتقٌّ من OHLCV يميّز المنفجر داخل مجتمع الهوية، وهو يتنبّأ بنفس فئة المعلومات المنفيّة، **ولا يرى المضارب**. **موضعه المشروط:** ذراعٌ `K4` في إطار `T-RANKER`، **بعد م-و حصرًا وبشرطٍ واحد: أن يرفع الظرفُ عدد المنفجرين في البِركة فوق ‏0-3/سنة** (`ranker_result.md:41`) — **وبلا ذلك تشغيلُه هدرٌ مُبرهَن**. ⚠️ ويجرّ `torch`، و`requirements.txt` مثبَّتٌ `==` ببروتوكول ترقيةٍ صريح (قرأتُه: `yfinance==1.5.2` · `pandas==3.0.5` · `numpy==2.4.6` · `requests==2.34.2`).

### ج. أفكارٌ فقط (2)

**5️⃣ `microsoft/qlib` — 🔴 لا يُضاف إلى `requirements.txt`.** السبب الذي أملكُ دليله: أربع مكتبات مثبَّتة اليوم تُثبَّت **في كل جوب** بما فيها الرادار اللحظي، والتثبيت مقفولٌ ببروتوكول (`requirements.txt:12-14`). **【غير مُتحقَّق: تعارض `pandas<3` وحجم الاعتماديات】** ⇒ يُقاس بخطوة `workflow` قبل أيّ قرار. **ما يُؤخذ: فكرتان لا كود** — سعةٌ + تدوير (مشكلتنا حرفيًّا: `Super_stock.py:13278-13279`) وتحليل المخاطرة. **والبديل الخفيف: `lightgbm` وحده بهدف ترتيب.**

**6️⃣ `OpenBMB/PilotDeck` — لا يُستورَد كوده** (**【الرخصة غير مُتحقَّقة — يُتحقَّق قبل أيّ لمس】**)، وفكرتُه المركزية «عزل مساحة العمل» **منفَّذةٌ عندنا سلفًا** بأداةٍ مستقلّةٍ في عمليّةٍ منفصلة (`split_hunter.py`) — وهي عمودُ v2 في §③.

### د. اختياريّ (1)

**7️⃣ `MemPalace/mempalace` — طبقة ذاكرةٍ للوكيل.** العائق مقيسٌ بحجم الملفّات أعلاه ‏+ نحو أربعين ملفَّ تسجيلٍ ونتيجة (‏27 prereg + 19 result، عددتُها). والمشروع اكتوى بهذا: حدُّ صدقٍ كُتب ثم لم يُقرأ عيبًا (عمى الافتر). **قيدان:** للوكيل لا للبوت (**لا `requirements.txt`**) · و**ملفّات المستودع تبقى المصدر الوحيد للحقيقة**.

### هـ. لا تُستعمل (5) — ويُعلَن السبب

| المستودع | السبب |
|---|---|
| `openclaw/openclaw` | لا يحلّ عائقًا مقيسًا: التسليم يعمل عبر `send_telegram`، وقاعدةُ المالك «لا قناة رابعة» منصوصة (`OPUS_EXECUTION_PACKAGE.md:850`) |
| `NousResearch/hermes-agent` | الجدولة تعمل وأعطالُها مقيسةٌ ومُعالَجة؛ **والعائق قِلّةُ المنفجرين لا الأتمتة** (`ranker_result.md:41`) |
| `hesreallyhim/awesome-claude-code` | فهرسٌ لا كود — يُقرأ مرّةً ولا يُضاف منه ملفّ |
| `microsoft/ai-agents-for-beginners` | تعليميّ — درسا «الوكلاء الجديرون بالثقة» و«ذاكرة الوكيل» يُقرآن عند تصميم م-ب |
| `Shubhamsaboo/awesome-llm-apps` | فهرسُ أنماط — **ولا سطرَ كودٍ يدخل** |

> 📌 **الخلاصة الصادقة: ‏3 تُستعمل · 1 مشروطة · 2 أفكار · 1 اختيارية · 5 لا.** وليس تقليلًا من قيمتها — **بل لأن العائق المقيس ليس نقصَ أدواتٍ ذكيّة، بل أن المجتمع المؤهَّل لا ينفجر** (`ranker_result.md:41-45`).

---

## ⑧ ما لا نملك بياناته — يُعلَن ولا يُخمَّن

| # | الفجوة | الأثر |
|---|---|---|
| 1 | **الإثراء الحقيقيّ للظرف** — المنشور يقسم مقامَين مختلفين | **الرقم الرئيسيّ مسحوب** حتى م-أ |
| 2 | **ربحية الظرف** — «لم تُقَس إطلاقًا» (`catalog_envelope_result.md:55`) | كلُّ v2 **فرضية** حتى م-و |
| 3 | **زمن تشغيل الظرف** — لا رقم في أيّ ملفّ (قرأتُ النتيجة كاملة) | جدوى التشغيل اليوميّ غير محسومة |
| 4 | **كلفة `RSI_NOW_HARD`/`SCORE_MIN`/`MIN_RR_T1`/`MAX_DROP_PCT` مفردةً** — يُنشَر أعلى **خمس** فقط (`additions_audit.md:70`) | ترتيب 4-6 بالفجوة والوسم لا بالكلفة |
| 5 | **حدّ ظرفٍ للشورت والفلوت** — خارج المعايير الأحد عشر | M13/M14 غير قابلتين للترتيب |
| 6 | **انحياز البقاء** — الكون قائمةُ ناسداك اليوم؛ ومِجَسّ المشطوبة **سُحب تفسيرُه** | كلُّ أرقامنا على ناجين |
| 7 | **عمى الافتر** — الشمعة اليومية لا تشمله، وNUWE انفجر فيه | أرقام الانفجار **أرضياتٌ لا أسقف** |
| 8 | **`h4_confirm` صفرٌ وقت الترتيب** — يُحسب في `enrich` (`:4341`) بعد `select_top` (`:13011`) | **المحور الثاني في `rank_key` (`:9163`) بلا أثرٍ بنيويًّا** |
| 9 | **محتوى المستودعات الاثني عشر** — `403` لكل مستودعٍ خارجيّ في هذي الجلسة | لا قرار قبل تحقّق |
| 10 | **سجلّات التشغيلات** — وجهة الـblob محجوبة | الأرقام تُقرأ من مُخرَجٍ يُعاد تشغيلُه لا من سجلٍّ قديم |

---

## ⑨ ما قد يُفشل الخطة — بصراحة

1. **أن يفشل م-أ نفسه**: إثراءٌ تحت 2× يُنهي v2 في يومها الأول. **وهذا مقصود** — الخطة مصمَّمة لتقتل فرضيتها بسرعةٍ ورخص.
2. **سجلُّ المشروع**: `T-ACC` · `T-SHORT` · `T-CMAG` · `T-EXIT` · `T-STOP` · `T-LIBERATION` · `T-RANKER` · `T-FAILS` · `M4-LADDER` — كلُّها فشلت أو «لا حكم».
3. **بصمة الانقلاب في السنة الثالثة** تكرّرت في `T-SHORT`/`T-CMAG`/`G5` ⇒ احتمالٌ معتبر أن تنقلب م-و.
4. **العدد مقابل السعة**: حتى لو ربح الظرف، المُرتِّب يقرّر أكثر ما يصل وهو «غير حاسم» ⇒ قد يخرج موجبًا في البِركة سالبًا في المُسلَّم — وهو حرفيًّا ما حدث في `T-CORE5`.
5. **‏31 رمزًا مترابطًا على 11 معيارًا** ⇒ الظرف يتمدّد بطبعه، و`LOO` يترك **رمزًا واحدًا** فيبقى تفاؤلٌ متبقٍّ (`catalog_envelope_result.md:61-62`).
6. **تشويه التقسيمات يسكن المنطقة التي يفتحها الظرف**: `M4` عند `P90` = **593%** و`P100` = **23,685%** (`:32`).
7. **خطر تشغيليّ حقيقيّ**: لقطة `CONFIG` من حالةٍ عالمية (`catalog_envelope.py:203`) ⇒ أيُّ توازٍ يترك البوّابات معطَّلةً بصمت.
8. **إغراءُ الحلقة الآلية** — عشر مقارناتٍ بلا تصحيح تُنتج «نتيجةً دالّة» بالصدفة.
9. **زمنُ الوكيل نفسه** — والسجلّ يقول إن أقفالًا كثيرة كُتبت فارغةً ولم تُكتشف إلا بالطفرة.
10. **🔴 والأخطر بعد الطعون: أن يُقرأ حكمُ م-و «ربحية» وهو وصفيّ داخل عيّنةٍ مُنقَّبة ‏27 مرّة.** ولهذا يُكتب الحدّ **قبل** التشغيل لا بعده.

---

## 🧭 خاتمة — سطران يُقرآن معًا

**المبشِّر:** ظرفُ الكاتالوج **أوّلُ بنيةٍ في المشروع تعمّم خارج عيّنتها** — سقط ‏10 نقاط فقط في `LOO` بينما «رابط Technology 2025» تبخّر من ‏32% إلى ‏12% (`catalog_envelope_result.md:19-21`). وهو يفتح المحور الوحيد الذي لم يُغلق: **تعريف المجتمع**.

**المقيِّد:** وهو **لم يُقس ربحًا قطّ**، **ورقمُ إثرائه مسحوب**، وسقفُه محكومٌ برقمٍ لا يتحرّك: **البِركة تُسلّم ‏0-3 منفجرين في السنة** (`ranker_result.md:41`).

⇒ **v2 ليس وعدَ ربح — هو أفضلُ فرضيةٍ مسنودةٍ عندنا، والترتيب أعلاه مصمَّمٌ ليقتلها في يومٍ واحد إن كانت خاطئة.**