# 💥🔗 `T-LINK100` — تسجيلٌ مسبقٌ **حاكم**: «الرابطُ المشترك» لكلّ سهمٍ انفجر ‏+100% فأكثر — **على السوق كلِّه لا على مرشّحينا**، ثلاثَ سنوات، بشاهدَين

> **مدفوعٌ ومدموجٌ قبل أيّ سطرِ أداةٍ وقبل أيّ رقم.** أمرُ المالك (‏2026-09-16) حرفيًّا:
> «ابيك تجيب جميع الاسهم اللي انفجرت بنسبة ١٠٠٪ و اكثر و تسوي عصف ذهني و اختبارات و تجارب
> الين تجيب الرابط المشترك لها». **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION`
> · لا عتبةَ تتحرّك · ولا وكلاء.**

---

## ⓪ ما هو مقيسٌ ومُغلَقٌ سلفًا — والفرقُ الذي يجعل هذا العقدَ محورًا لا إعادةَ فتح

🔴 **الاحتمالُ الأسبق: الفشل.** سؤالُ «الرابط المشترك» قِيس في هذا المستودع **ثماني مرّاتٍ
على الأقلّ** وفشل في كلّها، وكلُّها تُقرأ قبل هذا العقد:

| التجربة | المجتمع | الحكم | لماذا لا يُغلق هذا العقدَ |
|---|---|---|---|
| «الرابط المشترك 3 سنوات» (‏`bt_features`، 2026-07-15 · **مُقفَل** بشرط فتحٍ مؤرَّخ) | **مرشّحو الارتكاز المؤهَّلون** (‏574 · 74 منفجرًا) | لا قطاعَ ولا سمة | مجتمعُه **مَن اجتاز بوّاباتنا** — وهذا العقدُ على **السوق كلِّه** بما فيه ما رفضناه وما لم نرَه |
| `T-TIERLINK` / `T-TIERLINK-2` (‏09-02 · 09-06) | **مراسي السيولة الحيّة** (‏321/364) | ظاهرةٌ واحدة «‏J1 في البريماركت» | مجتمعُه **ما رست عليه أداتُنا**، والميزاتُ **لحظةَ المِرساة** لا قبل الانفجار · **و‏64% من الانفجارات بلا مِرساةٍ أصلًا** — وهم غيرُ المقيسين |
| `T-PREEXP-PROBE` (‏08-13) | 53 زوجًا (كتالوج + أسماكُنا) | `vol_x` وحدَه يميل · سلوكُ المضارب ضجيج | **مِجَسٌّ تشخيصيّ لا حكم**، عيّنةٌ صغيرةٌ مختارةٌ على النتيجة، **بلا شاهدٍ مقطعيّ** |
| `T-ACC`/`T-ACC-2` · `T-RANKER`/`2`/`3` · `T-CMAG` · `T-STABILITY` · `T-CANDLE` | مرشّحونا | فشلت | كلُّها **داخل الاختيار** |
| `scan_explosions` الحيّ (‏`wl["explosions"]`) | **السوقُ كلُّه** ‏≥50% منذ 07-17 | **‏343 صفًّا ‏≥100% (‏217 صفًّا نظيفًا من شبهة التقسيم = ‏91 حدثًا بعد طيّ الأيام المتتالية)** · `base_reason` عند القاع: `لا مستوى مختبَر` 95 · `M1 سعر` 91 · `M2` 68 · `M4` 60 · **`مرشّح` 15** | **قائمةٌ وأسبابُ رفض** — لا مقارنةَ بشاهد، ولا ميزاتٍ قبل الانفجار، ولا 3 سنوات، ولا مشطوبين |

⇒ **الجديدُ بالبناء (ثلاثةٌ لم تجتمع في تجربةٍ واحدة):** ① **المجتمع** = كلُّ سهمٍ في
السوق انفجر ‏+100% — **بما فيه المشطوبون** (المصدرُ يخدمهم بالبناء) ② **الميزاتُ قبل
الانفجار** (خمسُ جلساتٍ وبريماركتُ يوم الانفجار قبل 09:30) ③ **شاهدان**: مقطعيٌّ مطابَق
وذاتيٌّ زمنيّ — ثلاثَ سنوات. **ولا أعِد بنتيجة**: التنبّؤُ الأوّل (§⑤) أن **لا رابطَ يعبر**.

📋 **وقائمةُ «جميع الأسهم» التي طلبها المالك مسلَّمةٌ اليوم** في `explosions100_live.md`
(الحقبةُ الحيّة 07-17 ⟶ 09-15 من `wl["explosions"]`) — **وهي قائمةٌ لا حكم**، والقائمةُ
الثلاثيّةُ الكاملة تُخرجها الأداة.

---

## ① السؤالُ القابل للتكذيب

بين **كلّ** الأسهم التي انفجرت ‏+100% فأكثر في جلسة (تعريفُ §②)، هل توجد **ميزةٌ ملاحَظةٌ
قبل الانفجار** (نهايةُ الجلسة السابقة أو بريماركتُ يوم الانفجار قبل 09:30 نيويورك) تفصل
المنفجرين عن شاهدٍ مطابَق **بنسبةٍ ≥2×** وفواصلَ منفصلة، **في كلّ سنةٍ من الثلاث**،
وتصمد لتصحيح تعدّد المقارنات؟

---

## ② المجتمعُ والتعريفات — مثبَّتةٌ قبل أيّ جلب

- **المصدر:** شموعٌ يوميّةٌ **مجمَّعةٌ للسوق كلِّه** `/v2/aggs/grouped/locale/us/market/stocks/{day}`
  بـ`adjusted=false` (نداءٌ واحدٌ لليوم ⇒ ‏≈750 نداءً لثلاث سنوات) — **تخدم المشطوبين بالبناء**
  (السهمُ موجودٌ في يومه ولو شُطب بعده) ⇒ **لا انحيازَ بقاء**.
- **السنوات الحاكمة:** 2023 · 2024 · 2025. **و2026 حتى 09-15 وصفيّةٌ** (تُطبَع ولا تحكم) ولها
  **شاهدُ تكامل** (`V-L4`).
- **الانفجار (الحاكم):** `high(d) / close(d−1) ≥ 2.0` مع `close(d−1)` في ‏[`PRICE_LO`, 20$]
  (‏`PRICE_LO` = `kasih_scan.PRICE_LO` بالاسم = أرضيّةُ السعر النافذة) وحجمُ `d−1` > 0.
  **والقراءةُ الثانية** `close(d)/close(d−1) ≥ 2.0` **تُطبَع** ولا تحكم.
- **حارسُ التقسيم (‏`V-L2`):** أيُّ صفٍّ له تقسيمٌ (‏`/v3/reference/splits`) في ‏[d−3, d+3]
  **يُستبعَد ويُعَدّ** — لأن `adjusted=false` يصنع ‏+100% وهميًّا عند التقسيم العكسيّ (سابقةُ
  `INLF` و`LGCL`). **والمُستبعَدون يُطبَعون بالعدد**.
- **الحدثُ الأوّل للرمز في نافذة 20 جلسة يُعَدّ** — تكرارُ الرمز في أيّامٍ متتالية (نمطُ
  `NWGL`/`CELU` في السجلّ الحيّ) يُطوى إلى حدثٍ واحد.
- **الشاهدان (كلٌّ لكلّ حدث):**
  - **`CX` مقطعيٌّ مطابَق:** اليومُ نفسُه · خانةُ السعر نفسُها (‏`<1` · `1-3` · `3-10` · `10-20`)
    · عُشرُ الدولار اليوميّ نفسُه (`close(d−1)·volume(d−1)`) · **ولم يتحرّك ‏≥30%** في `d` ·
    **حتميٌّ**: أوّلُ رمزٍ بترتيب الأبجديّة داخل الخليّة لم يُستعمَل بعد.
  - **`CC` ذاتيٌّ زمنيّ (case-crossover):** الرمزُ نفسُه بنافذةٍ منتهيةٍ عند `d−21` (سابقةُ
    `T-PREEXP-PROBE`) بشرط ألّا يقع انفجارٌ في ‏[d−41, d−21].
- **الطبقةُ الدقيقة (بريماركت):** لكلّ حدثٍ وشاهدِه `CX`، شموعُ دقيقةِ يوم `d` من 04:00 إلى
  **09:29** فقط (`tier_fwd_report.fetch_day` بالاسم ثمّ قصٌّ زمنيّ) — **بسقفٍ مُعلَن**: أوّلُ
  ‏1,500 حدثٍ لكلّ سنة بترتيبٍ حتميّ (التاريخ ثمّ الرمز) **ويُطبَع المقصوص**.

---

## ③ الميزات — قائمةٌ مُغلَقة (لا تُضاف ميزةٌ بعد أيّ رقم)

كلُّها **قبل** 09:30 يوم `d`. الطبقةُ اليوميّة من `grouped` (بلا نداءٍ إضافيّ):

| الميزة | التعريف | السلال |
|---|---|---|
| `vol_x` | `volume(d−1) / median(volume[d−20..d−6])` | ‏<1 · 1-3 · ‏≥3 |
| `ret5` | `close(d−1)/close(d−6) − 1` | ‏<−20% · −20..+20% · ‏>+20% |
| `range_c` | `mean(range[d−5..d−1]) / mean(range[d−20..d−6])` (انضغاطُ المدى) | ‏<0.5 · 0.5-1 · ‏>1 |
| `gap1` | `open(d−1)/close(d−2) − 1` | ‏<0 · 0-10% · ‏>10% |
| `dd52` | `close(d−1) / max(high[d−252..d−1]) − 1` (هبوطٌ من قمّة السنة — نظيرُ `M2`) | ‏>−50% · −50..−90% · ‏<−90% |
| `sma_pos` | موضعُ `close(d−1)` من `SMA20` و`SMA50` (فوق/بين/تحت) | 3 |
| `rsi14` | `Super_stock.rsi` بالاسم على إغلاقات `[d−40..d−1]` | ‏<30 · 30-50 · ‏>50 |
| `quiet` | عددُ الجلسات منذ آخر يومٍ تحرّك فيه ‏≥50% (قاع→قمّة) | ‏<20 · 20-60 · ‏>60 |
| `px` | خانةُ `close(d−1)` | 4 خانات |
| `usd1` | `close(d−1)·volume(d−1)` | ‏<50k · 50-300k · ‏>300k |
| `rsplit180` | تقسيمٌ عكسيٌّ خلال 180 يومًا قبل `d` (من `splits` · نقطةُ زمنٍ صحيحة) | نعم/لا |
| `pm_gap` | `(آخرُ إغلاقِ دقيقةٍ قبل 09:30) / close(d−1) − 1` — الطبقةُ الدقيقة | ‏<10% · 10-30% · ‏≥30% |
| `pm_usd` | دولارُ البريماركت التراكميّ | ‏<100k · 100k-1M · ‏>1M |

⚠️ **ما لا يدخل ولماذا:** الفلوتُ الحيّ (ليس نقطةَ زمن ⇒ **وصفيٌّ يُطبَع** إن جُلب) · الدولةُ
(‏`T-CMAG` مُغلَق) · هل كان «مرشّحًا» عندنا (‏`bt_features` مُغلَق) · الصفقاتُ الخام (‏`T-ACC`
مُغلَق).

---

## ④ المعاييرُ والفروع — مكتوبةٌ قبل أيّ رقم

**القياس:** لكلّ ميزةٍ وسلّة: **الإثراء** = حصّةُ المنفجرين في السلّة ÷ حصّةُ الشاهد فيها،
بفاصل ويلسون لكلّ حصّة.

| المعيار | النصّ |
|---|---|
| **`LK1`** (حاكم) | سلّةٌ واحدةٌ على الأقلّ **إثراؤها ≥2× مقابل `CX` ومقابل `CC` معًا** · فاصلا ويلسون منفصلان · ‏n ≥ 50 في السلّة · **في كلّ سنةٍ من الثلاث** · وتصمد لبونفيروني (‏α = 0.05 ÷ عددِ السلال الكلّيّ) |
| `LK2` (وصفيّ) | ميزةٌ تكرّر **اتّجاهُها** في الثلاث ولو دون 2× — تُطبَع «مرشَّحةً لا رابطًا» |
| **الحرّاس** | `V-L1` تغطيةُ أيّام `grouped` ‏≥95% للسنة · `V-L2` حارسُ التقسيم يعمل ويُطبَع · `V-L3` نسبةُ مطابقة `CX` ‏≥90% · **`V-L4` شاهدُ التكامل:** على 07-17 ⟶ 09-15 تُستعاد ‏≥80% من الـ91 حدثًا النظيفة في `explosions100_live.md` (بالرمز وتاريخٍ ضمن ‏±3 أيام) · `V-L5` عددُ الأحداث > 0 لكلّ سنة (لا `no-op`) |

**الفروع** (ثلاثةٌ كلٌّ في سطره):
1. **رابطٌ مقيس** — `LK1` مستوفًى لميزةٍ واحدةٍ على الأقلّ ⇒ **تسجيلٌ أماميٌّ جديد** قبل أيّ استعمال.
2. **لا رابط** — الحرّاسُ خضراء ولا سلّةَ تعبر `LK1`.
3. **لا حكم** — حارسٌ ساقط.

**رموزُ الخروج:** ‏0 حكمٌ صدر · 2 لا مفتاح · 3 تغطيةٌ دون الحدّ · 4 صفرُ أحداث ·
**‏5 شاهدُ التكامل `V-L4` ساقط** · **‏6 حارسٌ ساقط** · **‏9 «لا حكم»**.

---

## ⑤ التنبّؤات — تُنشَر مع الحكم مؤكَّدةً أو مكذَّبة

| # | التنبّؤ |
|---|---|
| `LP1` | **لا ميزةَ يوميّةً** تعبر `LK1` (الاحتمالُ الأسبق بثماني تجارب) |
| `LP2` | `vol_x ≥3` أعلى ميزةٍ يوميّةٍ إثراءً (‏≈1.5×) **وتسقط** على 2× أو الفاصل |
| `LP3` | `pm_gap ≥30%` **تعبر 2×** في الثلاث مقابل `CX` — وهي **ليست «رابطًا خفيًّا»** بل الانفجارُ وقد بدأ قبل الجرس |
| `LP4` | `rsplit180` إثراؤه بين 1.5× و2× — لا يعبر |
| `LP5` | ‏≥70% من المنفجرين `px < 3$` |
| `LP6` | الإثراءُ مقابل `CC` **أضعفُ** منه مقابل `CX` لكلّ ميزة (الشاهدُ الذاتيّ أصدق) |
| `LP7` | القراءةُ بالإغلاق (‏`close/close ≥ 2`) **دون ثُلث** القراءة بالأعلى |

---

## ⑥ حدودُ الصدق — عيوبٌ مرشَّحة حتى تُحسم

1. **«رابطٌ» هنا إثراءٌ قبل الحدث لا تنبّؤٌ بالمقدار** ولا حافّةُ تداول (بلا وقفٍ ولا تكلفة).
2. **`pm_gap`/`pm_usd` مشروطان بالسقف** (‏1,500 حدث/سنة) ⇒ إن قُصّ فالطبقةُ الدقيقةُ وصفيّة.
3. الفلوتُ غيرُ نقطةِ زمن ⇒ لا يدخل الحكم.
4. **الشاهدُ `CX` لا يضبط القطاع** — يضبط السعرَ والدولار فقط (مُعلَن).
5. تعدّدُ المقارنات كبير (‏≈13 ميزة × 3 سلال) ⇒ بونفيروني في المعيار نفسِه.
6. **الحمل/الأفق المتعدّد الأيام ليس موضوعَ هذا العقد** — موضوعُ `T-OPHOLD`.
7. الكونُ «كلُّ السوق الأمريكيّ» أوسعُ من كون ناسداك في الفرز ⇒ يُطبَع تفكيكُ البورصة إن توفّر، ولا يُفلتَر.

---

## ⑦ سقفُ النجاح — والمحظورات

**سقفُ النجاح:** حكمٌ منشور، **وإن عبرت ميزةٌ**: اقتراحُ **تسجيلٍ أماميٍّ جديد** لاختبارها
كوسمِ عرضٍ — **لا بوّابةَ ولا ترتيب** بهذا العقد.
**المحظورات:** لا مسَّ جذرٍ · لا عتبةَ إنتاج · لا وزنَ ترتيب · لا كتمَ إشعار.

---

## ⑧ الأداة — مواصفةٌ ملزِمة (تُبنى **بعد** دمج هذا العقد)

- `link100_probe.py` + `.github/workflows/link100.yml` (يدويٌّ · مدخلُ `year` · بلا سرِّ تلغرام).
- `LINK100_DRY=1`: **أعدادُ المجتمع فقط** (أحداثٌ · مُستبعَدو التقسيم · مطابقةُ `CX`) **بلا إثراءٍ واحد**.
- الحتميّةُ بت-بت · القصُّ يُطبَع بعدّاده · `link100_result.md` بجانب هذا العقد.
- إعادةُ استعمالٍ بالاسم: `tier_fwd_report.fetch_day` · `Super_stock.rsi` · `kasih_scan.wilson` · `kasih_scan.PRICE_LO` · `Super_stock._fetch_splits` أو نظيرُها من `tierlink_probe`.

---

## ⑨ الأقفال (تُكتب مع هذا العقد، وكلٌّ يسقط بطفرةٍ مسمّاة)

- **`LKK0`** — بنيويّ: فروعُ §④ ثلاثةٌ كلٌّ في رقمه · «لا حكم» في الثالث وحدَه · رمزا 5 و9 منصوصان · و`LK1` يشترط الشاهدَين **معًا** والسنواتِ الثلاث.
- **`LKK1`** — §⓪ يُسمّي التجاربَ المُغلَقة (‏`bt_features` · `T-TIERLINK` · `T-PREEXP-PROBE`) ويُعلن الفرقَ الثلاثيّ (السوقُ كلُّه · قبل الانفجار · شاهدان) ويحمل أرقامَ السجلّ الحيّ (‏343 · 217 · 91).
- **`LKK2`** — §②/§③: التعريفُ الحاكم بالأعلى وقراءةُ الإغلاق وصفيّة · حارسُ التقسيم ‏±3 · قائمةُ الميزات مُغلَقةٌ نصًّا · و`PRICE_LO` بالاسم.
