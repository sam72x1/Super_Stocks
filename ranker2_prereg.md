# 🥇🚦 T-RANKER2 — تحسين المُرتِّب بعد ثبوت عنق الزجاجة (يُدفَع **قبل** أيّ رقم)

> **أمرُ المالك (2026-08-07 ليلًا):** «طيب حسن المرتب بعد ما ثبت انه عنق زجاجة».
> **وهذا الملفُّ مدفوعٌ قبل تشغيل أيّ ذراع** — أيُّ رقمٍ يظهر بعده يُقرأ على معياره
> المكتوب هنا، لا على معيارٍ يُختار بعد رؤية النتيجة.

---

## ① لماذا الآن — وما الذي تغيّر عن أحكام المُرتِّب الثلاثة السابقة

- **`T-RANKER` (2026-08-01):** فشلت أذرعُها، والاكتشافُ البنيويّ «لا يوجد ما يُرتَّب
  إليه» — البِركةُ آنذاك تُسلّم 0-3 منفجرين سنويًّا. **هذا السببُ سقط:** بِركةُ
  العشرين على لقطات PIT فيها **‏26 · 29 · 16 بالغَ +100%** (‏71 مجمَّعًا، ملحق ⑦).
- **`T-REPLAY10`:** المُرتِّب عند مستوى الصدفة، والسعةُ تقصّ 27-48%.
- **`T-RANKER-TIE`:** كاسرُ تعادل الظرف فشل — **وأداةُ الباكتيست لا تصل ظاهرةَ
  التعادل الحيّة** (مِشيةٌ متفرّقة: مرشّح-اثنان/جلسة).
- **وملحق ⑨ (الليلة):** من 71 منفجرًا في البِركة يصل **15 فقط (‏21%)** لخانات
  المحفظة ⇒ **عنقُ الزجاجة مقيس**، والمالك أمر بتحسينه.

**السؤال المسجَّل:** أيُّ ترتيبٍ يُسلّم أكثرَ منفجري البِركة إلى خانات السعة الحيّة؟
⚠️ وقيدُ صراحة: قيدُ المِشية المتفرّقة (‏`ranker_tie_result.md` §③) يمنع أسئلة
التعادل اليوميّ — لكنه **لا يمنع هذا السؤال**: التنافسُ هنا **عبر الأيام** (خاناتٌ
مشغولةٌ أسابيع)، وهو بالضبط ما تمثّله آلةُ `replay10`.

## ② الأداة والميزانية الثابتة

- **الأداة:** `replay10` الأمينة (سعة **10** = `WATCHLIST_SIZE` الحيّ · زمنٌ
  بالجلسات · لا نظرَ مستقبليّ — `no_fill` يحجز خانته حتى نهاية نافذته).
- **البِركة:** صفقاتُ `run_backtest` الإنتاجيّ نفسِه، تشغيلةٌ واحدة لكلّ سنة على
  لقطات PIT الثلاث (‏2024=`31199493957` · 2025=`31191327731` · 2026=`31199510868`)
  بـ`FAISAL_ONLY=1` (بوّاباتُ الإنتاج الحالية — وهي البِركة التي نحسّن تسليمها).
- **الأذرعُ كلُّها تقرأ نفسَ القائمة** من التشغيلة الواحدة ⇒ نفسُ اللقطة والكون
  والسعة والنافذة **بالبناء**، وتُطبَع القيود في المُخرَج (قاعدة §⑥ الموثّقة).
- الأعلام: `BT_REPLAY10=1` (تاريخ الخروج) · `BT_ENVVALS=1` (قيمُ الظرف + `in_band`)
  · `BT_POTENTIAL=1` (**إلزاميّ** — المقياسُ الأساسيّ يقرأ `mg_pre_stop`؛ غيابُه
  ⇒ توقّفٌ صريح لا «صفر منفجر»). سائقٌ مستقلّ `ranker2_arms.py` + workflow
  `ranker2.yml` — **صفر مسّ بمسار الإنتاج**، والتعديلُ الوحيد في `Super_stock.py`
  حقلٌ إضافيّ `in_band` داخل كتلة `BT_ENVVALS` المطفأة (يُحسب بدالّة الإنتاج
  `in_entry_band(r)` نفسِها — صفرُ رقمٍ جديد · مطفأ = بت-بت).

## ③ الأذرع — خمسٌ مثبَّتة ولا سادسة

| الذراع | المفتاح (داخل الجلسة) | المصدر |
|---|---|---|
| **`K-LIVE`** | داخل نطاق الدفعات أوّلًا ← جاهزية ← نقاط ← rr ← seq | **مُرتِّب الإنتاج الحاليّ** (T-PROX، قرار المالك 2026-08-07؛ سندُه «قرار فيصل = موقع السعر») |
| **`K-LEGACY`** | جاهزية ← نقاط ← rr ← seq | مُرتِّب ما قبل T-PROX (خطُّ الأساس التاريخيّ) |
| **`K-ENV`** | **عمقُ ظرف فيصل** أوّلًا ← جاهزية ← نقاط ← rr ← seq | حوافُّ الظرف المُعايَرة من أسهمه (`inferred`)؛ الامتناعُ (`None`) يغرق تحت كلّ مقيس |
| `K-FIFO` | seq | شاهدُ ضبط |
| `K-RAND` ×200 | تجزئةٌ حتمية بالبذرة | **شاهدُ الضبط الحاسم** |

- `h4_confirm` صفرٌ حيًّا وتاريخيًّا — مفتاحٌ خامل، يُعلَن ولا يُمثَّل.
- 🔒 **البوّاباتُ والدخول والوقف والأهداف لا تُمَسّ** — الفرقُ بين الأذرع ترتيبٌ فقط.
- 🔒 محاورُ سقطت بأدلّة قائمة (بصمة اليد `behav` · `FSTO` · قطاع/دولة/شورت)
  **لا تدخل أيَّ ذراع** — أقفالُها قائمة.

## ④ المقاييس — الأساسيُّ أولًا (مثبَّتة قبل الأرقام)

1. **الأساسيّ: «المنفجرون المُسلَّمون»** = عددُ المأخوذين الذين بلغوا
   `EXPLOSION_PCT`(=50%) قبل وقفهم (‏`mg_pre_stop`، والتعريفُ القانونيّ للانفجار
   في المستودع). يُنشَر معه بالغو +100% وعددُ المأخوذين والنسبة.
2. **حارسُ العائد:** متوسط `R` المحقَّق لكلّ صفقةٍ مأخوذة (`r_unit`) — الذراعُ
   المرشَّح للتبنّي يجب ألّا يكون أسوأ من `K-LIVE` بأكثر من **‏0.05R/صفقة**
   مجمَّعًا على الثلاث (مجموع R ÷ مجموع المأخوذ).
3. **موقعُ كلّ ذراعٍ من حزام العشوائيّ** (‏200 بذرة): يُنشَر المئينُ لكلّ سنة.

## ⑤ قاعدةُ القرار (قبل الأرقام)

- **يُتبنّى `K-ENV` بديلًا عن الإنتاج** فقط إن استوفى الثلاثة معًا:
  ① منفجروه المُسلَّمون **≥ `K-LIVE` في كلّ سنة** وأكثرُ منه صراحةً في سنتين
  على الأقلّ · ② فوق **المئين 90** من حزام العشوائيّ في سنتين على الأقلّ ولا
  ينزل تحت وسيطه في أيّ سنة · ③ حارسُ العائد مستوفًى.
  ⇒ عندها فقط: تعديل `rank_key` (جذرٌ، بأمر المالك القائم) + رفع `LOGIC_VERSION`
  + إبلاغُ المالك بتشغيل `force_renew=1` مرّة.
- **قراءةٌ ثانية مسجَّلة:** إن كان `K-LIVE` أسوأ من `K-LEGACY` باتّساق (أقلّ
  منفجرين مُسلَّمين في سنتين ولا يتفوّق في أيّ سنة) ⇒ **تحقيقُ T-PROX التاريخيّ
  فشل** ⇒ يُعرَض على المالك خيارُ التراجع (قرارُه؛ والقياسُ الحيّ P-1 مستمرّ).
- **وإن جلست الأذرعُ غيرُ العشوائية كلُّها داخل حزام العشوائيّ في السنوات الثلاث**
  ⇒ الحكمُ الصادق: **«الترتيبُ محايدٌ على هذي البِركة»** — لا يُغيَّر `rank_key`
  (تغييرٌ بلا حافةٍ مقيسة = ضجيج)، وتنتقل رافعةُ «تسليم المنفجرين» إلى سياسة
  الخانات/الإنتاجية وتُعرَض على المالك خياراتٍ مقيسةً — **وهذا جوابٌ مشروع لأمر
  «حسّن المُرتِّب»** لا تهرّبٌ منه.
- نتيجةٌ مختلطة ⇒ تُعرَض بلا ترجيح والقرارُ للمالك.

## ⑥ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر ولو كُذِّبت)

- **N1:** `K-LIVE` يُسلّم منفجرين ≥ `K-LEGACY` (أولويةُ القابل للتعبئة ترفع
  الإنتاجية عبر الخانات).
- **N2:** `K-ENV` داخل حزام العشوائيّ (عمقُ الظرف لا يميّز المنفجر — اتّساقًا مع
  خمسة عشر تأكيدًا لـ«الحافة = التوقيت لا الاختيار»).
- **N3:** المدى بين أفضل ذراعٍ غير عشوائية وأسوئها لا يتجاوز عرضَ حزام العشوائيّ
  في سنتين على الأقلّ (الترتيبُ يؤثّر قليلًا).
- **N4:** مرفوضو السعة بالمئات في 2024/2025 (التنافسُ حقيقيّ لا شكليّ).

## ⑦ حدودُ صدقٍ تُقرأ مع أيّ رقم

عيّناتُ المنفجرين المُسلَّمين صغيرة (آحادٌ إلى عشراتٌ دنيا) ⇒ ولذلك حزامُ
العشوائيّ هو الحكم لا الفرقُ الخام · المِشيةُ متفرّقة (لا تعادل يوميًّا كثيفًا —
مُعلَن، ولا يمسّ سؤالَ الإشغال عبر الأيام) · 2026 سنةٌ جزئية (‏7 أشهر) · 2025/2026
داخل-العيّنة لبوّابات العشرين — **مشتركةٌ بين الأذرع كلِّها فلا تحيز المقارنةَ
بينها** · انحيازُ بقاءٍ ساقطٌ في المُحكَّمتين (P3) · وسعةُ 10 هنا أضيق من سعة 15
في ملحق ⑨ فالأعداد لا تُقارَن عبر الملحقين مباشرةً.
