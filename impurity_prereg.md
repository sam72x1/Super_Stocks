# 🧹 تسجيلٌ مسبق — «فلتر الشوائب» (‏`T-IMPURITY`)

> **أمرُ المالك 2026-08-22: «سجّل فلتر الشوائب».** وسياقُه بلفظه: «كلها اسهم
> ارتكاز بحته و قويه جدااا — **ما ابي يدخل فيها شوايب و اسهم ضعيفة**».
> **مدفوعٌ قبل أيّ رقمٍ مجمَّع.** لا يُعدَّل بعد أوّل مُخرَج.

---

## ⓪ 🔴🔴 إفصاحٌ يسبق كلَّ شيء: هذا **إعادةُ فتحِ ملفٍّ مُغلَق**

`red_mute_result.md` قاس **هذي الفكرةَ بعينها** يوم 2026-08-20 وحكم **«لا
كتم»**، وأغلقها المالكُ بقوله «خلّه كذا»، ونصُّ الإغلاق: «⛔ **ولا يُعاد فتحُه
بحسن نيّة** … 🔓 وشرطُ الفتح: **أمرٌ صريحٌ منه**». **وقد صدر الأمرُ الصريح**
فأُعيد الفتحُ — ويُدوَّن أنه إعادةُ فتحٍ لا اكتشافٌ جديد.

⚖️ **وما سقط هناك ليس الفصلَ بل الكلفة:** «الأحمر» فصل بقوّةٍ (‏3.4/3.9/4.0%
مقابل 14.2/17.1/16.1%) **وعبر المعيارين ① و③**، وسقط **②** وحدَه: كان **‏40-42%
من كلّ المِرساة** فحمل **‏698 من 4,952 كاسحًا = ‏14.1%** — فوق حدّ العُشر.

⇒ 🎯 **فالسؤالُ الجديدُ الوحيد المشروع: هل ثمّةَ تعريفٌ للشوائب أضيقُ من
«الأحمر» تمرّ كلفتُه؟** — لا «هل يفصل» (مقيسٌ ويفصل)، ولا توسيعُه (الأوسعُ
يحمل كاسحين أكثر بالبناء).

🔴 **ولهذا: الاتّجاهُ تضييقٌ لا توسيع.** واقتراحي في ردّي على المالك («صفرُ
إشارةٍ خضراء مهما كان المحسوب») **أوسعُ** من الأحمر ⇒ يُقاس كذراعٍ لكن
**أتوقّع سقوطَه** (‏`I-P1` أدناه) — يُدرَج للمنحنى لا للاعتماد.

---

## ① التعريفاتُ — أربعُ أذرعٍ ولا خامسة

المكوّناتُ الأربعة `c3` · `c4` · `v2` · `v3` وسلالُها العليا **حرفيًّا كما
تكتبها `kasih2_scan`** (ثابتُ `TOP` في `kasih2_red_stats.py` — مصدرٌ واحد).
و«أخضر» = قيمةُ المكوّن **تساوي** سلّتَه العليا. و«محسوب» = قيمتُه ليست `None`.

| الذراع | التعريف | العلاقة |
|---|---|---|
| **`Z0`** | الأساس — بلا كتم | المرجع |
| **`Z1`** 🥇 | صفرُ خضراء **والأربعُ محسوبة** | ‏⊆ `Z2` — **الحاكمة** |
| **`Z2`** | صفرُ خضراء **ومحسوبٌ ثلاثةٌ فأكثر** | = **`is_red` حرفيًّا** |
| **`Z3`** | صفرُ خضراء **مهما كان المحسوب** | ‏⊇ `Z2` (الأوسع) |
| **`Z4`** | صفرُ خضراء **وبلا توليفة `J1`** | محورٌ ثانٍ منشور |

🔒 **`Z2` شاهدُ تكاملٍ حاكم لا ذراعُ حكم:** يجب أن يُعيد أرقامَ
`red_mute_result.md` **حرفيًّا** (‏3.4/3.9/4.0% · حصّةُ الكاسحين 14.1% ·
والحجمُ 40-42%). **وإن لم يفعل فالأداةُ معطوبةٌ والتشغيلةُ تُسحَب** — لا
تُفسَّر نتيجتُها (خروج 3).

🥇 **والحاكمةُ `Z1`** لأنها **الأضيق**: إن سقطت كلفتُها سقطت العائلةُ كلُّها
بنيويًّا (‏`Z1 ⊆ Z2 ⊆ Z3`)، وإن عبرت فهي وحدَها القابلةُ للاقتراح.

---

## ② البياناتُ والمقياس

- **المصدرُ نفسُه بلا تغيير:** ‏12 ملفَّ صفوفٍ من `kasih2_scan` — السنواتُ
  الثلاث (`32330346732` · `32330353463` · `32330360191`) وتسعُ جلساتٍ حديثة
  (‏08-06 → 08-19، منها `32345880211`…`32345891385`) = **‏44,966 صفًّا**.
- **المقياسُ الحاكم:** نسبةُ **«كاسح30»** (بلوغ ‏+30% قبل الخروج البنيويّ) —
  `KASIH_PCT` المنشور، لا رقمًا جديدًا.
- **فواصلُ ويلسون** بدالّة `kasih_scan.wilson` نفسِها.
- **قراءةٌ فقط:** صفرُ مسٍّ بالإنتاج · والأداةُ لا يستوردها `Super_stock`.

---

## ③ المعيارُ الثلاثيّ — **منقولٌ حرفيًّا من `red_mute_prereg.md §③`**

⛔ **ولا يُحرَّك حدٌّ منه بعد رؤية أيّ رقم.**

1. **الفصلُ التاريخيّ:** نسبةُ كاسح30 داخل الذراع **أدنى من نصف أساس سنتها**
   في كلّ سنةٍ من الثلاث، **وفاصلا ويلسون (داخل مقابل خارج) منفصلان في كلّ
   سنة**.
2. **كلفةُ الكتم مقبولة:** حصّةُ الذراع من **كلّ الكاسحين** مجمَّعًا
   **‏10% أو أقلّ** (لا نكتم قناةً تحمل عُشرَ الغنائم فأكثر).
3. **الأيامُ الحديثة لا تناقض:** على الجلسات المزادة، نسبةُ الذراع **لا
   تتجاوز أساسَ** العيّنة الحديثة نفسِها.

**والأرضية:** الذراع **‏≥200 صفًّا مجمَّعًا و‏≥40/سنة** — وإلا **«لا حكم» ولا
كتم**. (وهي `RED_MIN_TOTAL`/`RED_MIN_YEAR` القائمة، تُقرأ من مصدرها.)

**الثلاثةُ تلزم معًا.** وسقوطُ ② وحدَه يكفي للرفض — كما وقع للأحمر.

---

## ④ سقفُ النجاح — وما هو ممنوعٌ مهما كانت الأرقام

✅ **إن عبرت `Z1` الثلاثةَ:** **اقتراحٌ للمالك** بمحورِ تسليمٍ يكتم كرتَ `M5`
لتلك الشريحة — **بأرقامها ومنحنى كلفتها مطبوعَين**. ⛔ **ولا يُنفَّذ آليًّا**:
الكتمُ **إزالة**، وقرارُ التسليم قرارُه وحده (سابقةُ «خلّه كذا»).

⛔ **الممنوعُ مهما خرجت الأرقام:**
- أرقامُ المالك (‏5% · $30 ألفًا · 3× · أرضيةُ النبض) — **لا تُمَسّ**.
- بوّاباتُ الإطلاق و`liq_stage_events` و`liq_tier` وتعريفُ الفئات الثلاث.
- **وسمُ الكرت** (‏🥇🥈🥉 · الوقف · التوليفة · العدّاد) — يبقى كما هو.
- `M1`/`M30`/`Px` — **لا يمسّها** (التعريفُ من نافذة الخمس حصرًا).
- ⛔ **ولا تُضاف ذراعٌ خامسةٌ بعد رؤية الأرقام** ولا يُبدَّل تعريفُ ذراعٍ.
- ⛔ **ولا يُعايَر تعريفٌ كي يلتقط `RDAC` أو «خواتها» بأسمائها** — تلك
  دائريّةٌ صريحة، وهي عينُ ما أعدم تصنيفَ `A/B` في هذا المشروع.

---

## ⑤ تنبّؤاتٌ قابلةٌ للكسر — تُنشَر ولو كُذّبت

- **`I-P1`** (شبهُ محسومٍ بنيويًّا وأُعلنه كذلك): **`Z3` يسقط بالكلفة** —
  فهو سوبرست من الأحمر الذي حمل ‏14.1% ⇒ حصّتُه **‏14.1% فأكثر**.
- **`I-P2`** 🎯 **المخاطرةُ الحقيقية:** `Z1` حصّتُه من الكاسحين **تحت 10%**
  ⇒ يعبر ②. **وإن سقط فالعائلةُ كلُّها تسقط** ولا يبقى تعريفٌ مشروع.
- **`I-P3`:** نسبةُ كاسح30 في `Z1` **تحت 6%** مقابل أساسٍ ‏≈11%.
- **`I-P4`:** حجمُ `Z1` **‏15-30%** من المِرساة (أضيقُ من الأحمر 40-42%).
- **`I-P5`** ⚠️ **خطرُ التكرار:** إن كان الفرقُ بين `Z1` و`Z2` في الحجم
  **دون 10% نسبيًّا** فالأربعةُ محسوبةٌ في أغلب الصفوف ⇒ **التجربةُ تكرارٌ
  لـ`red_mute` بلا جديد** — **يُعلَن كذلك صراحةً ولا يُدَّعى اكتشاف**.
- **`I-P6`:** `Z4` بين `Z1` و`Z3` حجمًا، ولا يعبر ② (‏«بلا J1» يشمل ‏≈80%).

---

## ⑥ حدودُ صدقٍ تُقرأ **مع** الأرقام لا بعدها

1. 🔴🔴 **دائريّةٌ مُعلَنة:** المكوّناتُ الأربعة **اختيرت في `T-KASIH-2` على
   المقياس نفسِه وعلى الصفوف نفسِها** ⇒ فصلُها **وصفُ عيّنةٍ لا تنبّؤ**
   (منصوصٌ في docstring `liq_tier` نفسِه). **والاختبارُ الحقيقيُّ أماميّ.**
2. **إعادةُ فتحٍ لا اكتشاف** (‏§⓪) — و`Z2` يجب أن يُعيد المنشورَ حرفيًّا.
3. **«كاسح» لمسُ ‏+30% لا صفقةٌ منفَّذة** (بلا انزلاقٍ ولا حجم) ⇒ سقفٌ متفائل.
4. **الكتمُ إزالة:** إشعارٌ لم يُرسَل **لا يُسترجَع** — ولهذا حدُّ العُشر.
5. **التعريفُ من نافذة الخمس حصرًا** ⇒ سهمٌ «شائبةٌ» في الخمس قد يصلح بعدها،
   ويُقاس أثرُ ذلك **بحصّة الكاسحين ②** لا بالرأي.
6. **الشريحةُ الحديثة صغيرة** ⇒ ③ **شرطُ عدمِ تناقضٍ لا شرطُ إثبات**.
7. **وأيُّ عبورٍ لا يُثبت الربحية** — يُثبت أن الشريحةَ **أفقرُ كاسحين**، وهو
   غيرُ «الكتمُ يزيد الربح».

---

## ⑦ الحصيلةُ الحيّةُ التي دفعت المالكَ للأمر (تُدوَّن ولا تحكم)

على **‏86 محسومةً** من ثلاث جلساتٍ حيّة (‏08-19/20/21): **صفرُ خضراء ⟶ 1 من
30 = ‏3.3%** مقابل **11 من 56 = ‏19.6%** لمن عنده إشارةٌ فأكثر (‏≈6×).
⚠️ **وكلُّ فواصله متداخلة** (‏n صغير) ⇒ **وصفيٌّ غيرُ مسجَّلٍ مسبقًا يُنشَر
ولا يحكم** — وهو **الدافع** لا الدليل. والدليلُ ما تُخرجه هذي التجربة.

---

## ⑧ ملحقٌ مؤرَّخ 2026-08-22 — عطبان في الأداة قبل أيّ حكم (**التعريفاتُ لم تُمَسّ**)

أوّلُ تشغيلة (`32568994842`) **عبرت بوّابةَ التكامل** (‏`Z2` أعاد
`red_mute_result.md` رقمًا رقمًا) **وأخرجت عطبَين يمنعان قراءةَ حكمٍ**، فتُقرأ
تشغيلةً **باطلةً جزئيًّا** ويُعاد التشغيل. **ولا حرفَ تغيّر في تعريفِ ذراعٍ ولا
في عتبةٍ ولا في المعيار الثلاثيّ** — والمُصلَحُ **تنفيذُ** ما نُصّ عليه سلفًا.

**① `Z4` كان ذراعًا خامدة — حجمُها 0.0% في 44,447 صفًّا.** نصُّ §① «صفرُ خضراء
**وبلا توليفة `J1`**»، وكتبتُها `not r.get("j1")` **بافتراضٍ أن الحقل بوليان** —
و`kasih2_scan.j1_bucket` تكتب **سلسلةَ سلّة**: «توليفة (قوي/مضارب × فجوة 30%+)»
أو «الباقي» أو `None` ⇒ **السلسلتان كلتاهما صادقة** فالشرطُ كاذبٌ أبدًا.
🔴 **وهو صنفٌ موثَّقٌ في `wire-check §①` («المفتاحُ/الشكلُ المتخيَّل»)، ونظيرُه
وقع اليوم نفسَه في `tier_days_report`** (‏`anchor_cls` بدل `class`).
🔴 **وأخفاه فِكستشرٌ يكذب:** قفلُ `IMP3` كان يمرّر `j1=True` **بوليانًا** —
شكلٌ **يستحيل** أن يصل من الإنتاج (‏`wire-check §③`).
✅ **المُصلَح:** القراءةُ ببادئة «توليفة» **كما تقرؤها `kasih2_scan` نفسُها**
(‏`:395`)، **وغيرُ المحسوب (`None`) خارجَ الذراع بفائدة الشك** — نصُّ سابقة
`is_red` («ما دون ثلاثٍ محسوبةً يمرّ بفائدة الشك») لأن **الكتمَ إزالة**؛
وعددُه يُطبَع سطرًا تشخيصيًّا فلا يكون قرارًا صامتًا. وقفلُ `IMP3` **شُدِّد**
(الشكلُ من دالّة الإنتاج لا من رأسي) ومعه `IMP8` بثلاثة فوارق.

**② ستّةٌ من اثني عشر معرِّفَ تشغيلٍ لا وجود لها — لأنّي اخترعتُها.**
`CLAUDE.md` يكتب المجموعةَ **بمدًى مختصر** («السبعُ الجديدة `32345880211`…
`32345891385`») **فاستنتجتُ ما بينهما بدل قراءته**، فردّ GitHub **404** على
ستّة ⇒ نزلت **3 ملفّاتِ جلسة من 9** ⇒ **المعيار ③ سقط ميكانيكيًّا** (‏3 دون
أرضية `RECENT_MIN_DAYS`=5) **لا موضوعيًّا**، و‏44,447 صفًّا بدل 44,966.
🧭 **والدرس: معرِّفُ تشغيلٍ لا يُشتقّ من مدًى — يُقرأ من واجهة GitHub**؛
واختراعُه اختلاقٌ لا تقريب. **والمعرِّفاتُ الصحيحة مقروءةٌ من `list_workflow_runs`
لـ`kasih2.yml`:** `32345880211` · `32345882313` · `32345884227` · `32345886026`
· `32345888162` · `32345889879` · `32345891385` (السبع) ‏+ `32330168045` و
`32343667021` و`32343673576` ‏+ السنواتُ الثلاث.

⚖️ **وما يبقى صالحًا من التشغيلة الأولى:** بوّابةُ التكامل (‏`Z2` ≡ المنشور)
**وأرقامُ `Z1`/`Z2`/`Z3` في المعيار ①** (لا تتأثّر بغياب أيامٍ حديثة).
**والمعتمَدُ للنشر أرقامُ التشغيلة المُعادة** — والأولى تُذكَر ولا تُدفَن.
