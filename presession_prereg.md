# 🌙⏱️ `T-PRESESSION` — تسجيلٌ مسبق: «قائمةٌ قبل الافتر/البري بعشر دقائق»

> **يُدفَع قبل أيّ رقم.** المعيارُ والعتباتُ وحدُّ العيّنة وترتيبُ التراجع والتنبّؤاتُ
> **تُقفَل هنا** ولا تُحرَّك بعد رؤية أوّل جدول. وكلُّ تنبّؤٍ مكذَّبٍ **يُنشَر لا يُحذَف**.
> تاريخُ الدفع: 2026-09-03 · أمرُ المالك في اليوم نفسِه.

## ⓪ الدافع — نصُّ المالك

«فكرة الاداة قبل الافتر و البري ب10 دقايق توصلني قايمة بالأسهم اللي متوقع انها تنفجر
بعد 10 دقايق من بداية البري او الافتر … تسوي عصف ذهني جبار و اختبارات و تجارب على جميع
الأسهم اللي انفجرت … و تشوف السلوك المعتمد سواء من نمط شموع او عمليات شراء او اي شي انا
ما فكرت فيه … الهدف من الاداة اني ادخل قبل انفجار سهم الارتكاز ب10 دقايق مب ايام …
الكلام عن الاسهم اللي حققت +80٪ … الاسهم اللي ترتفع مثلا 50٪ اسحب عليها».

وسندُ فيصل: «المضارب الان جالس يحضره للافتر او البري» — أي أن **تحضيرًا** يسبق
الانفجارَ الممتدّ ويُقرأ من شريط الجلسة النظامية.

**السؤال بصيغةٍ قابلةٍ للتكذيب:** في لحظة القرار (عشرُ دقائقَ قبل الجلسة الممتدّة)،
هل توجد قراءةٌ من شموع الدقيقة **المتاحة حينها** تُرتّب الكونَ بحيث تحوي قائمةُ
**عشرة أسماء** نصيبًا من منفجري **‏+80% فأكثر** يفوق الصدفةَ فوقًا يُعتدّ به؟

## ① المجتمع والبيانات

- **المصدر:** ملفّاتُ Polygon المجمَّعة `us_stocks_sip/minute_aggs_v1` (شموعُ الدقيقة
  للسوق كلِّه بما فيه الجلسةُ الممتدّة 04:00–20:00 نيويورك) — **‏PIT بالبناء** (تحوي
  المشطوبين) — نفسُ أنبوبة `kasih_scan`/`ah_scan` بالاسم (`day_key` · `download` ·
  `parse_day`).
- **السنوات:** 2023 · 2024 · 2025 كاملةً (المعايرةُ على 2023–2024 والحكمُ خارج العيّنة على
  2025) **‏+ 2026 حتى تاريخه** خارجَ عيّنةٍ ثانية (وعليه فحصُ كوهورت المالك §⑪).
- **الكونُ في كلّ يوم:** الرموزُ التي **إغلاقُها النظاميُّ السابق** داخل ‏[‏`MIN_PRICE`
  الحيّ = 0.40 · `SPLIT_RADAR_PRICE_MAX` = 10] دولارًا — الحدّان **مُعادان** من `CONFIG`
  (نفسُ كون `kasih_scan`) لا مخترَعان · وبلا أرضيةِ سيولةٍ على الكون عمدًا (السهمُ الذي
  «يُحضَّر» قد يكون هادئًا) — وأرضيةُ السيولة تُطبَّق على **الوسم** لا على الكون.
- **الوحدةُ:** صفٌّ = (يوم · جلسة ∈ {AH, PM} · رمز). كلُّ صفٍّ يحمل ميزاتِه المحسوبةَ
  عند القرار ووسومَه بعد الجلسة.

## ② القراران والسعرُ المرجعيّ

| القرار | لحظةُ القرار (نيويورك) | بدءُ الجلسة | المرجعُ `ref` | مصدرُ الميزات |
|---|---|---|---|---|
| `AH` | **15:50** | 16:00 | آخرُ إغلاقِ دقيقةٍ **قبل 15:50** في يوم القرار | شموعُ يوم القرار حتى 15:49 (بري + نظاميّ) + الملخّصُ المتدحرج للأيام السابقة |
| `PM` | **03:50** | 04:00 | آخرُ إغلاقِ دقيقةٍ في **اليوم السابق** (افترُه حتى 19:59، وإلّا إغلاقُه النظاميّ) | شموعُ اليوم السابق كاملةً (بري + نظاميّ + افتر) + الملخّصُ المتدحرج |

🔒 **لا نظرَ مستقبليّ بالبناء:** كلُّ ميزةٍ تُحسَب من شموعٍ `window_start` **أقلُّ من** لحظة
القرار، ويُقفَل ذلك باختبارٍ يُثبت أن أكبرَ طابعٍ استُعمل أصغرُ من القرار.

## ③ الوسم

- **الوسمُ الحاكم `hit80_10`:** أعلى `high` في شموع ‏[بدء الجلسة، بدء الجلسة + 10 دقائق)
  ‏≥ ‏`ref × 1.80` **و** مجموعُ الدولار (‏`close × volume`) في النافذة نفسِها ‏≥ **‏$20,000**
  (أرضيةُ تنفيذٍ تمنع «انفجارَ» طبعةٍ من مئة سهم — رقمٌ `engineering` معلَن).
- **وسومٌ ثانوية مسجَّلة:** `hit80_30` (‏30 دقيقة) · `hit80_60` · `hit80_sess` (الجلسةُ
  الممتدّة كاملةً) — **وتراجعُ الحكم إليها بترتيبها** إن سقطت أرضيةُ العيّنة (‏§⑥).
- **سلّمٌ وصفيّ (لا يحكم):** ‏+30 · +50 · +100 وزمنُ أوّل بلوغٍ لـ+80.
- ⚠️ **الوسمُ لمسُ قمّةٍ لا تنفيذٌ** — سقفٌ متفائلٌ مُعلَن.

## ④ الميزاتُ المحسوبة عند القرار (مثبَّتةٌ قبل الأرقام — لا تُضاف بعدها)

كلُّها من شموع الدقيقة (`o h l c v` و`transactions`) بلا مصدرٍ آخر:

**(أ) جلسةُ القرار / اليومُ السابق:**
- `F1 day_ret` = آخرُ سعرٍ ÷ إغلاقِ الأمس − 1 · `F2 range_pos` = موضعُ آخر سعرٍ في مدى اليوم
  (‏0 = القاع · 1 = القمّة) · `F3 vwap_rel` = آخرُ سعرٍ ÷ VWAP اليوم − 1 · `F14 gap_open` =
  فتحُ 09:30 ÷ إغلاقِ الأمس − 1.
- `F4 ret_30` = آخرُ سعرٍ ÷ إغلاقِ الدقيقة قبل 30 دقيقة − 1 · `F5 vol_share_30` = حجمُ آخر
  30 دقيقة ÷ حجمِ اليوم · `F6 vol_accel` = حجمُ آخر 15 دقيقة ÷ حجمِ الـ15 التي قبلها ·
  `F7 usd_day` = دولارُ اليوم · `F8 n5` = عددُ الدقائق التي ارتفعت ‏≥5% (فتحًا-إغلاقًا) ·
  `F10 max_volx` = أقصى قفزةِ حجمِ دقيقةٍ عن متوسط سوابقها.
- `F9 anchor_today` = هل أطلقت بوّابةُ الإنتاج (`liq_stage_events` **بالاسم** على شموع
  اليوم حتى القرار) مِرساةً؟ (وسيطُ `now_ms` = لحظةُ القرار) — **صفرُ عتبةٍ جديدة**.
- `F11 spt_30` = متوسّطُ الأسهم لكلّ صفقةٍ في آخر 30 دقيقة (‏`volume ÷ transactions`) ÷
  نظيرُه لليوم — **وكيلُ الطبعات الكبيرة** (قاعدةُ فيصل «صفقةُ المضارب ‏≥1000 سهم» تُقرأ
  هنا بلا شريط الصفقات) · `F19 tx_last5` = صفقاتُ آخر 5 دقائق ÷ متوسّطِ 5 دقائق في اليوم
  (وكيلُ «شراء الإغلاق»).
- `F12 compress` = مدى آخر 30 دقيقة ÷ مدى اليوم (ضغطٌ ضيّقٌ مع حجمٍ صاعد = امتصاص) ·
  `F20 hold_vwap` = هل قاعُ آخر 30 دقيقة ‏≥ VWAP (ثباتٌ فوق المتوسط).
- `F15 halt_proxy` = عددُ الدقائق الغائبة داخل الجلسة النظامية بينما اليومُ نشط (وكيلُ
  توقّفات LULD).
- `F13 ext_prior` = نشاطُ الجلسة الممتدّة السابقة: للقرار `AH` — دولارُ بريماركت اليوم
  وعائدُه ونصيبُه من حجم اليوم؛ للقرار `PM` — دولارُ افتر الأمس وعائدُه (آخرُ افترٍ ÷
  الإغلاق النظاميّ − 1) ونصيبُه.
- `F16 price` = سعرُ المرجع (شريحةُ السعر).

**(ب) المتدحرج (من ملخّصاتِ الأيام السابقة الأقدم من يوم القرار):**
- `F17 dist_low20` = المرجع ÷ أدنى قاعِ 20 يومًا − 1 · `F17b dist_high20` · `F18 down_streak`
  = أيامٌ هابطةٌ متتالية · `F18b spike_hist` = عددُ أيام ‏+50% في آخر 60 يومًا وأيامٌ منذ
  آخرها · `F21 usd_rel20` = دولارُ اليوم ÷ وسيطِ دولار 20 يومًا.

⛔ **بلا صفقاتٍ خام في الطبقة الأولى** (‏`/v3/trades` مكلفةٌ وتُقصّ) — والطبقةُ الثانية
(‏case-control على شريط الصفقات للمنفجرين مقابل شواهد) **اختياريّةٌ مُعلَنة** ولا تدخل
الحكم إلّا بملحقٍ مؤرَّخٍ قبل أرقامها.

## ⑤ بروتوكولُ التقييم

1. **الترتيبُ لكلّ قرار** (يوم × جلسة): يُرتَّب الكونُ بدرجةٍ وتُؤخذ **أعلى 10** (كسرُ
   التعادل بالرمز أبجديًّا — حتميّ).
2. **الدرجاتُ المرشَّحة (مثبَّتة):** `S0` كلُّ ميزةٍ منفردةً (ترتيبٌ تنازليّ، والصاعدةُ
   بعلامتها المحدَّدة سلفًا) · **`S1` الحاكمة:** انحدارٌ لوجستيّ بتنظيم `L2` على الميزات
   المعيَّرة (‏z على المعايرة) **يُعايَر على 2023–2024 وحدهما** ويُقيَّم على 2025 ·
   وشاهدا ضبطٍ ساذجان: `B1` الترتيبُ بدولار اليوم · `B2` الترتيبُ بعائد اليوم.
3. **المقاييس (لكلّ جلسةٍ على حدة ومجمَّعًا):** `P@10` = نصيبُ العشرة الذين أصابوا الوسمَ
   الحاكم · `R@10` = نصيبُ منفجري القرار الذين دخلوا العشرة · `base` = منفجرون ÷ الكون ·
   `lift` = `P@10 ÷ base`.
4. **المتانة:** إعادةُ المعايرة بحذف سنةٍ (‏leave-one-year-out) وتُنشَر الثلاث.

## ⑥ المعايير — تلزم **معًا** (على 2025 خارج العيّنة، للجلستين)

| # | الشرط | الحدّ |
|---|---|---|
| ① | `P@10` ‏≥ ‏10 × `base` **و** ‏≥ 1.0% | مطلقٌ ونسبيّ |
| ② | `R@10` ‏≥ 10% | يلتقط عُشرَ المنفجرين فأكثر |
| ③ | `S1` يتفوّق على `B1` **و** `B2` في `P@10` | ليس دولارَ اليوم مقنَّعًا |
| ④ | إشارةُ الميزات الثلاث الأولى ثابتةٌ عبر سنتَي المعايرة والجلستين | لا انقلاب |
| ⑤ | أرضيةُ العيّنة: **‏≥30 منفجرًا** في وسم النافذة على 2025 لكلّ جلسة | وإلّا **«لا حكم»** لتلك النافذة |

**ترتيبُ التراجع المقفول:** إن سقطت ⑤ للنافذة العشرية ⇒ تُحكَم نافذةُ **‏30 دقيقة**، ثم
**‏60**، ثم **الجلسة كاملةً** — **ويُطبَع في كلّ مُخرَجٍ أيُّ نافذةٍ حكمت**، ولا يُقرأ حكمُ
نافذةٍ أوسع حكمًا على الأضيق. وإن سقطت ⑤ للأربع ⇒ **«لا حكم» كامل**.

**الصيغُ الثلاث للحكم:** «استوفت» (‏①–④ للنافذة الحاكمة) · «فشلت» (⑤ مستوفاة وسقط شرطٌ
من ①–④) · «لا حكم» (⑤ ساقطة).

## ⑦ التنبّؤات (قابلةٌ للكسر — وتُنشَر مكذَّبةً)

- `PS-P1` منفجرو **‏+80% في عشر دقائق** نادرون: **أقلُّ من 30/سنة** لجلسة `AH` وأقلُّ من
  **‏60/سنة** لـ`PM` ⇒ النافذةُ العشرية **«لا حكم»** وتحكم 30 أو 60 دقيقة.
- `PS-P2` أقوى ميزةٍ منفردةٍ **نشاطُ الجلسة الممتدّة السابقة** (‏`F13`) لا الشريطُ الدقيق.
- `PS-P3` ميزاتُ «التحضير» (‏`F11` · `F5` · `F19` · `F12`) رفعُها **موجبٌ لكن دون 3×** ولا
  تدخل الثلاثَ الأولى.
- `PS-P4` `P@10` خارج العيّنة للنافذة العشرية **دون 2%**.
- `PS-P5` `PM` أكثرُ قابليةً للتنبّؤ من `AH` (منفجرون أكثر وإشارةُ مواصلةٍ أقوى).
- `PS-P6` `S1` تتفوّق على `B1` بأقلَّ من ‏2× في `P@10`.

## ⑧ سقفُ النجاح وشكلُ الأداة

- **الأداةُ تُبنى في الحالتين** (أمرُ المالك). قائمةٌ ‏≤10 أسماء عند 15:50 و03:50 نيويورك
  **مع سطرِ صدقٍ إلزاميّ**: النافذةُ التي حكمت · `P@10` و`base` و`lift` خارج العيّنة ·
  و«لمسُ قمّةٍ لا تنفيذ».
- **إن استوفت:** تُرسَل تلغرام (قناةٌ مستقلّة).
- **إن فشلت أو «لا حكم»:** تعمل **صامتةً** — تُسجّل قائمتَها اليومية في سجلٍّ أماميّ
  (‏`presession_ledger.jsonl`) للتحقّق الأماميّ **ولا تُرسَل إلّا بأمرٍ صريحٍ من المالك**
  (بالنصّ: «أرسلها رغم الحكم»). ⚖️ إرسالُ قائمةٍ ثبت أنها لا تعمل ضجيجٌ، وكتمُها بلا
  إذنه مخالفٌ لأمر البناء — فالقرارُ له والحكمُ مطبوع.
- ⛔ **ممنوعٌ مهما كانت الأرقام:** مسُّ `M1`–`M14` أو الجذور · مسُّ أرقام المالك (‏5% ·
  ‏$30k · 3× · 10% · 20%…) · `LOGIC_VERSION` · تحويلُ الدرجة إلى بوّابةِ فرزٍ أو ترتيب.

## ⑨ حدودُ الصدق (مكتوبةٌ قبل الأرقام — كلُّ واحدٍ عيبٌ مرشَّح)

1. **الأخبار:** أكثرُ انفجارات الجلسة الممتدّة تُطلقها أخبارٌ (أرباح · FDA · استحواذ ·
   طرح) لا يراها الشريط — الأداةُ تقرأ التحضيرَ **إن وُجد** ولا تدّعي رؤيةَ الخبر.
2. **لمسٌ لا تنفيذ:** الوسمُ قمّةُ نافذةٍ، وسبريدُ الجلسة الممتدّة واسع.
3. **`ref` في `PM`:** آخرُ افترٍ قد يكون طبعةً شاذّة؛ يُنشَر توزيعُ الفرق بينه وبين الإغلاق
   النظاميّ ويُعاد الحسابُ بالإغلاق النظاميّ مرجعًا **وصفيًّا**.
4. **`transactions`:** عمودٌ من المزوّد بدلالته هو (عددُ الصفقات في الدقيقة) — وكيلٌ لا
   قراءةٌ لدفتر الأوامر.
5. **الكونُ سعريٌّ فقط** ⇒ الأرقامُ عن فئتنا لا السوق.
6. **دائريّةُ كوهورت المالك (‏§⑪):** أسماءٌ اختيرت **لأنها انفجرت** ⇒ فحصُ تغطيةٍ لا
   معدّلَ إصابة.
7. **الانحيازُ بالبقاء منتفٍ** (ملفّاتُ الفلات) — لكنّ الرموزَ المتبدّلةَ التسمية تُقرأ
   رمزين.

## ⑩ ما لا يُمَسّ

الجذورُ · بوّاباتُ السيولة الحيّة · أرقامُ المالك · صيّادُ المقسّم · `kasih_scan.resolve`.
الأداةُ قراءةٌ/تنبيهٌ فقط، خارج الاختيار والترتيب — مقفولٌ بالـAST.

## ⑪ فحصُ الكوهورت (وصفيٌّ خارج الحكم)

- **‏447 انفجارًا ‏≥80%** من سجلّ `weekly_watchlist.json["explosions"]` (‏2026-07-08 ⟶
  2026-09-01) · **كوهورتُ الذهب** (`gold_cohort.json`) · **سجلُّ التصنيف الأماميّ**
  (`tier_fwd_ledger.jsonl`).
- لكلّ اسمٍ: أين وقع في ترتيب `S1` عند قرار `AH` في يوم الانفجار−1 وقرار `PM` في يوم
  الانفجار؟ (**تغطيةٌ** — يُنشَر «كم منهم كان في العشرة» ولا يُعدّ إصابةً).

## ⑫ الميزانية والبنية

- تمريرةٌ واحدة لكلّ يومٍ على ملفّه (تنزيلٌ ⟶ اختزالٌ ⟶ حذف) بترتيبٍ زمنيّ، وملخّصٌ
  متدحرجٌ لكلّ رمز يُحمَل بين الأيام (‏20 يومًا · وحقولُ اليوم السابق لقرار `PM`).
- الكلفةُ المقيسة سلفًا (‏`T-AH`): يومٌ ‏≈27MB واختزالُه ثوانٍ ⇒ سنةٌ ساعاتٌ قليلة ⇒ كلُّ
  سنةٍ تشغيلةٌ مستقلّة، والتقريرُ يقرأ صفوفَ السنوات معًا.
- **بوّاباتُ الصلاحية (منفَّذةٌ في الكود لا موصوفة):** `V1` صفرُ نظرٍ مستقبليّ (أقصى
  طابعٍ استُعمل ‏< القرار) · `V2` تغطيةُ الأيام ‏≥95% وإلّا خروجٌ غيرُ صفريّ **وتسميةُ
  المفقود** · `V3` شاهدُ ضبط: `AAPL` في الكون العام يُقرأ بلا انفجار وبمُخرَجٍ سليم ·
  `V4` عدُّ الصفوف = الكون × القرارات (لا قصَّ صامت).

## ⑬ شكلُ ما شُحن (يُكتَب قبل أيّ رقمٍ كي لا يُوصَف بعده)

ثلاثةُ ملفّاتٍ لا واحد — **والفصلُ مقصود**:

| الملفّ | الدور | القيد |
|---|---|---|
| `presession_feats.py` | **المصدرُ الواحد** للميزات والثوابت | نقيٌّ تمامًا: بلا شبكةٍ ولا ملفّاتٍ ولا استيرادِ `Super_stock` |
| `presession_scan.py` | القياسُ التاريخيّ على الملفّات المجمَّعة | قراءةٌ فقط · لا يستورده الإنتاج |
| `presession_radar.py` | **الأداةُ الحيّة** (القائمة) | إشعارٌ/عرضٌ فقط · فاشلٌ-آمنٌ مطلق |

- **ولماذا وحدةُ ميزاتٍ ثالثة:** لو نسخت الأداةُ الحيّة الحسابَ لصار على الظاهرة
  **قراءتان** فيُقاس شيءٌ ويُرسَل غيرُه — وهو عينُ الدرس المدوَّن في `T-VOLBASE`.
  ومقفولٌ (‏`PS14`) أن `core_feats` مُعرَّفةٌ في المصدر الواحد وأن كلتا الأداتين
  تستوردانه ولا تُعرّفانه.
- **مفتاحُ الترتيب الحيّ مؤقّتٌ ومُعلَن:** `RANK_KEY = "usd_day"` = **خطُّ الأساس
  `B1` بعينه** (§⑤) — لا مفتاحٌ مُعايَرٌ ولا مخترَع، لأن الادّعاءَ قبل الرقم ممنوع.
  يُبدَّل **بسطرٍ واحد** بعد الحكم إلى ما يستوفي §⑥.
- **الافتراضُ صامت** (`PRESESSION_SEND` غيرُ مضبوط): يُكتَب `presession_ledger.jsonl`
  ولا تُرسَل رسالة — تطبيقًا لـ§⑧ حرفيًّا. ومقفولٌ سلوكيًّا بثلاث حالاتٍ مفرِّقة
  (‏`PS13`): صامتٌ ⇒ صفرُ إرسالٍ مع سجلٍّ وختم · بأمرِ المالك ⇒ إرسالٌ وختم · ورفضُ
  تلغرام ⇒ **لا ختمَ ولا سجلّ** فتُعاد المحاولةُ داخل نافذة القرار.
- **الوصلةُ الحيّة** في `operator_entry_live.py` داخل الحلقة بعد مسحَي السيولة، خلف
  `try/except`، بنافذةِ قرارٍ ستِّ دقائق (دورةُ العامل ‏≈60ث) وختمٍ `PRE:<يوم>:<AH|PM>`
  **داخل ددوب «هنا الدخول» القائم** — لا ملفَّ حالةٍ جديد ولا كاتبَ جديد.
- **وميزانيةُ جلبٍ مُعلَنة** `BUDGET_SEC=45`: خيطُ العامل يخدم مسحَ السيولة أيضًا فلا
  يُؤخَّر بجلبٍ مفتوح؛ وما تقصّه الميزانيةُ **يُطبَع بعدده** (لا قصَّ صامتًا).
- **وبوّابةُ المِرساة في الرادار** `anchor_via` تُحقَن بـ`Super_stock.liq_stage_events`
  **بالاسم**، ومِشيتُها مطابقةٌ لـ`kasih_scan.first_anchor` — **تكافؤٌ مقفولٌ سلوكيًّا**
  (‏`PS11`) في الاتجاهين ومعه فرعُ «بلا حاقن ⇒ صفر».

---

## ⑨-ملحق (2026-09-03، **بعد** صدور الحكم) — قرارُ تسليمٍ للمالك لا نتيجةَ قياس

> 🔴 **يُقرأ ملحقًا مؤرَّخًا لا تعديلًا للعقد:** كلُّ ما فوقه كُتب **قبل أيّ رقم**
> ولم يُمَسّ حرفًا. وما تحته **قرارُ مالكٍ اتُّخذ وهو يرى الأرقام** — فلا يُقدَّم
> استنتاجَ قياسٍ ولا يُحرَّك به حدٌّ من §⑥.

**أمرُه الحرفيّ: «شغّل البريماركت»** — بعد أن عُرضت عليه ثلاثةُ خيارات، فاختار:
① تبديلَ مفتاح الترتيب إلى عائلة الافتر · ② وتشغيلَ الإرسال **لقرار البريماركت
وحدَه**. والافترُ يبقى صامتًا يكتب السجلَّ الأماميّ.

### ⑨-1 قاعدةُ اختيار المفتاح — مُعلَنةٌ لأنها تُقيِّد الادّعاء

**المفتاحُ يُختار من سنتَي المعايرة (‏2023 و2024) وحدهما، وسنةُ التقييم (‏2025)
تبقى خارج العيّنة.** وبهذي القاعدة:

| السنة | المتصدّر منفردًا (`S0` · `PM · 10د`) | `P@10` | الرافعة |
|---|---|---|---|
| 2023 (معايرة) | **`post_hi_ret`** | 0.200% | 33.1× |
| 2024 (معايرة) | **`post_hi_ret`** | 0.794% | 112.5× |
| 2025 (تقييم) | `post_ret` ثم `post_hi_ret` | 1.200% / 1.040% | 89.3× / 77.4× |

⇒ **`post_hi_ret` هو المختار** (الأوّلُ في السنتين)، ونتيجتُه **خارج العيّنة**:
**‏26 إصابةً من 2,500 اسمًا مُسلَّمًا (‏`P@10` = 1.04% · رافعة 77.4×)** ومنها
**‏26 من 87 منفجرًا (‏`R@10` = 29.9%)** ⇒ يستوفي ①②③ من §⑥ على هذا القياس.

🔴 **و`post_ret` لم يُختَر رغم تصدّره سنةَ التقييم بـ1.20%** — لأنه **لم يتصدّر
2023** (كان في تعادلِ ثلاثِ إصاباتٍ عند 0.120%) ⇒ اختيارُه بعد رؤية 2025 قرارٌ
بعديّ. **وهذا الامتناعُ هو الفارق بين قياسٍ وبين انتقاءٍ يُجمِّل نفسَه.**

### ⑨-2 حدودُ صدقٍ تُقرأ مع الأرقام أعلاه — ولا تُطوى

1. **رقمُ `S0` ترتيبٌ بمفتاحٍ منفرد لا نموذجٌ**، وهو غيرُ رقم §⑥ الحاكم (‏0.40%)
   الذي قِيس بالنموذج المُدرَّب. **فلا يُقارَن الرقمان ولا يُقال إن الحكم انقلب.**
   **الحكمُ على النافذة الحاكمة يبقى «فشلت»** كما نُشر.
2. **والاختيارُ من بين 28 مفتاحًا** — أعلى ثمانٍ وعشرين قيمةً فيها ما يعلو صدفةً.
   المخفِّف: **رتابةُ العائلة** (‏`post_*` تتصدّر السنتين كلتيهما ومعها `pre_share`
   أقوى معاملٍ في الخلايا الثمانِ للنموذج) ⇒ اتّساقٌ لا نتوءُ سنة.
3. **الوسمُ لمسُ قمّةٍ لا صفقة** (‏+ أرضية $20,000) ⇒ **سقفٌ متفائل**، **ولا عائدَ
   قِيس إطلاقًا**.
4. **والافترُ لم يُبدَّل مفتاحُه** لأن `post_*` معدومةٌ بالتعريف في قرار 15:50 ⇒
   الترتيبُ بها يُفرِّغ القائمة. وهو صامتٌ فلا أثرَ للمفتاح فيه اليوم.

### ⑨-3 ما لم يتغيّر

- **§⑥ بحدودها كما هي** — لا تُرخى ولا يُعاد تعريفُ نافذةٍ حاكمة.
- **الوسمُ والميزاتُ والمجتمعُ والقراران** (‏§①-§④) بت-بت.
- **سقفُ القائمة `TOPK`=10** وميزانيةُ الجلب وبوّابةُ المِرساة والدِدوب.
- **صفرُ مسٍّ بالفرز أو الجذور أو أرقام المالك** ⇒ ولا `LOGIC_VERSION`
  (تسليمٌ/عرضٌ لا فرز).
- **والأقفال:** `PS20` (مفتاحٌ لكلّ جلسةٍ من `PF` والرسالةُ تفرّق) · `PS20-ب`
  (الوصلُ حيّ: `order_rows` بـ`rank_key(slot)` والترتيبُ **ينقلب** على عيّنةٍ
  تفرّق والسجلُّ يكتب مفتاحَ جلسته) · `PS21` (البوّابةُ دالّةٌ نقيّةٌ واحدة ·
  والفارقُ الحيُّ من `_maybe_presession` نفسِها) · `PS22` (الوصلُ في الـworkflow —
  وبلاه كان الشحنُ كلُّه `no-op`، بصمةُ `BT_CANDLE`).

---

## ⑩-ملحق (2026-09-03، **تصحيحُ المالك**) — النافذةُ الحاكمة هي **الجلسةُ كاملةً**

> 🔴🔴 **خطأُ قراءةٍ منّي في العقد نفسِه، أُعلنه ولا أُخفيه.**

**نصُّه اليوم حرفيًّا:** «مب أتوقع تنفجر في اول 10 دقايق — **اتوقع تنفجر خلال مدة
الجلسة كاملة** جلسة البري او الافتر … **لكن الاشعار يوصلني قبل بداية البري او
الافتر بـ10 دقايق**».

⇒ **«العشرُ دقائق» في أمره الأوّل وقتُ وصول الإشعار لا مدّةُ التوقّع** — وأنا
قرأتُها مدّةَ توقّعٍ فثبّتُّ في §⑥ أن النافذة الحاكمة «‏10 دقائق» وجعلتُ الجلسةَ
آخرَ خطوةٍ في ترتيب التراجع. **والصحيحُ عكسُه: الجلسةُ هي الحاكمة.**

### ⑩-1 لماذا هذا **ليس** تحريكَ هدف

1. **النوافذُ الأربع قِيست معًا منذ أوّل تشغيلة** (‏`hit80_10` · `hit80_30` ·
   `hit80_60` · **`hit80_s`**) — مثبَّتةً في §③ قبل أيّ رقم ⇒ **رقمُ الجلسة منشورٌ
   في الجدول نفسِه ولم يُنتقَ بعد رؤية النتائج.**
2. **والحكمُ لا ينقلب:** `PM · الجلسة` **«فشلت» أيضًا** — تعبر ①(‏`P@10` 2.92% ·
   `lift` 21.5×) وتسقط على **②** (‏`R@10` 8.3% دون 10%). ⇒ **لا تُرقَّى نتيجةٌ من
   فشلٍ إلى نجاحٍ بهذا التصحيح**، وإنّما يُقرأ الرقمُ على النافذة الصحيحة.
3. **والمعاييرُ الأربعةُ وحدودُها لا تُمَسّ** — ولا يُعاد تعريفُ «الوسم» ولا
   الأرضيةِ ولا `TOPK`.

### ⑩-2 ما يترتّب عليه عمليًّا

- **قراءةُ الحكم تصير على `PM · الجلسة`**، و`PM · 10د` يبقى منشورًا للقراءة.
- **ومفتاحُ الترتيب الحيّ يُعاد اختيارُه على وسم الجلسة بالقاعدة نفسِها**
  (‏§⑨-1: **من سنتَي المعايرة وحدهما**) — لأن `post_hi_ret` المشحون اختير على
  وسم العشر دقائق. ⇒ أُضيف إلى أداة الحكم جدولُ **`S0` لكلّ نافذة**
  (‏`S0_WINDOWS = (10, 0)`) بمقياس `eval_scores` نفسِه، ومقفولٌ سلوكيًّا (`PS23`).
- **وسطرُ الرسالة يقول النافذةَ الصحيحة** — «الجلسةُ كاملةً» لا «أوّلُ عشر دقائق».

### ⑩-3 وحدُّ صدقٍ يبقى

الفارقُ بين النافذتين **كبيرٌ في صالح الجلسة** (‏`P@10` 0.40% ⟶ 2.92% · إصابات
‏10 ⟶ 73 في سنة التقييم) — **ومع ذلك الحكمُ «فشلت»**، والوسمُ يبقى **لمسَ قمّةٍ
لا صفقة** ‏(+ أرضية $20,000) ⇒ **سقفٌ متفائل، ولا عائدَ قِيس.**
