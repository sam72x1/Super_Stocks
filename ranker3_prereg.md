# 🚦🕵️ `T-RANKER3` — «الارتكازُ الذي يجهّزه المضارب» مُرتِّبًا (تسجيلٌ مسبق)

> **يُكتَب ويُدفَع قبل أيّ رقم.** أمرُ المالك 2026-08-13 بعد حالة `DFSC`:
> «**فرقٌ بين ارتكازٍ يجهّزه المضارب ونشيط وسهمٍ خامل — فهنا الفكرة**»
> و«**المفروض يكون ترتيبُ الأسهم بناءً على هذي النتائج**» و«**لازم تكون دقيقًا جدًّا
> لأن هذي خطوة مفصلية**».
> **سقفُ النجاح مثبَّتٌ سلفًا: قياسٌ + توصية — ولا تغييرَ إنتاجيَّ إلّا بأمره الصريح.**

---

## ① الحدثُ الذي وُلدت منه — **مقيسٌ لا مُفترَض**

**‏`DFSC` — أوّلُ نتيجةٍ حقيقية للبوت، وأوّلُ دليلٍ قاطعٍ على عطب المُرتِّب معًا:**

| الحقيقة | القيمة (من `weekly_watchlist.json` و`alerts_history.json`) |
|---|---|
| رشّحه البوتُ | **2026-08-08** · سعرُ الترشيح **$1.26** · الدفعات 1.19/1.23/1.26 |
| درجتُه | **‏85 — أعلى درجةٍ في القائمة كلِّها** |
| بلغ | **`t1`** ✅ · الوقف $1.1067 |
| الآن | **$2.60** ⇒ **‏≥+106% من سعر الترشيح** (‏+203% من القاع 0.858) |
| طبقةُ التوقيت | `مسح 08-11` ← `منطقة الشراء 08-12` ← **`كسر الرقم الحرج 08-13`** — **سجّلها البوتُ الثلاث** |
| **ترتيبُه يوم 08-13** | 🔴 **‏#20 من 22 — خارج السعة 15** |

🔴 **والقلبُ مقيسٌ في جدولٍ واحد:** المفتاحُ الأوّل في `rank_key` **ثنائيّ**
(`in_entry_band`) ⇒ كلُّ من هو داخل نطاقه يعلو **كلَّ** من هو خارجه مهما كانت جودتُه:

| # | الرمز | داخل النطاق | الجاهزية | النقاط |
|---|---|---|---|---|
| **14** | `BBLG` | ✅ | **0** | **20** |
| 15 | `TIL` | ❌ | **70** | 75 |
| 16 | `QTI` | ❌ | **70** | 65 |
| 17 | `XAIR` | ❌ | 60 | **85** |
| **20** | **`DFSC`** | ❌ | 45 | **85** |

⇒ **القمّةُ (‏1-14) جاهزيتُها كلُّها ‏≤45، وأصحابُ 70/70/60/60/50 كلُّهم تحت الخطّ.**

⚖️ **وأمانةٌ تُكتَب قبل الأرقام ولو ضدّ التجربة:** سقوطُ `DFSC` إلى #20 **ليس كلُّه
خطأ** — سعرُه يومَها 2.24 ودفعاتُه 1.26 ⇒ **لم يعد قابلًا للدخول بخطّته**، والمفتاحُ
أُضيف لهذا (`T-PROX`، وله قياسٌ أماميٌّ معلَّق). **فالعيبُ المُثبَت هو الخلط**: قائمةٌ
واحدة تُرتِّب «مرشّحًا جديدًا» و«مركزًا نجح وارتفع» بقاعدةٍ واحدة.

📌 **وسندان مقيسان يسبقان هذا الحدث:** `T-REPLAY10` قاست المُرتِّبَ **عند مستوى
الصدفة** وسمّته «مَن يقرّر ما يصل المالك» · و**‏24 منفجرًا منذ 2026-08-10 كان الفارزُ
يقبلهم عند قاعهم (`base_reason=مرشّح`) ولم يصل المالكَ منهم ولا واحد (‏0 من 24)**.

---

## ② لماذا هذي ليست إعادةَ ملفٍّ مغلق — **يُكتَب صريحًا قبل أيّ رقم**

**أربعُ تجاربَ سابقةٍ فشلت** في «سلوكٍ يميّز المنفجر»: `T-ACC-1` · `T-ACC-2` ·
بصمةُ الارتفاع (`behav`) · `FSTO`. **ومعها `bt_features` (ثلاث سنوات · Bonferroni ·
`leave-one-year-out`) لم تُبقِ سمةً واحدة.** ⇒ **الاحتمالُ القبليُّ للفشل عالٍ ويُعلَن.**

**وثلاثةُ فروقٍ حقيقيةٍ تجعلها سؤالًا جديدًا لا إعادة:**
1. **المقياسُ مختلف:** تلك قاست **الاختيار** (هل تدخل البِركة؟) وهذي تقيس **الترتيب**
   داخل بِركةٍ مقبولةٍ سلفًا — وهو الموضعُ الذي **قِيس عند الصدفة** ويقرّر ‏~90%.
2. **المجتمعُ مختلف:** بعد «معايير فيصل وحدها» صارت البِركةُ ‏≈109 مرشّحًا/يوم — وحين
   شُغِّلت `T-RANKER` كان الحكمُ «**لا يوجد ما يُرتَّب إليه**». **الآن يوجد**: 24 منفجرًا
   مقبولًا في أربعة أيام.
3. **الفرضيةُ مختلفة:** تلك اختبرت سماتٍ **ساكنة** عند القاع؛ وهذي تختبر **سلوكًا
   قريبًا** (ما يفعله السهمُ في آخر يوم-يومين) **وتاريخَ انفجاراتٍ متكرّرة** — وهما
   فرضيتا المالك نصًّا.

---

## ③ 🔑 مادّةُ التجربة **موجودةٌ في الكود وتُرمى** — إعادةُ استعمالٍ لا بناء

| المكوّن | مصدرُه القائم | حالتُه اليوم |
|---|---|---|
| **`n_spikes`** = عددُ الانفجارات السابقة | `spike_info(c, exclude_last=BASE_WINDOW)` | 🔴 **يُحسب ثم يُرمى** — يظهر وسمَ عرضٍ «معيد إجرام (N)» فقط، **ولا يُخزَّن ولا يدخل الترتيب** |
| قفزةُ الحجم | `VOL_SPIKE_MULT` القائم | يُستعمل في `hand_activity_today` لا في الترتيب |
| كسرُ/قربُ الرقم الحرج | `crit` (محفوظٌ في الصفقة سلفًا) | يُخزَّن ولا يُرتَّب به |
| سلوكُ الشمعة الأخيرة | `hand_activity_today` (**بلا نظرٍ مستقبليّ** بنصّه) | عرضٌ فقط |
| الجاهزية/النقاط/`rr`/`in_band` | `rank_key` | مستعمَلة |

⇒ **صفرُ مؤشّرٍ مخترَع.** وكلُّ عتبةٍ تُستعمل **قائمةٌ في `CONFIG`** أو تُشتقّ من
البيانات نفسها (وسيط/متوسط) — **ولا رقمَ من عندي**.

---

## ④ الأذرع — **خمسةٌ ولا سادسة** · في تشغيلةٍ واحدة بميزانيةٍ ثابتة

| الذراع | المفاتيح | يجيب |
|---|---|---|
| **`R-0`** | الإنتاج: `in_band` → جاهزية → h4 → نقاط → `rr` | المرجع |
| **`R-ACT`** | **درجةُ النشاط أوّلًا** ثم مفاتيح الإنتاج | هل السلوكُ أقوى من كلّ شيء؟ |
| **`R-ACT2`** | `in_band` أوّلًا **ثم درجةُ النشاط** ثم الباقي | يحفظ قابليةَ التنفيذ ويرتّب داخل الفئة (**الأقربُ لعقد «لا إشعار إلا بالجاهز»**) |
| **`R-HIST`** | **`n_spikes` أوّلًا** ثم مفاتيح الإنتاج | يعزل **فرضية المالك (ب)** وحدها |
| **`R-RAND`** | ترتيبٌ عشوائيّ (‏200 خلطة) | 🔴 **شاهدُ الضبط — بدونه لا معنى لأيّ تفوّق** (درسُ `T-RANKER2`) |

**🥇 الحاكمة: `R-ACT2`** — لأنها الوحيدةُ التي لا تكسر عقدَ «لا إشعار إلا بالجاهز»
ولا تُلغي `T-PROX` (وله قياسٌ أماميٌّ معلَّق). و`R-ACT` تُقاس **لتفكيك الأثر** لا
لتُعتمَد ابتداءً.

**درجةُ النشاط = مجموعُ أربعةِ مكوّناتٍ مُثبَّتةٍ الآن (لا يُضاف ولا يُحذَف بعد الأرقام):**
- **`A1` قفزةُ الحجم**: حجمُ آخر جلسة ÷ متوسط 20 — عتبتُه `VOL_SPIKE_MULT` القائمة.
- **`A2` توسّعُ المدى**: مدى آخر جلسة ÷ **وسيط** مدى 20 (وسيطٌ لا متوسط: مقاومةُ الذيل).
- **`A3` موضعُ الإغلاق في مدى يومه**: قربُ القمّة = طلبٌ يمتصّ (نقيضُ `BBLG` الذي أغلق
  عند **‏34%** من مداه).
- **`A5` قربُ السعر من الرقم الحرج** `crit` (المحفوظِ سلفًا) — «على وشك التحرّر».

⛔ **و`behav_score` مُستبعَدٌ عمدًا من كلّ الأذرع** — عليه **قفلٌ صريحٌ** يمنعه من
`rank_key`/`select_top` (وُلد من فشلٍ مقيس). **ويُنشَر عمودًا تشخيصيًّا فقط**؛ وإدخالُه
يلزمه رفعُ ذلك القفل بأمر المالك، **ولا يُفعَل في هذي التجربة**.

---

## ⑤ المقياس — مثبَّتٌ قبل الأرقام

**الحاكم: `d50`** = المنفجرون **المُسلَّمون** (بلغوا `EXPLOSION_PCT`=50% قبل وقفهم)
**داخل خانات السعة الحيّة (15)** — بأداة `replay10` الأمينة، نفسِ مقياس `T-BASE-2` و
`T-CAP` و`T-STABILITY`.

🥇 **ومقياسٌ ثانٍ مسجَّلٌ الآن لأنه سؤالُ المالك نصًّا** («**فيصل يدخل في الأسبوع
سهمين ارتكاز فقط**»): **`d50@3`** = المنفجرون المُسلَّمون **داخل أعلى ثلاث خانات**.
⇒ **مُرتِّبٌ نافعٌ يجب أن يرفع `d50@3` أكثرَ ممّا يرفع `d50`** — وهذا **أشدُّ** لا أسهل.

**ويُنشَر ولا يحكم:** `d100` · الإشارات · المحسومة · الدقة · `R`/صفقة · `rejected_cap`.

---

## ⑥ حرّاسُ الاعتماد — **أربعةٌ، ولكلٍّ قراءةٌ محدَّدةٌ سلفًا**

| # | الحارس | الحدّ |
|---|---|---|
| **(أ)** | **`d50@3` للحاكمة ‏≥ الأساس في السنوات الثلاث — بلا انقلابِ إشارة** | شرطٌ لازم (درسُ `T-SHORT`/`T-CMAG`/`G5`) |
| **(ب)** | **وتتفوّق على `R-RAND`**: فوق المئين 95 في سنتين من ثلاث على الأقلّ | شرطٌ لازم — وإلّا **فهي زينة** |
| **(ج)** | `R`/صفقة **مجمَّعًا** لا يسوء | ‏≥ −0.02R |
| **(د)** | **شاهدُ `DFSC` الحيّ**: على لقطة 2026-08-13 ترفع الحاكمةُ `DFSC` **فوق** `BBLG` | شرطٌ لازم |

⇒ **سقوطُ (أ) أو (ب) ⇒ لا يُعتمَد.** وسقوطُ (ج) أو (د) وحدهما ⇒ **مقايضةٌ تُعرَض على
المالك** لا رفضٌ ولا اعتماد.
⚖️ **و(ج) تُقرأ «مجمَّعًا» صراحةً** — تصحيحًا لقصور `T-BASE-475`، **ولا تُختار القراءةُ
بعد رؤية الأرقام.**

---

## ⑦ حرّاسُ الصلاحية — **ولكلٍّ سطرُ كودٍ يُوقِف** (درسُ `envelope_prereg §⑦`)

| # | الحارس | الخروج |
|---|---|---|
| `V1` | **صفرُ نظرٍ مستقبليّ**: كلُّ مكوّنات النشاط تُحسب من شموعٍ **حتى يوم الترتيب** حصرًا — والنتيجةُ تُقاس **بعده**. مقفولٌ باختبارٍ يُبدّل المستقبلَ فلا تتغيّر الدرجة | 3 |
| `V2` | **الأذرعُ متمايزةٌ فعلًا**: ترتيبُ كلِّ ذراعٍ يخالف الأساسَ في ‏≥1% من الخانات — وإلّا **`no-op`** (بصمةُ `BT_CANDLE`/`T-CLIFF-2`) | 3 |
| `V3` | **فحصُ تكامل**: `R-0` يُعيد مِرساةَ `B120` المنشورة عند سعة 15 (‏2024:25 · 2025:26 · 2026:15) | يُعلَن (‏ℹ️) |
| `V4` | `child_env()` يرفع أعلامَ الباكتيست (بلاها `d50`=None و«‏+0» يُقرأ «لا فرق») | 2 |
| `V5` | **ميزانيةٌ ثابتة**: نفسُ اللقطة والسنة والسعة وسقوف الجلب لكلّ الأذرع (درسُ `T-CLIFF`) | 3 |
| `V6` | **العلمُ مطفأٌ ⇒ الفارزُ بت-بت** (قفلٌ سلوكيّ) | قفل |
| `V7` | **`behav_score` صفرُ ظهورٍ في أيّ ذراع** (قفلُه القائم لا يُخرَق) | قفل |
| `V8` | **لقطةٌ مفقودة ⇒ توقّف** (لا مضيَّ صامتٍ على كون اليوم) | 4 |

---

## ⑧ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر سقطت أو صمدت)

- **`Q1`** `R-ACT2` **يتفوّق** على `R-0` في `d50@3` — لأن الخانات الثلاث الأولى هي
  التي يفسدها المفتاحُ الثنائيّ أكثر من غيرها.
- **`Q2`** `R-HIST` (تاريخُ الانفجارات وحده) **أضعفُ** من `R-ACT2` — السلوكُ القريب
  أقوى من التاريخ البعيد.
- **`Q3`** `R-ACT` (النشاطُ أوّلًا) **يرفع `d50` ويخفض قابليةَ التنفيذ** — فيكسب
  بالمقياس ويخسر بالعقد.
- **`Q4`** الحارس (د) **يمرّ**: `DFSC` (نشاطٌ عالٍ: كسرُ الرقم الحرج + حجم) يعلو
  `BBLG` (أغلق عند 34% من مداه، `bars_after`=0).
- **`Q5`** 🔴 **الأرجحُ إحصائيًّا أن (ب) يسقط** — أربعُ تجاربَ سابقةٍ فشلت، والفروقُ
  في هذا المستودع تقع داخل الضجيج عادةً (‏±3 منفجرين). **يُكتَب الآن فلا يُقرأ الفشلُ
  مفاجأةً ولا النجاحُ حتميّة.**

---

## ⑨ ما لا يُدَّعى — يُكتَب قبل الأرقام

1. **لا تمسّ البوّابات:** `M1`-`M14` وأرقامُ الظرف والدخول والوقف والأهداف **لا
   تُمَسّ بحرف**. (والمالك نصَّ أن تعديل البوّابات **لاحقٌ** لا الآن.)
2. **ولا يُدَّعى أن `DFSC` كان سيُسلَّم أفضل** — البوتُ **سلّمه فعلًا** في 08-08 عند
   1.26 وبلغ `t1`. المقيسُ **ترتيبُ ما بعده** لا التقاطُه.
3. **وانحيازُ البقاء وتشويهُ التقسيمات وعمى الافتر** قائمةٌ — **لكنها مشتركةٌ بين
   الأذرع فتُطرَح من الفرق**.
4. **والعددُ صحيحٌ صغير** (‏7-28 منفجرًا/سنة) بسعة 15 ⇒ **‏±3 داخل الضجيج** — فأيُّ
   فرقٍ دون ذلك **لا يُبنى عليه** مهما بدا مغريًا.
5. **ولا يُقاس «هل يجهّزه المضارب» مباشرةً** — المقيسُ **أثرُه بالسعر والحجم على
   الشمعة اليومية**. وقراءةُ التدفّق اللحظيّ (Polygon) **لا تاريخَ لها** فلا تدخل.
6. **والمقارنةُ بأرقامٍ منشورةٍ عند سعة 10 باطلة** ⇒ المِرساةُ تُقاس في نفس التشغيلة.

---

## ⑩ إعلانُ مسٍّ بجذرٍ مقفول

تنفيذُ الأذرع يستلزم خطّافًا في **`rank_key`** — جذرٌ مقفول، **وهو أخطرُ ما مسسناه**
لأنه يقرّر العضوية. **بأمر المالك المباشر** وبنفس سابقة `BT_STABILITY_GATE`:
**علمٌ مطفأٌ افتراضيًّا** فالإنتاجُ **بت-بت** (`V6`)، وبصمتُه تُحدَّث **بإقرارٍ مؤرَّخ**.
**وبقيّةُ الجذور لا تُمَسّ.** وأيُّ اعتمادٍ لاحق **يمسّ العضوية** ⇒ `LOGIC_VERSION`
+ `force_renew=1` مرّة، **وقرارُه للمالك وحده**.

## ⑪ رموزُ الخروج

**‏0** = قِيس والحرّاسُ استُوفوا · **‏2** = أعلامٌ غائبة · **‏3** = بوّابةُ صلاحيةٍ
ساقطة ⇒ **لا رقمَ يُنشَر ولا يُفسَّر** · **‏4** = لقطةٌ مفقودة.

---

## ⑫-أ 🔴🔴 ملحقٌ مؤرَّخ **2026-08-13 — مدفوعٌ قبل أيّ رقم**: تعارضٌ بنيويّ في عقدي

**كشفَته عيّنةٌ تفريقيّةٌ صغيرة قبل التشغيل** (سهمٌ نشِطٌ خارج النطاق مقابل خاملٍ
داخله): **`R-ACT2` — الحاكمةُ — يستحيل عليها بنيويًّا أن ترفع `DFSC` فوق `BBLG`.**

**السبب رياضيّ لا تجريبيّ:** `R-ACT2` تُبقي `in_band` **مفتاحًا أوّلًا** (عمدًا: تحفظ
عقدَ «لا إشعار إلا بالجاهز» ولا تُلغي `T-PROX`) ⇒ **تُعيد الترتيبَ داخل فئة النطاق
فقط**، و`DFSC` كان **خارج** النطاق و`BBLG` **داخله**. ⇒ **الحارس (د) والحاكمةُ
متعارضان بالتعريف** — والذنبُ ذنبي في صياغة §⑥.

**التصحيح (يُثبَّت الآن، ولا يُختار بعد الأرقام):**
1. **الحارس (د) يُقاس على `R-ACT`** — الذراعُ الوحيدةُ القادرةُ بنيويًّا على تجاوز
   فئة النطاق. **ويبقى شرطًا لازمًا لها وحدها.**
2. **`R-ACT2` تبقى الحاكمة** للحرّاس (أ)(ب)(ج) — لأن سؤالها الحقيقيّ أضيقُ وأصدق:
   **«بين المرشّحين الجاهزين فعلًا، هل يرتّب النشاطُ أفضل؟»**
3. 🔑 **والنتيجةُ المعرفيةُ أهمُّ من الترتيب — وتُكتَب قبل الأرقام:** **حالةُ `DFSC`
   ليست مشكلةَ ترتيبٍ سلوكيّ أصلًا** — هي مشكلةُ **خلطِ قائمتين** (§⑫). فلو
   استوفت `R-ACT2` حرّاسَها، فذلك مكسبٌ **مستقلٌّ** عن `DFSC`؛ ولو سقطت، **لا يعني
   ذلك أن مشكلة `DFSC` بلا حلّ** — حلُّها في §⑫ لا هنا.

---

## ⑫ 📌 بندٌ منفصلٌ مُبلَّغٌ لا يُقاس هنا — **فصلُ القائمتين**

العيبُ المُثبَت في §① (خلطُ «مرشّحٍ جديد» بـ«مركزٍ نجح») **لا يحتاج تجربة**: هو
**تصحيحُ تصميمٍ مسنودٌ بالحالة** — مركزٌ ارتفع عن دخوله **ليس منافسًا على خانةِ
دخولٍ جديدة**. ⏸️ **وقرارُه للمالك منفصلًا عن هذي التجربة** فلا يختلط أثرُهما.
