# 🕯️ تسجيل مسبق — **T-CANDLE**: هل «حالة الزناد» في آخر الشموع تميّز الجاهز للانطلاق؟

> **يُكتب ويُدفع قبل أي تشغيل.** التاريخ: 2026-07-30 · الدافع: طلب المالك الصريح
> («سهم ارتكاز أوكيه، لكن لازم يكون فيه شي مميّز يخلّينا نعرف: هل بينطلق قريب، ولا يحقّق
> المعايير مثل أي سهم غيره؟ … ممكن عن طريق سلوك آخر الشموع في اليومي أو 4 ساعات»).
> **الأولى من أربعَ عشرةَ تجربةً تستهدف التوقيت لا الاختيار.**

---

## ⓪ 🔴 تصحيحٌ يتصدّر: استشهادُ JZ لا يسند الفرضية — وسندٌ أقوى يسندها

المالك استشهد بـ«**JZ عنده 12 شمعة ساقطة**». وبالتحقّق البصريّ من `TG_2174`: الشارت
عليه **سبعةُ** مستويات فوق السعر (‏2.560) وثمانيةٌ إجمالًا — **لا اثنا عشر**. ومُسندُ
جملة فيصل هو «**اهداف السهم**» وإبطالُها برفعة القروب. ⇒ **«12 شمعة» ليست سندًا لمقياس
جاهزيةٍ بعدد الشموع** (وقد صُحِّح سطرُ الكاتالوج تبعًا لذلك).

✅ **لكن حدسَ المالك مسنودٌ بنصَّين أقوى وأصرح، وهما أساسُ هذه التجربة:**

| المصدر | نصّ فيصل حرفيًّا | الدلالة |
|---|---|---|
| ⭐ **`TG_1870`** | قاع ⟶ اختبار أوّل مقاومة فوقه ⟶ رجوعٌ يختبر القاع **مع سحب سيولة** ⟶ «**هنا يكون rsi جاهز للانفجار**» | **أصرحُ نصّ جاهزيةٍ في المجموعة كلّها — ويُحسب من OHLCV وحده** |
| ⭐ **`TG_2037`** | «**ننتظر RSI عند 27 يتوافق مع شمعة 4 ساعات**» + «**3 قيعان = ارتفاع**» | اقترانُ حالة المؤشّر بشمعة **4 ساعات** = حرفيًّا ما اقترحه المالك |
| `TG_1815` | «شمعة ساقطة ع 4 ساعات **انتظر يختبرها فريم يومي**» | توزيعُ الأدوار: **4س يرسم · اليوميّ يؤكّد** |
| `TG_2184` | «**تجاوز الشمعه مع ثبات**» إيجابية · «**كسر الشمعه وعدم العوده**» سلبية | المرجع **شمعةٌ سابقة** لا فريم |
| `TG_2188/2191` | «**القاع 2 = ثبات او سحب سيوله**» · «تلاعب بالشموع ⇒ **متابعه فقط**» | نمطٌ مهمّ ⇓ |

🔎 **ونمطٌ مقاس يوجّه التصميم:** في هذي النصوص الشموعُ تُستعمل **للتنزيل** («متابعه فقط»)
أكثر مما تُستعمل للإثبات ⇒ **الأقرب للنجاح مِصفاةُ نفيٍ لا درجةُ ترشيح.**

## ⓵-أ ⚖️ وتصحيحُ المالك لوجهة السؤال (‏2026-07-30) — يُدوَّن لأنه يغيّر ما نقيسه

> «**متابعة** فوقتها يكون السهم **شبه جاهز** ينتظر المضارب فقط **أو تأكيدًا بسيطًا** —
> والمضارب هذا **شي طبيعي**. الكلام: **ليه السهم هذا بينفجر خلال فترة وجيزة جدًّا؟**»

⇒ ثلاثة تصحيحات لما كنتُ أكتبه:
1. **«متابعه فقط» = «شبه جاهز»** — أي حالةٌ **متقدّمة** على السلّم لا مبتدئة، تنتظر
   المضارب **أو تأكيدًا بسيطًا**. فتسميتي `faisal_wait` صحيحةٌ بشرط أن تُقرأ «قريبٌ من
   الجاهزية» لا «بعيدٌ عنها».
2. 🔴 **«المضارب شي طبيعي» ⇒ حضورُ المضارب ليس جوابًا، بل معطًى.** وأنا قلتُ في هذي
   الجلسة إن «المضارب هو الرابط الوحيد الذي صمد» — **والمالك يصحّح الوجهة:** كلُّ سهمٍ من
   هذي الفئة ينتظر مضاربًا، فالانتظارُ لا يميّز شيئًا. **المميِّز المطلوب: لماذا هذا
   السهم بعينه، ولماذا الآن.**
3. ⇒ **فالمُقاس يجب أن يكون الوشوك** (`+50%` خلال 10 جلسات) لا المقدار المطلق — وهذا
   نُقِّح في §⓷ قبل أي تشغيل.

## ⓵ ⛔ والنصّ الذي يقيّد الطموح — بلسان فيصل نفسه

> **`TG_2089`: «لاتعجل ع رزقك — السهم جاهز ع جميع المؤشرات والشموع · … · بانتظار فقط
> دخول المضارب»**

الجاهزيةُ الشمعيّة **مُحقَّقة** وهو **ينتظر**؛ والزنادُ الأخير **دخول المضارب** — خارج
OHLCV بنيويًّا. ويكرّرها بأربعة ألفاظ (`TG_1822` «ننتظر فقط مضارب» · `TG_1831` «عدم دخول
قبل المضارب» · DSY «فقط انتظار مضارب» · ONCO «تنتظر … هل يثبت اولا»).
⇒ **الشموع عند فيصل شرطٌ لازمٌ غير كافٍ.** فهذه التجربة **تقيس اللازم وتُقرّ بأن الكافي
خارجها** — وسقفُ نجاحها محدَّدٌ بذلك (§⑤).

## ⓶ السمة المُختبَرة — **واحدة**، مركّبةٌ من مبنيٍّ لا مُبتكَرة

**«حالة الزناد» `trigger_state`** — تُحسب على `df.iloc[:i]` حصرًا (ما قبل شمعة الإشارة):

1. **قاعٌ مكوَّن** ثابتٌ 3-8 جلسات (البوّابة القائمة D4).
2. **اختُبرت أوّل مقاومة فوق القاع ورُدَّ السعر** عنها.
3. **رجع لنطاق القاع فمسحه ثم استعاده** — بعمق `SPLIT_SWEEP_MIN/MAX` (‏7-13%، نطاق
   فيصل الموثّق) وإغلاقٍ فوق القاع بعده.
4. **RSI في 20-30** لحظتها (‏«rsi جاهز للانفجار» · «RSI عند 27»).
5. **[4س] الشمعة الساقطة لم يغطّها اليوميّ بعد** (`green_cover` القائمة · `TG_1815`).

⚠️ **صياغةُ الصدق:** ①و③ و⑤ **مبنيّةٌ جزئيًّا** (`bottom_test_state` · كشفُ المسح ·
`green_cover`) ⇒ فهذا **اختبارُ تركيبةٍ من مكوّناتٍ لم يُختبَر أيٌّ منها كمميِّز**، لا
ابتكارُ مقياس. ولا سمة سادسة ولا معدَّلٌ يُختار بعد الأرقام.

### 🔴 وكيف تختلف عمّا سقط — الشرطُ الذي لا يُسامح فيه
- **عن `behavior_rise_profile` (سقطت):** تلك **سجلٌّ تاريخيّ** (‏~800 بار · عدّ أحداثٍ:
  `n_pumps`/`repumps`/`sweeps` في أيّ موضع). وهذه **حالةُ آخر 3-12 بارًا** بلا عدٍّ خارج
  النافذة. ⚠️ **وتداخلٌ يُعلَن سلفًا:** «سحب السيولة» يظهر في أكثر من نصف قواعد الجاهزية
  **و**في `behav.sweeps` — فمسحٌ حديثٌ يدخل النافذتين ⇒ **يُقاس الارتباط بـ`behav_score`
  ويُنشَر مهما كان**؛ ارتباطٌ عالٍ ⇒ تُعلَن التجربة **مكرّرة** لا ناجحة.
- 🔴 **عن `fsto_oscillation` (سقطت — وهي الأقرب خطرًا):** تلك «سلوك آخر الشموع» بالحرف
  (‏`chop`=متوسط |ΔK| على 30 بارًا) وفشلت على السوق الكامل 2025. **واكتشافُها هو التحذير
  الحقيقيّ: شريحة `chop>12` فارغةٌ تمامًا بين المؤهَّلين — لأن M1-M5 تنتقي القاعَ الهادئ
  فتضغط تباينَ سلوك الشموع بحكم التصميم.** ⇒ `trigger_state` **تسلسلٌ بنيويّ** (ترتيبُ
  أحداث) لا **تباينٌ إحصائيّ** — وهذا فرقُها الجوهريّ. ويُقاس ارتباطها بـ`fsto_chop` ويُنشَر.

## ⓷ المقياس — مُثبَّتٌ قبل أي رقم

> 🔴 **تنقيحٌ مُسجَّل قبل أي تشغيل** (‏2026-07-30، بعد توضيح المالك في نفس اليوم — **لا
> نتيجةَ واحدة ظهرت بعد، والعلم `BT_CANDLE` لم يُشغَّل قطّ**، فالتنقيح مشروعٌ ويُعلَن):
> نصّ المالك: «**المضارب هذا شي طبيعي · الكلام: ليه السهم هذا بينفجر خلال فترة وجيزة
> جدًّا؟**» ⇒ **سؤاله عن الوشوك (‏imminence) لا عن المقدار وحده.** وقياسي الأوّل
> (`mg_pre_stop`) يقيس «كم أعطى قبل وقفه» **بلا حدٍّ زمنيّ** — فسهمٌ يبلغ +50% بعد
> ثلاثة أشهر يُحتسب فيه ناجحًا، وهو **ليس** ما سأله. فأُضيف الزمن للمقياس المساند.

- **الأساسي: توقّع العائد بوحدة المخاطرة (R)** — لا نسبة النجاح ولا «لمس t1».
  **السند ثلاثة أحكام مقيسة:** T-STOP (النجاح 34%→54% والتوقّع لا يتحرّك) · T-EXIT ·
  و**T-LIBERATION أمس: نجاح 87% مع توقّع ‏−0.04R وفارقٍ مقترنٍ ‏−0.56R**.
- **المساند ① «الانفجار الوشيك»** (الجواب المباشر لسؤال المالك): نسبة بلوغ **+50%** من
  الدخول **خلال `CANDLE_SOON_BARS` = 10 جلسات** من الإشارة، **قبل ضرب الوقف**.
  ⚠️ **والعشرة مُثبَّتة الآن بسندٍ لا باختيارٍ بعديّ:** «فترة وجيزة جدًّا» ونطاقُ فيصل
  المعلَن للتحرّك القريب (وصفة المقسّم: «متوسط حركة 20 < 30 يوم») ونافذةُ الحسم القائمة
  عندنا. **ولا يُجرَّب طولٌ ثانٍ ثم يُختار الأفضل** — ذلك p-hacking؛ ولو لزم فحصُ الحساسية
  فيُطبَع لـ{5, 20} **وصفيًّا خارج الحكم** ويُصرَّح بذلك.
- **المساند ②:** نسبة بلوغ +50% **بلا حدٍّ زمنيّ** (‏`mg_pre_stop`) — يُطبَع **بجانب** ①
  لا بدلًا منه، فيُقرأ الفرقُ بينهما: هو **بعينه** مقدارُ ما يخصّ «الوشوك» من الأثر.
- **+ عدد المُعبَّأة/غير المُعبَّأة صريحًا** و**وسيط الجلسات حتى بلوغ +50%** لمن بلغه.

## ⓸ المعيار المسجَّل مسبقًا — يُعتمد **فقط** إذا تحقّق كلّ ما يلي

1. **ثبات الاتجاه:** توقّع شريحة «حالة الزناد ✅» **أعلى** من شريحة «❌» في **الثلاث
   سنوات** (2023 · 2024 · 2025).
2. **حجم الفرق ≥ +0.15R** في **كلّ** سنة (نفس عتبة T-STOP/T-LIBERATION).
3. **العيّنة ≥30 صفقة مُعبَّأة في كل شريحة وكل سنة** — وإلّا فالحكم «**لا حكم: العيّنة لا
   تكفي**» لا «فشل» (تمييزٌ ألزمَنا به T-SHORT).
4. 🔴 **مقارنةٌ مقترنة إلزاميًّا** — يُحسب توقّع الأساس **مقصورًا على نفس الإشارات** التي
   حملت الحالة، ويجب أن يصمد الفارق **هناك أيضًا** (إسقاطًا لـ«فخّ الامتناع»، درس T1b).
5. **لا انقلاب مجمَّعًا** · و**Bonferroni غير لازم** (سمةٌ واحدة، شريحتان) — وهذا سببُ
   الاقتصار على سمةٍ واحدة.

## ⓹ سقف ما يُسمح به عند النجاح — يُحدَّد الآن

- ✅ الأقصى: **وسمُ توقيتٍ/سطرُ عرض** («🕯️ حالة الزناد مكتملة») + **اقتراحٌ** للمالك.
- ❌ **لا بوّابة M1-M14** (مقفولة بـC3) · ❌ لا وزن ترتيب في `rank_key` · ❌ لا مسّ
  بالدخول/الوقف/الأهداف · ❌ ولا تغيير عضوية `select_top`.
- والعلم `BT_CANDLE` **باكتيست حصريًّا مطفأ افتراضيًّا** ⇒ الإنتاج بت-بت (مقفول باختبار
  «مطفأ ⇒ قاموس الصفقة بت-بت»، نفس نمط `BT_LIBERATION`).

## ⓺ حدود الصدق — و**الاحتمال الراجح المُسجَّل: ستفشل**

أُسجّل الآن، قبل أي رقم، أن **الأرجح فشلُها** — بثلاثة أسبابٍ **مقيسة** لا مخاوف:
1. **القاعُ الهادئ يضغط التباين** (اكتشاف FSTO: شريحة الاضطراب فارغةٌ بين المؤهَّلين).
2. **الانتظارُ يهدم R** (‏T-LIBERATION أمس) — و«حالة الزناد» بطبيعتها **انتظارُ تأكيد**.
3. **الانفجارُ نادرٌ بنيويًّا** (`TG_1907` «مرة مرتين بالسنة») ⇒ إيجابياتٌ قليلة وفواصلُ
   متداخلة.

🔴 **④ وقياسُ قدرةٍ (‏power) أجريتُه قبل التشغيل — ويرجّح «لا حكم» لا «فشل»:** على **300
سلسلة عشوائية** تتحقّق «حالة الزناد» في **‏3.7%** فقط. وبإشاراتنا (‏168 · 110 · 147 محسومة
للسنوات الثلاث) يعني ذلك شريحةً ✅ بحجم **‏4-8 صفقات/سنة** — **أدنى بكثير من عتبة العيّنة
المسجَّلة (‏≥30/شريحة/سنة)**. ⇒ **الأرجح بنيويًّا أن المخرَج يكون «لا حكم: العيّنة لا
تكفي»**، وهو **ليس** فشلًا (تمييزٌ ألزمَنا به T-SHORT).
⇒ **فالتشغيل يبدأ بسنةٍ واحدة (‏2025) كفحصِ قدرة**: تُقرأ **أحجام الشرائح أوّلًا**، ولا
تُشغَّل السنتان الأخريان إلا إذا بلغت الشريحة ✅ ثلاثين. ⚠️ وهذا **ليس** انتقاءً بعديًّا:
شرطُ العيّنة **مُسجَّلٌ سلفًا** في §④-3، والمقروء هو **حجم** الشريحة لا اتجاهُها —
**وأرقام 2025 تُنشَر كاملةً مهما كانت.** ولو ثبت ضعفُ القدرة فالعلاج المشروع الوحيد هو
**تخفيف تعريفٍ مسجَّل مسبقًا جديد** (لا تعديلُ هذا) أو **إعلانُ أن الفرضية غير قابلة
للاختبار بحجم بياناتنا** — وكلاهما نتيجةٌ صادقة.

وحدودٌ أخرى: انحياز البقاء · تشويه التقسيمات في العوائد الأمامية (‏WOK/YYGH/WNW موثّقة) ·
والسمة **إعادةُ بناءٍ** لكلام فيصل بأدواتنا لا أرقامه هو (ففشلُها ينفي **تشغيلنا الآلي**
لا قراءته) · و**الكافي (دخول المضارب) خارجها بنيويًّا** (§⓵).

**فلماذا تُشغَّل إذن؟** لأنها **القاعدةُ الوحيدة التي يسمّيها فيصل «جاهزًا» بلفظه
وتُحسب من بياناتنا** — واختبارُها يحوّل السؤال من انطباعٍ إلى رقم، والفشلُ المسجَّل
يُغلق البابَ بدليلٍ بدل أن يبقى مفتوحًا بالظنّ.

## ⓻ 🔴 البند صفر — شرطٌ لازم سبق هذه التجربة (وقد نُفِّذ)

`update_tracking` كان يجمّد `max_gain_pct` عند لمس t1 ⇒ سؤال «هل تنفجر؟» **غيرُ قابل
للقياس** على السجلّ الحيّ. أُصلح بـ`observe_closed_alerts` (نافذة 60 جلسة، حقولٌ مستقلّة،
والحكم لم يُمَسّ). ⚠️ **وهذه التجربة تعتمد على مقياس الباكتيست (`mg_pre_stop`) لا على
السجلّ الحيّ** — فهي غير معتمدةٍ على ذلك الإصلاح، لكن **الجواب النهائي للمالك يعتمد
عليه**، فيُذكَر معه دائمًا.

---

**التوقيع:** يُدفع **قبل** أي تشغيل. أيّ تعديل بعد ظهور نتيجة = إبطالٌ للتجربة ويُعلَن.
