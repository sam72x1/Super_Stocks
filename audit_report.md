# 🔍 تقرير تدقيق مستقل — Codex (قراءة فقط)

> **المدقّق:** Codex · الموديل `gpt-5.6-sol` · جهد استدلال `xhigh` (الأقصى المتاح)
> **التاريخ:** 2026-07-14 · **الفرع:** `claude/e2a-measurement-clean` · **الكوميت:** `2f9268535b`
> **النطاق:** قراءة فقط — لم يُعدَّل أي ملف (`touchedFiles: []`) ولم يُطبَّق أي إصلاح.
> **الحالة:** تقرير للقراءة والقرار — لا commit ولا دفع.
>
> ⚠️ هذي **مخرجات المدقّق كما هي**، لم أتحقّق منها بنفسي ولم أنفّذ منها شيئًا.
> قبل تنفيذ أي بند: تأكّد أنه لا يمسّ الجذور (M1-M14 · rank_key · select_top ·
> classify_tier · entry_status · LOGIC_VERSION) ولا قرارًا محسومًا في `CLAUDE.md`.

---

# ملخص التدقيق

وجدت **5 عيوب P0، و8 عيوب P1، و6 عيوب P2**.

راجعت كامل `CLAUDE.md` أولًا، ثم كامل `test_bot.py`، ثم جميع الأنظمة المطلوبة: الحسابات المالية والأهداف/الوقف/RR/حجم المركز، تصحيح التقسيم، الفرز والتتبع، الترحيل والتخزين و`git_save`، الباكتيست و`acc_verify.py`، الرادار وE2، التقارير الفنية واليدوية، المراقب اللحظي، التقويم، وملفات GitHub Actions ذات الصلة.

لم أترك ملفًا مصدرِيًا مسمّى في الطلب بلا مراجعة. ما لم أصل إليه ديناميكيًا:

- لم أشغّل `test_bot.py` لأنه ينشئ ملفات مؤقتة ومخرجات داخل المستودع، وهذا يخالف شرط القراءة فقط؛ راجعت أقفاله ساكنًا.
- لم أستدعِ Yahoo أو Polygon أو Telegram حيًا، ولم أفحص سجل تشغيل GitHub Actions أو الأسرار الفعلية.
- لم أتحقق دلاليًا من كل سجل تاريخي في JSON؛ استعملت السجلات الحالية فقط لإثبات تصادم محدد.
- لم أجد تسريبًا مباشرًا في قصّ `backtest_symbol` (`df.iloc[:i]`) أو حد Polygon الحصري `timestamp.lt` في `acc_verify`؛ التسريب الزمني الحقيقي موجود في ترحيل القائمة أدناه.

## P0 — خطر مالي أو فقد حالة

### P0-1 — الترحيل يعيد تطبيق مستويات مشتقة من المستقبل على تاريخ الصفقة

[`Super_stock.py:5786`](/Users/kid/Super_Stocks/Super_stock.py:5786)، [`Super_stock.py:5800`](/Users/kid/Super_Stocks/Super_stock.py:5800)، [`Super_stock.py:5722`](/Users/kid/Super_Stocks/Super_stock.py:5722)، [`Super_stock.py:8248`](/Users/kid/Super_Stocks/Super_stock.py:8248) — `migrate_watchlist` يحسب الوقف والأهداف من كامل التاريخ الحالي، ثم `update_watchlist_status` يعيد تشغيلها على الشموع منذ تاريخ الترشيح القديم.

- **سيناريو ملموس:** سهم رُشّح في 1 يونيو بوقف `$2.60`؛ في يوليو تغيّرت `LOGIC_VERSION` وأُعيد حساب الوقف إلى `$3.00` باستخدام بيانات يوليو؛ بعدها تُفحص شمعة 3 يونيو ذات قاع `$2.90` بالوقف الجديد ويُسجل إيقاف رجعي، مع أن هذا الوقف لم يكن موجودًا وقتها. والعكس ممكن لهدف مشتق لاحقًا يُسجل كأنه تحقق قبل اشتقاقه.
- **الجذر/المحسوم:** يمس جذر نتائج التتبع وسلامة الحالة؛ لا يطعن في قرار منهجي محسوم.
- **فجوة القفل:** اختبار الترحيل في [`test_bot.py:1106`](/Users/kid/Super_Stocks/test_bot.py:1106) يثبت إعادة الحساب والحفاظ على `entry_ref`، لكنه لا يختبر تاريخ سريان المستويات الجديدة.

### P0-2 — فشل جلب التقسيم يتحول إلى عامل 1.0 ثم تُحسم نتائج مالية خاطئة

[`Super_stock.py:5508`](/Users/kid/Super_Stocks/Super_stock.py:5508)، [`Super_stock.py:5486`](/Users/kid/Super_Stocks/Super_stock.py:5486)، [`Super_stock.py:5710`](/Users/kid/Super_Stocks/Super_stock.py:5710)، [`Super_stock.py:9907`](/Users/kid/Super_Stocks/Super_stock.py:9907) — أي 401/429/تعطل Yahoo يجعل `_fetch_splits=None` ثم العامل `1.0`، لكن مسار التتبع يواصل حسم الوقف والأهداف بدل تأجيل الحسم غير الموثوق.

- **سيناريو ملموس:** تنبيه قبل تقسيم عكسي 1:10 بسعر `$1` و`t1=$1.20` و`t3=$2`؛ Yahoo يرد 429، بينما OHLC المعاد تحميله معدل ويعرض قممًا `$11–$13`؛ النظام يسجل `hit_t3` لأن `$13 ≥ $2`، مع أن الأهداف الصحيحة بعد التسوية `$12/$20`.
- **الجذر/المحسوم:** يمس جذر حصاد النتائج؛ لا يعارض كون التقسيم العكسي علامة إيجابية، بل يكشف أن تنفيذ F-02 يفشل مفتوحًا.
- **فجوة القفل:** الأقفال تختبر عاملًا معروفًا و`None→1.0`، ولا تختبر تقسيمًا حقيقيًا مع فشل مصدر الأحداث.

### P0-3 — محدد مقياس التقسيم يخطئ بعد هبوط قوي تالٍ للتقسيم

[`Super_stock.py:5520`](/Users/kid/Super_Stocks/Super_stock.py:5520)، [`Super_stock.py:5531`](/Users/kid/Super_Stocks/Super_stock.py:5531)، [`Super_stock.py:5711`](/Users/kid/Super_Stocks/Super_stock.py:5711) — `_scale_divisor` يقرر المقياس حسب قرب المستوى من سعر اليوم، وهي فرضية تنهار إذا تحرك السهم بقوة بعد التقسيم.

- **سيناريو ملموس:** مستوى مخزن `$1`، تقسيم عكسي 1:10 (`factor=0.1`)، ثم يهبط السعر المعدل من `$10` إلى `$2.50`؛ المسافة اللوغاريتمية إلى `$1` أصغر من المسافة إلى `$10`، فتختار الدالة المقياس القديم وتعيد المقارنة مع أهداف `$1–$2` على بيانات معدلة، مولدة أهدافًا زائفة.
- **الجذر/المحسوم:** يمس جذر تصحيح التقسيم، لكنه لا يطعن في القرار المحسوم بشأن التقسيم نفسه.
- **فجوة القفل:** الاختبارات تغطي سعرًا قريبًا من المقياس المعدل، لا هبوطًا بعد التقسيم يتجاوز نقطة المنتصف اللوغاريتمية.

### P0-4 — فشل Telegram يستهلك الدِدوب ويُسجل نجاحًا زائفًا

[`Super_stock.py:4619`](/Users/kid/Super_Stocks/Super_stock.py:4619)، [`Super_stock.py:6925`](/Users/kid/Super_Stocks/Super_stock.py:6925)، [`ignition_live.py:531`](/Users/kid/Super_Stocks/ignition_live.py:531)، [`pullback_live.py:63`](/Users/kid/Super_Stocks/pullback_live.py:63) — `send_telegram` يرجع `False` بدل الرمي، لكن المستدعين يتجاهلون القيمة بعد ختم الدِدوب، وE2 يستدعي `telegram_success`.

- **سيناريو ملموس:** Telegram يرد 429 لتنبيه انطلاق؛ التنبيه لا يصل، لكن `ignition_alert` و`seen` وhandoff تمنع إعادة المحاولة بقية المقطع/الجلسة، وملف القياس يقول إن التسليم نجح. وفي `pullback_live` تُدفع حالة `triggered` إلى Git قبل محاولة الإرسال.
- **الجذر/المحسوم:** لا يمس اختيار السهم؛ يمس جذر تسليم التنبيه وحقيقة قياس E2.
- **فجوة القفل:** اختبار [`test_bot.py:2557`](/Users/kid/Super_Stocks/test_bot.py:2557) يستبدل المرسل بدالة لا ترجع نجاحًا صريحًا ويتحقق من عدد النداءات فقط.

### P0-5 — JSON تالف يُفسر كحالة جديدة ثم تُكتب الحالة الفارغة فوق الأصل

[`Super_stock.py:4831`](/Users/kid/Super_Stocks/Super_stock.py:4831)، [`Super_stock.py:9706`](/Users/kid/Super_Stocks/Super_stock.py:9706)، [`Super_stock.py:9809`](/Users/kid/Super_Stocks/Super_stock.py:9809)، [`Super_stock.py:10048`](/Users/kid/Super_Stocks/Super_stock.py:10048) — فشل parsing في `weekly_watchlist.json` أو `alerts_history.json` يرجع بنية فارغة صالحة ظاهريًا، ثم يحفظها التشغيل اللاحق.

- **سيناريو ملموس:** ملف القائمة مقطوع بعد دفع Git أو تعديل يدوي؛ `load_watchlist` يرجع قائمة أولية، فيدخل البوت مسار “تأسيس أول قائمة”، فتضيع الأسهم النشطة والوقف والدِدوب والتاريخ؛ وبالمثل يُستبدل سجل التنبيهات القديم بتنبيهات التشغيل الحالي فقط.
- **الجذر/المحسوم:** يمس جذر التخزين الدائم؛ لا يمس قرارًا منهجيًا.
- **فجوة القفل:** [`test_bot.py:1128`](/Users/kid/Super_Stocks/test_bot.py:1128) يختبر الكتابة الذرية وround-trip، لا ملفًا موجودًا لكنه غير صالح نحويًا.

## P1 — عيوب مهمة

### P1-1 — `TARGET_CAP_MULT` ليس سقفًا فعليًا للأهداف

[`Super_stock.py:318`](/Users/kid/Super_Stocks/Super_stock.py:318)، [`Super_stock.py:2228`](/Users/kid/Super_Stocks/Super_stock.py:2228)، [`Super_stock.py:2263`](/Users/kid/Super_Stocks/Super_stock.py:2263)، [`analyze_one.py:369`](/Users/kid/Super_Stocks/analyze_one.py:369) — عند غياب ثلاثة مستويات داخل السقف، يختار الكود مقاومة فوق السقف ثم يضربها تكراريًا في `1.25`.

- **سيناريو ملموس:** السعر `$10` والسقف `$20` والمقاومة الوحيدة `$25`؛ تصبح الأهداف `$25/$31.25/$39.06`، ويرتفع `RR` المبني على `t1` وقد يؤثر في كسر تعادل `rank_key`.
- **الجذر/المحسوم:** يمس جذر حساب الأهداف وRR؛ لا يغير عتبات M2/M4/M5 أو أي قرار محظور.
- **فجوة القفل:** اختبار الثوابت في [`test_bot.py:5596`](/Users/kid/Super_Stocks/test_bot.py:5596) يثبت ترتيب الأهداف والحد الأدنى لـ`t1`، لكنه لا يثبت `t1/t2/t3 ≤ cap`.

### P1-2 — رؤوس الشموع الحمراء الأسبوعية تدخل `t1` رغم القرار المحسوم باستبعادها

[`Super_stock.py:1324`](/Users/kid/Super_Stocks/Super_stock.py:1324)، [`Super_stock.py:1345`](/Users/kid/Super_Stocks/Super_stock.py:1345)، [`Super_stock.py:2225`](/Users/kid/Super_Stocks/Super_stock.py:2225)، [`CLAUDE.md:163`](/Users/kid/Super_Stocks/CLAUDE.md:163) — الاستدعاء اليومي الافتراضي يعيد أخذ عينات أسبوعية داخليًا ويمرر إليها `include_red_heads=True` قبل الإضافة الأسبوعية الصريحة ذات `False`.

- **سيناريو ملموس:** سعر `$10` بلا مقاومة يومية قريبة، لكن الأسبوع المعاد تجميعه يحتوي شمعة حمراء بقمة `$11`؛ تدخل `$11` في `resist` وتصبح `t1` حتى لو كان `USE_MULTIFRAME_TARGETS=False`.
- **الجذر/المحسوم:** نعم، يمس جذر الهدف وينتهك قرارًا محسومًا في `CLAUDE.md`؛ هذا ليس اعتراضًا على القرار بل مخالفة تنفيذية له.
- **فجوة القفل:** اختبار [`test_bot.py:5613`](/Users/kid/Super_Stocks/test_bot.py:5613) يقارن تشغيل الخيار وإطفاءه، لكن المسارين يحتويان أصلًا الرأس الأسبوعي نفسه، لذلك يمكنه النجاح كاذبًا.

### P1-3 — سجل التنبيهات يتجاهل الوقف بعد `t1` ثم قد يمنح `t2/t3` لاحقًا

[`Super_stock.py:9920`](/Users/kid/Super_Stocks/Super_stock.py:9920)، [`Super_stock.py:9925`](/Users/kid/Super_Stocks/Super_stock.py:9925)، مقابل [`Super_stock.py:5752`](/Users/kid/Super_Stocks/Super_stock.py:5752) — `update_tracking` يفحص الوقف فقط عندما `best==0`، بينما مسار القائمة ينهي النموذج عند الوقف حتى بعد هدف سابق.

- **سيناريو ملموس:** اليوم الأول يلمس `t1`، اليوم الثاني يهبط إلى الوقف، واليوم الثالث يصعد إلى `t3`؛ `alerts_history` يسجل `hit_t3`، بينما `weekly_watchlist` ينهي الصفقة عند وقف اليوم الثاني بعد `t1`.
- **الجذر/المحسوم:** يمس جذر نتيجة الصفقة ويخلق تناقضًا بين مخزنين؛ لا يطعن في قرار إبقاء المتابعة بعد الهدف، وإنما في استمرارها بعد حدث إنهاء صريح.
- **فجوة القفل:** لا يوجد قفل لتسلسل هدف → وقف → هدف أعلى.

### P1-4 — ملخص الباكتيست “الصادق” يتجاهل العوائد التنفيذية المخزنة

[`Super_stock.py:8431`](/Users/kid/Super_Stocks/Super_stock.py:8431)، [`Super_stock.py:8762`](/Users/kid/Super_Stocks/Super_stock.py:8762)، [`Super_stock.py:8826`](/Users/kid/Super_Stocks/Super_stock.py:8826)، [`Super_stock.py:8851`](/Users/kid/Super_Stocks/Super_stock.py:8851) — المحرك يخزن `ret_a/ret_b` بعد الفجوة والسبريد، لكن الملخص يعيد بناء العائد من الوقف النظري ويجعل كل خسارة `-1R`.

- **سيناريو ملموس:** دخول `$10` ووقف `$9`، ثم فجوة افتتاح إلى `$5` مع سبريد 4%؛ `ret_a≈-52%`، لكن الملخص يعرض `-10%` و`-1R`، رغم أن الرسالة تقول إن العوائد بعد تكلفة التنفيذ.
- **الجذر/المحسوم:** يمس جذر تقييم الباكتيست المالي، لا منطق الإنتاج أو القرارات المقفلة.
- **فجوة القفل:** [`test_bot.py:4049`](/Users/kid/Super_Stocks/test_bot.py:4049) يقفل الحالة النظرية بلا فجوة، واختبارات F-COST تختبر `_resolve_arm` منفصلًا فقط.

### P1-5 — باكتيست جزئي أو احتياطي يُعرض باسم “السوق الكامل بلا انحياز”

[`Super_stock.py:9434`](/Users/kid/Super_Stocks/Super_stock.py:9434)، [`Super_stock.py:9442`](/Users/kid/Super_Stocks/Super_stock.py:9442)، [`Super_stock.py:9497`](/Users/kid/Super_Stocks/Super_stock.py:9497)، [`Super_stock.py:9543`](/Users/kid/Super_Stocks/Super_stock.py:9543) — علم `market` يبقى صحيحًا عند سقوط كون ناسداك إلى العينة الاحتياطية، ولا توجد بوابة تغطية عند تحميل Yahoo الجزئي.

- **سيناريو ملموس:** Nasdaq Trader يرد 403، فيُختبر كون البوت الصغير لكن العنوان يظل “السوق الكامل”; أو يُطلب 3500 رمز ويُحمّل 350 فقط، ثم تعرض الرسالة `رموز: 3500` رغم تخطي 3150 بلا بيانات.
- **الجذر/المحسوم:** يمس صدق عرض الباكتيست والانحياز؛ لا يغير بوابات الإنتاج المقفلة.
- **فجوة القفل:** لا يوجد اختبار لسقوط الكون أو نسبة الرموز ذات التاريخ الصالح.

### P1-6 — فشل مسح الأرباح الكامل أو الجزئي يظهر كأنه “لا توجد أسهم”

[`technical_report.py:840`](/Users/kid/Super_Stocks/technical_report.py:840)، [`Super_stock.py:820`](/Users/kid/Super_Stocks/Super_stock.py:820)، [`technical_report.py:908`](/Users/kid/Super_Stocks/technical_report.py:908) — الإصلاح يميز الاستثناء فقط، لكن `get_universe` يبتلع خطأ الشبكة ويرجع `[]`، كما أن التحميل الجزئي لا يخضع لبوابة تغطية.

- **سيناريو ملموس:** ناسداك يرد 403؛ لا يحدث استثناء في `scan_nasdaq_earnings`، فيُمسح صفر رمز وتصل رسالة “لا توجد أسهم تطابق الشروط” بدل “تعذر المسح”.
- **الجذر/المحسوم:** لا يمس فرز قائمة الارتكاز الرئيسية؛ يمس صدق التقرير المستقل فقط.
- **فجوة القفل:** لا يوجد اختبار يمرر كونًا فارغًا أو تحميلًا جزئيًا من دون استثناء.

### P1-7 — حل تعارض `git_save` آخر-كاتب-يفوز على الملف كاملًا ويمحو تحديث الريموت

[`Super_stock.py:9736`](/Users/kid/Super_Stocks/Super_stock.py:9736)، [`Super_stock.py:9772`](/Users/kid/Super_Stocks/Super_stock.py:9772)، [`Super_stock.py:9777`](/Users/kid/Super_Stocks/Super_stock.py:9777) — عند تعارض rebase تُقرأ النسخة المحلية القديمة، ثم يُعتمد الريموت وتُكتب النسخة القديمة فوق الملف كاملًا بلا دمج سجلات أو حالات.

- **سيناريو ملموس:** التشغيل A يدفع `hit_t1`، والتشغيل B الأقدم يملك السجل نفسه بحالة `open`؛ B يتعارض، يعمل reset إلى A، ثم يعيد blob القديم كاملًا ويدفع `open`، فتضيع نتيجة A.
- **الجذر/المحسوم:** يمس جذر التخزين؛ مجموعة التزامن تقلل تعارض اليومي/المراقب لكنها لا تحمي من تعديل خارجي أو مستدعٍ مستقل.
- **فجوة القفل:** [`test_bot.py:717`](/Users/kid/Super_Stocks/test_bot.py:717) يقفل تنفيذ reset وإعادة الكتابة تحديدًا، ولا يختبر دمج تحديثين دلاليين.

### P1-8 — توكن Telegram قابل للتسرب داخل نص الاستثناء

[`Super_stock.py:4625`](/Users/kid/Super_Stocks/Super_stock.py:4625)، [`Super_stock.py:4645`](/Users/kid/Super_Stocks/Super_stock.py:4645)، [`Super_stock.py:4664`](/Users/kid/Super_Stocks/Super_stock.py:4664)، [`cline_notify.py:84`](/Users/kid/Super_Stocks/cline_notify.py:84) — التوكن موضوع في مسار URL، ثم يُسجل الاستثناء الخام بلا تنقيح.

- **سيناريو ملموس:** فشل DNS/TLS أو نفاد retries يولد استثناءً يتضمن `url: /bot<TOKEN>/sendMessage`؛ السطر يطبع الاستثناء كاملًا في سجل Actions.
- **الجذر/المحسوم:** عيب أمني في النقل والتسجيل، خارج منطق الاختيار.
- **فجوة القفل:** فحص أسرار E2 يغطي artifacts المقننة، لا رسائل استثناء HTTP الفعلية.

## P2 — تحسينات ذات مخاطر محدودة أو طبقات غير إنتاجية

### P2-1 — بوابة المضارب قد تعتمد على صفقات جلسة سابقة

[`Super_stock.py:6607`](/Users/kid/Super_Stocks/Super_stock.py:6607)، [`Super_stock.py:6619`](/Users/kid/Super_Stocks/Super_stock.py:6619)، [`Super_stock.py:6904`](/Users/kid/Super_Stocks/Super_stock.py:6904) — `operator_flow` يجلب آخر 250 صفقة بلا حد زمني لبداية الجلسة ثم يستعمل النتيجة كبوابة تنبيه الانطلاق.

- **سيناريو ملموس:** سهم قليل التداول لديه 10 صفقات اليوم و240 من أمس، وبين صفقات أمس block بحجم 1000؛ يظهر `has_operator=True` اليوم رغم غياب المضارب في الجلسة الحالية.
- **الجذر/المحسوم:** تنبيه/توقيت فقط، ولا يعيد order flow إلى `rank_key` أو الاختيار؛ لا يتحدى القرار المحسوم بإبقائه خارج الاختيار.

### P2-2 — حجم المركز قد يتجاوز رأس المال المتاح مرات عديدة

[`Super_stock.py:3737`](/Users/kid/Super_Stocks/Super_stock.py:3737) — `position_size` يقيد خسارة الوقف فقط ولا يقيد تكلفة الشراء بـ`ACCOUNT_SIZE` أو buying power.

- **سيناريو ملموس:** حساب `$10,000` ومخاطرة 1%، دخول `$10` ووقف `$9.99`؛ تنتج قرابة 10,000 سهم بتكلفة تقارب `$100,000`.
- **الجذر/المحسوم:** الدالة غير معروضة حاليًا، لذلك لا تمس الإنتاج الآن؛ إضافة قيد السيولة لا تناقض قرار المخاطرة الثابتة.
- **فجوة القفل:** [`test_bot.py:4030`](/Users/kid/Super_Stocks/test_bot.py:4030) يختبر مخاطرة الوقف فقط.

### P2-3 — manifest الخاص بـE2 يستطيع التصديق على مجموعة ملفات ناقصة

[`ignition_e2_manifest.py:47`](/Users/kid/Super_Stocks/ignition_e2_manifest.py:47)، [`ignition_e2_manifest.py:106`](/Users/kid/Super_Stocks/ignition_e2_manifest.py:106) — البناء يتخطى الملفات الغائبة، والتحقق يفحص فقط الأسماء التي سبق تسجيلها في manifest.

- **سيناريو ملموس:** يفشل إنشاء `deliveries.jsonl` قبل بناء manifest؛ يُحذف اسمه من `raw_files_sha256`، ثم ينجح `verify_manifest` لأنه لا يقارن مجموعة الأسماء بالقائمة القانونية.
- **الجذر/المحسوم:** قياس فقط، بلا `LOGIC_VERSION` ولا أثر على التنبيه؛ لكنه يضعف ادعاء fail-closed للقياس التأكيدي.

### P2-4 — فشل صفحة Polygon اللاحقة يعيد بيانات جزئية كأنها مكتملة

[`Super_stock.py:6498`](/Users/kid/Super_Stocks/Super_stock.py:6498)، [`Super_stock.py:6520`](/Users/kid/Super_Stocks/Super_stock.py:6520) — بعد نجاح صفحة أولى، أي 401/429 لاحق يرجع `out` بدل `None`.

- **سيناريو ملموس:** تُحمّل أول 50 ألف صفقة، ثم ترد الصفحة الثانية 429؛ `acc_components` يحسب نسبًا نهائية من نصف النافذة بلا علامة نقص.
- **الجذر/المحسوم:** لا أثر على الاختيار الإنتاجي لأن T-ACC متقاعد؛ لا يعيد فتح قرار تقاعده، لكنه يخالف عقد fail-safe لأداة القياس.

### P2-5 — دفع E2 عرضة لـnon-fast-forward ويمنع تشغيل البوابة الصارمة

[`.github/workflows/ignition.yml:22`](/Users/kid/Super_Stocks/.github/workflows/ignition.yml:22)، [`.github/workflows/ignition.yml:146`](/Users/kid/Super_Stocks/.github/workflows/ignition.yml:146)، [`.github/workflows/ignition.yml:159`](/Users/kid/Super_Stocks/.github/workflows/ignition.yml:159) — جلسة الرادار الطويلة تدفع مباشرة بلا fetch/rebase، وإذا تقدم `main` يفشل الدفع وتتخطى Actions خطوة analyzer التالية.

- **سيناريو ملموس:** المراقب اللحظي يدفع حالة إلى `main` أثناء جلسة ignition؛ assembler يبني commit على SHA قديم، و`git push` يرفض non-fast-forward، فلا تعمل `Analyzer strict gate` بينما artifact يُرفع بسبب `if: always()`.
- **الجذر/المحسوم:** E2/instrumentation فقط، بلا أثر على فرز أو إرسال الإنتاج.

### P2-6 — مفتاح إزالة التكرار لا يميز صفقتين مستقلتين بالسعر نفسه

[`Super_stock.py:7499`](/Users/kid/Super_Stocks/Super_stock.py:7499)، [`Super_stock.py:7514`](/Users/kid/Super_Stocks/Super_stock.py:7514) — الصفقة تُعرّف بـ`(symbol, entry_ref)` فقط، بلا `added/ref_bar/trade_id`.

- **سيناريو ملموس:** ترشيحان مستقلان للرمز نفسه بسعر `$14.05` في أسبوعين مختلفين ثم إغلاقهما؛ الثاني يُحذف من الإحصاء لأن الأول فاز بالمفتاح. والسجلات الحالية تثبت أن التصادم ممكن فعلًا: `LYEL` في [`weekly_watchlist.json:2717`](/Users/kid/Super_Stocks/weekly_watchlist.json:2717) و[`weekly_watchlist.json:2885`](/Users/kid/Super_Stocks/weekly_watchlist.json:2885) بالسعر نفسه وتاريخين مختلفين.
- **الجذر/المحسوم:** يمس إحصاء الأداء ودمج المصدرين، لا اختيار الأسهم أو قرارًا منهجيًا محسومًا.

## تحديات لقرار محسوم

لا توجد. الملاحظة الخاصة بالرؤوس الأسبوعية هي **مخالفة الكود للقرار المحسوم**، وليست اعتراضًا على القرار نفسه.

