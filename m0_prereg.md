# 🚨🔬 M0-PREREG — تسجيلٌ مسبق: **هل إشعارُ ما قبل الإغلاق يستحقّ خطرَه؟** ‏+ تغطيةُ المضارب

> **مدفوعٌ قبل أيّ رقم.** أمرا المالك 2026-08-17: «**قِس M0**» · و«**اذا تشوفه ايجابي
> وله دليل ف سوه ارفع تغطية المضارب**».

## ① السؤالُ محدَّدٌ ولا يتوسّع

`M0` يُطلق على **دقيقةٍ قيد التكوين**. والمخاطرةُ الجديدةُ **واحدةٌ فقط**: دقيقةٌ
تستوفي البوّابةَ **جزئيّةً** ثم **لا تستوفيها عند إغلاقها** ⇒ إشعارٌ ما كان `M1`
ليُرسله. **وكلُّ ما عدا ذلك مكسبُ أسبقيّةٍ خالص** — لأن الشمعةَ الجزئيّة **تُنقِص**
الحجمَ والسيولةَ فيستحيل أن تعبر بهما وهي جزئيّةٌ ثم تسقط بهما مكتملة؛ **المتغيّرُ
الوحيدُ القابلُ للانقلاب هو الرفعةُ وموضعُ الإغلاق**.

## ② المنهج — **ثوانٍ لا دقائق**، وإلّا فالقياسُ تخمين

القياسُ الأمينُ يستلزم معرفةَ **حالةِ الدقيقة في كلّ لحظةٍ داخلها** ⇒ تُجلَب
**شموعُ الثانية** (`/v2/aggs/.../1/second/`) ويُعاد بناءُ الجزئيّة بناءً حقيقيًّا:
الفتحُ أوّلُ ثانية · والإغلاقُ آخرُ ثانيةٍ حتى اللحظة · والقمّةُ/القاعُ تراكميًّا ·
والحجمُ مجموعًا. ⛔ **ولا يُقدَّر الجزءُ بكسرٍ من الدقيقة** — ذاك تخمينٌ لا قياس.
🔴 **وإن تعذّر جلبُ شموع الثانية يُوقَف بخروجٍ غيرِ صفريّ ولا يُنشَر رقم** (الصفرُ
عطبُ أداةٍ حتى يُنفى).

## ③ التصنيفُ الثلاثيّ — مثبَّتٌ قبل الأرقام

لكلّ دقيقةٍ في الجلسة تُصنَّف واحدةً من ثلاث:

| الصنف | التعريف | المعنى |
|---|---|---|
| **`A` مكسبٌ خالص** | الجزئيّةُ عبرت **و**المكتملةُ عبرت | `M0` يسبق `M1` ولا يُضيف خطرًا |
| **`B` ارتدادٌ** | الجزئيّةُ عبرت **والمكتملةُ لم تعبر** | 🔴 **الخطرُ الجديد كلُّه هنا** |
| **`C` متأخّرٌ بنيويًّا** | المكتملةُ عبرت **ولا جزئيّةَ عبرت قبلها** | `M0` لا يُضيف شيئًا |

**ونسبةُ الارتداد = `B ÷ (A + B)`** — وهي المقياسُ الحاكم.

## ④ الأذرعُ الأربعة — مثبَّتةٌ ولا تُزاد بعد الأرقام

| الذراع | الجزئيّةُ تُطلق إذا |
|---|---|
| **`E0`** | لا `M0` إطلاقًا (الأساسُ = `M1` وحده) |
| **`E1`** | 🥇 **المشحونُ الآن**: القفزةُ والأرضيةُ والاتجاهُ والرفعةُ ‏≥5% |
| **`E2`** | `E1` **‏+ سيولةٌ ‏≥‏2× الأرضية** (‏$60 ألفًا) — أشدُّ فيقلّ الارتداد |
| **`E3`** | `E1` **‏+ مضيُّ ‏≥20 ثانيةً من الدقيقة** (لا يُحكَم على ثوانٍ قليلة) |

## ⑤ المقاييس — مثبَّتةٌ قبل الأرقام

1. 🥇 **نسبةُ الارتداد** `B/(A+B)` لكلّ ذراع.
2. **الأسبقيّةُ المكتسبة**: وسيطُ **الثواني** الموفَّرة · و**فرقُ السعر** بين لحظة
   `M0` وإغلاق الدقيقة (‏= ما تدفعه زيادةً لو انتظرت).
3. **الإثمار** — **نفسُ تعريف `liq_move_prereg §④-ب` حرفيًّا** (بلوغُ ‏+3% خلال
   ‏15 دقيقة) لإشعارات `A` و`B` **كلٍّ على حدة**: فإن أثمرت `B` كإثمار `A` فالارتدادُ
   **اسمٌ لا خسارة**.
4. **العدد**: ‏`A` · `B` · `C` لكلّ ذراع.

## ⑥ قاعدةُ القرار — مكتوبةٌ قبل أيّ رقم

**`M0` يبقى مُشتغِلًا** (وهو المشحونُ الآن بأمر المالك) إن استوفى الثلاثة:
1. **نسبةُ الارتداد ‏≤40%**.
2. **وفرقُ السعر لصالحه ‏≥1.5%** وسيطًا (وإلّا فالأسبقيّةُ بلا قيمةٍ عمليّة).
3. **وإثمارُ `B` لا يقلّ عن إثمار `A` بأكثرَ من ‏3.6 نقطة** (تقلّبُ القياس المقيس).

⇒ **وإن سقط ①:** يُعتمَد **أشدُّ ذراعٍ تستوفي الثلاثة** (`E2` ثم `E3`)، **وإن لم
تستوفِ ذراعٌ فيُطفأ `M0`** ويُبلَّغ بالرقم — **ولا يُترَك مُشتغِلًا بحسن نيّة**.
⚖️ **والحكمُ يبقى للمالك في كلّ الأحوال**؛ وهذا العقدُ يقول ما **أوصي** به لا ما
يُنفَّذ بلا إذنٍ إن خالف أمرَه القائم.

## ⑦ تغطيةُ المضارب — ما يجعلها «إيجابيّةً ولها دليل»

`operator_flow` يقرأ آخر `limit` صفقة. **يُقاس بالأرقام لا بالتقدير:**
1. **عددُ صفقاتِ دقيقةِ الإشعار** فعلًا (يُعَدّ من الصفقات نفسِها).
2. **نسبةُ التغطية** لكلّ سقف: ‏250 · 5,000 · 20,000 · 50,000 (سقفُ صفحة Polygon).
3. **كلفةُ الزمن** لكلّ سقفٍ مقيسةً بالثانية.
4. **وأثرُه على الحكم**: هل يتغيّر `has_operator` بين السقوف؟ (وهو ما يكتم أو يمرّر.)

**والرفعُ يُعتمَد إن وإن فقط:** ‏250 يغطّي **أقلَّ من نصفِ** دقيقةِ الإشعار وسيطًا ·
**والسقفُ المقترح يغطّي ‏≥90%** · **وكلفتُه ‏≤3 ثوانٍ** للنداء الواحد · **ولا يقلب
حكمًا إلى الكتم** (الرفعُ يجب أن يُرخي لا يشدّ — وهو المتوقَّع بنيويًّا لأن الطبعاتِ
الكبيرةَ تُكتشَف أكثرُ بعيّنةٍ أوسع).
⇒ وإن فشل أيٌّ من هذي فيُبلَّغ الرقمُ **ولا يُرفَع**.

## ⑧ تنبّؤاتي — قابلةٌ للكسر

- **`P1`: نسبةُ الارتداد ‏≤25%** — لأن الجزئيّةَ يجب أن تعبر **أربعَ** بوّاباتٍ معًا،
  وعبورُها الأرضيةَ والقفزةَ وهي جزئيّةٌ دليلُ حركةٍ حقيقيّة.
- **`P2`: `A` تفوق `B` بمراتب** (‏`A/B ≥ 3`).
- **`P3`: فرقُ السعر لصالح `M0` سيكون ماديًّا (‏≥3%)** — لأن الدقيقةَ التي تُطلق
  تكون في قلب الاندفاعة (‏مِرساةُ `XOS` وحدها ‏+24.29%).
- **`P4`: `C` قليلٌ جدًّا** (‏≤10% من `A+C`) — لأن الجزئيّةَ أسهلُ عبورًا في الحجم.
- **`P5`: إثمارُ `B` أدنى من `A` لكن ليس صفرًا** — الارتدادُ يعني ضعفَ الدقيقة لا
  موتَ السهم.
- **`P6`: ‏250 صفقةً تغطّي دون ‏30%** من دقيقةِ الإشعار وسيطًا.
- **`P7`: رفعُ السقف لا يقلب حكمًا إلى الكتم** ولا مرّةً واحدة.

## ⑨ حدودُ صدقٍ تُقرأ مع النتيجة لا بعدها

1. **جلسةٌ واحدة** ⇒ مِجَسُّ خطرٍ لا تجربةَ عائد. **ولا صفقةَ تُقاس.**
2. **الإعادةُ تفترض أن المُشغِّلَ يرى الجزئيّةَ في اللحظة التي تعبر فيها** — والحيُّ
   يلفّ كلَّ ‏≈60 ثانية ⇒ **قد يفوته أوّلُ ثانيةِ عبورٍ فيصل متأخّرًا** ⇒ أرقامُ
   الأسبقيّة **سقفٌ**، ونسبةُ الارتدادِ **تُقاس على العبور الأوّل** وهي **أسوأُ حالةٍ**
   (فالانتظارُ يُقلّلها).
3. **«الإثمار» لمسُ ‏+3% لا ربحٌ محقَّق** ⇒ سقفٌ متفائل.
4. **وشموعُ الثانية قد تنقص** في الأسهم الرقيقة ⇒ الدقائقُ الناقصةُ **تُعَدّ وتُستبعَد**
   ولا تُخمَّن.
5. **والكونُ رقيقٌ** (أسهمُ فيصل) ⇒ لا يُعمَّم على كونٍ سائل.
6. **ولا يُقاس `M0` على الافتر والبريماركت منفصلًا** إن قلَّ المقام — ويُعلَن.
