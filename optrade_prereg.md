# 🕵️💥 `T-OPTRADE` — تسجيلٌ مسبقٌ **حاكم**: سياسةُ مضاربةٍ واحدةٌ بعد مِرساة «هنا الدخول»، مقيسةً بالتكلفة وبضبطِ زخم

> **مدفوعٌ ومدموجٌ قبل أيّ سطرِ أداةٍ وقبل أيّ رقم.** أمرُ المالك **«سجل السياسة»**
> (‏2026-09-16) بعد صدور `opcurve_result.md`، تنفيذًا لـ`OPUS_OPERATOR_PACKAGE.md §②`.
> **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · لا عتبةَ تتحرّك · لا
> أوامرَ تداولٍ ولا وسيطَ تداول · ولا وكلاء.**

---

## ⓪ إعلانٌ يسبق كلَّ شيء — ما رأيتُه قبل أن أكتب هذا العقد، وما يُقيّده

قرأتُ **كاملًا** قبل كتابة هذا الملفّ:

| المصدر | ما رأيتُه |
|---|---|
| `opcurve_result.md` ‏+ `opcurve_rows.tsv` (‏481 صفًّا × 22 عمودًا · التشغيلة `35083174367`) | **`h*` = 120 دقيقة** · **لا ذراعَ `P4`** · جداولُ المسار والسلال الثماني · القراءاتُ الوصفيّةُ §⑩ كلُّها · وأحجامُ السلال: **`H1` 289 · `H2` 192** · `pre` 161 · `reg` 228 · `after` 92 · `قوي` 109 · `متوسط` 70 · `ضعيف` 175 · `tier` مجهول **127** · `gap` <10% 176 · 10-30% 225 · **‏≥30% 80** · `J1` 52 · `R1` 40 · `T-C` 59 · **`pre ∧ gap≥30%` 50** · `exploded50` **‏52 من 481 (‏10.8%)** |
| `btcost_result.md` | **`C-MED` = ‏1.8018%** [1.7241 · 1.8868] · **`S-MED` = ‏−0.4587%** (سالبةٌ في ‏78.7% من سيقان الخسارة) · `C-P25` ‏0.8584% · **`C-P75` ‏3.5831%** · و**`C-MED` أرضيّةُ تكلفةٍ لا سقف** (‏`NBBO` قمّةُ الدفتر لا عمقُه) |
| `tierlink_result.md` (الممتدّة `35029466583`) | الأساس ‏10.8% · `قوي` 17.4% مقابل 6.1% · `gap≥30%` 21.9% مقابل 7.4% · `pre` 18.6% مقابل 6.9% · **و`J1` سقط على معيار التكرار** (‏35% ⟶ 11% بين النصفين) |

**وهذا يُعلَن ولا يُطوى**، و**ستّةُ قيودٍ** تنشأ عنه مباشرةً وتُكتب الآن:

1. **`h*` = 120 داخلَ العيّنة.** اشتُقّ من الرُّبيع الأعلى لـ`t_hit10` على **هذي الصفوف
   نفسِها**. ⇒ يحكم **لأنه ثُبّت في عقدٍ مدموجٍ (`opcurve_prereg.md §⑦`) قبل أيّ رقمٍ
   من `T-OPTRADE`**، **وتُطبَع معه حساسيّةٌ كاملة** على `{15, 30, 60, 120, 240, EOD}`
   في كلّ تقرير — **فإن كان ‏120 هو الأفضلَ بين الستّة فذلك يُقال صراحةً أنه اختيارٌ
   داخلَ العيّنة لا نتيجة.**
2. **`P2` (`pre ∧ gap≥30%`) تقاطعٌ بعديّ** اختير في `OPUS_OPERATOR_PACKAGE.md §⓪-ب`
   **بعد** رؤية أرقام إعادة القراءة ⇒ **وصفيٌّ من مولده** لا حاكم.
3. **`P3` (`قوي`)** وسمُ إنتاجٍ قائمٌ لا تقاطعٌ اخترعتُه، **لكنّ فصلَه رُئي** (‏17.4%
   مقابل 6.1%) ⇒ **وصفيٌّ كذلك**.
4. **أرضيّةُ `OT3` معروفةُ المصير سلفًا:** `P0`/`P1` تعبر (‏289 و192 فوق 150)،
   و**`P2` (‏50) و`P3` (‏109) تسقط** — وبالنصفين أشدّ. ⇒ **يُعلَن الآن أنهما لن
   تُحكَما**، ولا يُقال بعد التشغيل «اختُبرتا فلم تُحسَما».
5. **`J1` لا يدخل أيَّ ذراع** — سقط على معيار التكرار في `T-TIERLINK` الممتدّة.
6. 🔑 **وصفرُ رقمٍ من موضوع هذا العقد رأيتُه:** لا `R` واحدة · ولا عائدًا بعد تكلفة ·
   ولا توقّعًا ولا فاصلًا ولا نسبةَ إصابةٍ لسياسةٍ مركَّبة · ولا رقمَ ضبطِ زخم —
   **`opcurve_probe` امتنع عن حساب «‏+10% قبل الوقف» مركَّبًا عمدًا** (`opcurve_prereg.md
   §③`) ⇒ **تنبّؤاتُ §⑦ عمياءُ عن مادّتها**.

⚠️ **وقيدٌ سابعٌ على نفسي:** أرقامُ السلال أعلاه رأيتُها ⇒ **لا تُضاف سلّةٌ ولا ذراعٌ
بعد الأرقام**، والقائمةُ في §④ **مُغلَقة**.

---

## ① السؤال بصيغةٍ قابلةٍ للتكذيب

> **لو دخلتُ عند سعر كرت كلِّ مِرساة، ووقفي قاعُ المِرساة، وخرجتُ بـ`+10%` أو بالوقف أو
> بانتهاء نافذةِ ‏120 دقيقة — كم `R` أجني بعد التكلفة المقيسة؟ وهل يتفوّق ذلك على تطبيق
> السياسة **نفسِها** على دقيقةٍ أبكرَ للسهم نفسِه وفي الجلسة نفسِها (من اليوم نفسِه، أو
> من يوم التداول السابق إن تعذّر) لها الزخمُ نفسُه؟**

**التكذيب:** إن سقط `OT1` أو `OT2` ⇒ **السياسةُ لا تُوصى**، ويُنشَر الرقمُ كما خرج.
**ولا يُفتَح محورٌ مُغلَق** — قطعُ السياسة كلُّها محكومةٌ منفردةً (`T-TARGET10` عبرت ·
وقفُ القاع **اعتُمد بأمر المالك 2026-08-27/29** وإن كان `T-PIVOT-BOTTOM` دون البار ·
`T-RECLAIM-INTRADAY` تضرّ فلا إعادةَ دخول) — **والجديدُ هنا تركيبُها في مسارٍ واحدٍ
بالتكلفة**، وهو ما لم يُقَس قطّ (بحثُ الأرشيفين: `T-OPTRADE` يرد **إحالةً أماميّةً
فقط** — صفرُ عقدٍ وصفرُ نتيجةٍ وصفرُ ملفّ `optrade*` قبل هذا).

---

## ② المجتمع — **هو عينُ مجتمع `T-OPCURVE` لا مثيلٌ له**

- **الطبقاتُ الثلاث بلا انتقاء** (‏`opcurve_prereg.md §②` بحرفها): (A) كلُّ مِرساةِ
  سيولةٍ حيّة من تاريخ git لـ`op_entry_state.json` · (B) كروتُ `M5` في
  `tier_fwd_ledger.jsonl` · (C) وسمُ الزناد بعد `SHIP_ISO`.
- **الحدود:** `OPTRADE_SINCE` = `opcurve_probe.SINCE` ‏(2026-08-18) ·
  `OPTRADE_H2_FROM` = `opcurve_probe.H2_FROM` ‏(2026-09-01) · `OPTRADE_UNTIL` يُمرَّر
  إلى `OPCURVE_UNTIL` — **تُقرأ من الوحدة الحيّة لا تُكتب رقمًا** (‏`V-T2`).
- **البناءُ بإعادة استعمالٍ بالاسم لا بإعادة بناء:** `opcurve_probe.resolve_anchor` ·
  `trig_bucket` · `buckets_of` · `ny_hour` · `CARD_OFFSET_MS` · `HIT_PCT` ·
  `SHIP_MS` — ومعها `tierlink_probe.anchor_history`/`features` و
  `tier_fwd_report.fetch_day`/`load_ledger` **بالاسم** ⇒ **المِرساةُ والقاعُ
  والكرتُ والسلالُ هي عينُها**.
- 🔒 **`V-T1` — بصمةُ المجتمع:** على النافذة المجمَّدة `until = 2026-09-15` يجب أن
  يساوي **مُتعدَّدُ** `(date, sym)` المقيسُ هنا مُتعدَّدَ `opcurve_rows.tsv` **بت-بت**
  (‏481 صفًّا · `H1` 289 · `H2` 192). أيُّ فرقٍ ⇒ **خروج 5 بلا رقم**.
- **المِرساةُ المقروءة = آخرُ مِرساةٍ للرمز في يومه** (بعد أيّ إعادةِ رسوٍّ `A1`) — كما
  في `T-TIERLINK` و`T-OPCURVE`.

---

## ③ السياسةُ — نصُّ الحزمة، ثمّ تعريفُها التشغيليُّ بلا لبس

**نصُّ `OPUS_OPERATOR_PACKAGE.md §②` حرفيًّا:**

> «دخولٌ عند سعر كرت المِرساة **‏+ نصف `C-MED`** (‏0.90%) · وقفٌ = **قاعُ المِرساة** ·
> خروجٌ = **`T10` (‏+10%)** أو الوقف أو **نافذةُ ‏120 دقيقةً من سعر الكرت** … **ولا
> ذراعَ حملٍ ليليّ `P4`** … **ولا إعادةَ دخولٍ بعد الكنس».**

**وتعريفُها التشغيليُّ — كلُّ سطرٍ مثبَّتٌ الآن:**

| البند | التعريف |
|---|---|
| **لحظةُ القرار `t0`** | `anchor_ms + 4 دقائق` = بدايةُ شمعة الكرت (`opcurve_probe.CARD_OFFSET_MS` بالاسم) |
| **سعرُ الدخول `e5`** | سعرُ كرت `M5` — `tier_days_report.true_e5` **بالاسم** (‏`V-C7` في `T-OPCURVE`) |
| **«‏+ نصف `C-MED`»** | **ليست آليّةً جديدة**: هي `k(c) = (1−c/2)÷(1+c/2)` في `fcost_arms.ret_at` **بالاسم** — نصفُ الدورة على الدخول ونصفُها على الخروج. ⇒ **هُويّةُ التكلفة هي هُويّةُ `T-FCOST`/`T-BTCOST` بت-بت لا شبيهُها** |
| **الوقف** | `alow` = قاعُ شمعة المِرساة — `opcurve_probe.resolve_anchor` **بالاسم** (‏استردادُ ‏09-16 ⇒ صفرُ مِرساةٍ بلا قاع) |
| **زمنُ الوقف** | `sym_day_probe.exit_point` **بالاسم**: أوّلُ إغلاقِ **دقيقة** دون `alow` بعد `t0` · وسعرُ الخروج **إغلاقُ تلك الدقيقة** لا `alow` (قد يكون أعمقَ — وهذا مقصودٌ ومتحفّظ) |
| **الهدف `T10`** | أوّلُ **لمسةِ قمّةِ** دقيقةٍ عند `e5 × 1.10` بعد `t0` (‏`HIT_PCT` = 10 بالاسم) — عائدُه الخامّ **‏+10.0% بالضبط** |
| **النافذة** | `h*` = **‏120 دقيقةً جداريّة** من `t0`؛ عند انتهائها بلا هدفٍ ولا وقفٍ ⇒ الخروجُ بإغلاق **آخرِ شمعةٍ ضمن النافذة** |
| **‏🔴 فضُّ التعادل داخل الدقيقة** | إن وقع اللمسُ والكسرُ **في الدقيقة نفسِها** ⇒ **الوقفُ يفوز** (متحفّظ). وتُطبَع حصّةُ هذي الحالة، ويُطبَع معها البديلُ «الهدفُ يفوز» **وصفيًّا** ليُقاس أثرُها. (ترتيبُ اللمس داخل الدقيقة مجهولٌ — حدُّ `T-BTCOST §⑦-3`) |
| **‏🔴 شرطُ القابليّة للتداول** | لا دخولَ إن كان `e5 ≤ alow` (المخاطرةُ معدومةٌ أو سالبة ⇒ `r_fixed` بلا معنًى). **وهو شرطٌ يُعرَف عند `t0` بلا نظرٍ مستقبليّ**، **وواحدٌ لكلّ الأذرع**، **ويُعَدّ ويُطبَع بالنصفين** |
| **وحدةُ الحكم `R`** | `tranche_arms.r_fixed(ret_pct, e5, e5, alow)` **بالاسم** ⇒ `R = ret% ÷ 100 × e5 ÷ (e5 − alow)` |
| **العائدُ بعد التكلفة** | `fcost_arms.ret_at(r0, outcome, c, s)` **بالاسم** · الحاكم **`(c, s) = (C-MED, S-MED)`** · و`outcome = "loss"` **إن كان الخروجُ بالوقف** (مطابقةً لتعريف السيقان التي قِيس عليها `S-MED`) |
| **الأرقامُ لا تُكتَب بيدي** | `C-MED` و`S-MED` **يُستخرَجان من `btcost_result.md` نصًّا** لا يُطبَعان ثوابتَ في الأداة (‏`V-T3`) |
| **لا إعادةَ دخول** | مِرساةٌ واحدةٌ = صفقةٌ واحدة. ما بعد الخروج **لا يُقاس ولا يُعوَّض** (`T-RECLAIM-INTRADAY` تضرّ ‏−0.135R) |
| **لا `P4`** | لا حملَ ليليّ — `opcurve_result.md §⑦` (قاعدةُ `O`: الليلُ أدنى في النصفين) |

🔴 **وشبكةُ حساسيّةٍ تُطبَع دائمًا ولا تحكم:** `c ∈ {0, C-P25, C-MED, C-P75}` ×
`s ∈ {0, S-MED}`. **و`S-MED` سالبٌ ⇒ يُجمّل الخاسر** ⇒ **الخليّةُ `(C-MED, 0)` تُطبَع
ملاصقةً للحاكمة في كلّ تقرير**، وإن انقلبت الإشارةُ في أيّ خليّةٍ **وُسم الحكمُ «هشّ»
في سطره**.

---

## ④ الأذرع — قائمةٌ **مُغلَقة**، لا تُضاف ذراعٌ بعد الأرقام

| الذراع | التعريف | الصفة |
|---|---|---|
| **`P0`** 🥇 | السياسةُ كاملةً على **كلّ** مِرساةٍ قابلةٍ للتداول | **الحاكمةُ الوحيدة** |
| **`P1`** | السياسةُ نفسُها **بلا `T10`** — الخروجُ بالوقف أو بانتهاء النافذة | **مقترنةٌ — تُقاس ولا تحكم** (نفسُ الصفوف · نفسُ الدخول ⇒ فرقُ **قاعدةِ خروجٍ** لا فرقُ عيّنة · وموضوعُ التنبّؤ `OP5`) |
| **`P2`** | `P0` على `pre ∧ gap≥30%` وحدَها (‏n=50) | **وصفيّةٌ — بعديّة** (§⓪-2) |
| **`P3`** | `P0` على `قوي` وحدَها (‏n=109) | **وصفيّة** (§⓪-3) |
| ~~`P4`~~ | — | **لا وجودَ لها** (قاعدةُ `O`) |

⚠️ **والفرقُ نافذٌ لا زخرفة: `OT1`/`OT2`/`OT3` على `P0` وحدَها** (§⑥) · وما عداها
**يُقاس ويُنشَر بفاصله ولا يُقلَب توصيةً** مهما كان رقمُه · **ولا تُقرأ أرضيّةٌ
ساقطةٌ نجاحًا ولا فشلًا**.

---

## ⑤ الضبطان الحاكمان — ودرسان مُطبَّقان قبل وقوعهما

### ⑤-أ `C-MOM` — ضبطُ الزخم (‏**حاكم**)

**السببُ نصًّا:** في `T-PMGATE` فصلت بوّابةُ **زخمٍ صرفٍ** ‏+18.25 نقطة بينما مرجعُ
فيصل ‏+13.74 ⇒ **المرجعُ لم يُضِف بل نقَص**، ولولا الضبطُ لخرجنا برقمٍ موجبٍ يُصدَّق.
**وكلُّ مِرساةٍ هنا سهمٌ صاعدٌ بالتعريف** ⇒ أيُّ سياسةٍ عليه تبدو «تعمل».

**التعريف — حتميٌّ بلا بذرةٍ ولا عشوائيّة.** لكلّ مِرساةٍ `(sym, day, anchor_ms)` تُختار
**دقيقةٌ بديلة** ثمّ تُطبَّق عليها السياسةُ **نفسُها حرفيًّا**:

1. **بركةُ المرشّحين — مستويان بالأولويّة، وأوّلُ مستوًى غيرِ فارغٍ يفوز** (قاعدةٌ
   لا اختيار):
   - **`D0`** — دقائقُ **اليوم نفسِه** و**نوعِ الجلسة نفسِه** (`tod`)، **السابقةُ
     للمِرساة**، **وبصفرِ تداخل**: نافذةُ البديلة تنتهي عند `t0` أو قبله ⇒
     `anchor_ms − placebo ≥ h* + 4 = 124 دقيقة`.
   - **`D1`** — إن خلا `D0`: دقائقُ **يوم التداول السابق** لنوع الجلسة نفسِه
     (التداخلُ مستحيلٌ بنيويًّا) — بكلفةِ **جلبةِ يومٍ واحدةٍ إضافيّةٍ على الأكثر**
     لكلّ (رمز·يوم).
2. **المطابقة:** الدقيقةُ التي عائدُها الدقيقيُّ `(c − o) ÷ o` **أقربُ** إلى عائد شمعة
   المِرساة · **وفضُّ التعادل: الأقدمُ طابعًا** ⇒ حتميّةٌ تامّةٌ بلا بذرة.
3. **التطبيق:** `t0′ = placebo + 4د` · `e5′` بـ`true_e5` على البديلة · الوقفُ **قاعُ
   شمعة البديلة** · الهدفُ `e5′ × 1.10` · النافذةُ `h*` · التكلفةُ نفسُها · `r_fixed`
   نفسُها · وفضُّ التعادل داخل الدقيقة نفسُه · وشرطُ القابليّة للتداول نفسُه.
4. **بلا مرشّحٍ في المستويين** ⇒ **تُعَدّ وتُطبَع ولا تُخمَّن**، وتخرج من `OT2` وحدَها.
   **وحصّةُ `D0` مقابل `D1` تُطبَع دائمًا** — فإن غلب `D1` فذلك حدُّ صدقٍ يُقال لا يُخفى.

🔑 **ولماذا «دقيقةٌ أبكر» لا «سهمٌ آخرُ بالعائد نفسِه» كما في نصّ الحزمة؟** يُعلَن
التغييرُ صراحةً بسببَيه: **(أ)** هذا الضبطُ يُثبّت **السهمَ والجلسةَ** بالضبط (وفي `D0`
**اليومَ** أيضًا) لا تقريبًا ⇒ يُزيل التباسَ «سهمٌ آخرُ في قطاعٍ آخرَ بسيولةٍ أخرى» ·
**(ب)** كلفتُه جلبةُ يومٍ واحدةٍ على الأكثر، بينما المقطعيُّ يلزمه جلبُ **كونِ يومٍ
كاملٍ بالدقائق لكلّ يوم**. ⚠️ **وحدُّه يُقال:** **لا يضبط الاختيارَ المقطعيّ** (أيُّ
الأسهم نالت مِرساةً أصلًا) — يبقى مفتوحًا ومكتوبًا.

**و`C-MOM-15` (‏وصفيٌّ · حدٌّ من الجهة المعاكسة):** نفسُ المطابقة بفارقِ **‏15 دقيقةً
فقط** ومع **السماح بالتداخل**. **ومُعلَنٌ الآن أنه منحازٌ نحو الصفر بالبناء** (نافذتُه
تبتلع حركةَ المِرساة نفسَها) ⇒ **يُطبَع ولا يحكم**، وفائدتُه أنه **يحدّ الفرقَ من
أسفل**.

### ⑤-ب `C-ANY` — دقيقةٌ سابقةٌ بلا مطابقةِ عائد (‏**وصفيّ**)

نفسُ بركةِ `C-MOM` (‏`D0` ثمّ `D1`) لكنّ البديلةَ **تُنتقى برتبةٍ ثابتة** — الوسطى
زمنيًّا — **بلا أيّ مطابقةِ عائد** ⇒ حتميّةٌ بلا بذرة. يفصل «هل الأثرُ زخمٌ أم مجرّدُ
الوجود في هذا السهم في هذي الجلسة؟» — **وصفيٌّ بنصّ هذا السطر.**

### ⑤-ج `C-FILL` — **بلا موضوعٍ هنا · يُعلَن ولا يُطوى · ويحلُّ محلَّه `C-SUB`**

نصُّ الحزمة يطلب **ضبطًا مطابَقَ التعبئة** (أرضيّةُ درس `T-WAIT-23W`). **وهو بلا موضوعٍ
في هذا العقد، ويُقال لماذا بدل أن يُسقَط بصمت:**

1. **صفرُ ذراعٍ تُقلّل التعبئة:** الدخولُ عند الكرت **غيرُ مشروطٍ بسعر** — كلُّ مِرساةٍ
   قابلةٍ للتداول تُعبَّأ في **كلّ** ذراع. (‏`V-T5` يُثبته عدديًّا.)
2. **المقياسُ لكلّ صفقةٍ لا بالتصفير:** `R` متوسّطٌ على **المُعبَّأة** (`e_resolved`
   بالاسم) — و**لا صفرَ يُحتسَب لغير المُعبَّأ**، وهو الشرطُ الذي بغيابه ربحت أذرعُ
   `T-WAIT-*` آليًّا.
3. **لكنّ المرضَ نفسَه يعود في الأذرع الجزئيّة:** `P2`/`P3` **تتداول أقلّ**، وشريحةٌ من
   مجتمعٍ سالبٍ قد تبدو أفضلَ لمجرّد قلّة تداولها. ⇒ **`C-SUB`:** كلُّ ذراعٍ جزئيّةٍ
   تُقارَن **بمُكمِّلها تحت السياسة نفسِها** (‏`P0` على الصفوف **خارج** الشريحة) — لا
   بالمجتمع كلِّه (‏`P2 − P0|الشريحة` ≡ صفرٌ بالبناء، فالمقارنةُ بالمجتمع **خدعةٌ
   حسابيّة** لا نتيجة). **ووصفيٌّ كذلك** لأن أرضيّةَ `P2`/`P3` ساقطةٌ سلفًا.

---

## ⑥ المعايير والحكم — نصُّ الحزمة، والفروعُ الثلاثةُ كلٌّ في سطرها

**المعاييرُ بنصّ `OPUS_OPERATOR_PACKAGE.md §②` حرفيًّا:**

> «`OT1` توقّعُ `R` **بعد التكلفة** موجبٌ في **نصفَي** الفترة (‏08-16⟶09-01 ·
> 09-01⟶اليوم) وفاصلُ بوتستراب **عنقوديٌّ بالرمز** لا يلمس الصفر · `OT2` `P0 − C-MOM`
> موجبٌ في النصفين · `OT3` أرضيةُ **‏≥150 مِرساةً مُعبَّأةً في كلّ نصف** — وإلّا **«لا
> حكم» وحصادٌ أماميّ** (لا تُرفَع العيّنةُ بتوسيع الكون).»

**وتثبيتُها التشغيليّ:** المعاييرُ الثلاثةُ **على `P0` وحدَها** · الفاصلُ من
`exitmgmt_arms.boot_ci` **بالاسم** (عنقودُه الرمز · بذرةٌ ثابتة `99991` · ‏10,000
إعادة) · و`OT2` على **الفروق المقترنة** (كلُّ مِرساةٍ مطروحًا منها بديلتُها) لا على
متوسّطَين مستقلَّين. **والبدايةُ الفعليّةُ `2026-08-18`** (أوّلُ يومٍ في المجتمع) لا
`08-16` — يُقال ولا يُخفى.

🔒 **وترتيبُ العمليّات مثبَّتٌ لا يُجتهَد فيه: `ret_at` أوّلًا (التكلفةُ نسبةٌ من
السعر) ثمّ `r_fixed` (المقامُ `e5 − alow` **ثابتٌ** لا يتحرّك بالتكلفة) ثمّ المتوسّطُ
على الصفوف** — لا العكس. **والإحصاءُ الحاكم متوسّطُ `R` لكلّ صفقة**، و«متوسّطُ
العائد ٪» يُطبَع سياقًا لا حكمًا.

🔑 **وأرضيّةُ `OT2` الخاصّة: ‏≥150 زوجًا مقترنًا في كلّ نصف** (مرآةُ أرضيّة `OT3` ·
ومصدرُ نقصها بنيويٌّ: شرطُ صفرِ التداخل يُقصي المِرساةَ المبكّرةَ في جلستها).
**ودونها `OT2` «لا حكم»، والفرعُ 1 يمتنع** — لأن التوصيةَ بسياسةٍ بلا ضبطِ زخمٍ هي
خطأُ `T-PMGATE` بعينه · **ويُنشَر رقمُ `OT1` موسومًا** لا مطويًّا.

**والفروع:**

1. **الفرعُ 1 — تُوصى:** `OT3` ✅ **و**`OT1` ✅ **و**`OT2` ✅ ⇒ سقفُ النجاح في §⑨.
2. **الفرعُ 2 — فشلت:** `OT3` ✅ وسقط `OT1` أو `OT2` ⇒ **لا تُوصى**، ويُنشَر الرقم.
3. **الفرعُ 3 — لا حكم:** سقط `OT3` (أو أزواجُ `OT2` دون ‏150 في نصف) ⇒ **«لا حكم»**
   وحصادٌ أماميّ · **ولا تُرفَع العيّنةُ بتوسيع الكون ولا بتمديد النافذة.**

**رموزُ الخروج:** ‏0 صدر حكمٌ (الفرعان 1 و2) · 2 لا مفتاح · 3 تغطيةُ الجلب دون 80% ·
4 صفرُ مِرساة · **5 المجتمعُ يخالف `opcurve_rows.tsv`** · **9 «لا حكم» (الفرعُ 3)**.

---

## ⑦ التنبّؤاتُ القابلة للكسر — تُكتب الآن وتُنشَر مكذَّبةً أو مؤكَّدة

**نصُّ الحزمة حرفيًّا:**

> «`P0` **سالبٌ بعد التكلفة** (‏86% خاسرة) · `P2` **موجبٌ لكن ‏n<150 ⇒ لا حكم** ·
> **و`C-MOM` يلتهم نصفَ الفرق على الأقلّ**.»

**وأربعةٌ تُضاف الآن، عمياءُ عن مادّتها:**

- **`OP4`** حصّةُ الخروج بالوقف في `P0` **أكبرُ من** حصّة الخروج بالهدف (`T10`) في
  **النصفين** (‏`opcurve_result.md §⑩-3`: كسرُ القاع ‏43-73% بحسب الفجوة).
- **`OP5`** `P1 − P0` **سالب** (نزعُ `T10` يضرّ) — امتدادٌ لحكم `T-TARGET10` المعتمَد.
- **`OP6`** حالةُ التعادل داخل الدقيقة (لمسٌ وكسرٌ في الدقيقة نفسِها) **دون ‏5%** من
  الصفقات، وأثرُ قلبها على توقّع `P0` **دون ‏0.05R**.
- **`OP7`** حصّةُ البديلة من **اليوم نفسِه** (`D0`) **دون ‏60%** من الأزواج —
  الجلساتُ قصيرةٌ وشرطُ صفرِ التداخل قاسٍ ⇒ يغلب `D1`.

---

## ⑧ حدودُ صدقٍ مكتوبةٌ قبل النتيجة

1. **لمسٌ لا تنفيذ — والاتّجاهان يُقالان معًا:** الهدفُ لمسةُ قمّةٍ (متفائل)،
   والوقفُ إغلاقُ دقيقةٍ (متحفّظ). `C-MED` **أرضيّةُ تكلفةٍ لا سقف**، و«اللمسُ ليس
   تعبئة» غيرُ منمذَج (‏`T-BTCOST §⑦-1,2`) ⇒ **الرقمُ سقفُ أداءٍ لا أرضية.**
2. **`C-MED`/`S-MED` مستعارتان:** قِيستا على **مجتمع محرّك الباكتيست** (بوّابات فيصل
   منذ 2026-08-06) لا على مجتمع المِرساة ⇒ **نقلٌ مُعلَن لا قياس**، وهذا سببُ شبكة
   الحساسيّة في §③.
3. **‏≈4 أسابيعَ من سوقٍ واحد** (‏08-18 ⟶ 09-15) والنصفان يختلفان (`t_peak` ‏21 ⟶ 71
   دقيقة · `close_reg` ‏−4.3 ⟶ −1.7) ⇒ **لا يُعمَّم على نظامِ سوقٍ آخر.**
4. **المجتمعُ = ما صمد في آخر لقطةِ يومه** — المكتومُ ببوّابة المضارب ليس فيه.
5. **`tier` مجهولٌ لـ127 مِرساة** ⇒ `P3` تُقرأ على **‏109 معروفةٍ فقط** ومقامُها يُطبَع.
6. **`C-MOM` لا يضبط الاختيارَ المقطعيّ** (§⑤-أ) · و**`D1` يضبط السهمَ والجلسةَ
   لا اليوم** ⇒ حصّتُه تُطبَع · و`C-ANY`/`C-MOM-15` **وصفيّان**.
7. **بلا رسوم ولا طابورٍ ولا تعبئةٍ جزئيّة ولا فوائدِ اقتراض** · وانحيازُ البقاء قائم.
8. **‏64% من انفجارات السجلّ بلا مِرساةٍ أصلًا** (`OPUS_OPERATOR_PACKAGE.md §⓪-ب`) ⇒
   **سقفُ أيّ سياسةٍ تُبنى على الإشعار ‏≈36% من الفرص** مهما كان رقمُها هنا.
9. **`h*` = 120 داخلَ العيّنة** (§⓪-1) — والحساسيّةُ تُطبَع.
10. **الحصّادُ الأماميّ** (`T-J1PM` · `T-TC-YIELD`) لا يُمَسّ ولا يُقرأ منه رقمٌ هنا.

---

## ⑨ سقفُ النجاح — وما لا يُفعَل تحت أيّ نتيجة

**إن عبرت `P0` الفرعَ 1** ⇒ **اقتراحُ سطرِ تسليمٍ للمالك** («‏🎯 دخولٌ مضاربيّ: سعرٌ ·
وقفٌ · هدف») **بقرار المالك وحدَه** — يُعرَض ولا يُشحَن.

**وما لا يخرج من هذا العقد مهما كانت الأرقام:**
- **لا تنفيذَ أوامرَ تداولٍ آليّة · ولا ربطَ وسيطِ تداول.**
- **لا مسَّ جذرٍ** من الاثني عشر · ولا `LOGIC_VERSION` · ولا `force_renew`.
- **لا مؤشّرَ اختيارٍ جديدٍ في الفرز** (البابُ مُغلَقٌ بالدليل §0-ز/ط/ي).
- **لا فتحَ محورٍ مُغلَق** (`T-PMGATE` · `T-RSI-*` · `T-PRESESSION` …) إلّا بشروطه الثلاثة.
- **ولا رقمَ من هذا العقد يُشحَن عتبةً.**

---

## ⑩ الأقفالُ المطلوبة قبل الدفع (والطفراتُ التي تُسقطها)

- **`OTK0`** — **بنيويٌّ لا عضويّة** (درسُ `RKK0`): الفروعُ الثلاثةُ في §⑥ **ثلاثةُ
  أسطرٍ مرقّمة**، كلُّ فرعٍ يُسمّى في سطره، و«لا حكم» في **الثالث وحدَه** · وسطرُ
  رموز الخروج يحمل **‏5** و**‏9** بمعنَييهما · **وأرضيّةُ `OT2` تمنع الفرعَ 1**.
- **`OTK1`** — §⑨ يحمل سقفَ النجاح **بقرار المالك** والمحظوراتِ الخمسةَ بأسمائها
  (‏لا تنفيذَ أوامر · لا وسيط · لا جذر · لا مؤشّرَ اختيار · لا رقمَ يُشحَن).
- **`OTK2`** — **الضبطان من النصّ ومن الكود معًا:** §⑤ يُعلن `C-MOM` **حاكمًا**
  ببركةِ **`D0`/`D1`** و**صفرِ تداخل**، و`C-MOM-15` **منحازًا نحو الصفر بالبناء**،
  و`C-FILL` **بلا موضوع** ويُسمّي بديلَه `C-SUB` · **وسلوكيًّا من الكود الحيّ:**
  `exitmgmt_arms.boot_ci` **يستعمل خريطةَ العناقيد فعلًا** (خريطتان مختلفتان ⇒
  متوسّطان مختلفان) · و`fcost_arms.k_of(0) == 1.0` و`ret_at(r,·,0,0) == r` هُويّةً ·
  و`tranche_arms.r_fixed` تُعيد `None` عند `entry ≤ stop`.
- **`OTK3`** — **الحدودُ من الوحدة الحيّة لا من نصّي:** `opcurve_probe.SINCE` و
  `H2_FROM` مذكورتان في §② **وقيمتاهما تُقرآن من الوحدة** وتطابقان النصّ · و§⓪ يُعلن
  التلوّثَ **بأرقامه** (`h*` = 120 · لا `P4` · 481 · 289 · 192 · 1.8018 · −0.4587).
- **`OTK4`** — التنبّؤاتُ الثلاثةُ المنقولةُ حرفيًّا حاضرة، و`OP4`-`OP7` **أربعةٌ كلٌّ
  في سطره** · و`opcurve_rows.tsv` موجودٌ بـ**‏481 صفًّا و22 عمودًا** كما يقول §②.
- **وأقفالُ الأداة (`OTA*`) تُكتب مع الأداة**: صفرُ نداءٍ محظور (AST) · صفرُ إسنادٍ
  إلى `CONFIG` · الاستيرادُ بالاسم · بصمةُ المجتمع `V-T1` · الحتميّة · رموزُ الخروج ·
  ووصلةُ الـworkflow.

---

## ⑪ الأداةُ حين تُبنى — بعد دمج هذا العقد لا قبله

- `optrade_arms.py` ‏+ `.github/workflows/optrade.yml` (**يدويٌّ بلا كرون** ·
  `contents: read` · **بلا سرِّ تلغرام** فالإرسالُ مستحيلٌ بنيويًّا · سرُّ
  `POLYGON_API_KEY` وحدَه · مدخلاتٌ `since`/`h2_from`/`until` موصولةٌ بالبيئة).
- **تُعيد الاستعمالَ بالاسم:** `opcurve_probe` (المجتمعُ والمِرساةُ والسلال) ·
  `tier_days_report.true_e5` · `sym_day_probe.exit_point` · `tranche_arms.r_fixed` ·
  `fcost_arms.ret_at`/`k_of`/`e_resolved`/`fill_frac` · `exitmgmt_arms.boot_ci` ·
  `btcost_arms.pctile`. **صفرُ منطقِ حسمٍ مكرَّر.**
- **المُخرَج:** توقّعُ `R` لكلّ ذراعٍ × النصفين × شبكةِ الحساسيّة · `OT1`/`OT2`/`OT3`
  بنصّها · الفرعُ برقمه · التنبّؤاتُ الستُّ مؤكَّدةً/مكذَّبة · حصّةُ كلّ سببِ خروج ·
  والعدّاداتُ (غيرُ القابل للتداول · بلا بديلة · التعادلُ داخل الدقيقة) — **وكلُّ قصٍّ
  يُعلَن بعدّاده**. النتيجةُ في `optrade_result.md` **بجانب هذا العقد لا فوقه**.
- **🧪 وضعُ جدوى `OPTRADE_DRY=1`** يطبع **ثلاثةَ أعدادٍ فقط**: القابلَ للتداول بالنصفين ·
  تغطيةَ البديلة وانقسامَها `D0`/`D1` · وحصّةَ التعادل داخل الدقيقة — **صفرُ `R` وصفرُ عائدٍ وصفرُ فرقٍ وصفرُ
  فاصل**. ⚠️ **ودرسُ `T-PMGATE` يُكتب معه: «وضعُ الجدوى يُجيز ما يمرّ به وحدَه»** —
  فلا يُقرأ إذنًا للمسار كلِّه.

---

> **التاريخ:** 2026-09-16 · **الأمر:** «سجل السياسة» · **الحالة:** مدفوعٌ ومدموجٌ قبل
> أيّ سطرِ أداةٍ وقبل أيّ رقم. **والعقدُ بعد دمجه لا يُعدَّل** — أيُّ تغييرٍ **ملحقٌ
> مؤرَّخ** كما في `opcurve_prereg.md §⑪`.
