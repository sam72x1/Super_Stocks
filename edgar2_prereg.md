# 📰🏛️② `T-PRE-EDGAR-2` — تسجيلٌ مسبق: العقدُ الثاني للحافز الخبريّ

> **مدفوعٌ قبل أيّ سطرِ كودٍ لأداته وقبل أيّ رقمِ دراسة.** أمرُ المالك: **«سجّل EDGAR-2»**
> (‏2026-09-24) بعد أن انتهى العقدُ الأوّل «غيرَ قابلٍ للقياس».
> **بجانب `edgar_prereg.md` لا فوقه:** العقدُ الأوّل لا يُعدَّل، وحكمُه (‏`edgar_result.md §②`)
> باقٍ كما نُشر، وتنبّؤاتُه المكذَّبة (‏`EG-P1` · `EG-P2`) لا تُحذف.

---

## ⓪ لماذا عقدٌ ثانٍ — وما الذي رأيتُه قبل كتابته (إفصاحٌ كامل)

**الحكمُ السابق:** سقطت `F4` — ‏46 نداءً في 10.82ث ⇒ **‏4.25/ث** والحدُّ 5/ث — **بصفرِ حجب**
(‏46 من 46 ناجحة · صفرُ `403`/`429` · صفرُ عطلِ شبكة). والسببُ مُعلَنٌ قبل الرقم في رأس
`edgar_probe.py`: اشتقاقُ الحدّ في `edgar_prereg.md §②-ب-6` **حسب زمنَ الكبح وحدَه وأهمل
زمنَ الشبكة**.

🔴 **ما رأيتُه قبل كتابة هذا العقد — القائمةُ كاملة:**

| رُئي | القيمة |
|---|---|
| `V-E8` | ✅ — السرُّ مضبوطٌ (‏45 محرفًا) |
| `F1` | ‏46/60 = **76.7%** |
| `F5` | ‏14/60 = **23.3%** ومعه قائمةُ الغائبين (‏`KRSP.WS` · `LUNRW` · `BSII` · …) |
| `F4` | ‏4.25/ث · حجبٌ 0 · شبكةٌ 6.25ث (وسيطُ النداء 126ms) · كبحٌ 4.50ث · إسقاطُ الكادنس المشحون 3.54/ث |

**وما لم يُرَ إطلاقًا:** `F2` · `F3` · **أيُّ قيمةٍ لـ`acceptanceDateTime`** · أيُّ إيداع · أيُّ
صفٍّ من `CASE` أو `CTRL` · `EG1` · `EG2` · أيُّ إصابة ⇒ **صفرُ رقمِ دراسة.**

⚖️ **ولذلك يُعلَن صراحةً: استبدالُ `F4` كُتب بعد رؤية 4.25.** ودفاعُه الوحيد أن `F4` بوّابةُ
**جدوى** (هل يُجيب المصدرُ نداءاتِنا؟) لا بوّابةُ فرضيّة — ولا يستطيع أيُّ حدٍّ فيها أن يُحرّك
`EG1 − EG2`. **و`F1`/`F5` لم يعودا تنبّؤَين** (رُئيا) — يُطبَعان كما هما ولا يُراهَن عليهما.

---

## ① الحاكمُ — منقولٌ حرفيًّا ولا يُمَسّ

> «لكلّ منفجرِ ‏+100% في 2025 (من صفوف E1) مقابل شاهدٍ حتميّ من غير المنفجرين: هل يوجد إيداعُ
> SEC (‏`8-K`/`424B`/`S-1`) **قُبل بين 16:00 أمس و03:50** اليوم؟ المصدر
> `data.sec.gov/submissions` (‏`acceptanceDateTime`) · المعيار: الفرقُ **‏≥ 15 نقطةً بفاصلَي
> Wilson منفصلَين**. …»
> — `presession_ceiling_prereg.md §⑤` (مدفوعٌ `bd79d3f`) · ومنقولٌ في `edgar_prereg.md §①`
> (المقتطَعُ بعد «…» شرطُ البناء «بعد E1/E2 وبأمرٍ صريح» — وقد تحقّق: E1/E2 مُشغَّلتان والأمرُ «سجّل EDGAR-2»)

```
EG1 = نسبةُ CASE التي فيها إيداعٌ داخل النافذة
EG2 = نسبةُ CTRL التي فيها إيداعٌ داخل النافذة
النجاح ⟺ (EG1 − EG2) ≥ 15.0 نقطةً مئويّة  ∧  فاصلا Wilson 95% منفصلان تمامًا
```

🔒 **وكلُّ ما يُضاف في هذا العقد يُشدِّد ولا يُرخي** — أثرُه الوحيدُ الممكن **زيادةُ احتمال
«لا حكم» أو «غيرُ قابلةٍ للقياس»** لا زيادةُ احتمال النجاح.

---

## ② ما يبقى من العقد الأوّل حرفيًّا — بالإحالة لا بإعادة الصياغة

| البند | في `edgar_prereg.md` | الحال هنا |
|---|---|---|
| المجتمعُ · `CASE` · `CTRL` الحتميّ · القصُّ المُعلَن | §③ | **حرفيًّا** ‏+ تعريفٌ تشغيليٌّ أدقّ (§②-ب أدناه) |
| `FORMS` · النافذةُ · منعُ `filingDate` في القرار | §④ | **حرفيًّا** ‏+ حسمُ منطقة التوقيت (§④ أدناه) |
| الأرضيّاتُ `EG3`-`EG5` | §⑤ | **حرفيًّا** ‏+ `EG6` تُشدِّد |
| بوّاباتُ الصلاحية `V-E0`-`V-E8` | §⑥ | **حرفيًّا** ‏+ `V-E9`-`V-E14` تُشدِّد |
| حدودُ الصدق السبعة | §⑧ | **حرفيًّا** ‏+ خمسةٌ جديدة |
| سقفُ النجاح | §⑨ | **حرفيًّا** |

**`FORMS` بنصّها:** `8-K · 8-K/A · 424B1 · 424B2 · 424B3 · 424B4 · 424B5 · 424B7 · S-1 · S-1/A · S-3 · S-3/A`
— **مطابقةٌ تامّة** لحقل `form` (لا بادئة ولا `8-K12B` ولا `6-K`).

**والنافذةُ بنصّها:** `acceptanceDateTime` داخل **`[16:00 نيويورك من يوم التداول السابق ،
03:50 نيويورك من اليوم]`** — مغلقةُ الطرفين كما كُتبت · ويومُ التداول السابق بتقويم السوق
(نهايةُ الأسبوع ‏+ `market_calendar.HOLIDAYS` — عينُ `presession_radar.prev_bday`).

### ②-ب التعريفُ التشغيليّ لـ`CASE`/`CTRL` — تدقيقٌ لا تغيير

قُرئ من الكود قبل أيّ رقم (‏`presession_report.py`):

1. **الصفوف:** ملفُّ الـartifact `presession-rows-2025` من التشغيلة **`33724367680`** — هو هو مصدرُ
   الـ702 المنشورة (‏`presession_ceiling_result.md` · الجدول الوصفيّ ‏+100% · 2025) ⇒ **ميزانيةٌ
   ثابتة، ولا يُعاد مسحُ السوق**. **والصفوفُ المنشورة لا المصحَّحة بـ`T-EARLY`** (الـ702 منها).
2. **`CASE`** = صفوفٌ ليست شاهدًا (‏`wit` فارغ) · `sess == "PM"` · `day` يبدأ بـ`2025` ·
   `maxs` عدديّ و‏`≥ 100` — **عينُ `_c100`** في كتلة `T-PRE-CEIL`
   (‏`presession_report.py:1320` في `main` · `CEIL_HUNDRED = 100.0` السطر 478 · والنافذةُ 0 =
   `maxs` بـ`MAX_KEYS` السطر 57) ⇒ **`V-E1`: ‏702 ‏±2%.**
3. **`CTRL`** = من اليوم نفسِه والجلسة نفسِها · ليست شاهدًا · `maxs` عدديّ و‏`< 100` ⇒ مرتّبةٌ
   تصاعديًّا بـ`sha256(f"{day}:{sym}")` ⟶ **أدنى `N_d`** حيث `N_d` = عددُ `CASE` في ذلك اليوم
   (‏1:1 يومًا بيوم). يومٌ لا يكفيه غيرُ منفجريه ⇒ يُؤخذ كلُّهم **ويُطبَع العجز**.
4. 🔴 **`maxs` المعدومة** (ليست عددًا) ⇒ **خارج الطرفين** ويُطبَع عددُها — لا تُحسَب «غيرَ منفجرة».
5. **الترتيبُ مُثبَّت:** `CTRL` يُسحَب **قبل** قصّ CIK ⇒ القصُّ يُقاس على الطرفين ويبقى `EG5`
   ذا معنًى في الاثنين (لو سُحب بعده لصار قصُّ `CTRL` صفرًا بالبناء).
6. **مطابقةُ الرمز بالخريطة حرفيّةٌ** كما في المرحلة صفر (‏`s in cmap`) — لا تحويلَ `.`⟶`-`
   (الضمانات والوحدات ليست أسهمًا عاديّة، وإقصاؤها صحيح).

---

## ③ المرحلة صفر v2 — ما تغيّر وما بقي

**العيّنةُ نفسُها** (‏`sha256("edgar-probe:" + رمز)` أدنى 60 من رموز صفوف 2025) — لأن `F1`/`F5`
رُئيا عليها، وعيّنةٌ جديدة لا تُضيف إلّا ضجيجَ سحب. **وكلُّ بوّابةٍ تُوقف برمز خروجٍ غيرِ صفريّ:**

| # | البوّابة | الحدّ | عند السقوط |
|---|---|---|---|
| **F1** | تغطيةُ CIK | ‏≥ 70% (**حرفيًّا**) | **‏5** |
| **F2** | حضورُ `acceptanceDateTime` في `filings.recent` لكلّ CIK ناجح | ‏= 100% (**حرفيًّا**) | **‏5** |
| **F3** | العمقُ إلى 2025-01-02 عبر `recent` أو `files[]` | ‏≥ 90% (**حرفيًّا**) | **‏5** |
| **F4′** | **يستبدل `F4`:** كلُّ نداءات `submissions` متتاليةٌ بكبح **`time.sleep(0.15)`** بينها ⇒ **صفرُ `403`/`429`** ‏**و**الردودُ الناجحة ‏≥ 95% من النداءات · والمعدّلُ يُطبَع **بلا حدّ** | كما في الخانة | **‏5** |
| **F5** | الغائبون اليومَ عن الخريطة | **بلا حدّ** (**حرفيًّا**) | — |
| **TZ-P · TZ0-TZ3** | **جديدة** — حسمُ منطقة التوقيت (§④) | §④ | **‏5** |

**لماذا `F4′` هو الاشتقاقُ الصحيح:** الكادنسُ المشحون `0.15ث` قائمٌ في الإنتاج
(‏`Super_stock.py:5446` في `form4_insider_buys` · `:5764` في `enrich` · `:6345` في
`_offering_event`) **والدراسةُ نفسُها مُثبَّتةٌ عليه** (§⑤) ⇒ `F4′` يختبر **النمطَ الذي ستستعمله
الدراسة بالضبط**، بينما حدُّ 5/ث كان يطلب سرعةً **لا تحتاجها الدراسة ولا يبلغها الكمونُ المقيس**.
**وشرطُ «‏≥95% ناجحة» إضافةٌ تُشدِّد** (العقدُ الأوّل لم يشترطه). **ولا إعادةَ محاولةٍ في المرحلة
صفر** — كلُّ نداءٍ محاولةٌ واحدة كما في المِجَسّ الأوّل.

---

## ④ منطقةُ التوقيت — تُحسَم بالقياس لا بالافتراض (**إضافةٌ جديدة · تُشدِّد**)

### ④-أ لماذا — عيبٌ في العقد الأوّل يُعلَن

`edgar_prereg.md §④` كتب: «`acceptanceDateTime` بتوقيت شرق أمريكا **مع الإزاحة**» — **افتراضٌ لم
يُتحقَّق منه قطّ**: الحقلُ **غيرُ مقروءٍ في المستودع كلِّه** قبل المِجَسّ (‏§②-ب-3 هناك)، والمرحلةُ
صفر الأولى **لم تطبع قيمةً واحدةً منه**. **والمنقولُ (غيرُ مقيسٍ هنا)** أن قيمه على شكل
`2025-03-14T18:22:05.000Z` — بلاحقة **`Z`** لا بإزاحةٍ شرقيّة.

⇒ **تفسيران يفترقان 4-5 ساعات:**
- **`A`** — الأرقامُ **ساعةُ نيويورك الجداريّة** واللاحقةُ وسمٌ لا يُعتدّ به.
- **`B`** — اللاحقةُ صادقة (‏`Z` = UTC · أو إزاحةٌ صريحةٌ كما هي) ⟶ يُحوَّل إلى `America/New_York`
  بـ`zoneinfo` (فاشلٌ-آمن: تعذّرُه ⇒ خروج 3 لا تخمين).

🔴 **والخطأُ في أيٍّ منهما يُفسد الحكمَ في الاتّجاهين:** قراءةُ `A`-الحقيقيّة على أنها `B` تُدخل
إيداعاتِ **صباح اليوم** (‏06:00-07:50 نيويورك صيفًا و06:00-08:50 شتاءً — **بعد لحظة القرار
03:50**) إلى النافذة = **نظرٌ مستقبليّ صريح**، وتُخرج جُلَّ إيداعات المساء الحقيقيّة · والعكسُ
يُدخل إيداعاتِ **ما قبل الإغلاق** (من الظهيرة حتى 16:00). ⇒ **المُحلِّلُ الساذج
(‏`datetime.fromisoformat` يقرأ `Z` توقيتَ UTC — مُجرَّبٌ محلّيًّا) كان سيبني الحكمَ على `B`
بصمت** — ولهذا **لا يُفترَض أيٌّ منهما**.

### ④-ب الحَكَمُ المستقلّ — قاعدةُ 17:30 (منصوصةٌ في العقد الأوّل نفسِه)

`edgar_prereg.md §④` يقول: «SEC تُسنِد `filingDate` إلى يوم العمل التالي لكلّ ما يُقبَل بعد 17:30
نيويورك» (‏Regulation S-T ‏`17 CFR 232.13(a)(1)` — منقول). ⇒ **تحت التفسير الصحيح** وعلى `FORMS`
(لا استثناءَ ساعاتٍ ممتدّةٍ لها — استثناءاتُ 22:00 خاصّةٌ بـForms 3/4/5 وتسجيلات 462(b) وليست في
`FORMS`؛ **افتراضٌ مُعلَن**):

```
t_NY ≤ 17:30:00  ⇒  filingDate == تاريخ(t_NY)
t_NY > 17:30:00  ⇒  filingDate  > تاريخ(t_NY)
```

و**ساعاتُ EDGAR** 06:00-22:00 نيويورك في أيّام العمل (‏منقول: EDGAR Filer Manual) ⇒ فحصٌ ثانٍ.

🔒 **`filingDate` يُقرأ هنا وحدَه — للتحقّق لا للقرار:** دالّةٌ واحدةٌ اسمُها **`tz_verdict`**
هي الموضعُ الوحيد الذي يَرِد فيه الاسم (‏`V-E12` بالـAST) — **وقرارُ النافذة لا يراه إطلاقًا**
(‏`V-E3` حرفيًّا).

### ④-ج البوّابات

**عيّنةُ التوقيت:** إيداعاتُ `FORMS` من `recent` لكلّ CIK ناجحٍ في العيّنة، التي يقع
`filingDate`ها في **[2024-12-01 ، 2026-01-31]** (مدى الدراسة بهامش).

| # | البوّابة | الحدّ |
|---|---|---|
| **TZ-P** | كلُّ `acceptanceDateTime` في العيّنة يطابق النمطَ المثبَّت `YYYY-MM-DDTHH:MM:SS[.ffffff][Z أو ±HH:MM]` | **صفرُ استثناء** |
| **TZ0** | حجمُ العيّنة `n` | ‏≥ 100 |
| **TZ1** | اتّساقُ الفائز بقاعدة 17:30 على العيّنة كلِّها | ‏≥ 98% |
| **TZ2** | `D` = الإيداعاتُ التي يختلف فيها حكمُ القاعدة بين `A` و`B` ⇒ ‏`|D| ≥ 30` **و**الفائزُ صائبٌ في ‏≥ 95% منها — **أو** `A ≡ B` في كلّ الإيداعات (صيغةٌ تحمل إزاحتَها ⇒ لا التباس) | كما في الخانة |
| **TZ3** | حصّةُ أوقات الفائز **خارج 06:00-22:00 نيويورك أو في سبتٍ/أحد** | ‏≤ 1% |

**الفائز** = الأكثرُ اتّساقًا على `D`. **وسقوطُ أيٍّ منها ⇒ خروج 5 «غيرُ قابلةٍ للقياس — منطقةُ
التوقيت لم تُحسَم»** ولا يُختار تفسيرٌ بالرأي.
**والحدودُ مُعلَّلةٌ قبل أيّ قيمة:** ‏98% لأن القاعدةَ على **بدء الإرسال** لا القبول (بضعُ ثوانٍ
حول 17:30) ولتعديلاتِ تاريخٍ نادرة · ‏95% على `D` لأنه المجتمعُ المُميِّز وحدَه · ‏1% لإرسالٍ بدأ
قبل 22:00 وقُبل بعدها.

---

## ⑤ الدراسة — ما يُضاف (تُشدِّد ولا تُرخي)

1. **`V-E0` منفَّذةٌ في التشغيلة نفسِها:** أداةُ الدراسة **تُعيد المرحلة صفر أوّلًا** على العيّنة
   نفسِها (‏`F1` · `F2` · `F3` · `F4′` · `TZ`) وتخرج 5 إن سقطت — **لا اعتمادَ على تشغيلةٍ سابقة**.
2. **الجلب:** كلُّ CIK فريدٍ مرّةً واحدة · متتاليًا · **`time.sleep(0.15)`** بين النداءات ·
   عطلُ شبكةٍ أو `5xx` ⇒ حتى 3 محاولات (انتظار 2ث ثم 4ث) · **`403`/`429` ⇒ توقّفٌ 60ث ومحاولةٌ
   واحدة؛ فإن تكرّر ⇒ خروج 5 بلا حكمٍ جزئيّ** (‏`V-E10`). وتُعاد التشغيلةُ **مرّةً واحدةً بعد
   ‏≥24 ساعة**؛ فإن حُجبت ثانيةً ⇒ «غيرُ قابلةٍ للقياس» نهائيًّا. **وأيُّ فشلٍ آخر بعد المحاولات
   (‏`404` · جسمٌ غيرُ صالح) ⇒ ذلك الـCIK «مجهول»** ويدخل عدّادَ البند 4.
3. **صفحاتُ `files[]`:** تُجلب **فقط** لـCIK لا يبلغ `recent`ه أبكرَ بدايةِ نافذةٍ تلزمه، و**فقط**
   الصفحاتُ التي يتقاطع مداها (‏`filingFrom`/`filingTo`) مع ‏[أبكرُ ما يلزم ‏−7 أيّام ، أبعدُه ‏+7] —
   **اختيارُ صفحاتٍ للتغطية لا قرار**.
4. **التغطيةُ صفًّا صفًّا:** الإصابةُ تُحسَب **فقط** إن كانت بياناتُ الـCIK تغطّي نافذةَ الصفّ،
   أي واحدٌ من ثلاثة: **(أ)** `files[]` فارغة ⇒ `recent` هو التاريخُ كلُّه · **(ب)** أقدمُ قبولٍ
   في `recent` ‏≤ بدايةِ النافذة · **(ج)** كلُّ صفحةٍ يتقاطع مداها مع النافذة ‏±7 أيّام جُلبت
   بنجاح — **وإلّا «مجهول»** يُستبعَد ويُعَدّ: **`V-E11`: المجهولُ ‏≤ 2% في كلّ طرف** وإلّا
   خروج 5. (‏±7 لأن `filingDate` يلي القبولَ بيوم عملٍ واحدٍ على الأكثر ‏= 4 أيّامٍ تقويميّة
   مع عطلةٍ طويلة.) **افتراضٌ مُعلَن (كالعقد الأوّل في `recent_depth`):** صفحاتُ `files[]` مع
   `recent` **متلاصقةٌ** تغطّي التاريخَ بلا فجوة ⇒ نافذةٌ لا تتقاطع مع صفحةٍ ولا مع `recent` =
   «لا إيداع» لا «مجهول». **وصفحةٌ جُلبت بلا `acceptanceDateTime` لا تُغطّي شيئًا.**
5. **منطقةُ التوقيت على مجتمع الدراسة:** **`V-E9`** يُعيد `TZ-P`/`TZ0`-`TZ3` على إيداعات الدراسة
   كلِّها ⇒ **الفائزُ نفسُه** والبوّاباتُ عابرة، وإلّا خروج 5. **وقبولٌ غيرُ قابلٍ للتحليل خارج مدى
   العيّنة يُعَدّ ويُتجاهَل** (‏`TZ-P` داخل المدى يضمن ألّا يقع شيءٌ منه في نافذة).
6. **`EG6` (جديدةٌ · تُشدِّد):** إعادةُ معاينةٍ **عنقوديّةٌ بالرمز** — `B = 2000` ·
   `numpy.random.default_rng(20260924)` · تُسحَب رموزُ كلّ طرفٍ **داخله وحدَه** بالإرجاع (بعددِ
   رموزه الفريدة) وتدخل صفوفُ كلِّ رمزٍ مسحوبٍ بتكراره ⇒ **`numpy.percentile(…, 2.5)` لـ(‏`EG1 − EG2`)
   ‏> 0 تمامًا** وإلّا **«لا حكم»**. **لماذا:** الصفوفُ متعنقدةٌ بالرمز (رمزٌ ينفجر أيّامًا عدّة)
   وWilson يفترض الاستقلال ⇒ فاصلُه أضيقُ من الحقيقة.
6-ب. **تدقيقُ الحاكم دون مسّه:** Wilson ‏95% = `presession_report.wilson` (‏`z = 1.96` — بُنيت لفواصل
   تقارير `T-PRESESSION` نفسِها) · **«منفصلان تمامًا»** = الحدُّ الأدنى لـ`CASE` **أكبرُ تمامًا** من
   الحدّ الأعلى لـ`CTRL` · والفرقُ بلا تقريب.
7. **بوّاباتٌ إضافيّة (كلُّها تُشدِّد):**

| # | البوّابة | الخروج |
|---|---|---|
| **V-E9** | منطقةُ التوقيت تُعاد على مجتمع الدراسة ⇒ الفائزُ نفسُه و`TZ-P`/`TZ0`-`TZ3` عابرة | **‏5** |
| **V-E10** | صفرُ حجبٍ بعد التوقّف المُسجَّل (البند 2) | **‏5** |
| **V-E11** | المجهولُ التغطية ‏≤ 2% في كلّ طرف | **‏5** |
| **V-E12** | **بالـAST:** `filingDate` لا يَرِد إلّا داخل `tz_verdict` — وصفرُ ورودٍ في دالّة القرار | **‏3** |
| **V-E13** | كلُّ إصابةٍ محسوبةٍ تقع وقتُها (بالفائز) **داخل** نافذة صفّها — تأكيدٌ وقتَ التشغيل | **‏3** |
| **V-E14** | لا تكرارَ لـ(يوم، رمز) داخل أيّ طرف | **‏3** |

8. **وصفيٌّ لا يحكم (يُطبَع):** الفرقُ لكلّ أسرةٍ وحدَها — **`F-8K`** = ‏`8-K`·`8-K/A` ·
   **`F-424`** = ‏`424B1`·`424B2`·`424B3`·`424B4`·`424B5`·`424B7` · **`F-REG`** = ‏`S-1`·`S-1/A`·`S-3`·`S-3/A`
   (‏`ED2-P7` صادقٌ ⟺ فرقُ `F-424` **أكبرُ تمامًا** من فرقِ `F-8K`؛ و`ED2-P8` ⟺ نسبةُ قصّ `CASE` أكبرُ
   تمامًا) · قصُّ CIK لكلّ طرف **وفرقُهما** · عددُ الصفوف التي يومُ
   تداولها السابق إغلاقٌ مبكّر (‏2025-07-03 · 11-28 · 12-24) · صفوفُ **2025-04-21** (نافذتُها تشمل
   الجمعةَ العظيمة: السوقُ مغلقٌ وEDGAR يعمل) · عددُ `maxs` المعدومة · بصمةُ اللوحة `sha256`.
9. 🔴 **لا يُملأ `sec_offering_index.json`** — `edgar_prereg.md §②-ب-7` وعد به «عرَضًا» وهو يناقض
   `V-E6` (صفرُ كتابةِ حالة) في العقد نفسِه ⇒ **`V-E6` أولى**، والفهرسُ اقتراحٌ مستقلٌّ بأمرٍ مستقلّ.
   **يُعلَن هذا التعارضُ في العقد الأوّل ولا يُخفى.**

### ⑤-ب رموزُ الخروج والسطرُ الأخير

`0` حكمٌ صادر (نجاح أو فشل) · `6` «لا حكم» (‏`EG3`-`EG6`) · `5` غيرُ قابلةٍ للقياس (المرحلة صفر ·
التوقيت · الحجب · التغطية) · `4` أنبوبةٌ صامتة (‏`V-E5`) · `3` عطبُ أداة · `2` مُدخَلاتٌ/إعدادٌ ناقص.
**والسطرُ الأخير بصيغةٍ ثابتة** (‏`case`/`ctrl` = الصفوفُ المحلَّلة بعد القصّ والمجهول):
`JUDGE verdict=<نجاح|فشل|لا حكم> EG1=<%> EG2=<%> diff=<نقاط> case=<n> ctrl=<n> tz=<A|B> exit=<n>`

**وترتيبُ البوّابات مُثبَّت — وأوّلُ سقوطٍ يُنهي التشغيلةَ بخروجه:**
`V-E8` ⟶ `V-E0` (المرحلةُ صفر كاملة) ⟶ اللوحة (‏`V-E1` · `V-E2` · `V-E14` · `V-E4`) ⟶ القصّ
(‏`EG3`-`EG5`) ⟶ الجلب (‏`V-E10`) ⟶ `V-E9` ⟶ `V-E11` ⟶ الإصابات (‏`V-E13`) ⟶ `V-E5` ⟶ `EG6` ⟶ الحاكم.
**والمُدخَلُ مثبَّتٌ في الـworkflow لا مُمرَّر:** صفوفُ التشغيلة `33724367680` وحدَها (‏`V-E1` يُسقط
غيرَها بالبناء).

---

## ⑥ بطاقةُ التنبّؤ (قابلةٌ للكسر · تُقيَّم حرفيًّا · تُنشَر مهما كانت)

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| **ED2-P1** | `F4′` تعبر — صفرُ حجبٍ بالكادنس المشحون | أيُّ `403`/`429` أو نجاحٌ دون 95% |
| **ED2-P2** | `F2` تعبر (‏100%) | غيابُ الحقل في أيّ CIK ناجح |
| **ED2-P3** | `F3` تعبر (‏≥90%) | دون 90% |
| **ED2-P4** | **الفائز `A`:** الأرقامُ ساعةُ نيويورك **رغم لاحقة `Z`** | فوزُ `B` أو «لم تُحسَم» |
| **ED2-P5** | 🔴 **= `EG-P3` حرفيًّا:** الحاكمةُ تسقط — الفرقُ في **‏[‏+3 · +12] نقطة** | فرقٌ ‏≥15 أو ‏≤0 |
| **ED2-P6** | **= `EG-P4` حرفيًّا:** `EG2` ‏≥ 8% | `EG2 < 8%` |
| **ED2-P7** | **= `EG-P5` حرفيًّا:** `424B*` وحدَها أقوى فصلًا من `8-K` وحدَها | العكسُ أو تساوٍ |
| **ED2-P8** | **جديد:** قصُّ CIK في `CASE` **أكبرُ** منه في `CTRL` (المنفجرون يُشطَبون ويُبدَّلون أكثر) | العكسُ أو تساوٍ |

🔴 **`ED2-P5`/`P6`/`P7` منقولةٌ بلا تعديل** — كُتبت قبل أيّ رقمٍ في العقد الأوّل ولم يُرَ بعدها
رقمُ دراسةٍ يُبرّر تغييرَها. **والتنبّؤُ الحاكمُ ما زال يراهن على السقوط.**

---

## ⑦ حدودُ الصدق — السبعةُ حرفيًّا (‏`edgar_prereg.md §⑧`) وخمسةٌ جديدة

8. **‏23.3% من عيّنة المرحلة صفر غائبٌ عن الخريطة** ⇒ القصُّ المتوقَّع قرابةُ الرُّبع، **وفرقُه بين
   الطرفين يُطبَع** — قصٌّ متفاوتٌ قد يُحيّز لو ارتبط الغيابُ بالإيداع (لا يُقاس بمصدرٍ مجانيّ).
9. **الصفوفُ متعنقدةٌ بالرمز** ⇒ Wilson أضيق ⇒ **`EG6`** أُضيف لهذا بعينه.
10. **النافذةُ الحرفيّة تُبقي 16:00 في أيّام الإغلاق المبكّر الثلاثة** (الإغلاقُ الحقيقيّ 13:00) ⇒
    إيداعُ 13:00-16:00 فيها خارجُ النافذة بالتعريف — **تحفّظٌ لا تسريب**، ويُعَدّ.
11. **حَكَمُ التوقيت يفترض أن قاعدةَ 17:30 تسري على `FORMS`** — مُعلَنٌ لا مقيس، و`TZ1`/`TZ2`
    يُسقطان التجربةَ لو كان خاطئًا (اتّساقٌ دون الحدّ) لا يُمرّرانه.
12. **تعديلُ `F4` جاء بعد رؤية 4.25** — مُعلَنٌ في §⓪ وفي كلّ نتيجة.

---

## ⑧ سقفُ النجاح — منقولٌ حرفيًّا (‏`edgar_prereg.md §⑨`)

**أقصى ما يُنتجه هذا العقد مهما كانت الأرقام: اقتراحٌ للمالك + سطرُ عرضٍ تشخيصيّ.**

⛔ **ولا يُفعَل مهما كانت النتيجة:** لا مفتاحَ ترتيبٍ جديد · لا بوّابةَ إطلاقٍ ولا كتم · لا مسَّ
بـ`FLOOR_BY_SLOT` ولا `PREFILTER_CAP` ولا `PREFILTER_KEY` ولا `TOPK` ولا `MIN_DAY_USD` ·
لا مسَّ بـ`M1`-`M14` ولا بالدخول/الوقف/الأهداف · **لا `LOGIC_VERSION`** ·
**ولا يُضاف `6-K` ولا نموذجٌ ولا نافذةٌ ثانية بعد رؤية الجدول.**

---

## ⑨ ترتيبُ التنفيذ

1. **هذا العقدُ يُدمَج أوّلًا** — قبل أيّ سطرٍ من الأداتين.
2. **المرحلة صفر v2:** `edgar2_probe.py` ‏+ `.github/workflows/edgar2_probe.yml` (يدويّ · قراءةٌ فقط ·
   `SEC_CONTACT` مُصرَّحٌ به) ⟶ النتيجةُ في **`edgar2_result.md`** **بلا رقمِ دراسة**.
3. **إن عبرت:** `edgar2_study.py` ‏+ `.github/workflows/edgar2_study.yml` ‏+ الأقفال ‏+ جولةُ طفرات ⟶
   السويّة (خروج 0 · صفر فشل) ⟶ دمج ⟶ تشغيل على `main`.
4. **الحكمُ في `edgar2_result.md`** بجانب هذا العقد لا فوقه — ومعه تقييمُ `ED2-P1`-`P8` حرفيًّا.
5. **لا تغييرَ إنتاجيّ بلا «نفّذ» صريح** (§⑧).

---

## ⑩ ملحقٌ مؤرَّخ — `EG6` حارسٌ للنجاح وحدَه (‏2026-09-24 · بعد المرحلة صفر v2 · **قبل أيّ رقمِ دراسة**)

**العيب — في هذا العقد نفسِه، أمسكه تصميمُ أداة الدراسة لا رقمٌ:** §⑤-ب ثبّت الترتيبَ
«… ⟶ `V-E5` ⟶ `EG6` ⟶ الحاكم» مع «أوّلُ سقوطٍ يُنهي». وفرقٌ قريبٌ من الصفر يُسقط `EG6` بالبناء
(المئينُ 2.5 لفرقٍ صفريّ سالبٌ) ⇒ يُنشَر **«لا حكم» بدل «فشل»** ⇒ **التجربةُ عاجزةٌ عن السقوط
النظيف**، وهو نقيضُ «التنبّؤُ الخاطئ يُنشَر ولا يُحذف».

**التصحيح:** `EG6` يحرس **النجاحَ وحدَه** — الترتيب: … ⟶ `V-E5` ⟶ **الحاكم** ⟶ `EG6`:

| الحاكم (§①) | `EG6` | الحكم | الخروج |
|---|---|---|---|
| ساقط | (يُطبَع وصفيًّا) | **فشل** | ‏0 |
| عابر | ساقط | **لا حكم** | ‏6 |
| عابر | عابر | **نجاح** | ‏0 |

**لماذا ليس إرخاءً:** احتمالُ «نجاح» **لم يتغيّر حرفًا** (يلزمه الحاكمُ و`EG6` معًا في الصيغتين) ·
والمنقولُ الوحيد خانةُ (حاكمٌ ساقط ∧ `EG6` ساقط) من «لا حكم» إلى «فشل» ⇒ **صارت الأداةُ قادرةً
على نشر السقوط**. وكلُّ ما سواه في §⑤-ب باقٍ بحرفه (‏`EG3`-`EG5` قبل الجلب · ثم `V-E10` · `V-E9` ·
`V-E11` · `V-E13` · `V-E5`).

**ما رُئي قبل هذا الملحق:** أرقامُ المرحلة صفر v2 وحدَها (التشغيلة `36038678449` على
`main@11bc4edd`) — جدوى وتوقيت: `F1` ‏76.7% · `F4′` ‏46/46 بلا حجب · `F2`/`F3` ‏100% · وحسمُ
التوقيت **`B`** (‏580 إيداعًا · `D` = 350 · `B` صائبٌ في 350 و`A` في 0). **صفرُ رقمِ دراسة:** لا
`CASE` ولا `CTRL` ولا `EG1`/`EG2` ولا إصابة.

**وتثبيتٌ لما حسمته المرحلة صفر بنصّ §④ (ليس تغييرًا):** الفائزُ **`B`** ⇒ اللاحقةُ `Z` صادقة ·
النوافذُ تُقرأ بـ`to_ny(·, "B")` · و`V-E9` يُعيد الحسمَ على مجتمع الدراسة ويشترط `B` نفسَه.
