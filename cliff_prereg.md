# ⛰️ T-CLIFF — التسجيل المسبق (يُكتب ويُدفع **قبل** أي رقم)

> **الحالة عند الكتابة: صفر رقم من هذي التجربة.** المعايير والأذرع مثبَّتة قبل النظر،
> فما خالفها بعد التشغيل يُكتب مخالفًا ولا يُعاد ضبط المعيار عليه.
> **الدافع:** حكم `hunter_six_result.md` (2026-07-31) — **الحاجب الأول للستة داخل
> الصيّاد هو مُرشّح الكليف: 124 يومًا عبر ثلاثة أسهم** (EHGO 51 = 100% من نافذته ·
> SPRC 46 · DSY 27). **التصنيف:** بحث/قياس — صفر مسّ إنتاج · صفر تغيير عتبة · صفر تنبيه.

---

## ① السؤال — ولماذا هذا البند بالذات دون غيره

الحواجز التي أوقفت الستة **ثلاثة أصناف** (بحكم T-HUNTER-SIX §④):

| الصنف | المثال | هل يجوز اختباره؟ |
|---|---|---|
| **هندسيّ بحت** | **⓿ مُرشّح الكليف** · سقف السعر $10 | 🟢 **نعم — وهذا موضوع التجربة** |
| فيصليّ منصوص | ④ «لم يصعد 20%» · أرضية $1 · ③ «حافظ 3ج» | 🔴 لا يُمَسّ بلا سندٍ جديد من فيصل |
| بنيويّ استنتاجيّ | ① نافذة 120 · ② نطاق الـ÷2 | 🟡 يحتاج تسجيلًا مستقلًّا |

**فالسؤال المحصور:**
> مُرشّح الكليف بوّابةُ **تكلفة شبكة** (يقصّ الكونَ قبل جلب التقسيمات) — وليس شرطًا من
> شروط فيصل الستة. **فكم يكلّفنا من الالتقاط؟ وكم يوفّر من الضجيج؟**

⚖️ **وسقف النجاح مُعلَن سلفًا: أرقامٌ واقتراحٌ للمالك.** لا يتغيّر `SPLIT_CLIFF_PCT`
في الإنتاج بهذي التجربة مهما كانت نتيجتها — التغيير قرارُ مالكٍ منفصل.

---

## ② الأذرع — مُثبَّتة الآن، لا سادسَ لها

| الذراع | التعريف | لماذا هذي بالذات |
|---|---|---|
| **C0** | الأساس: هبوط **يومٍ واحد** ‏≥30% داخل 120 يومًا | خطّ القياس |
| **C1** | يومٌ واحد ‏≥**25%** | **SPRC مقيسٌ عند ‏−28.3%** ⇒ هذي الذراع تلتقطه وC0 لا |
| **C2** | يومٌ واحد ‏≥**20%** | تخفيفٌ أوسع — لقياس منحنى الكلفة لا لتوصية |
| **C3** | **تراكميّ**: هبوط ‏≥40% خلال ‏≤20 جلسة (بلا شرط يومٍ واحد) | فرضية «الهادئ المتدرّج»: EHGO هبط 2.80→1.30 **سلّميًّا** فلا كليفَ له أصلًا |
| **C4** | **بلا مُرشّح كليف** + تأكيد التقسيم مباشرةً من اللقطة | **الحدّ الأعلى للالتقاط** والحدّ الأدنى للدقّة — يقيس ما يُقرّبه الكليف أصلًا |

**ولا تُمَسّ أيُّ عتبةٍ أخرى في أيّ ذراع** — الشروط الستة (①-⑥) تبقى كما هي في
الإنتاج حرفيًّا، والذراع تغيّر **مُرشّح الدخول وحده**. (وتُنفَّذ بمفتاح بحثيّ في
أدوات الفحص لا في `CONFIG` الإنتاجي.)

---

## ③ المقياسان — كلاهما إلزاميّ، والحكم لا يصدر بأحدهما

**⚠️ القاعدة المؤسِّسة (معيار المالك، `OPUS_EXECUTION_PACKAGE.md` §🌟-2):** التقاط
الستة **وحده** يتحقّق بإلغاء كل البوّابات، وذلك هدمٌ لا نجاح. فكل ذراع تُقاس بالاثنين:

1. **الاسترجاع (Recall):** كم من الثمانية يحقّق **≥1 يوم مطابقة كاملة قبل انفجاره**
   (نفس معيار T-HUNTER-SIX حرفيًّا، ونفس دالّة الحكم `scan_split_hunter`).
2. **الكلفة (Precision proxy):** **عدد المطابقين الكاملين في اليوم على السوق الكامل**
   من اللقطة — **متوسطًا ووسيطًا وأقصى**. وتُقاس بجانبها **كلفة الشبكة**: عدد الرموز
   الداخلة لمرحلة تأكيد التقسيم (مرحلة `probe`) لأنها الكلفة التي وُضع الكليف لأجلها.

**خط الأساس يُقاس ولا يُفترَض:** معدّل C0 اليوميّ يُطبع أولًا من نفس اللقطة، وكل
ذراعٍ تُقارَن به نسبةً (‏×) لا بعددٍ مطلق.

---

## ④ المعيار — مثبَّت الآن بالأرقام

ذراعٌ تُوصَف بأنها **«مرشّحة للعرض على المالك»** ⟺ استوفت **الشرطين معًا**:

- **(أ) استرجاعٌ حقيقيّ:** تلتقط **≥1 سهمًا إضافيًّا** من الستة (‏AZI·DSY·EHGO·ZCMD·
  JZ·SPRC) **قبل انفجاره** — ولا يُحتسب NUWE (ملتقَطٌ سلفًا) ولا JEM.
- **(ب) كلفةٌ محتمَلة:** معدّل المطابقين اليوميّ ‏**≤3× معدّل C0** المقيس.
  (‏3× سقفٌ مُثبَّت الآن قبل الأرقام؛ سنده: عقد المالك «لا إشعار إلا بالجاهز» وواقع
  الصيّاد المرصود ‏~1 مطابق/يوم — فثلاثة أضعافه أقصى ما يُعرَض بلا نقض العقد.)

**وما لا يستوفي الشرطين يُكتب «فشل» صراحةً** ولا يُعاد تدويره بمعيارٍ جديد.
**و«لا حكم» تُعلَن حين تكون العيّنة غير كافية** (مثلًا: ذراعٌ لا يدخلها أيّ يومِ
سوقٍ كامل) — وهي **ليست فشلًا**.

---

## ⑤ التنبّؤات القابلة للكسر (مع ما يكذّب كلًّا)

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| **Q1** | **C1 (25%) لا تلتقط SPRC** رغم أنها تتجاوز عتبته المقيسة (‏−28.3%) — لأن جدارًا ثانيًا مقيسًا خلفه: ④«لم يصعد» يقرأ له **+243.3%** | أن يخرج SPRC مطابقًا كاملًا في C1 |
| **Q2** | **C3 (التراكميّ) هي وحدها التي قد تلتقط EHGO** — لأن هبوطه سلّميّ بلا يومِ ‏−30%، وشروطه الأخرى **لم تُقَس قطّ** (الكليف قطع قبلها في 51/51 يومًا) | أن يلتقطه C1/C2 (أي أنّ له كليفًا بين 20-30%) أو ألّا يلتقطه C3/C4 (أي أن جدارًا آخر يمنعه) |
| **Q3** | **C4 (بلا مُرشّح) تكشف السقف الحقيقي**: عدد المطابقين لا يتضخّم كثيرًا لأن الشروط الستة نفسها هي القامعة الحقيقية — الكليف يوفّر **نداءات شبكة** أكثر مما يمنع **مطابقين** | أن يقفز معدّل المطابقين فوق 3× في C4 ⇒ الكليف بوّابةُ دقّةٍ فعليّة لا مجرّد توفير |
| **Q4** | **DSY لا تلتقطه أيّ ذراع** — لأن جداره الثاني المقيس (نافذة 120، 16 يومًا) خارج نطاق هذي التجربة | أن يخرج DSY مطابقًا في أيّ ذراع |
| **Q5** | **الحصيلة النهائية: ذراعٌ واحدة على الأكثر تستوفي المعيار، وتلتقط سهمًا واحدًا (EHGO)** — لا الستة | التقاط سهمين فأكثر، أو صفر |

🔴 **وأُسجّل صراحةً أن تنبّؤَين من تنبّؤاتي في T-HUNTER-SIX كُذِّبا** (P1 وP5) —
فاحتمالُ كسر ما فوق حقيقيّ، وهو المقصود.

---

## ⑥ حدود الصدق (تُعلَن قبل النتيجة لا بعدها)

1. **اللقطة تبدأ 2026-01-01** ⇒ نافذة القياس ~7 أشهر، **ليست ثلاث سنوات**. فالكلفة
   المقيسة **موسمٌ واحد** لا حكمَ دهرٍ ⇒ أيّ توصية تبقى **مشروطة بإعادة القياس**.
2. **الفلوت ليس point-in-time** (يُجلب اليوم) — وقد يقلب حكم ⑤ لأسهمٍ في السوق الكامل.
3. **قناة «الطرح الجديد» (SEC) معطَّلة** في الفحص ⇒ فئةٌ من الأحداث المؤسِّسة غير مرئية.
4. **الشمعة اليومية لا تشمل الافتر** ⇒ لمساتُ قاعٍ ممتدّة غير مرئية (نفس حدّ AH-GUARD).
5. **عيّنة الستة ثمانية أسهم** — تشخيصٌ لا إحصاء؛ لا يُشتقّ منها معدّل نجاحٍ ولا عائد.
6. **الكلفة المقيسة «مطابقون/يوم» لا «إنذارات كاذبة»** — لأن الحكم على جودة المطابق
   يتطلّب متابعةً أمامية غير متاحة هنا. ⇒ **لا يُقال «دقّة» بل «كلفة تسليم»**.

---

## ⑦ سقف النجاح — مثبَّت

**أرقامٌ + جدولُ أذرع + اقتراحٌ واحدٌ للمالك.** لا تغيير `SPLIT_CLIFF_PCT` ولا أي
عتبة إنتاجية · لا رسالة تلغرام جديدة · ولا `LOGIC_VERSION`. وأي تفعيلٍ لاحقٍ يمرّ
بقرار المالك + قفل «مطفأ ⇒ بت-بت» + طفرة.
