# 🎯 حزمةُ تنفيذٍ لأوبيس — دفعةُ فيصل 2026-09-18 (‏85 صورة ‏+ دليلُ إليوت 57 صفحة)

**الأمر:** المالك 2026-09-18 «**جهّز كل شي لأوبيس 5 … أنت الإشراف والتجهيز وهو التنفيذ**» بعد أن أرسل
‏85 صورةً في 16 دفعة ‏+ ملفَّ «موجات إليوت وطريقة فيصل». **الدور:** أنا (المدير التنفيذيّ) قرأتُ
المادّةَ كلَّها بعيني، وثبّتُّ كلَّ إبرةٍ في الكود، وأثبتُّ النماذجَ الأوّلية بفِكستشراتٍ من أرقام فيصل
الحرفيّة ‏+ **‏10 طفراتٍ سقطت كلُّها**؛ **وأوبيس ينفّذ ما فيها ولا يجتهد خارجها** — كلُّ انحرافٍ يلزمه
أن يتوقّف ويُبلغ (‏§⓪). **سوابقُها:** `OPUS_EXECUTION_PACKAGE.md` · `OPUS_METHOD_UPDATES_PACKAGE.md` ·
`OPUS_GOLD_ENTRY_PACKAGE.md` — بنفس التقسيم.

**المرجعُ الكامل للمادّة:** `FAISAL_IMAGES_CATALOG.md §سابع وعشرون` (الصورُ الـ59 المتمايزة بأسمائها
`*_20260918_NN_*` في `faisal_images/`) · `FAISAL_ELLIOTT_NOTES.md` (الدليلُ صفحةً صفحة) ·
`FAISAL_SOURCE_LEDGER.md §دفعة 2026-09-18` (وسمُ كلّ رقم).

---

## ⓪ الحدود الحمراء — يُوقَف فورًا ويُبلَّغ (STOP)

1. **لا يُمَسّ جذرٌ محميّ بحرف** — الاثنا عشر ببصماتها على رأس هذي الحزمة (‏§②-0)، وأيُّ فرقٍ في
   بصمةٍ بعد التنفيذ = **توقّف**: `rank_key` · `select_top` · `classify_tier` · `analyze_ticker` ·
   `apply_short_gate` · `apply_float_gate` · `scan_market` (**عدا كتلة الإثراء العرضية داخل حارسها**،
   كما سُمح لـ`rsi27_price` سابقًا) · `backtest_symbol` · `scan_ignition` · `scan_split_hunter`
   ودرعُه `_HUNTER_PINS` · `entry_status` · `build_interpretation`. **ولا تُمَسّ:** `_red_candle_heads` ·
   `resistance_levels` · `refine_targets_4h` · `four_hour_levels` · `key_levels` · دوالُّ الوقف · الدفعات.
2. **لا `LOGIC_VERSION` ولا `force_renew`** — كلُّ ما في المسار (أ) **عرضٌ/تخزينُ حقلٍ عرضيّ** (سابقةُ
   `BK1`-`BK9`: حقلٌ عرضيّ في `make_watch_entry`/`update_watchlist_status` **لا يرفع الإصدار**).
3. **لا قناةَ تلغرام رابعة** ولا رسالةَ جديدة — سطرٌ واحدٌ داخل الكروت القائمة.
4. **لا رقمَ يُخترَع.** كلُّ عتبةٍ هنا **منطوقةٌ بلسان فيصل** أو **مُعاد استعمالُها بالاسم** من `CONFIG`
   بوسمها القائم، **أو موسومةٌ `engineering` صراحةً** (‏§④). ما ليس كذلك **لا يُضاف**.
5. **لا يُعاد فتحُ محورٍ مُغلَق** بأيّ صيغة: `T-CANDLE` (الشموعُ الانعكاسية — و«‏Three Inside Up»
   منها) · `T-TRAIL`/`T-EXITMGMT`/`T-OPHOLD`/`T-OPTRADE` (إدارةُ الخروج) · `T-RSI40`/`T-RSI-RANK`
   (‏RSI بوّابةً/ترتيبًا) · `T-PMGATE` (نافذةُ 04:30 نيويورك = «لندن 7-8») · `T-WAIT-LOWER`/
   `T-WAIT-RSI27`/`T-WAIT-23W` (انتظارُ سعرٍ أدنى) · `T-GOLD-ENTRY P6` (حفظُ 5 جلسات) ·
   `T-STABILITY` (ثباتٌ يرفض). **المسارُ (ب) يقترح تسجيلًا مسبقًا ولا ينفّذه.**
6. **بوّابةُ الدفع:** `python3 test_bot.py` **خروج 0 وصفر فشل** قبل أيّ دفع · و`company_cache.json`
   يُستعاد بـ`git checkout --` قبل الكوميت (تلوّثُ التشغيل الموثَّق).
7. **الطفرات في `git worktree` معزول** وعلى شجرةٍ ملتزمة · المقياسُ **رمزُ الخروج ‏+ أسماءُ الأقفال
   الساقطة ‏+ هل انهار** — لا عدُّ `❌` · وكلُّ مرساةٍ تطابق **مرّةً واحدة** (‏`count == 1`) ·
   **والطفرةُ الناجية تُفحَص أوّلًا: هل غيّرت سلوكًا أصلًا؟** (الصنف ④-3).
8. **إن اختلفت إبرةٌ عمّا في §② (اسمُ دالّةٍ أو نصٌّ) — قف وأبلغ**، لا تبحث عن «ما يشبهها». الأرقامُ
   السطرية أدناه من رأس `d9cbcf6b` **وتُراجَع بالاسم لا بالسطر** إن انزاحت.
9. **ممنوعٌ `≥ ≤ > <`** في أيّ نصٍّ معروض (قاعدةُ العرض المُلزِمة) — السطرُ المقترَح يقول «أعلى من
   القاع بـ15% إلى 25%».
10. **الـPDF والتفريغُ الصوتيّ لا يُدفَعان** (عملُ صديق فيصل · المستودعُ عامّ).

---

## ① النصُّ المصدر — حرفيًّا بأرقام صوره (لا يُعاد صياغتُه)

| # | النصّ | الصورة | الوسم |
|---|---|---|---|
| N1 | «**غالبًا بعد الشمعة الساقطة اختبارُ مقاومةٍ من ‏25% لـ15%**» ومثاله «‏1.83+25% = 2.28 (اختبر 2.30)» | `X_20260918_11_afterhours` | `faisal_verbatim` |
| N2 | «**كيف تحسب: ‏0.69+15% = 0.794**» | `TG_20260918_40_NUWE` | `faisal_adopted` (قناةٌ تعليمية) |
| N3 | «‏0.71+23% = 0.87» · «‏2.10+40% = 2.94» · «يُقاس **بارتداد مضارب ‏15%** وهبوطٍ لاختبار الثبات» | `X_13_NUWE` · `X_24_SXTC` · `X_48_WETO` | `faisal_verbatim` |
| N4 | «**هدف 5 = إغلاقُ الشمعة الهابطة** ‏5.782 · **هدفٌ أكبر = إغلاقُ أعلى شمعةٍ هابطة** ‏6.609» (بخطّ يده) | `X_20260918_27_CUPR` | `faisal_verbatim` |
| N5 | «**رأسُ شمعةٍ حمرا اختبرَ وهبط · رأسُ شمعةٍ ثانيةٍ حمرا اخترق**» (يؤكّد الرؤوس جنبَ الإغلاقات) | `TG_20260918_37_NUWE` | `faisal_adopted` |
| N6 | «أو انتظر يثبت فوق ‏80 سنتًا — **جسمُ آخر شمعةٍ يسار قبل يهبط**» | `TG_20260918_38_NUWE` | `faisal_adopted` |
| N7 | «القاعُ ‏69 سنتًا على 4 ساعات · وعلى اليوميّ يختلف ‏71 سنتًا · **دقّةُ التحليل مهمّة هذا علم**» | `TG_20260918_38_NUWE` | `faisal_adopted` |
| N8 | «**الدعمُ الأسبوعيّ واليوميّ يتّفقان على ‏1.70 ⇒ فشلُ السهم تحته مباشرة**» (تُعاد الإشارةُ من دفعة 09-05) | `X_20260905_03` | `faisal_verbatim` |
| N9 | «الأبيضُ مقاومة · النهجُ الصحيح يختبر المقاومةَ ويرجع يختبر دعمًا — **مب شرط نفسه يمكن فوقه بشوي**» | `TG_20260918_68`≡`TG_20260905_07` | `faisal_verbatim` |
| N10 | «‏Rsi **لا بدّ يستمرّ فوق 50 عشان يكمل صعودَه**» · وشارتُه يحمل **`ROC(12,6)`/`ROCMA`** (‏يظهر مرّتين) | `X_20260918_81`≡`X_20260905_04` · `TG_37` | `faisal_verbatim` · `faisal_inferred` |
| N11 | «**تحطّ طلبك تحت المتوسّطات ‏10%** · الفرق ‏20% · لو هبط ففي مرونةٌ بوقف الخسارة · لو فوق المتوسّط وهبط لسحب السيولة هنا مشكلة» | `X_20260918_21_entry_below_ma` | `faisal_verbatim` |
| N12 | «**‏100% ⟵ جني ربحٍ ‏70% من سيولتك · ‏30% تُترَك لأعلى هدف · هبط أكثرَ من ‏20% خلال ‏5 دقائق ⟵ جني المتبقّي** · صعودُ المضارب لـ100% بشمعةٍ لا تتجاوز 5 دقائق» | `TG_20260918_73`≡`TG_20260905_01` | `third_party` (مجموعة — الكاتبُ غيرُ مُثبَت) |
| N13 | «**القاع = وقف**» · «انتظارُ ضغطٍ قبل الصعود **كما هو متعارَفٌ عليه**» · «الطلباتُ بعد القاع» | `TG_20260918_72`≡`TG_20260905_02` | `third_party` |
| N14 | «متى تقرّر إيجابيّةَ السهم: **إذا ما كسر القاعَ أكثرَ من ‏5 جلسات · يصعد ‏20% بعد القاع · يرجع يختبر الدعم — هذا أصلُ التحليل** · الدخولُ بعد الاختبار · **طلباتٌ فوارقُ ‏5 سنتات** عند الدعم» | `X_20260918_85_YMT` | `faisal_verbatim` |
| N15 | «**‏$BTOG ‏200% — الرمزُ الجديد $SGRX**» · و«‏KWM غدًا تتداول برمزٍ جديد — بعضُها يعطي نسبةً كويّسة وبعضُها لا» | `X_74`≡`X_20260905_08` · `X_20260918_29_KWM` | `faisal_verbatim` |
| N16 | «ذكرتُ تحليلي لأسهم الارتكاز **لا يُبنى ع السيولة الحالية — نبحث عن السيولة السابقة المتمركّزة**» · «هبط وكسر 6 كم نسبةُ الوقف لا تمثّل **‏7% من دخولنا 6.50**» | `X_20260918_61_VEEE` | `faisal_verbatim` |
| N17 | «**ذيلُ شمعة التقسيم السابقة ‏5.87 هدف**» (بخطّه على مستوًى 🟠) | `CH_20260918_05_CPOP` | `faisal_verbatim` |
| N18 | «**دائمًا أرسم المقاومةَ باللاين الأسود**» · «مقاومات لاين أسود» · «**الأزرقُ فجوةٌ لازم يغطّيها**» · «**المناطقُ الصفرا لو تجاوزها تعطيه أهدافًا**» | `X_84` · `CH_17` · `TG_69` · `X_77` | `faisal_verbatim` |
| N19 | «**يحلّل ع فريم 5 دقائق**» · «فريم 4 ساعات كذلك **30 دقيقة**» · الدليل ص22: «‏5m يدير التنفيذ» | `X_48_WETO` · `X_84_YMT` · PDF | `faisal_verbatim` · `third_party` |
| N20 | «**جسمُ الشمعة لا بدّ أعلى من فيواب 2.50**» · «جسمُ شمعةٍ فوق المستوى أقوى من ذيلٍ عابر» | PDF ص36/38/43 | `faisal_verbatim` · `third_party` |

---

## ② حالةُ الكود اليوم — إبرٌ مثبَّتة (على `d9cbcf6b` · تُراجَع بالاسم)

### ②-0 بصماتُ الجذور الاثني عشر (SHA-256 على `ast.dump` · 12 محرفًا) — **يجب أن تبقى هي هي بعد التنفيذ**
```
rank_key 12304e2e8763 · select_top ea82d29a84aa · classify_tier 4000f581bdcc · analyze_ticker d36af8284ef9
apply_short_gate 6c3abbe6830c · apply_float_gate 750a84d4c2c5 · scan_market 61552990c603 · backtest_symbol ee2379df02ea
scan_ignition bf59c3a495ba · scan_split_hunter 1c6d651d25b5 · entry_status a9f12435b523 · build_interpretation 792fe1aab01b
```
(‏`scan_market` وحدَها يجوز أن تتغيّر بصمتُها **فقط** إن وُضع التخزينُ داخل كتلة الإثراء المحروسة — كما وقع مع
`rsi27_price` وأثبته `BK9` بالـAST. وإن لم تُختَر تلك الطريقة فتبقى مطابقة.)

### ②-1 ما هو **موجودٌ سلفًا** — ولا يُعاد بناؤه (🔴 تصحيحاتٌ على ملاحظاتي أثناء الاستلام)
| الدعوى | الحقيقةُ في الكود | الإبرة |
|---|---|---|
| «‏Stoch RSI 14/14/3/3 ليس عندنا» (قلتُه في الدفعة 11) | **خطأ — موجود:** `stoch_rsi(close, 14, 3, 3)` ويدخل الجاهزية (`SCORE_STOCHRSI`) | `Super_stock.py:1181` · `:3627-3633` |
| «‏MA5/MA20 (الدائرة الزرقا/البرتقالي) زوجٌ ثالث» | **موجود:** `ma5`/`ma20` تُحسبان وتدخلان الجاهزية (`SCORE_MA_SHORT` «فيصل يستخدمها مع 30/50») | `:3658-3665` |
| «طلباتٌ فوارقُ 5 سنتات» جديد | **موجود:** `descending_ladder(level, step=FAISAL_LADDER_STEP=0.05)` بالاتّجاهين (‏`IMG_0647`) — ويُستعمل في النظام الرابع | `:7573` · `:614` · `:8018-8020` |
| VWAP | **موجود:** `rolling_vwap` (عرض) · `vwap_entry_confirm` (مقيسٌ وسقط) | `:1348` · `:12961` |
| خطُّ العنق · فيبو إعادة الدخول · «‏30% تحرّر ⟸ تواصل» · «‏50% لا مطاردة» | **موجودة:** `neckline_level` · `fib_reentry` · `first_target_release` · `rise_from_bottom`/`first_rise_suffix` | `:7453` · `:7498` · `:7556` · `:7384`/`:4508` |
| رؤوسُ الشموع الحمرا أهدافًا | **موجود:** `_red_candle_heads` يأخذ **`High`** (‏N5 يؤكّده) | `:2624-2640` |
| ذيولُ الحمرا وبدايةُ الصاعدة دعومًا | **موجود:** `faisal_candle_supports` (‏`red_tails` = Low · `green_origins` = Open) — فحصُ اليد فقط | `:4527` · `hand_check.py:346` |
| شمعةُ الفجوة الساقطة (قيمة=Close · رأس=High) | **موجود:** `falling_gap_candle` | `:6856` |
| ربعُ الساعة | **موجود:** `operator_sustain` / `OPERATOR_SUSTAIN_MIN=15` | `:6310` · `:667` |
| «القاع = وقف» | **مشحون:** `PIVOT_STOP_AT_LOW=True` (2026-08-29) · و`STOP_BELOW_LOW_PCT=(5,7)` تحت القاع سندُه `TG_2062` | `:358-360` |
| «الدعمُ يُختبَر ثم المقاومة ثم الدعم أعلى قليلًا» | **مقيس:** `T-FUSE` `F2` (قاعٌ جديدٌ يُحفَظ) ‏+0.225R | الكاتالوج §خامس وعشرون-② |
| الألوان 🔴 ⚫ 🔵 🟣 🟡 ⚪ | **موثَّقة** (رأس الكاتالوج · §رابع وعشرون-① · §خامس وعشرون-①) | — |

### ②-2 ما هو **غائبٌ فعلًا** (الجديدُ الصافي)
| الميزة | الدليل على الغياب |
|---|---|
| **إغلاقاتُ** الشموع الهابطة أهدافًا (‏N4) | `_red_candle_heads` رؤوسٌ فقط؛ `falling_gap_candle` شمعةٌ واحدة (الفجوة) · الكاتالوج §رابع وعشرون-④: «الجسمُ غيرُ محسوم» |
| **جسمُ آخر شمعةٍ قبل الهبوط** (‏N6) | `grep "جسم" Super_stock.py` = تعليقٌ واحد · لا دالّة |
| **نطاقُ اختبار المقاومة ‏15-25% من القاع** (‏N1-N3، سبعةُ نصوص) | لا مفتاحَ `*_TEST_BAND*` · `faisal_wait_zone` = **10-20% تحت** (عكسُ الاتّجاه) |
| **توافقُ دعمَي الفريمين** (‏N8) | الكاتالوج §رابع وعشرون-②: «ليس عندنا شيءٌ منه — `tf_count` تقيس توافقَ إشاراتِ انعكاس لا تطابقَ مستويات» |
| **`ROC(12,6)`** (‏N10) | `roc` دالّةٌ **داخلية** في `kst` فقط (‏`:1106`) · غيرُ معروضٍ ولا مخزَّن |
| **تأكيدُ الاختراق بالجسم** (‏N20) | `tested_level` يعدّ **اللمسات** (‏`:8263`) · لا «جسمٌ فوق المستوى» |
| **الأحداثُ المؤسِّسة:** تغييرُ الرمز (‏N15) | `grep "تغيير الرمز\|رمز جديد"` = صفر في الكود والأرشيف |
| **`EMA 9/26`** (‏`X_54_indicators`) | `ema(close, 30/50)` فقط (‏`:3427-3428`) |

### 🔴 ثلاثُ حقائقَ تُقرأ قبل أيّ كود (ثبّتُّها بنفسي)
1. **الوقفُ الحيُّ للارتكاز = القاعُ نفسُه** (‏`PIVOT_STOP_AT_LOW=True`) ⇒ نصُّ فيصل «‏7% من دخولنا» (‏N16) **لا يحرّك شيئًا**: نسبتُه محسوبةٌ من الدخول والمشحونُ مرجعُه القاع؛ **يُدوَّن ولا يُنفَّذ**.
2. **«‏5 جلسات ‏+ 20%» (‏N14) تخالف نصَّه الآخر «حافظ ع قاعه 3 جلسات» (‏`IMG_0151` = `STABILITY_MIN=3`)** — **والخمسةُ قِيست وسقطت** (‏`T-GOLD-ENTRY P6` ‏−0.032R في الثلاث) ⇒ لا يُغيَّر رقم؛ ويُدوَّن التعارضُ في الدفتر بالنصّين.
3. **«الطلباتُ فوارقُ 5 سنتات» مبنيّةٌ سلفًا** (‏`descending_ladder`) ⇒ لا تُعاد.

---

## ③ المسار (أ) — عرضٌ فقط · يُنفَّذ عند «نفّذ» (صفرُ رقمٍ مخترَع · صفرُ مسٍّ بجذر)

### ③-1 خمسُ دوالَّ نقيّة ‏+ سطرُ عرضٍ واحد — **النصُّ المُثبَت** (يُلصق كما هو بعد `candle_supports_line` في `Super_stock.py`)

> أُثبتت على فِكستشراتٍ من أرقام فيصل الحرفيّة (‏CUPR 6.609/5.782 · NUWE 0.85⟶0.69 · PPBT 1.682/1.694 ·
> ‏0.69+15% = 0.7935) **و‏10 طفراتٍ سقطت كلُّها** (‏§⑥). المفاتيحُ الجديدة في `CONFIG` **ثلاثةٌ** (‏§④).

```python
def red_candle_closes(df, price, span: int = 130):
    """🎯 **إغلاقاتُ الشموع الهابطة أهدافًا** — فيصل بخطّ يده على `$CUPR` (`X_20260918_27`):
    «هدف 5 = **إغلاقُ الشمعة الهابطة** 5.782 · هدفٌ أكبر = **إغلاقُ أعلى شمعةٍ هابطة** 6.609».
    عائلةٌ ثانيةٌ بجانب `_red_candle_heads` (الرؤوس — يؤكّدها هو أيضًا: «رأسُ شمعةٍ حمرا اختبرَ
    وهبط» `TG_20260918_37`). **إضافةٌ عرضيّة**: لا تدخل `resistance_levels` ولا t1/t2/t3 المقفولة.
    الاعتبارُ بنفس عتبة الرؤوس (`RES_RED_HEAD_MIN_DROP` — `engineering` موسومة). نقيّة · فاشلة-آمنة."""
    try:
        o = df["Open"].values.astype(float)
        c = df["Close"].values.astype(float)
        n = len(c)
        thr = CONFIG.get("RES_RED_HEAD_MIN_DROP", 3.0) / 100.0
        px = float(price)
        if n < 5 or px <= 0 or px != px:
            return []
        out = []
        for i in range(max(0, n - span), n):
            if o[i] > 0 and c[i] < o[i] and (o[i] - c[i]) / o[i] >= thr and c[i] > px:
                out.append(round(float(c[i]), 4))
        return sorted(set(out))
    except Exception:                    # noqa: BLE001
        return []


def last_body_before_drop(df, price, span: int = 90):
    """🕯️ **جسمُ آخر شمعةٍ قبل الهبوط** — «أو انتظر يثبت فوق 80 سنتًا: **جسمُ آخر شمعةٍ يسار قبل
    يهبط**» (`TG_20260918_38` على `$NUWE`). المستوى = **أعلى الجسم** `max(Open, Close)` لآخر شمعةٍ
    ذاتِ جسمٍ معتبر (`RES_RED_HEAD_MIN_DROP`) سبقت هبوطًا لا يقلّ عن `FAISAL_BODY_DROP_PCT` من إغلاقها
    إلى أدنى قاعٍ لاحق، **فوق السعر** (مستوى ثباتٍ مستهدَف لا تاريخ). يرجع
    `{"level", "ago", "drop_pct"}` أو `None`. نقيّة · فاشلة-آمنة · بلا نظر مستقبليّ (الهبوطُ مضى)."""
    try:
        o = df["Open"].values.astype(float)
        c = df["Close"].values.astype(float)
        lo = df["Low"].values.astype(float)
        n = len(c)
        px = float(price)
        if n < 3 or px <= 0 or px != px:
            return None
        thr = CONFIG.get("RES_RED_HEAD_MIN_DROP", 3.0) / 100.0
        need = float(CONFIG.get("FAISAL_BODY_DROP_PCT", 15.0))
        best = None
        for i in range(max(0, n - span), n - 1):
            if o[i] <= 0 or abs(c[i] - o[i]) / o[i] < thr:
                continue
            after_low = float(lo[i + 1:].min())
            if after_low <= 0 or c[i] <= 0:
                continue
            drop = (1.0 - after_low / c[i]) * 100.0
            top = max(float(o[i]), float(c[i]))
            if drop >= need and top > px:
                best = {"level": round(top, 4), "ago": int(n - 1 - i),
                        "drop_pct": round(float(drop), 2)}
        return best                                   # آخرُ مطابقٍ = الأقربُ زمنيًّا
    except Exception:                    # noqa: BLE001
        return None


def bounce_test_band(low):
    """📐 **نطاقُ اختبار المقاومة بعد القاع** — سبعةُ نصوص: «غالبًا بعد الشمعة الساقطة اختبارُ
    مقاومةٍ من **25% لـ15%**» (`X_20260918_11`) · «كيف تحسب: **0.69+15% = 0.794**» (`TG_40`) ·
    «0.71+23%» · «1.83+25%» · «يُقاس **بارتداد مضارب 15%**» (`X_48`) · و«2.10+40%» حالةٌ أكبر
    تُذكَر ولا تدخل النطاق. **عرضٌ لا هدف** — الأهدافُ المقفولة لا تُمَسّ. يرجع `(أدنى, أعلى)` أو None."""
    try:
        x = float(low)
        if x <= 0 or x != x:
            return None
        lo_p = float(CONFIG.get("FAISAL_TEST_BAND_LO_PCT", 15.0))
        hi_p = float(CONFIG.get("FAISAL_TEST_BAND_HI_PCT", 25.0))
        return (round(x * (1 + lo_p / 100.0), 4), round(x * (1 + hi_p / 100.0), 4))
    except (TypeError, ValueError):
        return None


def support_agreement(daily_support, weekly_support, tol_pct: float = None):
    """📏 **توافقُ دعمَي الفريمين** — «الدعمُ الأسبوعيّ واليوميّ **يتّفقان** على 1.70 ⇒ فشلُ السهم
    تحته مباشرة» (`X_20260905_03` على `$PPBT`). التسامحُ الافتراضيّ = «دقّةُ الخطأ لا تتجاوز **2%**»
    (الدليل ص54 · `faisal_verbatim`). يرجع `{"agree", "level", "gap_pct"}`. نقيّة."""
    try:
        d, w = float(daily_support), float(weekly_support)
        tol = float(tol_pct if tol_pct is not None else CONFIG.get("FAISAL_LEVEL_TOL_PCT", 2.0))
        if d <= 0 or w <= 0 or d != d or w != w:
            return {"agree": False, "level": None, "gap_pct": None}
        gap = abs(d - w) / max(d, w) * 100.0
        ok = gap <= tol
        return {"agree": ok, "level": round(min(d, w), 4) if ok else None,
                "gap_pct": round(gap, 2)}
    except (TypeError, ValueError):
        return {"agree": False, "level": None, "gap_pct": None}


def roc_state(close, n: int = 12, ma: int = 6):
    """📊 **ROC(12,6)** كما يظهر في شارت فيصل مرّتين (`X_20260905_04` · `TG_20260918_37`) —
    مؤشّرٌ مساندٌ **لا بوّابة** (الاستعمالُ مرئيٌّ والقاعدةُ غيرُ منطوقة ⇒ `faisal_inferred`).
    يرجع `(roc, rocma)` مقرَّبَين أو `(None, None)`. نقيّة."""
    try:
        r = close.pct_change(n) * 100.0
        m = r.rolling(ma).mean()
        rv, mv = float(r.iloc[-1]), float(m.iloc[-1])
        if rv != rv or mv != mv:
            return None, None
        return round(rv, 3), round(mv, 3)
    except Exception:                    # noqa: BLE001
        return None, None


def faisal_levels_line(closes, body, band, agree, rocv) -> str:
    """🧭 سطرٌ واحدٌ يجمع الخمسة (فحصُ اليد · التقرير الفنيّ · وكرتُ التجديد/اليوميّ عبر الحقول
    المخزَّنة). **بلا `≥ ≤ > <`** (قاعدةُ العرض). «» إن لم يوجد شيء."""
    parts = []
    if closes:
        parts.append("إغلاقات الهابطة " + " · ".join(f"${v:.2f}" for v in closes[:3]))
    if body:
        parts.append(f"جسم آخر شمعة قبل الهبوط ${body['level']:.2f}")
    if band:
        parts.append(f"نطاق اختبار المقاومة ${band[0]:.2f} إلى ${band[1]:.2f} "
                     "(أعلى من القاع بـ15% إلى 25%)")
    if agree and agree.get("agree"):
        parts.append(f"الدعم اليومي والأسبوعي يتفقان عند ${agree['level']:.2f} (كسره = فشل)")
    if rocv and rocv[0] is not None:
        parts.append(f"ROC {rocv[0]:.1f} (متوسطه {rocv[1]:.1f})")
    return "🧭 " + " · ".join(parts) if parts else ""
```

### ③-2 الوصلُ (بالترتيب · كلُّ نقطةٍ تُثبَت بالـAST من نقطة النداء الحيّة — `wire-check`)
1. **`hand_check.py`** (المصدرُ الأوّل — `df` متاحٌ): بعد سطر `r["candle_supports"] = ...` (‏`:346`):
   `r["faisal_levels"] = bot.faisal_levels_line(bot.red_candle_closes(df, px), bot.last_body_before_drop(df, px), bot.bounce_test_band(r.get("pivot") or 0), bot.support_agreement(r.get("support") or 0, r.get("weekly_support") or 0), bot.roc_state(df["Close"]))`
   ثم يُطبع بعد سطر `candle_supports_line` بنفس الأسلوب. **`weekly_support`**: إن لم يكن حقلًا قائمًا في `r`
   فيُمرَّر `0` ⇒ `agree=False` ⇒ لا يُطبع الجزءُ — **لا يُخترَع دعمٌ أسبوعيّ**؛ فحصُ وجوده: `grep -n "weekly_support\|w_support\|support_w" Super_stock.py` — وإن غاب يُبلَّغ في التقرير بندًا مسمًّى (‏§⑧).
2. **`technical_report.py`**: نفسُ السطر في قسم المستويات (‏`TICKER=` وضع).
3. **كرتُ التجديد واليوميّ** (اختياريٌّ في هذي الحزمة — **يُنفَّذ فقط إن نجحت 1 و2 وخرجت السويّة 0**):
   يُخزَّن `faisal_levels` نصًّا **داخل حارس الإثراء العرضيّ في `scan_market`** (الموضعُ نفسُه الذي كان
   يحمل `rsi27_price` — `BK9` القديم يصف الحارس) ⟶ `make_watch_entry` ينسخه ⟶ `update_watchlist_status`
   يجدّده ⟶ يُطبع في `build_message` و`build_daily_message` **بعد سطر الأهداف** وقبل التحذيرات.
   **إن رفض `BK9`/الأقفال** (لأن البصمة تغيّرت خارج الحارس) ⇒ **توقّف وأبلغ**، ولا تُرخِ القفل.

---

## ④ المفاتيحُ الجديدة في `CONFIG` — بجانب `SPLIT_SWEEP_*` (‏`:672-674`) وبوسمها في الدفتر

```python
    # 📐 دفعة 2026-09-18 — عرضٌ فقط (خارج الفرز والجذور):
    "FAISAL_TEST_BAND_LO_PCT": 15.0,     # «اختبار مقاومة من 25% لـ15%» · «0.69+15%» · «ارتداد مضارب 15%»  faisal_verbatim
    "FAISAL_TEST_BAND_HI_PCT": 25.0,     # «1.83+25%» · حدُّ النطاق المنطوق                                  faisal_verbatim
    "FAISAL_LEVEL_TOL_PCT": 2.0,         # «دقّة الخطأ لا تتجاوز 2%» (الدليل ص54)                          faisal_verbatim
    "FAISAL_BODY_DROP_PCT": 15.0,        # ⚙️ عمقُ الهبوط الذي يجعل الشمعةَ «قبل الهبوط» — engineering (أُعيد رقمُ PRESS_DROP_PCT بلا ادّعاء سند)
```
**وصفوفُ الدفتر** لهذي الأربعة جاهزةٌ في `FAISAL_SOURCE_LEDGER.md §دفعة 2026-09-18` — **تُنقل كما هي**.

---

## ⑤ الأقفال المطلوبة (بأسمائها — كلٌّ يجب أن **يسقط** بطفرته · تُوضع بعد كتلة `BK9`)

| القفل | يُثبت | الطفرةُ التي تُسقطه |
|---|---|---|
| `FB1` | `red_candle_closes` على فِكستشر CUPR (‏6 شموع: هابطتان معتبرتان · هابطةٌ 1% · خضراوان · وإغلاقٌ تحت السعر) = `[3.3, 5.782, 6.609]` · وفوق السعر 7.0 = `[]` · و`price=0`/`NaN` = `[]` | `m1` تشمل الخضراء · `m2` تُعرض ما تحت السعر |
| `FB2` | `last_body_before_drop` على فِكستشر NUWE (‏5 شموع 0.85⟶0.69) = `level 0.85 · ago 3` · وبعمق 60% = `None` · وبسعر 0.90 = `None` | `m3` أدنى الجسم · `m4` بلا عمق · `m10` بلا حارس السعر |
| `FB3` | `bounce_test_band(0.69) == (0.7935, 0.8625)` · `(0)`/`("x")` = `None` · **والقيمتان تُقرآن من `CONFIG` بالاسم** (طفرةٌ تبدّل المفتاح تُسقطه) | `m5` يُقلب الاتّجاه |
| `FB4` | `support_agreement(1.682, 1.694)` يتّفق عند 1.682 · `(1.70, 2.10)` لا · `(None, 1)` لا · والتسامحُ من `FAISAL_LEVEL_TOL_PCT` | `m6` بلا تسامح |
| `FB5` | `roc_state` على `linspace(1,2,40)` موجب · وعلى سلسلةٍ من عنصرين `(None, None)` | `m9` NaN يُرجَع رقمًا |
| `FB6` | `faisal_levels_line` **بلا `≥ ≤ > <`** في 6 تركيبات · و«» عند الفراغ التامّ | `m8` علامةُ مقارنة |
| `FB7` | **AST:** `hand_check.py` ينادي الستَّ **بالاسم** من نقطة النداء الحيّة (لا ذكرًا في docstring) | قطعُ نداءٍ واحد |
| `FB8` | **`getsource`:** الستُّ **خارج** `rank_key`/`select_top`/`classify_tier`/`entry_status`/`analyze_ticker`/`backtest_symbol`/`resistance_levels`/`refine_targets_4h` | إقحامُ نداءٍ في `resistance_levels` |
| `FB9` | بصماتُ الجذور الاثني عشر = §②-0 (باستثناء `scan_market` إن نُفِّذ ③-2-3 داخل الحارس — وعندها `BK9`-نمط بالـAST يُثبت أن الإسناد **داخل** `try` الإثراء) | نقلُ التخزين خارج الحارس |
| `FB10` | صفوفُ الدفتر للمفاتيح الأربعة حاضرةٌ بوسمٍ من الستّة (يُوسَّع قفلُ الدفتر القائم) | حذفُ صفّ |

---

## ⑥ الطفرات (‏`git worktree` معزول · شجرةٌ ملتزمة · `PYTHONDONTWRITEBYTECODE=1` · مرساةٌ فريدة)

الطفراتُ العشر أعلاه **شُغِّلت على النموذج الأوّلي وسقطت كلُّها** (‏`scratchpad/proto` — المرساةُ لكلٍّ
`count == 1` · صفرُ انهيار · الاستعادةُ خضراء). أوبيس يعيدها **على `test_bot.py` الحقيقيّ** بعد وضع
الأقفال، بمِنصّة `lock-and-mutate` (رمزُ الخروج ‏+ أسماءُ الأقفال ‏+ `Traceback`). **طفرةٌ تنجو =
قفلٌ ضعيف ⇒ يُشدَّد القفلُ لا تُحذف الطفرة.** ويُضاف `w1`: حذفُ سطر النداء في `hand_check.py` ⟶ `FB7`.

---

## ⑦ البوّابة والدفع
`python3 test_bot.py` ⟶ خروج 0 · `git checkout -- company_cache.json` · commit برسالةٍ تصف
**العرضَ فقط** وتُنهى بالتذييلين المعتمدين · push ⟶ PR مؤقّت ⟶ دمجٌ فوريّ في `main` (الدفعُ المباشر
503) · تحقّقٌ من CI · ثم **الذاكرة** (بندٌ في `DECISIONS_ARCHIVE.md` ‏+ صفٌّ في فهرس `CLAUDE.md` ‏+
`HANDOFF_ARCHIVE.md`/`HANDOFF.md`) — **وإعادةُ اشتقاق أرقام الفهرسين** (‏`MEM5`/`HND5`/`HND6`).

---

## ⑧ التقرير للمالك (عربيّ · يُختم بخياراته حرفيًّا)
يذكر: ما شُحن بالاسم · بصماتُ الجذور قبل/بعد · عددُ الاختبارات وخروجها · الطفراتُ (كم سقطت/نجت/انهارت)
· **وبندٌ مسمًّى لكلّ ما لم يُنفَّذ وسببُه** (مثل: `weekly_support` غائبٌ ⇒ الجزءُ الرابع من السطر خامد).

---

## ⑨ المسار (ب) — **تسجيلاتٌ مسبقةٌ مقترَحة · لا تُنفَّذ إلّا بأمرٍ مستقلّ لكلٍّ** (قابلةٌ للتكذيب)

| # | المحور | لماذا هو محورٌ جديد (لا إعادةُ فتح) | الشرطُ الإلزاميّ في العقد | التوقّعُ المسبق |
|---|---|---|---|---|
| B1 | **خروجٌ جزئيّ 70/30 عند ‏+100% ‏+ وقفٌ زمنيٌّ (‏−20% خلال 5 دقائق)** (‏N12) | حكمُ `T-EXIT` نصّ: «⏳ غيرُ مُختبَر: **الخروجُ الجزئيّ**» · و`T-EXITMGMT`/`T-TRAIL` قاستا نصفًا/تتبّعًا **على الشمعة اليوميّة** · والزنادُ الزمنيُّ بالدقائق **غيرُ قابلٍ للقياس يوميًّا** ⇒ مكانُه **قناةُ السيولة** (حيث قِيست `T-TARGET10`) | ⚠️ الكاتبُ `third_party` (مجموعة) ⇒ **يُثبَت من المالك أنه كلامُ فيصل قبل التسجيل** · ضبطُ زخمٍ صرفٍ **حاكم** (درس `T-PMGATE`) · هُويّةُ التكلفة `fcost_arms.k` بالاسم · ‏≥150 زوجًا · **و`T-TRAIL` أُغلق بشرط «محورٍ جديد لم يُقَس»** — يُستوفى بهذا | الاحتمالُ الأسبقُ الفشل (ستُّ تجاربَ خروجٍ سقطت) |
| B2 | **دخولٌ عند ‏10% تحت المتوسّطات** (‏N11) | المقيسُ «‏10-20% تحت **سعر التحليل**» و«سعرُ RSI» — **مرجعٌ ثالثٌ يتحرّك مع الزمن** | **ضبطٌ مطابَقُ التعبئة** إلزاميّ (درس `T-WAIT-23W`: كلُّ ذراعٍ تُقلّل التعبئة تربح آليًّا) · أيُّ متوسّطٍ؟ (‏30/50 المشحون أم 20/30/50) يُثبَّت قبل الأرقام | فشل (أربعُ تجاربِ دخولٍ سبقت) |
| B3 | **تغييرُ الرمز حدثًا مؤسِّسًا رابعًا** (‏N15) | صفرُ ذكرٍ في الكود والأرشيف · حالتان (‏KWM بلا نتيجة · BTOG⟶SGRX ‏+200%) · «النقلُ من OTC» أُغلق لأن المنتقل ليس ارتكازًا — **وتغييرُ الرمز يقع على سهمٍ له تاريخ** فليس الحالةَ نفسَها | مصدرٌ آليّ (‏8-K 5.03 من قناة SEC القائمة أو `symbolChange` في المزوّد) · **قياسٌ فقط** كـ`T-LINK100` مقابل شاهدين · وفيصل نفسُه «بعضُها لا» | لا رابطَ يعبر |
| B4 | **توافقُ الدعمين مُميِّزًا** (‏N8) | مقترَحٌ في §رابع وعشرون-② ولم يُسجَّل | يلزمه **تعريفُ دعمٍ أسبوعيٍّ** واحدٌ مثبَّت قبل الأرقام · ضبطٌ عشوائيٌّ حتميّ | ضجيج |
| B5 | **«السيولةُ السابقةُ المتمركّزة» مقابل `M5`** (‏N16) | أوّلُ نصٍّ يخالف مرجعَ بوّابةِ هويّة · و`M5` لها شاهدُ حمايةٍ مقيس (‏YYAI) وفتحُها أسقط النجاح 80⟶17 | **قياسٌ وصفيٌّ أوّلًا** (هل «أقصى سيولةِ 20 يومًا في آخر 6 أشهر» يميّز داخل المؤهَّلين؟) **بلا مسٍّ بـ`M5`** | لا يميّز (كلُّ مؤشّرٍ عند القاع سقط: §0-ز) |
| B6 | **موسميّةُ سبتمبر** | مسجَّلةٌ مقترَحةً في §رابع وعشرون-⑥ | لقطاتُ PIT الثلاث | لا فرق |

---

## ⑩ ما **لا** يُفعَل (حتى لو بدا تحسينًا)
- لا يُضاف «‏Three Inside Up» ولا أيُّ نمطِ شموع (‏`T-CANDLE` صفرٌ من ستّة · وهو `third_party` أصلًا).
- لا تُبدَّل `STABILITY_MIN=3` بـ5 ولا `ENTRY_STEP_PCT` بسنتات ولا مرجعُ الوقف ولا `M5` — كلُّها **مدوَّنةٌ
  بالتعارض في الدفتر** لا مشحونة.
- لا يُؤخذ رقمٌ من تطبيق «مراقب استراتيجية فيصل» (‏`APP_*`): **أرقامُه (ثبات 3 · ارتداد 10% · نطاق 10-40% ·
  فلوت 5م) ليست أرقامَ فيصل** — نصُّه الحرفيّ (‏N14) يقول **5 جلسات و20%**.
- لا يُضاف `EMA 9/26` ولا `Stoch RSI` (موجود) ولا فريمُ 5/30 دقيقة للتحليل — **بلا تسجيلٍ مسبق**.
- لا يُرفَع `RSI` مواصلةً (‏N10) إلى أيّ قرار — نظامُه «بعد الانطلاق» ونظامُنا «عند القاع» (§رابع وعشرون-⑤).
- لا يُعاد ذكرُ «سعر RSI 27» في أيّ سطر (أمرُ «اقفل RSI»).

## ⑪ حدودُ صدقٍ تُكتب قبل الأرقام وتُنشَر معها
- **`weekly_support`** قد لا يكون حقلًا قائمًا ⇒ جزءُ التوافق **خامدٌ حتى يُعرَّف** (يُبلَّغ لا يُخترَع).
- **مستوياتُ الشارتات في الصور مُولَّدةٌ آليًّا حسب المدى المعروض** (تبدّلت لأربعة أسهم: SXTC · NUWE · VEEE ·
  ELPW) ⇒ **لا يُستشهد برقمِ مستوًى من شارتٍ إلّا ما كتبه فيصل بخطّه أو نصّه**.
- ‏11 من الصور مكرّرةٌ حرفيًّا و4 تكبيرات ⇒ **‏59 مادّةً متمايزة** (لا 85).
- الصورُ من مجموعات/محادثات (‏`TG_*`/`WA_*`) **بلا توقيعٍ** ⇒ `third_party` حتى يُثبت المالكُ كاتبَها
  — **وثلاثةٌ منها أثقلُ ما في الدفعة** (‏N12 · N13 · «‏Jz/Ezra/Icu لقطوها قروبات ع 20%»).
