# 📉🚪 `T-RSI40` — النتيجة: **فشلت (‏`RS1` ساقطة)**

> **العقد:** `rsi40_prereg.md` مدفوعٌ `5ba799d` **قبل أيّ سطرِ أداةٍ وقبل أيّ رقم**،
> وملحقاه `§⑦`/`§⑧` مدفوعان **قبل أوّل تشغيلة**. هذا الملفُّ **بجانب العقد لا فوقه**.
>
> **التشغيلات (‏main `42729cb`):** 2023 `34404618834` (لقطة `31574165523`) ·
> 2024 `34404626674` (`31199493957`) · 2025 `34404633691` (`31191327731`) ·
> **‏2026 وصفيّة** `34404639957` (`34322427155`، جُمِّدت **قبل** أيّ رقمٍ من هذي التجربة).
> **الأربعُ خرجت بـ0 · والحرّاسُ الخمسة عبروا في كلٍّ منها.**

---

## ① السؤال والحكم

**السؤال:** بوّابةُ `RSI_NOW_HARD` النافذة **‏71.1186** من ظرف الكاتالوج، ودليلُ فيصل
التعليميّ يقول «مستحيل يصعد إذا RSI بمناطق 40» و«‏RSI حول 40 يعني أن جزءًا من
الارتداد حصل بالفعل» (ص65) في خمسة مواضع ⇒ **كم يُكلّف شدُّها إلى نصّه؟**

🔴 **الحكم: `RS1` ساقطة — والشدُّ يُكلّف مُسلَّمين ولا يُربّح.**
المعيارُ المسجَّل يشترط ألّا يُنقص `H40` المُسلَّمين عند ‏+100% **في أيّ سنةٍ من
الثلاث**، والمقيس **‏−3 · +3 · −5** ⇒ **سالبٌ في سنتين من ثلاث.**

---

## ② الأرقام (‏سعة 15 · `rank_live` · محورُ جلساتٍ موحَّد)

### الحاكم — المُسلَّمون عند ‏+100% (`d100`)

| السنة | `H0` ‏71.12 | **`H40`** 🥇 | `H50` | `H30` |
|---|---|---|---|---|
| 2023 | 13 | **10 (‏−3)** 🔴 | 16 (‏+3) | 4 (‏−9) 🔴 |
| 2024 | 9 | **12 (‏+3)** | 9 (‏0) | 2 (‏−7) 🔴 |
| 2025 | 12 | **7 (‏−5)** 🔴 | 11 (‏−1) 🔴 | 4 (‏−8) 🔴 |
| **المجمَّع** | **34** | **‏29 (‏−5 · −14.7%)** | ‏36 (‏+2 · +5.9%) | ‏10 (‏−24 · −70.6%) |
| ‏2026 **وصفيّة** | 1 | 2 (‏+1) | 2 (‏+1) | 2 (‏+1) |

### الكلفة — الإشارات و`d50` و`R/صفقة`

| | `H40` | `H50` | `H30` |
|---|---|---|---|
| قصُّ الإشارات (مجمَّع) | **‏−44.91%** | ‏−10.09% | ‏−87.78% |
| `d50` (مجمَّع) | ‏−9 | ‏+2 | ‏−44 |
| فرقُ `R/صفقة` سنويًّا | **‏−0.049 · −0.158 · −0.087** | ‏+0.055 · −0.046 · −0.019 | ‏+0.177 · −0.139 · −0.153 |

📌 **ورقمٌ يُدوَّن: جدارُ `H0` يُطلق ‏68-104 رفضةً فقط في السنة** مقابل ‏9.5-10.9 ألفًا
لـ`H40` ⇒ **البوّابةُ الحيّةُ عند 71.12 شبهُ خاملة** — وهو ما جعل السؤال ذا معنًى.

---

## ③ `RS2` — الكلفةُ بأسمائها (لا تُجمَّل)

**مَن أسقطهم `H40` من المُسلَّمين عند ‏+100%:**

- **2023 (‏خرج 6 · دخل 3):** `ADAP` · `ASTR` · `GCT` · `IBRX` · `PALI` · `TCBP`
  ⟵ ودخل `HOTH` · `PHIO` · `VEDU`.
- **2024 (‏خرج 2 · دخل 5):** `BOF` · `CHNR` ⟵ ودخل `ARQQ` · `NEON` · `OM` · `PALI` · `RAYA`.
- **2025 (‏خرج 8 · دخل 3):** `ELWS` · `EVGN` · `IOVA` · `NUKK` · `NXTC` · `ORKT` ·
  `UPC` · `XOS` ⟵ ودخل `CAMP` · `YI` · `ZURA`.

---

## ④ الاكتشافان — أثقلُ من الحكم

### أ · الإشارةُ تنقلب ⇒ الفرقُ ضجيجٌ لا حافّة

`H40 − H0` بالسنة: **‏−3 · +3 · −5** (‏و‏+1 في 2026 الوصفيّة). وهذا **النمطُ نفسُه**
الذي أسقط `T-SHORT` (‏+10 · +29 · −7) و`T-CMAG` وتصنيفَ A/B: فرقٌ يبدو مادّيًّا في
سنةٍ ويُعاكس نفسَه في التالية. **وحدُّ العقد كان يقوله سلفًا** («فروقٌ صغيرةٌ داخل
الضجيج»، و`T-CEILING` قاس ‏±3) — **و‏±3 إلى ±5 هو بالضبط ما صدر.**

### ب · لكنّ `R/صفقة` **لا** ينقلب — وهو ضدَّ `H40` في السنوات الأربع

**‏−0.049 · −0.158 · −0.087** (‏و‏−0.101 في 2026) ⇒ **شدُّ البوّابة إلى 40 يُسوّئ
التوقّعَ لكلّ صفقةٍ باطّراد** بينما يقصّ ‏≈45% من الإشارات. ⚠️ **وليس معيارًا
مسجَّلًا** فلا يحكم — لكنه **يسدّ بابَ «الفرقُ ضجيجٌ فربّما ينفع بمقياسٍ آخر»**:
لا ينفع بالمقياسين.

---

## ⑤ التنبّؤات — **‏3 من 5 تحقّقت** وتُنشَر مكذَّبةً كما سُجِّلت

| | التنبّؤ | الحصيلة |
|---|---|---|
| `RS-P1` | `H40` يُنقص المُسلَّمين في سنةٍ ⇒ `RS1` تسقط | ✅ **تحقّق** (‏سنتان) |
| `RS-P2` | القصُّ يتجاوز ‏25% | ✅ **تحقّق** (‏−41.3 · −45.2 · −48.3) |
| `RS-P3` | `H50` أقلُّ ضررًا من `H40` **رتيبًا** | ❌ **مكذَّب** — 2024 تعكسه (‏+0 مقابل +3) |
| `RS-P4` | `H30` يُنقص في الثلاث كلِّها | ✅ **تحقّق** (‏−9 · −7 · −8) |
| `RS-P5` | بين الخارجين **سهمٌ من كتالوج فيصل** | ❌ **مكذَّب** — ‏16 اسمًا وصفرٌ من الـ35 |

🔴 **و`RS-P5` أهمُّ المكذَّبَين:** ظننتُ أن شدَّ البوّابة سيُخرج أسهمًا من كتالوج
فيصل نفسِه فيصير **حجّةً نصّيّةً ضدّ النصّ**. لم يقع: **الخارجون كلُّهم خارج
الكاتالوج** ⇒ الحجّةُ ضدّ الشدّ **رقميّةٌ خالصة** لا نصّيّة.

---

## ⑥ `H50` لا يُرشَّح — ولا انتقاءَ بعد الأرقام

`H50` مجمَّعُه **‏+2** ويبدو مغريًا، **لكنه يسقط على `RS1` نفسِها**: ‏−1 في 2025
⇒ **لا يستوفي «‏≥0 في كلّ سنة»** ⇒ **لا يُذكر مرشَّحًا لتسجيلٍ لاحق** بنصّ `§④`.
⚖️ **وهذا مقصود:** لو رشّحتُه لكان **انتقاءَ ذراعٍ نجت من الضجيج بعد رؤية الأرقام** —
وهو بعينه ما تمنعه قاعدةُ التسجيل المسبق.

---

## ⑦ ولا يُكذَّب فيصل — والفرقُ في **نقلِنا** لا في نصّه

نصُّ الدليل **وصفُ حالة** («‏RSI حول 40 يعني أن جزءًا من الارتداد حصل») لا **أمرَ
رفضٍ صلب**، وتحويلُه بوّابةً قاطعةً **نقلُنا نحن** — نفسُ ما أُعلن في
`wait_rsi23w_prereg.md §⓪` عن «‏RSI 23-27».

📌 **والأهمّ: روحُ النصّ منفَّذةٌ عندنا سلفًا وبالمكان الصحيح** — `RSI_MAX_NOW`
النافذة **‏35.78** (من وسيط الكاتالوج) تُعطي **نقصًا لينًا** لكلّ ما تجاوز ‏35.78،
أي أن «‏40 إشارةُ حذر» **مطبَّقةٌ كنقصٍ لا كرفض**. والمقيسُ هنا أن **ترقيتَها إلى
رفضٍ صلبٍ هي التي تُكلّف** — لا الملاحظةُ نفسُها.

---

## ⑧ حدودُ الصدق (‏`§①` من العقد — تُقرأ مع الأرقام لا بعدها)

1. **انحيازُ بقاء** (كونُ ناسداك اليوم؛ المشطوبةُ غائبة) ⇒ ليست حكمَ سوقٍ نظيفًا.
2. **`M13`/`M14` لا تعملان في الباكتيست** ⇒ داخلٌ جديدٌ قد تحذفه `M14` حيًّا.
3. **«يُقبَل مرشّحًا» ≠ «يصل المالك»** — و`d100` يقيس التسليمَ لا الربح.
4. **المُرتِّبُ عند مستوى الصدفة بسعة 15** ⇒ فروقُ ‏±3 داخل الضجيج (وهو ما وقع).
5. **بلا افتر · ولا انزلاق** في هذا المقياس.
6. **‏2026 جزئيّةٌ ووصفيّة** — طُبعت ولم تدخل الحكم.
7. **`R/صفقة` ليس معيارًا مسجَّلًا** — عُرض شاهدًا مساندًا لا حاكمًا.

---

## ⑨ سقفُ النجاح — ما لم يتغيّر

**صفرُ تغييرٍ في الإنتاج.** `Super_stock.py` **بت-بت مع `origin/main`** في الأربع
(‏`V-R4` طبعها: `5b82b71b052d` في الطرفين) · ولا `LOGIC_VERSION` · ولا عتبةَ
تحرّكت · و**`RSI_NOW_HARD` تبقى ‏71.1186** بأرقام الظرف.

🔓 **وشرطُ إعادة الفتح مؤرَّخ:** لا يُعاد قياسُ هذي البوّابة إلّا بـ**محورٍ جديدٍ لم
يُقَس** (‏40 و50 و30 **كلُّها محكومة**، فإعادةُ عتبةٍ ليست محورًا) ‏+ **مجتمعٍ يرفع
انحيازَ البقاء** (كونٌ point-in-time يشمل المشطوبة) ‏+ **تسجيلٍ مسبقٍ جديد** ‏+
**إذنِ المالك**.
