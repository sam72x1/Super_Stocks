# 🧠🌙 عصفٌ ذهنيٌّ وتسجيلٌ مسبق — «ليه نسبةُ قائمةِ ما قبل الجلسة منخفضة؟ وكم أقصى ما يمكن رفعُها؟»

**التاريخ:** 2026-09-06 · **الأمر:** «سقطة قويه جدا … المفروض النسبة اعلى من كذا و بمراحل ·
سو عصف ذهني و اختبارات و تجارب عشان تطلع بأدق و افضل نتيجة تقدر عليها» · **الحالة:** عقدٌ
مدفوعٌ **قبل أيّ رقمٍ وقبل أيّ سطرِ كودِ أداة**. القياسُ فقط — `Super_stock.py` و`presession_radar.py`
لا يُمَسّان بحرف · لا `LOGIC_VERSION` · وسقفُ النجاح **اقتراحٌ للمالك** لا شحنٌ آليّ.

---

## ⓪ التشخيص — قبل أيّ تجربة: أين تقع النسبةُ المنخفضة فعلًا؟

الأرقامُ المنشورة (كلُّها مقيسة، لا واحدٌ منها جديد):

| المقياس | المنشور | المصدر |
|---|---|---|
| القائمةُ الحيّة (‏`day_ret`×60 · أرضية 0.69492) تلتقط من منفجري `pre` ‏+100% | **‏3/9 = 33%** (المحاكاة 2/9) | `tierlink2_result §③` |
| `R@10` بالمفتاح المشحون على 2025 (‏+80% · الجلسة) | **‏11.4%** (‏100 من 878) | `presession_result §④-4` |
| أرضيةُ `F1` تُسلّم على 2025 | **‏47 من 435** (‏5.35% استرجاعًا) | `topk_result` |
| كلفةُ السقف 60 على الإصابات | **‏29 ⟶ 7** (‏`day_ret`)، **‏29 ⟶ 1** (‏`usd_day`) | `presession_dev_result §⑤` |
| كلفةُ أرضية $100k | **‏47 ⟶ 29** | `presession_dev_result §④` |
| الرادارُ الحيُّ (‏`R1`) يلتقط من متحرّكي ‏+30% **قبل** بلوغهم +30% | **‏24.0%** (القوائم 12.9%) | `premarket_radar_result` |
| ‏8 من 11 منفجرِ `pre` ‏+100% **لا مقدّمةَ افترٍ لهم** (‏`post_hi_ret` 0.02-0.05 · رتب 111-466) | **‏73%** | `tierlink2_result §③` |

**⇒ ثلاثُ طبقاتٍ للنسبة المنخفضة، مرتّبةً بالأثر (استنتاجٌ قويّ من الأرقام أعلاه، لا قياسٌ جديد):**

1. **🕳️ عمًى بنيويّ (الأثقل):** القرارُ يُتَّخذ **‏03:50 من بيانات الأمس**، وأغلبُ منفجري
   البريماركت **يبدؤون حركتَهم بعد 04:00 بلا مقدّمةٍ في افتر الأمس** (‏8 من 11). لا مفتاحُ
   ترتيبٍ يُصلح هذا — المعلومةُ **غيرُ موجودةٍ لحظةَ القرار**. (فرضيّةٌ تُقاس في E1 بسقفِ
   العرّاف: لو أن **أفضلَ مفتاحٍ ممكن بأثرٍ رجعيّ** لا يلتقط إلّا قليلًا فالعمى بنيويّ.)
2. **✂️ قصُّ الهندسة:** السقفُ 60 (جلبٌ تسلسليّ داخل 45 ثانية) والأرضيةُ $100k يُسقطان
   معًا **‏47 ⟶ 7** إصابةً في السنة — مقيسٌ سلفًا. علاجُه هندسيّ (جلبٌ متوازٍ) لا فرضيّة.
3. **🔑 المفتاح:** `post_hi_ret` هو الأوّلُ في سنتَي المعايرة، والتحسينُ بمفتاحٍ منفردٍ
   قِيس مرارًا (‏28 مفتاحًا · `S0`) — وما لم يُقَس هو **دمجُ المفاتيح** و**سقفُ الدمج**.

**والقيدُ الحاكم على أيّ تجربةٍ هنا:** الفشلُ السادسَ عشرَ لعائلة «سلوكٌ يميّز المنفجر» يقول إن
الرافعةَ **التوقيت** لا الاختيار — والقائمةُ ليلًا **اختيارٌ بلا توقيت**. ⇒ إن أثبت E1 أن سقفَ
الاختيار منخفض، فالمخرَجُ المشروع الوحيد **نقلُ الثقل إلى الرادار الحيّ** (E2) لا تلميعُ المفتاح.

---

## ① قائمةُ القتل — ما لا يُعاد فتحُه (مقيسٌ ومُغلَق)

| الفكرة | لماذا لا | المرجع |
|---|---|---|
| تبديلُ مفتاح الترتيب بمفتاحٍ منفرد آخر | ‏28 مفتاحًا قِيست · `post_hi_ret` الأوّلُ في سنتَي المعايرة · و«ليه فات AMIX»: لا مفتاحَ يلتقط >1/6 | `presession_result §④-4/§④-7` |
| نموذجٌ لوجستيّ على 28 مفتاحًا | `S1` 2.92% **دون** المفتاح المنفرد 4.0% (يُفرِط) | `presession_result §④-4` |
| تحريكُ الأرضية/السقف/`TOPK` بعد رؤية الأسماء | `p-hacking` مُعلَن · و`T-TOPK` قاست السلّم كلَّه | `topk_result` · `tierlink2_result §③` |
| إرخاءُ بوّابة المِرساة (‏5%/$30k/3×) | `T-GATE` · `T-CUMRISE` · `T-VOLBASE` ثلاثتُها فشلت بالمعايير نفسِها | `gate_result` وأخواتها |
| أرضيةُ دولارٍ على الافتر | `T-PREFLOOR2` مستلزَمةٌ لا شرط | `presession_dev_result §④` |
| إعادةُ تسمية النافذة الحاكمة بعد الأرقام | تصحيحُ المالك 09-03 ثبّت **الجلسةَ كاملةً** | `presession_prereg §⑩` |

---

## ② الفرضيّاتُ المرتَّبة (بقوّة السند لا بجاذبيّتها)

- **H1 (E1 — سقفُ العرّاف):** لو كان **لكلّ صفٍّ أفضلُ رتبةٍ عبر المفاتيح الثماني والعشرين كلِّها**
  (اختيارٌ بأثرٍ رجعيّ مستحيلٌ حيًّا)، فكم من المنفجرين يقع داخل أعلى 10/60/100؟ هذا **سقفٌ صلبٌ لكلّ
  دمجِ مفاتيحَ ممكن** من هذي الميزات. إن كان منخفضًا فالسؤالُ مُغلَقٌ بالبناء عند 03:50.
- **H2 (E1 — الدمج):** دمجُ رتبٍ (‏RRF) لأفضل خمسة مفاتيحَ **مختارةٍ من سنتَي المعايرة وحدهما**
  يرفع `R@10` على 2025 فوق المفتاح المنفرد.
- **H3 (E1 — تشريحُ الفوت):** الفائتون يفوتون لأن `post_hi_ret` **معدومٌ** لهم (لا افترَ أمس) لا
  لأن رتبتَهم منخفضة ⇒ تُقاس حصّةُ المعدوم بين المنفجرين.
- **H4 (E2 — الرادارُ الحيّ على سنةٍ كاملة):** إن كانت المعلومةُ تظهر **بعد** 04:00، فالرادارُ الحيُّ
  (بوّابةُ الإنتاج `liq_stage_events` على مرشِّح اللقطة) يلتقط من منفجري ‏+100% نسبةً **أعلى بمراحل**
  من القائمة الليليّة — وبأيّ ثمنٍ (رسائل/جلسة · وسعرُ الالتقاط عن إغلاق الأمس).
- **H5 (E2 — زنادٌ أبسط):** زنادٌ تراكميٌّ («سيولةُ البريماركت المتراكمة ‏≥ X **و**السعرُ ‏≥ ‏+Y% عن
  الأمس») **بلا قفزة حجمٍ 3×** يلتقط أبكر من مِرساة الإنتاج — وثمنُه ضجيجٌ يُقاس بعدد الزنادات/جلسة.
  ⚠️ يُقرأ مع حكم `T-VOLBASE`/`T-GATE` (المِرساةُ نفسُها لا تُمَسّ) — هذا قياسُ **طبقة تسليمٍ
  موازية** لا تعديلُ بوّابة.
- **H6 (E3 — مؤجَّلة، لا كودَ في هذي الدفعة):** الحافزُ الخبريّ (إيداعات SEC · `acceptanceDateTime`)
  يقع بين إغلاق الأمس و04:00 ⇒ معلومةٌ **متاحةٌ عند 03:50 ولا نقرؤها**. تُسجَّل هنا **فرضيّةً فقط**؛
  بناؤها يلزمه مِجَسُّ جدوى على مصدر SEC أوّلًا.

---

## ③ E1 — `T-PRE-CEIL` سقفُ الاختيار عند 03:50 (على الصفوف المرفوعة، بلا مسحٍ جديد)

**المجتمع:** صفوفُ `presession_rows_{2023,2024,2025}` نفسُها (التشغيلات `33724354384` ·
`33724361819` · `33724367680`) — **ميزانيةٌ ثابتةٌ بالبناء** · الجلسةُ الحاكمة `PM` · النافذةُ
الحاكمة **الجلسةُ كاملةً** (‏`hit80_s`) · ووسمٌ ثانٍ **‏+100%** = `maxs ≥ 100` (رقمُ المالك
«‏+100%» من `T-TIERLINK-2` — يُقاس **وصفيًّا** بجانب الحاكم لا بدلًا منه).

**الأداة:** كتلةٌ جديدة في `presession_report.py` خلف علمٍ **مطفأٍ افتراضًا** `PRESESSION_CEIL=1`
(المنشورُ بت-بت بلاه — سابقةُ `DEV_ON`)، تقرأ الرتبَ من `group_ranks` **المصدرِ الواحد** ⇒ لا مقياسَ ثانٍ.

**الأذرع (مثبَّتةٌ سلفًا — ولا ذراعَ تُضاف بعد الأرقام):**
- `K0` المفتاحُ المشحون `post_hi_ret` — مرجعُ التكامل (يجب أن يُعيد `R@10 = 100/878 = 11.4%` على 2025 · `V-C0`).
- `K-ORACLE` أفضلُ رتبةٍ لكلّ صفٍّ عبر الميزات الثماني والعشرين (كلُّ ميزةٍ باتّجاهها المسجَّل) — **سقفٌ لا ذراعٌ تُشحَن**.
- `K-RRF5` دمجُ رتبٍ متبادل (‏`1/(60+rank)`) لأفضل **خمسة** مفاتيحَ بـ`R@10` **على 2023+2024 مجمَّعَين** (لا 2025).
- `K-RRF-ALL` الدمجُ نفسُه على الثماني والعشرين (شاهدُ ضبط: هل الاختيارُ يفيد أم يضرّ؟).

**المقاييس:** `R@10` و`R@60` (الاسترجاعُ عند سقف الجلب الحيّ) و`P@10` — على `hit80_s` (الحاكم) و`maxs≥100` (وصفيّ) · **وتشريحُ الفوت:** حصّةُ المنفجرين الذين `post_hi_ret` معدومٌ لهم، ووسيطُ رتبة `post_hi_ret` بين مَن هو غيرُ معدوم.

**المعايير (تلزم كما كُتبت):**
- **C1 (سقف):** `K-ORACLE R@60` على 2025 ‏**≥ 50%** ⇒ «المعلومةُ موجودةٌ عند 03:50 والمشكلةُ في الاختيار». دونها ⇒ **«عمًى بنيويّ» — يُغلَق محورُ المفاتيح ويُنقَل الثقلُ إلى E2.**
- **C2 (دمج):** `K-RRF5 R@10 − K0 R@10 ≥ +3.0 نقطة` على 2025 **و**الفرقُ غيرُ سالبٍ في كلٍّ من 2023 و2024 (وصفيًّا) ⇒ اقتراحُ تبديل المفتاح إلى الدمج (بقرار المالك).
- **C3 (فوت):** حصّةُ المعدوم بين منفجري 2025 ‏**≥ 50%** ⇒ «الفائتُ بلا مقدّمةِ افتر» يتأكّد على 878 لا على 11.
- الأرضيةُ: منفجرون ‏≥ 30 في كلّ خليّةٍ تُحكَم (وإلّا «لا حكم»).

**التنبّؤات (تُكتَب قبل الرقم وتُكذَّب برقمٍ واحد):**
- `CE-P1`: `K-ORACLE R@60` على 2025 يقع في **‏[35%، 50%)** ⇒ C1 **تسقط بفارقٍ ضيّق** (السقفُ أعلى من الحيّ بمراتب لكنه دون النصف).
- `CE-P2`: `K-RRF5` يضيف **‏≤ +2 نقطة** على `R@10` ⇒ C2 تسقط (المفاتيحُ من عائلةٍ واحدة `post_*` فالدمجُ يكرّر).
- `CE-P3`: حصّةُ المعدوم بين منفجري 2025 **‏≥ 55%** ⇒ C3 تتحقّق.
- `CE-P4`: `K-RRF-ALL` **أدنى** من `K-RRF5` (الضجيجُ يُغرق).
- `CE-P5`: على وسم ‏+100% يكون `K0 R@10` **أدنى** منه على ‏+80% (المضاعِفون أقلُّ مقدّمةً لا أكثر).

**سقفُ النجاح:** إن استُوفي C2 ⇒ **اقتراحٌ** بتبديل `rank_key("PM")` إلى الدمج — بسطرٍ واحدٍ في
`presession_feats` وبقرار المالك · وإن سقط C1 ⇒ **حكمٌ بنيويّ** يُغلق محورَ المفاتيح ويوجّه الجهدَ إلى E2.

---

## ④ E2 — `T-PM-CURVE` منحنى الالتقاط/الكلفة للرادار الحيّ على **سنةٍ كاملة**

**السؤال:** لو تركنا القائمةَ الليليّة كما هي، **كم يلتقط الرادارُ الحيّ** (‏`R1` = مرشِّحُ اللقطة
‏+ `liq_stage_events` الإنتاجيّة) من منفجري البريماركت ‏+30/+50/+100% **قبل** بلوغهم الحدّ، وبكم رسالةٍ
في الجلسة، وبأيّ سعرٍ عن إغلاق الأمس؟ — على **‏2025 كاملةً** لا 15 جلسة.

**الأداة:** `pm_curve_scan.py` **مستقلّةٌ** تستورد دوالَّ `pm_radar_scan` **بالاسم** (‏`parse_pre` ·
`mover_t30` · `candidate_index` · `replay_anchor`) ⇒ `pm_radar_scan.py` **لا يُمَسّ** (أرقامُه المنشورة
قابلةٌ للإعادة بت-بت — سابقةُ `CAP15`) · بلا كون قوائم (لا `git` في سنةٍ كاملة) · وضعُ `legacy` ·
كونُ السعر [‏`MIN_PRICE`، 10]$ · `pm_curve.yml` يدويٌّ بلا كرون · `contents: read`.

**الأذرع (مثبَّتة):**
- `R1` مِرساةُ الإنتاج على مرشِّح اللقطة (‏`$30,000` آخرُ دقيقة ثم إعادةٌ من `i0+1`) — **الحاكمة**.
- زناداتٌ تراكميّةٌ **بلا قفزة حجم** — طبقةُ تسليمٍ موازيةٌ تُقاس لا تُشحَن:
  `T-A` (‏$30k تراكميّ ∧ ‏+10%) · `T-B` (‏$100k ∧ ‏+10%) · `T-C` (‏$100k ∧ ‏+20%) · `T-D` (‏$30k ∧ ‏+5%).
  الأرقامُ **مُعادةٌ** (‏`LIQ_MIN_USD` · `IGNITION_USD_OPERATOR` · `LIQ_MIN_MOVE_PCT` · «‏10%/20%»
  من سلّم `T-TOPK`/`T-GATE`) — صفرُ رقمٍ مخترَع.

**سلّمُ المتحرّكين:** ‏+30 (‏`faisal_verbatim`) · ‏+50 · ‏+100 (رقمُ المالك) — بأرضية `MOVER_USD` نفسِها.

**المقاييس لكلّ ذراعٍ وسلّم:** الالتقاطُ (الزنادُ **قبل** دقيقة بلوغ الحدّ) · **الرسائل/جلسة** (وسيطٌ ومجموع) · **تأخّرُ الالتقاط** (سعرُ الزناد ÷ إغلاق الأمس − 1، وسيط) · ونسبةُ الزنادات التي بلغ سهمُها ‏+30% بعدها («إثمار» — دقّةُ الزناد).

**بوّاباتُ الصلاحية:** `V-K0` تغطيةُ الملفّات ‏≥ 95% من أيام العمل (المفقودُ يُسمّى) · `V-K1` `R1` على المدى
المشترك مع `T-PM-RADAR` (‏2026-08-18 ⟶ 09-05 لا يُقاس هنا — المدى 2025) ⇒ بدلًا منه **شاهدُ ضبطٍ داخليّ**:
`T-A` ⊇ `T-D` في الالتقاط بالبناء (أرضيةٌ واحدةٌ ورفعةٌ أشدّ) وإلّا خروج 3 · `V-K2` الأرضيةُ 100
متحرّكًا لكلّ سلّمٍ يُحكَم.

**المعايير:** لا «اعتماد» هنا — القياسُ **منحنى كلفةٍ** يُقرأ مع E1. **الحكمُ الوحيد المسجَّل:**
- **C4:** التقاطُ `R1` لمنفجري ‏+100% **قبل +100%** على 2025 ‏**≥ 60%** ⇒ الرادارُ الحيُّ **يرى ما تعمى
  عنه القائمة** (مقابل 22-33% للقائمة) — وتُعرَض النسبةُ على المالك مع كلفتها.
- **C5:** أفضلُ زنادٍ تراكميّ يلتقط ‏+100% قبل +30% (أي **أبكرَ من المِرساة** على المتحرّك نفسِه) بنسبة ‏≥ 40%
  **وبرسائلَ ‏≤ 3× رسائلِ `R1`** ⇒ اقتراحُ طبقةِ تسليمٍ موازية (بقرار المالك).

**التنبّؤات:**
- `PC-P1`: `R1` يلتقط **‏65-80%** من منفجري ‏+100% قبل +100% (يعبر C4) — لكن بتأخّرٍ وسيطٍ **‏+25..+40%** عن الأمس.
- `PC-P2`: `R1` يلتقط **‏≤ 30%** منهم **قبل +30%** (يوافق 24% المنشورة).
- `PC-P3`: `T-D` (‏$30k ∧ +5%) أبكرُ الزنادات لكنها الأكثرُ ضجيجًا (‏≥ 5× رسائل `R1`) ⇒ تسقط C5 بالكلفة.
- `PC-P4`: `T-B` (‏$100k ∧ +10%) تلتقط ‏+100% قبل +30% بنسبة **‏40-55%** برسائلَ ‏≤ 2× `R1` ⇒ **المرشَّحُ الوحيد لعبور C5**.
- `PC-P5`: وسيطُ رسائل `R1` في الجلسة على 2025 **‏15-30** (لا 20 كما في الأسبوعين — السوقُ أهدأ في المتوسّط).

**حدودُ صدقٍ مكتوبةٌ قبل الأرقام:** لمسٌ لا تنفيذ · الكونُ فوق `MIN_PRICE` (‏`RITR`/`SGRX` خارجَه) ·
مَن لا إغلاقَ أمسٍ باسمه خارج المقام (وضعُ `legacy`) · لا ربحيّةَ تُقاس · و**الرادارُ يحتاج عاملًا حيًّا من
04:00** (الخلَفُ والكرونات — حدُّ تسليمٍ لا قياس).

---

## ⑤ E3 — `T-PRE-EDGAR` (مسجَّلةٌ فرضيّةً · **لا كودَ في هذي الدفعة**)

لكلّ منفجرِ ‏+100% في 2025 (من صفوف E1) مقابل شاهدٍ حتميّ من غير المنفجرين: هل يوجد إيداعُ SEC
(‏`8-K`/`424B`/`S-1`) **قُبل بين 16:00 أمس و03:50** اليوم؟ المصدر `data.sec.gov/submissions`
(‏`acceptanceDateTime`) · المعيار: الفرقُ ‏≥ 15 نقطةً بفاصلَي Wilson منفصلَين. **تُبنى بعد E1/E2
وبأمرٍ صريح** — لأن جدوى المصدر (سقوفُ النداء · تغطيةُ رموزنا بـCIK) غيرُ مقيسة.

---

## ⑥ E4 — اقتراحٌ هندسيٌّ مقيسٌ سلفًا (بلا تجربةٍ جديدة)

**رفعُ سقف الجلب من 60 بالتوازي** (‏`ThreadPoolExecutor` داخل `BUDGET_SEC` نفسِه): الجدولُ المنشور يقول
‏7 ⟶ 29 إصابةً في السنة بلا سقف (أرضيةٌ وحدها) — أي **‏×4** على المُسلَّم بلا تغيير مفتاحٍ ولا أرضية.
⚠️ **يبدّل ما يصل المالك** (‏≈1.7 اسمًا/ليلة ⟶ ‏≈1.2/ليلة أكثر) ⇒ **قرارُ مالك** («نفّذ التوازي»).
ولا يُقاس هنا ثانيةً — الرقمُ موجود.

---

## ⑦ ترتيبُ التنفيذ وسقفُ الدفعة

1. هذا العقدُ يُدفَع **أوّلًا** · 2. أداةُ E1 + أقفالُها · 3. أداةُ E2 + أقفالُها · 4. السويّة (خروج 0 ·
صفر فشل) ⇒ دفعٌ ⇒ دمجٌ مؤقّتٌ في `main` (‏`workflow_dispatch` لا يرى ملفًّا على فرع) ⇒ تشغيل ·
5. النتائجُ في `presession_ceiling_result.md` **بجانب** هذا العقد لا فوقه · 6. لا تغييرَ إنتاجيّ بلا «نفّذ».

**ما لا يُفعَل مهما كانت الأرقام:** لا تحريكَ أرضيةٍ أو سقفٍ أو `TOPK` · لا مسَّ ببوّابة المِرساة ·
لا ذراعَ تُضاف · لا ترقيةَ وسمٍ وصفيٍّ (‏+100%) إلى حاكم.
