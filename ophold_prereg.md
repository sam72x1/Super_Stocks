# 🕵️📈🪙 `T-OPHOLD` — تسجيلٌ مسبقٌ **حاكم**: سياسةُ **حَمْلٍ** بعد مِرساة «هنا الدخول» بلا سقفِ ‏+10%، مقيسةً بالتكلفة وبضبطِ زخمٍ وبمقارنةٍ مقترنةٍ بسياسة `T-OPTRADE`

> **مدفوعٌ ومدموجٌ قبل أيّ سطرِ أداةٍ وقبل أيّ رقم.** أمرُ المالك **«سجّل الحمل»**
> (‏2026-09-16) بعد اعتراضه على `T-OPTRADE`: «فيه أسهم كثيره جدااا نبه عنها البوت بهنا
> دخول المضارب وبعد كم يوم ترتفع نسب جنونية مثلا RETO امس واليوم DLXY».
> **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · لا عتبةَ تتحرّك · لا
> أوامرَ تداولٍ ولا وسيطَ تداول · ولا وكلاء.**

---

## ⓪ إعلانُ التلوّث — ما رأيتُه قبل كتابة هذا العقد، وما يُقيّده

هذا العقدُ **ليس أعمى**، ويُعلَن ذلك قبل أيّ شيء. قرأتُ كاملًا:

| المصدر | ما رأيتُه |
|---|---|
| `opcurve_rows.tsv` (‏481 صفًّا × 22) و`opcurve_result.md` | **كلَّ صفٍّ**، ومنها صفُّ `RETO` ‏2026-09-15: `t_hit10` = 1 دقيقة · `close_reg` = **‏+658.3%** · `up_ext` = **‏+1679%** · `t_peak` = 326 دقيقة · لم يُكسَر القاع. والوسائطُ المنشورة: `close_reg` فاصلُه **‏[−4.2, −1.9]** (المِرساةُ النموذجيّة تُغلق **تحت** سعر الكرت) · `up_ext` وسيط ‏+8.4 · `t_peak` وسيط 38 وبين `exploded50` **‏192 دقيقة** [43·364] · `exploded50` ‏52 من 481 (‏10.8%) |
| `tierlink_result.md` | `exploded100` ‏4.4% · وسيط `mg_5d` ‏+2.9% **ملوَّثٌ بالتقسيم** (‏LGCL · FGL) |
| أحكامُ سياسات الخروج السابقة | `T-EXIT` (‏+20%/+100% ثابتَين **على مجتمع الارتكاز** ⇒ سالبان) · `T-TARGET10` (‏+10% يعبر **على مجتمع الارتكاز**) · `T-EXITMGMT` (إدارةُ الخروج صفرٌ مقيس) · `T-MANAGE-25` (‏±0.01R) — **كلُّها على باكتيست الارتكاز لا على مراسي السيولة** |
| `T-OPTRADE` (العقد والأداة) | السياسةُ وهُويّةُ التكلفة والضبطُ `C-MOM` — **ولم تُشغَّل، فصفرُ `R` لأيّ سياسةٍ رأيتُه** |

**وما يلزم عن ذلك ويُكتب الآن:**

1. 🔑 **الاتّجاهُ معروفٌ لي والمقدارُ لا.** أعرف أن الوسيطَ سالبٌ والذيلَ ضخم. **لا
   أعرف**: متوسّطَ `R` بعد التكلفة لأيّ ذراعِ حَمْل · ولا حصّةَ الوقف تحت الحمل · ولا
   الفرقَ المقترنَ مع `+10%` · ولا فاصلَ أيٍّ منها · ولا نتيجةَ ضبط الزخم. **هذي هي
   الكمّيّاتُ الحاكمة، وهي عمياء.**
2. **فالتجربةُ «تأكيديّةٌ مشروطة» لا عمياء** على المجتمع المقروء (‏481) ⇒ **لا تُرقّى
   إلى دليلٍ أوّليٍّ إلّا بحصادٍ أماميّ** (§⑧ `H-FWD`) على مراسٍ **لم يرَها أحد**.
3. **لا تُضاف ذراعٌ ولا سلّةٌ بعد قراءة أيّ رقمٍ من هذا العقد** — الأذرعُ مُغلَقةٌ في §④.
4. **حجّةُ `RETO` ليست دليلًا** — صفٌّ واحدٌ مختارٌ على نتيجته. تُنشَر في التقرير
   **كحالةٍ** لا كوزن.

---

## ① السؤالُ القابل للتكذيب

هل سياسةُ **حَمْلٍ** بعد مِرساة «هنا الدخول» — **بلا هدفٍ ثابت**، بوقفٍ = قاعُ المِرساة،
وخروجٍ عند إغلاق الجلسة النظاميّة — تُحقّق:
- (أ) توقّعًا **موجبًا** بـ`R` **بعد التكلفة المقيسة**،
- (ب) **وتتفوّق مقترنةً** على سياسة `T-OPTRADE` (‏+10% في 120 دقيقة) **على الصفوف نفسِها**،
- (ج) **وتتفوّق مقترنةً** على ضبطِ زخمٍ صرف (`C-MOM`)؟

**والثلاثةُ معًا** — فسؤالُ المالك «الحملُ أفضل من ‏+10%» يُجاب بـ(ب)، لكنّ (أ) و(ج)
يمنعان أن يكون الجوابُ «نعم لأن المجتمعَ كلَّه يصعد» أو «نعم لأن الزخمَ وحدَه يفعلها».

---

## ② المجتمع — عينُ مجتمع `T-OPCURVE`/`T-OPTRADE` وبحارسه

- **المصدرُ والطبقاتُ والترتيب** كما في `optrade_prereg.md §②` حرفيًّا؛ **و`V-H1` = `V-T1`
  نفسُه**: على النافذة المجمَّدة (`until = 2026-09-15`) يجب أن يطابق مُتعدَّدُ `(date, sym)`
  صفوفَ `opcurve_rows.tsv` (‏481 · `H1` 289 · `H2` 192) **وإلّا خروج 5 قبل أيّ رقم**.
- **الصفوفُ القابلةُ للتداول** = ما يقبله `optrade_arms.simulate` (‏`e5 > alow` وشموعٌ بعد `t0`).
- **النصفان** `H1`/`H2` كما هما (`H2_FROM = 2026-09-01`).
- 🔁 **`H-FWD` (§⑧):** كلُّ مِرساةٍ **بعد** `2026-09-15` تُقرأ من السجلّ الحيّ نفسِه
  بالدوالّ نفسِها — **ولا تُقرأ حكمًا قبل بلوغ أرضيّتها**.

---

## ③ السياسةُ والقياس — بالأسماء لا بالشبيه

| العنصر | التعريف | المصدر بالاسم |
|---|---|---|
| الدخول | سعرُ الكرت `e5` عند `t0 = anchor_ms + 4·60_000` | `tier_days_report.true_e5` · `opcurve_probe.resolve_anchor` |
| التكلفة | `k(c) = (1−c/2)/(1+c/2)` · `c = C-MED` · `s = S-MED` · وشبكةُ `c × s` **تُطبَع** كما في `T-OPTRADE §③` | `fcost_arms.ret_at` · `optrade_arms.read_costs` |
| الوقف | قاعُ المِرساة `alow` · أوّلُ **إغلاقِ دقيقةٍ** تحته · سعرُ الخروج = ذلك الإغلاق | `sym_day_probe.exit_point` |
| `R` | `r_fixed(ret, e5, e5, alow)` بمقامٍ ثابت · **الترتيب:** `ret_at` ⟶ `r_fixed` ⟶ المتوسّط | `tranche_arms.r_fixed` |
| الفاصل | بوتستراب عنقوديٌّ بالرمز | `exitmgmt_arms.boot_ci` عبر `optrade_arms.cl_map`/`ci_of` |
| الجلسةُ النظاميّة | آخرُ شمعةِ دقيقةٍ إغلاقُها قبل 16:00 نيويورك | `opcurve_probe.ny_hour` |

**لا إعادةَ دخول · لا `P4` (الليلُ أدنى في النصفين بنصّ `T-OPCURVE`) · لا وقفَ يتحرّك في
الذراع الحاكمة.**

---

## ④ الأذرع — قائمةٌ مُغلَقة

| الذراع | الخروج | الدور |
|---|---|---|
| **`H0`** | = `P0` من `T-OPTRADE` **بالحرف** (‏`simulate(use_target=True, h_min=120)`) | **مرجعُ الاقتران** — ويجب أن يُعيد `P0` بت-بت إذا شُغّلت `T-OPTRADE` (‏`V-H2`) |
| **`HC`** 🥇 | **إغلاقُ الجلسة النظاميّة** أو الوقف — أيُّهما أوّلًا (‏`simulate(use_target=False, h_min=None)`) | **الحاكمةُ الوحيدة** |
| `HX` | آخرُ شمعةٍ في الجلسة الممتدّة (‏≤20:00) أو الوقف | وصفيّة — **`C-MED` مقيسةٌ على الجلسة النظاميّة** وسبريدُ الافتر خارجَها ⇒ لا تحكم |
| `HP` | آخرُ شمعةِ بريماركت الغد أو الوقف — للصفوف التي لها `pm` وحدَها | وصفيّة (تُقارَن **بمُكمِّلها** `C-SUB` لا بالمجتمع) |
| `HT` | وقفٌ زاحف: من كلّ قمّةِ إغلاقِ دقيقةٍ جديدة، الخروجُ عند إغلاقٍ تحتها بمقدار `(e5 − alow)` = **‏1R** · والوقفُ الابتدائيّ `alow` | وصفيّة |
| `HH` | نصفُ المركز عند `T10` (لمسُ الأعلى) والنصفُ الآخر إلى إغلاق النظاميّة أو الوقف | وصفيّة |

**والضوابط:**
- **`C-P0`** 🥇 **حاكم:** الفرقُ المقترن `HC − H0` **لكلّ صفٍّ** (نفسُ المجتمع بالبناء).
- **`C-MOM`** 🥇 **حاكم:** كما في `optrade_prereg.md §⑤` حرفيًّا (بركةُ `D0` ثمّ `D1` ·
  بصفرِ تداخل ‏≥124 دقيقة · التطابقُ على عائد الدقيقة) — **مطبَّقًا على `HC`** بالخروج نفسِه
  (إغلاقُ النظاميّة أو «قاعٌ» يُشتقّ للشاهد بنسبة `alow/e5` نفسِها).
- `C-SUB` لـ`HP` (الجزئيّةُ تُقارَن بمُكمِّلها).
- ⚠️ **`C-MOM-15` لا يُعاد** (منحازٌ نحو الصفر بالبناء وقد أُعلن هناك).

---

## ⑤ المعاييرُ والفروع — مكتوبةٌ قبل أيّ رقم

| المعيار | النصّ |
|---|---|
| **`OH1`** | متوسّطُ `R` لـ`HC` بعد التكلفة **موجبٌ في النصفين** والفاصلُ المجمَّع لا يلمس الصفر |
| **`OH2`** | `HC − H0` مقترنًا **موجبٌ في النصفين** والفاصلُ المجمَّع لا يلمس الصفر |
| **`OH3`** | `HC − C-MOM` مقترنًا **موجبٌ في النصفين** والفاصلُ المجمَّع لا يلمس الصفر |
| **الأرضيّة** | ‏≥150 صفًّا قابلًا للتداول في كلّ نصف · و**‏≥150 زوجَ `C-MOM` في كلّ نصف** — وسقوطُها **يمنع الفرعَ 1** |

**الفروع** (ثلاثةٌ كلٌّ في سطره):
1. **تُوصى** — `OH1` و`OH2` و`OH3` معًا والأرضيّتان مستوفاتان ⇒ **ثمّ** `H-FWD` (§⑧) قبل أيّ اقتراحِ تسليم.
2. **فشلت** — أيُّ معيارٍ من الثلاثة ساقطٌ والأرضيّتان مستوفاتان.
3. **لا حكم** — أرضيّةٌ ساقطة.

**رموزُ الخروج:** ‏0 حكمٌ صدر · 2 لا مفتاح · 3 تغطيةٌ دون 80% · 4 صفرُ مراسٍ ·
**‏5 المجتمعُ يخالف `V-H1`** · **‏6 حارسٌ ساقط** (`V-H2`/التكلفة/`h*`) · **‏9 «لا حكم»**.

---

## ⑥ التنبّؤات — تُنشَر مع الحكم مؤكَّدةً أو مكذَّبة

| # | التنبّؤ |
|---|---|
| `HP1` | وسيطُ `R` لـ`HC` **سالب** (متّسقٌ مع `close_reg` المقروء) — **والمتوسّطُ موجب** |
| `HP2` | الذيلُ يحمل الحافّة: **أعلى 10% من الصفوف تحمل أكثرَ من نصف** مجموع `R` الموجب لـ`HC` |
| `HP3` | `HC − H0` **موجبٌ في `H1`** وإشارتُه في `H2` **مجهولةٌ لي** (الأهدأُ إغلاقًا) — أُعلنه لا أتنبّأ به |
| `HP4` | حصّةُ الوقف تحت `HC` **فوق 50%** من الصفوف |
| `HP5` | `C-MOM` يأكل **نصفَ** فرقِ `HC` عن الصفر فأكثر (‏`1 − (HC − C-MOM)/HC ≥ 0.5`) |
| `HP6` | `HX` أعلى متوسّطًا من `HC` وأوسعُ فاصلًا · و`HP` أدنى من `HC` (الليلُ أدنى) |
| `HP7` | `HT` بين `H0` و`HC` متوسّطًا |

---

## ⑦ حدودُ الصدق — عيوبٌ مرشَّحة حتى تُحسم

1. **مجتمعٌ مقروء** (⓪) ⇒ الفرعُ 1 **لا يُرقّى** بلا `H-FWD`.
2. **لمسٌ لا تنفيذ** للوقف والإغلاق (إغلاقُ الدقيقة لا تعبئةٌ فعليّة).
3. `C-MED`/`S-MED` **مستعارتان** من مجتمع المحرّك (كما في `T-OPTRADE`) ⇒ الشبكةُ تُطبَع.
4. **نظامُ سوقٍ واحد** (‏08-18 ⟶ 09-15) — وقد بَرَد بين النصفين.
5. **‏64% من الانفجارات بلا مِرساةٍ أصلًا** ⇒ سقفُ أيّ سياسةٍ ‏≈36% من الفرص (لا تُصلحه هذي التجربة — موضوعُ `T-LINK100`).
6. `HX`/`HP` تعبران إلى جلساتٍ **لا تغطّيها التكلفةُ المقيسة** ⇒ وصفيّتان ولو عبرتا.
7. **تعدّدُ المقارنات** في الوصفيّات (‏4 أذرع) ⇒ لا تُرقّى واحدةٌ منها بلا تسجيلٍ جديد.
8. الوقفُ = قاعُ المِرساة **بلا تسامح** ⇒ حصّةُ الوقف قد تكون **أعلى** من الواقع التنفيذيّ لمن يضع تسامحًا — يُقال ولا يُعدَّل هنا.

---

## ⑧ الحصادُ الأماميّ `H-FWD` — الشرطُ الذي يجعل الفرعَ 1 دليلًا

- **المجتمع:** مراسي «هنا الدخول» **بعد `2026-09-15`** من السجلّ الحيّ، بالدوالّ نفسِها.
- **القراءةُ الحاكمة الأولى:** عند **‏≥150 صفًّا قابلًا للتداول** أو في **‏2026-11-15** أيُّهما
  أوّلًا — وأيُّ قراءةٍ قبلها **وصفيّةٌ تُطبَع بوسمٍ صريح**.
- **المعيار:** `OH1`-`OH3` نفسُها على المجتمع الأماميّ (نصفاه بتاريخ المنتصف).
- **الترقية:** الفرعُ 1 هنا **و**الفرعُ 1 في `H-FWD` ⇒ **اقتراحُ سطرِ تسليم** (§⑨) لا أكثر.
- ⏳ **ولا وعدَ يتعفّن:** الأداةُ تطبع في كلّ تشغيلة عددَ الصفوف الأماميّة وتاريخَ الأرضيّة.

---

## ⑨ سقفُ النجاح — والمحظورات

**سقفُ النجاح:** **اقتراحٌ للمالك** بسطرِ تسليمٍ على كرت المِرساة (مثلًا: «لا تسقف الهدف
بـ‏+10% — وقفُك القاع») **بقراره وحدَه**.
**المحظورات:** ① لا تنفيذَ أوامر · ② لا وسيطَ تداول · ③ لا مسَّ جذرٍ من الاثني عشر ·
④ لا مؤشّرَ اختيارٍ ولا ترتيب · ⑤ لا عتبةَ إنتاجٍ تتحرّك بهذي التجربة.

---

## ⑩ الأداة — مواصفةٌ ملزِمة (تُبنى **بعد** دمج هذا العقد)

- `ophold_arms.py` + `.github/workflows/ophold.yml` (يدويٌّ بلا كرون · بلا سرِّ تلغرام).
- **إعادةُ استعمالٍ بالاسم لا نسخ:** `optrade_arms.simulate` · `trade_r` · `placebo_pool` ·
  `read_costs` · `read_hstar` · `build_rows` · `attach_trades` · `cl_map` · `ci_of` · `tod_of`
  — **وأيُّ منطقِ حسمٍ مكرَّرٍ في الأداة عيبٌ** (يُقفَل بـAST).
- **`V-H1`** = مطابقةُ `opcurve_rows.tsv` (خروج 5) · **`V-H2`** = `H0` يطابق `P0` بت-بت على
  الصفوف نفسِها (يُثبَت سلوكيًّا داخل السويّة) · التكلفةُ و`h*` بالنصّ لا باليد (خروج 6).
- **وضعُ جدوى `OPHOLD_DRY=1`:** يطبع ثلاثةَ أعدادٍ فقط (القابلُ للتداول لكلّ نصف · تغطيةُ
  الشاهد `D0/D1` · **حصّةُ الوقف تحت `HC`**) **ويعود قبل أيّ نداءِ تكلفةٍ أو `R`** — ⚠️ وهو
  يكشف `HP4` جزئيًّا، يُعلَن.
- **الحتميّة:** المدخلاتُ نفسُها ⇒ مُخرَجٌ بت-بت.
- **المُخرَج:** `ophold_result.md` بجانب هذا العقد لا فوقه، وجدولُ `⟦TSV⟧` لكلّ صفٍّ بأذرعه الستّة.

---

## ⑪ الأقفال (تُكتب في السويّة مع هذا العقد، وكلٌّ يسقط بطفرةٍ مسمّاة)

- **`OHK0`** — بنيويّ: فروعُ §⑤ ثلاثةٌ كلٌّ في رقمه · «لا حكم» في الثالث وحدَه · ورمزا 5 و9 منصوصان · والأرضيّةُ تمنع الفرعَ 1.
- **`OHK1`** — §④: ذراعٌ حاكمةٌ **واحدة** (`HC`) وضابطان حاكمان (`C-P0` · `C-MOM`) · و`HX`/`HP`/`HT`/`HH` وصفيّةٌ نصًّا · و`C-MOM-15` لا يُعاد.
- **`OHK2`** — §⓪ يُعلن التلوّثَ بأرقامه (‏481 · 289 · 192 · `RETO` · ‏+658 · ‏+1679) ويُقرّ أن الاتّجاهَ معروفٌ والمقدارَ لا · و§⑧ يُسمّي أرضيّةَ `H-FWD` وتاريخَها.
