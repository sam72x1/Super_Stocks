# 🔒📏 T-BORROW — تسجيل مسبق: **«المتاح للاقتراض» — قياسُه، وسدُّ الثقب الذي يمنع قياسَه**

> **مدفوعٌ قبل أيّ رقمٍ نتيجة.** أمرُ المالك 2026-08-11: قال «**1**» = «قس المتاح» بعد
> ملاحظته الحيّة: «القائمة اللي قبل الأخيرة الشورت منخفض والاقتراض مرتفع وهذا دليل
> إيجابي، لكن آخر قائمة كلّها تقريبًا الشورت مرتفع جدًّا».

---

## ① السؤال — بصياغةٍ قابلةٍ للتكذيب

هل **عددُ الأسهم المتاحة للاقتراض** (‏`shares_available` من ChartExchange = **مقياسُ
فيصل الظاهر** في تغريداته) يحمل معلومةً عن جودة المرشّح — بدرجةٍ تُبرّر **تحويلَ مقياس
بوّابة `M13` من حجم شورت FINRA اليوميّ إلى «المتاح»**، أو إضافتَه مفتاحَ ترتيب؟

**ولماذا الآن:** `M13` تقيس `finra_short` بسقف 40,000، **وسندُها الفيصليّ ساقط**
(وسمُها `unsourced` في `FAISAL_SOURCE_LEDGER.md`: سندُها الأوّل لقطةُ محادثةٍ مع طرفٍ
ثالث، والثاني رقمٌ لسهم `$ERNA` ومعناه مقلوب). **والعتبةُ الوحيدة بلسان فيصل «تحت 20
ألف» وهي على المتاح لا على حجم FINRA** ⇒ فالبوّابةُ تقيس كمّيّةً غير التي ينطق بها.

## ② 🔴 حدُّ الجدوى — يُكتَب أوّلًا لأنه يحكم شكلَ التجربة كلَّها

**فحصُ جدوى (عدٌّ فقط · بلا أيّ علاقةِ نتيجة · أُجري قبل كتابة هذا الملفّ):**

| المصدر | يحمل «المتاح»؟ |
|---|---|
| `weekly_watchlist.json → stocks` (الحيّ) | **10 من 10** ✅ |
| `weekly_watchlist.json → history` | **0 من 17** 🔴 |
| `weekly_watchlist.json → pullback` | **0 من 6** 🔴 |
| `alerts_history.json` (‏101 سجلًّا · 39 محسومة) | **‏0** — يحمل `short`(FINRA) و`short_pct` فقط 🔴 |
| `ctb_log.jsonl` (حصادُ الاقتراض) | **170 من 170** ✅ · 9 أيام · 59 رمزًا |
| تقاطعُ الحصاد × المحسومة (رمز+تاريخ) | **‏0** 🔴 (بالرمز وحده: 6) |

🔴 **النتيجة البنيوية: لا يمكن قياسُ حافّةِ «المتاح» بأثرٍ رجعيّ — لا تاريخيًّا (لا
تاريخ له مجانًا، مؤكَّدٌ سلفًا) ولا على سجلّنا الحيّ**، لأن الرقم **يُجلَب ويُعرَض ثم
يُرمى**: لا يُخزَّن مع القرار ولا مع نتيجته. ⇒ بعد ستّة أسابيع من التشغيل الحيّ **لا
نملك صفًّا واحدًا** يربط «المتاح لحظةَ الترشيح» بـ«ما حدث بعده».

⚖️ **وهذا هو صنفُ العيب المدوَّن في هذا المستودع** («نجلبه ونعرضه ولا نسجّله») —
ولذلك التجربةُ **ثلاثيّة**: ما يمكن قياسُه الآن · وما هو وصفيّ · **وسدُّ الثقب** حتى
تصير الحافّةُ قابلةً للقياس أصلًا.

## ③ الأجزاء الثلاثة — مثبَّتةٌ الآن ولا رابعَ يُضاف بعد الأرقام

### 🅐 قياسُ الكلفة على القائمة الحيّة (يُحسَب الآن)
لكلّ حدٍّ من **‏20,000 · 40,000 · 100,000** (‏`20K` نصُّ فيصل الحرفيّ · `40K` بوّابتُنا
القائمة · `100K` مرجعٌ متساهل): **كم من الأسهم المُسلَّمة يسقط** وكم يبقى، **ومَن هم
بالاسم**. ⚠️ **كلفةٌ لا حافّة** — لا يُدَّعى منها ربحٌ ولا خسارة.

### 🅑 فصلُ الفئات من `ctb_log.jsonl` (وصفيّ · بحارسٍ يُسقطه)
وسيطُ «المتاح» في: `faisal_exec`+`faisal_entered` (اختياراتُه المنفَّذة) مقابل
`faisal_negative` (ما رفضه) مقابل `control_market` (لوحةُ الشاهد الحتميّة) مقابل
قائمتنا (`bot_selected`+`bot_watchlist`).
**المعيارُ المسجَّل — يُعدُّ «فصلًا» فقط إذا:**
1. وسيطُ `faisal_exec` **أقلّ** من وسيط `control_market` بـ**‏≥5×**، **و**
2. نسبةُ «تحت 40 ألفًا» في `faisal_exec` **أعلى** من `control_market` بـ**‏≥30 نقطة**، **و**
3. ‏`n ≥ 20` صفًّا في كلّ من الفئتين المقارنتين.

🔴 **وثلاثةُ حدودِ صدقٍ تُقرأ معه إلزاميًّا ولا تُطوى:** ① **الفئاتُ موسومةٌ لاحقًا
للحدث** (دائريّةٌ مدوَّنة في `borrow_labelled_set.md`) ② **«المتاح» يتحرّك خمسَ مراتب
يوميًّا** (ELAB «لا يوجد أسهم متاحة» بنصّه مقابل 350,000 بحصادنا) فالصالحُ **المستوى
والاتجاه** لا الرقمُ المفرد ③ **‏9 أيامٍ ليست حكمًا**، والفصلُ — إن ظهر — **ليس حافّة**:
هو أن فيصل **يختار** أسهمًا متاحُها قليل، **لا** أن قليلَ المتاح يربح.

### 🅒 سدُّ الثقب (الإجراء الوحيد الذي يمسّ الكود)
تخزينُ `shares_available` و`borrow_fee` **لحظةَ الترشيح** في سجلّ التنبيهات
(`record_new_alerts`) — وهو **موضعٌ له سابقةٌ منصوصة في الكود نفسِه**: «‏A3 (خطة
الضبط 2026-07-03): سمات التعلّم — كانت 7/10 من الصفقات المحسومة بلا `tier`/`rsi`/
`float` … كلّها متاحة في `r` وقت الإنشاء (**تتبّع فقط — لا يمسّ الفرز**)» ⇒ أُضيف
حقلين بنفس المبرّر وبنفس الطبقة.
🔒 **قيودٌ مثبَّتة:** طبقةُ **تسجيلٍ محضة** — صفرُ أثرٍ على الاختيار/الترتيب/الدخول/
الوقف/الأهداف/العضوية · **لا `LOGIC_VERSION`** · والجذورُ الأربعة عشر byte-identical ·
والحقلُ الغائب = `None` (سجلٌّ قديم) لا كذبة.

## ④ قاعدةُ القرار — **ما لا أفعله مهما كانت الأرقام**

🔒 **`SHORT_GATE_MAX` و`M13` لا تُمَسّان في هذي التجربة، ولا يُقترَح مسُّهما**:
`M13` **قرارُ مالكٍ مقفولٌ بـC3**، و🅐 كلفةٌ و🅑 وصفيٌّ **وكلاهما لا يصلح سندًا
لتغيير بوّابة**. سقفُ النجاح المسموح هنا **ثلاثةُ أشياء فقط**:
1. **رقمُ الكلفة** يُسلَّم للمالك ليقرّر (وهو الوحيد الذي يملك تغييرَ العتبة).
2. **وصفُ الفصل** يُنشَر بحدوده.
3. **‏🅒 يُنفَّذ** فيصير القياسُ الحقيقيّ ممكنًا لاحقًا.

**والقياسُ الحقيقيُّ للحافّة يُسجَّل الآن ولا يُحسَب اليوم** (‏`T-BORROW-FWD`):
بعد **‏≥60 صفقةً محسومةً تحمل «المتاح»**، تُقارَن نسبةُ النجاح والحركةُ المتاحة بين
شريحتَي «متاح ≤ 40 ألفًا» و«فوقها» بفاصل Wilson؛ **ولا حكمَ قبل بلوغ العيّنة**،
**ويُكتَب الحكمُ مرّةً واحدة** عند بلوغها أوّلَ مرّة (لا «شغّل حتى تعجبني النتيجة»).

## ⑤ تنبّؤات قابلة للكسر (قبل أيّ رقم)

- **B1:** ‏🅐 بحدّ **40 ألفًا** يسقط **‏≥7 من 10** من القائمة الحالية.
- **B2:** ‏🅐 بحدّ **20 ألفًا** (نصُّ فيصل) يسقط **‏≥9 من 10**.
- **B3:** ‏🅑 يستوفي معياره الثلاثيّ (الفصلُ يظهر).
- **B4:** **‏`LGHL` ينجو** من الحدَّين (متاحُه **صفر**) — فالبوّابةُ الجديدة **لا
  تُصلح** الحالةَ التي أثارت الملفَّ كلَّه، تمامًا كما سقطت `T-CHASE` من قبل.
- **B5:** ‏🅒 بعد التنفيذ: أوّلُ صفٍّ يحمل «المتاح» يظهر في **أوّل** تشغيلةٍ تُرشّح
  سهمًا جديدًا — **ويُثبَت بتشغيلٍ حقيقيّ لا بوجود الحقل**.

## ⑥ حدود صدق عامّة

مصدرٌ واحد (ChartExchange · بيانات IBKR كلَّ 15 دقيقة) وقاطعُ دائرةٍ قد يُصمته ⇒
«تعذّر ≠ صفر» · ولقطةٌ واحدة يوميًّا · و«المتاح» **ليس عمقَ الدفتر** · والرسومُ تغطيتُها
ضعيفة (‏6% تاريخيًّا) فلا تُخلَط · **ولا أثرَ إنتاجيًّا لهذي التجربة على الاختيار.**

---
*التنفيذ: `borrow_gate_check.py` (‏🅐+🅑) · وتعديلُ `record_new_alerts` (‏🅒).
النتيجة: `borrow_gate_result.md`.*
