# 🗓️🔍② `T-EARLY-2` — «قِس الباقي»: هل يقلب حدُّ 16:00 الثابت في أدوات البحث الأربع حكمًا منشورًا؟ — تسجيلٌ مسبق

> **أمرُ المالك «قِس الباقي» (2026-09-24 · ضمن «جميع ما سبق»)** — البندُ المفتوح في `early_close_result.md §⑥-4`.
> **مدفوعٌ قبل أيّ رقم:** لا تشغيلةَ ولا صفَّ ولا عدَّ تعرّضٍ قبل دمج هذا الملفّ. ولا يُعدَّل بعد أيّ رقم — وما يلزم
> يُضاف **ملحقًا مؤرَّخًا** لا تعديلًا.
> **قراءةٌ/قياسٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · ولا ثابتٌ حيٌّ يتغيّر بنتيجته** (يُرفع للمالك قرارًا).

---

## §⓪ السؤال ومصدرُه

`early_close_result.md §⑥-4`: «أدواتُ حدّ 16:00 الثابت خارج العقد لم تُقَس: `kasih_scan.parse_day` ·
`pm_radar_scan.parse_pre` · `liq_noise_probe.session_ref` · `event_exec.ny_session_key` — تتجاهل الإغلاقَ المبكّر
**في كلّ السنوات**». **والسؤال: هل يقلب ذلك حكمًا منشورًا؟** — لا «هل الأرقامُ تتحرّك» (تتحرّك بالبناء).

---

## §① الآليّة — من قراءة الكود قبل أيّ رقم

| الدالّة | ما يثبّت 16:00 | ما يتأثّر | اليومُ المعرَّض |
|---|---|---|---|
| `kasih_scan.parse_day` | خريطةُ «إغلاق الأمس» = آخرُ شمعةٍ ‏≤16:00 | اليومُ **التالي**: كونُ السعر · فلترُ الفجوة وسلّتُها · وما يُبنى على إغلاق الأمس | **`D1`** |
| `pm_radar_scan.parse_pre` | الخريطةُ نفسُها («النظاميّة ‏≤16:00 = إغلاقُ الأمس ليوم الغد») | اليومُ **التالي**: عتباتُ المتحرّك والزناد (`prev_close × (1+pct)`) والكون | **`D1`** |
| `liq_noise_probe.session_ref` | الجلسةُ النظاميّة [09:30، 16:00) | **اليومُ نفسُه**: شموعُ 13:00-16:00 تُعَدّ نظاميّة | **`D`** |
| `event_exec.ny_session_key` | الجلسةُ النظاميّة [09:30، 16:00) | **اليومُ نفسُه** | **`D`** |

- **الشموعُ نفسُها لا تتغيّر** في `parse_day`/`parse_pre` (‏04:00-20:00 بالبناء) — **القناةُ الوحيدة خريطةُ الإغلاق.**
- 🔴 **والخريطةُ تُحمَل — تصحيحٌ قبل الدمج:** كلُّ الأسر الثماني تُحدِّث `prev_close.update(closes)` يومًا يومًا ⇒ رمزٌ
  تداول يومَ `D` ولم تُسجَّل له شمعةٌ حتى الإغلاق يومَ `D1` **يحمل إغلاقَ `D` إلى أوّل يومٍ يتداول فيه** — فالأثرُ ليس
  «يومًا واحدًا» كما كتبتُ أوّلًا.
- ⇒ **نافذةُ التعرّض `W`** لكلّ `D` = **عشرةُ أيّام تداولٍ أوّلُها `D1`** (من `market_calendar`) — عددٌ **هندسيٌّ مُعلَن**:
  رمزٌ بلا شمعةٍ واحدةٍ من 04:00 حتى الإغلاق عشرةَ أيّامٍ متتالية ثمّ مِرساةٌ بسيولتها نادر، **وإن وقع يسقط الحارسُ
  (§⑥ `V-R3`) فيُعلَن ولا يُنسَب**.
- و`T-EVENT-EXEC` (‏`D`): `W` = جلساتُ `D` وما يحمله منها (صفقةٌ مفتوحةٌ عبره) — **وبلا صفوفٍ لا يُتحقَّق من الحصر** (§⑧).

### الأيّامُ المعرَّضة — من `market_calendar` لا باليد (‏2023-2025 · ثمانيةٌ وثمانية)

- **`D`** (أيّامُ الإغلاق المبكّر): 2023-07-03 · 2023-11-24 · 2024-07-03 · 2024-11-29 · 2024-12-24 · 2025-07-03 ·
  2025-11-28 · 2025-12-24.
- **`D1`** (يومُ التداول التالي — يتخطّى العطلات): 2023-07-05 · 2023-11-27 · 2024-07-05 · 2024-12-02 · 2024-12-26 ·
  2025-07-07 · 2025-12-01 · 2025-12-26.

---

## §② المجتمع — الأحكامُ المنشورة التي تمرّ بالدوالّ الأربع

| الأسرة | الأداة (المستدعي) | النوع | التشغيلاتُ الحاكمة (2023 · 2024 · 2025) ⟵ **شيفرتُها** (`head_sha`) | الصفوف |
|---|---|---|---|---|
| `T-KASIH` | `kasih_scan` | `D1` | 32279408237 · 32279428391 · 32279447496 ⟵ `e1e0011` | `kasih-rows-<سنة>` |
| `T-KASIH-2` | `kasih2_scan` | `D1` | 32330346732 · 32330353463 · 32330360191 ⟵ `bfb237d` | `kasih2-rows-<سنة>` |
| `T-EXIT-STOP` | `exit_stop_arms` | `D1` | 32466021172 ⟵ `afeb165` · 32470017803 ⟵ `5ce3808` · 32475169858 ⟵ `f419e60` | `exit-stop-rows-<سنة>` |
| `T-REARM` | `rearm_arms` | `D1` | 33679393353 ⟵ `9b2eaa2` · 33679406751 · 33679414398 ⟵ `0411871` | `rearm-rows-<سنة>` |
| `T-RECLAIM-INTRADAY` | `reclaim_arms` | `D1` | 32594670879 · 32594674386 · 32594677943 ⟵ `c403903` | `reclaim-rows-<سنة>` |
| `T-TARGET10` | `target10_arms` | `D1` | 32933890931 · 32933896037 · 32933901123 ⟵ `a889781` | `target10-rows-<سنة>` |
| `T-PM-CURVE` | `pm_curve_scan` | `D1` | 34038851451 (**2025 وحدَها**) ⟵ `a2d6b9a` | `pm-curve-rows` |
| `T-C-TRIGGER` | `tc_arms` | `D1` | 34083600056 (2023 · حكم) · 34083601641 (2024 · حكم) · 34078665702 (2025 · ضبط) ⟵ `c6e9d23` | `tc-arms-rows` |
| `T-EVENT-EXEC` (المصحَّحة) | `event_exec` | `D` | 30661912318 · 30661920247 · 30661925257 (على اللقطة `30598885456`) ⟵ `34b50cf` | **لا صفوفَ مرفوعة** |

- **صفرُ تعرّضٍ بالبناء — يُطبَع ولا يُعاد تشغيلُه:** `LIQ-NOISE` و`LIQ-MOVE` (‏`session_ref` على جلساتٍ حيّةٍ من
  2026-08) و`T-TIER-DAYS` (جلستا 2026-08-20/21) و**`T-PM-RADAR`** (‏`parse_pre` على `PMR_FROM=2026-08-17` ⟶
  `PMR_TO=2026-09-04`) وتشغيلةُ `T-C-TRIGGER` الوصفيّة 2026 (‏34083607102) وتشغيلاتُ اليوم الواحد (‏2026-08-17 · 08-24 ·
  08-28) — **لا إغلاقَ مبكّرًا في 2026 قبل 11-27.** والتشغيلتان المُلغاتان في `T-C-TRIGGER` خارج المجتمع.
- **خارج النطاق:** أسرةُ `presession_scan` (قاسها `T-EARLY`) · و`sym_day_probe` (تشخيصٌ بلا حكم) و`tier_days_report`
  (حكمُه 2026 وحدَها).

---

## §③ الطريقة — إعادةُ الحاكمة **على شيفرتها هي** برقعةِ علمٍ مطفأةٍ افتراضًا

- 🔴 **لماذا لا على `main`:** شيفرةُ الأسر تغيّرت منذ حاكماتها — `first_anchor` ينادي `S.liq_stage_events` الإنتاجيّة،
  وأُضيف إليها الزنادُ الموازي `T-C` في 2026-09-09 — ⇒ إعادتُها على `main` تقيس **الانجرافَ مع العلم** فلا يُسنَد فرقٌ
  إلى الحدّ. **والتشغيلاتُ على شيفرة الحاكمة تُلغي القناةَ الثانية بالبناء.**
1. **الرقعة `early_rest_patch.py`** (على `main`) تُطبَّق **وقتَ التشغيل** على شيفرة الحاكمة (‏`head_sha` تشغيلتِها) في الدوالّ
   الثلاث: `kasih_scan.parse_day` · `pm_radar_scan.parse_pre` · `event_exec.ny_session_key` — **بمرساةٍ فريدةٍ وإلّا تتوقّف
   قبل أيّ قراءة** (تحقّقتُ قبل الدمج: المرساةُ موجودةٌ **مرّةً واحدة** في الشيفرات التسع الحاكمة).
   - العلمُ **`EARLY_CLOSE_CAL`** يُقرأ **`== "1"` حرفيًّا** · **مطفأٌ ⇒ 16:00 = الشيفرةُ المنشورة سلوكًا** · مرفوعٌ ⇒
     `close_ny_min` من `market_calendar` **اليوم** (يُنسَخ مع الرقعة لأن تقويمَ الحاكمات لم يكن يعرف إغلاقاتِ 2023-2025) ·
     **ومكافئٌ لـ`presession_radar.reg_close_for` يومًا يومًا** (قفلٌ سلوكيّ) ⇒ مصدرٌ واحدٌ للحدّ معنًى.
   - و`session_ref` **لا يُمَسّ** (أسرتاه صفرُ تعرّضٍ بالبناء).
2. **لكلّ أسرةٍ وسنة:** workflow عامٌّ واحد **`early_rest_run.yml`** يأخذ شيفرةَ الحاكمة ومُدخَلاتِها (**تُقرأ من سجلّها لا من
   الذاكرة**) ‏+ `early_cal=1`، ويرفع صفوفَه.
   - و`T-EVENT-EXEC` تُشغَّل **مطفأةً ومرفوعةً معًا** (بلا صفوفٍ منشورة — §⑥ `V-R6`).
3. **المقارنة `early_rest_compare.py`:** صفوفُ المرفوعة مقابل صفوف الحاكمة (‏artifact) **داخل `W` وخارجها** · وسطرُ الحكم وكلُّ
   معيارٍ من سجلَّي التشغيلتين.

---

## §④ ما يُقاس ويُنشَر

- **`ER1` التعرّض:** صفوفُ كلّ أسرةٍ **داخل `W`** (المرفوعةُ مقابل الحاكمة) — **مضافٌ · محذوفٌ · متغيّر** — ومقامُها
  (صفوفُ السنة كلُّها) · **وصفوفُ `D1` وحدَها تُطبَع منفصلةً** (حصّةُ الحمل = الباقي).
- **`ER2` الحكم:** سطرُ حكم كلّ أسرةٍ وسنة **بحرفه** — المرفوعةُ مقابل الحاكمة (أو المطفأة في الاحتياط) — وكلُّ معيارٍ
  من معايير عقدها: **ناجحٌ/ساقطٌ كما كان؟**
- **`ER3` الثوابتُ الحيّة المشتقّة** من هذي الأحكام (ما شُحن منها — `T-REARM` · `T-TARGET10` · `T-C-TRIGGER` وغيرُها):
  هل يتغيّر الرقمُ المشتقّ؟

---

## §⑤ فروعُ الحكم — ثلاثةٌ لا غير

1. **الفرعُ 1 «لا يقلب»:** كلُّ أسرةٍ **حكمُها وكلُّ معيارٍ فيها كما نُشر** ‏+ حارسُها عابرٌ لها (`V-R3`، و`V-R6` لـ`T-EVENT-EXEC`).
   - ⇒ الأحكامُ قائمة، **ويُكتب تحت كلٍّ سطرٌ مؤرَّخ** بأرقام التعرّض.
2. **الفرعُ 2 «يقلب»:** **أسرةٌ واحدةٌ فأكثر** انقلب فيها فرعُ الحكم **أو** معيارٌ واحدٌ من ناجحٍ إلى ساقطٍ أو العكس.
   - ⇒ **تصحيحٌ مؤرَّخ** في نتيجتها · وإن كان الحكمُ قد اشتُقّ منه ثابتٌ حيّ **يُرفع للمالك ولا يُغيَّر آليًّا.**
   - وتحرّكُ رقمٍ **بلا** انقلاب معيارٍ يُطبَع «تحرّكٌ بلا انقلاب» — **وليس الفرعَ 2.**
3. **الفرعُ 3 «لا قياس»:** سقوطُ حارسٍ في أسرةٍ **ولم يُسعفه الاحتياط** (§⑥-`V-R3`) أو سقوطُ `V-R2` أو `V-R6` ⇒ تُعلَن الأسرةُ بعينها «لا قياس».
   - ويُقرأ الحكمُ العامّ **بالأضعف**: أسرةٌ «لا قياس» ⇒ لا يُقال «لا يقلب» عن المجتمع كلِّه.

⛔ **ولا يُرخى معيارٌ في أيّ أسرةٍ بعد رؤية رقم**، ولا يُعاد تعريفُ «الانقلاب».

---

## §⑥ حرّاسٌ تُطبَع في كلّ تقرير

- **`V-R1`** الأيّامُ المعرَّضة من `market_calendar` = قائمتا §① **بحذافيرها** (‏8 ‏+ 8) · ونوافذُ `W` مشتقّةٌ منها.
- **`V-R2`** كلُّ تشغيلةٍ جديدةٍ **تُطبِّق الرقعةَ بمرساتها الفريدة** وتكتمل **برمز الخروج نفسِه** الذي أخرجته الحاكمة وترفع صفوفَها.
- **`V-R3` الإسناد:** صفوفُ المرفوعة **خارج `W`** مطابقةٌ لصفوف الحاكمة **بت-بت** (الشيفرةُ نفسُها والبياناتُ ثابتة — والعلمُ
  وحدَه يختلف).
  - **وإن سقط لأسرةٍ** ⇒ **احتياطٌ مسجَّل:** تشغيلُها **بالعلم مطفأً** على الشيفرة نفسِها، والمقارنةُ حينها بين المطفأ والمرفوع
    (والحارسُ نفسُه عليهما). وإن سقط ثانيةً ⇒ الأسرةُ «لا قياس».
  - **و`T-EVENT-EXEC` بلا صفوفٍ منشورة ⇒ تدخل الاحتياطَ مباشرةً:** حارسُها `V-R6`.
- **`V-R4`** العلمُ مطفأً **بت-بت** (‏16:00) · ومرفوعًا يقرأ 13:00 يومَ الإغلاق المبكّر **ويكافئ `reg_close_for`** على كلّ يومٍ
  من 2023-2025 (قفلٌ سلوكيٌّ قبل أيّ تشغيل).
- **`V-R5`** قراءةٌ فقط · `Super_stock.py` بت-بت · والإنتاجُ لا يستورد الرقعةَ ولا أداةَ المقارنة.
- **`V-R6` (`T-EVENT-EXEC`) إعادةُ الإنتاج:** المطفأةُ تُعيد أرقامَ حكمها المنشور **بحرفها** (‏`E-REAL` بثلاث خانات والمحسومة
  لكلّ سنة — `event_exec_result_corrected.md §①`) — وإلّا «لا قياس» (لا يُسنَد فرقٌ إلى العلم في بياناتٍ لم تثبت).

---

## §⑦ ما لا يُمَسّ

- لا ثابتَ حيًّا ولا عتبةَ ولا جذرًا · ولا `LOGIC_VERSION` · **ولا نتيجةً منشورةً تُمحى** (التصحيحُ مؤرَّخٌ تحتها).
- ولا يُعدَّل عقدُ أيّ أسرة.

---

## §⑧ حدودُ صدقٍ تُقال قبل النتيجة

1. **الدوالُّ الأربع وحدَها** — حدٌّ ثابتٌ آخرُ في أداةٍ أخرى **خارج هذا العقد** ويُذكر إن صادفه القياس.
2. **المقارنةُ بصفوف الحاكمة كما رُفعت** — ما لم تحفظه الأداةُ صفًّا لا يُقارَن صفًّا (ويُقارَن بسطر الحكم).
3. **`T-EVENT-EXEC` بلا صفوفٍ مرفوعة** ⇒ مقارنتُها بالمنشور **بأرقام الحكم وحدَها** (§⑥)، ولقطتُها تنتهي **2026-10-29** ⇒
   تُشغَّل قبلها.
   - **وحصرُها لا يُتحقَّق منه صفًّا** (بلا صفوف) ⇒ يُقرأ فرقُ المرفوعة عن المطفأة **إسنادًا بالبناء** لا بحارس.
   - **وصفوفُ الحاكمات تنتهي أيضًا:** أبكرُها `kasih-rows-2023` في **2026-11-17** (‏90 يومًا) ⇒ المقارنةُ كلُّها قبله.
4. **الرقعةُ وقتَ التشغيل:** ما يُقاس = **شيفرةُ الحاكمة ‏+ العلم** لا `main` — والرقعةُ نفسُها على `main` مقفولة.
5. **معرفتي المسبقة:** قرأتُ عناوين نتائج هذه الأسر (للمجتمع)، **ولم أحسب تعرّضًا ولا صفًّا واحدًا.** وأعرف من `T-EARLY` أن
   الأثرَ هناك كان في الخانة الثالثة من أرضيةٍ واحدة.

---

## §⑨ تنبّؤاتي — تُنشَر مكذَّبةً أو مؤكَّدة

| # | التنبّؤ | ثقتي |
|---|---|---|
| `EP2-1` | **الفرعُ 1**: لا أسرةَ ينقلب فيها فرعٌ ولا معيار | عالية |
| `EP2-2` | صفوفُ الأيّام المعرَّضة تتحرّك (مضافٌ أو محذوفٌ أو متغيّر) في **أسرتين فأكثر** | متوسّطة |
| `EP2-3` | `V-R3` يعبر من أوّل مرّة في **الأسر الثماني كلِّها** (الشيفرةُ نفسُها والبياناتُ ثابتة) | عالية |
| `EP2-4` | لا ثابتَ حيًّا مشتقًّا يتغيّر | عالية |
| `EP2-5` | `V-R6` يعبر: مطفأةُ `T-EVENT-EXEC` تُعيد أرقامَ حكمها بحرفها في السنوات الثلاث | متوسّطة |

---

## §⑩ الخطوةُ التالية — بأمر المالك «قِس الباقي» نفسِه (لا أمرَ جديد)

- **الأداة:** الرقعة `early_rest_patch.py` ‏+ workflow عامّ `early_rest_run.yml` ‏+ أداةُ المقارنة `early_rest_compare.py` ‏+ أقفالٌ
  وطفرات — وما يلزم من قرارات بناءٍ يُضاف **ملحقًا مؤرَّخًا هنا قبل أيّ رقم**.
- **ثمّ التشغيلاتُ والمقارنةُ والنتيجة** `early_rest_result.md` — و`T-EVENT-EXEC` **قبل 2026-10-29**.

---

## §⑪ ملحقٌ مؤرَّخ (‏2026-09-24) — قراراتُ البناء قبل أيّ رقم

> **مدفوعٌ مع الأداة وقبل أيّ تشغيلة:** لا صفَّ ولا تعرّضَ ولا سطرَ حكمٍ جديدًا قبل دمجه. **ولا يُعدِّل شيئًا من §⓪-§⑩** —
> يُسمّي ما بُني وكيف يُشغَّل.

1. **الرقعةُ على وحدة الأسرة وحدَها** (`early_rest_patch.FAMILIES`): أسرُ `kasih_scan` الستّ ⟵ `kasih_scan.py` · `pm_curve`/`tc_arms` ⟵
   `pm_radar_scan.py` · `event_exec` ⟵ `event_exec.py` — لأن `pm_radar_scan.py` **لم تكن موجودةً** في شيفرات آب، ورقعُ الثلاث
   معًا كان سيُسقط أسرَ آب كلَّها على وحدةٍ لا تستوردها.
2. **🔴 التقويمُ يُستورَد وقتَ التحميل لا وقتَ النداء** — عيبٌ في مسوّدتي أمسكه القفلُ السلوكيّ `ERK4` قبل أيّ تشغيل: الاستيرادُ
   الكسول داخل `_early2_bound` كان يفشل **بصمت** إلى 16:00 ⇒ **المرفوعةُ مطفأةٌ وهي تُعلن غيرَ ذلك** (عَلَمٌ ميّت). الآن: مرفوعٌ بلا
   تقويمٍ ⇒ تسقط التشغيلةُ صراحةً (`ERK3`).
3. **الشيفرةُ والمُدخَلاتُ تُقرأ من التشغيلة الحاكمة نفسِها لا من يدي:** `early_rest_run.yml` يأخذ **رقمَ الحاكمة وحدَه** — منه
   `head_sha` (الـAPI) وكتلةُ `env:` من سجلّ خطوة `Run python <السكربت>` (`parse_gov_env`) **بقائمةٍ بيضاء = workflow الأسرة**
   (مقفولةٌ بـ`ERK1` على الـworkflows الحيّة، وتحقّقتُ منها في شيفرات الحاكمات الاثنتي عشرة). والناقصُ يُسقط الرقعة (`V-R2`).
4. **المقارنة** `early_rest_compare.py` ‏+ `early_rest_compare.yml` (يدويّ): `W` من التقويم (نهايةُ الأسبوع والعطلات — تتخطّى
   2025-01-09) · مجموعاتٌ متعدّدة من JSON مرتَّب المفاتيح · **صفٌّ بلا `day` يُسقط `V-R3`** لا يُنسَب خارجَ `W` بصمت · وخروجُها
   `0` عابر · `5` ساقط · `2` مُدخَلٌ ناقص. **و`ER2` يُقرأ من فرق مُخرَجَي التشغيلتين** (`--logs`: خطوةُ `Run python <سكربت>`
   في الحاكمة و`Run python "$E2_SCRIPT"` في المعادة، بلا طوابع الوقت) — **لا من ذاكرتي** — ولـ`T-EVENT-EXEC` ثلاثةُ فروقٍ:
   الحاكمة⟵المطفأة (`V-R6`) · المطفأة⟵المرفوعة (العلمُ وحدَه) · الحاكمة⟵المرفوعة.
5. **`T-EVENT-EXEC`:** مطفأةٌ ومرفوعة على `34b50cf` مع اللقطة `30598885456` · و`V-R6` يُقرأ من سجلّ المطفأة مقابل
   `event_exec_result_corrected.md §①` بحرفه.
6. **خطّةُ التشغيل — ثمانٍ وعشرون تشغيلة:**
   - **الأسرُ الثماني ذات الصفوف:** ‏22 مرفوعة (`early_cal=1`) — ستُّ أسرٍ × 3 سنوات ‏+ `pm_curve` واحدة ‏+ `tc_arms` 3.
   - **`T-EVENT-EXEC`:** ‏6 (3 مطفأة ‏+ 3 مرفوعة) **قبل 2026-10-29**.
   - **‏22 مقارنة** للصفوف والمُخرَج ‏+ **3** لـ`T-EVENT-EXEC` (المُخرَج وحدَه) · والاحتياطُ (مطفأةٌ ‏+ مقارنةٌ ثلاثيّة)
     **لمن يسقط `V-R3` وحدَه**.
7. 🔒 **أقفال `ERK1`-`ERK9`:** الأسرُ التسع والقائمةُ البيضاء = workflow الأسرة **والسكربتُ ينادي دالّةَ دخول الوحدة
   المرقوعة** (`tc_arms` يستورد الوحدتين معًا ⇒ «يستوردها» لا يكفي) · الرقعةُ نداءٌ واحدٌ داخل الدالّة المقصودة (AST) ·
   تكافؤُ `reg_close_for` على كلّ يومٍ 2023-2025 · وحداتٌ مرقوعةٌ فعلًا (المطفأةُ = غيرُ المرقوعة بت-بت) · الاستخراجُ من السجلّ ·
   الـworkflowان · وأداةُ المقارنة على صفوفٍ وسجلّاتٍ اصطناعيّة.
8. 🔴 **تصحيحُ عددٍ في §③ قبل أيّ رقم:** «الشيفرات التسع الحاكمة» **اثنتا عشرة شيفرةً مميّزة** لتسع أسر (‏`exit_stop`
   ثلاثٌ · `rearm` اثنتان · والباقي واحدةٌ لكلّ أسرة) — **والتحقّقُ شملها كلَّها** (مرساةٌ واحدة · رقعةٌ تُترجَم) ·
   وكذلك في بند الأرشيف وفي وصف #395.
