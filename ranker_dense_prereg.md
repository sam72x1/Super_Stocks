# `T-RANK-DENSE` — «رتّب المرتب» (تسجيلٌ مسبقٌ مدفوعٌ قبل أيّ سطرِ كودِ أداة)

> **أمرُ المالك (2026-08-27):** «سجّل الشمعة الانعكاسية … بعدها **رتّب المرتب**
> بعدها سجّل وقف القاع للارتكاز». هذا عقدُ الشقّ الثاني.

---

## ⓪ لماذا الآن — ولماذا هذي ليست التجربةَ الرابعة المكرّرة

المُرتِّبُ قِيس **ثلاث مرّات** وخرج «عند مستوى الصدفة» في كلّها:

| التجربة | الحكم | الفجوةُ المُعلَنة في نتيجتها |
|---|---|---|
| `T-REPLAY10` | ③ غيرُ حاسم (‏`R0−R2` ‏+0.012 · −0.074 · +0.086 ⇒ ينقلب) | — |
| `T-RANKER` | فشلت (‏1 من 5) | «‏**لا يوجد ما يُرتَّب إليه**» |
| `T-RANKER-TIE` | فشلت (‏0 من 4 في كلّ سنة) | 🔴 «‏**الأداةُ أرقُّ 50-100 مرّة من الظاهرة الحيّة، والفشلُ قائمٌ على ما قِيس ولا يُعمَّم على السؤال الحيّ**» |

🔴 **والسببُ البنيويّ مقيسٌ ومكتوبٌ في الكود لا مستنتَج:** `backtest_symbol` يمشي
بـ`BACKTEST_STEP = 5` **ويقفز `i += fwd` (‏40 جلسة) بعد كلّ صفقة** ⇒ **يستحيل
بنيويًّا أن يظهر رمزٌ في جلستين متتاليتين** ⇒ كثافةُ المرشّحين في أداة القياس
**‏1.03/جلسة (‏2023) و2.08 (‏2025)**، بينما **الجلسةُ الحيّة الواحدة تُنتج 59-109
مرشّحًا مؤهَّلًا** (تشغيلة `31127966449` ‏= 59 · و`faisal_only_live` ‏= 109).

⇒ **المُرتِّبُ لم يُختبَر قطُّ في الحالة التي يعمل فيها.** وحين يكون المرشّحُ
واحدًا في اليوم **لا يحكم الترتيبُ شيئًا** — فالثلاثةُ السابقة قاست ترتيبَ صفٍّ
من واحد. **وهذي التجربةُ تُصلح المقياسَ نفسَه أوّلًا ثم تسأل السؤال.**

📌 **وأهمّيّتُه مقيسة:** الفرزُ يؤهّل ‏59-109/يوم والسعةُ **‏15** ⇒ **المُرتِّبُ
يقرّر مَن يصل المالكَ في نحو تسعةِ أعشار الحالات** — وهو آخرُ طبقةٍ في المنظومة
لم يُثبَت لها أثرٌ موجب.

---

## ① السؤال

> على **كثافةٍ حيّة** (عشراتُ المرشّحين في الجلسة الواحدة)، هل يوجد مفتاحُ ترتيبٍ
> **مقيسٌ سلفًا** يرفع صافيَ `R` لليوم فوق مُرتِّب الإنتاج الحاليّ؟

---

## ② المجتمعُ والمحرّك — إعادةُ استعمالٍ بالاسم وبوّابةُ تطابقٍ بت-بت

- **المجتمع:** لقطاتُ PIT المجمَّدة الثلاث — 2023 `31574165523` · 2024
  `31199493957` · 2025 `31191327731` (نفسُ لقطات كلّ تجاربنا فالمقارنةُ قائمة).
- **الكونُ والإعدادات:** كما هي في الإنتاج (`FAISAL_ONLY=1` · بصمةُ الحوافّ
  `019c983e1691`) — **صفرُ عتبةٍ تُمَسّ**.
- **المِشيةُ الكثيفة:** لكلّ رمزٍ ولكلّ جلسةٍ داخل السنة **بخطوةِ 1 وبلا قفزةِ
  `fwd`** يُنادى **`analyze_ticker` الإنتاجيّ نفسُه** على `df.iloc[:i]`.
- **الحسمُ بمحرّك الإنتاج:** `_resolve_arm` **بالاسم** بالدخول/الوقف/الهدف نفسِها
  (‏`entry = متوسط الدفعات` · `stop = r["stop"][0]` · `t1 = r["t1"]` · التعبئةُ
  أوّلُ جلسةٍ `low` عندها يبلغ الدخول) — **وبوّابةُ التقسيم الوهميّ `_pit_raw_price`
  مطبَّقةٌ كما في الإنتاج**.
- **المحفظة:** `replay10.replay` **بلا تعديلِ حرف** (الأداةُ المدقَّقة التي أصلحت
  عيبَ النظر المستقبليّ `P0-01`) بسعة **‏15** = `WATCHLIST_SIZE` الحيّ.
- **بناءُ المرشّحين:** `replay10.candidates_from_trades` **بالاسم** — فلا يُخترَع
  شكلُ مرشّحٍ ثانٍ.

🔒 **و`Super_stock.py` لا يُمَسّ بحرف.** الأداةُ تُكرّر **ستّةَ أسطرِ** حسابِ
الدخول/التعبئة فقط، **ومقفولةٌ بـ`RV0`**: تُشغَّل `backtest_symbol` الإنتاجية على
عيّنةِ رموزٍ وتُطابَق صفقاتُها **حقلًا حقلًا** بما تنتجه الأداةُ عند الفهارس نفسها
— **أيُّ تفرّقٍ واحد ⇒ خروج 3 وعطبُ أداةٍ لا نتيجة** (سابقةُ `V0` في `kasih_scan`
و`CV0`-أ في `candle_arms`).

⚖️ **وميزانيةٌ واحدةٌ للأذرع بالبناء:** الأذرعُ الخمسُ تقرأ **مجرى المرشّحين
نفسَه** بنفس الكون والنافذة والسعة والسقوف — فلا يتفرّق ذراعان بأثر الميزانية
(العيبُ المقيس في `T-CLIFF`)، والقيودُ تُطبَع في المُخرَج.

---

## ③ الأذرعُ الخمس — ولا سادسة

| الذراع | المفاتيح | الدور |
|---|---|---|
| **`Q0`** | `replay10.rank_live` = مُرتِّبُ الإنتاج حرفيًّا (‏داخلُ النطاق ← جاهزية ← نقاط ← `rr` ← `seq`) | **المرجع** (لا يُرقّى) |
| **`Q1`** 🥇 | **بنيةُ الضغط أوّلًا** ثم مفاتيحُ الإنتاج حرفيًّا | **الحاكمةُ الوحيدة** |
| **`Q2`** | **القاعُ الطازج أوّلًا** ثم مفاتيحُ الإنتاج حرفيًّا | **شاهدُ تكذيب** |
| **`Q3`** | عشوائيّ (`make_rank_random`، 200 بذرة) | **شاهدُ الصدفة** |
| **`Q4`** | `rank_fifo` (الأقدمُ إشارةً أوّلًا) | شاهدُ ضبطٍ ثانٍ |

### `Q1` — ولماذا هي بالذات (صفرُ رقمٍ مخترَع)

مفتاحُها **الخليّةُ `F2` من `T-FUSE`**: قاعٌ صمد ثلاثَ جلساتٍ فأكثر **ثم كُنس**
(‏`swept_hold=True` و`hold_sessions` ثلاثٌ فأكثر) — تُقرأ بـ`press_read` +
`swept_after_hold` + `fuse_arms.cell_of` **الثلاثةِ بالاسم**، بلا عتبةٍ جديدة.

**وسندُها مقيسٌ منشور:** `F2` أعطت **‏+0.225R وموجبةً في السنوات الثلاث** — **أعلى
خليّةٍ قسناها في تاريخ المشروع**، فوق `HOLD3` (‏+0.174R).

⚖️ **وليست دائريّة:** قِيست على **حلقات الجلوس المضغوط** (مجتمعُ `press_read`)،
وتُختبَر هنا **مفتاحَ ترتيبٍ داخل مرشّحي الفارز** — مجتمعان مختلفان ومقياسان
مختلفان. **وما يُختبَر هو انتقالُها لا صحّتُها.**

### `Q2` — شاهدُ التكذيب (وهو أهمُّ ما في التصميم)

مفتاحُها **الخليّةُ `F1`**: قاعٌ طازجٌ (‏0-2 جلسة) بعد كنس — و`T-FUSE` قاست أنها
**‏−0.079R أسوأُ من بقيّة الحلقات**. ⇒ **إن تفوّقت `Q2` على `Q0` أيضًا فالمكسبُ
ليس لبنية `F2` بل لأيّ مفتاحٍ بنيويٍّ كان** ⇒ **`Q1` تسقط تفسيريًّا ولو عبرت
عدديًّا، ويُعلَن ذلك.**

⛔ **ولا تُضاف ذراعٌ بعد رؤية الأرقام** — ولا يُبدَّل مفتاحُ `Q1` ولا تُحرَّك عتبةُ
`READY_HOLD` (‏3) ولا `FRESH_MAX` (‏2).

---

## ④ المقياسُ والمعايير — مُعادةٌ بلا تحريك

**المقياسُ الحاكم:** `replay10.net_r_per_day` = **صافي `R` لليوم** عند مخاطرةٍ
ثابتة (المقياسُ المسجَّل في `T-REPLAY10` حرفيًّا). ولا يُقرأ من نسبة النجاح شيء
(درسُ `T-STOP`: النجاح 34⟶54% والتوقّعُ لم يتحرّك · و`T-LIBERATION`: نجاحٌ 87%
بتوقّعٍ سالب).

**المقياسُ المساند (يُنشَر ولا يحكم):** عددُ **المنفجرين المُسلَّمين** (‏`exploded`
داخل الخانات) و`rejected_cap` و`slot_days`.

**تُعتمَد `Q1` إن استوفت الأربعةَ معًا:**

1. **`Q1 − Q0` موجبٌ في السنوات الثلاث** بلا انقلاب.
2. **الفاصلُ 95% للفرق المجمَّع لا يلمس الصفر** (‏`cluster_bootstrap_ci` بعنقودِ
   الجلسة — الأداةُ القائمة).
3. **`Q1` فوق المئين 95 لشاهد العشوائيّ `Q3` في سنتين من ثلاث** على الأقلّ
   (قاعدةُ `T-RANKER` §⑤ حرفيًّا).
4. **الأرضية:** أربعون جلسةً فأكثر ومئةٌ وخمسون صفقةً مأخوذةً فأكثر **في كلّ سنة**
   — وإلّا **«لا حكم»** ولا يُطبَع فرق.

**والحكمُ يُكتب مرّةً واحدة** عند اكتمال السنوات الثلاث. ⛔ ولا تُعاد تشغيلةٌ
بمعيارٍ مبدَّل («شغّل حتى تعجبك النتيجة» ممنوع).

---

## ⑤ بوّاباتُ الصلاحية — تُنفَّذ في الكود لا تُوصَف

> درسُ `envelope_prereg §⑦`: «حارسُ صلاحيةٍ مكتوبٌ وغيرُ منفَّذٍ ليس حارسًا — هو نيّة».

| البوّابة | الشرط | عند السقوط |
|---|---|---|
| **`RV0`** | صفقاتُ الأداة تطابق `backtest_symbol` الإنتاجية **حقلًا حقلًا** على عيّنةِ رموز | **خروج 3** — عطبُ أداة |
| **`RV1`** | **وسيطُ المرشّحين لكلّ جلسةٍ أربعون فأكثر** (كثافةٌ حيّة) | **خروج 4** — ما زالت رقيقةً ⇒ لا حكم |
| **`RV2`** | الأذرعُ **تتفرّق**: `Q1` تأخذ صفقةً لا تأخذها `Q0` في كلّ سنة | **خروج 4** — `no-op` لا نتيجة |
| **`RV3`** | تغطيةُ الرموز **‏95% فأعلى** والمفقودُ يُسمّى بعدده | **خروج 3** |
| **`RV4`** | `Q0` يُعيد إنتاج عضويّة الإنتاج بمعيار `gate_a` القائم | يُطبَع ويُعلَن |

🔒 **وحارسُ العلم الميّت:** إن خرج جدولُ الأذرع **متطابقًا بت-بت** فهي بصمةُ
`no-op` (‏`BT_CANDLE` سابقةً) ⇒ **يُعلَن ولا يُفسَّر**.

---

## ⑥ التنبّؤات — قابلةٌ للكسر، تُنشَر مكذَّبةً كما تُنشَر محقَّقة

- **`RK-P1`** (الأثقل، ويراهن ضدّ الحاكمة): أرجّح أن تعبر `Q1` المعيارَ ① وتسقط
  في **②** (الفاصلُ يلمس الصفر) — لأن `+0.225R` قِيست على مجتمعٍ آخر، والانتقالُ
  يخفّفها.
- **`RK-P2`**: `Q2` (القاعُ الطازج) **أسوأُ من `Q0`** مجمَّعًا — مرآةُ `−0.079R`.
- **`RK-P3`**: وسيطُ المرشّحين لكلّ جلسةٍ يقع بين **‏40 و140**.
- **`RK-P4`**: `Q4` (‏FIFO) **لا يتفوّق على `Q0` مجمَّعًا** (تفوّق في 2024 وحدها
  عند الكثافة الرقيقة).
- **`RK-P5`**: `rejected_cap` يتجاوز **ثمانين بالمئة** من المرشّحين (الحيُّ ‏≈90%).
- **`RK-P6`**: نسبةُ المرشّحين الذين لهم قراءةُ ضغطٍ أصلًا **دون النصف** — وإن
  نزلت تحت **العُشر** فـ`Q1` **قريبةٌ من `no-op`** ويُعلَن ذلك قبل أيّ تفسير.

---

## ⑦ سقفُ النجاح — مثبَّتٌ سلفًا

🥇 **بأمر المالك «رتّب المرتب»: إن عبرت `Q1` المعاييرَ الأربعة فهي تُعتمَد
مباشرةً** — تُشحن في `rank_key` **مفتاحًا أوّلَ** فوق مفاتيح الإنتاج **بلا حذفِ
مفتاحٍ قائم**، مع رفع `LOGIC_VERSION` وإبلاغِ المالك بتشغيل `force_renew=1` مرّة.

⛔ **وممنوعٌ مهما كانت الأرقام:** مسُّ أيٍّ من `M1`-`M14` أو عتباتِ الظرف أو
`WATCHLIST_SIZE` أو الدخول/الوقف/الأهداف · وترقيةُ `Q2`/`Q3`/`Q4` · وإضافةُ ذراع.

🔴 **وإن لم تعبر ذراع: المُرتِّبُ يبقى كما هو ويُقال ذلك صراحةً** — «لا مُرتِّبَ
أفضلَ مقيسًا» لا «كلُّ المرتِّبات سواء».

---

## ⑧ حدودُ صدقٍ مكتوبةٌ قبل أيّ رقم

1. 🔴 **الفريمُ يوميّ حصرًا** ⇒ أقوى مميِّزاتنا المقيسة (‏`k2` والتصنيف الثلاثيّ —
   ‏19.7-22.6% مقابل 7.7-9.3% على 44,200 مِرساة) **دقائقيّةٌ فيستحيل قياسُها هنا**.
   فما يُقاس هو **أفضلُ ترتيبٍ يوميّ**، لا أفضلُ ترتيبٍ ممكن.
2. **«منفجرٌ مُسلَّم» لمسُ سعرٍ لا صفقةٌ منفَّذة** ⇒ سقفٌ متفائلٌ للأذرع بالتساوي.
3. **التعبئةُ بأمرِ حدٍّ عند متوسط الدفعات بلا انزلاق** — والانزلاقُ قِيس في
   `T-SLIP` على قناةٍ أخرى ولا يُنقَل.
4. `h4_confirm` **خاملٌ صفرٌ** حيًّا وتاريخيًّا (موثَّق منذ 2026-06-24) فلا يُمثَّل.
5. **انحيازُ البقاء** مخفَّفٌ باللقطة المجمَّدة (تشمل المشطوبين) **وغيرُ منفيّ**.
6. **تشويهُ التقسيمات** قائمٌ في السلسلة المعدَّلة (وبوّابةُ الوهميّ تخفّفه ولا تُلغيه).
7. **وعمى الجلسة الممتدّة قائم** — الشمعةُ اليومية جلسةٌ نظاميّة (‏`T-AH`).
8. **والدائريّةُ الجزئيّة مُعلَنة:** `F2` اختيرت بعد رؤية أرقام `T-FUSE`، فالمختبَرُ
   هنا **انتقالُها إلى مجتمعٍ ثانٍ** — وهو أنظفُ اختبارٍ متاح، وليس اختبارًا خارج
   العيّنة بالمعنى التامّ.
