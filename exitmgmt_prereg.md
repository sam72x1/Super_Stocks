# 🎚️🚪 `T-EXITMGMT` — «سجّل إدارة الخروج» (تسجيلٌ مسبق)

> **أمرُ المالك 2026-09-04:** «سجّل إدارة الخروج».
> **مدفوعٌ قبل أيّ رقم.** ولا تُضاف ذراعٌ ولا يُحرَّك حدٌّ بعد رؤية الأرقام.

---

## ⓪ لماذا هذي التجربة مشروعة — وهي **المحورُ الوحيد الذي فتحته `T-T1MOVE`**

نصُّ `T-T1MOVE` عند إغلاقها (‏2026-09-04): «🔓 **وشرطُ الفتح:** محورٌ جديد،
وأقربُه **إدارةُ الخروج بعد الدخول** (وقفٌ متحرّك · خروجٌ جزئيّ · هدفٌ زمنيّ) —
ولم يُقَس أيٌّ منها قطّ على مجتمع الفارز اليوميّ».

⇒ **أربعُ تجاربِ دخولٍ متتالية سقطت، وكلُّها غيّرت ما قبل الدخول:**

| التجربة | ما غيّرته | الحصيلة |
|---|---|---|
| `T-TRANCHE` | رفعُ سلّم الدفعات ‏+2% | ‏−0.199R |
| `T-LIBVOL` | دخولٌ على كسرٍ بسيولة | ‏−0.879R |
| `T-LIBVOL-2` | الكسرُ نفسُه بوقفِ قاع الكسر | ‏−0.820R |
| `T-T1MOVE` | الدخولُ نفسُه بهدفٍ متحرّك | ‏−0.130R |

**وما بعد الدخول لم يُقَس على هذا المجتمع إطلاقًا.** والمقيسُ سابقًا لا يسدّه:
`T-EXIT` قاست **أهدافًا ثابتةً أبعد** (‏+20% ⟶ −0.05..−0.25R · +100% ⟶
−0.62..−0.90R) وخلصت أن «أفضلَ هدفٍ هو المرجع» — **ولم تقس هدفًا أقرب ولا
جنيًا جزئيًّا ولا وقفًا يتحرّك** · و`T-MANAGE-25` قاست وصفةً واحدة (‏75/25 مع
تمتيعِ الربع) على **مجتمع `ARMED`** (‏143 صفقة) لا على مجتمع الفارز.

### ⓪-ب الميكانيكا المقيسة التي وجّهت التصميم (تُكتب قبل أيّ رقمِ ذراع)

من `T-T1MOVE §③` و`T-TRANCHE`: الوقفُ المشحون **هو المِرساةُ نفسُها**
(`PIVOT_STOP_AT_LOW=True`) وأدنى دفعةٍ عندها بالضبط ⇒ **المخاطرةُ ‏≈2.9% من
الدخول** · و`k = (t1 − entry) ÷ (entry − stop)` **مقيسٌ ‏≈8.7-8.9** ⇒ **الهدفُ
`t1` يقع ‏≈+25% فوق الدخول**، ونسبةُ الربح **‏10.6-12.1%** والتعبئةُ ‏85-87%
والحصيلةُ **‏−0.18..−0.26R**.

🔑 **وثلاثُ نتائجَ تتبع من هذي الأرقام وتحكم التصميم:**

1. **الرابحُ يركض 8.7R أصلًا** ⇒ محورُ «دعه يركض» **مقيسٌ ميّتٌ مرّتين**
   (`T-EXIT` و`T-MANAGE-25`) ولا يُعاد.
2. **رقمُ فيصل «‏+30%»** (`first_target_release` — «‏30٪ · تحرّر ⟸ تواصل · لم
   يتحرّر ⟸ جني ربح») **يقع فوق `t1` نفسِه** في هذا المجتمع ⇒ **لا يصلح
   مستوى جنيٍ جزئيّ هنا**، واستعمالُه كان سيُنتج ذراعًا **خامدةً بالبناء**.
   ⇒ يُعلَن ولا يُستعمَل. (وهو النظامُ الآخر: قراءتُه على حركةِ السهم من قاعه
   لا على دخولٍ مخاطرتُه ‏2.9%.)
3. **الخسارةُ أكبرُ من `−1R`**: الحسمُ بـ`min(stop, open)` والوقفُ ضيّقٌ ‏2.9%
   ⇒ فجوةُ افتتاحٍ عاديّة تعبره **بمضاعفات**. ⇒ **ولا تمنع ذلك أيُّ إدارةِ
   خروجٍ داخل اليوم** — يُكتب صراحةً قبل الأرقام.

⚠️ **وإن خرجت مطابقةً لسابقاتها فهي تكرارٌ يُعلَن لا اكتشاف** — والقيمةُ حينها
أن محورَ «ما بعد الدخول» يسقط أيضًا، فيُغلق ملفُّ الفارز اليوميّ كلُّه بالقياس.

---

## ① المجتمع والأداة

- **المجتمع:** صفقاتُ الباكتيست الإنتاجيّة نفسُها على لقطات PIT الثلاث
  (‏2023 · 2024 · 2025) بفلتر الطول **`MIN_BARS + 60` حرفيًّا** — **نفسُ ما مشت
  عليه `T-TRANCHE` و`T-T1MOVE`** فتكون النتائجُ قابلةً للمقارنة مباشرةً.
  ⇒ **المتوقَّع 4,817 زوجًا · 1,375 رمزًا** — وتطابقُهما **فحصُ تكاملٍ مجّانيّ**.
- **الأداة:** `exitmgmt_arms.py` + `exitmgmt.yml` (يدويّ · بلا كرون · قراءةٌ فقط).
- **الخطّةُ بدوالّ الإنتاج بالاسم** (`analyze_ticker` · `_anchor_mode`/
  `tested_level`) — **صفرُ منطقٍ مكرَّر في بناء الخطّة**.
- 🔴 **والمُصيِّرُ الجديد ضرورةٌ لا اختيار:** `_resolve_arm` تأخذ وقفًا واحدًا
  وهدفًا واحدًا فيستحيل أن تعبّر عن جنيٍ جزئيّ أو وقفٍ يتحرّك. ⇒ يُكتب مُصيِّرٌ
  واحد `resolve_exit`، **وبوّابةُ `V0` تشترط أن يُعيد `_resolve_arm` بت-بت عند
  إطفاء إدارة الخروج** — فلا يصير على المجتمع مقياسان.
- **الدخولُ والوقفُ والهدفُ لكلّ الأذرع = الإنتاجُ المشحون بلا تغيير**
  (عزلُ أثر الخروج عن أثر الدخول والوقف شرطُ صلاحية — درسُ `T1b`/`T-STOP`/
  `T-LIBVOL-2`) ⇒ **المتغيّرُ الوحيد: ماذا نفعل بعد أن نملك.**

## ② الأذرعُ الخمس — **مثبَّتةٌ سلفًا ولا سادسة**

**والمستوى المشترك `mid = entry + (t1 − entry) ÷ 2`** — **منتصفُ المسافة إلى
الهدف · صفرُ رقمٍ مخترَع** (يساوي `k/2 ≈ 4.35R`، ويتحرّك مع كلّ صفقةٍ بخطّتها).

| الذراع | ما يقع بعد الدخول | الهدف | الوقف |
|---|---|---|---|
| **`X0`** | لا شيء — الإنتاجُ المشحون | `t1` | ثابت | ← **بوّابةُ تكامل** |
| **`X1`** 🥇 **الحاكمة** | **نصفُ المركز يخرج عند `mid`** · والنصفُ الآخر يواصل | `t1` | ثابت |
| **`X2`** | **نصفٌ يخرج عند `entry × 1.10`** | `t1` | ثابت |
| **`X3`** | **بلا جنيٍ** — الوقفُ ينتقل إلى **التعادل** بعد بلوغ `mid` | `t1` | متحرّكٌ مرّةً واحدة |
| **`X4`** | `X1` **‏+** `X3` معًا | `t1` | متحرّك |

**تعريفاتٌ لا تُترك للتأويل:**

- **الترتيبُ داخل الشمعة محافظٌ كالإنتاج:** الوقفُ يُفحَص **أوّلًا** في كلّ
  شمعة؛ فشمعةٌ يقع فيها `low ≤ stop` **و**`high ≥ mid` تُحسم **وقفًا**.
- **حارسُ `F-L1` يسري على `mid` كما يسري على `t1`:** لا يُحسم أيٌّ منهما على
  **شمعة التعبئة** نفسِها (ترتيبُ اللمس داخلها مجهول).
- **انتقالُ الوقف إلى التعادل يبدأ من الشمعة التالية** لبلوغ `mid` (لا يُحرَّك
  وقفٌ إلى الماضي داخل الشمعة نفسِها).
- **الساقُ الباقية** تُحسم بالقواعد نفسِها، وغيرُ المحسومة عند نهاية النافذة
  تخرج بآخر إغلاق (‏= `open` في الإنتاج).
- **وقبل بلوغ `mid` الأذرعُ الخمسُ متطابقةٌ بالبناء** ⇒ الفرقُ كلُّه من
  الصفقات التي تبلغه.

**والوحدة:** `R₀ = entry − stop` **واحدةٌ لكلّ الأذرع** — وهي هنا متطابقةٌ
بالبناء لأن **الدخولَ لم يتغيّر** (بخلاف `T-TRANCHE`/`T-T1MOVE`) ⇒ محورٌ أنظفُ
من سابقيه. والعائدُ المركَّب:
`R = Σⱼ wⱼ × (exitⱼ × (1 − s/2) − entry × (1 + s/2)) ÷ (entry − stop)`.

**📒 ومصادرُ الأرقام تُوسَم قبل الاستعمال:**

| الرقم | الوسم | السند |
|---|---|---|
| `mid` = منتصفُ المسافة | **بنيويّ** | صفرُ رقمٍ مخترَع — نقطةٌ تُشتقّ من خطّة الصفقة نفسِها |
| **‏+10%** في `X2` | `engineering` (مُعادٌ بدلالةٍ مختلفة) | `LIQ_TARGET10_PCT` رقمُ المالك المقيسُ في `T-TARGET10` — **وهناك خروجٌ كامل على قناة السيولة، وهنا جنيٌ جزئيّ على الفارز** ⇒ الدلالةُ مختلفةٌ وتُعلَن |
| **النصف** (‏0.5) | `engineering` | القسمةُ المحايدة؛ **ولا تُجرَّب نسبةٌ ثانية بعد الأرقام** |
| التعادل = `entry` | **بنيويّ** | سعرُ الدخول نفسُه، لا عتبةَ جديدة |

⛔ **وما استُبعد عمدًا ويُعلَن سببُه:**
- **وقفٌ متحرّكٌ بمضاعِف ATR** — يلزمه مضاعِفٌ **مخترَع** ⇒ خارج القاعدة.
- **هدفٌ زمنيّ** — يُرجَّح أنه **خامدٌ بنيويًّا** (وقفٌ ‏2.9% في نافذةِ 40 جلسة
  ⇒ شبهُ كلِّ صفقةٍ تُحسم) · ⚠️ **ولا يُدَّعى بلا قياس**: تطبع الأداةُ حصّةَ
  `open`، فإن بلغت **‏5% فأكثر** من المُعبَّأة صار المحورُ ذا مادّةٍ **ويلزمه
  تسجيلٌ مستقلّ** (`EX-P6` أدناه).
- **قاعدةُ فيصل «‏+30%» بحرفها** — فوق `t1` في هذا المجتمع (‏§⓪-ب).

## ③ المقياسُ الحاكم

**متوسّطُ `R₀` المحقَّق لكلّ صفقة** — لا نسبةُ الربح (خدعت **خمس** مرّاتٍ
مقيسة: `T-STOP` · `T-LIBERATION` · `T-RECLAIM-INTRADAY` · `T-TRANCHE` ·
`T-T1MOVE`). والحكمُ على **`X1 − X0` مقترنًا** (نفسُ الصفقة) لا على المتوسّطَين
منفصلَين. و**`no_fill` = صفرُ عائدٍ ويدخل المقام** (‏§⑨-4 من `T-TRANCHE`).

## ④ المعاييرُ الأربعة — **تلزم معًا**

① `X1 − X0` **‏≥ +0.15R** مجمَّعًا — **رقمٌ مُعادٌ حرفيًّا** من `T-STOP` ·
`T-LIBERATION` · `T-RECLAIM-INTRADAY` · `T-PIVOT-BOTTOM` · `T-T1MOVE`
**ولا يُحرَّك بعد الأرقام**.
② **موجبُ الإشارة في السنوات الثلاث** بلا انقلاب.
③ فاصلُ ثقةٍ 95% (بوتستراب عنقودُه **الرمز** مجمَّعًا عبر السنوات · بذرةٌ
ثابتة 99991 · n=10000) **لا يلمس الصفر**.
④ أرضيةُ عيّنة: **‏150 صفقةً مجمَّعًا و30 لكلّ سنة**.

⛔ **وسقفُ النجاح مثبَّتٌ سلفًا: اقتراحُ سطرِ عرضٍ للمالك — بلا شحن.**
والجنيُ الجزئيّ **لا يمسّ `t1` ولا `rr` ولا العضويّة** (بخلاف `T-T1MOVE`)
⇒ لو أمر المالكُ بالشحن فهو **سطرُ عرضٍ** («‏🎯 جنِ نصفًا عند $X»)
⇒ **لا `LOGIC_VERSION` ولا `force_renew`**.

## ⑤ بوّاباتُ الصلاحية — رمزُ خروجٍ حقيقيٌّ لكلٍّ

- **`V0`** `resolve_exit` **مطفأةَ الإدارة** تُعيد `_resolve_arm` **بت-بت**
  (‏`outcome` والعائد) على عيّنةٍ عشوائيّةٍ كبيرةٍ ببذرةٍ ثابتة ⇒ وإلّا **خروج 3**.
- **`V1`** `X0` يُعيد أساسَ `T-TRANCHE`/`T-T1MOVE` (‏**−0.2602 · −0.1807 ·
  −0.2172**) **بت-بت** ⇒ وإلّا **خروج 3**.
- **`V2`** الأذرعُ **تتفرّق** فعلًا (`X1 ≠ X0` و`X3 ≠ X0` و`X2 ≠ X1`) ⇒ وإلّا
  **خروج 4** (بصمةُ `no-op`).
- **`V3`** **نسبةُ التعبئة متطابقةٌ في الخمس** — إدارةُ الخروج لا تمسّ التعبئة
  بالبناء ⇒ اختلافُها **عطبُ أداة** ⇒ **خروج 3**.
- **`V4`** تغطيةُ الأيام ‏≥95% والمفقودُ يُسمّى بتواريخه.
- **`V5`** السويّةُ خضراء.
- **`V6`** قراءةٌ فقط بالـAST (صفرُ إرسالٍ وصفرُ كتابةِ حالة).

🔒 **ورمزُ الخروج لا يحمل الحكم** (درسُ `T-T1MOVE` و`ignition.yml`): **صفرٌ ما
دام القياسُ سليمًا** مهما كان الحكم · و**3/4 للأعطاب وحدَها**.

⚖️ **وميزانيةٌ واحدةٌ بالبناء** (قاعدة `karpathy/autoresearch`): السنواتُ الثلاث
في **تشغيلةٍ واحدة** بنفس الكود ونفس السقوف ونفس الكون.

## ⑥ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر مكذَّبةً كانت أو محقَّقة)

- **`EX-P1`** 🥇 **الأخطرُ عليّ — وهو يختزل التجربة إلى رقمٍ واحد.**
  بفرضِ خسارةٍ عند الوقف بالضبط (‏−1R) وبلا فجوة، الفرقُ **مشتقٌّ لا مقدَّر**:

  ```
  X1 − X0 = (k/4)·(pₘ − 2·p_w)  +  ½·(pₘ − p_w)
  ```

  (الحدُّ الأوّل ثمنُ **نصفِ الرابح الكبير**، والثاني **ربحُ الساق الأولى في
  الصفقة التي تبلغ المنتصفَ ثم تخسر**) حيث `pₘ` حصّةُ مَن يبلغ `mid` و`p_w`
  حصّةُ مَن يبلغ `t1`. ⇒ **الإشارةُ موجبةٌ إن وإن فقط:**

  ```
  pₘ  >  p_w × (2k+2) ÷ (k+2)   ≈  1.81 × p_w   عند k = 8.7
  ```

  ومع `p_w ≈ 0.11` فالشرطُ **`pₘ > 0.20`** · **والبارُ (‏+0.15R) يُعبَر عند
  `pₘ ≈ 0.26`** (والاشتقاقُ **على المُعبَّأة**؛ وبضمّ `no_fill`=0R يُضرب الفرقُ
  في نسبة التعبئة ‏≈0.86 فتزحف عتبةُ البار إلى `pₘ ≈ 0.265` — والإشارةُ لا تتغيّر) ⇒ **النافذةُ بين الصفر والبار ضيّقةٌ جدًّا: `pₘ` بين 0.20
  و0.26.** ⇒ **أرجّح `X1 − X0` في ‏[+0.00 · +0.15]**: موجبٌ ولا يبلغ البار.
  📌 **وحدُّ صدقٍ في الاشتقاق نفسِه:** الخسائرُ الحقيقيّة **أسوأُ من ‏−1R**
  (فجوةٌ تعبر وقفًا بـ‏2.9% — ‏§⓪-ب) ⇒ الحدُّ الثاني يكبر بمقدار
  `½·(pₘ − p_w)·(|L| − 1)` ⇒ **الواقعُ يحابي `X1` أكثرَ من هذا الاشتقاق**،
  وعتبةُ `pₘ` الحقيقيّة **أدنى** من 0.20.
- **`EX-P2`** `pₘ` **بين 0.20 و0.30** — وهو تنبّؤٌ **مقترنٌ بـ`EX-P1`**: لو جاء
  فوق 0.26 سقط `EX-P1` وعبرت التجربةُ البار، ولو جاء دون 0.20 انقلبت الإشارة.
- **`EX-P3`** `X3 − X0` **موجبٌ وأصغرُ من ‏+0.05R** — لأن أغلبَ الخسارة تقع
  **قبل** بلوغ `mid` فلا يحميها انتقالُ الوقف.
- **`EX-P4`** `|X4 − (X1 + X3 − X0)| ≤ 0.03R` (الأثران يجمعان بلا تفاعلٍ يُذكر).
- **`EX-P5`** نسبةُ «الربح الجزئيّ» ترتفع بقوّة في `X1` **والتوقّعُ لا يتبعها**
  ⇒ **المرّةُ السادسة** التي تخدع فيها نسبةُ الربح.
- **`EX-P6`** حصّةُ `open` **دون 5%** من المُعبَّأة ⇒ الهدفُ الزمنيُّ خامدٌ
  بنيويًّا. (وإن بلغت 5% فأكثر ⇒ **تسجيلٌ مستقلّ**، ولا تُضاف ذراعٌ هنا.)

## ⑦ حدودُ صدقٍ مكتوبةٌ **قبل** الأرقام

1. **الحسمُ بلمسة ذيلٍ بلا انزلاق ⇒ يحابي الجنيَ الجزئيّ** (يكفي ذيلٌ يلمس
   `mid` وقد لا يُنفَّذ هناك حجم) ⇒ **القراءةُ متحفّظةٌ تجاه `X1`**: رقمُها
   **سقفٌ متفائل**.
2. **نصفُ المركز افتراضٌ محاسبيّ** — بلا انزلاقٍ ولا عمولةٍ ولا قيدِ حجم؛
   وفي فئتنا الرقيقة تقسيمُ الأمر ممكنٌ لكنه ليس مجّانيًّا.
3. **العضويّةُ مثبَّتةٌ بالبناء** — لا يتغيّر `t1` ولا `rr` ⇒ **لا يتبدّل مَن
   يصل المالك**، وهذا أنظفُ من `T-TRANCHE`/`T-T1MOVE` حيث كان التبدّلُ مُعلَنًا
   وغيرَ قابلٍ للتقييم.
4. **الوقفُ الضيّق (‏≈2.9%) والفجوةُ تعبره بمضاعفات** — ولا تمنع ذلك أيُّ
   إدارةِ خروجٍ داخل اليوم ⇒ **سقفُ ما تستطيعه هذي التجربة محدودٌ سلفًا**.
5. **النظامُ سالبُ التوقّع في كلّ الأذرع** ⇒ الجدولُ يقارن **خسائرَ لا أرباحًا**،
   ولا دعوى ربحية.
6. انحيازُ بقاءٍ · تشويهُ تقسيماتٍ · عمى افترٍ — كما في كلّ باكتيستاتنا.
7. **نافذةُ الحسم 40 جلسة** (`BACKTEST_FORWARD_DAYS`) ⇒ ما بعدها غيرُ مقيس.

## ⑧ ما لا يُمَسّ مهما كانت الأرقام

`M1`-`M14` · أرقامُ المالك · **الوقفُ المشحون** · **الدخولُ وصيغةُ الدفعات** ·
`t1` و`rr` والعضويّة · `WATCHLIST_SIZE` · `kasih_scan.resolve` · درعُ صيّاد
المقسّم · وقنواتُ التنبيه كلُّها.
