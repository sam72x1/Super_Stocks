# `T-RSI40` — «سقفُ RSI الحالي: نصُّ الدليل (‏40) مقابل حافّة الكاتالوج (‏71.12)»
## تسجيلٌ مسبقٌ **قبل أيّ سطرِ أداةٍ وقبل أيّ رقم**

> **أمرُ المالك:** «سجّل RSI 40» (2026-09-09) — بعد أن كشف تدقيقُ اليوم أن **دليلَ
> فيصل التعليميّ يناقض عتبةَ الفرز الحيّة**، وأن التناقضَ **في الفرز لا في التسليم**.

---

## ⓪ الحقيقةُ المقيسة أوّلًا — ما هو الحيُّ اليوم بالضبط

🔴 **وأوّلُ ما يُصحَّح: صياغتي أنا في تقرير التدقيق كانت غيرَ دقيقة.** قلتُ «أرضيتُنا
الحيّة: الآن ≤40» — **والحيُّ ليس 40**. مقروءًا من الكود والظرف المشحون (`envelope_p100.json`
· `pct=100` · `asof=2026-08-06` · 25 رمزًا):

| المفتاح | القيمة الحيّة | مصدرُها | دورُها |
|---|---|---|---|
| `RSI_OVERSOLD` | **33.0** | وسيطُ الكاتالوج (`soft_median.rsi_min`) | «المثاليّ» لقاع RSI — **نقصٌ لين** |
| `RSI_OS_HARD` | **69.0** | حافّةُ الكاتالوج (`edges.rsi_min`) | **رفضٌ صلب** على قاع RSI |
| `RSI_MAX_NOW` | **35.78** | وسيطُ الكاتالوج (`soft_median.rsi_now`) | «المثاليّ» لـRSI الآن — **نقصٌ لين** |
| **`RSI_NOW_HARD`** | **‏71.12** | حافّةُ الكاتالوج (`edges.rsi_now`) | 🔴 **بوّابةُ الرفض الفعليّة** (`:3410`) |

⇒ **التناقضُ الحقيقيُّ في الحدّ الصلب لا في اللين:** الفارزُ **يقبل** سهمًا `RSI` الآن
**‏71** ويرفض عند 71.12؛ واللينُ (‏35.78) **لا يرفض** — يُحصى نقصًا فقط.

**ونصُّ الدليل (مقروءٌ صفحةً صفحة — `FAISAL_GUIDE_NOTES.md`، خمسةُ مواضعَ متّسقة):**
- ص65: «‏RSI حول 40 يعني أن **جزءًا من الارتداد حصل بالفعل** · إذا جاء خبر والسهم فوق 40
  فقد يصعد سريعًا ثم يهبط **لأنه لم يمرّ بضغط كافٍ**».
- ص78: «يبدأ الانفجار غالبًا عندما RSI في 23-27 **لا 40 وما فوق**» · و«**ليس ارتكازًا:
  فوق 40 من البداية**».
- ص80 «متى لا تنطبق»: «‏**RSI 40+ من البداية**».
- ص81: «‏RSI 23-27 — **إذا كان 40+ فليس الإعداد المفضّل**».
- ص85: «‏RSI تشبّع **والأفضل 23-27**».

---

## ① السؤال

> على مجتمع الفرز نفسِه ومحرّكه الإنتاجيّ: **ماذا يُكلّف — وماذا يوفّر — شدُّ بوّابة
> الرفض `RSI_NOW_HARD` من حافّة الكاتالوج (‏71.12) إلى ما ينصّ عليه الدليل (‏40)؟**

---

## ⓪-ب حدودُ صدقٍ **تُكتب قبل الأرقام** — ولا تُصاغ بعدها

1. 🔴 **النقلُ نقلُنا لا نصُّ فيصل** — وهو **عينُ الدرس الذي كلّفنا اليوم مرّتين**
   (`R23` أمرَ حدٍّ · و`S-NF8` سندًا مستعارًا): نصُّه يصف **جودةَ الإعداد** («ليس
   الإعدادَ المفضّل» · «ليس ارتكازًا») **ولا يقول «ارفض عند 40»**. وتحويلُ الوصف إلى
   **بوّابةِ رفضٍ صلبة** خطوةٌ منّا ⇒ **تُقاس ولا تُفترَض**.
2. **الظرفُ ليس اجتهادًا بل قياسٌ على أسهمه:** ‏71.12 هي **أقصى ما احتمله كتالوجُ فيصل
   نفسُه** (‏P100 على 25 رمزًا) ⇒ الشدُّ إلى 40 **قد يُخرج أسهمًا من كتالوجه هو**.
   **وهذا احتمالٌ مُعلَنٌ قبل القياس، ولو وقع فهو حجّةٌ ضدّ الشدّ لا معه.**
3. **التشديدُ لا يُضيف أبدًا:** `H40 ⊆ H0` بنيويًّا ⇒ أقصى ما يُرجى **حفظُ التسليم مع
   قصّ الكلفة**. فمن يقرأ «تحسّنًا في الالتقاط» فقد أخطأ القراءة.
4. **انحيازُ البقاء** في لقطات PIT (الكونُ الناجي) — يُعلَن ولا يُعالَج هنا.
5. **المُسلَّمون ليسوا ربحًا** — لا ربحيّةَ في هذي التجربة إطلاقًا.
6. **يمسّ العضويّة** ⇒ لو عبرت، فالشحنُ يلزمه **`LOGIC_VERSION` مرفوعٌ وكلمةُ المالك**،
   ولا يقع بهذي التجربة.

---

## ② المجتمعُ والمحرّك — صفرُ مسٍّ بالإنتاج

- **لقطات PIT الثلاث** (‏2023 `31574165523` · 2024 `31199493957` · 2025 `31191327731`)
  بإعدادات الإنتاج (`FAISAL_ONLY=1`) — **نفسُ مجتمع تجارب البوّابات** (`T-BASE-475` ·
  `T-CEILING` · `T-SLOT`) فالميزانيةُ واحدةٌ والمقارنةُ مباشرة.
- **وسنةٌ خارج العيّنة 2026** على لقطةٍ تُجمَّد **قبل أيّ رقم** — تُطبَع **ولا تحكم**.
- المحرّك `backtest_symbol` **الإنتاجيّ** · والتبديلُ **عَلَمُ باكتيستٍ واحد**
  (`BT_RSI_NOW_HARD`) في `_apply_backtest_overrides` ⇒ **الإنتاجُ بت-بت بلا العَلَم**.
- `Super_stock.py` **لا تُمَسّ جذورُه**؛ والعَلَمُ يعمل في وضع `BACKTEST` حصريًّا.

---

## ③ الأذرع — أربعٌ ولا خامسة (تُثبَّت هنا؛ إضافةُ ذراعٍ بعد الأرقام ممنوعة)

| الذراع | `RSI_NOW_HARD` | لماذا |
|---|---|---|
| `H0` | **‏71.12** (الحيّ) | الأساس — والشاهدُ الذي يجب أن يُعيد المنشور |
| `H40` 🥇 **الحاكمة** | **‏40** | نصُّ الدليل الحرفيّ في خمسة مواضع |
| `H50` | **‏50** | القيمةُ الافتراضيّة قبل الظرف (`engineering`) — شاهدٌ وسيط |
| `H30` | **‏30** | **وصفيّةٌ لا تحكم**: هل الأعمقُ أفضل؟ (‏«تشبّع» ص85) |

**والباقي كلُّه ثابت:** `RSI_MAX_NOW` (اللين) · `RSI_OVERSOLD` · `RSI_OS_HARD` ·
وكلُّ بوّابةٍ أخرى **لا تُمَسّ** — فالفرقُ المقيسُ يُنسَب إلى محورٍ واحد.

---

## ④ الحكم — **قبل أيّ رقم**

**المقياسُ الحاكم:** عددُ **المنفجرين المُسلَّمين** (إشارةٌ عُبِّئت ثم بلغت ‏+100%
داخل النافذة) — نفسُ مقياس `T-SLOT`/`T-CEILING`. ويُطبَع معه سلّما ‏+30% و+50%.

- **`RS1` (الحاكم):** `H40` **لا يُنقص** المُسلَّمين عند ‏+100% في **أيّ سنةٍ** من الثلاث
  (‏فرقٌ ‏≥ 0 لكلّ سنة) **و**يقصّ **الإشارات** بـ**‏≥10%** مجمَّعًا.
- **`RS2` (الكلفة الصادقة):** يُطبَع لكلّ سنةٍ **مَن خرج بالضبط** (أسماءُ المنفجرين
  الذين أسقطهم الشدُّ) — **فالخسارةُ تُسمّى لا تُجمَّل**.
- **الأرضية:** ‏≥ **100** إشارةٍ مُعبَّأة في `H0` لكلّ سنة، وإلّا **«لا حكم»**.
- `H50`/`H30` **وصفيّان** يُطبَعان بنفس الجدول **ولا يحكمان** — إلّا أن يعبر أحدُهما
  `RS1` وحدَه فيُذكر **مرشَّحًا لتسجيلٍ لاحق لا حكمًا** (تعدّدُ مقارنات).

### بوّاباتُ الصلاحية (عطبُ أداةٍ لا نتيجة)
| | الشرط | الخروج |
|---|---|---|
| `V-R0` | `H0` يُعيد أرقامَ المنشور لسنته **بت-بت** | **3** |
| `V-R1` | `H40 ⊆ H0` إشاراتٍ (تشديدٌ لا يُضيف صفًّا) | **3** |
| `V-R2` | الأذرعُ **تتفرّق عدديًّا** عن `H0` (وإلّا `no-op` = غيابُ قياس) | **4** |
| `V-R3` | سنةُ اللقطة = سنةُ القياس | **4** |
| `V-R4` | العَلَمُ **لا ينفُذ خارج وضع BACKTEST** (يُثبَت بالكود لا بالنيّة) | **3** |

---

## ⑤ التنبّؤات — تُنشَر مكذَّبةً أو محقَّقة، ولا تُحذف

- **`RS-P1`:** `H40` **يُنقص** المُسلَّمين عند ‏+100% في سنةٍ واحدةٍ على الأقلّ ⇒ **`RS1` تسقط**.
  (‏الاحتمالُ الأسبق: الظرفُ مقيسٌ على أسهم فيصل نفسِها، فشدُّه دون حافّته يُخرج بعضَها.)
- **`RS-P2`:** القصُّ في **الإشارات** يتجاوز **‏25%** (‏أكبرُ ممّا يبدو — `RSI` الآن فوق 40
  شائعٌ في المرشّحين).
- **`RS-P3`:** `H50` **أقلُّ ضررًا** من `H40` في المُسلَّمين وأقلُّ قصًّا في الكلفة (رتابة).
- **`RS-P4`:** `H30` يُنقص المُسلَّمين في **الثلاث سنوات كلِّها** (شدٌّ مفرط).
- **`RS-P5`:** بين المُسلَّمين الذين يُسقطهم `H40` **سهمٌ واحدٌ على الأقلّ من كتالوج فيصل**
  ⇒ **حجّةٌ نصّيّةٌ ضدّ النصّ نفسِه** (وهي أثمنُ ما قد تُخرجه هذي التجربة).

---

## ⑥ سقفُ النجاح

**قياسٌ وتوصية.** لا تتحرّك عتبةٌ ولا `LOGIC_VERSION` بهذي التجربة، **والشحنُ كلمةُ
المالك**. والنتيجةُ تُدوَّن في `rsi40_result.md` **بجانب هذا العقد لا فوقه**.

⚖️ **وإن سقطت `RS1`، فالمكسبُ ليس صفرًا:** يصير عندنا **رقمٌ مقيس** يُجيب سؤالًا ظلّ
مفتوحًا في الدفتر («محور قياس») — وتُغلَق الفجوةُ بين نصّ الدليل وعتبتنا **بالدليل لا
بالسكوت**.
