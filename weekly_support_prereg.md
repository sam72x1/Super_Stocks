# 🧱📅 `T-WKSUP` — تسجيلٌ مسبقٌ: **الدعمُ الأسبوعيّ** وإيقاظُ بندِ توافق الفريمين

> **أمرُ المالك (2026-09-18): «سجل الدعم الأسبوعي».**
> **هذا العقدُ يُدفَع ويُدمَج قبل أيّ سطرِ أداةٍ وقبل أيّ رقم.** ولا يُعدَّل بعد
> الدمج — أيُّ تغييرٍ يكون **ملحقًا مؤرَّخًا** أسفله.

---

## §⓪ الدعوى ومصدرُها — حرفيًّا لا مُستنتَجًا

`X_20260905_03.png` (‏PPBT · يوميّ+أسبوعيّ · الكاتالوج `§خامس وعشرون`):

> «وغير مرشح ابدا لذلك · **الدعم الاسبوعي واليَومي يتفقان على 1.70** بمعنى فشل
> السهم تحت الرقم هذا مباشره · **دعوم الموجات نفس الشموع 1.68**»

وشارتاه `X_20260905_05.jpg` (أسبوعيّ) و`_06.jpg` (يوميّ) يعطيان الأرقام:

| الفريم | 🔴 الأحمر (دعم/وقف بمفتاح ألوان فيصل) | القمّة | القاع الظاهر |
|---|---|---|---|
| أسبوعيّ | **‏1.694** | ‏8.690 | **‏1.160** |
| يوميّ | **‏1.980 · 1.682** | — | — |

🔑 **وأثقلُ ما في المصدر أنه يكذّب أبسطَ تنفيذ:** القاعُ الظاهر على الشارت الأسبوعيّ
**‏1.160** والدعمُ الأسبوعيُّ الذي رسمه فيصل **‏1.694** ⇒ **«الدعمُ الأسبوعيّ» ليس
أدنى قاعٍ في التاريخ الأسبوعيّ**، بل **قاعُ السوينغ الأسبوعيُّ القريب**. وأيُّ
تنفيذٍ يأخذ `min` على نافذةٍ طويلةٍ **يعطي 1.160 ويكذب على الحكم**.

## §① ما هو قائمٌ وما هو غائب — مقيسٌ لا مُدَّعًى

- `support_agreement(daily_support, weekly_support, tol_pct)` **مبنيّةٌ وموصولةٌ**
  في سطر «🧭 مستويات فيصل»، وتسامحُها `FAISAL_LEVEL_TOL_PCT=2` **`faisal_verbatim`**
  (‏الدليل ص54: «دقّةُ الخطأ لا تتجاوز 2%»). و**‏1.694 مقابل 1.682 = فرقٌ 0.71%**
  ⇒ داخلَ التسامح ⇒ المصدرُ والتسامحُ متوافقان **قبل أيّ كود**.
- **ولا حقلَ `weekly_support` في المستودع** — مقيسٌ بالـAST: ستُّ نداءاتٍ لـ
  `resample_ohlc(df, "W")` (‏1525 · 2676 · 2975 · 3062 · 3801 · 3870) **ولا واحدةٌ
  منها تمسّ دعمًا أو قاعًا** (كلُّها مقاوماتٌ وأهدافٌ وفجوات) ⇒ البندُ **خامدٌ
  فعلًا**: صفرٌ من 41 سهمًا في القائمة الحيّة.
- والدعمُ اليوميُّ عندنا = `pivot_stability(lows, closes)["pivot"]` = **أدنى قاعٍ في
  آخر `PIVOT_LOOKBACK`=25 جلسة**. ⚠️ **و`PIVOT_LOOKBACK` نفسُه `faisal_inferred`
  بوسمِ «تعذّر التحقّق»** في الدفتر ⇒ **ما يُشتقّ منه لا يُوسَم فيصليًّا.**

## §② التعريفُ المُقاس — واحدٌ حاكمٌ وواحدٌ شاهد

**`W-SWING` 🥇 (الحاكم):** الدعمُ الأسبوعيُّ = `pivot_stability` **نفسُها** على شموعِ
`resample_ohlc(df, "W")` بنافذةٍ **`PIVOT_LOOKBACK_WK`**، أي **أدنى قاعٍ أسبوعيٍّ في
النافذة**. والنافذةُ **مُشتقّةٌ لا مُختارَة**: ‏25 جلسةً ÷ 5 جلساتٍ في الأسبوع =
**‏5 أسابيع**. 🔴 **ووسمُها `engineering` صريحًا — بلا ادّعاءِ سند** (‏أصلُها
`faisal_inferred` غيرُ مُتحقَّق، والاشتقاقُ منّي).

**`W-MIN` (شاهدُ ضبطٍ يُنشَر ولا يُشحَن):** أدنى قاعٍ في **كلّ** التاريخ الأسبوعيّ
المتاح — وهو التنفيذُ الساذج. **غرضُه إظهارُ الفرق**، ويُتوقَّع أن يعطي ‏≈1.160
على PPBT فيسقط.

⚖️ **ولا يُجلَب شيءٌ من الشبكة زائدًا:** `resample_ohlc` تعمل على `df` نفسِه
المُحمَّل سلفًا ⇒ **صفرُ نداءٍ إضافيّ للمزوّد**.

## §③ معيارُ القبول — مسجَّلٌ قبل أيّ رقم

| # | المعيار | الحدّ |
|---|---|---|
| `WS1` 🥇 | يُعيد `W-SWING` دعمَ فيصل الأسبوعيَّ على PPBT **as-of 2026-09-05** | داخلَ **‏2%** من **‏1.694** |
| `WS2` 🥇 | ويُحكَم بالتوافق مع الدعم اليوميّ في اليوم نفسِه | `support_agreement(...)` تُرجع `agree=True` عند مستوًى داخلَ ‏2% من **‏1.70** |
| `WS3` | ولا يكون التوافقُ **شِبهَ دائم** (وإلّا فالسطرُ ضجيجٌ لا معلومة) | حصّةُ الإطلاق على القائمة الحيّة **‏5%-70%** |
| `WS4` | والشاهدُ يفرّق فعلًا (وإلّا فالتعريفُ غيرُ ذي أثر) | `W-MIN ≠ W-SWING` في **‏≥30%** من رموز القائمة |

🔴 **وحدُّ التاريخ إلزاميّ:** القياسُ على PPBT يكون **بقصِّ البيانات عند
2026-09-05** (تاريخُ الصورة) — قراءةُ اليوم تقيس شارتًا آخر، وهو بعينه العيبُ
المدوَّن «مرجعٌ بائتٌ يكذب».

## §④ فروعُ الحكم

1. **`WS1` و`WS2` تعبران** ⇒ **يُشحَن** `weekly_support` حقلًا **عرضيًّا فقط**
   (داخلَ كتلة الإثراء المستثناة في `scan_market` ‏+ التجديدِ اليوميّ) فيستيقظ بندُ
   التوافق · وتُنشَر حصّةُ الإطلاق · وتُضاف صفوفُ الدفتر · وأقفالٌ ‏+ طفراتٌ تُسقطها.
2. **تعبر `WS1` وتسقط `WS3`** ⇒ يُشحَن الحقلُ **ويُكتَم سطرُ التوافق** حتى تُعاير
   النافذة بتسجيلٍ لاحق (‏الحقلُ نافعٌ لغيره).
3. **تسقط `WS1`** ⇒ **لا يُشحَن شيء** · ويُنشَر الفرقُ بالأرقام · **ولا تُعاير
   النافذةُ لتعبر** (مطاردةُ نتيجة) — المعايرةُ تلزمها تجربةٌ مسجَّلةٌ جديدةٌ وإذنُ المالك.

## §⑤ حرّاسٌ تُطبَع في كلّ تقرير

- `V-W1` الأداةُ **قراءةٌ فقط**: صفرُ كتابةِ حالةٍ · صفرُ إرسالِ تلغرام · بلا سرٍّ.
- `V-W2` الجذورُ الاثنا عشر **بصمتُها مطابقة** قبل الشحن وبعده، عدا `scan_market`
  **وحدَها** بكتلةِ الإثراء المستثناة بنصّها.
- `V-W3` `PIVOT_LOOKBACK` اليوميّ **لا يُمَسّ** (‏قفلٌ على قيمته 25).
- `V-W4` النافذةُ الأسبوعيّة **مُشتقّةٌ بالحساب** لا مكتوبةً بيدي (‏25 ÷ 5) ويُطبَع
  الاشتقاق.
- `V-W5` التعريفان يُقاسان على **نفس `df`** ونفس القصّ ⇒ الفرقُ فرقُ تعريفٍ لا بيانات.
- `V-W6` تُطبَع **تغطيةُ** القراءة (كم رمزًا نجح جلبُه) — و«لا يوجد» تُقال بعدَدِها.

## §⑥ حدودُ صدقٍ تُقال قبل النتيجة

1. **حالةٌ واحدةٌ موثَّقة** (‏PPBT) ⇒ عبورُ `WS1` **لا يُثبت التعريفَ عمومًا**، بل
   ينفي أنه يخالف الحالةَ الوحيدة المتاحة. **ولا يُوصَف بأنه «مُثبَت».**
2. **الرقمُ ‏1.694 مقروءٌ من شارت** (الكاتالوج) لا من نصٍّ رقميّ ⇒ فيه خطأُ قراءةٍ
   محتمَل، والتسامحُ ‏2% يستوعبه لكنه يبقى مذكورًا.
3. **التعريفُ نقلُ إطارٍ** (‏قاعدةُ اليوميّ على الأسبوعيّ) — وقاعدتُنا المُلزِمة
   `faisal-gates §②` تسمّيه نقلًا يُعلَن، لا قاعدةً فيصليّة.
4. **الأسبوعيُّ من `resample` لا من المزوّد**: حدُّ الأسبوع عندنا `"W"` (‏نهايةُ
   الأحد) وشارتُ فيصل قد يبدأ الأسبوعَ يومًا آخر ⇒ **فرقُ حدٍّ محتمَلٌ بشمعةٍ واحدة**.
5. **الشمعةُ الأسبوعيّةُ الأخيرةُ غيرُ مكتملة** داخل الأسبوع ⇒ الدعمُ قد يتحرّك
   خلاله. (وهو نفسُ سببِ كون التجديد الكامل **الجمعةَ بعد الإغلاق**.)
6. **البندُ عرضٌ فقط:** لا يدخل فرزًا ولا ترتيبًا ولا وقفًا ولا هدفًا، **ولا
   `LOGIC_VERSION`** — وأيُّ دورٍ قراريٍّ لاحقٍ يلزمه تسجيلٌ جديدٌ وإذنُ المالك.

## §⑦ تنبّؤاتي — تُنشَر مكذَّبةً أو مؤكَّدة

- `Q1` `W-SWING` **يعبر `WS1`** (‏≈1.69) — **ثقةٌ متوسّطة**: الاشتقاقُ معقولٌ لكنه
  اشتقاقي.
- `Q2` `W-MIN` **يسقط بفارقٍ كبير** (‏≈1.16 = ‏−31%) — **ثقةٌ عالية** (الشارتُ يُظهره).
- `Q3` حصّةُ الإطلاق على القائمة الحيّة **‏15%-45%** — **تخمين**.
- `Q4` `WS4` **تعبر** (التعريفان يفترقان في أغلب الرموز) — **ثقةٌ عالية**.
- `Q5` حدُّ الأسبوع (`§⑥`-4) **لا يقلب حكمَ `WS1`** — **ثقةٌ متوسّطة**.

## §⑧ ما لا يُمَسّ بهذي التجربة

الجذورُ الاثنا عشر · `PIVOT_LOOKBACK`=25 · الوقفُ والأهدافُ والعضويّةُ والترتيب ·
`FAISAL_LEVEL_TOL_PCT`=2 · سطرُ المختصرة وعقدُه (`FB13`/`FB14`) · وصيّادُ المقسّم.
**ولا تُرسَل رسالةُ تلغرامٍ من الأداة إطلاقًا.**

---

## 📎 ملحقٌ مؤرَّخ §⑦ — **قياسُ فرضيّة التقسيم** (2026-09-18، أمرُ المالك «قِس التقسيم»)

> العقدُ أعلاه **مدموجٌ ولا يُعدَّل**. هذا ملحقٌ يُثبّت **تفسيرَ النتيجة قبل
> قراءة أيّ رقم**، ولا يُغيّر معيارًا ولا يُحيي `WS1`.

### ما الذي يُقاس ولماذا
نتيجةُ `§①` أعطت `W-SWING` = **‏1.16** حيث رسم فيصلٌ **‏1.694**، **والدعمُ اليوميُّ
الإنتاجيُّ أعطى ‏1.16 أيضًا** حيث رسم **‏1.682**. وأُعلن في `weekly_support_result.md`
تفسيرٌ بديلٌ **لم يُقَس**: أن يكون الفرقُ **فرقَ مقياسٍ** (تسويةُ تقسيم) لا خطأَ
تعريف. **هذا الملحقُ يقيسه ولا شيءَ غيرَه.**

### المقاييس الثلاثة
- **`D1` أحداثُ التقسيم:** كلُّ تقسيمٍ لـ`PPBT` بتاريخه ونسبته من المصدر نفسِه
  (`_fetch_splits` الإنتاجيّة — **لا يُعاد كتابةُ الجلب**).
- **`D2` العاملُ المطلوب:** النسبةُ التي تُصالح الرقمين = **‏1.694 ÷ 1.16** —
  **تُحسَب ولا تُكتَب بيدي** — ومثلُها لليوميّ (‏1.682 ÷ 1.16). ويُقارَن بحاصل
  ضربِ نِسَب التقسيمات الواقعة **بعد `2026-09-05`** (‏`_split_scale_factor`
  الإنتاجيّة) بتسامح **‏2%** (‏`FAISAL_LEVEL_TOL_PCT`).
- **`D3` شكلُ القاع:** شمعةُ القاع ‏1.16 وجاراتُها — **هل هو ذيلٌ لحظيّ**
  (‏الإغلاقُ أعلى منه كثيرًا) أم **مستوى تماسك**؟ وكم جلسةً أغلقت تحت ‏1.694؟

### الفروعُ — مكتوبةٌ قبل الرقم
1. **يوجد تقسيمٌ بعد 2026-09-05 وعاملُه يطابق `D2` داخلَ 2%** ⇒ **الرقمان مقياسان
   مختلفان لا خطأَ تعريف** ⇒ يُعاد فتحُ المحور **بتسجيلٍ جديد** يشترط **القياسَ
   على بياناتٍ غيرِ معدَّلة** — ولا يُشحَن شيءٌ بهذا الملحق.
2. **لا تقسيمَ بعد التاريخ (أو لا يطابق)** ⇒ **التفسيرُ البديلُ يسقط** ⇒ **فرضيّةُ
   «عطبِ التعريف» تبقى قائمةً بلا منازعٍ معروف** — ويبقى الحكمُ الفرعَ 3.
3. **تعذّر الجلب** ⇒ **«لا قياس»** يُعلَن بعدَده ولا يُفسَّر.

### حدودُ صدقٍ تُقال قبل الرقم
- **الفرعُ 2 لا يُثبت عطبَ التعريف** — ينفي **منازعًا واحدًا** فقط.
- **`D3` وصفيٌّ** ولا يحكم: شكلُ القاع **قرينةٌ** على أن فيصلًا يرسم عند التماسك
  لا عند الذيل، **وليس دليلًا** — فحالةٌ واحدةٌ لا تُعمَّم (‏`§⑥`-1 قائم).
- **ولا يُعاد حسابُ `WS1`** بأيّ عاملٍ هنا: تصحيحُ مقياسٍ بأثرٍ رجعيٍّ بعد رؤية
  النتيجة **مطاردةُ نتيجة** بنصّ `§④`.
