# 🔬 M4-ISO — التسجيل المسبق (يُكتب ويُدفع **قبل قراءة أي رقم**)

> **الحالة عند الكتابة: التشغيلات أُطلقت ولم أفتح سجلّ أيٍّ منها.** المعيار يُثبَّت الآن.
> **اللقطة:** `30598885456` · **التشغيلات:** `30622461125` · `30622462625` · `30622464358`.
> 🔒 باكتيست/بحث — صفر مسّ إنتاج · لا `LOGIC_VERSION`.

---

## ① السؤال — **معزولًا هذي المرّة**

`C5+` أعطى أفضل أرقامنا، لكنه **يحزم ثلاثة تغييرات معًا**: فتح `M4` ‏+ إسقاط الحواجز
التسع المشتقّة ‏+ إسقاط `M13`/`M14`. ⇒ **لا يجوز نسبُ المكسب إلى `M4`.**

> **المُختبَر:** فتحُ `M4` **وحده** (`BASE_RANGE_MAX_PCT` ⟶ بلا حدّ عمليًّا)، **وكل شيءٍ
> آخر يبقى كما هو في الإنتاج اليوم** — بما فيه الحواجز التسع و`M13`/`M14` وأرضية RSI.

**الأذرع:** `F0` (الإنتاج · تشغيلات قائمة `30616888750` · `30616894472` · `30616901408`)
مقابل **`M4-OFF`**. **الوضع الحاكم: المحفظة (‏top-15)** — لأن الحكم على **المُسلَّم**.
**السنوات: 2023 · 2024 · 2025.**

---

## ② المقياس — مثبَّت الآن

**الأساسيّ:** نسبة بلوغ **+50% قبل الوقف** داخل **محفظة top-15** (`mg_pre_stop`).
**المساند:** العدد المطلق للمنفجرين المُسلَّمين · وتوقّع R عند هدف +20% (خام).
**وليس `win_rate`** (دروس T-STOP وT-LIBERATION).

---

## ③ المعيار — **معيار تحسّن، لا معيار عدم-تدهور**

🔴 **وهذا فرقٌ مقصود عن `core5_prereg`:** هناك كان السؤال «هل الأربع عشرة تستحقّ
كلفتها؟» فكفى **عدم التدهور**. وهنا السؤال **«هل الإلغاء يُحسّن؟»** — وادّعاء التحسّن
يحتاج بارًا أعلى.

**يُقترَح إلغاء `M4` على المالك إن استوفى الثلاثة معًا:**
1. **تحسّنٌ في السنوات الثلاث كلّها** (‏`mg_pre_stop` المحفظة موجب الفرق) — **لا انقلاب**.
2. **الفرق المجمَّع ‏≥1.5 نقطة** (‏≈ منفجرَين إضافيَّين في السنة على قاعدة ‏~8.75%
   و‏~90-135 صفقة مُسلَّمة/سنة = أثرٌ يُلمَس لا ضجيج).
3. **‏≥30 صفقة فارقة/سنة.**

**ويُرفَض الاقتراح** إن سقط أيُّ شرط — **ولا يُعاد تفسير الرقم بعديًّا** بحجّة الضجيج
أو العيّنة. ⚠️ **وحالة SPRC لا تُحتَسب في الحكم**: هي **دليل وجود** لحالةٍ محجوبة، لا
**دليل معدَّل** — ولا تُستعمل لترجيح كفّة.

---

## ④ التنبّؤات القابلة للكسر

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| **N1** | عدد الإشارات يرتفع ارتفاعًا كبيرًا (‏≥×1.5) — `M4` ‏49% من الرفض الأول | ألّا يرتفع ⇒ فالحواجز التالية تلتقط مَن يمرّ |
| **N2** | **التحسّن لن يستوفي المعيار** — لأن `C5+` نفسه (الأقوى) انقلب في 2025 بـ‏−0.42 | استيفاؤه ⇒ **أول تعديل أساسٍ مسنودٍ في تاريخ المشروع** |
| **N3** | الأثر أصغر من أثر `C5+` — لأن الحواجز التسع تبقى فتلتقط الداخلين الجدد | أن يكون أكبر ⇒ `M4` هو المحرّك وحده |

🔴 **وسجلّي اليوم يجعل الكسر واردًا:** ‏3 من 5 تنبّؤات كُذِّبت في T-CORE5، وحكمُ وضعٍ
واحد كان سيخرج مقلوبًا لولا تثبيت الوضعين.

---

## ⑤ سقف النجاح — مثبَّت الآن

**أرقامٌ ‏+ اقتراحٌ واحدٌ للمالك.** إلغاء `M4` يمسّ **العضوية** ⇒ **قرار مالكٍ حصريّ**
مع `LOGIC_VERSION` و`force_renew=1`. **ولا يُمَسّ في هذي التجربة:** `M1` · `M2` · `M3` ·
`M5` · أرضية RSI · `M13` · `M14` · الوقف · الأهداف · الدخول.

## ⑥ حدود الصدق (تُعلَن قبل النتيجة)

1. **انحياز البقاء** · **تشويه التقسيمات** · **الشمعة اليومية بلا افتر** (أرضيات لا أسقف).
2. **المحفظة محاكاة**، وترتيبُها من `rank_key` **المقيس غير مميِّز** ⇒ نتائجها تحمل
   تذبذبًا حقيقيًّا (‏`core5_result` §④). ⇒ **حتى النجاح هنا لا يعني «حافة»، بل «بوّابة
   كانت تحجب بلا مقابل».**
3. **`M4` قد يكون درعًا في سياقٍ لم نقسه** (‏T-GATES §⑤: القاعدة ‏7,930% = انهيارٌ ‏−99%
   حقيقيّ، و`M4` يؤدّي وظيفته هناك) ⇒ **الإلغاء الكامل ليس الخيار الوحيد؛ قد يكون
   الصواب حدًّا أعلى بكثير بدل الإلغاء** — وهذا يُقاس في تجربةٍ لاحقة لا هنا.
