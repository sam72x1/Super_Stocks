# ⚖️🧱✅ `T-SPLITNORM` — النتيجة: **الفرعُ 1 · التسويةُ تُعتمَد مقياسًا لهذا المحور وحدَه**

> **العقد:** `splitnorm_prereg.md` (مدموجٌ `fe2c9bb1` **قبل** أيّ سطرِ أداةٍ وأيّ رقم).
> **الأداة:** التسويةُ داخل `support_def_probe.py` خلف `SUPDEF_NORM` (مدموجةٌ `e6d5bb23`
> ‏⟶ `a0bba6da` ‏⟶ `b94d7c5a`).
> **التشغيلةُ الحاكمة:** `35470642402` على `main` (`b94d7c5a`) · خروج **‏10** = *الفرعُ 1* لا عطب.
> **قراءةٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · ولم يُشحَن شيء ·
> و`pivot_stability` لم تُمَسّ.**

---

## ① الحرّاس — كلُّها عبرت، واثنان منها أدّيا عملَهما قبل ذلك

| الحارس | المُخرَج |
|---|---|
| **`V-N1`** الخامُّ ≡ المنشور | ✅ **بت-بت**: `{below: 6, n: 9, p: 0.5078, med: 31.03, rand: 50.83}` = أرقامُ `35455722208` حرفيًّا |
| **`V-N3`** قسمةٌ لا ضرب | ✅ هُويّةٌ عند ‏1 · والقسمةُ صحيحة · **وليست ضربًا** |
| **`V-N4`** اشتقاقُ `§③` مقابل المحسوب | ✅ `NEXR` ‏1.7120 ÷ 0.0909091 = **‏18.8320** (اشتقاقي ‏18.832 · فرق **‏0.000%**) · `NXTT` ‏5.8300 ÷ 0.01 = **‏583.0000** (اشتقاقي ‏583.0 · فرق **‏0.000%**) |

🔑 **و`V-N4` يُثبت أن اشتقاقي اليدويَّ في `§③` كان صحيحًا تمامًا حيث ينطبق** —
وهو الغرضُ من كتابته قبل التشغيل: ليُدقَّق عليّ لا ليُصدَّق.

## ② المعايير

| المعيار | المقيس | الحكم |
|---|---|---|
| **`SN1`** الحياد | كلُّ صفٍّ عاملُه ‏1 يطابق نظيرَه الخامَّ — **المختلف: لا شيء** | ✅ |
| **`SN2`** توسُّعُ المجتمع | حاكمةٌ خامًّا **‏9** · مُسوّاةً **‏8** ⇒ **دخل ‏0** · **خرج ‏1** (`NXTT 2026-01-22`) | (أعمى · نُشر) |
| **`SN3`** صفوفُ الكاتالوج | ‏2 صفّ · **منها بعاملٍ ‏≠ 1: صفر** | (أعمى · نُشر) |
| الشواذّ | سقفُ صفوف العامل ‏1 = **‏76.69%** · والباقي خارجَه: **لا شيء** | ✅ |

⇒ **الفرعُ 1** بنصّ `§⑤`: `SN1` ✅ **و**كلُّ صفٍّ `sf ≠ 1` صار خطؤُه **داخلَ مدى
أخطاء صفوف `sf = 1`** (‏`NEXR` ‏+0.47% مقابل سقفٍ ‏76.69%) ⇒ **لم يبقَ شاذٌّ
بمرتبةٍ عشريّة** — وقد كان ‏+1005% و+9849% قبل التسوية.

## ③ 🔴🔴 أثقلُ ما في التشغيلة — واكتشافٌ لم أتوقّعه

**مستوى `NXTT` المسجَّل ‏5.830، مُسوًّى = ‏583.0 = سعرُ المرجع بالضبط.**

⇒ قاعدةُ **وسم مؤشّر السعر** (`§⑩-ⓔ`، تسامحُها ‏0.25%) تُسقطه ⇒ **الصفُّ ليس
«مستوى دعمٍ» أصلًا بل مؤشّرَ السعر على الشارت.**

🔑 **ولا تراه إلّا بعد التسوية:** على المقياس الخامّ يبدو المستوى ‏5.830 بعيدًا
عن المرجع ‏583.000 بمرتبتين، فتمرّ قاعدةُ الوسم عليه **ولا تراه** ⇒ **فارقُ
المقياس كان يُخفي صفًّا مُساءَ التصنيف.**

⇒ **والحكمُ المنشورُ سابقًا (`SD1` ‏6/9) كان يحتسب صفًّا ليس دعمًا** — وهذا
اكتشافٌ **عن القياس السابق** لا عن تعريف الدعم.

## ④ 🔴 الخطأُ الرابعُ في عقدي — ويُنشَر كما نُشر الثلاثة قبله

`§③` اشتقّ أن المجتمعَ المُسوّى سيقرأ **‏7/9 = ‏77.78% · `p = 0.1797`**.
**والمقيسُ: ‏6/8 = ‏75.0% · `p = 0.2891`.**

**السبب:** اشتقاقي افترض أن `NXTT` **يبقى** في المجتمع وينقلب إلى «أدنى»
(‏580.0 < 583.0) — **ولم أتوقّع أن قاعدةَ وسم مؤشّر السعر تُسقطه أصلًا**.
⇒ **تفاعلُ التسوية مع `§⑩-ⓔ` كان خارجَ حسباني**، وهو بعينه ما يجعل الاختبارَ
نافعًا رغم التلوّث: **الجزءُ الذي لم أشتقّه هو الذي علّمني**.

🔒 **ولم يُغيَّر حدٌّ ولا معيارٌ بعد رؤية هذا** — `SD1` المُسوّى **وصفيٌّ بنصّ
`§③`** ولا يدخل أيَّ فرع.

## ⑤ التنبّؤات — تُنشَر مكذَّبةً أو مؤكَّدة

| # | التنبّؤ | ثقتي المُعلَنة | النتيجة |
|---|---|---|---|
| `PN1` | `SN1` الحياد يعبر | عالية | ✅ **مؤكَّد** |
| `PN2` | `SN2` = صفرُ صفٍّ **جديد** يدخل | متوسّطة | ✅ **مؤكَّد** (دخل ‏0) — 🔴 **ولم أتوقّع خروجَ صفّ** |
| `PN3` | `SN3` = صفرُ صفِّ كاتالوجٍ بعاملٍ ‏≠ 1 | **منخفضة** | ✅ **مؤكَّد** — والأثرُ أن الحكمَ السابق **لم يكن ملوَّثًا بمقياس الكاتالوج** |
| `PN4` | `SD1` المُسوّى = ‏7/9 · `p = 0.1797` | **مشتقٌّ — لا يُحسَب** | 🔴 **مكذَّب** (‏6/8 · `0.2891`) — `§④` |
| `PN5` | وسيطُ `SD2` ينخفض عن ‏31.03% | **مشتقٌّ جزئيًّا — لا يُحسَب** | ✅ (‏**‏16.53%**) |

⇒ **الثلاثةُ الحاكمة أُصيبت، والمشتقُّ الوحيدُ الذي حسبتُه بثقةٍ كُذِّب** —
ويُقالان معًا.

## ⑥ الجدولُ المُسوّى — وصفيٌّ موسومٌ بحالة العمى (`§④`-4)

| الرمز | التاريخ | الأصل | العامل | فيصل-خام | فيصل-مُسوًّى | `A0` | خطأ% |
|---|---|---|---|---|---|---|---|
| `PPBT` | ‏2026-09-05 | cat | ‏1 | ‏1.682 | ‏1.682 | ‏1.16 | **‏−31.03** |
| `SXTC` | ‏2026-09-18 | cat | ‏1 | ‏2.064 | ‏2.064 | ‏1.78 | **‏−13.76** |
| `NEXR` | ‏2026-04-01 | indicator | **‏0.0909091** | ‏1.712 | **‏18.832** | ‏18.92 | **‏+0.47** |
| `PRFX` | ‏2026-05-29 | indicator | ‏1 | ‏2.157 | ‏2.157 | ‏1.28 | **‏−40.66** |
| `GRI` | ‏2026-04-10 | price | ‏1 | ‏2.100 | ‏2.100 | ‏2.115 | **‏+0.71** |
| `TRUG` | ‏2026-03-27 | price | ‏1 | ‏3.967 | ‏3.967 | ‏3.5 | **‏−11.77** |
| `TURB` | ‏2026-04-01 | price | ‏1 | ‏2.819 | ‏2.819 | ‏0.657 | **‏−76.69** |
| `VEEE` | ‏2026-05-13 | price | ‏1 | ‏5.990 | ‏5.990 | ‏5.0 | **‏−16.53** |

**وصفيًّا (لا يحكم):** `SD1` مُسوًّى ‏6/8 = ‏75.0% `p=0.2891` · `SD2` **‏16.53%** ·
`SD3` **‏50.83%** (أسوأُ من `A0` ✅) · `SD1-NEW` ‏50.0% `p=1.0`.

## ⑦ ما يعنيه هذا الحكم — وما **لا** يعنيه

**يعني:**
- **التسويةُ حياديّةٌ مُثبَتة**: صفرُ تغييرٍ في صفوف العامل ‏1 — فهي لا تُحرّك
  رقمًا حيث لا تقسيم.
- **وتُصلح المقياسَ حيث يوجد تقسيم**: الشاذُّ الوحيدُ الباقي (‏`NEXR` ‏+0.47%)
  صار **داخلَ مدى** بقيّة الصفوف بدل ‏+1005%.
- **وتكشف صفًّا مُساءَ التصنيف** (`NXTT`) لم يكن يُرى على المقياس الخامّ.
- ⇒ **المقياسُ المُسوّى هو المقياسُ الصحيح لهذا المحور** إن فُتح يومًا.

**لا يعني:**
- ⛔ **لا يُثبت انحيازَ تعريف الدعم** — `SD1` بقيت ساقطةً (‏75% دون ‏80% و`p`
  فوق ‏0.10)، كما أُعلن في `§③` **قبل** التشغيل.
- ⛔ **ولا يُشحَن منه شيء**: صفرُ حقلٍ · صفرُ مفتاحٍ في `CONFIG` · صفرُ سطرِ
  عرض · ولا `LOGIC_VERSION` · و`pivot_stability` لم تُمَسّ.
- ⛔ **ولا يفتح `T-SUPDEF`**: الحارسُ `SUPDEF_REOPEN` باقٍ فوق التسوية ولم
  ترفعه، والمحورُ **مُغلَقٌ** وشروطُ فتحه الثلاثة كما هي.

## ⑧ حدودُ صدقٍ تُقرأ مع كلّ رقمٍ أعلاه

1. **العيّنةُ ثمانيةُ صفوف** من ثمانية رموز — و`sign_test_p(8,8) = 0.0078`
   فالعبورُ ممكنٌ بنيويًّا عند الإجماع، **وهذا وصفيٌّ ولا يُغري بمطاردته**.
2. **وصفٌّ واحدٌ فقط** في المجتمع عاملُه ‏≠ 1 بعد خروج `NXTT` ⇒ **شرطُ الشواذّ
   في الفرع 1 حُسم بصفٍّ واحد** — فالحكمُ «التسويةُ تُعتمَد مقياسًا» **يستند
   إلى حالةٍ واحدةٍ مُصحَّحة ‏+ حيادٍ مُثبَتٍ على سبع**، لا إلى إحصاءٍ عريض.
3. **التسويةُ لا تُصلح ما لا تقسيمَ فيه** — مقيسٌ اليومَ على `PPBT`
   (`weekly_support_result.md` ملحق `§⑦`: صفرُ تقسيمٍ بعد تاريخ الشارت،
   والعاملُ المطلوبُ ‏1.46 ليس نسبةَ تقسيمٍ أصلًا).
4. **ثغرةُ `§⑤` المُعلَنة** («`SN1` ✅ والشواذُّ باقية») **لم تُستدعَ** في هذي
   التشغيلة — تبقى مُعلَنةً لمن يشغّلها لاحقًا.
5. **و`SD1` المُسوّى مشتقٌّ سلفًا في `§③`** فلا يُقرأ حكمًا ولا تنبّؤًا.

## ⑨ سجلُّ التشغيلات — ويُنشَر كاملًا

| # | التشغيلة | الخروج | ماذا حدث |
|---|---|---|---|
| ‏1 | `35464551419` | **‏13** | **`V-N1` سقط وأوقف قبل أيّ حكم** — النداءُ الخامُّ كان يقرأ العلمَ فيُسوَّى (عيبي) |
| ‏2 | `35466292668` | **‏10** | الحكمُ صدر · **لكنّ سطرَ `SN2` طبع «خرج ‏0» وصفٌّ قد خرج** (عيبي) ⇒ لا يُنشَر رقمٌ من سطرٍ يكذب |
| **‏3** | **`35470642402`** | **‏10** | **الحاكمة** — كلُّ الحرّاس ✅ و`SN2` صادق |

🧭 **والدرسُ: حارسٌ سقط في أوّل تشغيلةٍ ليس عطلًا — هو الفرقُ بين رقمٍ يُنشَر
ورقمٍ يُصدَّق.** ولولا `V-N1` لنُشر «‏6/8 · `p=0.2891`» **على أنه المسارُ
الخامّ** وهو مُسوًّى.
