# 🧱🔬 `T-SUPDEF` — تسجيلٌ مسبق: **هل «أدنى قاعٍ في النافذة» هو دعمُ فيصل؟**

> **أمرُ المالك (2026-09-18): «سجّل تعريف الدعم».**
> **يُدفَع ويُدمَج قبل أيّ سطرِ أداةٍ وقبل أيّ رقم** — ولا يُعدَّل بعد الدمج
> (التغييرُ **ملحقٌ مؤرَّخ** أسفله).
> 🔴 **وهذا المحورُ يمسّ جذرًا** (`pivot_stability` ⟶ الوقفُ والدفعاتُ والأهداف)
> ⇒ **لا يُشحَن منه شيءٌ بهذا العقد مهما كانت النتيجة.** أقصى ما يُنتجه: **رقمٌ
> يُبرّر تسجيلًا لاحقًا** أو **إغلاقُ المحور**.

---

## §⓪ من أين جاء السؤال

حكمُ `T-WKSUP` (‏`weekly_support_result.md`) أخرج سطرًا لم يكن مقصودًا: على
`PPBT` as-of 2026-09-05 أعطى **الدعمُ اليوميُّ الإنتاجيُّ ‏1.16** حيث رسم فيصلٌ
**‏1.682**، وأعطى الأسبوعيُّ ‏1.16 حيث رسم **‏1.694**.
وملحقُ `§⑦` **أسقط المنازعَ الوحيد**: لا تقسيمَ بعد تاريخ الصورة (‏العاملُ
المطلوب ‏1.4603 وحاصلُ الضرب ‏1.0) ⇒ **المقياسُ واحدٌ والفرقُ فرقُ تعريف**.
وقرينةُ `D3`: قاعُنا ‏1.16 **ذيلٌ لحظيّ** (إغلاقُه ‏1.41 = أعلى بـ‏21.6%).

⇒ **الدعوى المقيسة:** تعريفُنا **يقع تحت** ما يرسمه فيصل **بانتظام**، لأننا نأخذ
**أدنى ذيلٍ** وهو يرسم **مستوًى بنيويًّا**.

## §① الحالةُ الوحيدةُ المتاحة — والجدوى **مقيسةٌ قبل العقد**

- في `FAISAL_IMAGES_CATALOG.md` **‏42 مستوًى أحمرَ برقمٍ** في **‏40 صفًّا**.
- ومنها **‏6 صفوفٍ فقط في دفعاتٍ مؤرَّخةٍ باسم الملفّ** (‏`X_2026…` · `CH_…` ·
  `APP_…` · `WA_…`): **PPBT · DCOY · ELPW · NUWE · DAIC · SXTC**.
- والباقي `IMG_xxxx` **بلا تاريخٍ في المستودع** — **وتأريخُه من رقم الصورة
  استنتاجٌ غيرُ سليم ولا يُفعَل**.

🔴 **وهذا سقفٌ صارمٌ يُعلَن قبل أيّ رقم:** العيّنةُ **أحاديّةُ الرقم**، فلا تحتمل
**اختيارَ تعريفٍ فائزٍ** من بين بدائل (ذلك **إفراطُ ملاءمة** بيقين). ⇒ **العقدُ
وصفيٌّ بالتصميم:** يقيس **انحيازَ التعريف القائم** ولا يُرشّح بديلًا.

## §② ما يُقاس

**`A0` 🥇 (الحاكم — التعريفُ الإنتاجيّ):** `pivot_stability(lows, closes)["pivot"]`
= أدنى قاعٍ في آخر `PIVOT_LOOKBACK`=25 جلسة، **as-of تاريخِ الصورة**.

**وصفيّان يُنشَران ولا يُرشَّحان** (‏سابقةُ `C-MOM` و`W-MIN`):
- **`A1`** أدنى **إغلاقٍ** في النافذة نفسِها (يتجاهل الذيول).
- **`A2`** «أرضيةُ التماسك»: أدنى قاعٍ **لُمس أكثرَ من مرّة** (قاعٌ آخرُ ضمن
  `FAISAL_LEVEL_TOL_PCT`=2% منه في النافذة) — وإلّا فالأدنى التالي.

**وشاهدُ ضبطٍ `A-FAR`:** أدنى قاعٍ في **‏120** جلسة — تعريفٌ **معلومُ الخطأ**
غرضُه إثباتُ أن المقياسَ يفرّق أصلًا. **سقوطُه شرطُ صحّةٍ للقياس.**

**قاعدةُ القراءة (تُثبَّت الآن):** حين يحمل الشارتُ عدّةَ مستوياتٍ حمراء
⟵ **المعنيُّ هو الأقربُ تحتَ سعرِ الإغلاق يومَ الصورة** (هو الذي «كسرُه فشل»).
وإن كانت كلُّها فوق السعر ⟶ **الصفُّ يُستبعَد ويُعلَن استبعادُه بعدَده.**

## §③ المعايير — قبل أيّ رقم

| # | المعيار | الحدّ |
|---|---|---|
| `SD0` | أرضيّةُ العيّنة | **‏≥5** ثلاثيّات (رمز · تاريخ · مستوى) من **‏≥3 رموزٍ متمايزة** |
| `SD1` 🥇 | **انحيازُ الإشارة**: `A0` **أدنى** من مستوى فيصل | في **≥‏80%** من الثلاثيّات · واختبارُ إشارةٍ ثنائيُّ الذيل `p ≤ 0.10` |
| `SD2` 🥇 | **ماديّةُ الانحياز**: وسيطُ \|الخطأ\| النسبيّ لـ`A0` | **‏≥10%** (‏وإلّا فالفرقُ تجميليٌّ لا بنيويّ) |
| `SD3` | **صحّةُ المقياس**: `A-FAR` أسوأُ من `A0` | وسيطُ \|خطئه\| **أكبر** |

## §④ الفروع

1. **`SD1` و`SD2` و`SD3` تعبر** ⇒ **الانحيازُ مقيسٌ ومادّيّ** ⇒ **يُنشَر رقمًا**،
   **ويُقترَح** تسجيلٌ لاحقٌ بميزانيّةٍ أوسعَ يفحص تعريفًا بديلًا — **ولا يُمَسّ
   `pivot_stability` بهذا العقد.**
2. **`SD1` تعبر و`SD2` تسقط** ⇒ الانحيازُ **موجودٌ وغيرُ مادّيّ** ⇒ **يُغلَق
   المحورُ** إغلاقًا مُنفَّذًا بشرطِ فتحٍ مؤرَّخ.
3. **`SD0` تسقط أو `SD3` تسقط** ⇒ **«لا قياس»** — يُعلَن بعدَده ولا يُفسَّر،
   **ولا يُرفَع العدُّ بتأريخٍ مُستنتَج**.

## §⑤ حرّاسٌ تُطبَع

- `V-S1` الأداةُ **قراءةٌ فقط**: صفرُ كتابةِ حالةٍ · صفرُ إرسال · بلا سرّ.
- `V-S2` الجذورُ الاثنا عشر **بصمتُها مطابقة** قبل وبعد — **و`pivot_stability`
  تُنادى ولا تُعدَّل** (صفرُ إسنادٍ إلى `CONFIG`).
- `V-S3` كلُّ ثلاثيّةٍ تُطبَع بسطرها (رمز · تاريخ · مستوى فيصل · `A0` · `A1` ·
  `A2` · `A-FAR` · الخطأ) ⇒ **الحكمُ يُراجَع صفًّا صفًّا**.
- `V-S4` **القصُّ as-of إلزاميّ** لكلّ ثلاثيّة (شارتُ اليوم شارتٌ آخر).
- `V-S5` تُطبَع **التغطية**: كم ثلاثيّةً طُلبت · كم نجح جلبُها · وكم استُبعدت
  ولماذا.

## §⑥ حدودُ صدقٍ — تُقال قبل النتيجة

1. **العيّنةُ ‏≈6** ⇒ **لا حكمَ عامًّا**، والنتيجةُ **وصفٌ لحالاتٍ موثَّقة**.
2. **المستوياتُ مقروءةٌ من شارتاتٍ** لا من نصٍّ رقميّ ⇒ خطأُ قراءةٍ محتمَل.
3. **فيصلٌ يرسم لغرضٍ** (‏«كسرُه فشل») وقد يرسم مستوًى **مكسورًا سلفًا** — كما في
   `PPBT` («‏وغير مرشح ابدا») ⇒ **المقارنةُ تقيس فرقَ التعريف لا صوابَ أحدهما.**
4. **`A1`/`A2` وصفيّان** ولا يُرشَّحان حتى لو تفوّقا — **إفراطُ الملاءمة عند n=6
   يقينٌ لا احتمال**.
5. **ولا يُغيَّر `PIVOT_LOOKBACK` ولا أيُّ عتبةٍ** بهذا العقد.

## §⑦ تنبّؤاتي — تُنشَر مكذَّبةً أو مؤكَّدة

- `P1` `SD1` **تعبر** (‏`A0` أدنى في ≥80%) — **ثقةٌ عالية** (‏`A0` أدنى قاعٍ
  بالتعريف، وفيصلٌ نادرًا ما يرسم عند الذيل).
- `P2` `SD2` **تعبر** (وسيطُ الخطأ ‏≥10%) — **ثقةٌ متوسّطة**.
- `P3` **`A1` أقربُ من `A0`** إلى مستوى فيصل — **ثقةٌ متوسّطة** (وصفيٌّ لا يُرشَّح).
- `P4` `SD0` **تعبر بشقّ الأنفس** (‏5-8 ثلاثيّات) — **ثقةٌ متوسّطة**.
- `P5` `A-FAR` **يسقط** — **ثقةٌ عالية**.

## §⑧ ما لا يُمَسّ

`pivot_stability` · `PIVOT_LOOKBACK` · الجذورُ الاثنا عشر · الوقفُ والدفعاتُ
والأهدافُ والعضويّة · وسطرُ «🧭 مستويات فيصل» وعقدُه · وحكمُ `T-WKSUP`.
**ولا تُرسَل رسالةُ تلغرامٍ من الأداة إطلاقًا.**

---

## 📎 ملحقٌ مؤرَّخ §⑨ — **قاعدةُ الاستخراج** (2026-09-18، قبل بناء الأداة وقبل أيّ رقم)

> استخراجُ الحالات الستِّ من الكاتالوج كشف مسألتين **لم ينصّ عليهما `§②`**،
> وتُحسَمان **الآن** لا بعد رؤية النتيجة.

### ⓐ الفريمُ جزءٌ من المقارنة
`A0` تعريفٌ **يوميّ** (‏`pivot_stability` على الشموع اليوميّة). ومستوًى رسمه فيصلٌ
على شارت **4 ساعات** ليس قابلًا للمقارنة به — **خلطُ فريمَين**.
⇒ **الحاكمُ: الصفوفُ ذاتُ المستوى الأحمر على الشارت اليوميّ وحدَها.**
⇒ **وصفوفُ 4س تُطبَع موسومةً `[4س — وصفيّ]` ولا تدخل أيَّ معيار.**
⇒ **وصفٌّ لا يُميَّز فريمُه يُطبَع `[فريمٌ ملتبس — وصفيّ]` ولا يدخل.**

### ⓑ ثلاثيّةٌ واحدةٌ لكلّ (رمز · تاريخ · فريم)
حين يحمل الشارتُ عدّةَ مستوياتٍ حمراء ⟵ **قاعدةُ `§②` تُطبَّق** (الأقربُ تحتَ
إغلاق يوم الصورة)، **ولا تُحسب المستوياتُ ثلاثيّاتٍ مستقلّة** — وإلّا لانتُفخ العدُّ
بلا شهادةٍ جديدة.

### ⓒ الجدولُ المُستخرَج — يُعلَن الآن بمصدره سطرًا سطرًا

| # | الرمز | التاريخ | الفريم | 🔴 المعلن | سطرُ الكاتالوج |
|---|---|---|---|---|---|
| 1 | PPBT | 2026-09-05 | يوميّ | 1.980 · 1.682 | 1211 |
| 2 | DCOY | 2026-09-05 | 4س · يوميّ (ملتبس) | 2.851 | 1218 |
| 3 | ELPW | 2026-09-18 | يوميّ | 2.500 | 1461 |
| 4 | NUWE | 2026-09-18 | 4س | 0.7119 | 1464 |
| 5 | DAIC | 2026-09-18 | 4س | 1.986 | 1466 |
| 6 | SXTC | 2026-09-18 | يوميّ | 2.064 | 1474 |

🔴 **وأثرُه على الأرضيّة يُقال الآن صراحةً:** الحاكمُ **‏3 صفوفٍ يوميّة** (‏PPBT ·
ELPW · SXTC) وأرضيّةُ `SD0` **‏≥5** ⇒ **الفرعُ 3 «لا قياس» مرجَّحٌ قبل التشغيل**.
**ولا تُخفَّض الأرضيّةُ ولا يُضَمّ 4س لرفع العدّ** — ذلك مطاردةُ نتيجةٍ بنصّ `§④`.
**والقيمةُ الباقيةُ للتشغيل:** جدولٌ مقيسٌ يُظهر **ما المتاح وما ينقص**، وهو
بذاته ما يُبرّر (أو يُسقط) تسجيلًا لاحقًا بميزانيّةٍ أوسع.

### ⓓ ما لا يُفعَل
لا تُستعمل صورةٌ بلا تاريخ · ولا يُستنتَج تاريخٌ من رقم الصورة · ولا يُضاف رمزٌ
من خارج الكاتالوج · **ولا تُعدَّل الأرضيّةُ ولا المعايير**.

---

## 📎 ملحقٌ مؤرَّخ §⑩ — **المجتمعُ الموسَّع بمرساة السعر · وشاهدُ ضبطٍ يفرّق بالبناء**
(‏2026-09-18، أمرُ المالك «سجّل ملحق الدعم» · **يُدفَع ويُدمَج قبل أيّ سطرِ أداةٍ وقبل أيّ رقم**)

> **لماذا الآن:** `§⑦` من `support_def_result.md` نصَّ على **ثلاثةٍ تفتح المحورَ معًا**
> وقد اجتمعت: ① **عيّنةٌ تبلغ الأرضيّة** — تواريخُ صورٍ «ليست في المستودع» صارت **مقيسةً
> من سعرٍ مسجَّلٍ على الشارت نفسِه** ② **شاهدُ ضبطٍ يفرّق فعلًا** بدل `A-FAR` الخامد
> ③ **إذنُ المالك**.
>
> 🔴 **وما لا يتغيّر بهذا الملحق — ويُقرأ قبل كلّ سطرٍ تحته:** الفروعُ الثلاثةُ بحرفها ·
> أرضيّةُ `SD0` (‏**‏≥5** ثلاثيّات من **‏≥3** رموز) · حدَّا `SD1` (‏80% و`p ≤ 0.10`)
> و`SD2` (‏**‏≥10%**) · قاعدةُ `§⑨-ⓐ` (اليوميُّ وحدَه يحكم) · «**لا يُشحَن منه شيءٌ
> مهما كانت النتيجة**» · و«**لا يُمَسّ `pivot_stability`**».

### ⓐ مصدرُ التاريخ — ولماذا هو **ليس** «تأريخًا مُستنتَجًا»

`§①` و`§⑨-ⓓ` يمنعان **استنتاجَ التاريخ من رقم الصورة**، والمنعُ **قائمٌ ونافذٌ كما هو**.
والداخلُ هنا مصدرٌ آخرُ تمامًا: **السعرُ المسجَّل على الشارت نفسِه** («‏AH 4.500 (‏+0.67%)» ·
«‏3.105 (‏−13.15%)») يُقارَن بسلسلة الأسعار، فإن طابق **يومَ تداولٍ واحدًا لا غير** داخل
النافذة ⟶ ذلك هو تاريخُ الشارت. فالتاريخُ **مُشتقٌّ من قياسٍ موثَّقٍ على الصورة** لا من
ترتيبِ ملفٍّ ولا من جوارٍ في دفعة.

⇒ **الداخلُ حصرًا:** صفوفُ `faisal_price_anchor.py` بحكم **`unique`** وبمرساةٍ **ذاتِ نسبةِ
تغيّرٍ مسجَّلة** (قيدان: الإغلاقُ المُشتقّ **و**النسبة) — عند التسامح **المسجَّل في الأداة
‏0.75%**.
⛔ **والخارجُ حصرًا:** `ambiguous` · `none` · `no_price` · **وكلُّ مرساةٍ رخوةٍ بلا نسبة**
(قيدٌ واحدٌ لا يكفي لتأريخِ صفٍّ يدخل معيارًا) · **وكلُّ صفٍّ يعبر بالتسامح الضيّق ‏0.10%
وحدَه** (‏`tight_*`) — لأن ذلك التسامحَ **اختير بعد رؤية الأرقام** فلا يُؤرِّخ صفًّا حاكمًا،
ومكانُه `ⓖ` وصفيًّا.

### ⓑ الصفوفُ الداخلة — بمصدرها، **ولا رقمَ مكتوبٌ بيدي**

المستوياتُ تُقرأ من **`faisal_levels_table.tsv`** (كلُّ صفٍّ فيه مُثبَتٌ على سطر الكاتالوج
الذي يدّعيه بقفل `LVT1`) بالمرشِّح: `role = support` · `frame = daily` · `auto_chart = 0` ·
ونفسُ `(رمز · سطر)`. **وكلُّ مستوياتِ السطر الحمراءِ اليوميّة تُجمَع ثمّ تُطبَّق قاعدةُ
`§②`** (الأقربُ تحت سعر المرجع) — **ولا تُحسب مستوياتٍ مستقلّة** (‏`§⑨-ⓑ`).

| # | الرمز | سطرُ الكاتالوج | 🔴 المستويات اليوميّة | نوعُ المرساة | السعرُ المسجَّل | تاريخُ الشارت |
|---|---|---|---|---|---|---|
| 7 | TRUG | 41 | 4.470 · 3.967 | `ah` | 4.500 (‏+0.67%) | 2026-03-27 |
| 8 | TURB | 45 | 2.819 | `spot` | 3.105 (‏+32.13%) | 2026-04-02 |
| 9 | GRI | 74 | 2.100 | `spot` | 2.145 (‏−12.45%) | 2026-04-13 |
| 10 | VEEE | 240 | 5.990 · 5.760 | `spot` | 6.210 (‏−13.15%) | 2026-05-14 |

🔒 **والجدولُ أعلاه توثيقٌ للقارئ لا مصدرٌ للأداة:** الأداةُ **تُعيد اشتقاقَ** الأربعة
بنداءِ `faisal_price_anchor.targets()` و`match_days()` **بالاسم** وبثوابتِه نفسِها
(`W0`/`W1`/`TOL`) وعلى الشموع نفسِها (`start_override = W0`) ومع `_fetch_splits` — ثمّ
**حارسُ هُويّةٍ يوقف** إن لم يُعِد **هذي الأربعةَ بتواريخها بالضبط** (‏`V-S6` · خروج **‏6**).
فدريفُ مزوِّدٍ يُغيّر المجتمعَ **يُوقف القياسَ ولا يُغيّره بصمت**.

### ⓒ لحظةُ القصّ: **`match_date` لا `date`**

`V-S4` يفرض القصَّ as-of، والملحقُ يُثبّت **أيّ يومٍ هو**: **آخرُ جلسةٍ مكتملةٍ كان فيصلٌ
يراها لحظةَ اللقطة** — وهي `match_date` في الأنواع الثلاثة بالبناء:

| النوع | اللقطة | آخرُ شمعةٍ مكتملةٍ يراها | ‏`match_date` |
|---|---|---|---|
| `ah` | بعد إغلاق `D` | شمعةُ `D` | `D` ✓ |
| `pre` | بريماركت `D+1` | شمعةُ `D` | `D` ✓ |
| `spot` | أثناء جلسة `D` | شمعةُ `D−1` (شمعةُ `D` **ناقصة**) | `D−1` ✓ |

⇒ **القصُّ عند `match_date`** — فلا يدخل الحسابَ قاعُ يومٍ لم يكتمل على شاشته (**تسريبٌ**
صريح لو أُخذ `date` في نوع `spot`). **وحساسيّةٌ وصفيّةٌ تُطبَع** بالقصّ عند `date` لصفوف
`spot` وحدَها، **ولا تدخل أيَّ معيار**.

### ⓓ سعرُ المرجع لقاعدة `§②` — **آخرُ سعرٍ مرئيٍّ على الشارت**

قاعدةُ `§②` تختار «الأقربَ تحتَ **سعرِ الإغلاق يومَ الصورة**». وفي `spot` **لا إغلاقَ بعد**،
والمرئيُّ هو **السعرُ المسجَّل نفسُه**. ⇒ **سعرُ المرجع:** إغلاقُ `match_date` في `ah`/`pre` ·
**والسعرُ المسجَّل** في `spot`. **وحساسيّةٌ وصفيّةٌ** بإغلاق `match_date` في الثلاثة.

⚠️ **وإفصاحٌ واجب:** رأيتُ — وأنا أتحقّق أن الأربعةَ حمراءُ يوميّة — أن **`TURB` هو الصفُّ
الوحيد** الذي تُبدّله هذي القاعدة (مستوياتُه فوق إغلاق `D−1` ودون سعرِه المسجَّل)، فيدخل
بالقاعدة ويخرج بالحساسيّة. **والقاعدةُ تُكتَب لأنها الأصحُّ وصفًا للشاشة لا لأنها تُكبّر
العيّنة** — ولهذا تُنشَر الحساسيّةُ معها دائمًا، والفرقُ يُقرأ.

### ⓔ **مستوى السعر ليس دعمًا** — وسمُ المؤشِّر يُستبعَد

خطٌّ أحمرُ **منطبقٌ على السعر اللحظيّ** هو **مؤشّرُ السعر على الشارت** لا مستوًى مرسومًا.
⇒ يُستبعَد كلُّ مستوًى يقع ضمن **‏0.25%** من سعر المرجع (**`engineering` مُعلَن** — لا سندَ
فيصليًّا له)، **ويُطبَع عددُ المستبعَدات بهذي القاعدة**.

⚠️ **وإفصاحٌ واجب:** الحالةُ التي أرتني الحاجةَ هي **`TRUG`** (مستواه ‏4.470 والإغلاقُ
المُشتقّ ‏4.4701 = فرقُ ‏0.002%) ⇒ بالقاعدة يُستبعَد ويُختار ‏3.967. **والعتبةُ ضيّقةٌ عمدًا**
(‏0.25% لا 2%) حتى لا تبتلع **دعمًا حقيقيًّا يقف السعرُ عليه** — وهي حالةٌ شائعةٌ ومقصودة.
**وحساسيّةٌ وصفيّةٌ تُطبَع بلا هذي القاعدة.**

### ⓕ شاهدُ الضبط — `A-FAR` يتقاعد و**`A-RAND`** يحكم

`A-FAR` **خامدٌ بالبناء على هذا المجتمع** (‏`support_def_result.md §③`: ≡ `A0` بت-بت لأن
الرموزَ صنعت قاعَها حديثًا) ⇒ شرطُ الصحّة `SD3` لا يُمكن أن يُقاس به. **والبديلُ مُسجَّلٌ
الآن قبل أيّ رقم:**

**`A-RAND` — مستوًى بلا معلومة (النموذجُ الصفريّ):** لكلّ ثلاثيّة: المدى
`[أدنى Low ، أعلى High]` على **نافذة `A0` نفسِها** (‏`PIVOT_LOOKBACK` **تُقرأ** من `CONFIG`
ولا تُضبَط) · **‏999 سحبةً منتظمةً** ببذرةٍ حتميّة `"{رمز}|{asof}|T-SUPDEF-ARAND"` ·
**وخطأُ الصفّ = وسيطُ ‏\|الخطأ\| على السحبات** (لا خطأُ وسيطِ السحبات — ذلك ينهار إلى منتصف
المدى). `SD3` تعبر إذا كان **وسيطُ خطأ `A-RAND` على الصفوف الحاكمة أكبرَ من وسيط خطأ `A0`**.

🔑 **ولماذا هذا الشاهدُ بعينه:** ① **يفرّق بالبناء** (مستوًى من كلّ المدى لا من قاعه) ②
**معلومُ الخطأ** بالمعنى المقصود في `§②`: صفرُ معلومة ③ **وليس ختمًا مطاطيًّا** — لو جلس
مستوى فيصل وسطَ المدى و`A0` في قاعه، **يفوز الشاهدُ وتسقط `SD3`**، وذاك **حكمٌ لا عطب**
(‏`ⓗ` يُلزم بطباعة سببِ الفرع 3 مفصولًا).
🔒 **وشرطُ ما قبل التشغيل (الخطُّ الأحمر «الشاهدُ يُختار على خصائص المجتمع»):** يُثبَت على
**فِكستشرٍ يصنع قاعَه في آخر خمس شمعات** — وهي الحالةُ التي أخمدت `A-FAR` — أن خطأ `A-RAND`
**يفارق** خطأ `A0`؛ وإلّا **يُوقَف بخروج ‏7 قبل أيّ جلب**.

### ⓖ ما يبقى **وصفيًّا** ولا يدخل معيارًا

`A1` · `A2` (‏`§⑥-4`) · **و`A-FAR` يبقى مطبوعًا** (فيبقى رقمُ النتيجة السابقة قابلًا للمقارنة
وتبقى خموديّتُه مرئيّة) · **و`A-MID`** (منتصفُ المدى) · **و`A-HIGHLOW`** (أعلى قاعٍ في النافذة)
· وصفوفُ **4س/الملتبس** بوسمها · **والصفوفُ الخمسةُ التي تعبر بالتسامح الضيّق ‏0.10% وحدَها**
(‏`SBDS` · `NEXR` · `CIIT` · `ALMU` · `NXTT`) تُطبَع كتلةً منفصلةً معنونةً **«وصفيّ — خارج كلّ
معيار»**.
⛔ **ولا يُرشَّح أيٌّ من هذي بديلًا للتعريف مهما تفوّق** — `§⑥-4` نافذ: **إفراطُ الملاءمة عند
هذا الحجم يقينٌ لا احتمال.**

### ⓗ حرّاسٌ ورموزُ خروج

- **`V-S6`** هُويّةُ المجتمع: إعادةُ اشتقاق الأربعة تُطابق `ⓑ` رمزًا وتاريخًا · وإلّا **خروج ‏6**.
- **`V-S7`** شاهدُ الضبط يفارق `A0` على فِكستشر `ⓕ` · وإلّا **خروج ‏7**.
- **`V-S8`** يُطبَع **سببُ الفرع 3 مفصولًا**: «الأرضيّة» · «الشاهدُ لا يفرّق» · **«الشاهدُ
  تفوّق على `A0`»** — فلا تُطوى الحالةُ الثالثةُ داخل «لا قياس» بلا بيان.
- **`V-S9`** تُطبَع **كلُّ** الحساسيّات الثلاث (‏`ⓒ` · `ⓓ` · `ⓔ`) ولو لم تغيّر شيئًا.
- والرموزُ القائمةُ كما هي: 0 = الفرعُ 1 · 2 = الفرعُ 2 · 3 = الفرعُ 3 · 5 = حارس.
  **و`V-S1`-`V-S5` نافذةٌ بحرفها.**

### ⓘ حدودُ صدقٍ — تُقال قبل أيّ رقم

1. **آليّةُ `SD1` عند هذا الحجم = إجماعٌ أو لا شيء:** بقيم `sign_test_p` **كما تُعيدها الأداة**
   (‏تُقرّب لأربع خانات): عند `n = 5` ⟵ ‏5/5 ⟶ `p = 0.0625` ✓ و‏4/5 ⟶ `p = 0.375` ✗ · وعند
   `n = 6` ⟵ ‏6/6 ⟶ `p = 0.0312` ✓ و‏5/6 ⟶ `p = 0.2188` ✗. ⇒ **صفٌّ واحدٌ مخالفٌ يُسقط `SD1`**
   مهما بلغت النسبة. هذا **حسابٌ لا تنبّؤ**.
   🔴 **وهذي الأرقامُ أمسكت عيبًا في مسوَّدتي قبل الدمج:** كتبتُ أوّلًا ‏`0.03125` و`0.219`
   (‏الرياضيّتَين) والأداةُ تُعيد ‏`0.0312` و`0.2188` ⇒ **الوثيقةُ تُطابق الأداةَ لا العكس**،
   وقفلُ `SDX5` يقارنها **بالنداء الحيّ** فلا تفترقان بعد اليوم.
2. **العيّنةُ تبقى صغيرة** (‏6 حاكمة في أفضل الأحوال) ⇒ النتيجةُ **وصفُ حالاتٍ موثَّقة** لا
   حكمٌ عامّ — `§⑥-1` نافذ.
3. **المستوياتُ مقروءةٌ من شارتات** (‏`§⑥-2`) · **والتواريخُ احتماليّةٌ لا يقينيّة**: مطابقةُ
   سعرٍ واحدٍ في نافذةِ ‏20 شهرًا بتسامح ‏0.75% **قد تُصيب يومًا غيرَ اليوم**، ووحدانيّةُ
   المطابقة تُقلّل الاحتمالَ ولا تُلغيه.
4. **بياناتُ المزوِّد قد تُنقَّح** ⇒ `V-S6` يُوقف بدل أن يُغيّر بصمت.
5. **الصفوفُ الجديدةُ من ‏2026-03/05 والقديمةُ من ‏2026-09** — حِقبتان، والمقياسُ نسبيٌّ لكلّ
   صفٍّ فلا يُجمَع سعرٌ إلى سعر.
6. **ولا يُشحَن شيءٌ من هذا العقد ولا ملحقِه مهما كانت النتيجة.**

### ⓙ تنبّؤاتي — تُنشَر مكذَّبةً أو مؤكَّدة

- `Q1` **`SD0` تعبر** (‏≥5 ثلاثيّات من ≥3 رموز) — **ثقةٌ متوسّطة** (‏2 قائمة ‏+ 4 داخلة،
  وسقوطُ اثنتين على قاعدة القراءة يُسقطها).
- `Q2` **`SD1` تعبر بالإجماع** (كلُّ الصفوف `A0` أدنى) — **ثقةٌ عالية** (‏`A0` أدنى قاعٍ
  بالتعريف)، **ومكذَّبٌ بصفٍّ واحدٍ مخالف**.
- `Q3` **`SD2` تعبر** (وسيطُ \|الخطأ\| ‏≥10%) — **ثقةٌ منخفضة**: القائمتان ‏31.0% و‏13.8%
  والجديدةُ مجهولة، **ووسيطُ ستّةٍ ينهار بصفَّين قريبَي الصفر**.
- `Q4` **`SD3` تعبر** (‏`A-RAND` أسوأ) — **ثقةٌ متوسّطة**، **ويُكذَّب** لو كان مستوى فيصل
  وسطَ المدى عادةً.
- `Q5` **صفٌّ واحدٌ على الأقلّ من الأربعة يسقط** بقاعدة القراءة أو بقاعدة `ⓔ` — **ثقةٌ عالية**.
- `Q6` **الفرعُ الأرجحُ إجمالًا: ‏1 أو ‏3** — والفرعُ 2 (انحيازٌ غيرُ مادّيّ) **الأقلُّ ترجيحًا**.

### ⓚ ما لا يُفعَل

لا تُخفَّض الأرضيّةُ ولا تُرفَع · ولا يُضَمّ 4س ولا الملتبسُ ولا الرخوُ ولا `tight` لبلوغها ·
ولا يُستنتَج تاريخٌ من رقم صورة · ولا يُغيَّر `PIVOT_LOOKBACK` ولا `FAISAL_LEVEL_TOL_PCT` ·
ولا تُبدَّل عتبةُ `ⓔ` بعد رؤية النتيجة · ولا يُبدَّل شاهدُ الضبط مرّةً ثانية (**تبديلُ شاهدٍ
بعد رقمٍ = مطاردةُ نتيجة** — وهذا التبديلُ الوحيدُ المأذون، وسببُه **خموديّةٌ مُثبَتةٌ
بالبناء** لا رقمٌ لم يعجبني) · **ولا تُرسَل رسالةُ تلغرامٍ ولا يُمَسّ `pivot_stability`.**
