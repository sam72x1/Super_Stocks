# 🪑 T-SLOT — سياسةُ الخانات: هل تحريرُ خانة «غير المُعبَّأ» مبكّرًا يُسلّم منفجرين أكثر؟ (يُدفَع **قبل** أيّ رقم)

> **أمرُ المالك (2026-08-07 ليلًا):** عرضتُ بعد T-RANKER2 أن الرافعة الباقية غير
> المقيسة هي سياسةُ الخانات («السعة 10 ترفض ~440 إشارة/سنة، والخانةُ المحجوزة
> أسابيع تمنع منفجرًا قادمًا») — فقال **«قسها»**.
> **وهذا الملفُّ مدفوعٌ قبل تشغيل أيّ ذراع.**

---

## ① الظاهرة والمصطلح

في آلة `replay10` (المطابِقة لدلالات القائمة الحيّة): الاسمُ المُختار يحجز خانةً
من إشارته حتى حسمه — والاسمُ الذي **لا تُعبَّأ طلباتُه إطلاقًا** (`no_fill`: السعر
هرب فوق نطاق الدفعات) يحجزها حتى نهاية النافذة (‏40 جلسة). وحيًّا أسوأ: «تُشطب
بالستوب فقط» والهاربُ فوق دخوله لا يلمس ستوبه ⇒ حجزٌ غيرُ محدود. **فالمقيسُ هنا
أرضيةُ الهدر لا سقفُه.**

**والسابقتان الإنتاجيتان لهذا النوع من التحرير قائمتان بقرار المالك:** صاحبُ
الهدف المحقَّق يبقى متابَعًا **ولا يحجز خانة** (⑨ خيار ب) · والمحمول `exited`
كذلك («افتح الباب» 2026-08-06). المقترحُ المقاس من الجنس نفسه: **تحريرُ الخانة
مع بقاء الاسم متابَعًا** — لا شطبَ ولا مساسَ بقاعدة «تُشطب بالستوب فقط».

## ② الأداة والميزانية

- نفسُ عدّة T-RANKER2 حرفيًّا: `replay10` · سعة **10** · بِركةُ العشرين على لقطات
  PIT الثلاث (‏2024=`31199493957` · 2025=`31191327731` · 2026=`31199510868`) ·
  `FAISAL_ONLY=1` · **المُرتِّبُ مثبَّتٌ على `rank_live` (الإنتاج) في كلّ الأذرع**
  — فالمتغيّرُ الوحيد سياسةُ الخانة (تجربةُ متغيّرٍ واحد).
- إضافتان بنيويّتان مُعلَنتان: حقل `fill_date` (تاريخُ التعبئة من فهرس المحرّك
  نفسه `fut.index[filled]`) خلف `BT_REPLAY10` · ووسيطان اختياريان `free_of` في
  `replay()` و`extra_dates` في `candidates_from_trades` — **الافتراضيّ None =
  بت-بت** (مقفولٌ باختبار تطابقٍ كامل وطفرة).
- 🧪 **بوّابةُ صلاحيةٍ حاكمة قبل قراءة أيّ ذراع:** ذراعُ الأساس `S-LIVE` يجب أن
  يُعيد إنتاج أرقام T-RANKER2 لذراع `K-LIVE` **حرفيًّا** (‏d50 = 22 · 14 · 6) —
  فإن اختلفت فالأداةُ معطوبة ولا تُقرأ التجربة.

## ③ الأذرع — ثلاثٌ مثبَّتة ولا رابعة

| الذراع | السياسة | مصدرُ الرقم |
|---|---|---|
| **`S-LIVE`** | الحاليّة: الخانة حتى الحسم/النافذة | خطُّ الأساس (الإنتاج) |
| **`S-NF5`** | غيرُ المُعبَّأ بعد **5 جلسات** يحرّر خانته ويبقى متابَعًا | إيقاعُ التجديد الأسبوعيّ المعتمَد من المالك (اثنين→جمعة = 5 جلسات تداول) — بنيويّ لا مخترَع |
| **`S-NF8`** | كذلك بعد **8 جلسات** | سقفُ قاعدة فيصل الموثّقة «ثبات الدعم **3-8 جلسات**» — من الكاتالوج (`inferred`) |

- مَن تعبّأ **خلال** المهلة ⇒ السلوكُ الحيّ حرفيًّا (لا تغيير).
- مَن تعبّأ **بعد** المهلة («تعبئةٌ متأخّرة») ⇒ خانتُه كانت قد حُرِّرت؛ نُبقي
  نتيجتَه و`R` كما هما **ولا نعيد شحن السعة** — وهذا **تفاؤلٌ مُعلَن** يُحاسَب
  عليه الذراعُ في قاعدة القرار أدناه (بندُ التعديل الصادق).

## ④ المقاييس (الأساسيّ أولًا — مثبَّتة قبل الأرقام)

1. **المنفجرون المُسلَّمون** (‏`mg_pre_stop` ≥ `EXPLOSION_PCT` بين المأخوذين) —
   نفسُ مقياس T-RANKER2، ومعه بالغو +100% وعددُ المأخوذين.
2. **التعديلُ الصادق للتفاؤل:** `d50_adj` = المُسلَّمون **ناقصَ** منفجري التعبئة
   المتأخّرة (مَن تعبّأ بعد المهلة بين مأخوذي الذراع) — فيُنسَب للذراع أسوأُ
   تفسيرٍ لتفاؤله.
3. **تشخيصُ الهدر:** حصّةُ «مَن لم يتعبّأ قطّ» من أيام-الخانات في `S-LIVE`
   (‏`slot_days`) — كم من السعة يأكله الميّت؟
4. حارسُ العائد: متوسط `R`/مأخوذة للذراع لا يقلّ عن `S-LIVE` بأكثر من
   ‏0.05R مجمَّعًا (يُنشَر وصفيًّا سنةً بسنة).

## ⑤ قاعدةُ القرار (قبل الأرقام)

- **يُقترَح على المالك** اعتمادُ `S-NF5` أو `S-NF8` (الأفضلُ منهما إن تأهّلا) إن
  استوفى الثلاثة معًا: ① **‏`d50_adj` ≥ ‏`d50` الأساس في كلّ سنة** وأكثرُ منه
  صراحةً في سنتين على الأقلّ ② مجمَّعُ بالغي +100% لا ينقص عن الأساس ③ حارسُ
  العائد مستوفًى. **والتنفيذُ قرارُ مالكٍ حصريّ** — يمسّ دلالات سعة القائمة
  الحيّة (من جنس سابقتَي ⑨/«افتح الباب») ويلزمه عندها `LOGIC_VERSION` +
  `force_renew=1`.
- إن لم يستوفِ أيُّ ذراعٍ ⇒ الحكمُ الصادق: **«تحريرُ غير المُعبَّأ ليس الرافعة»**
  — وتُنشَر حصّةُ الهدر المقيسة (③) كما هي، ويُغلَق هذا الباب بلا تغيير.
- نتيجةٌ مختلطة ⇒ تُعرَض بلا ترجيح والقرار للمالك.

## ⑥ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر ولو كُذِّبت)

- **P1:** `S-NF5` يأخذ صفقاتٍ أكثر من `S-LIVE` في السنوات الثلاث (سعةٌ محرَّرة
  تُمتَصّ).
- **P2:** `d50_adj` لـ`S-NF5` أعلى من أساسه في سنتين على الأقلّ (أطروحةُ «الوزن
  الميّت» صحيحة).
- **P3:** التعبئةُ المتأخّرة أقلّية (أقلّ من 20% من المُعبَّآت تتعبّأ بعد 5
  جلسات) — التعبئةُ قرب الدعم سريعةٌ بطبعها (وسيط أيام الذروة 1-3 مقيس).
- **P4:** `S-NF8` بين الاثنين (اتّجاهٌ رتيب مع المهلة).

## ⑦ حدودُ صدقٍ تُقرأ مع أيّ رقم

نفسُ حدود T-RANKER2 (عيّناتٌ صغيرة · مِشيةٌ متفرّقة · 2026 جزئية · 2025/2026
داخل-العيّنة مشتركةٌ بين الأذرع) · «عدمُ إعادة شحن» المتأخّر تفاؤلٌ يُقيَّد
بـ`d50_adj` لكن لا يُمحى · والأثرُ الحيّ قد يفوق المقاس (الحجزُ الحيّ غيرُ محدود
والنافذة هنا 40 جلسة) — فالنتيجةُ السالبة هنا أقوى من الموجبة، والموجبةُ أرضية.
