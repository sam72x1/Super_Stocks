# 🔗② T-TIERLINK-2 — تسجيلٌ مسبق: «+100% في البري والافتر بالتصنيف — ومَن كانت أداةُ القائمة الجديدة ستنبّه عنه؟» (2026-09-06)

> **مدفوعٌ قبل سطرِ كودِ أداةٍ واحد وقبل أيّ رقم.** يُبنى على `tierlink_prereg.md` (العقد الأمّ)
> ويُعيد استعمال أداته **بالاسم** (‏`tierlink_probe.anchor_history/measure/features`) — مقياسٌ
> واحدٌ لا اثنان.

## ⓪ سؤالُ المالك حرفيًّا

«هل لقيت الرابط المشترك في جميع الأسهم اللي جاني تنبية عنها وقت البري و الافتر من بعد بناء
اداة المتابعة اللحظية و بالتصنيف اللي يكون قوي و ضعيف و متوسط و تجاوزت نسبة 100٪ و إذا اي
ف وش الأسهم اللي كان بتنتبهي عنها اداة القايمة الجديدة؟»

⇒ **ثلاثةُ أسئلةٍ مقيسة لا سؤالٌ واحد:** ① مَن من مراسي البري/الافتر تجاوز **‏+100%**
(بالاسم وبالفئة) ② هل «الرابط» المنشور في `T-TIERLINK` («J1 في البريماركت») يصمد على
مقياس **‏+100%** ③ ومَن من هؤلاء كانت **أداةُ ما قبل الجلسة** (‏`presession_radar` —
«القائمة الجديدة») ستدرجه **صباحَ يومه**؟

## ① المجتمع

- كلُّ مِرساةِ سيولةٍ سجّلها العاملُ الحيّ في `op_entry_state.json` (تاريخ git) منذ
  **‏2026-08-18** حتى يوم التشغيل — **لا المنفجرون وحدَهم** (وإلّا كانت دائريّة).
- الجلسةُ من `anchor_ms` بتوقيت نيويورك: **`pre`** (‏قبل 09:30) · `reg` · **`after`**
  (‏من 16:00). **الحاكمُ `pre` و`after`** (نصُّ المالك) و`reg` يُطبَع للاكتمال.
- الفئةُ من سجلّ `M5` (‏`tier_fwd_ledger.jsonl`) أو من `k2` المخزَّن — **بدالّة
  `features` نفسِها** · ومَن بلا فئة يُوسَم «؟» ويُطبَع ولا يُسقَط.

## ② المقاييس (مُغلَقة)

- **`mg_day`** = أقصى قمّةٍ بعد كرت `M5` في يومه ÷ سعر الكرت `e5` − 1 — **بدالّة
  `measure` نفسِها** (‏`exploded50` · `exploded100`). الحاكمُ **`exploded100`**.
- **للافتر إضافةً:** `mg_ext` = الأقصى **حتى نهاية بريماركت اليوم التالي** (‏09:30)
  من سعر الكرت — لأن انفجارَ الافتر يمتدّ إلى بريماركت الغد (‏`FTFT` · `ZCMD`) ويُقصّ
  عند 20:00 بالمقياس اليوميّ. **يُطبَع بجانب `mg_day` ولا يُبدّله** (المقياسُ المنشور
  يبقى قابلًا للمقارنة مع `T-TIERLINK`).
- `close_ret` كما هو.

## ③ الرابط على +100% — قاعدةُ القراءة مثبَّتة

الرابطُ المنشور (‏«J1 في البريماركت» · `gap≥30% · قوي · v2 30-60%`) **يُطبَع على
`exploded100` بنفس المعيار الرباعيّ** (‏≥2× · فواصلُ منفصلة · **n≥20 لكلّ سلّة** ·
تكرارٌ في النصفين). ⚠️ **وحدُّ صدقٍ يسبق الرقم:** `exploded100` كان 14 من 321 في
`T-TIERLINK` ⇒ **الأرجحُ أن تسقط الأرضيةُ (‏n<20) فيُطبَع «لا حكم» ويبقى الحكمُ المنشور
على `exploded50` هو النافذ** — ولا تُخفَّض الأرضيةُ بعد رؤية العدد.

## ④ «القائمةُ الجديدة» — أيُّ قائمة؟ ثلاثةُ أجوبةٍ لا واحد

من صفوف `presession_scan` (‏`presession_rows_*.jsonl.gz` على المدى نفسِه):

- **(أ) الرادارُ المقيس (السوقُ كلُّه):** صفُّ `PM` ليوم المِرساة `D` للرمز موجودٌ
  و`post_hi_ret ≥ FLOOR_BY_SLOT["PM"]` (‏0.69492 · المشحون) ⇒ **«كانت القائمةُ
  المقيسة ستدرجه»**. تُطبَع رتبتُه بـ`post_hi_ret` بين صفوف يومه.
- **(ب) الرادارُ الحيُّ محاكًى:** المرشِّحُ الرخيص المشحون **يُحاكى على صفوف المسح**:
  صفوفُ `PM` ليوم `D` بـ`usd_day ≥ MIN_DAY_USD` (‏100,000) مرتَّبةً بـ`day_ret`
  تنازليًّا **وأعلى `PREFILTER_CAP`** (‏60) — ثم أرضيةُ (أ). ⚠️ **محاكاةٌ مُعلَنة لا
  الدالّةُ الحيّة**: الحيُّ يقرأ لقطةَ Polygon المجمَّعة والمسحُ ملفّاتِ Polygon
  المسطّحة — والفارقُ قِيس في `T-PRECAP §⑧` (نموذجان) **ويُعلَن هنا لا يُخفى**.
- **(ج) الافتر:** صفوفُ `AH` ليوم `D` بالمرشِّح نفسِه ثم **أعلى `TOPK`** (‏10) بـ
  `rank_key("AH")` = `usd_day` **بلا أرضية** (المشحون منذ «شغّل الافتر» 09-05).
- **لكلّ مِرساةٍ في المجتمع** (لا للمنفجرين وحدَهم) يُحسب الثلاثة ⇒ يُطبَع **معدّلُ
  الالتقاط بين المنفجرين +100% ومعدّلُه بين غير المنفجرين** — فيُقرأ هل القائمةُ
  تميّز أم تلتقط الجميعَ سواءً.
- **مَن لا صفَّ له** (خارج كون السعر 0.40-10.00$ · أو يومُه غيرُ ممسوح) يُوسَم
  **«غيرُ قابلٍ للوسم»** ويُعَدّ ويُطبَع بالاسم — **ولا يُعَدّ «لم يُدرَج»**.

## ⑤ تنبّؤاتٌ قابلةٌ للكسر (قبل الأرقام)

- **TL2-P1:** `exploded100` بين مراسي `pre`+`after` **‏3-7%**.
- **TL2-P2:** «J1 في البريماركت» أعلى بضِعفين على `exploded100` **لكنّ الأرضيةَ 20 تسقط
  ⇒ «لا حكم»** (تكرارٌ اتّجاهيّ لا حكم).
- **TL2-P3:** الرادارُ المقيس (أ) يدرج **‏≤35%** من منفجري البريماركت ‏+100% —
  والمحاكى (ب) **‏≤15%** (المرشِّحُ يقصّ ‏98% من الإصابات — `T-PRECAP`).
- **TL2-P4:** قائمةُ الافتر (ج) تدرج **‏≤20%** من منفجري الافتر ‏+100%.
- **TL2-P5:** حصّةُ «🥇 قوي» بين منفجري ‏+100% المصنَّفين **‏≥40%** (وهم أقلّيّةُ
  المجتمع ‏≈20%).
- **TL2-P6:** معدّلُ إدراج (أ) بين **غير** المنفجرين **دون 10%** (القائمةُ نادرةُ
  التسليم بالبناء — ‏1.74 اسمًا/ليلة).

## ⑥ بوّاباتُ الصلاحية (منفَّذةٌ في الكود لا موصوفة)

- **`G1` الجلب:** تغطيةُ شموع المراسي **‏≥80%** وإلّا خروج 3 (كما `T-TIERLINK`).
- **`G2` المسح:** لكلّ يومِ مِرساةٍ داخل المدى **صفوفُ `PM` و`AH`** في ملفّ المسح؛ يومٌ
  بلا صفوف ⇒ مراسيه «غيرُ قابلٍ للوسم»؛ **وفوق 20% من أيام المراسي بلا مسح ⇒ خروج 3**.
- **`G3` no-op:** صفرُ مِرساةٍ ⇒ خروج 4 · وصفرُ صفِّ مسحٍ ⇒ خروج 4.
- **`G4` التكامل:** الأساسُ `exploded50`/`exploded100` على المدى ‏08-18..09-01 يُعيد
  أرقامَ `tierlink_result.md §①` (‏33/321 · 14/321) **بت-بت** حين يُقصَر المدى عليه
  (وضعُ `TIERLINK_UNTIL`) — وإلّا عطبُ أداةٍ لا نتيجة.

## ⑦ سقفُ النجاح — وصفيٌّ فقط

- **جوابُ المالك بالأسماء** (‏§⓪ ①③) · **ولا بوّابةَ ولا عتبةَ ولا كتمَ ولا ترتيبَ ولا
  `LOGIC_VERSION`** · `Super_stock.py` و`presession_radar.py` **لا يُمَسّان بحرف**.
- الحكمُ المنشور في `tierlink_result.md` **لا يُستبدَل**؛ هذا ملحقٌ عليه.
- `FLOOR_BY_SLOT` · `PREFILTER_CAP` · `MIN_DAY_USD` · `TOPK` · `LIQ_*` **لا تتحرّك** —
  ولو أظهر الجدول أن تحريكَها يلتقط أكثر (تحريكُ عتبةٍ بعد رؤية الأسماء = `p-hacking`).

## ⑧ ما لا يُفعَل

- لا مرجعَ مخترَع لمن لا صفَّ له · لا خفضَ أرضيةِ n · لا إضافةَ مقياسٍ بعد الأرقام ·
  و**`mg_ext` للافتر يُطبَع بجانب `mg_day` لا بدلًا منه.**
