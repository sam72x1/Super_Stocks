# خطط التنفيذ — تدقيق عميق 2026-08-15

أُنشئت بأمر `/improve deep` على الرأس `4e9b143`. **مرحلة التدقيق والتخطيط فقط — لم يُعدَّل
أيّ كود مصدر.** ستّة وكلاء قراءة-فقط مسحوا الكود، وكلُّ سطرٍ مُقتبَسٍ فُتِح والتُقط بنفسي.

> **الدفعةُ السابقة (001–013، تدقيق 2026-07-28) مؤرشَفةٌ في `plans/archive/`** — كلُّها
> `DONE` عدا 013 (`BLOCKED_EXTERNAL`). لا تُعاد، ولم أُكرّر أيًّا من نتائجها أو من
> مرفوضاتها الاثنتي عشرة. الترقيمُ يُكمِل من 014 (متتابعٌ عبر تاريخ المشروع).

> **لكل منفِّذ:** اقرأ ملفّ الخطة **كاملًا** قبل البدء · شغّل كلّ أمر تحقّق وتأكّد من
> النتيجة المتوقّعة قبل الخطوة التالية · التزم بشروط `STOP` ولا تجتهد · حدّث صفّك في
> الجدول أدناه عند الانتهاء (إلّا إذا أبلغك مراجعٌ أنه يتولّى الفهرس).
>
> **بوّابة الاختبار الرسمية الوحيدة:** `python3 test_bot.py` — النجاح = **خروج 0 وصفر
> فشل**، لا عدد مقتبس. البيئة **بلا إنترنت** (كل الجالبات محقونة) — أي اختبار جديد يحقن
> جالباته ولا يمسّ الشبكة ولا ملفّات الحالة الحقيقية (استعمل مساراتٍ مؤقّتة).
>
> **قواعد مُلزِمة من `CLAUDE.md` تسري على كل خطة:**
> 1. **لا تلمس جذور الاختيار** (`analyze_ticker` · `rank_key` · `select_top` ·
>    `classify_tier` · `entry_status` · `backtest_symbol` · `apply_short_gate` ·
>    `apply_float_gate`) إلّا إذا نصّت الخطة صراحةً — **ولا خطة في هاتين الدفعتين تفعل**.
> 2. **لا تغيّر عتبات M1-M14 ولا أرقام كاتالوج فيصل.**
> 3. أيّ تعديلٍ يمسّ الدخول/الوقف/الأهداف/المستويات ⇒ يلزمه `LOGIC_VERSION` — **ولا خطة
>    هنا تستدعيه**؛ إن وجدت نفسك مضطرًّا فهذي حالة `STOP`.
> 4. **لا `push` ولا `merge` ولا فتح PR** إلّا بطلب صريح من المالك.
> 5. **كلُّ قفلٍ/اختبارٍ يجب أن يسقط بالطفرة** (اكسر الكود عمدًا وتأكّد أن الاختبار يسقط)
>    — سكِلّ `lock-and-mutate`. القفلُ الذي لا يسقط ليس قفلًا.
> 6. رسائل الكوميت تنتهي بـ:
>    `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`

## ترتيب التنفيذ والحالة — الدفعة الأولى (014–029)

| # | العنوان | الأولوية | الجهد | الخطر | يعتمد على | الفئة | الحالة |
|---|---------|:---:|:---:|:---:|---|---|---|
| 014 | تقويم السوق: حارس تقادم + بيانات 2027 (قنبلة موقوتة صامتة تنفجر ~2027-01-01) | P1 | S | LOW | — | bug | ✅ DONE (2026-08-15) |
| 015 | `press_radar`: أرضية تغطية (خنق ياهو يكتم جلسةً كاملة صامتًا) | P1 | S | LOW | — | bug | ✅ DONE (2026-08-15) |
| 016 | بوّابة فحصٍ ساكن في CI (ruff F-codes + pyflakes، تقريرٌ أولًا) | P1 | S | LOW | — | dx | ✅ DONE (2026-08-15) |
| 021 | تحصين وكيل Cline (توكن كتابة على بيانات قابلة للحقن) + تعميم كتم الأسرار | P1 | M | LOW | — | security | ✅ DONE (2026-08-15) |
| 017 | تخبئة `_fetch_splits` بالتشغيلة (‏300+ نداء ياهو مكرّر/تشغيلة) | P2 | S | LOW | — | perf | ✅ DONE (2026-08-15) |
| 018 | كرت 4س يطبع «ذيل مسح ثم استعادة (تأكيد)» على أسهم لم تمسح | P2 | S | MED | — | bug | ✅ DONE (2026-08-15) |
| 019 | ثلاثة عيوب مفتاح-متخيَّل/حدّي في العرض (`all_zones` · شورت 0% · `fintel_short`) | P2 | S | LOW | — | bug | ✅ DONE (2026-08-15) |
| 020 | مصالحة تعليقات CONFIG/الترويسة (توثيق يكذب: بوّابات لينة تُوسَم إلزامية) | P2 | S | LOW | — | docs | ✅ DONE (2026-08-15) |
| 023 | اختبارات توصيف لدوال الصيّادين المكشوفة (ledger/outcomes/e2_recover/press_radar) | P2 | M | LOW | — | tests | ✅ DONE (2026-08-15) |
| 027 | تصادم كتابة السجلّ المشترك (`git_save` يمحو صفوف صيّاد) + إعادة القراءة O(n) | P2 | M | MED | 023 (مُستحسَن) | bug | ✅ DONE (2026-08-15) |
| 022 | `.env.example` + جدول متغيّرات بيئة الصيّادين | P3 | S | LOW | — | dx | ✅ DONE (2026-08-15) |
| 024 | `e2_recover`: تخطّي الملخّص المختلف التاريخ بدل دمجه تحت تاريخٍ خطأ | P3 | S | LOW | — | bug | ✅ DONE (2026-08-15) |
| 025 | `cache: pip` للـworkflows البحثية (‏69/70 بلا تخبئة) | P3 | S | LOW | — | dx | ✅ DONE (2026-08-15) |
| 026 | حساب حزمة المؤشّرات/interp مرّةً/تشغيلة + مشاركة `.info` (مخاطرة الأقفال) | P3 | M | MED | 023 | perf | 🔴 مرفوضة (تمسّ جذورًا مقفولة · المكسب وقت CI · الخطر انزياح صامت في المُسلَّم) |
| 028 | مُرشِّح أقسام اختياريّ لـ`test_bot.py` (يبقى بوّابة الخروج-0 كما هي) | P3 | M | MED | — | dx | 🔴 مرفوضة (تمسّ مسطرة البوّابة نفسها — والعيبُ من صنفها وقع فعلًا 2026-08-15) |
| 029 | **spike:** هيكل تجارب مشترك للـ25 أداة `*_arms.py`/`*_scan.py` | P3 | L | LOW | — | direction | 🔴 مؤجَّلة (إعادةُ الهيكلة تُجوّف الأقفال بصمت — درسٌ مدوَّن) — **⚠️ كُمِّم حجمُها في تدقيق 08-21، انظر «توجيه» أدناه** |

القيم: `TODO` · `IN PROGRESS` · `DONE` · `BLOCKED (السبب في سطر)` · `REJECTED (المبرّر في سطر)`.

**سجلُّ تنفيذ الدفعة الأولى (2026-08-15 — أمر المالك «سو كل شي»):** ‏13 خطةً نُفِّذت
ودُفعت في كوميتين `c081fc3` و`1fb538e` (السويّة ‏2759 · صفر فشل)، وثلاثٌ لم تُنفَّذ بسند
(‏026 · 028 · 029).

---

## ⚠️⚠️ تنبيهٌ حاسمٌ لكل من ينفّذ الدفعة الثانية (030–037)

**هذه الخطط مؤرَّخةٌ على `origin/main` (`5cb88df`) — والكودُ الذي تعدّله موجودٌ هناك،
لا على لقطةٍ أقدم.** إن كان الرأسُ المحلّيُّ خلف `origin/main` فالملفّاتُ والدوالُّ التي
تشير إليها الخطط (‏`operator_entry_live.py` · `kasih_scan.py` · `liq_tier` ·
`gate_probe.py` …) **غيرُ موجودةٍ في الشجرة**. **قبل تنفيذ أيّ خطةٍ من 030–037 يجب
أن يكون الرأسُ `5cb88df` أو خلَفًا له** (‏`git merge --ff-only origin/main`). كلُّ
خطةٍ تحمل شرطَ `STOP` صريحًا إن لم يكن كذلك (فاشلٌ-آمن). ‏✅ تحقّق 2026-08-21: الشجرة
حُدِّثت إلى `5cb88df` والكودُ حاضر.

## ترتيب التنفيذ والحالة — الدفعة الثانية (030–037)

أُنشئت بأمر `/improve` (‏تدقيقٌ ثانٍ 2026-08-21) على `origin/main` `5cb88df`، بعد
مصالحة التقادم. ‏030–033 كانت مسوَّدةً في جلسةٍ سابقة (‏تدقيق الرقعة الجديدة)؛ ‏034–037
من هذي الجولة. أربعةُ وكلاءِ قراءة-فقط مسحوا الكودَ الجديد، وكلُّ سطرٍ مُقتبَسٍ فُتِح
والتُقط بنفسي على الشجرة الحيّة.

| # | العنوان | الأولوية | الجهد | الخطر | يعتمد على | الفئة | الحالة |
|---|---------|:---:|:---:|:---:|---|---|---|
| 030 | `pullback_live` يُعيد دفعَ ددوب «هنا الدخول» عند إخفاق الإرسال | P1 | S | LOW | — | bug | ✅ DONE (مدموجٌ في `origin/main` · `pullback_live.py:163`) |
| 031 | `git_save` يدمج `op_entry_state.json` بالمفتاح (رنّرا حيّان يدهسانه) | P2 | M | MED | 030 (مُستحسَن) | bug | ✅ **مُنفَّذةٌ — بانتظار الالتزام** (‏`Super_stock.py`+`test_bot.py` فقط · **201 إضافة · صفر حذف** · السويّة **‏3230 نجح · 0 فشل** على `python3.11` · **‏6/6 طفراتٍ سقطت بأقفالها المسمّاة بلا انهيار**). 🛑 **شرطُ `STOP` الأوّل وقع فعلًا فصُحّحت القاعدة:** الخطةُ افترضت `{رمز: {date, sent}}` **وهو غيرُ موجود** — القيمتان مختلفتا الشكل: نصُّ تاريخٍ ISO للرموز (`:13010`) وقاموسٌ **بلا مفتاح `date`** لمفاتيح `LIQ:` (`:13515`) مرجعُه `anchor_ms`. المعتمَدُ **قاعدةٌ رباعيّةٌ بالشكل لا بالبادئة** (‏وحيدٌ يُبقى · نصّان ⇒ الأحدث · قاموسان ⇒ أحدثُ `anchor_ms` كاملةً وعند التساوي اتّحادُ `sent` وأقصى علاماتِ المياه · ونوعان مختلفان ⇒ المحلّيّ = سلوكُ اليوم). 🔑 **والعلّةُ أدقُّ ممّا وُصفت:** `pullback_live` **لا يكتب `LIQ:` إطلاقًا** لكنه يدفع الملفَّ كاملًا بقواميسَ بائتة فيدهس تقدّمَ المراحل. ⛔ **ولا تعميم:** `OPM7` يقفل بقاءَ أيّ ملفٍّ قاموسيٍّ آخر «آخر-كاتبٍ-يفوز» بت-بت · و`company_cache.json` (يتغيّر بتشغيل السويّة) **يُستبعَد من الكوميت** · 2026-08-21 |
| 032 | `kasih_scan`/`kasih2_scan` يخرجان بغير-صفر عند نقص تغطية S3 | P2 | S | LOW | — | bug/tests | TODO |
| 033 | حذف خريطتَي وسمٍ ميّتتين (`_KASIH_F2_AR` · `_KASIH2_SHORT`) | P3 | S | LOW | — | tech-debt | TODO — **+ الثابت الميّت `FLOOR_TIER_SWEEP`، انظر «إضافة إلى 033»** |
| 034 | كرتُ النبض `Px` يُظهر التصنيفَ الحقيقيّ (قوي/متوسط/ضعيف) ويُبقي شارةَ «توليفة» | P1 | S | LOW | — | bug | ✅ **DONE (مدموج `bbbb74f` في main)** — منفَّذٌ في الوركتري `agent-a88e74f268b0d589c` وراجعتُه: `Super_stock.py` داخل النطاق [‏`LOGIC_VERSION`/`kasih_j1`/`kasih2_wave_feats` بت-بت] · السويّة **‏3216 نجح · 0 فشل** على python3.11 · أقفال PX1-PX4 مفرِّقةٌ ومُثبَتةٌ بالطفرة · دُمج ملفّان فقط (`Super_stock.py`+`test_bot.py`) و`company_cache.json` مُستبعَدٌ بالتحقّق · الدفعُ لـ`origin` بيد المالك · 2026-08-21 |
| 035 | حارسُ تغطيةٍ في مِجَسّات الجلسة الستة (‏green-zero على مجتمعٍ منحاز) | P1 | S | LOW | — | bug/tests | ✅ **DONE (ملتزمٌ `59ca404` · مدموجٌ `3e54a5c` في main)** — السويّةُ بعد الدمج **‏3220 نجح · 0 فشل** على `python3.11` (‏⚠️ `python3` المحلّيّ 3.9.6 يسقط على `sys.stdlib_module_names` — قيدُ بيئةٍ لا كود) · الدفعُ لـ`origin` بيد المالك · (راجعتُه قبل الالتزام: `probe_common.coverage_bad` نقيّةٌ ومنطقُها صحيح · ستّةُ مِجَسّاتٍ تحرس `return 3` بعد بوّابة الصفر · `kasih2_red_stats` أرضيةٌ ⟶ `return 5` · انحرافُ `alert_filter_check` **صحيحٌ ومسنود** [`by_sym` كونُ الجلبِ لا `rows`] · السويّة **‏3216 نجح · 0 فشل** على python3.11 · أقفال PC1-PC2 مفرِّقةٌ ومُثبَتةٌ بالطفرة [`>`↔`<`] · الدمجُ 3-way نظيفٌ [هنكاتُ test_bot.py غيرُ متداخلة مع 034 · `Super_stock.py` غيرُ ممسوس فـ`_event_j1` محفوظ] · 2026-08-21) |
| 036 | `git_save` يدمج `weekly_watchlist.json` بالمفتاح (كرونٌ متزامن يدهس القائمة) | P2 | M | MED | 031 (مُستحسَن) | bug | TODO |
| 037 | مصالحة `CLAUDE.md`: 5 أدوات غائبة + جردُ workflows بائت | P3 | S | LOW | — | docs | TODO |

## ملاحظات الاعتماديات والترتيب (الدفعة الثانية)

- **‏034 و035 أعلى رافعةٍ ويُنفَّذان أوّلًا:** كلاهما HIGH-confidence · S · LOW-risk ·
  تحقّقٌ نظيف · ويطابقان قيمَ المستودع المدوَّنة. ‏034 عيبُ عرضٍ يمسّ المالك مباشرةً
  (‏التصنيفُ الخطأ في السطر الذي يقرؤه من الإشعار — يكسر أمرَه 2026-08-20). ‏035 يسدّ
  صنفَ «الرقم الأخضر الكاذب» الذي هُدِّم منه المستودعُ قبلًا (‏حادثةُ `exit_stop_arms`
  المقيسة) على ستّ أدواتٍ يقرأ المالكُ مُخرَجَها ليقرّر («اطفي M0» · «شغّل c1»).
- **‏031 قبل 036 (مُستحسَن):** كلاهما يعدّل بلوكَ حلّ التعارض في `git_save` لملفّات
  الحالة القاموسية. ‏036 أعلى خطرًا (MED): دمجٌ خاطئٌ يفقد مرشّحين جددًا أو **يُحيي
  سهمًا مشطوبًا** ⇒ خطوةُ تحقيقٍ إلزامية + أقفالٌ تُثبت الضمانات الثلاث.
- **‏032 و033 و037 مستقلّة.** ‏032 صنفُ 035 نفسِه لكن على أدواتٍ أخرى (‏S3 · أيام
  مفقودة) — لا تتداخل ملفّاتُهما. المرجعُ لكليهما `exit_stop_arms.coverage_bad`.

## توجيه (‏direction — خياراتٌ للمالك)

- **‏029 (الهيكل المشترك للأدوات البحثية) كُمِّم حجمُه ⇒ قيمتُه ارتفعت:** تدقيقُ 08-21
  قاس **‏~7 بلوكاتٍ منسوخةً عبر ~20 أداة (‏~2,000 من 6,550 سطرًا جديدًا)** —
  رياضيّاتُ Wilson (‏5 نسخٍ متباينة) · حلقةُ تنزيل S3 (‏3) · غلافُ Polygon · إخراجُ
  JSONL · حارسُ المفتاح · قارئُ سجلّ الرادار. **ونسختان تباعدتا فعلًا** (‏معالجةُ الأيام
  المفقودة) — وهو ما يُغذّي عيبَ 035. ⇒ ‏029 لم يعد «تنظيفًا نظريًّا» بل له نطاقُ
  انفجارٍ محدَّد، وينفّذ فيه 035 حارسَه أوّلًا. القرارُ للمالك: يبقى مؤجَّلًا أم يُفتَح
  بعد أن صار مقيسًا؟ — ويلزمه بوّابةُ تطابقٍ بت-بت لكل ذراعٍ منقولة.

## إضافةٌ إلى الخطة 033 (‏دون خطةٍ مستقلّة)

تدقيقُ 08-21 وجد ثابتًا ميّتًا ثالثًا من الصنف نفسِه: **`FLOOR_TIER_SWEEP` في
`exit_stop_arms.py`** (‏تعليقُه «‏تُقرأ لاحقًا» ولم يُوصَل قطّ · ظهورٌ وحيدٌ في المستودع
= تعريفُه). أنظفُ حلٍّ: يُحذف ضمن نطاق 033 (‏نفسُ صنف «كودٌ موجودٌ ولا يُنادى») — أو في
كوميتٍ صغيرٍ مستقلّ. تحقّق بـ`grep -n "TIER_SWEEP" exit_stop_arms.py` قبل الحذف، والتزم
بشرط «ظهورٌ وحيد».

## نتائج فُحصت ورُفضت في هذه الجولة (‏08-21 — لا تُعاد)

بالإضافة إلى مرفوضات الدفعة الأولى (التسعة الأصلية، في نسخة origin من الفهرس):

10. **مِجَسّات البحث تضع `apiKey` في الرابط وتطبع الاستثناء الخام بلا `_redact_secrets`
    المركزيّ** — كامنٌ لا تسريبَ حاليّ (‏`urllib` لا يحمل الرابطَ الكامل في `str(exc)` ·
    Actions يقنّع السرّ · لا شيءَ يُكتَب في مُخرَجٍ مدفوع). نفسُ صنف المرفوض السابق وما
    حصّنته 021. لا يستحقّ خطة (تحصينٌ دفاعيّ · وأيُّ علاجٍ يمسّ مفتاحًا محروقًا يلزمه
    تدويرٌ لا حذف).
11. **مِجَسّات الجلسة تقيس مجتمعًا غيرَ قابلٍ لإعادة الإنتاج** (‏`resolve_day` = «الآن» ·
    `_load_universe` حالةٌ حيّة) — قيدٌ موثَّقٌ سلفًا ومخفَّفٌ بتثبيت `GATE_DAY`.
    تشخيصٌ لا عيب — رافعةٌ منخفضة، يُترَك لـ029 أو للعقود المسبقة.
12. **workflows الجديدة بلا صلاحيات/تخبئة/تحصينِ حقن** — فُحص: كلُّ الـ20 لها `cache:
    pip` و`permissions:` مُقيَّدة والمدخلاتُ عبر `env:` حصرًا. نظيف.
13. **تغطيةُ اختبارِ المسارات الإنتاجية الجديدة** — مغطّاةٌ في `test_bot.py`. لا فجوةَ حرجة.
14. **`CONCURRENCY-01` العامّة** — لم تُرفَض بل **حُوّلت إلى 036** (weekly_watchlist) و031
    (op_entry_state). الملفّاتُ القاموسية ذاتُ الكاتب الواحد تبقى «آخر-كاتبٍ-يفوز» بأمان.
