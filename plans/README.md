# خطط التنفيذ — تدقيق عميق 2026-07-28

أُنشئت بأمر `/improve deep` على الرأس `a6457bf` (فرع `improve/deep-audit-20260728-224701`).
**مرحلة التدقيق والتخطيط فقط — لم يُعدَّل أي كود مصدر.**

> **لكل منفِّذ:** اقرأ ملف الخطة **كاملًا** قبل البدء · نفّذ كل أمر تحقّق وتأكّد من
> النتيجة المتوقّعة قبل الخطوة التالية · التزم بشروط `STOP` ولا تجتهد · حدّث صفّك في
> الجدول أدناه عند الانتهاء.
>
> **بوّابة الاختبار الرسمية الوحيدة:** `python3 test_bot.py` — النجاح = **خروج 0 وصفر
> فشل**، لا عدد مقتبس (الأعداد تتقادم). الأساس عند كتابة هذه الخطط: **1305 نجح · 0 فشل**.
>
> **قواعد مُلزِمة من `CLAUDE.md` تسري على كل خطة:**
> 1. **لا تلمس جذور الاختيار** (`analyze_ticker` · `rank_key` · `select_top` ·
>    `classify_tier` · `entry_status` · `backtest_symbol` · `apply_short_gate`) إلا
>    إذا نصّت الخطة على ذلك صراحةً وذكرت رفع `LOGIC_VERSION`.
> 2. **لا تغيّر عتبات M1-M14 ولا منهجية فيصل المحسومة.** أي خطة هنا لا تفعل ذلك.
> 3. **لا `push` ولا `merge` ولا فتح PR** إلا بطلب صريح من المالك.
> 4. أي تعديل يمسّ الدخول/الوقف/الأهداف/المستويات ⇒ ارفع `LOGIC_VERSION`. **لا خطة
>    في هذه الدفعة تستدعي ذلك** — إن وجدت نفسك مضطرًّا فهذي حالة `STOP`.
> 5. رسائل الكوميت تنتهي بـ:
>    `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`

## ترتيب التنفيذ والحالة

| # | العنوان | الأولوية | الجهد | يعتمد على | الحالة |
|---|---------|----------|-------|-----------|--------|
| 001 | اختبارات Characterization للمسار الحيّ (أساس إلزامي) | P1 | M | — | **DONE** (1305 → 1323 · +18 · 5 طفرات) |
| 002 | إحياء قياس «ربع الساعة» في رادار الانطلاق (ميت في الإنتاج) | P1 | S | 001 | **DONE** (1323 → 1331 · +8 · طفرتان) |
| 003 | ردم الفلوت اليومي يستعمل مصدرًا ميتًا (`ce_float_info`) | P1 | S | 001 | **DONE** (1331 → 1341 · +10 · طفرتان) |
| 004 | تحصين بوّابتَي M13/M14 ضد القيم غير الرقمية (انهيار الفرز كاملًا) | P1 | S | 001 | **DONE** (1341 → 1345 · بصمة 19 حالة IDENTICAL · 3 طفرات) |
| 005 | حارس استثناء لكل رمز في كتلة إثراء `scan_market` | P2 | S | 001 | **DONE** (1345 → 1349 · 3 طفرات · قفل عضوية) |
| 006 | `monitor_pullback`: تحميل مجمَّع بدل نداء لكل رمز | P2 | M | 001 | **DONE** (1349 → 1352 · 3 طفرات) |
| 007 | تثبيت الاعتماديات + فحص دخان حيّ (CI أخضر وإنتاج مكسور) | P2 | S | — | **DONE** (1365 → 1370 · طفرتان) |
| 008 | كشف «تجديد الجمعة لم يحدث» (كرون مُسقَط بلا أثر) | P2 | S | 001 | **DONE** (1352 → 1365 · 3 طفرات) |
| 009 | إصلاح مسار artifact الـCSV في `daily_screener.yml` (نمط بلا منتِج) | P3 | XS | — | TODO |
| 010 | صلاحيات GitHub Actions الصريحة + منع حقن مدخلات الـdispatch | P3 | S | — | TODO |
| 011 | تنظيف الدوال الميتة + مزامنة التوثيق (D9 وغيره) | P3 | S | — | TODO |
| 012 | **تحقيق (قراءة فقط):** متى يصل التقرير اليومي فعلًا؟ | P2 | S | — | TODO |
| 013 | **تحقيق (قراءة فقط):** أساس الحجم في رادار الانطلاق يخلط البريماركت | P3 | S | — | TODO |

القيم: `TODO` · `IN PROGRESS` · `DONE` · `BLOCKED (السبب في سطر)` · `REJECTED (المبرّر في سطر)`

## ملاحظات الاعتماديات

- **001 قبل 002/003/004/005/006/008**: كلها تلمس مسارات حيّة حسّاسة (الفرز · الحالة ·
  التنبيه). الخطة 001 تُثبّت **سلوك اليوم** باختبارات توصيف قبل أي تعديل، فيصير أي
  انحراف مرئيًّا فورًا. القاعدة من `CLAUDE.md`: «القفل الذي لم يسقط مرّة واحدة عمدًا ليس
  قفلًا» — فكل خطة تطلب **اختبار طفرة** (كسر الكود عمدًا والتأكّد أن الاختبار يسقط).
- **007 و009 و010 و011 مستقلّة** تمامًا ويمكن تنفيذها بأي ترتيب.
- **012 و013 تحقيقان لا تعديل فيهما** — مخرجهما تقرير + قرار مالك، وقد يولّدان خططًا لاحقة.

### ما تعلّمناه من تنفيذ 001 (2026-07-28) — يسري على 004 و005

- **F3 وF4 صارا مؤكَّدَين بالاستنساخ لا بالقراءة.** الطفرتان M4/M5 (محاكاة إصلاحَي 005
  و004) أسقطتا **بالضبط** الاختبارات المُعلَّمة ⇒ الخطتان ستنقلبان كما هو مخطَّط.
- **004 تقلب خمسة اختبارات لا اثنين** (أُضيف M13 بنفس الانكشاف + اختبار «التناقض»
  الذي يزول بالإصلاح). الجدول الدقيق داخل الخطة 004 خطوة 5.
- **005 تقلب اختبارًا واحدًا بالضبط**، والاختبار المجاور «المسار السليم» يجب أن يبقى
  أخضر — سقوطه = اللفّ ابتلع المسار الصحيح أيضًا.
- **الطفرة M3 كشفت فجوة تغطية حقيقية**: قفلا `monitor_pullback` الساقطان كانا
  **من التوصيف الجديد حصرًا** — لا اختبار قائم التقط الطفرة.
- 🔴 **درس إجرائي:** مِجَسّ تشخيصي كتبتُه ضرب شبكة ياهو فعليًّا لأن `S.yf` لم يُضبَط
  رغم أن نصّ الطباعة قال `yf=None`. `SUPER_STOCKS_TESTING=1` يحرس **git فقط لا الشبكة**
  ⇒ أي مِجَسّ يدوي يجب أن يحقن الجالبات صراحةً ويتحقّق من الحقن لا من نصّ الطباعة.

## النتائج المؤكَّدة التي صارت خططًا

| الرمز | النتيجة | الدليل | التصنيف | الخطة |
|-------|---------|--------|---------|-------|
| F1 | قياس «ربع الساعة» (`_fire_sustain`) ميت في الإنتاج لسببين مستقلَّين | `ignition_live.py:624` · `ignition_e2_assemble.py:296` · `Super_stock.py:9457` | مؤكَّد | 002 |
| F2 | ردم الفلوت اليومي يستدعي `ce_float_info` وهي ميتة منذ 07-24 | `Super_stock.py:11172-11178` مقابل `11252-11259` | مؤكَّد | 003 |
| F3 | `apply_float_gate` **و**`apply_short_gate` تنهاران على قيمة غير رقمية ⇒ سقوط الفرز كلّه بلا حفظ حالة | `Super_stock.py:7069,7079` · `7023,7027` مقابل الحارس في `7115-7119` | **مؤكَّد** (اُستُنسخ 2026-07-28: نصّ⇒`TypeError` · `NaN`⇒`ValueError` · dict⇒`TypeError` — على البوّابتين) | 004 |
| F4 | `scan_market` كتلة الإثراء بلا حارس لكل رمز | `Super_stock.py:7400-7410` مقابل `7785-7802` | **مؤكَّد** (اُستُنسخ: استثناء في رمز واحد يتسرّب من `scan_market`) | 005 |
| F5 | `monitor_pullback` ينادي `download_history` لكل رمز على حدة | `Super_stock.py:9770` · `1142-1169` | مؤكَّد | 006 |
| F6 | اعتماديات غير مثبَّتة + اختبارات بلا شبكة ⇒ CI أخضر وإنتاج مكسور | `requirements.txt` · `.github/workflows/tests.yml` | مؤكَّد | 007 |
| F7 | لا كشف لسقوط كرون التجديد الأسبوعي | `Super_stock.py:6887-6899` · `daily_screener.yml:60` | مؤكَّد | 008 |
| F8 | `path: screener_report_*.csv` لا منتِج له إطلاقًا | `daily_screener.yml:71` · `Super_stock.py:10777` | مؤكَّد | 009 |
| F9 | 10 workflows بلا `permissions:` + حقن مدخل في `acc_verify.yml:43` | `.github/workflows/` | مؤكَّد | 010 |
| F10 | 7 دوال ميتة + `CLAUDE.md §D9` يصف وصلًا مقطوعًا | `Super_stock.py:1040,4538,4585,5674,5697,5728,5751,8081` | مؤكَّد | 011 |
| F11 | تصحيح الكرون 07-28 قِيس على **بدء الجوب** لا **وصول التقرير** | `daily_screener.yml:17-30` · `Super_stock.py:11067,11242` | يحتاج بيانات حيّة | 012 |
| F12 | أساس حجم `_ignition_signal` يخلط البريماركت أول 30 دقيقة | `Super_stock.py:8186-8189,9265-9268,9310` | يحتاج تجربة | 013 |

## نتائج فُحصت ورُفضت (لا تُعاد في الجولة القادمة)

1. **«الـassembler يسجّل `emitted` لا `delivered` في `ignition_log.json`»** — مرفوض:
   قرار مقصود وموثّق. `e2_recover.py:131-139` ينصّ صراحةً على أن الإعادة تكون
   للمُطلَقة (`alert_emitted`)، و`_delivered_fires` (`e2_recover.py:172`) يعرض
   المُسلَّمة **بشكل منفصل** عمدًا. المقياس هو جودة **إشارة الرادار** لا موثوقية تلغرام.
2. **«التزامن يُلغي تجديد الجمعة»** (`daily_screener` + `pullback_monitor` بمجموعة
   `super-stocks-state`) — مرفوض عمليًّا: GitHub يلغي **المعلَّق** عند وصول أحدث منه في
   نفس المجموعة، لكن `pullback_monitor` مهلته 15د والفاصل 30د ⇒ الجاري ينتهي قبل وصول
   التالي، فنافذة تعليق التجديد أقصر من دورة المراقب. (السقوط الحقيقي المحتمل = إسقاط
   GitHub للكرون نفسه، وهو **F7/الخطة 008**.)
3. **«`POLYGON_API_KEY` يتسرّب عبر `&apiKey=` في `Super_stock.py:8997`»** — مرفوض
   حاليًّا: لا مسار إنتاجي يسجّل ذلك الاستثناء (`8999` يبتلعه بصمت و`8987` يطبع الرمز
   والحالة فقط)، والمفتاح يُرسَل في `Authorization` في كل بقية المنافذ. **يبقى مسجَّلًا
   كخطر كامن**: أي `log(e)` يُضاف مستقبلًا في `polygon_base_trades` يسرّبه، و
   `_redact_secrets` (`6574`) يغطّي توكن تلغرام فقط. يُعاد النظر عند أول تعديل هناك.
4. **دوال محفوظة عمدًا** (`key_levels_block` · `h4_levels_block` · `position_size_line`
   · `acc_line` · `silent_accumulation` · `half_down_target`) — **ليست ميتة، بل مؤجَّلة
   بقرار موثّق في `CLAUDE.md`. لا تُحذف.** (الخطة 011 تستثنيها صراحةً.)
5. **«`entry_ref` = سعر الترشيح لا متوسط الدفعات»** — قرار مالك محسوم 2026-06-24،
   موثّق في `CLAUDE.md`. ليس عيبًا.
6. **«الوقف 5-7% داخل منطقة مسح السيولة»** — اختُبر رسميًّا (T-STOP,
   `stop_sweep_prereg.md`) وفشل البديل على ثلاث سنوات. مُغلَق.
7. **«`removed` تُصفَّر عند التجديد فيعود المشطوب بالستوب للترشيح»** — سلوك مقصود:
   `EXCLUDE_STOPPED_FROM_RENEWAL` يُطبَّق **قبل** التصفير (`Super_stock.py:10822-10824`)،
   والقائمة الجديدة تُبنى بأسبوع جديد. لا فقد حالة (الأرشفة في `history`).
8. **`_stamp_restore` لا يُرجِع `triggered_date`** (`pullback_live.py:36-38`) — أثر
   تجميلي فقط: الحقل يُعاد كتابته في الدورة التالية عند إعادة الإطلاق. لا يستحقّ تعديلًا.
9. **`send_telegram` لا يعيد المحاولة على 429** — مقايضة مقصودة: المستدعون
   (`pullback_live` · `ignition_live` · `split_hunter`) يحترمون القيمة المرجَعة ويعيدون
   المحاولة بالدورة التالية بعد استرجاع الأختام. مغطّى بالتصميم الحالي.
10. **نمو `weekly_watchlist.json` (334 كيلوبايت)** — كل السجلّات التراكمية مسقوفة
    (`save_watchlist:6872-6880` · `accumulate_explosions:7313-7321` ·
    `record_reject_stats:7339-7340`). لا نمو غير محدود.
11. **`requests` بلا `timeout`** — فُحص كل الملفات آليًّا: الموضع الوحيد
    (`telegram_collect.py:119`) هو lambda افتراضي والمستدعون يمرّرون `timeout=60`
    صراحةً. لا انكشاف.
12. **`bare except`** — صفر مواضع في المستودع كلّه (فُحص آليًّا).
