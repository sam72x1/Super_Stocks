# خطط التنفيذ — تدقيق عميق 2026-08-15

أُنشئت بأمر `/improve deep` على الرأس `4e9b143`. **مرحلة التدقيق والتخطيط فقط — لم يُعدَّل
أيّ كود مصدر.** ستّة وكلاء قراءة-فقط مسحوا الكود، وكلُّ سطرٍ مُقتبَسٍ فُتِح والتُقط بنفسي.

> **الدفعةُ السابقة (001–013، تدقيق 2026-07-28) مؤرشَفةٌ في `plans/archive/`** — كلُّها
> `DONE` عدا 013 (`BLOCKED_EXTERNAL`). لا تُعاد، ولم أُكرّر أيًّا من نتائجها أو من
> مرفوضاتها الاثنتي عشرة. الترقيمُ يُكمِل من 014 (متتابعٌ عبر تاريخ المشروع).

> **لكل منفِّذ:** اقرأ ملفّ الخطة **كاملًا** قبل البدء · شغّل كلّ أمر تحقّق وتأكّد من
> النتيجة المتوقّعة قبل الخطوة التالية · التزم بشروط `STOP` ولا تجتهد · حدّث صفّك في
> الجدول أدناه عند الانتهاء (إلّا إذا أبلغك مراجعٌ أنه يتولّى الفهرس).
>
> **بوّابة الاختبار الرسمية الوحيدة:** `python3 test_bot.py` — النجاح = **خروج 0 وصفر
> فشل**، لا عدد مقتبس. البيئة **بلا إنترنت** (كل الجالبات محقونة) — أي اختبار جديد يحقن
> جالباته ولا يمسّ الشبكة ولا ملفّات الحالة الحقيقية (استعمل مساراتٍ مؤقّتة).
>
> **قواعد مُلزِمة من `CLAUDE.md` تسري على كل خطة:**
> 1. **لا تلمس جذور الاختيار** (`analyze_ticker` · `rank_key` · `select_top` ·
>    `classify_tier` · `entry_status` · `backtest_symbol` · `apply_short_gate` ·
>    `apply_float_gate`) إلّا إذا نصّت الخطة صراحةً — **ولا خطة في هذه الدفعة تفعل**.
> 2. **لا تغيّر عتبات M1-M14 ولا أرقام كاتالوج فيصل.** الخطة 020 تُصحّح **تعليقات** فقط
>    (صفر تغيير قيمة/سلوك).
> 3. أيّ تعديلٍ يمسّ الدخول/الوقف/الأهداف/المستويات ⇒ يلزمه `LOGIC_VERSION` — **ولا خطة
>    هنا تستدعيه**؛ إن وجدت نفسك مضطرًّا فهذي حالة `STOP`.
> 4. **لا `push` ولا `merge` ولا فتح PR** إلّا بطلب صريح من المالك.
> 5. **كلُّ قفلٍ/اختبارٍ يجب أن يسقط بالطفرة** (اكسر الكود عمدًا وتأكّد أن الاختبار يسقط)
>    — سكِلّ `lock-and-mutate`. القفلُ الذي لا يسقط ليس قفلًا.
> 6. رسائل الكوميت تنتهي بـ:
>    `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`

## ترتيب التنفيذ والحالة

| # | العنوان | الأولوية | الجهد | الخطر | يعتمد على | الفئة | الحالة |
|---|---------|:---:|:---:|:---:|---|---|---|
| 014 | تقويم السوق: حارس تقادم + بيانات 2027 (قنبلة موقوتة صامتة تنفجر ~2027-01-01) | P1 | S | LOW | — | bug | TODO |
| 015 | `press_radar`: أرضية تغطية (خنق ياهو يكتم جلسةً كاملة صامتًا) | P1 | S | LOW | — | bug | TODO |
| 016 | بوّابة فحصٍ ساكن في CI (ruff F-codes + pyflakes، تقريرٌ أولًا) | P1 | S | LOW | — | dx | TODO |
| 021 | تحصين وكيل Cline (توكن كتابة على بيانات قابلة للحقن) + تعميم كتم الأسرار | P1 | M | LOW | — | security | TODO |
| 017 | تخبئة `_fetch_splits` بالتشغيلة (‏300+ نداء ياهو مكرّر/تشغيلة) | P2 | S | LOW | — | perf | TODO |
| 018 | كرت 4س يطبع «ذيل مسح ثم استعادة (تأكيد)» على أسهم لم تمسح | P2 | S | MED | — | bug | TODO |
| 019 | ثلاثة عيوب مفتاح-متخيَّل/حدّي في العرض (`all_zones` · شورت 0% · `fintel_short`) | P2 | S | LOW | — | bug | TODO |
| 020 | مصالحة تعليقات CONFIG/الترويسة (توثيق يكذب: بوّابات لينة تُوسَم إلزامية) | P2 | S | LOW | — | docs | TODO |
| 023 | اختبارات توصيف لدوال الصيّادين المكشوفة (ledger/outcomes/e2_recover/press_radar) | P2 | M | LOW | — | tests | TODO |
| 027 | تصادم كتابة السجلّ المشترك (`git_save` يمحو صفوف صيّاد) + إعادة القراءة O(n) | P2 | M | MED | 023 (مُستحسَن) | bug | TODO |
| 022 | `.env.example` + جدول متغيّرات بيئة الصيّادين | P3 | S | LOW | — | dx | TODO |
| 024 | `e2_recover`: تخطّي الملخّص المختلف التاريخ بدل دمجه تحت تاريخٍ خطأ | P3 | S | LOW | — | bug | TODO |
| 025 | `cache: pip` للـworkflows البحثية (‏69/70 بلا تخبئة) | P3 | S | LOW | — | dx | TODO |
| 026 | حساب حزمة المؤشّرات/interp مرّةً/تشغيلة + مشاركة `.info` (مخاطرة الأقفال) | P3 | M | MED | 023 | perf | TODO |
| 028 | مُرشِّح أقسام اختياريّ لـ`test_bot.py` (يبقى بوّابة الخروج-0 كما هي) | P3 | M | MED | — | dx | TODO |
| 029 | **spike:** هيكل تجارب مشترك للـ25 أداة `*_arms.py`/`*_scan.py` | P3 | L | LOW | — | direction | TODO |

القيم: `TODO` · `IN PROGRESS` · `DONE` · `BLOCKED (السبب في سطر)` · `REJECTED (المبرّر في سطر)`.

## ملاحظات الاعتماديات والترتيب

- **P1 أولًا (014·015·016·021):** أعلى رافعةٍ وكلُّها آمنة/غير-جذرية وذات تحقّقٍ نظيف.
  014 و015 عيبان صامتان يمسّان المالك مباشرةً (014 ينفجر بعد ~4.5 شهر). 016 يسدّ صنفًا
  كاملًا لا تراه السويّة (بلا إنترنت). 021 تحصينٌ أمنيّ حقيقي (وكيلٌ ذاتيّ بتوكن كتابة).
- **023 قبل 026 و027 (مُستحسَن):** توفّر شبكةَ أمانٍ توصيفية (طفرة تُثبت الأقفال) قبل
  إعادة الهيكلة الحسّاسة. 026 و027 هما الأعلى خطرًا (يمسّان دوالّ مقفولة بت-بت والمسار
  المشترك `git_save`) — نُفّذا **آخرًا** وبعد بناء بوّابة التطابق.
- **الباقي مستقلّ** ويُنفَّذ بأيّ ترتيب. 018 يحمل حالة `STOP` صريحة: إن لمست `four_hour_context`
  الاختيارَ فهي قرارُ مالكٍ + `LOGIC_VERSION` (خارج نطاق الخطة).
- **026 و028 و029 أدنى رافعةٍ** (كلفةُ CI/بيئة تطوير) — أُدرجت لاكتمال «كلّ النتائج»؛
  026 خصوصًا مخاطرتُه أسوأُ من عائده، فأجّله ما لم يكن المسار اليوميّ مُقيَّدًا بالزمن فعلًا.

## الدفعة السابقة (مؤرشَفة — لا تُعاد)

`plans/archive/` تحوي خطط التدقيق 2026-07-28 (001–013): تثبيت اختبارات توصيف، إحياء «ربع
الساعة»، مصدر الفلوت الميت، تحصين M13/M14، حارس إثراء `scan_market`، تحميل الارتداد المجمَّع،
كشف سقوط كرون التجديد، تثبيت الاعتماديات، وغيرها — **كلُّها `DONE`** عدا 013
(`BLOCKED_EXTERNAL`: نافذة الرادار [0,30) لم تُراقَب في أيّ جلسة). موثَّقةٌ في `CLAUDE.md`.

## نتائج فُحصت ورُفضت في هذه الجولة (لا تُعاد)

1. **`segment_fires.json` مُخرَجٌ ميت (بلا قارئ)** — معروفٌ وموثَّقٌ سلفًا في `ignition_e2_assemble.py:322`
   وفي `CLAUDE.md`؛ أثرُه صفر (كتابةٌ مهدورة). لا يستحقّ خطة.
2. **كتمُ الأسرار Telegram-فقط** — كامنٌ لا تسريبَ حاليّ (كلُّ مسارات المفتاح تطبع نوعَ
   الاستثناء لا القيمة، وActions يقنّع الأسرار). أُدمج **دفاعًا عميقًا** في الخطة 021 لا خطةً مستقلّة.
3. **قرار عدم قفل الاعتماديات المتعدّية** — قرارٌ معماريٌّ موثَّقٌ للمالك (requirements.txt)،
   لا عيب؛ يبقى مرصودًا (وكيل perf أوصى بلقطة `pip freeze` دورية — اقتراحٌ لا خطة).
4. **`requests` بلا timeout** — فُحص: كلُّ الـ26 نداءً في `Super_stock.py` تحمل `timeout=`.
5. **حقن مدخلات workflow / صلاحيات مفقودة** على الـ70 workflow الإنتاجية — مُحصَّنةٌ سلفًا
   (كلٌّ يعلن `permissions:` ويمرّر المدخلات عبر `env:`)؛ الشاذُّ الوحيد وكيل Cline = الخطة 021.
6. **`send_telegram` بلا إعادة على 429** — مقايضةٌ مقصودة (المستدعون يعيدون بالدورة التالية).
7. **`entry_ref` = سعر الترشيح لا متوسط الدفعات** — قرارُ مالكٍ محسوم 2026-06-24، ليس عيبًا.
8. **الوقف 5-7% داخل منطقة المسح** — اختُبر رسميًّا (`T-STOP`) وفشل البديل ثلاث سنوات. مُغلَق.
9. **الدوالُّ المؤجَّلة عمدًا** (`key_levels_block` · `h4_levels_block` · `position_size_line`
   · `acc_line` · `silent_accumulation` · `half_down_target`) — ليست ميتة، بل مؤجَّلةٌ بقرارٍ
   موثَّق. لا تُحذف.
