# تحقيق 013 — هل ينتفخ أساس حجم رادار الانطلاق أول 30 دقيقة؟

---

## 📋 التسجيل المسبق (يُدفع **قبل** رؤية أي رقم)

> 🔴 هذا المستودع يعمل بالتسجيل المسبق (`*_prereg.md`) ويوثّق حادثة **سُحبت** فيها
> نتيجة لأن المقياس اختير بعد الأرقام. هذا القسم مدفوع في كوميت مستقلّ قبل التحليل.

### السؤال

`_ignition_signal` يشترط `vol_x = حجم آخر دقيقة ÷ متوسط الدقائق السابقة ≥ 3`، والنافذة
من `polygon_minute_bars(sym, minutes=30)` وهي **نافذة متدحرجة بالساعة الحائطية بلا أي
حدّ جلسة** (`Super_stock.py`: `frm = now_ms - minutes*60_000`). ⇒ بين جرس الافتتاح
وجرس+30 دقيقة تكون «الدقائق السابقة» **دقائق بريماركت** حجمها أصغر بمراتب، فيُقسَم حجم
سوقي على متوسط بريماركتي.

**السؤال:** هل `vol_x` للمرشّحين الخام في أول 30 دقيقة من الجلسة أعلى **منهجيًّا** منه
في بقيّة الجلسة؟

### المقياس (مثبَّت الآن)

- **الأساسي:** **وسيط** `vol_x` (لا المتوسط — الذيل يشوّه؛ نفس اختيار T-CMAG).
- **الشريحتان:** `trigger_bar_start` داخل [الافتتاح، الافتتاح+30د) مقابل خارجها.
  الافتتاح من `market_session_now` **بتاريخ الجلسة** (الصيف/الشتاء يزيحانه ساعة).
- **حدّ الحكم:** «ينتفخ» = وسيط الشريحة الأولى **يساوي 1.5 ضعف الثانية فأكثر**
  **و** كل شريحة فيها **20 مرشّحًا فأكثر**.
- **مساند ①:** نسبة `alert_emitted=True` في كل شريحة (هل الانتفاخ يعبر البوّابات؟).
- **مساند ②:** نسبة الإنذار الكاذب لكل شريحة عبر `ignition_log.json` + `_ignition_outcome`
  (لو توفّرت نتائج محسومة تبلغ `IGNITION_OUTCOME_MIN`=8).

### الأحكام الثلاثة الممكنة (لا رابع)

1. **«ينتفخ»** — الحدّان مستوفيان.
2. **«لا ينتفخ»** — العيّنة كافية والحدّ غير مستوفًى ⇒ **الفرضية سقطت** (نتيجة حقيقية).
3. **«العيّنة لا تكفي»** — شريحة تحت 20 ⇒ لا حكم.

🔴 **ممنوع** تحريك الحدّ أو تغيير المقياس بعد رؤية الأرقام. والتمييز بين (2) و(3)
إلزامي — المستودع يفرضه صراحةً.

### سقف النتيجة المسموح (محدَّد سلفًا)

**تقرير فقط.** أي تعديل على `_ignition_signal` أو `polygon_minute_bars` أو أي عتبة
(`IGNITION_VOL_MULT` · `minutes=30` · `min_bars`) **يحتاج تسجيلًا مسبقًا جديدًا وموافقة
المالك بالأرقام** — لأنه تغيير شرط إطلاق حيّ.

---

## ✅ النتيجة (2026-07-28/29)

### الحكم

# 🟡 «العيّنة لا تكفي» — **والسبب بنيوي لا إحصائي**

الشريحة الأولى **فارغة تمامًا (0 مرشّح)** لأن **الرادار لم يكن يراقب في تلك النافذة
أصلًا**، لا لأن الانتفاخ غير موجود. ⇒ **الفرضية لم تُختبَر بعد** (ليست «سقطت»).

### الأرقام

**المصدر:** 11 artifact من نوع `ign-assembled-*` (قراءة فقط عبر `gh run download`)
⇒ **10 جلسات فريدة** (2026-07-15 → 07-28). استُعمل الملفّ **المدموج على مستوى الجلسة**
لا ملفّات المقاطع (تفادي الازدواج)، وعند تكرار جلسة عبر artifacts تفوز الأكثر مرشّحين
(حتميّ). **مجموع الشرائح = مجموع المرشّحين ✓** (14 = 0 + 14 + 0 غير مصنَّف).

| الشريحة | العدد | وسيط `vol_x` | المتوسط | المدى | المُطلَق |
|---------|-------|--------------|---------|-------|----------|
| **[0, 30) أول نصف ساعة** | **0** | — | — | — | 0 |
| [30, ∞) بقيّة الجلسة | 14 | 4.55 | 6.39 | 3.2 – 17.9 | 10 |

الحدّ المسجَّل مسبقًا: 20 مرشّحًا لكل شريحة ⇒ **غير مستوفًى** ⇒ الحكم (3).

### لماذا الشريحة الأولى فارغة — الدليل

`segment_open/session.json` لكل جلسة يحمل `first_successful_poll_at` مقابل
`expected_open_iso`:

| الجلسة | الافتتاح | أول مسح ناجح | تأخّر التغطية |
|--------|----------|--------------|----------------|
| 2026-07-15 | 13:30 | 15:02 | **+93 د** |
| 2026-07-16 | 13:30 | 15:14 | **+104 د** |
| 2026-07-17 | 13:30 | 14:54 | **+84 د** |
| 2026-07-20 | 13:30 | 15:21 | **+111 د** |
| 2026-07-21 | 13:30 | 15:18 | **+108 د** |
| 2026-07-22 | 13:30 | 15:17 | **+108 د** |
| 2026-07-23 | 13:30 | 15:25 | **+115 د** |
| 2026-07-24 | 13:30 | 08:51 | −279 د (تشغيل يدوي قبل الجرس) |
| 2026-07-27 | 13:30 | 15:51 | **+141 د** |
| 2026-07-28 | 13:30 | 15:34 | **+125 د** |

**وسيط تأخّر التغطية 108 دقائق (84–141).** ⇒ نافذة [0,30) **خارج المراقبة في كل جلسة
مجدولة**، فصفر مرشّح فيها **نتيجة حتمية للتغطية لا قياس لـ`vol_x`**.

### تأكيد جانبي مهمّ (لم يكن مقصودًا)

هذه الأرقام **تُثبت مستقلّةً** ما وثّقه `CLAUDE.md` عن `start_coverage_late` وعن أن
الرادار «كان يفوّت أول ساعتين من كل يوم — بما فيها الافتتاح، وهو أنشط نافذة وسببُ
وجوده». ⇒ **تصحيح الكرون إلى `11 11` (2026-07-28) كان ضروريًّا فعلًا** — والبيانات هنا
تُصدّقه بأرقام مستقلّة عن تلك التي بُني عليها.

## 🚧 BLOCKED_EXTERNAL — ما يلزم للحسم

**البيانات المطلوبة:** جلسات رادار **تغطّي الافتتاح فعلًا** حتى تتراكم **20 مرشّحًا
خامًا فأكثر** داخل [0,30) — وهذا **غير متاح اليوم بأي حال** لأن التغطية بدأت بعد
الافتتاح بـ84-141 دقيقة في كل جلسة مسجَّلة.

**متى يصير متاحًا:** بعد أن يعمل كرون `11 11` (المُصلَح 2026-07-28) لعددٍ كافٍ من
الجلسات. بمعدّل 1.4 مرشّح/جلسة (14 ÷ 10) يلزم تقريبًا **15-20 جلسة مغطّاة** لبلوغ 20
مرشّحًا في الشريحة الأولى — أي **3-4 أسابيع تداول** تقريبًا من 2026-07-29.

**أين تُجلب:** artifacts `ign-assembled-<run_id>` من `ignition.yml` (احتفاظ 90 يومًا).

**المِجَسّ الآمن للحسم لاحقًا** (قراءة فقط · صفر شبكة إنتاج · صفر تشغيل حيّ):

```bash
# 1) تنزيل كل الجلسات المدموجة المتاحة
mkdir -p /tmp/e2dl && gh api \
  "repos/sam72x1/Super_Stocks/actions/artifacts?per_page=100" \
  --jq '.artifacts[] | select(.expired==false and (.name|startswith("ign-assembled")))
        | .workflow_run.id' \
| while read id; do
    gh run download "$id" -n "ign-assembled-$id" -D "/tmp/e2dl/$id" \
      -R sam72x1/Super_Stocks
  done

# 2) تشغيل التحليل بالمعيار المسجَّل مسبقًا **بلا تعديل**
python3 plans/013_probe.py     # ✅ محفوظ وجاهز — مُختبَر على البيانات الحالية
```

**شروط الحسم (من التسجيل المسبق — لا تُحرَّك):**
1. **20 مرشّحًا فأكثر في كل شريحة**، وإلا يبقى الحكم «العيّنة لا تكفي».
2. «ينتفخ» = وسيط [0,30) **يساوي 1.5 ضعف** وسيط [30,∞) **فأكثر**.
3. **بوّابة تغطية إلزامية:** تُحتسَب الجلسة فقط لو `first_successful_poll_at` لا يتجاوز
   `expected_open_iso` بأكثر من دقيقتين (`START_TOLERANCE_MIN`) — وإلا فنافذتها الأولى
   غير مراقَبة وإدخالها يُنتج نفس الصفر المضلِّل.
4. **صفر تغيير كود بلا تسجيل مسبق جديد وموافقة المالك** (تغيير شرط إطلاق حيّ).

## ⚠️ حدود الصدق

- العيّنة الكلّية صغيرة (**14 مرشّحًا خامًا في 10 جلسات**) حتى في الشريحة الثانية —
  فحتى لو امتلأت الأولى، بلوغ 20/شريحة يحتاج أسابيع.
- `session_complete` لم يُفحَص هنا؛ الملخّصات تحمل `termination=normal` لكن اكتمال
  الجلسة يحكمه المدقّق الصارم لا هذا الحقل.
- `trigger_bar_start` = **بداية** شمعة الدقيقة (Polygon) — الاصطلاح المستعمَل في التبويب.
- الجلسة اليدوية 2026-07-24 (‏08:51، قبل الجرس) دخلت العدّ الكلّي؛ استبعادها لا يغيّر
  الحكم (الشريحة الأولى تبقى صفرًا).

## 🧭 الأثر على الكود

**صفر.** لم يُعدَّل حرف واحد من `_ignition_signal` ولا `polygon_minute_bars` ولا أي
عتبة — وهو سقف النتيجة المحدَّد سلفًا في التسجيل المسبق. الخطر النظري (خلط البريماركت
في أساس الحجم) **يبقى قائمًا نظريًّا وغير مُقاس**، ويصير **قابلًا للقياس لأول مرة** بعد
أن يبدأ الرادار فعليًّا من الجرس.

🔴 **ملاحظة تشغيلية للمالك:** بعد إصلاح الكرون سيراقب الرادار نافذة الافتتاح لأول مرة
⇒ **إن كان الانتفاخ حقيقيًّا فسيظهر أثره الآن**، وأخطر صوره أن إنذارًا كاذبًا عند الجرس
**يستهلك خانة الدِدوب الوحيدة** لذلك السهم بقيّة اليوم (`ignition_alert` مرة/سهم/يوم)
فيُكتَم اشتعال حقيقي بعده. يستحقّ متابعة أول أسبوعين.

## ✅ المِجَسّ محفوظ وجاهز

`plans/013_probe.py` — مُختبَر على البيانات المتاحة اليوم ويعطي:
`جلسات متاحة: 10 · مؤهّلة بالتغطية: 1` ⇒ يستبعد 9 جلسات نافذتها الأولى غير مراقَبة
(‏+84 إلى +141 دقيقة) ويحكم **«العيّنة لا تكفي»** — أي أن **بوّابة التغطية تعمل** ولا
يمكن للمِجَسّ أن يُنتج حكمًا زائفًا من صفرٍ سببه انعدام المراقبة.

شغّله لاحقًا بلا أي تعديل: `python3 plans/013_probe.py [جذر_التنزيلات]`.
