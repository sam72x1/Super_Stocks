# Plan 001: تثبيت سلوك المسار الحيّ باختبارات Characterization قبل أي إصلاح

> **تعليمات المنفِّذ**: اتبع الخطة خطوةً خطوة. شغّل كل أمر تحقّق وتأكّد من النتيجة
> المتوقّعة قبل الانتقال. عند أي شرط في «STOP conditions» توقّف وأبلغ — لا تجتهد.
> عند الانتهاء حدّث صفّ الخطة في `plans/README.md`.
>
> **فحص الانحراف (شغّله أولًا)**:
> `git diff --stat a6457bf..HEAD -- Super_stock.py test_bot.py`
> لو تغيّر أي ملف داخل النطاق منذ كتابة الخطة، قارن مقتطفات «الحالة الحالية» بالكود
> الحيّ قبل المتابعة؛ أي عدم تطابق = حالة STOP.

## Status

- **Priority**: P1
- **Effort**: M
- **Risk**: LOW (إضافة اختبارات فقط — صفر تعديل على كود المصدر)
- **Depends on**: none
- **Category**: tests
- **Planned at**: commit `a6457bf`, 2026-07-28

## Why this matters

الخطط 002-006 و008 تلمس مسارات **حيّة حسّاسة**: بوّابة الفلوت M14 · كتلة إثراء
`scan_market` · تحميل بيانات مراقب الارتداد · قرار التجديد الأسبوعي. `CLAUDE.md` ينصّ
على قاعدة مُلزِمة: «اختبارات Characterization يجب أن تسبق أي إصلاح يغيّر سلوكًا حسّاسًا»،
وعلى درس أشدّ: «**القفل الذي لم يسقط مرّة واحدة عمدًا ليس قفلًا**» (تدقيق 2026-07-27،
حيث تبيّن أن قفلين من الأقفال الموجودة كانا فارغين).

سويّة الاختبارات اليوم قوية جدًّا في الدوال النقيّة، لكنها **لا تُثبّت السلوك الحالي**
للنقاط الأربع التي ستُعدَّل. هذه الخطة تكتب توصيفًا لسلوك **اليوم** (لا السلوك المرغوب)
حتى يصير أي انحراف لاحق مرئيًّا فورًا، وحتى يقدر مراجِع الخطط 002-006 أن يثبت أن
الإصلاح لم يغيّر شيئًا سوى ما قصده.

## Current state

الملفات:

- `Super_stock.py` — البوت كاملًا (13,729 سطرًا). النقاط المعنيّة أدناه.
- `test_bot.py` — سويّة الاختبارات (8,781 سطرًا). سكربت واحد يعمل بلا إنترنت، كل
  الجالبات محقونة/مقلَّدة. **لا pytest** — نمط يدوي بدالّة `check`.

### نمط الاختبار في هذا المستودع (اتّبعه حرفيًّا)

`test_bot.py` يستورد الوحدة كـ`S` ويستعمل دالّة `check(label, condition)` مسطّحة على
مستوى الوحدة (لا أصناف، لا pytest). ابحث عن `def check(` في أعلى الملف واقرأ توقيعها
قبل الكتابة. الأسطر تبدأ بإيموجي موضوعي ثم وصف عربي. مثال حقيقي من الملف
(`test_bot.py:7356-7359`):

```python
check("⏱️ قياس الإطلاق: صمد ≥15د ⇒ operator_ok=True (رفعة مضارب)",
      _fs1.get("operator_ok") is True and _fs1.get("sustain_min", 0) >= 15)
check("⏱️ قياس الإطلاق: سُحق بعد 5د ⇒ operator_ok=False (رفعة بلا إذن)",
      _fs2.get("operator_ok") is False and _fs2.get("sustain_min") == 5)
```

الاختبارات الجديدة تُضاف **في نهاية الملف** قبل كتلة الطباعة النهائية (ابحث عن
`النتيجة:` في آخر الملف لتحديد الموضع). **لا تعِد ترتيب اختبارات قائمة.**

### حارس إلزامي: وضع الاختبار

`Super_stock.py:13382` و`ignition_live.py:182,214` تفحص
`os.environ.get("SUPER_STOCKS_TESTING") == "1"` لمنع git/شبكة حقيقيَّين. تأكّد أن
`test_bot.py` يضبط هذا المتغيّر (ابحث عن `SUPER_STOCKS_TESTING` فيه) — **لا تُلغِه ولا
تحقن `runner=os.system`**؛ ذلك أعاد حادثة موثّقة (كتابة بيانات وهمية على `main`).

### النقطة أ — بوّابة الفلوت M14 (`Super_stock.py:7042-7087`)

```python
7056        for r in results:
7057            fl = r.get("float")          # قد يكون مجلوباً مسبقاً من enrich
7058            if fl is None:               # غير مجلوب بعد → نجلبه الآن
...
7069            if fl is not None and fl >= limit:
7070                # v2.7: لا يُحذف — يُسجّل نقصًا وينزل لقائمة المراقبة B
7071                r.setdefault("soft_fails", []).append("فلوت كبير")
...
7076            else:
7077                if fl is not None:
7078                    r.setdefault("flags", []).append(
7079                        f"فلوت {int(fl):,} (صغير ✅)")
7080                else:
7081                    r.setdefault("flags", []).append(
7082                        "فلوت غير متاح — مُرِّر بفائدة الشك")
```

الحدّ من `CONFIG["FLOAT_GATE_MAX"]` والبوّابة مفعّلة بـ`CONFIG["FLOAT_GATE_REQUIRED"]`.

### النقطة ب — كتلة إثراء `scan_market` (`Super_stock.py:7388-7411`)

```python
7388        for sym, df in history.items():
7389            r = analyze_ticker(sym, df)
7390            if r:
...
7400                r["behav"] = behavior_rise_profile(df)
7401                r["fsto_osc"] = fsto_oscillation(
7402                    full_stoch(df["High"], df["Low"], df["Close"])[0])
7403                r["klinger"] = klinger_state(...)
7405                r["cci"] = cci_state(...)
7407                r["bottom_test"] = bottom_test_state(df)
7408                r["pump_scar"] = group_pump_scar(df)
7409                r["trendline"] = descending_trendline(df, r["price"])
7410                r["interp"] = build_interpretation(r)
7411                results.append(r)
```

**لا يوجد `try/except` حول هذه الكتلة** — بعكس النداءات المطابقة في
`update_watchlist_status` (`Super_stock.py:7785-7802`) المحاطة بـ`try/except Exception`.

### النقطة ج — تحميل مراقب الارتداد (`Super_stock.py:9758-9782`)

```python
9766        for e in entries:
9767            if e.get("status") == "triggered":
9768                continue
9769            try:
9770                d = download_history([e["symbol"]])
9771                df = d.get(e["symbol"])
...
9776                if lp <= e["entry"][1] * buf:        # نزل لسعر الدعم
9777                    e["status"] = "triggered"
9778                    e["triggered_date"] = dt.date.today().isoformat()
9779                    triggered.append(e)
```

### النقطة د — قرار التجديد (`Super_stock.py:6887-6899`)

```python
6887 def should_renew(wl: dict, force: bool = False,
6888                  renew_signal: bool = False) -> bool:
...
6895     if force:
6896         return True
6897     if not wl.get("stocks") and not wl.get("removed"):
6898         return True  # أول تشغيل — تأسيس فوري
6899     return renew_signal
```

## Commands you will need

| الغرض | الأمر | المتوقّع عند النجاح |
|-------|-------|---------------------|
| تثبيت | `pip install -r requirements.txt` | exit 0 |
| بوّابة الاختبار | `python3 test_bot.py` | exit 0 · «0 فشل» في آخر سطرين |
| عدد الاختبارات (للمقارنة فقط) | `python3 test_bot.py \| tail -3` | سطر «النتيجة: N نجح · 0 فشل» |
| فحص صياغة | `python3 -m pyflakes test_bot.py` | exit 0 (لو `pyflakes` مثبّت؛ وإلا تخطّاه) |

## Scope

**In scope** (الملفات الوحيدة التي تُعدَّل):
- `test_bot.py` (إضافة فقط، في نهاية الملف)
- `plans/README.md` (تحديث صفّ الحالة)

**Out of scope** (لا تلمسها إطلاقًا):
- `Super_stock.py` — **صفر تعديل في هذه الخطة.** الغرض توصيف السلوك القائم لا تغييره.
- أي ملف `.py` آخر · أي ملف `.yml` · `CLAUDE.md` · `HANDOFF.md`.
- إعادة ترتيب/تعديل/حذف أي اختبار قائم في `test_bot.py`.

## Git workflow

- الفرع: أنشئ `advisor/001-characterization` من الفرع الحالي.
- كوميت واحد في النهاية، بالعربي مع إيموجي (نمط المستودع)، مثال:
  `🧪 اختبارات توصيف للمسار الحيّ قبل إصلاحات التدقيق (صفر تعديل مصدر)`
  وينتهي بـ `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
- **لا push ولا PR.**

## Steps

### Step 1: التقط الأساس

شغّل `python3 test_bot.py` واحفظ آخر سطرين في ملاحظاتك (سيُقارن لاحقًا). يجب أن يكون
الخروج 0 و«0 فشل». لو لم يكن كذلك ⇒ **STOP**: المستودع ليس أخضر أصلًا.

**Verify**: `python3 test_bot.py; echo "EXIT=$?"` → `EXIT=0` وسطر «0 فشل».

### Step 2: توصيف بوّابة الفلوت M14 (سلوك اليوم حرفيًّا)

أضِف في نهاية `test_bot.py` كتلة تحت تعليق `# ===== توصيف (Characterization) — خطة 001 =====`
تختبر `S.apply_float_gate` على **قوائم مبنيّة يدويًّا** (بلا شبكة: مرّر `float` مضبوطًا
مسبقًا في كل سجلّ حتى لا تُنفَّذ كتلة `yf.Ticker`). فعّل البوّابة مؤقّتًا إن لزم عبر
`S.CONFIG["FLOAT_GATE_REQUIRED"]` وأعِد القيمة الأصلية بعد الكتلة.

غطِّ **بالضبط** هذه الحالات (كلها تصف سلوك اليوم):
1. `float` أصغر من `FLOAT_GATE_MAX` ⇒ السهم يبقى · لا يُضاف «فلوت كبير» إلى
   `soft_fails` · يُضاف flag يحوي «صغير».
2. `float` أكبر من أو يساوي الحدّ ⇒ السهم **يبقى في القائمة** (لا يُحذف) · يُضاف
   «فلوت كبير» إلى `soft_fails` · يُضاف flag يحوي «فلوت كبير».
3. `float` = `None` ⇒ السهم يبقى · لا نقص · flag يحوي «غير متاح» (فائدة الشك).
4. `FLOAT_GATE_REQUIRED=False` ⇒ القائمة تُعاد **كما هي بلا أي تعديل**
   (قارن بـ`==` على نسخة عميقة).

**Verify**: `python3 test_bot.py; echo "EXIT=$?"` → `EXIT=0` و«0 فشل»، وعدد الاختبارات
زاد بمقدار عدد `check` التي أضفتها.

### Step 3: توصيف `refloat_gate_recheck` مقابل `apply_float_gate`

أضِف اختبارًا واحدًا صريحًا يوثّق **الفرق القائم اليوم** بين الدالّتين في التعامل مع
قيمة فلوت غير رقمية (نصّ مثل `"12M"`):
- `S.refloat_gate_recheck([...])` **لا يرمي** (محميّ بـ`except (TypeError, ValueError)`
  في `Super_stock.py:7115-7119`) ويُبقي السهم.
- `S.apply_float_gate([...])` **يرمي `TypeError`** اليوم. وثّق ذلك بـ`try/except` داخل
  الاختبار: النتيجة المتوقّعة **الآن** هي وقوع الاستثناء.

سمِّ الاختبار بوضوح أنه توصيف لسلوق قائم، مثلًا:
`"🧪 توصيف: apply_float_gate ترمي على فلوت نصّي (يُغيَّر في خطة 004)"`.

> هذا الاختبار **سيُقلَب** في الخطة 004 بعد الإصلاح. اذكر ذلك في تعليق فوقه بالحرف:
> `# ⚠️ خطة 004 ستقلب هذا التوقّع بعد التحصين — لا تحذفه، عدّله.`

**Verify**: `python3 test_bot.py; echo "EXIT=$?"` → `EXIT=0` و«0 فشل».

### Step 4: توصيف `should_renew`

أضِف اختبارات تصف السلوك الحالي بالكامل:
1. `should_renew({"stocks":[{}],"removed":[]}, force=True, renew_signal=False)` → `True`
2. `should_renew({"stocks":[],"removed":[]}, False, False)` → `True` (تأسيس أول)
3. `should_renew({"stocks":[{}],"removed":[]}, False, True)` → `True`
4. `should_renew({"stocks":[{}],"removed":[]}, False, False)` → `False`
5. `should_renew({"stocks":[],"removed":[{}]}, False, False)` → `False`
   (قائمة فارغة لكن فيها مشطوبون ⇒ ليست «أول تشغيل»)

**Verify**: `python3 test_bot.py; echo "EXIT=$?"` → `EXIT=0` و«0 فشل».

### Step 5: توصيف `monitor_pullback` بجالب محقون

`monitor_pullback` **لا تقبل جالبًا محقونًا اليوم** — تنادي `download_history` مباشرة
(`Super_stock.py:9770`). لتوصيفها بلا شبكة، بدّل `S.download_history` مؤقّتًا بدالّة
مزيّفة داخل الاختبار وأعِد الأصلية في النهاية (نمط monkeypatch يدوي؛ ابحث في
`test_bot.py` عن أمثلة قائمة لتبديل دوالّ الوحدة واتّبع نفس الأسلوب).

سجّل عدّاد نداءات للدالّة المزيّفة، ثم أكّد بالضبط:
1. **عدد النداءات = عدد الأسهم غير المُطلَقة** (هذا هو السلوك المكلف الذي تُصلحه خطة 006 —
   ثبّته الآن كي يُقاس التحسّن).
2. سهم سعره ≤ `entry[1] * (1 + PULLBACK_TRIGGER_PCT/100)` ⇒ `status == "triggered"`
   و`triggered_date` مضبوط ويظهر في المُرجَع.
3. سهم سعره أعلى من ذلك ⇒ يبقى `"watching"` ولا يظهر في المُرجَع.
4. سهم `status == "triggered"` مسبقًا ⇒ **لا يُنادى له جالب إطلاقًا**.
5. سهم يرمي جالبه استثناءً ⇒ يُتخطّى بلا كسر البقيّة (`continue`).

> ⚠️ استعمل `S.yf` غير `None` (البوّابة في `9762`). لو كانت `None` في بيئتك، اضبطها
> مؤقّتًا لكائن وهمي وأعِدها. لا تلمس `Super_stock.py`.

**Verify**: `python3 test_bot.py; echo "EXIT=$?"` → `EXIT=0` و«0 فشل».

### Step 6: توصيف حصانة `scan_market` تجاه استثناء رمز واحد

اكتب اختبارًا يوثّق السلوك **الحالي**: لو رمت إحدى دوال كتلة الإثراء
(`Super_stock.py:7400-7410`) على رمز واحد، فإن `scan_market` **تنهار كاملةً**.

الطريقة (بلا شبكة): ثبّت `S.MODE` على `"TEST"` مؤقّتًا (فيستعمل `CONFIG["TEST_TICKERS"]`)،
وبدّل `S.download_history` بجالب مزيّف يُرجع إطارًا اصطناعيًّا واحدًا يجتاز `analyze_ticker`
— إن تعذّر بناء إطار يجتاز البوّابات خلال محاولتين، **لا تجتهد**: بدّل بدلًا من ذلك
`S.build_interpretation` بدالّة ترمي، وبدّل `S.analyze_ticker` بدالّة تُرجع dict ثابتًا
صالحًا، ثم أكّد أن `S.scan_market()` ترمي. أعِد كل ما بدّلته.

سمِّ الاختبار: `"🧪 توصيف: استثناء في كتلة إثراء scan_market يُسقط الفرز كلّه (يُغيَّر في خطة 005)"`
وضع فوقه التعليق:
`# ⚠️ خطة 005 ستقلب هذا التوقّع بعد إضافة الحارس — لا تحذفه، عدّله.`

**Verify**: `python3 test_bot.py; echo "EXIT=$?"` → `EXIT=0` و«0 فشل».

### Step 7: اختبار الطفرة (إلزامي — «القفل الذي لم يسقط ليس قفلًا»)

لكل مجموعة أضفتها، اكسر الكود عمدًا **مؤقّتًا** وتأكّد أن الاختبار **يسقط فعلًا**، ثم
أرجِع الكود. طبّق على الأقل ثلاث طفرات:

1. في `Super_stock.py:7069` غيّر `fl >= limit` إلى `fl > limit * 10` →
   يجب أن يفشل اختبار الحالة 2 من الخطوة 2.
2. في `Super_stock.py:6899` غيّر `return renew_signal` إلى `return True` →
   يجب أن يفشل اختبار الحالة 4 من الخطوة 4.
3. في `Super_stock.py:9767` غيّر `if e.get("status") == "triggered"` إلى `if False` →
   يجب أن يفشل اختبار الحالة 4 من الخطوة 5.

> 🔴 **المقياس نفسه يحتاج شاهد ضبط** (درس موثّق في `CLAUDE.md` 2026-07-28: `grep -c '❌'`
> كان يعطي صفرًا للانهيار أيضًا). لذلك احكم على الطفرة بـ**رمز الخروج والسطر الأخير معًا**:
> `python3 test_bot.py; echo "EXIT=$?"` — الطفرة الناجحة تعطي `EXIT` غير صفري **و**
> سطر «N فشل» بعدد أكبر من صفر. لو أعطت انهيارًا قبل الوصول للطباعة، فالطفرة غير صالحة —
> اختر طفرة أدقّ.

**بعد كل طفرة أرجِع `Super_stock.py` إلى حالته الأصلية:**
`git checkout -- Super_stock.py` ثم أكّد `git status --porcelain Super_stock.py` فارغ.

**Verify**: بعد إعادة كل الطفرات → `git diff --stat a6457bf..HEAD -- Super_stock.py`
لا يُظهر أي تغيير، و`python3 test_bot.py; echo "EXIT=$?"` → `EXIT=0` و«0 فشل».

### Step 8: سجّل عدد الاختبارات الجديد في الخطة

اكتب في `plans/README.md` (صفّ 001) الحالة `DONE` وأضف بين قوسين عدد الاختبارات الجديد
بصيغة `(الأساس N → M)` من آخر سطر في مخرج السويّة.

## Test plan

- **الملف**: `test_bot.py` — إضافة في النهاية فقط، تحت ترويسة
  `# ===== توصيف (Characterization) — خطة 001 =====`.
- **النمط المرجعي**: `test_bot.py:7334-7371` (كتلة `operator_sustain`/`_fire_sustain`) —
  انسخ أسلوبها: متغيّرات `_`-بادئة على مستوى الوحدة ثم `check(...)`.
- **التغطية**: بوّابة الفلوت (4 حالات) · فرق `refloat_gate_recheck`/`apply_float_gate`
  (1) · `should_renew` (5) · `monitor_pullback` (5) · حصانة `scan_market` (1).
- **التحقّق**: `python3 test_bot.py` → exit 0 · «0 فشل» · العدد الكلّي زاد ≥16.

## Done criteria

كلها إلزامية:

- [ ] `python3 test_bot.py; echo "EXIT=$?"` → `EXIT=0` وسطر «… · 0 فشل»
- [ ] العدد الكلّي للاختبارات زاد ≥16 عن الأساس المسجَّل في الخطوة 1
- [ ] `git status --porcelain` لا يُظهر أي ملف معدَّل غير `test_bot.py` و`plans/README.md`
- [ ] `git diff a6457bf..HEAD -- Super_stock.py` **فارغ تمامًا** (صفر تعديل مصدر)
- [ ] الطفرات الثلاث في الخطوة 7 أُجريت وأسقطت اختباراتها ثم أُرجعت (وثّق ذلك في رسالة الكوميت)
- [ ] الاختباران المُعلَّمان بـ«يُغيَّر في خطة 004/005» يحملان تعليق التحذير المطلوب
- [ ] صفّ 001 في `plans/README.md` محدَّث

## STOP conditions

توقّف وأبلغ (بلا اجتهاد) إذا:

- `python3 test_bot.py` لم يكن أخضر **قبل** أي تعديل منك (الخطوة 1).
- مقتطفات «الحالة الحالية» لا تطابق الكود الحيّ (المستودع انحرف عن `a6457bf`).
- وجدت نفسك مضطرًّا لتعديل `Super_stock.py` لجعل اختبارٍ يمرّ — **هذه الخطة لا تعدّل
  المصدر إطلاقًا**؛ الإصلاح مكانه الخطط 002-006.
- تعذّر بناء توصيف الخطوة 6 بعد محاولتين بالطريقتين المذكورتين ⇒ أبلغ وتخطَّ الخطوة 6
  فقط (لا تُسقط بقيّة الخطوات)، وسجّلها `BLOCKED` بسطر في `plans/README.md`.
- اضطررت لضبط `SUPER_STOCKS_TESTING` بغير `"1"` أو لحقن `runner=os.system`.

## Maintenance notes

- هذه الاختبارات **توصيفية**: هي تصف ما يفعله الكود اليوم، لا ما يجب أن يفعله. اختباران
  منها مُعلَّمان صراحةً بأنهما سيُقلَبان (004 و005) — **قلبهما مقصود ويجب أن يظهر في
  المراجعة**، بينما تغيّر أي توصيف آخر بلا خطة = انحدار.
- المراجِع يجب أن يتأكّد أن `git diff` على `Super_stock.py` **فارغ** في هذه الخطة.
- مؤجَّل عمدًا: تحويل السويّة إلى pytest — قرار معماري للمالك، وخارج نطاق هذه الدفعة.
