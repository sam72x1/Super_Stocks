# `T-WAIT-23W` — «سجّل 23 والأسبوعيّ»: اختبارٌ **تأكيديّ** بضبطٍ مطابَقِ التعبئة وحصادٍ لم يُرَ (تسجيلٌ مسبقٌ **قبل أيّ رقم**)

> **أمرُ المالك (2026-09-09):** «سجّل 23 والأسبوعيّ» — بعد حكم `T-WAIT-RSI27` الذي عبرت فيه ذراعان **وصفيّتان** وحدَهما.
> وقبله: «معك الصلاحية كاملة لكن اشتغل حسب بيانات فيصل لانها ادق و اصح».
> 🔎 **وهذا العقدُ نفسُه مرّ بمراجعةٍ خصوميّةٍ بخمس عدساتٍ قبل دفعه** (‏77 ملاحظة · ستٌّ فتّاكة) — لأن العقدَ المعيب لا يُصلَح بعد صدور الأرقام.

## ⓪ المصدر — وأينَ ينتهي نصُّ فيصل ويبدأ **نقلُنا**
| الرقم | المصدر | الوسم | ملاحظةٌ لازمة |
|---|---|---|---|
| `RSI 23` | `TG_2043`: «ضغط الارتكاز لابد قبل ينفجر يكون **RSI بين 23-27**» | `faisal_verbatim` **نصًّا** · `inferred` **استعمالًا** | 🔴 **نصُّه يصف حالةَ ضغطٍ للفرز، لا أمرَ حدٍّ للدخول.** تحويلُه إلى «انتظر السعرَ الذي يُنزل RSI إلى 23» **نقلُنا نحن**؛ فما يُختبَر هنا **نقلُنا لا قاعدتُه** — سقوطُه لا يُكذّب فيصل، ونجاحُه لا يُصادق على نصّه. |
| `RSI 27` أسبوعيًّا | GWAV (دليل ص91 · **شارتٌ أسبوعيّ**): «سحب السيوله المتوقع من 2.70 **يمثل هدف الشورت = RSI 27**» | `faisal_verbatim` (‏**حالةٌ واحدة** — كما في دفتر المصادر) | هنا النصُّ **سعرٌ فعلًا** لا حالةَ فرز ⇒ سندُ `R27w` أقوى من `R23`. لكنّ **التعميم من سهمٍ واحدٍ إلى كلّ الأسهم استقراؤنا**. |
| الوقفُ **النسبيّ** (يتحرّك مع الدخول) | — | `inferred` | وقفُ فيصل **بنيويٌّ ثابتٌ تحت الدعم**. والذراعُ بالوقف البنيويّ قِيست ونُشرت (`R27abs`) فتخطّت **‏90%** من الصفوف ⇒ الوقفان لا يجتمعان، وهذا **حدُّ صدقٍ لا تبرير**. |
| أرضية 100 صفقة · تعبئة 30% · بار +0.05R | `T-EXIT` · `T-WAIT-LOWER` · `T-WAIT-RSI27` | `engineering` | مُعادةٌ من أحكامٍ سابقة لا مخترَعة. |
| بار الضبط +0.025R · إزاحة ⌊n/3⌋ · بذرة 23 · 2000 عيّنة · 97.5% · حدود 20%/80%/2% | — | `engineering` | مثبَّتةٌ هنا قبل أيّ رقم. |
| عمقُ 32% · 45% · 59% (‏سعرُ RSI 27 · 23 · الأسبوعيّ تحت سعر التحليل) | مقيسٌ في `T-WAIT-RSI27` | `measured` | 🔴 **وسحبُ GWAV الفعليّ عند فيصل ‏≈17%** ⇒ الأذرعُ تقيس **انتظارًا أعمقَ بكثيرٍ ممّا وصف**. وذراعُ «المسار التدريجيّ» (الأقربُ لعمقه) **مؤجَّلةٌ صراحةً** لعقدٍ خاصٍّ بها: تعريفُها يحتاج نموذجَ انحدارٍ متعدّدَ الشموع، وإقحامُها هنا يزيد المقارنات. |

## ① السؤال — ولماذا لا يكفي ما صدر
> هل تصمد ذراعا فيصل **(أ)** أمام ضبطٍ **مطابَقِ التعبئة** يجرّدهما من محتوى RSI، **و(ب)** على مجتمعاتٍ **لم تُرَ فيها قطّ**؟

🔴 **حدُّ الصدق الأوّل — الانتقاء:** الذراعان **اختيرتا وصفيًّا** على 2023-2025 من بين ثلاثٍ. عبورُ اثنتين من ثلاثٍ بفاصلٍ 95% هو ما يُنتجه
تعدّدُ المقارنات ⇒ **إعادةُ قياسهما على المرئيّ ليست دليلًا** (الأداةُ حتميّة فستُعيدها بت-بت — وهذا **بوّابةُ صلاحية** لا نتيجة).

🔴🔴 **حدُّ الصدق الثاني — الالتباسُ الحاكم:** المقياسُ يحتسب `no_fill` **صفرًا** والتوقّعُ سالبٌ ‏≈ −0.2R ⇒ **أيُّ ذراعٍ تتاجر أقلّ تتحسّن
آليًّا**، والدليلُ منشورٌ عندنا: `R27abs` أعطت **+0.2027R** بـ**‏90% تخطٍّ**. ولأن الضبطَ المطابقَ **للعمق وحده** يترك فرقًا في التعبئة
يميل **لصالح** الذراع، فالضبطُ الحاكم هنا **مطابَقُ التعبئة** (‏`M23`/`M27w`) لا مطابَقُ العمق.

🔴 **حدُّ الصدق الثالث — الاحتمالُ الأسبق الفشل:** ستُّ تجاربِ توقيتِ دخولٍ سبقت (‏`T-LIBVOL` −0.879 · `T-LIBVOL-2` −0.820 ·
`T-TRANCHE` −0.199 · `T-RECLAIM-INTRADAY` −0.135 · `T-WAIT-LOWER` فشلت بمجمَّعٍ غيرِ سالب · `T-WAIT-RSI27` فشلت بشعرة) — **ولا واحدةٌ شُحنت.**

## ② المجتمعات — ودورُ كلٍّ منها **مثبَّتٌ قبل أيّ رقم**
| المجتمع | اللقطة | الدور |
|---|---|---|
| المرئيّ 2023 · 2024 · 2025 | PIT `31574165523` · `31199493957` · `31191327731` | **بوّابةُ صلاحيةٍ ووصفٌ — لا يُحكَم به** |
| المرئيّ الجزئيّ 2026 حتى 07-10 | `freeze` `34322427155` | وصفيٌّ (سبق أن قُرئ) |
| **`H-BACK` غيرُ مرئيّ** 2022 · 2021 | PIT **بُنيتا اليوم**: 2022 كون `34331476520` ⟶ لقطة `34331756534` (as-of 2022-01-03) · 2021 كون `34331479110` ⟶ لقطة `34331758846` (as-of 2021-01-04) | **يُحكَم به** |
| **`H-FWD` غيرُ مرئيّ** إشاراتٌ **بعد 2026-07-10 حصرًا** | **لقطةُ PIT لكون 2026-01-02** (لا `freeze`) تُبنى بعد إغلاق 2026 | **يُحكَم به · قراءةٌ واحدة** (§⑦) |

- **المحرّك واحدٌ للجميع:** `backtest_symbol` بأعلام `FAISAL_ONLY=1 · BT_REPLAY10=1 · BT_ENVVALS=1` · نافذة 40ج · سبريد 0 · وقفُ القاع مشحون
  — **نفسُ ميزانيّة** `T-TRANCHE`/`T-WAIT-LOWER`/`T-WAIT-RSI27`، وتُطبَع في كلّ مُخرَج.
- **إعادةُ استعمالٍ بالاسم لا نسخ:** `wait_rsi27_arms.arms_for` (تُنتج `R0`/`R23`/`R27w` بت-بت) و`agg`/`decomp`/`r0_of`/`stop_for`
  و`bootstrap_ci` منها · و`_resolve_arm`/`rsi_target_price`/`analyze_ticker` الإنتاجيّة عبرها.
  **`Super_stock.py` و`wait_rsi27_arms.py` لا يُمَسّان بحرف.**
- 🔴 **قيودٌ تُعلَن ولا تُجمَّل:**
  1. **اللقطتان الخلفيّتان بُنيتا قبل دفع هذا العقد** — بناءُ مجتمعٍ لا قياس؛ **ولا رقمَ ذراعٍ واحدٍ صدر قبل الدفع**. والأداةُ كُتبت
     بجانب العقد وتُدفَع **بعده** بكوميتٍ منفصل.
  2. **`H-FWD` امتدادٌ متّصلٌ زمنيًّا** للنافذة التي قُرئت وصفيًّا (‏2026 حتى 07-10) ⇒ نظامٌ قريب، فهو **أضعفُ استقلالًا** من `H-BACK`.
  3. **كونُ PIT مثبَّتٌ عند أوّل يومٍ من السنة** ⇒ إدراجاتُ السنة نفسِها غائبة (أثقلُها في **2021**) — **قصورُ تغطيةٍ يُعلَن**.
  4. **أسعارُ اللقطات مُسوّاةٌ بتقسيماتٍ لاحقة** (قيدٌ قائمٌ في كلّ لقطاتنا) — ليس جديدًا هنا ويُذكر.
  5. **أذرعُ الضبط تُحسَم على قيمِ الصفّ المدوَّرة لأربع خانات** بينما الحاكمتان على قيمٍ خام داخل `arms_for` ⇒ **فارقُ تدويرٍ أقصاه 1e-4**.
  6. **بوّاباتُ الإنتاج مُعايَرةٌ على مادّةٍ حديثة** ⇒ 2021/2022 تختبر الذراعَ على مجتمعٍ لم تُعايَر عليه البوّابة — يُصعّب ولا يُسهّل، **وليس نقاءً تامًّا**.
- ⚠️ **عدمُ تماثلٍ يُثبَّت قبل الأرقام:** 2021 و2022 نظامان مختلفان ⇒ **النجاحُ دليلٌ قويّ والفشلُ أضعف** — **ومع ذلك: الفشلُ فشل**،
  ويُمنَع صراحةً تفسيرُه بـ«اختلاف النظام» للنجاة به.

## ③ الأذرع — **سبعٌ مُعلَنةٌ وناتجان عرَضيّان** (إضافةُ ذراعٍ بعد الأرقام ممنوعة)
| الذراع | الدخول | الدور |
|---|---|---|
| `R0` | متوسّطُ دفعات الإنتاج `entry0` · وقفُه `stop0` حرفيًّا | الأساس |
| **`R23` 🥇** | `min(سعرُ RSI 23 اليوميّ, entry0)` · `None` ⇒ `entry0` | **حاكمة** |
| **`R27w` 🥇** | `min(سعرُ RSI 27 **الأسبوعيّ**, entry0)` (الأسبوعُ الناقص يُسقَط) · `None` ⇒ `entry0` | **حاكمة** |
| **`M23` · `M27w`** | عمقٌ **ثابتٌ يُحَلّ عدديًّا** لكلّ سنةٍ بحيث **تتساوى عددُ التعبئة** مع ذراعه | **ضبطٌ حاكمٌ لـ`G2`** |
| `C23` · `C27w` | `min(ref × (1 − d[π(i)]), entry0)` — عمقُ صفٍّ آخر بإزاحةٍ حتميّة | ضبطٌ **مساند** (يُطبَع ولا يحكم) |
| `R27` · `R27abs` | ناتجان عرَضيّان من `arms_for` المُعادِ استعمالُها | **وصفيّان** — يُطبَعان ولا يحكمان |

- **العمقُ الخام:** `d_i = max(0, 1 − سعرُ RSI_i / ref_i)` · `None` ⇒ `d_i = 0`. **وعند `d = 0` يكون دخولُ الضبط `entry0` حرفيًّا**
  (تناظرٌ مع الذراع — فـ`ref` قد يقع **تحت** متوسّط الدفعات فلا يصحّ `min(ref, entry0)`).
- **الإزاحة `π` حتميّة:** ترتيبٌ بـ`(symbol, date)` **داخل السنة الواحدة** ثم `π(i) = (i + ⌊n/3⌋) mod n`. **لا عشوائيّة.**
- **حلُّ `M`:** بحثٌ ثنائيٌّ على عمقٍ ثابتٍ في `[0, 0.99]` (‏60 تنصيفًا) يُصغّر `|تعبئة(M) − تعبئة(G)|`، بالاعتماد على **أدنى قاعٍ في النافذة**
  لكلّ صفّ (‏محسوبٌ مرّةً واحدة) ⇒ **حتميٌّ ورخيص**. والعمقُ المحلول **يُطبَع** لأنه بذاته جوابٌ عمليّ: «أيُّ انتظارٍ ثابتٍ يشتري التعرّضَ نفسَه؟»
- 🔴 **ما هي أذرعُ الضبط:** **نماذجُ عدمٍ إحصائيّة لا استراتيجيّاتٌ قابلةٌ للتنفيذ** (‏`C` تأخذ عمقَ صفٍّ آخر قد يكون لاحقًا · و`M` يُحَلّ على
  المجتمع كلِّه) ⇒ **لا تُقترَح للشحن أبدًا.**
- **مقامٌ واحدٌ مفروض:** أيُّ صفٍّ يتعذّر بناءُ ضبطه **يُسقَط من المجتمع كلِّه ويُعدّ ويُطبَع** — فلا تُقارَن متوسّطاتٌ على مقاماتٍ مختلفة.
- `no_fill` = صفرُ عائدٍ ويدخل المقام.

## ④ الحُكم — قبل أيّ رقم
**المقدِّرُ الحاكم — واحدٌ لا ثلاثة:** متوسّطُ `R₀` بوحدة `entry0 − stop0`، وفاصلُه **بوتستراب عنقوديٌّ بالرمز** (‏2000 معايَنة · بذرة 23 ·
مُقدِّرُ نسبةِ المجاميع) عند **‏97.5% ثنائيّ الجانب**. **والفاصلُ الصفّيُّ عند 95% يُطبَع للمقارنة مع المنشور ولا يحكم.**
🔎 **وتسميةٌ دقيقة:** فاصلٌ ثنائيُّ الجانب 97.5% وحدُّه الأدنى فوق الصفر = اختبارٌ أحاديٌّ عند **‏1.25%** — أشدُّ من بونفيروني المطلوب
لذراعين (‏2.5%)، ويُقال كما هو لا كما يُزيّن. **والعنقدةُ بالرمز لأن الصفوف ليست مستقلّة** (الرمزُ يتكرّر) فمعايَنةُ الصفوف تُضيّق الفاصلَ كذبًا.

لكلّ ذراعٍ حاكمة `G`، وعلى **كلّ** مجتمعٍ يُحكَم به:
- **`G1`** `G − R0`: **موجبٌ في كلّ سنةٍ محكومة** ‏**و**مجمَّعًا ‏≥ **+0.05R** ‏**و**حدُّ الفاصل الأدنى **فوق الصفر**.
- **`G2` (الضبطُ مطابَقُ التعبئة — الشرطُ الذي يفصل المعلومةَ عن قلّة التداول)** `G − M_G`: مجمَّعًا ‏≥ **+0.025R** ‏**و**حدُّ الفاصل الأدنى **فوق الصفر**.
- **`G3`** `تعبئة(G) ÷ تعبئة(R0)` ‏≥ **‏30%** في كلّ سنةٍ محكومة (**المقامُ تعبئةُ `R0` لا عددُ الصفوف** — والمرئيُّ 34-40%).
- **`G4` (المقياسُ الثاني — يمنع الفوزَ بقلّة التداول وحدَها)** توقّعُ **الصفقة المُعبَّأة** في `G` ‏**ليس أسوأ** من توقّع الصفقة المُعبَّأة في `R0`، مجمَّعًا.
- **الأرضية:** ‏≥ **100** مُعبَّأة في `R0` **و**في `G` لكلّ سنة؛ السنةُ دونها **تخرج وتُطبَع**، **وإن بقيت سنةٌ واحدةٌ فقط في `H-BACK` فالمجتمعُ «لا قياس»**.
- **التوصية:** `G` **تُوصى** فقط إذا عبرت `G1∧G2∧G3∧G4` على **`H-BACK` و`H-FWD` معًا** — أي على **ثلاث** سنواتٍ محكومةٍ لا سنتين
  (‏2021 · 2022 · والحصادُ الأماميّ) التزامًا بقاعدة «الحدُّ الأدنى ثلاث سنوات». وقبل اكتمال الثلاث لا يصدر إلّا وسمُ **«مرشَّحٌ مستمرّ»**.
- **④-ب تفكيكُ الحافة إلزاميّ** لكلّ حاكمة (‏`B0`/`B1`/`B2`/`B3` بهويّةٍ تُفحَص) — قاعدةُ `T-WAIT-RSI27` المدوَّنة.

**بوّاباتُ الصلاحية (عطبُ أداةٍ لا نتيجة — خروجٌ غير صفريّ):**
- **`V-S0`** (السنوات المرئيّة) `R0`/`R23`/`R27w` تُعيد المنشورَ **بت-بت** — والقيمُ التسع في **ملحق `wait_rsi27_result.md` المؤرَّخ 2026-09-09**
  الذي يُدفَع **مع هذا العقد** ليكون القفلُ قابلًا للتدقيق من ملفٍّ منشور.
- **`V-S6`** (كلُّ مجتمع · **مرساةُ الآلة في `H-BACK`**) حسمُ `R0` = حسمُ `backtest_symbol` في ‏≥99% من الصفوف.
- **`V-S1`** (سلوكيّ · **قابلٌ للسقوط**) كلُّ ضبطٍ يختلف حسمُه عن حسم ذراعه في ‏≥ **‏20%** من الصفوف — وإلّا فالضبطُ عديمُ الأثر.
- **`V-S2`** الإزاحةُ تُغيّر العمقَ في ‏≥ **‏80%** من الصفوف **المُبنَّدة** (‏لا كلّ الصفوف — فكتلةُ العمق الصفريّ في `R27w` تجعل الحدَّ العامَّ إنذارًا كاذبًا).
- **`V-S3`** تغطيةُ اللقطة: بلا حصادٍ أماميّ ⇒ سنةُ اللقطة = سنةُ القياس · ومع حصادٍ ⇒ **as-of بعد تاريخ الحصاد** (‏وإلّا استحال تشغيلُ `H-FWD`).
- **`V-S4`** تعبئةُ `G` ‏≤ تعبئةُ `R0` بالبناء · **`V-S5`** الحاكمتان تتفرّقان عن `R0`.
- **`V-S7`** مقامٌ واحد: عددُ صفوف كلّ ذراعٍ متساوٍ بعد الإسقاط.
- **`V-S8`** المطابقة تحقّقت: `|تعبئة(M) − تعبئة(G)|` ‏≤ **‏2%** من تعبئة `G` — وإلّا يُعلَن `G2` **غيرَ قابلٍ للمقارنة** (ولا يُقرأ عبورًا).

## ⑤ التنبّؤات (تُنشَر كما تقع — لا تُحذف)
- **`WS-P1`** `R23 − R0` موجبٌ في 2022 و2021 كلتيهما (الأثرُ آليٌّ فلا يعتمد على النظام).
- **`WS-P2`** `R23 − M23` مجمَّعًا **دون +0.025R** ⇒ **`G2` تسقط لـ`R23`** — أي أن أكثر الأثر «تداولٌ أقلّ» لا قراءةُ RSI.
- **`WS-P3`** العمقُ المحلول لـ`M23` بين **‏35% و55%**.
- **`WS-P4`** `R27w − R0` **أصغرُ** من `R23 − R0` على `H-BACK` (نصفُ صفوف `R27w` ‏≡ `R0` بالبناء).
- **`WS-P5`** `تعبئة(R23) ÷ تعبئة(R0)` على `H-BACK` بين **‏30% و45%** (المرئيّ **‏34-40%**).
- **`WS-P6`** **`G4` تسقط لـ`R23`** في سنةٍ واحدةٍ على الأقلّ (الصفقةُ المُعبَّأة الأعمقُ ربحت أقلَّ في المرئيّ: 5.0% مقابل 11.3%).
- **`WS-P7`** التوقّعُ **يبقى سالبًا** في كلّ ذراعٍ وكلّ سنةٍ من `H-BACK`.

## ⑥ سقفُ النجاح
**قياسٌ وتوصية.** لا يتحرّك `ENTRY_*` ولا وقفٌ ولا `LOGIC_VERSION` · **وأذرعُ الضبط لا تُشحَن أبدًا** · والشحنُ كلمةُ المالك.
والانضباطُ المعتاد لازم: **أقفالٌ في `test_bot.py` وجولةُ طفراتٍ تُسقط كلَّ قفلٍ بقفله** قبل أيّ تشغيل. النتيجةُ في `wait_rsi23w_result.md`.

## ⑦ الالتزامُ المؤرَّخ لـ`H-FWD` — لأن «حدَّ صدقٍ يَعِد ولا يُنفَّذ يتحوّل سطرًا يكذب»
- **الموعد:** بعد إغلاق 2026 (‏≈2027-01-05) · **قراءةٌ واحدةٌ فقط**؛ وأيُّ قراءةٍ أبكر **وصفيّةٌ تُطبَع ولا تحكم**. **تعدُّدُ النظرات يُبطل الفاصل.**
- **البناء:** لقطةُ **PIT** لكون 2026-01-02 (لا `freeze`) — فيطابق `H-BACK` في نوع الكون ولا يخلط انحيازَ البقاء بالحكم.
- **الإلزام:** تُنشأ **مهمّةٌ مجدولةٌ لمرّةٍ واحدة** بهذا التاريخ تُشغّل السلسلة وتكتب الحكم — **فلا يبقى الوعدُ ذاكرةً**؛ ورقمُها يُدوَّن هنا عند إنشائها.
