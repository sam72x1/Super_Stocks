# 📉🚦 `T-RSI-RANK` — تسجيلٌ مسبق: **RSI مفتاحَ ترتيبٍ لا بوّابةَ رفض**

> **مدفوعٌ ومدموجٌ في `main` قبل أيّ سطرِ أداةٍ وقبل أيّ رقم.**
> النتيجةُ — حين تصدر — في `rsi_rank_result.md` **بجانب هذا الملفّ لا فوقه**.
> 🔒 **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · لا `force_renew`.**

---

## ⓪ الدعوى ومصدرُها

**ص65 من دليل طريقة فيصل (‏`faisal_adopted`):**

> «ضغطُ سهم الارتكاز قبل الانفجار يجب أن يكون **غالبًا في منطقة RSI 23-27**.
> ولماذا لا 40؟ **RSI حول 40 يعني أن جزءًا من الارتداد حصل بالفعل** … »
> و**«`RSI 23-27` = إنذارُ ضغطٍ لا أمرُ شراء»**.

**وص67 تُكمل الحدَّ صراحةً:** من أخطاء الدخول «**الدخولُ لأن RSI منخفض فقط بلا دعم**».

⇒ **النصُّ نفسُه ينفي أن يكون RSI بوّابةً، ويصفه إشارةَ أولويّةٍ داخل شروطٍ أخرى.**

### ⓪-ب 🔴 هذا **ليس** إعادةَ فتحِ `T-RSI40` — والفرقُ يُنصّ عليه

`T-RSI40` (‏2026-09-09) وُلد من **هذي الصفحة بعينها** وقاس **شدَّ بوّابةِ الرفض**
`RSI_NOW_HARD` من ‏71.1186 إلى ‏40/50/30 — **فشل وأُغلق**، وشرطُ فتحه المنصوص:
«‏① محورٌ جديدٌ لم يُقَس (‏**40 و50 و30 كلُّها محكومة ⇒ إعادةُ عتبةٍ ليست محورًا**)».

| | `T-RSI40` (مُغلَق) | `T-RSI-RANK` (هنا) |
|---|---|---|
| ما يفعله RSI | **بوّابةُ رفض** — يُقصي مرشّحين | **مفتاحُ ترتيب** — لا يُقصي أحدًا |
| الكلفة | مُسلَّمون يُفقَدون | **صفرُ مُسلَّمٍ يُفقَد** (الميزانيةُ ثابتة) |
| الميزانية | تتغيّر مع العتبة | **ثابتةٌ بالبناء** (`WATCHLIST_SIZE`) |
| السؤال | كم يُكلّف الشدّ؟ | **مَن يستحقّ الخانةَ أوّلًا؟** |

🔑 **وشرطُ الفتح ① مستوفًى بدليلٍ لا بدعوى:** بحثُ **خمسِ أدواتِ ترتيبٍ** في
المستودع (‏`ranker_arms` · `ranker2_arms` · `ranker3_arms` · `ranker_tie_arms` ·
`press_rank_arms`) و`presession_feats` ⇒ **صفرُ ذكرٍ لـRSI في أيٍّ منها**، و`T-PRERANK`
حصر مفاتيحَه صراحةً بما يُحسب من الشمعة المجمَّعة (`o,h,l,c,v,vw`) وRSI ليس منها.
⇒ **محورُ «‏RSI مفتاحَ ترتيب» بصفرِ مطابقةٍ في المستودع كلِّه.**
و② (كونٌ يرفع انحيازَ البقاء) **متاحٌ وقد قِيس** · و③ **إذنُ المالك** = أمرُه «سجّل RSI ص65».

### ⓪-ج الآليّةُ **مُثبَتةٌ قبل كتابة هذا العقد** (درسُ `T-RSI40 §⑦`)

عقدُ `T-RSI40` نصَّ على عَلَمٍ تبيّن أنه **`no-op` مُثبَتًا** فأُصلح بملحق. فهنا
**قُرئ الكودُ أوّلًا** وتُنصّ النتيجة:

1. `backtest_symbol` يبعث خلف `BT_ENVVALS`: `trade["env_vals"]["rsi_now"] = r.get("rsi")`
   ⇒ **قراءةُ RSI لحظةَ الإشارة متاحةٌ بلا حقلٍ جديدٍ وبلا مسٍّ بالإنتاج.**
2. `replay10.replay(cands, outcome_of=…, ranker=…)` يقبل **مُرتِّبًا محقونًا**،
   و`rank_live` هو مُرتِّبُ الإنتاج المُعاد بناؤه هناك ⇒ **`rank_key` في
   `Super_stock.py` لا تُمَسّ بحرف.**
3. `ceiling_arms.portfolio` يحسم على **محورِ جلساتٍ موحَّد** (`extra_dates` = اتّحادُ
   تواريخ الأذرع) ⇒ الميزانيةُ والمحورُ واحدان لكلّ الأذرع.
4. `replay10.make_rank_tie_random(seed)` قائمٌ ⇒ شاهدُ العشوائيّ **حتميّ**.
5. `env_vals["drop_pct"]` متاحٌ وهو **عمقٌ سعريٌّ خالٍ من RSI** ⇒ ضبطُ العمق نظيف.

⚠️ **و`env_depth` لا يصلح ضبطًا** لأنه عمقٌ **داخل ظرف الكاتالوج** و`rsi_now` أحدُ
معاييره (`CRITERIA: rsi_now → RSI_MAX_NOW`) ⇒ **ملوَّثٌ بالمقيس**، ويُعلَن ذلك هنا.

---

## ① التعريفات

- **`rsi_now`** = `env_vals["rsi_now"]` — قراءةُ RSI **لحظةَ الإشارة** بدالّة الإنتاج.
  **والغيابُ (`None`) لا يُخمَّن**: يُعامَل «خارج المنطقة» وينزل لآخر فئته (نفسُ عُرف
  `rank_live` مع `in_band`).
- **«المنطقة»** = `23 ≤ rsi_now ≤ 27` — **حدّان من فيصل حرفيًّا** (‏23 حدُّه الأدنى
  `TG_2043` · 27 قراءةُ GWAV ص91) · **ولا مركزَ ولا وزنَ ولا رقمَ من عندي.**
- **الميزانية** = `CONFIG["WATCHLIST_SIZE"]` النافذة (‏15) — قيمةُ إنتاجٍ لا مخترَعة.
- **`FWD` المقياسُ الحاكم** = عددُ المُسلَّمين البالغين **‏+100%** خلال نافذة الحسم،
  بالميزانية نفسِها والمحور نفسِه — **نفسُ مقياس `RS1` في `T-RSI40`** فيُقارَن بما نُشر.

---

## ② الأذرع — أربعٌ ولا خامسة

| الذراع | المُرتِّب | الدور |
|---|---|---|
| **`K0`** | `rank_live` بت-بت | **الأساس** (مُرتِّبُ الإنتاج) |
| **`K27`** 🥇 | **ثنائيّ**: «داخلُ المنطقة أوّلًا» ثمّ **`rank_live` حرفيًّا** | **الحاكمة** |
| **`KC`** | تصاعديًّا بـ`rsi_now` ثمّ `rank_live` | **وصفيّة** — أهي المنطقةُ أم «الأدنى دائمًا أفضل»؟ |
| **`KR`** | `rank_live` لكن **المنطقةُ أخيرًا** (مقلوبة) | **وصفيّة** — شاهدُ اتّجاهٍ يكشف الصدفة |

🔑 **و`K27` تنسخ سابقةً شحنها المالكُ بنفسه** (`T-PROX` 2026-08-07): «**ثنائيٌّ لا
مستمرّ، وترتيبٌ لا رفض**» — الفئةُ الأولى تتقدّم، **وداخلَ كلّ فئةٍ الترتيبُ
byte-identical** مع الإنتاج، ولا يُقصى أحد.

---

## ③ الضبطان — **حاكمان لا وصفيّان** (درسُ اليوم)

**السببُ يُنصّ قبل أيّ رقم:** في `T-PMGATE` فصلت بوّابةُ فيصل ‏+13.74 نقطة **وبدت
نجاحًا**، ثمّ أظهر ضبطُ الزخم الصرف `C-MOM` أن مرجعَها **ينقص ‏4.5 نقاط عن الزخم
المجرَّد** ⇒ **الضبطُ الوصفيُّ كان سيسمح برقمٍ موجبٍ يُصدَّق.** ونفسُه قلب
`T-WAIT-23W`. ⇒ **هنا الضبطان حاكمان بنصّ العقد.**

| الضبط | المُرتِّب | ما يعزله |
|---|---|---|
| **`C-DEPTH`** | «الأعمقُ هبوطًا أوّلًا» بـ`drop_pct` ثمّ `rank_live` | هل **مرجعُ RSI** يضيف على **عمقٍ سعريٍّ خام**؟ |
| **`C-RAND`** | `rank_live` بكاسرِ تعادلٍ عشوائيٍّ حتميّ | هل المفتاحُ إشارةٌ أم ضجيج؟ |

⚠️ **و`C-RAND` ليس بارًا منخفضًا بلا معنى:** `T-RANK-DENSE` قاس أن **المُرتِّبَ
الإنتاجيَّ أسوأُ من العشوائيّ في السنوات الثلاث** ⇒ «‏`K27` يتفوّق على `K0`»
**وحدَه لا يكفي حكمًا** — ولهذا `RK3` مُلزِمة.

---

## ④ المعايير — **مسجَّلةٌ ولا تتغيّر بعد الأرقام**

- **`RK1`** (حاكم): `K27 − K0` **موجبٌ في السنوات الثلاث** وفاصلُ البوتستراب
  المجمَّع (عنقوديٌّ بالرمز · ‏5,000 إعادة · بذرةٌ ثابتةٌ تُطبَع) **لا يلمس الصفر**.
- **`RK2`** (حاكم): `K27 − C-DEPTH` **موجبٌ في سنتين على الأقلّ** وفاصلُه المجمَّع
  **لا يلمس الصفر** ⇒ **مرجعُ فيصل يضيف على العمق الخام.**
- **`RK3`** (حاكم): `K27 − C-RAND` **موجبٌ في الثلاث**.
- **الأرضية:** ‏**‏100 مُعبَّأة** في كلّ سنةٍ وإلّا **«لا حكم»** لتلك السنة
  (‏`engineering` — نفسُ `FLOOR_FILLED` في `T-RSI40` فيُقارَن بما نُشر).

### الفروع الثلاثة
1. **تُوصى** — `RK1` و`RK2` و`RK3` **ثلاثتُها** تعبر.
2. **فشلت** — تعبر الأرضيةُ في سنتين فأكثر **ويسقط** معيارٌ حاكمٌ واحدٌ فأكثر.
3. **«لا حكم»** — الأرضيةُ تسقط في **سنتين فأكثر**.

🔴 **وسقوطُ سنةٍ واحدةٍ على الأرضية ليس «لا حكم» بل الفرعُ 2** — نفسُ ما وقع في
`T-AHEXT-2` وما أصلحه `PGA10`.

---

## ⑤ الحرّاس — تُطبَع في كلّ تقرير

| | الحارس |
|---|---|
| `V-K1` | **`Super_stock.py` بت-بت** مع `origin/main` (بصمةُ الجذور الاثني عشر) |
| `V-K2` | `K0` **يُعيد أرقامَ الأساس المنشورة** أو يُعلَن الفرقُ وسببُه الحسابيّ |
| `V-K3` | **الميزانيةُ والمحورُ والكونُ والنافذةُ واحدةٌ لكلّ الأذرع** وتُطبَع |
| `V-K4` | **عددُ المرشّحين المعروضين متطابقٌ بت-بت** بين كلّ الأذرع (ترتيبٌ لا إقصاء) |
| `V-K5` | `K27` **داخلَ كلّ فئةٍ** يطابق `K0` بت-بت (سابقةُ `T-PROX`) |
| `V-K6` | `rsi_now` **حاضرٌ في ‏≥90%** من الصفوف وإلّا يُعلَن ولا يُخمَّن |
| `V-K7` | **صفرُ نظرٍ مستقبليّ**: المُرتِّب يقرأ حقولَ لحظة الإشارة حصرًا |
| `V-K8` | البذرةُ والإعاداتُ والعتباتُ **تُطبَع** — ولا عشوائيّةَ خفيّة |

---

## ⑥ التنبّؤاتُ القابلة للتكذيب — **تُنشَر مكذَّبةً لا تُحذَف**

| # | التنبّؤ |
|---|---|
| `P1` | `K27` يتفوّق على `K0` — **لأن `K0` أسوأُ من العشوائيّ مقيسًا** |
| `P2` | **`RK2` هو الذي يسقط** (‏`C-DEPTH` يلتقط أكثرَ ما يلتقطه RSI) — وهو أرجحُ الفروض عندي |
| `P3` | `KC` (الأدنى دائمًا) **أضعفُ** من `K27` ⇒ المنطقةُ لا الرتابة |
| `P4` | `KR` المقلوبة **أسوأُ** من `K0` — وإلّا فالمفتاحُ صدفة |
| `P5` | حصّةُ الصفوف داخل ‏23-27 **أقلُّ من 25%** من المؤهَّلين |

---

## ⑦ حدودُ الصدق — **مُعلَنةٌ قبل أيّ رقم**

1. **أربعُ تجاربِ توقيتِ دخولٍ سابقة كلُّها لم تُوصَ**، وثلاثُ تجاربِ مُرتِّبٍ
   (`T-RANKER`/`T-RANKER2`/`T-RANKER3`) **فشلت** ⇒ **الاحتمالُ الأسبقُ الفشل.**
2. **انحيازُ البقاء قائم** (كونُ اللقطة) — و`T-SURVIVORSHIP` قاس أن أثرَه
   **‏−0.79 نقطة و−0.031R** أي داخلَ الضجيج، فلا يُدَّعى أنه يقلب حكمًا.
3. **المقيسُ عددُ المُسلَّمين لا الربحيّة** — ولا يُقرأ توقّعًا ماليًّا.
4. `rsi_now` من الشمعة اليوميّة المغلقة ⇒ **لا يقرأ لحظةَ التنفيذ.**
5. **تعدّدُ مقارنات:** أربعُ أذرعٍ وضبطان ⇒ الوصفيّاتُ `KC`/`KR` **مرشَّحاتٌ لا أحكام**.

---

## ⑧ ما **لا** يحدث أبدًا بهذي التجربة

- ❌ لا تتحرّك `RSI_NOW_HARD` ولا `RSI_MAX_NOW` ولا `RSI_OVERSOLD` ولا `RSI_OS_HARD`.
- ❌ لا تُمَسّ `rank_key` ولا `select_top` ولا أيُّ جذرٍ من الاثني عشر.
- ❌ لا تُقصى مرشّحةٌ واحدة — **ترتيبٌ لا رفض**.
- ❌ لا `LOGIC_VERSION` ولا `force_renew` ولا رسالةٌ تُرسَل من الأداة.
- ❌ **ولا تُرفَع عيّنةٌ ولا تُبدَّل عتبةٌ بعد رؤية رقم** — العقدُ مدموجٌ لا يُعدَّل،
  وأيُّ تعديلٍ يلزمه **ملحقٌ مؤرَّخٌ يُدفَع قبل قراءة الرقم التالي**.

---

## ⑨ شرطُ الإغلاق — **مكتوبٌ قبل النتيجة**

إن سقطت `RK2` (أي **لم يُضف مرجعُ فيصل على العمق الخام**) ⇒ **يُغلَق محورُ
«‏RSI مؤشّرَ أولويّةٍ في الفرز» إغلاقًا مُنفَّذًا لا مكتوبًا** (سابقةُ `T-RSI40`
و`T-PMGATE`)، ولا يُعاد إلّا بمصدرٍ ثانٍ للدعوى **غيرِ الدليل والتفريغ** ‏+ تسجيلٍ
جديدٍ ‏+ إذنِ المالك.

---

## ⑩ ملحقٌ مؤرَّخ — **2026-09-12، مدفوعٌ قبل قراءة أيّ رقمٍ من الأداة**

> §⑧ ينصّ: «**ولا تُرفَع عيّنةٌ ولا تُبدَّل عتبةٌ بعد رؤية رقم** — العقدُ مدموجٌ
> لا يُعدَّل، وأيُّ تعديلٍ يلزمه **ملحقٌ مؤرَّخٌ يُدفَع قبل قراءة الرقم التالي**».
> وهذا هو. **صفرُ رقمٍ من الأداة قُرئ حتى لحظة دفعه** — والمقيسُ أدناه كلُّه على
> **فِكستشرٍ صناعيّ** (‏40 رمزًا × 28 جلسةً مولَّدة) لا على أيّ لقطةٍ تاريخيّة.

### ⑩-أ 🔴🔴 دعوى «صفرُ مُسلَّمٍ يُفقَد» **مكذَّبةٌ بالقياس** — وهي عمادُ §⓪-ب

§⓪-ب يُخرج هذي التجربةَ من إغلاق `T-RSI40` بحجّة «**ميزانيّةٌ ثابتة · صفرُ مُقصًى
وصفرُ مُسلَّمٍ يُفقَد**». **والقياسُ يكذّب شطرَها الثاني:** الميزانيةُ ثابتةٌ
**خاناتٍ لا صفقات** — المركزُ يحجز خانتَه حتى خروجه، فاختلافُ الترتيب يغيّر
**مُدَدَ الحجز** فيغيّر **عددَ الصفقات**. على الفِكستشر: تعبئةُ الأذرع
**‏160 ⟶ 186**، و**`K27` تُسلّم ‏+14 صفقةً أكثرَ من `K0`**.

⇒ **`FWD` الخام يخلط «ترتيبًا أفضل» بـ«صفقاتٍ أكثر»** — وهو **درسُ `T-WAIT-23W`
المدوَّن مقلوبًا** (هناك ربحت ذراعٌ لأنها **تقلّل** التعرّض، وهنا قد تربح لأنها
**تزيده**). وهناك **قلبَ الضبطُ الحكمَ**.

### ⑩-ب 🎚️ `M27` — ضبطٌ مطابَقُ التعبئة **حاكم**، و`RK4` معه

**`M27`** = `K0` **بسعةٍ مُعايَرة** (بحثٌ صاعدٌ على السعة الصحيحة حتى تقارب
تعبئتُه تعبئةَ `K27`) ⇒ `K27 − M27` **يعزل الترتيبَ عن عدد الصفقات**.

- **`RK4`** (حاكم): `K27 − M27` **موجبٌ في السنوات الثلاث** وفاصلُه المجمَّع
  **لا يلمس الصفر**.
- **والفرعُ 1 يصير:** `RK1` و`RK2` و`RK3` **و`RK4`** أربعتُها تعبر.
- ⚖️ **وهو تشديدٌ محضٌ لا إرخاء:** لا يمكن لـ`RK4` أن يجعل حكمًا ساقطًا ناجحًا.
- 📐 ويُطبَع معه **فرقُ المعدّل** (`d100/filled`) **وصفيًّا** لا حاكمًا.
- ⚠️ **وحدُّ صدقٍ مقيس:** التعبئةُ **ليست رتيبةً في السعة** (مقيسٌ على فِكستشر:
  ‏186 عند سعة 15 · **192** عند 17 · **151** عند 22) لأن المركزَ يحجز خانتَه
  فتتشابك القرارات ⇒ **المطابقةُ التامّة مستحيلةٌ بمقبضٍ صحيحِ العدد**؛ المطلوبُ
  **تقليصُ الفجوة** لا انعدامُها. تُطبَع `match_gap` ونسبتُها في كلّ سنةٍ ولا تُطوى.
- 🔴 **وضبطٌ مكسورٌ لا يمرّ بصمت:** فجوةٌ فوق **‏10% من الهدف** (`MATCH_MAX_GAP_PCT`
  · `engineering`) **تُسقط `RK4`** — لأن المعيارَ يقيس «الترتيبَ عند عددِ صفقاتٍ
  واحد»، فإن لم يتطابق العددُ فلا موضوعَ له. **وهذا تشديدٌ ثالث.**

### ⑩-ج 📐 كونُ عناقيد البوتستراب — **كلُّ رمزٍ مرشَّح لا المُصيبون**

كان الفاصلُ يأخذ عناقيدَه من **اتّحاد المُصيبين** فقط ⇒ **مجموعةٌ مُنتقاةٌ
بالنتيجة**، ويسقط من الكون كلُّ رمزٍ مساهمتُه صفر. **والأثرُ مقيسٌ واتّجاهُه
معلوم: الفاصلُ ينكمش** (عرضٌ ‏6 مقابل 7 في مثالٍ صغير · و35 عنقودًا بدل 40 في
الفِكستشر) ⇒ شرطُ «لا يلمس الصفر» **كان متساهلًا**.
⇒ الكونُ الآن = **كلُّ رمزٍ مرشَّح**، ومساهمةُ مَن لم يُصِب **صفرٌ يُعَدّ**.
**وهذا تشديدٌ أيضًا.**

### ⑩-د 🔒 خمسةُ إحكاماتٍ أخرى — كلُّها تشديد

1. **الفاصلُ على السنوات المؤهَّلة وحدَها** (كان يبتلع سنةً أسقطتها الأرضيةُ
   فيقرأ المعياران مجتمعَين مختلفَين).
2. **`V-K3` يُقاس** (السعةُ والمحورُ كما استعملهما `replay` لكلّ ذراع) **ويوقف**
   — كان يُطبَع تطابقًا تافهًا بالبناء.
3. **`V-K2` يوقف**: §⑤ يجيز المضيَّ «أو يُعلَن الفرقُ **وسببُه الحسابيّ**»
   والأداةُ لا تملك السبب ⇒ تتوقّف حتى يُعلنه إنسانٌ بإقرارٍ صريح.
4. **شاهدُ حياةٍ للحاكمة `K27`** لا للضبط وحدَه: لو طابقت `K0` فالتجربةُ بلا
   موضوع وتتوقّف (`no-op` صريح).
5. **الطفلُ يُجرَّد من مفاتيح تلغرام**: `run_backtest` فيه إرسالٌ ووثائقُ CSV بلا
   حارس، ودعوى «صفرُ إرسال» كانت تخصّ **ملفَّ الأداة** لا العمليةَ الابنة.

### ⑩-هـ 🎲 `D-RAND` — سابعةٌ **وصفيّةٌ** تُعلَن ولا تحكم

§② يقول «أربعٌ ولا خامسة» و§③ يضيف ضبطين ⇒ **ستّ**. والأداةُ تبني **سابعةً**:
`D-RAND` (عشوائيٌّ كامل) — لأن تبريرَ `C-RAND` في §③ يستند إلى `T-RANK-DENSE`
**وقياسُه كان على العشوائيّ الكامل**. ⇒ **يُعلَن هنا صراحةً: `D-RAND` وصفيّةٌ
خارجَ `RK1`-`RK4` وخارجَ كلّ معيار**، وتُطبَع ليُقرأ الغموضُ لا ليُحسَم به.

### ⑩-و ⚠️ ما **لا** يتغيّر بهذا الملحق

`RK1`/`RK2`/`RK3` **بنصّها** · الأرضيةُ 100 · الفروعُ الثلاثة · المنطقةُ
‏[23, 27] · `FWD` = ‏+100% · البذرةُ والإعادات · الحرّاسُ الثمانية · §⑦ حدودُ
الصدق · §⑧ ما لا يحدث · §⑨ شرطُ الإغلاق. **وكلُّ ما أُضيف يُشدِّد أو يُعلِن،
ولا يُرخي معيارًا واحدًا.**
