# OPUS_EXECUTION_SPEC.md — حزمةُ تنفيذٍ لدفعة 32 صورة (2026-09-22/23 · `TG_50575`…`TG_50606`)

> **إلى المُنفِّذ (أوبيس):** هذا عقدُ تنفيذٍ لا اقتراح. تُقرأ أقسامُه العشرون بالترتيب، ولا يُكتب سطرُ كودٍ قبل §9 (حالةُ الكود المثبَّتة) و§8 (الحدودُ الحمراء). المرجعُ التحليليّ الكامل في `SOURCE_ANALYSIS.md` والمصفوفةُ في `REQUIREMENTS_MATRIX.md` والغوامضُ في `UNCERTAINTIES.md` — **ولا يعتمد أيُّ سطرٍ هنا على بندٍ من الغوامض**.
> 🔴 **لا يُنفَّذ إلّا بأمر المالك «نفّذ»** على هذي الحزمة (سابقةُ `OPUS_FAISAL_BATCH_2026-09-18_PACKAGE.md`).
> ⚖️ **لا وكلاء** («مب مسموح لك تستخدم اي ايجنت انت سو كل شي») · **لا PR بلا طلب** · **لا معرِّفَ نموذجٍ في أيّ ملفٍّ مدفوع**.

---

## 1. OBJECTIVE
سطرُ عرضٍ واحد **«🧭 دورة الارتكاز»** في الكرت واليوميّ وفحص اليد يقول في أيّ مرحلةٍ من مراحل فيصل الأربع السهمُ الآن — بأرقامه الحرفيّة (`TG_50584`: ارتدادُ اختبارٍ **‏20% غالبًا** · سحبٌ **‏5% تحت أدنى شمعة القاع** أو ثبات · ثباتٌ بعد السحب = الأهداف) — **عرضٌ/سياقٌ فقط**، صفرُ مسٍّ بالفرز أو الدخول أو الوقف أو الأهداف أو العضويّة.

## 2. CONTEXT
- الدفعةُ **تؤكّد أكثرَ ممّا تُضيف**: 8 قواعدَ مبنيّةٌ سلفًا · 5 مُغلَقةٌ بالقياس · 6 لا تُبنى لضعف السند · **ومتطلَّبٌ واحدٌ ذو كود** (R-01) ووسمٌ اختياريّ (R-02).
- الفجوةُ المقيسة: أجزاءُ الدورة موجودةٌ متفرّقةً (`method_sequence` في صيّاد النهج العلميّ · `bottom_test_state` «القاع 2» · `FAISAL_TEST_BAND` نطاقُ الاختبار · `SPLIT_SWEEP_*` نطاقُ السحب) **ولا سطرَ في كرت الارتكاز يجمعها مرحلةً واحدة**.
- سابقةٌ تُحتذى: المسارُ أ من حزمة 2026-09-18 (خمسُ دوالَّ نقيّة + سطرٌ + مفاتيح + أقفال `FB1`-`FB12` + طفرات) نُفِّذ بأمر «نفّذ» والجذورُ بت-بت.

## 3. SOURCES (بالترتيب: الحاكم ثمّ المساند)
| المصدر | الوسم | ما يُؤخذ منه |
|---|---|---|
| `TG_50584` (فيصل · X · 2026-09-22) | `faisal_verbatim` | الدورةُ الرباعيّة بأرقامها 20% و5% · «ثبات **أو** سحب» |
| `TG_50586` + `TG_50579` (فيصل) | `faisal_verbatim` | «اختبار مقاومه لازم يرجع يختبر الدعم — قواعد ثابته لكل الاسهم» ⇒ التعميم |
| `TG_50599` (القناة + تصديقُ فيصل) | `faisal_adopted` | «متوسط حركه 10<20 بعد القاع الاول» · مفرداتُ وايكوف (R-02) |
| `X_85_YMT` · `IMG_0486`/`0488` · ONCO (`IMG_0150-0153`) · CCHH (`§سادس عشر`) | قائمة | ‏5 جلسات/20% · ‏≥10% · 5% · 7-13% |
| **لا يُؤخذ:** `TG_50606` (مجهول) · `TG_50592`/`50585`-التطبيق (`third_party`) · `TG_50603` (دفعاتٌ تحت الدعم) | — | انظر `UNCERTAINTIES.md` U-01/U-02 |

## 4. KNOWLEDGE BASE SUMMARY
`SOURCE_ANALYSIS.md §①` (‏26 بندًا KB-01…KB-26) و`§②` (‏10 أنماط P1…P10). الحاكمُ هنا: **P1** (التسلسلُ الرباعيّ · خمسةُ مصادر في أربعة أيّام · `CONFIRMED`).

## 5. REQUIREMENTS
- **R-01 (إلزاميّ):** `pivot_cycle_state` + `pivot_cycle_line` + مفتاحان + تخزينٌ/تجديدٌ + وصلٌ ثلاثيّ + دفتر.
- **R-02 (اختياريّ · يُنفَّذ إن لم يُكلّف سطرًا إضافيًّا في الكرت):** وسمُ وايكوف داخل السطر نفسِه.
- **R-21 (توثيق):** منفَّذٌ مع هذي الحزمة (الكاتالوج §ثامن وعشرون · الدفتر §دفعة 2026-09-22 · README) — **لا يُعاد**.

## 6. RULES — بوسمها
| القاعدة | الوسم | الرقم | السند |
|---|---|---|---|
| المرحلة 2 = ارتدادٌ من القاع يختبر المقاومة | **GENERAL RULE** | حدٌّ أدنى **‏10%** (قائم `METHOD_BOUNCE_MIN_PCT`) · نموذجيّ **‏20%** (جديد `PIVOT_TEST_TYPICAL_PCT`) | `IMG_0486` · `TG_50584` · `X_85` · `TG_50599` |
| المرحلة 3 = عودةٌ لاختبار القاع: **ثبات** (لا إغلاقَ تحته بأكثر من التسامح) **أو سحبٌ** ‏≤5% تحت أدنى شمعة القاع ثمّ استعادة | **GENERAL RULE** | **‏5%** (جديد `PIVOT_SWEEP_PCT`) · التسامحُ القائم `METHOD_HOLD_TOL=0.015` | `TG_50584` · ONCO `SPLIT_SWEEP_PCT=5` · CCHH 7-13 (**الطيفُ يُذكَر في السطر لا يُشحَن حدًّا**) |
| المرحلة 4 = ثباتٌ بعد الاختبار/السحب ⇒ «تحقيق الأهداف» | **GENERAL RULE** | ثباتٌ **‏≥3 جلسات** بعد أدنى الاختبار بلا إغلاقٍ تحت القاع (‏`IMG_0486` «ثبات 3-5» · `X_85` «5») — يُعلَن «‏3-5» | `TG_50584` |
| سهمُ «الهبوط بعد الصعود العالي» يُدار بالكلاسيكي مع الشروط | **GENERAL RULE** (مبنيّ M1-M5) | — | `TG_50584` |
| «الدخول عند الدعم 2» | **POSSIBLE RULE · مُغلَق كسياسة** | — | `TG_50599` · `T-SWEEP-RECLAIM` |
| كلُّ 25% مقاومة · تصحيح 30% | **POSSIBLE RULE · unsourced** | — | `TG_50606` |
| أمثلةُ DCOY/LIMN/EDBL/NXTT/MSGY/CIIT/ZCMD | **EXAMPLE** | — | — |

## 7. DATA
لا بياناتٍ جديدةً تُجلَب: الدالّةُ تعمل على `df` اليوميّ المتاح في `scan_market`/`update_watchlist_status`/`hand_check` (نفسُ `df` الذي تُمرَّر له `bottom_test_state`). **صفرُ نداءٍ شبكيٍّ جديد.**

## 8. CONSTRAINTS / RED LINES — يُوقَف فورًا ويُبلَّغ
1. **الجذورُ الاثنا عشر بت-بت** (بصماتُها في §9) — أيُّ اختلافٍ = توقّف.
2. **`pivot_stability` لا تُمَسّ** (بالاسم).
3. **لا `LOGIC_VERSION`** (عرضٌ فقط) · لا `force_renew`.
4. **الإسنادُ داخل حارس `try`** في `scan_market` و`update_watchlist_status` (نمط `BK9`/`FB11`) — سقوطُ الدالّة لا يمسّ العضويّة.
5. **لا رقمَ إلّا الموسوم في §6** · صفرٌ من التطبيق/Notes/القناة.
6. **لا فتحَ محورٍ مُغلَق** (`T-SWEEP-RECLAIM` · `T-WAIT-LOWER` · `T-TRAIL` · `T-PMGATE` · `T-CANDLE` · `T-MA-LADDER` · `T-AHEXT`) — الجردُ من `grep -ln "CLOSED_RC" *.py`.
7. **`method_sequence` و`bottom_test_state` و`trigger_state` لا تُعدَّل** — تُستدعى فقط.
8. **ممنوعٌ `≥ ≤ > <`** في أيّ نصٍّ معروض.
9. الملفّاتُ التي **لا تُدفَع**: `منهج فيصل.pdf` · دليلُ إليوت · التفريغُ الصوتيّ.
10. **`company_cache.json`** إن تغيّر بالاختبار ⇒ `git checkout -- company_cache.json`.

## 9. EXISTING CODE STATE — إبرٌ مثبَّتة على `origin/main` `d2c15de7f` (‏2026-09-23)
### 9-0 بصماتُ الجذور (SHA-256 على `ast.dump` · 12 محرفًا) — **يجب أن تبقى هي هي**
```
rank_key 12304e2e8763 · select_top ea82d29a84aa · classify_tier 4000f581bdcc · analyze_ticker d36af8284ef9
apply_short_gate 6c3abbe6830c · apply_float_gate 750a84d4c2c5 · scan_market 8e1d20907dea · backtest_symbol ee2379df02ea
scan_ignition bf59c3a495ba · scan_split_hunter 1c6d651d25b5 · entry_status a9f12435b523 · build_interpretation 792fe1aab01b
```
⚠️ `scan_market` و`analyze_ticker` **ستتغيّر بصمتُهما بالضرورة** إذا أُضيف الإسنادُ داخلهما (كما في `FB11`) — فالقاعدةُ: **تتغيّر الكتلةُ العرضيّة داخل الحارس فقط**، ويُثبَت بـ`PC5` أن الاختلافَ محصورٌ في عقدةِ `Try` واحدة (قارن `ast.dump` بعد إسقاط تلك العقدة). الباقية العشرُ بت-بت بلا استثناء.
### 9-1 ما هو موجودٌ سلفًا (لا يُعاد بناؤه)
| الاسم | الموضع (على `main` اليوم) | ما يفعل |
|---|---|---|
| `method_sequence(df, win, bounce_min, tol)` | `Super_stock.py:7361` | قاع ⟶ اختبار مقاومة ‏≥`METHOD_BOUNCE_MIN_PCT`(10) ⟶ رجوعٌ يختبر القاع ⟶ ثباتٌ بلا إغلاقٍ تحته (`METHOD_HOLD_TOL`=0.015) · يعيد `{ok,bottom,peak,bounce_pct,retest_low,undercut_pct,touches}` |
| `bottom_test_state(df, lookback=60, tol_pct=3.0, sep=3)` | `:6690` | عددُ لمسات القاع ⇒ «القاع 2» |
| `bottom_test_line(bt, sweep_zone)` | `:6717` | سطرُ 🔁 |
| `CONFIG["SPLIT_SWEEP_MIN/MID/MAX_PCT"]=7/10/13` · `SPLIT_SWEEP_PCT=5` | `:672-675` | نطاقُ سحب المقسّم (ONCO/CCHH) |
| `CONFIG["METHOD_BOUNCE_MIN_PCT"]=10` · `METHOD_HOLD_TOL=0.015` | `:578-579` | حدودُ التسلسل |
| `CONFIG["FAISAL_TEST_BAND_LO/HI_PCT"]=15/25` | `:678-679` | نطاقُ اختبار المقاومة (عرض) |
| نقاطُ التخزين/التجديد لـ`bottom_test` | `:6937` (صيّاد) · `:10844` (`make_watch_entry`) · `:12060` (`scan_market` داخل حارس) · `:12603` (`update_watchlist_status`) | **النمطُ الذي يُحتذى حرفيًّا** |
| نقاطُ العرض لـ`bottom_test_line` | `:8320` (`build_message`) · `:8646` (صيّاد) · `:9733` (تحليل) · `:18291` (`build_daily_message`) · `hand_check.py:193` (بعد `candle_supports_line`) | **يُوصَل السطرُ الجديد مباشرةً بعد `_bt` في كلٍّ** |
| `faisal_wait_suffix` · `first_rise_suffix` · `faisal_candle_supports` | `:4452` · `:4513` · `:4532` | لا تُمَسّ |
| أقفالُ السابقة `BK1`-`BK9` · `FB1`-`FB12` | `test_bot.py:45445-45780` | **تُوضع أقفال `PC*` بعد `FB12`** |
### 9-2 الغائبُ فعلًا (الجديدُ الصافي)
`pivot_cycle_state` · `pivot_cycle_line` · `PIVOT_TEST_TYPICAL_PCT` · `PIVOT_SWEEP_PCT` · (اختياريّ) `_WYCKOFF_TAG` + `WYCKOFF_TAG_ON` · الحقلُ `pivot_cycle` في القائمة.

## 10. GAP ANALYSIS
| الجزء | موجود؟ | الفجوة |
|---|---|---|
| المرحلة 1-2 (قاع + ارتداد ‏≥10%) | ✅ `method_sequence` | لا وسمَ «نموذجيّ 20%» |
| المرحلة 3-ثبات | ✅ `method_sequence` (undercut ‏≤tol) | — |
| المرحلة 3-سحب | ⚠️ `trigger_state` تشترط **كسرًا 7-13%** (نقيضُ «حافظ») · `bottom_test_state` تعدّ اللمسات بلا اتّجاه | **فرعُ «سحب ‏≤5% ثمّ استعادة»** غائب |
| المرحلة 4 (ثبات بعد الاختبار ⇒ أهداف) | جزئيّ (`touches`) | عدُّ جلسات الثبات **بعد** أدنى الاختبار |
| سطرٌ واحدٌ في كرت الارتكاز | ❌ | `pivot_cycle_line` |

## 11. ARCHITECTURE
```
scan_market ──(داخل try الإثراء العرضيّ)──► r["pivot_cycle"] = pivot_cycle_state(df)
make_watch_entry ──► يخزّن الحقل · update_watchlist_status ──(try)──► يجدّده يوميًّا
build_message / build_daily_message / hand_check ──► pivot_cycle_line(r.get("pivot_cycle")) بعد سطر 🔁
```
- **نقيّة · بلا نظرٍ مستقبليّ · فاشلة-آمنة (`None` عند أيّ عطل) · لا تقرأ إلّا `df` المُمرَّر.**
- الفرعُ «ثبات» = `method_sequence(df, win)` كما هي. الفرعُ «سحب» = بعد القمّة، أدنى قاعٍ لاحق `L` مع `bottom×(1−PIVOT_SWEEP_PCT/100) ≤ L < bottom×(1−tol)` **وآخرُ إغلاقٍ فوق `bottom`** ⇒ المرحلة 3-سحب؛ ومع ثباتٍ ‏≥3 إغلاقاتٍ فوق `bottom` بعد `L` ⇒ المرحلة 4.
- **الأولويّة عند تعارض الفرعين:** الثباتُ أوّلًا (المنصوصُ «ثبات **أو** سحب» يقدّم الثبات).

## 12. PHASED PLAN — مراحلُ صغيرةٌ قابلةٌ للتحقّق (كلُّ مرحلةٍ commit مستقلّ)
### Phase 0 — التثبيت قبل اللمس
- **OBJECTIVE:** إثباتُ نقطة البداية. **FILES:** لا شيء يُعدَّل. **CHANGES:** `git fetch --force origin main` · `git checkout -B <branch> origin/main` · تشغيلُ بصمات §9-0 وتطابقُها · `python3 test_bot.py` خروج 0. **DEPENDENCIES:** أمرُ «نفّذ». **TESTS:** البصماتُ 12/12 · السويّةُ خضراء. **EXPECTED:** شجرةٌ نظيفة على `main` الحيّ. **FAILURE:** أيُّ بصمةٍ مختلفة ⇒ توقّف وإبلاغ (المستودعُ تحرّك).
### Phase 1 — المفتاحان + صفّا الدفتر
- **FILES:** `Super_stock.py` (`CONFIG` بجانب `:672-675`) · `FAISAL_SOURCE_LEDGER.md` (§دفعة 2026-09-22). **CHANGES:** `"PIVOT_TEST_TYPICAL_PCT": 20.0,  # faisal_verbatim TG_50584 · X_85` و`"PIVOT_SWEEP_PCT": 5.0,  # faisal_verbatim TG_50584 · IMG_0150 ONCO`. صفّان في الدفتر بوسم `faisal_verbatim` واحدٍ لكلّ. **TESTS:** `PC6` (صفّان موجودان بوسمٍ واحد) · قفلُ الدفتر القائم «صفر صفٍّ بلا وسم». **EXPECTED:** السويّة خضراء. **FAILURE:** صفٌّ بوسمين/بلا وسم.
### Phase 2 — `pivot_cycle_state` (نقيّة)
- **FILES:** `Super_stock.py` بعد `bottom_test_line` (‏`:6748`). **CHANGES:** الدالّةُ كما في §11 بتوقيع `pivot_cycle_state(df, win=None, typical=None, sweep=None, hold_bars=3)` تعيد `{stage, bottom, bounce_pct, typical, retest_low, sweep_pct, held_bars, branch∈{"hold","sweep",None}}` أو `None`. docstring يحمل نصَّ `TG_50584` حرفيًّا وافتراضَ U-11 (الارتدادُ من أعلى قمّةٍ بعد القاع · السحبُ من أدنى شمعة القاع). **DEPENDENCIES:** Phase 1. **TESTS:** `PC1` (قطعُ `df` عند بارٍ = نفسُ الناتج للبارات السابقة — بلا نظرٍ مستقبليّ) · `PC2` (ثلاثةُ مساراتٍ اصطناعيّة: ثبات ⇒ 4 · سحب 4% ثمّ استعادة ⇒ 4 · سحب 9% ⇒ ليس 4) · `PC3` (‏12% ⇒ `typical=False` · 22% ⇒ `True`). **EXPECTED:** ثلاثةُ أقفالٍ خضراء. **FAILURE:** أيُّ استثناءٍ يتسرّب (يجب `None`).
### Phase 3 — `pivot_cycle_line` (+ وسمُ وايكوف الاختياريّ)
- **FILES:** `Super_stock.py`. **CHANGES:** سطرٌ واحد: `🧭 دورة الارتكاز: المرحلة N/4 — <وصف> (فيصل: ارتدادٌ نموذجيّ 20% · سحبٌ حتى 5% تحت شمعة القاع)`، الأوصاف: 1 «قاعٌ مكوَّن» · 2 «ارتدادٌ يختبر المقاومة X%» (+«نموذجيّ» عند ‏≥20) · 3 «عودةٌ تختبر القاع — ثبات/سحب Y%» · 4 «ثباتٌ بعد الاختبار ⇒ تحقيق الأهداف». إن `WYCKOFF_TAG_ON` أُلحق «(وايكوف: …)» من `_WYCKOFF_TAG`. **بلا `≥ ≤ > <`.** **TESTS:** `PC4` (السطرُ يظهر مع الحقل ويغيب بدونه — فارقٌ محدَّد) · `PC9` (الوسمُ يغيب عند إطفاء المفتاح) · قفلُ «لا علاماتِ مقارنة» القائم. **FAILURE:** السطرُ يظهر بلا حقل.
### Phase 4 — التخزين والتجديد
- **FILES:** `Super_stock.py` `:6937` (صيّاد — اختياريّ) · `:10844` (`make_watch_entry`) · `:12060` (`scan_market` **داخل نفس `try`** الذي يحمل `bottom_test`) · `:12603` (`update_watchlist_status`). **CHANGES:** `r["pivot_cycle"] = pivot_cycle_state(df)` بجانب `bottom_test` في كلٍّ. **TESTS:** `PC8` (بالـAST: الإسنادُ في `scan_market` داخل `Try` — نسخةُ `FB11`) · `PC5` (الجذورُ: 10 بت-بت · و`scan_market`/`analyze_ticker` تختلفان **فقط** بعقدة الحارس · `pivot_stability` بت-بت). **FAILURE:** إسنادٌ خارج الحارس · أو جذرٌ آخر تغيّر.
### Phase 5 — الوصلُ الثلاثيّ
- **FILES:** `Super_stock.py` `:8320` و`:18291` (بعد `_bt`) · `hand_check.py:193` (بعد `_csl`). **CHANGES:** `_pc = pivot_cycle_line(r.get("pivot_cycle"))` ويُلحَق بالأسطر حيث يُلحَق `_bt`. **TESTS:** `PC7` (AST: نداءُ `pivot_cycle_line` داخل `build_message` و`build_daily_message` و`hand_check` — من نقطة النداء الحيّة لا من وجود الدالّة). **FAILURE:** الدالّةُ معرَّفةٌ ولا تُنادى (صنفُ «ميزة موصولة» الكاذب — درسُ ربع الساعة).
### Phase 6 — جولةُ الطفرات (worktree معزول · شجرةٌ ملتزمة · `PYTHONDONTWRITEBYTECODE=1` · مرساةٌ فريدة `count == 1`)
| الطفرة | التغيير | القفلُ المتوقَّع سقوطُه |
|---|---|---|
| `p1` | قلبُ إشارة السحب (`bottom×(1+…)`) | `PC2` |
| `p2` | حذفُ فرع الثبات | `PC2` |
| `p3` | `PIVOT_SWEEP_PCT=13` | `PC2` (سحب 9% يصير 4) |
| `p4` | قطعُ الوصل من `build_message` | `PC7` |
| `p5` | حذفُ صفّ الدفتر | `PC6` |
| `p6` | إخراجُ الإسناد من `try` في `scan_market` | `PC8` |
| `p7` | الدالّةُ تقرأ `df.iloc[-1]` بعد القطع (نظرٌ مستقبليّ) | `PC1` |
| `p8` | تبديلُ وسمَي وايكوف | `PC9` |
| `p9` | الإسنادُ في `select_top` (عمدًا) | `PC5` |
- **المقياس:** رمزُ الخروج + أسماءُ الأقفال الساقطة + هل انهار (`Traceback`). **9 من 9 تسقط بقفلها المسمّى**؛ ناجيةٌ ⇒ افحص ④-3 (بلا أثرٍ سلوكيّ) قبل تسميتها عيبًا؛ وطفرةٌ تكشف كودًا ميّتًا ⇒ يُحذف الكود لا يُخترع قفل. **تُستعاد الشجرة في `finally` وتُعاد السويّة.**
### Phase 7 — البوّابة والدفع
- `python3 test_bot.py` خروج 0 وصفرُ فشل على الشجرة **التي تُدمَج نفسِها** (cmp-identical مع worktree الطفرات) · `git checkout -- company_cache.json` · commit بالترويسة المعتمدة · push `-u` · PR · انتظارُ CI الخضراء · squash · ثمّ `git fetch --force` وإعادةُ الفرع من `origin/main`.
### Phase 8 — الذاكرة والتقرير
- بندٌ في `DECISIONS_ARCHIVE.md` عبر `add_memory.py` (المقتطعُ يُشتقّ لا يُكتب) · سطرٌ في `HANDOFF.md` عبر `add_handoff.py` · مؤشّرٌ في `CLAUDE.md` تحت «الملفّات المهمّة» (سطرٌ واحد) · تحقّق `MEM1<250,000` و`HND1<150,000` · التقريرُ للمالك بالعربيّة يُختم بخياراته حرفيًّا.

## 13. TESTS / LOCKS (الأسماءُ فريدة — `PC` غيرُ مستعملة في السويّة اليوم)
`PC1` بلا نظرٍ مستقبليّ · `PC2` الفرعان والعتبة · `PC3` وسمُ 20% · `PC4` فارقُ السطر · `PC5` الجذور + `pivot_stability` · `PC6` الدفتر · `PC7` الوصلُ بالـAST · `PC8` الإسنادُ داخل الحارس · `PC9` وسمُ وايكوف. **كلُّ قفلٍ يلزمه طفرةٌ تُسقطه (§12 Phase 6) وإلّا فهو زينة.** القفلُ الذي ينادي كودًا يرمي يُلفّ بـ`try` ويعيد `⛔` نظيفًا لا انهيارًا.

## 14. MUTATIONS
§12 Phase 6 · المِنصّة من `.claude/skills/lock-and-mutate` تُنسخ كما هي · لا `grep -c '❌'`.

## 15. HANDLING UNCERTAINTIES
- U-11 (مرجعُ القياس) وU-12 (النافذة) **افتراضان آمنان مُعلَنان في الـdocstring** — لا يُسأل عنهما.
- U-01/U-02/U-13 **لا تدخل الكود** بأيّ صورة؛ إن وجدتَ نفسَك تحتاج رقمًا منها ⇒ **توقّف**.
- سؤالٌ واحدٌ للمالك (في التقرير لا قبل التنفيذ): كاتبُ `TG_50606`.

## 16. REPORTING (للمالك · عربيّ · بلا سلسلة تفكير)
① ما نُفِّذ بالأسماء والأسطر ② بصماتُ الجذور قبل/بعد ③ حصيلةُ الطفرات (‏n/n بأسمائها) ④ لقطةُ سطرٍ حيٍّ واحد من الكرت ⑤ ما **لم** يُنفَّذ ولماذا (R-11…R-20) ⑥ الخياراتُ حرفيًّا: «ادمج» · «أطفئ وايكوف» · «ارجع» · وسؤالُ `TG_50606`.

## 17. DO-NOTS
لا بوّابةَ جديدة · لا وزنَ ترتيب · لا تغييرَ في `ENTRY_TRANCHES` أو اتّجاهها · لا هدفَ «‏100%» ثابتًا (`T-EXIT`) · لا سلّمَ 25% · لا عدَّ موجات · لا كاشفَ رأسٍ وكتفين · لا تعديلَ `method_sequence`/`trigger_state`/`bottom_test_state` · لا استيرادَ خارجيّ جديد (`requirements.txt` مقفول) · لا `try/except ImportError` في السويّة · لا إعادةَ تسمية للصور.

## 18. HONESTY LIMITS (تُكتب قبل الأرقام وتُنشَر معها)
- السطرُ **وصفٌ لا تنبّؤ**: كونُ السهم في «المرحلة 4» لم يُقَس أنه يرفع احتمالَ الانفجار — وكلُّ ما قِيس داخل أسهم الهويّة يقول إن الاختيارَ لا يميّز (‏§0-ز/ط/ي · «الحافّة = التوقيت»). **ممنوعٌ** أن يُقال في السطر «جاهزٌ للانفجار».
- أرقامُ 20%/5% **بلسان فيصل** لا مقيسةٌ عندنا؛ وأيُّ قياسٍ لاحق يلزمه تسجيلٌ مسبق.
- `method_sequence` تُقرأ على آخر `win` بارًا — سهمٌ دورتُه أطولُ من النافذة يُعطي `None` بصدق لا مرحلةً خاطئة.

## 19. OWNER DECISIONS (تُطلَب بعد التنفيذ لا قبله)
- «ادمج» / «ارجع» · «أطفئ وايكوف» (R-02) · جوابُ `TG_50606`.

## 20. FINAL ACCEPTANCE CRITERIA
1. `python3 test_bot.py` خروج 0 · صفرُ فشل · على الشجرة المدموجة نفسِها.
2. بصماتُ الجذور: 10 بت-بت · `scan_market`/`analyze_ticker` تختلفان بعقدة حارسٍ واحدة فقط (مُثبَت بـ`PC5`) · `pivot_stability` بت-بت.
3. أقفال `PC1`-`PC9` خضراء **وطفراتُ `p1`-`p9` تُسقط كلًّا بقفلها المسمّى** (‏9/9 · صفرُ انهيار).
4. صفّان في الدفتر بوسم `faisal_verbatim` · قفلُ الدفتر أخضر.
5. السطرُ يظهر حيًّا في الكرت واليوميّ وفحص اليد (لقطةٌ في التقرير) ويغيب حين لا حقل.
6. لا `LOGIC_VERSION` · لا نداءٌ شبكيٌّ جديد · لا استيرادٌ جديد.
7. `MEM1`/`HND1` تحت السقف · بندُ أرشيفٍ + صفُّ فهرسٍ + سطرُ هاندوف مُدرَجة بالأدوات لا باليد.
8. CI خضراء على الـPR قبل الدمج · squash · الفرعُ مُعادٌ من `origin/main` بعده.
