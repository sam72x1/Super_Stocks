# 🌙⛰️② `T-AHEXT-2` — إعادةُ التصميم بمجتمعٍ غيرِ منحاز

> **تسجيلٌ مسبقٌ ثانٍ، مدفوعٌ قبل أيّ سطرِ أداةٍ من النسخة الثانية وقبل أيّ رقمٍ
> منها.** أمرُ المالك «اعد التصميم» (‏2026-09-10) بعد أن أسقطت بوّابةُ `AX5`
> حكمَ التشغيلة الأولى (`ahext_result.md` · الفرعُ 3).

---

## ⓪ إقرارُ التلوّث — يُكتب أوّلًا لأنه يحدّ كلَّ ما بعده

🔴 **رأيتُ أرقامَ التشغيلة الأولى قبل كتابة هذا العقد.** وهذا ليس تفصيلًا
إجرائيًّا: التسجيلُ المسبق يستمدّ قوّتَه من الجهل بالنتيجة، **وأنا لستُ جاهلًا بها
الآن**. فأُعلن **بالضبط** ما رأيتُ حتى يُقرأ هذا العقدُ بحجمه الحقيقيّ:

| ما رأيتُه | القيمة |
|---|---|
| `AX1` الفرق (‏`FRESH − AH`) | **‏−3.5 نقطة** (‏1.67σ) — **بعكس** اتّجاه الدعوى |
| `E-AH` فوق 120% · `E-FRESH` فوق 120% | ‏22.64% · 19.14% |
| `AX3` مئينُ 90 لـ`E-AH` | ‏183.6% |
| `AX4` (مرجعُ الافتر) | ‏97.8% |
| `AX6` حصّةُ `E-AH` | ‏34.9% |
| `AX5` الفوات | ‏63.7% |

⚖️ **وعليه فهذي التجربةُ ‏«تأكيديّةٌ مشروطة» لا عمياء**، ويُقرأ حكمُها بهذا القيد
مهما كان اتّجاهُه. **ولا تُرقَّى إلى دليلٍ أوّليٍّ إلّا بسنةٍ لم تُقَس بعدُ في
أيّ نسخة** (‏§⑥ أدناه).

## ⓪-ب القاعدةُ التي تحكم ما يتغيّر وما لا يتغيّر

🔒 **يتغيّر شيءٌ واحدٌ فقط: المجتمع.** لأن العيبَ المقيس **في الغربال** لا في
المقاييس. وكلُّ ما عداه **يُنسَخ من العقد الأوّل بحرفه**:

| **لا يتغيّر** (مقفولٌ بـ`V-B1`) | **يتغيّر** |
|---|---|
| `CAP_HI = 120` · `AH_MIN = 20` · `PM_MIN = 20` | الغربالُ ⟶ **ثلاثُ طبقاتٍ وأوزان** |
| تعريفُ الذراعين ونافذتاهما ومرجعُهما | القصُّ ⟶ **معاينةٌ حتميّةٌ داخلَ كلّ طبقة** |
| `AX1` `AX2` `AX3` `AX4` `AX6` وصيغُها | الجلبُ ⟶ **متوازٍ** (كلفةٌ لا منهج) |
| نطاقُ السعر · السنواتُ الثلاث · شكلُ الفروع الثلاثة | `AX5` ⟶ **يسقط لأن موضوعَه زال** |

🔴 **وهذا القيدُ هو كلُّ ما يحمي التجربةَ من مطاردة النتيجة:** لو غيّرتُ `CAP_HI`
أو `AH_MIN` بعد أن رأيتُ ‏−3.5، لصار الاختيارُ نفسُه هو النتيجة. **فالمقاييسُ
مجمَّدةٌ بحرفها ولو خرج الحكمُ ضدّ الدعوى.**

---

## ① العيبُ الذي يُصلَح — بالبناء لا بالتخفيض

الغربالُ الأوّل يقرأ **فجوةَ الافتتاح** `open(D)/close(D-1)`، فيلتقط مَن **بقي**
مرتفعًا حتى 09:30 ويُسقط مَن **ذاب** قبلها — **وذوبانُ البري هو ما تصفه الدعوى
نفسُها**. ومقيسًا: **فواتُ ‏63.7%** [‏33-86% بفاصل ويلسون].

🔴 **وعيبٌ ثانٍ لم يذكره العقدُ الأوّل أصلًا:** أيّامُ **الفجوة السالبة**
(`gap < 0`) كانت **تُسقَط كلَّها** — طبقةٌ كاملةٌ غائبة.

**العلاجُ ليس خفضَ العتبة** (‏يُضخّم الكلفةَ ولا يُلغي الانحياز) **بل معاينةً
طبقيّةً موزونة**: يُقسَّم كونُ (رمز·يوم) إلى **ثلاث طبقاتٍ تستوعبه كلَّه**،
وتُعايَن كلُّ طبقةٍ حتميًّا، **ويُعاد لكلّ صفٍّ وزنُه** فيصير التقديرُ **غيرَ
منحازٍ بالبناء**.

| الطبقة | التعريف | المعاينة | الوزن |
|---|---|---|---|
| **`H`** | `gap ≥ 15%` | **إحصاءٌ شاملٌ** (سقف `H_CAP`) | `N_H / n_H` |
| **`L`** | `0 ≤ gap < 15%` | عيّنةٌ حتميّةٌ `L_N` | `N_L / n_L` |
| **`Z`** | `gap < 0` | عيّنةٌ حتميّةٌ `Z_N` | `N_Z / n_Z` |

⇒ **`H ⊎ L ⊎ Z` = كونُ (رمز·يوم) كلُّه داخلَ نطاق السعر** — فلا «فائتٌ» يُقدَّر،
ولذلك **يسقط `AX5` بزوال موضوعه** ويحلّ محلَّه حارسٌ من نوعٍ آخر (‏§③).

⚠️ **ويبقى قيدٌ يُقال:** الوزنُ يصحّح **الانحيازَ** ولا يخلق **دقّة** — طبقةٌ
عيّنتُها صغيرةٌ ووزنُها كبير تُدخل ضجيجًا. ولهذا `V-B4` أدناه.

## ①-ب التقديرُ الموزون — صيغُه مكتوبةٌ قبل أيّ رقم

لكلّ ذراعٍ (`E-AH` / `E-FRESH`) على صفوف المجتمع (‏`pm_peak ≥ PM_MIN`):

    النسبةُ فوق CAP_HI  =  Σ wᵢ·1[pm_peakᵢ > CAP_HI]  ÷  Σ wᵢ
    حجمُ العيّنة الفعّال =  (Σ wᵢ)²  ÷  Σ wᵢ²            ← كيش
    فاصلُ ويلسون ‏95%    يُحسَب على `n_eff` لا على العدد الخام
    المئينُ 90 (`AX3`)   مئينٌ **موزون**: أصغرُ قيمةٍ تراكمُ وزنِها ‏≥ 0.90 من الكلّ

**والفرقُ وخطؤه المعياريّ** بصيغة العقد الأوّل نفسِها، **بإحلال `n_eff` محلّ `n`**.

🔑 **ولماذا كيش لا العدد الخام:** صفٌّ وزنُه 45 ليس خمسةً وأربعين مشاهدة — وحسابُ
ويلسون على العدد الموزون **يُضيّق الفاصلَ كذبًا** فيصنع ثقةً لا سند لها. وهذا
**أخطرُ ما في التصميم الموزون**، ولذلك كُتب هنا قبل أيّ رقم.

## ①-ج الميزانيّةُ والتوازي

`H_CAP` · `L_N` · `Z_N` **مثبَّتةٌ في الأداة ومطبوعةٌ في كلّ تقرير**، وواحدةٌ
للذراعين بالبناء (‏درسُ `T-CLIFF`). والجلبُ **متوازٍ** بعددِ خيوطٍ مثبَّت —
**كلفةٌ لا منهج**: نفسُ النداءات ونفسُ الصفوف، والنتيجةُ **حتميّةٌ لأن الترتيب
والمعاينة لا يعتمدان على زمن الوصول** (‏`V-B5`).

---

## ② الذراعان والمقاييس — منسوخةٌ بحرفها

`E-AH` = `ah_rise ≥ 20%` · `E-FRESH` = دونها · و«لا شمعةَ افتر بعد جلبةٍ ناجحة»
= **صعودٌ صفر** = `E-FRESH` (ملحقُ العقد الأوّل §⑨-2، **يبقى**).
`AX1` `AX2` حاكمتان · `AX3` `AX4` `AX6` وصفيّة · **`AX5` يسقط**.

**ويُضاف وصفيٌّ واحدٌ لا يحكم:** **`AX7`** = المقاييسُ نفسُها **على طبقة `H`
وحدَها** — فتُقابَل بأرقام التشغيلة الأولى مباشرةً. 🔑 **وهو شاهدُ صحّةٍ لا
اكتشاف:** لو خالف `H` أرقامَ الأولى مخالفةً مادّيّةً فالخللُ في الأداة لا في
السوق، **ويُحقَّق قبل قراءة أيّ رقمٍ آخر**.

---

## ③ الحرّاسُ وحدُّ العيّنة

- **`V-B4` حدُّ الدقّة:** كلُّ ذراعٍ يلزمها `n_eff ≥ 150` — وسنةٌ لا تستوفيه في
  ذراعٍ **لا تدخل `AX1`/`AX2`**. (**والنطاقُ منصوصٌ في كلّ بندٍ يستعمله** — درسُ
  ثغرةِ `T-UNIVERSE` في عقدي.)
- **`V-B6` صحّةُ الوزن:** مجموعُ أوزان الطبقات يساوي حجمَ الكون بهامشٍ دون 1%،
  **ويُقرأ سلوكيًّا** لا بالثقة.
- **`V-B7` التغطية:** نسبةُ الجلب الناجح في **كلّ طبقةٍ على حدة** ‏≥80%، وطبقةٌ
  دونها ⇒ **لا حكم** (‏وليس «تُهمَل»: إهمالُها يُعيد الانحيازَ من الباب نفسِه).
- وحرّاسُ العقد الأوّل تُنسَخ كما هي: `V-A1`-`V-A8` (‏الإغلاقُ الرسميّ · المنطقةُ
  الزمنيّة · لا تسرّبَ بين الجلستين · صفرُ نظرٍ مستقبليّ · الذراعان مُفكَّكتان ·
  تسويةُ التقسيم · قراءةٌ فقط · اللقطةُ مثبَّتةٌ ومطبوعة).

## ④ قاعدةُ القراءة — الفروعُ الثلاثةُ بشكلها الأوّل

- **الفرعُ 1 — تثبت:** `AX1` موجبٌ في السنوات الثلاث وفاصلُه المجمَّع لا يلمس
  الصفر **و**`AX2` (‏≥2σ في سنتين) ⇒ **اقتراحٌ للمالك، بلا شحنٍ حتى يأمر**.
- **الفرعُ 2 — تفشل:** يسقط أحدُ المعيارين أو تنقلب الإشارةُ بين السنوات ⇒
  **يُغلَق المحورُ** بشرطِ فتحٍ مؤرَّخ.
- **الفرعُ 3 — لا حكم:** `V-B4` غيرُ مستوفًى في سنتين فأكثر · **أو** `V-B7` يسقط
  في أيّ طبقة · **أو** `V-B6` يسقط.

🔴 **وبندٌ يخصّ هذي النسخة وحدَها:** إن خرج الحكمُ **الفرعَ 1** فهو **مشروطٌ
بإقرار §⓪** — ويُكتب في النتيجة بهذي الصفة، **ولا يُقتبَس لاحقًا بلا قيده**.

## ⑤ تنبّؤاتي — مكتوبةٌ وأنا أعلم أرقامَ الأولى

- **`Q1`** — الفرقُ يبقى **سالبًا** (أي بعكس الدعوى) بعد التصحيح. *(أرجّحه لأنّي
  رأيتُ ‏−3.5؛ والتنبّؤُ هنا **ضعيفُ القيمة** بحكم §⓪ وأقولها.)*
- **`Q2`** — طبقتا `L`+`Z` **تحملان أكثرَ من نصف** المجتمع الموزون.
- **`Q3`** — نسبةُ «فوق 120%» في المجتمع المصحَّح **أقلُّ** منها في `H` وحدَها
  للذراعين معًا (‏الذائبون أضعفُ بطبيعتهم).
- **`Q4`** — `n_eff` للمجتمع المصحَّح **أقلُّ من ثُلث** عدد صفوفه الخام.
- **`Q5`** — `AX7` يُعيد أرقامَ التشغيلة الأولى بفارقٍ دون **نقطتين** لكلّ ذراع.

## ⑥ حدودُ صدقٍ مُعلَنةٌ قبل الأرقام

1. **التلوّثُ في §⓪ — أثقلُها**، ويحدّ الحكمَ مهما كان اتّجاهُه.
2. **الوزنُ يصحّح الانحيازَ لا الدقّة** — طبقةٌ صغيرةُ العيّنة كبيرةُ الوزن تُدخل
   ضجيجًا، و`n_eff` هو الذي يُظهره.
3. **المصدرُ تفريغٌ آليّ** ⇒ `faisal_adopted`، **والرقمان 100 و120 يُقاسان ولا
   يُشحنان عتبةً** (منسوخٌ من العقد الأوّل).
4. **انحيازُ البقاء قائم** (كونُ اللقطة) — درسُ `T-UNIVERSE`.
5. **«مجمل» غامضة** ⇒ قراءتان: الأولى تحكم والثانية تُطبَع.
6. **ولا شيءَ من هذي التجربة يمسّ الإنتاج** — لا عتبةَ ولا `LOGIC_VERSION`.

## ⑦ العواقب

| الفرع | ما يقع |
|---|---|
| 1 | اقتراحٌ للمالك **موسومًا بقيد §⓪** · ولا شحنَ حتى يأمر |
| 2 | **إغلاقُ المحور** بشرطِ فتحٍ مؤرَّخ: مصدرٌ ثانٍ للدعوى أو تعريفٌ مختلفٌ مسجَّل |
| 3 | **«لا حكم» بعدّاده** — ولا يُقرأ نفيًا ولا إثباتًا |

**والنتيجةُ تُكتب في `ahext2_result.md` بجانب هذا العقد لا فوقه.**
