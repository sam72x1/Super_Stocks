#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""🪝 صيّاد أسهم التقسيم (منهج فيصل) — أداة **مستقلة تمامًا** عن فارز الارتكاز (14 بوابة).

الفكرة (طلب المستخدم 2026-07-24): فكرة/نمط مختلف عن البوت الأساسي — تمسح ناسداك
**يوميًّا بعد الإغلاق** عن مقسّم عكسي يطابق setup فيصل **الصارم**، وإذا لقت سهمًا يطابق
كل الشروط الأساسية ترسله لتلغرام مباشرة.

🔴 **قراران للمالك (2026-07-31) ينسخان ما كان موثّقًا هنا — والنصّ القديم يبقى مذكورًا
عمدًا لئلّا يُعاد بحسن نيّة:**
① **«صامتة لو لا مطابق» أُلغيت.** صار يومُ «لا مطابق» يرسل «لا يوجد سهم يطابق الشروط
   اليوم» — لأن الصمت كان **مبهمًا**: لا يفرّق «لا مرشّح» عن «سقطت التشغيلة»، ووصولُ
   الرسالة نفسه صار **دليلَ حياة**. ⚠️ **ولا يمسّ شروط الترشيح**: الخمسة كما هي حرفيًّا
   ولا يخرج مرشّحٌ ناقص — التغيير في **الإبلاغ** لا في **الحكم**.
② **التسليم ~3 فجرًا بتوقيت السعودية** (بعد إغلاق الافتر): كرونان ‏00:13/01:13 UTC
   و`session_gate` تحسم أيّهما يعمل بالفصلين، ودِدوبٌ بطبقتين يمنع التكرار صيفًا.

الشروط الصارمة (كلها): مقسّم عكسي حديث + وصل قمة-ما-بعد-التقسيم÷2 + حافظ القاع 3
جلسات + فلوت<2مليون (ياهو) + خالٍ من رفعة قروب. السياق (عرض): المتاح<20ألف + الرسوم
(ChartExchange) + متوسطات 20/30/50 + تكرار التقسيم.

🔒 عرض/تنبيه فقط — لا تمسّ حالة البوت ولا الفرز ولا LOGIC_VERSION.
🔴 **تصريحُ تغيير (⓿-و، 2026-07-31): لم تعد «لا تحفظ شيئًا» حرفيًّا.** تكتب الآن ملفًّا
واحدًا هو **ختم آخر مسحٍ ناجح** (`split_hunter_stamp.json` — حقلٌ واحد: تاريخ الجلسة)
ويُدفع بالنمط المحمي (`git_save`) ⇒ الـworkflow يحتاج `permissions: contents: write`.
السبب: الصيّاد **بلا حارس سقوط** بينما التجديد الأسبوعي له `renewal_staleness`؛ ولو
أسقط GitHub تشغيلته لصمت صمتًا تامًّا، وصمتُه يُقرأ «لا مقسّم اليوم» وهو غلط. والختم
**لا يمسّ قرارًا واحدًا هنا**: لا دِدوب ولا كتم ولا ترشيح — يقرؤه المسار اليومي وحده.
التشغيل: python split_hunter.py (يلزم TELEGRAM_BOT_TOKEN/CHAT_ID · ياهو للفلوت)."""


MIN_COVERAGE_PCT = 60.0        # 🩺 أرضية تغطية المسح (أقلّ منها = لم نفحص السوق)
# 🌙 وسم الفاشل-الآمن المفتوح: يُرسَل التنبيه **ولا يُكتَم**، لكن بلا ثقةٍ كاذبة.
AH_UNVERIFIED_TAG = "⚠️ لم يُتحقّق من سعر الافتر"


def ah_guard(S, rows, session_date, fetch=None):
    """🌙⛔ **حارس الافتر (AH-GUARD)** — يمنع التنبيه **البائت**.

    العيب المُثبَت (2026-07-30): الصيّاد يعمل بعد إغلاق الافتر بالتصميم، لكن بياناته
    شمعة ياهو اليومية و**هي لا تشمل الافتر** ⇒ رشّح NUWE بعد أن انفجر ‏+100% في الافتر.

    القاعدة — **صفر عتبة مخترعة**: نعيد تطبيق شرط فيصل ② «لم يصعد»
    (`SPLIT_ROSE_MAX_PCT` كما هو) على **السعر الممتد** مقابل مرجعَي الصفّ نفسه:
    `price` (إغلاق الجلسة الذي بُني عليه الكرت كلّه: القاع÷2 · الهدف · الوقف) و`ref`
    (قمة ما بعد الحدث = الهدف +100%). كسرُ أيّهما ⇒ **يُكتَم** مع سطر سجلّ صريح.

    ⚠️ **لماذا المرجعان لا `ref` وحده** (تصريحٌ لا اجتهادٌ صامت): في NUWE نفسه
    ‏`ref ≈ 2×price`، والانفجار بلغ ‏~`ref` ⇒ ‏`ext/ref − 1 ≈ 0%` فلا يكتم شيئًا؛
    الذي يلتقط «+100% في الافتر» فعلًا هو ‏`ext/price − 1`. فأُبقي شرط `ref` كما نُصّ
    عليه **وأُضيف** مرجع `price` — والعتبة واحدة لم تُمسّ.

    🔒 **فاشل-آمن مفتوح (fail-open):** بلا مفتاح/تعذّر الجلب/صفر شموع/مرجع تالف ⇒
    الصفّ **يبقى** ويُوسَم في `unverified` (تعذّر ≠ صفر · لا كتم صامت ولا ثقة كاذبة).
    يرجع `(kept, unverified)`. `fetch(sym, date)` محقون للاختبار بلا شبكة."""
    kept, unverified = [], []
    try:
        th = float(S.CONFIG["SPLIT_ROSE_MAX_PCT"])
    except Exception:                                            # noqa: BLE001
        return list(rows or []), [str(r.get("symbol") or "") for r in (rows or [])]
    f = fetch if fetch is not None else S.extended_last_price
    for r in rows or []:
        sym = str(r.get("symbol") or "")
        try:
            ext = f(sym, session_date)
            ext = float(ext) if ext is not None else None
        except Exception:                                        # noqa: BLE001
            ext = None
        if ext is None or ext != ext or ext <= 0:                # ⚠️ NaN ليس None
            unverified.append(sym)
            kept.append(r)
            continue
        bases, broke = [], None
        for name, raw in (("إغلاق الجلسة", r.get("price")),
                          ("قمة ما بعد الحدث", r.get("ref"))):
            try:
                b = float(raw)
            except (TypeError, ValueError):
                continue
            if b != b or b <= 0:
                continue
            bases.append(name)
            rise = (ext / b - 1.0) * 100.0
            # نفس صيغة المِجَسّ حرفيًّا: القرار على الخام و«أكبر من» لا «أكبر أو يساوي»
            if broke is None and rise > th:
                broke = (name, b, rise)
        if not bases:                     # لا مرجع صالح ⇒ لم نتحقّق (لا نكتم بالظنّ)
            unverified.append(sym)
            kept.append(r)
            continue
        if broke:
            S.log(f"⛔ {sym}: شرط «لم يصعد» مكسور بالافتر (+{broke[2]:.0f}% عن "
                  f"{broke[0]} ${broke[1]:.2f} ← ${ext:.2f}) — تنبيه بائت أُلغي")
            continue
        kept.append(r)
    return kept, unverified


def session_gate(now_utc=None, close_hour=20):
    """⏰ **بوّابة التوقيت** (طلب المالك 2026-07-31: «الفرز بعد الافتر ~3 فجرًا بتوقيت
    السعودية»).

    ‏3 فجرًا بالسعودية = **00:00 UTC** — وهي **بالضبط** لحظة إغلاق الافتر **صيفًا**
    (‏20:00 ET = 00:00 UTC بـEDT)، **لكنها قبله بساعةٍ شتاءً** (‏EST ⇒ الإغلاق 01:00
    UTC). فكرونٌ واحد عند 00:13 كان سيُعيدنا **داخل الافتر شتاءً** — وهو **العيب
    نفسه** الذي أُصلح في 2026-07-27 (‏«22:17 UTC = 18:17 ET أثناء الافتر»).

    فالحلّ **كرونان** (‏00:13 و01:13 UTC) **وهذي البوّابة تحسم أيّهما يعمل**: تقرأ
    ساعة **نيويورك** فتصيب الفصلين ذاتيًّا بلا جدولٍ يدويّ.
      • ‏ET قبل ‏20:00 ⇒ الافتر **لم يُغلق** ⇒ `(False, None)` — لا مسح (شتاءً).
      • وإلّا ⇒ `(True, تاريخ الجلسة)` = **التاريخ النيويوركيّ الحاليّ** (‏20:00–23:59
        ET هي مساء يوم الجلسة نفسه).

    ‏`close_hour=20` **حدٌّ محافظ**: يوم الإغلاق المبكّر ينتهي افترُه ‏17:00 ET فيبقى
    ‏20:00 بعده بأمان. دالّة **نقيّة** (تُحقَن `now_utc` للاختبار)."""
    import datetime as _dt
    from zoneinfo import ZoneInfo as _Z
    now = now_utc or _dt.datetime.now(_dt.timezone.utc)
    if now.tzinfo is None:
        now = now.replace(tzinfo=_dt.timezone.utc)
    et = now.astimezone(_Z("America/New_York"))
    if et.hour < int(close_hour):
        return (False, None)
    return (True, et.date())


def _stamp(S, session_date) -> None:
    """🔔 **ختم آخر مسحٍ ناجح** (⓿-و) — يُكتب ويُدفع بالنمط المحمي.

    ⚠️ يُستدعى في **كلا** مساري النجاح (مطابق · «لا يوجد») فيوم «لا مطابق» تشغيلةٌ
    ناجحة تمامًا وهو بالضبط اليوم الذي يجب ألّا يُخلَط بالسقوط.
    🔴 **ونُقل ليكون بعد الإرسال (2026-07-31)** — كان قبله. السبب أن الختم صار **مرجع
    الدِدوب** بين الكرونين: لو خُتم قبل إرسالٍ فاشل لقرأه الكرون الثاني «سُلِّم» فضاعت
    رسالة الليلة. فصار عقدُه «فُحِص **وسُلِّم**»، ومسارُ رفض تلغرام (rc=1) **لا يختم**
    ⇒ الكرون الثاني يُعيد المحاولة، ولو فشل الاثنان نطق حارسُ التقادم.

    🔒 **فاشل-آمن مطلق:** أي عطلٍ في الختم أو الدفع **يُسجَّل ولا يُسقط شيئًا** —
    التنبيه الرابح لا يجوز أن يضيع من أجل حارس. وتحت الاختبار `git_save` نفسها
    خاملة (حارسها الموثّق) فلا git حقيقي."""
    try:
        if S.record_hunter_run(session_date):
            S.git_save([S.HUNTER_STAMP_FILE])
        else:
            S.log("⚠️ ختم الصيّاد لم يُكتب — سيقرأ التقرير اليومي تقادمًا (تحذير لا صمت).")
    except Exception as e:                                       # noqa: BLE001
        S.log(f"⚠️ ختم الصيّاد: {e}")


def _fail(S, why: str) -> int:
    """🚨 **عطل صريح لا صمت.** عقد الأداة أن الصمت محجوز لـ«لا مرشّح كامل اليوم»؛
    فأي مسار فشل يجب أن **يُبلَّغ** وإلا قرأه المالك «لا سهم مقسّم اليوم» وهو غلط.
    (هذا بعينه الناقل الموثّق في CLAUDE.md: «أخطر ناقل — تفويت بصمت».)
    يرجع 1 ⇒ الوظيفة **حمراء** في Actions أيضًا فلا يضيع العطل لو تعذّر تلغرام."""
    S.log(f"🪝 صيّاد المقسّم: {why}")
    try:
        S.send_telegram("🪝 <b>صيّاد أسهم التقسيم</b>\n\n"
                        f"⚠️ تعذّر المسح اليوم: {S.esc(why[:200])}\n"
                        "<i>الصمت اليوم عطل لا «لا مرشّح» — يُعاد غدًا آليًّا.</i>"
                        f"\n\n{S.FOOTER}")
    except Exception:                                            # noqa: BLE001
        pass
    return 1


def run(fetch_ext=None, now_utc=None):
    import Super_stock as S
    # ⏰ بوّابة التوقيت (كرونان: 00:13 صيفًا · 01:13 شتاءً — والبوّابة تحسم أيّهما).
    #    وتُتخطّى بالتشغيل اليدويّ (`workflow_dispatch`) فيبقى الفحص الفوريّ ممكنًا.
    _manual = __import__("os").environ.get("HUNTER_FORCE", "").strip() == "1"
    _open_ok, _sess_et = session_gate(now_utc)
    if not _open_ok and not _manual:
        S.log("⏰ صيّاد المقسّم: الافتر لم يُغلق بعد لهذي الجلسة — لا مسح "
              "(التشغيلة التالية تتكفّل به).")
        return 0
    # 🔁 **دِدوب الجلسة — طبقتان، ومرجعُهما واحد: ختمُ آخر جلسةٍ سُلِّمت.**
    #    الكرونان يقعان **كلاهما** بعد الإغلاق صيفًا (‏20:13 و21:13 ET) ⇒ لولا الدِدوب
    #    لوصلت الرسالة مرّتين كل ليلة سبعة أشهر. ولا يجوز حلُّها بكرونٍ واحد «مختار»:
    #    GitHub **يُسقط** تشغيلات كرون (موثّق) ⇒ الكرون الثاني شبكةُ أمانٍ واجبة.
    #      • **المسار السريع (قبل المسح)** — يوفّر تحميل السوق كاملًا: تاريخ نيويورك
    #        الآن مساوٍ للمختوم ⇒ هي التشغيلة الثانية لنفس المساء.
    #      • **والحاسم (بعد المسح، أدناه)** مرجعُه **جلسة البيانات** — وهو الذي يصيب
    #        يوم العطلة (البيانات لم تتقدّم فلا رسالة ثانية) حيث يخطئ السريع.
    #    ⚠️ `load_hunter_stamp` تُرجع **نصًّا أو None** (لا قاموسًا) — والمقارنة نصّية.
    _prev = None
    try:
        _prev = S.load_hunter_stamp()
    except Exception as e:                                       # noqa: BLE001
        S.log(f"⚠️ صيّاد المقسّم: تعذّر قراءة الختم ({e}) — نُكمل (فاشل-آمن).")
    if _sess_et is not None and not _manual and _prev == _sess_et.isoformat():
        S.log(f"🔁 صيّاد المقسّم: مساء {_sess_et} سُلِّم سلفًا — لا تكرار.")
        return 0
    # ⚠️ **كل مسارات الفشل كانت ترجع 0 بصمت** (تدقيق 2026-07-27): وظيفة خضراء بلا
    # رسالة = مطابقٌ تمامًا ليوم «لا مرشّح» ⇒ العطل غير مرئي. صارت كلها تمرّ بـ`_fail`.
    if S.yf is None:
        return _fail(S, "yfinance غير متاح على الرنر.")
    try:
        uni = S.get_universe()
    except Exception as e:                                       # noqa: BLE001
        return _fail(S, f"تعذّر جلب كون ناسداك ({e}).")
    if not uni:
        return _fail(S, "كون ناسداك رجع فارغًا (تعذّر تحميل قائمة الرموز).")
    try:
        hist = S.download_history(uni)
    except Exception as e:                                       # noqa: BLE001
        return _fail(S, f"تحميل البيانات فشل ({e}).")
    if not hist:
        return _fail(S, "لا بيانات مُحمَّلة إطلاقًا.")
    # 🩺 **حارس التغطية** — الناقل الأخطر: خنق ياهو يُنزل التغطية من ~3400 رمز إلى
    # بضع مئات، فيمشي المسح ويطبع «0 مطابق» ويصمت، والمالك يقرأ الصمت «لا سهم مقسّم».
    # الحقيقة أننا **لم نفحص السوق**. (نفس حارس المسار اليومي الموثّق بـCLAUDE.md.)
    cov = 100.0 * len(hist) / max(1, len(uni))
    if cov < MIN_COVERAGE_PCT:
        return _fail(S, f"تغطية ناقصة: {len(hist)} من {len(uni)} رمزًا ({cov:.0f}%) — "
                        f"أقل من الأرضية {MIN_COVERAGE_PCT:.0f}%، فلم يُفحَص السوق.")
    try:
        rows = S.scan_split_hunter(hist)
    except Exception as e:                                       # noqa: BLE001
        return _fail(S, f"انهار المسح ({e}).")
    # 🔎 **الرموز تُطبَع دائمًا** (إصلاح رصد 2026-07-30): كان السجلّ يقول «1 مطابق
    # كامل» بلا اسم، فيوم NUWE لم يكن في السجلّ ما يُقرأ لاحقًا لمعرفة مَن رُشِّح.
    _syms = " · ".join(str(r.get("symbol") or "?") for r in rows) or "—"
    S.log(f"🪝 صيّاد المقسّم: فحص {len(hist)} من {len(uni)} رمزًا ({cov:.0f}%) "
          f"→ {len(rows)} مطابق كامل: {_syms}")
    # 📅 تاريخ **الجلسة** من البيانات لا `date.today()` على الرنر: الكرون صار فجر UTC
    # فتاريخ اليوم هناك = **اليوم التالي** للجلسة (جلسة الجمعة تُوسَم سبتًا).
    # ⚠️ رُفِع فوق بوّابة «لا مطابق» (⓿-و): الختم يحتاجه في **كلا** المسارين، ويومُ
    # الصمت الشرعيّ هو بالذات ما يجب ألّا يُخلَط بسقوط التشغيلة. الحساب نفسه لم يتغيّر.
    try:
        sess = max(df.index[-1] for df in hist.values() if len(df)).date()
    except Exception:                                            # noqa: BLE001
        sess = None
    # 🔁 **الدِدوب الحاسم** (مرجعه جلسة البيانات لا ساعة الرنر): نفس الجلسة سُلِّمت
    #    سلفًا ⇒ لا رسالة ثانية. وهو الذي يصيب **يوم العطلة**: البيانات لم تتقدّم عن
    #    الجمعة، فلا يُرسَل «لا يوجد» عن يومٍ لم يتداول فيه السوق أصلًا.
    if sess is not None and not _manual and _prev == sess.isoformat():
        S.log(f"🔁 صيّاد المقسّم: جلسة {sess} سُلِّمت سلفًا — لا تكرار.")
        return 0
    # 🌙⛔ حارس الافتر: شمعة ياهو لا تشمل الافتر، والصيّاد يُسلّم **بعده** ⇒ نتحقّق
    # من السعر الممتد لـ**يوم الجلسة نفسه** ونكتم البائت. بلا يوم جلسة = لا تحقّق
    # (فاشل-آمن مفتوح: يُرسَل موسومًا، لا كتم بالظنّ).
    _n0, unverified = len(rows), []
    if rows:
        if sess is None:
            unverified = [str(r.get("symbol") or "") for r in rows]
        else:
            rows, unverified = ah_guard(S, rows, sess, fetch=fetch_ext)
    if not rows:
        # 🔔 **قرار المالك 2026-07-31 — ينسخ «عقد الصمت» السابق:** كان يومُ «لا مطابق»
        #    صامتًا تمامًا، والمالك طلب رسالةً صريحة. والسبب وجيه: الصمت كان **مبهمًا**
        #    (لا مرشّح؟ أم سقطت التشغيلة؟) — والآن **وصولُ الرسالة نفسه دليلُ حياة**.
        #    ⚠️ ولا يمسّ **شروط الترشيح**: لا يُرسَل مرشّحٌ ناقص، والخمسة كما هي.
        #    ⚠️ ويشمل مَن كتمهم حارس الافتر — وإلّا عاد الصمتُ من بابه، فيُصرَّح بعددهم.
        S.log(f"🪝 صيّاد المقسّم: لا مطابق قابل للإرسال اليوم (كتم الافتر {_n0}) — "
              "تُرسَل رسالة «لا يوجد».")
        _none = S._rtl_join([
            "🪝 <b>صيّاد أسهم التقسيم</b>", "",
            "لا يوجد سهم يطابق الشروط اليوم.",
        ] + ([f"🌙 وكُتِم {_n0} مطابقًا بعد أن تحرّك في الافتر (تنبيهٌ بائت لا يخرج)."]
             if _n0 else []) + [
            f"🩺 فُحِص {len(hist)} من {len(uni)} رمزًا ({cov:.0f}% تغطية)"
            + (f" · جلسة {S.esc(str(sess))}" if sess else ""),
        ]) + "\n\n" + S.FOOTER
        if not S.send_telegram(_none):
            S.log("⛔ صيّاد المقسّم: تلغرام رفض رسالة «لا يوجد».")
            return 1
        _stamp(S, sess)                 # 🔔 ⓿-و: «السوق فُحِص وسُلِّم» — تاريخٌ واحد
        return 0
    try:
        msg = S.build_split_hunter_alert(rows, today=sess) + (
            "\n" + S._rtl_join([
                f"{AH_UNVERIFIED_TAG} لـ: {S.esc(' · '.join(unverified))} — "
                "قد يكون تحرّك بعد الإغلاق ولم نتمكّن من قراءته."])
            if unverified else "") + "\n\n" + S.FOOTER
    except Exception as e:                                       # noqa: BLE001
        return _fail(S, f"تعذّر بناء التنبيه رغم وجود {len(rows)} مطابق ({e}).")
    # ⚠️ **نتيجة الإرسال كانت مُهمَلة** والسجلّ يطبع «✅ أُرسل» بلا شرط: رفضُ تلغرام
    # (400 على HTML) كان يُضيّع **المطابق** والوظيفة خضراء والسجلّ يكذب.
    if not S.send_telegram(msg):
        S.log("⛔ صيّاد المقسّم: تلغرام رفض التنبيه — المطابق لم يصل.")
        return 1
    S.log("✅ أُرسل تنبيه صيّاد المقسّم لتلغرام.")
    _stamp(S, sess)                     # 🔔 ⓿-و: «السوق فُحِص وسُلِّم» — تاريخٌ واحد
    return 0


if __name__ == "__main__":
    raise SystemExit(run())
