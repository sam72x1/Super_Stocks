# 🪝 T-HUNTER-SIX — التسجيل المسبق (يُكتب ويُدفع **قبل** أي تشغيل)

> **الحالة عند الكتابة: صفر رقم.** لم تُشغَّل الأداة على أي لقطة بعد. كل ما تحته
> تنبّؤات ومعايير **مثبَّتة قبل النظر** — فما خالفها بعد التشغيل يُكتب مخالفًا ولا
> يُعاد ضبط المعيار عليه (دستور الأقفال ⑦: «prereg يُدفع قبل الأرقام»).
>
> **العقد:** `OPUS_EXECUTION_PACKAGE.md` ④-د. **الأداة:** `hunter_six_check.py`.
> **التصنيف:** بحث/تشخيص فقط — صفر مسّ إنتاج · لا بوّابة · لا وزن · لا تنبيه.

---

## ① السؤال

صيّاد المقسّم (`scan_split_hunter`) هو **الرابح الحيّ الوحيد** (أصاب NUWE ‏+100%)
و**لم يُختبَر على الستة قطّ**. السؤال بالضبط:

> **عند لحظة فيصل لكلٍّ من الثمانية — أيُّ شرطٍ من شروط الصيّاد الستة كان يمرّ،
> وأيُّه يسقط، وبأي هامش؟**

وليس «هل نغيّر عتبة» — **سقف هذي التجربة أرقامٌ تُعرض على المالك**، لا تعديل.

## ② المُختبَر: شروط الصيّاد الستة كما هي في الإنتاج اليوم

| # | الشرط | الدالّة/العتبة الحيّة | ملاحظة |
|---|---|---|---|
| ⓿ | مُرشّح OHLCV: سعر ضمن [`SPLIT_RADAR_PRICE_MIN`=1.0 ، `SPLIT_RADAR_PRICE_MAX`=10.0] **و** كليف هبوط يوم ‏≥`SPLIT_CLIFF_PCT`=30% داخل `SPLIT_LOOKBACK_DAYS`=120 | داخل `scan_split_hunter` | بوّابة تكلفة، لكنها **تحجب** كأي شرط |
| ① | حدث مؤسِّس حديث (تقسيم عكسي ≤120 يومًا) | `_split_setup_probe` خطوة (1) | ⚠️ قناة «الطرح الجديد» (SEC) **معطَّلة** في هذا الفحص — انظر ⑥-حدّ 3 |
| ② | وصل القمة÷2: السعر داخل [`half`×0.70 ، `half`×1.25] | `near_bottom` | `half` = `_post_split_high`÷2 |
| ③ | حافظ القاع 3 جلسات داخل النطاق نفسه | `held_ok` | آخر ثلاثة إغلاقات |
| ④ | «لم يصعد بعد الحدث»: `ref/first_val−1` ‏≤`SPLIT_ROSE_MAX_PCT`=20% | `didnt_rise` | `first_val` = افتتاح يوم الحدث، **وارتدادٌ صامت للإغلاق** عند غيابه/NaN |
| ⑤ | فلوت ‏<`SPLIT_RADAR_FLOAT_MAX`=2,000,000 | `ff(sym)` = `_yahoo_float` **المتساهل** | **المجهول يُقصى صامتًا** (`flt is not None and …`) |
| ⑥ | خالٍ من رفعة قروب | `group_pump_scar(df).found` | العتبة `EXPLOSION_PCT`=50% + حجم ‏≥`VOL_SPIKE_MULT`=5× |

**ولا تُمَسّ أيٌّ من هذي القيم في هذي التجربة** — الأداة تقرأها من `CONFIG` وتطبع
الهامش، ولا تكتب فيها. (الاستثناء الوحيد المُعلَن: ذراع تشخيصية اختيارية تُعيد نداء
**نفس الدالّة** بنافذة عمرٍ موسَّعة لتفصل «سقط على العمر وحده» عن «سقط على غيره» —
تُطبع في عمود منفصل موسوم `عدّاد` ولا تُحتسب في الحكم. تُفعَّل بـ`HUNTER_SIX_COUNTER=1`.)

## ③ المقياس والحكم — مثبَّتان الآن

- **الحكم لكل يوم** = مُخرَج `scan_split_hunter` نفسها على `{sym: df_مقصوص}` بـ`today=D`
  (‏«مطابق كامل» = رجعت صفًّا). **الأداة لا تحكم بنفسها** — تشخيصها لكل شرط يُقارَن
  بالحكم المرجعيّ، وأي اختلاف يُطبع `⚠️ تعارض` بدل أن يُبتلع.
- **معيار نجاح الرمز:** ‏≥1 **يوم مطابقة كاملة** داخل النافذة **وقبل** يوم الانفجار.
- **مُخرَج التجربة** = جدول أيام لكل رمز + سطر خلاصة: أول يوم مطابقة (إن وُجد)،
  وإلا **الشرط/الشروط الساقطة وعدد الأيام التي سقط فيها كلٌّ منها**.
- ⛔ **لا يُشتقّ من هذي التجربة أي تعديل عتبة تلقائيًّا.** سقفها المُعلَن سلفًا:
  **أرقامٌ + اقتراحٌ للمالك** (البنود المعلَّقة في ④-هـ-1/2/3 قراراتُ مالك).

## ④ التنبّؤات القابلة للكسر (منقولة من الحزمة ④-د بأرقامها)

> كلٌّ منها **تنبّؤٌ مسبق**، ومكتوبٌ معه **ما الذي يكذّبه**. النتائج المخالفة تُعلَن
> مخالفةً بالنصّ (سُنّة `T-CMAG`/`T-SHORT`).

### P1 — DSY يسقط على **نافذة 120 يومًا وحدها**
- **الرقم:** التقسيم `2026-02-23`؛ لحظة فيصل ‏~`2026-07-01` ⇒ **128 يومًا > 120**
  ⇒ `_split_setup_probe` يرجع `None` في خطوته الأولى.
- **وبقية شروطه تمرّ** (من بطاقة فيصل `IMG_0291` نفسها): افتتاح 3.47 · أعلى 4.12
  ⇒ `half` = **2.06** والنطاق **[1.442 ، 2.575]** ودخول فيصل **1.85 داخله** ✅ ·
  `rose` = 4.12/3.47−1 = **+18.73%** مقابل حدّ 20 ⇒ **يمرّ بفارق 1.27 نقطة** ✅ ·
  الفلوت **1.25م** < 2م ✅.
- **ما يكذّبه:** (أ) أن يُطبَع عمر التقسيم يوم اللحظة **≤120** (خطأ في حسابي أو في
  تاريخ اللحظة) · (ب) أن يسقط DSY على شرطٍ آخر **أيضًا** في كل أيام النافذة (فلا
  يكون «‏120 وحدها») · (ج) أن يمرّ الشرط ① ثم يسقط على ②/③/④/⑤/⑥.
- **حدّ صدق مسبق:** «‏~07-01» تقديرٌ لليوم لا يقين؛ الأداة تطبع الأيام كلها فيُقرأ
  الحكم على **كل** يوم في النافذة لا على يومٍ واحد.

### P2 — SPRC يسقط على **هندسة النطاق من الأعلى** أو على مدخل الكليف — **لا** على الفلوت ولا الحداثة
- قاع تجميعه ‏4.18–5.99 يقع **فوق** سقف [÷2×1.25] ⇒ `near_bottom=False`
  (عكس درس HAO الذي اخترق النطاق **لتحت** ⇒ النطاق مضبوط على طرفٍ واحد).
- الفلوت **553,392** (مقيس في `company_cache`) < 2م ⇒ **الفلوت يمرّ**.
- **ما يكذّبه:** أن يُطبع `near_bottom=True` في أي يوم من النافذة، أو أن يكون
  عمود الفلوت ❌ (فيصير السقوط عليه هو الأول)، أو أن يسقط على ⑥ (القروب) **قبل** أن
  يصل النطاق أصلًا — وهذي الأخيرة **احتمال قائم صراحةً** (انفجار يونيو ‏+276% داخل
  نافذة 90 بارًا لـ`group_pump_scar`) ⇒ **إن سقط على ⑥ فالتنبّؤ P2 مكسور جزئيًّا**
  ويُكتب كذلك.

### P3 — EHGO يجتاز الشروط الستة، والسؤال الوحيد **مدخل الكليف**
- بطاقته (`TG_1822`) تستوفي نصّ الوصفة حرفيًّا؛ الفلوت **1.62م** (بطاقة فيصل) /
  **1,151,428** (`company_cache`) — **كلاهما < 2م** فالشرط ⑤ يمرّ بأي مصدر.
- **الشكّ الحقيقي:** هبوطه 2.80→1.30 **سلّميّ** — فهل فيه يومُ ‏−30% يستوفي
  `SPLIT_CLIFF_PCT`؟ **إن لم يكن ⇒ مدخل الكليف يحجب فئة «الهادئ» بكاملها**، وهي
  عين ما يصطاده الصيّاد.
- **ما يكذّبه:** أن يُطبع كليف ‏≤−30% في النافذة **ويُطابق** ⇒ EHGO كان سيُلتقط
  (وعندها السؤال ينقلب إلى «لماذا لم يُنبَّه حيًّا؟»)، أو أن يسقط على ⑤/⑥.

### P4 — ZCMD يسقط على **أرضية السعر $1** والفلوت المجهول
- قاعه **0.810** < `SPLIT_RADAR_PRICE_MIN`=1.0 ⇒ يسقط في المُرشّح ⓿ قبل أي مِجَسّ.
- وفلوته **غائب** عن `company_cache` وعن بطاقات فيصل ⇒ الشرط ⑤ يُقصيه صامتًا **حتى
  لو** مرّ السعر.
- **ما يكذّبه:** أن تكون كل أيام النافذة فوق $1 (فالأرضية لا تحجبه)، أو أن يُجاب
  الفلوت من مصدرٍ موثَّق.

### P5 — JZ يسقط على **شرط ⑤ (القروب)**
- «دخل قروب غبي رفع السهم من 2.50 ل 3.30» = **+32%**، وعتبة الكاشف 50% ⇒ **قد لا
  يراه أصلًا**؛ والمرجَّح أن يقتله حدثٌ آخر أكبر داخل نافذة 90 بارًا.
- وهندسته: يوم التغريدة **2.56** فوق سقف النطاق **2.49**، ويوم المسح **1.884**
  لمس ÷2=**1.99** وأغلق داخله.
- **ما يكذّبه:** `group_pump_scar` يرجع `found=False` في كل النافذة (فلا يكون ⑤
  هو القاتل)، أو أن يسقط على ⓿/①/② أولًا.
- ⚠️ **JZ هو البند المفقود من خريطة رفض الستة** — فأيّ رقمٍ يخرج له **معلومة جديدة**
  لا تأكيدُ سابق.

### P6 — AZI: **لا تنبّؤ** (لحظته غير موثَّقة عندي)
لم أجد في الحزمة ولا في الكاتالوج تاريخًا صريحًا للحظة AZI. ⇒ الأداة تُعلن ذلك
صراحةً وتمشي آخر نافذة من اللقطة **موسومةً «لحظة غير موثّقة»**. **ولا يُقرأ ناتجُها
حكمًا على AZI** حتى يُمرَّر تاريخُه بـ`HUNTER_SIX_MOMENTS`. (المتوقَّع وصفيًّا فقط:
عائلة B — دورة كاملة بلا شرط ÷2 — فسقوطه على ② مرجَّح.)

### P7 — JEM: يسقط، والمرجَّح على ② أو ⑥ لا على الفلوت
فلوته **93,759–94ألف** ≪ 2م فالشرط ⑤ يمرّ يقينًا؛ و«حافظ 3ج» كان **مصادفة توقيت**
باللقطة القديمة (مدوَّن 2026-07-24) ⇒ المرجَّح ③ أو ②.
**ما يكذّبه:** مطابقة كاملة في أي يوم (فيصير JEM ملتقَطًا، خلافًا لتوثيقنا).

### P8 — NUWE (شاهد الضبط الموجب) — **إن لم يطابق فالأداة نفسها متّهمة**
NUWE رشّحه الصيّاد حيًّا (تشغيلة `30422469629`) ثم حقّق +100%. ⇒ **يجب** أن تُخرِج
الأداة له **مطابقة كاملة** في يوم/أيام قبل الانفجار متى غطّته اللقطة.
**ما يكذّبه:** صفر مطابقة ⇒ **عيبٌ في الأداة أو في اللقطة، لا نتيجةٌ عن NUWE** —
ويُعلَن كذلك ولا تُفسَّر بقية الأرقام قبل حلّه (دستور ⑧: أثبت أن التجربة اشتغلت).

---

## ⑤ الأعمدة المُلزَمة لكل يوم (وإلا فالجدول ناقص)

`len(df)` مقابل `MIN_BARS`=120 · السعر والكليف مقابل حدودهما · **عمر التقسيم**
مقابل 120 · `ref`/`half`/**حدّا النطاق** و**بُعد السعر عن أقرب حدّ بالنسبة** ·
`rose_pct` مقابل 20 · **فرع المرجع** (افتتاح يوم الحدث ‏/ ارتداد صامت للإغلاق ‏/
لا قاعدة) · **مصدر الفلوت** (`floatShares` ‏/ `sharesOutstanding` ‏/ `company_cache` ‏/
بطاقة فيصل ‏/ مجهول) · `group_pump_scar` كاملًا (`found`/`jump_pct`/`n_pumps`/
`bars_ago`/`broke_support`) · **الحكم المرجعيّ** من `scan_split_hunter` · **علم
التعارض**.

## ⑥ حدود الصدق — تُعلَن الآن لا بعد النتيجة

1. **الفلوت ليس point-in-time.** لا مصدر تاريخيّ للفلوت عندنا؛ قيمُ البطاقات
   مؤرَّخة بلحظة فيصل (فهي الأقرب)، وقيم `company_cache` **اليوم**. ⇒ عمود الفلوت
   **يُقرأ استرشاديًّا** ويُطبع مصدرُه دائمًا. (وهذا نفسه صنف «افتراضات المصدر
   والتوقيت» الذي فتحته المصيبة 8.)
2. **الأسعار في اللقطة معدَّلة بالتقسيم** كما يعطيها المزوّد؛ وتقسيمٌ يقع **بعد**
   as-of اللقطة يعيد تسوية الأرقام رجعيًّا ⇒ **لقطةٌ أحدث قد تغيّر أرقام رمزٍ ما**،
   ولذلك رقم اللقطة يُطبع في كل تشغيلة ويُذكر مع أي رقم يُقتبَس منها.
3. **قناة «الطرح الجديد» معطَّلة** (نداء SEC شبكيّ، والبيئة محجوبة) ⇒ الحدث
   المؤسِّس في هذا الفحص = **التقسيم العكسي فقط**. رمزٌ حدثُه طرحٌ جديد سيُقرأ
   «بلا حدث مؤسِّس» — **حدّ مُعلَن لا نتيجة**.
4. **الاقتراض/المتاح (CE) خارج الحكم** أصلًا في الصيّاد (سياق عرض) ⇒ غيابه لا يغيّر
   مطابقةً.
5. **الشمعة اليومية لا تشمل الافتر** (المصيبة 8) ⇒ يومُ مطابقةٍ يقع انفجارُه في
   الافتر يُقرأ هنا بإغلاقه الرسميّ. كل حكم «قبل الانفجار» يُقرأ بهذا القيد.
6. **تغطية اللقطة قيدٌ صريح:** رمزٌ لحظتُه بعد `as-of` **يُعلَن خارج التغطية**
   ولا يُعطى صفرًا صامتًا. لقطة `29935819997` (‏as-of 2026-07-22) تغطّي
   EHGO 05-22 · DSY ~07-01 · ZCMD 07-02 · SPRC 07-16 — و**لا** تغطّي JZ 07-27+
   ولا SPRC 07-28 ولا NUWE 07-29 ⇒ **تلزم لقطة تشمل ≥07-30** لاكتمال التجربة.

## ⑦ ما لا تفعله هذي التجربة (سقفٌ مثبَّت)

لا تعدّل عتبة · لا تفتح بوّابة · لا ترسل تلغرام · لا تكتب حالة · لا تلمس
`Super_stock.py` ولا `test_bot.py` · ولا تُحوّل أي رقم إلى قرار. **مُخرَجها سجلٌّ
مقروء ‏+ CSV اختياري، والقرار للمالك.**
