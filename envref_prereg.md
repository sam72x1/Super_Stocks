# 🧭 T-ENVREF — تسجيل مسبق: «الظرف الصادق» (مسطرة + نطاق بنفس الأساس)

> **مدفوع قبل أيّ رقم — بما فيه رقم النطاق الصادق نفسه.** أمر المالك 2026-08-08:
> «**1 نفذها**» بعد حكم T-M2REF (`m2ref_result.md` §④): حدّا M2 [89.6، 99.7]
> قيسا من الكاتالوج **على بيانات معدَّلة**، فتغيير المسطرة وحدها (R1) = خلط
> مقياسين — وأسهم فيصل نفسها (AZI/NUWE/TDIC) تسقط تحت الأرضية بالمسطرة الصادقة.

## ① السؤال المسجَّل

إذا قيس **نطاق M2 نفسه** من كتالوج فيصل بالمرجع الصادق (قمة ما بعد آخر تقسيم
عكسي — نفس مسطرة R1)، ثم شُغّل الفارز بالمسطرة الصادقة **والنطاق الصادق معًا**
(ذراع R2)، فهل يتفوّق على الإنتاج (R0) في المنفجرين المُسلَّمين دون كسر حارس
العائد؟

## ② القياس — صفر منهجية جديدة

- **الأداة:** `catalog_envelope.py` **نفسه بلا أي تغيير في المِرساة/النافذة/
  التجميع** — يُضاف حقن مشروط بعلم `ENV_SPLIT_REF=1`: سياق تقسيمات اللقطة يُمرَّر
  لكل رمز و`BT_SPLIT_REF_M2` يُرفع ⇒ `drop_pct` يخرج من `analyze_ticker`
  **الإنتاجي بنفس مسار كود الذراع R1 حرفيًّا**. العلم مطفأ ⇒ بت-بت (مقفول).
- **اللقطة:** نفس لقطة الظرف الإنتاجي `31070013412` (asof 2026-08-06).
- **المُخرَج:** حدّا M2 الصادقان = `drop_pct` من ظرف **P90** + «المثالي» من
  الوسيط P50 — **نقرأ مفتاح `drop_pct` وحده**؛ بقية المعايير لا تُمَسّ
  (قياسها غير متأثر بالمرجع).
- **بوّابة صلاحية الأداة (V-T):** تشغيلة بالعلم **مطفأً** على نفس اللقطة يجب أن
  تعيد أرقام الظرف الإنتاجي **بت-بت** (`drop_pct` = [89.588…، 99.7218…] والوسيط
  96.917…) — تسقط ⇒ الأداة تغيّرت لا المسطرة ⇒ لا حكم.
- الملف الإنتاجي `envelope_p90.json` في المستودع **لا يُمَسّ** (الـworkflow
  الجديد لا يدفع شيئًا — القراءة من السجل).

## ③ الذراعان (ولا ثالثة)

| ذراع | الوصف |
|---|---|
| **R0** | الإنتاج كما هو (بوّابة الصلاحية V1: يعيد d50=22/14/6 بت-بت) |
| **R2** | `BT_SPLIT_REF_M2=1` **+** حدود M2 الثلاثة من الظرف الصادق (`MIN_DROP_FLOOR`/`MAX_DROP_PCT` من P90 · `MIN_DROP_PCT` من الوسيط) تُطبَّق في الطفل بعد الاستيراد **وتُطبَع قبل/بعد** (V4) |

نفس آلة T-M2REF حرفيًّا (لقطات PIT الثلاث · سعة 10 · `rank_live` · بلا free_of ·
عمليات معزولة) — عبر `m2ref_arms.py` مُوسَّعًا: وجود `M2REF_BAND` ⇒ الذراعان
R0+R2؛ غيابه ⇒ السلوك القديم R0+R1 (توافق خلفي مقفول).

## ④ المقاييس وقاعدة القرار (تُقفَل الآن — نفس T-M2REF)

- الأساسي d50 لكل سنة + المجمَّع · حارس العائد ≥ −0.05R · d100 وصفي.
- بوّابات: V1 (R0 بت-بت 22/14/6) · V2 («العلم فعّال» N>0 في R2) · V4 (الحدود
  الصادقة مطبَّقة ومطبوعة) · V-T (أعلاه).
- **يُقترح الاعتماد** فقط إذا: d50(R2) ≥ d50(R0) في كل سنة **و**المجمَّع أكبر
  بأكثر من 1 **و**الحارس مستوفى **و**البوّابات خضراء. المجمَّع ∈ {−1,0,+1} ⇒
  «لا فرق مادي». أي سنة أسوأ ⇒ لا اعتماد. **أي اعتماد = قرار مالك** (عضوية ⇒
  `LOGIC_VERSION` + `force_renew`) — التجربة نفسها صفر مسّ إنتاج.

## ⑤ تنبّؤات قابلة للكسر (قبل أي رقم)

- **E1:** الأرضية الصادقة **أدنى بكثير** من 89.6 (جيش shifted_low الثمانون +
  سقوط AZI/NUWE يقتضيانه) — تقديرًا دون 80%.
- **E2:** R2 يسترجع معظم الرموز التي أزالها R1 خطأً (خلط المقياسين) ⇒ إشاراته
  بين R0 وR1.
- **E3:** التسليم: لا قفزة — عنق الزجاجة (المُرتِّب/السعة) قائم؛ النجاح =
  d50 ≥ R0 مع R أفضل.
- **E4:** حارس العائد يبقى موجبًا (الضجيج المزال أكثر من المسترجَع).

## ⑥ حدود الصدق

نافذة القياس 20 جلسة قبل الانفجار (عقد الظرف كما هو) · n=25 رمزًا · 2026 جزئية
· الاتجاه لا p-values · المرجع الصادق رهن سجل تقسيمات اللقطة · «الظرف يتمدد
بطبعه» — النطاق الصادق قد يكون أوسع فيدخل ضجيجًا جديدًا: d50 والحارس هما الحكم.

---
*التشغيل: `envelope_ref.yml` (قياس النطاق، تمريرتان: علم مطفأ للصلاحية ثم
مرفوع) ثم `m2ref.yml` بمدخل `band` للسنوات الثلاث. النتيجة: `envref_result.md`.*
