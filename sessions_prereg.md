# 🕐🪟 `T-SESSIONS` — تسجيلٌ مسبق: **«‏3 جلسات · العصر 7-8 يحقّق هدفًا ويهبط · ‏9 مساءً يقفز»**

> **أمرُ المالك (2026-09-20): «سجل التسوية و نفذ جميع النواقص معك الصلاحية
> كاملة تنفذ كل شي لا تجيني إلا وانت مخلص كل شي».**
> **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · ولا يُشحَن شيءٌ
> مهما كانت النتيجة.** مدفوعٌ ومدموجٌ **قبل أيّ سطرِ أداةٍ وقبل أيّ رقم**.
> 🔴 **وهذي الذراعُ الوصفيّةُ وحدَها** — والحاكمةُ (شموعُ دقيقةٍ لآلاف الأيّام)
> **لا تُشغَّل إلّا بأمرٍ مستقلٍّ من المالك بعد قراءة هذي.**

---

## §⓪ الدعوى ومصدرُها — حرفيًّا لا مُستنتَجًا

**الكاتالوج `L316`** (‏القسم «ثالثًا: دفعة إضافية 8051‑8188» · وسمُ الجرد
`partial`):
> «**‏3 جلسات · بالماركت يطلع العصر 7‑8 يحقق هدف ويهبط · 8:30‑9 جلسة لندن ·
> تجاوز جلسة نيويورك غالبًا يدبل (80%) · اطلع بالجلسة الأولى عند التوزيع**»

الوسم **`faisal_verbatim`** للنصّ · **والتحويلُ إلى ساعات نيويورك
`faisal_inferred`** ومكتوبٌ هنا صراحةً كي لا يُقرأ لاحقًا وكأنه نصُّ فيصل:
‏7-8 مساءً بتوقيت السعودية = **‏12:00-13:00 ET** · و‏9 مساءً = **‏14:00 ET**.

🔴 **وحدُّ صدقٍ في المصدر نفسِه يُقال أوّلًا:** عبارةُ «**‏8:30-9 جلسة لندن**»
**لا تتّسق مع ساعات لندن** بأيّ تحويل (لندن تغلق ‏11:30 ET) ⇒ **تُطبَع كما هي
ولا تُفسَّر ولا يُبنى عليها دلو**. ووسمُ الجرد `partial` **يعني أن الاقتباسَ
نفسَه مجتزأ** ⇒ **أضعفُ مصادر الحزمة سندًا، ويُقال قبل أيّ رقم.**

### والمحورُ جديدٌ بالبحث لا بالتسمية
`grep -c "T-SESSIONS"` = **‏1 · 1 · 1** في `DECISIONS_ARCHIVE.md`
و`HANDOFF_ARCHIVE.md` و`CLAUDE.md`، وثلاثتُها **وصفُه مقترَحًا** في حزمة
2026-09-18 ⇒ **صفرُ حكمٍ منشور**.

🔒 **وهو خارجَ `T-PMGATE` المُغلَق — ويُثبَت لا يُدَّعى:** ذاك قاس نافذةَ
**‏04:30 نيويورك** (فتحُ البريماركت ‏+ 30 دقيقة، من التفريغ الصوتيّ) وأُغلق
بخروج ‏8؛ **وهذا نوافذُ الجلسة النظاميّة (‏09:30-16:00 ET)** ⇒ لا تقاطعَ في
الساعة ولا في المجتمع ولا في المصدر. **ونافذةُ `T-PMGATE` لا تُقاس هنا ولا
تُعاد**: تُذكَر في هذي الفقرة **للتمييز وحدَه**، ولا تظهر في `§②` (التعريف)
ولا في `§③` (الأذرع) ولا في `§④` (المعايير) — **و`SSK1` يقفل ذلك موضعيًّا**
(لا بغياب النصّ من الوثيقة كلِّها، فذاك ادّعاءٌ يكذّب نفسَه: الفقرةُ تذكره).

---

## §① المجتمع — **الموجودُ وحدَه، وصفرُ جلب**

صفوفُ `opcurve_rows.tsv` (‏**‏481** مِرساة · `reg` **‏228** · `pre` 161 ·
`after` 92) — مُخرَجُ `T-OPCURVE` المنشور — **مضمومةً إلى لحظةِ المِرساة
`anchor_ms`** المُشتقّة من **تاريخ git لـ`op_entry_state.json`** بالآلة
القائمة `liq_trig_read.py` (تمشيه سلفًا وحتميًّا ورجعيًّا).

⇒ **صفرُ نداءٍ للمزوّد** — وهو شرطُ كون هذي الذراع «الأرخص».

🔴 **و`reg` وحدَه** (‏228 صفًّا): `pre`/`after` خارج الجلسة النظاميّة فدلاءُ
الساعة لا تنطبق عليها — **تُطبَع عددًا ولا تدخل أيّ حساب**.

### 🔴🔴 وقياسُ جدوى سبق التسجيل — والنتيجةُ تُعيد تصميمَ العقد قبل كتابته

**قِيس قبل أيّ سطرِ أداةٍ وقبل أيّ توزيع** (‏أعدادٌ فقط · صفرُ `Δ` · صفرُ
حصّةِ دلو):
- **`op_entry_state.json` في `git`: ‏15,707 لقطةً بمدى ‏2026-09-04 ⟶ 09-19**
  — **ولا شيءَ قبل 09-04** (الحالةُ لم تكن تُدفَع).
- **`opcurve_rows.tsv` بمدى ‏2026-08-18 ⟶ 09-15** · و`reg` = 228
  (‏**‏123 في أغسطس** و105 في سبتمبر).
- ⇒ **التقاطعُ ‏71 صفًّا فقط** (‏70 منها بـ`t_peak`) — أي **‏31.1%** من `reg`.

⚖️ **والأثرُ على التصميم يُكتَب الآن لا بعد الرقم:** أرضيّةُ **‏150** التي
تقتضيها قاعدتُنا **غيرُ قابلةٍ للبلوغ بنيويًّا** بهذي المصادر ⇒ **الفرعُ 1
«ظاهرةٌ مقيسة» مستحيلٌ في هذي الذراع بالبناء**، ولا يُخفَّض الحدُّ لتعبر
(«معايرةٌ بعد رؤية الرقم» — والمقيسُ هنا **حجمُ العيّنة لا نتيجتُها**).
⇒ **تُعاد صياغةُ `§④`/`§⑤`: هذي ذراعٌ وصفيّةٌ لا تحكم**، تُنشَر أرقامُها
ومعها ضبطاها، **والحكمُ يبقى للذراع الحاكمة بأمرٍ مستقلّ**.

---

## §② التعريفُ المُقاس — واحدٌ ومكتوبٌ بحرفه

لكلّ صفِّ `reg` له `anchor_ms` و`t_peak` (دقائقُ القمّة **بعد** المِرساة):

1. **`peak_et`** = ساعةُ نيويورك للحظة `anchor_ms + t_peak` (بتوقيتٍ صيفيٍّ/
   شتويٍّ صحيحٍ من `market_calendar` القائمة — **لا إزاحةٌ ثابتة**).
2. **الدلاء الأربعة** بنصّ الحزمة: **‏{09:30-12 · 12-13 · 13-14 · 14-16}**
   — وحصّتُها الزمنيّةُ من الجلسة (‏6.5 ساعات) **‏38.46% · 15.38% · 15.38% ·
   30.77%** على الترتيب، **تُحسَب لا تُكتَب بيدٍ**.
3. **`held_and_jumped`** = المِرساةُ **لم تكسر قاعَها حتى ‏14:00 ET** ثمّ
   بلغت **‏+10%** بعده — المكوّنان من `t_stop` و`t_hit10` الموجودَين في
   الملفّ، **بلا أيّ حسابٍ جديد**.

🔴 **وحدُّ صدقٍ في قلب التعريف:** `t_peak` قمّةُ **نافذةِ `T-OPCURVE` (‏120
دقيقة)** لا قمّةَ اليوم ⇒ **قمّةُ نافذةٍ لا قمّةُ جلسة**، وهو **نقلُ تعريفٍ
مُعلَن**. وأثرُه معروفُ الاتّجاه: **يحصر القممَ قرب المِرساة** فيَنحاز إلى
دلو ساعتِها — **ولذلك `C-SHUF` حاكمٌ لا وصفيّ** (أدناه).

---

## §③ الأذرع والضبطان

| الذراع | التعريف |
|---|---|
| **`S-OBS`** 🥇 | التوزيعُ المرصود لـ`peak_et` على الدلاء الأربعة |
| **`C-TIME`** 🥇 | **ضبطٌ حاكم** — الحصّةُ **الزمنيّة** لكلّ دلو (لا مرصودة) |
| **`C-SHUF`** 🥇 | **ضبطٌ حاكم** — خلطُ ساعات المراسي **عشوائيًّا حتميًّا** ‏999 سحبةً وإعادةُ الحساب: هل يتقدّم دلوٌ بالصدفة؟ |
| `S-HOLD` | `held_and_jumped` نسبةً — **وصفيٌّ يُطبَع ولا يحكم** |

⚠️ **والضبطان حاكمان لا وصفيّان — بدرسِ `T-PMGATE` بحرفه:** أيُّ تقسيمٍ
لليوم إلى دلاءَ **يُعطي دلوًا متقدّمًا آليًّا** إن كانت المراسي نفسُها
مُكتَلةً في ساعةٍ بعينها ⇒ السؤالُ النافعُ الوحيد: **هل يتقدّم دلو ‏12-13
على حصّته الزمنيّة وعلى الخلطِ معًا؟**

---

## §④ ما يُقاس ويُنشَر — **أرقامٌ وصفيّةٌ لا معاييرُ نجاح**

بعد جدوى `§①`، **لا معيارَ نجاحٍ في هذي الذراع** — وهذي الثلاثةُ **تُحسَب
وتُنشَر كاملةً** ولا يُبنى عليها حكم:

- **`SS1`** حصّةُ دلو **‏12-13** من القمم **مقابلَ حصّتِه الزمنيّة**
  (‏15.38%) ومعها **فاصلُ Wilson 95%** — **ويُنشَر سواءٌ عبر ‏1.5× أم لا.**
- **`SS2`** موضعُ الحصّة المرصودة من **توزيع `C-SHUF`** (الشريحةُ المئويّة).
- **`SS3`** «يدبل ‏80%»: نسبةُ `held_and_jumped` بين `reg` المقاسة.
  **ولا يُعَدّ معيارًا** (‏«يدبل» = ‏+100% والنافذةُ ‏120 دقيقةً فلا تحتمله)
  — **يُطبَع ليُكذَّب أو يُؤكَّد لا ليُحسَم به**.

🔒 **وكلُّ رقمٍ يُطبَع مقرونًا بمقامه** (‏`n` لكلّ دلو) — فلا نسبةَ بلا عدد.

## §⑤ فروعُ الحكم — **ثلاثةٌ، والأوّلُ مُغلَقٌ بالبناء**
1. **الفرعُ 1 — «ظاهرةٌ مقيسة»: ⛔ غيرُ متاحٍ في هذي الذراع** — العيّنةُ
   المتاحةُ **‏71** والأرضيّةُ المُلزِمة **‏150** (‏`§①`) ⇒ **لا يُدَّعى
   إثباتٌ مهما بلغت الأرقام**، ولا يُخفَّض الحدُّ لتعبر.
2. **الفرعُ 2 — «لا ظاهرة»:** إن كانت حصّةُ ‏12-13 **دون** حصّته الزمنيّة
   **أو دون الشريحة 50 من `C-SHUF`** ⇒ **إشارةٌ سالبةٌ تُنشَر**، و**لا
   تُقترَح الذراعُ الحاكمة** (فلا يُنفَق ‏≈1,900 يومَ جلبٍ على اتّجاهٍ سالب).
3. **الفرعُ 3 — «لا قياس»:** ضمُّ `anchor_ms` نجح لأقلَّ من **‏50 صفًّا** ·
   أو سقوطُ حارس — **يُعلَن بعدده ولا يُفسَّر**.

⚖️ **والمُخرَجُ النافعُ الوحيدُ لهذي الذراع: قرارٌ للمالك** — أتُشغَّل
الحاكمةُ (كلفةٌ عالية) أم يُغلَق المحور؟ **وهو ما تُجيبه الأرقامُ الوصفيّةُ
ولو لم تُثبت شيئًا.**

---

## §⑥ حرّاسٌ تُطبَع في كلّ تقرير — والسقوطُ يوقف

- **`V-S1`** المجتمعُ **عينُ `opcurve_rows.tsv`**: ‏481 صفًّا و`reg` = 228
  **بت-بت** — وإلّا **خروج 5**.
- **`V-S2`** نسبةُ الصفوف التي وُجد لها `anchor_ms` **تُطبَع**، ودون ‏90%
  ⇒ **الفرعُ 3**.
- **`V-S3`** حصصُ الدلاء الزمنيّةُ **تُحسَب من حدودها** ومجموعُها ‏100.0%
  **بهامشِ ‏0.01** — وإلّا **خروج 5** (‏فرقٌ يعني تعريفَ دلوٍ مكسورًا).
- **`V-S4`** التوقيتُ الصيفيُّ/الشتويّ **من `market_calendar`** لا بإزاحةٍ
  ثابتة، **ويُطبَع عددُ الصفوف في كلّ نظامٍ** — فلو كان أحدُهما صفرًا فالضبطُ
  منحلّ ويُقال.
- **`V-S5`** `C-SHUF` **حتميٌّ ببذرةٍ ثابتةٍ تُطبَع**، وإعادتُه تعطي الرقمَ
  نفسَه.
- **`V-S6`** الأداةُ **لا ترسل ولا تكتب حالةً** (‏`selfcheck_readonly`
  المُشدَّد) · **وبلا سرِّ تلغرام في الـworkflow**.
- **`V-S7`** الجذورُ الاثنا عشر **بصمتُها مطابقة** · **وصفرُ نداءٍ للمزوّد**
  (‏يُقفَل بالـAST: لا `requests` ولا `polygon_*` ولا `download_history`).

---

## §⑦ ما لا يُمَسّ بهذي التجربة

- ⛔ **الجذورُ الاثنا عشر** · **و`pivot_stability` لا تُمَسّ** · ولا عتبةُ
  توقيتٍ في الإنتاج تتحرّك · ولا `CONFIG` تُضبَط.
- ⛔ **لا `LOGIC_VERSION`** · **ولا شحنَ حقلٍ أو مفتاحٍ أو سطرِ عرض** مهما
  كانت النتيجة — **والفرعُ 1 عرضٌ على المالك لا تنفيذ**.
- ⛔ **ولا تُعايَر حدودُ الدلاء ولا ‏1.5× ولا ‏14:00 بعد رؤية أيّ رقم.**
- ⛔ **ولا تُشغَّل الذراعُ الحاكمة** (شموعُ دقيقةٍ لمجتمع `T-LINK100`) **بلا
  أمرٍ مستقل** — كلفتُها ‏≈1,900 يومَ حدثٍ × نداء.

---

## §⑧ حدودُ صدقٍ تُقال قبل النتيجة

1. **المصدرُ `partial` وجملةُ «لندن» غيرُ متّسقة** ⇒ **أضعفُ سندٍ في الحزمة**.
2. **`t_peak` قمّةُ نافذةِ ‏120 دقيقة لا قمّةُ جلسة** (‏§②) ⇒ انحيازٌ
   **معروفُ الاتّجاه** نحو دلو ساعةِ المِرساة، و`C-SHUF` هو ما يعزله.
3. **حقبةٌ واحدةٌ حيّة** (‏08-18 ⟶ 09-15) ⇒ **سنةٌ واحدة** ⇒ **وصفيّةٌ بنصّ
   قاعدة «ثلاثُ سنوات»**، ولا تُقرأ حكمًا على السوق.
   🔴 **وأضيقُ منها فعليًّا: ‏09-04 ⟶ 09-15 (‏71 صفًّا)** لأن مصدرَ
   `anchor_ms` لا يمتدّ قبل 09-04 (‏`§①`) ⇒ **أسبوعان لا شهر.**
4. **سجلُّ محاور التوقيت سالبٌ بامتياز:** `T-PMGATE` «لا حكم» ثمّ أُغلق ·
   `T-PRESESSION` فشلت **مرّتين** · `T-QUARTER` «لا يفصل» · `T-WAKE` صفرٌ من
   ثلاث ⇒ **الاحتمالُ الأسبقُ الفشل**.
5. **والمنحنى الأملسُ هو النمطُ المرصود مرّتين** (`T-PMGATE` عبر أربع
   بوّابات · و`T-OPCURVE` عبر الفجوة) ⇒ **تنبّؤي أدناه ليس حياديًّا.**

## §⑨ تنبّؤاتي — تُنشَر مكذَّبةً أو مؤكَّدة

| # | التنبّؤ | ثقتي |
|---|---|---|
| `SP1` | **`SS1` يسقط** — التوزيعُ أملسُ بلا دلوٍ مميَّز | **عالية** |
| `SP2` | دلو **‏09:30-12** يتقدّم (لا ‏12-13) لأن المراسي نفسَها مُكتَلةٌ صباحًا | متوسّطة |
| `SP3` | `C-SHUF` **يُفسّر أكثرَ الفرق** إن ظهر فرقٌ | متوسّطة |
| `SP4` | «يدبل ‏80%» **لا يتحقّق بأيّ تعريف** — و`held_and_jumped` دون ‏25% | **عالية** |
| `SP5` | ضمُّ `anchor_ms` ينجح لأكثرَ من ‏60 صفًّا من ‏71 (‏≥85%) | متوسّطة |

🔴 **وتنبّؤٌ سقط قبل أن يُسجَّل — يُنشَر ولا يُطوى:** كانت صيغةُ `SP5` الأولى
«الفرعُ 3 واردٌ لسقوط الأرضيّة»، **وقياسُ الجدوى حوّله من تنبّؤٍ إلى حقيقةٍ
معلومة** (‏71 دون 150) ⇒ **أُعيدت صياغةُ العقد نفسِه** بدل تسجيل تنبّؤٍ
أعرف جوابَه. والتسلسلُ مكتوبٌ في `§①` كاملًا.

---

## §⑩ ملحقٌ مؤرَّخ ‏2026-09-20 — **مصدرُ `anchor_ms` هو الآلةُ التي بَنت المجتمع**

> **العقدُ أعلاه مدموجٌ ولا يُعدَّل** — وهذا ملحقٌ مؤرَّخٌ يُقرأ معه، كما في
> `T-SUPDEF §⑩`-`§⑫` و`T-PMUSD §⑫` و`T-LINK100-2 §⑬`. **وهو مكتوبٌ قبل أيّ
> سطرِ أداةٍ وقبل أيّ رقم** — لا بعد نتيجة.

### ⓐ الواقعةُ التي كشفها بناءُ الأداة

`§①` سمّى **`liq_trig_read.py`** آلةَ المشي، وهي تُعطي مفتاحًا ثلاثيًّا
`(رمز · يوم · لحظةُ مِرساة)` ⇒ **الرمزُ الواحد في اليوم الواحد قد يحمل أكثرَ
من مِرساة** (إعادةُ المِرساة `rearm_n`). بينما `opcurve_rows.tsv` — **مجتمعُنا
بنصّ `§①`** — صفٌّ واحدٌ لكلّ `(يوم · رمز)`، بَناه **`tierlink_probe.anchor_history`**
بقاعدةٍ مكتوبةٍ في الكود: **آخرُ لقطةٍ في اليوم، وأطولُ حمولةٍ تفوز**.

⇒ **الضمُّ على `(يوم · رمز)` يحتاج قاعدةَ اختيارٍ حين تتعدّد المراسي**،
وأيُّ قاعدةٍ أخترعها الآن (‏«الأولى» مثلًا) **تختلف عن قاعدة المجتمع نفسِه
في كلّ رمزٍ أُعيدت مِرساتُه** ⇒ لحظةٌ لا تخصّ الصفَّ الذي تُنسَب إليه.

### ⓑ القرار — **الأقوى لا الأسهل**

`anchor_ms` يُقرأ من **`tierlink_probe.anchor_history` بالاسم** — وهي:
1. **تمشي `git log` لـ`op_entry_state.json` نفسِه** (شرطُ `§①` الجوهريّ:
   الموجودُ وحدَه · صفرُ جلبٍ من المزوّد · حتميّةٌ ورجعيّة) — تُراجَع بالعين
   في `tierlink_probe.py`.
2. **وهي عينُ الآلة التي أنتجت `opcurve_rows.tsv`** ⇒ التطابقُ على
   `(يوم · رمز)` **بالبناء لا بالدعوى**، فلا قاعدةَ اختيارٍ تُخترَع.

🔒 **و`liq_trig_read` لا يسقط بل يصير شاهدَ هُويّة:** `git_snapshots` +
`collect` يُنادَيان **بالاسم** ويُطبَع **عددُ `(يوم · رمز)` التي يتّفق فيها
أقدمُ `anchor_ms` من `collect` مع قيمة `anchor_history`، وعددُ المختلف**
— **ولا يُوقِف الاختلافُ التشغيلة** (‏إعادةُ المِرساة تُنتجه بالتصميم) بل
**يُنشَر رقمًا** فيُقرأ حجمُ الأثر بدل أن يُخمَّن.

### ⓒ حارسٌ يُضاف — `V-S8`

**`V-S8`** يُطبَع في كلّ تقرير: `n_anchor_history` · `n_collect_keys` ·
**المتّفقُ** · **المختلفُ ومعه أوّلُ خمسةٍ بأسمائها**. وهو **وصفيٌّ لا
يوقف** — لأن الاختلافَ معلومُ السبب ومحسوبٌ سلفًا؛ **والذي يوقف هو `V-S1`**
(المجتمعُ بت-بت) و`V-S2` (تغطيةُ الضمّ).

### ⓓ ما **لا** يغيّره هذا الملحق

⛔ لا مجتمعَ (`opcurve_rows.tsv` · `reg` وحدَه) · ⛔ ولا تعريفَ الدلاء
الأربعة ولا `held_and_jumped` · ⛔ ولا الأذرعَ والضبطَين · ⛔ ولا
**«الفرعُ 1 مُغلَقٌ بالبناء»** ولا الأرضيّةَ ‏150 ولا ‏50 · ⛔ ولا الحرّاسَ
`V-S1`-`V-S7` · ⛔ ولا «صفرُ شحنٍ مهما كانت النتيجة» · ⛔ ولا `LOGIC_VERSION`.

---

## §⑪ ملحقٌ مؤرَّخ ‏2026-09-20 — **`V-S9`: نسخةٌ ضحلةٌ ⇒ لا قياس**

> **العقدُ أعلاه مدموجٌ ولا يُعدَّل** — وهذا ملحقٌ مؤرَّخٌ يُقرأ معه.
> 🔴 **ويُكتَب بعد صدور الحكم**، ويُعلَن ذلك صراحةً: **لا يغيّر عتبةً ولا
> معيارًا ولا فرعًا ولا رقمًا مقيسًا** — بل يُضيف **حارسَ صلاحيّةِ مُدخَل**
> كان غيابُه قد أنتج قراءةً كاذبةً مُثبَتة.

### ⓐ الواقعةُ المقيسة
أوّلُ تشغيلةٍ محلّيّة أعطت `V-S2` = ‏**75/228 = ‏32.9%** ⇒ «الفرعُ 3».
والتشغيلةُ على Actions بـ`fetch-depth: 0` أعطت **‏228/228 = ‏100%** ⇒
**الفرعُ 2**. والسببُ مقيسٌ لا مخمَّن:

```
git rev-parse --is-shallow-repository          ⟶ true
git log origin/main -- op_entry_state.json | wc -l ⟶ 15,707  (أقدمُها 2026-09-04)
# بعد git fetch --unshallow                    ⟶ 25,440  (أقدمُها 2026-08-17)
```

⇒ **`git log` على نسخةٍ ضحلةٍ يُرجع تاريخًا مبتورًا بلا أيّ خطأٍ ظاهر**،
و`anchor_history` لا ترى إلّا مراسيَ ما بعد قاع النسخة.

🔴 **وهذا يُبطل سندَ `§①` نفسِه** («لا شيءَ قبل 09-04» · «التقاطعُ ‏71 =
‏31.1%») **وسندَ إغلاق الفرع 1 بالبناء** — **ولم يغيّر الحكم** لأن القياسَ
خرج سالبًا فلم يكن الفرعُ 1 مطروحًا. **والتفصيلُ في `sessions_result.md §①`.**

### ⓑ `V-S9` — حارسٌ يوقف قبل أيّ رقم
`sessions_probe.py` يفحص `git rev-parse --is-shallow-repository` **قبل**
قراءة المجتمع، ويطبع **عددَ مراجع `op_entry_state.json` وأقدمَ تاريخٍ فيها**.
**والنسخةُ الضحلة تُوقف التشغيلةَ بخروج ‏4** (`RC_POP`) برسالةٍ تُسمّي
العلاج (`git fetch --unshallow` أو `fetch-depth: 0`) — **لا تحذيرٌ يُتجاوَز**.

⚖️ **وهو تشديدٌ محضٌ لا إرخاء:** لا يمرّ به مُدخَلٌ كان يسقط، **ويُسقط
مُدخَلًا كان يمرّ وهو كاذب**. **والرقمُ المنشور لا يتغيّر** لأن التشغيلةَ
الحاكمة جرت على نسخةٍ كاملةٍ أصلًا.

### ⓒ ما **لا** يغيّره هذا الملحق
⛔ لا مجتمعَ ولا دلاءَ ولا `held_and_jumped` · ⛔ ولا الأذرعَ والضبطَين ·
⛔ ولا الأرضيّاتِ (‏150 · 50 · 90%) ولا الفروعَ الثلاثة · ⛔ ولا
`read_verdict` (ما زال **لا يُرجع الفرعَ 1 أبدًا**) · ⛔ ولا «صفرُ شحنٍ
مهما كانت النتيجة» · ⛔ ولا `LOGIC_VERSION`.
