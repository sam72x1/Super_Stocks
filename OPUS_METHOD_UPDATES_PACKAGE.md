# 🎯 حزمةُ تنفيذٍ لأوبيس — تحديثا صيّاد «النهج العلمي» (قِسْ ثم طبِّق)

**الأمر:** المالك 2026-08-15 «**جهز ١ـ٢ لاوبيس واهو ينفذ**» — بعد أن عُرض عليه
تحديثان منصوصان في منشور فيصل الجديد، فاختار **الجمع بينهما**: ① القياس أوّلًا
② ثم التطبيق. **الدور:** أنا (المدير التنفيذيّ) كتبتُ هذي الحزمة بعد تثبيت كلّ
سطرٍ في الكود بعيني؛ **وأوبيس ينفّذ ما فيها ولا يجتهد خارجها** — كلُّ انحرافٍ
يلزمه أن يتوقّف ويُبلغ (‏§⓪).

**سابقتُها:** `OPUS_EXECUTION_PACKAGE.md` (2026-07-30) بنفس التقسيم.

---

## ⓪ الحدود الحمراء — يُوقَف فورًا ويُبلَّغ (STOP)

1. **لا يُمَسّ جذرٌ محميّ بحرف:** `rank_key` · `select_top` · `classify_tier` ·
   `entry_status` · `analyze_ticker` · `apply_short_gate` · `apply_float_gate` ·
   `scan_market` · `backtest_symbol` · `scan_ignition` · **`scan_split_hunter`
   ودرعُه كلُّه** (حمايةُ المالك المنصوصة «اتركها على جنب وأبلغني») ·
   `build_interpretation` · `entry_readiness` · `faisal_split_plan` ·
   `in_entry_band` · `tested_level` · `pivot_stability` · `_resolve_arm` ·
   `fill_picks` · `replay10.py`.
   **الموضعُ المأذون وحده:** `scan_method_hunter` و`method_founding`
   و`build_method_alert` و`method_hunter.py` و`CONFIG` (مفاتيح `METHOD_*`).
2. **لا `LOGIC_VERSION` ولا `force_renew`** — صيّاد النهج العلمي **خارج الفرز
   والحالة** (‏`method_hunter.py:28` «عرض/تنبيه فقط — خارج الفرز والحالة، ولا
   `LOGIC_VERSION`»)، فرفعُ الإصدار يُعيد بناء قائمةٍ لا علاقة لها بالتغيير.
3. **لا قناةَ تلغرام رابعة** ولا رسالةَ جديدة — التغييرُ داخل رسالة الصيّاد
   القائمة.
4. **لا يُعاد تشغيل `T-METHOD`** ولا تُحدَّث أرقامُ `method_result.md` — بنصّ
   القاعدة المدوَّنة: «تُقيَّد لا تُعاد … ولا تُعاد إلّا بتسجيلٍ مسبقٍ جديد».
5. **لا رقمَ يُخترَع.** كلُّ عتبةٍ في هذي الحزمة **منطوقةٌ بلسان فيصل** ومُثبَتةٌ
   في `FAISAL_SOURCE_LEDGER.md`؛ وما ليس كذلك **لا يُضاف**.
6. **بوّابةُ الدفع:** `python3 test_bot.py` **خروج 0 وصفر فشل** قبل أيّ دفع.
7. **الطفرات في `git worktree` معزول حصرًا** وعلى شجرةٍ ملتزمة (‏§⑥).
8. **إن اختلفت إبرةٌ عمّا في §② (‏رقمُ سطرٍ أو نصٌّ) — قف وأبلغ**، لا تبحث عن
   «ما يشبهها». الحزمةُ كُتبت على `831982b`.

---

## ① النصّ المصدر (حرفيًّا — ولا يُعاد صياغتُه)

**المنشور الجديد (2026-08-15، موثَّقٌ عندنا في `FAISAL_PRESSURE_MODEL.md:218-226`
تحت عنوان «كيف تعرف انه بيدبل»):**

> هابطٌ من صعودٍ عالٍ **«من 10 ايام لشهر»** · الشورت والدعم على يومي و4س ·
> تحقّق الدعم **ورفض يكسر اقل سعر هبط له** · **+ RSI تحت 30** · … الشورت مهم ·
> **ماعليه حراج**.

**وما عندنا اليوم مقابله:**

| البند | النافذ اليوم | سنده | النصّ الجديد |
|---|---|---|---|
| نافذة الحدث المؤسِّس | **20-30 جلسة** | `IMG_0489` «تنتظر هبوطه فترة زمنيه · **20 < 30 يوم**» (‏`FAISAL_SOURCE_LEDGER.md:159`، وسمُه `faisal_verbatim`) | **«من 10 ايام لشهر»** ⇒ ‏10-30 |
| RSI | **لا شيء** (أُسقط عمدًا) | `method_sequence` docstring: «الصور الستّ تعدّد شروطها … **وليس فيها `RSI`** — فإقحامُه بوّابةً كان زيادةً منّي» | **«RSI تحت 30»** ⇒ صار منصوصًا |

⚖️ **وقرارُ إسقاط RSI كان صحيحًا وقتَه وصار بائتًا بنصّه الجديد** — وهذا **نصُّ
درس `T-MINFLOOR` المدوَّن**: «حين يكون مفهومُه منصوصًا، يُنفَّذ كما نصّه ولا
يُستبدَل بمعياري ثم يُسمّى باسمه» («لا تتفلسف»).

📌 **ولفيصل نصٌّ ثانٍ عن RSI لا يتعارض:** «يتم قراءة RSI = **يكون من 22 لـ27**»
(‏`IMG_0531` · مبنيٌّ عندنا في `faisal_rsi_zone` بمفتاحَي `FAISAL_RSI_LO=22` /
`FAISAL_RSI_HI=27` في `Super_stock.py:553-554`). **‏«تحت 30» أوسعُ منه ويحتويه**
⇒ البوّابةُ عند 30 والنطاقُ 22-27 يبقى **سطرَ عرض**. لا تعارضَ يُدَّعى.

⚠️ **حدُّ صدقٍ يُكتب ولا يُحَلّ باجتهاد:** «يوم» في نصّه قد تكون **يومَ تقويم**
لا **جلسة تداول** («شهر» ‏≈ 21 جلسة لا 30). كودُنا يعدّ **جلسات** منذ 2026-08-01
وبه قِيست الأرقام المنشورة ⇒ **تبقى الوحدةُ جلسات، ويُعلَن الالتباس** في التقرير
والدفتر. **تغييرُ الوحدة سؤالٌ مستقلّ لا يُحسم في هذي الحزمة.**

---

## ② حالةُ الكود اليوم — إبرٌ مثبَّتة (‏على `831982b`)

| # | الملفّ:السطر | النصّ الحرفيّ (الإبرة) |
|---|---|---|
| N1 | `Super_stock.py:525` | `    "METHOD_DECLINE_MIN": 20,            # «تنتظر هبوطه … 20 < 30 يوم» — حرفيّ IMG_0489` |
| N2 | `Super_stock.py:526` | `    "METHOD_DECLINE_MAX": 30,            # الحدّ الأعلى من النصّ نفسه` |
| N3 | `Super_stock.py:6647` | `def method_founding(df, rise_min=None, win_bars=None, dmin=None, dmax=None,` |
| N4 | `Super_stock.py:6665-6666` | `    dmin = int(CONFIG["METHOD_DECLINE_MIN"] if dmin is None else dmin)` / `    dmax = int(CONFIG["METHOD_DECLINE_MAX"] if dmax is None else dmax)` |
| N5 | `Super_stock.py:6684` | `def method_sequence(df, win, bounce_min=None, tol=None):` |
| N6 | `Super_stock.py:7126` | `def scan_method_hunter(history, today=None, fetch_pump=None, fetch_offering=None,` |
| N7 | `Super_stock.py:7169` | `            fnd = method_founding(df, stats=stage)  # ① أرخص شرطٍ أوّلًا` |
| N8 | `Super_stock.py:7249-7268` | كتلةُ الشرط ⑥ (المتاح للاقتراض) — **آخرُ شرطٍ قبل `rows.append`**، وهي **النموذجُ الذي تُحاكيه بوّابةُ RSI حرفيًّا** (‏`_near` ثم `continue` · والمجهول يمرّ) |
| N9 | `Super_stock.py:7271` | `            rows.append({` — **قاموسُ الصفّ، ولا مفتاحَ `rsi` فيه** |
| N10 | `Super_stock.py:7290-7292` | سطرُ السجلّ: `f"🔬 النهج العلمي: فوق أرضية السعر {stage['price']} · داخل نافذة 20-30 "` — **‏«20-30» مغروسٌ نصًّا** |
| N11 | `Super_stock.py:7539` | `                     "(شرط فيصل: 20 إلى 30 يومًا)")` — **مغروسٌ في الكرت** |
| N12 | `Super_stock.py:7567-7569` | `        for _x in faisal_rule_lines(r.get("df"), price=r.get("price"),` … `rsi_val=r.get("rsi")):` |
| N13 | `Super_stock.py:7018` | `def faisal_rule_lines(df, price=None, entry=None, avail=None, rsi_val=None):` |
| N14 | `Super_stock.py:6798` | `def faisal_rsi_zone(value, lo=None, hi=None):` |
| N15 | `Super_stock.py:975` | `def rsi(close: pd.Series, period: int = 14) -> pd.Series:` — **وتنتهي بـ`return out.fillna(50.0)`** |
| N16 | `method_hunter.py:18-20` | docstring: «حدثٌ مؤسِّس (صعودٌ عالٍ ثم هبوط **20-30** جلسة)» |
| N17 | `method_scan.py:33-34` | `DECLINE_MIN_BARS = 20` / `DECLINE_MAX_BARS = 30` — **أداةُ بحثٍ مجمَّدة** |
| N18 | `method_scan.py:192` | `    ts = S.trigger_state(df)          # عتباتٌ إنتاجية` |

### 🔴 ثلاث حقائقَ تُقرأ قبل أيّ كود (ثبّتُّها بنفسي)

**(أ) سطرُ RSI في كرت الصيّاد ميّتٌ بنيويًّا اليوم.** `build_method_alert` يمرّر
`rsi_val=r.get("rsi")` (‏N12) و`scan_method_hunter` **لا يضع مفتاح `rsi` قطّ**
(‏N9) ⇒ `faisal_rule_lines` تستقبل `None` **دائمًا** فتُسقط سطرَ «‏📐 RSI …»
**في كلّ رسالةٍ منذ بنائه**. هذا صنفُ «**سطرُ عرضٍ بلا حقلٍ = كذبة**» المدوَّن
عندنا — ويُصلَح ضمن المرحلة ب (‏§④-3) لا كأثرٍ جانبيّ بل كبندٍ مُسمًّى.
🧭 **وسابقتُه في المستودع نفسِه:** صيّاد المقسّم وقع في العيب ذاته وأُصلح، وتعليقُه
مكتوبٌ في `Super_stock.py:7922-7925` («وثلاثتُها لا وجود لها في الصفّ ⇒ كانت
ستكون **أسطرًا ميّتة** لا يكشفها اختبارٌ أخضر») — **والكاتبُ عنده في
`split_hunter.py:285-292`**:
```python
            _r["_rsi"] = (float(S.rsi(_dfx["Close"]).iloc[-1])
                          if _dfx is not None and len(_dfx) > 15 else None)
```
⇒ **يُنسَخ النمطُ القائم ولا يُكتب من الذاكرة** (قاعدةٌ مدوَّنة).

**(ب) `rsi()` لا تُرجع `NaN` أبدًا** — تنتهي بـ`fillna(50.0)` (‏N15) ⇒ سلسلةٌ
قصيرة أو تالفة تُقرأ **‏50** فتعبر بوّابةَ «تحت 30» ⇒ **الفشلُ مفتوحٌ بالبناء
(تعذّرٌ ⇒ يمرّ)** وهو المطلوب — **لكنه يُعلَن ويُقفَل** لا يُترك مصادفةً.

**(ج) أداةُ القياس التاريخية `method_scan.py` بائتةٌ عن الإنتاج:** ما زالت تقرأ
`trigger_state` (‏N18) وهي **القراءةُ المنسوخة** التي صحّحها المالك 2026-08-01
إلى `method_sequence`. ⇒ **يُمنَع استعمالُها لقياس هذين التحديثين**، وتوصيلُها
بالإنتاج مشروعٌ مستقلٌّ بتسجيلٍ مسبقٍ خاصّ به. **ولهذا المرحلةُ (أ) تقيس القمعَ
لا العائد** (‏§③-2).

---

## ③ المرحلة (أ) — القياس **قبل** أيّ تعديلٍ إنتاجيّ

### ③-1 ما يُسلَّم في هذي المرحلة

1. `method_window_prereg.md` — **يُدفَع قبل تشغيل أيّ لقطة** (لا رقمَ يسبق العقد).
2. `method_window_arms.py` — أداةُ قياسٍ خارج الإنتاج (لا يستوردها `Super_stock`).
3. `.github/workflows/method_window.yml` — يدويٌّ بمدخل `year` (بلا كرون).
4. تشغيلُ اللقطات الثلاث ثم `method_window_result.md`.

### ③-2 السؤال والمقياس (يُثبَّت في العقد **قبل** الأرقام)

**السؤال:** كم يُدخل التوسيعُ (‏10-30) وكم يُخرج شرطُ RSI تحت 30 — **من قمع
الصيّاد نفسِه**؟

**المقياسُ قمعٌ وكلفة لا عائد.** يُكتب في العقد صراحةً: **لا تُدَّعى ربحيةٌ ولا
حافةٌ من هذي التجربة**؛ سببُه المُثبَت في §②-(ج) — أداةُ العائد الوحيدة بائتة.

**الأذرع الأربع (مثبَّتةٌ الآن — لا تُضاف خامسةٌ بعد الأرقام):**

| الذراع | النافذة | شرط RSI |
|---|---|---|
| `A0` (الأساس = الإنتاج اليوم) | 20-30 | لا شيء |
| `A1` | **10-30** | لا شيء |
| `A2` | 20-30 | **تحت 30** |
| `A3` | **10-30** | **تحت 30** |

**العدّاداتُ لكلّ ذراعٍ ولقطة، على نفس أزواج (رمز، جلسة) حرفيًّا:**
`price` (فوق أرضية السعر) · `window_ok` · `founding` · `seq` (التسلسل الرباعيّ) ·
`entry_zone` · `gap_ok` · `rsi_pass` · **`match`** (المطابق الكامل) ·
**والرموزُ الداخلة/الخارجة بأسمائها** (‏`A1∖A0` و`A0∖A2`) ·
**وتوزيعُ `bars_since_peak` في سلّتين 10-19 و20-30** (‏وهو الرقمُ الذي يجيب
«كم يضيف التوسيع؟» مباشرةً) · وشريحةُ الكتالوج **وصفيًّا لا حكمًا**.

### ③-3 التصميم الإلزاميّ للأداة (ميزانيةٌ ثابتةٌ **بالبناء** لا بالوعد)

🔒 **تمريرةٌ واحدة والأذرعُ مشتقّة منها.** لكلّ (رمز، جلسة) يُنادى **مرّةً
واحدة**:

```python
fnd = S.method_founding(df_i, dmin=0, dmax=30)      # دالّةُ الإنتاج بالاسم
```

ثم تُشتقّ عضويةُ الأذرع من `fnd["bars_since_peak"]` (‏≥20 لـ`A0`/`A2` · ≥10
لـ`A1`/`A3`) ومن `rsi_last` المحسوب مرّةً. **فيستحيل بنيويًّا** أن تختلف
الميزانية أو العيّنة بين الأذرع (‏وهي القاعدة المستخرَجة من إفساد `T-CLIFF`).

- `dmax=30` **في الأذرع الأربع** ⇒ شريحةُ `df.tail(win_bars + dmax + 5)` داخل
  `method_founding` **متطابقة**، والمختلفُ **مقارنةُ `dmin` وحدها**.
- **قفلٌ إلزاميّ (‏MW1):** على عيّنةٍ مفرِّقة، عضويةُ الذراع **المشتقّة** تطابق
  نداءً مباشرًا `method_founding(df, dmin=20, dmax=30)` و`(dmin=10, dmax=30)` —
  وإلّا فالاشتقاقُ مقياسٌ ثانٍ لا اختصار.
- `method_sequence(df_i, win=int(fnd["bars_since_peak"]) + 2)` **بنفس نداء
  الإنتاج حرفيًّا** (‏`Super_stock.py:7177`).
- **‏RSI = `float(S.rsi(df_i["Close"]).iloc[-1])`** — **الفريمُ اليوميّ حصرًا**،
  وسندُه قاعدةُ المالك المنصوصة «**يمشي من الفريم الكبير إلى الصغير**» ومعها
  `IMG_0446` («يوميّ/أسبوعيّ لأسهم التجميع») ⇒ اليوميُّ حاكم. **يُعلَن في العقد.**
- **المشيُ سببيّ:** كلُّ نداءٍ على `df.iloc[:i]` حصرًا ⇒ لا شمعةَ لاحقة تُقرأ.
- **الخطوة `STEP=5` جلسات** — **تُعلَن**، وهي واحدةٌ للأذرع الأربع.
- **4 ساعات خارج القياس التاريخيّ** (‏الأداةُ لا تجلب شبكةً): يُعلَن أن
  الأعدادَ **أرضيّةٌ** مقابل الحيّ الذي يقرأ الفريمين — **والمقارنةُ بين الأذرع
  لا بينها وبين الحيّ**.
- 🔴 **والأذرعُ تمرّر عتباتِها صراحةً ولا تقرأ `CONFIG` أبدًا** (‏`A0` تمرّر
  `dmin=20` نصًّا · و`A1` تمرّر 10 · وحدُّ RSI ثابتٌ في الأداة). **سببُه ملزِم:**
  المرحلةُ (ب) ستغيّر `CONFIG` ⇒ أداةٌ تقرؤه تصير **بعد التطبيق تقيس عالمًا آخر
  وتُسمّيه الأساس** — وهو حرفيًّا ما وقع في `CAP15`/`REPLAY10🔒` وعولج بمهربٍ
  صريح. ⇒ **أرقامُ (أ) تبقى قابلةً لإعادة الإنتاج بت-بت بعد (ب)** — مقفولٌ
  بـ`MW11`.

### ③-4 حرّاسُ الـno-op ورموزُ الخروج (بلا شرحٍ مبهم)

| الرمز | الحالة |
|---|---|
| `2` | اللقطة مفقودة (`BT_FROZEN_PATH`) |
| `4` | **صفرُ أزواجٍ مقيسة** — بصمةُ الـno-op |
| `5` | `A1` **ليست فوق-مجموعةٍ** من `A0` (‏`A0 ⊄ A1`) = عطبُ أداةٍ لا نتيجة |
| `6` | `rsi_pass` = **كلُّ** الصفوف أو **صفرُها** في اللقطات الثلاث ⇒ يُعلَن أن المقياس لم يفرّق (قد يكون حقيقةً — لكنه يُطبَع بوصفه كذلك لا يُبتلع) |
| `0` | قياسٌ سليم |

⚠️ **و«‏A1 == A0» ليست عطلًا** — قد تكون نتيجةً حقيقية (لا أحدَ يقع في 10-19)؛
ويميّزها **توزيعُ السلّتين** المطبوع، فلا يُقرأ صفرٌ على أنه عطل ولا العكس.

### ③-5 اللقطات والتشغيل

`BT_FROZEN_PATH` من لقطات PIT الثلاث: **2023=`31574165523` ·
2024=`31199493957` · 2025=`31191327731`** (نفسُها في `repeat_prereg.md`).
`method_window.yml` يدويٌّ بمدخل `year`، ويرفع الصفوف artifact **باسمٍ مُدخَل لا
مغروس** (درسُ `O14أ`).

### ③-6 تنبّؤاتٌ مسجَّلةٌ قابلةٌ للكسر (تُكتب في العقد قبل الأرقام)

**سندُها مقيسٌ من ظرف الكاتالوج النافذ** (طُبع في تشغيلة 2026-08-15 بالبصمة
`019c983e1691`): وسيطُ قاع RSI في نوافذ أسهم فيصل **`RSI_OVERSOLD=33.0`**
ووسيطُ RSI الحاليّ **`RSI_MAX_NOW=35.78`** ⇒ **‏«تحت 30» يقع دون وسيط كتالوجه
نفسِه**.

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| `MP1` | سلّةُ `bars_since_peak` ‏10-19 **غيرُ فارغة** في اللقطات الثلاث ⇒ التوسيعُ يضيف فعلًا | سلّةٌ فارغةٌ في أيّ لقطة ⇒ التوسيعُ لا أثرَ له وتُنشَر كذلك |
| `MP2` | شرطُ RSI **يقصّ أكثر من نصف** المطابقين (‏`match(A2) < 0.5 × match(A0)`) مجمَّعًا | قصٌّ أقلُّ من النصف |
| `MP3` | `A3` **لا يُصفَّر** (‏`match(A3) ≥ 1` مجمَّعًا على الثلاث) ⇒ يُشحن بوّابةً بنصّ §④-0 | صفرٌ في الثلاث ⇒ يُشحن وسمَ عرضٍ ويُعلَن |

⚖️ **و`MP3` هو الشرطُ الوحيد الذي يبدّل شكلَ التطبيق — ومكتوبٌ قبل الأرقام**،
فلا يُخترَع معيارٌ بعدها.

### ③-7 التقرير (`method_window_result.md`)

جدولُ الأذرع × اللقطات + الرموزُ الداخلة/الخارجة + توزيعُ السلّتين + شريحةُ
الكتالوج + **حدودُ الصدق كما في §③-2/③-3**. **ولا حكمَ ربحيةٍ ولا توصيةَ عتبة.**

---

## ④ المرحلة (ب) — التطبيق (بعد نشر أرقام أ)

### ④-0 قاعدةُ التطبيق — **مثبَّتةٌ الآن قبل أيّ رقم** (فلا تتحرّك بعده)

- **U1 (النافذة ‏10-30) يُطبَّق بلا شرط.** نصٌّ حرفيّ · **وتوسيعٌ رتيبٌ محض**
  (‏`10-30 ⊇ 20-30`) ⇒ **لا يُخرج مرشّحًا كان يمرّ**، وكلفتُه حجمٌ لا حكم.
- **U2 (‏RSI تحت 30) يُطبَّق بوّابةَ رفضٍ بلا شرط أيضًا** — نصٌّ حرفيّ، وقاعدةُ
  «لا تتفلسف» ملزِمة. **الاستثناءُ الوحيد المُعلَن سلفًا:** إن أظهر القياس أنه
  **يُصفّر الأداة** (‏`match(A2)=match(A3)=0` في **اللقطات الثلاث** *و* صفرٌ على
  البِركة الحيّة في تشغيلةٍ واحدة) ⇒ يُشحَن **وسمَ عرضٍ وعدّادًا** بدل بوّابة،
  **ويُعلَن للمالك بكلمةِ تفعيلٍ واحدة** («فعّل شرط RSI»). أيُّ حالةٍ أخرى =
  بوّابة. **ولا معيارَ ثالثٌ يُخترَع بعد الأرقام.**

### ④-1 U1 — النافذة

1. `Super_stock.py:525` ⟵ `"METHOD_DECLINE_MIN": 10,` **مع تعليقٍ مؤرَّخ** يذكر
   **النصّين معًا**: «‏20 < 30 يوم» (`IMG_0489`) **و**«من 10 ايام لشهر»
   (منشور 2026-08-15) — والأحدثُ أوسعُ فيُعتمَد، **والقديمُ لا يُمحى من السجلّ**.
2. `METHOD_DECLINE_MAX` **يبقى 30** («لشهر») — لا يُمَسّ.
3. **كلُّ نصٍّ يعرض «20-30» يُشتقّ من `CONFIG`** (‏N10 · N11 · N16 · وdocstring
   `method_founding`) ⇒ **قفلٌ سلوكيّ** (‏MW2) يضبط `CONFIG` مؤقّتًا على قيمةٍ
   ثالثة (‏مثلًا 12) ويشترط ظهورَ «12» في نصّ الكرت والسجلّ — **وإلّا فسطرُ
   عرضٍ يكذب** (الفخُّ المدوَّن).
4. `FAISAL_SOURCE_LEDGER.md:159` يُحدَّث: القيمةُ الجديدة · **الاستشهادان معًا**
   · الوسمُ يبقى `faisal_verbatim` · وعمودُ الأثر يُعاد حسابُه من أرقام (أ).
5. `method_scan.py:33-34` **لا يُمَسّ** (‏أداةُ بحثٍ مجمَّدةٌ بأرقامها المنشورة)
   — ويُضاف سطرُ تعليقٍ واحدٍ فقط يقول إنها مجمَّدةٌ على نافذة `IMG_0489`.

### ④-2 U2 — شرط RSI

**مفتاحٌ جديد في `CONFIG` بجوار `METHOD_*`:**

```python
"METHOD_RSI_MAX": 30.0,   # «+ RSI تحت 30» — منشور 2026-08-15 (حرفيّ)
```

**وكتلةُ الشرط تُوضَع بعد كتلة الاقتراض (‏N8) وقبل `rows.append` (‏N9)**،
**مُحاكِيةً نموذجَ N8 حرفيًّا**:

```python
            # ⑦ **RSI تحت 30** — «+ RSI تحت 30» (منشور 2026-08-15، حرفيّ).
            #    🔒 الفريمُ **اليوميّ** (قاعدة المالك: من الفريم الكبير للصغير)
            #    · و«تعذّرٌ ⇒ يمرّ» — و`rsi()` تُرجع 50 عند القصور (لا NaN)
            #    فالفشلُ مفتوحٌ بالبناء، وهذا مقصودٌ ومقفول.
            try:
                _rsi = float(rsi(df["Close"]).iloc[-1])
            except Exception:                                    # noqa: BLE001
                _rsi = None
            if _rsi is not None and _rsi >= float(CONFIG["METHOD_RSI_MAX"]):
                _near(f"RSI {_rsi:.0f} — شرط فيصل: تحت "
                      f"{int(CONFIG['METHOD_RSI_MAX'])}")
                continue
```

**وثلاثةُ قيودٍ إلزامية معها:**

- **المقارنة `>=` تَرفض** («تحت 30» ⇒ ‏30 نفسها **ليست** تحت 30).
- **الرفضُ يُسمّى في `_METHOD_NEAR`** بالرقم — فلا يختفي اسمٌ صامتًا (قاعدةُ «لا
  قصّ صامت»).
- **عدّادٌ في `stage`**: مفتاح `"rsi_ok"` يُزاد عند المرور، ويُطبَع في سطر السجلّ
  الختاميّ (‏N10) بين `entry_zone` و`مطابق كامل`.

### ④-3 إحياءُ سطر RSI الميّت (بندٌ مُسمًّى لا أثرٌ جانبيّ)

`rows.append` (‏N9) يكتسب **`"rsi": _rsi`** ⇒ يعمل سطرُ «‏📐 RSI …» في
`faisal_rule_lines` (‏N13) لأوّل مرّة، ويظهر معه نطاقُ فيصل 22-27. **قفلٌ
(‏MW6)** يشترط ظهور السطر في كرتٍ حقيقيّ.

### ④-4 التوثيق (إلزاميّ في نفس الكوميت)

`FAISAL_PRESSURE_MODEL.md §⑧-② ` يُحدَّث من «⏸️ يحتاج قرارَ المالك» إلى
**«نُفِّذ»** بتاريخه وأرقام (أ) · `FAISAL_SOURCE_LEDGER.md` (صفّ ① محدَّث + صفٌّ
جديد لـ`METHOD_RSI_MAX`) · `method_hunter.py` docstring · `CLAUDE.md` +
`HANDOFF.md` (كتلةُ ذاكرةٍ موجزة بالنتيجة والكلمات الراجعة).

---

## ⑤ الأقفال المطلوبة (بأسمائها — كلٌّ يجب أن **يسقط** بطفرته)

| الاسم | يحرس |
|---|---|
| `MW1` | اشتقاقُ عضوية الأذرع في الأداة == نداءٌ مباشر لـ`method_founding` بالـ`dmin` نفسه (عيّنةٌ مفرِّقة: `bars_since_peak` = 14 ⇒ في `A1` لا `A0`) |
| `MW2` | **سلوكيّ**: نصُّ الكرت وسطرُ السجلّ يقرآن النافذة من `CONFIG` (ضبطٌ مؤقّتٌ على 12 ⇒ يظهر «12») |
| `MW3` | `CONFIG["METHOD_DECLINE_MIN"] == 10` و`MAX == 30` (يُسقط ارتدادًا صامتًا) |
| `MW4` | **سلوكيّ مفرِّق**: سهمٌ RSI‏=35 يُرفَض وسهمٌ RSI‏=25 يمرّ — **وبنفس بقيّة الشروط** |
| `MW5` | تعذّرُ حساب RSI ⇒ **يمرّ** (‏`Close` تالف/قصير) — والفشلُ مفتوح |
| `MW6` | المرفوضُ بـRSI **حاضرٌ في `_METHOD_NEAR`** بسببه المُسمّى · والمقبولُ يحمل `rows[i]["rsi"]` وسطرَ «‏📐 RSI» في الكرت |
| `MW7` | حدُّ التخوم: RSI‏=30.0 **بالضبط ⇒ يُرفَض** («تحت 30») |
| `MW8` | العدّاد `rsi_ok` يظهر في سطر السجلّ ويتحرّك (لا صفرٌ جامد) |
| `MW9` | **بصمةُ الدرع**: `scan_split_hunter` و`_split_setup_probe` و`analyze_ticker` **بت-بت** (‏AST) بعد التعديل |
| `MW10` | الأداةُ خارج الإنتاج: `Super_stock.py` **لا يستورد** `method_window_arms` |

---

## ⑥ الطفرات (تُدار في `git worktree` معزول · شجرةٌ ملتزمة · `PYTHONDONTWRITEBYTECODE=1` + `rm -rf __pycache__` · الإبرةُ تطابق **مرّةً واحدة**)

1. `METHOD_DECLINE_MIN` ‏10 ⟶ 20 (ارتدادٌ صامت) ⇒ يجب أن يسقط `MW3`.
2. النافذةُ في الكرت تُغرَس «‏20 إلى 30» نصًّا ⇒ `MW2`.
3. `method_founding` تتجاهل وسيط `dmin` وتقرأ `CONFIG` دائمًا ⇒ `MW1`.
4. مقارنةُ RSI تُقلَب (`<=` بدل `>=`) ⇒ `MW4`.
5. حدُّ التخوم يصير `>` بدل `>=` ⇒ `MW7`.
6. تعذّرُ RSI **يرفض** (‏`_rsi = 0.0` بدل `None`، أو `continue` في `except`) ⇒ `MW5`.
7. حذفُ نداء `_near` من كتلة RSI (رفضٌ صامت) ⇒ `MW6`.
8. `rows.append` بلا مفتاح `rsi` ⇒ `MW6` (شقُّه الثاني).
9. حارسُ الـno-op في الأداة يُحذف (صفرُ صفوفٍ ⇒ خروج 0) ⇒ قفلُ الخروج 4.
10. الأداةُ تُشتقّ `A1` من `dmin=20` (تسريبُ ذراع) ⇒ `MW1`.

**‏10 طفرات — وكلُّها يجب أن تسقط.** أيُّ ناجيةٍ = **قفلٌ أعمى**: يُقوّى القفل
(عيّنةٌ **تفرّق**) ثم تُعاد الطفرة حتى تسقط، ويُذكَر ذلك في التقرير. وبعد كلّ
جولة: `git status` + التحقّق أن الشجرة **مطابقةٌ لرأس الدفع بت-بت**.

---

## ⑦ البوّابة والدفع

- `python3 test_bot.py` ⇒ **خروج 0 · صفر فشل** (‏العدد يتقادم فلا يُقتبس).
- الفرع: `main` مباشرةً · **ولا PR** إلّا بطلبٍ صريح.
- ذيلُ الكوميت **حرفيًّا**:
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
  `Claude-Session: https://claude.ai/code/session_01LBuXeYuQbmaEjVK9gwExgJ`
- **لا يُذكر معرّفُ نموذجٍ في أيّ ملفٍّ مدفوع.**
- كوميتان منفصلان: **(أ) القياس** (عقد + أداة + workflow + نتيجة) ثم
  **(ب) التطبيق** (كود + توثيق) — فيُقرأ السجلُّ بأن العقدَ سبق الأرقام والأرقامُ
  سبقت التعديل.

---

## ⑧ التقرير للمالك (عربيّ · يُختم بخياراته حرفيًّا)

**يبدأ بالنتيجة:** كم أضاف التوسيع (بالأسماء) · وكم أخرج شرط RSI · وكم صار
المطابقُ الكامل. ثم الكلفةُ والحدود. **ويُختم بالخيارات المكتوبة كما يكتبها:**

- قل «**ارجع 20**» — لإرجاع نافذة الحدث المؤسِّس إلى 20-30.
- قل «**ألغِ شرط RSI**» — ليعود سطرَ عرضٍ بلا رفض.
- قل «**غيّر حدّ RSI إلى XX**» — لتعديل الرقم وحده.
- ولو لم يكن عليه قرار: **قُلها صراحةً** («لا شيء معلّق عليك»).

---

## ⑨ ما **لا** يُفعَل (حتى لو بدا تحسينًا)

- لا تُغيَّر `METHOD_BOUNCE_MIN_PCT` ولا `METHOD_ENTRY_PCT` ولا `METHOD_STOP_PCT`
  ولا `METHOD_SHORT_MAX` — **كلُّها منطوقةٌ بأرقامه** ولم يُطلَب مسُّها.
- لا تُحوَّل «‏من 10 ل 20٪» إلى سقفٍ رافض (سقفٌ وصفيّ — قرارٌ مدوَّن).
- لا يُنقَل الصيّاد إلى فريم 4س ولا يُغيَّر ترتيبُ الفريمين (قاعدةُ المالك
  المنصوصة، والمِجَسّ `METHOD_4H_PROBE` يبقى مطفأً).
- لا يُمَسّ `trigger_state` ولا `method_sequence` (‏القراءةُ صحّحها المالك بنفسه).
- لا تُضاف بوّابةُ «شمعة انعكاسية» ولا «حراج» — **«ماعليه حراج» مخدومٌ سلفًا**
  بشرط القروب (‏`group_pump_scar`) بقرارٍ مدوَّن من المالك («الحراج = القروبات»).
- لا يُقاس العائدُ بـ`method_scan.py` (‏§②-ج).

---

## ⑩ حدودُ صدقٍ تُكتب **قبل** الأرقام وتُنشَر معها

1. **وحدةُ «يوم»** جلسةٌ عندنا وقد تكون تقويميّةً عنده (‏§①) — مُعلَنة، غيرُ محسومة.
2. القياسُ **قمعٌ لا عائد** — ولا يُقال «أفضل» ولا «أسوأ»، بل «يُدخل كذا ويُخرج كذا».
3. الأداةُ التاريخية **يوميّةُ الفريم** والحيُّ يقرأ الفريمين ⇒ أعدادُها **أرضيّة**.
4. الخطوة 5 جلسات ⇒ الأعدادُ **عيّنةٌ متّسقة** بين الأذرع لا حصرٌ كامل.
5. لقطاتُ PIT تشمل المشطوبين ⇒ انحيازُ البقاء مخفَّفٌ لا ممحوّ · والسلاسل
   **معدَّلةٌ بالتقسيمات** فقد يكون «الحدث المؤسِّس» أثرَ تعديل (وهو الحارسُ الذي
   بُني في `method_scan.split_in_window` **وغيرُ مبنيٍّ في الإنتاج**) — **يُعلَن**.
6. شريحةُ الكتالوج **مختارةٌ على النتيجة** ⇒ وصفيّةٌ مهما جاءت.
7. **الوصفةُ كلُّها «قيد الإثبات الأماميّ»** (‏`method_result.md`: 27 دون 30 ⇒
   «لا حكم») — وهذي الحزمةُ **لا تغيّر ذلك**، وسجلُّ الحصاد (`hunter_ledger`) هو
   مَن يحكم لاحقًا.
