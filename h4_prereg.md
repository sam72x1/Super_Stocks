# 🕓 `T-H4` — تسجيلٌ مسبق: **فريمُ الأربع ساعات**

> **أمرُ المالك (2026-08-28): «اعتماد ١ ٢ ٣ ٤».** هذا العقدُ الأوّل من أربعة،
> **مدفوعٌ قبل سطرِ كودِ أداةٍ واحد** ولا يُمَسّ بعد أوّل رقم.

## ⓪ لماذا — دعوًى باطلةٌ كتبتُها بيدي

`T-CANDLE` أُغلقت بنصّي حرفيًّا: «**المحورُ مُغلَق ولا يُعاد فتحُه إلّا بمحورٍ
جديد — وهو فريمُ 4 ساعات، وغيرُ متاحٍ بلقطاتنا**». و`method_hunter` فيه سؤالٌ
مفتوحٌ منذ 2026-08-01 بالنصّ نفسِه.

🔴 **والدعوى باطلة، والدليلُ مقيسٌ من المستودع:**
- `fetch_4h` ⟵ `yf.download(period="60d", interval="1h")` ⇒ ستّون يومًا **حيًّا فقط**.
- **صفرُ نداءٍ لـ`/hour/` من Polygon** في المستودع كلِّه.
- **وشموع الدقيقة مُثبَتةٌ لثلاث سنوات**: `T-EVENT-EXEC` كذّبت تنبّؤي `W1`
  حرفيًّا («تاريخ شموع الدقيقة يغطّي 2023») · و`T-AH` قاست `minute_aggs`
  **‏≈27MB/يوم واختزالَه 6.1ث** ⇒ سنةٌ ‏≈0.4 ساعة.
- و`_frame_probe` (‏تشغيلة `30694240461`) قاس على 156 مستوفيًا: **اليوميّ 14 ·
  ‏4س 16 · والتقاطعُ 5 من 25 = ‏20%** ⇒ «**اختيارُ الفريم يبدّل ثمانين بالمئة
  ممّن يصل المالك**».

⇒ **السؤالُ الحاكم: هل يتغيّر الحكمُ حين نقيس على الفريم الذي يعمل عليه فيصل؟**

## ① اتفاقيةُ الدلاء — **مثبَّتةٌ قبل أيّ رقم ولا تُحرَّك**

شمعةُ 4 ساعاتٍ = تجميعُ دقائق **بتوقيت نيويورك** في الدلاء
**‏[04:00, 08:00) · [08:00, 12:00) · [12:00, 16:00) · [16:00, 20:00)**.

⚖️ **وهي إعادةُ إنتاجٍ لا اختيار:** تطابق (أ) `resample_ohlc(h1, "4h")`
الإنتاجية على فهرسٍ بتوقيت نيويورك، و(ب) نافذةَ `kasih_scan.parse_day`
القائمة (`04:00 ≤ mod < 20:00`) حرفيًّا. `O`=أوّل · `H`=أقصى · `L`=أدنى ·
`C`=آخر · `V`=مجموع. والدلوُ الفارغ **يُسقَط ولا يُملأ**.

⛔ **ولا تُجرَّب اتفاقيةٌ ثانية** (‏09:30-anchored) بعد رؤية الأرقام — تجريبُ
مِرساتين واختيارُ الأجمل هو تحريكُ هدفٍ ممنوع. وإن لزمت فبعقدٍ جديد.

## ② المجتمع

لقطاتُ PIT الثلاث (‏2023 `31574165523` · 2024 `31199493957` · 2025
`31191327731`) — نفسُ مجتمع `T-CANDLE` و`method_hunter` حرفيًّا، فالمقارنةُ
داخل الميزانية الثابتة (‏قاعدةُ `karpathy/autoresearch` المعتمَدة).

## ③ الأذرع — **ذراعان لا ثالثة**

| الذراع | التعريف |
|---|---|
| **`D0`** | القاعدةُ على **اليوميّ** — إعادةُ إنتاجٍ للمنشور |
| **`H1`** | القاعدةُ **نفسُها حرفيًّا** على شموع 4س المبنيّة |

**والقاعدتان المقيستان:**
- **`R-CANDLE`**: أنماطُ فيصل الستّة الانعكاسية (`wake_read.rev`) — نفسُ
  تعريف `T-CANDLE` بلا حرفٍ زائد.
- **`R-SEQ`**: `method_sequence` الإنتاجيّة **بالاسم** (قاعٌ ⟶ ارتدادُ
  `METHOD_BOUNCE_MIN_PCT` ⟶ رجوعٌ للنطاق ⟶ ثبات) — بلا نسخةٍ منها.

## ④ المقياسُ الحاكم — مثبَّتٌ قبل الأرقام

**`R` المحقَّق لكلّ حلقة** بمحرّك الحسم الإنتاجيّ نفسِه، **لا نسبةُ النجاح**
(‏`T-STOP` ‏34⟶54% بتوقّعٍ ثابت · `T-LIBERATION` نجاحٌ 87% بتوقّعٍ سالب).

**والمقياسُ المساند (وصفيٌّ لا يحكم):** عضويّةُ المُلتقَطين — كم يتقاطع
`D0` مع `H1`؟ (‏`_frame_probe` قاسه ‏20% على مجتمعٍ آخر).

## ⑤ المعايير — الأربعةُ تلزم معًا

1. `H1 − D0` **‏≥ +0.15R** مجمَّعًا (‏عتبةٌ **مُعادةٌ** من `T-STOP`/
   `T-LIBERATION`/`T-RECLAIM-INTRADAY` — لا تُحرَّك)
2. **موجبٌ في السنوات الثلاث** بلا انقلاب
3. فاصلُ ثقةٍ 95% للفرق **لا يلمس الصفر**
4. أرضية: **‏150 حلقةً محسومةً مجمَّعًا و30 لكلّ سنة** — ودونها **«لا حكم»**
   يُعلَن ولا يُفسَّر

## ⑥ بوّاباتُ الصلاحية — تُنفَّذ في الكود لا تُوصَف

> 🔴 درسُ `envelope_prereg §⑦`: «حارسٌ مكتوبٌ وغيرُ منفَّذٍ ليس حارسًا — هو نيّة».

| البوّابة | الشرط | الإخفاق |
|---|---|---|
| **`HV0`** | شموعُ 4س المبنيّة من الدقائق **تطابق `fetch_4h` الحيّة** على عيّنةٍ حيّة: نفسُ عدد الشموع في اليوم ونفسُ `O/H/L/C` ضمن تسامح **‏0.5%** | خروج **3** — عطبُ أداة لا نتيجة |
| **`HV1`** | التغطية **‏≥95%** من أيام التداول · والمفقودُ **يُسمّى بتواريخه** | خروج **3** |
| **`HV2`** | عددُ شموع 4س لليوم الكامل = **‏4** في ‏≥95% من الأيام | خروج **3** |
| **`HV3`** | شاهدُ ضبط **`AAPL`**: 4 شموع/يوم · وحجمٌ موجبٌ في كلٍّ | خروج **3** |
| **`HV4`** | `D0` يُعيد رقمًا **منشورًا** من `T-CANDLE` بت-بت | خروج **3** |
| **`HV5`** | الذراعان **تفترقان** (‏عضويةٌ مختلفة) — وإلّا `no-op` | خروج **4** |

## ⑦ التنبّؤات — قابلةٌ للكسر، وأوّلُها يراهن على الفشل

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| `H-P1` | **‏`H1 − D0` لا يبلغ +0.15R** — أرجّح فشلَ المحور رغم بنائه | يبلغها |
| `H-P2` | التقاطعُ بين الذراعين **‏≤40%** (‏`_frame_probe` قاس 20%) | فوق 40% |
| `H-P3` | ‏4س **يُنتج حلقاتٍ أكثرَ ‏2× فأعلى** (شموعُه ×4-6) | أقلّ من ×2 |
| `H-P4` | `HV0` **يعبر** — الدلاءُ صحيحة | يسقط ⇒ عطبُ أداة |
| `H-P5` | معدّلُ بلوغ الهدف على 4س **أدنى** (حلقاتٌ أكثرُ وأرقّ) | أعلى |

## ⑧ سقفُ النجاح — مثبَّتٌ سلفًا

**نجاحٌ ⇒ اقتراحٌ للمالك بإعادة فتح `T-CANDLE`/`method_hunter` على 4س، وسطرُ
عرض.** ⛔ **ولا مسَّ بـ`M1`-`M14` ولا بالوقف ولا بالأهداف ولا بأيّ عتبة**
مهما كانت الأرقام. **والقناةُ نفسُها (`h4_build`) تبقى أداةَ قياسٍ خارج
الإنتاج** — `Super_stock.py` لا يستوردها (مقفول).

## ⑨ حدودُ صدقٍ — مكتوبةٌ قبل الأرقام

1. **اتفاقيةُ الدلاء اختيارٌ** — منصفةٌ لأنها تُعيد إنتاجَ الحيّ، **وليست
   يقينًا** أن منصّةَ فيصل تُرسي عندها.
2. **الأيامُ ذاتُ الإغلاق المبكّر** (‏≈3/سنة) دلوُها الأخير مبتور — يُعَدّ ويُعلَن.
3. **بلا انزلاق** — لمسٌ لا تنفيذ (سقفٌ متفائلٌ **بالتساوي على الذراعين**).
4. **انحيازُ البقاء منتفٍ** (‏`minute_aggs` تشمل المشطوبين) — وهي ميزةٌ على
   قياساتنا اليوميّة من ياهو.
5. **الفريمُ ليس المنهجية**: نجاحُ 4س لا يعني أن فيصل «يشتغل به وحدَه» —
   نصُّه في مادّةٍ أخرى «**كلاهما**».
