# 🔎 المجموعةُ الحقيقيّة لمُعرِّف الشارت — مجمَّدةٌ قبل أيّ بحث

> العقد `chart_finder_prereg.md §④-§⑤`. **البطاقاتُ كُتبت بقراءتي للصورة وجُمِّدت ببصمة `sha256`
> (`MANIFEST.sha256`) قبل أيّ تشغيلٍ عليها.** والمفتاحُ `KEY.tsv` من الكاتالوج **لا يقرؤه البحث**.
> 🔒 **لا صورةَ هنا** — المرجعُ `faisal_images/` بمعرّفها، والبطاقةُ أرقامٌ فقط بلا رمز.

## الاختيار (حتميّ · بنصّ العقد)
صفوفُ `FAISAL_IMAGE_PASS2.md` و`FAISAL_IMAGE_PASS3.md` التي خليّتُها الأولى «معرّف ‏+ رمزٌ واحد» ووسمُها 🎯 بلا 🔁
وملفُّها موجود (**‏189**) ⟵ مرتَّبةً بـ`sha256(المعرّف + "20260924")` ⟵ قُرئت بالترتيب حتى بلغ المقبولُ **40**:
**‏48 صورةً رُئيت · 40 قُبلت · 8 استُبعدت** (‏7 «ليست شارتًا» · 1 «بلا رقمين مقروءين») — والسجلُّ الكامل
صفًّا صفًّا بقرار كلٍّ وسببه وملاحظة القراءة في `SELECTION.jsonl`.

## الصنف (للمعيار `K2`) — من الظاهر في الصورة وحدَه
| الصنف | القاعدة | العدد |
|---|---|---|
| `daily_axis` | الفريمُ يوميٌّ أو أسبوعيٌّ **ظاهرٌ نصًّا** (Daily · 1D · يومي · Weekly · أسبوعي) ورقمان مقروءان على محور السعر | 12 |
| `intraday` | الفريمُ لحظيٌّ ظاهر (دقائق/ساعات) أو جلسةٌ ممتدّةٌ مظلَّلة | 20 |
| `unknown` | الفريمُ غيرُ ظاهر (قصٌّ بلا وسم) — **لا يُفترض يوميًّا ولو بدا كذلك** | 8 |

⇒ **`K2` دون حدّه (20) فحكمُه «لا حكم» بنصّ العقد** — والحكمُ يقوم على `K1` و`K3`.

## ما يُعلَن قبل التشغيل (حدودُ صدق)
1. **صورٌ غيرُ مستقلّة** (المصدرُ أو اللحظةُ نفسُها) — تُعدّ كلٌّ بطاقةً كما ينصّ العقد، ويُطبع أثرُها:
   `TG_1930`/`TG_2080` (شارتٌ واحد) · `TG_1937`/`TG_1997` · `TG_1807`/`TG_1835` · `TG_2012`/`TG_2011` ·
   `TG_2032`/`TG_2028`/`TG_2029` (اللحظةُ نفسُها بفريماتٍ مختلفة) · `TG_2046`/`TG_2121` (اللحظةُ نفسُها).
2. **رمزٌ ظاهرٌ في 4 صور** (أسطورةُ الشارت أو الترويسة أو نصُّ التغريدة: `TG_1872` · `TG_2022` · `TG_1834` ·
   `TG_1835`/`TG_2029`) — **ولا يدخل البطاقة**؛ والتسرّبُ العامّ مُعلَنٌ في العقد §⑧-1 (أرى الرمزَ في كلّ صورة).
3. **حقولٌ مسجَّلةٌ لا تقرؤها الأداة** (`axis_md` تواريخُ شهر/يوم بلا سنة · `captured_by` تاريخُ النشر ·
   `chart_type` خطٌّ/مساحة · `last_bar` شمعةٌ بلا تاريخ) — **بياناتٌ لنسخةٍ قادمة** لا تُستعمل هنا: إضافةُ دعمها بعد
   رؤية هذي البطاقات تصميمٌ على مجموعة الاختبار، والعقدُ لا يجيز إلّا إصلاحَ عيبٍ كشفته المصنوعة.
4. **قيمٌ مشتقّة لا مقروءةٌ مباشرة** (آخرُ سعرٍ من ترويسة «بعد الإغلاق/قبل الافتتاح» ونسبتها) — والاشتقاقُ حسابٌ
   من نصّين مقروءين، وكلُّ حالةٍ موسومةٌ في `SELECTION.jsonl` · وما كان غامضًا **أُسقط ولم يُخمَّن** (`TG_1940` · `TG_1911`).
