# 🔎 المجموعةُ الحقيقيّة الثانية لمُعرِّف الشارت — مجمَّدةٌ قبل أيّ بحث

> العقد `chart_finder_prereg.md §⑩` (ملحقُ v1.1) · والحكمُ بـ`§⑤` **حرفيًّا** · والتنبّؤاتُ `CP8`-`CP11` مكتوبةٌ في العقد قبل أيّ نظر.
> **البطاقاتُ كُتبت بقراءتي للصورة وجُمِّدت ببصمة `sha256` (`MANIFEST.sha256`) وتُدمج قبل أيّ تشغيلٍ عليها.**
> والمفتاحُ `KEY.tsv` من الكاتالوج **لا يقرؤه البحث**. 🔒 **لا صورةَ هنا** — المرجعُ `faisal_images/` بمعرّفها.
> 🔒 **مقفولٌ في السويّة:** `CFD38` (الاختيارُ بنصّ §⑩: الموضعُ 49 ⟵ 189 بلا قفز · الأسبابُ الأربعة · 75/66) و`CFD39`
> (البصمة · المفتاحُ = الكاتالوج · الصنفُ من الفريم المكتوب · لا صورة) — **‏13 طفرةً سقطت كلٌّ بقفلها وحدَه** وشاهدُ ضبطٍ
> (لحظيٌّ **بتاريخ** يمرّ) نجا كما يجب.

## الاختيار (حتميّ · بنصّ العقد §⑩-1)
المعينُ نفسُه (‏**189** صفًّا مؤهَّلًا) بترتيب `sha256(المعرّف + "20260924")` ⟵ **من الموضع 49 حتى 189** (‏الـ48 الأولى رُئيت
للمجموعة الأولى) · **بلا حدٍّ أعلى: كلُّ مقبولٍ دخل**.
**‏141 صورةً رُئيت كلُّها بالعين · 75 قُبلت · 66 استُبعدت** — والسجلُّ الكامل صفًّا صفًّا بقرار كلٍّ وسببه وملاحظة القراءة في
`SELECTION.jsonl` (الموضعُ `i` صفريّ: 48 ⟵ 188).

| سببُ الاستبعاد (أربعةٌ لا غير · §⑩-2) | العدد |
|---|---|
| «لحظيٌّ بلا تاريخ — خارج النسخة الأولى» | 40 |
| «ليست شارتًا» | 16 |
| «بلا رقمين مقروءين» | 10 |
| «الرمزُ غيرُ مؤكَّد في الكاتالوج» | 0 |

## الصنف (للمعيار `K2`) — بقاعدة `chart_cards/real/README.md` نفسِها
| الصنف | القاعدة | العدد |
|---|---|---|
| `daily_axis` | يوميٌّ/أسبوعيٌّ **ظاهرٌ نصًّا** (Daily · 1D · Weekly · أو «Daily» في لوحة نمطٍ مؤرَّخة) ورقمان مقروءان | **46** (‏1D ‏34 · 1W ‏12) |
| `unknown` | الفريمُ غيرُ ظاهر (قصٌّ بلا وسم) — **لا يُفترض يوميًّا ولو بدا كذلك** | **29** |
| `intraday` | لحظيٌّ **بتاريخٍ مقروء** | **0** (‏لم يقع لحظيٌّ مؤرَّخ · والأربعون اللحظيّةُ كلُّها بلا تاريخ فاستُبعدت) |

⇒ **`K2` هذه المرّة فوق حدّه (46 ≥ 20) فيُحكم عليه بنصّ §⑤** · و`K1` يُحكم عليه إن بلغ «واثق» 10 (‏`CP8`).
**والمسارات:** 3 بطاقاتٍ بمرساةٍ يوميّةٍ مؤرَّخة من لوحة النمط (`TG_2116` · `TG_1929` · `TG_1871`) · بطاقةٌ بنافذةٍ مؤرَّخة
(`TG_2016`) · و**71 بلا تاريخ** (لوحةُ السوق — §②).

## قواعدُ البناء — كُتبت قبل فتح أيّ صورةٍ من الموضع 49
الأوقات (UTC · 2026-09-24): القواعد 1-8 **22:13:52** · القاعدة 9 **22:14:26** · **القاعدة 10 أُضيفت أثناء القراءة 22:36:57** (مُعلَنة).
1. **المعينُ والترتيب:** الصفوفُ الـ189 بترتيب sha256(المعرّف+"20260924") · من الموضع 49 حتى 189 · بلا حدٍّ أعلى.
2. **القبول (نطاقُ النسخة الأولى بنصّ §⑩-2):** يوميٌّ/أسبوعيٌّ **ظاهرٌ نصًّا** ورقمان مقروءان ⟵ `daily_axis` · فريمٌ غيرُ ظاهر
   ورقمان ⟵ `unknown` (لا يُفترض يوميًّا) · لحظيٌّ (دقائق/ساعات ظاهرة · أو جلسةٌ ممتدّةٌ مظلَّلة) **بتاريخٍ مقروء** ⟵ `intraday`.
3. **«تاريخٌ مقروء» للّحظيّ:** يومٌ وشهرٌ **وسنةٌ** ظاهرةٌ لبيانات الشارت نفسِها · **وتاريخُ نشر التغريدة ليس تاريخَ الشارت** ·
   وشهرٌ/يومٌ بلا سنة ليس تاريخًا (الإكمالُ بالاستنتاج ممنوع).
4. **أسبابُ الاستبعاد أربعةٌ لا غير** (الجدول أعلاه).
5. **«رقمان مقروءان»:** من {أعلى الشاشة · أدناها · آخرُ سعر · حقول المرساة} **نصًّا** (وسمٌ/ترويسة) أو من البكسل بتسامحٍ مصرَّح ·
   **وما كان غامضًا يُسقط ولا يُخمَّن**.
6. **البطاقةُ بلا رمز** (`validate_card`) · وكلُّ حقلٍ كما قُرئ · وحقولُ «لا تقرؤها الأداة» تُسجَّل ولا تُستعمل.
7. **الصنفُ بقاعدة الأولى نفسِها.**
8. **لا بحثَ ولا تشغيلَ على أيّ بطاقةٍ قبل التجميد والدمج في main.**
9. **حالاتٌ لم تقع في الأولى:** الشهريُّ الظاهر كالأسبوعيّ · صورةٌ بشارتين ⟵ الأكبر (أو الأعلى) · رمزٌ ظاهرٌ يخالف الكاتالوج ⟵
   يُستبعد · صورةٌ مطابقةٌ لأخرى (المصدرُ أو اللحظةُ نفسُها) ⟵ **تُقبل وتُعلَن** لا تُستبعد.
10. **(أُضيفت عند `TG_1833` · تُعلَن):** شارتان متساويان جنبًا إلى جنب ⟵ **الأيسرُ هندسيًّا** · والبديلُ «المرفقُ أوّلًا» يعطي
    القرارَ نفسَه في كلّ حالات التساوي المرئيّة حتى إضافتها (`TG_1964` · `TG_1833` · `TG_1963`) ويتغيّر السببُ وحدَه في `TG_1833` ·
    وطُبِّقت بعدها على `TG_2019` و`TG_2091` (استُبعدتا بسبب الأيسر «بلا رقمين» — والأيمنُ في `TG_2091` لحظيٌّ بلا تاريخ على أيّ حال).

## ما يُعلَن قبل التشغيل (حدودُ صدق)
1. **صورٌ غيرُ مستقلّة** — تُعدّ كلٌّ بطاقةً كما ينصّ العقد، ويُطبع أثرُها:
   - **داخل هذه المجموعة (اللحظةُ نفسُها أو اللقطةُ نفسُها):** `TG_1981`/`TG_1983` (شارتٌ واحد) · `TG_1822`/`TG_1923` ·
     `TG_2069`/`TG_2068` · `TG_1933`/`TG_1986` (لحظةُ البري) · `TG_1871`/`TG_1866` · `TG_1869`/`TG_1887`/`TG_1888` ·
     **ونسخٌ مطابقةٌ للّقطة نفسِها:** `TG_2114`/`TG_2110` · `TG_1945`/`TG_1912` · `TG_2017`/`TG_2062` (بدقّةٍ أخرى).
   - **مع المجموعة الأولى (المحترقة):** `TG_2016`↔`TG_2067` · `TG_1988`↔`TG_1930`/`TG_2080` · `TG_1936`↔`TG_1937`/`TG_1997` ·
     `TG_2060`↔`TG_2029`/`TG_2032`/`TG_2028`.
   ⇒ **الفعّالُ داخلها ≈65 لا 75** · والسهمُ نفسُه بلحظاتٍ أخرى (ENPH · ELAB · GWAV · TRUG · CDIO …) **ليس عدمَ استقلال** ويُذكر في `SELECTION.jsonl`.
2. **الرمزُ ظاهرٌ في الصورة** (ترويسةُ موموو غالبًا · وأسطورةُ التطبيق الداكن في `TG_2014`/`TG_1888`/`TG_2023`) — **ولا يدخل البطاقة**؛
   والتسرّبُ العامّ معلَنٌ في العقد §⑧-1 (الاختبارُ الأعمى الأقوى بيد المالك).
3. **حقولٌ مسجَّلةٌ لا تقرؤها الأداة:** `axis_md` (شهر/يوم بلا سنة · وأحدُها «01/02/2026» في `TG_2115` — **ولا نافذةَ منه** لأن طرفيها
   يُستنتجان من مواضع الوسوم ولا يُقرآن) · `last_bar` (شمعةٌ بلا تاريخ) · `anchor_src` (نصُّ لوحة النمط) · `pattern_date`.
4. **قيمٌ مشتقّة (آخرُ سعرٍ من ترويسة «بعد الإغلاق/قبل الافتتاح» ونسبتها) — 17 بطاقة** موسومةٌ «مشتقٌّ» في `SELECTION.jsonl`.
   و**في `unknown` قرائنُ لحظيٍّ لا تُفترض** (القاعدة 2): `TG_2088` وسمُ السعر = سعرُ الأفتر · `TG_2045` آخرُ سعرٍ = سعرُ البري وقفزتُه.
5. **قراءاتُ بكسل بتسامحٍ مصرَّح — 4 بطاقات:** `TG_1844` (الأدنى) · `TG_2014` (الأعلى تحت نصّ الأسطورة) · `TG_1888` (الطرفان
   بمعايرةٍ مُتحقَّقة من جسم الشمعة الأخيرة) · `TG_2023` (الطرفان) — **وما كان غامضًا أُسقط** (أدنى `TG_2014` · طرفا `TG_1891` فاستُبعدت).
6. **عددُ الشموع:** تقديرٌ صريح في `TG_1857` (≈168) · `TG_1887` (≈113) · `TG_2098` (≈36) · `TG_1882` (≈80) (الوسطى مخفيّةٌ أو
   الشموعُ ملتصقة) · **ومحذوفٌ** حيث لا يُقرأ (`TG_2014` · `TG_2030`) فيُترك للمدى الافتراضيّ · والباقي عدٌّ بالبكسل (مكبَّرًا حين لزم).
   **والشمعةُ المقصوصة عند الحافة تُعدّ** — وفي `TG_2050`/`TG_1869` هي أعلى من وسم القمّة (الوسمُ للشموع الكاملة).
7. **إفصاحٌ عن طريقة القراءة:** (أ) **فُتحت `TG_2029` سهوًا** عند الموضع 141 — وهي من المجموعة الأولى المجمَّدة لا من هذا المعين ⟵
   لا أثر. (ب) **البحثُ عن رمز الكاتالوج طبع صفَّ الكاتالوج كاملًا** (وفيه أرقامٌ من قراءةٍ سابقة) للبطاقات حتى `TG_2014` (الموضع 136)
   وأحيانًا قبل كتابة البطاقة · ومن `TG_2081` (137) **يُطبع الرمزُ وحدَه** — والأرقامُ المكتوبة في كلّ تلك البطاقات **وسومٌ نصّيّةٌ ظاهرةٌ
   في الصورة** عدا قراءات البكسل في البند 5.
