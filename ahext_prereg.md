# 🌙⛰️ `T-AHEXT` — «المشتعلُ في الافتر مسقوفٌ في البري»

> **تسجيلٌ مسبقٌ مدفوعٌ قبل أيّ سطرِ أداةٍ وقبل أيّ رقم.** أمرُ المالك «سجّل السقف»
> (‏2026-09-10) بعد أن أرسل **تفريغَ الملفّ الصوتيّ** الذي كان موسومًا في `CLAUDE.md`
> «لم يُفرَّغ». **ولا يُغيَّر هذا العقدُ بعد صدور أوّل رقم** — أيُّ ملحقٍ يُدفَع مؤرَّخًا
> وقبل قراءة الأرقام، ويُعلَن في النتيجة.

---

## ⓪ المصدرُ ووسمُه — يُقال أوّلًا لأنه يحدّ الحكم

الدعوى من **تفريغٍ آليٍّ** لجلسةٍ صوتيّةٍ يشرح فيها فيصل سلوكَ الافتر/البري/الماركت.
الجملةُ الحاكمة:

> «اذا كان صعد بالافتر حده 100 بالمئة مجمل الى 120 مجمل ما يتجاوزها … ما يروح بعيد
> بالبريء. اذا ما كان صعد دفعة واحدة في البريء يختلف. دفعة واحدة في البريء يجيب 300
> اربعمية خمسمية في المئة. **لكن امتداد للافتر لا مستحيل** … ما شفته تجاوز اكتر مئة
> بالمئة بالبريء، السهم الصاعدة اللي في الافتر.»

🔴 **ووسمُ الدفتر `faisal_adopted` لا `faisal_verbatim`، وهذا قيدٌ لا تفصيل:**
التفريغُ آليٌّ ومشوَّشٌ في مواضعَ مقيسة (‏«تسعين/92» · «شذكا» · «طاخ طيخ») ⇒ **الصياغةُ
منقولةٌ والمعنى واضحٌ، لكنّ الحرفَ غيرُ مضمون.** ولذلك:

- **الرقمان 100 و120 لا يُشحنان عتبةً** — يُقاسان (‏`AX3` أدناه يقرأ المظروف كما
  فعل `envelope_p90`)، فإن طابقا فذاك سندٌ، وإن خالفا فالمقيسُ هو الحاكم.
- **ولا يُبنى على التفريغ وحدَه قرارُ إنتاج** — هذي تجربةُ قياسٍ لا شحن.

## ⓪-ب السؤالُ التشغيليُّ الحقيقيّ خلف الدعوى

تسليمُنا الحيُّ اليوم (‏`PRESESSION_SEND: PM` · مِرساةُ `R1 ∪ T-C` · مفتاحُ ترتيب
البريماركت) **يعامل كلَّ متحرّكي البري سواءً**. فإن كان «امتدادُ الافتر» **مسقوفًا**
فهو مرشّحٌ رديءٌ بطبيعته، و«الطازج» هو الذي يستحقّ الرسالة.
⇒ **الفرقُ إن ثبت رافعةُ ترتيبٍ/تسليم، وإن سقط فقد أُغلق محورٌ كان سيُغري.**

🔎 **ومحورٌ جديدٌ فعلًا لا إعادةَ فتح:** فحصٌ بالكود على المستودع كلِّه أعطى **صفرَ
مطابقة** لأيّ تمييزٍ بين «صعد بالافتر» و«طازجٌ في البري» — لا حقلَ ولا وسمَ ولا سطر.

---

## ① الكونُ والنافذةُ والتعاريف — تُثبَّت هنا فلا تُختار بعد الأرقام

- **السنوات:** 2023 · 2024 · 2025 (‏حدُّ المشروع الأدنى ثلاثُ سنوات — سنتان انقلبتا
  علينا مرّتين: `T-SHORT` · `T-CMAG`).
- **الكون:** رموزُ اللقطة المجمَّدة نفسِها التي تمشيها أدواتُنا (‏`load_frozen_dataset`).
- **نطاقُ السعر:** `prev_close` بين **‏0.30$ و20$** — سياقُ فيصل صغارُ القيمة، والنطاقُ
  **هندسيٌّ مُعلَن** لا فيصليّ.
- **`prev_close`** = إغلاقُ **الجلسة النظاميّة** لليوم `D-1` (‏لا آخرُ طبعةٍ ممتدّة).
- **نافذةُ الافتر** = `D-1` من **‏16:00 إلى 20:00 بتوقيت نيويورك**.
- **نافذةُ البري** = `D` من **‏04:00 إلى 09:30 بتوقيت نيويورك**.
- **`ah_rise`** = ‏(أعلى سعرٍ في نافذة الافتر ÷ `prev_close`) − 1.
- **`pm_peak`** = ‏(أعلى سعرٍ في نافذة البري ÷ `prev_close`) − 1.
  🔑 **والمرجعُ `prev_close` قراءةٌ مُعلَنةٌ لكلمة «مجمل»** — وهي **الأرجحُ** لأن فيصل
  يقارن مسارَين يبدآن من القاعدة نفسِها. **والقراءةُ الثانية** (‏المرجعُ إغلاقُ الافتر)
  **تُحسب وتُطبَع وصفيًّا `AX4`** ولا تحكم — فغموضُ المصدر يُظهَر ولا يُحسَم لصالحنا.
- **دخولُ المجتمع:** صفٌّ يدخل إن كان **`pm_peak ≥ 20%`** (‏«متحرّكُ بريماركت» —
  عتبةٌ هندسيّةٌ مُعلَنة).

## ①-ب القمعُ والميزانيّة — تُثبَّت قبل التشغيل وتُطبَع في المُخرَج

**المشيُ على دقائق كلِّ رمزٍ في ثلاث سنوات مستحيلُ الكلفة** ⇒ قمعٌ من مرحلتين:

1. **غربالٌ يوميٌّ رخيص** من اللقطة: `gap = open(D)/close(D-1) − 1 ≥ GAP_MIN = 15%`.
2. **جلبُ دقائق** للمرشّحين وحدَهم، نداءً واحدًا يغطّي `D-1 16:00 → D 09:30`.

- **سقفٌ سنويٌّ `CAND_CAP = 2000` صفًّا**، والعيّنةُ عند التجاوز **حتميّةٌ** (‏ترتيبٌ
  بـ`sha256(symbol|date)` وأخذُ الأوائل) — **لا عشوائيةَ بلا بذرة** ولا قصٌّ صامت:
  المُسقَطُ يُطبَع بعدده.
- **الميزانيّةُ والنافذةُ والسقفُ واحدةٌ للذراعين بالبناء** (‏درسُ `T-CLIFF`: ذراعان
  بميزانيتين يقيسان الميزانيةَ لا الذراع).

🔴 **وعيبُ الغربال يُعلَن قبل أن يُقاس لا بعده:** يومٌ ارتفع في البري ثمّ **ذاب قبل
‏09:30** يعطي `gap` صغيرًا فيسقط من الغربال — **وذوبانُ البري هو ما يصفه فيصل نفسُه**
(‏«يرجع تداول عند خمسين في المئة») ⇒ الغربالُ **منحازٌ ضدّ الذائبين**.
⇒ **`AX5` مِجَسُّ الذوبان:** عيّنةٌ حتميّةٌ من **‏300 صفًّا** بـ`gap` في [0%, 15%) تُجلَب
دقائقُها ويُقاس فيها **كم منها `pm_peak ≥ 20%`** ⇒ **معدّلُ الفوات مقيسٌ لا مفترَض**،
وإن تجاوز **‏25%** فالحكمُ الفرعُ 3 (لا حكم) مهما كانت الأرقامُ الأخرى.

---

## ② الذراعان — مُفكَّكتان ومستوفيتان

| الذراع | التعريف |
|---|---|
| **`E-AH`** | `ah_rise ≥ AH_MIN` (**‏20%** — هندسيّةٌ مُعلَنة: «صعد بالافتر» يحتاج حدًّا) |
| **`E-FRESH`** | `ah_rise < AH_MIN` |

**و`E-AH ⊎ E-FRESH` = المجتمعُ كلُّه** — يُقرأ **سلوكيًّا** (‏`V-A5`) لا بالثقة.

---

## ③ المقاييس — حاكمةٌ ووصفيّةٌ مفصولتان

**الحاكمة:**

- **`AX1`** — `P(pm_peak > 120% | E-FRESH) − P(pm_peak > 120% | E-AH)` **موجبٌ في
  السنوات الثلاث**، وفاصلُ فرقِ النسبتين المجمَّع **لا يلمس الصفر**.
- **`AX2`** — الفرقُ نفسُه **يتجاوز ضجيجَه (‏≥2σ) في سنتين من ثلاث** على الأقلّ.

**الوصفيّة (تُطبَع ولا تحكم):**

- **`AX3`** — **المئينُ 90 لـ`pm_peak` داخل `E-AH`**: أين يقع من ‏100-120% التي قالها؟
- **`AX4`** — القراءةُ الثانية لـ«مجمل» (المرجعُ إغلاقُ الافتر).
- **`AX5`** — معدّلُ فوات الغربال (أعلاه · **وهو حاكمٌ سلبًا**: تجاوزُه 25% ⇒ لا حكم).
- **`AX6`** — حصّةُ `E-AH` من المجتمع (يجيب: هل الامتدادُ نادرٌ أصلًا؟).

**حدُّ العيّنة:** سنةٌ فيها **أقلُّ من 100 صفٍّ في أيٍّ من الذراعين لا تدخل `AX1`/`AX2`**
— **والنطاقُ منصوصٌ هنا وفي كلّ بندٍ يستعمله** (‏درسُ `T-UNIVERSE`: نصُّ حدِّ العيّنة
في بندٍ واحدٍ أنتج ثغرةً في عقدي اضطررتُ لإعلانها بعد الأرقام).

---

## ④ قاعدةُ القراءة — ثلاثةُ فروعٍ مكتوبةٌ قبل أيّ رقم

- **الفرعُ 1 — تثبت:** `AX1` ✅ **و**`AX2` ✅ **و**`AX5` ‏≤25% ⇒ «امتدادُ الافتر مسقوفٌ
  مقارنةً بالطازج» ⇒ **يُقترَح على المالك** وسمُ عرضٍ أو مفتاحُ ترتيب — **ولا يُشحَن
  بهذي التجربة** (تُشحن بأمرٍ مستقلّ).
- **الفرعُ 2 — تفشل:** الفرقُ يسقط عن أحد المعيارين أو تنقلب إشارتُه بين السنوات
  ⇒ **لا تمييزَ** ⇒ **يُغلَق المحورُ** بشرطِ فتحٍ مؤرَّخ.
- **الفرعُ 3 — لا حكم:** حدُّ العيّنة غيرُ مستوفًى في **سنتين فأكثر**، **أو** `AX5`
  يتجاوز 25%، **أو** تغطيةُ دقائق Polygon للنافذتين دون **‏80%** ⇒ **يُعلَن «لا حكم»
  ولا يُقرأ نفيًا** (‏درسُ `T-METHOD`: ‏27 دون 30 = لا حكم لا فشل).

---

## ⑤ أقفالُ الصحّة `V-A1`..`V-A8` — لكلٍّ طفرةٌ تُسقطه

| القفل | ما يضمنه |
|---|---|
| `V-A1` | `prev_close` = الإغلاقُ **النظاميّ** لا آخرُ طبعةٍ ممتدّة |
| `V-A2` | النافذتان **بتوقيت نيويورك مع التوقيت الصيفيّ** لا UTC مثبَّتًا (عطبٌ مقيسٌ عندنا مرّتين) |
| `V-A3` | الافترُ لليوم `D-1` والبري لليوم `D` — **لا تسرّبَ بين الجلستين** |
| `V-A4` | **صفرُ نظرٍ مستقبليّ**: لا شيءَ بعد 09:30 من `D` يدخل تعريفَ ذراعٍ أو مقياس |
| `V-A5` | مجموعُ الذراعين = المجتمع **سلوكيًّا**، وتسريبُ صفٍّ واحدٍ يُسقط المساواة |
| `V-A6` | تسويةُ التقسيم **متّسقةٌ عبر حدّ الجلستين** (‏عطبُ `SPRB` ‏52.42 مقابل 3,933.96) |
| `V-A7` | **قراءةٌ فقط**: صفرُ كتابةِ حالة · صفرُ إرسالِ تلغرام · لا تُستورَد في الإنتاج |
| `V-A8` | اللقطةُ والمدى والسقوفُ **مثبَّتةٌ ومطبوعةٌ** في كلّ تقرير |

**وبوّابةُ سنةِ اللقطة (‏نمطُ `SNAP1`) إلزاميّة:** سنةُ اللقطة تطابق سنةَ القياس وإلّا
خروجٌ غيرُ صفريّ — وُلدت من عطبٍ أعطى **عشرَ مرّاتٍ فرقًا بخروجٍ صفريٍّ صامت**.

---

## ⑥ تنبّؤاتي — تُنشَر مكذَّبةً كانت أو مؤكَّدة

سجلّي الأخيرُ **‏4 من 5 مكذَّبة** في `T-UNIVERSE` و**صفرٌ من 6** في `T-WAIT-RSI27`
⇒ أكتبها **متوقّعًا الخطأ** لا مدافعًا عنها:

- **`P1`** — `P(>120% | E-AH)` **دون 10%**.
- **`P2`** — الفرقُ `E-FRESH − E-AH` **‏10 نقاطٍ فأكثر في السنوات الثلاث**.
- **`P3`** — المئينُ 90 لـ`E-AH` يقع **داخل [80%, 140%]** (‏أي رقمُ فيصل داخلَ مظروفنا).
- **`P4`** — **أغلبُ متحرّكي البري ليسوا امتدادَ افتر** (‏`E-FRESH` هي الأكثريّة).
- **`P5`** — الأثرُ يصمد بعد تقييدِ السعر والفلوت.

---

## ⑦ حدودُ صدقٍ مُعلَنةٌ قبل الأرقام

1. **المصدرُ تفريغٌ آليّ** ⇒ الحرفُ غيرُ مضمون (‏§⓪).
2. **«مجمل» غامضة** ⇒ قراءتان، الأولى تحكم والثانية تُطبَع (`AX4`).
3. **الغربالُ أعمى عن الذائبين** ⇒ `AX5` يقيس الفوات، وتجاوزُه 25% يُلغي الحكم.
4. **تغطيةُ Polygon للممتدّ عند المغمورين قد تكون رقيقة** ⇒ تُطبَع، ودونَ 80% لا حكم.
5. **انحيازُ البقاء قائم** (كونُ اللقطة) ⇒ الأثرُ **حدٌّ أدنى** لا مطلق (‏درسُ `T-UNIVERSE`).
6. **‏300-500% للطازج ليست موضوعَ الحكم** — تُطبَع وصفيًّا؛ الحاكمُ **الفرقُ** لا الأسقف.
7. **ولا شيءَ من هذي التجربة يمسّ الإنتاج** — لا عتبةَ ولا `LOGIC_VERSION` ولا تسليم.

---

## ⑧ العواقب

| الفرع | ما يقع |
|---|---|
| 1 | **اقتراحٌ للمالك** بوسمٍ أو مفتاحِ ترتيب — **بلا تنفيذٍ حتى يأمر** |
| 2 | **إغلاقُ المحور** بشرطِ فتحٍ مؤرَّخ: مصدرٌ ثانٍ للدعوى **أو** تعريفٌ مختلفٌ مسجَّلٌ مسبقًا |
| 3 | **«لا حكم» يُعلَن بعدّاده** — ولا يُقرأ نفيًا ولا إثباتًا |

**والنتيجةُ تُكتب في `ahext_result.md` بجانب هذا العقد لا فوقه.**

---

## ⑨ ملاحقٌ مؤرَّخةٌ — **‏2026-09-10 · قبل بناء الأداة وقبل أيّ تشغيلة ورقم**

> أربعةُ تصحيحاتٍ خرجت من **قراءةٍ خصوميّةٍ لكودي قبل أن يدخل المستودع**، لا من
> رؤية أرقام. تُدفَع مع العقد فيبقى السجلُّ صادقًا.

**‏1 — مقدِّرُ `AX5` يُعمَّم ولا يُقرأ خامًا.** نصُّ §①-ب قال «كم منها `pm_peak ≥ 20%`»،
وهي **نسبةُ إصابةٍ داخل العيّنة** لا **حصّةُ الفائت من المجتمع**. المُنفَّذ:

    مُقدَّرُ الفائت = (إصاباتُ العيّنة ÷ حجمِها) × عددَ أيّام [0, GAP_MIN)
    الفوات%        = مُقدَّرُ الفائت ÷ (مُقدَّرُ الفائت + حجمِ المجتمع)

وهو ما يقابل الحدَّ 25% في §④ ⇒ **الحدُّ يُقاس على ما يعنيه لا على رقمٍ أسهل.**

**‏2 — «لا شمعةَ افتر» = صعودٌ صفر = `E-FRESH`، لا «تعذّرَ قياس».** 🔴 وهذا **أثقلُ
التصحيحات**: كودي الأوّل جعل النافذةَ الفارغةَ `None` فتُقصى من المجتمع — أي أنه
**يُقصي أنقى الطازجين** (سهمٌ لم يُتداوَل في الافتر أصلًا) ⇒ تصير `E-FRESH` «أسهمًا
تداولت في الافتر ولم تصعد» = **مجتمعٌ آخرُ يقلب معنى الذراع**. والتمييزُ محفوظ:
**تعذّرُ الجلب** (`bars is None`) يُسقط الصفَّ قبلها، **والنافذةُ الفارغةُ بعد جلبةٍ
ناجحة** حقيقةٌ سوقيّة. ويُطبَع `ah_bars` لكلّ صفٍّ فيُدقَّق.

**‏3 — سنةُ 2026 وصفيّةٌ خارج العيّنة.** تُشغَّل وتُطبَع **ولا تدخل `AX1`/`AX2`**
(سابقةُ `WR3` في `T-WAIT-RSI27`). والسنواتُ الحاكمةُ الثلاثُ كما هي.

**‏4 — `AX4` مرجعُه إغلاقُ الافتر لا قمّتَه.** نصُّ §① يقول «المرجعُ **إغلاقُ**
الافتر»، وكودي الأوّل استعمل **قمّتَه** ⇒ صُحّح بـ`window_last_close`.

🔒 **ولا يُبنى على هذي الملاحق أيُّ رقم** — الأداةُ لم تُشغَّل بعد.
