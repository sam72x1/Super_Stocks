# 🔓💧 `T-LIBVOL` — تسجيلٌ مسبق: **الكسرُ بسيولة**

> **أمرُ المالك (2026-08-28): «اعتماد ١ ٢ ٣ ٤».** العقدُ الرابع — **مدفوعٌ قبل
> سطرِ كودِ أداةٍ واحد** ولا يُمَسّ بعد أوّل رقم.

## ⓪ لماذا ليست إعادةَ فتحٍ ممنوعة

**نصُّ فيصل** (صورة `HCWB`، 2026-08-28): «**كم الدخول الاهم · اختراق 3.26
بسيوله ممكن يواصل**».

🔑 **والقاعدةُ شقّان، واختبرنا شقًّا واحدًا:** `T-LIBERATION` فشلت (‏`L1`
‏−0.62/−0.59/−0.48R) — **وفحصتُ عقدَها `liberation_prereg.md`: صفرُ ورودٍ
لكلمة سيولةٍ أو حجمٍ أو `vol_x`.** ذراعاها `L1` (أقرب حاجز) و`L2` (أعلى
مقاومة) **بلا أيّ شرطِ سيولة**.

⚖️ **وشرطُ إعادة الفتح المدوَّن هو «محورٌ مختلفٌ حقًّا»** — والشقُّ الثاني من
القاعدة نفسِها **لم يُقَس قطّ** ⇒ محورٌ مختلفٌ لا تحريكُ عتبة.

📌 **وليس تكرارًا لرادار الانطلاق:** `_ignition_signal` يقرأ **دقائقَ حيّةً**
داخل الجلسة (قفزةُ حجم ×3 + كسرُ الرقم الحرج) و`T-OPFIRE` قاست إنذارَه
الكاذبَ **‏67.6%**. وهذا يقيس **إغلاقًا يوميًّا فوق الحاجز مع حجمٍ يوميّ**
= قرارُ دخولٍ لا زنادُ لحظة.

## ① التعريفُ الإجرائيّ — مثبَّتٌ ولا يُحرَّك

- **الحاجز** = `L1` نفسُه حرفيًّا: `build_interpretation(r)["critical_number"]["price"]`
  (‏نفسُ تعريف `liberation_prereg §①` — بلا حرفٍ زائد).
- **الكسر** = **إغلاقُ** جلسةٍ فوق الحاجز خلال `BT_LIB_WAIT` = **20 جلسة**
  (‏الرقمُ **مُعادٌ** من العقد السابق لا مخترَع).
- **السيولة** = حجمُ جلسةِ الكسر **‏≥ `VOL_SPIKE_MULT` × متوسّطِ 20 جلسةً
  سابقة** (‏`VOL_SPIKE_MULT` عتبةٌ **قائمةٌ في `CONFIG`** — صفرُ رقمٍ مخترَع).
- **الوقف** = **وقفُ الأساس بلا تغيير** (عزلُ أثرِ الدخول عن الوقف — درسُ
  `T1b`/`T-STOP`).
- **الحسم** = `_resolve_arm` الإنتاجيّة نفسُها.

## ② الأذرع — ثلاثٌ لا رابعة

| الذراع | التعريف |
|---|---|
| **`V0`** | الأساس (التعبئةُ عند متوسّط الدفعات) — **مقصورٌ على نفس الإشارات المُعبَّأة في `V1`** |
| **`V1`** | كسرٌ **+ سيولة** (**الحاكمة**) |
| **`V2`** | كسرٌ **بلا** شرط سيولة = `L1` — **إعادةُ إنتاجٍ لـ`T-LIBERATION`** |

🔴 **والمقارنةُ مقترنةٌ إلزاميًّا** (`V0` على نفس المُعبَّأين) — إسقاطًا لـ«فخّ
الامتناع»: فرقٌ ناتجٌ عن مجرّد تفادي الخاسر بالامتناع **لا يُحتسَب حافّة**.

## ③ المقياسُ الحاكم

**`R` المحقَّق لكلّ صفقة** — لا نسبةُ النجاح (‏`T-LIBERATION` أعطت **‏87%
نجاحًا بتوقّعٍ سالب**، وهو أشدُّ درسٍ في المستودع).

## ④ المعايير — الأربعةُ تلزم معًا

1. `V1 − V0` **‏≥ +0.15R** مجمَّعًا (عتبةٌ مُعادة)
2. موجبٌ في السنوات الثلاث بلا انقلاب
3. فاصلُ 95% لا يلمس الصفر
4. أرضية **‏150 محسومةً مجمَّعًا و30/سنة**

## ⑤ بوّاباتُ الصلاحية

| البوّابة | الشرط | الإخفاق |
|---|---|---|
| **`LV0`** | **`V2` يُعيد أرقامَ `T-LIBERATION` المنشورة بت-بت** (‏−0.62/−0.59/−0.48R) | خروج **3** — عطبُ أداة |
| **`LV1`** | `V1` و`V2` **تفترقان** (شرطُ السيولة يقصّ فعلًا) | خروج **4** |
| **`LV2`** | نصيبُ `V1` من `V2` **بين ‏10% و80%** — وإلّا الشرطُ خامدٌ أو بلا أثر | يُعلَن ويُقاس |
| **`LV3`** | الحسمُ من `_resolve_arm` **بالاسم** · والوقفُ من الأساس (AST) | قفلُ سويّة |
| **`LV4`** | لا نظرَ مستقبليّ: الهدفُ من `filled+1` (‏اتفاقيةُ `F-L1`) | قفلٌ سلوكيّ |

## ⑥ التنبّؤات

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| `LB-P1` | **لا تبلغ +0.15R** — الميكانيكا نفسُها (دخولٌ أغلى بوقفٍ ثابت) | تبلغها |
| `LB-P2` | **نسبةُ النجاح ترتفع فوق 70%** والتوقّعُ لا يتبعها (نمطُ `T-LIBERATION`) | لا ترتفع |
| `LB-P3` | شرطُ السيولة يقصّ **‏40-70%** من كسور `V2` | خارجه |
| `LB-P4` | `V1` **أفضلُ من `V2`** ولو لم تبلغ العتبة (السيولةُ تُنقّي) | أسوأ أو مساوٍ |
| `LB-P5` | `LV0` يعبر | يسقط ⇒ عطبُ أداة |

## ⑦ سقفُ النجاح

**اقتراحٌ للمالك + سطرُ عرض** — ⛔ **ولا زنادَ دخولٍ آليّ ولا مسَّ
بـ`activation_state` ولا بالوقف ولا بالأهداف.**

## ⑧ حدودُ صدق

1. **`VOL_SPIKE_MULT` عتبةٌ قائمةٌ لغرضٍ آخر** — رقمُها مُعادٌ ودلالتُها هنا
   مختلفة (يُوسَم في دفتر المصادر كما وُسم `WALL_MIN_USD`).
2. **«بسيوله» بلفظ فيصل غيرُ مكمَّم** — لا رقمَ في نصّه؛ فالعتبةُ **اجتهادُنا**
   ولا تُنسَب إليه.
3. الحسمُ **بلمسة ذيلٍ بلا انزلاق** ⇒ سقفٌ متفائلٌ بالتساوي على الأذرع.
4. **`V2` إعادةُ إنتاجٍ لا إعادةُ فتح**: حكمُ `T-LIBERATION` قائمٌ ولا يُنقَض.
