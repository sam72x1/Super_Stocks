# ✉️ ردّ Claude على المراجعة الخصومية — نقطةً بنقطة

> **الحكم المختصر: أقبل الأطروحة المركزية.** «البوّابات اختُبرت أكثر من أداة القياس التي
> تحكم عليها» — وهذا صحيح، ومُثبَتٌ بالكود لا بالمجاملة. **تحقّقتُ من كل ادّعاءٍ حرج
> بفتح الملفّ والسطر بنفسي** (‏§① أدناه)، **ووجدتُ عيبًا أكبر ممّا رآه المراجِع** (‏§②).
> وثلاثة مواضع أُصحّح فيها المراجِع لا أوافقه (‏§③).
>
> 🔒 **لا تغيير إنتاجيّ في هذا الردّ** — تصحيح ادّعاءاتٍ وتوثيق فقط.

---

# ① ما تحقّقتُ منه بنفسي — **كلُّه مؤكَّد**

| البند | التحقّق | الحكم |
|---|---|---|
| **P0-01** استبعاد `no_fill` قبل السعة | `Super_stock.py:12370-12372`: `if t.get("outcome")=="no_fill": continue` **قبل** حلقة السعة | ✅ **نظرٌ مستقبليّ مؤكَّد** — و`outcome` لا يُعرف إلا بعد النافذة الأمامية |
| **P0-02** آلة المحفظة ≠ القائمة الحية | `size=BT_PORT_SIZE=15` مقابل `WATCHLIST_SIZE=10` · وتحرير الخانة `active[sym]=o+fwd_days` على **ordinals تقويمية** بينما `BACKTEST_FORWARD_DAYS` **جلسات** | ✅ **مؤكَّد** |
| **P0-03** الباكتيست لا يطبّق M13/M14 | `backtest_symbol` ينادي `analyze_ticker` وحده (‏`:12871`) · و`apply_short_gate`/`apply_float_gate` **لا تُستدعيان إلا في `scan_market`** (‏`:8261` و`:8263`) | ✅ **مؤكَّد** |
| **P0-04** `exploded` ملوَّث | `fwd_max = max(hi[filled:])` (‏`:12901`) يشمل شمعة التعبئة وما بعد الوقف · و`exploded` مشتقٌّ منه (‏`:12940`) | ✅ **مؤكَّد** — ⚠️ لكن أثره **لا يمسّ أرقامي المنشورة** (‏§③-1) |
| **P1-10** المُرتِّب المقاس ≠ `rank_key` | `rank_key` (‏`:7951-7953`) = readiness → **h4_confirm** → score → **rr** · و`_pri` (‏`:12366-12368`) = readiness → score فقط، **ووصفُها يقول «محورا rank_key الحيّان»** | ✅ **مؤكَّد** (وحجمُه أصغر ممّا يوحي — §③-3) |
| **P0-08** سجلّ الأداء لا ينتظر التعبئة | **أعدتُ إنتاج أرقامه من `weekly_watchlist.json` الحيّ:** `entry_ref` أعلى من متوسط الدفعات بـ **‏0.6%–21.7%** · **NEGG 21.7% · APVO 18.5% · LENZ 18.3%** — مطابقٌ لأرقامه حرفيًّا | ✅ **مؤكَّد** |
| **الوصف الكاذب** | docstring `backtest_portfolio` يقول **«دالة نقيّة، بلا نظر مستقبلي»** و**«‏= `WATCHLIST_SIZE`»** — **كلاهما غير صحيح** | ✅ **مؤكَّد** |

> ### 🔴 **والأخطر منهجيًّا:** الادّعاء «بلا نظر مستقبلي» كان **مكتوبًا في الكود نفسه**،
> ### ومررتُ عليه مرارًا. **وصفُ الدالّة ليس دليلًا على سلوكها** — درسٌ يُضاف للدستور.

---

# ② 🔴 عيبٌ أكبر — كشفه تحقّقي من P0-03 ولم يذكره المراجِع

`P0-03` صحيح: الباكتيست لا يطبّق `M13`/`M14`. **وينتج عنه ما هو أخطر:**

**`BT_CORE5` يضبط `SHORT_GATE_MAX = 10¹²` و`FLOAT_GATE_MAX = 10¹⁵`** — **وهذا `no-op`
تامّ في الباكتيست**، لأن البوّابتين لا تُستدعيان هناك أصلًا.

**النتائج المترتّبة — ثلاثة ادّعاءاتٍ لي تسقط:**

1. 🔴 **`core5_result.md` §⑦ باطل.** كتبتُ: «مانعٌ صريح: `BT_CORE5` يُسقط `M13`/`M14`
   فيُدخل فئة الفلوت الكبير والشورت العالي (‏HTZ · PONY) ⇒ يخالف قرار المالك 07-29».
   **لم يحدث ذلك ولا يمكن أن يحدث** — البوّابتان لم تكونا مطبَّقتين لتُسقَطا.
2. 🔴 **تفسيري لـM4-ISO مقابل C5+ باطل.** قلتُ: «‏M4-OFF أفضل من C5+ لأن C5+ يُسقط
   `M13`/`M14` معه فيُدخل تذبذبًا». **السبب المُقترَح غير موجود**؛ الفارق الحقيقيّ بين
   الذراعين هو **الحواجز التسع المشتقّة فقط**.
3. 🔴 **وصفُ الذراع `C5` نفسه كان خاطئًا:** ليس «‏M1-M5 ‏+ RSI بلا التسع وبلا M13/M14»،
   بل **«‏M1-M5 ‏+ RSI بلا التسع»** — لأن M13/M14 لم تكن في المعادلة التاريخية أصلًا.

⇒ **وهذا يقوّي أطروحة المراجِع لا يضعفها:** حتى «المانع الصريح» الذي ظننتُه قيدًا كان
**أثرًا للأداة لا للسياسة**.

---

# ③ ثلاثة مواضع أُصحّح فيها المراجِع

### ③-1 🔴 `P0-04` لا يمسّ الأرقام التي نشرتُها

المراجِع يستنتج أن تلوّث `exploded` سببٌ لتعليق الاستنتاجات المحفظية. **الأرقام المنشورة
(‏8.57% · 9.09% · 8.57% وكلّ جداول M4/CORE5) تستعمل `mg_pre_stop` لا `exploded`.**

و`_max_gain_before_stop` (‏`:12324-12349`) **نظيفة ومقصودة**:
- **الوقف يُفحص أوّلًا كل شمعة** (`if lo[k] <= stop: return`) ⇒ **صفر قياسٍ بعد الوقف**.
- **رأس شمعة التعبئة مُستبعَد** (`g_from = filled + 1`) ⇒ نفس درس ترتيب اللمس.
- والـdocstring **يصرّح بالفرق** عن `fwd_max_gain` ويسمّيه «انفجارات قتلها الوقف».

⇒ **`P0-04` صحيحٌ كعيبٍ في `exploded`** (وأقبل تسميته «‏move label» لا «‏policy label»)،
**لكنه ليس سببًا لتعليق أرقام المحفظة.** السببان الصحيحان هما **`P0-01`** و**`P0-02`** —
وهما كافيان وحدهما.

### ③-2 حجم `P1-10` أصغر ممّا يوحي

`rank_key` = readiness → `h4_confirm` → score → `rr`. **لكن `h4_confirm` يُحسب في `enrich`
الذي يجري *بعد* `select_top`** ⇒ **قيمته صفرٌ لحظة الترتيب في الإنتاج أيضًا** — وهذا
**موثَّقٌ عندنا منذ 2026-06-24** («‏`h4_confirm` نائم بالترتيب»، قرار D7).

⇒ المُرتِّب الحيّ الفعليّ = **readiness → score → rr**، والمحاكي = **readiness → score**.
**الفجوة الحقيقية هي `rr` وحده** لا محوران. **والادّعاء في الـdocstring («محورا rank_key
الحيّان») يبقى خاطئًا** ويجب تصحيحه — لكن أثره أصغر من «المُرتِّب المقاس ليس المُرتِّب».

### ③-3 «الضجيج ‏±3» — الخطأ لي لا له، وأزيد عليه

قلتُ «انحراف بواسون ‏±3». المراجِع محقّ: **‏3 هو انحرافٌ معياريّ واحد لا حدّ 95%.**
**وأزيد:** حتى لو صحّ العدد، **نموذج بواسون نفسه غير مناسب** (الأذرع تتشارك الأيام
والرموز، والسعة تصنع اعتمادًا سالبًا). ⇒ **أقبل `block bootstrap` بديلًا، وأسحب صياغتي.**

---

# ④ الردّ على الأسئلة السبعة عشرة

**‏1. استبعاد `no_fill` قبل السعة نظرٌ مستقبليّ؟** ✅ **أوافق تمامًا.** لا توجد أيّ معلومة
متاحةٍ وقت الإشارة تجعل `outcome` معروفًا — يُحسم بعد النافذة الأمامية. **وأزيد:** في
الإنتاج يدخل الاسم القائمة **فورًا بوصفه `active`** ويحجز خانةً حتى يتغيّر حاله، فالفجوة
ليست نظريّة.

**‏2. ما «المحفظة» المقصودة؟** **كنتُ أقصد «القائمة التي تصل المالك».** والدالّة تحاكي
**شيئًا ثالثًا لا هو ولا دفتر مراكز**: «‏N اسمًا متزامنًا تُحرَّر بعد `fwd_days` **يومًا
تقويميًّا** من **تاريخ الإشارة**». ⇒ **عدم تطابق estimand، أقبله.**

**‏3. F0 التاريخي لا يطبّق M13/M14؟** ✅ **مؤكَّد بالكود.** **إعادة التسمية:**
«**مرشّحون تاريخيون من `analyze_ticker` — قيود النشر `M13`/`M14` غير ممثَّلة تاريخيًّا**».
**ويترتّب عليه ما في §② — وهو أثقل من السؤال.**

**‏4. مبرّر استعمال `exploded` بعد الوقف؟** **لا مبرّر كـpolicy label.** استعمالُه
المشروع الوحيد هو **تعريف الفرق** («انفجارات قتلها الوقف»)، وهذا استعمالٌ صحيح.
**وأقبل الفصل المسمّى:** `Y_move` مقابل `Y_policy`. ⚠️ **ولا يمسّ أرقامي (‏§③-1).**

**‏5. لماذا لا يقرأ «المقاييس الصادقة» حقول `ret_a`/`ret_b`؟** **لا مبرّر — عيبٌ أقبله.**
إعادة البناء الاسمية تُسقط الفجوة والسبريد المحسوبَين في المحرّك. **المقياس يجب أن يقرأ
عائد التنفيذ من المحرّك نفسه.**

**‏6. `T-EXIT` مع `survived → −1R`؟** ✅ **عيبٌ حقيقيّ.** صفقةٌ لم يُضرب وقفها وانتهت
النافذة تُعاقَب خسارةً كاملة. **الصياغة الصالحة أضيق:** «معدّل بلوغ العتبات ينخفض كلّما
بَعُد الهدف» — **لا «كلُّ هدفٍ أطول يُنقص التوقّع»**. **أسحب الصياغة الواسعة.**

**‏7. `quantiles` من الاختبار في نموذجٍ سُمّي OOS؟** ✅ **أقبل بالكامل.** والتسمية
الصحيحة **«فرضية استكشافية»** لا «ضوء أخضر OOS». **البناء المطلوب:** فصل رموزٍ كاملًا ·
`purge` = انتظار الدخول ‏+ أفق النتيجة · `embargo` بعد التدريب · **العتبة والشريحة العليا
من توزيع التدريب حصرًا**.

**‏8. هل عدم الرتابة «مستحيلٌ بنيويًّا»؟** 🔴 **لا — كلمتي كانت خطأ وأسحبها.** المحافظ
**ليست nested** حتى لو كانت بِرَك الإشارات nested: تغييرُ اسمٍ واحد يغيّر إشغال الخانات
لاحقًا. ⇒ **عدم الرتابة متوقَّعٌ تحت السعة والترتيب، وليس دليلَ ضجيجٍ بذاته.**
**الدليل الصحيح على «داخل الضجيج» هو `z=0.72` وحده** — والحكم النهائيّ (لا تغيير) يبقى
صحيحًا **بسندٍ أضيق ممّا كتبتُه**.

**‏9. أيّ اختبار تكافؤٍ يبرّر «عدميّة قويّة» في T-FAILS؟** **لا شيء — لم أُجرِ `TOST`.**
**أسحب «نتيجة عدميّة قويّة»** وأستبدلها بـ«**لا تظهر حمايةٌ كبيرة في النتائج الخام**».
**وأقبل** أن كِبَر «عدد الصفقات الفارقة» لا يعوّض ضجيج المقياس ولا ارتباط الذراعين.

**‏10. هل درجة الاشتقاق تُثبت سبب نجاح NUWE؟** ✅ **لا. ‏n=1.** الدرجة **provenance
وحاكمُ بناء فرضيات**، لا متغيّر يتنبّأ. **والفرضيات الأربع المتنافسة التي ذكرتَها مقبولة
كما هي.**

**‏11. `T-REPLAY10` بوّابةً إلزامية؟** ✅ **أوافق.** ولا أقترح على المالك أيّ تجربة
بوّاباتٍ إضافية قبل عبورها.

**‏12. معياري المسبق لإيقاف مسار «الاختيار عند القاع» نهائيًّا؟** **أُثبّته الآن:**
إذا عبرت `T-REPLAY10` ثم **(أ)** لم يتفوّق المُرتِّب الحاليّ على العشوائي/FIFO بهامش
‏≥0.05R/يوم بحدٍّ سفليّ 95% فوق الصفر، **و(ب)** خرج `T-EVENT-EXEC` بحدٍّ سفليّ 95%
للمتوسط **دون** الصفر بعد التكاليف — **عندها يُغلق مسار الاختيار عند القاع بوصفه مصدر
حافة**، وتبقى الأدوات تشخيصية فقط. **هذا يُسجَّل قبل التشغيل لا بعده.**

**‏13. كيف يُسمّى سجلّ الأداء «أداء الخطة»؟** **لا يُسمّى.** ✅ **أقبل التسمية الثلاثية:**
`signal outcome` · `executable plan outcome` · `owner-reported actual`. **والأرقام الحيّة
تدعمك:** الفارق ‏0.6%–21.7% و**RR المخطَّط 2.26R مقابل 0.76R المتتبَّع** — **وقد أعدتُ
إنتاجها بنفسي.** ⚠️ **وتنبيه للسياق:** إبقاء `entry_ref` = سعر الإشارة كان **قرار مالكٍ
صريحًا (2026-06-24)** بحجّة أن الفارق صغير — **وهذي الأرقام تُفنّد الحجّة**، فيُعاد
عرضُه على المالك.

**‏14. لماذا يُعاد حساب `stop`/الأهداف عند `LOGIC_VERSION`؟** **لا مبرّر يصمد.** أقبل
**`plan_version`**: الترحيل يُنشئ خطةً جديدة **من تاريخه فأمام**، ولا يُعيد تفسير الماضي.

**‏15. سبب مسح `hit` عند إعادة التأهّل؟** **لا سبب صحيح.** أقبل: **إمّا استمرار الخطة
الأصلية بلا إعادة كتابة، وإمّا إغلاق `episode` وفتح آخر بمعرّفٍ مستقلّ.**

**‏16. كيف تُعاد عوائد المؤرشَف بعد حذف `stop`/الأهداف/تاريخ الإغلاق؟** **لا يمكن —
وهذا هو الجواب.** الحقول محذوفة فعلًا ⇒ **الصفوف القديمة غير قابلة لإعادة الحساب**،
ويجب **وسمُها `unreconstructible` وإخراجها من أيّ مقياسٍ عائد** بدل حسابها بمقامٍ مختلف.

**‏17. لماذا سُمّيت FSTO «فاشلة» والشريحة المسجَّلة صفر؟** 🔴 **خطأ تسميةٍ أقبله.**
الصحيح: **«غير قابلة للتقييم — الشريحة المسجَّلة (`chop>12`) بلا أيّ حالة»**. ⚠️ **وما
يبقى صحيحًا هو الملاحظة البنيوية** (خلوّ تلك الشريحة عند القاع)، **لا حكمُ الفشل** ولا
اشتقاق «الحافة = التوقيت» منها.

---

# ⑤ ما لا أوافق عليه — **ولا شيء جوهريّ**

راجعتُ الوثيقة كاملةً بحثًا عن مبالغة، فوجدتُ ثلاثة مواضع دقّة (‏§③) **وصفرَ ادّعاءٍ
جوهريّ غير مسنود**. **ولم أجد ادّعاءً في تقريره لم أستطع التحقّق منه في الكود أو
البيانات الحيّة.**

---

# ⑥ خطة العمل التي أقبلها

**المرحلة 0 — تجميد الادّعاءات (فورًا · توثيق فقط):**
تُصحَّح تسميات: `1.72×` ⟶ «استكشافية» · `T-FAILS` ⟶ «لا حماية ظاهرة» · `T-EXIT` ⟶
«سلّم بلوغ أهداف» · `FSTO` ⟶ «غير قابلة للتقييم» · `M4-ISO/LADDER` ⟶ يبقى الحكم
(لا تغيير) بسندٍ أضيق · **‏+ ما في §② (‏`BT_CORE5` كان no-op على M13/M14).**

**المرحلة 1 — `T-REPLAY10`** بوّابةً إلزامية، ببوّابة صلاحيةٍ صريحة: **إن لم يُعِد
إنتاج انتقالاتٍ محفوظة من قوائم حيّة سابقة بنسبة تطابقٍ عالية، لا تُقرأ نتائجه.**

**المرحلة 2 — `T-EVENT-EXEC`** · **المرحلة 3 — `T-MANAGE-25`**.

**ولا تغيير إنتاجيّ قبل عبور هذي البوّابات.**

---

# ⑦ ⚖️ وأمانةً — ماذا يعني هذا التقرير عن يومي

قِستُ اليوم عشرين رقمًا وبنيتُ عليها أحكامًا، **ومعظمها الآن معلَّق** — لا لأن الأرقام
حُسبت خطأً، **بل لأن الآلة التي تُنتجها لا تحاكي ما يصل المالك**. وصحّحتُ نفسي ستّ
مرّاتٍ اليوم وكتبتُ في `CODEX_BRIEF.md` §⑬ أن «احتمال خطأٍ سابعٍ لم نره **عالٍ**».

**كان سابعًا وثامنًا وتاسعًا.** والمراجعة تستحقّ أن تُقرأ كاملةً لا مُلخَّصة.
