# 📡🩸 `T-SLIP-NBBO` — الانزلاقُ على **العرضِ القائم** لا الصفقةِ المجمَّعة (تسجيلٌ مسبق)

> **الأمر:** المالك 2026-08-27 **«قس NBBO»** — تنفيذًا للخطوة التي أوصى بها
> `slip_result.md §⑧` ونصَّ العقدُ السابق على أنها **تلزمها تسجيلةٌ مسبقةٌ
> جديدةٌ وإذنٌ صريح** — وقد أذن.
> 🔒 **الدفع:** هذا الملفُّ على `origin/main` **قبل سطرِ كودٍ واحد** من الأداة،
> وقبل أيّ تشغيلة، وقبل أيّ رقم.
> **قراءةٌ/قياسٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION`.**

---

## ⓪ ما الذي يضيفه هذا القياسُ على `T-SLIP`؟

`T-SLIP` قاست التعبئةَ من **شموع الدقيقة** — وهي **صفقاتٌ مجمَّعة**، وحدُّ
الصدق الأوّل في عقدها نصَّ: «التعبئةُ الحقيقيّة على `NBBO`، وإغلاقُ دقيقة
الزناد تقريبٌ منصفٌ **وليس التعبئةَ الحقيقية**».

وبقي فيها **موضعُ ضعفٍ أعلنتُه بنفسي:** `Q2` تسمح بتعبئةٍ **أعلى** من الوقف
حين ترتدّ دقيقةُ الزناد، وأمرُ وقفٍ سوقيّ لا يفعل ذلك ⇒ اضطررتُ إلى نشر
`Q2` و`Q2c` معًا لأن العقدَ ثبّت `Q2`.

🥇 **و`NBBO` يزيل الالتباسَ من أصله:** أمرُ الوقف السوقيّ يُنفَّذ على **أفضل
طلبٍ قائم** (`NBB`) لحظةَ إطلاقه. فالسعرُ المقيسُ هنا **هو التعبئة** لا
تقريبٌ لها — ولا حاجةَ لذراعٍ مقصوصةٍ ولا لاختيارٍ بين قراءتين.

## ⓪-ب ما أثبته مِجَسُّ الجدوى **قبل** هذا العقد (‏`nbbo_probe.py` · تشغيلة `33044844970`)

مِجَسُّ جدوى **بلا تسجيلٍ مسبقٍ وبسقفِ نجاحٍ صفر** (سابقة `nbbo_history_verdict`)
— لم يطبع **رقمَ نتيجةٍ واحدًا**، وأجاب أربعةَ أسئلةٍ تصميميّةٍ كانت ستُخمَّن:

| # | السؤال | المقيس | الأثرُ على التصميم |
|---|---|---|---|
| ① | كثافةُ الاقتباسات في دقيقة الزناد | وسيط **48** · p90 232 · أقصى 503 · **صفرٌ بلغ سقفَ 25,000** | لا خطرَ قصٍّ صامت · والسقفُ يبقى مع **عدّادِ قصٍّ** |
| ② | هل `/v3/trades` يحمل طابعًا زمنيًّا؟ | **نعم** (`sip_timestamp`) — و`event_exec.hist_trades` **يُسقطه** | ⇒ **لحظةُ الزناد تُحدَّد بالمِلّي** لا تُخمَّن داخل الدقيقة |
| ③ | الخام مقابل المعدَّل | **‏7 من 30 خارج ‏2%** · أدناها **‏0.0333 = تقسيمُ 1:30** | ⇒ **حارسُ مقياسٍ خامٍّ إلزاميّ** (‏`V2b` أدناه) |
| ④ | زمنُ النافذة | اقتباسات 0.29ث + صفقات 0.29ث ‏≈**0.58ث** | ⇒ سنةٌ في ‏≈8 دقائق داخلَ سقف الجوب |

🔴 **و③ وحدَه كان سيُفسد القياسَ صامتًا:** الشموعُ `adjusted=true` والاقتباسات/
الصفقاتُ **خامٌّ دائمًا** (لا وسيطَ تعديلٍ في `/v3/quotes`)، **وحارسُ
`scale_ok` القائم يقارن معدَّلًا بمعدَّل فلا يرى هذا الفرق إطلاقًا** ⇒ بلا
حارسٍ جديدٍ كنّا سنقارن وقفًا معدَّلًا بطلبٍ خام فنُخرج تعبئةً أبعدَ **ثلاثين
ضعفًا** ونحسبها انزلاقًا.

⚖️ **وأُعلن صراحةً أن المِجَسّ شكّل التصميم** (الحارس `V2b` · تحديدُ الزناد
بالمِلّي · نافذةُ الرجوع) — **ولم يُشكّل ولا عتبةً من عتبات الحكم**: المعايير
الأربعة والأرضياتُ **مُعادةٌ حرفيًّا من `slip_prereg.md §⑤` بلا تحريك.**

---

## ① السؤال المسجَّل

عند إطلاق الوقف، **ما أفضلُ طلبٍ كان قائمًا في تلك المِلّي ثانية؟** وهل يصمد
فارقُ «وقفه قاعه» (‏`P1` مقابل `P0`) حين تُقاس التعبئةُ على **العرض القائم**
بدل الصفقة المجمَّعة؟

## ② المصدرُ ونموذجُ التنفيذ

**نموذجُ أمر الوقف السوقيّ — خطوتان لا واحدة:**
1. **الإطلاق:** صفقةٌ تُطبَع عند الوقف أو دونه.
2. **التنفيذ:** الأمرُ يصير بيعًا سوقيًّا فيُنفَّذ على **`NBB` السائد في تلك
   اللحظة**.

⇒ **لحظةُ الإطلاق** من `/v3/trades` (‏`sip_timestamp` — مُثبَتٌ بالمِجَسّ) ·
**وسعرُ التنفيذ** من `/v3/quotes` (‏`bid_price` · `bid_size`).

🔒 **وإعادةُ استعمالٍ بالاسم — صفرُ منطقِ قياسٍ منسوخ:** المِشيةُ والخططُ من
`gold_entry_arms.walk_symbol_gold(..., with_plan=True)` · والجلسةُ والزنادُ
وحارسُ المقياس وحسابُ `R` والإحصاءُ من **`slip_arms`** بالاسم
(`session_slice` · `trigger_index` · `scale_ok` · `loss_r` · `_stats` ·
`_paired` · `fetch_day` · `PARMS` · `PUB`) · وشموعُ الدقيقة من
`event_exec.hist_minute_bars`. **الجديدُ الوحيد: جالبا الاقتباسات والصفقات
بطابعهما الزمنيّ** (‏`hist_quotes`/`hist_trades` الموجودتان **تُسقطان الطابع**
ولا تُمَسّان لأن أرقامَهما منشورة) — **ولا تحملان أيَّ منطقِ قياس.**

🔴 **نافذةُ الاقتباسات ترجع `NBBO_LOOKBACK_MS`=300,000 (خمسَ دقائق) قبل دقيقة
الزناد** — لأن `NBBO` **دالّةٌ درجيّة**: الطلبُ السائد لحظةَ `T` هو **آخرُ
اقتباسٍ عنده أو قبله**، وفي أسهمنا الرقيقة قد لا يتغيّر الاقتباسُ داخل الدقيقة
إطلاقًا (المِجَسّ: **‏3 من 30 دقيقةً بصفر اقتباس**). واشتراطُ اقتباسٍ **داخل**
الدقيقة كان سيُسقط تلك الحالات بلا سبب.

**ونافذةُ الجلسة النظاميّة وحدَها (‏09:30-16:00 نيويورك)** كما في `T-SLIP`
حرفيًّا — للسبب نفسِه.

## ②-ب حارسُ المقياس الخامّ `V2b` — الجديدُ الإلزاميّ

الخططُ (`avg`/`stop`) **معدَّلة** والاقتباساتُ **خام** ⇒ يُشتقّ لكلّ حدثٍ
**معاملُ تقسيمٍ** من دقيقة الزناد نفسِها بلا أيّ نداءٍ إضافيّ:

```
factor = آخرُ صفقةٍ خامّةٍ في دقيقة الزناد ÷ إغلاقُ الشمعة المعدَّل
```

**ويُتحقَّق بثلاث نِسَبٍ مستقلّة** (إغلاقٌ · قاعٌ · قمّة): تتّفق ضمن
`RAW_TOL`=2% ⇒ **مُتحقَّق** · وإلّا **يُستبعَد الحدثُ ويُعَدّ باسمه
`raw_scale_unverified`** — لا يُصحَّح ولا يُخمَّن (سابقةُ `T-OPFIRE`).

ثم تُردّ الاقتباساتُ إلى فضاء الخطة: **`fill = bid_raw ÷ factor`**.
⚖️ **و`R` لا يتأثّر بالفضاء أصلًا** لأن `loss_r = −(avg−fill)/(avg−stop)`
**متجانسٌ من الدرجة صفر** — ضربُ الثلاثة بمعاملٍ واحدٍ لا يغيّره؛ فالتحويلُ
**احتياطُ وضوحٍ لا شرطُ صحّة**، والحارسُ هو الشرط.

## ③ أذرعُ التنفيذ — أربعةٌ ولا خامسة (تُثبَّت الآن)

| الذراع | قاعدةُ التعبئة |
|---|---|
| **`N0`** | **عند مستوى الوقف بالضبط** — الافتراضُ النافذ · **بوّابةُ التكامل** |
| **`N1`** | **الحاكمة:** `NBB` **السائد لحظةَ أوّلِ صفقةٍ عند الوقف أو دونه** |
| **`N2`** | زمنُ التوجيه: `NBB` السائد بعد `NBBO_LAT_MS`=1000 مِلّي من تلك اللحظة |
| **`N3`** | أسوأُ حال: **أدنى طلبٍ** بين لحظة الإطلاق ونهاية دقيقة الزناد |

**والقراءةُ العامليّة مثبَّتةٌ سلفًا:** `N1−N0` = **أثرُ العرض القائم وحدَه**
· `N2−N1` = **أثرُ زمن التوجيه وحدَه** · `N3−N1` = **ذيلُ ما بقي من الدقيقة**.

⚖️ **ولا ذراعَ مقصوصة:** `NBB` **هو** سعرُ التنفيذ فلا معنى لقصّه عند الوقف —
وهذا بعينه ما كان يُلبِس `Q2`/`Q2c` التباسًا في `T-SLIP`.

**وحالاتٌ تُسمّى ولا تُخمَّن:**
- لا صفقةَ عند الوقف أو دونه داخل دقيقة الزناد ⇒ `no_trade_trigger`.
- لا اقتباسَ في النافذة كلِّها (دقيقةُ الزناد ‏+ خمسُ دقائقَ قبلها) ⇒ `no_quotes`.
- الطلبُ السائد **فوق** الوقف لحظةَ الإطلاق ⇒ **يُقاس كما هو** (تعبئةٌ أفضل)
  **ويُعَدّ وصفيًّا** — فهو معلومةٌ حقيقيّة لا عطل.

## ④ الأذرعُ المقيسة والمقياسُ الحاكم

يُعاد حسابُ `E` (متوسّطُ `R` لكلّ صفقةٍ محسومة) لـ**`P0` · `P1` · `P2`** تحت
كلّ ذراعِ تنفيذ — **بنفس تعريفات `T-SLIP` حرفيًّا** (الرابحةُ `+R_win` ·
والمُعبَّأةُ فقط في المقام).

**والمقياسُ الحاكم: `E(P1) − E(P0)` تحت `N1`.**

## ⑤ معاييرُ الحكم — **مُعادةٌ حرفيًّا من `slip_prereg.md §⑤` بلا تحريك**

1. الفارقُ **‏+0.15R فأكثر مجمَّعًا** تحت `N1`.
2. **موجبٌ في السنوات الثلاث** (لا انقلاب).
3. **فاصلُ 95% للفرق المقترن لا يلمس الصفر** في السنوات الثلاث.
4. **الأرضية:** ‏40 محسومةً لكلّ ذراعٍ لكلّ سنة و150 مجمَّعًا.

🔴 **والحكمُ مكتوبٌ الآن بطرفيه:**
- **عبرت الأربعة ⇒** الحافّةُ **تصمد على سعر التنفيذ الحقيقيّ** ⇒ يُرفع
  **اقتراحٌ** للمالك، ويُرقّى `slip_result.md` من «تقريبٍ منصف» إلى **مسنودٍ
  بالعرض القائم**.
- **سقط أيٌّ منها ⇒** الفارقُ المنشور **أثرُ افتراضِ تعبئةٍ لا حافّة** ⇒
  **يُغلَق الملفّ بلا اعتماد**، ويُصدَّر `gold_entry_result.md` **و**
  `slip_result.md` ببانرِ تقييد.

## ⑥ بوّاباتُ الصلاحية

- **`V0`** — `N0` يُعيد أرقامَ `gold_entry_result §①` **بت-بت** (تسعةُ مراسٍ
  سنويّة ‏+ المجمَّع `+0.174`/`+0.863`/`+0.584` والفارقُ `+0.689R`). **أيُّ
  تفرّقٍ ⇒ عطبُ أداةٍ لا نتيجة ⇒ خروج 3 ولا يُقرأ رقم.**
- **`V1`** حارسُ الـ`no-op`: عددُ الخسائر التي تغيّر سعرُ تعبئتها **تحت كلّ
  ذراع** يُطبَع. **صفرُ تغيّرٍ في `N1` ⇒ خروج 4** (بصمةُ `BT_CANDLE`).
- **`V2`** حارسُ المقياس المعدَّل — **`slip_arms.scale_ok` بالاسم** بلا تغيير.
- **`V2b`** **حارسُ المقياس الخامّ** (‏§②-ب) — ثلاثُ نِسَبٍ تتّفق ضمن 2%.
- **`V3`** **التغطية ‏≥90%** من أحداث الوقف. دونها ⇒ النتيجةُ **«جزئيّة»**
  ويُطبَع المستبعَدون **بأسبابهم المسمّاة** (‏`no_minutes` · `scale_mismatch`
  · `no_trigger` · `raw_scale_unverified` · `no_trade_trigger` · `no_quotes`
  · `quotes_truncated` · `budget_cut`). **ولا قصَّ صامتٌ إطلاقًا.**
- **`V4`** **تكاملٌ عبر الأدوات:** عددُ أحداث الوقف لكلّ سنة يطابق المنشورَ في
  `slip_result.md §①` (‏**4255 · 4902 · 4895**) وإلّا خروج 3 — فالمجتمعُ واحدٌ
  بالبناء والمقارنةُ مع `T-SLIP` مشروعة.
- **`V5`** أقفال السويّة `SN*` ‏+ جولةُ طفراتٍ كاملة: كلُّ قفلٍ يسقط مرّةً
  عمدًا، **وطفرةٌ تنهار لا تسقط = قفلٌ هشٌّ يُصلَّب**. وpreflight يرفض البدءَ
  عند مُشغِّلٍ آخرَ حيّ (‏`ps -eo pid,args`).

## ⑦ تنبّؤاتي المسجَّلة (قابلةٌ للكسر — تُنشَر ولو كُذِّبت)

- **`NP1`:** الطلبُ السائد لحظةَ الإطلاق **عند الوقف أو دونه في أكثرَ من 70%**
  من الأحداث (الصفقةُ التي تكسر الوقف بيعٌ على الطلب، فالطلبُ عندها أو دونه).
- **`NP2`** *(الأهمّ وأخطرُها عليّ)*: **الفارقُ تحت `N1` أعلى من `+0.566R`**
  المقيسِ تحت `Q2` — أي أن **شمعةَ الدقيقة كانت تقريبًا متشائمًا**، لأن `Q2`
  تأخذ **إغلاقَ الدقيقة كلِّها** بينما `N1` تأخذ **لحظةَ الإطلاق**. وأتوقّعه
  في نطاق **‏+0.55R إلى +0.72R**.
- **`NP3`:** زمنُ التوجيه (`N2−N1`) يكلّف **أقلَّ من 0.05R** مجمَّعًا.
- **`NP4`:** `no_quotes` بعد نافذة الرجوع **أقلُّ من 3%** من الأحداث.
- **`NP5`:** معاملُ التقسيم يختلف عن الواحد في **‏10% إلى 30%** من الأحداث
  (المِجَسّ رأى 23.3% **قبل** حارس `scale_ok`).
- **`NP6`:** **وسيطُ حجم الطلب** عند التعبئة **أقلُّ من 1000 سهم** — أي أن
  قمّةَ الدفتر رقيقةٌ عند الوقف (قراءةُ «العروض شبه المُفرَّغة» بالأرقام).

## ⑧ سقفُ النجاح — **مثبَّتٌ سلفًا وبلا تغيير**

مهما كانت الأرقام: **اقتراحٌ للمالك ‏+ سطرُ عرضٍ تشخيصيّ.** لا أتمتةَ دخولٍ
ولا خروج · لا تغييرَ خطةِ إنتاج · لا مسَّ بوّابةٍ ولا عتبة · **ولا مسَّ
`STOP_BELOW_LOW_PCT` ولا `kasih_scan.resolve` ولا صيّادِ المقسّم ولا أرقامِ
المالك (`LIQ_*`)** · ولا مسَّ أيٍّ من الجذور (تُفحَص ببصمة AST قبل كلّ دفعة)
· **ولا مسَّ `slip_arms.py` ولا `event_exec.py`** (أرقامُهما منشورة — سابقةُ
`CAP15`).

## ⑨ حدودُ صدقٍ تُكتب **قبل** الأرقام

1. 🔴 **مجموعةُ الخسائر ثابتةٌ ولا يُعاد تقريرُها:** الزنادُ يبقى **قاعَ شمعة
   الدقيقة المعدَّلة** كما في `T-GOLD-ENTRY`/`T-SLIP`. و`NBBO` **يُعيد تسعيرَ
   التعبئة فقط** ولا يقرّر **هل كان الوقفُ ليُطلَق أصلًا**. ⇒ وسيطُ الوقف
   المُطلَق بقاعدة «الطلبُ يبلغ الوقف» **مجتمعٌ آخرُ خارجَ النطاق** — يُعَدّ
   وصفيًّا ولا يدخل الحكم.
2. **`NBBO` قمّةُ الدفتر لا عمقُه:** مركزٌ أكبرُ من الحجم المعروض يمشي أعمق ⇒
   **الأرقامُ أرضيّةُ ضررٍ لا سقفُه** (كما في `T-SLIP`).
3. **بلا نمذجةِ طابورٍ ولا تعبئةٍ جزئية** — خروجُ المركز كلِّه بسعرٍ واحد.
4. **انزلاقُ الدخول والهدف خارجَ النطاق عمدًا** — وعدمُ التناظر على الهدف
   (‏17.17R لـ`P1` مقابل 5.15R لـ`P0`) **مُعلَنٌ ولا يُقاس هنا** ⇒ هذا القياسُ
   **لا يلتقط كلَّ ما يحابي `P1` في الافتراض**.
5. **إيقافاتُ التداول** تظهر فجوةً عند الاستئناف ولا تُميَّز.
6. **انحيازُ البقاء في اللقطات** مُعلَنٌ ولا يُصحَّح.
7. 🔴 **وانحيازُ الاستبعاد باقٍ ويتوسّع:** حارسا المقياس (`V2`+`V2b`) يستبعدان
   **عائلةَ التقسيم العكسيّ** — وهي عائلةُ فيصل بعينها — **واتّجاهُ الأثر
   غيرُ مقيس**. ⇒ قيدٌ على التعميم لا تصحيحٌ للرقم.
8. **الطوابعُ `sip_timestamp`** — وقتُ إبلاغ المُجمِّع لا وقتُ المطابقة في
   البورصة؛ الفرقُ ميكرو ثوانٍ ولا يُدَّعى أدقُّ منه.
