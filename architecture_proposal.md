# 🏗️ اقتراح المعمارية — **«درجة الاشتقاق»** · الطريق إلى أفضل توليفة

> **طلب المالك حرفيًّا (‏2026-07-31):** «لو أثبتت بالأرقام في وقتها بيكون الأساس كله من
> فيصل ونعيد تشكيل البوّابات، والإضافات اللي من عندنا نرخّيها — أو بالأصحّ نحاول نطلع
> بأفضل دقّة وتوليفة، لأنها بتكون هي اللي تغنينا عن فيصل نفسه وتحلّ محلّه … بمعنى نحاول
> تكون التوليفة والبوّابات الإضافية مبنيّة على **أقلّ درجة من الكاتالوج**، لأن بتكون
> هذي **الحافة حقّتنا**.»
>
> 🔒 **اقتراحٌ للمالك — صفر تنفيذ · صفر مسّ إنتاج.** كل بندٍ يمسّ العضوية = **قرار مالكٍ
> حصريّ** مع `LOGIC_VERSION` و`force_renew=1`.

---

## ① ما تُلزمنا به الأرقام اليوم — ثلاث حقائق مقيسة

| # | الحقيقة | الرقم | المصدر |
|---|---|---|---|
| **ح1** | **الحاجب من عندنا** | الجدار الأول `engineering` **‏94-97%** · والجدار الوحيد **‏95-100%** | `additions_audit.md` |
| **ح2** | **إسقاط البوّابات المشتقّة لا يُحسّن المُسلَّم** | المحفظة ‏+0.80 · +2.02 · **−4.13** ⇒ انقلاب | `core5_result.md` |
| **ح3** | **المُرتِّب لا يميّز — بل يتذبذب حول بِركته** | ‏+2.2 · +3.0 · **−1.8** نقطة عن نسبة البِركة | `core5_result.md` §④ |

> ### ⇒ **الاستنتاج الذي تفرضه الثلاث معًا:**
> **المشكلة ليست «كم بوّابة» ولا «أيّ عتبة» — بل أن منظومتنا كلَّها بوّاباتٌ بلا
> مُرتِّب، ومجتمعٌ واحدٌ بلا وصفات.** ولذلك: تشديدُ البوّابات يخنق (‏0.2% قبولٍ على
> أسهم فيصل)، وتخفيفُها يُدخل ضجيجًا يوزّعه مُرتِّبٌ عشوائيّ على خمس عشرة خانة.

---

## ② المقياس المقترَح — **«درجة الاشتقاق» `derivation_degree`**

حقلٌ جديد في `FAISAL_SOURCE_LEDGER.md` بجانب الوسم القائم:

| الدرجة | التعريف **الإجرائيّ** (لا الذوقيّ) | أمثلة مقيسة |
|---|---|---|
| **‏0 — نصّ** | جملةٌ لفيصل فيها **الرقم والشرط معًا**، مُتحقَّقٌ منها بصريًّا بصورة مُسمّاة | «تحت 20 ألف» (`IMG_0151`) · «شرط ما تجاوز 20٪» (`IMG_0153`) · RSI ‏≤32/≤40 · «حافظ 3 جلسات» · «÷2» · «صفقة المضارب 1000 سهم» · «ربع الساعة» |
| **‏1 — خطوة واحدة** | قاعدةٌ لفيصل **بلا رقم**، والرقم مشتقٌّ منها باشتقاقٍ واحدٍ مُعلَن | نطاق المسح 7-13% · «الفجوة هدف» · «رأس الحمرا مقاومة» |
| **‏2 — من عندنا** | لا جملةَ مصدرٍ إطلاقًا · رقمٌ هندسيّ أو موروثٌ من قرارٍ قديم | `MIN_PRICE`=1.5 · `MAX_DROP_PCT`=97 · `BASE_RANGE_MAX_PCT`=40 · `MIN_DOLLAR_VOL`=200K · `WATCH_MAX_FAILS`=3 · `SCORE_MIN` · `NEAR_PCT` · `SHORT_GATE_MAX`=40,000 |

**والحصيلة الحاليّة من الدفتر:** ‏**7 بوّابات درجة 0 · 3 درجة 1 · 24 درجة 2** — منها
**أربعٌ بسندٍ ساقطٍ أو معدوم**.

---

## ③ قانون الطبقات — **القاعدة الوحيدة الجديدة**

| الطبقة | الصلاحية | شرط البقاء |
|---|---|---|
| **درجة 0** | **بوّابة** (تحجب) | **لا تُمَسّ.** تغييرها = مخالفة نصّ فيصل ⇒ قرار مالكٍ مع سندٍ بصريّ مضادّ |
| **درجة 1** | **بوّابة مشروطة** | تبقى **فقط** إن نجت من **leave-one-out مسجَّل**: أسقِطها وحدها، وقِس المحفظة ثلاث سنوات. تدهورٌ دالّ ⇒ تبقى · صفرٌ ⇒ **تنزل درجة 2** |
| **درجة 2** | **عرض/سياق فقط — لا تحجب** | لا تصير بوّابةً إلا بـ leave-one-out **يُثبت** أنها تحمي، بثباتٍ في الثلاث |

🔴 **ولماذا هذا القانون تحديدًا:** لأننا قِسنا أن **الحواجز درجة 2 تقصّ ‏87-88% وتشتري
صفرًا** (‏`core5_result` §②) — فبقاؤها بوّاباتٍ **بلا شاهد** يخالف دليلنا نحن.
⚖️ **لكن «درجة 2» ≠ «تُحذَف»:** `M5` درجة 2 **ولها شاهد حمايةٍ مقيس** (‏C3 ‏80%→17% ·
YYAI ‏$86K ثم −68%) ⇒ **تبقى بوّابةً بجدارة الشاهد لا بجدارة النسب.** وهذا بالضبط ما
يقيسه leave-one-out: **البوّابة تكسب مكانها بالقياس، لا بالنَّسَب ولا بالأقدمية.**

---

## ④ 🥇 والحافة التي طلبها المالك — **دفتر وصفات لا فارزًا واحدًا**

**الدليل الحيّ الوحيد على النجاح في المشروع كلّه:** صيّاد المقسّم **‏5/5 شروطه درجة 0**،
وأصاب **NUWE قبل انفجاره بيومين** — بينما الفارز (‏M1-M5 ‏**صفر** درجة 0) لم يُخرج
منفجرًا قطّ. ⇒ **ليست «البساطة» فضيلةً بذاتها، بل قِصَر المسافة إلى النصّ.**

**المقترَح:** أن تصير البنية **دفترَ وصفات**، كلُّ وصفةٍ منها:
1. **مأخوذةٌ من setup فيصليّ مسمّى** بصورةٍ مرجعية · ✅ **شروطها درجة 0 حصرًا**.
2. لها **قناة تنبيهٍ مستقلّة** وسجلٌّ مستقلّ (كالصيّاد اليوم).
3. تُقاس **وحدها** بـ`mg_pre_stop` وتوقّع R — فلا يذوب نجاحها في متوسط فارزٍ عامّ.
4. **لا تُدمَج في الفارز العامّ** (درس C3: إجبار المقسّم على بوّابات الارتكاز = 80%→17%).

**الوصفات المرشَّحة الجاهزة نصًّا** (من الكاتالوج · بلا اشتقاق):
① وصفة المقسّم — **منفَّذة وناجحة** (الصيّاد) ·
② «نموذج الهبوط الإيجابيّ» ÷2 (‏`IMG_0153`) — **منفَّذة عرضًا** ·
③ **الطرح الجديد** حدثًا مؤسِّسًا — منفَّذة ·
④ ⏳ **«شمعة 4س هابطة ⟶ انتظر اليوميّ يختبرها»** (`IMG_0080`) — **غير مبنيّةٍ وصفةً** ·
⑤ ⏳ **«القاع 2» — ثبات أو سحب سيولة** (‏7-13%) — **مبنيّة وسمًا لا وصفة**.

---

## ⑤ 🔑 والأولوية الأولى التي كشفها اليوم — **المُرتِّب**

**ح3 تقول إن `rank_key` ضجيج.** ومع سعةٍ ثابتة (‏15 خانة) ترفض **‏64%** من الإشارات،
فإن **المُرتِّب هو آخر بوّابةٍ فعليّة في المنظومة — وهو الوحيد الذي لم نبنِ له شيئًا.**

**ولماذا هو الأرجح للنجاح دون غيره:** كل تجاربنا الخمس عشرة بحثت عن **إشارةٍ تميّز
داخل OHLCV** وفشلت — **والمُرتِّب لا يحتاج إشارةً جديدة**، بل يحتاج **ترتيبًا بما
أثبتناه سلفًا أنه يعمل: التوقيت** (رادار الانطلاق · كنسة الدقيقة · تدفّق المضارب).

**المقترَح (تجربة مسجَّلة لا تنفيذ):** `T-RANK` — تُقارن ثلاثة مُرتِّبات على **نفس
البِركة ونفس السعة**: ‏(أ) الحاليّ · (ب) عشوائيّ **شاهدَ ضبط** · (ج) مُرتِّبٌ بقُرب
السعر من الدعم وحده. **وإن لم يتفوّق (أ) على (ب) — وهو المتوقَّع من ح3 — فتلك أثمن
نتيجةٍ يمكن أن نخرج بها**، لأنها تنقل كل الجهد من البوّابات إلى الترتيب.

---

## ⑥ ما **لا** يقترحه هذا الملف — صراحةً

1. **لا حذف بوّابة الآن.** ‏`M4` وحده يحمل ‏49% من الرفض وشاهد SPRC — **وC5+ لم تُقَس
   محفظته بعد** ⇒ الاقتراح عليه ينتظر رقمه.
2. **لا مسّ بـ`M13`/`M14`** — قرار المالك 07-29 محسوم، ولا تُعاد فتحُه بنتيجةٍ إحصائية.
3. **لا مسّ بالوقف ولا الأهداف ولا الدخول** — أُعدمت تجاربها كلُّها بالأرقام.
4. **لا تعميم من أسهم فيصل** — العيّنة اختيارٌ لاحقٌ للحدث ⇒ **تشخيصٌ لا معدَّل**.

---

## ⑦ الخطوات بالترتيب — كلٌّ منها **يقف على قرار المالك**

| # | الخطوة | التكلفة | القرار المطلوب |
|---|---|---|---|
| **‏1** | إضافة عمود `درجة` للدفتر (‏34 بوّابة) ‏+ قفل اختبار | ساعة · **صفر مسّ إنتاج** | ✅ يمكن تنفيذه بلا قرار (توثيق) |
| **‏2** | **`T-RANK`** — تسجيل مسبق ‏+ ثلاثة مُرتِّبات ‏+ شاهد عشوائيّ | ‏9 تشغيلات | **قرار مالك: نبدأ؟** |
| **‏3** | `leave-one-out` لكل بوّابة درجة 2 (‏8 بوّابات × 3 سنوات، بوضع المحفظة) | ‏24 تشغيلة | **قرار مالك** |
| **‏4** | وصفة فيصلية جديدة (④ أو ⑤ من §④) بقناةٍ مستقلّة | يوم | **قرار مالك** |
| **‏5** | أيّ تعديل بوّابةٍ فعليّ | — | **قرار مالك ‏+ `LOGIC_VERSION` ‏+ `force_renew=1`** |

---

## ⑧ ⚖️ وأمانةً — الفرق بين ما أثبتناه وما نأمله

**أثبتناه:** الحاجب من عندنا (‏94-97%) · الحواجز المشتقّة تقصّ بلا عائد · المُرتِّب
ضجيج · السعة تقصّ 64% · وصفة فيصل الحرفية أصابت NUWE.

**لم نُثبته:** أن الطبقات الثلاث **سترفع** النتيجة. **خمس عشرة تجربةً مسجَّلة سبقت،
وكلُّها فشلت** — وهذا الاقتراح **ليس استثناءً مضمونًا**، بل **طريقةٌ أنظف للفشل السريع**:
كل بوّابةٍ تكسب مكانها بقياس، وكل وصفةٍ تُقاس وحدها، وكل رقمٍ يُعرف من أين جاء.

🔴 **والاحتمال الذي يجب أن يبقى مكتوبًا:** قد يكون الجوابُ أن **الاختيار عند القاع
مستحيلٌ أصلًا** (وهو ما تقوله خمس عشرة تجربة)، وأن كلَّ ما يمكن بناؤه هو **وصفةٌ حرفية
‏+ توقيتٌ لحظيّ**. ⇒ **وحتى في هذي الحالة يبقى هذا الاقتراح صحيحًا** — لأنه بالضبط ما
يجعل الوصفة والتوقيت هما مركز المنظومة بدل الفارز العامّ.
