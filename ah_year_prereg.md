# 🌙📏 `T-AH-YEAR` — معايرةُ `ah_guard` على مدى سنة (تسجيلٌ مسبق)

> **يُكتَب ويُدفَع قبل أيّ رقم.** أمرُ المالك 2026-08-12: **«سنة افتر»**.
> **وهو تسجيلٌ إلزاميٌّ لا اختياريّ:** `ah_prereg.md §④-4` ينصّ حرفيًّا «أيُّ معايرةٍ
> تلزمها سنةٌ كاملة **بتسجيلٍ جديد**» — فهذا هو، ولا يُشغَّل شيءٌ قبل دفعه.

---

## ⓿ عيبُ قياسٍ اكتشفتُه **قبل** أيّ رقم — وهو أوّلُ إسهام التجربة

قرأتُ الكودَ سطرًا سطرًا فوجدتُ أن `T-AH` **قاس مقياسًا لا يستعمله الحارس**:

| | ما يقرؤه | المرجع |
|---|---|---|
| **الحارسُ الحيّ** `ah_guard` | `extended_last_price` = **آخرُ** إغلاق دقيقةٍ ممتدّة (بأكبر طابعٍ زمنيّ) | `Super_stock.py:11433-11443` |
| **`T-AH`** (المنشور) | `post_high` = **أعلى** دقيقةٍ ممتدّة | `ah_scan.py: reduce_minutes` |

و`post_high ≥ post_close` **دائمًا** ⇒ 🔴 **رقمُ `Q2` المنشور (‏1.68%) سقفٌ أعلى لا
معدَّلُ الحارس** — والفرقُ ليس تفصيلًا: سهمٌ يقفز ‏+100% في الافتر **ثم يعود** لإغلاق
الجلسة **ليس تنبيهًا بائتًا** (السعرُ الذي يفتح عليه الغدُ قريبٌ من الخطّة)، والذي
**يحفظ** قفزتَه إلى إغلاق الافتر **هو** البائت. ⇒ **الحارسُ يقرأ الصحيح، والقياسُ كان
يقرأ الخطأ.**

⇒ **لذلك تقيس هذي التجربةُ المقياسين معًا وتنشر الفجوةَ بينهما** (‏`Y2`)، ولا يُصحَّح
`ah_result.md` بالحذف بل **ببانرِ تقييدٍ مؤرَّخ** (الرقمُ صحيحٌ لما قاسه، وعنوانُه كان
يعدُ بأكثر منه — وهو صنفُ «العنوانُ المغلوط يضلّل المشخِّص» المدوَّن).

---

## ① ما **يجوز** أن يُعاير وما **لا** يجوز — قبل أيّ رقم

`ah_guard` يقارن السعرَ الممتدّ **بمرجعين** والعتبةُ واحدة (`SPLIT_ROSE_MAX_PCT`=20):

| المرجع | معناه | وسمُ الدفتر | يُعاير؟ |
|---|---|---|---|
| **`ref`** = قمةُ ما بعد الحدث | «الصعودُ بعد التقسيم» = شرطُ فيصل ④ حرفيًّا | 🗣️ **`faisal_verbatim`** (`IMG_0153`: «شرط ما تجاوز 20٪ اثناء الصعود») | ⛔ **لا — ولا يُقاس هنا إطلاقًا** |
| **`price`** = إغلاقُ الجلسة | «حركةُ الافتر» — **دلالةٌ أخرى**، أُضيف 2026-07-30 بتصريحٍ أنّ «الرقم مُعادٌ لا مخترَعٌ لكن دلالتَه مختلفة» | ⚙️ عتبةٌ **مستعارة** لم تُقَس قطّ | ✅ **هذا وحده موضوعُ التجربة** |

🔒 **وسقفُ النجاح مثبَّتٌ سلفًا:** **منحنى كلفةٍ يُعرَض على المالك** + على الأكثر
**اقتراحٌ لمرجع `price` وحده**. **ولا تغييرَ كودٍ ولا عتبةٍ في هذي التجربة** — ولا
مسٌّ بـ`SPLIT_ROSE_MAX_PCT` ولا بـM1-M14 ولا بالشروط الخمسة ولا بالدخول/الوقف/الأهداف.
أيُّ تغييرٍ **قرارُ مالكٍ** بعد الأرقام.

---

## ② حدُّ المدى — يُعلَن قبل التشغيل لا بعده

🔴 **«سنة» هنا = سنةٌ حتى تاريخه (‏2026-01-02 → 2026-08-11) لا اثنا عشر شهرًا.**
السبب: `market_calendar.py` **مُتحقَّقٌ لـ2026 حصرًا** (‏`CALENDAR_VERSION=2026.1`)،
وتقويمٌ غيرُ مُتحقَّق **يَسِم الإغلاقَ المبكّر (‏13:00) «نظاميًّا»** فيخلط ثلاثَ ساعاتٍ
ممتدّةٍ بالنظاميّ = **عينُ العيب الذي بُني له الحارس `V3`/`AH1`**. ⇒ **الأداةُ ترفض
أيّ تاريخٍ خارج مدى التقويم بخروجٍ غيرِ صفريّ** (`VY-CAL`)، ولا تُختلَق عطلاتُ سنةٍ
أخرى. ⏸️ ومدُّ التقويم إلى 2023-2025 خطوةٌ مستقلّةٌ صغيرة **بإذن المالك** بعدها.
**والعدَدُ المتوقَّع ‏≈154 يومَ تداول = ‏77× عيّنة `T-AH`** (يومان) — ويُطبَع فعليًّا.

---

## ③ الأسئلةُ المسجَّلة (ولا يُضاف سؤالٌ بعد الأرقام)

- **`Y1` مقياسُ الحارس نفسِه:** معدَّلُ `post_close / reg_close − 1` عند العتبات
  المثبَّتة **‏10 · 15 · 20 · 25 · 30 · 50 · 100%** على فئتنا وعلى الكون
  (‏**20% هي العتبةُ الحيّة** — والباقي منحنى كلفةٍ لا اختيارٌ بعديّ).
- **`Y2` فجوةُ التلاشي:** من الرموز-الأيام التي بلغ **أعلاها** ‏≥20%، كم نسبةُ مَن
  **أغلق افترَه دون 20%**؟ ⇒ الرقمُ الذي يحسم «هل المهمُّ القمّةُ أم الإغلاق؟»
  وكم يخصم من رقم `T-AH` المنشور.
- **`Y3` الثبات:** المعدَّلُ شهرًا شهرًا (‏8 أشهر) + أدناه وأقصاه ⇒ **اختبارُ
  «هل تنقلب الإشارة؟»** الذي أسقط `T-SHORT` و`T-CMAG` و`G5`.
- **`Y4` صدقُ العيّنة:** المجتمعُ `reg_close < $10` **ليس مرشّحي الصيّاد** (تاريخهم
  غيرُ محفوظ) ⇒ يُقاس المعدَّلُ في ثلاث شرائحِ سعرٍ **مثبَّتةٍ سلفًا** (‏≤$2 ·
  $2-5 · $5-10 — لأن أمثلة فيصل للمقسّم تتمركز ‏≈$3، مدوَّنٌ سلفًا): إن كان المعدَّل
  **حسّاسًا للسعر** فسقفُ $10 يبخس أو يبالغ، ويُقال بالرقم.
- **`Y5` الجدوى:** الأيامُ المقيسة · المفقودة · الزمنُ · و**معامِلُ التوسيع مطبوعًا**.

---

## ④ حرّاسُ الصلاحية — **ولكلٍّ سطرُ كودٍ يُوقِف وقفلٌ يُثبت أنه يُوقِف**

> ⚖️ درسُ `envelope_prereg §⑦` الذي انتهكتُه بنفسي: **حارسٌ مكتوبٌ في التسجيل وغيرُ
> منفَّذٍ في الكود ليس حارسًا — هو نيّة.** فكلُّ صفٍّ أدناه له رمزُ خروجٍ وقفل.

| # | الحارس | لماذا | الخروج |
|---|---|---|---|
| `VY1` | **شاهدُ ضبطٍ في كلّ يومٍ مقيس**: `AAPL` حاضرٌ ونصيبُ ممتدّه في (0, 20)% · ووسيطُ السنة داخل المدى · **وأيُّ يومٍ يغيب فيه الشاهد يُعَدّ ويُعلَن** | «الصفرُ عطبُ أداةٍ حتى يُنفى» (درسُ PIT: قُرئ 0/40 وكاد يُدفَن المشروع) | **3** |
| `VY2` | أسماءُ الأعمدة **من الترويسة** | «قرأتُ `available` والحقلُ `shares_available`» | **5** |
| `VY3` | حدُّ الجلسة من `market_calendar` لا 16:00 ثابتة | الإغلاقُ المبكّر ⇒ مقياسان | — |
| `VY4` | `post_close` = إغلاقُ **أكبرِ** دقيقةٍ ممتدّة لا آخرِ صفٍّ في الملفّ | ترتيبُ الملفّ ليس عقدًا — **ونفسُ صيغة الإنتاج** («بأكبر طابعٍ زمنيّ لا بترتيب الورود») | — |
| `VY5` | **نسبٌ داخلَ اليوم حصرًا** · ويُمنَع نحويًّا قياسُ `ref` في هذي الأداة | الملفّاتُ **خامّة** وياهو **معدَّل** ⇒ نسبةُ `post/reg` يُلغي التعديلُ فيها نفسَه، ونسبةُ `ext/ref` **تخلط مقياسين** (درسُ `V9` في `T-OPFIRE`) | — |
| `VY6` | **تغطية**: أيامٌ مقيسةٌ ‏≥90% من أيام التداول في المدى · وإلّا **الأرقامُ أرضيّةٌ مُعلَنة** · وشهرٌ دون 15 يومًا **لا يدخل حكمَ `Y3`** | فجوةٌ صامتةٌ تُقرأ تغطيةً كاملة | **6** |
| `VY7` | **تراكمٌ انسيابيّ:** لا يُحتفَظ بسنةٍ من الصفوف في الذاكرة — عدّاداتٌ ومصفوفاتُ سماتٍ فقط | ‏≈3 ملايين صفٍّ = انهيارُ رنر يُقرأ «لم يُقَس» | — |
| **`VY8`** | **فحصُ تكاملٍ ذاتيّ:** حين تُحفَظ الصفوف (عيّنةٌ صغيرة) **يجب أن تُطابق عدّاداتُ المُراكِم مُخرَجَ `bucket_counts` بت-بت** وإلّا خروج **7** | ⇒ **مسارُ السنة يُصادِقه رقمُ `T-AH` المنشور نفسُه** (مسارُ قياسٍ واحدٌ لا نسختان) | **7** |
| `VY9` | معامِلُ التوسيع **مطبوع** | درسُ المِجَسّ: أخطأ ‏12× بإسقاطٍ بلا معامِل | — |
| `VY-CAL` | **رفضُ أيّ تاريخٍ خارج مدى التقويم المُتحقَّق** | §② أعلاه | **8** |

---

## ⑤ ما لا يُدَّعى — يُكتَب قبل الأرقام

1. **ليس حكمًا على `T-LABEL-AUDIT`** (قاس تغيُّرَ **وسمِ نتيجة** فوجده 3.4%): ذاك
   سؤالٌ عن **الأثر على الوسم**، وهذا عن **معدَّل الظاهرة** ⇒ **لا يُنقَض حكمٌ بقياسٍ
   لسؤالٍ آخر.**
2. **معدَّلُ الظاهرة لا دقّةُ الحارس** (منقولٌ حرفيًّا من `ah_prereg §②`): مرشّحو
   الصيّاد التاريخيون غيرُ محفوظين ⇒ **لا يُقال إن الحارس «صائب» ولا «مبالغ»**؛
   والمقيسُ **كم مرّةً كان سيُفعَّل على مجتمعٍ شبيه**.
3. **ولا حافّةَ ولا ربحية:** `post_close` سعرٌ **مطبوعٌ** لا سعرٌ نُفِّذ بحجم (سيولةُ
   الافتر رقيقة — وسيطُ نصيبها 1.09% مقيسٌ سلفًا) ⇒ سقفٌ متفائل، **وإن كان أقلَّ
   تفاؤلًا بكثيرٍ من `post_high`**.
4. ✅ **وميزةٌ نادرةٌ تُذكَر بصدقها: بلا انحياز بقاء** — الملفّاتُ المجمَّعة تحوي كلَّ
   رمزٍ تداول يومَه بمن شُطب لاحقًا، بخلاف باكتيستاتنا على كون ناسداك اليوم.
5. **ولا مسٌّ بالإنتاج:** صفرُ استيرادٍ في `Super_stock` · صفرُ كتابةِ حالة · صفرُ
   تلغرام · قراءةُ S3 فقط · والسرَّان من Secrets **ولا تُطبَع قيمتُهما**.

---

## ⑥ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر سقطت أو صمدت)

- **`PY1`** فجوةُ التلاشي **‏≥50%**: أكثرُ من نصفِ مَن بلغت قمّتُه ‏≥20% يُغلق افترَه
  دونها ⇒ **رقمُ `T-AH` (‏1.68%) ينزل إلى النصف أو أقلّ.**
- **`PY2`** المعدَّل **رتيبٌ تنازليًّا مع السعر** (‏≤$2 أعلى من $5-10).
- **`PY3`** المعدَّلُ الشهريّ **لا يتجاوز ‏2×** من أدناه إلى أقصاه (ثبات) — وسقوطُه
  يعني أن رقمًا سنويًّا واحدًا لا يصف الظاهرة.
- **`PY4`** ‏≥100% على **مقياس الحارس** أقلُّ من **‏0.05%** من فئتنا.
- **`PY5`** الكلفةُ الفعلية **‏≤2 ساعة** لمدى ‏≈154 يومًا.

---

## ⑦ رموزُ الخروج («أخضرُ» لا يُقرأ إذنًا)

**‏0** = قِيس والحرّاسُ استُوفوا · **‏2** = تعذّرُ تهيئة · **‏3** = شاهدُ الضبط سقط ⇒
**لا رقمَ يُنشَر** · **‏4** = تعذّرُ تنزيل/فكِّ ضغط · **‏5** = ترويسةٌ ناقصة ·
**‏6** = تغطيةٌ دون 90% · **‏7** = **فحصُ التكامل الذاتيّ سقط** (مساران لا مسار) ·
**‏8** = تاريخٌ خارج مدى التقويم المُتحقَّق.
