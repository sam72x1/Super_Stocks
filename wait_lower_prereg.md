# `T-WAIT-LOWER` — «انتظر اقل سعر 10% او 20% تحت» (تسجيلٌ مسبقٌ **قبل أيّ رقم**)

> **المصدر:** فيصل حرفيًّا (13/06/2026 · دليلُ طريقة فيصل ص54): «اذا حللنا سهم ارتكاز
> **ممنوع الدخول** · تابع السهم اقل سعر يكون تحت المجهر يوم يومين سبوع · انتظر اقل دخول
> · هذي اسهم محلله جاهزه فقط **انتظر اقل سعر 10% او 20% تحت**». ومعه (GWAV ص91): «سحب
> السيوله المتوقع من 2.70 **يمثل هدف الشورت = RSI 27** · يتم تحليله بعد سحب السيوله».
> **أمرُ المالك (2026-09-08):** «سو عصف ذهني وتجارب واختبارات» ⇒ سقفُ النجاح **قياسٌ
> وتوصية** — الشحنُ كلمةٌ منه.

## ⓪ لماذا التجربةُ مشروعة — وما حدُّ صدقها المسبق
- النظامُ **ارتكاز** بنصّ فيصل نفسِه («اسهم الارتكاز … انتظر اقل سعر») ⇒ لا استيرادَ نظام.
- 🔴 **حدُّ صدقٍ يُكتب قبل الأرقام:** أربعُ تجاربِ دخولٍ سابقة **كلُّها ضرّت** (‏`T-LIBVOL`
  ‏−0.879R · `T-LIBVOL-2` ‏−0.820R · `T-TRANCHE` ‏−0.199R · `T-RECLAIM-INTRADAY` ‏−0.130R)
  وخلاصتُها المدوَّنة «تغييرُ ما قبل الدخول يضرّ باتّساق». **فالاحتمالُ الأسبق أن تفشل**،
  والتجربةُ تُجرى لأن الجملةَ **نصُّ فيصل الصريح** ولم تُقَس قطّ بهذي الصيغة (الأذرعُ السابقة
  كانت **فوق** المِرساة أو **بعد** سحبٍ واسترداد، لا حدًّا **تحت** سعر التحليل).
- ⚖️ **انحيازٌ بنيويٌّ مُعلَن ضدّ الفرضية:** النافذةُ الأماميّة واحدةٌ (`BACKTEST_FORWARD_DAYS`)
  فالذراعُ التي تتعبّأ متأخّرةً يبقى لها وقتٌ أقلُّ لبلوغ `t1` (العالقُ يُحسَم بآخر إغلاق) —
  وهو نفسُ ما يلقاه الإنتاجُ عند التعبئة المتأخّرة، فالمقارنةُ عادلةٌ **ومحافِظة** للفرضية.

## ① السؤال
> على صفقات الفارز الإنتاجية نفسِها، هل **أمرُ حدٍّ أدنى بـ10% أو 20% تحت سعر التحليل**
> (أو عند **سعر RSI 27**) — مع الهدف `t1` نفسِه ووقفٍ **بنفس النسبة** تحت التعبئة الجديدة —
> يُحسّن التوقّعَ بوحدة R الثابتة، **مع احتساب ما لم يُعبَّأ صفرًا في المقام**؟

## ② المجتمعُ والمحرّك — صفرُ مسٍّ بالإنتاج
- لقطاتُ PIT الثلاث (‏2023 `31574165523` · 2024 `31199493957` · 2025 `31191327731`) بإعدادات
  الإنتاج (`FAISAL_ONLY=1`) **ونفسُ أعلام `T-TRANCHE`** (`BT_REPLAY10=1` · `BT_ENVVALS=1`) ⇒
  المجتمعُ نفسُه الذي قِيست عليه تجاربُ الدخول الأربع (ميزانيةٌ واحدة). صفقاتُ `backtest_symbol`
  الإنتاجية · إعادةُ الخطّة بـ`tranche_arms.plan_at` (تنادي `analyze_ticker` عند الفهرس نفسِه) ·
  الحسمُ بـ`_resolve_arm` الإنتاجية بالاسم · سعرُ RSI 27 بـ`rsi_target_price` الإنتاجية بالاسم ·
  و`R₀` بـ`tranche_arms.r_fixed` بالاسم.
- **سعرُ التحليل** = إغلاقُ شمعة الإشارة (آخرُ شمعةٍ رآها `analyze_ticker`).
- `Super_stock.py` **لا يُمَسّ بحرف** · الأداةُ قراءةٌ فقط (قفل AST كـ`TV6`) · الإنتاجُ لا يستوردها.

## ③ الأذرع — أربعٌ ولا خامسة (تُثبَّت هنا؛ إضافةُ ذراعٍ بعد الأرقام ممنوعة)
| الذراع | الدخول | الوقف | الهدف |
|---|---|---|---|
| `W0` | متوسّطُ دفعات الإنتاج (كما هي) `entry0` | وقفُ الإنتاج `stop0` | `t1` |
| `W10` 🥇 الحاكمة | `min(سعرُ التحليل × 0.90, entry0)` | `entry_W × (stop0/entry0)` — **نفسُ النسبة** | `t1` |
| `W20` | `min(سعرُ التحليل × 0.80, entry0)` | كذلك | `t1` |
| `W27` | `min(rsi_target_price(الإغلاقات حتى الإشارة, 27), entry0)` — وإن كان RSI عند 27 أو تحته أصلًا (`None`) فالسحبُ وقع ⇒ `entry0` | كذلك | `t1` |

- **القيدُ الواحد `min(·, entry0)`:** لا ذراعَ تدخل **فوق** خطّة الإنتاج — فالنصُّ «ممنوع الدخول
  … انتظر اقل سعر» **منعٌ لا إذنُ ملاحقة**؛ وحين لا يبنّد القيدُ (سعرُ التحليل بعيدٌ فوق الدفعات
  أصلًا) تساوي الذراعُ `W0` وتُسهم بصفرِ فرق — **ويُطبَع معدّلُ «التبنيد» لكلّ ذراع** حتى لا يُقرأ
  فرقٌ صغيرٌ على أنه أثرٌ ضعيفٌ وهو أثرٌ قويٌّ على قليل.
- التعبئة = أوّلُ شمعةٍ في النافذة الأمامية `Low ≤ الدخول`. **`no_fill` = صفرُ عائدٍ ويدخل المقام**
  (المقامُ واحدٌ للأذرع — فالتعبئةُ الأقلّ **كلفةٌ تُحسَب لا تُخفى**، §⑨-4 من `T-TRANCHE`).
- **③-ب قراءةٌ ثانية للحاكمة (وصفيّةٌ لا تحكم):** `W10` بالوقف **المطلق** `stop0` (وقفُ فيصل بنيويٌّ
  تحت الدعم لا يتحرّك مع الدخول) — تُطبَع بجانب الحاكمة؛ وإن كان `entry_W10 ≤ stop0` فـ`skip` يُعدّ ولا يُخفى.

## ④ الحُكمُ — قبل أيّ رقم
- **المقياسُ الحاكم:** متوسّطُ `R₀` (بوحدة وقفِ الإنتاج الثابتة `entry0 − stop0`، `tranche_arms.r_fixed`).
- **`WL1` (الحاكم):** `W10 − W0` **موجبٌ في السنوات الثلاث كلٍّ على حدة** **و**مجمَّعًا ‏≥ **+0.05R**.
- **`WL2`:** تعبئةُ `W10` ‏≥ **‏30%** من تعبئة `W0` (وإلّا القاعدةُ «تنتظر ولا تشتري»).
- **الأرضية:** ‏≥ **100** صفقةٍ مُعبَّأة في `W0` **و**الحاكمة لكلّ سنة (‏`FLOOR_DECIDED` من `T-EXIT`) وإلّا «لا حكم».
- `W20`/`W27` **وصفيّان** يُطبَعان بنفس الجدول ولا يحكمان — إلّا أن يعبر أحدُهما `WL1`+`WL2`
  وحدَه فيُذكر **مرشَّحًا لتسجيلٍ لاحق** لا حكمًا.
- **بوّاباتُ الصلاحية (عطبُ أداة لا نتيجة):** `V-W0` حسمُ `W0` يطابق حسمَ الإنتاج في ‏≥99% من
  الصفوف (وإلّا خروج 3) · `V-W1` تعبئةُ `W10` ‏≤ تعبئة `W0` بالبناء (وإلّا خروج 3) · `V-W2` تفرّقٌ
  عن `W0` (وإلّا `no-op` خروج 4) · `V-W3` سنةُ اللقطة = سنةُ القياس (وإلّا خروج 4).

### ④-ملحق (مؤرَّخ 2026-09-09 · **قبل قراءة أيّ رقم** · من المراجعة الخصومية للأداة)
- (أ) **تفسيرُ `V-W2`:** الحاكمةُ `W10` وحدَها تُسقط القياس (خروج 4)؛ الذراعُ الوصفيّ المتطابق مع `W0`
  **يُطبَع ولا يُسقط** — وإلّا حجب ذراعٌ وصفيٌّ متدهور (`W27 ≡ W0`) الحاكمةَ.
- (ب) **`W0` وقفُها `stop0` حرفيًّا** لا `entry0 × (stop0/entry0)` — فالقسمةُ والضربُ قد يُبعدان القيمةَ
  بخطأ تمثيلٍ فتُفقَد لمسةُ وقفٍ مطابِقة (`Low == stop0`) ويتآكل `V-W0` بلا سبب.
- (ج) **التشغيلاتُ الثلاث الجارية** (2023 `34307345750` · 2024 `34307348104` · 2025 `34307349569`) على
  الكود `5866b50` **قبل** (أ) و(ب): إن أسقطت `V-W0` أو `V-W2` أُعيدت على الكود المعدَّل، وإلّا قُرئت
  كما هي **مع ذكر نسبة `V-W0`** — والفرقُ المتوقَّع من (ب) دون 1% من الصفوف.

## ⑤ التنبّؤات (تُنشَر مكذَّبةً أو محقَّقة — لا تُحذف)
- **`WL-P1`:** تعبئةُ `W10` بين **‏35% و60%** من تعبئة `W0` (كثيرٌ لا يعود 10% تحت).
- **`WL-P2`:** `W10 − W0` مجمَّعًا **بين ‏−0.15R و+0.05R** ⇒ **يسقط `WL1`** (الاحتمالُ الأسبق من
  التجارب الأربع).
- **`WL-P3`:** تعبئةُ `W27` **أقلُّ** من `W10` (سعرُ RSI 27 أعمقُ من ‏−10% غالبًا).
- **`WL-P4`:** نسبةُ الربح **بين المُعبَّأ** في `W20` **أعلى** من `W0`، **والتوقّعُ بالمقام الكامل أدنى**.
- **`WL-P5`:** قيدُ `min` يبنّد في **أكثرَ من نصف** الصفوف لـ`W10` (أغلبُ إشارات الفارز قريبةٌ من دفعاتها).

## ⑥ سقفُ النجاح
قياسٌ وتوصية. **لا يتحرّك `ENTRY_*` ولا وقفٌ ولا `LOGIC_VERSION` بهذي التجربة** — والشحنُ كلمةُ
المالك بعد رؤية الجدول. والنتيجةُ تُدوَّن في `wait_lower_result.md` بجانب هذا العقد لا فوقه.
