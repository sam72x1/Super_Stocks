# 🕯️ `T-CANDLE` «أيُّ شمعةٍ انعكاسية تفصل؟» — التسجيل المسبق

> **الأمر:** المالك 2026-08-27 — «سجّل الشمعة الانعكاسية مثل الهمر المقلوب او
> غيره من الشموع اللي تشوف فيصل مركز عليها **و هذي ارجع للجميع الاسهم حقته
> عشان تتاكد**».
> **يُدفَع قبل أيّ سطرِ أداةٍ وقبل أيّ رقم.** بحث/قياس حصرًا · صفرُ مسٍّ
> بالإنتاج · لا `LOGIC_VERSION`.

---

## ⓪ مسحُ الكتالوج أوّلًا — تنفيذًا لشقّ أمره «ارجع للجميع الاسهم»

مُسحت ملفّاتُ توثيق فيصل كلُّها (`FAISAL_IMAGES_CATALOG.md` ·
`FAISAL_METHODOLOGY_NOTES.md` · `FAISAL_PRESSURE_MODEL.md` ·
`FAISAL_IMAGE_PASS2.md`/`PASS3.md` · `FAISAL_SCIENTIFIC_METHOD.md`).
**النتيجة — كلُّ شمعةٍ نطق بها فيصل، بسندها:**

| النموذج | نصُّه | المصدر | حالتُنا |
|---|---|---|---|
| **① همر** (مطرقة) | «الشرط تكوين نماذج الهبوط الاجابيه والاكتفاء وتحقيق شمعه انعكاسيه ايا كانت نوع هذي الشمعه · **همر > نجمة صباح > هرامي وت ايفر**» — **والترتيبُ ترتيبُه** | `IMG_0448` | ✅ `reversal_candle` ① |
| **② نجمة صباح** | المرتبة الثانية · وPLRZ «راس شمعة القاع» زنادَ ارتداد | `IMG_0448` | ✅ ② |
| **③ هرامي** | المرتبة الثالثة | `IMG_0448` | ✅ ③ |
| **④ «أيًّا كانت»** | «وت ايفر» = *whatever* (تصحيحُ إسنادٍ 2026-08-15) ⇒ **بابٌ مفتوح** لا اسمُ نموذج | `IMG_0448` | ✅ ④ (ابتلاعٌ صاعد — **اصطلاحُنا** للمرتبة) |
| **⑤ همر مقلوب** 🥇 | «**كان دخولي ع الدعم ع شمعة الهمر المقلوبه**» — **فيصل يصف دخولَه هو** · و`LNAI` «شمعة همر مقلوبه او الشهاب الساقط» · «شمعة همر خضرا مقلوبه» | دفعة 37 صورة · `TG_2032`/`TG_2060`/`TG_2002` | ✅ `extra_candle` |
| **⑥ دوجي** | `AMCI` «**انطلاقته من شمعة الدوجي**» | دفعة 37 صورة | ✅ `extra_candle` |
| **⑦ ابتلاع صاعد** | `ADIL` كاشفُه «Engulfing (Bullish)» مع **كسر خطّ الترند الهابط** | `TG_1929`/`TG_1940` | ✅ (هو «وت» عندنا) |
| ⚠️ **همر أحمر** | «**شمعة همر حمرا = صعود قصير الأمد** ويؤخذ منها الوتد الهابط فقط» — **تحفّظٌ لا إيجاب** | دفعة 37 صورة | 🔴 `reversal_candle` **لا تميّز اللون** |
| 🔴 شهابٌ ساقط · رجلٌ مشنوق · غربانٌ ثلاثة | انعكاسٌ **هابط** عند القمّة (تحذير) | — | `_hanging_man` تحذيرًا |

🔑 **وثلاثُ خلاصاتٍ من المسح تُثبَّت قبل الأرقام:**
1. **الأنماطُ الستّة المذكورة كلُّها مكشوفةٌ عندنا سلفًا** — و`wake_read.rev`
   **يُرجع اسمَ النمط لا بوليانًا** ⇒ التفصيلُ ممكنٌ بصفر منطقٍ جديد وصفرِ
   رقمٍ مخترَع (شرطُ سكِل `faisal-gates`).
2. **وترتيبُ فيصل منصوصٌ** — فالحاكمةُ تُشتقّ منه لا من ذوقي.
3. 🔴 **والفريمُ «كلاهما»** (سُئل «يوميّ ولا 4 ساعات؟» فأجاب «كلاهما» —
   `IMG_0450`) · و`LNAI` «ادنى قاع 2.08 = **ع فريم 4 ساعات** شمعة همر صاعده»
   · و`MNDR` همرٌ على 4س ⇒ **نصفُ قاعدته على 4 ساعات، ولقطاتُنا يوميّة** ⇒
   **الشقُّ الرباعيُّ غيرُ قابلٍ للقياس بهذي الأداة** — يُعلَن ولا يُدَّعى.

## ① السؤال — وما الذي يجعله جديدًا

قِيست «الشمعةُ الانعكاسية» **مجموعةً** مرّتين وكلاهما ضعيف:
`press_wake §⑯` (‏16.9/18.4/17.9% بلوغَ هدفٍ داخل الحافظين) و`T-FUSE` اليوم
داخل `F1` (‏`E` = ‏−0.075 · +0.114 · +0.230 — اتّجاهٌ موجبٌ في الثلاث وفواصلُ
**متداخلة**). **ولم يُقَس قطُّ أيُّ نمطٍ بعينه.**

⇒ **السؤال:** داخل الحلقات التي تحمل شمعةً انعكاسية، هل **اسمُ النمط** يفصل؟
وبالتحديد: هل تنعكس **مرتبةُ فيصل المنصوصة** في الأرقام؟

## ② المجتمع والمحرّك — نفسُ آلة `T-FUSE` بالاسم (بوّابةُ تكاملٍ مجّانية)

- اللقطات: `PIT` الثلاث — 2023=`31574165523` · 2024=`31199493957` ·
  2025=`31191327731`.
- الحلقاتُ من `press_wake_arms.walk_symbol_wake` **نفسِها** (المجمَّدة
  `CAP15`) ثم إثراءُ `fuse_arms.enrich_episode` **بالاسم** — صفرُ مِشيةٍ
  منسوخة، والحسمُ `rebound_arms.resolve_episode` كما هو.
- **الحقلُ الجديد الوحيد:** اسمُ النمط من `press_radar.wake_read(sl)["rev"]`
  **بالاسم** (يُنادي `reversal_candle` ثم `extra_candle` الإنتاجيَّتين) ‏+
  **لونُ الشمعة** (‏`c > o` — **حقيقةٌ لا عتبة**) لشقّ «الهمر الأحمر».

## ③ الأذرع — مثبَّتةٌ الآن ولا ذراعَ تُضاف بعد الأرقام

**المجتمعُ الحاكم `REV-ALL`** = الحلقاتُ التي `rev` فيها ليس `None`.

| الذراع | التعريف | الدور |
|---|---|---|
| **`C1` الحاكمة** | **«همر»** (مرتبةُ فيصل الأولى) مقابل **بقيّة الأنماط** داخل `REV-ALL` | **الحكم** — هل الاسمُ يفصل؟ |
| `C2` | همر مقلوب | مسمّاةٌ (أمرُ المالك · ودخولُ فيصل نفسِه) |
| `C3` | نجمة صباح | مرتبةُ فيصل ② |
| `C4` | هرامي | مرتبةُ فيصل ③ |
| `C5` | وت (ابتلاع) | مرتبةُ فيصل ④ |
| `C6` | دوجي | `AMCI` |
| `REV-NONE` | بلا شمعةٍ إطلاقًا | شاهدُ العزل |

**وقاعدةُ الترقية مثبَّتةٌ سلفًا (سابقة `T-KASIH-2`/`T-STRONG-2`):** يُنشَر
الجدولُ كاملًا، **ولا يُرقّى نمطٌ إلى اقتراحٍ إلّا إذا عبر معاييرَ §④ الأربعة
على نفسه ‏+ نجا من تصحيح بونفيروني ×6** (ستّةُ أنماط = ستُّ مقارنات).

**واستكشافيّاتٌ تُنشَر ولا تُرقّى إطلاقًا:** لونُ الهمر (أخضر/أحمر — تحفّظُ
فيصل المنصوص) · النمطُ داخل `F2` (أعلى خليّة في `T-FUSE`) · النمطُ مع
`tested_level` حاضرًا (تسلسل `WNW`).

## ④ المعايير — مُعادةٌ من `T-FUSE` بلا تحريك

1. **فرقُ التوقّع** `E(الذراع) − E(بقيّة `REV-ALL`)` **‏+0.15R فأكثر مجمَّعًا**.
2. **موجبٌ في السنوات الثلاث** (لا انقلاب).
3. **فاصلا Wilson لنسبة بلوغ الهدف منفصلان مجمَّعًا.**
4. **الأرضية:** ‏40 محسومةً في الذراع لكلّ سنة و150 مجمَّعًا — دونها **«لا
   حكم»** ويُطبع العدّاد (لا يُخفَّض الحدُّ ولا تُدمَج ذراعان لتُبلَغ).

**والحكمُ مكتوبٌ بطرفيه الآن:** عبرت `C1` ⇒ «الاسمُ يفصل» ⇒ اقتراحٌ للمالك
**بتسمية النمط في سطر العرض** بدل «شمعة انعكاسية» المبهمة. سقطت ⇒ يُدوَّن أن
**عائلةَ الشموع لا تفصل لا مجموعةً ولا مفصَّلة** — وهو الفشلُ الثالث لهذي
العائلة بعد §⑯ و`T-FUSE`، ويُغلَق المحورُ بلا إعادة فتحٍ إلّا بمحورٍ جديد
حقيقيّ (فريمُ 4 ساعات — وهو غيرُ متاحٍ بلقطاتنا).

## ⑤ بوّابات الصلاحية

- **`CV0` تكاملٌ مزدوج:** (أ) شريحةُ `REV-ALL` داخل `F1` تعيد أرقام `T-FUSE`
  المنشورة **بت-بت** (‏481/401/485 حلقةً و‏−0.075/+0.114/+0.230R) — تفرّقٌ ⇒
  **خروج 3** (عطبُ أداةٍ لا نتيجة). (ب) `F-H3` مجمَّعةً تعيد §⑬
  (‏5371 · 19.08% · +0.174R).
- **`CV1`** حارسُ الـ`no-op`: صفرُ حلقاتٍ أو ذراعٌ حاكمةٌ فارغة ⇒ خروج 4.
- **`CV2`** مجموعُ الأنماط الستّة = حجمُ `REV-ALL` بالضبط (تقسيمٌ تامٌّ
  منفصل — أوّلُ مطابقةٍ تفوز بترتيب الإنتاج) · وأيُّ اسمٍ خارج الستّة يُعَدّ
  ويُسمّى (لا قصَّ صامتًا).
- **`CV3`** الأنماطُ من `wake_read` **بالاسم** — قفلُ AST يمنع تعريفَ أيٍّ من
  `reversal_candle`/`extra_candle`/`wake_read` محلّيًّا (مقياسٌ واحدٌ لا اثنان).
- **`CV4`** أقفالُ السويّة `CD*` + جولةُ طفراتٍ كاملة (كلُّ قفلٍ يسقط مرّةً
  عمدًا · وطفرةٌ تنهار لا تسقط = قفلٌ يُصلَّب) · وpreflight يرفض البدء عند
  مُشغِّلٍ آخرَ حيّ.

## ⑥ تنبّؤاتي — تُنشَر ولو كُذِّبت

- **`CD-P1`** *(الأخطر عليّ)*: **لا نمطَ يعبر المعايير الأربعة مع بونفيروني**
  ⇒ الاسمُ لا يفصل — امتدادُ سقوط أنماط الشموع كمميِّز (‏`FU-P5`).
- **`CD-P2`**: «همر مقلوب» **أندرُ بمراتب** من «همر» ⇒ الأرجحُ «لا حكم»
  بالأرضية لا «فشل» — وهذا **ليس نفيًا لقاعدة فيصل** بل قصورُ عيّنة.
- **`CD-P3`**: «دوجي» **أكثرُها عددًا** (جسمٌ ‏≤10% شائع) **وأضعفُها فصلًا**.
- **`CD-P4`**: لونُ الهمر يفصل **في اتّجاه فيصل** (الأحمرُ أضعف) لكن دون الدلالة.
- **`CD-P5`**: النمطُ داخل `F2` يرتفع للجميع (‏`F2` موجبةٌ أصلًا +0.225R) ⇒
  **لا يُنسَب للشمعة** — فخُّ الإسناد يُعلَن قبل وقوعه.
- **`CD-P6`**: «وت» (الابتلاع) أكثرُ عددًا من «هرامي».
- **`CD-P7`**: ترتيبُ فيصل (‏همر > نجمة صباح > هرامي > أيًّا كانت) **لا يظهر
  رتيبًا** في `E` — وإن ظهر فهو **أقوى نتيجةٍ ممكنة** في الملفّ كلِّه.

## ⑦ سقفُ النجاح — مثبَّتٌ سلفًا وبلا تغيير

مهما كانت الأرقام: **اقتراحٌ للمالك + تسميةُ النمط في سطر عرضٍ قائم.**
⛔ لا بوّابةَ رفضٍ ولا كتمَ ولا ترتيبَ اختيار · ولا مسَّ `reversal_candle`/
`extra_candle`/`wake_read`/`walk_symbol_wake`/`press_read` (منشورةُ الأرقام)
· ولا أرقامَ المالك · ولا أيَّ جذرٍ (بصمةُ AST قبل كلّ دفعة).

## ⑧ حدودُ صدقٍ تُكتب قبل الأرقام

1. 🔴 **الفريمُ نصفُه غيرُ مقيس:** فيصل «كلاهما» ولقطاتُنا يوميّة ⇒ همرُ
   `LNAI` على 4س وهمرُ `MNDR` **خارج القياس** — والنتيجةُ لا تُعمَّم عليهما.
2. **الشمعةُ تُقرأ يومَ قراءة الضغط** لا يومَ الدخول (‏`resolve_episode`
   يدخل بخطّة المرآة بعدها) ⇒ نقيس «هل حضورُها يومَ القراءة يفصل».
3. **الأسماءُ متنافسةٌ لا مستقلّة:** أوّلُ مطابقةٍ تفوز بترتيب الإنتاج
   (‏همرٌ ينطبق عليه هرامي يُوسَم «همر») ⇒ الأرقامُ لِـ**التسميةِ** لا
   للهندسةِ الخالصة.
4. **تعدّدُ المقارنات** (‏6 أنماط) ⇒ بونفيروني ×6 شرطُ ترقية.
5. «بلغ الهدف» **لمسٌ لا تنفيذ** · `E` بخطّة المرآة **لا ربحيةَ البوت** ·
   بلا افتر · `w=40` حصرًا · وانحيازُ بقاءٍ مخفَّفٌ لا معدوم.
6. **القياسان السابقان لمجموعها ضعيفان** ⇒ الأرجحُ سلفًا أن التفصيل لا
   يُنقذها (‏`CD-P1`) — وهذا **تنبّؤٌ مكتوبٌ لا اعتذارٌ بعديّ**.
