# 🕵️⏳ `T-PRELINK` — تسجيلٌ مسبقٌ حاكم: «التنبيه اليوم والانفجار بعد أيام» — ما الذي يميّز تنبيه «هنا الدخول» الذي ينفجر **لاحقًا**؟ وهل يمكن التمركزُ بين التنبيه والانفجار؟

> **مدفوعٌ ومدموجٌ قبل أيّ سطرِ أداةٍ وقبل أيّ رقم.** أمرُ المالك (‏2026-09-25) برومبتُ `T-PRELINK` بنصّه، وتصحيحُه
> الحاكم قبله: «**ركز التنبية يجيني اليوم بعد اسبوع مثلا ينفجر مب في نفس اليوم**». **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ
> بالإنتاج ولا بالجذور · لا `LOGIC_VERSION` · لا تلغرام · لا عتبةَ تتحرّك · ولا وكلاء.**
> ⚖️ قواعدُ المشروع أعلى من أيّ سطرٍ هنا (`CLAUDE.md`): تسجيلٌ مسبقٌ قبل أيّ رقم · لا يُرخى معيارٌ بعد رؤية رقم ·
> التنبّؤُ الخاطئ يُنشَر · والحكمُ يُقرأ **بالأضعف**.

---

## ⓪ ما هو مقيسٌ سلفًا — والفرقُ الذي يجعل هذا العقدَ محورًا جديدًا لا إعادةَ فتح

🔴 **الاحتمالُ الأسبق: الفشل.** «الرابطُ المشترك» قِيس في هذا المستودع مرارًا وفشل في كلّ مرّة — لكنّ **كلَّ قياسٍ
على المراسي كان على يوم المرساة وحدَه**. الجدولُ من `grep -n` في `DECISIONS_ARCHIVE.md` (‏2026-09-25 · أرقامُ الأسطر
= مطلعُ البند) و`grep -ln CLOSED_RC *.py`:

| العائلة/التجربة | المجتمع · الأفق | الحكم | ما الجديد هنا · مسموحةٌ أم مُغلَقة |
|---|---|---|---|
| `T-OPLINK` (‏س 29) | 711 مرساة · **يومُ المرساة** (`exploded50` من سعر الكرت) | الفرع 2 «لا رابط» للأساسيّات · ما يفصل بريماركت/J1/فجوة | **الأفقُ 1-20 جلسة لم يُقَس** · و`mg_5d` وُعد به ولم يُطبع (انحرافٌ مُعلَنٌ الآن في `opentry_link_result.md §⑩`) — مسموحة |
| `T-SHADOW` (‏س 23/25) | المراسي · يومُ المرساة | داخلَ العيّنة `S` 36.8% مقابل 10.7% · أماميٌّ حتى 12-31 | يومُ المرساة وحدَه — لا تعارض |
| `T-TIERLINK`/`-2` (‏س 29 · 2541) | 321/364 مرساة · اليوم (+`mg_5d` وصفيّ **ملوَّثٌ بالتقسيم**: LGCL/FGL) | ظاهرةٌ واحدة «J1 في البريماركت» | `mg_5d` لم يكن حاكمًا ولم يُسوَّ بالتقسيم — مسموحة |
| `T-OPHOLD`/`-2` (‏س 1648 · 1619) | المراسي · **حملٌ حتى أوّل إغلاقٍ نظاميّ** | «لا حكم» · `HN` −2.10R · `H1` −0.46R (وصفيّ) | الحملُ **أيّامًا** لم يُقَس — والتكلفةُ عند لحظة المرساة مقيسةٌ فتُراعى في §⑥ — مسموحة |
| `T-OPCURVE` · `T-OPTRADE` (‏س 1660 · 1539) | المراسي · منحنى ما بعد المرساة في اليوم · سياسةُ مضاربةٍ يومَها (‏−0.70R) | وصفيّ · «لا حكم» | كلاهما داخل اليوم — مسموحة |
| `T-REARM` (‏س 3979) | مراسي `kasih` 2023-2025 · المرساةُ الثانية بعد الخروج البنيويّ | استوفت 3/3 (اقتراحٌ للمالك) | يُعاد استعمالُها **تشخيصًا** فقط: هل الانفجارُ المتأخّر يأتي بمرساةٍ جديدة؟ (‏§①-5) |
| `T-LINK100`/`-2` (‏س 1602 · 322) | السوقُ كلُّه بالمشطوبين · **منفجرو +100% في جلسة** · ميزاتُ الأمس والبريماركت | `vol_x≥3` و`ret5>20%` إثراء (الفرع 1) · «الضغطُ لأدنى قاع» لا تعبر | المجتمعُ **كلُّ منفجر** لا **المراسي**؛ ولا تحكم بالدقّة · **وميزتا الفرع 1 تدخلان هنا ضبطًا للزخم** — مسموحة |
| `T-PMUSD` (‏س 1587) | منفجرو الجلسة النظاميّة · سيولةُ البريماركت **في صباح الانفجار** | الفرع 1 (‏20.9/12.5/8.9×) | إشارةُ صباحِ الانفجار لا ما قبله بأيّام — مسموحة (تُقاس هنا في **يوم الانفجار** وصفًا فقط · لا منبّئًا) |
| `T-J1PM` (‏س 25 · 4206) | المراسي · أماميّ · يومُ المرساة | «لا حكم» (‏15/42 · أرضيّة 60) | يومُ المرساة — لا تعارض |
| «مِجَسُّ اليومين قبل الانفجار» (‏س 8694) | 53 زوجًا كتالوج+أسماكنا · **case-crossover** | `vol_x` وحدَه يميل · سلوكُ المضارب (الصفقات) ضجيج | مِجَسٌّ تشخيصيّ لا حكم · ومجتمعُه ليس المراسي — مسموحة |
| `T-CANDLE` (‏س 5436) · `T-STABILITY` (‏س 9351) · `T-DORMANT` (‏س 4649) · `T-WAKE` (‏س 4739) · `T-VOLBASE` (‏س 5088) · `T-RANKER3` (‏س 8801) · `T-PRESS-*` (‏س 4908 · 8513 · 8616) | **مرشّحو الفرز** (الاختيار) | فشلت كلُّها | مجتمعُها **الفرزُ لا المراسي** ونتيجتُها الفرزُ/الوقف لا الانفجارُ المتأخّر ⇒ العائلاتُ 1-3 هنا **ليست إعادةً لها** — مسموحة (ولا تُشحَن منها عتبة) |
| `T-SHORT` (‏س 2268 · وبند «يوم القياس» في `CLAUDE.md`) | مرشّحو الفرز · شورت FINRA PIT | انقلبت بالسنة الثالثة ⇒ لا تغيير على M13 | يُقاس هنا **وصفًا** على المراسي (‏§④-6) ولا يُقترح تغييرٌ على M13 — مسموحة |
| `T-ACC`/`T-ACC-2` (`CLAUDE.md` «رادار التجميع الصامت») | مرشّحو الفرز · الصفقاتُ عند القاع | فشل بالسنتين · «أُغلق ملفُّ التجميع/الصفقات كمؤشّر **اختيار**» — لا `T-ACC-3` بلا **محورٍ جديد + تسجيلٍ + موافقة** | المحورُ هنا **جديد** (المراسي · النتيجةُ المتأخّرة · الصفقاتُ **بعد** المرساة لا عند القاع) · التسجيلُ هذا · **والموافقة**: انظر ⓪-ب |
| `T-BOOK` (`CLAUDE.md` «تصحيح مقاس 2026-07-30»: «لا `T-BOOK` ولا سطرَ عرضٍ قبل تسجيلٍ مسبق وموافقة المالك») | — | لم يُقَس قطّ (NBBO تاريخيّ متاح ‏≥3.3 سنة · قمّةُ الدفتر لا العمق) | التسجيلُ هذا · **والموافقة**: انظر ⓪-ب · **ولا سطرَ عرض** يُشحَن مهما كانت النتيجة |
| `T-PRE-EDGAR`/`-2` (‏س 119 · 101) — **مُغلَقٌ مُنفَّذًا** (`EDGAR_REOPEN`) | منفجرو البريماركت · إيداعُ SEC الليليّ | «فشل» (‏5.65% مقابل 3.37%) | **لا يُفتح** — إيداعُ SEC **ليلةَ** الانفجار لا يُقاس هنا منبّئًا. ما يُقاس في §④-5 مختلفٌ بالبناء: **حدثٌ معلَنٌ سلفًا** (تاريخُه مكتوبٌ قبل يوم 0) داخل النافذة |
| `T-RSI-RANK` · `T-PMGATE` · `T-SUPDEF` · `T-TRAIL` · `T-SESSIONS` · `T-WAVES` · `T-HRT` · `T-RSI40` · `T-FILLOFF` (`CLOSED_RC` في 11 ملفًّا) | — | مُغلَقةٌ مُنفَّذًا | **لا تُمَسّ**: لا ترتيبَ بـRSI · لا بوّابةَ توقيتٍ بالبريماركت+30 · لا تعريفَ دعمٍ جديد · لا وقفَ متحرّك · لا ساعةَ قمّة · لا HRT · لا RSI 40 · لا إزاحةَ تعبئة |

**ⓐ الجديدُ بالبناء (ثلاثةٌ لم تجتمع في تجربةٍ واحدة):** ① **النتيجةُ** = انفجارٌ **بعد** يوم المرساة (الجلسات 1-10 حاكمةً) من
**سعر التمركز** (إغلاق يوم 0) لا من سعر الكرت ② **المجتمعُ** = مراسي «هنا الدخول» الحيّة (A) **ومراسي `kasih` 2023-2025 بالمشطوبين**
(C) — والمراسي الرجعيّة مطابقةٌ لبوّابة الإنتاج بشاهد `V1` في `kasih_result.md` (‏21 مطابقة حرفيّة بالسعر على 2026-08-17) ③
**ضبطٌ مطابقُ الزخم بلا مرساة** في اليوم نفسِه، **وضبطُ «المرساة نفسها»** (هل يضيف الرابطُ على كون المراسي).

**ⓑ المحاورُ المشروطة بإقرار المالك — كيف قُرئ إقرارُه:** برومبتُ المالك المُرسَل يحمل السطرَ الاختياريّ **بنصّه ولم يُحذف**:
«أُقرّ بفتح كلّ محورٍ مُغلَقٍ تستوفي فيه هذه التجربةُ شروطَ فتحه بحرفها، على مجتمع المراسي وحده، وأوافق على T-BOOK بتسجيلٍ مسبق.»
⇒ **يُقرأ إقرارًا لعائلة الصفقات/NBBO (‏§④-4) على مجتمع المراسي وحدَه (A فقط · قراءةٌ · سقفُ نداءاتٍ مكتوب)** — وهو
الفعلُ الأقلّ خطرًا والأسهلُ رجوعًا: قراءةٌ لا شحن. ⚠️ **وإن لم يقصده المالك فكلمتُه «الغ فتح المحاور» تُسقط §④-4
قبل التشغيل أو تُسقط جدولَها من الحكم بعده** (تُطبَع في بابٍ مستقلّ فلا تُلوّث الحكم). **ولا يفتح هذا الإقرارُ ما له مفتاح `REOPEN`**
(الصفُّ الأخير في الجدول) لأن شروطَ فتحها **لا تستوفيها هذه التجربة بحرفها** (دعواها نفسُها).

**ⓒ قائمةُ المالك اليدويّة (B) وصلت فارغة** ⇒ **اختيارُ المالك الفعليّ لم يُختبر** ويُقال ذلك في النتيجة والتقرير؛ وتُقاس
**شروطُه الثلاث** على A كما يمكن (‏§②) و**شرطُ RSI وحدَه** على C (الفلوت/المتاح بلا تاريخ). وإن أرسل القائمة لاحقًا تُقاس
**ملحقًا مؤرَّخًا** بالتعريفات نفسِها — لا يُعدَّل هذا العقد.

---

## ① التعريفات — مثبَّتةٌ قبل أيّ رقم (`engineering` إلّا ما وُسم)

1. **المرساة:** تنبيهُ «هنا الدخول» = أوّلُ `M1` لرمزٍ في يومه (A: `LIQ:*` بـ`anchor_ms` من تاريخ git لـ`op_entry_state.json` عبر
   `tierlink_probe.anchor_history` **بالاسم** · C: صفوفُ `kasih_rows_{2023,2024,2025}.jsonl` — `first_anchor` بدالّة الإنتاج
   `liq_stage_events` **نفسها** على كون \[`PRICE_LO`، `PRICE_HI`\]$ من الملفّات المجمَّعة).
2. **يومُ 0** = الجلسةُ النظاميّة التي يقع فيها **أوّلُ إغلاقٍ نظاميّ بعد لحظة المرساة**: مرساةُ البريماركت/الجلسة ⇒ يومُها ·
   مرساةُ الافتر (‏≥16:00 نيويورك) ⇒ **الجلسةُ التالية** (تُعَدّ وتُطبَع منفصلةً).
3. **سعرُ التمركز الحاكم** = **إغلاقُ يوم 0** (`close0`). وللمقارنة تُطبَع نتائجُ: سعرِ الكرت (‏A: `e5` بالاسم · C: `entry`) وافتتاحِ يوم 1.
4. **النتيجةُ الحاكمة «انفجارٌ متأخّر» (`late100_10`)** = `max(high[1..10]) / close0 − 1 ≥ +100%` على **الجلسات 1-10 بعد يوم 0**
   (يومُ 0 نفسُه **مُستبعَد** من الحساب). ومعها تُطبَع (وصفًا لا حكمًا): `late50_10` · و`late100_5` · `late100_20` · `late50_5` · `late50_20`.
   **التسويةُ بالتقسيم:** السلسلةُ اليوميّة `adjusted=true` (تقسيماتٌ فقط عند Polygon) ⇒ النِّسَبُ داخل السلسلة ثابتة؛ **وسعرُ
   الكرت** يُسوّى بـ`chart_finder.split_factor` **بالاسم** (جداءُ `from/to` للتقسيمات بعد يوم 0). وأيُّ صفٍّ له تقسيمٌ داخل
   \[يوم 0 − 1، يوم 0 + 20\] **يُوسَم `split_in_win`** ويُطبَع الحكمُ **به وبدونه** (`V-P2`).
5. **التصنيفُ الثلاثيّ لكلّ مرساة:** `same_day` (A: `exploded50` من `tierlink_probe.measure` **بالاسم** = لمسُ +50% من سعر الكرت في
   يوم المرساة · C: `high0_raw / entry − 1 ≥ 50%` حيث `high0_raw` أعلى يوم 0 من الشمعة اليوميّة) · `late` (‏`late50_10` ولم يكن
   `same_day`) · `none`. **والحاكمُ الثنائيّ** في كلّ جدولٍ هو `late100_10` (نعم/لا) على **كلّ** المراسي — ويُطبَع بجواره
   الجدولُ نفسُه على **غير `same_day`** (المتمركزُ بعد يومٍ هادئ).
6. **لكلّ متأخّر:** `days_to_peak` (الجلسةُ التي وقعت فيها القمّة داخل 1-20) · `mdd_before_peak` = أدنى `low` قبل القمّة ÷ `close0` − 1 ·
   `held_low0` = هل بقي كلُّ إغلاقٍ قبل القمّة فوق `low0` (أدنى يوم 0) · `rearmed` = مرساةٌ جديدة للرمز في \[1، يوم القمّة\] (A: من
   تاريخ git · C: من صفوف `kasih` نفسِها).
7. **الاكتمال:** تُحتسب المرساةُ في أفقٍ ما فقط إذا اكتملت جلساتُه كلُّها قبل يوم التشغيل؛ الناقصةُ تُطبَع بعددها ولا تحكم (`V-P4`).
8. **الفوجُ k (‏§④-3):** لكلّ `k ∈ {1,2,3}` مجتمعُ الفوج = المراسي التي **لم تبلغ** +50% من `close0` في الجلسات 1..k (لا `same_day`
   ولا انفجارٌ مبكّر) · الميزاتُ من الجلسات ≤ k · والنتيجةُ `max(high[k+1..10]) / close_k − 1 ≥ +100%` **من إغلاق يوم k** (سعرُ التمركز
   المتأخّر). المُخرَجون من الفوج يُعَدّون ويُطبَعون.

---

## ② الأساسُ ولماذا تختلف تجربةُ المالك — أسئلةٌ تُجاب بالأرقام قبل الروابط

1. **معدّلُ الأساس** في A وفي C لكلّ سنة: `late100_10` · `late50_10` · والأفقان 5/20 · والتصنيفُ الثلاثيّ بنِسَبه.
2. **هل التنبيهُ نفسُه يسبق الانفجارَ المتأخّر فوق الزخم؟** الشاهدُ `CM` (‏§③) بالمقياس نفسِه من إغلاق اليوم نفسِه: نسبةُ المراسي ÷ نسبةُ
   `CM` — وتُطبَع أيضًا مقابل الشاهد العشوائيّ `CR`.
3. **شروطُ المالك الثلاث** (RSI14 < 30 عند إغلاق الأمس · فلوت < 4م مؤرَّخًا · متاح < 20k مؤرَّخًا): على A كلٌّ وحدَها ثمّ مجتمعةً
   (المؤرَّخُ وحدَه يحكم · غيرُ المؤرَّخ وصفًا · المقامُ يُطبَع) · وعلى C **RSI وحدَه** (لا فلوتَ ولا متاحَ رجعيًّا — مقيسٌ 2026-09-25).
4. **B فارغة** ⇒ يُطبَع: «اختيارُ المالك الفعليّ لم يُختبر». وتُجاب الأسئلةُ الخمسة بوسمها (مؤكَّد/استنتاجٌ قويّ/فرضيّة): الفرقُ من
   **الأفق**؟ من **التنبيه نفسه**؟ من **المتاح غير المقيس**؟ من **العيّنة والذاكرة**؟ من **شيءٍ في عين المالك**؟ — الرابعةُ
   والخامسةُ **لا تُحسمان بلا B** ويُقال ذلك.

---

## ③ المجتمعُ والشواهد — مثبَّتةٌ قبل أيّ جلب

- **A (الاكتشاف):** كلُّ مرساةٍ من `anchor_history(since="2026-08-17")` حتى `until` (افتراضُه: آخرُ يومٍ اكتملت بعده 10 جلسات ·
  والأفقُ 20 على من اكتمل). **شاهدُ الهُويّة `V-P1`**: على `2026-08-18 ⟶ 09-01` تُعيد الأداةُ `exploded50` **‏33/321**
  و`exploded100` **‏14** بت-بت مع `T-OPLINK`/`T-TIERLINK`.
- **C (الحاكم · ثلاثُ سنوات):** صفوفُ `kasih_rows_2023/2024/2025.jsonl` من artifacts التشغيلات `32279408237` · `32279428391` ·
  `32279447496` (تُنزَّل بـ`gh run download` داخل الـworkflow · وإن تعذّر التنزيل **تُعاد بـ`kasih_scan` على S3** بالمفاتيح نفسِها ·
  وإلّا فالفرعُ «لا قياس»). **شاهدُ الهُويّة `V-P6`:** عددُ الصفوف لكلّ سنة **‏10,851 · 14,312 · 19,037** بت-بت مع `kasih_result.md`.
  المصدرُ يشمل المشطوبين ⇒ لا انحيازَ بقاء.
- **مصدرُ الشموع اليوميّة:** C: `/v2/aggs/grouped` بـ`adjusted=false` لكلّ يومِ تداولٍ من `2022-11-01` إلى `2026-02-15` (نداءٌ/يوم)
  + **تقسيماتُ السوق كلُّها** (`chart_finder.load_all_splits` **بالاسم** منذ `2022-10-01`) ⇒ تسويةُ كلّ نافذةٍ بـ`split_factor` ·
  A: `/v2/aggs/ticker` بـ`adjusted=true` للرمز (كما `T-OPLINK`) · **وشاهدُ الطريقة `V-P8`:** مسارُ C (grouped + تسوية) يُطبَّق على
  مراسي A نفسِها ويُقارَن `late100_10` صفًّا بصفّ ⇒ اتّفاقٌ ≥ 95% وإلّا تُطبَع الفروق ولا يُفسَّر C.
- **الشاهد `CM` (مطابقُ الزخم بلا مرساة · الحاكم):** لكلّ مرساةٍ رمزٌ من كون اليوم نفسِه **بلا مرساةٍ في ذلك اليوم** ولا في 20 جلسةً
  قبله، بخانة السعر نفسِها (`<1` · `1-3` · `3-10`)، وأقربُ عائدِ يوم 0 (`close0/close(−1) − 1`) وأقربُ `vol_x0`
  (حجمُ يوم 0 ÷ وسيطُ 20 يومًا) — بحدٍّ أقصى للفرق ‏±50% نسبيًّا وإلّا يُعَدّ «بلا شاهد» ويُطبَع. C: من grouped · A: من grouped
  الأيّام نفسِها. **بذرةٌ ثابتة 20260925** لكسر التعادل.
- **الشاهد `CR` (عشوائيّ):** رمزٌ عشوائيّ من كون اليوم نفسِه (السعرُ داخل \[`PRICE_LO`، `PRICE_HI`\]) بالبذرة نفسِها.
- **الضبطُ الداخليّ الحاكم للروابط:** غيرُ المنفجرين من المراسي نفسِها (المُكمِّل).

---

## ④ العائلاتُ العشر — الميزاتُ مُغلَقةٌ هنا (point-in-time · لا نظرَ مستقبليّ · ولكلٍّ أبكرُ لحظةِ رصد)

كلُّ ميزةٍ تُسلَّل ثلاثيًّا/رباعيًّا بحدودٍ مكتوبة هنا؛ **السلّةُ «الأعلى» تُعلَن مسبقًا** (بين قوسين) ولا تُختار بعد الأرقام.

1. **شمعةُ يوم 0** (تُرى عند الإغلاق): `close_pos0` = (close−low)/(high−low) (`≥0.7` · 0.3-0.7 · `<0.3`؛ الأعلى: ≥0.7) · `retreat0` =
   close0/سعر الكرت − 1 (`>0` · −10..0 · `<−10%`؛ الأعلى: >0) · `wick_up0` = (high−max(o,c))/high (`<5%` · 5-15 · `≥15`؛ الأعلى: <5) ·
   `vol_x0` (‏`<3` · 3-10 · `≥10`؛ الأعلى: ≥10) · `usd0` = close×volume (`<300k` · 300k-1M · `≥1M`؛ الأعلى: ≥1M) ·
   `above_low0` = close0 > أدنى شمعة المرساة (نعم/لا؛ الأعلى: نعم) · `tod` = وقتُ المرساة (‏pre · reg · after؛ الأعلى: pre — رابطُ `T-TIERLINK` يُعاد على الأفق الجديد) · **A فقط:** `ah0` = تغيّرُ الافتر بعد الإغلاق (`>+5%` · ±5 · `<−5`).
2. **ما قبل المرساة (1-20 جلسة · من الأمس فما قبل):** `range20` = (max high − min low)/min low على 20 جلسة (`<40%` · 40-120 · `≥120`؛
   الأعلى: <40) · `rsi14` (‏`<30` · 30-40 · 40-55 · `≥55`؛ الأعلى: <30) · `rsi_slope5` = rsi14 − rsi14 قبل 5 جلسات (`<−5` · ±5 · `>+5`؛
   الأعلى: >+5) · `vol_dry` = متوسّط حجم 5 ÷ متوسّط 20 (`<0.5` · 0.5-1.5 · `≥1.5`؛ الأعلى: <0.5) · `vol_x_prev` = حجم الأمس ÷ وسيط
   d−20..d−6 (`<3` · `≥3`؛ الأعلى: ≥3 — ميزةُ `T-LINK100` بحرفها) · `ret5_prev` = close(−1)/close(−6) − 1 (`≤+20%` · `>+20%`؛
   الأعلى: >20) · `dist_low60` = close(−1)/min low 60 − 1 (`<10%` · 10-30 · `≥30`؛ الأعلى: <10) · `sweeps20` = عددُ الجلسات التي
   كسر فيها low أدنى 10 جلساتٍ سابقة ثمّ أغلق فوقه (‏0 · 1-2 · ≥3؛ الأعلى: ≥3) · `failed_pumps60` = عددُ جلساتٍ high/close(−1) ≥ +30%
   ثمّ إغلاقٌ تحت +10% (‏0 · 1 · ≥2؛ الأعلى: 0).
3. **الهدوءُ بعد المرساة (الفوج k=1,2,3 · §①-8):** `held_low0_k` (كلُّ إغلاقٍ ≤k فوق low0؛ نعم/لا؛ الأعلى: نعم) · `inside_k` (عددُ
   الشموع الداخليّة في 1..k؛ الأعلى: =k) · `vol_dry_k` = متوسّط حجم 1..k ÷ حجم يوم 0 (`<0.3` · 0.3-1 · `≥1`؛ الأعلى: <0.3) · `close_k_vs_0`
   = close_k/close0 − 1 (`>0` · −15..0 · `<−15%`؛ الأعلى: >0). **A فقط (صفقات):** `bid_block_k` = كتلُ الطلب ≥1000 في يوم k (‏`_operator_blocks` بالاسم).
4. **ضغطُ المضارب — A فقط · مشروطٌ بـ⓪-ب · سقفُ 2 نداء/يوم × (يوم 0 + k≤3):** من `/v3/trades` (آخر ≤ 5,000 صفقةٍ قبل 16:00 لكلّ يوم ·
   `acc_components` و`_operator_blocks` **بالاسم**): `aggr_buy0` (‏`<45%` · 45-55 · `≥55`؛ الأعلى: ≥55) · `bid_block0` (نعم/لا) ·
   `dark0` (‏`<30%` · 30-50 · `≥50`) · `iceberg0` (‏`uniform_prints` نعم/لا) · ومن `/v3/quotes` عند 15:55-16:00: `spread0`
   (‏`<2%` · 2-5 · `≥5`) و`ask_thin0` = ask×ask_size ≤ $1,000 (نعم/لا؛ الأعلى: نعم — «تفريغُ العروض»). **NBBO قمّةُ الدفتر لا العمق — يُعلَن.**
5. **أحداثٌ معلَنةٌ سلفًا داخل النافذة — A فقط:** من `sec_recent_filings` **بالاسم** بتاريخ إيداعٍ ≤ يوم 0: `proxy_in_win` (DEF/PRE 14A
   وتاريخُ الاجتماع داخل 1-20؛ نعم/لا) · `offering_recent` (‏424B خلال 60 يومًا قبل يوم 0؛ نعم/لا) · `split_announced` (إيداعٌ يذكر
   تقسيمًا عكسيًّا قبل يوم 0 وتنفيذُه داخل النافذة؛ نعم/لا). **الأرباحُ والتجاربُ السريريّة بلا مصدرٍ as-of (مقيسٌ: `days_to_earnings`
   غيرُ قابلٍ للاختبار) ⇒ «لا قياس» صريحًا.** والخبرُ المفاجئ يومَ الانفجار **وصفٌ لا منبّئ**.
6. **البنية:** `px0` = close0 (`<1$` · 1-3 · 3-10؛ الأعلى: <1) · `rsplit_days` = أيّامٌ منذ آخر تقسيمٍ عكسيّ (‏`≤30` · 31-120 · >120 · لا؛
   الأعلى: ≤30) · `rsplit_count365` (‏0 · 1 · ≥2؛ الأعلى: ≥2) · **A فقط مؤرَّخًا:** `float_d` (‏`<4م` · 4-20 · ≥20) · `avail_d` (‏`<20k` · 20-100k ·
   ≥100k) · `fee` (‏`<20%` · 20-100 · ≥100) · `finra_short` (‏`<20k` · 20-40k · ≥40k) — المقامُ المؤرَّخ يُطبَع.
7. **تاريخُ الرمز مع المضارب:** `prior_anchors60` = مراسٍ سابقة للرمز في 60 جلسة (‏0 · 1 · ≥2؛ الأعلى: ≥2) · `days_since_anchor`
   (‏`≤5` · 6-20 · >20 · لا) · `prior_expl60` = جلساتٌ high/close(−1) ≥ +50% في 60 جلسة قبل يوم 0 (‏0 · 1 · ≥2؛ الأعلى: 0) ·
   **A فقط:** `hunter_prev` (ظهورٌ سابق في `hunter_watchlist`/«تحت المتابعة»؛ نعم/لا).
8. **التوليفات — العددُ مثبَّت:** شروطُ المالك الثلاث (‏§②-3) · **‏12 زوجًا مكتوبًا هنا لا أكثر:** `rsi14<30 ∧ range20<40` · `rsi14<30 ∧ vol_x0≥10` · `held_low0_1 ∧ vol_dry_1<0.3` · `held_low0_2 ∧ inside_2=2` · `above_low0 ∧ close_pos0≥0.7` · `vol_x_prev≥3 ∧ ret5_prev>20`
   · `px0<1 ∧ rsplit_days≤30` · `prior_anchors60≥2 ∧ above_low0` · `retreat0>0 ∧ usd0≥1M` · `dist_low60<10 ∧ rsi14<30` · `sweeps20≥3 ∧ above_low0` · `rsplit_count365≥2 ∧ vol_x0≥10` · **وثلاثيّةٌ واحدة:** `rsi14<30 ∧ above_low0 ∧ held_low0_1`.
9. **أنماطُ المنفجرين المتأخّرين:** جدولٌ صفٌّ/منفجر بكلّ الميزات (A كاملًا · C عيّنة 200 بالبذرة) ثمّ **تجميعٌ بالاتّفاق**: كلُّ
   توليفةٍ من ميزتين تظهر في ≥ 50% من المنفجرين تُقاس **دقّتُها على غير المنفجرين** بالمعيار §⑤ — **والتشابهُ بين المنفجرين وحدهم لا
   يُحتسب** (مثالُه المكتوب سلفًا: 73% من المنفجرين فلوتهم < 4م — و69% من غير المنفجرين كذلك).
10. **عمودُ «وش شفت فيه» من B:** **فارغٌ في هذه النسخة** ⇒ لا ميزة · ويُذكر.

**عددُ المقارنات المسجَّل للتعدّد:** أُحاديّات §④ (‏≈ 40 على C · ≈ 60 على A) + 13 توليفةً + أنماطُ §④-9 ⇒ `n_tests` يُطبَع و`z` بونفيروني
= `link100_probe.z_bonf(n_tests)` **بالاسم**.

---

## ⑤ معيارُ «رابط» — يُثبَّت الآن ولا يتحرّك

الميزةُ تُسمّى **«رابطًا»** فقط إن استوفت **كلَّها معًا** على `late100_10` (الحاكم) **بالسلّة المُعلَنة «الأعلى» في §④**:
1. **الدقّة لا الإثراء:** `P(late100_10 | السلّة الأعلى) ≥ 2 × P(late100_10 | المُكمِّل)`.
2. **فاصلا Wilson ‏95%** (`kasih_scan.wilson` بالاسم) **لا يتداخلان** بـ`z` بونفيروني على `n_tests` (لا `z=1.96`).
3. **n ≥ 20** في السلّة الأعلى **و** في المُكمِّل (A) · **n ≥ 50** (C).
4. **التكرار:** A — الاتّجاهُ نفسُه في نصفَي الفترة (الفاصلُ وسيطُ تواريخ المراسي · n ≥ 10 في السلّة الأعلى في كلٍّ) · C — الشروطُ 1-3
   **في كلّ سنةٍ من الثلاث**.
5. **يضيف على الزخم:** داخل كلّ طبقةٍ من طبقات `(ret0 bucket × vol_x0 bucket)` بـn ≥ 20، نسبةُ السلّة الأعلى ÷ المُكمِّل ≥ 1.5 **ومجمَّعًا
   (Mantel-Haenszel) ≥ 2×**. (ميزاتُ الزخم نفسُها `vol_x0`/`ret0` تُقاس بلا هذا الشرط ويُقال إنها زخم.)
6. **ويضيف على المرساة:** نسبةُ السلّة الأعلى ≥ 2 × نسبةِ `late100_10` في الشاهد `CM` (وإلّا فهي «تُعيد الزخم» لا ترابط بالتنبيه).

**والحكمُ يُقرأ بالأضعف:** عابرٌ على A وساقطٌ على C في سنةٍ واحدة = **غيرُ عابر**.

**الفروع:**
- **1 «رابطٌ مقيس»** — ميزةٌ واحدةٌ على الأقلّ تستوفي 1-6 على C **وعلى A** (أو A «لا قياس» بـn مع C عابرًا كاملًا — يُقال).
- **2 «لا رابط»** — التغطيةُ (`V-P4`) ≥ 80% وn كافٍ ولا ميزةَ تستوفي؛ وما يعبر 1-4 ويسقط على 5/6 يُنشَر «زخمٌ/مرساةٌ لا رابط».
- **3 «لا قياس»** — C غيرُ متاح (تعذّر artifacts وS3) أو التغطية < 80% أو `V-P1`/`V-P6`/`V-P8` ساقطة.

**ولا يُشحَن شيءٌ مهما كانت النتيجة**: لا بوّابةَ ولا عتبةَ ولا سطرَ عرض. الفرعُ 1 يُقرأ **«قيدَ الإثبات الأماميّ»** ويُسجَّل حصّادٌ أماميّ
بعقدٍ مستقلّ إن أمر المالك.

---

## ⑥ سؤالُ التمركز — سياساتٌ مسجَّلة (تُقاس على C ثلاثَ سنوات · بالـR · بعد التكلفة)

- **المقيسُ سلفًا ويُراعى:** الدخولُ لحظةَ المرساة مكلف — وسيطُ إغلاق يوم المرساة من سعر الكرت **−6.5%** (`T-TIERLINK`) والحملُ حتى
  أوّل إغلاقٍ نظاميّ **−2.10R** للصفقة (`T-OPHOLD-2` وصفيٌّ بلا حكم) ⇒ **لا سياسةَ هنا تدخل لحظةَ المرساة**.
- **`POS-0`** (الحاكمة): دخولٌ عند `close0` · وقفٌ = `low0 × (1 − 2%)` (تحت أدنى يوم 0) · خروجٌ بهدف +100% أو **بالوقت بعد 10 جلسات**
  عند إغلاقها · التنفيذُ عند الإغلاق التالي للإشارة · **التكلفة**: انزلاقٌ 1% للطرف + عمولةٌ صفر (‏1% `engineering` مكتوبٌ هنا — `T-OPTRADE` يستخرج تكلفته نصًّا من `T-BTCOST` وهي كلفةُ دورةٍ لحظيّة لا يوميّة).
- **`POS-k`** (‏k=1,2,3): كما `POS-0` لكنّ الدخولَ عند `close_k` **بشرط `held_low0_k`** والوقفَ تحت `min(low0..low_k)` والخروجَ بعد
  10 جلساتٍ من الدخول.
- **`POS-L`**: `POS-0` مقيَّدةً بكلّ رابطٍ عابرٍ في §⑤ (إن وُجد).
- **الضبط:** `C-MOM` = `POS-0` على الشاهد `CM` بالسياسة نفسِها · `C-FILL` = مطابقُ التعبئة (المُعبَّأ فقط في الذراعين).
- **الحكم (بالأضعف على السنوات الثلاث):** «التمركزُ بين التنبيه والانفجار ممكنٌ بتوقّعٍ موجبٍ بعد التكلفة» فقط إذا كان متوسّطُ R لـ`POS-*`
  **موجبًا في كلّ سنة** وفاصلُ البوتستراب (‏1,000 · بذرة 20260925) لا يلمس الصفر **و`POS − C-MOM` موجبًا** · وإلّا «غيرُ ممكن»
  ويُطبَع: **متى يصير موجبًا؟** (أصغرُ k أو رابطٌ يقلبه إن وُجد).

---

## ⑦ التنبّؤاتُ القابلة للكسر (قبل الأرقام — تُنشَر ولو خابت)

- **`PL-P1`** معدّلُ `late100_10` في C بين **3% و8%** في كلّ سنة، و`late50_10` بين 10% و22%.
- **`PL-P2`** المراسي ÷ `CM` على `late100_10` بين **1.3× و2.5×** (التنبيهُ يضيف على الزخم لكنه لا يضاعف مرّتين ونصف).
- **`PL-P3`** لا ميزةَ من العائلات 1-2 و6-7 تستوفي 1-6 في السنوات الثلاث (نمطُ الأحكام الأحد عشر السابقة: الحافّة = التوقيت).
- **`PL-P4`** `held_low0_2` (العائلة 3) **تفصل** على A بالشروط 1-3 **وتسقط** على C على الشرط 5 (تعيد الزخم) أو على سنةٍ واحدة.
- **`PL-P5`** شروطُ المالك الثلاث مجتمعةً على A: n < 20 ⇒ «لا قياس»؛ و`rsi14<30` وحدَه على C < 1.5× في السنوات الثلاث.
- **`PL-P6`** `POS-0` سالبةٌ بعد التكلفة في السنوات الثلاث (وسيطُ R < 0) · و`POS-2` أعلى من `POS-0` لكنها لا تعبر الصفر بفاصلها.
- **`PL-P7`** `rearmed` بين المنفجرين المتأخّرين ≥ 40% (الانفجارُ المتأخّر يأتي غالبًا بتنبيهٍ جديد — وهو ما يعمل عليه الزنادُ الحيّ).
- **`PL-P8`** الأفقُ يفسّر جزءًا من فرق تجربة المالك: `late100_20` ≥ 1.5 × `exploded100` نفس اليوم على A.

---

## ⑧ حرّاسُ الصلاحيّة (يُطبَعون قبل أيّ رقمٍ حاكم)

- **`V-P1`** هُويّةُ A: `exploded50` **‏33/321** و`exploded100` **‏14** على `08-18 ⟶ 09-01` — وإلّا لا يُفسَّر رقمُ A.
- **`V-P2`** التقسيم: كلُّ صفٍّ `split_in_win` يُوسَم ويُعَدّ؛ الجداولُ الحاكمة تُطبَع **مرّتين** (به وبدونه) ويُقرأ الحكمُ بالأضعف.
- **`V-P3`** صفرُ نظرٍ مستقبليّ: ميزاتُ العائلات 1-2 و6-7 من صفوفٍ ≤ يوم 0 (وميزاتُ 2 ≤ يوم 0 − 1) · ميزاتُ الفوج k ≤ يوم k · والنتيجةُ
  > k — **قفلٌ على صفٍّ مصنوع** يحمل شمعةَ يوم النتيجة يجب ألّا تدخل.
- **`V-P4`** التغطية: (المراسي المكتملة النافذة والمقيسة ÷ الكلّ) ≥ 80% لكلّ سنة (C) وللفترة (A) وإلّا خروج 3.
- **`V-P5`** قراءةٌ فقط بالـAST (صفرُ إرسال · صفرُ كتابةِ حالة · الإنتاجُ لا يستوردها) · بلا مفتاح ⇒ خروج 2 قبل أيّ قراءة.
- **`V-P6`** هُويّةُ C: صفوفُ `kasih` **‏10,851 · 14,312 · 19,037** بت-بت.
- **`V-P7`** تغطيةُ grouped: أيّامُ التداول المجلوبة ≥ 98% من تقويم كلّ سنة (`market_calendar` بالاسم) وإلّا تُطبَع الأيّامُ الناقصة ويُقيَّد الحكم.
- **`V-P8`** شاهدُ الطريقة: مسارُ C على مراسي A ⇒ اتّفاقُ `late100_10` صفًّا بصفّ ≥ 95% وإلّا لا يُفسَّر C.
- **`V-P9`** الشاهدان `CM`/`CR` بحجم المراسي نفسِه يوميًّا (أو «بلا شاهد» مطبوعًا) وبذرةٌ مطبوعة.

---

## ⑨ حدودُ صدقٍ مكتوبةٌ قبل النتيجة

- `late100_10` **لمسُ قمّةٍ لا تنفيذ** — الحكمُ على الربحيّة في §⑥ وحدَه.
- **C تقريبٌ لتنبيهات الإنتاج** (كونٌ محدودُ السعر · مرساةٌ/رمز/يوم · بلا بوّابة المضارب ولا فلتر التسليم) — مطابقٌ بـ`V1` في
  `kasih_result.md` على يومٍ واحد؛ الفرقُ عن التسليم الحيّ **لا يُقاس هنا**.
- A ‏≈ 6 أسابيع (والأفقُ 20 على أقلَّ منها) ⇒ أيُّ رابطٍ على A وحدَه «قيدُ الإثبات».
- الفلوت/المتاح/الأحداث/الصفقات **A فقط** ومنحازةٌ لقوائمنا (المخزَّنُ المؤرَّخ) ⇒ تُقرأ بالمقام المطبوع.
- الأحداثُ الطبيّة والأرباح **بلا مصدرٍ as-of** ⇒ لا تُقاس (لا يُخمَّن تاريخ).
- **B فارغة** ⇒ سؤالُ «ليش تجربتي تختلف» يُجاب في شقّه القابل للقياس (الأفق · التنبيه · المتاح) ويبقى شقُّه الشخصيّ (العيّنة · العين) **مفتوحًا**.

---

## ⑩ ملحقٌ مؤرَّخ (‏2026-09-25) — «فوق الدولار»: الشروطُ نفسُها بعد استبعاد أسهم السنتات

> **أمرُ المالك بعد الحكم (نصُّه):** «ياخي مب منطق كيف كذا؟ طيب استخدم هذي الشروط على الأسهم اللي سعرها فوق الدولار
> استبعد أسهم السنتات و نشوف النتيجة». **مدفوعٌ ومدموجٌ قبل أيّ رقمٍ من المقطع.** قراءةٌ فقط · لا شيءَ يُشحَن مهما كانت
> النتيجة · **والمعيارُ §⑤ بحرفه** (‏`z=3.33` نفسُه · n ≥ 20 على A · n ≥ 50 على C · الأضعف) — **لا يُرخى شيءٌ لأن المقطعَ أصغر.**

**⑩-1 ما هو مرئيٌّ سلفًا (إفصاحٌ قبل الأرقام).** سلّةُ `px0` «أقلّ من 1$» في الحاكمة `36163255276` — وهي إغلاقُ يوم 0
**مسوًّى بالتقسيم** إلى آخر النافذة لا السعرُ الخامّ — أظهرت على `late100_10`: A ‏16/92 = 17.4% مقابل 19/319 = 6.0% ·
C2023 ‏4.7% مقابل 5.3% · C2024 ‏7.9% مقابل 7.2% · C2025 ‏8.4% مقابل 5.8% ⇒ **معدّلُ الأساس فوق الدولار مرئيٌّ تقريبًا**
(A ‏≈6% · C ‏≈5-7%) **ولا يُكتب تنبّؤًا**. ويُنبَّه: قرابةُ نصف متأخّري A (‏16 من 35 بالسلّة المسوّاة) دون الدولار ⇒ المقطعُ على A
صغير. **وغيرُ المرئيّ:** شروطُ المالك داخل المقطع · الـ58 داخله · التمركزُ داخله · والشاهدُ `CM` داخله.

**⑩-2 التعريف.** «فوق الدولار» = **سعرُ الكرت الخامُّ لحظةَ التنبيه ≥ $1.00** (‏حقلُ `a.entry` في الصفوف: A ‏`e5` بالاسم ·
C ‏`entry` من `kasih_scan` = سعرُ دقيقةِ المرساة) — **الحاكم**. والحسّاسيّة: **إغلاقُ يوم 0 الخامّ ≥ $1.00**
(‏`close0 × entry ÷ entry_adj` — يُزيل تسويةَ التقسيم). **والحكمُ يُقرأ بالأضعف بين التعريفين.** صفٌّ بلا سعرٍ خامّ يُعَدّ
ويُستبعد ولا يُخمَّن. والحدُّ 1.00 **ثابتٌ في الأداة لا مُدخَل** (لا تجربةَ عتبةٍ بعد الرقم).

**⑩-3 المصدر.** **صفوفُ الحاكمة نفسُها** (artifact `prelink-rows-36163255276` · احتفاظ 90 يومًا) — لا جلبَ جديدًا ولا Polygon
⇒ المقطعُ **جزءٌ من المجتمع المنشور** لا مجتمعٌ آخر. والشاهدان يتبعان مرساتَهما كما بُنيا (`CM` بخانة السعر نفسِها ·
و`CR` عشوائيٌّ قد يكون دون الدولار ⇒ وصفٌ لا حكم). وتجديدُ المرساة على C من صفوف السنة نفسِها كما في الحاكمة.

**⑩-4 حارسُ الهُويّة `V-X1` (قبل أيّ رقمٍ من المقطع).** إعادةُ الحساب على **المجتمع الكامل** من الصفوف تُعيد المنشورَ
بت-بت: الصفوف A ‏413 · C ‏10,842 / 14,299 / 19,010 · `late100_10` ‏35/411 · 560/10,842 · 1,046/14,299 · 1,205/19,010 ·
متوسّطُ `POS-0` ‏−0.545 · −0.322 · −0.240 · −0.280 (ثلاثُ خانات) · وتجديدُ المرساة على C ‏523/560 · 953/1,046 · 1,111/1,205 ·
**والفرعُ 2** — وإلّا خروج 3 «لا قياس» ولا يُطبع رقمٌ من المقطع. و`V-X2`: السعرُ الخامّ حاضرٌ في ‏≥95% من صفوف كلّ مجتمع وإلّا
يُطبع النقصُ ويُقيَّد الحكم.

**⑩-5 ما يُقاس (بالترتيب · بالدوالّ نفسِها بالاسم):**
1. الأساس داخل المقطع: الآفاق 5/10/20 · التصنيفُ الثلاثيّ · المراسي ÷ `CM` و`CR` · المتأخّرون وتجديدُ المرساة قبل القمّة.
2. **شروطُ المالك** داخل المقطع بالمعيار الستّة: على A ‏`rsi14` «أقلّ من 30» · `float_d` «أقلّ من 4م» · `avail_d` «أقلّ من
   20k» · `owner3` (الثلاثةُ مجتمعةً مؤرَّخًا) · وعلى C ‏`rsi14` وحدَه (لا فلوتَ ولا متاحَ رجعيًّا — مقيسٌ 2026-09-25).
3. البطاريّةُ الكاملة (الـ58) داخل المقطع بالمعيار نفسِه.
4. التمركز `POS-0`…`POS-3` و`C-MOM` و`POS-0 − C-MOM` بحكم §⑥ نفسِه.

**⑩-6 الفروع.** **1 «رابطٌ فوق الدولار»** — ميزةٌ (من 2 أو 3) تستوفي 1-6 على C في السنوات الثلاث **بالتعريفين معًا** وعلى A
(أو A «لا قياس») · **2 «لا رابط فوق الدولار»** — `V-X1` عابرٌ ولا ميزة · **3 «لا قياس»** — `V-X1` ساقطٌ أو الصفوفُ غائبة.
**ولا يُشحَن شيءٌ في أيّ فرع** — والفرعُ 1 «قيدُ الإثبات الأماميّ».

**⑩-7 التنبّؤات (قبل الأرقام — تُنشَر ولو خابت):**
- **`PX-P1`** المراسي ÷ `CM` داخل المقطع على C بين 1.0× و2.0× في السنوات الثلاث (لا تضاعف).
- **`PX-P2`** `rsi14` «أقلّ من 30» داخل المقطع على C دون ضعفَي مُكمِّلها في كلّ سنة ⇒ يسقط على ①.
- **`PX-P3`** `owner3` داخل المقطع على A: من استوفاها أقلّ من 20 ⇒ «لا قياس».
- **`PX-P4`** لا ميزةَ من الـ58 تعبر داخل المقطع ⇒ الفرعُ 2.
- **`PX-P5`** `POS-0` داخل المقطع سالبٌ بعد التكلفة في السنوات الثلاث · و`POS-0 − C-MOM` سالبٌ في سنتين على الأقلّ.
- **`PX-P6`** تجديدُ المرساة قبل القمّة بين متأخّري المقطع على C ‏≥80% في كلّ سنة.

**⑩-8 حدودُ صدق (قبل النتيجة).** مقطعٌ لاحقٌ بطلب المالك **بعد** رؤية الحكم الكامل ⇒ يُقرأ **فحصًا لفرضيّته** («السنتات
تُفسد القياس») لا اكتشافًا جديدًا · والفلوتُ/المتاحُ المؤرَّخان على A وحدَها وقليلان · وB فارغة كما هي.

**⑩-9 قراءةُ التنبّؤات آليًّا (‏2026-09-25 · مكتوبةٌ مع الأداة وقبل تشغيلها — لا تُغيّر معيارًا ولا تنبّؤًا).** الأداةُ
`prelink_px.py` تطبع لكلّ تنبّؤٍ حالةً من ثلاث: **✅** صحيحٌ في كلّ ما يشمله · **❌** كذّبه جزءٌ واحد · **⚪** لا تكذيبَ لكن فيه «لا حكم».
والحاكمُ **تعريفُ الكرت** والحسّاسيّةُ تُطبع بجواره.
- `PX-P1`: النسبةُ = معدّلُ `late100_10` في المقطع ÷ معدّلُ `CM` في المقطع (الشاهدُ كما بُني) · داخل [1.0, 2.0] شاملًا في كلّ سنة ·
  وشاهدٌ غائبٌ أو معدّلُه صفر ⇒ ⚪.
- `PX-P2`: المعيارُ ① لـ`rsi14` «أقلّ من 30» **ساقطٌ** في كلّ سنةٍ من C · و«لا حكم» (n دون 50) ⇒ ⚪.
- `PX-P3`: صفوفُ A في المقطع التي `owner3` فيها «نعم» أقلُّ من 20.
- `PX-P4`: لا رابطَ في المقطع بالتعريف · والفرعُ بتقاطع التعريفين (§⑩-6).
- `PX-P5`: **متوسّطُ** `POS-0` (كما يُطبع · لا الفاصل) سالبٌ في كلّ سنةٍ من C **و**متوسّطُ `POS-0 − C-MOM` سالبٌ في سنتين على الأقلّ.
- `PX-P6`: `k/n ≥ 0.80` لتجديد المرساة في كلّ سنةٍ من C · وn صفر ⇒ ⚪.
- **ومصدرُ الصفوف مثبَّتٌ في الـworkflow** (‏`36163255276` لا مُدخَل) · وصفٌّ بلا سعرٍ خامّ يُعَدّ ولا يُخمَّن · وسعرٌ محسوبٌ يقلّ عن
  1.00 بأقلَّ من ‏10⁻⁹ يُعَدّ دولارًا (خطأُ فاصلةٍ عائمة لا سعر).
