# 🔥🩸 `T-FUSE` «سجّل الفتيل» — النتيجة (2026-08-27)

> **العقد:** `fuse_prereg.md` مدفوعٌ `323b474f` **قبل أيّ سطرِ أداةٍ وقبل أيّ
> رقم** (+ تعديل ① مؤرَّخ: إعادةُ تسمية صورٍ خالصة). **الأداة:** `fuse_arms.py`
> على كوميت `0fc663fd`. **التشغيلات** (السنةُ مقروءةٌ من مُخرَج كلّ تشغيلةٍ لا
> من ترتيب الإطلاق): 2023=`33056047250` · 2024=`33056058342` ·
> 2025=`33056066445` — على لقطات PIT الثلاث (‏`31574165523` · `31199493957` ·
> `31191327731`). قراءةٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION`.

---

## ⓪ الخلاصة في سبعة أسطر

1. 🔴 **فشلت — والعيّنةُ هائلة (‏10,791 محسومةً في الحاكمة) فالفشلُ حقيقيٌّ لا نقصَ عيّنة.**
2. «الفتيل» (كُنسٌ + قاعٌ طازج 0-2ج) **أدنى من مُكمِّله**: الفرقُ المجمَّع **‏−0.079R** (الحدُّ +0.15) والإشارةُ تنقلب سنويًّا.
3. 🔑 **والاكتشافُ انقلابُ الأطروحة: `F2` (كُنسٌ ثم عاد فحفظ ‏3ج فأكثر) أعلى خليّةٍ في التجربة كلِّها** — ‏+0.225R مجمَّعًا (فوق `HOLD3` نفسِها +0.174R) وموجبةٌ في السنوات الثلاث.
4. ⇒ **الكنسةُ علامةٌ لا لحظةَ دخول** — الدخولُ المقيس بعد أن **يعود القاعُ الجديد فيحفظ**، وهو حرفيًّا سلوكُ فيصل الحيّ («هل يثبت اولا» · WNW «اختبر القاعَ ولا كسره وثبت»).
5. ✅ **والبنيةُ حقيقية** (‏`FU-P2` تحقّق): `F1` فوق `F3` (طازجٌ بلا بنية) **في السنوات الثلاث** — فوسمُ 🩸 يصلح **ترتيبَ انتباهٍ** بين القيعان الطازجة، لا زنادًا.
6. 🥇 **بوّابتا التكامل عبرتا بت-بت**: `F-H3` مجمَّعةً تعيد §⑬ حرفيًّا (‏5371 · 19.08% · +0.174R) · وصفرُ حلقةٍ خارج الخلايا وصفرُ حلقةٍ لم تُثرَ في الثلاث.
7. ⛔ **الأثرُ الإنتاجيّ صفر** — بل الحكمُ **يصادق الإنتاجَ القائم** (‏`READY_HOLD=3` بوّابةُ «🟢 جاهز» والمكنوسُ الطازجُ في المتابعة موسومًا): التصميمُ الحاليّ هو بالضبط ما اختارته الأرقام.

## ① بوّابات الصلاحية — كلُّها خضراء

| البوّابة | النتيجة |
|---|---|
| `FV0`-أ (اتّساق الحلقة مع `press_read` عند فهرسها) | ✅ **صفرُ تفرّقٍ في 20,182 حلقة** (لا `mismatch` ولا `age_mismatch` ولا `gone` — سطرُ «حلقاتٌ لم تُثرَ» غائبٌ من التشغيلات الثلاث = القاموس فارغ) |
| `FV0`-ب (مِرساة §⑬) | ✅ **بت-بت**: محسومة 1660+1857+1854 = **5371** · فائزة 351+333+341 = 1025 ⇒ **‏19.08%** · E = **‏+0.174R** |
| `FV1` (لا `no-op`) | ✅ حلقات 6625/6515/7042 · الحاكمة ممتلئة |
| `FV2` (قفل `age` بت-بت مع `high_w`) | ✅ صفرُ `age_mismatch` في الثلاث |
| `FV3` (لا قصَّ صامتًا) | ✅ «خارج الخلايا: 0» مطبوعةٌ في الثلاث |
| `FV4` (الأقفال + الطفرات) | ✅ §⑥ أدناه |

## ② الخلايا — مجمَّعةً عبر الثلاث (‏r_win=5.15 في التشغيلات كلِّها)

| الخليّة | ن | محسومة | بلغ الهدف | Wilson | E | no_fill |
|---|---|---|---|---|---|---|
| `F-ALL` | 20,182 | 19,337 | 16.61% | [16.1·17.1] | +0.021R | 2.6% |
| `F-H3` (مِرساة) | 5,706 | 5,371 | 19.08% | [18.1·20.2] | **+0.174R** | 5.2% |
| 🔥 **`F1` الفتيل (الحاكمة)** | **11,188** | **10,791** | **16.04%** | **[15.4·16.7]** | **‏−0.013R** | 1.5% |
| **`F2` كُنسٌ·عاد فحفظ** | 3,851 | 3,609 | **19.92%** | **[18.7·21.3]** | **‏+0.225R** | 5.5% |
| `F3` طازجٌ بلا بنية | 3,288 | 3,175 | 14.33% | [13.2·15.6] | ‏−0.119R | 2.0% |
| `F4` جلوسٌ هادئ | 1,855 | 1,762 | 17.37% | [15.7·19.2] | +0.068R | 4.4% |
| `F̄1` المُكمِّل | 8,994 | 8,546 | 17.32% | [16.5·18.1] | +0.065R | 4.0% |

وسنةً بسنة (فرقُ الحاكمة عن مُكمِّلها `E(F1) − E(F̄1)`):

| السنة | E(F1) | E(F̄1) | الفرق |
|---|---|---|---|
| 2023 | ‏−0.109R | +0.126R | **‏−0.234R** |
| 2024 | +0.014R | +0.035R | ‏−0.021R |
| 2025 | +0.058R | +0.043R | +0.015R |
| **مجمَّعًا** | **‏−0.013R** | **+0.065R** | **‏−0.079R** |

## ③ الحكم بمعايير §④ — كما سُجِّلت حرفيًّا

1. فرقُ التوقّع ‏+0.15R فأكثر مجمَّعًا: 🔴 **‏−0.079R** — سالبٌ أصلًا.
2. موجبٌ في السنوات الثلاث: 🔴 **ينقلب** (‏−0.234 · −0.021 · +0.015).
3. فاصلا Wilson منفصلان: 🔴 متداخلان (‏[15.4·16.7] مع [16.5·18.1]) — **والاتّجاهُ معكوس** (المُكمِّلُ أعلى).
4. الأرضية (‏40/سنة · 150 مجمَّعًا): ✅ مستوفاةٌ بـ**‏72 ضعفًا** (‏3746/3360/3685).

⇒ **بنصّ الطرف السالب المكتوب سلفًا في §④:** الوسمُ 🩸 يبقى عرضًا كما هو،
**وتوقيعةُ الكتالوج (‏91% عشيّة الانفجار) التقاطٌ بلا تمييزٍ عكسيّ** — والرقمُ
البنيويُّ الذي يفسّرها: **حالةُ «الفتيل» تغطّي 55.4% من كلّ حلقات الجلوس
المضغوط** (‏11,188 من 20,182) ⇒ حالةٌ يقضي فيها المضغوطُ نصفَ حياته
**يستحيل أن تكون إشارةَ توقيت** — نظيرُ درس «الأحمر 40% من الكروت» في
`red_mute_result` من الجهة المقابلة.

## ④ الاكتشافات — أثمنُ من الحكم

### 🥇 ① الانقلاب: المالُ في «عاد فحفظ» لا في الكنسة — تأكيدٌ ثالثٌ مستقلٌّ لقاعدة فيصل

`F2` (كُنس قاعٌ محفوظٌ ثم تكوَّن قاعٌ جديدٌ **وحفظ 3 جلساتٍ فأكثر**) هي
**أعلى خليّةٍ في التجربة**: ‏+0.365R · +0.192R · +0.121R — **موجبةٌ في
الثلاث بلا انقلاب**، ومجمَّعُها **‏+0.225R فوق `HOLD3` نفسِها** (‏+0.174R)،
وفاصلُها [18.7·21.3] **منفصلٌ تمامًا** عن فاصل الحاكمة [15.4·16.7].
⇒ «**اذا حافظ ع ادنى قاع طبق النموذج**» تُقاس صحيحةً للمرّة الثالثة
(‏`T-PRESS-BT-2` ⟶ اعتمادُ `READY_HOLD=3` ⟶ وهنا)، **والكنسةُ قبل الحفظ
تُحسِّن فوقها** (‏`F2` فوق بقيّة `H3`: ‏+0.225 مقابل +0.068) — ⚠️ بتحفّظ:
هذا الفرقُ الأخير ينقلب في 2025 (‏+0.121 مقابل +0.151) فيُنشَر ولا يُبنى عليه.
⚖️ **وترقيةُ `F2` حاكمةً بعد الأرقام ممنوعةٌ بالبناء** (الحاكمةُ سُمّيت
سلفًا `F1`) — لكنّ اتّجاهها يطابق حكمًا منشورًا سلفًا (‏`HOLD3`) لا اكتشافًا
بعديًّا خالصًا.

### 🥇 ② البنيةُ حقيقيةٌ والطزاجةُ هي القاتل (`FU-P2` تحقّق)

`F1 − F3` (نفسُ الطزاجة، بفارق بنية الحفظ-فالكنس): **‏+0.095 · +0.097 ·
+0.147R — موجبٌ في السنوات الثلاث بثباتٍ لافت.** ⇒ الكنسةُ بعد حفظٍ **تحمل
معلومةً حقيقية** فوق أيّ قاعٍ طازجٍ خام — لكنها لا تكفي لقلب سالبِ الطزاجة
(‏`F1` تبقى دون مُكمِّلها). **فالوسمُ 🩸 القائمُ في الرادار (المكنوسُ يتقدّم
المتابعةَ موسومًا) مسنودٌ ترتيبَ انتباهٍ — ويبقى كما هو بلا ترقية.**

### 🥇 ③ القاعُ الطازج يتعبّأ أسهلَ ويكسب أقلّ (`FU-P6` تحقّق)

`no_fill` في `F1` **‏1.5%** مقابل **‏4.4%** في `F4` (وكلَّ سنةٍ على حدة:
‏1.8/1.6/1.3 مقابل 2.9/3.8/6.1) ⇒ لحظةُ الكنسة تُعبِّئ طلباتِك فورًا —
**لأن السكّين ما زالت حيّة**؛ والجالسُ الحافظ يتعبّأ أقلّ لكنه حين يتعبّأ
يكسب أكثر. سهولةُ التعبئة **ليست ميزة** — هي عَرَضُ التوقيت الخاطئ.

## ⑤ بطاقة التنبّؤات — تُنشَر كما سُجِّلت

| التنبّؤ | النتيجة |
|---|---|
| `FU-P1` («تعبر ①③ لكن دون 30%») | 🔴/✅ **شقُّ العبور مكذَّب** (لم تعبر أصلًا) · وشقُّه الجوهريّ تحقّق بأوسعَ ممّا توقّعت: **‏16.04% فقط** — «الأغلبُ لا ينفجر حتى بعد الكنسة» |
| `FU-P2` (‏`F1−F3` موجبٌ في الثلاث) | ✅ **حرفيًّا** (‏+0.095/+0.097/+0.147) |
| `FU-P3` (‏`age ≥ 20` يرفع داخل `F1`) | 🔴 **مكذَّب — ينقلب**: 2023 نعم (‏15.30 مقابل 11.97) · 2024 لا (‏16.35 مقابل 16.81) · 2025 معكوس (‏16.79 مقابل 18.26) ⇒ «رزق لم يكتب» **لم يظهر له أثرٌ ثابت** بهذا التقريب (عمرُ الهبوط من قمّة النافذة لا من شمعة الهبوط حرفيًّا — حدُّ صدق) |
| `FU-P4` (‏`F2` بين `F1` و`F4`) | 🔴 **مكذَّبٌ بعنف — `F2` فوقهما معًا** وفوق كلّ الخلايا (الاكتشاف ①) |
| `FU-P5` (الشمعةُ الانعكاسية لا تضيف — فواصل متداخلة) | ✅ بحرفه (الفواصلُ متداخلةٌ في الثلاث) — ⚠️ **مع أمانةٍ ضدّي: الاتّجاهُ موجبٌ في السنوات الثلاث** داخل `F1` (‏−0.075⟶+0.114⟶+0.230 مقابل خليّتها) — أوّلُ نمطِ شموعٍ يعطي اتّجاهًا ثابتًا عندنا؛ يُنشَر ولا يُرقّى (‏§⑧-5) |
| `FU-P6` (‏`no_fill` في `F1` أدنى من `F4`) | ✅ **في الثلاث** (الاكتشاف ③) |

**وممّا يُنشَر من الاستكشافيات بلا حكم:** داخل `F-H3` «المستوى المُختبَر
غائب» أعلى من «حاضر» في الثلاث (‏+0.392/+0.136/+0.146 مقابل
+0.078/+0.021/+0.090) — اتّجاهٌ غريبٌ ضدّ حدس `WNW`، فرقُه الكبير في 2023
وحدَها والفواصلُ متداخلة ⇒ لا يُقرأ منه شيء · و«قفزةُ الحجم» داخل `F-H3`
لافتةٌ (‏23-24% في 2024/2025) بمقامٍ هزيل (‏39-43) ⇒ ضجيج.

## ⑥ الأقفال والطفرات (`FV4`)

أقفال `FU1`-`FU8` قائمةٌ في السويّة (‏3,399 اختبارًا · صفر فشل). جولةُ
الطفرات العشر — كلٌّ على قفله المسمّى:

**‏10 من 10 سقطت، كلٌّ بقفلها المسمّى، وصفرُ انهيارٍ وصفرُ ناجية:**

| الطفرة | القفل الساقط |
|---|---|
| `mf1` ‏`FRESH_MAX` 2⟶1 | `FU6` (ومعه `FU2` — تخومُ الخليّة تُشتقّ من الثابت نفسِه) |
| `mf2` قلبُ `cell_of` (‏F1↔F2) | `FU2` |
| `mf3` تعريفُ دالّةِ محرّكٍ محلّيًّا (`mirror_plan`) | `FU1` |
| `mf4` إسقاطُ نداء `walk_symbol_wake` (‏`eps = []`) | `FU1` |
| `mf5` إزاحةُ `age_of` بواحد | `FU3` (ومعه `FU7` — الإثراءُ يقرؤها) |
| `mf6` نداءُ `send_telegram` في `main` | `FU4` |
| `mf7` فكُّ وصلة `year` في الـworkflow (قيمةٌ مغروسة) | `FU5` |
| `mf8` إسقاطُ سطر «خارج الخلايا» | `FU8` |
| `mf9` الحاكمةُ الفارغة ترجع 0 بدل 4 | `FU6` |
| `mf10` إماتةُ فحص `mismatch` | `FU7` |

المِنصّة: مرساةٌ تُشترط فريدةً (‏count=1) · استعادةٌ بعد كلّ طفرةٍ مع
`SIGTERM` حارسًا · والمقياسُ رمزُ الخروج + أسماءُ الأقفال + كشفُ الانهيار.
والشجرةُ بعد الجولة **مطابقةٌ بت-بت** (‏`git diff` صفر).

## ⑦ حدودُ صدقٍ تُقرأ مع الحكم

1. «بلغ الهدف» **لمسٌ لا تنفيذ** والهدف ×1.5 بخطّة المرآة — **ليست ربحيةَ البوت**.
2. بلا افتر (‏`wake_read` أرضيّة) · `w=40` حصرًا · وانحيازُ بقاءٍ مخفَّفٌ لا معدوم.
3. `age` يُقاس من **قمّة نافذة الأربعين** لا من «شمعة الهبوط» بلفظ فيصل الحرفيّ — تقريبٌ مُعلَن، وسقوطُ `FU-P3` يُقرأ على هذا التقريب لا على قاعدته.
4. تفوّقُ `F2` على بقيّة `H3` ينقلب في 2025 — يُنشَر وصفًا لا حكمًا.
5. توقيعةُ الكتالوج (‏91%) قِيست على عيّنةٍ مختارةٍ على النتيجة — وهذي التجربةُ **هي اختبارُها العكسيّ وقد أسقطته**: الالتقاطُ الموجب حقيقيّ، والتمييزُ العكسيّ معدوم (الحالةُ 55% من الكون).
6. الاستكشافياتُ خمسٌ على نفس البيانات ⇒ أيُّ خيطٍ منها يلزمه تسجيلٌ مستقلٌّ قبل أيّ بناء.

## ⑧ التوصية — بسقف §⑦ المثبَّت سلفًا

**لا تغيير.** الوسمُ 🩸 يبقى عرضًا/ترتيبَ انتباهٍ كما هو (وسندُه الآن مقيسٌ
من `FU-P2`)، وبوّابةُ «🟢 جاهز» تبقى `READY_HOLD=3` — **وقد خرجت من هذي
التجربة أقوى**: أعلى خليّةٍ مقيسة (‏`F2` ‏+0.225R) هي بالضبط «كُنس ثم عاد
فحفظ 3 جلسات» التي يلتقطها التصميمُ القائم (الكنسةُ تَسِم والحفظُ يُجهِّز).
وجوابُ هدف المالك («ادخل وخلال أسبوعٍ ينفجر»): **الفتيلُ يُشعَل عند الكنسة
لكنّ الدخولَ المقيسَ الوحيدَ الموجبَ بعد أن يثبت القاعُ الجديد** — وهو عينُ
ما يفعله فيصل حيًّا («هل يثبت اولا» على ONCO · وWNW دخل بعد «اختبر 2.30 ولا
كسرها وثبت فوق 2.40»).
