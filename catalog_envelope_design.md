# 📐 ظرف الكاتالوج — قرارات التصميم (تُثبَّت **قبل أيّ رقم**)

> **فكرة المالك حرفيًّا (‏2026-08-05):** «الحدّ الأدنى المقصود فيها مثلًا من الكتالوج
> **سهم واحد فقط RSI 23 فيكون هذا الحدّ الأدنى — وقِسها على باقي النواقص**» ·
> «نبي نسوي **مزيج الحدّ الأدنى من الكتالوج** لأن **كلها أسهم انفجرت وحقّقت ارتفاعًا
> جنونيًّا**».
>
> ⇒ **المبدأ: لا رقمَ مخترَعًا. كلُّ حدٍّ يُقاس من أسهم فيصل نفسها في لحظتها.**

🔒 **التصنيف: قياسٌ وصفيّ · صفر مسّ إنتاج · لا `LOGIC_VERSION` · الجذور byte-identical.**
🔴 **وهذا الملفّ مدفوعٌ قبل تشغيل الأداة ولو مرّة** — فلا يُعدَّل بعد رؤية الأرقام.

---

## ① لماذا يلزم تثبيتُ القرارات مسبقًا هنا تحديدًا

الظرف **مُفصَّلٌ على الكاتالوج بالبناء** ⇒ سيستوعبه **‏100% مهما اخترنا**. فلو تُركت
قراراتُ التصميم مفتوحة لَأمكن تحريكُ أيٍّ منها حتى تخرج النتيجة «حلوة» — وهذا
`p-hacking` بثوبٍ وصفيّ. ولذلك تُثبَّت الستّة أدناه الآن، **ويُنشَر ما يخالف التوقّع.**

---

## ② القرارات المثبَّتة

### D1 — المجتمع

| المجموعة | التعريف | الدور |
|---|---|---|
| **‏A — المنفجرون** | رمزٌ من الكاتالوج **تحقّق انفجارُه في البيانات** (تعريف D2) | **مصدر الظرف** |
| **‏C — شاهد الضبط** | رموزٌ من السوق الكامل **لم يتحقّق لها انفجار** في نفس المدى | **مقياس التمييز** |
| **‏X — المستبعَدون** | رمزٌ لم يُعثر له على انفجار (بيانات ناقصة/خارج المدى) | **يُعلَن ولا يُدفَن** |

الرموز الأساسية = الواحد والثلاثون في `additions_audit.md §③` (كاتالوج فيصل + الستة).
🔴 **والعضوية تُقرَّر بالبيانات لا بيدي**: لا أُصنّف رمزًا «منفجرًا» برأيي — الأداة تجده
أو تُعلن أنها لم تجده.

### D2 — لحظة الإرساء (‏`explosion_day`)

> أوّلُ جلسةٍ يبلغ فيها `high` نسبة **‏+100%** فأكثر فوق **أدنى `low`** في العشرين
> جلسةً السابقة لها.

**لماذا هذا التعريف بعينه:** ‏① **موضوعيّ** (لا تأريخ يدويّ لواحدٍ وثلاثين رمزًا)
‏② **يطابق سبب وجودها في الكاتالوج** («انفجرت وحقّقت ارتفاعًا جنونيًّا»)
‏③ **يطابق مثالي فيصل المنصوص** («المثالي 100% فأكثر» في M3، وهدف وصفة المقسّم +100%)
‏④ **الأولى لا الأكبر** — لأن فيصل يشتري قبل الحركة الأولى لا بعد أكبرها.

### D3 — نافذة الشراء (‏`ENTRY_WINDOW`)

> **‏20 جلسة قبل** `explosion_day` — أي `[explosion_day−20 , explosion_day−1]`.
> **ولا تشمل يوم الانفجار** (وإلا صار نظرًا مستقبليًّا).

`‏20` ≈ شهر تداول، وأوسعُ من `BASE_WINDOW`=15 بهامش. **وتُقاس الحساسية عند 10 و40
وتُنشَر**، لكن **الحكم الأساسيّ على 20** — مثبَّتٌ الآن.

### D4 — كيف تُقرأ القيم

نُرخي **كلّ** البوّابات الصلبة دفعةً واحدة ⇒ `analyze_ticker` **الإنتاجيّ نفسه** يمضي
إلى النهاية ويُرجع القيم المحسوبة. **صفر منطقٍ مكرّر** (نفس مبدأ `wall_stack.py`).

⚠️ **حدُّ دقّةٍ يُعلَن:** قيم `M1·M2·M3·M4·M5·RSI` تُحسب **قبل** أي بوّابة تتأثّر
بالإرخاء ⇒ **مضبوطة تمامًا**. أمّا الحواجز المشتقّة (`نواقص` · `جاهزية` · `نقاط` ·
`RR`) فتُقرأ **«القيمة التي كانت ستكون»** لو لم تُحجَب قبلها — وهو المطلوب هنا.

### D5 — حافة الظرف واتّجاهها

| المعيار | الاتّجاه | الحافّة |
|---|---|---|
| `price` · `best_spike` · `dollar_vol` · `readiness` · `score` · `rr` | البوّابة **حدٌّ أدنى** | **أصغرُ** قيمةٍ لوحظت |
| `base_range` · `rsi_min` · `rsi_now` · `n_soft_fails` | البوّابة **سقف** | **أكبرُ** قيمةٍ لوحظت |
| `drop_pct` | **أرضيةٌ وسقف** | **[أصغر · أكبر]** |

**ونُخرج نسختين معًا:**
- **`P100`** — يستوعب **كلَّ** المنفجرين (فكرة المالك حرفيًّا: سهمٌ واحد بـRSI 23 ⇒ الحدّ 23)
- **`P90`** — يُسقط أشدَّ **‏10%** تطرّفًا (حصانةٌ من شاذٍّ واحد يفتح الحدّ بلا داعٍ)

**الأساسيّ `P100`** (هو نصُّ الفكرة)، و`P90` يُنشَر معه دائمًا.

### D6 — 🔴 المقياس الحاسم: **الكلفة**

**الالتقاط وحده صفرُ معلومة** (الظرف مُفصَّل عليهم). فالحكم على:

> **كم مطابقًا يعطي الظرف على السوق الكامل في اليوم؟**

**وعتباتٌ مُعلَنة الآن:**

| مطابق/يوم | الحكم |
|---|---|
| **أكثر من 50** | 🟠 **يلزمه مُرتِّبٌ مُثبَت** — العدد وحده لا يُسقطه (‏تعديل ①) |
| **‏5 – 50** | 🟡 صالحٌ **بشرط مُرتِّب** (والمُرتِّب عندنا «غير حاسم») |
| **أقل من 5** | 🟢 قابلٌ للتنفيذ مباشرةً — أداةٌ مثل صيّاد المقسّم |

**وشرطٌ ثانٍ للتمييز:** لو استوعب الظرف **شاهد الضبط (‏C) بنسبةٍ قريبة من A**
⇒ **الظرف لا يميّز**، والعيب في **اختيار المعايير** لا في حدودها.

---

## 📌 تعديل ① — بأمر المالك · **قبل صدور أيّ رقم** (‏2026-08-05، تشغيلة
`30986665773` ما زالت `in_progress` لحظة التعديل)

> **نصُّ المالك:** «الهدف نبي نفتك من البوّابات اللي مسوّينها بفلسفة زايدة عن طريق
> الحدّ الأدنى · **صح ممكن تزيد الأسهم لكن على الأقل بتكون كلها مشابهة تمامًا لأسهم
> انفجرت من قبل وعندنا في الكتالوج**.»

**ما يتغيّر:** كنتُ كتبتُ «فوق 50 مطابق/يوم ⇒ **الظرف بلا قيمة**» — وهذا **حكمي أنا
لا حكم المالك**، ويعامل **العددَ** فشلًا بينما المالك يقبله صراحةً ثمنًا للأصالة.
⇒ صار **«يلزمه مُرتِّبٌ مُثبَت»**: العدد **لا يُسقط الظرف**، بل ينقل السؤال إلى
**التسليم**.

**⚠️ وما لا يتغيّر — وهو قيدٌ حقيقيّ لا تحفّظ:** السعة **‏15 خانة**، والمُرتِّب
قِيس **«غير حاسم»** (‏`T-REPLAY10`: لم يُثبَت تفوّقه على العشوائيّ ولا تكافؤه)
والسعةُ تقصّ **‏27-48%** أصلًا. ⇒ **ظرفٌ يعطي 200 سهمًا مع مُرتِّبٍ عند الصدفة
يُسلّم 15 عشوائيًّا من 200** — أي أن **توسيع المجتمع بلا مُرتِّبٍ يميّز = مقامرةٌ
أوسع لا فرزٌ أفضل** (وهو حرفيًّا ما قاسته `T-CORE5`). فالكلفة تبقى **مقيسةً
ومنشورة**، لكنها صارت **مُدخَلًا لقرار المالك** لا حكمًا آليًّا بالإعدام.

🔒 **وسلامةُ التسجيل محفوظة:** التعديل **مؤرَّخٌ ومنسوبٌ لصاحبه ومدفوعٌ قبل أوّل
رقم** — والفرق بينه وبين `p-hacking` أن الأخير يحرّك المعيار **بعد** رؤية النتيجة
ولمصلحتها. وأصلُ النصّ يبقى أعلاه بالجدول **مشطوبًا لا ممحوًّا**.

---

## ③ ما لا تفعله هذي الأداة — يُعلَن سلفًا

1. **لا تمسّ الفارز ولا الجذور ولا `CONFIG` خارج نطاقها** (تُعيدها في `finally`).
2. **لا تقترح رقمًا للإنتاج.** مُخرَجُها **جدولٌ** — والقرار للمالك.
3. **لا تُصنّف رمزًا يدويًّا.** العضوية من البيانات.
4. **لا تُخفي متعذّرًا.** كلُّ رمزٍ بلا انفجارٍ يُطبَع بسببه.

## ④ حدود صدقٍ بنيوية (قائمةٌ مهما كانت النتيجة)

- **انحياز البقاء** — الكون قائمةُ ناسداك اليوم؛ المشطوبة غائبة.
- **تشويه التقسيمات** — السلاسل معدَّلة (‏NUWE يُقرأ 908.25 لسهمٍ بـ$20).
- **عمى الافتر** — الشمعة اليومية لا تشمله، وNUWE انفجر فيه.
- **‏n صغير** — واحدٌ وثلاثون رمزًا على أحد عشر معيارًا ⇒ الظرف **يتمدّد** بطبعه.
- **الكاتالوج اختيارٌ لاحقٌ للحدث** ⇒ **تشخيصٌ لا معدَّل**؛ لا يُشتقّ منه «لو فتحنا كذا لربحنا كذا».
