# `T-PRESS-RANK` — «مَن الثمانيةُ الذين يصلونك؟» (تسجيلٌ مسبقٌ مدفوعٌ قبل أيّ سطرِ كودِ أداة)

> **أمرُ المالك (2026-08-27):** «قس عدد الصفقات **شتغل على قناة الضغط**».
> هذا عقدُ الشقّ الثاني.

---

## ⓪ لماذا هذي بالذات — الفجوةُ مقيسةٌ لا مُفترَضة

قناةُ الضغط هي **القناةُ الموجبةُ الوحيدة المقيسة** عندنا:
`HOLD3` ‏+0.174R ⟶ وقفُ القاع `T-GOLD-ENTRY` **‏+0.689R** ⟶ يصمد على شموع
الدقيقة `T-SLIP` ‏+0.566R ⟶ وعلى العرض القائم `T-SLIP-NBBO` ‏+0.576R.

**والمقيسُ من السجلّ الحيّ (‏`press_radar_ledger.jsonl` · 397 صفًّا · 10 جلسات):**

| جلسة | 🟢 جاهز | 👀 متابعة | كروتٌ كاملة |
|---|---|---|---|
| 08-14 | 48 | 71 | **8** |
| 08-15 | 27 | 32 | **8** |
| 08-16 | 27 | 32 | **8** |
| 08-17 | 25 | 31 | **8** |
| 08-24 | 14 | 25 | **8** |
| الوسطيّ | **‏15.9** | 23.8 | **8.0** |

⇒ **‏`ALERT_CAP`=8 وحدها تحدّد مَن يأخذ كرتًا كاملًا، ووسيطُ الجاهزين 10 —
فالمُرتِّبُ يبنّد فعلًا في نصف الجلسات وفي أعلاها يقصّ ‏83%.**

🔴 **و`alert_rank` لم يُقَس ضدّ العائد قطّ.** مفاتيحُه السّتّة اختيرت في
`press_prereg §⑪` على **«التقاط كتالوج فيصل»** — ومجتمعُ الكتالوج **مختارٌ على
النتيجة** (اختيرت أسماؤه لأنها انفجرت) ⇒ **دائريّةٌ مُعلَنة**، وهي بعينها
العلّةُ التي قتلت `A/B` و«رابط Technology 2025». **وكلُّ ما فُعل به بعدها
تجميدُه بت-بت لا قياسُه.**

⇒ **هذي التجربةُ تقيس المُرتِّبَ حيث الحافّةُ موجبة** — بعكس `T-RANK-DENSE`
التي قاست حيث كلُّ ذراعٍ خاسرة.

---

## ① السؤال

> على مجتمع حلقات الضغط، هل يوجد ترتيبٌ **مقيسٌ سلفًا** لأسهم «🟢 جاهز» يرفع
> صافيَ `R` **المُسلَّم** تحت سقف `ALERT_CAP`=8 كرتًا في الليلة، فوق ترتيب
> الإنتاج الحاليّ؟

---

## ② المجتمعُ والمحرّك — إعادةُ استعمالٍ بالاسم وبوّابةُ تكاملٍ منشورة

- **المجتمع:** لقطاتُ PIT الثلاث — 2023 `31574165523` · 2024 `31199493957` ·
  2025 `31191327731` (نفسُ لقطات كلّ تجاربنا).
- **المِشيةُ كثيفة:** لكلّ رمزٍ ولكلّ جلسةٍ **بخطوة 1 وبلا قفزة `WAIT`** يُنادى
  **`press_radar.press_read(sl, w=ALERT_W)`** — نفسُ نداء الإنتاج والتنبيه.
  **والكلفةُ مقيسةٌ لا مقدَّرة: 1.48ms للنداء ⇒ ‏≈22 دقيقةً للسنة.**
- **الجاهزُ** = `hold_sessions` **‏`READY_HOLD`=3 فأكثر** — تعريفُ الإنتاج حرفيًّا.
- **الصحوةُ** `press_radar.wake_read` **بالاسم** (بلا الافتر — **أرضيّةٌ** مُعلَنة،
  نفسُ حدّ `press_wake_arms §⑯`).
- **الخطّةُ والحسم:** `rebound_arms.mirror_plan` و`rebound_arms.resolve_episode`
  **بالاسم** · و`R` من `press_backtest.r_win_value()` بصيغة `§⑯` حرفيًّا
  (فائزةٌ ‏+`r_win` · خاسرةٌ ‏−1).
- **الدِدوب:** `press_radar.should_alert` **بالاسم** (‏`REALERT_DAYS`=5) —
  فمَن أُرسل ليلةَ D لا يُعاد قبل D+5، **وهو جزءٌ من السلوك لا تبسيطٌ**.
- 🔒 **صفرُ مسٍّ بـ`Super_stock.py` و`press_radar.py` و`rebound_arms.py`
  و`press_wake_arms.py`** (‏الأخيرةُ مجمَّدةٌ `CAP15` لأن أرقامها منشورة).

⚖️ **وميزانيةٌ واحدةٌ بالبناء:** الأذرعُ تقرأ **مجرى الجاهزين نفسَه** بنفس
الكون والنافذة والسقف والدِدوب — فلا يتفرّق ذراعان بأثر الميزانية (العيبُ
المقيس في `T-CLIFF`).

---

## ③ الأذرع — أربعٌ ولا خامسة

| الذراع | الترتيب | الدور |
|---|---|---|
| **`A0`** | **الإنتاجُ حرفيًّا**: المستيقظُ أوّلًا (‏`fire`) ثم `alert_rank` | **المرجع** (لا يُرقّى) |
| **`A1`** 🥇 | **🩸 المكنوسُ بعد حفظٍ أوّلًا** ثم ترتيبُ `A0` حرفيًّا | **الحاكمةُ الوحيدة** |
| **`A2`** | عشوائيّ (‏`200` بذرة · الوسيط) | **شاهدُ الصدفة** |
| **`A3`** | الأقدمُ جاهزيّةً أوّلًا (‏FIFO) | شاهدُ ضبطٍ ثانٍ بلا معلومة |

### `A1` — ولماذا هي بالذات (صفرُ رقمٍ مخترَع)

مفتاحُها `swept_hold` من **`press_read` نفسِها** — بلا عتبةٍ جديدة. وسندُها
**مقيسٌ منشور:** `T-FUSE` وجدت خليّةَ **`F2`** (قاعٌ صمد ثلاثًا فأكثر ثم كُنس
ثم حُفظ) **أعلى خليّةٍ قِيست في تاريخ المشروع: ‏+0.225R موجبةً في السنوات
الثلاث** — فوق `HOLD3` (‏+0.174R). و`press_prereg §⑮-ب` قاس أن **‏21 من 23**
من كتالوج فيصل عشيّةَ انفجاره حمل هذي البصمة.

🔴 **وهي ليست ضربةً لازب — والعقدُ يقولها قبل الأرقام:** `T-FUSE` نفسُها نصّت
أن **تفوّقَ `F2` على بقيّة `H3` ينقلب في 2025**. ⇒ **`A1` اختبارٌ حقيقيٌّ لا
تحصيلُ حاصل.**

📌 **و`swept_hold` مستعمَلٌ اليوم في ترتيب قسم «👀 المتابعة» وحدَه — وليس في
`alert_rank` ولا في اختيار الكروت الثمانية.** فما تقيسه `A1` هو **نقلُه إلى
قرار الكرت**.

⛔ **ولا تُضاف ذراعٌ بعد رؤية الأرقام** · ولا يُبدَّل مفتاحُ `A1` · ولا تُحرَّك
`ALERT_CAP` (‏8) ولا `READY_HOLD` (‏3) ولا `REALERT_DAYS` (‏5) ولا `ALERT_W` (‏40).

---

## ④ المقياسُ والمعايير — مُعادةٌ بلا تحريك

**المقياسُ الحاكم:** **صافي `R` المُسلَّم لكلّ جلسة** = مجموعُ `R` للكروت
المُرسَلة ÷ عددُ الجلسات. (‏`R` بصيغة `§⑯`: فائزةٌ ‏+`r_win_value()` ·
خاسرةٌ ‏−1 · و`no_fill`/`open` صفرٌ **لكنها تستهلك كرتًا** — نظيرُ `r_unit`
في `replay10`.) ولا يُقرأ من نسبة البلوغ شيءٌ وحدَها (درسُ `T-STOP`
و`T-LIBERATION`).

**المقياسُ المساند (يُنشَر ولا يحكم):** عددُ الكروت · نسبةُ بلوغ الهدف بفاصل
Wilson · وعددُ الجاهزين المقصوصين بالسقف.

**تُعتمَد `A1` إن استوفت الأربعةَ معًا:**

1. **`A1 − A0` موجبٌ في السنوات الثلاث** بلا انقلاب.
2. **الفاصلُ 95% للفرق المجمَّع لا يلمس الصفر** (بوتستراب **عنقودُه الجلسة** ·
   ‏10,000 سحبة · بذرة 54321 — نفسُ إجراء `T-RANK-DENSE` حرفيًّا).
3. **`A1` فوق المئين 95 لشاهد العشوائيّ `A2` في سنتين من ثلاث** (قاعدةُ
   `T-RANKER §⑤` حرفيًّا).
4. **الأرضية:** أربعون جلسةً فأكثر و**‏150 كرتًا مُسلَّمًا** فأكثر في كلّ سنة —
   وإلّا **«لا حكم»** ولا يُطبَع فرق.

**والحكمُ يُكتب مرّةً واحدة** عند اكتمال السنوات الثلاث. ⛔ ولا تُعاد تشغيلةٌ
بمعيارٍ مبدَّل.

---

## ⑤ بوّاباتُ الصلاحية — تُنفَّذ في الكود لا تُوصَف

> درسُ `ranker_dense_result §⓪`: **«‏`RV4` كُتبت في العقد ولم تُكتَب في الكود»**
> — فكلُّ صفٍّ هنا يلزمه سطرُ كودٍ يُوقِف **وقفلٌ يُثبت أنه يُوقِف**.

| البوّابة | الشرط | عند السقوط |
|---|---|---|
| **`PV0`** | حلقاتُ `HOLD3` بلا سقفٍ ولا دِدوب تُعيد أرقامَ `§⑬` المنشورة: **المجموعُ عبر الثلاث ‏5371 محسومة · 19.08% · ‏+0.174R** | **خروج 3** — عطبُ أداة |
| **`PV1`** | وسيطُ الجاهزين لكلّ جلسة **‏3 فأكثر** (وإلّا السقفُ لا يبنّد فلا سؤال) | **خروج 4** |
| **`PV2`** | الأذرعُ **تتفرّق**: `A1` تُسلّم كرتًا لا تُسلّمه `A0` في كلّ سنة | **خروج 4** — `no-op` |
| **`PV3`** | تغطيةُ الرموز **‏95% فأعلى** والمفقودُ يُسمّى بعدده | **خروج 3** |
| **`PV4`** | الدِدوبُ نافذ: صفرُ رمزٍ يُسلَّم مرّتين داخل `REALERT_DAYS` في أيّ ذراع | **خروج 3** |

🔒 **وحارسُ العلم الميّت:** إن خرج جدولُ الأذرع **متطابقًا بت-بت** فهي بصمةُ
`no-op` ⇒ **يُعلَن ولا يُفسَّر** (سابقةُ `BT_CANDLE`).

---

## ⑥ التنبّؤات — قابلةٌ للكسر، تُنشَر مكذَّبةً كما تُنشَر محقَّقة

- **`PR-P1`** (الأثقل، ويراهن **ضدّ** الحاكمة): أرجّح أن تعبر `A1` المعيارَ ①
  **وتسقط في ②** — لأن `+0.225R` قِيست على **الحلقة** لا على **الكرت المُسلَّم
  تحت سقفٍ ودِدوب**، والانتقالُ يخفّفها (نظيرُ `RK-P1` وقد تحقّق).
- **`PR-P2`**: `A0` **فوق** العشوائيّ `A2` مجمَّعًا — بعكس ما وجدناه في
  الفارز اليوميّ، لأن المجتمعَ هنا موجبُ التوقّع فالترتيبُ له ما يرتّب إليه.
- **`PR-P3`**: وسيطُ الجاهزين لكلّ جلسة **بين 3 و40**.
- **`PR-P4`**: نسبةُ الجلسات التي **يبنّد** فيها السقف (جاهزون أكثرُ من 8)
  **بين 30% و70%**.
- **`PR-P5`**: `A3` (‏FIFO) **دون** `A0` مجمَّعًا — عكسَ ما وقع في `T-RANK-DENSE`.
- **`PR-P6`**: حصّةُ 🩸 المكنوس بعد حفظٍ من الجاهزين **بين 20% و60%** — وإن
  نزلت تحت **العُشر** فـ`A1` **قريبةٌ من `no-op`** ويُعلَن ذلك قبل أيّ تفسير.

---

## ⑦ سقفُ النجاح — مثبَّتٌ سلفًا

🥇 **إن عبرت `A1` المعاييرَ الأربعة فهي تُقترَح على المالك** — **مفتاحًا أوّلَ
في `alert_rank` بلا حذفِ مفتاحٍ قائم**، وشحنُها **قرارُه هو** لأنها تغيّر
**مَن يصله**. ⚖️ **وهي ترتيبُ عرضٍ لا بوّابة** ⇒ **لا اسمَ يُسقَط** (درسُ
`WETO`) ⇒ **لا `LOGIC_VERSION` ولا `force_renew`**.

⛔ **وممنوعٌ مهما كانت الأرقام:** مسُّ `ALERT_CAP` أو `READY_HOLD` أو
`REALERT_DAYS` أو `ALERT_W` · أو أيٍّ من `M1`-`M14` أو الدخول/الوقف/الأهداف ·
أو ترقيةُ `A2`/`A3` · أو تحويلُ `swept_hold` إلى **فلتر**.

🔴 **وإن لم تعبر ذراع: `alert_rank` يبقى كما هو ويُقال ذلك صراحةً** — «لا
ترتيبَ أفضلَ مقيسًا» لا «كلُّ الترتيبات سواء».

---

## ⑧ حدودُ صدقٍ مكتوبةٌ قبل أيّ رقم

1. 🔴 **الدائريّةُ الجزئيّةُ مُعلَنة:** `swept_hold` اختير بعد رؤية أرقام
   `T-FUSE` — فالمختبَرُ هنا **انتقالُه إلى قرار الكرت**، لا صحّتُه. وهو أنظفُ
   اختبارٍ متاح وليس اختبارًا خارج العيّنة بالمعنى التامّ.
2. 🔴 **الصحوةُ بلا الافتر** (‏`extended_last_price` غيرُ قابلٍ للقياس من
   اليوميّ) ⇒ `A0` **أضعفُ هنا ممّا هو حيًّا** — **وهذا يحابي الحاكمةَ
   `A1`**، فيُقرأ الفرقُ **سقفًا لها لا أرضيّة**.
3. **«فائزة» = بلوغُ هدفٍ بلمس سعرٍ لا صفقةٌ منفَّذة** ⇒ سقفٌ متفائلٌ للأذرع
   **بالتساوي** (وحدةُ المخاطرة واحدةٌ بالبناء: كلُّ الأذرع تستعمل
   `mirror_plan` نفسَها).
4. **الوقفُ هنا وقفُ `mirror_plan`** (‏7% تحت المِرساة) **لا وقفُ القاع
   المعتمَد** — لأن أرقامَ `§⑬` المنشورة التي تُعيدها `PV0` محسوبةٌ عليه.
   ⇒ **الأرقامُ قابلةٌ للمقارنة مع `§⑬` وغيرُ قابلةٍ للمقارنة مع `T-GOLD-ENTRY`.**
5. **بِركةُ الرادار الحيّة أضيقُ من الكون** (قوائمُنا الخمس بسقفٍ مُعلَن) بينما
   المِشيةُ هنا على **الكون كلِّه** ⇒ عددُ الجاهزين هنا **سقفٌ** لِما يقع حيًّا.
6. **انحيازُ البقاء** مخفَّفٌ باللقطة وغيرُ منفيّ · **وتشويهُ التقسيمات** قائم ·
   **وعمى الجلسة الممتدّة** قائم (‏`T-AH`).
7. **والمقياسُ يكافئ التسليمَ الأقلَّ إن كان التوقّعُ سالبًا** — وهو **ليس
   كذلك هنا** (‏`HOLD3` موجب) لكنّ السقفَ ثابتٌ عند 8 في الأذرع الأربع
   **فالعددُ مضبوطٌ بالبناء** ولا يتكرّر فخُّ `T-RANK-DENSE §④`.

---

## ⑨ ملحقٌ مؤرَّخ (2026-08-27) — **مدفوعٌ قبل أيّ رقم** · قيودُ تنفيذٍ مقيسة

> يُكتب بعد بناء الأداة وقبل أوّل تشغيلة، تنفيذًا لقاعدة «حارسٌ مكتوبٌ وغيرُ
> منفَّذٍ ليس حارسًا» ولقاعدة «لا يُحرَّك شيءٌ بعد رؤية الأرقام».

### ⑨-1 `plan` غائبٌ بنيويًّا — والمفتاحُ الأوّلُ في `alert_rank` خامد

`alert_rank` مفتاحُه الأوّل `r.get("plan")` = **خطّةٌ محفوظةٌ في ذاكرة البِركة
الحيّة** (`press_radar_state.json`). وهذي الذاكرةُ **لا وجودَ لها تاريخيًّا**
بالبناء ⇒ يُمرَّر `None` **لكلّ صفٍّ في الأذرع الأربع** ⇒ المفتاحُ ثابتٌ فلا
يرتّب شيئًا. **مُعلَنٌ ولا يُحابي ذراعًا** (‏`A1` ترتيبُها الثانويُّ هو `A0`
نفسُه فتفقده معها).

### ⑨-2 `prev_q` مقيسُ الاستحالة — يُمرَّر `None` ويُعلَن

المفتاحُ الثاني `r.get("prev_q")` يأتي من `press_radar.prev_qualified` وهي
تنادي `S.analyze_ticker` حتى **‏24 مرّة** لكلّ صفّ. **والمقيس على هذي البيئة:
`analyze_ticker` ‏84.2ms للنداء** ⇒ الصفُّ الواحد حتى ‏2.0 ثانية،
**والتخزينُ المؤقّت لا ينقذ** (عيّناتُ الجلسات المتتالية تقع على بقايا قسمةٍ
مختلفة فتغطّي المدى كلَّه) ⇒ **‏≈8.6 ساعةً للسنة الواحدة** فوق سقف الـworkflow
(‏300 دقيقة). ⇒ يُمرَّر `None` **للأذرع الأربع بالتساوي**.

🔴 **وأثرُه يُقال صريحًا:** `A0` هنا **أضعفُ ممّا هو حيًّا** — يفقد مفتاحَي
الذاكرة معًا ⇒ **مع الحدّ §⑧-2 (الصحوةُ بلا الافتر) يصير في `A0` ثلاثةُ إضعافٍ
مُعلَنة** ⇒ **الفرقُ `A1 − A0` يُقرأ سقفًا لـ`A1` لا أرضيّة**، وسقوطُها رغم
ذلك يكون **حكمًا قويًّا** وعبورُها **لا يكفي وحده** للترقية بلا هذا القيد مكتوبًا
في نصّ النتيجة.

⇒ **والاسمُ الدقيق:** `A0` = **ترتيبُ الإنتاج بمفاتيحه القابلة للحساب تاريخيًّا**
(المستيقظُ أوّلًا · ثم مستوًى مُختبَر ⟶ حفظٌ ⟶ ركضةٌ ⟶ عمق).

### ⑨-3 ذاكرةُ دِدوبٍ لكلّ ذراعٍ على حدة

الأذرعُ تُسلّم رموزًا مختلفة، فذاكرةٌ مشتركةٌ كانت ستجعل ذراعًا تكتم أخرى ⇒
لكلّ ذراعٍ `mem` خاصّة، و`should_alert` يُطبَّق **قبل** الترتيب كترتيب الإنتاج
(الدِدوب في حلقة البِركة ثم `rows.sort` ثم السقف). و`PV4` يفحص الأربع.

### ⑨-4 الفاصلُ يُجمَّع خارج التشغيلة كما في `T-RANK-DENSE` حرفيًّا

كلُّ تشغيلةٍ سنةٌ واحدة، فتطبع الأداةُ سطرَ **`DIFFS`** = فرقُ `A1 − A0` **لكلّ
جلسة**، ويُجمَّع الفاصلُ من الثلاث بـ`boot_ci` (‏عنقودُه الجلسة · 10,000 سحبة ·
بذرة 54321) — نفسُ إجراء `ranker_dense.py` سطرًا بسطر.

### ⑨-5 تصحيحُ كلفةٍ منشورة في §②

كتبتُ «‏`press_read` ‏1.48ms ⇒ ‏≈22 دقيقةً للسنة» — **والمقيس على هذي البيئة
‏0.17ms** ⇒ الكلفةُ الفعليّةُ دقائقُ لا عشراتُها. **والكلفةُ ليست معيارًا**،
لكنّ الرقمَ المنشورَ يُصحَّح لا يُترَك.

### ⑨-6 `PV0` تُنفَّذ سنةً بسنة بأرقام §⑬ المنشورة

المنشورُ مجمَّعٌ (‏5371) وسنويُّه **‏1660 · 1857 · 1854** (‏`T-FUSE §FV0`-ب) ⇒
الأداةُ تشترط **المحسومةَ السنويّة** بت-بت وإلّا خروج 3، **وتطبع الفائزةَ** ليُجمَّع
‏1025 ⟶ ‏19.08% ⟶ ‏+0.174R خارجَ التشغيلة. والمِشيةُ نفسُها **مستدعاةٌ بالاسم**
(`press_wake_arms.walk_symbol_wake`) فلا مقياسَ ثانٍ.
