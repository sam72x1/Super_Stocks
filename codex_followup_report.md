# 🔍 تقرير متابعة تدقيق Codex — فرع `claude/audit-fixes`

> **التاريخ:** 2026-07-15 · **الفرع:** `claude/audit-fixes` · **الأساس:** `origin/main`
> **آخر SHA:** `d97c8ef` · **الاختبارات:** `python3 test_bot.py` → **857 نجح · 0 فشل · خروج 0**
> **لم يُدمج شيء إلى `main`، ولم يُفتح أي PR.**
>
> **القيد الحاكم:** لم أمسّ الجذور (`rank_key` · `select_top` · `classify_tier` · `entry_status` ·
> M1-M14 · `LOGIC_VERSION`) — مُثبَت ببصمة SHA-256 لكل دالّة جذر مقابل `origin/main` بعد كل دفعة.
> كل ما نُفِّذ هو طبقات **سلامة · صدق عرض · بحث/تقارير**. كل ما يمسّ جذرًا أو قرارًا محسومًا
> **لم يُنفَّذ** — وُثِّق أدناه بانتظار قرارك.

---

## ✅ ما نُفِّذ ودُفِع (3 دفعات)

### `aa7060a` — مراجعة Codex على `1e322c5`: انحدار قاتل + تسريب + حارس

| # | العيب | لماذا خطير | الحل |
|---|---|---|---|
| **①** | **انحدار P0 أدخله `1e322c5`**: `COMPANY_CACHE = _load_company_cache()` يُنفَّذ **وقت الاستيراد** (سطر 4825) بينما `_handle_corrupt_state_file` كان معرَّفًا **بعده** (4845) | كاش شركة تالف ⇒ `NameError` عند الاستيراد ⇒ **كل workflow يموت قبل أي شيء**: لا فرز، لا تنبيه، لا نسخة احتياطية، ولا حتى التحذير الذي أُضيف. أسوأ من العيب الذي كان يعالجه. **مُثبَت بإعادة إنتاج في عملية جديدة.** | نُقلت الدالّة فوق أول استعمال + **قفل يشغّل عملية جديدة** بكاش تالف (الاختبار الوحيد الذي يمسك خطأ ترتيب التعريف — الاستيراد داخل نفس العملية لا يكشفه) |
| **②** | **P1-8 ناقص**: `resp.text` يُسجَّل **خامًا**، والتوكن **المرمّز-URL** (`:`→`%3A`) يمرّ من المُخفي | فشل TLS/وكيل يعكس الرابط ⇒ التوكن في سجل Actions **العلني** | التغطيتان أُضيفتا في `Super_stock.py` و`cline_notify.py` |
| **③** | **حارس الحادثة ناقص**: يُتجاوَز بحقن `runner=os.system`، و`_fresh_watchlist`/`_fetch_head_sha` في رادار الانطلاق ينفّذان `git` **بلا حارس** | أي اختبار ينسى المحاكاة = **إعادة حادثة 2026-07-14** (كتابة وهمية على `main`) | الثلاثة محروسة تحت `SUPER_STOCKS_TESTING` |

### `db2752b` — صدق التقارير (عرض فقط)

| # | العيب | سيناريو الفشل | الحل |
|---|---|---|---|
| **P1-5** | الباكتيست: عند فشل جلب ناسداك يسقط الكون إلى **أسهم رشّحها البوت أصلًا** (منحازة اختيارًا) — والعنوان يبقى «🌍 باكتيست السوق الكامل **(بلا انحياز اختيار)**» | ناسداك يردّ 403 ⇒ العيّنة = ناجحو البوت ⇒ نسبة نجاح متفائلة **تُقرأ كحكم سوق حيادي**. هذا عكس الحقيقة حرفيًّا | `market_fallback` + عنوان يصرّح بالانحياز ومصدر الكون |
| **P1-6** | أداة الأرباح: `get_universe` **يبتلع** فشل الشبكة ويرجع `[]` بلا استثناء. **والأخطر:** `render_scan` لم يكن يميّز `None` عن `[]` أصلًا | إصلاح 2026-07-12 كان يُرجع `None` عند التعذّر — لكن رسالة «تعذّر المسح» **لم تكن تظهر أبدًا**: يصل «لا توجد أسهم تطابق الشروط» بينما المسح لم يعمل | كون فارغ ⇒ `None` · و`None` ⇒ رسالة تعذُّر صريحة |

### `d97c8ef` — بحث/تقارير فقط

| # | العيب | سيناريو الفشل | الحل |
|---|---|---|---|
| **P2-4** | `polygon_base_trades` يُرجع **نصف النافذة كأنها كاملة** (`return out or None`) عند خنق صفحة لاحقة | تُحمَّل 50 ألف صفقة ثم 429 ⇒ `acc_components` يحسب نسبًا نهائية من **نصف النافذة بلا علامة نقص** = تجربة تُحسم على بيانات ناقصة | فشل صفحة ⇒ `None` (عقد الطبقة الموثّق: فاشل-آمن لا بتر صامت) + اختبار سلوكي يحاكي 429 |
| **P2-6** | `_dedup_closed` بمفتاح `(رمز, entry_ref)` | ترشيحان مستقلّان لنفس السهم بنفس سعر الدخول في أسبوعين (**تصادم مثبَت في سجلّك: LYEL**) ⇒ الثانية **تُحذف من الإحصاء بصمت** فتنحاز نسبة النجاح | أُضيف `added` للمفتاح (موجود في كلا المصدرين) |

---

## 🔍 حكم Codex على الإصلاحات الستة (`/codex:adversarial-review --base origin/main`)

**الحكم: `needs-attention` — لا تدمج.** لكن أهمّ ما أكّده صراحةً:

> «**الجذور المذكورة و`LOGIC_VERSION` غير مُغيَّرة.**»

سبع ملاحظات. **ثلاث نُفِّذت** (الانحدار القاتل · التسريب المتبقّي · ثغرة الحارس — أعلاه).
**أربع لم تُنفَّذ** لأنها تمسّ الحسّاس (تفاصيلها في القسم التالي):

1. **الملف التالف ما زال يُكتَب فوقه** — التنبيه وحده لا يمنع الفقد.
2. **فشل الإرسال ما زال يستهلك الدِدوب** — 429 عابر يُسكت السهم بقية اليوم.
3. **`pullback_live` يحفظ الحالة قبل الإرسال** — يمسّ قرارًا موثّقًا.
4. **الأقفال السبعة الأصلية تنجح كاذبًا** (تفحص نص المصدر لا السلوك) — أقفالي الجديدة سلوكية، لكن أقفال `a0c6947` الأصلية ما زالت كما هي.

---

## ⏸️ يحتاج قرار المالك

### 🔴 النقطتان الحسّاستان (لم أنفّذهما — بأمرك)

#### (أ) الدِدوب عند فشل الإرسال — `ignition_live.py:535-558` + `Super_stock.py:6964`

**العيب (مؤكَّد):** `scan_ignition` يختم `s["ignition_alert"] = today` **قبل** الإرسال. الإصلاح الحالي
يسجّل الفشل في القياس، لكنه **يضيف الصف إلى `seen` و`session_fires` بلا شرط** ⇒ 429 عابر أو انقطاع
لحظي = **السهم لا يُفحَص بقية اليوم** (والـhandoff ينقل الكتم للمقطع الثاني). أي: **تفويت لحظة دخول** —
وهي بالضبط الحافة الوحيدة المثبتة في المشروع (التوقيت).

| الخيار | ماذا يعني | الثمن |
|---|---|---|
| **① إلغاء الختم عند الفشل** (توصيتي) | `for r in rows: r[0].pop("ignition_alert", None)` عند `sent_ok=False` + عدم إدخالها `seen`/handoff ⇒ يُعاد الفحص الدورة القادمة | لو وصل لمستلم وفشل آخر (`send_telegram` يجمّع bool واحدًا) **يتكرّر التنبيه على من وصله**. ⚖️ **تكرار أهون من تفويت دخول** |
| **② تسليم لكل مستلم** (الأنظف) | `send_telegram` يرجع نتيجة لكل chat_id · إعادة المحاولة للفاشل فقط | تغيير عقد `send_telegram` = يمسّ كل المستدعين (اليومي/الارتداد/الرادار) — أوسع من «إصلاح» |
| **③ إبقاء الحال** | تنبيه واحد فقط مضمون/يوم | تفويت صامت عند أي خنق |

**توصيتي: ①** — الكود جاهز عندي (3 أسطر + قفل سلوكي)، أنفّذه بكلمة منك.

#### (ب) سلوك الملف التالف — `Super_stock.py:4845` + كل الكتّاب

**العيب (مؤكَّد):** المعالِج الحالي ينسخ ويحذّر، ثم **يُرجع بنية فارغة** — فيقرأها التشغيل كأنها
«أول قائمة» ويحفظها **فوق الملف التالف**، والدفع يمحو النسخة السليمة من `git` أيضًا. والنسخة
`.corrupt-*` **تزول مع الرنر المؤقّت**. أي: التحذير جعل الفقد **صاخبًا** لا **ممنوعًا**.

| الخيار | ماذا يعني | الثمن |
|---|---|---|
| **① حجر صحّي (fail-closed)** (توصيتي) | `CORRUPT_STATE` = مجموعة الملفات التالفة · `_atomic_write_json` **يرفض** الكتابة عليها (كل الكتّاب يمرّون منها = نقطة واحدة) ⇒ الملف يبقى كما هو وسجلّ git يبقى مرجع الاسترجاع | البوت يعمل «بلا حالة» لذلك الملف حتى تتدخّل (لا يحفظ). **هذا هو المطلوب: توقّف الكتابة، لا توقّف البوت** |
| **② رفع الملف التالف كـartifact** | `.corrupt-*` يبقى للتشخيص بعد موت الرنر | لا يمنع الفقد وحده — مكمّل لا بديل |
| **③ إبقاء الحال** | تحذير تلغرام فقط | إن لم تصحُ على التحذير قبل التشغيل التالي = **فقد نهائي** |

**توصيتي: ① + ②** — الكود جاهز عندي، أنفّذه بكلمة منك.

---

### 🟠 بنود تمسّ الجذور/المحسوم (لم أنفّذها — تشخيص + إصلاح مقترح)

#### P1-2 — رؤوس الشموع الحمرا **الأسبوعية** تدخل `t1` (مخالفة تنفيذية لقرار محسوم)

* **الموضع:** `Super_stock.py:1351-1352` داخل `resistance_levels`.
* **الدليل:** الاستدعاء اليومي `resistance_levels(df, price)` افتراضه `include_red_heads=True` ⇒
  يعيد التجميع أسبوعيًّا **ويضيف `_red_candle_heads(wk, price)`** = رؤوس حمرا أسبوعية داخل مقاومات
  اليومي ⇒ تصل `t1`. بينما `Super_stock.py:2243` يستدعي الأسبوعي صراحةً بـ`include_red_heads=False`
  احترامًا لـ`CLAUDE.md:166` («**الأسبوعي بلا رؤوس حمرا** حفاظًا على ثبات t1»).
* **سيناريو:** سعر `$10` بلا مقاومة يومية قريبة، وشمعة أسبوعية حمرا رأسها `$11` ⇒ `t1 = $11`
  **حتى لو** `USE_MULTIFRAME_TARGETS=False`.
* **إصلاحي المقترح:** فصل العلم: `include_red_heads` لليومي فقط، ووسيط منفصل
  `include_weekly_red_heads=False` (افتراضيًّا) للكتلة الأسبوعية الداخلية.
* **لماذا لم أنفّذه:** يغيّر `t1` ⇒ يغيّر RR ⇒ قد يغيّر كسر التعادل في `rank_key` = **جذر**.
  ⚠️ القرار قرارك: هل النية أن الأسبوعي **مطلقًا** بلا رؤوس حمرا، أم الاستثناء للاستدعاء الصريح فقط؟

#### P1-1 — `TARGET_CAP_MULT` ليس سقفًا فعليًا

* **الموضع:** `Super_stock.py:2265-2271`. عند غياب 3 مستويات تحت السقف: يأخذ **أقرب مقاومة فوق
  السقف** ثم يضرب في `1.25` تكراريًّا.
* **سيناريو:** سعر `$10`، سقف `$20`، المقاومة الوحيدة `$25` ⇒ أهداف `$25/$31.25/$39.06` (السقف 2×
  متجاوَز 2×). `t1` مضخّم ⇒ RR مضخّم.
* **ملاحظة إنصاف:** التعليق في الكود يقول «هدف حقيقي أفضل من عشوائي» = **قرار مقصود**، لا سهو.
  السؤال لك: هل `TARGET_CAP_MULT` **سقف صلب** (يُقصّ عنده) أم **تفضيل** يُتجاوَز عند الحاجة؟
* **لم أنفّذه:** يمسّ الأهداف وRR = جذر.

#### P0-1 — الترحيل يطبّق مستويات مشتقّة من **المستقبل** على تاريخ الصفقة

* **الموضع:** `migrate_watchlist` (يعيد حساب الوقف/الأهداف من تاريخ **اليوم**) ثم
  `update_watchlist_status` يقيّم الشموع **منذ تاريخ الترشيح القديم**.
* **سيناريو:** رُشِّح 1 يونيو بوقف `$2.60`؛ في يوليو تغيّرت `LOGIC_VERSION` فأُعيد الحساب إلى `$3.00`
  ببيانات يوليو؛ ثم تُقيَّم شمعة 3 يونيو (قاع `$2.90`) بالوقف الجديد ⇒ **«وقف» رجعي لم يكن موجودًا وقتها**.
* **إصلاحي المقترح:** المستويات المعاد اشتقاقها تسري **من تاريخ الترحيل فقط** (`levels_effective_from`)،
  والشموع قبله تُقيَّم بالمستويات الأصلية المخزَّنة.
* **لم أنفّذه:** الترحيل + حسم النتائج = جذر تتبّعي صريح استثنيتَه.

#### P0-2 / P0-3 — تصحيح التقسيم العكسي

* **P0-2:** فشل جلب التقسيم (401/429/عطل ياهو) ⇒ `_fetch_splits=None` ⇒ العامل `1.0` **ويستمر الحسم**
  على OHLC **معدَّل** ⇒ `hit_t3` زائف (مثال Codex: تنبيه `$1` وt3 `$2`، تقسيم 1:10، ياهو يردّ 429،
  القمم المعدَّلة `$11-13` ⇒ `13 ≥ 2` = هدف محقَّق وهمي).
* **P0-3:** `_scale_divisor` يختار المقياس بقرب المستوى من سعر اليوم — فرضية تنهار بعد **هبوط قوي
  تالٍ للتقسيم** ⇒ يختار المقياس القديم ⇒ أهداف زائفة.
* **إصلاحي المقترح (P0-2):** فشل جلب التقسيم ≠ «لا تقسيم». عند تعذّر الجلب لسهم **مشكوك في تقسيمه**
  (قفزة سعر ≥ 3× بين شمعتين متتاليتين) ⇒ **تأجيل الحسم** لا حسمه بعامل `1.0`.
* **لم أنفّذهما:** تصحيح التقسيم جذر ماليّ صريح استثنيتَه. ⚠️ **هذان أخطر بندَين ماليَّين في التقرير
  كلّه** — لو أردت بندًا واحدًا نبدأ به بعد الحسّاستَين، فليكن **P0-2**.

#### P1-3 — الوقف بعد الهدف: تناقض بين المخزنَين

* `update_tracking` (سجل التنبيهات): `if lows[i] <= _stop_c and best == 0` ⇒ **الوقف بعد `t1` يُتجاهَل**
  ويستمر منح `t2/t3` لاحقًا. التعليق يقول: «إلا إذا حقق هدفًا قبله، فالصفقة رابحة أصلًا» = **مقصود**.
* `update_watchlist_status` (القائمة): الوقف **يُنهي** النموذج ولو بعد هدف.
* **النتيجة:** هدف→وقف→هدف أعلى ⇒ `alerts_history` يقول `hit_t3` والقائمة تقول «ضرب الستوب».
  الإحصاء يُبنى من **دمج** المصدرين ⇒ الحقيقة تعتمد على أيّهما سبق.
* **قرارك المطلوب:** ما هي **الحقيقة** عندك؟ (أ) الوقف بعد `t1` يُنهي الصفقة (متحفّظ) · (ب) بعد `t1`
  الصفقة رابحة والوقف لا يُنهيها (فيصل «يمتّع» + وقف متحرّك). ثم يُوحَّد المساران على الجواب.

#### P1-4 / P1-7 (تشخيص فقط)

* **P1-4:** ملخّص الباكتيست يعيد بناء العائد من الوقف النظري ويجعل كل خسارة `-1R` **متجاهلًا**
  `ret_a/ret_b` المخزَّنَين (بعد الفجوة والسبريد) ⇒ فجوة افتتاح إلى `$5` من وقف `$9` تُعرض `-10%`
  بدل `≈-52%`. **لم أنفّذه:** يغيّر أرقام الباكتيست التي بُنيت عليها قرارات B3/C3 = خطر إعادة تفسير
  حكم محسوم بلا قصد.
* **P1-7:** `git_save` عند التعارض: يقرأ ملفاتنا → `reset --hard` → **يعيد كتابة ملفاتنا كاملة** فوق
  الريموت ⇒ تحديث دلاليّ من تشغيل آخر (مثلًا `hit_t1`) **يُمحى** بنسخة أقدم `open`. **لم أنفّذه:**
  يمسّ ⑬ الموثّق ويحتاج **دمجًا دلاليًّا** (لا آخر-كاتب-يفوز) = تصميم يستحق قرارك.

#### P2 المتبقّي (تشخيص)

* **P2-1** بوابة المضارب قد تقرأ صفقات **جلسة أمس** (`operator_flow` بلا حدّ زمني لبداية الجلسة) ⇒
  `has_operator=True` بلا مضارب اليوم في سهم خامل. طبقة توقيت/تنبيه — إصلاحه ممكن (قصّ زمني عند
  الافتتاح) لكنه **يغيّر بوّابة إطلاق** ⇒ تركته لك.
* **P2-2** `position_size` لا يقيّد تكلفة الشراء بـ`ACCOUNT_SIZE` (وقف ضيّق ⇒ 10,000 سهم بـ$100k من
  حساب $10k). الدالّة **غير معروضة** حاليًّا ⇒ لا أثر إنتاجي الآن.
* **P2-3** manifest الـE2 يصادق على مجموعة ملفات **ناقصة** (لا يقارن بقائمة قانونية) ⇒ يُضعف ادّعاء
  fail-closed. قياس فقط.
* **P2-5** دفع الـassembler عرضة لـnon-fast-forward ⇒ تُتخطّى البوّابة الصارمة. قياس فقط.

---

## 🚚 خطة إنزال إصلاحات السلامة إلى `main` (مقترح — لم يُنفَّذ)

الفرع الحالي يحمل **E2 كاملًا + إصلاحات التدقيق**. إنزال السلامة وحدها لا يحتاج E2:

### ينفصل نظيفًا عن E2 (قابل للـcherry-pick إلى فرع من `main` مباشرة)

| الإصلاح | الملفات | لماذا نظيف |
|---|---|---|
| **الانحدار القاتل** (ترتيب `_handle_corrupt_state_file`) | `Super_stock.py` | مستقل تمامًا — **وهو الأعجل: بدونه أي كاش تالف يقتل كل workflow** |
| **P1-8** التسريب (توكن حرفي + مرمّز + `resp.text`) | `Super_stock.py` · `cline_notify.py` | أمني بحت، بلا تبعية |
| **P0-5** الحجر الصحّي (لو أقررته) | `Super_stock.py` | نقطة واحدة (`_atomic_write_json`) |
| **P1-5 / P1-6** صدق التقارير | `Super_stock.py` · `technical_report.py` | عرض فقط |
| **P2-6** مفتاح الدِدوب | `Super_stock.py` | تقارير فقط |
| **حارس `git_save`** | `Super_stock.py` (+`ignition_live.py` جزئيًّا) | الجزء في `Super_stock` مستقل |

### متشابك مع E2 (لا ينفصل)

| الإصلاح | لماذا |
|---|---|
| **P0-4** (احترام قيمة الإرسال + الدِدوب) | يعيش في `ignition_live.py` الذي أُعيد بناؤه بالكامل في E2 (`_SafeRecorder` · المقاطع · handoff). النسخة على `main` **لا تحوي المقاطع أصلًا** ⇒ نقله = نقل E2 أو إعادة كتابته لبنية `main` القديمة |
| **P2-4** (نافذة Polygon) | مستقل تقنيًّا، لكنه يخدم طبقة القياس أساسًا ⇒ ينزل مع E2 بلا استعجال |

**التوصية:** فرع سلامة صغير من `main` (بلا E2) يحمل الصفّ الأول من الجدول الأعلى، ويُدمج بعد
مراجعتك — بذلك تُحمى `main` **قبل** أن يُحسم ملف E2 (الذي قد يطول).

---

## 🧪 نتيجة الاختبارات

```
python3 test_bot.py  →  857 نجح · 0 فشل · خروج 0
```

* **خط الأساس** عند بداية العمل (`1e322c5`): 846 نجح · 0 فشل.
* **أُضيف 11 قفلًا** — كلها **سلوكية** (تنفّذ المسار فعلًا) لا نصّية:
  عملية جديدة بكاش تالف · التوكن المرمّز-URL · `resp.text` عبر المُخفي · `cline_notify` بالشكلين ·
  حقن `runner=os.system` لا يتجاوز الحارس · `_fresh_watchlist`/`_fetch_head_sha` بلا git ·
  كون فارغ ⇒ `None` · «تعذّر المسح» ≠ «لا توجد أسهم» · `market_fallback` · تصادم LYEL ·
  خنق الصفحة الثانية من Polygon ⇒ `None`.
* **الجذور:** بصمة SHA-256 لـ`rank_key`/`select_top`/`classify_tier`/`entry_status`/`analyze_ticker`/
  `backtest_symbol`/بوّابتَي الشورت والفلوت = **مطابقة حرفيًّا لـ`origin/main`** بعد كل دفعة.
  `LOGIC_VERSION` **بلا تغيير**.
* **الحارس:** `SUPER_STOCKS_TESTING=1` مفعّل، ولا شيء في السويّة يلمس git أو الشبكة
  (تحقّقت أن الشجرة تبقى نظيفة بعد كل تشغيل).

---

## 📌 خلاصة القرارات المنتظِرة منك

1. **الدِدوب عند فشل الإرسال** → أوصي بالخيار ① (إلغاء الختم · تكرار أهون من تفويت).
2. **الملف التالف** → أوصي بالخيار ① + ② (حجر صحّي + رفع الملف التالف كـartifact).
3. **P0-2 (فشل جلب التقسيم يحسم على بيانات معدَّلة)** → أخطر بند ماليّ متبقٍّ؛ أقترح أن يكون التالي.
4. **P1-2** → هل الأسبوعي **مطلقًا** بلا رؤوس حمرا؟ (الكود يخالف الوثيقة اليوم).
5. **P1-3** → ما الحقيقة: الوقف بعد `t1` يُنهي الصفقة أم لا؟ (المخزنان متناقضان الآن).
6. **خطة `main`** → هل أفتح فرع سلامة صغير من `main` بلا E2؟

*(لا شيء من هذه نُفِّذ. كلها بانتظارك.)*
