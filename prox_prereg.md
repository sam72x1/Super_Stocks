# 🥇② T-PROX — تسجيلٌ مسبق: **القريبُ من الدخول يتقدّم** (يُدفَع قبل أيّ رقم)

> **قرارُ المالك (2026-08-07):** اختار الخيار ② بعد أن رأى «صفر جاهز» —
> «‏2، المهمّ ما يأثّر على بوّابات فيصل، ونمشي على الحدّ الأدنى للكاتالوج
> والاستفادة القصوى من 12 مستودع».

---

## ① العطلُ المقيس — لا المُفترَض

على القائمة الحيّة 2026-08-07، **العشرةُ المرشّحون كلُّهم فوق نطاق دفعاتهم**
بـ **‏4.5% إلى 48.1%**، وتسعةٌ منهم `entry_mode = no_entry_far`. وثلاثةُ أرقامٍ تحسم
التشخيص:

1. **نافذةُ «جاهز» عرضُها ‏11.3% فوق القاع** بنيويًّا (‏3 دفعات × 3% ثم تسامحُ 5%).
2. **`entry_ref == last_price` للعشرة** ⇒ **رُشِّحوا بعيدين، لم يرتفعوا بعد الترشيح.**
3. **`entry_readiness` لا تقرأ `tranches` إطلاقًا** — سبعةُ مكوّنات (ثبات · RSI ·
   MACD · KST · قربُ المتوسط · جفافُ بيع · مسحُ سيولة) **ولا واحدٌ منها يقرأ سعرَ
   الدخول**. والدليلُ الحاسم **علاقةٌ عكسيّة**: **`YHC` جاهزيتُه 90 (الأعلى) وهو
   الأبعدُ ‏+48.1%**، و**`XAIR` جاهزيتُه 60 (الأدنى بين المُسلَّمين) وهو الأقربُ
   ‏+4.5%**.

> 🔴 **تصحيحٌ ذاتيّ (كُتب بعد القياس، والنصُّ الأصليّ كان يقول «`XAIR` الوحيدُ
> القابل للدخول»): `XAIR` ليس قابلًا للدخول — حالتُه `👀 متابعة` («فوق نطاق
> الدفعات بـ4.5% — ضع الطلبات وانتظر النزول للنطاق») لأن عتبةَ المالك
> `ENTRY_READY_BAND_TOL_PCT = 0`. **والمقيس أدقُّ وأقوى:** `entry_status` لكلّ
> **العشرة** = `watch`، و`in_entry_band` لكلّ العشرة = `False` ⇒ **‏0 من 10 داخل
> النطاق** لا واحد. والوحيدان `ready_now` في القائمة كلِّها (**‏PSTV** و**IDN**)
> **محمولان لا مُرشَّحان** فلا يُحسبان في عدّاد «🟢 جاهز» (قرارُ المالك 2026-07-29).
> والحجّةُ لا تتغيّر بل تشتدّ: الجاهزيةُ **تعاكس** القربَ بين المُسلَّمين.

⇒ **المُرتِّبُ يقدّم الأبعدَ عن الدخول بالبناء**، والسعةُ (10) تُستهلَك في أسماءٍ
لا يمكن أن تُعبَّأ فيها طلبات. و**‏≈49 مرشّحًا قُصّوا بالسعة** يومَ التجديد.

**والقياسُ الكامل للأساس (‏2026-08-07، قبل التغيير):** أنماطُ الدخول للعشرة =
‏9 `no_entry_far` + 1 `sweep_confirmed` · حالاتُهم = **‏10 `watch` · صفر `ready_now`**
· فوق السقف: YHC ‏+48.1 · HCWB ‏+36.9 · YMAT ‏+34.4 · NXTS ‏+26.6 · ERNA ‏+25.3 ·
HTCR ‏+20.7 · ISPC ‏+17.0 · CMND ‏+15.1 · PLYX ‏+12.7 · XAIR ‏+4.5.

## ② التغيير — مفتاحٌ واحد، بلا رقمٍ جديد، بلا رفض

يُضاف إلى **`rank_key` وحده** مفتاحٌ **أوّلٌ ثنائيّ**:

```
داخلَ النطاق  ⟺  price ≤ max(tranches) × (1 + ENTRY_READY_BAND_TOL_PCT/100)
```

- **ثنائيّ لا مستمرّ:** «داخل» قبل «خارج» — ثم **بقيّةُ المفاتيح كما هي حرفيًّا**
  (جاهزية → h4 → نقاط → rr). فالترتيبُ **داخل كلِّ فئة byte-identical**.
- **صفرُ رقمٍ مخترَع:** `tranches` و`price` من مُخرَج `analyze_ticker` نفسِه،
  والعتبةُ `ENTRY_READY_BAND_TOL_PCT` **هي عتبةُ المالك المعتمَدة أمس** بلا تعديل.
- **ليس بوّابةَ رفض:** لا يُقصى أحد؛ من هو خارج النطاق ينزل ولا يُحذف. فإن كان
  الجميعُ خارجَه فالترتيبُ **مطابقٌ لليوم حرفيًّا**.

## ③ 🔒 ما لا يُمَسّ — شرطُ المالك حرفيًّا

**بوّاباتُ فيصل الستّ عشرة كما هي**، و`M1`-`M14`، و`MIN_DROP_FLOOR`، و`analyze_ticker`،
و`apply_short_gate`/`apply_float_gate`، و`classify_tier`، و`entry_status`،
و`entry_readiness`، والوقفُ والأهدافُ وصيغةُ الدفعات، و`WATCHLIST_SIZE`،
وفصلُ «🔄 متابعة لمركزك» عن عدّاد «جاهز» (قرارُ المالك 2026-07-29).
**الجذرُ الوحيد الذي يتغيّر: `rank_key`** — بإذنٍ صريح.

## ④ السند — من المستودع نفسِه لا من رأيي

1. **قاعدةُ القرار الموثّقة** في docstring `entry_status`: «قرارُ فيصل = **موقعُ
   السعر** لا اكتمالُ بوّابات» — **والمُرتِّبُ اليوم يخالفها**.
2. **`readiness` لا تقرأ `tranches`** (مقيس أعلاه) ⇒ لا تُغني عن معيار القرب.
3. **`T-REPLAY10`** قاست المُرتِّب **عند مستوى الصدفة** وسمّته «مَن يقرّر ما يصل
   المالك» ⇒ هو الموضعُ المسنودُ للتحسين، لا البوّابات.
4. **عقدُ المالك «لا إشعار إلا بالجاهز»** — والقائمةُ اليوم تُسلّم عشرةً لا يجهز منهم أحد.

## ⑤ ⚠️ حدُّ صدقٍ يُكتَب **قبل** أيّ رقم: الباكتيست **لا يستطيع** التحقّق من هذا

`backtest_symbol` يقفز `i += fwd` بعد كلّ صفقة و`BACKTEST_STEP=5` بينها ⇒ كثافةُ
المرشّحين **‏1.03/جلسة (2023) و2.08 (2025)** مقابل **‏109 في جلسةٍ حيّة**
(مقيسٌ في `ranker_tie_result.md`). ومفتاحُ ترتيبٍ **لا أثرَ له حين يكون المرشّحُ
واحدًا في اليوم** ⇒ **أيُّ تشغيلة باكتيست ستخرج ‹لا فرق› وهي `no-op` بنيويّ**.

⇒ **هذا تصحيحُ تصميمٍ مسنودٌ نصًّا، لا حافّةٌ مقيسة.** ولا يُدَّعى له عائد.
**والقياسُ الوحيد الصالح أماميّ** — ويُسجَّل أدناه.

## ⑥ المقياسُ الأماميّ — مثبَّتٌ الآن

يُقاس يوميًّا من `weekly_watchlist.json` بلا أداةٍ جديدة:

- **`P-1` نصيبُ القابلين للدخول بين المُسلَّمين** = عددُ المرشّحين داخل النطاق ÷ 10.
  **الأساسُ المقيس: ‏0/10 اليوم · و0-4 من 12-18 في الأيام العشرة الماضية.**
- **`P-2` عددُ أيام «صفر جاهز»** في كلّ عشرة أيامِ اختيار. **الأساس: 7 من 10.**

**يُعتمَد التغييرُ ناجحًا** إن ارتفع `P-1` فوق الأساس **وانخفض `P-2`** خلال **‏20 يومَ
اختيار**. **وإن لم يتغيّرا فالمعنى أن البِركة نفسَها لا تحوي قريبًا** — وهي نتيجةٌ
تُنشَر ولا تُخفى، وتنقل السؤالَ إلى البوّابات لا إلى المُرتِّب.

## ⑦ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر ولو كُذِّبت)

- **`X1`** أوّلُ تجديدٍ بعد التغيير يُسلّم **‏≥1 سهمًا داخل النطاق**. *(يكذّبه صفر ⇒
  البِركةُ خاليةٌ من القريبين، والعلّةُ فوق المُرتِّب.)*
- **`X2`** يتبدّل **‏≥3 من العشرة** المُسلَّمين مقارنةً بالترتيب القديم على نفس البِركة.
- **`X3`** التغييرُ **لا يمسّ عدد المؤهَّلين** (‏43 تبقى 43) — لأنه ترتيبٌ لا رفض.
- **`X4`** لا يتغيّر ترتيبُ **من هم في الفئة نفسها** (داخل/خارج) ولا حرف.

## ⑧ الأثرُ والإجراء

يمسّ **العضوية** ⇒ **`LOGIC_VERSION` مرفوع + `force_renew=1` مرّةً واحدة**
(يُبلَّغ المالكُ صراحةً — ولا يُطلَب منه في غير هذي الحالة).

## ⑨ انضباطٌ مستورَدٌ من الاثني عشر مستودعًا — مطبَّقٌ هنا بالاسم

- **`karpathy/autoresearch` — الميزانيةُ الثابتة:** أيُّ مقارنةٍ للمُرتِّب تقع على
  **نفس البِركة ونفس اليوم ونفس السعة** ⇒ الفرقُ منسوبٌ للمفتاح وحده. (وهنا محقَّقٌ
  **بالبناء**: المفتاحُ يُطبَّق على `results` نفسِها قبل `select_top`.)
- **`karpathy/autoresearch` — مقياسٌ لا يتأثّر بحجم التغيير:** المقياسُ **نسبةٌ**
  (`P-1`) لا عددٌ خام.
- **`multica-ai/andrej-karpathy-skills` — Surgical Changes:** **مفتاحٌ واحد** في
  **دالّةٍ واحدة**، وكلُّ سطرٍ مُغيَّرٍ منسوبٌ إلى هذا الطلب.
- **`multica-ai/...` — Goal-Driven:** معاييرُ النجاح (`P-1`/`P-2`) مكتوبةٌ **قبل**
  التنفيذ مع حلقةِ تحقّق.
- **`addyosmani/agent-skills` — source-driven:** كلُّ عنصرٍ في §④ بسنده.
- **`addyosmani/agent-skills` — doubt-driven:** التنفيذُ يلزمه **أقفالٌ سلوكيّة
  وطفراتٌ تُسقطها**، وحدُّ صدق §⑤ مكتوبٌ قبل الأرقام لا بعدها.
- **`microsoft/qlib` — PIT:** غيرُ مطبَّق، ويبقى حدَّ صدقٍ قائمًا (انحيازُ البقاء).

## ⑩ حدودُ صدقٍ تُقرأ مع أيّ نتيجة

القربُ من الدخول **ليس دليلَ ربحية** — هو دليلُ **قابليةِ تنفيذ**. وقد يكون القريبُ
قريبًا لأنه ضعيف. **ولا يُدَّعى عائدٌ من هذا التغيير**، بل **قابليةُ تنفيذٍ** فقط،
والربحيةُ تبقى غيرَ مقيسة كما هي منذ اليوم الأوّل.

---

## ملحق ①: أوّلُ قياسٍ بعد التنفيذ (2026-08-07، تشغيلة `31182967421`)

| المقياس | الأساس (قبل) | بعد التغيير |
|---|---|---|
| **`P-1` داخل النطاق من المُسلَّمين** | **‏0 من 10** | **‏3 من 10** (`QTI` ‏−2.0% · `OLOX` ‏−3.7% · `AKAN` ‏−1.3%) |
| **«🟢 جاهز» من المُسلَّمين** | **‏0** | **‏3** |
| المؤهَّلون / المُسلَّمون | — | ‏44 / 10 (‏**34 قُصّوا بالسعة**) |

**‏`X1` ✅ تحقّق** (‏≥1 داخل النطاق) — والثلاثةُ في المراتب **1 · 2 · 3**، و**`YHC`
الذي كان الأوّلَ بجاهزية 90 نزل إلى الرابعة**.

🔴 **وحدُّ صدقٍ على `X2`: لا أستطيع نسبَ تبدّلِ الأسماء إلى المفتاح اليوم.** سببان:
① البِركةُ نفسُها تبدّلت بيومٍ كامل (‏44 مؤهَّلًا اليوم) فليست «نفس البِركة» التي
يشترطها نصُّ `X2` · ② **جاهزيةُ المُرشَّحين الجدد لا تُخزَّن يومَ الترشيح**
(‏`make_watch_entry` تضع `readiness: None` ويملؤها التحديثُ اليوميّ) ⇒ يستحيل اليوم
إثباتُ أنهم دون 90، **ولو كانوا فوقها لتصدّروا بالمفتاح القديم أيضًا وصار التغييرُ
`no-op`**. ⇒ **`X2` يبقى «لا حكم» اليوم**، ويُقاس حين تتوفّر جاهزيةُ المُسلَّمين.

⚠️ **ويومٌ واحد ليس حكمًا** — التسجيلُ يشترط **‏20 يومَ اختيار** لكلٍّ من `P-1`/`P-2`.

📌 **وملاحظةٌ وصفيّة تُنشَر ولا يُبنى عليها:** ثلاثةٌ من **المحمولين** جاهزون الآن
(`PSTV` · `IDN` · `KLXE`) وهم خارج عدّاد «🟢 جاهز» بقرار المالك 2026-07-29 — فعددُ
الجاهزين الحقيقيّ في القائمة كلِّها **‏6**، والمعروضُ في العدّاد **‏3**.

📊 **وتوزيعُ الرفض الكامل (‏3383 رمزًا):** `M2_هبوط_تحت_40`=2666 · `M1_سعر`=570 ·
`M10_RSI_فات_القطار`=22 · `نواقص_فوق_5`=19 · `M2_هبوط_فوق_97`=17 · `M3_انفجار_تحت_60`=15
· `M5_سيولة`=9 · `M4_base_واسعة`=6 · `M10_RSI_ما_تشبّع`=5 · `M4_انفجر_فعلاً`=4 ·
**`بعيد_عن_الدخول(0%)`=1** · `نقاط_تحت_35`=1. ثم `M13` نقلت 8 لقائمة B و`M14` حذفت
`RGC` (‏56.3م).
