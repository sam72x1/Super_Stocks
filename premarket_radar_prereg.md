# 🌅📡 `T-PM-RADAR` — رادارُ البريماركت على السوق كلِّه — تسجيلٌ مسبق (2026-09-05)

> **أمرُ المالك:** «سجّل رادار البري» (2026-09-05) بعد تشخيص `ANPA`: أداةُ ما قبل
> الجلسة تقرّر عند **03:50 نيويورك** من بيانات الأمس، و`ANPA` بدأ حركته **04:23** ⇒
> خارجَ نافذتها **بالبناء**؛ وقناةُ السيولة الحيّة (`liq_stage_events`) أمسكته عند
> 04:51 **لأنه في قوائمنا** (`near_watch`). **الثقبُ المقيس:** سهمٌ خارجَ قوائمنا
> الخمس يتحرّك في البريماركت **لا تراه أيُّ قناة**.
> ⛔ **يُدفَع هذا الملفُّ قبل سطرِ كودِ أداةٍ واحد وقبل أيّ رقم.** وسقفُ النجاح
> **اقتراحٌ للمالك** — لا شحنَ بلا «نفّذ».

## ⓪ ما ليس محلَّ اختبار (مثبَّتٌ سلفًا)

- **بوّاباتُ المِرساة لا تُمَسّ:** ‏5%/دقيقة · `LIQ_MIN_USD` $30,000 · قفزةُ حجمٍ ‏3× ·
  اتّجاهٌ داخل (`_inflow`) — **أرقامُ المالك** وثمنُها مقيس (`T-GATE` · `T-CUMRISE` ·
  `T-VOLBASE` ثلاثتُها فشلت في تحسينها). الرادارُ **لا يضيف عتبة ولا يُرخي عتبة**.
- **قناةُ القوائم لا تُمَسّ:** `scan_liq_stages` على `live_watch_universe` كما هو
  بت-بت. الرادارُ **كونٌ ثانٍ** يُمرَّر لنفس الدالّة `liq_stage_events` **بالاسم**.
- **أداةُ ما قبل الجلسة لا تُمَسّ** (سؤالٌ آخر: ترتيبُ الأمس لا حركةُ اليوم).

## ① الفرضيّة

**H:** توسيعُ كون قناة السيولة في البريماركت من قوائمنا (‏≈300-400 رمز) إلى **السوق
كلِّه** (‏≈3,400 رمز · سعرٌ داخل [`MIN_PRICE`، `SPLIT_RADAR_PRICE_MAX`]) يلتقط
متحرّكي البريماركت من صنف `ANPA` **بنفس البوّابات** وبكلفةِ رسائلَ مقبولة.

**والمانعُ الهندسيّ المعلوم:** مسحُ 3,400 رمزًا بنداءِ دقائقَ لكلٍّ كلَّ دورةٍ مستحيلٌ
(القناةُ الحالية تمسح ‏≈400 في ‏≈60ث). ⇒ **مرشِّحٌ رخيصٌ بنداءٍ واحد** ثم الدقائقُ
للمرشَّحين وحدَهم.

## ② التصميم (يُثبَّت قبل أيّ رقم)

1. **المرشِّح:** لقطةُ السوق `/v2/snapshot/locale/us/markets/stocks/tickers` —
   **نداءٌ واحدٌ** يُرجع لكلّ رمزٍ آخرَ شمعةِ دقيقة (`min`) وإغلاقَ الأمس
   (`prevDay.c`). يُرشَّح مَن: سعرُه داخل النطاق · **وسيولةُ دقيقته الأخيرة
   `min.c × min.v ≥ LIQ_MIN_USD`** (الأرضيةُ نفسُها — لا رقمَ جديد) · وهو خارجَ
   `live_watch_universe` (وإلّا تكرّر مع القناة القائمة).
2. **الدقائق:** للمرشَّحين وحدَهم `polygon_minute_bars` بنافذة `LIQ_WINDOW_MIN`
   ثم `liq_stage_events(rows, state, rearm=True)` **الإنتاجيّة** ⇒ نفسُ المراحل
   (`M1`/`M5`/`M30`/`Xs`/`Tg`) ونفسُ الفلتر `alert_filter.json` ونفسُ الدِدوب
   (مفتاحُ حالةٍ `LIQ:` بنطاقٍ موسوم `PMR:` لئلّا يلتبس بالقوائم).
3. **النافذة:** البريماركت وحدَه **‏04:00 ⟶ 09:30 نيويورك** (كونُ الجلسة النظاميّة
   والافتر يبقى القوائمَ — الافترُ خارجَ هذا التسجيل).
4. **الكرت:** كرتُ السيولة نفسُه (`build_liq_stage_alert`) بوسم مصدرٍ «🌅 رادار
   البري — خارج قوائمنا» فيُعرَف أنه بلا خطّة دخولٍ محفوظة.
5. **ميزانيةٌ ثابتة:** دورةٌ ‏≤60ث بسقفِ مرشَّحين `PMR_CAP` **يُعلَن قصُّه** في
   السجلّ (الرقمُ يُثبَّت من فحص الجدوى `V0` **قبل** أيّ قياسِ التقاط، ويُطبَع).

## ③ فحصُ الجدوى `V0` (بوّابةٌ قبل أيّ ذراع — خروجٌ 3 عند سقوطها)

- اللقطةُ تعود خلال **‏≤15ث** وتغطّي **‏≥95%** من كون ناسداك (بـ`prevDay`).
- في **ساعة بريماركت حيّة واحدة** (‏04:00-05:00 نيويورك): عددُ المرشَّحين لكلّ
  دورةٍ **يُطبَع** ولا يُحكَم عليه — هو الذي يُثبّت `PMR_CAP` ويُدوَّن.
- **شاهدُ ضبط:** `ANPA` يوم 2026-09-04 (من ملفّات Polygon المجمَّعة ببوّابات
  الإنتاج بالاسم): المِرساةُ الحيّة كانت 04:51 **لأنه في قوائمنا**؛ الرادارُ يجب أن
  يُنتج **المِرساةَ نفسَها بالدقيقة** (‏`V0-ب` تكافؤُ الكونين على رمزٍ مشترك — وإلّا
  فالمقياسُ اثنان لا واحد).

## ④ الأذرع (لا خامسة)

| الذراع | التعريف |
|---|---|
| `R0` | القناةُ الحالية: قوائمُنا وحدَها (بت-بت) |
| `R1` | **الحاكمة:** `R0` ‏+ رادارُ السوق بالمرشِّح `min.c×min.v ≥ LIQ_MIN_USD` |
| `R2` | وصفيّة: `R1` بمرشِّحٍ إضافيّ **صعودٌ عن إغلاق الأمس ‏≥ `LIQ_MIN_MOVE_PCT`** (‏5% — الرقمُ مُعادٌ بدلالةٍ يوميّة، `engineering`) |
| `R3` | وصفيّة: `R1` مقصورًا على `near_watch` **+ الظرف** (مَن رفضه الفارز وهو داخل ظرف فيصل) |

## ⑤ المقاييس والمعيار (كلُّها قبل الأرقام)

**الكونُ المقيس:** جلساتُ بريماركت **‏2026-08-17 ⟶ 2026-09-04** (‏14 جلسة · ملفّات
Polygon المجمَّعة · حتميّ). **«المتحرّك»** = بلغ **‏+30% عن إغلاق الأمس** داخل
البريماركت **بسيولةٍ تراكميّة ‏≥$100,000** حتى 09:30 (الرقمان مُعادان:
`faisal_verbatim` «‏30%» · و`IGNITION_USD_OPERATOR`).

1. **الالتقاط (الحاكم):** نسبةُ المتحرّكين الذين أُنتجت لهم مِرساةٌ **قبل** بلوغ
   ‏+30% — `R1` ‏≥ **‏70%** والـ`R0` تُطبَع للمقارنة.
2. **الكلفة:** رسائلُ أوّلِ إشعار (‏`M1`) لكلّ جلسة في `R1` ‏≤ **‏3×** رسائل `R0`
   (الوسيط على الجلسات).
3. **التأخّرُ لا يسوء:** وسيطُ صعود المِرساة عن إغلاق الأمس في `R1` **لا يتجاوز**
   `R0` بأكثر من **‏5 نقاط** (ثمنُ البوّابة المقيس ‏+17.1% في `T-GATE`).
4. **صفرُ فقدٍ في القوائم:** كلُّ مِرساةٍ في `R0` تظهر في `R1` **بالدقيقة نفسها**
   (‏`R1 ⊇ R0` بالبناء — سقوطُه **عطبُ أداة** لا نتيجة).

**والأربعةُ تلزم معًا.** الأرضية: **‏≥20 متحرّكًا** في الكون المقيس، وإلّا «لا حكم».

## ⑥ التنبّؤات (قابلةٌ للكسر — تُنشَر مكذَّبةً لا تُحذف)

- **`PR-P1`:** `R0` تلتقط **أقلَّ من 40%** من المتحرّكين (معظمُهم خارجَ قوائمنا).
- **`PR-P2`:** `R1` تلتقط ‏≥70% **لكنّ كلفتَها تتجاوز 3×** (السوقُ كلُّه صاخبٌ فجرًا)
  ⇒ **أرجّح سقوطَ الحاكمة على الكلفة** — وعندها `R2` تُقرأ منحنى كلفةٍ للمالك.
- **`PR-P3`:** `ANPA` يظهر في `R1` عند **‏04:5x** (المِرساةُ الحيّة نفسُها).
- **`PR-P4`:** `R3` تلتقط **دون 30%** (الظرفُ يضيّق أكثرَ ممّا يفيد).

## ⑦ حدودُ صدقٍ (مكتوبةٌ قبل الأرقام)

- **لمسٌ لا تنفيذ:** «التقاط» = مِرساةٌ قبل +30%، لا ربحيّة — **ولا عائدَ يُقاس هنا.**
- **اللقطةُ الحيّة لا تُعاد تاريخيًّا:** القياسُ يُعيد المرشِّح من ملفّات الدقائق
  المجمَّعة **بنفس الشرط** (سيولةُ آخر دقيقة) وهو **تقريبٌ لِما ستراه اللقطة** —
  فرقُ التغطية يُقاس في `V0` ويُعلَن.
- **الكونُ 14 جلسة** = سقفٌ متفائل للاستقرار؛ **لا ثلاثُ سنوات** (ملفّاتُ الدقائق
  متاحة، لكنّ سؤالَ «كم يُكلّف» يُحسم بأسبوعين والحكمُ الطويل بالحصاد الأماميّ).
- **الرقيقُ جدًّا** (سعرٌ دون `MIN_PRICE`) خارجَ الكون — `CHAI`/`RITR` فئةٌ لا يراها.
- **ما يُعرَض بلا خطّةِ دخولٍ محفوظة** ⇒ الكرتُ يقول ذلك صراحةً.

## ⑧ سقفُ النجاح

استيفاءُ الأربعة ⇒ **اقتراحٌ للمالك** بشحن `R1` مقطعَ بريماركتٍ في
`operator_entry_live.py` خلف علمٍ `PM_RADAR` **مطفأ** افتراضًا · **ولا مسَّ**
بـ`liq_stage_events` ولا بالفلتر ولا بأرقام المالك. **الشحنُ بأمر «نفّذ» حصرًا.**

---

## ⑨ ملحقٌ مؤرَّخ (2026-09-05، بعد صدور الحكم — يُدفَع **قبل** أيّ رقمٍ ثانٍ)

> **السبب:** الحكمُ صدر (`premarket_radar_result.md` — فشلت، ① ‏24.0% مقابل 70)،
> وكشفت قراءةُ الكود بعده أن **الحالةَ المؤسِّسة للتجربة (`SGRX` يوم 09-04) مستبعَدةٌ
> من القياس بالبناء**: `parse_pre` تُسقط كلَّ شموع بريماركت مَن لا إغلاقَ أمسٍ **باسمه**.
> ⛔ **والحكمُ المنشور لا يُستبدَل** — كلُّ ما في هذا الملحق **يُنشَر بجانبه**، والمُغلَقُ
> في `result §⑧` **لا يُعاد فتحُه**.

### ⑨-1 الافتراضُ بت-بت (شرطُ صلاحيةٍ لا خيار)

كلُّ ما يلي خلف `PMR_MODE` **افتراضُه `legacy`** ⇒ **إعادةُ إنتاج التشغيلة
`33997013910` حرفيًّا** (‏24.0% · 263 متحرّكًا · 149/298 رسالة) — يُقفَل بالسويّة.
⇒ **الرقمُ الأوّل يبقى قابلًا للمقارنة مهما أُصلح بعده.**

### ⑨-2 تشخيصٌ دائمٌ لا يمسّ رقمًا (‏`log` فقط · في الوضعين)

1. **أربعةُ أسبابٍ مُسمّاة** بدل «أو» الواحدة، وعدّادُ كلٍّ لكلّ يوم: **بلا إغلاقِ
   أمسٍ باسمه** · خارجَ النطاق السعريّ · بلا شمعةِ بريماركت · بلا مرشِّحٍ ولا قوائمَ
   ولا متحرّك. (‏«حكمٌ سالبٌ بلا سببٍ مُسمًّى يخفي تشخيصَه» — وقد أخفاه على `SGRX`.)
2. **حجمُ العمى يُقاس لا يُفترَض:** عدّادُ «له شموعُ بريماركت **وبلا** إغلاقِ أمسٍ
   باسمه» لكلّ يوم ‏+ أعلى خمسةٍ منهم بسيولة البريماركت.
3. **عدُّ الجلسات من `len(days)`** لا رقمًا مغروسًا (‏«‏14» بائتةٌ والمقيسُ 15).
4. **عدّادُ المرجع البائت:** كم صفًّا `prev_close`ه أقدمُ من الجلسة السابقة (القاموسُ
   تراكميٌّ لا يُصفَّر ⇒ قيمتُه «آخرُ إغلاقٍ معروف»).

⚖️ **و`V0-ب` العامّ (‏82.3%) يبقى مطبوعًا بلا عتبة — عمدًا:** تحويلُه بوّابةً بحدٍّ
أختاره **بعد** رؤية الرقم هو تحريكُ هدفٍ بعينه. ⇒ يُعلَن حدًّا في `result §⑦-2`،
**وتُشترَط بوّابتُه في أيّ تسجيلٍ لاحق يقوم على تكافؤ الكونين** لا هنا.

### ⑨-3 الوضعُ الثاني `PMR_MODE=minc` (يُغيّر المقيس ⇒ يُعلَن قبل الرقم)

**السبب:** العقد §②-1 يقول «سعرُه داخل النطاق» **بلا حسم** `min.c` أم `prevDay.c`،
واللقطةُ الحيّة تحمل `min.c` ⇒ **المرشِّحُ الحيُّ يستطيع تصفيةَ السعر من الدقيقة
نفسِها بلا `prevDay.c`** — والأداةُ حسمتها `prevDay` فصارت **أشدَّ من التصميم الذي
تقيسه**. في هذا الوضع:

- **بوّابةُ النطاق السعريّ من إغلاق الدقيقة** لا من إغلاق الأمس ⇒ مَن لا إغلاقَ أمسٍ
  باسمه **يدخل كونَ المرشَّحين** (وهو ما يجيب سؤالَ المالك: هل يُرسي الرادارُ على
  `SGRX` يوم 09-04 ومتى؟).
- 🔒 **ومقامُ «المتحرّك» مجمَّدٌ بالبناء:** يبقى شرطُه **كما هو حرفيًّا** (‏+30% عن
  **إغلاق الأمس** · سيولةٌ تراكميّة ‏≥$100,000 · **وإغلاقُ الأمس داخل النطاق**) ⇒
  **‏263 متحرّكًا هم هم في الوضعين** (قفلٌ يشترط تساوي المقام). ⇒ **البسطُ وحدَه
  يمكن أن ينمو، فالرقمُ الثاني مقارنةٌ صارمةٌ للمعيار ① على المقام نفسِه.**
- **الأذرعُ الأربعة والمعاييرُ الأربعة وبوّاباتُ المالك: لا تُمَسّ.**
- ⚠️ **وكلفةُ `R1` قد ترتفع** (كونُ المرشَّحين يتّسع) ⇒ **تُقاس وتُنشَر ولا تُقارَن
  مباشرةً بـ‏×2.00** (مجتمعُ المرشَّحين تغيّر).

### ⑨-4 ما لا يُفعَل في هذا الملحق (يُعلَن لئلّا يُفعَل صامتًا)

- **لا خريطةَ إعادةِ تسمية** (تلزمها `/vX/reference/tickers/.../events` بمفتاحٍ غيرِ
  موصولٍ بالـworkflow) ⇒ **`SGRX` يبقى خارجَ مقام المتحرّكين**، ويُجاب عنه **تشخيصًا**
  (هل صار مرشَّحًا؟ وفي أيّ دقيقةٍ أرسى؟) لا عدًّا في النسبة.
- **لا مرجعَ بديلٌ مخترَع** لمن لا إغلاقَ أمسٍ له (‏«‏+30% عن أوّل شمعةِ بريماركت»
  تعريفٌ آخرُ لا يُخلَط بالمنشور).
- **لا تغييرَ لعتبةٍ ولا لذراعٍ ولا لمعيار** · ولا مسَّ بـ`liq_stage_events` ولا
  بأرقام المالك (‏5% · $30,000 · 3×) · ولا `LOGIC_VERSION`.

### ⑨-5 سقفُ نجاح الرقم الثاني

**وصفيٌّ فقط.** لا يُبدّل الحكمَ المنشور · ولا يُنتج اقتراحَ شحن · ولا يُعيد فتح
المُغلَق. **والشحنُ يبقى مشروطًا باستيفاء الأربعة — ولم يُستوفَ.**

---

## ⑩ ملحقٌ مؤرَّخ (2026-09-06، بعد §⑩ من النتيجة — يُدفَع **قبل** أيّ سطرِ كودٍ وقبل أيّ رقم)

> **السبب:** أمرا المالك «**قِس مرجع المرشِّح**» و«**حارسٌ مكتوبٌ غيرُ منفَّذٍ ليس
> حارسا**». الأوّلُ يخصّ `result §⑦-8`/`§⑧-3`: العقدُ §②-1 يقول «سعرُه داخل النطاق»
> **بلا حسم** `min.c` أم `prevDay.c` ⇒ **‏24.0% قد تكون أرضيةً لا سقفًا**. والثاني يخصّ
> `§⑦-4` أعلاه: العقدُ يَعِد «فرقُ التغطية يُقاس في `V0` ويُعلَن» **وصفرُ قياسٍ في
> الكود** — وهو الصنفُ المدوَّن «حارسٌ مكتوبٌ وغيرُ منفَّذٍ ليس حارسًا — هو نيّة».
> **الحكمُ المنشور «فشلت (‏3 من 4)» لا يُستبدَل**؛ ما يلي يُقاس ويُنشَر بجانبه.

### ⑩-1 قياسُ مرجع المرشِّح — أين يمكن للمرجع أن يحرّك ①؟

**حسابٌ قبل الرقم:** البسطُ (المُلتقَطون) **ثابتٌ على المقام المجمَّد** (‏§⑨-3: `R1`
بوضع `minc` أعطت 63 = المنشور). ⇒ **القناةُ الوحيدة** التي يستطيع المرجعُ أن يحرّك
بها ① هي **المقام**: متحرّكون يُقصيهم `prevDay.c` ويقبلهم `min.c`. وهم بالتعريف:
إغلاقُ أمسٍ **موجودٌ باسمه لكن خارجَ النطاق**، ودقائقُ بريماركتِهم **داخل** النطاق
تبلغ ‏+30% عن إغلاق الأمس بسيولةٍ تراكميّة ‏≥ `MOVER_USD`.

- **الحزامُ الوحيد الممكن حسابيًّا: `[PRICE_LO/1.3، PRICE_LO)` = ‏[0.3077، 0.40)$.**
  الطرفُ العلويّ **مستحيل**: مَن إغلاقُ أمسِه فوق `PRICE_HI` يلزمه ‏+30% **فوق 10$**
  فتخرج دقيقتُه من النطاق ⇒ لا تُقبَل بـ`minc` أصلًا.
- **`D_wide`** = المتحرّكون بالشرط الخام (`mover_t30`: ‏+30% · ‏≥$100,000 · **بلا**
  بوّابةِ نطاقٍ على إغلاق الأمس) على شموع `minc`. **`D_wide ⊇ D_frozen` بالبناء**
  (‏`mover_for` = `mover_t30` مقيَّدةً بالنطاق). `n_extra = |D_wide| − |D_frozen|`.
- **`capture_wide` = مُلتقَطو `R1` في `D_wide` ÷ `|D_wide|`**، ويُطبَع بجانب `capture`
  المنشور. **قاعدةُ القراءة (مثبَّتةٌ الآن):** `capture_wide ≥ 24.0` ⇒ الرقمُ المنشور
  **أرضيةٌ** (المرجعُ الأوسع لا يُنقص) · `< 24.0` ⇒ **سقفٌ** (المرجعُ الأوسع يُغرق
  المقام). **وفي الحالتين الحكمُ لا يتغيّر** (الفارقُ عن 70 بمرتبة).
- **مَن لا إغلاقَ أمسٍ باسمه** (‏`no_prev`، حالةُ `SGRX`) وأرسى له `R1` بـ`minc`:
  يُعَدّ **«غيرَ قابلٍ للوسم»** ويُطبَع عددُه — **لا يُخترَع له مرجع** (‏§⑨-4)
  ولا يدخل بسطًا ولا مقامًا.
- **جانبُ `prevDay.c` غيرُ قابلٍ للقياس من الملفّات:** لا نعرف أيَّ مرجعٍ تحمله
  اللقطةُ الحيّة فعلًا؛ **يُعلَن ولا يُدَّعى** — والحسمُ من مقطعٍ حيّ.
- **تنبّؤات قابلة للكسر:** `RF-P1` ‏`n_extra ≤ 13` (‏≈5% من 263؛ الحزامُ ضيّقٌ
  و`MIN_PRICE` أرضيةُ الكون) · `RF-P2` ‏`capture_wide ∈ [22، 27]` (المقامُ يتحرّك
  قليلًا فلا يقلب القراءة) · `RF-P3` **غيرُ القابلين للوسم بمِرساة `R1` ‏≤3** على
  الجلسات المقيسة.

### ⑩-2 حارسُ التغطية — تنفيذُ `§⑦-4` بثلاث طبقاتٍ مُسمّاة

المرجعُ الحيّ = مراسي `op_entry_state.json` من التاريخ (‏`lists_universe`) **داخل نافذة
البريماركت وحدَها** (‏04:00-09:30 نيويورك)؛ ما خارجها ليس ممّا تقيسه الأداة فلا يدخل.

- **الطبقة A — استبعادُ الكون بسببٍ مُسمّى (ليس عيبًا):** مِرساةٌ حيّةٌ لرمزٍ **بلا
  شمعةِ بريماركت** في ملفّ اليوم · أو **بلا إغلاقِ أمسٍ باسمه** · أو **خارجَ النطاق**
  — تُعَدّ وتُطبَع بسببها، **ولا تدخل مقامَ التغطية**: الأداةُ لا تدّعي رؤيتَها
  (‏§⑦ «الرقيقُ دون `MIN_PRICE` خارجَ الكون»).
- **الطبقة B — التغطيةُ الحاكمة:** بين المراسي الحيّة **داخل الكون** (شمعةُ بريماركت
  ‏+ إغلاقُ أمسٍ داخل النطاق): نسبةُ مَن أعطته الإعادةُ **مِرساةَ `R0`** (أيَّ دقيقة).
  **الحدُّ `COVERAGE_MIN = 90.0`** — **رقمٌ مُعادٌ من `T-SLIP W3`** (‏«‏≥90% وإلّا
  تُوسَم جزئيّة») لا مخترَع. **دونه ⇒ خروجٌ 3 قبل جدول الأذرع** (بوّابةُ صلاحيةٍ لا
  إعلام — درسُ `RV4`).
- **الطبقة C — التكافؤُ بالدقيقة (‏`V0-ب` العامّ):** يبقى **بلا عتبة** بالقرار
  المُعلَن في `result §⑦` (‏«‏82.3% يُطبَع بلا عتبةٍ منفَّذة»)؛ تحريكُه الآن إلى عتبةٍ
  بعد رؤية 82.3 **تحريكُ بارٍ** ⇒ يُطبَع كما هو.
- **إن سقطت B:** يُوسَم الحكمُ المنشور **«جزئيًّا»** ببانرٍ مؤرَّخ ويُطبَع المستبعَدون
  بالاسم — **ولا يُعاد التشغيلُ بضبطٍ حتى يعبر** (عطبُ أداةٍ يُصلَح بتسجيلٍ لا بتجربة).
- **تنبّؤات:** `RF-P4` **التغطية ‏≥90%** (الكونُ الحيُّ هو ما يقرؤه العامل من
  الملفّات نفسِها) · `RF-P5` **الطبقةُ A يهيمن عليها «خارجَ النطاق دون
  `MIN_PRICE`»** لا «بلا شمعة».

### ⑩-3 ما لا يُفعَل

- **لا خريطةَ إعادةِ تسمية** ولا مرجعَ مخترَع لمن لا إغلاقَ أمسٍ له (‏§⑨-4 كما هو).
- **لا تغييرَ لعتبةٍ (‏70 · 3× · +5 · 20) ولا ذراعٍ ولا معيار** بعد الأرقام · ولا
  `LOGIC_VERSION` · و**`Super_stock.py` لا يُمَسّ بحرف**.
- **صفوفُ `legacy` بت-بت في قيمها** (الحقلُ الجديد `t30_wide` يساوي `t30` هناك
  لأن مَن خارج النطاق **لا شموعَ له** في ذلك الوضع — مقفولٌ باختبار).

### ⑩-4 سقفُ النجاح

**وصفيٌّ فقط.** `capture_wide` يقيّد **قراءةَ** الرقم المنشور (أرضيةٌ/سقف) ولا
يُبدّله · وحارسُ التغطية **يحرس الأداة** ولا يُنتج حكمًا · **ولا اقتراحَ شحن** —
الشحنُ يبقى مشروطًا باستيفاء الأربعة ولم يُستوفَ.

### ⑩-5 الإغلاق (2026-09-06، أمرُ المالك «اقفل §⑩»)

**مُغلَق.** النتيجةُ في `premarket_radar_result.md §⑪`: حارسُ التغطية منفَّذٌ وعبر (‏95.8%) ·
ومرجعُ المرشِّح **سقفٌ** (‏21.1 < 24.0) · والحكمُ المنشور «فشلت (‏3 من 4)» نافذٌ بلا تغيير.
⛔ **لا يُعاد فتحُ §⑩ بتحريك عتبةٍ أو ذراع.** 🔓 **وما يبقى مفتوحًا يلزمه تسجيلٌ مؤرَّخٌ جديد
وأمرٌ صريح:** الفاقدون الخمسة (‏4.2%) · جانبُ `prevDay.c` من مقطعٍ حيّ · القيدُ الحاكم
(المرشِّح أم بوّابةُ 3×) · الافتر · الربحية.
