# 🌅📡 `T-PM-RADAR` — رادارُ البريماركت على السوق كلِّه — تسجيلٌ مسبق (2026-09-05)

> **أمرُ المالك:** «سجّل رادار البري» (2026-09-05) بعد تشخيص `ANPA`: أداةُ ما قبل
> الجلسة تقرّر عند **03:50 نيويورك** من بيانات الأمس، و`ANPA` بدأ حركته **04:23** ⇒
> خارجَ نافذتها **بالبناء**؛ وقناةُ السيولة الحيّة (`liq_stage_events`) أمسكته عند
> 04:51 **لأنه في قوائمنا** (`near_watch`). **الثقبُ المقيس:** سهمٌ خارجَ قوائمنا
> الخمس يتحرّك في البريماركت **لا تراه أيُّ قناة**.
> ⛔ **يُدفَع هذا الملفُّ قبل سطرِ كودِ أداةٍ واحد وقبل أيّ رقم.** وسقفُ النجاح
> **اقتراحٌ للمالك** — لا شحنَ بلا «نفّذ».

## ⓪ ما ليس محلَّ اختبار (مثبَّتٌ سلفًا)

- **بوّاباتُ المِرساة لا تُمَسّ:** ‏5%/دقيقة · `LIQ_MIN_USD` $30,000 · قفزةُ حجمٍ ‏3× ·
  اتّجاهٌ داخل (`_inflow`) — **أرقامُ المالك** وثمنُها مقيس (`T-GATE` · `T-CUMRISE` ·
  `T-VOLBASE` ثلاثتُها فشلت في تحسينها). الرادارُ **لا يضيف عتبة ولا يُرخي عتبة**.
- **قناةُ القوائم لا تُمَسّ:** `scan_liq_stages` على `live_watch_universe` كما هو
  بت-بت. الرادارُ **كونٌ ثانٍ** يُمرَّر لنفس الدالّة `liq_stage_events` **بالاسم**.
- **أداةُ ما قبل الجلسة لا تُمَسّ** (سؤالٌ آخر: ترتيبُ الأمس لا حركةُ اليوم).

## ① الفرضيّة

**H:** توسيعُ كون قناة السيولة في البريماركت من قوائمنا (‏≈300-400 رمز) إلى **السوق
كلِّه** (‏≈3,400 رمز · سعرٌ داخل [`MIN_PRICE`، `SPLIT_RADAR_PRICE_MAX`]) يلتقط
متحرّكي البريماركت من صنف `ANPA` **بنفس البوّابات** وبكلفةِ رسائلَ مقبولة.

**والمانعُ الهندسيّ المعلوم:** مسحُ 3,400 رمزًا بنداءِ دقائقَ لكلٍّ كلَّ دورةٍ مستحيلٌ
(القناةُ الحالية تمسح ‏≈400 في ‏≈60ث). ⇒ **مرشِّحٌ رخيصٌ بنداءٍ واحد** ثم الدقائقُ
للمرشَّحين وحدَهم.

## ② التصميم (يُثبَّت قبل أيّ رقم)

1. **المرشِّح:** لقطةُ السوق `/v2/snapshot/locale/us/markets/stocks/tickers` —
   **نداءٌ واحدٌ** يُرجع لكلّ رمزٍ آخرَ شمعةِ دقيقة (`min`) وإغلاقَ الأمس
   (`prevDay.c`). يُرشَّح مَن: سعرُه داخل النطاق · **وسيولةُ دقيقته الأخيرة
   `min.c × min.v ≥ LIQ_MIN_USD`** (الأرضيةُ نفسُها — لا رقمَ جديد) · وهو خارجَ
   `live_watch_universe` (وإلّا تكرّر مع القناة القائمة).
2. **الدقائق:** للمرشَّحين وحدَهم `polygon_minute_bars` بنافذة `LIQ_WINDOW_MIN`
   ثم `liq_stage_events(rows, state, rearm=True)` **الإنتاجيّة** ⇒ نفسُ المراحل
   (`M1`/`M5`/`M30`/`Xs`/`Tg`) ونفسُ الفلتر `alert_filter.json` ونفسُ الدِدوب
   (مفتاحُ حالةٍ `LIQ:` بنطاقٍ موسوم `PMR:` لئلّا يلتبس بالقوائم).
3. **النافذة:** البريماركت وحدَه **‏04:00 ⟶ 09:30 نيويورك** (كونُ الجلسة النظاميّة
   والافتر يبقى القوائمَ — الافترُ خارجَ هذا التسجيل).
4. **الكرت:** كرتُ السيولة نفسُه (`build_liq_stage_alert`) بوسم مصدرٍ «🌅 رادار
   البري — خارج قوائمنا» فيُعرَف أنه بلا خطّة دخولٍ محفوظة.
5. **ميزانيةٌ ثابتة:** دورةٌ ‏≤60ث بسقفِ مرشَّحين `PMR_CAP` **يُعلَن قصُّه** في
   السجلّ (الرقمُ يُثبَّت من فحص الجدوى `V0` **قبل** أيّ قياسِ التقاط، ويُطبَع).

## ③ فحصُ الجدوى `V0` (بوّابةٌ قبل أيّ ذراع — خروجٌ 3 عند سقوطها)

- اللقطةُ تعود خلال **‏≤15ث** وتغطّي **‏≥95%** من كون ناسداك (بـ`prevDay`).
- في **ساعة بريماركت حيّة واحدة** (‏04:00-05:00 نيويورك): عددُ المرشَّحين لكلّ
  دورةٍ **يُطبَع** ولا يُحكَم عليه — هو الذي يُثبّت `PMR_CAP` ويُدوَّن.
- **شاهدُ ضبط:** `ANPA` يوم 2026-09-04 (من ملفّات Polygon المجمَّعة ببوّابات
  الإنتاج بالاسم): المِرساةُ الحيّة كانت 04:51 **لأنه في قوائمنا**؛ الرادارُ يجب أن
  يُنتج **المِرساةَ نفسَها بالدقيقة** (‏`V0-ب` تكافؤُ الكونين على رمزٍ مشترك — وإلّا
  فالمقياسُ اثنان لا واحد).

## ④ الأذرع (لا خامسة)

| الذراع | التعريف |
|---|---|
| `R0` | القناةُ الحالية: قوائمُنا وحدَها (بت-بت) |
| `R1` | **الحاكمة:** `R0` ‏+ رادارُ السوق بالمرشِّح `min.c×min.v ≥ LIQ_MIN_USD` |
| `R2` | وصفيّة: `R1` بمرشِّحٍ إضافيّ **صعودٌ عن إغلاق الأمس ‏≥ `LIQ_MIN_MOVE_PCT`** (‏5% — الرقمُ مُعادٌ بدلالةٍ يوميّة، `engineering`) |
| `R3` | وصفيّة: `R1` مقصورًا على `near_watch` **+ الظرف** (مَن رفضه الفارز وهو داخل ظرف فيصل) |

## ⑤ المقاييس والمعيار (كلُّها قبل الأرقام)

**الكونُ المقيس:** جلساتُ بريماركت **‏2026-08-17 ⟶ 2026-09-04** (‏14 جلسة · ملفّات
Polygon المجمَّعة · حتميّ). **«المتحرّك»** = بلغ **‏+30% عن إغلاق الأمس** داخل
البريماركت **بسيولةٍ تراكميّة ‏≥$100,000** حتى 09:30 (الرقمان مُعادان:
`faisal_verbatim` «‏30%» · و`IGNITION_USD_OPERATOR`).

1. **الالتقاط (الحاكم):** نسبةُ المتحرّكين الذين أُنتجت لهم مِرساةٌ **قبل** بلوغ
   ‏+30% — `R1` ‏≥ **‏70%** والـ`R0` تُطبَع للمقارنة.
2. **الكلفة:** رسائلُ أوّلِ إشعار (‏`M1`) لكلّ جلسة في `R1` ‏≤ **‏3×** رسائل `R0`
   (الوسيط على الجلسات).
3. **التأخّرُ لا يسوء:** وسيطُ صعود المِرساة عن إغلاق الأمس في `R1` **لا يتجاوز**
   `R0` بأكثر من **‏5 نقاط** (ثمنُ البوّابة المقيس ‏+17.1% في `T-GATE`).
4. **صفرُ فقدٍ في القوائم:** كلُّ مِرساةٍ في `R0` تظهر في `R1` **بالدقيقة نفسها**
   (‏`R1 ⊇ R0` بالبناء — سقوطُه **عطبُ أداة** لا نتيجة).

**والأربعةُ تلزم معًا.** الأرضية: **‏≥20 متحرّكًا** في الكون المقيس، وإلّا «لا حكم».

## ⑥ التنبّؤات (قابلةٌ للكسر — تُنشَر مكذَّبةً لا تُحذف)

- **`PR-P1`:** `R0` تلتقط **أقلَّ من 40%** من المتحرّكين (معظمُهم خارجَ قوائمنا).
- **`PR-P2`:** `R1` تلتقط ‏≥70% **لكنّ كلفتَها تتجاوز 3×** (السوقُ كلُّه صاخبٌ فجرًا)
  ⇒ **أرجّح سقوطَ الحاكمة على الكلفة** — وعندها `R2` تُقرأ منحنى كلفةٍ للمالك.
- **`PR-P3`:** `ANPA` يظهر في `R1` عند **‏04:5x** (المِرساةُ الحيّة نفسُها).
- **`PR-P4`:** `R3` تلتقط **دون 30%** (الظرفُ يضيّق أكثرَ ممّا يفيد).

## ⑦ حدودُ صدقٍ (مكتوبةٌ قبل الأرقام)

- **لمسٌ لا تنفيذ:** «التقاط» = مِرساةٌ قبل +30%، لا ربحيّة — **ولا عائدَ يُقاس هنا.**
- **اللقطةُ الحيّة لا تُعاد تاريخيًّا:** القياسُ يُعيد المرشِّح من ملفّات الدقائق
  المجمَّعة **بنفس الشرط** (سيولةُ آخر دقيقة) وهو **تقريبٌ لِما ستراه اللقطة** —
  فرقُ التغطية يُقاس في `V0` ويُعلَن.
- **الكونُ 14 جلسة** = سقفٌ متفائل للاستقرار؛ **لا ثلاثُ سنوات** (ملفّاتُ الدقائق
  متاحة، لكنّ سؤالَ «كم يُكلّف» يُحسم بأسبوعين والحكمُ الطويل بالحصاد الأماميّ).
- **الرقيقُ جدًّا** (سعرٌ دون `MIN_PRICE`) خارجَ الكون — `CHAI`/`RITR` فئةٌ لا يراها.
- **ما يُعرَض بلا خطّةِ دخولٍ محفوظة** ⇒ الكرتُ يقول ذلك صراحةً.

## ⑧ سقفُ النجاح

استيفاءُ الأربعة ⇒ **اقتراحٌ للمالك** بشحن `R1` مقطعَ بريماركتٍ في
`operator_entry_live.py` خلف علمٍ `PM_RADAR` **مطفأ** افتراضًا · **ولا مسَّ**
بـ`liq_stage_events` ولا بالفلتر ولا بأرقام المالك. **الشحنُ بأمر «نفّذ» حصرًا.**
