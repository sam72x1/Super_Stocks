# 🧱② T-BASE-2 — تسجيل مسبق: **سلّمُ سقف عرض القاعدة (‏40 · 80 · 120 · 23,685)**

> **مدفوعٌ قبل أيّ رقم.** أمر المالك 2026-08-08: قال «**1**» = «قس الوسط» بعد نتيجة
> `T-BASE` (‏40 يرفض LGHL لكنه يُكلّف **‏22.6%** من المنفجرين المُسلَّمين).

## ① السؤال

`T-BASE` أثبت طرفَي المقايضة: **‏23,685 يقبل LGHL** (عرضُ قاعدته المقيس **138.2%**)
و**‏40 يرفضه لكن بثمنِ 12 منفجرًا من 53**. والسؤالُ الباقي — ولم يُقَس قطّ —
**هل يوجد سقفٌ وسطيّ يرفض LGHL ويُبقي أكثر المنفجرين؟**

## ② الأذرع (مثبَّتة — أربعةٌ ولا خامسة)

| ذراع | `BASE_RANGE_MAX_PCT` | الدور |
|---|---|---|
| **B23685** | 23684.631623040408 | الإنتاج اليوم (الأساس) |
| **B120** | 120.0 | وسطيّ — **يرفض LGHL بفارقٍ ضيّق** (138.2 > 120) |
| **B80** | 80.0 | وسطيّ — يرفض LGHL بفارقٍ مريح |
| **B40** | 40.0 | حكمُ `M4-LADDER` (يُعاد **بميزانيةٍ واحدة** = فحصُ تكامل مجاني) |

🔑 **والأربعةُ في تشغيلةٍ واحدة بميزانيةٍ ثابتة** (نفسُ اللقطة · المُرتِّب · السعة ·
النافذة · سقوفُ الجلب) — القاعدةُ المستخرَجة من `karpathy/autoresearch` والتي أفسد
فقدُها `T-CLIFF`. **والفرقُ الوحيد بين الأذرع سقفُ البوّابة.**

⚠️ **ولا ذراعَ خامسة تُضاف بعد رؤية الأرقام** (‏60 · 100 · 150 …) — ذلك اختيارٌ
بعديّ. إن أرادها المالك فتجربةٌ مسجَّلةٌ جديدة.

## ③ البيانات

لقطات PIT الثلاث: **2024=`31199493957` · 2025=`31191327731` · 2026=`31199510868`**
— نفسُها في `T-MINFLOOR`/`T-CHASE`/`T-BASE` فتُقارَن الأرقام مباشرةً.

## ④ بوّابةُ صلاحيةٍ حاكمة (تُقرأ **قبل** أيّ تفسير — خمسةُ شروط)

1. **القيمُ النافذة أربعٌ متمايزة** تُطبَع من `CONFIG` بعد التطبيق.
2. **العدّادُ يشتعل تدرّجًا**: `M4_base_واسعة` في B40 ‏> B80 ‏> B120، و‏≈0 في B23685.
3. **الإشاراتُ رتيبةٌ تنازليًّا**: `B23685 ≥ B120 ≥ B80 ≥ B40` (السقفُ الأضيق لا
   يُنتج إشاراتٍ أكثر — وأيُّ خرقٍ لهذا **عطبُ أداة** لا نتيجة).
4. **المقياسُ الحاكم محسوبٌ لا غائب** (‏`d50`/`مأخوذة` ≠ `None`) — البوّابةُ التي
   أُضيفت بعد التشغيلات الباطلة.
5. **فحصُ تكامل:** `B40` يعيد أرقام `base_result.md` (‏d50 = 17 · 15 · 9) و`B23685`
   يعيد أرقام `T-MINFLOOR` (‏d50 = 20 · 22 · 11). **خرقُه يُبطل التشغيلة كلَّها.**

## ⑤ المقاييس (تُقفَل الآن)

1. **`d50`** بسعة الإنتاج (الحاكم) + `d100`.
2. **`R`/صفقة** مرجَّحًا بالمأخوذين.
3. **نسبةُ الدقة** بمقامها.
4. **الحجم**: الإشارات · المحسومة · المأخوذون · المرفوض بالسعة.
5. **عدّادُ `M4_base_واسعة`** (كلفةُ الحماية).

## ⑥ قاعدةُ القرار — **أشدُّ من `T-BASE` عمدًا** (أربعةُ أذرعٍ ترفع خطرَ المرور بالحظّ)

**تُوصى ذراعٌ وسطيّة** إذا استوفت **الأربعة**:
- **(أ)** `d50` المجمَّع لم ينقص أكثر من **10%** عن B23685.
- **(ب)** **ولا سنةٌ واحدة** تنقص أكثر من **20%** في `d50`. 🔴 **وهذا تشديدٌ مقصود**:
  شرطُ `T-BASE` الثالث كان **عدميًّا** (اشترط «سنةٌ تسوء وأخرى تتحسّن» ولم تتحسّن
  سنةٌ قطّ فمرّ شكليًّا) — فيُستبدَل بحدٍّ سنويٍّ حقيقيّ.
- **(ج)** `R`/صفقة المجمَّع لم يسُؤ أكثر من **0.05R**.
- **(د)** **ترفض LGHL** (يُثبته المِجَسّ §⑧).

**وإن استوفت الذراعان:** تُقدَّم **الأعلى `d50`**؛ وعند التساوي **الأضيق سقفًا**
(حمايةٌ أكثر بنفس التسليم).
**وإن لم تستوفِ أيٌّ منهما:** **لا اعتماد** — ويُبلَّغ الرقمُ كما هو، ويُقيَّد أن
المقايضة **ثابتةٌ ولا مخرجَ وسطيّ فيها**، والقرارُ للمالك في الحالتين.

## ⑦ تنبّؤات قابلة للكسر (قبل أيّ رقم)

- **P1:** الرتابةُ تتحقّق: `d50` و`الإشارات` تنقصان كلَّما ضاق السقف.
- **P2:** **B120 يستوفي (أ)** — نقصُ `d50` مجمَّعًا ‏≤10%.
- **P3:** **B80 لا يستوفي (أ)** — نقصُه بين 10% و20%.
- **P4:** الدقةُ ترتفع في كلّ ذراعٍ أضيق (نمطُ `T-BASE`: ‏+0.7 إلى +1.8).
- **P5:** **‏2025 أسوأُ السنوات** في كلّ ذراع (كانت ‏−31.8% عند 40).
- **P6:** المِجَسّ: **الأربعةُ ما عدا B23685 ترفض LGHL** (‏138.2% > 120 > 80 > 40).

## ⑧ مِجَسٌّ تشخيصيّ **خارج الحكم**

`BASE_PROBE_SYMS` (افتراضُه `LGHL`) يطبع `base_range` المقيس وحكمَ **كلّ** ذراع
بـ`_diagnose_symbol` الإنتاجيّة. **لا يدخل أيَّ مقياسٍ من ⑤** — وهو الذي يحسم الشرط (د).

## ⑨ حدود صدق

انحيازُ بقاء · **تشويهُ تقسيمات** (‏M4 يقيس مدًى على سلسلةٍ معدَّلة فبعضُ «العرض»
تقسيمٌ لا ضخّة — وهذا يقيّد **كلَّ** أذرع M4) · بلا افتر · المُرتِّبُ عند الصدفة ·
و`LGHL` **حالةٌ تشخيصية لا اختبار** (حدثُه بعد as-of لقطة 2026) · وأربعةُ أذرعٍ
ترفع خطرَ المرور بالحظّ ⇒ ولذلك شُدِّدت القاعدة في ⑥.

---
*التنفيذ: `base2_arms.py` + `base2.yml`. النتيجة: `base2_result.md`.*
