# 🥇🎯 T-GOLD-ENTRY — التسجيل المسبق (يُدفَع قبل سطرٍ واحدٍ من الأداة)

> **الأمر:** المالك 2026-08-27 «ارسلها لأوبيس» بعد حزمة
> `OPUS_GOLD_ENTRY_PACKAGE.md` (عصفُ صور الذهب الـ22).
> **هذا الملفُّ نصُّ §⑤ من الحزمة، مع أربعة تدقيقاتٍ تنفيذية مُعلَنة في §⑦
> أدناه — كُتبت كلُّها قبل أيّ رقم**، لأن الحارسَ المكتوب غيرَ المنفَّذ
> «ليس حارسًا — هو نيّة» (درسٌ مدوَّن).
>
> 🔒 **الدفع:** هذا الملفُّ على `origin/main` **قبل** إنشاء
> `gold_entry_arms.py`، وقبل أيّ تشغيلة، وقبل أيّ رقم.

---

## ① السؤال المسجَّل

هل «آلة دخول فيصل» المستخلصة من صور 2026-08-27 — **مراقبةُ القاع ثم طلباتٌ
فوقه بوقفٍ عنده ثم بديلُ الانطلاق** — تعطي توقّعًا (‏`R` لكل صفقة) أعلى من
خطة الإنتاج الحالية **على نفس الحلقات**؟

## ② الهيكل — إعادةُ استعمالٍ بالاسم لا نسخ

`press_wake_arms.py::walk_symbol_wake` هي المِشيةُ المعتمدة، وتنادي بالاسم:
`press_radar.press_read(sl, w=40)` · `rebound_arms.mirror_plan` ·
`rebound_arms.resolve_episode` · وقفزةَ `rebound_arms.WAIT` بعد كلّ مطابقة.
`gold_entry_arms.py` يستورد **الدوالَّ نفسَها بالاسم** ويضيف خططَ الأذرع —
**صفرُ منطقٍ مكرَّر، ومحرّكُ الحسم واحد** (`resolve_episode`) في كلّ ذراع.

🔴 **ومِشيةٌ واحدة لكلّ الأذرع (ميزانيةٌ ثابتة بالبناء لا بالوعد):** تُقرأ
`press_read` **مرّةً واحدةً لكلّ فهرس**، وكلُّ الأذرع تُقيَّم على **مجموعة
الحلقات نفسِها** (نفسُ اللقطة · نفسُ الكون · نفسُ السنة · نفسُ الكادنس).
الأذرعُ تختلف في **الخطة والأهلية فقط** — ولا ذراعَ تغيّر الكادنس ولا عدد
الحلقات. (درسُ `T-CLIFF`: فرقُ الميزانية قُرئ فرقَ ذراع.)

## ③ الأذرع — سبعٌ ولا ثامنة (تُثبَّت الآن قبل أيّ رقم)

- **P0 — الأساس/بوّابةُ التكامل (تشغَّل أوّلًا وتحكم):** خطةُ الإنتاج
  `mirror_plan(press_low)` — دفعاتٌ صعودًا من قاع الضغط بخطوة
  `ENTRY_STEP_PCT` والوقفُ `STOP_BELOW_LOW_PCT` الأعمقُ تحته.
  **تُعيد أرقامَ §⑬/§⑯ المنشورة بت-بت:** مجموعُ HOLD3 عبر الثلاث =
  **‏5371 محسومة · ‏19.08% · ‏+0.174R**. أيُّ تفرّقٍ ⇒ **عطبُ أداةٍ لا
  نتيجة** ⇒ خروج 3 ولا يُقرأ رقمٌ واحدٌ بعدها.
- **P1 — وقفُ القاع:** نفسُ حلقات HOLD3 ونفسُ دفعات الإنتاج، **والوقفُ =
  قاعُ الضغط نفسُه** (`press_low`) بدل «القاع −7%» («**وقفه قاعه**» —
  `checklist_7points.jpg`).
- **P2 — سلّمُ فيصل (الحاكمة المرشَّحة):** الطلبات **فوق القاع**:
  `press_low × (1.01, 1.03, 1.05)` **والوقفُ = القاع**. النِّسَبُ من أرقام
  AMIX الحرفية (وقف 4.60 · دفعات 4.65/4.75/4.85 = ‏+1.1%/+3.3%/+5.4%) —
  وسمُها `faisal_adopted`.
- **P3 — بديلُ الانطلاق (مركَّب):** إن تعبّأت دفعاتُ P2 خلال **K=5 جلسات**
  ⇒ حصيلةُ P2 نفسُها؛ وإلّا ⇒ الدخولُ عند **أوّل إغلاقٍ يكون فيه متوسّطُ 50
  فوق متوسّط 200** داخل نافذة `WAIT` («تدخل مباشره ع خط متوسط 50 اكبر من
  200 · يعني باول انطلاقة السهم») **ووقفُه القاعُ نفسُه**.
  ⚠️ **K=5 هندسيّ مُعلَن** (نصُّه «خلال كم جلسه» بلا رقم).
- **P4 — بوّابةُ خلوّ القروبات:** فوق P2 — تُستبعَد الحلقةُ إذا:
  **(أ)** مدى تذبذّب نافذة الجلوس (من يوم القاع إلى اليوم) يتجاوز **‏10%**
  («سهم جاهز محافظ ع تذبذب نقاط 10٪ حد اقصى» — رقمُه) · **أو (ب)** وُجدت
  في آخر عشر جلساتٍ شمعةٌ مداها **‏20% فأكثر** وجسمُها **دون 30% من مداها**
  («شموع غبيه توصل 20٪ … بدون جسم»).
- **P5 — الوصفةُ الكاملة:** P2 ‏+ P4 ‏+ **RSI يوميّ دون 30** ‏+ **الإغلاقُ
  فوق متوسّطَي 30 و50** (ترويسةُ القائمة السباعية).
- **P6 — حفظُ خمس جلسات:** خطةُ P0 نفسُها لكن الأهليةُ `hold_sessions`
  خمسٌ فأكثر (بند ⑦ «‏5 جلسات») — يجيب: أرقمُ فيصل الأدقّ 3 أم 5؟

## ④ المقياس الحاكم — مثبَّتٌ قبل الأرقام

**متوسّطُ `R` لكل صفقةٍ محسومة** عبر ثلاث سنوات، حيث:
`R = +R_win` للرابحة و`−1` للخاسرة · و`R_win = 0.5×المتوسّط ÷ (المتوسّط −
الوقف)` **من خطة الذراع نفسِها** (نسبيّةٌ بلا وحدة). **وغيرُ المُعبَّأة
(`no_fill`) والمفتوحةُ (`open`) خارج المقام** — كما في §⑬ حرفيًّا.

🔴 **والهدفُ ثابتٌ في كلّ الأذرع** (‏`TARGET_X=1.5` من متوسّط الدفعات)
عمدًا: التجربةُ عن **الدخول** — فتثبيتُ الخروج يعزل ما نقيسه.

📌 **ومقياسٌ ثانويٌّ وصفيٌّ يُطبَع دائمًا ولا يحكم أبدًا:** «‏`R` لكل
**حلقة**» (‏`no_fill` = 0R) — لأن P3 يحوّل حلقاتٍ ميتةً إلى صفقات، وهو أثرٌ
لا يظهر في المقياس الحاكم. **ولا يجوز أن يقلب حكمًا** (سابقةُ «نسبة الموجبة
مقياسٌ يخدع بثبات»: `T-STOP` · `T-LIBERATION` · `T-RECLAIM-INTRADAY`).

## ⑤ معايير الحكم — تلزم كلُّها معًا

1. الذراعُ تتفوّق على P0 بفارق **‏+0.15R فأكثر مجمَّعًا** (الحدُّ المُعادُ
   من `T-STOP`/`T-LIBERATION` بلا تحريك).
2. **الإشارةُ لا تنقلب:** الفارقُ موجبٌ في السنوات الثلاث كلِّها.
3. **فاصلُ 95% للفارق لا يلمس الصفر.**
4. **الأرضية: 40 حلقةً محسومةً لكلّ ذراعٍ لكلّ سنة و150 مجمَّعًا** (المُعادُ
   من `T-EXIT-STOP §V3`). دونها ⇒ **«لا حكم» يُعلَن** ولا يُلتفّ عليه بدمج
   أذرعٍ ولا بخفض أرضية.

## ⑥ بوّابات الصلاحية

- **`V0`** = P0 أعلاه (التكاملُ بت-بت مع §⑬) ⇒ تفرّقٌ = خروج 3.
- **`V1`** حارسُ الـ`no-op`: لكلّ ذراعٍ سطرُ «العلمُ فعّال: N حلقةً تفرّقت
  حصيلتُها عن P0». **صفرُ تفرّقٍ لأيّ ذراع ⇒ خروج 4** (بصمةُ `BT_CANDLE`).
- **`V2`** الأذرعُ تتفرّق فيما بينها فعلًا — عيّنةٌ سلوكيّةٌ في السويّة
  تُنتج ثلاثةَ أجوبةٍ مختلفةٍ لـP1/P2/P3 على المدخل نفسِه.
- **`V3`** تغطيةُ الكون **‏95% فأكثر** والمستبعَدون يُعَدّون بسببهم المُسمّى
  (تفصيلُ التكييف في §⑦-3).
- **`V4`** أقفال السويّة `GE1`-`GE12` + جولةُ طفراتٍ كاملة: كلُّ قفلٍ يسقط
  مرّةً عمدًا، **وطفرةٌ تنهار لا تسقط = قفلٌ هشٌّ يُصلَّب**.

## ⑦ تدقيقاتٌ تنفيذية — أربعةٌ تُعلَن الآن قبل أيّ رقم

**‏1) وحدةُ المخاطرة لكلّ ذراعٍ من خطّتها هي.** `press_backtest.r_win_value()`
ثابتٌ محسوبٌ **بنسب خطة الإنتاج وحدها** — واستعمالُه لذراعٍ وقفُها مختلف
**يقيس بمسطرةٍ ليست مسطرتَها** (صنفُ «مقياسان لا واحد»). فتُحسب
`r_win_of(tranches, stop)` نقيّةً لكلّ ذراع، **ويُقفَل** أن قيمتَها لخطة
الإنتاج **تساوي `r_win_value()` بت-بت** (‏`GE3`).

**‏2) الشمعةُ «الغبيّة» تُقاس على اليوميّ — وهذا تقريبٌ مُعلَن.** اللقطةُ
المجمَّدة **يوميّةٌ حصرًا**، وبناءُ 4 ساعاتٍ منها مستحيلٌ بالاتّجاه. فتُقاس
(ب) على الشمعة اليوميّة **بحدّ فيصل نفسِه (‏20% مدًى)** ونسبةِ جسمٍ **دون
30%** (‏هندسيّ). ⚠️ **وهو أضيقُ التقاطًا من قاعدته** (شمعةُ 4س بمدى 20% قد
تقع داخل يومٍ مداه أقلّ) ⇒ **يُنشَر «تقريبٌ يوميّ» في كلّ سطرٍ يذكره، ولا
يُسمّى قاعدةَ فيصل.**

**‏3) `V3` تُكيَّف على الشاسيه الحقيقيّ — والعيبُ المذكور لا ينطبق.** عيبُ
`KS.weekdays` (عدُّ عطلات السوق «أيامًا مفقودة») يخصّ أدواتِ **ملفّات
الأيام**؛ وهذي الأداةُ تمشي **لقطةً مجمَّدة** بلا ملفّات أيامٍ إطلاقًا ⇒ لا
«أيامَ مفقودة» فيها بنيويًّا. **فـ`V3` تصير تغطيةَ رموزٍ**: (رموزٌ مُشيت) ÷
(رموزٌ في اللقطة) **‏≥95%**، والمستبعَدون يُعَدّون بسببٍ مُسمًّى (تاريخٌ
أقصرُ من `MIN_BARS+5` · إطارٌ تالف). **يُصرَّح بهذا التكييف هنا** ولا
يُدَّعى أن العيبَ أُصلح في أداةٍ لا يمسّها.

**‏4) `P3` يلزمه 200 شمعة.** متوسّطُ 200 غيرُ معرَّفٍ قبلها، والمِشيةُ تبدأ
عند `MIN_BARS=60` ⇒ حلقاتُ الفهارس دون 200 **غيرُ قابلةٍ للقياس في P3
وحدَه**: تُستبعَد من مقامه **وتُعَدّ وتُطبَع باسمها**. وإن كسر ذلك أرضيةَ
الأربعين ⇒ **«لا حكم لـP3»** — وهي نتيجةٌ مشروعةٌ مُعلَنةٌ سلفًا.

## ⑧ تنبّؤاتي المسجَّلة (قابلةٌ للكسر — تُنشَر ولو كُذِّبت)

- **`GP1`:** P1 يحسّن قليلًا ولا يبلغ ‏+0.15R.
- **`GP2`:** P2 الأقربُ للعبور (تعبئةٌ أعلى بمخاطرةٍ أضيق).
- **`GP3`:** P3 يضيف تسليمًا ماديًّا (‏`R` لكل حلقةٍ أعلى) **وتوقّعُه لكلّ
  صفقةٍ أدنى من P2** (دخولٌ أغلى بالبناء — نمطُ `T-LIBERATION`).
- **`GP4`:** P4 يحسّن النقاءَ ويقصّ العيّنة — والخطرُ أن يكسر أرضية الأربعين.
- **`GP5`:** P6 لا يتفوّق على HOLD3 ماديًّا (سلّمُ الحفظ هضبةٌ 2-5 قمّتُها 3).
- **`GP6` (مضافٌ الآن بحكم البنية، لا بعد رقم):** الوقفُ عند القاع يرفع
  `R_win` من ‏≈5.15 إلى ‏≈17 **ويرفع نسبةَ ضربِ الوقف بالقدر نفسِه** —
  فالحكمُ يقع على أيّهما غلب، **ولا أزعم اتّجاهه سلفًا**.

## ⑨ سقفُ النجاح — مثبَّتٌ سلفًا

مهما كانت الأرقام: **اقتراحٌ للمالك + سطرُ عرضٍ تشخيصيّ.** لا أتمتةَ دخول ·
لا تغييرَ خطة إنتاج · لا مسَّ بوّابةٍ ولا عتبة. وأيُّ اعتمادٍ يمسّ
الدخول/الوقف/العضوية ⇒ **قرارُ مالكٍ صريح** + `LOGIC_VERSION` +
`force_renew=1`.

🔒 **وممنوعٌ بنصّ هذا العقد:** مسُّ أرقام المالك (`LIQ_*`) · مسُّ
`kasih_scan.resolve` · مسُّ صيّاد المقسّم · مسُّ أيٍّ من الجذور الاثني عشر
(تُفحَص ببصمة AST قبل كلّ دفعة).

## ⑩ حدودُ صدقٍ تُكتب قبل الأرقام

- **«التعبئة» لمسٌ لا تنفيذ** (بلا انزلاق) — **سقفٌ متفائلٌ بالتساوي على
  الأذرع السبع** فلا يحابي واحدة.
- حلقاتُ HOLD3 **كونُ `press_read`** لا كلَّ تنبيهات البوت الحيّة ⇒
  التعميمُ على قناة السيولة يُقاس أماميًّا ولا يُدَّعى.
- شمعةُ القروبات **تقريبٌ يوميّ** (§⑦-2).
- المقارنةُ داخليّة (‏P0 مقابل الأذرع) على **نفس الحلقات** ⇒ أثرُ السوق
  والانحيازاتُ المشتركة **تُطرَح**؛ وما لا يُطرَح هو انحيازُ البقاء في
  اللقطة نفسِها — **مُعلَنٌ ولا يُصحَّح**.
- `R` هنا **بهدفٍ ثابتٍ ووقفٍ آليّ** — وفيصل يدير حيًّا (مسحٌ واستعادةٌ
  يدويّة · خروجٌ بقرار) ⇒ **المقيسُ الجزءُ القابلُ للأتمتة من طريقته لا
  الطريقةُ كلُّها.**
