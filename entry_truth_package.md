# 📦🎯 حزمة التنفيذ لأوبيس — «صدق نقاط الدخول» (أمر المالك 2026-08-13: «نفذ ١ وسجل ب»)

> **العقد:** مواصفةٌ كاملة — أوبيس ينفّذ حرفيًّا ولا يجتهد خارجها؛ الغموض ⇒ توقّف
> واسأل. **الترتيب إلزاميّ:** تُنفَّذ **بعد** دمج `list_separation_package.md`
> (تسلسلٌ لا تداخل). السند: `entry_points_audit.md` (الغلطان المقيسان) +
> `anchor_prereg.md` (عقد التجربة ب).

---

# الجزء أ — إصلاح الصدق الإنتاجيّ (أمر «نفذ ١»)

**الغلط المقيس:** ‏88% من التنبيهات الحية سُعِّرت فوق نطاق دفعاتها (وسيط ‏+13.6%
فوق الدعم) ⇒ `rr` المحسوب من متوسط الدفعات **خيالٌ غير قابل للتنفيذ** (مخطَّط
2.26R مقابل واقع 0.76R — `P0-08`) ⇒ البوّابة تقبل ملاحقاتٍ بأرقامٍ وهمية والكرت
يطبع نقاطًا ميتة.

## أ-1 · `rr` الصادق (داخل `analyze_ticker` — **مسُّ جذرٍ بأمر المالك المباشر**)

**الموضع:** كتلة `rr` في `Super_stock.py:3534-3543` (بعد
`entry_ref = round(sum(tranches)/len(tranches), 4)` وحساب `rr`/`rr2` وقبل بوّابة
`MIN_RR_T1`). **أضف:**

```python
        # 🎯 صدقُ RR (أمر المالك 2026-08-13 «نفذ أ» — entry_points_audit.md §③-1):
        # فوق نطاق الدفعات لا تعبئةَ اليوم عند المتوسط (أوامر حدٍّ تحت السوق)
        # ⇒ العائدُ القابل للتنفيذ يُقاس من السعر الحالي. داخل النطاق ⇒ بت-بت.
        # (فوق النطاق: price > المتوسط ⇒ البسط أصغر والمخاطرة أكبر ⇒ rr أدنى
        #  دائمًا — فالاستبدال تشديدٌ رتيب لا يرفع rr أبدًا.)
        _band_top = max(tranches) * (1 + CONFIG.get(
            "ENTRY_READY_BAND_TOL_PCT", 0.0) / 100.0)
        if price > _band_top:
            _risk_now = max(price - stop_lo, 1e-9)
            rr = (t1 - price) / _risk_now
            rr2 = (t2 - price) / _risk_now
```

**قيود:** صفر رقم جديد (النطاق بعتبة `ENTRY_READY_BAND_TOL_PCT` المعتمَدة نفسها
— نفس صيغة `in_entry_band` حرفيًّا) · المقارنة `>` الصارمة (المساوي للسقف داخل
النطاق ⇒ بت-بت) · **داخل النطاق بت-بت** (لا سطر يتغيّر) · المتغيّر المحلّي
`entry_ref` في هذي الكتلة **لا يُمَسّ** (يبقى متوسط الدفعات — يُستهلَك في العرض
والتتبّع كما هو؛ حقل التتبّع المخزَّن قرار مالكٍ قائم 2026-06-24 لا يُقرَّب).
**النتيجة:** اسمٌ فوق نطاقه بهامشٍ رفيع حتى هدفه يسقط الآن على بوّابة `rr`
الصادقة بدل أن يمرّ بأرقام وهم — **وهذا يغيّر العضوية ⇒ `LOGIC_VERSION` يُرفَع
بوسمٍ يحوي `rrtruth`** (يُدمَج مع وسم `listsplit` لو نُفِّذا معًا).

## أ-1-ب · نفسُ الصدق في `enrich` (🔴 إضافة مؤرَّخة 2026-08-13 بعد تدقيق)

`Super_stock.py:5008-5013`: تنقيحُ 4س يعيد حساب `r["rr2"]` من **متوسط الدفعات**
⇒ بعد أ-1 كان السجلُّ سيحمل `rr` صادقًا و`rr2` مخطَّطًا = **خلطُ مقياسين في
الصفّ الواحد** (ويُصدَّر في CSV المشرف `:14931`). ⇒ **طبّق الفرعَ نفسَه هناك**:
لو `price > _band_top` (نفس الصيغة) يُحسب `rr2` المنقَّح من `price` بدل
المتوسط — داخل النطاق بت-بت. قفل `ET7`: سجلٌّ فوق نطاقه بعد `enrich` ⇒
`rr` و`rr2` كلاهما من السعر (مقياسٌ واحد)، وطفرةُ حذف الفرع تُسقطه.

## أ-2 · العرض: لا نقاطَ ميتة (عرض فقط)

في `build_message` و`build_daily_message`: حين يكون السجلّ **كاملَ البيانات**
(`tranches` غير فارغة و`price` صالح) **والسعرُ فوق `_band_top`** — استبدل سطرَ
الدفعات — **بنصّه الحرفيّ في الموضعين**: «**📥 الشراء (دفعات):**»
(‏`Super_stock.py:8709`) و«**📥 دخول:**» (‏`:13792`) — بـ:

```
⏳ فوق نطاق الدخول — انتظر رجوعه للنطاق $X–$Y (الطلبات تُعبَّأ داخله فقط)
```

حيث `X` = أدنى دفعة و`Y` = أعلى دفعة. **الدفعات تبقى مخزَّنةً كما هي** (يقرؤها
`in_entry_band`/NF8/فصل القائمتين) — المُستبدَل سطرُ العرض فقط. ناقص البيانات ⇒
السلوك القديم حرفيًّا (لا سطر ⏳ على نقص — «سطرُ عرضٍ بلا حقلٍ = كذبة»).
**نطاق wire-check (تحديدٌ مؤرَّخ بعد تدقيق):** المسار الحيّ الذي **يبلغ** السطرَ
هو **كروت التجديد** (`build_message` تُنادى لكل pick في `:14907` — والمرشّح فوق
نطاقه واقعٌ مقيس: ‏88% من التنبيهات) ⇒ عليه الإثباتُ الحيّ. أمّا المسار اليوميّ
(`build_daily_message` بـ`ready_only=True` و🟢 داخل النطاق فقط منذ 2026-08-06)
**فلا يصيّر كرتَ فوق-النطاق أصلًا** ⇒ يُثبَت فرعُه **بعيّنة وحدةٍ محقونة**
ويُصرَّح بخموله الحيّ — لا يُطالَب أوبيس بمستحيل.

## أ-3 · أقفال وطفرات الجزء أ (سكِل `lock-and-mutate`)

| # | القفل | العيّنة المفرِّقة |
|---|---|---|
| `ET1` | داخل النطاق ⇒ `rr` **بت-بت** مع القديم (وحالة التخوم: `price == _band_top` بالضبط ⇒ القديم) | سعران: تحت السقف وعنده |
| `ET2` | فوق النطاق + هامشٌ رفيع للهدف ⇒ `rr` الصادق تحت `MIN_RR_T1` ⇒ نقصٌ/رفضٌ باسمه — **والكود القديم كان يقبله** (عيّنة تفاضلية) | نسخة الملاحقة المقيسة (‏+13.6%) |
| `ET3` | فوق النطاق وهامشٌ غنيّ ⇒ يُقبَل ويطبع سطر ⏳ **ولا يطبع** سطر الدفعات | — |
| `ET4` | `rr` الصادق **‏≤** المخطَّط دائمًا في فرع فوق-النطاق (رتابة التشديد) | 20 حالة عشوائية |
| `ET5` | ناقص البيانات ⇒ سلوك قديم حرفيًّا (لا ⏳ ولا تغيير rr) | `tranches=[]` |
| `ET6` | `t1 ≤ price` (هدفٌ سبق بلوغه) ⇒ `rr ≤ 0` ⇒ يسقط على البوّابة | فات القطار حقًّا |

**الطفرات (على شجرة ملتزمة · `PYTHONDONTWRITEBYTECODE=1` + `rm -rf __pycache__`
· استعادة + `git status` بعد كل جولة):** ① حذف الفرع ⟶ `ET2` ② `>=` بدل `>`
⟶ `ET1` (حالة التخوم) ③ `entry_ref` بدل `price` في البسط ⟶ `ET4` ④ حذف
استبدال العرض ⟶ `ET3` ⑤ `stop_hi` بدل `stop_lo` ⟶ عيّنة `ET2` (تفرّق
بالوقفين). **كل طفرة تُسقط قفلها المسمّى وإلا يُصلَح القفل.**

## أ-4 · 🔴 ملحقٌ مؤرَّخ 2026-08-13 — الأقفال القائمة التي **ستسقط حتمًا** مع أ-1 (تخويلُ تحديثٍ مسمًّى، أخرجه تدقيقٌ خصوميّ قبل التنفيذ)

بوّابة القبول «خروج 0» **مستحيلةٌ بلا هذا الملحق** — خمسةُ أقفالٍ قائمة تعارض
أ-1 بالبناء (مقيسة: بذرةُ السويّة سعرها 3.6 وسقفُ دفعاتها 3.5 ⇒ فوق النطاق ⇒
rr ينزل من ‏≈1.78 إلى ‏≈0.7 تحت `MIN_RR_T1`):

| الموضع | ما فيه | التحديث المخوَّل |
|---|---|---|
| `test_bot.py:388-393` | دبّوس سلوكيّ `rr == 1.7809` على البذرة | **يُعاد تثبيتُه بقيمٍ مقيسة على الشجرة بعد التعديل** (لا تخمين) — والقيمتان القديمة/الجديدة تُذكران في التعليق |
| `test_bot.py:7372-7387` | قفل «RR من متوسط الدفعات **لا** السعر الحالي» (نقيض أ-1 بالاسم) | تُقلَب دلالتُه: «**داخل النطاق** من المتوسط · **فوقه** من السعر» بعيّنتين للفرعين |
| `test_bot.py:10204-10212` | الثابت (5) «صيغة RR من المتوسط» على 60 عيّنة فوق نطاقها | نفس القلب الدلاليّ |
| `test_bot.py:197-208` | «الفحص اليدوي = الأساسي (RR بالضبط)» | 🔴 **يستلزم مرآةَ أ-1 حرفيًّا في `analyze_one.py:595-598`** — **تغييرُ كودٍ إلزاميّ يُضاف لنطاق الحزمة** (وإلّا انكسر التطابق `_diag["rr"] ≠ r0["rr"]`) |
| `test_bot.py:1417-1418` | فكستشر «📥 دخول:» اليومي بسعرٍ فوق نطاقه | **يُنقَل سعرُ الفكستشر داخل النطاق** (غرضُ القفل أن السطر يُصيَّر — يُصان بغرضه) |

**وقاعدةُ الشلّال للبذرات المشتركة:** أيُّ فكستشر مشترك غرضُ اختباره **غير** rr
وسعرُه فوق نطاقه ⇒ **يُنقَل سعرُه داخل النطاق** (يصون غرضَ الاختبار الأصليّ) —
لأن نقص «العائد» الجديد قد يرفع نواقصَه فيقلب عضويته ويُسقط فحوصًا لا علاقة لها
بـrr. **وأيُّ قفلٍ يسقط خارج هذي القائمة ⇒ توقّف وراجع** (لا تحديثَ صامتًا).
🔒 وتحديثُ الخمسة **بإقرارٍ مؤرَّخٍ في نصّ كلّ قفل** يذكر أ-1 وتاريخَ أمر المالك.

---

# الجزء ب — أداة تجربة `T-ANCHOR` (بناءٌ بلا تشغيل — العقد `anchor_prereg.md`)

**البناء فقط. لا تشغيل ولا رقم — التشغيل بأمر المالك على الـworkflow.**

1. **علم `BT_ANCHOR`** في `CONFIG` (افتراضه `""`) + صفّه في جدول
   `_apply_backtest_overrides` + مدخله في `backtest.yml` **موصولًا بـ`env`**
   (قاعدة P1 المقفولة: مدخلٌ بلا env = مدخلٌ ميّت).
2. **داخل `analyze_ticker`** قبل بناء الدفعات (‏`:3353`):
   ```python
        _anchor = pivot
        if CONFIG.get("BT_ANCHOR"):
            _tl = tested_level(df)          # نفس الشريحة المقصوصة — صفر نظر مستقبليّ
            if _tl:
                _anchor = float(_tl["level"])
            elif CONFIG["BT_ANCHOR"] == "tested_strict":
                return _reject("M_لا_مستوى_مختبر")
   ```
   ثم تُبنى الدفعات من `_anchor` بدل `pivot`، **ويُعاد اشتقاق الوقف من `_anchor`
   بنفس صيغة 5-7% وحارس «الوقف تحت أدنى دفعة» كما هما** — والافتراض `""` ⇒
   `_anchor = pivot` ⇒ **بت-بت** (قفل `V6`).
   ⚠️ `tested_level` **قائمة** (‏`:7514`) — تُستعمَل كما هي بلا أيّ معايرة.
3. **`anchor_arms.py`** — استنساخ نمط `stability_arms.py` حرفيًّا: الأذرع
   `A0/B1/B2` · `child_env()` يرفع `BT_REPLAY10`+`BT_ENVVALS`+`BT_POTENTIAL` ·
   القياس `d50`/`d100`/`R` عبر `replay10` بسعة 15 · مِرساة التكامل `V3` =
   ‏25/26/15 (ثلاثيّة الحالة) · عدّادا التغطية و`M_لا_مستوى_مختبر` يُطبَعان ·
   الحرّاس (أ)-(د) بقراءات `anchor_prereg.md §④` حرفيًّا · رموز الخروج §⑧.
4. **`anchor.yml`** — مدخلا `years`/`frozen_run_id` · `R3-7`: كل مدخل موصول env.
5. **أقفال:** `AN1` العلم مطفأ ⇒ الفارز بت-بت (سلوكيّ) · `AN2` بلا نظرٍ
   مستقبليّ (تبديل شموع المستقبل لا يغيّر `_anchor`) · `AN3` `tested_strict`
   يرفض بالاسم حين لا مستوى · `AN4` `tested` يرتدّ لـ`pivot` حين لا مستوى ·
   `AN5` الوقف أُعيد اشتقاقه من `_anchor` (عيّنة يفترق فيها المستويان) —
   وطفرةٌ لكلٍّ تُسقطه.

---

# بوّابات القبول (للحزمة كلّها — بالترتيب)

1. بصمات AST قبل/بعد: **الجذر الوحيد المتغيّر `analyze_ticker`** (بأمر المالك —
   كتلتا `rr` والمِرساة حصرًا) والبقية `IDENTICAL` (القائمة في
   `list_separation_package.md §⑥`).
2. `python3 test_bot.py` ⇒ خروج 0 وصفر فشل · الطفرات كلّها أسقطت أقفالها.
3. `LOGIC_VERSION` مرفوع (`rrtruth`) — **مرّة واحدة مع `listsplit` إن دُمجا معًا**.
4. تشخيصٌ يُطبَع في رسالة الـcommit: عدد أسماء القائمة الحية التي كان `rr`
   الصادق سيغيّر حكمها اليوم (عدّ فقط — لا حكم).
5. تحديث `CLAUDE.md`/`HANDOFF.md` بنفس الـcommit · رسالة commit بالعربية
   بذيول المشروع الحرفية · **لا PR** · دفع بـfetch+rebase عند الرفض ·
   **لا معرّف نموذج في أي ملف مدفوع**.
6. بعد الدفع: إبلاغ المالك «شغّل Daily Pivot Screener بـ`force_renew=1` مرّة»
   (تكفي مرّة واحدة بعد آخر حزمة).
