# 🩸📏 `T-SLIP` — قياسُ الانزلاق على الوقف (تسجيلٌ مسبق)

> **الأمر:** المالك 2026-08-27 **«قس الانزلاق»** — بعد أن أظهر
> `gold_entry_result.md §④` أن فارقَ `T-GOLD-ENTRY` كلَّه (‏+0.689R) **مرهونٌ
> بافتراضٍ واحد**: أن الوقفَ يُنفَّذ **عند مستواه بالضبط**.
> 🔒 **الدفع:** هذا الملفُّ على `origin/main` **قبل سطرِ كودٍ واحد** من الأداة،
> وقبل أيّ تشغيلة، وقبل أيّ رقم.
> **قراءةٌ/قياسٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION`.**

---

## ① السؤال المسجَّل

عندما يُضرب الوقف، **بكم كنّا سنخرج فعلًا** لا افتراضًا؟ وهل يبقى فارقُ
**«وقفه قاعه»** (‏`P1` مقابل `P0`) قائمًا بعد إدخال الانزلاق الحقيقيّ؟

🔴 **ولماذا هذا السؤالُ بعينه:** مخاطرةُ `P0` **‏0.10** من القاع ومخاطرةُ
`P1` **‏0.03** ⇒ **الانزلاقُ نفسُه بالدولار يكلّف `P1` ‏3.3 أضعافَ ما يكلّف
`P0` بوحدة المخاطرة**. فالافتراضُ **ليس محايدًا بين الذراعين**، وهذا تصحيحٌ
ذاتيٌّ منشورٌ سلفًا في `gold_entry_result.md §④`.

## ② المصدر والمقياس

**شموعُ الدقيقة التاريخية من Polygon** عبر `event_exec.hist_minute_bars`
**بالاسم** (‏`adjusted=true` **عمدًا** ليطابق مقياسَ اللقطة المجمَّدة
`auto_adjust=True`) — **صفرُ جالبٍ جديد وصفرُ منطقٍ مكرَّر**. والحلقاتُ
والخططُ والحسمُ من `gold_entry_arms` و`rebound_arms` **بالاسم** كذلك.

🔴 **ونافذةُ البحث الجلسةُ النظاميّة وحدَها (‏09:30-16:00 نيويورك)** — لأن
الشمعةَ اليوميّة في اللقطة **نظاميّةٌ حصرًا** (‏`prepost=False`)، فبحثٌ في
البريماركت كان **سيُطلق وقفًا لم ترَه الشمعةُ التي قرّرت الخسارة أصلًا** =
مقياسان لا واحد.

## ③ أذرعُ التنفيذ — أربعةٌ ولا خامسة (تُثبَّت الآن)

| الذراع | قاعدةُ التعبئة عند ضرب الوقف |
|---|---|
| **`Q0`** | **عند مستوى الوقف بالضبط** — الافتراضُ النافذ اليوم · **بوّابةُ التكامل** |
| **`Q1`** | **واعٍ بالفجوة فقط:** افتتاحُ الجلسة النظاميّة دون الوقف ⇒ التعبئةُ **عند الافتتاح** · وإلّا عند الوقف |
| **`Q2`** | **الحاكمة:** واعٍ بالفجوة **‏+** التعبئةُ عند **إغلاق دقيقة الزناد** (أوّلُ دقيقةٍ قاعُها يبلغ الوقف) |
| **`Q3`** | أسوأُ حال: واعٍ بالفجوة **‏+** التعبئةُ عند **قاع دقيقة الزناد** |

**والقراءةُ العامليّة مثبَّتةٌ سلفًا:** `Q1−Q0` = **أثرُ الفجوة وحدَه** ·
`Q2−Q1` = **أثرُ ما داخل الدقيقة وحدَه** · `Q3−Q2` = **ذيلُ دقيقة الزناد**.

⚖️ **ولماذا `Q2` هي الحاكمة:** أمرُ الوقف يُرسَل عند اللمس ويُنفَّذ خلال
ثوانٍ، **وإغلاقُ دقيقة الزناد سعرٌ تداول فعلًا** قريبٌ من تلك اللحظة — لا هو
القاعُ المتطرّف ولا هو مستوى الوقف الذي قد لا يكون متاحًا. و`Q3` تُنشَر
**حدًّا أعلى للضرر** لا حكمًا.

## ④ الأذرعُ المقيسة والمقياسُ الحاكم

يُعاد حسابُ `E` (‏متوسّطُ `R` لكلّ صفقةٍ محسومة) لـ**`P0` · `P1` · `P2`**
تحت كلّ ذراعِ تنفيذ، حيث:

- الرابحة تبقى **`+R_win`** كما هي (الهدفُ أمرُ حدٍّ يُنفَّذ عند سعره أو أفضل).
- **والخاسرة تصير `−(الدخول − سعرُ التعبئة الفعليّ) ÷ (الدخول − الوقف)`** —
  أي **`−1` هي الحالةُ المثلى**، وكلُّ انزلاقٍ يجعلها أعمق.
- المُعبَّأةُ فقط في المقام (‏`no_fill`/`open` خارجَه) — **كما في `T-GOLD-ENTRY`
  حرفيًّا**.

**والمقياسُ الحاكم للسؤال: `E(P1) − E(P0)` تحت `Q2`.**

## ⑤ معاييرُ الحكم — تلزم كلُّها معًا (مُعادةٌ بلا تحريك)

1. الفارقُ **‏+0.15R فأكثر مجمَّعًا** تحت `Q2`.
2. **موجبٌ في السنوات الثلاث** (لا انقلاب).
3. **فاصلُ 95% للفرق المقترن لا يلمس الصفر.**
4. **الأرضية:** ‏40 محسومةً لكلّ ذراعٍ لكلّ سنة و150 مجمَّعًا.

🔴 **والحكمُ مكتوبٌ الآن بطرفيه:**
- **عبرت الأربعة ⇒** الحافّةُ **تصمد أمام التنفيذ الواقعيّ** ⇒ يُرفع
  **اقتراحٌ** للمالك (وسقفُ النجاح كما هو: اقتراحٌ وسطرُ عرض — **القرارُ له**).
- **سقط أيٌّ منها ⇒** الفارقُ المنشور **‏+0.689R أثرُ افتراضِ تعبئةٍ لا حافّة**
  ⇒ **يُغلَق الملفّ بلا اعتماد**، ويُصدَّر `gold_entry_result.md` ببانرِ تقييد.

## ⑥ بوّاباتُ الصلاحية

- **`W0`** — `Q0` يُعيد أرقامَ `gold_entry_result §①` **بت-بت** على الحلقات
  نفسِها (‏`E(P0)`=+0.174 · `E(P1)`=+0.863 · `E(P2)`=+0.584 مجمَّعًا، وسنةً
  بسنة كما نُشرت). **أيُّ تفرّقٍ ⇒ عطبُ أداةٍ لا نتيجة ⇒ خروج 3 ولا يُقرأ رقم.**
- **`W1`** حارسُ الـ`no-op`: **عددُ الخسائر التي تغيّر سعرُ تعبئتها** يُطبَع
  لكلّ ذراع. **صفرُ تغيّرٍ في `Q2` ⇒ خروج 4** (بصمةُ `BT_CANDLE`).
- **`W2`** **حارسُ المقياس:** إغلاقُ الجلسة المشتقُّ من الدقائق يُقارَن
  بإغلاق اللقطة اليوميّ؛ فرقٌ يتجاوز **`SLIP_SCALE_TOL`=2%** ⇒ **الحدثُ
  يُستبعَد ويُعَدّ باسمه `scale_mismatch`** (سابقةُ `T-OPFIRE`) — ولا يُصحَّح
  ولا يُخمَّن.
- **`W3`** **التغطية:** نسبةُ أحداث الوقف التي وصلت لها شموعُ دقيقةٍ صالحة
  **‏≥90%**. دونها ⇒ النتيجةُ تُوسَم **«جزئيّة»** ويُطبَع المستبعَدون
  **بأسبابهم المسمّاة** (‏`no_minutes` · `scale_mismatch` · `no_trigger` ·
  `budget_cut`). **ولا قصَّ صامتٌ إطلاقًا.**
- **`W4`** **الحلقاتُ نفسُها:** مجموعُ الحلقات والمحسومة لكلّ ذراعٍ يطابق
  `T-GOLD-ENTRY` (‏20,182 حلقة · P0 محسومة 5371 …) وإلّا خروج 3.
- **`W5`** أقفال السويّة `SL1`-`SL*` + جولةُ طفراتٍ كاملة: كلُّ قفلٍ يسقط
  مرّةً عمدًا، **وطفرةٌ تنهار لا تسقط = قفلٌ هشٌّ يُصلَّب**.
  🔒 **وpreflight الطفرات يرفض البدءَ عند مُشغِّلٍ آخرَ حيّ** (‏`ps -eo
  pid,args` مع استثناء الذات والأب — درسُ اليوم المدوَّن)، **وكلُّ «نجت» تُعاد
  مُفرَدةً قبل أن تُعلَن.**

## ⑦ تنبّؤاتي المسجَّلة (قابلةٌ للكسر — تُنشَر ولو كُذِّبت)

- **`SP1`:** وسيطُ الانزلاق **‏≤1.0%** من مستوى الوقف (أغلبُ الستوبات عاديّةٌ
  لا كنس).
- **`SP2`:** المتوسّطُ **ضعفُ الوسيط فأكثر** (ذيلٌ تقوده الفجوات).
- **`SP3`:** **أثرُ الفجوة (`Q1−Q0`) أكبرُ من أثرِ ما داخل الدقيقة (`Q2−Q1`).**
- **`SP4`:** **الفارقُ تحت `Q2` يبقى فوق ‏+0.15R** — أراهن على نجاة الحافّة،
  لأن حسابَ الحساسيّة المنشور يضع نقطةَ التعادل عند **‏3.16%** انزلاقٍ وسطيّ
  وأتوقّع المقيسَ دونها.
- **`SP5`:** انزلاقُ `P0` **بالدولار أكبرُ** من انزلاق `P1` (وقفٌ أعمق = سوقٌ
  أسرع وأرقّ)، **وبوحدة المخاطرة ينقلب** فيصير `P1` الأكبرَ ضررًا.
- **`SP6`:** الفجوةُ الكاملة (افتتاحٌ دون الوقف) **أقلُّ من 15%** من أحداث
  الوقف.

## ⑧ سقفُ النجاح — مثبَّتٌ سلفًا

مهما كانت الأرقام: **اقتراحٌ للمالك + سطرُ عرضٍ تشخيصيّ.** لا أتمتةَ دخولٍ ولا
خروج · لا تغييرَ خطةِ إنتاج · لا مسَّ بوّابةٍ ولا عتبة · **ولا مسَّ
`kasih_scan.resolve` ولا صيّادِ المقسّم ولا أرقامِ المالك (`LIQ_*`)** · ولا
مسَّ أيٍّ من الجذور (تُفحَص ببصمة AST قبل كلّ دفعة).

## ⑨ حدودُ صدقٍ تُكتب **قبل** الأرقام

1. **شموعُ الدقيقة صفقاتٌ مجمَّعة** — وأمرُ الوقف يُنفَّذ على `NBBO`. فإغلاقُ
   دقيقة الزناد **سعرٌ تداول فعلًا** وهو تقريبٌ منصف، **وليس التعبئةَ الحقيقية.**
2. **بلا نمذجةِ طابورٍ ولا تعبئةٍ جزئية** — يُفترَض خروجُ المركز كلِّه بسعرٍ
   واحد. مركزٌ كبيرٌ في سهمٍ رقيقٍ يخرج أسوأ ⇒ **الأرقامُ أرضيّةُ ضررٍ لا سقفه.**
3. **انزلاقُ الدخول غيرُ مقيسٍ عمدًا** — الدفعاتُ أوامرُ حدٍّ **تحت** السوق
   فتُنفَّذ عند سعرها أو أفضل؛ والعيبُ الحقيقيّ هناك «اللمسُ ليس تعبئة» وهو
   يُنقص **عددَ** الصفقات لا يُعمّق الخسارة ⇒ **خارج النطاق ومُعلَن.**
4. 🔴 **وعدمُ تناظرٍ يُعلَن ولا يُقاس هنا:** «اللمسُ ليس تعبئة» يسري على
   **الهدف** أيضًا، وسعرُ الهدف واحدٌ بالدولار للذراعين **لكنّ ثمنَه بوحدة
   المخاطرة ‏17.17R لـ`P1` مقابل 5.15R لـ`P0`** ⇒ **تفويتُ هدفٍ يضرّ `P1`
   أكثر** — أي أن هذا القياسَ **لا يلتقط كلَّ ما يحابي `P1` في الافتراض.**
5. **إيقافاتُ التداول (`LULD`)** تظهر فجوةً عند الاستئناف — تلتقطها الأذرعُ
   الواعيةُ بالفجوة، ولا تُميَّز عن فجوة الافتتاح.
6. **انحيازُ البقاء في اللقطات** مُعلَنٌ ولا يُصحَّح.
7. **المقارنةُ داخليّةٌ على الحلقات نفسِها** ⇒ أثرُ السوق يُطرَح؛ وما يبقى هو
   ما نقيسه.
