# تطويرُ قناة ما قبل الجلسة — تسجيلٌ مسبق (مدفوعٌ قبل أيّ رقم)

**أمرُ المالك 2026-09-03:** «طيب راقب لكن هذي **أداة مهمة جدًّا** فأبي كل يوم بعد
الافتر يوصلني تقرير … الهدف منه **تطويرها من جميع النواحي** فنحتاج عصف ذهني
واختبارات وتجارب عشان نطلع بأفضل نتيجة … **يهمّني جدًّا الجودة مب الكمية**»
ثم: «جميع ما سبق إذا مافيه تعارض ومعك الصلاحية كاملة **سو عصف ذهني واختبارات
وتجارب** عشان تتأكد إنّا وصلنا لأفضل شي نقدر عليه».

> 🔒 **ولا تعارضَ مع قاعدةٍ قائمة:** «لا تُضاف قناةُ تلغرام رابعة **بلا طلب
> المالك**» (‏2026-07-27) — **وقد طلبها صراحةً**، وهي في workflow مستقلٍّ لا
> يمسّ المسارَ اليوميّ أصلًا.

---

## ⓪ ما لا يُمَسّ مهما كانت الأرقام

- 🔒 **الحكمُ الرسميّ «فشلت»** (‏`presession_prereg §⑥`) لا يتغيّر بأيّ رقمٍ هنا.
- 🔒 **الأرضيةُ المشحونة `F1` = 0.69492 والمفتاحُ `post_hi_ret`** لا يُبدَّلان —
  تبديلُهما بعد رؤية أرقام هذا الملفّ **`p-hacking` بحرفه**.
- 🔒 **أرقامُ المالك** (‏`LIQ_*` · `STOP_BELOW_LOW_PCT` · `WATCHLIST_SIZE` …)
  والفرزُ والجذورُ وصيّادُ المقسّم — **صفرُ مسّ**.
- 🔒 **ولا `LOGIC_VERSION`** في شيءٍ من هذا الملفّ (تسليمٌ/حصادٌ/قياس).

---

## ① الفجواتُ المعروفةُ التي وُلد منها هذا العقد (تُدوَّن قبل القياس)

1. 🔴 **بِركةُ الحيّ ليست بِركةَ المقيس.** القياسُ مشى على **كون السعر كلِّه**؛
   والرادارُ الحيُّ يجلب **أعلى 60 بسيولة اليوم بأرضية $100,000** — طبقتان
   **لم تُقاسا قطّ** (`T-PRECAP` أدناه).
2. 🔴 **الجلسةُ النظاميّةُ عمياء.** الوسمُ الحاكم `hit80_s` نافذتُه
   ‏04:00 ⟶ 09:30 حصرًا ⇒ سهمٌ يهدأ بريماركتَ ثم يطير بعد الجرس **يُعَدّ فوتًا**
   (`T-PREDAY`).
3. 🔴 **لا عائدَ مقيسًا إطلاقًا** — «انفجر» لمسُ قمّةٍ بأرضية تنفيذٍ $20,000
   (`T-PREDAY` الذراعُ الثانية).
4. 🔴 **السجلُّ الأماميُّ يرى العشرةَ وحدَهم** — فيستحيل أن نسأل «هل انفجر مَن
   لم نرتّبه؟». **يُصلَح فورًا وبلا تجربة** (‏§⑥: حصادٌ لا حكم).
5. ⚠️ **سنةُ تقييمٍ واحدة** · **وحجمُ التسليم ينجرف** مع نشاط السوق.

---

## ② `T-PRECAP` — «هل يفقد سقفُ الستّين إصابات؟»

**بلا مسحٍ جديد** — على صفوف التشغيلات المرفوعة نفسِها ⇒ **الميزانيةُ ثابتةٌ
بالبناء** والمتغيّرُ **قصُّ المرشِّح وحدَه**.

**الذراعان (مثبَّتتان):**
- `Q0` = **ما قِيس**: الأرضيةُ `F1` على **كلّ** صفوف القرار.
- `Q1` = **ما يُشحَن**: الأرضيةُ `F1` على الصفوف التي **رتبتُها بـ`usd_day`
  تنازليًّا ‏60 أو أقلّ ‏و‏ `usd_day` لا يقلّ عن 100,000** — أي بِركةُ الرادار.

**المقياسُ الحاكم:** `hits(Q1) ÷ hits(Q0)` على **2025 خارج العيّنة**.

**قاعدةُ القرار — مثبَّتةٌ قبل الرقم:**
- **‏0.90 فأكثر** ⇒ السقفُ **بريء** ⇒ **يُغلق المحور** ولا يُرفَع.
- **دون 0.90** ⇒ **يُقترَح رفعُ السقف** ومعه **منحنى كلفة**: أسماءٌ/قرار
  وإصاباتٌ عند **‏60 · 120 · 240 · بلا سقف**، وزمنُ الجلب المُسقَط من الرقم
  المقيس في السجلّ الحيّ (‏يُطبَع لا يُخمَّن).

⚠️ **حدُّ صدقٍ مكتوبٌ قبل الرقم:** `usd_day` في الصفّ محسوبٌ على **الجلسة
النظاميّة حتى القطع**، والمرشِّحُ الحيُّ يرتّب بـ`c × v` من **الشمعة اليومية
المجمَّعة** (تشمل الجلسةَ الممتدّة) ⇒ **الترتيبان متقاربان لا متطابقان**
والرقمُ **تقديرٌ لا إعادةُ إنتاج**.

**دعوى `C1` (قابلةٌ للتكذيب):** أتوقّع النسبة **‏0.85 فأكثر** — لأن سهمًا طار
افترُه ‏+70% أمسِ يكون دولارُ يومه مرتفعًا فيقع داخل الستّين غالبًا.

---

## ③ `T-PREFLOOR2` — «هل تُحسّن أرضيةُ سيولةِ الافتر الدقّةَ فوق `F1`؟»

**الدافع:** `post_hi_ret` = قمّةُ افترِ الأمس ÷ إغلاقه ⇒ سهمٌ «طار ‏+70%» على
**ألفَي دولار** حركةٌ وهميّة. و`post_usd` (دولارُ الافتر) **في الصفّ سلفًا** ⇒
بلا مسحٍ جديد.

**الأذرعُ الأربع — مثبَّتةٌ كاملةً ولا تُضاف خامسة:**

| ذراع | التعريف |
|---|---|
| `G0` | `F1` وحدَها (**الأساسُ المشحون**) |
| `G1` | `F1` ‏و‏ `post_usd` لا يقلّ عن **50,000** |
| `G2` | `F1` ‏و‏ `post_usd` لا يقلّ عن **100,000** |
| `G3` | `F1` ‏و‏ `post_usd` لا يقلّ عن **250,000** |

🏷️ **والأرقامُ الثلاثة `engineering` مُعلَنة** — ليست من فيصل ولا من الكاتالوج،
وهي **مضاعفاتُ أرضية الوسم** `LABEL_MIN_USD`=20,000 (‏×2.5 · ×5 · ×12.5).

**المقياسُ الحاكم:** الدقّة `P` على **2025 خارج العيّنة**.
**والمعيارُ الرباعيّ — كلُّه يلزم:**
1. الدقّةُ ترتفع **‏3 نقاطٍ فأكثر** عن `G0`.
2. الإصاباتُ لا تنقص أكثرَ من **‏25%**.
3. **الاتّجاهُ نفسُه في 2023 و2024** (لا انقلاب) — الحدُّ المعتاد في المستودع.
4. **فاصلا Wilson 95%** للذراع و`G0` **لا يتداخلان**.

⇒ **استوفت ذراعٌ** ⇒ اقتراحٌ للمالك (وعند التعادل **الأضيقُ قصًّا**).
⇒ **سقطت كلُّها** ⇒ **يُغلَق المحور** ولا يُعاد بتحريك رقمٍ رابع.

**دعوى `C2`:** أتوقّع `G1` تعبر ①②③ **وتسقط على ④** (تداخلُ الفواصل عند مقامٍ
‏≈400)، فيكون الحكمُ «لا تُعتمَد» ويبقى `F1` وحدَه.

---

## ④ `T-PREDAY` — «هل تتنبّأ القائمةُ بيوم التداول؟» ‏+ **أوّلُ عائدٍ مقيس**

**يلزمه مسحٌ جديد** — والوسمان والعائدُ في **تشغيلةٍ واحدة** فالميزانيةُ ثابتة
والذراعان **قابلتان للمقارنة مباشرةً**.

### ④-أ الوسومُ المضافة (**إضافةٌ لا تبديل** — القائمةُ بت-بت)

- `hit80_reg` — أعلى قمّةٍ في **‏09:30 ⟶ 16:00** لا تقلّ عن `ref × 1.80`
  بأرضية `LABEL_MIN_USD` على تلك النافذة.
- `hit80_full` — من **‏04:00 إلى 16:00** (بريماركتٌ + نظاميّة).

**المقياسُ الحاكم `P1`:** رافعةُ `hit80_reg` للقائمة المشحونة (`F1`) فوق أساسِ
السوق **لا تقلّ عن ‏10×** — **نفسُ حدّ العقد الأمّ §⑥ حرفيًّا، بلا تحريك**.
⇒ عبَرت ⇒ **يُقترَح أن تقول الرسالةُ «والتوقّعُ يمتدّ ليوم التداول»**.
⇒ سقطت ⇒ **الرسالةُ تبقى كما هي** («البريماركت وحدَه») — وهو **المشحونُ اليوم**.

### ④-ب العائدُ المحقَّق — ذراعان ولا ثالثة

- **الدخول:** إغلاقُ **أوّل دقيقةٍ من البريماركت** (‏04:00) — أوّلُ سعرٍ قابلٍ
  للتنفيذ **بعد** وصول الرسالة (‏03:50).
- **الوقف:** **قاعُ شمعة الدخول** — وقفُ القاع المعتمَد عند المالك (‏2026-08-29)
  ⇒ **صفرُ رقمٍ مخترَع**.
- **الهدفان:** `D10` = **‏+10%** (رقمُ المالك المقيس في `T-TARGET10`) ·
  `D30` = **‏+30%** (‏`faisal_verbatim`: «صعود أقلها 30-50%»).
- **الحسم:** الوقفُ يُفحَص **أوّلًا** داخل كلّ شمعة (قراءةٌ متحفّظة)، والنافذة
  **البريماركت** ثم — وصفيًّا — **إلى إغلاق الجلسة النظاميّة**.
- **الوحدة:** `R` = (الخروج − الدخول) ÷ (الدخول − الوقف).

**المعيار `P2`:** متوسّطُ `R` **موجبٌ في السنوات الثلاث** ⇒ يُقترَح.
وإلّا ⇒ **لا دعوى ربحية**، ويُنشَر الرقمُ سالبًا كما هو.

⚠️ **حدودُ صدقٍ قبل الرقم:** بلا انزلاقٍ ولا عمقِ دفتر ⇒ **سقفٌ متفائل** ·
والدخولُ عند 04:00 قد لا يُنفَّذ بحجمٍ في سهمٍ رقيق · **وقاعُ شمعة الدخول قد
يساوي الدخولَ** فيُستبعَد الصفُّ ويُعَدّ بسببٍ مُسمًّى (لا يُطوى).

**دعوى `C3`:** أتوقّع **`P1` يعبر** (مَن يطير بريماركتَ كثيرًا ما يواصل)
**و`P2` يسقط** — هدفٌ ثابتٌ مع دخولٍ متأخّر، وهو النمطُ الذي سقط في
`T-LIBERATION` و`T-LIBVOL` و`T-TRANCHE` ثلاثَ مرّات.

---

## ⑤ بوّاباتُ صلاحيةِ الأداة (تسبق أيّ دعوى — وكلٌّ برمز خروجٍ حقيقيّ)

- **`V0`** الأذرعُ الجديدة **تُعيد أرقامَ `topk_result.md` بت-بت** حيث يجب:
  `Q0` ≡ `F1` (‏435 اسمًا · 47 إصابة على 2025) و`G0` ≡ `F1` كذلك.
  **أيُّ تفرّقٍ ⇒ عطبُ أداةٍ لا نتيجة ⇒ خروجٌ غيرُ صفريّ.**
- **`V1`** الأذرعُ **تتفرّق** فعلًا (‏`Q1 ≠ Q0` أو يُعلَن «قصٌّ صفر» صراحةً ·
  و`G1`/`G2`/`G3` مختلفةُ الأعداد) — وإلّا فهي `no-op` تُعلَن ولا تُفسَّر.
- **`V2`** الوسومُ القائمة (`hit80_10/30/60/s`) **بت-بت** بعد إضافة الجديدة.
- **`V3`** تغطيةُ الصفوف تُطبَع، والمفقودُ يُسمّى.

## ⑥ خارجَ العقد — حصادٌ لا حكم (يُنفَّذ فورًا بلا تجربة)

1. **توسيعُ السجلّ الأماميّ:** يُسجَّل **كلُّ صفٍّ مرتَّب** لا العشرةُ وحدَهم،
   بحقلٍ `in_top` ⇒ **الشاهدُ المضادُّ يصير موجودًا** (‏«هل انفجر مَن لم نرتّبه؟»).
2. **تقريرُ الافتر اليوميّ** (`presession_digest.py`): يحسم قائمةَ اليوم بشموع
   بريماركتِه، ويعرض **المُسلَّم · المقصوصَ بالأرضية · وغيرَ المرتَّب**.
   🔒 **و«لا حكم» إلزاميّ** حتى تبلغ العيّنة **‏40 مُسلَّمًا محسومًا** و**‏150
   صفًّا مجمَّعًا** (الأرضياتُ المعتادةُ في المستودع، مُعادةٌ لا مخترَعة) —
   وقبلها **يُطبَع العدّادُ وحدَه**.
3. **ولا قرارَ تسليمٍ يتغيّر بأيٍّ من الاثنين.**

## ⑦ الأقفال

`PD1` السجلُّ يرى **كلَّ** المرتَّبين و`in_top` يفرّق · `PD2` التقريرُ يحسم
بنافذة البريماركت وحدَها ولا ينظر مستقبلًا · `PD3` «لا حكم» تحت الأرضية ·
`PD4` `V0` يُعيد `F1` بت-بت · `PD5` الأذرعُ تتفرّق · `PD6` الوسومُ القائمة
بت-بت بعد الإضافة · `PD7` كلُّ مدخلٍ في الـworkflow موصولٌ ببيئةٍ يقرؤها
السكربت. **وكلٌّ منها تُسقطه طفرةٌ مسمّاة.**

---

## ⑧ ملحقٌ مؤرَّخ (2026-09-04) — **نموذجا البِركة: حدّان لا تقديرٌ واحد**

**يُدفَع قبل أيّ رقمٍ من الذراع الجديدة.** التشغيلةُ الأولى (`33822163559`)
صدرت بنموذجِ بِركةٍ **واحد** (`usd_day` وحدَه)، وقراءتُها أخرجت شكًّا حقيقيًّا
يمسّ الاستنتاج نفسَه:

**الشكّ.** المرشِّحُ الحيّ (`presession_radar.prefilter`) يرتّب بـ`c × v` من
**الشمعة اليوميّة المجمَّعة** لدى Polygon، وأداةُ القياس ترتّب بـ`usd_day` =
مجموعُ (إغلاقِ الدقيقة × حجمها) على **الجلسة النظاميّة وحدَها**. فإن كان حجمُ
الشمعة المجمَّعة **يشمل** الجلسةَ الممتدّة، فالسهمُ الذي قفز افترًا — وهو
بالضبط ما يختاره `post_hi_ret` — يُرفَع في ترتيب المرشِّح **في اللحظة التي
نريده فيها**، فيكون القصُّ الحقيقيُّ **أرحمَ** ممّا قاسته الذراعُ الأولى.

**ولا يُحسم بالتخمين.** المنفذُ محجوبٌ عن هذي البيئة، ونصُّ المزوّد لا يُقرأ
من هنا ⇒ **يُقاس الحدّان بدل أن يُختار أحدُهما**:

- **`P-reg`** — البِركةُ بـ`usd_day` (الجلسة النظاميّة). **الحدُّ المتشائم**:
  يفترض أن المجمَّعة لا تشمل الممتدّة.
- **`P-all`** — البِركةُ بـ`usd_day + pre_usd + post_usd` (دولارُ اليوم
  كاملًا). **الحدُّ المتفائل**: يفترض أنها تشمله.

**والحقيقةُ بينهما**، ويُنشَر المدى لا رقمٌ واحد. **ولا يُختار طرفٌ بعد رؤية
الأرقام** — القراءةُ الحاكمة هي **المتشائم `P-reg`** لأنها القراءةُ التي لا
تدّعي ما لم نتحقّق منه، والمتفائلُ يُنشَر بجانبه سقفًا.

**ولا يتغيّر شيءٌ آخر:** `PRECAP_KS` كما هي (‏60/120/240/∞) · وعتبةُ `PRECAP_OK`
‏= 0.90 **لا تُحرَّك** · و`V0` تبقى شرطًا سابقًا لأيّ رقم · وسقفُ النجاح كما
هو في §⓪ (**عرضٌ للمالك بلا تنفيذ**).

🔒 **وقفلٌ جديد `PD9`:** النموذجان **معرَّفان بالبناء** (مصدرٌ واحدٌ لأسماء
مفاتيحهما مشتقٌّ من `PF`) · **ويتفرّقان سلوكيًّا** على عيّنةٍ يقلب فيها دولارُ
الافتر ترتيبَ البِركة · **وكلاهما يُطبَع** فلا يُنشَر حدٌّ بلا صاحبه.

🔴 **وسببُ الملحق يُدوَّن لا يُطوى:** حدُّ الصدق المطبوع في التشغيلة الأولى
(«البِركةُ الحيّة **تقريبٌ** لا نسخة») **كان صحيحًا وناقصًا** — أعلن الفارقَ
ولم يُقدّر اتّجاهه ولا حجمه. وقاعدتُنا المدوَّنة أن **كلَّ حدّ صدقٍ عيبٌ
مرشَّحٌ حتى يُحسم**؛ فهذا حسمُه بقياسِ طرفَيه.

---

## ⑨ `T-PRERANK` — **بأيّ مفتاحٍ يُرتَّب المرشِّحُ الرخيص؟** (‏2026-09-04)

**يُدفَع قبل أيّ رقم.** أخرجت `T-PRECAP` أن المرشِّحَ الرخيص يرتّب بـ**أكبر
دولارِ يوم** والمفتاحَ المشحون `post_hi_ret` يختار **أعنفَ قفزةِ افترٍ أمس**،
وهما شبهُ متعامدَين ⇒ السقفُ يُسقط 98% من الإصابات. **والسؤالُ الطبيعيُّ
بعده: هل يوجد مفتاحُ ترتيبٍ للمرشِّح — متاحٌ من النداء المجمَّع الواحد نفسِه —
يسترجع الإصاباتِ بلا رفعِ سقفٍ ولا كلفةِ جلبٍ إضافية؟**

### ① المجتمعُ والقيد

الصفوفُ القائمة نفسُها (‏`33724354384` · `33724361819` · `33724367680` ·
`33777092830`) ⇒ **صفرُ إعادةِ مسح**. والأذرعُ **كلُّها مفاتيحُ يستطيع المرشِّحُ
حسابَها من الشمعة المجمَّعة بنداءٍ واحد** (‏`o,h,l,c,v,vw`) — فلا يُقترَح مفتاحٌ
يستحيل حسابُه حيًّا.

### ② الأذرع — خمسٌ ولا سادسة (مثبَّتةٌ قبل الأرقام)

| ذراع | مفتاحُ ترتيب المرشِّح | الاتجاه |
|---|---|---|
| `R0` | `usd_day` | تنازليّ — **المشحونُ حيًّا · خطُّ الأساس** |
| `R1` | `day_ret` | تنازليّ |
| `R2` | `range_pos` | تنازليّ |
| `R3` | `vwap_rel` | تنازليّ |
| `R4` | **عشوائيٌّ حتميّ** | — **شاهدُ الضبط** |

**وكلُّها بالأرضية نفسِها** (‏$100,000) **والسقف نفسِه** (‏60) ⇒ **ميزانيةٌ
ثابتة**: المتغيّرُ **ترتيبُ المرشِّح وحدَه**، والمأخوذُ لكلٍّ ‏≤60 اسمًا/ليلة.

🔴 **وشاهدُ الضبط إلزاميٌّ ومسجَّلٌ قبل الأرقام**: أربعةُ مفاتيحَ = أربعُ
مقارنات ⇒ بلاه يكون **أفضلُها صدفةً**. وأيُّ ذراعٍ لا تتفوّق عليه **تُلغى**.

### ③ المقياسُ الحاكم والمعيار

الحاكم `hits(cap=60) / hits(Q0)` على **2025 خارج العيّنة** بنموذج **`P-reg`**
(المتشائم، §⑧). **وتُعتمَد ذراعٌ اقتراحًا للمالك إن استوفت الأربعة معًا:**

1. النسبةُ **‏≥0.30** (‏= ‏≈14 ضعفًا عن المشحون 0.021).
2. **تتفوّق على `R4` العشوائيّ** في سنة التقييم.
3. **الاتّجاهُ نفسُه في 2023 و2024** (سنتا المعايرة — وصفيًّا لا اختيارًا).
4. **وكلفتُها بالأسماء تُعلَن** (المأخوذُ في السنة) ولا تُطوى.

### ④ سقفُ النجاح — **عرضٌ للمالك بلا تنفيذ**

مهما كانت الأرقام: **لا يُبدَّل مفتاحُ المرشِّح ولا السقفُ ولا الأرضيةُ ولا
`BUDGET_SEC` بلا أمرٍ صريح** — لأن ذلك **يبدّل ما يصل المالك**. والمخرَجُ
جدولٌ وتوصيةٌ معلَّلة.

### ⑤ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر مكذَّبةً لا تُحذَف)

- **`C3`** — أرجّح `day_ret` أفضلَ الأربعة (صعودُ أمس يقترن بقفزةِ افترِه)،
  بنسبةٍ بين **‏0.20 و0.50**.
- **`C4`** — أرجّح **`usd_day` المشحون أسوأَ من العشوائيّ `R4`**، لأنه شبهُ
  متعامدٍ مع مفتاح الترتيب: يُقصي الرقيقَ بالبناء والمفتاحُ يختاره.
- **`C5`** — أرجّح `range_pos` ضعيفًا (‏إغلاقٌ عند قمّة المدى النظاميّ لا يقول
  شيئًا عن الافتر الذي يليه).

🔒 **قفل `PD10`:** الأذرعُ **خمسٌ لا سادسة** · وشاهدُ الضبط **حتميٌّ قابلٌ
لإعادة الإنتاج** (بذرةٌ من الرمز واليوم لا مولّدُ عشوائيٍّ حرّ) · والأذرعُ
**تتفرّق سلوكيًّا** (مفتاحان يقلبان المختار) · والأرضيةُ والسقفُ **ثابتان في
الخمس** فلا يختلط أثرُ الترتيب بأثر الميزانية.
