# HANDOFF — ذاكرة المشروع الكاملة (للاستمرار في محادثة جديدة)

> 🔴 **شرط المستخدم المُلزِم (لكل جلسة):** المستخدم **دائمًا** يضغط `force_renew=0`.
> **لا تطلب منه `force_renew=1` إلا إذا أنت (Claude) سوّيت تعديلًا جوهريًا يمسّ
> الدخول/الوقف/الأهداف/المستويات** — وقتها **نبّهه صراحة**: «شغّل Daily Pivot
> Screener بـ force_renew=1». غير ذلك = اسكت، الترحيل الآلي + تجديد الجمعة بعد الإغلاق يكفيان.

> ⚠️ **بعد أي تعديل يمسّ الدخول/الوقف/الأهداف/المستويات: ارفع `LOGIC_VERSION`**
> (أعلى `Super_stock.py`). البوت يكتشف التغيّر ويعيد حساب القائمة كاملة **تلقائيًا**
> في أول تشغيل (`migrate_watchlist`) — بلا انتظار يوم التجديد ولا تدخّل يدوي.
> ضمانات إضافية: كل رسالة مختومة «🧾 إصدار SHA» (`code_version`) · Action
> بـ`force_renew=1` لإعادة بناء فورية · 167 اختبار + حُرّاس قرارات.

> **للمستخدم:** انسخ «نص التشغيل» أدناه في أي محادثة جديدة على نفس الـ repo.
> **لـ Claude:** هذا الملف = الحالة الكاملة. اقرأه + `CLAUDE.md` + `Super_stock.py`
> + `git log` قبل أي عمل. لا تعِد عمل تم. التزم بقرارات فيصل المحسومة. المستخدم
> لا يتقبّل أي خطأ ولا الفلسفة الزائدة — طابق منهجية فيصل الموثّقة بدقة.

> 🔬 **يوم مسح الصور الكامل (2026-07-27) — 308 صورة · الحالة النهائية:**
> ① **المسح اكتمل 308/308** (`FAISAL_IMAGE_PASS2.md`، سطر لكل صورة بمعرّفها ونصّها
> العربي وحكمها): ~270 🎯 ارتكاز · 9 ⚠️ نظام آخر · **3 🚫 لا تخصّ الارتكاز** · 45 مكرّرة.
> الطريقة: 116 قرأتُها بنفسي · 143 بمسح متوازٍ (12 وكيلًا) · 40 مكرّرة بـdHash · 6 يدويًّا.
> **21 ادّعاء «قاعدة جديدة» ⇒ صمد 5 فقط** بعد تحقّق خصومي (وكيل مستقلّ يطعن في كل ادّعاء)
> ثم تحقّقتُ من الخمسة بنفسي.
> ② **أُصلحت عيوب حقيقية:** الصيّاد كان يعرض **عكس** شرط فيصل ① («متوسط 20<30<50» ترتيب
> هابط) · وسمي HTCR خطأً «هادئ» وهو +23% فوق حدّ 20% · `hasattr(splits,"index")` صحيح
> للقوائم فكان المِجَسّ يرجع None صامتة · عطلا مُجمِّع الصور (`int("")` + «إمّا نعلّق
> الطابور أو نفقد الصورة»).
> ③ **نُفِّذ:** مجموعة متوسطات 20/30/50 · إبلاغ «قريب من شرط لم يصعد» · N8 «المشتريات
> الموحّدة» · «القاع التالي بنسبة السهم نفسه» · **«أهداف الشورت»** (فحص اليد + صيّاد
> المقسّم، بلا رسالة تلغرام ثالثة).
> ④ **⛔ T-STOP فشلت** (`stop_sweep_prereg.md`): الوقف خارج منطقة المسح (13,15) مقابل
> (5,7) على ثلاث سنوات ⇒ −0.1/+0.1/0.0R، الاتجاه ينقلب والعيّنة تسقط. **🔴 والدرس:
> نسبة النجاح قفزت 34%→54% في 2024 بينما التوقّع لم يتحرّك** — تثبيت المقياس (R) قبل
> الأرقام هو ما منع إعلان «تحسّن 20 نقطة» الباطل. **التأكيد الثاني عشر: الوقف ليس الرافعة.**
> ⑤ **🚧 تدقيق «عدم الخلط»** (سؤال المالك «متأكد ما خلطت ميزة التقسيم بأساس البوت؟»):
> **الجواب: لا خلط بالجذور** (الـ13 byte-identical · لا دالّة تقسيم/شورت جديدة في أيٍّ من
> 19 مسار فرز/تقرير · إشارة `_post_split_high` في `analyze_ticker` **سابقة** ومُغلقة
> ببوّابتين) — **لكن التدقيق أخرج عيبين حقيقيين في وصلي بفحص اليد:** مرجع الـ÷2 كان يقبل
> مفتاحًا **عامًّا** (`ref`) = بابُ اختلاق «هدف هبوط» على ارتكاز غير مقسّم ⇒ صار خاصًّا
> بالحدث المؤسِّس؛ والإطار كان يُقرأ من مفتاح **غير موجود** (`r["df"]`) فسطرا «القاع
> التالي»/«سحب السيولة» **ميتان دائمًا** ⇒ أُصلح للوسيط `df`. + **حاجز خلط صريح بالمُخرَج**
> («منظومة الـ÷2 لا تنطبق هنا … سياق عامّ»). 5 أقفال انحدار · **1225 اختبارًا**.
> **🧭 الدرس: «الأداة تعمل بلا استثناء» ليس دليل سلامة — السطر الميت والمفتاح الغائب صامتان.**
> ⑥ **⏰🛡️ صيّاد المقسّم صار بعد إغلاق الافتر + تدقيق 5 عدسات (بأمر المالك «ادمج وارفع
> بأعلى دقة»):** الكرون `17 22` كان **أثناء** الافتر (18:17 ET) ⇒ صار `13 1 * * 2-6`
> (بعد 20:00 ET بالفصلين · خانة الأيام تنزاح إلزاميًّا). و**دحض خصومي (25 حكمًا: 17
> مؤكَّدة/8 مدحوضة) أخرج 8 عيوب من كودي** — أخطرها «القاع التالي» كانت تقيس أقصى تراجع
> لا الساق الأولى (قاع مُختلَق)، وغياب حارس التغطية، وكل مسارات الفشل صامتة، ونتيجة
> `send_telegram` مُهمَلة. **🔴 وقفلان من أقفالي كانا فارغين** (كُشفا بالطفرة) ⇒
> **القفل الذي لم يسقط مرّة واحدة عمدًا ليس قفلًا.** 1245 اختبارًا · جذور byte-identical.
> ⑦ **⏳ متبقٍّ بقرار المالك:** (أ) ~~دمج الفرع في `main`~~ — **تمّ الدمج** بأمر المالك.
> (ب) ~~18 ميغا صور~~ — **محسوم (المالك 2026-07-27): `faisal_images/` مرجع دائم لا
> يُحذف.** التوثيق النصّي يحفظ الكلام لا **الشارت**، وقواعد فيصل كثيرٌ منها بصريّ
> (ألوان المستويات · شكل الشمعة · موضع الفجوة · لقطة التِّيب). **لا تقترح حذفها ولا
> تقليمها مستقبلًا** — وحذفها أصلًا لا يسترجع مساحة (الصور في تاريخ git، يلزمها
> `filter-repo`). الفهرس الكامل + تحذير «لا تبنِ على هذي» في `faisal_images/README.md`.
> (ج) بنود مسجَّلة بلا تنفيذ: همر على 4س · تضارب مصادر الفلوت · إعادة الشراء خبرًا إيجابيًّا ·
> نقل من OTC (بلا مصدر) · موجة 20% · توقيت «بعد 11» (نظام آخر ⇒ بوّابة سياق).

> ✅ **تحقّق حيّ من SEC (2026-07-27، مِجَسّ Actions مؤقت · حُذف بعد القراءة · تشغيلتان
> `30273347581`/`30273707000`):** ① **خطأ رابط Form 4 مُثبَت 100% على بيانات حقيقية:**
> 520/520 (CMTL) · 589/589 (AAPL) · 223/223 (APVO) · 128/128 (TYGO) · 95/95 (NMRA) ·
> 25/25 (NEGG) · 21/21 (PONY) — **كل** إيداعات Form 4 يأتي `primaryDocument` فيها
> **بسابقة `xsl…/`**، والرابط بالسابقة يرجع `HTTP 200` **بلا** `<transactionCode>`
> (HTML مُصيَّر) بينما **المجرَّد** يرجع XML فيه الوسم ⇒ لولا التجريد كانت الميزة ميتة
> 100% وتستهلك ميزانية SEC صامتةً (200 ≠ إخفاق). ② **الميزة تعمل طرفًا لطرف:** APVO
> إيداع 2025-11-12 ⇒ المحلّل استخرج **شراء 13,513 سهم**. وبقية الرموز `None` **سلوكًا
> صحيحًا** (منح/بيع برموز A/M/S لا شراء سوق مفتوح). ③ **الطرح الجديد: منطقه صحيح
> و«لا شيء» هو الجواب الصحيح لا عطل** — NEGG أحدثُ نموذج طرح لديه `EFFECT 2026-06-12`
> **والنشرة النهائية أقدم** (`424B5 2025-07-15`): بالكود القديم كان EFFECT **يحجبها**
> فتموت الميزة، والآن يمسح القائمة كلها فيجدها ثم يرفضها **لعمرها فقط** (خارج 120 يومًا)
> — وهذا **بالضبط سيناريو الحجب الذي أصلحناه، مُثبَتًا على بيانات حيّة**. TYGO كذلك
> (`424B5 2026-02-25` خارج النافذة). ولا رمز في القائمة اليوم لديه نشرة نهائية داخل
> النافذة ⇒ الصمت صادق. **وEFFECT/F-3/S-3/424B3 موجودة فعلًا في بيانات القائمة** ⇒
> خطر «التسجيل الرفّي يُعرَض حدثًا مؤسِّسًا» كان حقيقيًّا لا نظريًّا.

> 🧾 **بطاقة فيصل الفرزية — نُفِّذت الثلاثة (2026-07-27، PR #208 · عرض/سياق فقط · لا
> LOGIC_VERSION · 1153 اختبار):** ① **«طرح جديد» حدثًا مؤسِّسًا** موازيًا للتقسيم
> (`_SEC_OFFERING` قناة مجانية + `_offering_event` بسقف `OFFERING_PROBE_CAP`=20) —
> `_split_setup_probe(offering=)` يقبله **فقط عند غياب تقسيم عكسي حديث** ⇒ `offering=None`
> = السلوك السابق بت-بت؛ والشرط ① بالصيّاد صار «حدث مؤسِّس» **والخمسة الباقية لم تُمَسّ**.
> **⚠️ التضييق الحاسم:** الحدث المؤسِّس = **نشرة نهائية فقط** (`424B1/424B4/424B5`)؛
> `S-1/S-3/F-3/EFFECT/424B3` **تسجيلات روتينية** كانت ستُعيد ضبط مرجع الـ÷2 لتاريخ
> اعتباطي وتُسقط بوّابة «لم يصعد» ⇒ سياق عرض لا setup. + `half_down_target` (قاعدة MWC
> «هبوط 4.70 ⇒ مستهدف 2.35» = ÷2 على **المستوى السائد**، مشروطة باستقراره 3 جلسات
> **وبقرب السعر منه**). ② **مرجع «لم يصعد» = افتتاح يوم الحدث** (`_event_day_open`) لا
> إغلاق أول شمعة — مُثبَت بالبطاقات: HTCR 3.72→4.58 = +23% هادئ ✅ · BNKK 3.69→7.19 =
> +95% انضخّ ❌. ③ **Form 4 شراء الداخليين** (`_parse_form4` تشترط **P+A**، تُهمل البيع
> والمنح · متوسط سعر **مرجَّح** · إفصاح مختلط يُصرَّح ببيعه · ميتا مجانية والمستند بسقف
> `FORM4_BUDGET`=12/تشغيلة) + **«خبره عدم قبوله = هبوط»** (`news_acceptance` بحدّ حداثة
> **21 يومًا** إلزامي).
> **وستّ صغائر أُصلحت معها:** قاطع دائرة تراكمي مشترك ⇒ عدّادان مستقلّان يُصفَّران كل
> إثراء · FINRA يعيد المحاولة عند ردّ غير 200 لا الاستثناء فقط **وصار له ميزانية
> (`FINRA_BUDGET`=400) وقاطع دائرة وسقف كاش** (كان بلا أيٍّ منها) · تسرّب regex في Form 4
> (كتلة بلا سعر تلتقط رقم حاشية) ⇒ قراءة داخل حدود العنصر · متوسط السعر **مرجَّح** ·
> إفصاح مختلط **يُصرَّح ببيعه** · قنوات SEC الجانبية مسقوفة الحجم. **وصدق العرض:** أُزيل
> سطر «خبر لم يُقبَل» من كرت التحليل (الحقل غير محسوب هناك = وعد كاذب؛ يبقى باليومي وفحص
> اليد حيث الإطار متوفّر) · وصياغة فحص اليد صارت «لا شراء مؤكَّد ضمن آخر مستندَين · أو
> تعذّر الجلب — **ليس نفيًا قاطعًا**» بدل نفيٍ لا نملكه. **1158 اختبار.**
> 🔴 **خمس ثغرات لقّاها التدقيق الخصومي (ثلاث عدسات) في كودي أنا — درس مُلزِم:** (أ)
> **`NaN` ليس `None`** — احتياط `if x is None` لا يلتقط NaN و`not (nan and nan>0…)`
> يعطي True ⇒ **بوّابة تفشل مفتوحةً** (كانت تمرّر مقسّمًا انضخّ +95%). (ب) **سطر عرض
> بلا حقل = كذبة**: أسطر فحص اليد كانت ميتة وتطبع «لا إفصاح شراء» كحقيقة. (ج) **كل
> مقارنة بين حدث قديم ورقم يتحرّك اليوم تحتاج حدّ حداثة** وإلا صارت تحذيرًا دائمًا
> كاذبًا. (د) **نموذج SEC روتيني ≠ حدث**. (هـ) عدّاد قاطع دائرة **تراكمي** يعيش عبر
> التشغيلات = إطفاء صامت للميزة.

> 🎯 **حكم T-EXIT النهائي (2026-07-27، `exit_policy_prereg.md` · ثلاث سنوات): فشلت —
> «الطمع» يهدم التوقّع بثبات تام، ومستوى الهدف ليس الرافعة.** على **نفس الصفقات والوقف**:
> +20% ⇒ −0.05R (2024) · −0.16R (2023) · −0.25R (2025) — و**+100% ⇒ −0.62/−0.80/−0.90R**
> (يُبلَغ في **1-3% فقط** قبل ضرب الوقف). أفضل هدف = المرجع نفسه في الثلاث ⇒ **الفرق
> +0.00R** والشرط «≥100 محسومة/سنة» **مستوفًى في الثلاث (147/109/168)** ⇒ **الفشل نتيجة
> حقيقية لا ضعف عيّنة**. ⇒ **يُغلق سؤال «فيصل يمتّع» آليًّا:** الحمل لهدف أكبر **بسياسة
> ثابتة** أسوأ لا أفضل — **وهذا لا يكذّب فيصل** (هو يمتّع بإدارة حيّة: تحريك وقف · إعادة
> دخول · قراءة مضارب — لا بهدف ثابت). واتساق مع قياس الصباح: «11% تعطي 50%+» = «+50%
> بُلغ 7-12%» ⇒ **السقف في قلّة الواصلين لا في مستوى الهدف** ⇒ الحافة = التوقيت.
> 🔴 **وتصحيح ذاتي مسجَّل:** التشغيلة الأولى طبعت «تتبّع +0.79R متفوّقة» و**سُحبت**:
> ذراعا t2/t3 لم تعملا صامتتين (قاموس الصفقة يحمل `t1` فقط والجذر مقفول) وذراع التتبّع
> **متحيّزة منهجيًّا** (تربح على أي قمة موجبة ولو +1%؛ وقف متحرّك لا يُقاس بلا مسار سعر).
> أُسقطت ولم «تُعدَّل»، وحلّ محلّها سلّم نسب ثابتة. **⏳ غير مُختبَر ويُصرَّح به:** t2/t3
> الفعليان · الوقف المتحرّك · الخروج الجزئي — كلٌّ يحتاج قرارك + تسجيلًا مسبقًا جديدًا.

> 🧪 **يوم القياس الكبير (2026-07-27) — أربعة أرقام تحكم كل قرار قادم:**
> **① نسبة النجاح الحقيقية عبر ثلاث سنوات** (السوق الكامل، البوابات الحالية): 2023 **27.4%**
> (225 إشارة) · 2024 **33.6%** (150) · 2025 **25.9%** (199). **② العائد المحقّق بخروج «الهدف
> الأول»: −0.2R (2025) · +0.0R (2024) ⇒ متعادل إلى سالب.** ⚠️ وهذا **يقيس سياسة البيع عند t1
> فقط وهو صامت عن t2/t3 في الاتجاهين** (الخسائر الـ109 ضربت وقفها قبل t1 فلا يغيّرها أي حمل ·
> والـ38 ربحًا لمساتُ t1 قد يرتدّ جزء منها لو حُملت). **③ الحركة المتاحة قبل الوقف (المقياس
> الحقيقي لقوة الاختيار): 11% فقط من المُعبَّأة تعطي 50%+ · وسيط الصعود المتاح +6% ⇒ «حدّي» عند
> الحافة الدنيا للمعيار المسجَّل.** **④ تخفيف البوابات (99.5/60ج/100K) لا يحسّن ولا يخرّب:**
> 2025 25.9%↔25.9% · 2024 33.6%↔32.4% · انفجار 11%↔10% — يضاعف الكمّية فقط. **⇒ البوابات تبقى
> كما هي، وادّعاء «17%» في قفل C3 كان مبنيًّا على 6 صفقات محسومة ولم يتكرّر على 243+213.**
> (السجلّ الكامل: `backtest_2025_live_winrate.md` · تشغيلات 30242542591/30243610769/30245557453/
> 30245566129/30245745665/30245747100/30247477895.)

> 🔕 **لا إشعار إلا بالجاهز — رادار التقسيم (2026-07-29، قرار المالك).** كان يرسل 12
> سهمًا أغلبها بـ❌ (مطابق كامل واحد فقط). الآن `split_radar_ready` تُبقي **المطابق الكامل
> وحده** (معايير فيصل الخمسة معًا)، وصفر مطابق ⇒ **صمت تامّ** لا رسالة فارغة ولا «لا يوجد».
> 🔒 عرض/تنبيه فقط · خارج الجذور · لا LOGIC_VERSION · 4 اختبارات و4 طفرات كلها سقطت.
> ⚖️ صار متّسقًا مع صيّاد المقسّم (وُلد صامتًا-إلا-بالمطابق) ومع «اليومي: الجاهز فقط».

> 🔴 **M14 صارت بوّابة صلبة — قرار المالك «يكون مستبعد تماما» (2026-07-29).**
> الفلوت الكبير كان **نقصًا يُسجَّل** منذ v2.7، فظهر حيًّا HTZ (129م) وPONY (277م) رغم
> الحدّ 50م — كلاهما بـ3 نواقص = `WATCH_MAX_FAILS` بالضبط فيمرّان. الآن **فلوت معلوم
> وكبير ⇒ يُحذف** كـM1-M5، و`refloat_gate_recheck` تُخرِج **بلا شرط عدد النواقص**.
> ⚠️ **المجهول يبقى بفائدة الشك** (تعذّر ≠ كبير). 🔄 **والمحمولون يُوسَمون ولا يُخرَجون**
> — الاستبعاد للترشيح لا لمتابعة مركز قائم.
> 🔒 **`apply_float_gate` هو الجذر الوحيد الذي تغيّر** (بأمر المالك)، والاثنا عشر الباقية
> byte-identical. **`LOGIC_VERSION` = `2026.07.29-m14hard…` ⇒ يلزم `force_renew=1` مرة.**
> 5 طفرات كلها سقطت · والقفل القديم «فلوت كبير لا يُحذف» سقط أوّلًا (أدّى دوره) ثم حُدِّث.

> 📉🕵️ **درسا YYAI (2026-07-29، خمس صور — عرض/تحذير فقط):** فيصل على YYAI ($11→85 سنتًا):
> «لايوجد شورت لهبوط السهم · مضارب هبط بالسهم · اعلان استحواذ + هبوط».
> ① `_dump_actor`: انهيار **بلا شورت يفسّره ⇒ المضارب يصرّف** (يُلحق بحدث `dump`) —
> **أوّل ما يقرأ خروج المضارب** بعد أن كان كل شيء يقرأ دخوله. ② الأحداث المعلنة صارت
> **ذات اتجاهين** («قد يستعمله باب خروج») بدل «يوم انفجار محتمل» وحدها.
> ✅ **وفحص جانبي:** YYAI عند $7.54 كان يقرأ مرشّحًا نموذجيًّا (RSI 25.5) ثم نزل −68%
> إضافية — لكن سيولته ≈$86 ألف **تحت أرضية M5** ⇒ البوت يرفضه. **الدرع الذي أسقط أسهم
> فيصل الأربعة حمانا هنا** — الوجه الآخر للقرار نفسه.

> 🔁 **مسكة حيّة: سهم بفلوت 277 مليونًا في القائمة (2026-07-28، مُصلَح بقرار المالك).**
> فحص الحالة الحيّة (لا البنية) كشف أن **PONY** فلوته المخزَّن `277,657,352` = **5.5× الحدّ**
> (50م) بينما وسمه «فلوت غير متاح — مُرِّر بفائدة الشك» و`soft_fails` بلا «فلوت كبير».
> **السبب:** M14 تُطبَّق **قبل** `enrich`؛ تعذّر الجلب لحظتها ⇒ فائدة الشك (سليم) — ثم ملأ
> `enrich` القيمة **ولا شيء يُعيد الحكم**. **والأثر ليس عرضيًّا:** بـ3 نواقص والحدّ 3، لو
> كانت القيمة حاضرة لصارت 4 ⇒ `classify_tier` **يرفضه** فما كان دخل أصلًا.
> ✅ `refloat_gate_recheck` بعد enrich في مسارَي الاختيار — تستدعي **المعيار القائم نفسه**.
> ⚠️ **يمسّ العضوية ⇒ `LOGIC_VERSION` مرفوع (`2026.07.28-m14recheck…`) — شغّل `force_renew=1`
> مرة واحدة.** MSTR فلوته **ما زال مجهولًا فعلًا** ⇒ فائدة الشك في محلّها (لا خلل).
> 🔴 **ودرس من الطفرة:** الدالّة صارت في **مسار الاختيار الحيّ**، فكشفت الطفرة أن استثناءً
> فيها (فلوت بقيمة غير رقمية) كان **يكسر الفرز اليومي كلّه** ⇒ حُصِّنت فاشلة-آمنة: أي خلل
> يُبقي السهم كما هو، فلا يسقط أحد بسبب عطل قراءة.

> 🔴⏰ **العائق الثاني (أخطر): الرادار كان يفوّت أول ساعتين من كل يوم (2026-07-28، مُصلَح).**
> المدقّق الصارم على الجلسات التسع المسترجَعة: **`session_complete=0`** والسبب واحد يتكرّر —
> `start_coverage_late(108-141د)`. قياس بدء 11 تشغيلة: **GitHub يتأخّر 95-152 دقيقة (وسيط
> ~119)** فيبدأ الرادار **15:05-15:50 UTC** بدل الافتتاح 13:30. ⇒ **ليس عيب قياس فقط بل عيب
> منتج**: الرادار مبنيّ ليصطاد لحظة الاشتعال «**من الافتتاح**»، وكان يغيب عن أنشط نافذة يوميًّا.
> ✅ **الإصلاح:** كرون **11:11 UTC** (الافتتاح −139د فيعوّض الوسيط؛ بأسوأ تأخّر مرصود يبدأ
> ~13:43) · سقف انتظار الجرس **90→150** دقيقة (كان يكفي صيفًا ولا يكفي شتاءً: 14:30 UTC) ·
> `IGNITION_MAX_RUNTIME_MIN` 290→330 و`timeout` 300→345 حتى لا يقصّر مقطع الافتتاح عن حدّ
> التقسيم فتنشأ فجوة انتقال. **الانتظار قبل الجرس لا يُنبّه أبدًا ⇒ التقديم فاشل-آمن.**
> مقفول باختبار **حسابي** على أرقام التأخّر (ثلاث طفرات تُسقطه). ⚠️ البنية كانت سليمة أصلًا
> (`_segment_window` يحسب من الافتتاح الفعلي لا من بدء الرنر) — **العلّة الجدولة وحدها.**

> 🔴 **قرار المالك: بوّابة «موافقة Codex» متقاعدة (2026-07-28).** «انسَ Codex — كل شيء بيني
> وبينك». فالإذن إذن المالك وحده. ⚠️ **وما لا يسقط معها:** التسجيل المسبق قبل النتائج · فصل
> القياس عن التنبيه · الجذور المقفولة · اختبار الطفرة — انضباط منهجي لا مجاملة لمراجِع.

> 🔴♻️ **عطل حقيقي دام 13 يومًا — دفع assembler الرادار كان عاريًا (2026-07-28، مُصلَح):**
> الـ«فشل» الأحمر على `ignition.yml` منذ 07-15 كان **موثَّقًا خطأً** بأنه «بوّابة قياس E2
> الصارمة فقط». السجلّ الحيّ (تشغيلة `30281846097`) يثبت العكس: **البوّابة لم تُشغَّل أصلًا**
> (skipped)، **والفاشل `git push`**:
> `[main 64ab2e0] 🔬 E2: ملخّص…` ثم `! [rejected] main -> main (fetch first)`.
> الجلسة تمتدّ ~4.5 ساعة فيتحرّك `main` أثناءها، والدفع بلا fetch/rebase/إعادة محاولة ⇒
> **ملخّص كل جلسة يضيع ومعه `ignition_log.json`/`ignition_universe.json`**. جلسة واحدة فقط
> (07-24) نجح دفعها من عشر ⇒ **هذا سبب «0/20» الحقيقي، لا ندرة الأحداث.**
> ✅ **الإصلاح:** نمط fetch+rebase + 5 محاولات بتراجع أُسّي + `exit 1` صريح (نفس المُثبَت في
> `telegram_collect.yml` — تدقيق كل الـworkflows أظهر أن `ignition.yml` كان **الوحيد** بلا
> حماية دفع) · والبوّابة الصارمة صارت **`if: always()`** فلا تُتخطّى عند فشل الدفع.
> ♻️ **والبيانات لم تضع:** كل تشغيلة ترفع `ign-assembled-<run_id>` (90 يومًا) **قبل** الدفع →
> `e2_recover.py` + `e2_recover.yml` (يدويّ، `gh run download`) يدمجان الفهرس: تعارض يوم من
> تشغيلتين ⇒ تفوز **الأكثر دورات** (حتميّ) · مجلّد بلا ملخّص **يُتخطّى لا يُخمَّن** · idempotent ·
> **الفهرس وحده يُدفَع** (`e2_measurement/` مُستثنى عمدًا بمواصفة E2 §5/§12 — و`git add` على
> مسار مُستثنى **يتخطّاه صامتًا**، فأُضيف قفل عامّ يمنع ذلك في أي workflow).
> 🔴 **ثلاثة دروس:** (أ) «الأحمر مفهوم» ادّعاءٌ — **اقرأ أي خطوة فشلت** لا تستنتج · (ب) خطوةٌ
> تُتخطّى تُقرأ خطأً كأنها فشلت · (ج) **مقياس اختبار الطفرة نفسه يحتاج شاهد ضبط**: كان
> `grep -c '❌'` يعطي صفرًا **للانهيار أيضًا** فقرأتُ «القفل نجا» بينما القفل قتل الطفرة
> (صُحِّح إلى رمز الخروج + السطر الأخير).

> ⛔ **T-CMAG — «هل الدولة تحدّد مقدار الصعود؟» (2026-07-28، `country_magnitude_result.md`):
> فشلت — الشروط الخمسة كلها سقطت.** الأرقام: **2023** الصين 5 صفقات وسيط 7% مقابل 171/7%
> (‏−0 نقطة) · **2024** 8/13% مقابل 112/7% (‏+6) · **2025** 10/5% مقابل 146/7% (‏−2) ⇒
> **الاتجاه ينقلب**، والمقياس المساند **مجمَّعًا متطابق**: 17.4% (4/23) مقابل 17.5% (75/429)
> بفاصلين متداخلين، والعيّنة الصينية 5·8·10 دون حدّ 20.
> 🌏 **والاكتشاف البنيوي الأهمّ: الصين 23 صفقة من 452 = 5.1% داخل المؤهَّلين** ⇒ كون أسهم
> فيصل الخمس صينية **انحياز ناجين من فئة نادرة**، لا خاصية إحصائية للدولة.
> 📉 المتوسط ضعفا الوسيط أو ثلاثة أضعافه في كل خلية — تثبيت **الوسيط** قبل الأرقام هو ما
> منع صورةً متضاربة كلَّ سنة. ✅ واتّساق: النجاح 27.4/33.0/26.0 ≈ المقاس سابقًا
> (27.4/33.6/25.9)، ووسيط بقية الدول **ثابت 7%** في الثلاث.
> **الأثر صفر:** لا بوّابة ولا وزن ولا حتى سطر العرض الذي كان سقف النجاح · جذور byte-identical.
> ⚠️ حدّ مُعلَن: الوسيط **المجمَّع** لم يُحسَب (تنزيل الـartifacts محجوب بسياسة الشبكة —
> الأرقام من سجلّ التشغيل)؛ لا يغيّر الحكم (①④ من بيانات السنوات و③ من أعداد صحيحة).
> **التأكيد الثالث عشر: الحافة = التوقيت لا الاختيار.**

> 🔬 **تفاصيل تصميم T-CMAG (للرجوع، `country_magnitude_prereg.md` مدفوع قبل أي تشغيل ·
> فرع `claude/country-magnitude`):** كان آخر بند مفتوح من مسح الصور.
> **الفرق عن الدولة التي سقطت سابقًا جوهريّ:** `backtest_country_thread` القائمة تقيس
> `exploded` **ثنائيًّا** (احتمال — عائلة سقطت كاملةً)، والجديدة `backtest_country_magnitude`
> تقيس `mg_pre_stop` **المستمرّ** = «كم أعطانا السهم من دخولنا قبل أن يضرب وقفه». سند فيصل:
> «صيني **نقيسه مع الاسهم السابقه**» = قياس مقدار **بالأقران** لا انتقاء.
> **المقياسان:** وسيط `mg_pre_stop` (أساسي) + نسبة بلوغ 30% بفاصل Wilson (مساند).
> **المعيار الخماسي:** ثبات الاتجاه في الثلاث · فرق ≥5 نقاط و≥1.5× كلَّ سنة · Wilson منفصلان
> مجمَّعًا · ≥20 صفقة/شريحة/سنة · لا انقلاب. **سقف النجاح محدَّد سلفًا: سطر عرض فقط.**
> **التشغيلات:** 2023 `30379807286` · 2024 `30379820033` · 2025 `30379826198`
> (`bt_features=1` + `bt_potential=1`). التجميع بـ`cmag_assemble.py` (المعيار **شيفرةً** لا
> حكمًا بعديًّا)، والحكم في `country_magnitude_result.md`.
> 🔴 **درس الطفرة تكرّر:** حدّ الفرق (5 نقاط) **لم يكن مقفولًا** — شرط النسبة كان يستره،
> ولم تكشفه إلا الطفرة ⇒ أُضيفت اختبارات تخوم تفصل الحدّين (11 طفرة، كلها سقطت).
> ⚖️ **وتمييز صدق مكتوب قبل النتائج:** «فشلت الفرضية» ≠ «العيّنة لا تكفي» — الثانية **لا حكم**.

> ⛔ **T-SHORT — فرضية «الشورت تحت 10 آلاف» (2026-07-27، `short_thread_prereg.md`): فشلت.**
> طلب المستخدم اختبار ملاحظته أن أسهم فيصل شورتها تحت 10 آلاف. بُنيت أداة **point-in-time
> حقيقية** (ملفات FINRA اليومية المؤرَّخة `CNMSshvol<تاريخ>.txt` — يوم الإشارة أو قبله حصرًا،
> رمز غائب = مجهول لا صفر) خلف علم `BT_SHORT` مطفأ. **النتيجة: 2025 +10 نقاط · 2024 +29 · 2023
> −7 ⇒ انقلبت في السنة الثالثة ⇒ فشل شرط الثبات ⇒ لا تغيير على M13 ولا أي بوابة.**
> 🔴 **درسان منهجيان مثبَّتان:** (أ) **سنتان لا تكفيان — الحد الأدنى ثلاث سنوات مستقلّة** (كتبتُ
> بعد سنتين أنها «أول فرضية لا تنقلب» فأسقطت الثالثة الادّعاء). (ب) **غياب التدرّج بين الشرائح
> كان تحذيرًا مبكرًا** (10-20 ألفًا كانت الأسوأ) — علامة ضجيج عيّنة لا عتبة حقيقية.
> ⚠️ **حدّ لا يُنسى:** المُختبَر = **حجم الشورت اليومي (FINRA)**؛ و«شورت» فيصل = **المتاح
> للاقتراض (ChartExchange)** و**لا تاريخ له إطلاقًا** (تأكّد بتدقيق مستقل: المتاح والرسوم
> والفلوت الحر — لا مصدر as-of مجاني لأيٍّ منها). فالنفي **لا يُعمَّم على المتاح**، وسبيله
> الوحيد **حصاد أمامي** عبر `borrow_hist` ثم تسجيل مسبق جديد + موافقة المستخدم.

> 🧾 **بطاقة فيصل الفرزية (2026-07-27، 5 صور: HTCR · BNKK · MWC · SVRE · MBRX):** حقوله الثابتة
> = الجنسية · القطاع · **الحدث المؤسِّس** («قسم» **أو «طرح جديد»**) + تاريخه · **افتتاح** يوم
> الحدث · **أعلى** بعده · الفلوت · الشورت · **أخبار مؤرَّخة مرتّبة**. الشورت: 0 · 4K · 7K · 20K ·
> 50K (وسيط 7 آلاف — يدعم ملاحظة المستخدم وصفيًّا، **ولا يثبتها إحصائيًّا** كما أعلاه؛ وSVRE
> 50 ألفًا فوق سقف M13 ⇒ ميل لا قاعدة). ⚠️ **والفلوت ليس صغيرًا دائمًا** (MWC 26.68م · BNKK
> 7.84م) ⇒ شرط «فلوت أقل من 2 مليون» **خاصّ بـrecipe التقسيم الضيّق ولا يُعمَّم على فرز فيصل**.
> 🆕 **ثلاثة لم تُنفَّذ بعد:** (1) «**طرح جديد**» حدثًا مؤسِّسًا موازيًا للتقسيم (MWC: هبوط 4.70
> → مستهدف **2.35 = 4.70÷2** ⇒ الـ÷2 على **المستوى السائد** لا على قمة ما بعد التقسيم حصرًا).
> (2) مرجع الـ÷2 عند فيصل = **افتتاح** يوم الحدث (3.72 · 3.69 · 8.10) ودالّتنا `_split_day_value`
> تستعمل **الإغلاق**. (3) **شراء الداخليين (Form 4)** خبرٌ محوري عنده («ارتفعت **بسبب** المدير
> اشترى 1.7 مليون سهم» · «اشترى الرئيس 12 ألف سهم») ونحن نقرأ ملفات SEC ولا نلتقطه · و«**خبره
> عدم قبوله = هبوط**» (MBRX) = نسخة خبرية من قاعدة LABT. ✅ **وتأكيدات:** KST(10,15,20,30,10,10,
> 10,15,9) وMACD(12,26,9) حرفيًّا بشارت SVRE · مفتاح الألوان 🔵/⚫/🔴 · موعد إعلان مجدول (MBRX 30/6).

> 🔴 **$JZ — الحالة المرجعية لقاعدة «القروب يُلغي الأهداف» (2026-07-27، الكاتالوج §عشرون):**
> ستّ صور توثّق الحدث بأرقامه: «السهم **أهدافه عالية جدًا ومحققة بيد مضارب أقلها 100%** (وبطنه
> 300-400%) · **دخل قروب غبي رفع السهم من 2.50 لـ3.30** · باعوا عند 3 · **المضارب هبط بالسهم إلى
> 1.90** · **إذا المضارب الآن ألغى جميع الأهداف والصعود** · **ضغط السهم بـ86 ألف سهم من 2.80 لـ1.90**».
> ⇒ **سند حيّ كامل للقاعدة المنفَّذة** (`pump_voids_targets_line`). قواعد مرافقة مؤكَّدة: «**عدم كسر
> 2.35/2.30 — تعتبر دعم مقاومة سابقة**» (الدعم المنقلب) · «**الشمعة الساقطة سابقًا مداها 2.87 —
> اختبرها اليوم وهبط**» (`_red_candle_heads`) · «**4 مقاومة مهم يتحرر منها · ثباته فوق 5 بيرفكت**»
> (سلّم التحرر) · CCI عبر الفريمات (يومي −64 · 4س +128 · 1س −106). **لا كود جديد — تأكيد وسند.**
> ⏳ سؤال «لماذا لم يرشّح البوت JZ» يُجاب بتشخيص البوابات الفعلي (فحص اليد + باكتيست الرمز) لا بالتخمين.

> ⚖️ **تدقيق التعارض: فصل «مضاربة الزخم» عن «منهجية الارتكاز» (2026-07-27، PR #197 · الكاتالوج
> §تاسع عشر) — قاعدة مُلزِمة للمستقبل:** سأل المستخدم «ممكن أرسلت صورًا تخصّ استراتيجية مضاربة لا
> الارتكاز — تأكّد ما فيه تعارض»، **والفحص أثبت أن سؤاله في محلّه ولقيتُ خللًا أدخلتُه أنا**:
> `rebound_entry_state` (اتفاق الفريمات 5/15/30د) **مصدرها قناة تعليمية** («شرح سابق للدكتور فيصل …
> **مع بعض الاضافات من باب النصح**») لا فيصل مباشرة، و**نظامها مضاربة زخم** (شرطها المكتوب «السهم
> **محقق ارتفاع** + سيولة عالية») بينما **سهم الارتكاز عند قاعه لم يصعد** — وكنت ربطتها بفحص اليد
> **بلا شرط السياق** فتعطي «إشارة دخول» لقاع راكد. **صُحِّح:** `REBOUND_MIN_RISE_PCT`=20 بوّابة
> سياق إلزامية داخل `entry` + سطر «**لا ينطبق** … منهجية الارتكاز عند القاع منفصلة» + حدود المصدر
> بالـdocstring + اختبارات قفل. وأُلغي ادّعاء «تُكمِّم لا تدخل الدعم الأول» → **تشابه لا تطابق**.
> **📏 ونُفِّذ في موضعه الصحيح:** «حقق متوسط 30 يوم **من تاريخ التقسيم**» (LABT) =
> `split_ma_maturity`/`split_ma_line` **في صيّاد المقسّم** (المتوسط لا ينضج قبل مرور 30 جلسة منذ
> التقسيم؛ قبلها نافذته تخلط ما قبل/بعد التقسيم) — عرض/سياق فقط.
> **🧭 القاعدة المُلزِمة:** قبل تنفيذ أي قاعدة من صورة حدِّد ① مصدرها (فيصل أم إعادة شرح) ② نظامها
> (ارتكاز/تقسيم/توقيت/زخم) ③ شرطها المكتوب — وإن اختلف النظام فـ**بوّابة سياق إلزامية** لا مجرّد
> ملاحظة توثيقية. **الجذور و`analyze_ticker`/`scan_ignition`/`scan_market` byte-identical · 1087
> اختبار · منهجية الارتكاز لم تُمَسّ.**

> ⭐⭐ **«اتفاق الفريمات» وصيد الارتداد (2026-07-27، PR #196 · الكاتالوج §ثامن عشر) — أقوى مادة
> تنفيذية:** فيصل (قناته التعليمية IMG_0305/0306) يعطي **نظام دخول لحظي كامل**: «تفتح الشارت على
> **5د و15د و30د** — إذا الثلاثة **متفقة وثابتة على نفس الدعم** فالارتداد قريب وقوي … صعد 13.54 →
> اختبر الدعم 10.21 → ارتد 11.46 → **رجع مرة ثانية** وثبت → **هنا الدخول فورًا مع الارتداد الثاني
> والثبات** (طلع 14.70 = **+45%**) · **الخروج: كسر الدعم وأقفل تحتها اطلع فورًا** · **يشتري «طلب»
> لا ماركت من «العرض»**». ⇒ **يُكمِّم «لا تدخل الدعم الأول»: الأول لا · الثاني نعم**، ويضيف بُعدًا
> **مفقودًا تمامًا** (فريمات لحظية؛ فريماتنا كانت شهري/أسبوعي/يومي). **نُفِّذ:** `_resample_minute_bars`
> + `rebound_entry_state` + `rebound_entry_line` موصولة بـ**فحص اليد** (تعيد استخدام دقائق Polygon ·
> فاشلة-آمنة · خارج مسار التنبيه). **⚠️ خللان أمسكهما الاختبار:** النافذة كانت «عدد شموع» (12 شمعة =
> ساعة على 5د و6 ساعات على 30د = مناطق مختلفة) → **نافذة زمنية موحّدة**؛ وسطر العرض كان يحجب «كسر
> الدعم» خلف «الارتداد الأول» → **الكسر أولًا**. **تأكيدات:** IMG_0302 «الإيجابية = **تجاوز شمعة
> سابقة مع ثبات** لا كسر مقاومة فريم» · IMG_0303 LABT «تهييض قروبات ⇒ **المضارب هبط به لـ1.66**»
> ثم «**القروبات غفلوا عنه**» فنجح + «**برامج آلية تحترم الدعوم والمقاومات**» = تبرير سلّم الأهداف.
> **⏳ موثّق بلا كود:** «حقق **متوسط 30 يوم من تاريخ التقسيم**» (يلزمه ربط نافذة المتوسط بتاريخ
> التقسيم). 1079 اختبار.

> ⭐ **إكمال دفعة الذهب 13/13 (2026-07-27، PR #194 · الكاتالوج §سابع عشر):** الستّ التي تعذّر
> عرضها تلقائيًّا **قُرئت مباشرة من `/root/.claude/uploads/`** (الملفات كانت موجودة — العطل في
> العرض فقط؛ **درس: جرّب القراءة من القرص قبل طلب إعادة الإرسال**). **الجديد:** ① ⭐ **قاعدة «ربع
> الساعة»** (JZ‑1س): «لو كان مضارب **يعطيك مجال تبيع وتذبذب يصل مداه أكثر من ربع ساعه** — طلع
> السهم **بدون اذن مضارب** ضغطه المضارب للهاويه» = **معيار زمني يميّز الرفعة المأذونة عن القروب**،
> وهو ميكانيكا قاعدة «القروب يلغي الأهداف» → **نُفِّذ** `operator_sustain`+`OPERATOR_SUSTAIN_MIN`=15
> (قياس/تشخيص فقط · يُستهلَك في مسار الدقيقة المردوم بعد التنبيه). ② **AZI:** «تحرر من 1.36 **شيله
> من العرض بأي سعر**» → +170% ثم +600% = التحرر **زناد تنفيذ** لا ملاحظة. ③ **DSY:** «**هدفك عالي
> شرط 3.20 تحرر**» = طبقة `activation_state` حرفيًّا · «أخذته من العرض الليلي 1.85 · دفعة أخرى 1.70
> غدًا» = `ENTRY_TRANCHES` · بطاقة كاملة (فلوت 1.25م · شورت 100 · استعادة امتثال). ④ **EHGO:**
> «1.20>1.10 طلبه» = الطلب **تحت** السوق. ⑤ **EZRA/EDBL:** «رُفِع أكثر من مرة **بدون مضارب** ⇒
> **متابعه فقط**» و«**القاع 2 = ثبات أو سحب سيوله**» — **نُفِّذتا بالكامل (PR #195)**:
> `group_pump_scar` صار يعدّ الرفعات المستقلّة (`n_pumps`، فاصل 20ج — كان يُعيد آخر نمط فقط فتعذّر
> تطبيق القاعدة) → `pump_repeat_watch_only` بالكرت واليومي · و`bottom_test_state`/`bottom_test_line`
> لحالة «القاع 2» **مقرونةً بنطاق السحب 7–13%** (موصولة scan→تخزين→تجديد يومي→الكرت/اليومي/الصيّاد).
> **و⏱️ ربع الساعة وُصِلت بمسار حقيقي** (كانت دالّة بلا مستهلك): `_fire_sustain` داخل
> `record_ignition_fires(fetch_bars=…)` تُستدعى **عند نهاية الجلسة حصرًا** فتبقى خارج خيط التنبيه
> (درس مراجعة Codex P0-1: أي جلب على مسار التنبيه يؤخّره) + ختم `fired_ts_ms` لحظة الاشتعال في
> `ignition_live` (محلي بلا شبكة) + سطر بتقرير التطوير يصنّف «صمد ≥15د = رفعة مضارب» مقابل «سُحق
> فورًا = رفعة بلا إذن». بلا مفتاح/جالب = السلوك السابق حرفيًّا. **1071 اختبار.**
> 🔴 **تصحيح ذاتي (2026-07-28، `plans/002`): الوصلة كانت ميتة في الإنتاج بسببين مستقلَّين.**
> ① المقاطع (`ignition_live.py:624`) لا تنادي `record_ignition_fires` أصلًا — الفرع الذي يمرّر
> الجالب هو «الجلسة الواحدة» غير المستعملة في `ignition.yml`، وفرع المقاطع يكتب
> `segment_fires.json` **بلا قارئ**. ② والـassembler (النداء الحيّ الوحيد) كان بلا `fetch_bars`
> وبلا `fired_ts_ms` ⇒ `_fire_sustain` تُرجع `{}` دائمًا. **الاختبارات كانت خضراء لأنها تحقن
> الاثنين.** ✅ أُصلح بـ`_fires_from_candidates` (تملأ الطابع من `telegram_sent_at_ms` ثم
> `trigger_bar_start`) + تمرير `polygon_minute_bars` من الـassembler. **الدرس: «موصولة» تُثبَت
> من نقطة النداء الحيّة لا من وجود الدالّة.** (طفرتان أسقطتا الاختبارات ثم أُرجعتا · 1331 اختبار.)

> 🥇 **دفعة صور فيصل JZ·CCHH·DRCT·ZCMD (2026-07-27، PR #193 · الكاتالوج §سادس عشر) — 3 قواعد
> جديدة نُفِّذت:** ① **🩸 نطاق سحب السيولة = 7%–13%** (CCHH نصًّا: «من القاع 1.30 سحب السيوله
> **متعارف عليه من 7٪ ل 13٪** · 1.30−10٪=1.17») — **كانت عتبتنا 5%** من حالة ONCO وحدها = أضيق من
> قاعدته المعلنة → `SPLIT_SWEEP_MIN/MID/MAX_PCT`=7/10/13 والصيّاد يعرض **نطاقًا** والأرجح −10%
> (تحقّق: 1.21→1.13 والأرجح 1.17 = رقمه حرفيًّا). ② **⚠️ القروب يُلغي الأهداف** (JZ: «الاصل هذي
> اهداف السهم **12 شمعه ساقطه** — لما يحصل قروب يرفع السهم المضارب **يلغي الاهداف** ويهبط فيه») —
> كنّا نرصد القروب (N1) ونعرض الأهداف بلا تحفّظ → `pump_voids_targets_line` بالكرت واليومي؛ و«12
> شمعة ساقطة» تأكيد مباشر لسلّم `_red_candle_heads`. ③ **📉 CCI(14)** موجود في **كل** شوارته (JZ
> −56.5 · CCHH +105.8 · DRCT −43.0) وكان ناقصًا → `cci`/`cci_state`/`cci_line` موصولة مثل كلنجر
> (مساند لا بوّابة). **تأكيدات:** «الخطوط تعتبر اهداف مضارب» · «ثبات إيجابية عدم ثبات سلبية» ·
> «ضغط المضارب لقاع جديد 1.88/1.99» (المسح/التلاعب) · الفجوة=هدف · تكرار التقسيم · تحذير الصين ·
> القياس بالأقران. 🔒 عرض/تحذير فقط · الجذور و`analyze_ticker` byte-identical · لا LOGIC_VERSION ·
> **1054 اختبار**. ⚠️ لم تصل: 0290/0291/0293/0294/0295/0298.

> 🥇🪝 **$ONCO — أول تحقّق مستقلّ من فيصل على سهم اختاره البوت + «خطة فيصل» (2026-07-24، PR #192 ·
> الكاتالوج §خامس عشر):** «صيّاد أسهم التقسيم» رشّح ONCO تلقائيًّا، **ثم حلّله فيصل ودخله وردّ على
> المستخدم** = أقوى دليل تحقّق بالمشروع. **مطابقتان حرفيتان تثبتان قرارَين سابقَين:** «1.44 أعلى شمعة
> بعد التقسيم ÷2 = 72 · وصل 71 سنت» = مِجَسّنا حرفيًّا (**يثبت `_post_split_high`** — لو بقي غلطي
> «إغلاق أول شمعة» لاختلف الرقم) · «شورت 150 الف» = «متاح للاقتراض 150K» (**يثبت أن شورت فيصل =
> Available من ChartExchange**). **القطعة الناقصة المبنيّة `faisal_split_plan`:** ① التحرر (أقرب
> مقاومة فوق السعر — «ثبات فوق 92 = الأمان») ② أهداف بنيوية (رؤوس الشموع الحمرا ثم المقاومات: 1.19
> «رأس الشمعة الساقطة» · 1.43) بدل «+100%» المجرّد ③ الفجوة 1.88→3 ④ سحب السيولة = القاع الفعلي −5%
> (`SPLIT_SWEEP_PCT`) + «الدخول مع المضارب انتظارًا». **⚠️ «تحرر» فيصل = أقرب حاجز استعادة ≠
> `liberation` بالفارز (أعلى مقاومة)** → الفارز لم يُمسّ. **⚠️ فيصل دخل رغم متاح 150ألف** (فوق 20ألف)
> = سياق سيناريو لا رفض مطلق. 🔒 خارج الجذور و`analyze_ticker` (SHA مطابق) · لا LOGIC_VERSION ·
> **1043 اختبار** (قفل على أرقام ONCO الحرفية).

> 📊 **نسبة الربح الحيّة 2025 = 25.9% (2026-07-24، تشغيلة `30081028228` · `backtest_2025_live_winrate.md`):**
> سؤال المستخدم «كم نسبة الدقة؟» → باكتيست السوق الكامل حيًّا على رأس `8c9612f`: **199 إشارة · 147 محسومة ·
> 38✅/109🛑 = 25.9%** (Wilson 19-33%) · العائد المحقّق متوسط −2%/وسيط −10% · **توقّع −0.2R** · حافة عريضة
> (أعلى صفقتين = 10% من الربح) · 6 أشهر موجبة من 11. **⚠️ تصحيح ذاتي: كنت ذكرت «40-50% تاريخي» من تشغيلات
> قديمة — المعتمد الآن 25.9%.** **تجربة الوقف الزوجية:** A (لمسة الذيل، الإنتاج) −1.7% مقابل B (الإغلاق)
> −3.1% ⇒ **B−A = −1.4 نقطة/صفقة، لا يُتبنّى** (يؤكّد «الوقف ليس الرافعة»). **ولا مؤشّر اختيار يميّز الرابح**
> (نواقص −30ن مقلوبة · جاهزية 27% مقابل 23% · بصمة 29/30/14% · FSTO 25/30% — كلها فواصل متداخلة) وتوصية
> الباكتيست الحرفية: «الحلّ = محور مُثبَت (**سلوك المضارب**) لا بوابة صفرية» = **التأكيد العاشر: الحافة =
> التوقيت**. **حدود صدق:** الباكتيست أعمى عن المضارب بنيويًّا (تدفق Polygon لحظي بلا تاريخ · كل دواله خارج
> `backtest_symbol`) + انحياز بقاء + **تشويه تقسيمات صريح** (WOK دخول 578,860 · YYGH 494 · WNW 268) ⇒
> **25.9% أرضية متحفّظة لا سقف**. قياس فقط · صفر مسّ كود.

> 🩺 **لوحة «حالة جمع بيانات المضارب» + تشخيص جمع البيانات (2026-07-24، PR #187):** طلب المستخدم «اكيد
> بننسى» → ربط جمع بيانات المضارب بتقرير التطوير الأسبوعي. **`_collection_health_block` (module-level):**
> عدّادات تظهر **دائمًا حتى عند الصفر** (رادار الانطلاق: عدد الإطلاقات+آخره · حصّاد اليد: عدد المسحات ·
> جلسات E2 المكتملة N/20) في **مسارَي** العيّنة القليلة/الكافية — كان الفخّ: البلوكات التفصيلية تختفي عند
> فراغ سجلّها + التذكيرات ما تظهر إلا عند ≥10 صفقة محسومة (معكوس). **⚠️ تشخيص حاسم (لا تُنسى):** الـ«فشل»
> الأحمر على `ignition.yml` منذ 2026-07-15 = **بوّابة قياس E2 الصارمة فقط** (تصميم Codex: اكتمال القياس ≠
> استمرار الرادار)، **مو رادار معطّل** — مقطعا `open`/`close` ينجحان والسجلّ يُدفع قبل البوّابة. للفحص:
> انظر المقاطع لا الassembler. **الحالة الحيّة:** 3 إطلاقات (آخرها 2026-07-14) · 0 مسح حصّاد · 0/20 جلسة
> E2 = أحداث نادرة تتراكم للأمام (الحكم على «المضارب يرفع الدقّة» يحتاج 8 محسوم/20 مسح). عرض/تذكير فقط ·
> جذور byte-identical · لا LOGIC_VERSION · 1032 اختبار.
> **⏰ تصحيح تغطية الحصّاد (2026-07-24، طلب المستخدم «تأكد انهم يجمعون» — PR #191):** فحص أول يوم جمع كشف
> فجوتين: **(1) الافتتاح غير مغطّى** (أول تشغيل فعلي 15:46 والنظرة 90د تصل 14:16 فقط، بينما افتتاح الصيف
> 13:30 = أعلى نافذة مسح) · **(2) الدقيقة المستديرة تُسقَط** (5 تشغيلات فقط من 16 فتحة — نفس درس الكرون
> الموثّق). الكرون صار `13,43 13-21 * * 1-5`. **+ عدّادات تشخيصية** بسجلّ الحصّاد («تغطية الفحص: N نشط ·
> قرب الدعم M · بعيد/بلا شموع/مكرّر/بلا دعم») تفرّق «فحصنا ولم نجد» عن «لم نفحص شيئًا» (عطل صامت).
> **✅ مُثبَت بمحاكاة كاملة:** الأنبوب يلتقط المسح ويسجّل بصمة الامتصاص (`bid_block`) صحيحًا — فـ«0 مسح»
> ندرةُ حدث لا عطل. **والمفاتيح صحيحة** (`pivot`/`status:"active"` مؤكَّدة من `make_watch_entry`).
> الرادار سليم (111 لفّة · 8 رموز · E2 summary مدفوع). 1035 اختبار.

> 🪝 **صيّاد أسهم التقسيم — أداة مستقلة (2026-07-24، PR #179/#182):** طلب المستخدم أداة تختلف عن نمط
> البوت كامل، تمسح يوميًّا بعد الإغلاق وترسل المقسّم اللي يطابق كل شروط فيصل. `split_hunter.py`+`split_hunter.yml`
> (cron `17 22 * * 1-5`) · `scan_split_hunter` **5 شروط صارمة** (مقسّم عكسي+÷2+حافظ3ج+فلوت<2م ياهو+خالٍ من
> قروب) → يرسل المطابق الكامل فقط (صامت غير ذلك). المتاح CE<20ألف+الرسوم+متوسطات 20/30/50 = سياق (قرار
> المستخدم «أساسي موثوق + المتاح سياق»). **🔬 مِجَسّ Actions حاسم:** (1) الرادار/الصيّاد ياخذ الشورت من CE
> Available لا FINRA (تصحيح المستخدم، PR #179؛ M13 الفرزي FINRA يبقى قفل C3). (2) **CE overview مات للفلوت**
> (قِشرة JS 1035 حرفًا) → البديل **فلوت ياهو** `_yahoo_float` (مثبَت: JEM=93,759). (3) **JEM فلوته 94ألف = تحت
> 2م ✅ = يحقّق شرط فيصل فعلًا** (كنّا نستخدم CE الميت)؛ الفرز الآلي يرفضه (M4/M2) فهو **مثال تعليمي للصيد لا
> هدف إجبار** (C3). 🔒 خارج الجذور السبعة · لا LOGIC_VERSION · فاشل-آمن · 1025 اختبار.
> **⚖️ تصحيح ذاتي حاسم (2026-07-24، فرع `claude/golden-fixes`):** كنت غيّرت مرجع الـ÷2 في `_split_setup_probe`
> من `_post_split_high` (قمة ما بعد التقسيم) إلى `_split_day_value` (إغلاق أول شمعة) ظنًّا أنه «قيمة الشمعة»
> الفيصلية — **والتحقّق الحيّ كشف الخطأ**: JEM الموثّق = **6.90 ÷2 = 3.45** (قمة ما بعد التقسيم)، بينما إغلاق
> أول شمعة = 6.05 (يعطي ÷2 خاطئ 3.03). **أُرجِع المرجع للقمة `_post_split_high` (كان صحيحًا أصلًا)**، وصار
> `_split_day_value` قاعدةَ **معيار «لم يصعد بعد التقسيم» `didnt_rise`** (فيصل IMG_0150 «قسم ما أعطى صعود»:
> صعود القمة عن أول إغلاق أكثر من `SPLIT_ROSE_MAX_PCT`=50% = انضخّ، مو هادئًا) — **بوّابة صارمة سادسة في
> الصيّاد**. تحقّق JEM: ref=6.90 · half=3.45 · didnt_rise=True (صعد 14% فقط). 🔒 الجذور السبعة byte-identical
> (SHA مطابق) · لا LOGIC_VERSION · **1031 اختبار**.

> 🎯 **رادار أسهم التقسيم + تجربة «M2 مرجع ما بعد التقسيم» (2026-07-23، صور فيصل IMG_0143/0144/0149-0153
> · PR #172/#173/#174 على `main` · جذور byte-identical · لا LOGIC_VERSION):** سؤال «ليش ما يجيب JEM؟».
> **التجربة `BT_SPLIT_REF_M2`** (علم باكتيست + `_post_split_high` + `m2_split_ref_prereg.md`): M2 يقيس الهبوط
> من قمة ما بعد آخر تقسيم عكسي لا قمة 52أ. **النتيجة (لقطة `29935819997`):** الدالة **استخرجت مرجع فيصل
> الحرفي** (JEM 6.90→50% = القمة÷2 · WORX 2.33÷2=1.16) فـM2 يمرّره — **لكن JEM = 0 إشارة** (M4 القاعدة
> المنفوخة ترفضه · M4-الواعية no-op · الوسطى <40%؛ حتى بالعلمين معًا). **الشرط الضروري فشل → التأكيد التاسع
> «الحافة = التوقيت لا الاختيار».** **السبب (IMG_0151):** setup فيصل للمقسّم recipe ضيّق مستقل (فلوت<2م ·
> شورت<20ألف · حافظ 3ج · ÷2 · خالٍ من قروب) ≠ بوابات البوت العامة؛ إجباره عبر الاختيار = خطر C3 (80%→17%).
> **القرار (المستخدم): `BT_SPLIT_REF_M2` يبقى علم باكتيست، وبُني «🎯 رادار أسهم التقسيم» عرض/سياق فقط**
> (`_split_setup_probe`+`scan_split_radar`+`build_split_radar_section`، رسالة تلغرام مستقلة، بوّابة تكلفة
> [مُرشّح OHLCV رخيص→جلب مقيَّد]، CONFIG `SPLIT_RADAR_*`) **خارج الجذور السبعة · 1016 اختبار** — يرى المستخدم
> JEM/WORX ولا يفوته والنجاح الآلي محميّ. #173 أصلح التشخيص (كان يُستدعى قبل ضبط سياق splits). **✅ معايَر
> بـdry-run على اللقطة الكاملة (`split_radar_check.py`/`.yml` · PR #176/#177): القمع 3367→137→53→20→عرض 12؛
> أُضيف `SPLIT_RADAR_BAND_LOW=0.70` بعد أن كشف الفحص أسهمًا اخترقت ÷2 لتحت (HAO/AUUD/OIO = انهيار لا ثبات)
> فأسقطها؛ ظهر PAVS/CHSN/HUBC.** ⚠️ عتبتا كليف 45%/سعر<10 قابلتان للضبط حيًّا. مؤشّرات الصور:
> KST(10,15,20,30,10,10,10,15,9) مؤكَّد · MACD(10,20,3) بشارت WORX (لم يُمسّ).

> 🔬 **إعادة تحقّق أسهم فيصل الأربعة point-in-time (2026-07-23، لقطة `29935819997` · باكتيست فقط):**
> سؤال المستخدم «هل تتغيّر نتيجة AZI/DSY/EHGO/ZCMD بعد تعديلاتنا؟» → **لا: 0 إشارة، كلهم مرفوضون، أساس
> ومع M4-split معًا** (فرق ±1 عدّ) = برهان أن الأهداف الزرقاء **عرض بحت** (الفرز byte-identical). تنقيح
> «M5 فقط»: **M4 (عرض القاعدة) + M2 (نافذة الهبوط) غالبان، M5 ثانوي** (عدّ: AZI M4=33 · DSY M4=61 ·
> EHGO M5=35/M4=28 · ZCMD M2=63). M4-split لا يُنقذ حتى المقسّمة (نافذة 15ج أقصر من التقسيم). **إصلاح:**
> «رموز + لقطة مجمّدة» كانت لا تتركّب (التجميد يدهس الرموز = يمسح السوق كله) → أُصلح (تنسيق باكتيست، خارج
> الجذور، لا LOGIC_VERSION، 968 اختبار). التفاصيل: CLAUDE.md §«تأكيد رابع».

> 🥇 **توليفة فيصل «الذهبية» (IMG_0076) — نفي: ذهب مزيّف (2026-07-23، فرع `claude/audit-fixes` ·
> بحث فقط · صفر مسّ Super_stock.py):** المستخدم أرسل تغريدة فيصل «سهم مقسّم · متوسط حركة 30<50 · قاعدة
> 3ج · شورت<20ألف · فلوت<2م · هدف 100% · خالٍ من قروبات» («ذهب خالص») + اختار **اختبار تمييزي على
> السوق كامل**. **⚠️ تصحيح حاسم (صور IMG_0091/0094/0095/0096، بحث `from:kisar_ 30/50`):** «30/50» =
> **المتوسط الأسّي EMA 30/50** («متوسط الأسّي 30-50 يوم») لا التذبذب اليومي — كان فهمي الأول خطأً،
> المستخدم أمسكه. **القابل للقياس (السكربت `faisal_combo_backtest.py` + `faisal_combo.yml` + prereg):**
> مقسّم عكسي (بديل الفلوت) + EMA30/50 هابط (السعر تحت 20≤30≤50) + قاعدة3ج. النتيجة على لقطة
> `29935819997` (104,124 نقطة): بمقياس «انفجر ≥100%» بدت 2.02× (اجتازت N/Wilson/LOYO/Bonferroni) —
> **لكن المقياس القابل للتنفيذ (هدف+100% قبل وقف−50%، الوقف أولًا) قتلها:** نسبة نجاح التوليفة **41.5%
> < الأساس 45.3%** (رفع 0.92×) ومعدّل الانهيار **24% مقابل 9%** (رفع 2.62×). **نفي: تذبذب عنيف
> بالاتجاهين أثقل للأسفل، لا حافة** — «الانفجار 2×» بريقُ (قياس بلا وقف + انحياز البقاء). **قائمة فيصل
> الميكانيكية ليست حافة ميكانيكية؛ الذهب في يده (إدارة يدوية) لا في الشيك ليست. تأكيد خامس: الحافة =
> التوقيت لا الاختيار.** درس أداة: المقياس القابل للتنفيذ + عدم التماثل (نجاح مقابل انهيار) يفصلان الحافة عن التذبذب.
>
> **🖐️ متابعة — ميكانيكا فيصل (2026-07-23، المستخدم لم يقتنع بوقف −50%؛ وقف فيصل 5-7% تحت الدعم،
> R:R ~14:1): اكتشاف إيجابي.** `sweep_entry_outcome` (دخول بعد مسح-استعادة + وقف بنيوي + هدف 100%،
> القياس بالتوقّع R): **توقّع التوليفة موجب (R>0) — تداولها بطريقة فيصل يربح فعلًا!** لكن **ليس أعلى من
> الأساس ولا متّسقًا 3 سنوات** ⇒ نفي على «التوليفة تتفوّق». **المعنى البنّاء: الحافة في الطريقة (يد فيصل:
> المسح-الاستعادة + الوقف البنيوي + R:R العالي) لا الاختيار — تنطبق على أي فرصة.** جواب «هل نطلع بحل؟»:
> **نعم — البوت يملك يد فيصل مُقنَّنة** (رادار الانطلاق + الدفعات + الوقف البنيوي + سلّم الأهداف)، وهي الآن
> **مُثبَتة حافةً موجبة التوقّع بالدليل**. الحل = شحذ طبقة التوقيت الموجودة لا توليفة/AI منفصل. تأكيد سادس بنّاء.
>
> **🤖 «اليد الاصطناعية» — المرحلة 0 ضوء أخضر (2026-07-23، `AI_HAND_FEASIBILITY.md`، بحث/جدوى فقط):**
> طلب المستخدم استكشاف وكيل AI ينوب عن يد فيصل. الأمانة: وكيل RL كامل الآن=فخّ (لا تدفق تاريخي · أحداث
> نادرة · انحياز بقاء · لا أمثلة خبير). المسار 3 مراحل. **المرحلة 0 (`FC_LEARN=1`، logistic نقيّة بـnumpy،
> تدريب 2023-24/اختبار 2025):** نجاح الربع الأعلى **OOS 14.7% مقابل أساس 8.5% = رفع 1.72× خارج العيّنة**،
> وداخل التدريب 13.4% ≈ OOS ⇒ **عمّم ولم يحفظ (أول ضوء أخضر OOS بالجلسة)**. سقف أدنى (يومي فقط بلا تدفق
> لحظي — جوهر «المسح مقابل الكسر=الامتصاص» لم يدخل). **⏳ المرحلة 1 (بموافقة المستخدم): حصّاد تدفق حيّ**
> يسجّل كل مسح يراه الرادار + ميزات الامتصاص (Polygon) + النتيجة → دفعة معنونة تنقل النموذج لحافته. صفر مسّ إنتاج.

> 🔬 **تجربة M4 واعية للتقسيم — مُطلَقة، الحكم مُعلَّق (2026-07-22، فرع `claude/audit-fixes` · باكتيست
> فقط · الإنتاج byte-identical · لا LOGIC_VERSION · 968 اختبار خروج 0):** التشخيص الشامل
> (`faisal_gate_audit_report.md`) أثبت أن **M4 (عرض القاعدة أوسع من 40%) هو الحاجب الأكبر لأسهم فيصل —
> 28 من 47** (M2 فخّ التقسيم 8 · M3 4 · M5 4). طلب المستخدم «طبّق اكتشاف M2 على باقي البوابات». **الفرضية:**
> تقسيم عكسي **داخل نافذة القاعدة (15ج)** ينفخ الشموع الأقدم نسبةً للأحدث ⇒ «قاعدة واسعة» زائفة — نفس فخّ M2.
> **البناء:** `_split_aware_base_range(highs,lows,splits,cut)` (نقيّة فاشلة-آمنة، de-inflate النافذة، ≤cut
> بلا تسريب) + علم `BT_SPLIT_AWARE_M4` (CONFIG+env+workflow input) + فرع في analyze_ticker M4 خلف العلم+السياق
> (نمط M2 حرفيًّا) + run_backtest يضبط `_BT_SPLITS_CTX` عند أي علم. **التسجيل المسبق `m4_split_aware_prereg.md`**
> (المعيار قبل الأرقام): يُعتمد فقط لو إشارات جديدة ≥10 **و** Wilson لنجاحها ≥ الأساس **و** الاتجاه متّسق عبر
> 2023+2024+2025. **🔒 العلم مطفأ+السياق None ⇒ الإنتاج byte-identical** (7 جذور اختيار SHA مطابقة تجاه
> origin/main · analyze_ticker استثناء مُصرَّح). **⚖️ الحكم (باكتيست 3 سنوات على `29935819997`): نفي — no-op.**
> 2025 الذراعان **متطابقان byte-identical** (26/132 كلاهما) · 2024 أسقط إشارة خاسرة واحدة فقط (+0.3 نقطة) ·
> 2023 التجربة=22/91 (BASE أُعيد إطلاقه، فرق ~0). **تغيير الإشارات 0-1 فقط** (المعيار طلب ≥10) → فشل العتبة.
> **السبب:** نافذة M4=15ج فقط، فالتقسيم نادرًا يقع داخلها، وحتى حينها القاعدة de-inflated **تبقى >40%** (اتساع
> حقيقي). **أسهم فيصل الـ28 المحجوبة على M4 قواعدها واسعة فعلًا لا مُنفَّخة — اكتشاف M2 خاصٌّ بقمة الـ52أ ولا
> ينتقل لـM4.** M4 يبقى مقفولًا (C3/B3). تأكيد رابع: الحافة=التوقيت لا الاختيار. التفاصيل: `m4_split_aware_prereg.md`.
> ⚠️ تباين مرجع 2025 (26/132=19.7% هنا مقابل 18/61=30% بجدول M2): اللقطة الممتدّة تعطي lookback كامل فتتأهّل
> إشارات أكثر — الذراعان يتّفقان (ليس أثر ذراع) — يكشف أن صفّ 2025 بجدول M2 كان على لقطة مبتورة (حكم M2 نفي، لا يتأثّر).

> 🎯 **إعادة بناء الأهداف الكبرى + قرائن N6/N7 (2026-07-20، فرع `claude/audit-fixes` — 3 كوميتات
> b455813/4b21842/b696825 · 928 اختبار خروج 0 · لم يُدمج main):** شكوى المستخدم «الأهداف سنتات و
> تافهه — كبّرها إلا التحرر». الآلية المستخلَصة (DXST/TRUG/SPRC): الأهداف = المستويات الكبرى للدورة
> الكاملة، متباعدة بفجوات معنوية لا سنتات، بمرساة = قمة الدورة (hi52) لا سقف 2×، وفيب النطاق الكامل
> (pivot→hi52). **التنفيذ:** كتلة t2/t3 **إضافية بعد t1** في `analyze_ticker` (+مرآتها في
> `analyze_one.py`) · `TARGET_MAJOR_GAP_PCT=12`/`TARGET_ANCHOR_HEADROOM_PCT=5` · `refine_targets_4h`
> بالفجوة الكبرى · **LOGIC_VERSION مرفوع** (`…cycletargets…`). **🔒 t1/التحرر/rr/التصنيف/العضوية
> byte-identical** (مقارنة 8 بذور ضد origin/main + قفل getsource: الكتلة لا تعيد تعيين t1/rr/soft_fails؛
> جذور الاختيار السبعة بصمة SHA-256 مطابقة). t2/t3 اتّسعت من ~4% (سنتات) إلى 13-16%. الباكتيست الثلاثي
> (t1-hit/mg_pre_stop/الأيام) ثابت بنيويًّا (t1 لم يتغيّر). **N6/N7 (فحص اليد فقط، عرض/تحذير):** N6 شراء
> الإغلاق CP (طبعات محايدة التيك يعمى عنها `_operator_blocks`) · N7 طبعات آلية خارج NBBO باقتباس طازج ≤5ث ·
> `_flow_prints` نقيّة من صفقات polygon_flow (صفر نداء إضافي، `with_prints=False` افتراضي=byte-identical).
> **⚠️ N6 لم يُخمَّن رمز SIP** (يلزمه `/v3/reference/conditions`+مفتاح المالك؛ 403 من التطوير). التقرير:
> `targets_and_operator_report.md`. **⏳ للمالك: (1) شغّل `force_renew=1` مرة · (2) تحقّق DXST/TRUG/NAMI
> حيًّا بأداة التحليل — الأهداف الجديدة تطابق سلالم فيصل (5/7 · 2/2.30/3.50)، التحرر لم يتغيّر.**
> **🎨 المرحلة 2ب (2026-07-20، «ابي الثنتين و توضح» + صور FUSE/PPBT/ELAB لمفتاح ألوان فيصل):** مفتاح
> فيصل الموثّق (رأس الكتالوج): ⚫ مقاومة · 🔵 هدف بلا مقاومة (فراغ فوقه → يطير له). **t2=أقرب مستوى ·
> t3=القمة الكبيرة** (أعلى مستوى=قمة الدورة/فيب/فجوة) بدل ثالث-أقرب (نمط PPBT: قريبان ثم قفزة للقمة) ·
> **`targets_kind`[⚫/🔵]×3** يُعرض بالكرت/اليومي/الفحص/analyze_asof («🔵 نظيف»/«⚫ مقاومة»). LOGIC_VERSION
> `bluetargets`. 🔒 t1/التحرر/rr/العضوية byte-identical · جذور الاختيار SHA مطابقة. 933 اختبار. **أداة
> `analyze_asof.py`+workflow (مُسجّل على main) لفحص point-in-time أي سهم فيصل بتاريخ دخوله (البروتوكول
> المتّفق: المستخدم يرسل رمز+تاريخ قريب من دخول فيصل، أنا أفحص هل رشّحه البوت وقتها/بأي بوابة رفضه).**

> 🔄 **استمرارية القائمة عبر التجديد (2026-07-21، فرع `claude/audit-fixes` — عرض/تتبّع فقط · لا
> LOGIC_VERSION · 947 اختبار خروج 0):** شكوى المستخدم «PSTV وأسهم الأسبوع الماضي تختفي بصمت ولا أعرف
> مصيرها — لو كنت داخل فيها أبي أعرف وش صار (طلعت من الارتكاز؟ ضُربت الستوب؟)». الجذر: `run_weekly_renewal`
> كان **يستبدل `stocks` كاملةً** فالنشط غير المُعاد اختياره يُمحى. **الإصلاح (خيار المستخدم «إبقاء النشط +
> تقرير مصير الكل»):** الأسهم النشطة تُحمَل بوسم `cont_status` بدل المحو — **renewed** (أعاد التأهّل، يحفظ
> added/entry_ref) · **continues** (`analyze_ticker` يؤكّده ارتكازًا أو تعذّرت البيانات) · **exited** (لم يعد
> ارتكازًا، «⚠️ نتابعه لمركزك»). + **رسالة تلغرام مستقلة `build_fate_report`** («🔄 مصير أسهم الأسبوع الماضي»)
> + وسم بالكرت/اليومي · سقف `CONTINUITY_MAX`=30 (يُقلّم الأقدم «خرج» أولًا) · المُتخرِّج للارتداد يُستثنى (لا
> ازدواج). **🔒 جذور الاختيار السبعة byte-identical (بصمة SHA) · القرار الفرزي بلا تغيير.** لم يُدمج main بعد.
>
> 🔄 **متابعة بلا حدّ زمني + قسم يومي «متابعة لمركزك» (2026-07-21، تكملة — طلب المستخدم «ما ابي على
> أسبوع، أبي أي ارتكاز دخلت فيه يستمر في القائمة الين يصير له شي جديد»، 955 اختبار):** بعد إصلاح
> الاستمرارية، وضّح المستخدم أن المتابعة يجب ألّا تكون أسبوعًا واحدًا. **(1)** أُزيل حدّ السعة الزمني:
> `CONTINUITY_MAX` رُفِع 30→60 وصار **صمّام أمان أقصى فقط** — السهم النشط يُتابَع **الين يُضرب ستوبه أو
> يعود للترشيح** (حدث حقيقي، مطابقةً لقاعدة «تُشطب بالستوب فقط»)، لا يُسقَط بالسعة/الوقت. **(2)** قسم يومي
> دائم `build_position_watch_section` «🔄 متابعة لمركزك» في `build_daily_message`: يفصل المحمولة
> (continues/exited) عن قائمة الترشيح ويعرضها بحالتها/سعرها/ستوبها/الرقم-الحرج **كل يوم حتى بوضع الجاهز-فقط**
> (متابعة المركز أهمّ من اختصار الإشعار) — الين تُضرب ستوب أو تعود للترشيح فتخرج ذاتيًّا. 🔒 عرض/تتبّع ·
> جذور الاختيار byte-identical · cont_status ينجو migrate/update (تحديث بمكانه).
> **✅ دُمج في main (2026-07-21) مع أهداف/N6-N7/الاستمرارية معًا — بيانات main الحيّة محفوظة.**
>
> 🔀 **الحالة (2026-07-15): E2 + كل إصلاحات التدقيق مدموجة على `main`** (merge commit `af9d400`
> عبر PR #168 — **كود فقط · بيانات main الحيّة محفوظة**): نظام قياس E2 الكامل + إصلاحات التدقيق
> (①ب حجر الملف التالف · P1-8 إخفاء التوكن من السجلّ · P1-2 فصل رؤوس الحمرا الأسبوعية عن t1 ·
> P0-2 تأجيل حسم التقسيم غير الموثوق في `update_tracking`+`update_watchlist_status` · توثيق P1-3).
> **بوابة صلبة مُتحقَّقة (محليًّا + CI):** بصمة SHA-256 لكل الجذور الثمانية (rank_key·select_top·
> classify_tier·entry_status·analyze_ticker·backtest_symbol·بوّابتا الشورت والفلوت) مطابقة بايت-بايت
> لـmain قبل الدمج · `LOGIC_VERSION` ثابت · `test_bot.py` 874/0 · حارس `git_save` + استرجاع
> `ignition_universe.json` محفوظان (إصلاح الحادثة سليم). **PR #167 مُغلَق** (فائض — محتواه على main).
> ⚠️ **دمج الكود ≠ تشغيل Pilot:** علم `E2_MEASUREMENT` مطفأ افتراضيًا فرادار الانطلاق **بت-بت في
> الإنتاج**؛ تشغيل Pilot فعليًّا (رفع العلم لجلسة حيّة) يبقى **بوابة منفصلة تحتاج موافقة المالك** —
> لم يتغيّر سلوك الإنتاج. **⏳ مؤجَّلان بقرار موثّق (لا عطل):** **P0-3** (محدّد المقياس `_scale_divisor`
> — يمسّ قفل C3 + يحتاج باكتيست) · **②-معلّق** (عتبة «فشل مصدر أرباح» تعسّفية + ثوابت مخطّط الحجر
> — قرار المالك بالدليل). فرعا العمل: `claude/audit-fixes` + `claude/main-safety` (محفوظان مرجعًا).
> **⚖️ مُقفَل (2026-07-15 مساءً): حكم «الرابط المشترك» 3 سنوات — لا رابط.** معيار مسجَّل مسبقًا
> (`bt_features_prereg.md` a0f18ca قبل النتائج) على 2023/2024/2025: **574 مؤهَّلًا · 74 منفجرًا** —
> لا قطاع يستوفي (Bonferroni كلها تشمل الصفر · Technology 32%→12% انقلبت بLOYO = ضجيج سنة واحدة) ·
> الأرباح بلا as-of (فارغة 100%). حدّ مُصرَّح: انحياز البقاء. التقارير + الأداة (`BT_FEATURES` ·
> `start_override`) على `claude/audit-fixes` (ef56ae0). **الحافة = التوقيت لا الاختيار — لا إعادة
> فتح بلا point-in-time + as-of + تسجيل جديد + موافقة.** التفصيل: CLAUDE.md (بند الحكم تحت T-ACC).

> 🔬 **E2-A (قياس التوقيت اللحظي) منفَّذ ومدفوع — فرع مستقل `claude/e2a-ignition-measurement` من 49f3f0d
> (2026-07-13، مواصفة Codex، 812 اختبار · لا LOGIC_VERSION · لا مسّ جذور · لا معايرة):** حوّل رادار
> الانطلاق من «يسجّل الناجح للإرسال» إلى **تجربة prospective تسجّل كل مراحل القرار + التغطية + التنفيذ +
> التوقيت** دون تغيير تنبيه واحد. **① instrumentation ظلّي** (`scan_ignition(trace=None)` + `_emit_trace`
> بحمولة lazy = بت-بت عند None؛ اختبار تكافؤ + استثناء trace آمن + نص Telegram متطابق) + funnel كامل +
> تصحيح overclaim (E2-F09). **②** `ignition_measurement.py` (recorder فاشل-آمن · crash-safe · لا أسرار):
> exposure حقيقي/symbol-session · candidate لكل raw فريد حتى المكبوت (NBBO/توقيت/بوّابة) · emitted≠delivered ·
> مسار الدقيقة. **③** توصيل `ignition_live` خلف `E2_MEASUREMENT=1` (مطفأ=حرفيّ) + try/finally + telegram
> events · `ignition.yml` artifact if:always. **④** `E2_IGNITION_PREREGISTRATION_2026-07-13.yaml` (يستبعد
> GEOS) + `E2_IGNITION_SCHEMA.md` + `ignition_e2_analyze.py` (تدقيق تغطية فقط). **التوقيت فرضية غير مثبتة —
> لا نتيجة قبل ≥5 جلسات كاملة (E2-A) ثم E2-B الوصفي بموافقة المستخدم.** التقرير الكامل (15 بندًا):
> `CLAUDE_E2A_IMPLEMENTATION_REPORT.md`. **⏳ متبقٍّ: تشغيل حيّ على Actions يؤكّد التغطية (لم يُشغَّل من بيئة
> التطوير — Polygon محجوب) · مراجعة Codex للـPR · دمج بموافقة المستخدم.**

> 🔬 **E2-A — إصلاحات فجوات مراجعة Codex الثانية (2026-07-13، فرع نظيف `claude/e2a-measurement-clean`
> من أحدث `origin/main`، `schema_version`=2، 793 اختبار · قياس فقط · لا LOGIC_VERSION · لا تغيير تنبيه/
> عتبة/اختيار · لا معايرة):** Codex راجع `5708fdeb` وطلب 6 خطوات. **الخطوة 1 (فرع نظيف):** cherry-pick
> الكوميتات الخمسة على `origin/main` فالـPR = ملفّات E2-A **فقط** (بلا Phase C/E/P0 أو تاريخ جانبي — مؤكَّد
> `git diff --stat`). **الخطوة 2 (5 فجوات):** **2a** إنهاء ديناميكي (`_session_window`: الأبكر من الإغلاق
> الفعلي/بدء الجوب+`IGNITION_MAX_RUNTIME_MIN`=335/تجاوز صريح؛ أُزيل مثبّت 19:20؛ timeout 355؛ session.json
> يسجّل الإغلاق المتوقّع + علم «انتهت قبله») · **2b** `backfill_emitted` يردم مسار الدقيقة بعد التنبيه ·
> **2c** `trigger_bar_start` صريح + end=start+60000 + `bar_is_closed` حتمي · **2d** توحيد طابع NBBO (نانو→
> ملّي) + `quote_age_ms` + **executable = صالح ‏و‏ طازج ≤5ث** · **2e** `exposure_minutes` + أهلية recall
> بالشروط الثلاثة + مدقّق يرفض «مكتملة» عند فقد مسار/إغلاق مبكّر/عدم توازن دورات/تناقض emitted-delivered/
> نقص طابع مقفول. **🔴 قرار معماري مطروح على المالك/Codex:** سقف رنر GitHub 6س < جلسة 6.5س ⇒ جوب واحد من
> 13:35 لا يبلغ الإغلاق (20:00/21:00) — التغطية جزئية صريحة والمدقّق يرفضها كمكتملة؛ الخيارات (كرون أمتّ
> يضحّي بالافتتاح / جوبان متسلسلان / تعريف «مكتملة» = «نافذتها المقصودة بلا انقطاع»). **⏳ متبقٍّ: دفع الفرع
> + Draft PR + مراجعة Codex (الخطوات 3-5) · ثم Pilot واحد (الخطوة 6) بعد مراجعة نظيفة — لا confirmatory قبل
> الدمج.** التفاصيل: `CLAUDE_E2A_IMPLEMENTATION_REPORT.md §16`.

> 🔬 **E2 — المعمارية المجزّأة (ب+، 2026-07-13، مراجعة Codex الثالثة REQUEST CHANGES على `7e98422` ·
> 801 اختبار · قياس فقط · بت-بت في الإنتاج · لا LOGIC_VERSION):** Codex قبِل جوهر القياس لكن حجب Pilot الرأس
> السابق بـ**3 موانع P0** واختار **(ب+): جوبان متسلسلان + assembler** (رفض «أ» يضحّي بالافتتاح و«ج» كتعريف
> للجلسة لأن التسجيل المسبق يقفل regular_hours حتى الإغلاق). **نُفِّذ بالكامل:** `ignition.yml` صار **3 jobs**
> `open_segment`→`close_segment`(needs)→`assemble_e2_session`(needs). `close_segment` يستعيد أختام الدِدوب من
> handoff (`_apply_handoff_dedup` → **لا تنبيه Telegram مكرّر** عبر المقطعين، الاستثناء الإنتاجي الوحيد المقصود،
> مقفول باختبار). **P0-1** `_segment_window` يحسب الحدود **من الافتتاح الفعلي** لا بدء الرنر · **P0-2** الردم
> النهائي بعد الإغلاق في `ignition_e2_assemble.py` (يدمج bars/candidates/deliveries + counters `min/max` +
> backfill) · **P0-3** المدقّق يشترط **وصول المسار للإغلاق** (`path_last_bar ≥ close−3د`). **P1 (كلها):**
> `backfill_status` · **NBBO قياسي مستقلّ** `polygon_nbbo`/`fetch_measure_nbbo` (**قفل: لا يمسّ القرار**،
> measurement مفضَّل) + `operator_nbbo_*`/`nbbo_source` · watchlist provenance · latency · أثر الأداة
> (`instrumentation_timing` median/p95). المدقّق: `kind=segment|assembled|single`؛ **`session_complete`
> (الجزآن + المسار للإغلاق) وحده يعدّ نحو 5/20** · `segment_complete` لا. ملفّات جديدة: `ignition_e2_assemble.py`
> + `E2_IGNITION_SEGMENTED_ARCH.md`. **⏳ متبقٍّ: دفع الفرع + تحديث PR #167 + مراجعة Codex الرابعة على الرأس
> الجديد · ثم Pilot واحد `excluded_from_confirmatory` (بموافقته + المالك) — لا Pilot على رأسٍ لم يُراجَع.**
> التفاصيل: `CLAUDE_E2A_IMPLEMENTATION_REPORT.md §17`.

> 🔬 **E2 — مراجعة Codex الرابعة (2026-07-13، REQUEST CHANGES على `ae0b325` — 6 موانع P0 + 9 P1، كلها
> نُفِّذت · `schema_version`=3 · 815 اختبار · قياس فقط · scan_ignition بت-بت في الإنتاج · لا LOGIC_VERSION):**
> **P0-1 (حاسم):** جلب NBBO القياسي كان **تزامنيًّا** في scan_ignition قبل التنبيه (حتى 8ث تأخير — سلوك
> توقيت لا membership) — أُزيل كليًّا؛ المسجّل يجلبه **لا-تزامنيًّا** (worker+queue آمن-خيوط بقفل) ويربطه
> بـcandidate_id. **اختبار: جالب 1.2ث لا يؤخّر التنبيه («400ms).** الصلاحية من طابع المصدر لا انتهاء HTTP.
> **P0-2** كرون 13:18 (قبل افتتاح الصيف) + بوّابة `first_successful_poll ≤ open+2د`. **P0-3** `transition_gap_ms`
> مقاس ومقفول (>10د يرفض) + exposure = مجموع فترات المقاطع (لا max−min). **P0-4** `ignition_e2_manifest.py`:
> manifest مقفول (SHA-256 حقيقي) + سلسلة تحقّق + close **fail-closed** + الassembler يتحقّق + اختبارات عبث.
> **P0-5** المدقّق يفرض اكتمال كل مقطع + بدء open + الفجوة + السلسلة + لا تسليم مكرّر. **P0-6** `--strict`
> (خروج غير صفر) بلا `|| true`. **P1:** دمج candidate field-wise (لا يُسقط emitted) · cadence مطلقة · تفكيك
> latency · watchlist file SHA · NBBO status لكل cohorts · `market_calendar.py` (عطلات/إغلاق مبكر مثبَّت
> الإصدار) · دفع واحد من الassembler · provenance صادق · schema gate موسَّع. ملفّان جديدان:
> `ignition_e2_manifest.py` + `market_calendar.py`. **⏳ متبقٍّ: دفع + تحديث PR #167 + مراجعة Codex الخامسة ·
> ثم Pilot واحد (بموافقته + المالك).** التفاصيل: `CLAUDE_E2A_IMPLEMENTATION_REPORT.md §18`.

> 🏦 **حكم «قوة البوت» بالمحور المصحَّح — اكتمل ووُثّق (2026-07-12، PR #161 sha 922aee2 مُدمَج ·
> شُغِّل السوق الكامل 3562 رمز/سنة · `MARKET_BACKTEST_FINDINGS §0-م`):** الحكم = كم انفجر كل سهم
> من الدخول قبل وقفه (لا t1). **الناجحون (انفجر 50%+ قبل الوقف): 10% (2025) · 8% (2026)** · ~1 من
> 5 يعطي 30%+ متاح · وسيط +6/+10% (ذيل يحمل الحافة). **المعيار المسجَّل: حدّي-إلى-ضعيف** (أرضية لا
> سقف). **🔪 أخطر اكتشاف متطابق بالسنتين: الوقف الثابت يُمسَح ثم ينفجر السهم = 10 انفجارات/سنتين**
> (لو التُقط المسح: +50-67% التقاط). **تمييز المنفجر: صفر من الجاهزية/النقاط/النواقص (النقاط عكسي!)
> — بل الصبر (ذروة يوم 17 مقابل 2) وشريحة السعر $2-5 (انفجار 21%).** المحفظة top-15 لا تضيف (تأكيد
> رابع لتقاعد A/B). البوت فارز ناجح يصطاد انفجارات حقيقية (ENVB +321·APPS +257·INLF +155) وحافته
> **التوقيت اللحظي لا الفرز**. لا تغيير إنتاجي (جذور مقفولة C3). **🎯 استرداد المسح (بُني mg_sweep_*
> + كتلة الاسترداد، شُغِّل السنتين إصدار 5c123a3): الدخول-بعد-المسح اليومي يسترد 0 من 10 انفجارات
> مقتولة** (المقياس الدقيق mg_sweep؛ حتى BTOG لمس t1 +23 لكن لم يصمد ≥50 قبل مسحٍ ثانٍ). السبب بنيوي:
> الاستعادة اليومية متأخّرة/عالية (AIRE 11→21 · QNTM فوق الهدف) → RR منهار/مسح مزدوج. **تصحيحان
> بالأرقام: توقّع «يسترد عدّة» خطأ · وتقدير يدوي «1/10» over-count — الدقيق 0/10.** حافة المسح **لحظية
> فقط** (كنسة الدقيقة المبنية؛ اليومي عاجز بنيويًا). المهمة مكتملة (§0-م). PR #161+#162+#163 مُدمَجة.

> 🏦 **باكتيست قوة البوت — البناء (بنود 1-5/6) اكتمل على الفرع `claude/sharp-bohr-vz31gl`
> (2026-07-12، 745 اختبار · باكتيست حصريًا · لا LOGIC_VERSION):** `_max_gain_before_stop`
> (أقصى صعود من الدخول قبل الوقف) + `backtest_portfolio` (محفظة top-N بreadiness→score) +
> `backtest_potential_report` (شرائح ≥100/≥50/≥30 للكل وللمحفظة + «انفجارات قتلها الوقف»
> بالأرقام + المعيار المسجَّل + حدّا الصدق حرفيًا) موصولة في `run_backtest`/`backtest_symbol`
> خلف `BT_POTENTIAL`/`BT_PORTFOLIO` (مطفأة افتراضيًا، قفل B1 = الإنتاج يتجاهلها) +
> `backtest.yml` (مدخلات bt_potential/bt_portfolio/bt_port_size). أقفال getsource للدوال
> الثلاث خارج rank_key/select_top/classify_tier/analyze_ticker/update_tracking/
> update_watchlist_status. **⏳ البند 6/6 معلّق بموافقة المستخدم:** دمج في main + تشغيل
> السنتين (2025/2026 بـbt_potential=1&bt_portfolio=1) → عرض الحكم بالأرقام.

> 🏦 **خطة «باكتيست قوة البوت» جاهزة لأوبيس (2026-07-12، نسخة مصحَّحة): `BT_LADDER_PLAN.md`.**
> سؤال المستخدم «نسبة قوة البوت» — الباكتيست الحالي يقيس البوابات لا البوت (23-26% مقابل 71% حي).
> **⚠️ تصحيح المستخدم الحاسم غيّر المحور:** «البيع على أهداف البوت تمهيد فقط وتعتبر ولا شي —
> الحكم: كم نسبة الانفجار لكل سهم من نقطة الدخول وعدد الأسهم الناجحة من المجمل» → أُلغي سلّم
> الأثلاث (كان افتراض نمذجة)، والمحور الجديد: **`_max_gain_before_stop`** (أقصى صعود من الدخول
> **قبل** لمس الوقف — الوقف يبقى حاكمًا؛ رأس شمعة التعبئة/الوقف لا يُحسب = درس F-L1) خلف مفتاح
> `BT_POTENTIAL` + شرائح ≥100/≥50/≥30% + «انفجارات قتلها الوقف» (يقيس قضية §0-ب بالأرقام لأول
> مرة) + `backtest_portfolio` (أفضل 15 بreadiness/score — هل الانتقائية تضيف؟) + **معيار مسجَّل
> مقفول** (قوي = منفجرون ≥20% بالمحفظة بالسنتين + وسيط ≥+15%؛ والحكم النسبي top-15 مقابل الكل)
> + حدّا صدق يُطبعان حرفيًا («حركة متاحة لا عائد محقَّق» · «أرضية لا سقف — بلا لحظي»).
> باكتيست حصريًا · بند=كوميت · أوبيس ينفّذ (§7 بالخطة).

> ⚖️ **حكم السنتين على المحرّك المُصلَح وصل وأُقفل (2026-07-12، `MARKET_BACKTEST_FINDINGS §0-ل`):**
> **F-L1 عمليًا صفر تضخّم بالسنتين** (0 صفقة اختلف حكمها 2025+2026) → كل الأحكام السابقة نظيفة —
> أقوى تأكيد. **T1b بالمقارنة العادلة انقلب موجبًا بالسنتين** (+3.7 و**+9.1 بسفلي +1.1؛ على نفس
> صفقات 2026: المسح 6 أرباح مقابل 1 للأساس** — كان −2.1 سالبًا زائفًا بالمحرّك المتفائل) لكن
> **الرفض كنموذج استبدال يومي يصمد بالمعيار الخماسي** (تحويل<تفادٍ ✗✗ · تعبئة 18-24% ✗✗) →
> القضية مقفولة: الحافة حقيقية اتجاهًا ومكانها التطبيق اللحظي المبني (كنسة الدقيقة + الرادار).
> الوقف الزوجي (−1.8/−0.4) والتصنيف والبصمة كلها تصمد. لا تغيير إنتاجي. توثيق كامل:
> `BACKTEST_AUDIT_FOR_OPUS.md` (النتائج والحكم) — تحقق مستقل من الـCSVين.

> ✅ **F-L1 + F-COST نُفِّذا (2026-07-12، أوبيس — باكتيست حصريًا، لا يمسّ الفرز/الإنتاج، 723 اختبار):**
> **F-L1** (النظر المستقبلي الوحيد الحقيقي): `_resolve_arm(entry_intrabar=True)` لا يحسم الهدف على
> شمعة التعبئة الداخلية (الستوب يبقى محميًّا)؛ ذراع المسح `entry_intrabar=False` (دخول بإغلاق =
> يملك من الفتح) — يحل لا-تماثل L-2 فيُعاد حكم T1b. حقل `outcome_legacy` يقيس التضخّم بتشغيل واحد.
> **F-COST** `BT_SPREAD_PCT` (افتراضي 0=سلوك اليوم، نمط BT_* بقفل B1): نصف السبريد يرفع الدخول
> ويخفض الخروج (حساسية 1/3/5%). تقرير الباكتيست يطبع سطرَي F-L1/F-COST. **⏳ المتبقي 3-ب:** تشغيل
> Actions (2025+2026 بـbt_sweep_entry=1) لإعادة حكم T1b + طباعة B3/C3 على المحرّك المُصلَح —
> النتائج تلغرام، تُعرَض على المستخدم (أي انقلاب واسع = إيقاف وعرض).

> 🔬 **تدقيق النظر المستقبلي بمحرّك الباكتيست اكتمل — الخطة: `BACKTEST_AUDIT_FOR_OPUS.md`.** قراءة بالعين لـ`_resolve_arm`/`_sweep_confirmed_fill`/
> `backtest_symbol` + فحص آلي للإرساء الزمني (صفر `today()/utcnow()` بمسار التحليل).
> **الحكم: المحرّك أنظف من المتوقَّع — بنيته الزمنية سليمة، وتسريب حقيقي واحد: L-1 تفاؤل
> شمعة التعبئة** (`hi[filled] ≥ t1` يُحسب فوزًا رغم أن ترتيب اللمس داخل الشمعة مجهول —
> الستوب محميّ والهدف لا) **+ أثره L-2: مقارنة T1b كانت منحازة ضد ذراع المسح** (الأساس
> يُحسم من شمعة التعبئة والمسح من شمعة+1) **وحكم T1b سقط بفارق 0.4 نقطة فقط → قابل
> للانقلاب**. الخطة لأوبيس: F-L1 (الحسم المحافظ من filled+1 + حقل `outcome_legacy`
> للمقارنة بتشغيل واحد) → إعادة تشغيل T1b سنتين + طباعة B3/C3 (أي انقلاب واسع = إيقاف
> وعرض) → F-COST (`BT_SPREAD_PCT` افتراضي 0 + حساسية 1/3/5%). M-1 (تعبئة المتوسط)
> قرار حي موثَّق يبقى. **لم يُنفَّذ كود — تدقيق/خطة فقط.**

> ✅ **خطة إصلاح التدقيق نُفِّذت (2026-07-12، بأمر المستخدم «عدّل كل اللي متأكد منه» — 13 بندًا،
> بند=كوميت، اختبار-يفشل-أولًا، 709 اختبار/0 فشل):**
> **② بوابة CI** (`tests.yml` على كل دفع/PR؛ الوثائق صارت «خروج 0» لا عددًا) · **③ تحصين الفحوص**
> (عدّادات `_seen` مقيسة 18/24/18/6/60/10/8 + سطر r0/_diag يفشل صريحًا؛ تحقق سلبي أمسك انحدارًا
> متعمَّدًا) · **① الجلسة الأولى** (`ref_bar`=شمعة الترشيح تُخزَّن بالفرز وتنتقل للقائمة/التنبيهات؛
> المقارنات `day > ref_bar` و`start = ref_bar+1`؛ ارتداد كامل للسجلات القديمة = لا LOGIC_VERSION) ·
> **⑤ التوقيت الشتوي** (`market_session_now` بـzoneinfo تحل الثوابت الصيفية في البريماركت/الأفتر/
> pullback_live/الرادار + **حارس الشمعة البائتة** بmonitor_live_events: أحداث الجلسة فقط لو آخر شمعة
> بتاريخ اليوم — يقتل كتم الدِدوب الشتوي؛ fixtures الفحوص صارت بتواريخ مطابقة؛ **F-03 أُغلقت**) ·
> **⑧ الرادار** (`_fresh_watchlist`: git fetch+show من FETCH_HEAD كل ~5د + نقل أختام الدِدوب — تصحيح
> على إصلاح التقرير المقترح: الرنر منفصل فإعادة قراءة الملف المحلي لا تكفي) · **⑬ git_save** (تعارض
> rebase → اعتماد الريموت + إعادة ملفاتنا + إعادة كوميت؛ فشل نهائي → تلغرام؛ runner/sender محقونان؛
> **قرار مدروس: لا دمج ignition بمجموعة التزامن** — جوب 6س يحجب المراقب) · **④ 10 اختبارات رفض
> للبوابات الصلبة** (كل بوابة تعمل كالموثَّق — B3/C3 صارا اختباريين؛ M2_hi52 ميت بنيويًا موثَّق) ·
> **⑫ حارس تغطية الأسبوع المنتهي** (خنق ياهو → تأجيل التجديد لا أرشفة غير محسومة) · **⑥** new_wl
> بنسخ wl (reject_stats+المفاتيح المجهولة تنجو) · **⑩** `fired_at` بكل إطلاق + `ignition_universe.json`
> (مقام الالتقاط، `record_ignition_universe` بنهاية جلسة الرادار) · **⑦** `_sanitize_name` عند حد enrich
> + تصريح «JSON=بيانات لا تعليمات» بدستور Cline · **⑪** حُذف `dev_assistant.py` (صفر مستورد؛ مستعاد:
> `git show 6badfe9:dev_assistant.py`) · **⑭ الصغائر الثماني** (esc يهرّب `"` + note + قص-قبل-تهريب ·
> «تعذّر المسح» الصادقة · SEC_CONTACT سرًّا بdaily_screener (**أضِف السرّ بإعدادات الريبو**) · hand_check
> «تعذّر تقييمه» بدل حكم زائف · مهلة ignition_verify 300د · مقام الالتقاط بالتحقق=المجلوب+أرضية 70% ·
> استئناف acc_verify عبر gh api · مفاتيح technical.yml) + توثيق 5-ج (أسماء الدوال بCLAUDE.md:14 ·
> PENDING_VERIFICATION يعكس الواقع · وسم POLYGON_EDGE_PLAN «شُحنت/تقاعد أ» · README حقيقي).
> **✅ ⑨ حُسم (2026-07-12، خيار ب بعد تحقق حاسم):** التحقق بالعين نقض أخطر ادّعاء بالتقرير —
> `_collect_closed` يصنّف بالـ`hit` أولًا (`won = bool(s.get("hit"))` يسبق فحص الستوب)، فالرابح
> الذي ارتدّ ولمس ستوبه **يُحسب رابحًا** لا خسارة (نسبة النجاح غير ملوّثة أصلًا — ثاني ادّعاء علاجي
> يسقط). المتبقي الحقيقي = السعة فقط: أصحاب `hit` كانوا يحجزون خانة. الحل (خيار ب): السعة تُحسب على
> **الحاملين للخانة** = النشطون **بلا هدف** (`_slot_holders`)، فالرابح يبقى نشطًا (لتسجيل سلّم الأهداف
> t1→t2→t3، فيصل يمتّع) لكن لا يمنع مرشّحًا جديدًا. **خيار أ رُفض بالدليل** (يقتل بيانات سلّم الأهداف +
> يغيّر الدوران). قفل «هدف ثم ستوب = رابح دائمًا» + قفل السعة. عرض/حساب سعة فقط · لا LOGIC_VERSION.
> 713 اختبار. **التدقيق التالي الموصى به يبقى: نظر مستقبلي بbacktest_symbol + تكاليف تنفيذ.**
> كل شيء على فرع التطوير — **يُدمج بmain بموافقة المستخدم.**

> 🛠️ **خطة إصلاح التدقيق العميق (2026-07-12): `AUDIT_FIX_PLAN.md`.**
> المصدر `IMPROVE_DEEP_AUDIT_REPORT.md` (على main، 14 نتيجة). **تحقق Claude المستقل بعيني قبل أي
> تنفيذ (شرط المستخدم):** الست الأخطر مؤكَّدة كودًا وبيانات — ① الجلسة الأولى عمياء (`day > added`
> سطر 5578 + `added`=تاريخ التشغيل 4748/7876 + متتبّع التنبيهات 9175؛ ميدانيًا: 17 تنبيهًا محسومًا صفر
> بنفس اليوم) · ⑤ توقيت EDT مثبّت (`13*60+30` بpullback_live:44 — قنبلة نوفمبر تكتم التنبيه الحقيقي
> عبر الدِدوب) · ② صفر CI على الدفع · ⑧ رادار الانطلاق يلفّ 6س على قائمة مجمَّدة (**تصحيح على التقرير:
> إعادة قراءة الملف المحلي لا تكفي — رنر منفصل، يلزم git fetch دوري**) · ⑨ الهدف لا يشطب (السعة قطعية؛
> جزئية «رابح يُحسب خسارة» تحتاج قراءة `_wr`) · ⑬ git_save حلقة rebase ميتة + فقد صامت. ⑥⑩ مؤكَّدتان
> بالبيانات (reject_stats يُمسح كل جمعة · سجل الانطلاق بلا وقت). ③④⑦⑪⑫⑭ لم تُتحقق بعد — **قاعدة
> التنفيذ: تحقق بالعين لكل بند قبل لمسه، وما لا يثبت يسقط موثَّقًا.** الترتيب: ② أولًا دائمًا → ③ → ① (قرار
> LOGIC_VERSION عند التنفيذ) → ⑤ → ⑧ → ⑬ → ⑨ (يحتاج قرار المستخدم أ/ب) → ④ → البقية. **صفر تنفيذ بعد.**

> 🕵️ **السطر الرئيسي «شورت» = المتاح من ChartExchange (2026-07-11، شكوى المستخدم «شورت 800 والموقع
> 35 ألف ليه الغلط؟» — عرض فقط · لا LOGIC_VERSION):** الكرت كان يعرض الحجم اليومي (800) بالسطر
> الرئيسي، والمستخدم يتوقّع رقم موقعه (Available من CE = 35K). **تأكيد منهجي بمثالين لفيصل:** XHLD
> («شورته 600 ألف طاخ طيخ») و**DSY (IMG_9509-9510: «نسبة الاقتراض 725% عاليه جدا = اجابي · 7000 سهم
> متوفر»)** — فيصل يقرأ عمودَي CE (الرسوم + المتاح) لا الحجم اليومي. الحل: `_short_headline(r)` (دالة
> عرض: المتاح CE → الحجم اليومي → نسبة الفلوت → «—») تُستعمل في `build_message` و`build_daily_message`.
> **🔒 M13 لم تُمسّ** — تبقى على الحجم اليومي: قيد معماري حاسم (CE يُجلب في enrich **بعد** select_top؛
> جلبه لآلاف رموز الفرز = حظر) + جذر مقفول C3. السؤال «هل M13 يجب أن يكون المتاح؟» يبقى مفتوحًا (باكتيست
> منفصل). أرقام PTN الثلاثة كلها صحيحة كلٌّ بمعناه (رئيسي=متاح · اقتراض=رسوم+متاح · SI=رسمي). 656 اختبار.

> ✅ **§6 نُفِّذ (2026-07-11، أوبيس — طبقة تقارير فقط · لا LOGIC_VERSION):** تقرير التطوير
> كان **يستبعد** كل فرصة فائتة/انفجار بكسب ≥300% تحت `suspect_split` (INLF +11333% وهمي).
> الآن `_split_corrected_gain` + `_resolve_split_suspects` + `_resolve_explosion_suspects`
> (نقيّة، فاشلة-آمنة، `fetch` محقون للاختبار) **تصحّح** الكسب بعامل تقسيم عكسي **مؤكَّد من
> ياهو** بدل رميه: مصحَّح في [30%,300%) → نظيف يدخل الإحصاء («✅ مصحّح تقسيم») · تحت 30%
> (انفجار: تحت 50%) → ضجيج صرف يُسقَط · لا تقسيم/بقي ≥300% → يبقى موسومًا (سلوك اليوم).
> `_MISSED` يخزّن `window_start` (c[-11]) للدقّة · الانفجارات مرجعها `expl_date−1`. **أسوأ
> حالة = سلوك اليوم حرفيًا.** 🔒 الدوال الأربع خارج rank_key/select_top/classify_tier/
> entry_status/analyze_ticker/backtest_symbol (قفل getsource). **649 اختبار** (+12).

> 📋 **الجولة 3 — حكم أول تشغيل حي بعد الإصلاحين (2026-07-11): `ai_debate/round3_first_live_verdict_20260711.md`.**
> **F-01 مُثبَت حيًّا** (تجديد الجمعة بنى قائمة الارتداد بـ15 سهمًا + 441 انفجارًا متراكمًا — كانت
> تُبنى فارغة بصمت) · F-02 ينتظر أول تقسيم حقيقي على أسهم القائمة · نقص بيانات 11/17 صفقة بـtrades
> CSV = **بنيوي تاريخي لا خلل** (تنبيهات قبل إصلاح A3 بـ2026-07-03 لا تحمل الحقول فيزيائيًا في
> `alerts_history.json` — يشفى ذاتيًا، لا فعل) · الأداء 71%/17 (+0.5R · ذيل صحّي 29%) · مراقبة
> الجمعة القادمة: الإشارتان الضعيفتان (فجوات متعددة الفريمات 44% · معيد إجرام 38% — لا ضبط قبل
> ثبات أسبوعين) + بند اختياري لأوبيس §6 (تسوية `_split_scale_factor` بأرباح الفائتة/الانفجارات
> بدل استبعاد suspect_split — طبقة تقارير فقط، أولوية منخفضة، بقرار المستخدم).

> ⚖️ **إصلاحا تدقيق الجولة 1 (2026-07-10 مساءً — قبل تجديد الجمعة 22:07، طبقة تسوية/حسم لا تمسّ الفرز):**
> **F-01** تظليل `hist` بالتجديد (أُدخل 2026-07-04، كان سيُفرغ قائمة الارتداد كل جمعة بصمت
> ويُسقط كاشف الانفجارات) → اسم مستقل `arch` + قفل تكاملي «4ب» للتجديد الكامل مع `week_start`.
> **F-02** التقسيم العكسي أثناء التتبع = «hit» زائف يلوّث سجل النجاح → `_split_scale_factor`
> (نقية) + `_fetch_splits` (فاشل-آمن → عامل 1.0 = السلوك السابق حرفيًا) + `_scale_divisor`
> (مُختار تماسك المقياس ضد التصحيح المزدوج بعد ترحيل LOGIC_VERSION) — تسوية **مقارنات الحسم
> فقط** في `update_tracking` و`update_watchlist_status` (+وسم «بعد تسوية تقسيم» بالسبب المعروض).
> المخزّن لا يُمسّ · لا LOGIC_VERSION · **637 اختبار** (16 قفلًا جديدًا 4ب/4ج). التقريران:
> `ai_debate/round1_claude_analysis.md` + `round2_claude_cross_review.md` (على فرع التطوير).

> 💧 **سبريد لحظي بالتنبيهات (2026-07-10، المستخدم «اشتراكي 200$ ولحظي» = Polygon Advanced لحظي
> نداءات غير محدودة):** نقطة الدقّة — تقرير السبت **بعد إغلاق الجمعة** فالسبريد مغلق/صفري وقتها
> (لقطة فيديو DSY 21% كانت خارج الجلسة)، فلا يُعرض بكرت التجديد مهما كان الاشتراك (قضية ساعات
> سوق لا حد نداءات). **حيث يفيد اللحظي فعلًا = المراقب اللحظي أثناء الجلسة:** أُضيف `spread_line(...,
> brief=True)` لتنبيهات `monitor_live_events` (من نفس NBBO المجلوب ببوّابة المضارب) → «💧 سبريد
> واسع N% — سيولة ضعيفة، ادخل بأمر محدّد» عند الحدث. فحص اليد يعرض السبريد الكامل حيًّا أيضًا.
> 🔒 عرض/تحذير فقط (لا بوابة) · فاشل-آمن · لا LOGIC_VERSION. 621 اختبار.

> 🎬 **حزمة فيديو فيصل 1 (DSY، 10 دقائق — ZIP من المستخدم 2026-07-10): فحص دقيق + خطة أوبيس
> جاهزة (`FAISAL1_VIDEO_PLAN.md`):** الصور فُحصت بالعين وطوبقت مع المبني. **أغلب سير العمل مبني
> أصلًا** (أسبوعي→4س→تنظيف/دمج→CE CTB+متاح→MACD 12/26/9 حرفيًّا · القمم البعيدة ليست أهدافًا).
> **3 فجوات حقيقية فقط (عرض/سياق):** P1 سطر السبريد/السيولة (فيصل يبدأ بدفتر الأوامر — الحالة
> المرئية 21% خارج الجلسة؛ السطر يسم الجلسة؛ تحذير لا بوابة) · P2 SI الرسمي 37,993 + Days to
> Cover 0.30 (رقما Fintel اللذان قرأهما — من ياهو info بلا نداء جديد؛ لا يُخلط بfinra_short) ·
> P3 مؤشر KST(10,15,20,30,10,10,10,15,9) بإعدادات فيصل الحرفية (ظاهر بـ7+ شارتاته؛ سياق زخم
> 4س لا بوابة). **مرفوض بصراحة من تقرير الحزمة (اقتراحات مُعِدّه لا فيصل):** معادلة درجات موزونة
> (نقض حكم §0-ز) · بوابات منع بالسبريد · Squeeze Score (مقفل) · إعادة هندسة level scoring.
> **✅ نُفِّذت الثلاث (2026-07-10، «نفّذ»):** P1 `spread_line(bid,ask,session)` (سبريد نسبةً لمنتصف السعر = صيغة الفيديو 21%؛ عتبة 5% هندسية؛ **تحذير لا بوابة**؛ وسم الجلسة). P2 `short_interest_line` + حقلا `short_interest`(sharesShort)/`days_to_cover`(shortRatio) من ياهو info **منفصلان عن finra_short** (لا تلوّث مقياس) + كاش + make_watch_entry. P3 `momentum_kst_state` على 4س (kst الموجودة بإعدادات فيصل؛ 4 حالات؛ عرض/سياق لا نقاط ولا بوابة). العرض: الكرت (build_message) + فحص اليد (P1 من flow_raw). **🔒 كلها خارج rank_key/select_top/classify_tier/entry_status/apply_float_gate/backtest_symbol** (مقفول getsource) · لا LOGIC_VERSION. **620 اختبار** (قيم DSY الحرفية: سبريد 21% · SI 37,993 · DTC 0.30). **المرفوض بقي مرفوضًا** (درجات موزونة/بوابات منع/Squeeze Score المقفل — اقتراحات مُعِدّ التقرير لا فيصل).

> 🏢 **فلوت CE يحلّ «الفلوت مجهول» (2026-07-10، المستخدم «كثير أسهم يجيني الفلوت مجهول... حلها
> عن طريق المصدر الجديد»):** ياهو `.info` مخنوق فالفلوت يغيب. مجسّ Actions مؤقت أثبت أن CE يعرض
> الفلوت نظيفًا (`stat-flow-label">Float</div>...value">12.55M</div>` — GEOS 12.55M/PTN 1.38M/
> FEMY 2.50M). `_parse_ce_float`+`ce_float_info` (يميّز «Float» عن «Free Float» بالضبط · `_ce_num`
> يقرأ K/M/B). **آخر ملاذ في enrich** بعد ياهو+الذاكرة + **ردم يومي** لأسهم القائمة الغائب فلوتها
> (ثابت فيُخزَّن مرة). **🔒 حاسم: عرض فقط — خارج بوابة M14** (تشتغل أثناء الفرز قبل enrich وتُمرّر
> المجهول بفائدة الشك؛ مقفول getsource: CE خارج apply_float_gate/rank_key/select_top). حُذف المجسّ.
> 607 اختبار (قيم GEOS/PTN/FEMY الحقيقية + قفل M14). عرض/سياق فقط · لا LOGIC_VERSION.

> ⚖️ **ChartExchange = المرجع الأول للاقتراض صراحةً (2026-07-10، المستخدم «نعتمد الموقع مرجعنا
> الأول»):** أُعيد ترتيب سلسلة enrich من (Fintel→CE→iBorrow) إلى **(CE→Fintel احتياطًا→iBorrow)** —
> CE يُنادى أولًا دائمًا (مصدر فيصل، IBKR مباشر 15د)، وFintel/iBorrow يملآن الفجوة فقط. `refresh_borrow`
> اليومي يعتمد CE مباشرة أصلًا. **حدّ الصدق (لماذا ليس «مرجع كل شي»):** CE كشط HTML سهم-سهم — ممتاز
> لأسهم القائمة (~10، فاشل-آمن + قاطع دائرة)، لكن **لا يفرز 3500 سهم** (لا feed جماعي + Cloudflare)،
> فجوهر الفرز (OHLCV) يبقى على ياهو. رقم «شورت» فيصل (600 ألف/7000) = عمود Available المقروء أصلًا من
> صفحة CE، فلا كاشط شورت منفصل (تكرار). M13 (حجم FINRA) لم تُمسّ. 602 اختبار. عرض/سياق فقط.

> 🏆 **إطار الاقتراض النهائي بتأكيد فيصل المباشر (2026-07-10، IMG_9508-9510 $DSY):** المستخدم
> شكّك «متأكد أن الاقتراض وقود؟» → تدقيق الكاتالوج أثبت أن سردية «الرسوم تجبر الشورت يغطّي
> فيتسارع» **غير موثّقة من فيصل** (سردية سوق عامة دخلت منّي 2026-07-09) — إطاره الموثّق:
> TRUG يمدح «صفر شورت» · ELAB سكويزه شرطه «لا يوجد أسهم متاحة للشورت» · XHLD متاح ضخم = حرب.
> فأُزيلت، ثم قدّم المستخدم تغريدة DSY الحاسمة: «**نسبة الاقتراض 725% عاليه جدا للشورت =
> اجابي** · 7000 سهم متوفر» + «معلوماتك صحيحة وهذا تاكيد من فيصل». **`borrow_line` النهائي
> (4 فروع، كلها بنص فيصل):** رسوم عالية + متاح قليل = إيجابي (DSY) · متاح فوق 40 ألف = حرب
> وتصريف (XHLD) · متاح صفر = وسم ELAB · رسوم عالية بلا متاح = «الحكم الكامل يحتاج المتاح»
> (لا إيجابي أعمى). عتبتا الرسوم 20/5 هندسيتان (موسومتان كذلك). **600 اختبار** (حالتا
> DSY/ELAB الحرفيتان + قفل غياب سردية التغطية). عرض فقط · لا LOGIC_VERSION.

> 🔓 **حجر رشيد «شورت فيصل» + قراءته المركّبة (2026-07-10، IMG_9504-9506 — المستخدم: «فيصل
> معتمده للشورت» و«هذي الصورة أدق»):** تغريدة فيصل الأم: رابط CE «يطلع لك نسبة الشورت والاقتراض»
> + قائمته (P4 تقسيمات/ZCMD · قاعدة LABT حرفيًّا · أهداف 4H/يومي/أسبوعي — كلها مبنية عندنا).
> **الكشف:** تغريدة XHLD «سهم شورته 600 الف؟ طاخ طيخ الى الهاويه» — رقمه = عمود **Available
> (المتاح للاقتراض)** من صفحة CTB (برسوم 23%!). **⚠️ دقة (تصحيح المستخدم): XHLD سهم خبر انضخّ،
> ليس ارتكازًا** — فيصل علّم القاعدة عليه (قاعدة عامة على المتاح الضخم، لا حكم على مرشّح ارتكاز).
> وكون أرقامه التاريخية للارتكاز (20/35/40 ألف) «متاحًا» أيضًا = **استدلال غير مثبت** (قرينته
> الاستدارة فقط) — لذلك M13 لم تُمسّ. **قراءته المركّبة (المؤكَّدة): الرسوم وقود سكويز فقط لو
> المتاح قليل** — والصورة الأدق (9505):
> المتاح تضخّم **30 ألف→600 ألف في 3 أيام** والسهم +24% أفتر = تكدّس ذخيرة قبل الهاوية. **المبني
> (عرض فقط):** فرع «⚠️ ذخيرة هبوط لا وقود سكويز» في `borrow_line` عند متاح فوق `SHORT_GATE_MAX`
> (40 ألف — حد فيصل الموثّق) مهما كانت الرسوم — نصّه بمعنى «طاخ طيخ» الصحيح (توضيح المستخدم):
> «حرب وتصريف لا وقود سكويز: الشورت الضخم يحارب أي صعود ويصرّف فيه، مستحيل يرتفع بسببه» +
> `refresh_borrow` يوميًّا لأسهم القائمة + مسار
> `borrow_hist` (سقف 14) بسطر «كان 30K قبل 3 يوم (يتضخّم)». **597 اختبار** (9 جديدة، حالة XHLD
> حرفيًّا). **⚠️ سؤال جذري مفتوح للمستخدم:** M13 تقيس حجم شورت FINRA اليومي بعتبة 40 ألف بينما
> مقياس فيصل الظاهر = «المتاح» — لا تغيير بلا قراره (جذور + C3 + سجلّ 80%)؛ البيانات تتراكم للمقارنة.

> 🌐 **اعتماد ChartExchange مصدرًا للاقتراض (2026-07-10، اقتراح المستخدم «شف الموقع هذا» — وهو
> مصدر فيصل نفسه، رابطه بصورة 9431):** المنهج: بيئات التطوير محجوبة (403 بروكسي + 403 WebFetch)
> فبُني **مجسّ Actions مؤقت** (ce_probe.yml، PR #145، حُذف بعد القراءة) أثبت أن **بيئة البوت تصل
> HTTP 200 سريعًا** لصفحات borrow-fee/short-volume/short-interest، وأعطى **الشكل الحقيقي**: جملة
> `ctbtoday` ثابتة عبر الرموز «As of …, there were 550,000 shares available with a fee of 0.40%»
> (IBKR كل 15د). **تحقّق متقاطع:** GEOS طابق iBorrowDesk حرفيًّا (0.40%/550K) وPTN أحدث (12.43%/40K
> مقابل 11.83%/25K أمس) = نفس المصدر أعلى تحديثًا. **التنفيذ:** `_parse_ce_borrow` نقيّة (نافذة بعد
> المرساة + تجريد وسوم + regex يقبل فاصلة الآلاف بالرسوم 1,234.5%) + `ce_borrow_info` (ناسداك فقط،
> مهلة 8ث) + سلسلة enrich صارت **Fintel ← CE ← iBorrowDesk** بقاطعَي دائرة مستقلَّين (`_ce_fails`/
> `_bor_fails`) وسقف مشترك 25. **⚖️ قرار نطاق:** الاقتراض فقط — «Short Volume» عند CE مجموع بورصات
> = مقياس مختلف عن سلسلة FINRA المتّسقة (لا خلط مقاييس؛ درس sharesShort). 🔒 خارج الفرز (getsource) ·
> لا LOGIC_VERSION · فاشل-آمن مطلق. **588 اختبار** (6 جديدة بثوابت المجسّ الحقيقية).

> ⏰ **إصلاح تأخير الكرون المزمن (2026-07-10، اكتُشف بفحص «ناقص شي؟»):** سجلّ 12 يوم تشغيل
> جدولة أثبت أن كرون `0 7` يتأخر عند GitHub **2.2-3.6 ساعة كل يوم** (ينطلق فعليًّا 09:14-10:37
> UTC = تقرير الصباح يصل 12-13:40 السعودية بدل 10ص) — زحمة الدقيقة 0 المعروفة. الحل الموثّق:
> دقيقة غير مستديرة → `23 7 * * 2-5` (متابعة، ~10:23ص سعودية اسميًّا) و`7 22 * * 5` (التجديد
> 22:07) **مع تحديث نص مطابقة `RENEW_ON_CLOSE` حرفيًّا** (مقترنان — تحذير مكتوب بالملف). التأخير
> لو بقي جزئيًّا لا يضرّ التجديد (بعد الإغلاق بأي حال). جدولة فقط — صفر كود بايثون.

> 🪦 **تقاعد عرض «🔬 التجميع الصامت» نهائيًّا (2026-07-09، المستخدم سأل «وش يفيد هالسطر؟» ثم
> «سوّ الأصدق»):** بما أن تجربة T-ACC فشلت بالسنتين (غير مميِّزة للمنفجر — الأرقام تنقلب بين
> 2025/2026)، عرض «شراء عدواني % · طبعات كبيرة % · دارك % (آخر Nج)» = **نعرض إشارة سقطت
> باختبارها كأنها تفيد القرار**. الأصدق: إزالته. أُزيل من الكرت (`build_message`) واليومي
> (`build_daily_message`) وفحص اليد (`hand_check.py` عرضًا وجلبًا) و`make_watch_entry`، و**أُزيل
> جلبه من `enrich`** (`_acc_on`/`_acc_budget`/`silent_accumulation` في الفرز → توفير حتى 30 نداء
> Polygon/يوم لإشارة غير مُجدية). **محفوظ (research infra):** الدوال النقيّة (`acc_components`/
> `_tick_classify`/`polygon_base_trades`/`silent_accumulation`/`acc_line`) + `acc_verify.py` + `acc_verify.yml`
> لإعادة الاختبار لو ظهر محور جديد. **📊 «تدفق الأوامر» يبقى بفحص اليد** (توقيت حي مفيد ≠ تجربة فاشلة).
> عرض فقط · لا LOGIC_VERSION · مقفول (acc خارج rank_key/select_top كما كان). **582 اختبار.**

> 📝 **اختصار سطر المضارب بالتنبيهات (2026-07-09، طلب المستخدم على تنبيه GEOS الحي: «فيها
> فلسفة كثيرة... ابي شي مختصر جدا — المضارب طلب 1000 سهم فوق الدعم او كمية عرض 5 الاف عند
> المقاومه»):** `operator_line(of, s=None)` أُعيدت صياغته للغة أوامر المتداول: «شرى على الطلب ~N
> سهم» أولًا · «رفع بشراء ~N» · «طلب/عرض N سهم» موسومَين بأقرب مستوى (`_price_level_tag` جديدة:
> وقف/دعم/فرعي/حرج/مقاومة — «عند» ضمن 1.5% وإلا فوق/تحت؛ جدار «عند» مستوى يُسمّى به مباشرة:
> «طلب 400 سهم عند الرقم الحرج $7.00 ($7.03)»). `operator_flow` يلتقط **العرض** أيضًا من نفس نداء
> NBBO (ask/ask_size — صفر نداء إضافي، يغطي «عرض 5 آلاف عند المقاومة»). **أُسقط من التنبيهات:**
> حدود الصدق (L2/تصنيف تقريبي/Lee-Ready) + «📊 لقطة الأوامر» + تذييلا ℹ️ (`build_live_alert` سطر
> الحدث فقط؛ pullback_live لم يعد يجلب اللقطة = توفير نداءات؛ رادار الانطلاق أسقط سطر التدفق وجلبه
> باقٍ كبوابة كتم) — **التفصيل والحدود بقيا بفحص اليد فقط**. عرض فقط (لا بوابات جديدة، كتم البوابة
> كما هو). **583 اختبار** (أقفال: الترتيب/غياب الفلسفة/وسم المستوى/توافق خلفي quotes).

> 📅 **نقل التجديد لِما بعد إغلاق الجمعة (2026-07-09، طلب المستخدم «ابيه يبدأ بعد إغلاق
> الجمعة عشان الفريم الأسبوعي يكون صحيح»):** المشكلة: التجديد كان صباح الجمعة (07:00 UTC)
> = يقرأ إغلاق **الخميس** → الشمعة الأسبوعية (`resample_ohlc(df,"W")`) ناقصة يوم (اثنين→خميس)
> لبوابة M6/الفجوات/الأهداف/القمم الأسبوعية. **التصميم النهائي: مدفوع بإشارة جدولة لا بيوم
> الأسبوع.** كرون ثانٍ `0 22 * * 5` (الجمعة 22:00 UTC = بعد إغلاق السوق؛ الإغلاق 20:00 صيفًا/
> 21:00 شتاءً فـ22:00 آمن بالفصلين) يرفع `RENEW_ON_CLOSE=1` عبر
> `${{ github.event.schedule == '0 22 * * 5' && '1' || '0' }}`. `should_renew(wl, today,
> force)` → `should_renew(wl, force, renew_signal)` (أُزيل الاشتقاق من `weekday()`). كرون
> المتابعة `2-6`→`2-5` (الثلاثاء→الجمعة؛ السبت كان سيصير تكرارًا لإغلاق الجمعة). **الثابتان
> `WEEKLY_RENEW_DAY`/`WEEKLY_REPORT_DAY` حُذفا** (مقفول `not hasattr`). التقرير الأسبوعي فُصل
> عن الـweekday → `run_performance_system(weekly_report_now=True)` يُستدعى من التجديد فقط
> (فلا يُرسَل مبكرًا على إغلاق ناقص). `is_renew_day`→`had_prev_list=bool(wl["stocks"])` للعنوان.
> **6 أقفال/اختبارات should_renew (5 مسارات + غياب الثابتين) — 579 اختبار.** لا LOGIC_VERSION
> (جدولة فقط). ⚠️ **صدق: Friday-بعد-الإغلاق ليس «أدق» من السبت-صباحًا — كلاهما يقرأ نفس شمعة
> الجمعة المكتملة؛ الفرق أن القائمة توصل ليلة الجمعة (~22:00 UTC = 1ص السبت السعودية) بدل صباح
> السبت (أبكر ~9 ساعات، الحدّ الطبيعي لنهاية الأسبوع).** حقول الأحداث للقائمة الحالية تكتمل بأول
> تجديد = **الجمعة 2026-07-10 مساءً**. ملاحظة: لو غُيّر وقت الكرون لازم تغيير مطابقته بـRENEW_ON_CLOSE.

> 🏆🛡️ **الرسالة الذهبية + إصلاح فقدان الحفظ الصامت (2026-07-09 — أهم أحداث اليوم):**
> **التسلسل الدقيق (تصحيح المستخدم):** المستخدم أخذ ثلاثة من **مرشّحات قائمة البوت** (GEOS/FEMY/PTN)
> وسأل فيصل «تنفع كارتكاز؟» → فيصل **صادَق** («ايوه») وأرسل **مستوياته هو من تحليله** + «تأكد من
> الشورت والاخبار» (→ `FAISAL_IMAGES_CATALOG.md` قسم خامسًا). **المُثبَت:** مصادقة خبير المنهجية على
> اختيار البوت + **تطابق مستوياته المستقلة مع حسابات البوت** (GEOS: دفعات 6.28/6.47 = دعمَيه + الحرج
> 7.00 = مقاومته 7 · PTN: أدنى دفعة 9.75 = «ما يكسر 9.75» + هدف1 11.70≈11.73 · «اهداف رووس الشموع
> الحمرا» = تأكيد نصّي لقاعدة `_red_candle_heads` · شرطه الشورت/الأخبار = M13+scan_news_risk+SEC).
> **غير المُثبَت:** استقلالية الاختيار (الأسهم عُرضت عليه من القائمة). **أثناء التحقّق انكشفت حادثة:** سجلّ الأكشن «أُضيف: GEOS، FEMY، DTI، PTN»
> ثم «⚠️ حفظ ملف القائمة: Object of type bool is not JSON serializable» → **ضاعت الإضافات والتشغيل
> انتهى ✅ كذبًا** (والمراقبة/رادار الانطلاق لا يرونها). الجذر: `scan_explosions` مسار «قفزة» يخزّن
> suspect_split كـnp.bool في wl["explosions"] — **الدليل الجنائي: المخزَّن 129 «تجمّع» وصفر «قفزة»**
> = كل يوم فيه انفجار قفزة كان الحفظ يفشل بصمت. **أُصلح (PR #127):** bool() بالجذر (+ _MISSED) +
> **شبكة أمان `_json_default` في `_atomic_write_json`** (numpy→بايثون · تاريخ→isoformat · مجهول→str —
> يقتل الصنف نهائيًّا) + 3 أقفال اختبار. **+ سطر الاقتراض صار مفسَّرًا ذاتيًّا (PR #128، طلب المستخدم
> «ما اعرف الفايده»):** صعب 🔥 =وقود سكويز (يجبر الشورت يغطّي) · متوسط=جزئي · سهل=لا وقود ·
> `BORROW_MID_PCT=5` عرضي. 548 اختبار. **✅ تأكيد حي (نفس اليوم، تشغيل بإصدار 3807b81):** المستخدم
> أعاد التشغيل → **الحفظ نجح** (`bot data 2026-07-09` = 1577e6d) والقائمة كاملة بـ8 أسهم (فيها
> GEOS/FEMY/DTI/PTN بأرقام الرسالة الذهبية: FEMY دفعات 3.50/3.60/3.71 · GEOS 6.28/6.47 + حرج 7.00 ·
> PTN 9.75 + هدف1 11.70) — **والدليل الجنائي المعاكس: المخزَّن صار فيه انفجارات «قفزة» لأول مرة**
> (369 إدخالًا؛ كانت صفر «قفزة» = السمّ القديم يُحفَظ الآن بأمان). سطر الاقتراض المفسَّر ظهر حيًّا
> بالتقرير. المراقبة اللحظية + رادار الانطلاق يريان أسهم فيصل. (ملاحظة مخطط التخزين: مفتاح الوقف
> المخزَّن `stop` رقم واحد + `stop_hi` — لا `stop_lo` بالمخزَّن، ذاك عمود CSV فقط.)

> ✅ 📉 **ضغط/تصريف المضارب نُفِّذ ودُمج (2026-07-09، PR #125 — طلب المستخدم «ابي اعرف لو السهم
> ضغطة المضارب فيجيني اشعار»، من صورة LABT 9432: سيولة قطيع → المضارب ضغطه 3.10→1.97 بالأفتر):**
> حدثان بمراقب الجلسة (تنبيه خطر — **لا يُبوَّبان بالمضارب**، واجب): **dump** (يومي: إغلاق اليوم ≤ الأمس
> ×(1−PRESS_DROP_PCT=15%) = هبوط حادّ، من الشمعة اليومية بلا مفتاح) · **afterdump** (أفتر:
> `polygon_after_hours(sym, regular_close)` دقائق بعد 20:00 UTC، فحص زمني قبل الشبكة، يعيد استخدام
> `_premarket_summary` آخر بار مقابل الإغلاق → هبوط ≥15% = تصريف أفتر نمط LABT الحيّ). أيقونة 📉 ·
> `fetch_afterhours` قابل للحقن · **كرون pullback_monitor مُدّ 21→23 UTC** لتغطية الأفتر (يلزم مفتاح؛
> بلا مفتاح لا عمل). 🔒 polygon_after_hours خارج rank_key/select_top/analyze_ticker/backtest_symbol
> (getsource) · لا LOGIC_VERSION · فاشل-آمن. ⚠️ مرجع الأفتر=إغلاق ياهو (قد يتأخّر دقائق) · العتبة 15%
> تُعاير لاحقًا (LABT −40%). 542 اختبار.

> ✅ 🔒 **معدّل الاقتراض (Cost to Borrow) نُفِّذ ودُمج (2026-07-09، PR #123 — طلب المستخدم «الاقتراض
> أساس الارتكاز، فيصل يتكلم عنه كثير»):** نسبة الشورت موجودة أصلًا؛ الناقص = معدّل الاقتراض (تكلفة
> الاقتراض للشورت + الأسهم المتاحة). `_parse_fintel_borrow(html)` نقيّة تستخرجه من **نفس طلب Fintel**
> (صفر نداء إضافي) · `_parse_iborrow`/`iborrow_info` احتياط iBorrowDesk (بيانات IBKR، بقاطع دائرة يتوقّف
> بعد 3 إخفاقات + سقف 25 + مهلة 8ث — لا يعلّق الإثراء) · `borrow_line` «🔒 اقتراض: رسوم N% 🔥 صعب ·
> متاح X سهم» عند `BORROW_HIGH_PCT`(=20)+ · «—» عند التعذّر. يظهر بالكرت/اليومي/فحص اليد + يُخزَّن.
> **⚠️ حدّ صدق: لا API رسمي — Fintel يُحجب كثيرًا وiBorrowDesk مجتمعي، فالتغطية جزئية («—» عند الفشل)؛
> الشكل الحي غير مؤكَّد من بيئة التطوير (محجوبة) → يُؤكَّد بأول تشغيل Actions ويُضبط المحلّل لو اختلف.**
> 🔒 خارج rank_key/select_top/classify_tier/analyze_ticker/backtest_symbol (getsource) · enrich بعد
> select_top · **لا بوابة/وزن** (لا عتبة فيصل موثّقة — سياق + تحذير سكويز فقط) · لا LOGIC_VERSION. 535 اختبار.

> ✅ 🕵️ **بوّابة المضارب على التنبيهات نُفِّذت ودُمجت (2026-07-09، PR #120 — طلب المستخدم «لا
> إشعار إلا على شي يستاهل + علّمني لو دخل المضارب وكمياته على الطلب»):** قاعدة فيصل — صفقة المضارب
> ≥1000 سهم · يشتري على الطلب (امتصاص) · يفرّغ العروض لحظة الرفع. `_operator_blocks(trades, min)`
> نقيّة تصنّف الطبعات ≥`OPERATOR_MIN_SHARES`(=1000) بقاعدة التيك → شراء عدواني (صعودي) · على الطلب
> (هبوطي، امتصاص) · None لأقل من 20 صفقة. `operator_flow(sym)` غلاف Polygon فاشل-آمن (/v3/trades +
> NBBO) · `operator_line` عرض بحدّ صدق «تصنيف تيك تقريبي بلا L2». **بوّابة الرادار** (scan_ignition):
> لا يطلق إلا لو دخل المضارب؛ تعذّر القياس → احتياط تصنيف شمعة الدولار (يُكتَم القروب فقط). **بوّابة
> المراقب** (monitor_live_events): يكتم الأحداث الإيجابية (مسح/دخول/تجاوز) بلا مضارب ويُلحق كمياته؛
> **الخطر (كسر الوقف) لا يُبوَّب** · بلا مفتاح = سلوك سابق حرفيًّا. 🔒 خارج rank_key/select_top/
> classify_tier/analyze_ticker/backtest_symbol (getsource) · لا LOGIC_VERSION · فاشل-آمن. حُدّث قفل
> P1 (candle_class صار بوّابة رادار مشروعة — توقيت لا اختيار). 525 اختبار. **⚖️ العتبة 1000 سهم
> موثّقة من فيصل؛ أي معايرة لاحقة = سجلّ حي + موافقة المستخدم.** ملاحظة منهجية: «على الطلب» = التجميع
> (امتصاص) · «تفريغ العروض» = الإشعال (رفع عدواني) — مرحلتان، التنبيه يعرض الاثنين.

> 🚨 **أهم اكتشاف جوهري (2026-07-05) → `MARKET_BACKTEST_FINDINGS.md` (اقرأه):**
> أول **باكتيست سوق كامل بلا انحياز** (كل ناسداك، 5 أشهر، 103 إشارة) كشف 3 مشاكل
> جوهرية: (1) **72% ضربت الوقف** و4 أسهم ضربت وقفنا **ثم انفجرت** (+59..+118%) =
> «مسح السيولة» الذي يحذّر منه فيصل (البوت يدخل الدعم الأول فيُصفّى). (2) **فئة A
> ميتة** — 0 من 103 باكتيست و**0 من 58 حي** (كلها B؛ A تتطلّب صفر نواقص = مستحيل).
> (3) **طبقة «قراءة اليد» مفقودة** (معدل الاقتراض + سلوك المضارب 6 أشهر + الرقم الحرج).
> **قرار المستخدم:** وثّقناها، ثم نختبر الثلاث بمعيار مسجَّل مسبقًا (T1 المسح الأقوى)،
> **والأولوية القصوى = بناء «بروفايل سلوك المضارب» من OHLCV** (أقوى تعديل جوهري قادم،
> القسم 5 بالملف). + توسيع العيّنة **لسنة 2025 كاملة** (يلزم دعم «السنة» بفلتر الشهر).
> **لا تغيير جذري قبل التجربة + المراجعة الخصومية.** (المقفولات C3/B3 لا تُفتح — مجال مختلف.)

> 🩹 **إصلاح باكتيست 2025 «صفر إشارة» (2026-07-05، طبقة إدخال — لا تمسّ الفرز):** تشغيل
> 2025 كامل رجّع **0 إشارة بلا سطر فترة** (إصدار a14bbee). التشخيص من **سجل الأكشن**:
> المستخدم كتب «2025» في خانة **الشهر** والسنة فارغة → البوت بنى نافذة مشوّهة
> `2025-2025-01..2025-2025-31` فسقطت كل التواريخ خارجها. **ليس خللًا في PR #67** (الأذرع
> الزوجية سليمة). الحل: `_normalize_bt_period` — رقم يشبه سنة (4 خانات ≥2000) بخانة
> الشهر والسنة فارغة → يُنقل للسنة تلقائيًا (= السنة كاملة، نفس نتيجة الإدخال الصحيح
> تمامًا) · شهر غير صالح (ليس 1-12) يُفرَّغ · `_recent_month_window` يرفع ValueError لشهر
> خارج 1-12 (دفاع عميق). +4 اختبارات (257). **المستخدم يعيد تشغيل 2025 لحكم التجربة الزوجية.**

> 🎯 **حكم 2025 + أقوى تعديل قادم (2026-07-05، ألترا كود 7 وكلاء) → `MARKET_BACKTEST_FINDINGS.md` §0:**
> 2025 كامل (115 إشارة، نجاح 23%، عائد −3.44%). **حكم T1 «الوقف»: فشل نهائي** — الوقف بالإغلاق
> (−2.1 نقطة) **والوقف الأعمق** (يسوء رتيبًا −3→−7%) كلاهما يخسر. السبب: الخسائر **انهيارات عميقة**
> (86% لم تقترب الهدف · الانفجارات-الخاسرة الخمس غاصت −30..−69% قبل الركض = لا وقف يحملها). **الرافعة
> = توقيت الدخول لا الوقف** (فيصل: «لا تدخل الدعم الأول · مسح ثم استعادة»). **بُنيت تجربة «الدخول المؤكَّد
> بالمسح» (T1b، باكتيست فقط، مطفأة افتراضيًا):** `BT_SWEEP_ENTRY=1`، الدخول **معزول عن الوقف**
> (`BT_SWEEP_STOP`) والحكم على المُعبَّأة فقط + تفكيك تفادٍ/تحويل (حصّنته المراجعة الخصومية من فخّ الامتناع
> وخلط الوقف). 265 اختبار. **بنية المسح كانت نائمة أصلًا** (`r["sweep"]`=القاع−10%). **التالي:** المستخدم
> يشغّل أكشن Backtest بـ`bt_sweep_entry=1` على 2025 و2026 → إن صمد الفرق الزوجي (حدّ سفلي فوق صفر + تحويل≥تفادي
> + كل سنة) نفعّل الدخول-بعد-المسح إنتاجًا. المقفولات C3/B3 لا تُفتح — هذا **توقيت دخول** مجال مختلف.

> ⚖️ **حكم T1b النهائي (شُغِّل 2025+2026، إصدار 45cbf0c) → `MARKET_BACKTEST_FINDINGS.md` §0-و:** **يُرفض كبديل
> دخول** (يسقط 3 أرجل: تعبئة 16-24%≪40% · الحافة امتناع لا تحويل · لا يصمد سنتين — 2025 يلمس الصفر). **لكن
> الفرق الزوجي على المُعبَّأة موجب بالسنتين (+3.8/+9.9، معنوي 2026)** = «حين يحدث المسح فعلًا، الدخول بعده أفضل»
> (دليل مباشر لقاعدة فيصل «لا تدخل الدعم الأول»). النمط **نادر**، فينتقل لطبقة الترتيب السلوكية T0 (بصمة المسح =
> عامل جودة لا بوابة). **لا تغيير إنتاج · الكود يبقى أداة باكتيست مطفأة.** الحُرّاس الخصوميون منعوا اعتماد صافٍ
> خام مضلِّل (+196/+230). ✅ **فصل الباكتيست مُقفَل.** التالي = **سلوك المضارب T0** (شغّال، وبصمة المسح أول مكوّن).

> 🧬 **سلوك المضارب T0 — «طريقة ارتفاع اليد» بُنيت (2026-07-05، أولوية المستخدم القصوى «كيف يرفعها»؛
> طبقة عرض فقط، لا LOGIC_VERSION):** `behavior_rise_profile(df)` نقيّة (بلا نظر مستقبلي · بالبارات):
> عدد الرفعات + الأكبر + الحداثة + إعادة الرفع + **بصمة المسح** → درجة 0-100 + وصف، تُحسب حيًّا في
> `scan_market` (صفر أثر باكتيست)، تُخزَّن، وتُعرض «🧬 طريقة الارتفاع» بالكرت واليومي. **🔒 قفل سلامة:
> لا تدخل `rank_key` ولا تغيّر عضوية `select_top`** (مقفول باختبار — درس C3 ضد خنق ارتكاز فيصل).
> **الوقود مُستبعَد** (مكرّر M13/M14+مُعاير على ناجين) · **رد فعل الفجوات مُسقَط** (مفبرك). 270 اختبار.
> ✅ **حُسم (2026-07-08، §0-ح بملف النتائج):** تحقّق البصمة شُغِّل على السنتين (behav_score وقت الإشارة
> بلا تسريب) → **فشل معيار القبول بالسنتين** (2025 مسطّح +3 · 2026 +15 متداخل الفواصل، الاتجاه غير متسق)
> = **لا وزن ترتيب ولا أولوية فرز — تبقى عرضًا** (طلب المستخدم «أولوية لليد النشطة» أُجيب بالدليل).
> **الاكتشاف الأثمن:** نجاح البصمة العالية ينهار بثبات بالسنتين (~30%→~10-14%) — اليد النشطة تمسح وقفنا
> الثابت قبل الانطلاق (يتقاطع مع T1b وتحذير فيصل «لا تدخل الدعم الأول») → وسم تحذيري بسطر 🧬 عند 60+:
> «يد نشطة — حذارِ مسح الوقف قبل الانطلاق» (عرض فقط · قفل: behav خارج rank_key/select_top).

> ✅ 🕵️ **لوحة «علامات اليد» نُفِّذت ودُمجت (2026-07-08، خطة `HAND_EVIDENCE_PANEL_PLAN.md`):**
> طلب المستخدم: «أي سهم فيه شك أن وراه مضارب (شموع يومية/4س/طلبات/رفعة قروب ثم كسر دعوم) — اجمعها
> ووضّحها». `hand_evidence(r)` لوحة أدلة نوعية (قائمة + عدّ، **بلا درجة مبتدعة**): **N1** `group_pump_scar`
> «رفعة قروب (قفزة ≥EXPLOSION_PCT + حجم ≥VOL_SPIKE_MULT×) ثم كسر دعوم» · **N2** `managed_ceiling`
> «سقف مُدار 4س» (3 رؤوس حمرا+ عند نفس السعر) · **N3** لقطة bid/ask صادقة (السبريد الواسع؛ تدفق
> الطلبات الحي غير متاح → يُقال حرفيًا) · + الموجود (sweeps≥2 · sweep_confirmed · rotation≥100). سطر
> «🕵️ علامات اليد (N)» عند دليلين فأكثر بالكرت واليومي + القائمة الكاملة بعمق التقرير الفني. تُحسب
> pump_scar حيًّا (scan/يومي/analyze_one؛ مقفول خارج backtest/analyze_ticker) وتُخزَّن؛ hand_evidence
> مجمّع display-time من المخزّن. **🔒 عرض/تحذير فقط:** خارج rank_key/select_top/classify_tier/
> entry_status (مقفول) · الإطار المحسوم §0-ح (لا أولوية اختيار — اليد النشطة تمسح الوقف). **مراجعة
> خصومية 3 عدسات:** لقّت تكرار درجة البصمة على سطر 🧬 → أُزيل (اللوحة أدلة متمايزة لا درجة). **379 اختبار.**

> ✅ 🕵️ **تحديث نهاية اليوم «ماذا فعلت اليد اليوم» (2026-07-08، طلب المستخدم «يوصلني تحديث نهاية
> اليوم بخصوص الأسهم اللي وراها مضارب وأعرف وش سوّى»):** `hand_activity_today(s, df)` يقرأ شمعة
> اليوم مقابل السياق ويصف فعل اليد: كنس دعم/كسر دعم/دفاع عن سقف مُدار/شمعة بحجم ضخم (أخضر=رفع ·
> أحمر=توزيع/هزّ). `build_hand_digest` = ملخّص لأسهم القائمة التي وراءها بصمة يد (دليلان فأكثر) **أو**
> فعلت شيئًا اليوم (النشط أولًا) + سطر «🕵️ علامات اليد». `run_hand_digest` مسار خفيف مستقل
> (`SCREENER_MODE=DIGEST`): يحمّل رموز القائمة فقط + يحدّث البصمة/الرفعة من شمعة اليوم **ولا يحفظ
> القائمة** (إشعار فقط، لا سباق مع تشغيل الصباح). **workflow `hand_digest.yml` 22:00 UTC اثنين→جمعة**
> بعد إغلاق ناسداك (+ workflow_dispatch يدوي). 🔒 عرض/تحذير فقط · لا يمسّ الفرز/الحالة. **389 اختبار.**

> ✅ 🕵️ **قسم مستقل «أسهم فيها علامات يد» + توضيح الجاهزية (2026-07-08، طلبان متتاليان للمستخدم):**
> (1) «أبي أسهم علامة المضارب بقائمة لحالها عشان ما أضيع من كثر البيانات» → `build_hand_section(wl)`
> قائمة نظيفة أسفل التقرير اليومي (سطر لكل سهم يد بدليلين فأكثر)، وأُزيل سطر 🕵️ المبعثر من كرت اليومي
> (بقي بكرت التجديد الكامل build_message). (2) «الأهم توضّح إذا جاهزة للدخول» → كل سطر بالقسم **وبتحديث
> نهاية اليوم** يبدأ بوسم `entry_status`: «🟢 جاهز للدخول» أو «👀 متابعة»، **والجاهز يُرتّب أولًا**.
> 🔒 عرض فقط (partition عرضي، خارج الفرز/الاختيار). **395 اختبار.**

> ✅ 📏🔥 **حلقة قياس رادار الانطلاق نُفِّذت ودُمجت (2026-07-08، PR #118 — طلب المستخدم «وش نسوي
> بأداة التطوير؟» · قياس/عرض فقط):** كانت أداة التطوير تقيس **طبقة الاختيار** (السقف الميت §0-ز)
> وعمياء عن **الحافة الحيّة الوحيدة المثبتة** (رادار الانطلاق §0-ك) — والإنذار الكاذب لا يُقاس إلا حيًّا.
> الحلقة: (1) الرادار يسجّل كل إطلاق بسجلّ دائم مستقل `ignition_log.json` (`record_ignition_fires`/
> `load_ignition_log`، دِدوب مرة/سهم/يوم · سقف 500 · فاشل-آمن؛ `ignition_live` يحفظه بنهاية الجلسة عبر
> git_save — أُضيف `permissions: contents: write` لـ`ignition.yml`). (2) `_ignition_outcome(fire, df)` نقيّة:
> حقيقي (أعلى لاحق ≥`IGNITION_CONFIRM_PCT`=12%) · كاذب (إغلاق تحت الكسر قبل التأكيد) · معلّق — بلا نظر
> مستقبلي. (3) أداة التطوير `_ignition_log_block` تجلب الشموع اللاحقة (سقف 120، فاشل-آمن) → «إنذار كاذب X%
> من M محسوم» + تفصيل حسب تصنيف الشمعة (القروب يكذب أكثر؟ = دليل معايرة عتبات المضارب) · عيّنة <8 محسوم →
> «تتراكم» بصدق. **🔒 خارج rank_key/select_top/classify_tier/analyze_ticker/backtest_symbol (getsource) ·
> لا LOGIC_VERSION · لا قمع/وزن قبل عيّنة كافية + موافقتك.** 510 اختبار (12 جديد) · pyflakes نظيف.
> **الأثر:** أول أداة تقيس حافة Polygon حيًّا بدل الافتراض — النتيجة صادقة في الاتجاهين (قد تكشف أن الرادار
> أسوأ من إيحاء 83% أو تؤكّده). تتراكم الأرقام على أسابيع التشغيل الحي.

> ✅ 🕵️💰 **حزمة «قراءة المضارب» نُفِّذت كاملة ودُمجت (2026-07-08، أوبيس نفّذ خطة
> `FAISAL_OPERATOR_PACK_PLAN.md` — 9 صور فيصل بأرقام صريحة لأول مرة · طبقة عرض/تحذير فقط):**
> **PR-A (#115) = P1+P3:** P1 💰 وسم «شمعة مضارب/قروب» على تنبيه رادار الانطلاق بسيولة الشمعة
> بالدولار (`_ignition_candle_class`: ≥$100K مضارب · ≥$300K قوية · ≤$50K قروب يُصرَّف — CONFIG
> `IGNITION_USD_*`، `_ignition_signal` صار يعيد `usd`) — **وسم فقط لا فلترة** · P3 ⚠️ تحذير «سيولة
> قطيع قبل الرفعة» في `hand_activity_today` (حجم ضخم بلا كسر الرقم الحرج = قاعدة LABT −40%: المضارب
> يُهبِط عمدًا). **PR-B (#116) = P2+P4:** P2 🕳️ قرينة N5 «عروض شبه مُفرَّغة» في `hand_evidence`
> (`hand_check.py` يخزّن `flow_raw=polygon_flow`؛ سبريد ≥5% + دولارات أفضل عرض ≤$1000 = بصمة تجهيز
> مضارب — وكيل صادق، «عمق L2 غير متاح» داخل الدليل؛ **مسار الفرز لا يجلب flow_raw فلا نداء إضافي**) ·
> P4 🔁 `_split_frequency`/`_split_freq_line` وسم «تقسيمات متكررة (≥2/سنة) = نَفَس قصير ≤أسبوع» (ZCMD)
> في قسم D9 وفحص اليد. **⚠️ التأطير الصادق:** ترقية لحافة التوقيت المُثبَتة (§0-ك) + حماية —
> **ليست مؤشر اختيار** (الباب مُغلق بالدليل §0-ز/ط/ي). كل الخطوط الحمراء محفوظة: عرض/تنبيه فقط ·
> أقفال getsource (خارج rank_key/select_top/classify_tier/entry_status/analyze_ticker/backtest_symbol) ·
> لا LOGIC_VERSION · فاشل-آمن مطلق · العتبات موثّقة من كلام فيصل الحرفي (أي دور قرار لاحق = سجلّ حي +
> موافقة المستخدم). مراجعة خصومية إنلاين 3 عدسات لكل دمج. **498 اختبار (18 جديد) · pyflakes/ruff نظيف.**
> **🏁 لا مهام تطوير معلّقة — الحزمة آخر بند مخطَّط. المتبقّي: مراقبة إنتاجية (تشغيل الجمعة + سجلّ رادار
> الانطلاق الحي لقياس الإنذار الكاذب + إعادة تحقّق الانطلاق 2026 عند اكتمال السنة).**

> ✅🔥📏 **تحقّق رادار الانطلاق نجح (2025، شُغِّل 2026-07-08) → `MARKET_BACKTEST_FINDINGS.md` §0-ك +
> `IGNITION_VERIFY_PLAN.md`:** سؤال المستخدم «هل فادنا الاشتراك فعلًا؟» قِيس بمعيار مسجَّل مسبقًا: **18
> منفجرًا · الالتقاط 15/18=83% (≥50%) · الأبكرية +2% (≤+20%) → «حافة توقيت مفيدة بالدليل».** حين ينفجر
> زنبرك مؤهّل، الرادار يمسكه 83% والسهم +2% فقط من الافتتاح = الدخول قرب البداية. **أول عائد إيجابي
> مثبَت من Polygon** (الاختيار كله كان سلبيًّا). ⚠️ حدود صادقة: انحياز الناجين (**الإنذار الكاذب لم
> يُقَس تاريخيًّا → السجلّ الحي**) · عيّنة 18 · تقريبيّ (حاجز مُقرَّب). أداة `ignition_verify.py` +
> `ignition_verify.yml` (يدوي، دالّتان نقيّتان `_find_explosion_day`/`_ignition_first_fire` مقفولتان
> خارج الفرز). 471 اختبار. **تأكيد 2026 قيد التشغيل · لا وزن فرز — طبقة توقيت/تنبيه.**

> 🔥 **رادار الانطلاق اللحظي بُني ودُمج (2026-07-08، `IGNITION_PLAN.md` — النقلة النوعية بالدليل):**
> بعد أن أثبت T-ACC/OHLCV/A/B/اليد أن **الاختيار** عند القاع مستحيل، والرافعة الوحيدة = **التوقيت**
> (T1b + فيصل «لا تدخل الدعم الأول»)، والبوت أعمى عنه (Yahoo مؤخّر + بولنغ 30د) → حافة Polygon الحقيقية
> = **السرعة**. `ignition_live.py` (+ `ignition.yml`): **عامل جلسة واحد** من الافتتاح، يلفّ كل ~45ث يفحص
> القائمة المؤهّلة **كلها لحظيًّا** ويطلق «🔥 ادخل الآن» أول ما **يشتعل** زنبرك: قفزة حجم الدقيقة
> ≥`IGNITION_VOL_MULT`(=3)× + كسر الرقم الحرج صاعدًا + اتجاه صاعد + تأكيد `order_snapshot`. **رد فعل لا
> تنبّؤ** (فشل T-ACC لا ينطبق: الحجم/السعر واقعان، التدفق يؤكّد) → يسبق Yahoo بـ15-25د. دوال نقيّة
> (`_ignition_signal`/`_ignition_break_level`/`scan_ignition`/`build_ignition_alert`، حقن جالبات للاختبار).
> **🔒 إشعار/توقيت فقط — لا يحفظ القائمة (دِدوب بالذاكرة، صفر سباق حالة) · خارج rank_key/select_top/
> classify_tier/analyze_ticker/backtest_symbol (لا يمسّ الاختيار) · فاشل-آمن (بلا مفتاح=لا عمل) · لا
> LOGIC_VERSION.** القياس مسجَّل حيًّا (التقاط/أبكرية، لا رقم مُفبرَك). 465 اختبار (11 جديد). مراجعة 3 عدسات.

> 🔬 **T-ACC-2 مُسجَّل ويشتغل (2026-07-08، `T_ACC2_PREREGISTRATION.md`):** بعد فشل T-ACC-1، محور واحد
> جديد مُعلَّل: `block_buy_pct` = **عدوانية الشراء داخل الطبعات الكبيرة فقط** (اتجاه×حجم — T-ACC-1 قاس
> الاتجاه والحجم كلٌّ على حدة، لا تقاطعهما؛ «هل المال الكبير هو المُبادِر بالشراء؟»). نافذة 15ي بلا تغيير،
> **نفس المعيار المسجَّل** (فرق ≥10 + Wilson منفصلان + يصمد بالسنتين)، حارس صدق ≥5 طبعات مصنَّفة.
> مُضاف لـ`acc_components`+`_ACC_COMPS`+`_FIELDS` (خارج الكرت/الفرز، مقفول · لا LOGIC_VERSION). 454 اختبار.
> **⚖️ الحكم (شُغِّل 2026-07-08 → §0-ي): فشل بالسنتين** — `block_buy_pct` 2025 فرق −6 · 2026 فرق +14
> (متداخل بشدّة، الاتجاه ينقلب) = نفس بصمة ضجيج T-ACC-1. **أُغلق ملف «التجميع/الصفقات كمؤشّر اختيار».**
> الخلاصة القاطعة: 4 محاور صفقات × سنتين = 8 اختبارات كلها تفشل بالانقلاب — **داخل أسهم الهوية لا شيء
> عند القاع (شمعة أو صفقة خام) يتنبّأ بالمنفجر.** block_buy_pct يبقى حقلًا سياقيًا بلا وزن/كرت · صفر
> تغيير كود. لا T-ACC-3 بلا محور جديد + تسجيل + موافقة. **حافة Polygon = التوقيت لا الاختيار.**

> ⚖️ 🔬 **حكم T-ACC النهائي (شُغِّل على 2025+2026 يوم 2026-07-08) → `MARKET_BACKTEST_FINDINGS.md` §0-ط:
> فشل بالسنتين — التجميع الصامت يبقى عرضًا، لا وزن ترتيب.** العيّنة: 2025=106 إشارات (85 معبّأة، من
> 3562 رمزًا) · 2026=79 (62 معبّأة). لا مكوّن حقّق المعيار المسجَّل (فرق ≥10 + Wilson منفصلان + يصمد
> بالسنتين): شراء عدواني −7 ثم +17 (**ينقلب**) · طبعات كبيرة −4 ثم −9 (سالب صغير متداخل) · دارك −14
> ثم +2 (**ينقلب**). انقلاب الاتجاه = بصمة الضجيج نفسها (درس A/B وبصمة اليد §0-ز/ح). **صفر تغيير كود
> مطلوب** — الأقفال البنيوية جعلت الفشل بلا كلفة؛ القيمة النوعية بفحص اليد باقية. أي T-ACC-2 يلزمه
> تسجيل مسبق جديد + موافقة المستخدم (لا صيد post-hoc). أدوات: `acc_verify.yml` + `acc_report.yml`
> (يطبع CSV والحكم من artifacts في سجل الأكشن — بيئة الويب لا تنزّل artifacts). **حافة Polygon الحقيقية
> المتبقية = التوقيت** (كنسة الدقيقة ب + بريماركت ج + تدفق الأوامر لحظة الحدث)، لا الاختيار.

> ✅ 🌙 **Polygon الجزء ج نُفِّذ ودُمج (2026-07-08، `POLYGON_EDGE_PLAN.md` §ج — رادار البريماركت
> + session_ctx صادق) — اكتملت خطة حافة Polygon (3/3):** `_premarket_summary(bars, prev_close)`
> نقيّة (أعلى/أدنى/آخر + حجم تراكمي + تغيّر% عن إغلاق الأمس) · `polygon_premarket(sym)` (دقائق
> اليوم من `/v2/aggs/…/1/minute/…`، **فحص زمني قبل الشبكة**: None خارج نافذة 08:00–13:30 UTC =
> صفر نداء مهدور بالفرز الباكر 07:00 · فاشل-آمن). **حدث `premarket` 🌙** في `monitor_live_events`:
> تحرّك ≥`PM_MOVE_PCT`(=10 CONFIG) بحجم بريماركت → «راقب الافتتاح» (نفس الدِدوب، أيقونة 🌙). **كرون
> `pullback_monitor.yml` وُسِّع لـ`*/30 11-21`** (يشمل البريماركت). **إكمال `session_ctx` صادق:** enrich
> يملأ `pre_after` من Polygon لو المفتاح موجود+داخل النافذة ويبدّل السبب لـ«من Polygon» — وإلا None
> بسببه الصريح (يكمل P1-1 بشرف). **إصلاح لقيته المراجعة الخصومية (Lens 2):** قبل الافتتاح الشمعة
> اليومية=أمس (Yahoo بلا بار اليوم) فكانت أحداث الجلسة تُعيد إطلاق مسح/كسر الأمس صباحًا → أضفتُ
> **`premarket_only`** (يمرّره pullback_live لو الساعة <13:30 UTC): يتخطّى أحداث الجلسة ويكتفي
> برادار البريماركت الحي (وبلا مفتاح = صفر عمل = مطابق «التشغيلات المبكرة رخيصة»). 🔒 خارج
> rank_key/select_top/classify_tier/backtest_symbol · لا LOGIC_VERSION. **451 اختبار (11 جديد).**

> ✅ ⚡ **Polygon الجزء ب نُفِّذ ودُمج (2026-07-08، `POLYGON_EDGE_PLAN.md` §ب — كنسة الدقيقة
> الحية):** يرفع دقّة/توقيت تنبيه المسح القائم ببيانات Polygon **الحيّة** (Yahoo اليومي مؤخَّر).
> `polygon_minute_bars(sym, minutes=90)` (شموع الدقيقة من `/v2/aggs/…/1/minute/…`، فاشل-آمن →
> None) · `_minute_sweep(bars, support)` دالّة نقيّة: أدنى دقيقة خرقت الدعم >2% **ثم آخر إغلاق
> دقيقة استعاد فوقه** → True (خرق بلا استعادة/بلا خرق = False). في `monitor_live_events`: **لو**
> المفتاح موجود والمسار اليومي لم يرصد المسح **والسعر قرب الدعم (ضمن 8%)** → يفحص الدقائق على
> **نفس دعم `hand_activity_today`** (أدنى 20ج) → حدث `sweep` **نفسه** (لا نوع جديد، نفس الدِدوب).
> **🔴 فاشل-آمن:** بلا مفتاح = المسار اليومي حرفيًا (مقفول باختبار سلوكي + getsource). طبعات
> الاستعادة يغطيها سطر «📊 تدفق الأوامر» القائم. **سقف طلبات:** بوّابة القرب تحصر النداء بالأسهم
> قرب دعمها فقط (تحمي ميزانية المراقب 15د). 🔒 خارج rank_key/select_top/backtest_symbol · لا
> LOGIC_VERSION. مراجعة إنلاين 3 عدسات. **440 اختبار (7 جديد).** **التالي: الجزء ج (البريماركت).**

> ✅ 🔬 **Polygon الجزء أ نُفِّذ ودُمج (2026-07-08، `POLYGON_EDGE_PLAN.md` §أ — رادار التجميع
> الصامت + تجربة T-ACC):** فئة معلومات جديدة من الصفقات الخام عند القاع (بعد أن أثبت حكم السنتين
> أن كل مؤشرات OHLCV ضجيج §0-ز). **دوال نقية:** `_tick_classify` (قاعدة التيك: أعلى=+1·أدنى=-1·
> مساوٍ=يحمل الاتجاه·الأولى=0) · `acc_components` (من صفقات خام → شراء عدواني % [tick-rule] · طبعات
> كبيرة % [≥10× **وسيط** العيّنة، معايرة ذاتية نسبية] · دارك % [exchange==4 = FINRA TRF خارج
> البورصة، **موثّق** بمستندات Polygon] · None لو أقل من 30 صفقة). **غلاف فاشل-آمن:**
> `polygon_base_trades` (نافذة القاع، ترقيم صفحات، `end_date`=`timestamp.lt` للتجربة بلا تسريب) ·
> `silent_accumulation` (حي). **🔴 الخط الأحمر #1 محقّق:** بلا مفتاح/401/403/429/شبكة = None →
> يُعرض «—»، **صفر تعطيل للفرز** (enrich يقرأ المفتاح مرّة؛ بلا مفتاح يتخطّى القسم كليًّا · أسوأ
> تأخّر ~6د على ميزانية 300د). **العرض:** `acc_line` بالكرت + اليومي + فحص اليد (مع «—»).
> **تجربة T-ACC** (`acc_verify.py` + `acc_verify.yml` يدوي، input=السنة): يشغّل الباكتيست لسنة →
> لكل إشارة يجلب صفقات **ما قبل يوم الإشارة حصريًا** (بلا تسريب — تأكّد: `date`=آخر بار محلَّل، النتيجة
> بالنافذة الأمامية بعده) → `acc_verify_report` يبوّب كل مكوّن لأثلاث + Wilson، معيار مسجَّل مسبقًا
> (فرق انفجار 10+ نقاط + فاصلان منفصلان **ويصمد بالسنتين**). CSV تراكمي (استئناف). **🔒 قفل حاسم:**
> دوال التجميع خارج rank_key/select_top/classify_tier/entry_status/analyze_ticker/backtest_symbol
> (مقفول getsource) · **enrich يُنفَّذ بعد select_top بنيويًا** → acc لا يمكن أن يمسّ الاختيار · لا
> LOGIC_VERSION · لا وزن ترتيب قبل نجاح T-ACC + موافقة المستخدم. مراجعة خصومية إنلاين 3 عدسات
> (فاشل-آمن/بلا-تسريب/قفل-العرض) اجتازت. **433 اختبار (17 جديد).** **التالي: المستخدم يشغّل أكشن
> T-ACC Verify لـ2025 ثم 2026 (يلزم سرّ `POLYGON_API_KEY`) — الحكم بالأرقام. ثم الجزء ب/ج.**

> 💣 **المهمة الجاهزة للتنفيذ الآن (2026-07-08) → `POLYGON_EDGE_PLAN.md` (اقرأه كاملًا ونفّذ
> بالترتيب):** المستخدم عنده اشتراك **Polygon Advanced ($199)** وطلب «فكرة فتّاكة تسبقه بمراحل من
> ناحية الارتكاز». Claude خطّط، **أوبيس ينفّذ** — 3 أجزاء بثلاثة دمجات منفصلة:
> **أ) 🔬 رادار التجميع الصامت (الجوهرة، أولًا):** مكوّنات من الصفقات الخام عند القاع (شراء عدواني
> tick-rule · طبعات كبيرة نسبية · دارك بول TRF) — فئة معلومات جديدة كليًا بعد أن أثبت حكم السنتين أن
> كل مؤشرات OHLCV ضجيج (§0-ز) + **تجربة تحقق T-ACC مسجّلة مسبقًا** (`acc_verify.py`+workflow يدوي،
> معيار: فرق انفجار 10+ نقاط وفواصل Wilson منفصلة **ويصمد بالسنتين**، لكل مكوّن على حدة) — لا وزن
> ترتيب قبل نجاحها وموافقة المستخدم. **ب) ⚡ كنسة الدقيقة الحية** (monitor_live_events بدقائق Polygon
> لو المفتاح موجود — وإلا مسار Yahoo حرفيًا). **ج) 🌙 رادار البريماركت** (حدث premarket بالمراقب +
> توسعة الكرون 11-21 UTC + إكمال session_ctx بصدق من Polygon). **⚠️ شرط المستخدم الصلب بالخطة:**
> فاشل-آمن مطلق — انتهى الاشتراك/غاب المفتاح = «—» ويكمل كل شيء (قفل اختبار). عرض/تسجيل فقط ·
> لا LOGIC_VERSION · اختبارات بلا إنترنت (دوال نقية + فكستشرات). **الحالة: لم تُنفَّذ — جاهزة لأوبيس.**

> ✅ 🕵️ **أداة «فحص اليد» المستقلة (2026-07-08، طلب المستخدم «سهم أبي أتأكد لو وراه مضارب + يحلّله
> كسهم ارتكاز»):** `hand_check.py` (`HAND_CHECK=رمز` · workflow `hand_check.yml` يدوي input=ticker).
> `render_hand_check(sym, r, df)` دالة نقية قابلة للاختبار: حكم **نوعي بعدد القرائن** (لا درجة مبتدعة —
> §0-ح: 3+ «قرائن قوية» · 2 «مشتبه» · 1 «ضعيف» · 0 «لا قرائن») + قائمة القرائن (hand_evidence) +
> «ماذا فعلت اليد اليوم» (hand_activity_today) + بصمة 🧬 + **قسم «التحليل كسهم ارتكاز»** (مؤهّل →
> entry_status/interp_card_lines/دخول/أهداف · مرفوض → السبب من _REJECT_STATS). يعيد استخدام دوال
> البوت (behav/pump_scar/four_hour_levels/analyze_ticker/build_interpretation) بلا منطق فرز جديد ·
> صدق الطلبات (تدفق حي غير متاح يُقال حرفيًا). 🔒 عرض/تشخيص فقط · لا يمسّ الفرز/الحالة. **402 اختبار.**

> ✅ 🟢👀 **فصل «جاهز للدخول» عن «متابعة» نُفِّذ ودُمج (2026-07-08، خطة `ENTRY_READY_SPLIT_PLAN.md`):**
> طلب المستخدم: «فرّق بين سهم متابعة وسهم جاهز للدخول». دالة نقية `entry_status(s)` (بعد
> `readiness_ratio`) تقرأ **موقع السعر** من `entry_mode` بطبقة التفسير (منهجية المقطع P0-5: قرار فيصل
> = موقع السعر لا اكتمال بوابات، **لا عتبة جاهزية** — النسخة 1 بعتبة 75 أُلغيت لأنها كانت ستُبقي قسم
> «جاهز» فاضيًا للأبد = مصيبة A ثانية). near_support/sweep_confirmed → 🟢 جاهز للدخول الآن ·
> reclaim_wait/no_entry_far → 👀 متابعة + السبب وما يحوّله جاهزًا. احتياط للسجلّات القديمة بلا interp
> ينسخ صيغ build_interpretation حرفيًا (pivot*0.995 · max(trs)*1.05). العرض: التقرير اليومي **قسمان**
> (🟢/👀) + ترويسة عدّ «N سهم: X جاهز للدخول · Y متابعة» + ترقيم مستمر + سطر «👀 السبب» للمتابعة فقط ·
> سطر حالة بالكرت (build_message) والفحص اليدوي (يرث تلقائيًا). **🔒 عرض فقط:** خارج
> rank_key/select_top/classify_tier (مقفول) · الجاهزية لا تدخل القرار (مقفول ضد رجوع 75) · t1/t2/t3/الوقف
> لا تتغيّر · لا LOGIC_VERSION. **21 اختبار قبول · 346 اختبار كلها تنجح.** المستخدم يشغّل الأكشن
> بـforce_renew=0 والحالة تُحسب وقت العرض فتظهر فورًا للقديم والجديد.
> **+ إصلاحان من التقرير الحي الأول (2026-07-08، ملاحظة المستخدم «وين سلوك المضارب؟»):**
> (1) سطر 🧬 كان يغيب عن الأسهم المضافة **قبل** ميزة البصمة (تُخزَّن وقت الإضافة فقط — ظهر للجدد
> FEMY/GEOS وغاب عن القدامى) → **تجديد يومي** لـbehav+bars_after في update_watchlist_status
> (بحارس لا-يمسح: score=None لا يمحو بصمة صالحة). (2) **سطر 🕓 4س** أُضيف لأسطر التفسير (≤4 الآن):
> قصة شموع المقطع — «رأس الحمرا $X مقاومة — تجاوزه يؤكّد» / «مقاومة انقلبت دعمًا» / «ذيل مسح ثم
> استعادة» (weak لا يُعرض). **350 اختبار.**

> ✅ 🏁 **إكمال نواقص المقطع الثلاثة الأخيرة (2026-07-08، تدقيق «ناقص شي؟» — عرض فقط · لا LOGIC_VERSION):**
> (1) **تغطية الخضرا** (المقطع: «تغطية الحمرا بخضرا تعطي تأكيد»): `four_hour_levels` يحسب `green_cover`
> (آخر حمرا هل أغلقت بعدها خضرا فوق جسمها؟ True/False/None=لا حمرا) → حالة `waiting_green_cover`
> بسياق 4س + سطر «🕓 4س: الحمرا الأخيرة بلا تغطية خضرا — ننتظر التأكيد» ولاحقة «مغطّاة بخضرا» على سطر
> الحاجز. (2) **targets_src (P1-4)** كان موثّقًا كمنفَّذ وهو غير منفَّذ (غلطة توثيق ورثناها) → نُفِّذ فعلًا:
> استدلال بالمطابقة (مقاومة فرعية/أساسية · رأس حمرا 4س · التحرر · حافة فجوة · وإلا «سلّم المقاومات
> اليومي» الافتراضي الصادق) يُخزَّن في `interp.targets_src` + حقل `src` بأدوار المستويات — لا يغيّر الأهداف
> (قفل D5). (3) **عمق الارتكاز في `technical_report.py`**: `pivot_depth_section` — يظهر **فقط** لو
> السهم مؤهّل بالفارز (التقرير الكلاسيكي يبقى نقيًّا): حالة الدخول + أسطر التفسير + أدوار المستويات
> بمصادرها + الدورة + 🧬. **361 اختبار (كلها تنجح) · pyflakes نظيف · ruff صفر جديدة.**
> **🏁 لا مهام تطوير معلّقة — المتبقي الوحيد: مراقبة إنتاجية لأول تجديد بعد إغلاق الجمعة
> 2026-07-10 (كرون 22:00 UTC)** — تجديد القائمة + تقرير التطوير الأسبوعي + حصاد الأسبوع، والتحقّق
> أن القسمين 🟢/👀 وسطر 🕓 و🧬 والأحداث المعلنة يظهرون سليمين للجميع. ⚠️ **تأكيد بأول تشغيل: أن
> كرون الجمعة 22:00 رفع RENEW_ON_CLOSE=1 فعلًا (سجل «تجديد أسبوعي (الجمعة بعد إغلاق السوق)»)، وأن
> شمعة الجمعة كانت مكتملة بيانات yfinance وقتها.** (مراجِع Cline يبقى الجمعة 08:17 UTC — workflow
> مستقل يدقّق الكود لا القائمة.)

> 🧹 **تنظيف بقايا A بعد التقاعد (2026-07-08، سؤال المستخدم «كل الأسهم B و0 في A — مشكلة؟»):**
> الجواب: «0 في A» **ليس مشكلة فرز بل بقايا عرض/سجل** بعد التقاعد — A وهمية بالدليل ولن تُنتَج.
> أُزيلت البقايا كلها: سطر سجل الفرز «0 (A صارمة)+N (B مراقبة)» → «N مرشح مؤهّل (مرتّب بالجاهزية)» ·
> مسار الترقية B→A الميت في `check_promotions` (ترجع [] دائمًا، الاسم باقٍ للتوافق) · افتراض
> `make_watch_entry` صار "B" · وسيط `tier` أُزيل من `readiness_badge/tag/ratio` (+كل النداءات) ·
> حكم الفحص اليدوي 🅰️/🅱️ → «🎯 مؤهّل» موحّد · تقرير التطوير: شريحة القائمة تُوسَم
> «A (تاريخي — تصنيف متقاعد)/مؤهّل/مراقبة ارتداد» واقتراح «A أفضل من B» المستحيل أُزيل.
> وسم «تخفيف: كسر الدعم» الحي **باقٍ** (مفيد، يخص الخبر لا التصنيف). 7 أقفال 🧹 جديدة. **325 اختبار.**

> 🪦 **تقاعد تصنيف A/B (2026-07-05، حل جذري بالدليل) → `MARKET_BACKTEST_FINDINGS.md` §0-ز:** جهّزنا
> الباكتيست يسجّل النواقص/الجاهزية وشغّلناه على 2025+2026. **A ميتة (0 سهم بلغها) وضجيج** (النجاح موزّع
> بالتساوي على شرائح النواقص؛ الاتجاه ينقلب بين السنتين؛ الجاهزية كذلك). داخل أسهم الهوية **لا مؤشّر حالي
> يميّز المنفجر**. **القرار: تقاعد A/B الوهمي** — `classify_tier` فئة واحدة "B"+None(رفض محفوظ) · `rank_key`
> بلا مفتاح A/B (الجاهزية هي المحور) · لا إنتاج لـA (analyze_ticker/check_promotions) · شارة موحّدة 🎯 +
> «جاهز» بالجاهزية · ترقية/نزول B↔A متقاعدة. **🔒 عضوية select_top مطابقة حرفيًا (مقفولة) · W غير مُمَسّة ·
> بوابة الرفض محفوظة · لا LOGIC_VERSION.** 277 اختبار. (ملاحظة: مساعد التطوير شريحة A صارت فارغة = وسمية.)

> 🧭 **المهمة الكبيرة الجاهزة للتنفيذ (2026-07-05) → `INTERPRETATION_LAYER_PLAN.md` (اقرأه كاملًا قبل البدء):**
> المستخدم أرسل تقرير نواقص خارجيًا («ذهب» عنده — تطابق ~79%، الناقص **طبقة تفسير/قرار** لا مؤشرات) وطلب
> تصفيته وتجهيزه لأوبيس. صُفّي بندًا-بندًا ضد قراراتنا المحسومة: **معتمد** = setup_type · level_roles ·
> critical_number (فيصل موثّق) · activation_state (وسم فقط، الأهداف لا تتغيّر) · مصدر كل هدف · entry_mode
> (وصفي، sweep وصفًا لا شرطًا) · سياق 4س (بلا وقف تكتيكي) · risk_profile (تجميع، بلا تجريم الفلوت الصغير) ·
> ≤3 أسطر مدمجة بالكرت؛ مرحلة 2: خط الترند الهابط (مواصفة+مراجعة خصومية) · cycle/session دنيا · توسعة 🧬.
> **مرفوض:** وقفان (يصادم حكم الوقف) · أعلام فلوت/رسملة · استبدال الكرت · ثقة رقمية مخترعة. الخطوط الحمراء
> العشرة في الملف ملزِمة (عرض فقط · تطابق analyze_one · ثبات العضوية · حيّ فقط · لا LOGIC_VERSION).
> ⚠️ حدّ الاستخدام الأسبوعي حجب ورك فلو الوكلاء (يتصفّر 7 يوليو 9م UTC) — التنفيذ الثقيل بعده أو إنلاين بحذر.

> ✅ **المرحلة 1 من طبقة التفسير نُفِّذت ودُمجت (2026-07-05):** `build_interpretation(r)` + `interp_card_lines`
> — setup_type · **الرقم الحرج** · activation_state (الأهداف مفعّل/معلّق، وسم فقط) · entry_mode (وصفي) ·
> four_hour_context · risk_profile (تجميع، بلا تجريم الفلوت) · level_roles. مربوطة بـ scan_market +
> make_watch_entry + build_message/build_daily + analyze_one. عرض فقط · لا LOGIC_VERSION · لا تمسّ العضوية
> (مقفول: interp خارج rank_key). **287 اختبار.** الكرت صار يعرض 🧭 الإعداد · 🎯 الرقم الحرج · ⚠️ الخطر.
> **المتبقّي = المرحلة 2** (خط الترند الهابط بمواصفة+مراجعة خصومية · cycle/session دنيا · توسعة 🧬 · وسم
> الأهداف المعلّقة على سطورها) → `INTERPRETATION_LAYER_PLAN.md` §10-13.

> ✅ **المرحلة 2أ من طبقة التفسير نُفِّذت (2026-07-07، عرض/تفسير فقط · لا LOGIC_VERSION):**
> §11 `cycle_context` (جلسات منذ آخر رفعة/القاع + حالة النافذة 30-50ج؛ `bars_after` يُخزَّن من
> analyze_ticker — حقل تفسيري) · §12 `session_ctx` نسخة دنيا صادقة (snapshot من info في enrich؛
> pre/after غير متاح → سبب صريح لا تخمين) · §13 `behavior_tags` («صيد وقفات متكرّر» sweeps≥2 ·
> «رفعة قديمة وخمول طويل» BJDX recency≥120) تلحق بسطر 🧬 بالكرت واليومي · **وسم «(معلّق حتى $X)»
> على أسطر الأهداف** بالكرت + «(معلّق)» باليومي مع سطر «⏳ يتفعّل بتجاوز $X» (الأسعار نفسها لا تتغيّر
> — قفل D5). **+3 إصلاحات ربط لقاها الفحص:** (1) التفسير كان يُحسب **قبل** enrich ففاتته h4/أخبار/SEC
> → يُجدَّد الآن آخر enrich بحارس لا-يمسح؛ (2) **تجديد يومي** في update_watchlist_status (سقوط
> `last_price` بدل price — الرقم الحرج/وضع الدخول يتحرّكان مع السعر بدل التجمّد يوم الترشيح؛
> make_watch_entry يخزّن sec_filings/recent_split/bars_after/session_ctx للتجديد)؛ (3) analyze_one
> كان بلا 🧬/bars_after → أُضيفا (تطابق الفحص اليدوي). **304 اختبار.**

> ✅ **المرحلة 2ب — §10 خط الترند الهابط (2026-07-07، آخر بنود الخطة — اكتملت كلها):**
> `descending_trendline(df, price)`: قمم سوينغ **فريدة** (الهضبة المسطّحة ليست قمة — حارس أساسي ضد
> فبركة خط من ضجيج القاعدة، لقطه الاختبار) · لمستان+ · ميل سالب · تسامح 2% · لا خط لو اخترقته قمة
> وسيطة (يُسمح آخر 5 شموع = الكسر الحديث) · إسقاط عند آخر شمعة فقط (لا نظر مستقبلي) · state
> below/testing/broken (الكسر **بالإغلاق**). يغذّي الرقم الحرج (**أقرب** حاجز min — مواصفة §3) وتعليق
> الأهداف؛ المكسور لا يحجب. حيّ فقط (scan_market/analyze_one؛ مقفول باختبار: خارج backtest_symbol) ·
> يُجدَّد يوميًا في update_watchlist_status من بيانات اليوم (الخط الهابط ينزل يوميًا). أقفال: خارج
> rank_key/classify_tier · t1-t3 لا تتغيّر (D5). **المراجعة الخصومية نُفِّذت** (ورك فلو 3 عدسات + دحض؛
> انقطع الوكلاء بحدود API فحُصدت ملاحظاتهم من الترانسكربتات وحُكم عليها إنلاين — 7 ملاحظات بأدلة تشغيل):
> **5 أُصلحت:** (1) حد أدنى للانحدار (ازدواج قمة شبه أفقي 10→9.99 كان يُوسم «خط هابط» — الآن نزول
> الخط ≥ التسامح وإلا None)؛ (2) **مرساة متعددة** (قفل المرساة على أعلى قمة كان يُسقط الخط الحقيقي عند
> ازدواج القمة/الريتيست — الآن كل قمة مرشّحة مرساةً)؛ (3) **الحالة لكل مرشّح + القائم يُقدَّم** (خط مكسور
> كثير اللمسات كان يحجب خطًا قائمًا يسقف السعر فيختفي الحاجز)؛ (4) توحيد أعمار الحواجز (check_promotions
> يجدّد key_levels بعد بناء التفسير → صار يعيد بناءه بعد التحديث)؛ (5) تعليق حارس مضلّل (إسناد الترند
> بلا شرط **عمدًا** — None = الخط مات يختفي اليوم نفسه؛ وُثّق ضد «إصلاح» مستقبلي يخلق قيمة بالية) +
> تحصين قفل الباكتيست (يشمل analyze_ticker/_diagnose_symbol ضد refactor). **1 جزئية وُثّقت:** تقلب
> الخط بتطوّر البنية (رأس كسر فاشل يصير مرساة) لقطة يومية مقصودة. **1 سليمة:** مسار الباكتيست نظيف
> فعلًا. كل ملاحظة مؤكَّدة صارت **قفل اختبار**. **319 اختبار.**
> ⚠️ ملاحظة معروفة (مقصودة): مقاومة 4س في التفسير تبقى من يوم الترشيح (لا جلب 4س يومي إضافي —
> التكلفة الشبكية لا تبرَّر؛ الترند/المستويات اليومية تُجدَّد يوميًا).

> 🔬 **آخر فحص عميق (2026-06-26):** أُصلحت 4 بنود في **طبقة التحليل/التقارير فقط**
> (لم تُمسّ جذور الفرز): تصنيف `M4_base`=«ليس ارتكازًا» · CSV يرجع `short_pct` ·
> `was_pivot`=هوية M1-M3 (`_had_pivot_identity`) · **حارس التغطية اليومي** بـ
> `run_daily_watchlist` (كان غائبًا 4/5 أيام = أخطر ناقل تفويت صامت). 181 اختبار.
> **الشكوك المعلّقة + ما نفحصه بتقرير الأسبوع الجاي → `PENDING_VERIFICATION.md`**
> (تواتر التغطية · ثغرة RSI-لم-يتشبّع · عتبة M4 · حارس الأرشفة · صفقات محسومة).

> 🧰 **اقتباس أداتَي التطوير في أداة البوت المدموجة (أوبيس 2026-07-04، طلب المستخدم):**
> المستخدم أعطى أداتَي تطوير من بوتين يملكهما (`dev_assistant_standalone.py` +
> `dev_backtest_toolkit.py`) وطلب اقتباس **أقصى قيمة** منهما في `build_dev_assistant_report`.
> خطة workflow (6 وكلاء/عدسات + ناقد اكتمال) ثم تنفيذ. **طبقة تقارير فقط — لا تمسّ
> الفرز/الدخول/الوقف/الأهداف (لا LOGIC_VERSION).** نُفّذ 5 ميزات:
> - 📅 **«التطوير مقابل الأسبوع الماضي»** (`_weekly_compare_block`): قبل/بعد بتاريخ
>   **الإغلاق**، فرق نسبة النجاح بالنقاط (متين ضد ذيل صفقة)، حارس عيّنة <3. **يجيب
>   سؤال المستخدم المتكرّر تلقائيًا كل جمعة** («كم التطوير مقابل الأسبوع اللي راح؟»).
> - 📏 **مقاييس صادقة** (`_honest_metrics_block`): الوسيط + استبعاد الأعلى/الأدنى +
>   **اعتماد الذيل** (بركة الرابحين، خط أساس k/W، حارس W≥5) — يكشف إن كانت الحافة
>   يحملها ذيل قِلّة أم عريضة. (البيانات الحيّة: حافة **عريضة** — الوسيط 15% ≈ المتوسط 18%.)
> - 🎯 **توقّع R** (`_realized_r`): الهدف المُصاب مقابل الوقف. البيانات الحيّة **+0.9R**
>   رغم نجاح 80% (الرابح يلمس t1 غالبًا) — يكشف أن الحافة أواضع مما توحيه النسبة.
> - 📊 **أرضية Wilson 95%**: نجاح 80% أرضيته الموثوقة **49%** على 10 صفقات (حدّ إحصائي صلب).
> - **235 اختبار · pyflakes نظيف · رُندر على بيانات حيّة.**
> - ⚠️ **ميزة سادسة صُمّمت ونُفّذت ثم أُزيلت بعد مراجعة خصومية (7 وكلاء/3 عدسات):**
>   «صرامة الفائتة» (`would_stop`) كانت تقيس أقصى هبوط من `c[-11]` (رأس النافذة =
>   غالبًا وسط الانهيار قبل تكوّن القاع) وتسمّيه «ربح ورقي كان بيوقفنا». المراجعة
>   أثبتت (3 عدسات مؤكِّدة) أنها **تُخطئ وسم نمط الارتكاز نفسه** (انهار→تجمّع→انفجر)
>   وتُخفي فرصًا فائتة حقيقية من بسط «فائتة/مقام» — يناقض مبدأ «لا سهم >70% إلا ونعرفه».
>   لا مرجع دخول نظيف لسهم مرفوض في OHLCV → **أُزيلت كليًا** (الصدق أهم من ميزة تجمّل الأرقام).
> - 🧪 **مقاييس صادقة للباكتيست** (`backtest_honest_summary`، طلب المستخدم «أضف ميزة
>   الباكتيست»): اقتباس `honest_summary` — العائد المحقّق (رابح=t1 · خاسر=الوقف)
>   المتوسط+الوسيط · توقّع R · **فاصل ثقة Wilson 95%** (إشارة أم ضجيج؟ بلا bootstrap،
>   قابل لإعادة الإنتاج) · اعتماد الذيل · الأشهر الموجبة · سطر «معيار القرار المسجَّل
>   مسبقًا» (التجربة الزوجية لا تناسب تغيير البوابة). مراجعة خصومية (3 وكلاء) لقّت
>   ثغرة إفصاح: الصفقات «العالقة» (open) كانت خارج النسبة بلا عرض عددها → **أُصلح**
>   (تُفصح بـ`ℹ️` + `st["open"]`). باكتيست فقط، لا يمسّ الفرز. **239 اختبار.**
> - 📅 **قصر الباكتيست على شهر تقويمي** (`_filter_trades_by_month`، طلب المستخدم «احدد
>   الشهر ١ او ٢»): مدخل `month` (1-12) بـ`backtest.yml` → `BACKTEST_MONTH` → يقصر
>   الصفقات على ذلك الشهر من **آخر سنة متوفّرة** (البيانات ~800ي فقد يتكرّر الشهر).
>   فارغ/غير صالح = كل التاريخ. الفترة تظهر بالتقرير (`📅 الفترة: YYYY-MM`). باكتيست
>   فقط، لا يمسّ الفرز. **241 اختبار** (يشمل: فارغ · 13 غير صالح · شهر بلا صفقات · تعدّد السنوات).
> - 🎯 **رموز الباكتيست اختيارية + كون افتراضي** (`_default_backtest_symbols`، طلب المستخدم
>   «يكفي الشهر بلا تعبئة الرموز»): `symbols` صار `required:false` بـ`backtest.yml`، وعند
>   الفراغ → اتحاد **القائمة + الأرشيف + التنبيهات** = كون البوت المعروف (حي: 25 رمز).
>   فيكفي تحديد `month` وحده. ⚠️ انحياز اختيار (رموز رشّحها البوت) — التقرير يحذّر.
>   باكتيست فقط. **242 اختبار.**
> - 🌍 **باكتيست «السوق الكامل»** (طلب المستخدم «امسح السوق كامل لأسهم الارتكاز وشوف اللي
>   انفجر واللي لا»): **رموز فارغة = مسح ناسداك كامل** (بلا انحياز اختيار) — بسّطناها من
>   خانتين لخانة واحدة (أُزيل مدخل `market`؛ المستخدم كان ينسى تعبئته فيرجع لكون البوت
>   الـ25). `get_universe()` مقصورًا على شهر عبر `date_window` في `backtest_symbol`
>   (`month` المحدّد أو الافتراضي). رموز صريحة → تلك فقط. تعذّر ناسداك → كون البوت احتياطيًا. كل صفقة تحمل `fwd_max_gain`+`exploded` (صعد ≥50% بعد
>   الدخول = «انفجر» بمعنى فيصل). التقرير: «💥 انفجر فعلًا: X من Y (%)» + أقوى الانفجارات.
>   `timeout` رُفع لـ300د · وضع السوق يتخطّى التشخيص المفصّل ويسقّف اللوق.
>   ⚠️ **بَقّ HIGH لقّته المراجعة الخصومية (وأُصلح قبل الدمج):** الافتراضي كان «آخر شهر
>   مكتمل» لكن نافذته الأمامية (fwd=40ج) ناقصة (البيانات تنتهي اليوم) → يُستبعَد كل دخوله
>   بحارس `i<n-fwd` → **تقرير فارغ صامت**. الإصلاح: `_forward_window_complete` يختار أحدث
>   شهر نافذته مكتملة (حاليًا أبريل) + تحذير لو طُلب شهر حديث. **247 اختبار.**
>
> 🔎 **تحقيق أسهم فيصل الأربعة (تغريدة، أوبيس 2026-07-04) — «ليش ما رُشِّحت؟»:**
> AZI · DSY · EHGO · ZCMD (عملت ~5000% بشهر؛ التغريدة تصفها «تعتمد ع القروبات» +
> «صيني اخر»). المستخدم صحّح خطأً بتحليلي الأول (بيانات **بعد** الانفجار = قاعدة 500%
> مضلِّلة) → أضفت `cutoff` لـ`_diagnose_symbol` + مسح تاريخي (-15..-60ي) بـ`run_backtest`
> **يقيّم السهم عند قاعه قبل الركض** (باكتيست فقط، لا يمسّ الفرز).
> - **المفاجأة:** 3 من 4 (ZCMD/DSY/AZI) **كوّنت قاعدة ضيقة فعلًا (23-35%) وRSI مثاليًا
>   (26-28) عند قاعها** — M4/RSI كانتا ستُجازانها! لم تُرفض كـ«ليست ارتكازًا».
> - **السبب الحاسم الوحيد = M5 السيولة:** الأربعة تداولت **$10K-$170K/يوم** عند قاعها
>   (ZCMD سهم $68 يتداول ~150 سهم/يوم = قوقعة خامدة) بينما الأرضية $200K. حتى الأنظف
>   (EHGO، بلا تقسيم) يسقط عليه ($32-165K). M5 = الدرع الصحيح ضد شِلل الضخّ.
> - **فحص خصومي (Workflow، 7 وكلاء · 3 عدسات: سيولة/دخول · تطبيع التقسيم · دلو مضاربي):**
>   **الثلاث انهارت.** أي التقاط سيولي = القفل C3 بمسمّى جديد (80%→17%)؛ تطبيع التقسيم
>   لا ينقذ أحدًا (M5 يُلزِم)؛ الدلو المكشوف يقلب البوت لمُضخّم ضخّ. **الحكم: لا تغيير
>   جذري — الرفض حماية صحيحة (تأكيد رابع).** إشارة الضخّ («قروبات/صيني») **خارج OHLCV**
>   بنيويًا — البوت أعمى عنها؛ الأربعة انحياز ناجين خلفها جدار يانصيب. **226 اختبار.**
>   - ✅ **نُفّذ (طلب المستخدم «اي ضفه»):** رفعُ كاشف المتحرّكين لأعلى مستوى — انظر أدناه.
>
> 🚪 **رفع كاشف المتحرّكين (أوبيس 2026-07-04، طلب المستخدم: «لا سهم يرتفع >70% إلا وحنا
> عارفين بوابة رفضه بالضبط»):** طبقة تشخيص/تعلّم — **لا تمسّ الفرز** (لا LOGIC_VERSION).
> - **`base_reason`**: كل متحرّك يحمل الآن **البوابة الدقيقة التي رفضته عند قاعه قبل
>   الركض** (نشغّل `analyze_ticker` على شريحة ما قبل الانفجار) — لا الرفض الحالي بعد
>   الانفجار (كانا مختلفين: القاع=M5 · بعد الانفجار=M2/M4). «مرشّح» = اجتاز الفارز عند
>   قاعه (أقوى إشارة تفويت حقيقي → يُبرَز بـ⚠️ بالتقرير).
> - **شبكة «التجمّع»** (`_run_explosion`، `EXPLOSION_RUN_PCT=70`): تلتقط الصاعد التدريجي
>   >70% (قاع→قمة، نافذة 20ج) **بلا يوم قفزة ≥50%** — فلا يفلت متحرّك كبير. حقل `kind`
>   (قفزة/تجمّع). المسار الأصلي (قفزة ≥50%) بلا تغيير سلوك.
> - **التقرير:** 🚪 توزيع بوابة القاع على **كل** المتحرّكين + أكثر بوابة فوّتت ارتكازًا +
>   ⚠️ «كان مرشّحًا ولم يدخل». سقف السجل 600 (الارتكازات أولًا، لا قصّ صامت). **230 اختبار.**
> - ⚠️ **تصحيح المستخدم المهم (منهجية فيصل):** «فيصل يموت في الأسهم اللي **توها مقسمة**
>   لأن فرصة انفجارها عالية» → **التقسيم العكسي ليس سبب رفض؛ بل علامة ارتكاز إيجابية**
>   (هندسة فلوت). الأربعة سقطت على **السيولة M5 فقط، لا على كونها مقسّمة.** (أُصحّح تأطيري
>   السابق «تقسيم=قوقعة ضخّ».) **سؤال مفتوح يُراقَب بالأداة الجديدة:** هل M2 (هبوط>97%
>   منفوخ بالتقسيم) يرفض خطأً سهمًا مقسّمًا حديثًا **سائلًا** (اجتاز M5) يريده فيصل؟ لو
>   ظهر ذلك بتوزيع البوابات (مقسّم سائل مرفوض بـM2) → عندها فقط نبحث تطبيع التقسيم
>   (بموافقة + باكتيست). حتى الآن لا دليل (كل مقسّم رأيناه سقط على M5 أيضًا). **لا نمسّ M2
>   الآن (قفل C3 · الأداة تجمع الدليل أولًا).**
>
> 🧹 **تنفيذ `OPUS_NEXT_PLAN.md` (أوبيس 2026-07-04) — خطة الإغلاق:**
> - ✅ **N1** ثغرة RSI-لم-يتشبّع: حُسمت بالإبقاء (خيار أ — فيصل 23-27؛ القاع غير
>   المتشبّع نموذج ناقص). توثيق فقط، لا تغيير جذر. الخيار ب متاح لو أراده المستخدم.
> - ✅ **N2** تنظيف الأرشيف: `_dedup_history` (أحدث إدخال لكل week_start) في حارس
>   الأرشفة + `save_watchlist` (ذاتي الشفاء). حي: 16 إدخال → 5 أسابيع فريدة. قفلان.
> - 🕓 **N3/N4** مراقبة جمعة 2026-07-10 (تنبيه Cline + إضافات التقرير الجديدة).
> - **226 اختبار · pyflakes نظيف · لا LOGIC_VERSION (ذاكرة/توثيق فقط).**
> - 🎉 **`PENDING_VERIFICATION.md` بلا معلّقات فعّالة لأول مرة** — المشروع دخل وضع
>   «تشغيل ومراقبة» (التطوير القادم تقوده البيانات الحية، لا الفحص اليدوي).

> 🎛️ **تنفيذ `OPUS_TUNING_PLAN.md` (2026-07-03، بموافقة المستخدم «ابدأ») — القسمان أ+ب:**
> - ✅ **A1** `record_reject_stats`: مقام أسباب الرفض يُخزَّن يوميًا (56 يومًا) في
>   `weekly_watchlist.json` + قسم «🧮 مقام الرفض» بتقرير مساعد التطوير (نسبة فائتة/مقام).
> - ✅ **A2** وسم القفزات الوهمية (>300% = `suspect_split`): تُفصل من إحصاء الفائتة
>   وتُعلَّم 🔍 بالانفجارات (وسم لا حذف). **A3** التنبيهات الجديدة تحمل 9 سمات تعلم
>   (tier/sector/rsi/float/short/short_pct/drop/spike/rr) — كانت 7/10 صفقات بلا سمات.
> - ✅ **A4** حُسم بند التغطية: عيّنة 2026-07-03 = 94.6% (3366/3559) بلا تحذير + قياس دائم.
>   **A5** حارس العيّنة الصغيرة (<20 صفقة) + توثيق مفارقة القوة (خاسرون 80 > رابحون 63).
> - ✅ **B1** مفاتيح تجربة A/B (`BT_BASE_RANGE_MAX`/`BT_MIN_DROP_FLOOR`) تعمل في وضع
>   BACKTEST **حصريًا** — الإنتاج محصّن بقفل اختبار. + مدخلات بـbacktest.yml.
> - 🔬 **B2/B3 حُسمت (2026-07-03):** تجربة A/B على 132 رمزًا — A (40/40): محسومة=4
>   نجاح **50%** · B (50/35): محسومة=5 نجاح **40%** (خسارة إضافية، صفر أرباح جديدة،
>   رغم عيّنة منحازة لصالح B). **القرار: العتبتان تبقيان 40/40 + قفل اختبار B3.**
>   المقام اليومي: M4=309 (فائتة 11.7% · انفجارات ارتكازية 0.65%) · M2=1867 (0.05%).
> - ✅ **C4 (موافقة المستخدم):** استثناء `analyze_ticker` يُسجَّل تشخيصيًا (كان صامتًا) —
>   لا يغيّر النتيجة، + قفل «بيانات فاسدة → None بلا انهيار».
> - 🔬 **تجربة C — تحقيق «ليش ما يلتقط أسهم فيصل الموثّقة؟» (سؤال المستخدم):**
>   التشخيص (باكتيست بأسباب الرفض لكل رمز): 3 حواجز هندسية غير فيصلية تخنقها —
>   سقف 97% (ELAB -98.8% يُرفض 70 مرة!) · نافذة سرعة 20ج (AUUD 71) · سيولة 200K
>   (EHGO/FRSX). **فتحُها: الإشارات 1→7 لكن النجاح انهار لـ17% (1✅/5🛑)** — مناطق
>   إدارة فيصل اليدوية + تشويه التقسيمات التاريخي (VMAR $515). **القرار: تبقى
>   97/20ج/200K بقفل C3.** البوت = نسخة آلية أكثر انتقائية؛ سجله الحي 80% هو الحكم.
> - **221 اختبار · pyflakes نظيف · صفر تغيير على منطق الفرز الإنتاجي (لا LOGIC_VERSION).**

> 🎯 **تنفيذ `OPUS_EXECUTION_PLAN.md` (أوبيس 2026-07-03) — مكتمل:**
> - ✅ **القسم 2+3+4:** توثيق مصحّح + أقفال قرارات محسومة (MIN_TARGET_GAP/RSI27/
>   MACD12-26-9/EMA30-50/ثبات3-8/منع35%) + توثيق الاستبعاد.
> - ✅ **تنظيفات D1/D2/D3/D6:** حذف `ENTRY_ABOVE_PIVOT_PCT` الميت · تعليق `sweep` ·
>   تصحيح تعليق تنبيه الارتداد · توثيق «شمعة الصعود=دعم».
> - ✅ **إضافات عرض D8/D9/D10 (طبقة عرض فقط، لا تمسّ الفرز، لا LOGIC_VERSION):**
>   D8 تحذير «الرجل المشنوق» (`_hanging_man` → warnings) · D9 قسم «مراقبة التقسيم
>   العكسي» (`build_split_watch_section` + `_split_row`، يفعّل قاعدة ÷2 النائمة في
>   `run_daily_watchlist`) · D10 «تدوير الفلوت %» بالكرت واليومي (`rotation_pct`).
> - 📋 **القرارات الجذرية D4/D5/D7/D11: أُبقيت كما هي** (قرار المستخدم + التدقيق:
>   لا خطأ منهجي — ثبات 3-8، سلّم المقاومات، h4_confirm نائم، منع ملاحقة 35%).
> - **201 اختبار · pyflakes نظيف.** لا رفع LOGIC_VERSION (لا تغيير في منطق الفرز).

> 📲 **تنبيه تلقرام لمراجِع Cline (2026-07-01، طلب المستخدم):** بعد كل تدقيق أسبوعي،
> خطوة `cline_notify.py` بالـworkflow (قبل الـPR · `if: always()`) ترسل «الملخّص
> التنفيذي» للتلغرام (نفس أسرار البوت · بلا تكلفة Cline). طبقة عرض/إشعار فقط —
> لا تمسّ الجذور، ولا `LOGIC_VERSION`. أول تشغيل ناجح مؤكّد end-to-end: 2026-06-30.

---
## 🔑 نص التشغيل (انسخه في المحادثة الجديدة)
```
أنا نفس المستخدم — مشروع «بوت أسهم الارتكاز» (منهجية فيصل @kisar_).
اقرأ بالترتيب: HANDOFF.md ثم CLAUDE.md ثم git log --oneline -40 ثم
Super_stock.py كامل ثم FAISAL_METHODOLOGY_NOTES.md. بعدها لخّص لي الحالة
الحالية بإيجاز عشان أتأكد إنك فهمت، ثم نكمّل. لا تعيد عمل تم، ولا تعدّل
جذور البوت بدون سؤالي، والتزم بقرارات فيصل المحسومة في الملفات.
```
---

## ما هو المشروع
بوت تيليجرام يفرز **أسهم الارتكاز** (Nasdaq) حسب منهجية فيصل: سهم انفجر
≥100% ثم انهار ≥50%، فلوت صغير + شورت منخفض + تقسيم عكسي غالبًا، الآن يتجمّع
عند قاع متشبّع بفجوات فوقه = مرشّح انفجار. يشتغل على GitHub Actions (Yahoo
محجوب محليًا)، يرسل تلقرام.

## النظام الحالي (مكتمل ويعمل)
1. **متابعة يومية ~10:23ص السعودية** (cron `23 7 * * 2-5`) + **تجديد أسبوعي الجمعة بعد الإغلاق**
   (cron `7 22 * * 5`، `RENEW_ON_CLOSE=1`) → قائمة **A/B**. ⏰ الدقائق غير المستديرة مقصودة
   (زحمة كرون GitHub بالدقيقة 0 كانت تؤخّر 2.2-3.6 ساعة/يوم — سجلّ 12 يومًا).
2. **14 بوابة** (M1–M14). الأساسية الصارمة (هوية الارتكاز) = **M1-M5** + أرضية RSI.
   - A = صفر نواقص تأكيد · B = ≤3 نواقص · أكثر = رفض.
3. **قائمة ثابتة دائمة**: تُضاف لها الجديد، **لا تُحذف إلا بستوب/هدف** (لا ترفرف).
   التجديد الكامل = **الجمعة 22:00 UTC بعد إغلاق السوق** (`should_renew(wl, force,
   renew_signal)` بإشارة `RENEW_ON_CLOSE` — شمعة أسبوعية مكتملة، طلب المستخدم
   2026-07-09). `run_daily_watchlist` = فرز+دمج (متابعة).
4. **قائمة مراقبة الارتداد** (مستقلة، `wl["pullback"]`): ارتكاز ارتفع فوق دخوله →
   متابعة يومية + تنبيه لحظي عند نزوله للدعم. مرتّبة بالجودة، حد 15.
   - `pullback_live.py` + workflow `pullback_monitor.yml` كل 30د بالسوق.
5. **ترقية B→A** تلقائية + مؤشر صحة بيانات + retry تحميل (لا اختفاء عشوائي).

## قرارات فيصل المحسومة (لا تغيّرها بدون سؤال — موثّقة بالصور)
- الوقف **~7% تحت الدعم**: `STOP_BELOW_LOW_PCT=(5,7)` + `USE_ATR_STOP=False`.
  ⚠️ ATR للوقف كان خطأي (يعمّقه لـ18%) — أُلغي. فيصل 5-7% (ENPH/VMAR).
- MACD **12/26/9** · EMA **30/50** · Fibonacci أهداف (`USE_FIB_TARGETS=True`).
- RSI: قاع ≤27 مثالي (`RSI_OS_HARD=32` أرضية، 27-32 نقص) · الآن ≤40
  (`RSI_MAX_NOW=40`، 40-50 نقص، `RSI_NOW_HARD=50` رفض فوق 50).
- الدخول **دفعات** (فيصل @kisar_): `ENTRY_TRANCHES=3` عند الدعم وصعوداً بخطوة
  `ENTRY_STEP_PCT=3`% (مثال فيصل 1.70/1.75/1.80 وقف 1.50). الوقف 7% تحت الدعم.
- العائد/المخاطرة من **متوسط الدفعات** (entry_ref=mean، فيصل يمتّع) — موحّد
  في الكرت والتقرير اليومي (لا تناقض بين 🛡️ والنواقص).
- فلوت <50م (`FLOAT_GATE_MAX`) · شورت **≤40ألف** (`SHORT_GATE_MAX`؛ SPPL 35) · ثبات 3-8.
- **فحص أخبار آلي** `scan_news_risk`: طرح/تخفيف/شطب من عناوين ياهو → تحذير بالبطاقة.
- **منظومة 4س** `four_hour_levels` (طبقة مساندة، `fetch_4h` بـprepost): دعوم/أهداف
  من رؤوس/ذيول الشموع الحمرا + «مقاومة↗دعم» + ذيل المسح → سطر «🕓 4س».
- **A→B عند التخفيف**: `news_risk`+كسر الدعم/قاع أدنى → B (في `check_promotions`)،
  يرجع A عند الاستقرار فوق الدعم.
- **حُرّاس القرارات (Invariants) في `test_bot.py` قسم 9**: يقفلون الوقف 5-7%/
  لا ATR · الدفعات 3×3% · الشورت 40ألف/الفلوت 50م · عتبات RSI · أرضيات الهوية ·
  الضمان الذهبي + الأهداف + RR على 60 سهم صناعي · أن الأسبوعي لا يغيّر t1/RR ·
  أن غياب 4س لا يكسر · أن الفحص اليدوي = الأساسي. **أي كسر صامت = فشل اختبار.** (167/167)
- `SCORE_MIN=45` · `WATCH_MAX_FAILS=3` · `MIN_PRICE=1.5` · `NEAR_PCT=50` · `READY_PCT=75`.
- تحذير الصين/هونغ كونغ (`HIGH_RISK_COUNTRIES`) — تحذير فقط (تظل تظهر).

## شكل الرسالة (مصطلحات المستخدم)
- **البطاقة `build_message`**: مختصرة ومرتّبة (معتمد 2026-06-23) — كل معلومة مهمّة
  بسطرها، والصغيرة (فلوت/شورت/قطاع/دولة) بسطر واحد. التفاصيل في «تحديث تنسيق البطاقة» أسفل.
- رأس: جاهزية `X/100` (🟢🟡🔴) + `💪 القوة العامة` بشريط بصري. القطاع/الدولة بالعربي.
- **التقرير اليومي `build_daily_message`**: طُبّق عليه نفس الأسلوب المختصر (2026-06-23):
  سطر دخول+وقف بنسبته · أهداف الثلاثة بنسب بسطر · نواقص B مرقّمة «(n/14)».
  **«⚡ دخول المضارب» (Williams %R) سطرٌ ثابت في البطاقة واليومي** (طلب المستخدم).
  قسم الارتداد مختصر أصلًا (بلا تغيير).

## الملفات
- `Super_stock.py` (~4670س) — البوت. دوال مفتاحية: `analyze_ticker(sym,df,pullback=)`،
  `scan_market`، `scan_pullback`، `run_daily_watchlist`، `run_weekly_renewal`،
  `build_message`، `build_daily_message`، `build_pullback_section`، `monitor_pullback`،
  `make_watch_entry`، `make_pullback_entry`، `readiness_ratio`، `rank_key`.
- `analyze_one.py` — تحليل يدوي (يفوّض لـ`analyze_ticker`؛ يعرض A/B/مراقبة ارتداد).
- `pullback_live.py` — مراقبة لحظية.
- `technical_report.py` — **أداة مستقلة** (تستورد دوال البوت فقط): `TICKER=رمز` → تقرير
  فني كلاسيكي · `SCAN_EARNINGS=1` → أداة الأرباح. مصادر تقويم الأرباح بالأولوية:
  AlphaVantage(`ALPHAVANTAGE_KEY`)→FMP→Finnhub→yfinance. ثابت `SCAN_MIN_SCORE=60`.
- `test_bot.py` — **167 اختبار**. شغّله قبل أي دفع: `python3 test_bot.py`.
- `analyze_one.py` — الفحص اليدوي، **مطابق تمامًا لـ`analyze_ticker`** (مقفول باختبار).
- `.claude/hooks/session-start.sh` + `.claude/settings.json` — SessionStart hook
  لجلسات Claude Code ويب (يثبّت requirements + ruff/pyflakes تلقائيًا).
- `CLAUDE.md` — دليل سريع (يُقرأ آليًا). `FAISAL_METHODOLOGY_NOTES.md`/`FAISAL_IMAGES_CATALOG.md` — منهجية فيصل.
- `.github/workflows/`: daily_screener · pullback_monitor · scan_earnings (أداة الأرباح،
  يومي 06:00 UTC) · backtest · analyze/technical (يدوي).

## ضوابط العمل
- الفروع: طوّر على `claude/faisal-images-methodology-r6gfhq` ثم **ادمج في main** (الأكشن على main).
  ⚠️ git push لـ`main` متعطّل (503) → الدمج عبر GitHub API (PR مؤقت + دمج فوري).
- بعد كل تعديل: `python3 test_bot.py` → commit → push للفرعين.
- **حدّث HANDOFF.md + CLAUDE.md مع كل قرار/تعديل مهم** (عشان الذاكرة تبقى حيّة).
- رسائل commit تنتهي بـ Co-Authored-By + Claude-Session (انظر CLAUDE.md). لا PR إلا بطلب.
- لا تضع معرّف النموذج في أي ملف مدفوع.

## آخر حالة (2026-06-22) — مطابقة الصور 100% + فحص شامل أقصى + تصليبات
**مطابقة توثيق صور فيصل مع الكود**: تغطية **100%** — كل عنصر موثّق مُستوعَب.
آخر ثغرة (`williams_r` معرّفة وغير مربوطة) **رُبطت** بالتقييم (`SCORE_WILLIAMS`،
انعطاف %R من التشبع، تغريدة 7377) بإذن المستخدم، **وتُعرض في البطاقة**
كسطر «دخول المضارب ✓» (البطاقة: 🕯️ + %R بسطر 📐 · التقرير اليومي: سطر ⚡). التفاصيل في
`FAISAL_METHODOLOGY_NOTES.md` (تقرير المطابقة النهائي). خارج النطاق عمدًا:
توقيت الجلسات اللحظي فقط.

فحص كامل للبوت (اختبارات + compile + تحليل ساكن ruff/pyflakes + 3 وكلاء فحص
عميق). **المنهجية والمسارات الأساسية سليمة** (حُرّاس القرارات خضراء). طُبّقت 6
تصليبات لا تمسّ معادلات الدخول/الوقف/الأهداف ولا تحتاج force_renew:
- **A** `analyze_one.py`: إصلاح خطأ كتابة كان يرجّع عنصرين بدل 3 (`, None` خارج
  قوس النص) → التحليل اليدوي ما عاد ينهار على رمز ببيانات ناقصة.
- **B** `migrate_watchlist`: يحدّث الآن `rr/drop_pct/best_spike` مع الوقف/الأهداف
  (كانت تبقى قديمة). score/flags/h4/float/short تبقى (مشتقّة من الإثراء، تُبنى جمعةً).
- **C** كتابة JSON ذرّية (`_atomic_write_json`: temp ثم os.replace) لـwatchlist/
  alerts/companies → لا تلف صامت ولا فقدان للقائمة.
- **D** `git_save`: fetch+rebase قبل الدفع + 4 محاولات + كشف الفشل (لا دفع
  قسري) → لا تضيع بيانات تشغيل عند التزامن/الرفض.
- **E** حدود نمو في `save_watchlist`: notes≤250 · removed≤120 · replacements_log≤120.
- **F** `zip(strict=False)` · `technical.yml` صار يشغّل `technical_report.py`
  (كان مكرّرًا لـanalyze) · `daily_screener.yml` صار له `concurrency` group.
**138/138 اختبار** (أُضيف اختبار rr-في-الترحيل + اختبارا الكتابة الذرّية/الحدود).
لا مهام معلّقة معروفة.

## تحديث (2026-06-23) — تنظيف تقرير الفرص الفائتة (أداة التطوير فقط)
حلّلنا ملف `missed_*.csv` (155 فرصة فائتة) بأداة التطوير المدمجة. النتيجة:
**~74% من «الفائتة» ليست أسهم ارتكاز أصلًا** (رُفضت بهوية M1/M2/M3: سعر/هبوط/
انفجار خارج النطاق) → رفضها **صحيح**، لا تُرخِّ البوابات. الباقي M4 (قاعدة واسعة)
= أسهم ارتكاز فعلية تحرّكت → مكانها قائمة مراقبة الارتداد.
- **التعديل (آمن، أداة تطوير فقط — لا يمسّ الجذور ولا يحتاج force_renew):**
  `_missed_block` في `build_dev_assistant_report` صار يفصل «📌 ارتكاز تحرّك
  (راجع الارتداد)» عن «🗑️ ليس ارتكازًا (تجاهل صحيح)» — يزيل ضجيج 116 رفضًا
  صحيحًا ويركّز على ما يستحق المتابعة فعلًا. أُضيف اختبار تصنيف الفائتة.
- **مراقبة الارتداد**: فُحصت — `scan_pullback` يلتقط M4 ويرتّبها بالجودة، الحد 15
  متعمّد (تركيز على الأقوى). رفع الحد قرار للمستخدم — لم يُغيّر (التزامًا بـ«لا تمسّ
  ما لست متأكدًا منه»).
**147/167 اختبار.**

## تحديث (2026-06-23) — تطوير أداة التحليل الفني الكلاسيكي (`technical_report.py`)
فحص شامل للأداة الثالثة (قراءة كاملة + تشغيل فعلي على بيانات تركيبية + اختبار
الأنماط): **سليمة بلا أخطاء، مطابقة للمدرسة الكلاسيكية**. أُضيفت ٤ مؤشرات
كلاسيكية **كأسطر معلومات فقط** (لا تمسّ الدرجة/الحكم — مُثبت بأن الـsubscores
لم تتغير) تعيد استخدام دوال البوت المختبَرة:
- **ADX/DMI** (`dmi_adx`): قوة الاتجاه (قوي/ضعيف/عرضي) لا اتجاهه فقط.
- **بولنجر باند** (`bollinger`): %B + كشف الانضغاط (تجميع).
- **ستوكاستيك RSI** (`stoch_rsi`): زخم كلاسيكي مكمّل.
- **فيبوناتشي التصحيحي** (`fibonacci_levels`): مناطق 38.2/50/61.8.
كلها فاشلة-آمنة (try/except + حماية NaN للفريمات القصيرة). الدرجة/الحكم/الـ147
اختبار بلا تغيير.

## تحديث (2026-06-23 مساءً) — أداة الأرباح + قرارات نقاش (مهم للمحادثة الجديدة)
### مدموج على main:
1. **أداة الأرباح** (`technical_report.py`، ماسح `SCAN_EARNINGS=1`):
   - `SCAN_MAX_PRICE=30` (استبعاد أي سهم فوق $30) · `SCAN_EARN_WINDOW` 14→**3**
     (التنبيه يصل قبل الإعلان بـ3 أيام فقط) · نص الشروط يعرض النطاق $2–$30.
   - يعرض لكل مرشّح **أقرب دعم/مقاومة** (من الفريم اليومي) للتخطيط بعد الإعلان.
2. **«إعداد فني ضعيف»** بدل «بعيد عن الدخول» في `readiness_badge` — إزالة لبس مع
   «في منطقة الدفعات الآن» (النسبة تقيس اكتمال الإعداد الفني، مب مسافة السعر).
3. **تقرير الفرص الفائتة**: فصل «ارتكاز تحرّك (راجع الارتداد)» عن «ليس ارتكازًا».

### قرارات نقاش محسومة (توثيق فهم — لا كود):
- **بوابة الفلوت تبقى مرنة (نقص) مب صارمة (رفض)** — قرار المستخدم صريح. فلوت
  ≥50م = نقص «فلوت كبير» + ينزل B/مراقبة، **لا يُرفض** (`Super_stock.py` ~3050
  «لا يُحذف — يُسجّل نقصًا»). الرفض = هوية الارتكاز فقط (M1-M5). الفلوت الصغير
  أقوى، لكن الكبير لا ينفي كونه ارتكازًا. (لو طلب المستخدم لاحقًا رفضًا صارمًا
  >50م = سطر واحد + `force_renew`.)
- **RSI رقمان منفصلان**: **القاع** المثالي ≤27 (≤32 أرضية) = عمق التشبّع السابق ·
  **الآن** ≤40 مثالي (40-50 نقص · >50 رفض). السيناريو المثالي: السعر/RSI ينزل
  للتشبّع ~27 عند الدعم ثم **ينعطف صاعد** (RSI لسّا ≤40) → الدخول عند الدفعات
  (الشراء عند الدعم؛ RSI مرشّح توقيت لا زر دخول). RSI يتحرّك مع السعر.
- **VSTM** (مثال مرجعي): ارتكاز **حقيقي** (RSI قاع 25 · هبوط 62% · انفجار 92% ·
  قاعدة ضيقة) لكنه **ارتفع** (RSI 50، السعر $4.26 فوق منطقة الدفعات $3.55–3.76)
  → **مراقبة ارتداد**: ننتظر رجوعه للدعم ثم الدخول. نقاطه: انفجار 92% (حدّي، مب
  ≥100%) · لا فجوة-هدف فوق · فلوت 70.4م (كبير، نقص لا رفض).

### ملاحظات تشغيلية حرجة (اقرأها قبل أي دفع):
- ⚠️ **git push لـ`main` متعطّل (503 متكرر) في هذه البيئة** → الدمج في main يتم عبر
  **GitHub API (PR مؤقت + دمج فوري)** عبر أدوات mcp__github. الفرع
  `claude/faisal-images-methodology-r6gfhq` يُدفع بـgit عادي بلا مشاكل.
- ملف **القاعدة المعرفية للتداول** (48 صورة) استُلم: ~85% مطبّق أصلًا (تأكيد أن
  البوت على المنهج الكلاسيكي). **تجنّب التشعب السلبي**: إليوت/ICT الكامل/توقيت
  الجلسات اللحظي/كشف النماذج المعقّد (مثلثات/أعلام) — لا تناسب نيتش الارتكاز.
- **147/167 اختبار**. آخر إصدارات مدموجة: `be2186c` ثم تعديلات لاحقة.

## تحديث (2026-06-23) — تنسيق البطاقة المختصر (`build_message`، عرض فقط)
بطلب المستخدم: بطاقة الفرز (الجمعة) كانت مزدحمة (~18 سطرًا) → أُعيد تنسيقها مختصرة
ومرتّبة على نمط الرسائل النظيفة. **عرض فقط — لم يُمسّ أي حساب (دخول/وقف/أهداف/
مستويات)، فلا `LOGIC_VERSION` ولا `force_renew`.** الشكل المعتمد:
- رأس: `🅰️/🅱️ $الرمز · 🟢🟡🔴 الحالة X/100` + `💪 القوة العامة NN/100 ████ وصف`
  (شريط 10 خانات + 🔥قوي جدًا/💪قوي/⚖️متوسط/🔻ضعيف).
- `💰 سعر · فلوت · شورت · قطاع · دولة` (المعلومات الصغيرة بسطر واحد، بالعربي).
- مجموعة دخول: `📥 دفعات` · `🟢 الدعم الأساسي` · `🟢 الدعم الفرعي` · `⛔ الوقف (−%)`.
- مجموعة أهداف: `🎯 الهدف 1/2/3 (+%)` كلٌّ بسطره · `🔴 المقاومة الفرعية/الأساسية` ·
  `🚀 تحرر`. (الهدف لو = المقاومة الفرعية يُوسَم بها بلا تكرار · المقاومة الأساسية
  تُخفى لو ساوت التحرر.)
- مجموعة ختام: `⚖️ ربح/مخاطرة` · البوابات (A: «✅ اجتاز 14/14» · B: «🅱️ البوابات
  الناقصة (n من 14)» مرقّمة 1-/2-) · تحذيرات (تقسيم/SEC أحمر/تخفيف) · `🔗 روابط مختصرة`.
- مجموعات مفصولة بأسطر فاضية. دوال جديدة: `strength_bar` · `readiness_tag` ·
  `news_links_compact` (تعيد استخدام `readiness_badge`/مصادر `news_links`).
- **حُذف من البطاقة** (تبقى بالحساب/الداتا): سطر الهوية 📊 هبوط/انفجار/RSI · سطر
  التأكيد 🕯️ (الأنماط/الفريمات/«دخول المضارب») · سطر المؤشرات 📐 (MFI/ADX/%R) ·
  سطر حجم المركز · 🧱 (دُمج كأسطر دعم/مقاومة) · 🕓 4س · 🟡 قاب.
- **التقرير اليومي `build_daily_message`** صار بنفس الأسلوب المختصر (دخول+وقف% ·
  أهداف بنسب بسطر · نواقص مرقّمة «(n/14)»). حُذف منه ما حُذف من البطاقة، عدا:
  **«⚡ دخول المضارب ✓» (Williams %R) أُبقي في البطاقة واليومي بطلب المستخدم.**
- **RSI**: نص النواقص صار «RSI الآن X أعلى من 40» بدل «X>40» (إزالة علامة `>` بطلب
  المستخدم؛ وُحّد أيضًا في تقرير التطوير). تحديث ذاتي على التشغيل القادم (`check_promotions`).
- قسم الارتداد (`build_pullback_section`): مختصر أصلًا (سطر/سطرين لكل سهم) — بلا تغيير.
**152/152 اختبار** (فحوص البطاقة واليومي محدّثة + «دخول المضارب» مُعاد للاثنين).

## تحديث (2026-06-23) — تبسيط النصوص لمبتدئ + منع علامات المقارنة (عرض فقط)
> 🔴 **قاعدة لغة الرسائل (طلب المستخدم):** **ممنوع** علامات `≥ ≤ > <` في أي نص معروض —
> تُكتب كلماتٍ: «أعلى من / أقل من / فأكثر / أو أقل / على الأقل». والأرقام صريحة (كم).
> (العلامات مسموحة فقط في **تعليقات الكود** غير المعروضة.)

عناوين البوابات الناقصة (soft_fails) صارت مفهومة لمبتدئ — بلا «حدّي» ولا علامات. تظهر
في **البطاقة + اليومي + التحليل اليدوي** (مصدرها `analyze_ticker`):
- «المتوسط الأسي» → «السعر أقل من متوسطه المتحرك (30) بـX%» (يعرض **كم** الفرق، باتجاهه).
- «انفجار حدّي X%» → «الانفجار السابق X% (المثالي 100% فأكثر)» · «هبوط حدّي X%» →
  «الهبوط X% (المثالي 50% فأكثر)».
- «فجوة-هدف فوق» → «ما فيه فجوة سعرية فوقه (هدف)».
- «RSI تشبع حدّي (X)» → «قاع RSI X (المثالي 27 أو أقل)» · «MACD» → «MACD لسّا ما أعطى
  إشارة إيجابية» · «عائد/مخاطرة منخفض» → «العائد مقابل المخاطرة منخفض» · «توافق فريمات
  X/3» → «توافق الفريمات X من 3 (نحتاج 2 على الأقل)».
- **التحليل اليدوي** (`analyze_one.py`): بوابات M3/M4/M5/M6/M10/M12 أُزيلت منها `≥/≤`
  (سيولة … أو أكثر · قاعدة … أو أقل · RSI قاع … أو أقل …)، وسطر المتوسط: «السعر أقل من
  متوسطه المتحرك (30 يوم) بـX%».
- **تقرير التطوير**: شرائح RSI/الشورت/RR وعناوينها بلا علامات (… أو أقل / أعلى من … / … أو أكثر).
- **أُبقيت كما هي**: «شورت عالٍ»/«فلوت كبير» (واضحة + اختبارات صارمة) · «دخول المضارب ✓».
- **سطر الفريمات** (`timeframes_info`, طلب المستخدم): لمّا تُجتاز البوابة (2 فأكثر) يظهر
  «🕯️ الفريمات (2/3 · ⏳ = الباقي): شهري ✅ · أسبوعي ✅ · يومي ⏳» — **يسمّي كل فريم ويبيّن
  الناقص (⏳)** (مو نقص؛ المطلوب 2 من 3). `tf_count`+`tf_display` يُخزَّنان (`make_watch_entry`)
  ويُحدَّثان يوميًا (`check_promotions`).
- **تنظيف RTL شامل** (طلب المستخدم 2026-06-23 — البطاقة/اليومي/الارتداد):
  - **٪ أُزيلت من الأهداف والوقف** (كانت تتشوّش/تنعكس بالعربي): الأهداف أسعار فقط
    «🎯 أهداف: $X · $X · $X» · الوقف «⛔ وقف خسارة $X». (النسبة الوحيدة الباقية = فارق
    المتوسط، صيغتها الرقم والنسبة أولًا «أقل بـX% من متوسطه (30)» فلا تطفو ٪.)
  - **إجبار RTL لكل سطر** (`_rtl_join`: علامة RLM `‏` أول كل سطر) → كل سطر يبدأ من
    **اليمين** ويتّسق (لا عشوائية بين أسطر تبدأ برقم/$ وأخرى بعربي).
  - **علم الدولة** (`country_label`/`COUNTRY_FLAG`): «🇺🇸 أمريكا» في الكرت، **وأُضيفت
    الدولة للتقرير اليومي** (كانت ناقصة).
- **شرح «متى تُفتح» بوابة المتوسط** (طلب المستخدم 2026-06-23 — البطاقة + الفحص اليدوي):
  نقص المتوسط صار يوضّح **كيف يُفتح** بين قوسين بلغة مبتدئ: «السعر أقل بـX% من متوسطه
  المتحرك **(يفتح بصعود ~Y% أو بثبات أسابيع)**» — حيث Y = الصعود اللازم للوصول
  لـ`ema30×0.98` = `(ema30*0.98/price − 1)*100`. (للحالة فوق المتوسط: «يفتح برجوعه قرب
  متوسطه».) موحّد في `Super_stock.py` (soft_fail M12) و`analyze_one.py` (تفصيل البوابة).
- **إضافة دول لخريطة العلم/الترجمة** (2026-06-24): VFS كان «Vietnam» إنجليزي بلا علم
  (ناقصة من `COUNTRY_AR`/`COUNTRY_FLAG`). أُضيفت فيتنام + دول ناسداك الأجنبية الشائعة
  (شحن/ADR: اليونان/جزر مارشال/قبرص/موناكو/المكسيك/إندونيسيا…) — عرض فقط.
- **ثبات الشورت/بيانات الشركة** (2026-06-24 — إثراء/تخزين، لا يمسّ الفرز · موافقة المستخدم):
  السبب: المخزّن `short` كان **FINRA-فقط** فيختفي لو غاب الرمز عن تيب اليوم (شورت غايب
  عن 5 من 6)، وبوابة M13 تجلب الشورت ثم **ترميه**، و`COMPANY_CACHE` لا يحفظه، و`_fetch_info`
  يرمي الرد الجزئي (INSM/NGEN بلا بيانات إطلاقًا). الإصلاح: (1) حجم الشورت اليومي
  سلسلة متّسقة **Fintel→FINRA→الذاكرة** في `enrich`. (2) `finra_short` يُخزَّن في
  `COMPANY_CACHE` ويُسترجع في `check_promotions` (مع `float`/`short_pct`). (3) `apply_short_gate`
  يخزّن `srt` المجلوب بدل رميه (القرار لم يتغيّر). (4) `_fetch_info` يحتفظ بالرد الجزئي
  بدل `{}`. **فلوت NBP 64.5M يبقى B بالتصميم (v2.7) — قرار المستخدم: لا تغيير.**

- **فحص شامل 7 وكلاء + تصليبات (2026-06-24)** — لا يمسّ منطق الفرز ولا الثوابت:
  - 🔴 **علامات `<`/`>` في أكواد الرفض كانت تكسر رسالة تيليجرام (HTML 400)** على
    مسارات صفر-نتائج/تقرير الجمعة/تحليل سهم مرفوض → سُمّيت بالكلمات (`M3_انفجار_تحت_60`…)
    + `esc()` للعرض + شريحة الفلوت «أقل من 10م»/«أكثر من 30م».
  - 🔴 **حارس التجديد الأسبوعي**: لو خُنق فحص الجمعة (تغطية < `DATA_HEALTH_MIN_PCT`)
    لا تُمسح القائمة النشطة — يُؤجَّل التجديد + إشعار (كان يستبدلها بقائمة شبه فارغة).
  - **حفظ قبل الإرسال** في `run_daily_watchlist` (لا تضيع حالة اليوم لو فشل الإرسال).
  - **فصل مقياس الشورت**: `sharesShort` (Yahoo، ملايين) لا يدخل `finra_short` (حجم
    يومي) — كان يلوّث تقرير التطوير ويُوسَم «(FINRA)» خطأً. بديل Yahoo = `short_pct`.
  - **شرطة «—»** للفلوت/الشورت عند تعذّر الجلب (طلب المستخدم — تعذّر ≠ صفر).
  - تنقيح t2/t3 بالـ4س في الفحص اليدوي (= الكرت) · حد قائمة الارتداد على الإجمالي ·
    إزالة فرع وقف ATR الميت (الوقف 5-7% فقط) · توحيد `WATCH_MAX_FAILS=3` · إزالة `≥`/`≤`
    من `technical_report`/`gates_status` · مجموعة تزامن مشتركة للوركفلوز · `SCAN_EARNINGS`
    غير حسّاس لحالة الأحرف · إعادة حساب `rr2` بعد التنقيح.
  - **مقصود (لا تغيير):** الكرت المختصر بلا أسطر 📐/🧱/🕓 (الدوال محفوظة) — حُدّث التوثيق.
  ⚠️ **لا LOGIC_VERSION/force_renew** (طبقة فرز سليمة لم تُمَس · إثراء/عرض/تخزين فقط).

- **فحص عميق Workflow (273 وكيلًا · كشف→تحقّق خصومي 3 مشكّكين→تجميع · 2026-06-24)**
  أكّد 23 ملاحظة (دُمجت لـ16). أُصلحت 14 منها (لا تمسّ منطق الفرز/الثوابت):
  - 🔴 **كراش `apply_short_gate`**: `fintel_short` يرجّع dict {short_volume,…} لا int،
    فـ`srt >= limit`/`int(srt)`/write-back تنكسر متى نجح Fintel → نستخرج `short_volume`.
  - 🔴 **`migrate_watchlist`** كان يختم `logic_version` حتى لو تُخطّي سهم لنقص بيانات
    (تشغيل مخنوق) → الآن لا يُختم إلا لو رُحّل **كل** سهم نشط (وإلا يُعاد لاحقًا).
  - 🔴 **حارس التجديد** كان يُخدَع بفشل جلب الكون (تحوّل لعيّنة اختبار تغطيتها ~100%)
    → `scan_market` يضبط `universe_fallback`، والحارس يحفظ الستوبات ويؤجّل التجديد.
  - `run_weekly_renewal`: الأرشفة بعد الحارس + حفظ على مسار التأجيل (لا تضيع ستوبات).
  - **كاشف الانفجارات** (`scan_explosions`): dedup على **تاريخ الانفجار** لا تاريخ المسح
    (كان يُحسب ×5 عبر النافذة) · موقع اليوم بـ k لا `index` على قائمة مُرشَّحة ·
    استعادة `_REJECT_REASONS/_STATS` بعد تحليل الشرائح (لا تلوّث الإحصاء الحي).
  - `analyze_one`: شيك M2/M3 يستعمل **الأرضية** (40/60، نقص لا رفض) فلا يناقض حكم B ·
    `key_levels` بلا 4س (مطابق للبوت).
  - `enrich`: يحتفظ بقيمة بوابة الفلوت/الشورت كاحتياط قبل التصفير (لا «—» زائفة) ·
    `esc()` للقطاع في تقرير التطوير · `update_watchlist_status` ملفوف بـtry · تعليقات الوقف.
  - **`entry_ref`** المخزَّن = سعر الترشيح لا متوسط الدفعات (يؤثّر على تتبّع الأداء) —
    **قرار المستخدم: إبقاؤه كما هو، مُوثّق** (الأثر صغير · RR المعروض من المتوسط).
  - **توثيق:** ترتيب `h4_confirm` غير فعّال (يُحسب بعد الفرز فقيمته 0 وقت الترتيب) — مُوثّق.

- **الموجة الثانية للفحص (اكتمل التجميع · 39 مؤكَّدة · الحكم: «البوت سليم، الإصلاحات
  صحيحة وبلا انحدار»)** — العيوب المتبقّية كانت في الأدوات المستقلة/الاختبارات (خلايا
  ضربها الحد سابقًا). أُصلحت:
  - `technical_report.py`: `esc()` للرمز وللخطأ في مسار الفشل/التقرير (كان رمز/استثناء
    خام في HTML → 400 يُسقط الإشعار).
  - `pullback_live.py`: حفظ + git_save **قبل** الإرسال (مثل اليومي — لا تتكرّر التنبيهات).
  - اختبارات: شيك «t1/RR مقفولان» كان ميتًا (`abs(t1-t1)>0`) → حارس حقيقي · شيك MACD
    تكراري → أُضيف اختبار `S.macd` فعلي · اختبار «التجديد لا يمسح القائمة عند فحص فارغ».
  - **تنبيه الارتداد** (قرار المستخدم): التفعيل يبقى عند `entry[1]` (أعلى منطقة الدخول =
    بداية التعبئة، أسلوب فيصل)، لكن **صُحِّح الوصف**: «منطقة الدخول» بدل «الدعم» (عرض فقط).
عرض/إثراء/تخزين/أدوات فقط — لا يمسّ الفرز · لا LOGIC_VERSION/force_renew. **178/178.**

> 🏢 **ردم الفلوت من المصدر المُثبَت (2026-07-28، `plans/003`):** `ce_float_info` ميتة منذ
> 07-24 (قِشرة JS) وكانت **المصدر الوحيد** للردم اليومي في `run_daily_watchlist` وآخرَ ملاذ في
> `enrich` ⇒ الردم لم يملأ شيئًا أبدًا، وأسهمٌ خُنق ياهو عنها تبقى «فلوت غير متاح» فلا يعمل
> عليها `refloat_gate_recheck` (إصلاح PONY). صار المصدر `_yahoo_float(strict=True)`.
> ⚠️ **`strict` ليس تجميلًا:** الوضع الافتراضي يسقط لـ`sharesOutstanding` وتلك القيمة تصل
> بوّابة M14 وتُوسَم «فلوت كبير» (مُستنسَخ) = تشديد صامت. **كل نقطة يصل مخرجُها للاختيار
> تستعمل `strict=True`؛ أدوات العرض تبقى متساهلة عمدًا.** 10 اختبارات · طفرتان · 1341.

> 🛡️ **حارس النوع على بوّابتَي M13/M14 (2026-07-28، `plans/004`):** الطبقة التالية
> (`refloat_gate_recheck`) كانت محروسة والبوّابتان لا — وهما على مسار
> `scan_market → main → exit(1)`. مُستنسَخ: نصّ⇒`TypeError` · `NaN`⇒`ValueError` · dict⇒
> `TypeError` ⇒ **سقوط التشغيلة قبل `git_save`** (ضياع ستوبات اليوم وأختام الدِدوب بلا
> تقرير). الآن غير الرقمي = مجهول يمرّ بفائدة الشك. **القرار لكل مدخل رقمي byte-identical
> ببصمة قبل/بعد على 19 حالة** · المخزَّن للعرض لا يُمَسّ · الالتقاط ضيّق بقفل · 3 طفرات ·
> لا `LOGIC_VERSION` · 1345 اختبارًا.

> 🛡️ **حارس لكل رمز في كتلة إثراء `scan_market` (2026-07-28، `plans/005`):** ثمانية حقول
> عرض/تفسير بلا حارس ⇒ استثناء على رمز واحد يُسقط التشغيلة كلّها قبل `git_save` (مُستنسَخ).
> التطبيق كان غير متّسق: `update_watchlist_status` محروس والفرز لا. 🔒 `results.append(r)`
> **خارج** الحارس ⇒ العضوية والترتيب byte-identical (مقفول) · حارس مستقلّ لـinterp · يسجّل
> ولا يصمت · 3 طفرات (منها نقل append داخل الـtry ⇒ أسقط قفل العضوية) · 1349 اختبارًا.

> ⚡ **`monitor_pullback` تحميل مجمَّع (2026-07-28، `plans/006`):** كان نداء
> `download_history` **لكل رمز** — وهي دالّة دفعات ثقيلة (تراجع أُسّي + sleep + تمريرة
> ثانية) ⇒ حتى ~5.5 دقائق قبل `monitor_live_events` (تنبيه كسر الوقف) داخل مهلة 15د.
> الآن نداء واحد. قرار الإطلاق والترتيب byte-identical (مقفولان) · وسيط `fetch_hist`
> محقون · انهيار التحميل ⇒ [] بلا رمي · 3 طفرات · 1352 اختبارًا.

> 🔔 **رصد سقوط كرون التجديد (2026-07-28، `plans/008` — إشعار فقط):** `should_renew`
> مشروطة بإشارة `RENEW_ON_CLOSE` وحدها، فإسقاط GitHub لتشغيلة الكرون كان يمرّ **بلا أي
> أثر** ويُضيّع أسبوعًا كاملًا من التقارير. `renewal_staleness` (نقيّة) ترصد تجاوز
> `RENEWAL_STALE_DAYS`=8 على `week_start` وتُرسل تحذيرًا يطلب `force_renew=1`.
> 🔒 **`should_renew` byte-identical** (مقفولة: لا weekday ولا تقادم) — القرار لم يُمَسّ.
> الرصد قبل المتابعة وداخل try مطلق (انهياره لا يُسقطها — مُثبَت). 3 طفرات · 1365 اختبارًا.

> 📌 **تثبيت الاعتماديات + فحص دخان حيّ (2026-07-28، `plans/007`):** `requirements.txt`
> كان `>=` وكل workflow إنتاجي يثبّت وقت التشغيل ⇒ ترقية `yfinance` قد تكسر الإنتاج بين
> تشغيلتين لنفس الكوميت، **وسويّة الاختبارات بلا إنترنت فتبقى خضراء**. صار التثبيت `==`
> (yfinance 1.5.2 · pandas 3.0.5 · numpy 2.4.6 · requests 2.34.2) + بروتوكول ترقية داخل
> الملفّ + `deps_smoke.py`/`deps_smoke.yml` يفحص العقود الحيّة (كون · شكل الشموع · `.info`
> · طزاجة). 🔒 `tests.yml` **بقيت بلا إنترنت عمدًا** (فصل فشل المنطق عن عطل الشبكة) ·
> أداة الفحص مقفولة بـAST ضد أي كتابة حالة · بلا مفتاح Polygon · 5 اختبارات · طفرتان.

> 🔐 **صلاحيات صريحة + منع حقن الـdispatch (2026-07-28، `plans/010`):** 10 workflows كانت
> بلا `permissions:` فترث افتراضي المستودع (قد يكون write على كل النطاقات) — منها اثنان
> بكرون (`split_hunter` · `scan_earnings`) فالتعرّض مستمرّ. حُدِّد لكل ملفّ بالفحص (AST على
> سكربته + بحث `git push`) أنه **قراءة فقط** ⇒ `contents: read`، مع `actions: read`
> لمنزّلَي artifacts (`acc_report`/`acc_verify`، نمط `backtest.yml`). و`acc_verify.yml`
> كان يُدرج مدخل الـdispatch **داخل نصّ صدفة** — النمط المضاد الوحيد في المستودع (البقيّة
> تمرّر عبر `env:`) ⇒ صار `YEAR` بيئةً. 🔒 صون: الستّة التي تدفع ما زالت `contents: write`
> (قفل يفحص **سطر الإعلان** لا التعليق) · بلا مسّ كرون/سرّ/مهلة · 6 اختبارات · 3 طفرات.

> 🧹 **تنظيف الدوال الميتة + مزامنة D9 (2026-07-28، `plans/011` — تنفيذ جزئي بدليل):**
> حُذفت 5 بلا مستدعٍ ولا اختبار (`fetch_4h_signal` · `short_line` · `risk_lines` ·
> `news_block` · `splits_block`). **وأُبقيت 3 بدليل**: `build_split_watch_section` لها
> 4 اختبارات **وظيفية** (ميزة نائمة لا ميتة) و`split_watch_report` منتِجها،
> و`readiness_ratio` مرجعٌ في قفل تصميم قائم. **وحذف متسلسل رُصد ولم يُنفَّذ**:
> `news_links` صارت بلا مستدعٍ ⇒ تُراجَع مستقلّةً. **و`CLAUDE.md §D9` صُحِّح**: كان يصف
> وصل `split_watch_report` بالتقرير اليومي وهو **مقطوع منذ 2026-07-09**.
> 🔒 قفل `_C11_KEEP` يُشفّر قاعدة «**غير مستعمَلة ≠ ميتة**» لكل جولة تنظيف قادمة.
