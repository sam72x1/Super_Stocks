# 🌅🗂️ `T-SHADOW` — تسجيلٌ مسبقٌ أماميّ: **القائمةُ الظلّيّة** (مراسي البريماركت ∧ (J1 ∨ فجوة ‏≥30%)) مقابل «الجاهز»

> **أمرُ المالك (2026-09-25): «OPUS_READY_LIST_SPEC.md»** بعد التحويل إلى Opus 5.5 = «نفذ» على الحزمة
> (`OPENTRY_LINK_PROMPT.md §②`). هذا العقدُ **مدفوعٌ قبل أيّ رقمٍ تُنتجه الأداة**، ولا يُمَسّ بعد أوّل
> تشغيلة. أيُّ تعديلٍ لاحق = ملحقٌ مؤرَّخ. ⚖️ **وهو ليس في الحزمة حرفيًّا**: الحزمةُ طلبت قياسًا أماميًّا
> بلا معيارِ حكم، وقاعدةُ المشروع ② («التسجيلُ المسبق يُدمج قبل أيّ رقم») أعلى منها ⇒ أُضيف (انحرافٌ `D-6`).

## ⓪ ما قِيس قبلُ — لا يُعاد

| التجربة | ما حسمته | علاقتُها بهذا العقد |
|---|---|---|
| `T-TIERLINK` (2026-09-02 · `tierlink_result.md`) | ظاهرةٌ واحدة «J1 في البريماركت» على 321 مرساة | أصلُ الرابط · شاهدُ الهُويّة **33/321 · 14** |
| `T-J1PM` (2026-09-02 · `j1pm_prereg.md`) | اختبارٌ أماميٌّ لـ`J1 ∧ pre` على **الكروت المُسلَّمة** (`tier_fwd_ledger`) · أرضيّة 60/150 · قائم | **يبقى الحاكمَ لـ`J1 ∧ pre`** · هذا العقدُ أوسعُ مجتمعًا (كلُّ المراسي) ويضيف ذراعَ الفجوة ومقارنةَ «الجاهز» |
| `T-OPLINK` (2026-09-25 · `opentry_link_result.md` · `36125202136`) | الفرعُ 2 للأساسيّات · ما يفصل: بريماركت 24.4% · `J1` 27.3% · `gap≥30%` 29.8% · عضويّةُ «الجاهز» 5.3% | **داخلُ العيّنة لهذا العقد (المراسي حتى 2026-09-24)** — منه اشتُقّت القاعدة |
| `T-PMUSD` · `T-PMFWD` | سيولةُ البريماركت (الفرعُ 1) · حصّادُ مليون دولار على مرشّحينا (حسمٌ 2026-12-31) | محورٌ مجاور — لا يُقاس هنا |
| `T-OPTRADE` | ‏−0.70R للصفقة بعد المرساة | **«ينفجر» ≠ «يربح»** — حدُّ صدقٍ مُلزِم |
| `T-SESSIONS` · `T-WAVES` (مُغلَقان) · `T-PMGATE` (مُغلَق) | ساعةُ **قمّة** السعر داخل الجلسة · بوّابةٌ عند فتح البري ‏+30د | **دعوى أخرى**: هنا جلسةُ **المرساة** (قبل 09:30) من `tierlink_probe.features` لا ساعةُ القمّة ولا بوّابةُ توقيت ⇒ لا فتحَ لمحورٍ مُغلَق |
| `T-RANKER3` · `C3` · `T-RSI40` (مُغلَق) | لا مراسي في `select_top` · لا RSI 40 | خطٌّ لا يُمَسّ |

والمُغلَقُ تنفيذًا اليوم (`grep -ln CLOSED_RC *.py`): `edgar2_study` · `filloff_arms` · `hrt_arms` · `pmgate_arms` ·
`rsi40_arms` · `rsi_rank_arms` · `sessions_probe` · `support_def_probe` · `trail_arms` · `trail_op_arms` · `waves_probe` — **ولا يمسّ هذا العقدُ أيًّا منها.**

### انحرافاتٌ عن نصّ الحزمة — مُعلَنةٌ قبل التنفيذ (الحلُّ الأقلُّ خطرًا · الحزمة §«إذا وجدت تناقضًا»)
- **`D-1` R-02 لا يُنفَّذ:** الحزمةُ سمّت `Super_stock.py:17149` (`build_live_alert`) — وهو **تنبيهُ أحداث المراقب**
  («🚨 أحداث لحظية على القائمة» · يناديه `pullback_live.py:90`) لا كرتَ «هنا الدخول». الكرتُ هو
  `Super_stock.py:15879` (`build_liq_stage_alert` · يناديه `operator_entry_live.py:664`) **وفيه سلفًا**
  «🌅 J1 في البريماركت» (`Super_stock.py:15775` (`j1_premarket_flag`)) منذ 2026-09-02. وشارةٌ عامّة للبريماركت =
  **إطالةُ تنبيهٍ حيّ** (خطٌّ أحمر) وتكرارٌ لشارةٍ قائمة ⇒ **نُقلت إلى الفرع 1 من هذا العقد** اقتراحًا بأمر المالك.
- **`D-2` السجلُّ لا يحمل النتيجة:** مبدأُ `ctb_harvest.py` «النتيجةُ لا تُخزَّن في الصفّ أبدًا» ⇒ الصفُّ = الميزات
  (‏`tod` · `j1` · `gap` · `shadow` · `member` · `ready`) ‏+ المتاح؛ و`exploded50` يُحسَب وقتَ كلّ تشغيلة لأيّامٍ مكتملة.
- **`D-3` السجلُّ أماميٌّ وحدَه:** صفوفُ ما قبل `SHADOW_FWD_SINCE` تُطبَع موسومةً «داخل العيّنة — لا يحكم» ولا تُكتب.
- **`D-4` الدفعُ بخطوة الدفع المحميّة** (نمطُ `ctb_harvest.yml`) لا بـ`git_save` (الذي يحلّ التعارضَ بإعادة كتابة
  تاريخ `main` — درسُ «مرجعٌ بائتٌ يكذب») · والأداةُ نفسُها لا تنادي `git_save` ولا تكتب إلّا سجلَّها.
- **`D-5` R-03 بقناةٍ قائمة وحارسِ جلسة:** `ce_borrow_info` بالاسم (المصدرُ نفسُه لـ`ctb_harvest`) **ولا يُسجَّل المتاحُ
  إلّا إن كانت التشغيلةُ في يوم المرساة نفسِه بتوقيت نيويورك وبعد 20:00** ⇒ بلا جدولةٍ ليليّة (أمرٌ منفصلٌ للمالك) لا متاح.
- **`D-6` هذا العقد** (أعلاه).

## ① السؤال
**هل تبقى القاعدةُ الظلّيّة `S` تفصل المنفجرين على مراسٍ لم تُرَ عند اشتقاقها؟ وكم تلتقط من انفجارات المراسي
مقابل قائمة «الجاهز» على الأيّام نفسِها (وكم ضجيجًا تضيف)؟**
⚠️ **ولماذا «ظلّيّة» لا «جاهز»:** «الجاهز» يُحسب قبل الافتتاح (كرون 01:50 UTC) **قبل أيّ مرساة**، و`S` حدثٌ
لا يُعرف إلّا لحظةَ المرساة ⇒ لا يدخل قائمةً صباحيّةً بالبناء. فالمقارنةُ **قياسٌ** لا اقتراحُ دمج (R-00).

## ② المجتمع
- **المراسي:** كلُّ `LIQ:*` بمِرساةٍ من تاريخ git لـ`op_entry_state.json` — `tierlink_probe.anchor_history` **بالاسم**.
- **الأماميّ:** المراسي بتاريخ **‏≥ `SHADOW_FWD_SINCE` = 2026-09-26** (اليومُ التالي لدفع هذا العقد · أوّلُ جلسةٍ 2026-09-28).
  وما قبله **داخلُ العيّنة** (اشتُقّت منه القاعدة في `T-OPLINK`) يُطبَع ولا يحكم.
- **الأيّامُ المكتملة وحدَها:** يومُ المرساة يُقاس بعد **20:00 نيويورك** منه (نهايةُ الافتر) — وإلّا خارجُ التشغيلة.

## ③ التعريفات — مثبَّتة
- `tod`، `j1`، `gap` من `tierlink_probe.features` **بالاسم**؛ وفجوةُ من ليس في السجلّ من إغلاق الأمس في
  `tierlink_probe.daily_range` (الإصلاحُ نفسُه في `tierlink_probe.main` و`opentry_link_probe.main`).
- **`S` = `tod == "pre"` ∧ (`j1 == "J1"` ∨ `gap == "≥30%"`)** — والمجهولُ «؟» **ليس** `S`. و`S̄` = كلُّ مرساةٍ أخرى.
- **العضويّة:** `opentry_link_probe.membership` **بالاسم** على لقطة `weekly_watchlist.json` **‏≤ يوم المرساة**
  (`snapshot_before`) ⇒ `member` (نشطٌ في القائمة) و`ready` (`entry_status == ready_now`).
- **المقياس:** `exploded50` من `tierlink_probe.measure` **بالاسم** (أقصى قمّةِ يوم المرساة ‏≥ ‏+50% من سعر كرت `M5`).

## ④ المعيار الحاكم — الأربعةُ معًا (‏يُقرأ بالأضعف)
1. نسبةُ `S` **‏≥ ‏2×** نسبةِ `S̄` (عتبةٌ مُعادة من `T-OPLINK §⑤`).
2. فاصلا Wilson 95% **منفصلان** (أدنى `S` فوق أعلى `S̄`).
3. **الأرضية:** ‏`S` ‏≥ **60** و‏المجموعُ الأماميّ ‏≥ **150** (رقمان مُعادان من `T-J1PM §③`).
4. **النصفان:** تُقسَم نافذةُ الحكم عند وسيط تواريخها، و`S` أعلى من `S̄` في كلٍّ منهما بـ‏≥10 صفوفٍ من كلٍّ (‏`MIN_HALF`).

- **نافذةُ الحكم = أوّلُ يومٍ `D*` تبلغ فيه الصفوفُ الأماميّة حتى `D*` الأرضيةَ** — والحكمُ عليها وحدَها **مرّةً واحدة**
  ولا يُعاد بصفوفٍ لاحقة (لا إيقافَ اختياريّ ولا تحريكَ رقم).
- **قبل الأرضية:** «لا حكم» والعدّاداتُ الوصفيّة وحدَها. **الحسمُ الأقصى 2026-12-31** (رقمُ `T-PMFWD`).
- **التغطية:** جلبٌ دون 80% ⇒ لا يُفسَّر رقم (خروج 3).

### الفروع
1. **«تفصل أماميًّا»** (الأربعة) ⇒ **اقتراحٌ للمالك بلا تنفيذ:** شارةُ عرضٍ «🌅 مرساةٌ في البريماركت» على كرت `M5`
   للمراسي `S` التي لا تحمل «🌅 J1 في البريماركت» — **بأمره الصريح** (إطالةُ تنبيهٍ حيّ) · وأيُّ فلترٍ أو ترتيبٍ أو
   بوّابة = **تسجيلٌ مسبقٌ جديد** · و«الجاهز» لا يتغيّر (R-00).
2. **«لا تفصل»** ⇒ لا شارة · القائمةُ الظلّيّة تُغلَق بقرارٍ مؤرَّخ · و«الجاهز» لا يتغيّر.
3. **«لا قياس»** ⇒ الأرضيةُ لم تُبلَغ حتى 2026-12-31 أو التغطيةُ دون 80%.

### وصفيٌّ لا يحكم (يُطبَع في كلّ تشغيلة)
التقاطُ المنفجرين الأماميّين: `|E∩S|/|E|` مقابل `|E∩member|/|E|` و`|E∩ready|/|E|` · وضجيجُ `S` = `|S∖E|` لكلّ جلسة ·
والتقاطعُ `S∩member` · وملخّصٌ لكلّ يوم (مراسٍ · `S` · أعضاء · جاهز · تقاطع · منفجرٌ في كلٍّ).

## ⑤ فرضيّةُ المالك في المتاح (R-03) — تُقاس بالشروط الأربعة نفسِها
- **المصدر:** `Super_stock.ce_borrow_info` بالاسم **في جلسة المرساة نفسِها بعد 20:00 نيويورك** (`D-5`) · سقفٌ **40** نداءً
  للتشغيلة مُعلَن · والمتعذّرُ مجهولٌ لا صفر.
- **السلّة:** `avail < BORROW_AVAIL_MAX` (‏20,000 = نصُّ فيصل) مقابل الباقي بين المراسي الأماميّة ذات المتاح.
- **الحكم:** الشروطُ ①②④ أعلاه بأرضيةِ **100 مرساةٍ مؤرَّخة** (رقمُ الحزمة R-03) و20 في كلّ سلّة (‏`MIN_N`) ·
  ونافذةُ الحكم أوّلُ يومٍ تُبلَغ فيه · والحسمُ الأقصى 2026-12-31.
- ⚠️ **وحدُّ صدقٍ يُعلَن الآن:** بلا جدولةٍ ليليّة (أمرُ المالك) لا يُسجَّل متاح ⇒ «لا قياس» **بالبناء**.

## ⑥ التنبّؤات — قبل أيّ رقم
| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| **`SR-P1`** | نسبةُ `S` الأماميّة **دون** نسبتِها داخل العيّنة (تُطبَع في أوّل تشغيلة) **لكن ‏≥2×** `S̄` | ‏≥ نسبة العيّنة · أو دون 2× |
| **`SR-P2`** | `S` تلتقط من المنفجرين الأماميّين **أكثر** ممّا يلتقطه «الجاهز» (‏`ready`) · و«العضو» يلتقط **أقلّ من 10%** | `ready` ‏≥ `S` · أو العضو ‏≥10% |
| **`SR-P3`** | أكبرُ منفجرٍ أماميّ (‏`mg_day`) **خارج** `S` (الرابطُ يلتقط التكرار لا الحجم · كـ`JP-P4`) | داخلها |
| **`SR-P4`** | `S` تبلغ 60 صفًّا أماميًّا **قبل 2026-11-06** | بعده أو لا تبلغ |
| **`SR-P5`** | المتاح ‏<20k **لا يفصل** (اتّجاهُ `T-OPLINK` معكوس: 1/18 مقابل 3/21) — أو «لا قياس» بلا جدولة | يفصل بالأربعة |

## ⑦ الحرّاس
- **`V-S1`** تغطيةُ الجلب ‏≥80% وإلّا خروج 3 · **`V-S2`** شاهدُ الهُويّة: على 2026-08-18⟶09-01 `exploded50` **33/321 · 14**
  بت-بت مع `T-TIERLINK`/`T-OPLINK` (يُطبَع ✅/⚠️ ويُقرأ الفرقُ قبل أيّ حكم) · **`V-S3`** لا لقطةَ بعد يوم المرساة ·
  **`V-S4`** لا نتيجةَ ليومٍ غيرِ مكتمل · **`V-S5`** حارسُ جلسة المتاح · **`V-S6`** قراءةٌ فقط عدا سطرِ السجلّ
  (بالـAST) والإنتاجُ لا يستوردها · **`V-S7`** لا تلغرام ولا كرون (يدويّ · والجدولةُ أمرٌ منفصل بعد تشغيلتين ناجحتين).
- **رموزُ الخروج:** 0 صدر · 2 لا مفتاح · 3 تغطيةٌ دون 80% · 4 صفرُ مرساة (بصمةُ no-op) · 5 الحارسُ الذاتيّ.

## ⑧ حدودُ صدق — قبل النتيجة
1. **«انفجر» لمسُ قمّة لا صفقة** · و`T-OPTRADE` ‏−0.70R ⇒ التقاطُ المنفجر ليس ربحًا.
2. ميزاتُ المرساة من **آخر لقطةٍ لليوم** (كـ`T-TIERLINK`) · والعضويّةُ من لقطةٍ قد تكون **بعد** ساعة المرساة في اليوم نفسِه.
3. فترةٌ قصيرة وقائمةٌ عمرُها أسابيع — **لا تُعمَّم على سنوات** (قاعدةُ الثلاث سنوات للباكتيست لا تنطبق على حصّادٍ أماميّ).
4. `S ⊇ J1∧pre` ⇒ يتداخل مع `T-J1PM` على الأيّام نفسِها (مجتمعان مختلفان: كلُّ المراسي هنا · المُسلَّمةُ هناك).
5. **ولا يُستنتج من أيّ فرعٍ أن «الجاهز» أُعيد بناؤه** — لم يُعَد، بالدليل (`T-OPLINK`).
