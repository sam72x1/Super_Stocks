> 🔒 **تسجيلٌ مسبق (v2 — نهائيّ بعد الطعون الثلاثة) — يُدفَع قبل أيّ رقم، ولا يُمَسّ بعده.** أيُّ تعديلٍ بعد أوّل تشغيلة يُكتب **تحت** الملفّ بتاريخه وسببه، ولا يُحرَّر أعلاه سطرٌ واحد.
> 🔒 **قراءة/بحث فقط:** صفر مسّ إنتاج · الجذور التسعة (`rank_key` · `select_top` · `classify_tier` · `entry_status` · `analyze_ticker` · `apply_short_gate` · `apply_float_gate` · `scan_market` · `backtest_symbol`) **byte-identical** · لا `LOGIC_VERSION` · **ولا رسالة تلغرام** (الـworkflow بلا أسرار).
> 📌 كلُّ رقمٍ أدناه إمّا **`ملف:سطر` فتحتُه بنفسي**، أو **【مقيسٌ منّي في هذي الجلسة】**، أو **【حسابٌ منّي بافتراضٍ مُعلَن】**، أو موسومٌ **【غير مُتحقَّق】**. لا رقم بلا أحد الأربعة.

# 🧪 `T-ENVELOPE` — هل **التداول** بظرف الكاتالوج يربح؟

---

## ⓪ الحال قبل التشغيل

### ⓪-أ ما هو مقيسٌ سلفًا

| | |
|---|---|
| **الظرف** | `P90` · التقاط `LOO` **66.7% (20/30)** · تمييز **7.2%** · إثراء **×9.20** · **≈105 مطابق/يوم** (`envelope_p90.json:10-13`) · بصمة الحواف **`575dc1855c51`** 【حسبتُها بنفسي بنفس `edges_fingerprint` فطابقت ترويسة `envelope_candidates.jsonl`】 |
| **حيًّا** | جلسة 2026-08-05: كون 3564 · مغطّى 3383 (**94.9%**) · خارج الظرف 3263 · **داخله 120** · أخرجتهم `M14` **11** · فلوتٌ مجهول 4 · **المُخرَج 109** · `m13_measured: false` · قصٌّ 0 (ترويسة `envelope_candidates.jsonl:1`) |
| **الأساس المنشور** | صافي R/جلسة **‏−0.2437 · +0.0120 · −0.0891** · إجمالي **‏−26.6 · +1.3 · −12.3R** · مأخوذة 85·82·97 · جلسات 109·109·138 (`replay10_result.md:141-143`) · السعة تقصّ **‏27% · 30% · 48%** (`:180`) · البِركة تُسلّم **‏0-3 منفجرين/سنة** (`ranker_result.md:41`) |
| **الفارز على أسهم فيصل** | يقبل **‏0.2%** من 4219 يومًا (`additions_audit.md:52-54`) |

### ⓪-ب 🔴 وما ليس مقيسًا — بنصّ ملفّ النتيجة

> «‏**لم تُقَس الربحية إطلاقًا.** المقيس: هل يحوي الظرف إعدادات فيصل؟ — **لا** هل التداول بالظرف يربح؟» (`catalog_envelope_result.md:96-97`)

### ⓪-ج 🔴🔴 وثلاثةُ عيوبٍ **مقاسة قبل التشغيل** تُغيّر التصميم — لا تُذكر بعده

| # | العيب | الدليل الذي فتحتُه |
|---|---|---|
| **ع1** | **الحواف التي سيقرّر بها `E1` غير مُبرهَنٍ أنها مُخرَجُ الأداة.** الملفّ يقول عن نفسه `"source": "منقولٌ من سجلّ التشغيلة — يُستبدَل بمُخرَجٍ آليّ من الأداة"` (`envelope_p90.json:15`) بينما الأداة تكتب `"مُخرَجٌ آليّ من catalog_envelope.py"` (`catalog_envelope.py:501`)، **ويحمل أربعة مفاتيح لا تكتبها الأداة قطّ** (`loo_capture_pct`/`discrimination_pct`/`enrichment`/`matches_per_day_est` مقابل بلوب `:494-504`) 【قارنتُ مجموعتَي المفاتيح بنفسي】. وrun_id فيه `31011181491` بينما ترويسة النتيجة تسمّي المعتمَدة `30992071835` (`catalog_envelope_result.md:6`)، وجدولُها يطبع `price=1.64`/`dollar_vol=124,792` (`:80`،`:76`) بينما الملفّ `1.62`/`124,665`. 🔴 **والأثر مادّيّ: بحافّة 1.64 يصير مُخرَج الجلسة 107 لا 109** 【مقيسٌ منّي على الـ109】. 🔴 **والأخطر أن قفل السويّة الذي يدّعي حراسة هذا فارغ:** `test_bot.py:13584-13586` عنوانُه «وتُصدَّر **آليًّا** من الأداة لا نقلًا يدويًّا» **وهو يفحص وجودَ كود التصدير في `catalog_envelope.py` لا مصدرَ الملفّ** ⇒ أخضرُ والادّعاء غير صحيح؛ والقفل السلوكيّ (`source` تبدأ بـ«مُخرَجٌ آليّ») **يسقط اليوم**. |
| **ع2** | **`RECENT_RISE_BLOCK_PCT` (منع الملاحقة، ‏35%/5ج) هو المفتاح الوحيد المُرخى بلا معيارٍ في الظرف** 【مقيسٌ آليًّا: 13 مفتاح إرخاء مقابل 11 معيارًا تغطّي 12 مفتاحًا ⇒ الفارق مفتاحٌ واحد بعينه】 — يُضبَط `1e9` (`catalog_envelope.py:68`) وليس في `CRITERIA` (`:82-94`)، وبوّابتُه **رفضٌ صلب** حتى للارتداد (`Super_stock.py:2603-2604`)، وهي **قرارٌ جذريّ مقفول (D11)** في `CLAUDE.md`. ⇒ **`E1` كما يعمل الصيّاد الحيّ اليوم بلا أيّ حاجزٍ ضدّ ملاحقة ما ارتفع سلفًا.** |
| **ع3** | **الظرف مُقدَّرٌ من جلساتٍ قد تكون داخل الحركة، والزمنُ مجهولُ المدى.** نافذة المعايرة تنتهي عند `idx-1` — اليوم السابق لتسجيل الـ+100% (`catalog_envelope.py:256`،`:260`)، و`ANCHOR="last"` (`:37`) بلا أيّ حدٍّ أدنى للتاريخ (`:100-123`)، واللقطة تمتدّ **‏2022-01-01 → 2026-07-31** (`gates_result.md:4` + `envelope_p90.json:4`) ⇒ نافذةُ معايرة أيّ رمزٍ قد تقع **داخل** 2023/2024/2025. **وتواريخ المِرساة غير مخزَّنةٍ في أيّ ملفّ** (تُطبَع سجلًّا فقط: `catalog_envelope.py:271`،`:353`؛ وقرأتُ `catalog_envelope_result.md` كاملًا فلا تاريخ انفجارٍ واحدًا) ⇒ **التلوّث الزمنيّ غير قابل للقياس اليوم**. وحساسية النافذة المنشورة تغيّر **طولها** لا **نهايتها** (`:28` مع `:256`) ⇒ **إزاحةُ النهاية لم تُختبَر قطّ.** |

---

## ① السؤال — جملةٌ واحدة قابلة للتكذيب

> **هل استبدالُ تعريف المجتمع ببوّابات ظرف `P90` المجمَّدة — مع إبقاء كلِّ ما عداه كما هو حرفيًّا — يعطي توقّعًا أعلى بوحدة المخاطرة (`R`) لكل إشارة في السنوات الثلاث 2023/2024/2025، ويصمد بعد نزع رموز الكاتالوج وبعد إعادة حاجز منع الملاحقة؟**

**المتغيّر المعزول: تعريف المجتمع وحده.**

🔑 **وسلامةُ العزل مُبرهَنة سلوكيًّا لا مُدَّعاة** 【مقيسٌ منّي في هذي الجلسة】: أعدتُ نداء `analyze_ticker` على إطارٍ واحد بـ`RELAX_ALL` ثم بقيمٍ **متطرّفة مختلفة لكلّ مفاتيح الإرخاء الثلاثة عشر** (‏`MIN_PRICE=-1e6` · `RSI_OS_HARD=1e15` · `WATCH_MAX_FAILS=1e9` … ) ⇒ **الخطّة متطابقة تمامًا** (`pivot`/`stop`/`t1`/`t2`/`t3`/`tranches`/`rr`/`score`/`readiness`/`liberation`). وسندُه في الكود: كلُّ مفاتيح الإرخاء تُستعمل في **قرارِ رفضٍ أو نقصٍ** حصرًا (`Super_stock.py:2530`،`:2603`،`:2653`،`:2659`،`:2696`،`:2790`،`:2935`،`:3144`) بينما المستويات تُحسب بعدها (‏`pivot`=`:2939` · الوقف=`:2943-2945` · الدفعات=`:2957-2959` · `entry_ref`/`rr`=`:3138-3140`). ⇒ **الإرخاء يبدّل العضوية ولا يبدّل الخطّة، فالذراعان تتشاركان محرّك التخطيط حرفيًّا.**

⚠️ **واستثناءٌ واحد مقيس:** `n_soft` **يتأثّر** — بـ`MIN_RR_T1=-1e9` كان **‏6** وبـ`+1e15` صار **‏7** 【مقيسٌ منّي】 (السطر `:3144-3145`) ⇒ **حافّة `n_soft ≤ 6` مقيسةٌ على سلّمٍ يُسقِط نقصَ RR بنيويًّا** فلا تُقارَن بحدّنا 3 مباشرةً. متطابقٌ بين `E1` والصيّاد الحيّ (كلاهما بـ`decide`) فلا يحيز المقارنة، ويبقى قيدَ قابليّة نقل.

**ما لا يتغيّر بين الأذرع:** مصدرُ الخطّة (`analyze_ticker` الإنتاجيّ) · التعبئة بمتوسط الدفعات وأوّل لمسةٍ للقاع (`Super_stock.py:14204`،`:14211`) · الحسم `_resolve_arm` (`:13572`) · فهرس الخروج `_arm_a_exit_bar` (`:13611`) · النافذة الأمامية 40 (`:547`) · الخطوة 5 (`:548`) · `MIN_BARS` 120 (`:529`) · السعة 10 (`replay10.py:27` = `Super_stock.py:254`).

---

## ② الأذرع — مثبَّتةٌ بالاسم قبل أيّ رقم

| الذراع | تعريف المجتمع | الكون | الدور |
|---|---|---|---|
| **E0** | الفارز الحاليّ (`analyze_ticker` بعتبات الإنتاج) | اللقطة كاملةً | **الأساس** — يُحسَب في **نفس التشغيلة** |
| **E1** | `envelope_scan.decide` بحوافّ `P90` المجمَّدة | اللقطة كاملةً | الذراع كما تعمل حيًّا اليوم — **تشخيصية** |
| **E1-C** | `E1` **‏+ حاجز منع الملاحقة الإنتاجيّ** (‏35%/5ج) | اللقطة كاملةً | 🔴 **الذراع التي يجوز أن يُقترَح عنها شيء** |
| **E0-X · E1-X · E1-C-X** | كما هي، **ناقص الخمسة والثلاثين** | اللقطة ناقص الكاتالوج | شواهد نزع الدائرية |
| **E1-P100** | `decide` بحوافّ `P100` | اللقطة كاملةً | شاهد اتّساق المئينيّة |

🔴 **النافذ عند الاختلاف: `E1-C-X` مقابل `E0-X`.** مُسجَّلٌ الآن قبل أيّ رقم، تنفيذًا لـ`PIVOT_V2_PLAN.md:163` («والنافذ عند الاختلاف هو المستبعِد») **ومعه ترقيةٌ مُعلَنة: يجب أن يكون الحاجزُ الإنتاجيّ المقفول (D11) قائمًا في الذراع النافذة** — لأن `E1` بلا هذا الحاجز **تخالف قرارًا جذريًّا مقفولًا**، فلا يجوز أن يُبنى اقتراحٌ عليها.

**تنفيذ `E1-C` بلا سطر منطقٍ مستنسَخ:** جلسةٌ قبِلها `decide` تُعاد بـ`RELAX_ALL` **منقوصًا `RECENT_RISE_BLOCK_PCT`**؛ فإن رجع `measure_session` بـ`None` فقد رفضتها بوّابةُ الملاحقة **الإنتاجية نفسها** (`Super_stock.py:2603-2604`) — الفرقُ الوحيد بين النداءين. **صفر إعادةِ حسابٍ لـ`gain5`** فلا فرصة لصيغةٍ موازية تنحرف. الكلفة: نداءٌ إضافيّ على المقبولين وحدهم (‏≈3% من الزيارات).

**لماذا الاستبعاد بالخمسة والثلاثين لا بالثلاثين — وهو انحرافٌ مُعلَن عن الخطّة:** الخطّة تقول «يستبعد الواحد والثلاثين» (`PIVOT_V2_PLAN.md:163`)، والظرف مُعايَرٌ على **‏30** (`envelope_p90.json:6`) هم `CATALOG` الخمسة والثلاثون (`catalog_envelope.py:43-48`) ناقص `HTZ` (`:56-58`) ناقص **أربعةٍ لم تُنتج جلسةً مقيسة وهويّتُهم غير مدوَّنةٍ في أيّ ملفّ**. ⇒ الخمسةُ والثلاثون **مجموعةٌ فائقة ⇒ أشدُّ تحفّظًا بالبناء** ولا تحتاج إعادة معايرة. 🔒 **وتُجمَّد الآن مجموعةً وحيدة:** حُذف نهائيًّا أيُّ بابٍ من صنف «إن نُشرت أسماء الثلاثين يومًا» — **ممنوعٌ حسابُ نسخة الثلاثين في هذي التجربة إطلاقًا.**

**المُرتِّبات:** `rank_actual` (`replay10.py:57`) · `rank_fifo` (`:65`) شاهد ضبط · `make_rank_random` بـ**2000 بذرة** (`:77`). وتُستعمل في **المقياس الثانويّ وحده** (‏§③).

---

## ③ المقياسان — والتراتب مُثبَّتٌ قبل الأرقام

### ③-أ المقياس **الأساسيّ الحاكم**: `M-P` — التوقّع بوحدة المخاطرة لكلّ إشارة، **على البِركة بلا سعة**

`r_unit` من عائد التنفيذ الفعليّ `ret_a` ÷ `(دخول−وقف)/دخول` (`replay10.py:217-236`) فالوقف = **‏−1R** بالضبط، وغيرُ المُعبَّأة = **‏0.0R** (`:222-223`). `Δ_P` = فرقُ متوسطَي `r_unit` بين الذراعين.

**ولماذا هذا هو الأساسيّ — وهو انحرافٌ مُعلَن عن الخطّة (`PIVOT_V2_PLAN.md:162` يفترض مقياس المحفظة):** لأنه **المقياس الذي يقيس المتغيّر المعزول في §① وحده**، بينما مقياس المحفظة يمرّ عبر طبقة مُرتِّبٍ وسعةٍ **قِيست عند مستوى الصدفة** وتقصّ حتى **48%** (`replay10_result.md:180-182`) — أي أن ضجيج الحكم يأتي من طبقةٍ **مُعلَنةٍ خارج نطاق السؤال**. والفرق مقيسٌ رقميًّا في ③-ج.

### ③-ب المقياس **الثانويّ**: `M-D` — صافي `R`/جلسة بسعة 10

`net_r_per_day` (`replay10.py:271`). 🔒 **مقامٌ مثبَّتٌ مستقلٌّ عن الذراع:** عددُ **تواريخ الجلسات الفريدة في اللقطة داخل النافذة** — لأن `session_index` يُبنى من **اتّحاد تواريخ الصفوف المُمرَّرة** (`:205-214`) فيتغيّر المقام بتغيّر الذراع. 🔴 **وممنوعٌ منعًا باتًّا الاستشهاد بأرقام `replay10_result.md:141` كأساسٍ مقابل** — مقامُها 109·109·138 من ذراعٍ أخرى، والأساس الوحيد المقبول هو `E0` المحسوب في **نفس التشغيلة**.

### ③-ج 🔴 القوّة و`MDE` — **محسوبةٌ الآن قبل أيّ رقم**

【حسابٌ منّي بافتراضٍ مُعلَن: نموذج نقطتين (فوزٌ عند `+w` وخسارةٌ عند `−1R`) بنسبة النجاح من `gates_result.md:249-251` والمتوسط من `replay10_result.md:142-143`؛ ويسنده أن `CVaR5%` بلغ ‏−1.65..−2.64R (`:153`) فالذيل تحت ‏−1R موجودٌ فعلًا】

| | |
|---|---|
| `sd(r_unit)` | **2023 ‏1.22 · 2024 ‏1.43 · 2025 ‏1.76 ⇒ مجمَّعة ‏≈1.50 R/صفقة** |
| **`MDE` لـ`M-D`** (سعة 10 · 356 جلسة · `E0`=264 مأخوذة · `E1`≈200-400) | **‏0.23 – 0.27 R/جلسة عند z=2.5** ⇒ يلزمها **‏+81 إلى +97R** إجمالًا في ثلاث سنوات |
| للمقارنة | إجمالي الأساس **‏−37.6R** · **وقيمة المُرتِّب كلُّها +5.0R** (`replay10_result.md:172-174`) ⇒ المطلوب **‏≈18×** قيمةَ المُرتِّب الكاملة |
| **`MDE` لـ`M-P`** (بِركة `E1` ‏≈4239/سنة مقابل `E0` ‏428 مجمَّعًا) | **‏0.145 R/صفقة عند z=1.96** · ‏0.184 عند z=2.5 |
| **القوّة عند أثرٍ = 0.15R** | **‏53% (z=1.96)** · **‏32% (z=2.5)** |

🔴 **والنتيجة الحاسمة المكتوبة قبل الأرقام:**
1. **حدُّ الأثر المسجَّل (‏0.15R) أصغرُ من `MDE` مقياس المحفظة (‏0.23-0.27)** ⇒ **`M-D` لا يمكن أن يَبين أبدًا** ⇒ لا يجوز أن يحكم وحده، ولا أن يُغلق محورًا.
2. **`MDE` مقياس البِركة ‏0.145 ≈ حدّ الأثر** ⇒ التجربة تكشف **أثرًا كبيرًا فقط**، وقوّتُها عند الحدّ نفسه **‏53%**.
3. **وأكبرُ أثرٍ قِيس في المشروع كلِّه ‏+0.13R في سنةٍ واحدة ولم يثبت** (`m4_iso_result.md:65-66`)، والمجمَّعات الفعلية ‏−0.001R (`manage25_result.md:71`) و‏−0.004R (`event_exec_result_corrected.md:32`). ⇒ **الاحتمال الغالب «لا حكم»، وهذا مكتوبٌ سلفًا لا يُكتشَف بعدًا.**

🔒 **وقاعدةٌ إلزامية:** الأداة **تحسب `sd` المقيس و`MDE` من صفقات الأساس في نفس التشغيلة وتطبعهما قبل قراءة أيّ ذراع**، والحكم يستعمل **`MDE` المقيس** لا المُقدَّر أعلاه.

### ③-د المساند — يُنشَر ولا يحكم، ولكلٍّ تفسيرٌ مكتوب الآن

نسبة النجاح (**لا تحكم أبدًا**: `T-STOP` رفعتها ‏34→54% بلا تحرّك التوقّع · `T-LIBERATION` بلغت ‏75-87% بتوقّعٍ سالب — القرارَان في `CLAUDE.md`) · `rejected_cap`/`rejected_dup` · أقصى تراجع · `CVaR5%` (**تفسيره:** سوءٌ فيه لا يُسقط ولا يُجيز — إلّا إن ساء بأكثر من ‏0.5R فيُوسَم «إدارةُ مخاطرةٍ أسوأ») · **وسيط `mg_pre_stop`** (`Super_stock.py:14274`؛ **تفسيره:** أرضيةٌ لا سقف — أرضيتُه صفرٌ بالبناء `:13653` فالخاسر يُسجَّل 0.0 ⇒ نصفُ توزيعٍ مقصوص) · **شاهد `FIFO`** (**تفسيره المسجَّل:** تفوّقُه = «إشارةُ عطبٍ في المُرتِّب» لا غيابَ حافة — `replay10_result.md:169-170`).

### ③-هـ ممنوعٌ منعًا باتًّا

**`exploded`** — يُحسب من `max(hi[filled:])` على كامل النافذة **بلا أيّ اعتبارٍ للوقف** (`Super_stock.py:14214`،`:14253`) وهو **محرَّمٌ بنصّ الخطّة** (`PIVOT_V2_PLAN.md:164`) ومع ذلك تقرأه أداةُ الأذرع القائمة (`replay10_arms.py:71`) ⇒ **يُحذَف من هذي التجربة كلَّها**، وبديلُه `mg_pre_stop ≥ 100` (نظيفٌ بالبناء: الوقف يقطع القياس، `:13656-13657`). ومعه: `fwd_max_gain` · `sector`/`country`/`days_to_earnings` · أيُّ حقلٍ من `enrich` · فلوتُ اليوم أو شورتُ اليوم.

### ③-و مِرساةُ الجلسة — **`eligible_at` هي الحاكمة**

`trade["date"]` = `df.index[i-1]` = **يوم الإشارة** و`analyze_ticker` يقرأ حتى إغلاقه (`Super_stock.py:14185`،`:14227`)، بينما أوّلُ جلسةٍ يجوز فيها التنفيذ هي `fut.index[0]` المخزَّنة صراحةً في `eligible_at` (`:14296`) 【مقيسٌ منّي: `date=2023-06-16` و`eligible_at=2023-06-19`】. و`candidates_from_trades` يُرسي الجلسة على `t["date"]` (`replay10.py:252`) ⇒ **الخانة تُحجَز جلسةً قبل أن تكون الخطّة قابلة للتنفيذ**. ⇒ **المِرساة الحاكمة `eligible_at`** (مِرساةٌ من مصدر المحرّك نفسه)، و`date` تُنشَر حساسيةً. وهذا **سببٌ إضافيّ لمنع الاستشهاد بأرقام `replay10_result.md`.**

---

## ④ بوّابة القبول — **سبعةٌ معًا**، بأرقامها

| # | الشرط | الرقم |
|---|---|---|
| **①** | **ثبات الإشارة**: `Δ_P` موجبٌ في **كلّ سنةٍ مقبولة** (بعد قاعدة التلوّث ⑦-١) بلا انقلاب | 3/3 من المقبولة |
| **②** | **حجم الأثر**: `Δ_P` **المجمَّع** ‏≥ **‏+0.15 R/إشارة** — الحدُّ المسجَّل في الخطّة (`PIVOT_V2_PLAN.md:165`) ونفسُه في `manage25_prereg.md:79` | ‏≥0.15R |
| **③** | **العيّنة**: `E1-C-X` ‏**≥30 محسومة/سنة و≥150 مجمَّعًا** (`event_exec_prereg.md:110`) · و`E0` ‏≥100 إشارة/سنة (فحصٌ لا عقبة: المقيس 120·117·191) | كما هو |
| **④** | **الدلالة**: `z` **أحاديّ الطرف** ‏≥ **‏1.96** (بونفيروني على **العقدين الحقيقيّين** ⇒ α=0.025) على `Δ_P` المجمَّع · **و**فاصلا **`paired cluster bootstrap` بالرمز** و**`block bootstrap` الشهريّ لِـ`Δ_P`** كلاهما لا يلمس الصفر | z≥1.96 |
| **⑤** | **الصمود**: نفس الحكم في **`E1-C-X` مقابل `E0-X`** · **و**`E1-C` موجبةٌ أيضًا (لا يقوم الأثر على إسقاط D11) · **و**اتّفاق إشارة `Δ_P` بين `P90` و`P100` | كما هو |
| **⑥** | **الامتناع والتسليم**: عددُ المُسلَّم في `E1` ‏≥ **‏0.8×** `E0` كلّ سنة · **و**`Δ_D` **ليست سالبةً دالّة** (فاصلُها 95% لا يقع كلُّه تحت الصفر) | كما هو |
| **⑦** | **التركيز**: لا رمزٌ ولا شهرٌ يصنع أكثر من **‏20%** من فرق `Δ_P` (سابقةُ `event_exec_prereg.md:115`) | ≤20% |

**تعريف `z` مثبَّتٌ الآن بأربع كلمات فلا يُختار بعد الأرقام:** ① **أحاديّ الطرف** (الفرضية اتّجاهيّة) ② **بونفيروني على مقارنتين حقيقيتين فقط** (`E1C−E0` و`E1CX−E0X`) ⇒ α=0.025 ⇒ z≥1.96 ③ **العنقود = الرمز عبر السنوات الثلاث**، و`c_s` = فرقُ **مجموع** `r_unit` للرمز مقسومًا على **عدد إشاراته في الذراعين** (وحدةُ `Δ_P` هي R/إشارة، فالمقام إشاراتٌ لا جلسات) ④ `ddof=1`. و`SE = sd(c_s)·√k` حيث `k` عددُ العناقيد.
🔴 **والعقدان العشوائيّان (‏مقابل متوسط `make_rank_random`) لا يصحّ فيهما `z` عنقوديّ إطلاقًا** — سُحُبُه تعمل على **نفس** الصفقات فالفرق مزدوجٌ بنيويًّا ⇒ تُقاس بـ`randomization_test` القائمة (`replay10.py:333`) كعائلةٍ **ثانية** مصحَّحةٍ منفردة، **ولا تدخل بونفيروني الأولى**.
⚠️ **وحدٌّ مُعلَن على ④:** `cluster_bootstrap_ci` و`block_bootstrap_ci` القائمتان **تأخذان ذراعًا واحدة ولا تحسبان فاصلَ فرقٍ إطلاقًا** (`replay10.py:288`،`:313`) ⇒ فاصلُ الفرق **يُبنى في الأداة** بإعادة معاينة الرموز وحسابِ الفرق على العيّنة المعادة؛ **ولا يعيد تخصيص السعة** ⇒ **فاصلٌ متفائلُ الضيق، ويُصرَّح به مع كل رقم**.

### 🪜 سلّم الأحكام — مثبَّتٌ قبل الأرقام

| الحكم | الشرط | الأثر المسموح |
|---|---|---|
| **A · مقبولة** | السبعة **+** `Δ_P` لـ`E1-C-X` موجبٌ وحدُّه السفليّ 95% فوق الصفر | **اقتراحٌ مكتوبٌ للمالك** (وحده) |
| **A′ · مقيَّدة** | السبعة تمرّ لـ`E1` **ولا تمرّ لـ`E1-C`** | يُكتب حرفيًّا: **«الأثر يقوم على إسقاط منع الملاحقة — قرارٌ جذريّ مقفول (D11) ⇒ صفر اقتراح»** |
| **B · تحسّنٌ نسبيّ** | السبعة تمرّ لكن `Δ_P` المجمَّع لـ`E1-C-X` ‏≤0 | **«يخسر أقلّ — وليست إشارةَ ربح»** · صفر اقتراح |
| **لا حكم** | `\|Δ_P\| <` `MDE` **المقيس في هذي التشغيلة**، أو أيُّ شرط عيّنة/صلاحية غير مستوفًى | يُنشَر وصفيًّا · **صفر ادّعاء في أيّ اتجاه** |
| **نفيٌ محدود** | **`TOST` بحدَّي ‏±0.35 R/إشارة يمرّ** | يُكتب: **«توقّع الظرف مكافئٌ لتوقّع الفارز داخل ‏±0.35R»** — **وهذا هو الادّعاء السلبيّ الوحيد المسموح** |
| **D · باطلة** | أيُّ بندٍ من §⑧ | **تُعاد ولا تُفسَّر** — ولا يُقتبَس منها رقمٌ واحد |

🔴🔴 **وممنوعٌ منعًا باتًّا — مهما كانت الأرقام:** كتابةُ **«يُغلَق محور إعادة تعريف المجتمع بالقياس»**، أو أيُّ صياغةٍ من عائلة «فشلت» عند نتيجةٍ صفريّة. السبب مكتوبٌ قبل الأرقام: **‏`MDE` مقياس المحفظة (‏0.23-0.27R) يتجاوز حدَّ الأثر المسجَّل، وقوّة مقياس البِركة عند الحدّ ‏53%** ⇒ الصفرُ هنا «لم نستطع القياس» لا «قِسنا فلم نجد». والمستودع يميّز الاثنين بقاعدةٍ مكتوبة: «فليست المشكلة أننا لم نستطع القياس؛ قِسنا ولم نجد شيئًا» (`country_magnitude_result.md:51`، ومعيارُها المزدوج `:47-49`)، **وسحب «العدميّة القويّة» في `T-FAILS` كان لغياب `TOST` بعينه** (`fails_result.md:4-6`).
⚖️ **و‏±0.35R اختيرت بحسابٍ مُعلَن قبل الأرقام:** `TOST` يشترط `\|Δ\| + 1.645·SE < δ` وبـ`SE≈0.074` يكون δ=0.15 غير قابلٍ للاستيفاء عمليًّا (‏`\|Δ\|<0.029`) بينما δ=0.35 يستوعب `\|Δ\|<0.23` ⇒ **أصغرُ حدٍّ يجعل النفيَ ممكنًا أصلًا**. وهو **حدٌّ خشن** ويُوصَف كذلك.

---

## ⑤ سقف النجاح — مثبَّتٌ سلفًا

**الأقصى عند A: اقتراحٌ مكتوبٌ للمالك + إبقاء صيّاد الظرف الصامت يعمل.** **وممنوعٌ مهما كانت الأرقام** (`PIVOT_V2_PLAN.md:166`): مسُّ `M1-M14` أو أيِّ عتبةٍ في `CONFIG` · رفعُ `LOGIC_VERSION` · مسُّ المُرتِّب أو السعة · إضافةُ قناة رسائل · تغييرُ `envelope_p90.json` · تشغيلُ الصيّاد بتلغرام. وأيُّ خطوةٍ بعد الاقتراح تلزمها موافقةُ المالك وتسجيلٌ مسبقٌ جديد.

---

## ⑥ التنبّؤات — **عتبةٌ واحدة لكلٍّ، تغطّي الطرفين**

**شواهد توصيلٍ لا فرضيات** (تُنزَع من بطاقة التنبّؤات لأنها شبه محتومة — 【مقيسٌ منّي: كثافة `E1` ‏≈4239 إشارة/سنة مقابل 120-191 لـ`E0` ⇒ ‏×22-35】):

| # | الشاهد | ما يجعله **بطلانًا** |
|---|---|---|
| **W0** | `E1` تُنتج إشاراتٍ على رموزٍ من الكاتالوج في السنوات الثلاث | صفرُ إشارة كاتالوج في الثلاث ⇒ **باطلة (D)** لا «سالبة» |
| **W1** | إشارات `E1` ‏≥10× إشارات `E0` كلّ سنة | أقلّ من ‏10× ⇒ الأداة متّهمة ⇒ يُفحَص قبل القراءة |
| **W2** | `rejected_cap` في `E1` ‏≥80% | أقلّ ⇒ الأداة متّهمة |

**تنبّؤاتٌ حقيقية (عتبةٌ واحدة):**

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| **P1** | `mg_pre_stop ≥ 100` المُسلَّم في `E1` **أقلُّ من ‏3×** نظيره في `E0` (البِركة تُسلّم ‏0-3/سنة — `ranker_result.md:41`) | ‏≥3× ⇒ أوّلُ دليلٍ مقيس أن إعادة تعريف المجتمع ترفع **عرضَ** المنفجرين |
| **P2** | تنقلب إشارة `Δ_P` في سنةٍ واحدة على الأقلّ (بصمة `T-SHORT`/`T-CMAG`/`G5`) | ثباتُ الإشارة في الثلاث = **الشرط ①** |
| **P3** | `Δ_P` لـ`E1-C-X` **أصغرُ من** `Δ_P` لـ`E1` (بالإشارة لا بالقيمة المطلقة) | أكبرُ أو مساوٍ |
| **P4** | حارسُ السعر الوهميّ (`M1_phantom_split`) يرفض **عددًا غير صفريّ** في كلّ سنة (تشويهُ التقسيمات يسكن المنطقة التي يفتحها الظرف: `base_range` ‏593% عند `P90` و‏23,685% عند `P100` — `catalog_envelope_result.md:73`) | صفرٌ في الثلاث مع لقطة `splits` محمَّلة ⇒ الحارسُ ميّت أو الادّعاء خاطئ (وكلاهما يُبلَّغ) |
| **P5** | `Δ_P` بمقياس **التوقّع/صفقة** بين `E1` و`E1-X` داخل ‏**±0.05R** (‏9 من 109 مرشّحًا حيًّا أعضاءُ معايرة = **‏8.3%** 【حسبتُها】) | خارج ‏±0.05R |
| **P6** | نسبةُ نجاح `E1` **أدنى** من `E0` في سنتين من ثلاث (نمط `M4-ISO`: منفجرون أكثر ونجاحٌ أقلّ) | ‏≥`E0` في سنتين أو أكثر |
| **P7** | `\|E1 ∩ E0\|` بالزوج (رمز، تاريخ) **أقلُّ من ‏5%** من `E0` (【مقيسٌ منّي: **‏0 من 109** مرشّحًا حيًّا يجتاز عتبات الإنتاج الأحد عشر معًا】) | ‏≥5% |

---

## ⑦ حدود الصدق — وصنفُ الحكم **محسومٌ الآن**

### 🔴 الجملة الحاسمة

> **هذا حكمٌ وصفيّ داخل العيّنة — وليس حكمَ ربحيةٍ قابلًا للتعميم.** النفيُ فيه نافذٌ ضمن `TOST` وحده، **والإيجابُ فرضيةٌ للحصاد الأماميّ لا قرارُ منتج.**

**والأسباب مقاسة:** ① **‏27 ملفَّ تسجيلٍ مسبق** على نفس اللقطة ونفس السنوات 【عددتُها: `ls *_prereg.md` ⇒ 27】 ⇒ العائلة الإحصائية مستنفدة. ② **التلوّث الزمنيّ غير مقيس** (‏ع3) ⇒ **قاعدةٌ مكتوبة الآن: كلُّ سنةٍ يتقاطع معها مِرساةُ رمزٍ واحدٍ فأكثر تُنشَر وصفيًّا وتُخرَج من الحكم المجمَّع؛ وإن تلوّثت الثلاث فالحكم `A` يُستبدَل بـ`A′-أماميّ` (فرضيةٌ للحصاد) ولا يُقترَح شيء.** ③ **‏2026 لا تصلح مدًى محجوزًا** لأن الكاتالوج مادّةُ ‏2025-2026 بنصّ الكود (`catalog_envelope.py:35-36`) — **وهذي الحجّة نفسُها تُثير الشكّ في 2025**، فتُطبَّق عليها قاعدة ② بلا استثناء.

### القيود المرقّمة (تُقرأ مع الأرقام لا بعدها)

1. **الظرف يُقرأ رفعةً لا قاعًا** — بدليل حوافّه: `rsi_min ≤ 54` و`rsi_now ≤ 66.24` (`envelope_p90.json:16+`) مقابل أرضية الإنتاج 32 (`Super_stock.py:145`) وسقف 50 الذي تعليقُه «‏>50 = **فات الارتكاز (تحرّك)**» (`:148`)، ومنطقة فيصل المنصوصة 22-27 · و`base_range ≤ 593%` على نافذة 15 جلسة (`:133`). ⇒ **لا تُسمّى النتيجة يومًا «حكمًا على منهجية فيصل».**
2. **`E1` بلا حاجز ملاحقة** (‏ع2) — ولذلك `E1-C` هي النافذة.
3. **تسرّبُ المواصفة لا يُرفَع بالاستبعاد:** المعايير الأحد عشر واتّجاهاتها (`catalog_envelope.py:82-94`) وحصّةُ `P90` **مُقدَّرةٌ من ثلاثين رمزًا مترابطًا**؛ `E1-X` يرفع تسرّب **الأسماء** لا تسرّب **الأرقام المُقدَّرة**. **والصياغة النافذة: «مستقلٌّ عن الأسماء · غيرُ مستقلٍّ عن العائلة · وغيرُ مستقلٍّ عن السنة إلى أن تُنشَر المراسي».**
4. **حافّةٌ قد يُثبّتها رمزٌ واحد:** `envelope_edge` يُسقط `int(n×10%)` **جلسةً** والصفوفُ جلسات (‏≤20/رمز — `:142-144` مع `:256-268`) ⇒ عند `n≈600` المُسقَط ‏≈3 رموز. 【ومقيسٌ منّي أن الحوافّ هشّة فعلًا: `rsi_min` ‏−10% ⇒ المجتمع الحيّ ‏109 ⟶ **‏96 (‏−12%)** · وأرضية `drop_pct` ‏+10% ⇒ ‏109 ⟶ **‏72 (‏−34%)**】.
5. **‏`P90` معلمةٌ مُنتقاة:** التصميم يثبّت «**الأساسيّ `P100`** … و`P90` يُنشَر معه» (`catalog_envelope_design.md:76`) ثم ذيلٌ **بعد** التشغيل يجعل «`P90` هو المُرشَّح العمليّ» (`:149`) — **مبرِّرُه مُعلَنٌ قبلها** (`:74`) لكن الاختيارَ وقع بعد رؤية كلفة `P100` ⇒ تُحسَب معلمةً مُنتقاة، **ولذلك `P100` ذراعٌ مُعلَنة (الشرط ⑤)**.
6. **‏`M13`/`M14` غير ممثَّلتين تاريخيًّا** (البوّابتان في `scan_market` — `Super_stock.py:8932`/`:8998` — لا في `backtest_symbol`) ⇒ **متماثلٌ بين الذراعين**، لكن `E1` تقيس مجتمعًا **أوسعَ من الصيّاد الحيّ بمقدارٍ مقيس: ‏11 من 120 (‏9.2%) أخرجتهم `M14` حيًّا، و`M13` غير مقيسةٍ حيًّا أصلًا** (`m13_measured: false` في الترويسة · والإفصاح `envelope_hunter.py:190-193`) ⇒ **الحكم لا ينتقل ‏1:1 إلى الأداة الحيّة.**
7. **ثلاثةُ أُسسٍ لطول التاريخ:** المعايرة تقبل ‏≥60 شمعة (`catalog_envelope.py:262`) · والصيّاد الحيّ ‏≥60 (`envelope_hunter.py:152`) · ومشي `E1` يبدأ من **‏120** (`Super_stock.py:14174` مع `:529`). وعلى إطار 60 يرى `hi52 = tail(252)` ستّين شمعةً فقط (`:2540`) ⇒ `drop_pct` مبخوس ⇒ **أرضيتُه 79.17 قد تكون أثرَ طولِ تاريخٍ لا حدًّا فيصليًّا**. ⇒ **`E1` ليست الصيّادَ الحيّ للرموز قصيرة التاريخ.**
8. **الحوافُّ مقيسةٌ على سلّمٍ مُرخى:** `MIN_RR_T1=-1e9` (`catalog_envelope.py:75`) ⇒ نقصُ RR لا يُسجَّل ⇒ `n_soft ≤ 6` على مقياسٍ ينقص واحدًا 【مقيسٌ منّي: 6 مقابل 7】.
9. **انحياز البقاء:** الكون قائمةُ ناسداك اليوم ويستبعد المشطوبة — والكودُ يفصح بها بنفسه (`Super_stock.py:15698`).
10. **تشويهُ التقسيمات حاضرٌ في الطرفين ومركَّزٌ في منطقة الظرف** (‏P4) · **و`dollar_vol` بمقياسٍ معدَّل بلا أيّ حارس point-in-time** (`:2607`) بينما `_pit_raw_price` له **نقطةُ نداءٍ واحدة في الملفّ كلِّه** (تعريف `:1313` · استعمال `:14197`) ⇒ **محورٌ واحد من أحد عشر مصحَّح**. ⚖️ ومعايير النِّسَب (`drop_pct`/`base_range`/`best_spike`) **غير ملوَّثة** (التعديل ضربيّ منتظم فتتلاشى النسبة).
11. **بلا افتر:** الشمعة اليومية لا تشمله ⇒ أرقام الانفجار **أرضياتٌ لا أسقف** (سابقةُ `NUWE`).
12. **‏`h4_confirm = 0` دائمًا** تاريخيًّا وحيًّا (`replay10.py:59-60`) فلا يُدَّعى تمثيلُه.
13. **الظرف ليس «تخفيفًا» بل إعادةَ تشكيل:** أربعةٌ من حوافّه **أضيقُ** من حدّنا (السعر ‏1.62>1.50 · الانفجار السابق ‏100>60 · العائد/المخاطرة ‏1.72>1.00 · أرضية الهبوط ‏79.17>40 — `envelope_p90.json:16+` مقابل `Super_stock.py:122`،`:127`،`:130`،`:138`؛ ومقفولٌ في `test_bot.py:13589-13596`) ⇒ **المجموعتان متقاطعتان لا متداخلتان** 【بل **منفصلتان**: 0 من 109】 ⇒ **صياغةُ «الدلتا فوق الأساس» لا تصلح**، والتقريرُ يكتب حرفيًّا: **«هذا استبدالُ استراتيجيةٍ لا تخفيفُ بوّابة»** (درس `G5`).
14. **الحوافُّ المُعتمَدة هي `envelope_p90.json` وحدَه ببصمة `575dc1855c51`** — لا جدولُ `catalog_envelope_result.md:73-83` (فيه ‏1.64 و‏124,792 من تشغيلةٍ أسبق بـ31 رمزًا) ولا قائمةُ «الساقطون خارج العيّنة (‏11)» (`:64-65`) التي **تحوي `HTZ` المُستبعَد** ⇒ **مصدرُها التشغيلةُ ذات 31 رمزًا لا ذات 30** = جدولٌ مختلط المنشأ.
15. **الفاصل العنقوديّ متفائلُ الضيق** (لا يعيد تخصيص السعة) — §④.
16. **ولا بوّابةَ صلبةً مخفيّة خارج `RELAX_ALL`:** `GAP_REQUIRED=False` (`Super_stock.py:368`) ⇒ خاملة · و`RSI_GATE_REQUIRED=True` (`:151`) لكن حدَّيه مُرخَيان ⇒ **الإرخاءُ غيرُ المُغطَّى واحدٌ فقط: منع الملاحقة** 【مقيسٌ آليًّا】.

---

## ⑧ قاعدة الإبطال — ما يجعل التشغيلة **باطلة (D)** لا «سالبة»

| # | البطلان | الكشف |
|---|---|---|
| **V1** | **علمٌ خامل**: صفر صفقةٍ تحمل `exit_date` ⇒ `BT_REPLAY10` لم يُفعَّل (يُضبَط **قبل الاستيراد** لأن الجدول يُنفَّذ وقت التحميل — `Super_stock.py:653`) | خروج **2** (نمط `replay10_arms.py:83-86`) |
| **V2** | **تفرّقُ المحرّكين**: `exit_kind ≠ outcome` في أيّ صفقة | خروج **3** (نمط `:89-94`) |
| **V3** | **بصمةُ الظرف/اللقطة**: بصمة الحواف ≠ ~~`575dc1855c51`~~ **`bbc3cfde3e92`** · أو `pct≠90` · أو `n_symbols≠` ~~30~~ **28** · أو `excluded ≠ {HTZ, YYAI, XHLD}` · أو **‏`sha256` المانفست يختلف بين السنوات الثلاث** | خروج **5** — 🔒 **ومنفَّذٌ الآن في الكود** (`envelope_bt.check_v3` · `return 5`)، وكان **موصوفًا وغيرَ منفَّذ** ⇒ انظر **تعديل ⑦** |
| **V4** | **مقامان مختلفان**: مقام `M-D` غير موحَّد بين الأذرع الستّ | خروج **4** — 🔒 **وعُولج بنيويًّا** بـ`shared_axis` (جسرٌ واحد على اتّحاد الأذرع ⇒ `idx`/`outcome_of` مشتركان بالبناء) فيستحيل المقامُ المتفرّق أصلًا · انظر **تعديل ⑦** |
| **V5** | **تسرّبُ `CONFIG`**: بصمةُ `RELAX_ALL` قبل/بعد لا تتطابق (`envelope_scan.py:94-98`) · أو `BACKTEST_STEP` لم يُستعَد · أو `_REJECT_STATS`/`_REJECT_REASONS`/`_REJECT_SOFT_FAILS` (`Super_stock.py:2410`،`:2412`،`:2419`) لم تُستعَد بعد المشي | خروج **7** |
| **V6** | **ثوابتٌ متبدّلة**: `BACKTEST_FORWARD_DAYS≠40` · `BACKTEST_STEP≠5` · `MIN_BARS≠120` · `RP.CAPACITY≠10` · أو أيُّ علمٍ من `BT_SPLIT_*`/`BT_SWEEP_*`/`BT_LIBERATION`/`BT_CANDLE`/`BT_CORE5`/`BT_PORTFOLIO`/`BT_FEATURES`/`BT_SHORT`/`BT_STOP_PCT` مرفوع | خروج **8** |
| **V7** | **تغطيةٌ ناقصة**: تخطّي أكثر من **‏10%** من رموز اللقطة بلا سببٍ مسمّى · أو أقدمُ تاريخٍ باللقطة بعد بدء النافذة (السابقة المكتوبة `Super_stock.py:15784-15787`) | خروج **6** |
| **V8** | **قفلُ التكافؤ ساقط**: بناءُ الصفقة في `E1` لا يطابق `backtest_symbol` حقلًا-بحقل · أو مجموعةُ `E0` لا تطابق `run_backtest()` على نفس السنة واللقطة | **لا تُشغَّل السنة أصلًا** |
| **V9** | **صفر إشارة كاتالوج** في الثلاث (`W0`) | بطلانٌ لا نفي |
| **V10** | **مِجَسّاتُ ما قبل الأرقام غائبة**: جدولُ المِرساة · مِجَسّ الإزاحة · مِجَسّ تعديل الحجم · `sd`/`MDE` المقيسان — أيُّ واحدٍ منها غير مطبوع **قبل** أوّل رقمِ ذراع | تُعاد التشغيلة |
| **V11** | **مِجَسّ الجدوى**: أيُّ تشغيلةٍ بـ`ENVBT_SYMBOL_CAP>0` تُوسَم **«مِجَسّ — لا يُقرأ حكمًا»** ولا يُقتبَس منها رقم | وسمٌ في الرأس والذيل |
| **V12** | **تلوّثُ الأذرع بالكاش**: اتّحادُ ذراعين مبنيٌّ من كاشٍ مشترك ≠ تشغيلُهما منفردًا (قفل L8) | خروج **9** |

**وقاعدةٌ فوق ذلك كلِّه:** **يُطبَع سطرُ «الأداةُ اشتغلت» بعدّاداته ومِجَسّاته قبل أيّ رقمٍ يُقرأ**، والحكمُ يُطبَع **آخِرَ** المُخرَج.

---

## ⑨ ثوابتُ التشغيل — مجمَّدةٌ الآن

| | |
|---|---|
| **اللقطة** | `frozen-dataset` لتشغيلة **`30598885456`** (تجميدٌ من **2022-01-01** — `gates_result.md:4` · as-of **2026-07-31** — `envelope_p90.json:4`) ⇒ 2023 لها لوكباك 2022 يكفي `MIN_BARS`. ⚠️ **والاحتفاظ 90 يومًا** (`.github/workflows/freeze.yml:44`) ⇒ **الانتهاء ‏≈2026-10-28/29** 【حسابٌ منّي من as-of + 90 يومًا؛ **التاريخ الفعليّ للأرشيف غير مُتحقَّق**】 ⇒ التشغيل قبله، أو إعادةُ تجميدٍ **بنفس المدى** وإعلانُها **مع إعادة الأساس `E0` في نفس التشغيلة** |
| **الحواف** | `envelope_p90.json` · بصمة ~~`575dc1855c51`~~ **`bbc3cfde3e92`** · `pct=90` · `n_symbols=` ~~30~~ **28** · `excluded=` ~~{HTZ}~~ **{HTZ, YYAI, XHLD}** · `denominator_sessions=20` · **`source="مُخرَجٌ آليّ"`** · **`anchor_last_measured` = 28 تاريخًا** (‏تعديل ⑦) |
| **الحواف بأرقامها** (‏تعديل ⑦ — والقديمة مشطوبة) | السعر ‏≥ ~~1.62~~ **1.71** · الهبوط ∈ ~~[79.17 · 99.67]~~ **[76.48 · 99.68]** · الانفجار السابق ‏≥100.0 · عرضُ القاعدة ‏≤593.07 · السيولة ‏≥ ~~124,665~~ **132,010** · قاع `RSI` ‏≤54.0 · `RSI` الآن ‏≤66.24 · النواقص ‏≤6 · الجاهزية ‏≥15 · النقاط ‏≥45 · العائد/المخاطرة ‏≥ ~~1.72~~ **1.716** |
| **السنوات** | **2023 · 2024 · 2025** — ثلاث تشغيلات، نافذةُ كلٍّ `(YYYY-01-01, YYYY-12-31)` |
| **الأعلام** | `SCREENER_MODE=BACKTEST` · `BT_REPLAY10=1` · `BT_POTENTIAL=1` — **داخل السكربت قبل الاستيراد** · وكلُّ ما عداها **مطفأ** |
| **السعة · المُرتِّبات · العيّنات** | **10** · `rank_actual` حاكمًا لـ`M-D` · `2000` بذرةً للعدم · `10000` عيّنة لكل `bootstrap` |
| **الحدود المسجَّلة** | `EFFECT_R = 0.15 R/إشارة` · `Z_MIN = 1.96` (أحاديّ · بونفيروني على 2) · `TOST_δ = 0.35 R/إشارة` · العيّنة `≥30/سنة` و`≥150` مجمَّعًا · التركيز `≤20%` |

---

## ⑩ ما **لا** تجيبه هذي التجربة — يُكتب قبلها لا بعدها

1. **هل الظرف يربح حيًّا؟** لا — أعمى عن الافتر وعن المضارب وعن المشطوبة، ويقيس مجتمعًا أوسعَ ‏9.2% من الحيّ (‏M14) ومختلفًا عنه في عتبة طول التاريخ (‏60 مقابل 120).
2. **هل المُرتِّب يصلح؟** لا — قِيس «غير حاسم» ولا يُمَسّ هنا؛ أثرُه يُعزَل بقراءة المقياسين لا بإصلاحه.
3. **هل عتبةٌ بعينها من `M1-M14` خاطئة؟** لا — هذي تُقارن **مجتمعَين كاملَين**؛ وترتيبُ البوّابات مفردةً محورٌ آخر (`M4-ISO`/`M4-LADDER`).
4. **هل `M13`/`M14` تنفعان أو تضرّان؟** لا — غيرُ ممثَّلتين تاريخيًّا بالبناء.
5. **هل «‏≈105 مطابق/يوم» يُسلَّم؟** لا — السعة 10، و`M-P` يقيس **البِركة** لا المُسلَّم، وأيُّ حكمٍ على المُسلَّم يبقى ثانويًّا بـ`MDE` مُعلَن.
6. **هل الظرف مُقدَّرٌ من قاعٍ أم من رفعة؟** لا تجيبه التجربة — يجيبه **مِجَسّ الإزاحة** الذي يسبقها (‏§⑧-V10).

---

## ⑪ حصيلة الطعون — ما صمد وما سقط **بالدليل**

### أ. صمد فعُولج أو صار قيدًا صريحًا (‏20)

| الاعتراض | الحالة | العلاج في هذا التسجيل |
|---|---|---|
| نافذة المعايرة تلامس الاشتعال · ومنعُ الملاحقة مُرخًى بلا معيار | **صمد وتأكّد آليًّا** | ‏ع2 · ‏ع3 · ذراع `E1-C` نافذةً · مِجَسّ الإزاحة `K∈{0,5,10,20}` إلزاميًّا |
| المعايرة داخل سنوات الاختبار · والمراسي غير منشورة | **صمد** | جدولُ المِرساة إلزاميّ قبل أيّ رقم + قاعدةُ إخراج السنة الملوَّثة + `A′-أماميّ` |
| `d` غير مثبَّت و`L1` أعمى عنه | **صمد** | قفلٌ **تفريقيّ** على `d` (المواصفة ‏L3) + مِرساةُ `eligible_at` الحاكمة |
| هويّةُ اللقطة بنصٍّ نملكه · والمُحمِّل يحذّر ولا يفشل | **صمد** | قراءةُ المانفست و`rc=5` + تطابقُ `sha256` عبر السنوات الثلاث |
| الحارس الوهميّ يحرس محورًا من أحد عشر | **صمد** | مِجَسّ تعديل الحجم إلزاميّ + شريحةٌ خالية من التقسيم حساسيةً + قيد ⑦-10 |
| ② خُفِّض 0.15→0.05 · والمقام يعتمد على الذراع | **صمد** | **أُعيد 0.15R** + مقامٌ مستقلٌّ عن الذراع + **منعُ الاستشهاد بأرقام `replay10_result`** |
| ‏60 مقابل 120 شمعة | **صمد وأوسع** (ثلاثة أُسس) | قيد ⑦-7 صريحًا |
| **الحواف منقولةٌ بالعين** | **صمد — وأثبتتُه بنفسي** | ‏ع1 · **حاجب P0** (‏§⑬-1) + بيانُ القفل الفارغ |
| رمزٌ واحد يقدر أن يُثبّت حافّة | **صمد** | نشرُ ثبات الحوافّ `LOO` (محسوبةٌ سلفًا `catalog_envelope.py:405`) + الرمزُ الأكثر تحريكًا |
| أعضاءٌ من «الفئة السالبة» في المعايرة | **صمد** | قرار مالك (‏§⑬-2) — ولا أحسمه |
| نطاقُ `drop_pct` بصمةُ تقسيم | **صمد** | ‏P4 + شريحة بلا تقسيم |
| حافّةُ الجاهزية 15 لا تُسلَّم بسعة 10 | **صمد — ومقيسٌ منّي** | استدلالٌ بطبقتين: `M-P` أساسيًّا و`M-D` ثانويًّا |
| المجموعتان منفصلتان و`V3` القديم ميّت | **صمد — 0/109** | `V3` صار **بصمة ظرف**، والانفصالُ صار **عدّاد `P7`** وشرطَ صياغةٍ («استبدالٌ لا دلتا») |
| بوّابة ④ غير قابلة للتنفيذ بالدالّتين القائمتين | **صمد** | فاصلُ فرقٍ يُبنى في الأداة + تصريحٌ بأنه متفائلُ الضيق |
| `P90` رُقِّيت بعد رؤية الكلفة | **صمد جزئيًّا** (المبرِّر مُعلَنٌ قبلها) | ذراع `P100` (الشرط ⑤) + وسمُها معلمةً مُنتقاة |
| القوّة و`MDE` | **صمد — وأعدتُ حسابه بنفسي** | ‏§③-ج + **منعُ «فشلت»/«يُغلَق»** + `TOST` بحدٍّ قابلٍ للاستيفاء |
| `exploded` محرَّمٌ ومستعمَل | **صمد** | حُذف · و`P1` على `mg_pre_stop ≥ 100` |
| نطاقاتٌ ميّتة في التنبّؤات · و`P1`/`P2` شبه محتومان · و`P5` بالقيمة المطلقة | **صمد** | عتبةٌ واحدة لكلٍّ · و`W1`/`W2` شواهدُ توصيل · و`P3`/`P5` بالإشارة |
| `z` ناقصُ التعريف بأربع جهات · والعقدان العشوائيّان | **صمد** | تعريفٌ رباعيّ صريح + `randomization` عائلةً ثانية |
| بابُ تبديل مجموعة الاستبعاد · واستبعادٌ غير متماثل · ومُقارِنان للأساس | **صمد** | تجميدٌ عند 35 وحذفُ البند · وعدّادُ الفقد لكل ذراع مع وسم «مُحمَّل» · وأساسٌ واحدٌ في نفس التشغيلة |

### ب. سقط بالدليل — ويُذكر لا يُتجاهَل (‏7)

| الاعتراض/الشبهة | لماذا سقط |
|---|---|
| **«حيلةُ القصّ تسرّب»** | 【مقيسٌ منّي】: `backtest_symbol(df.iloc[:i+fwd+1], step=1, window=(d,d))` أنتجت **صفقةً واحدة مطابقةً حقلًا-بحقل** للصفقة من الإطار الكامل بخطوة 5 (**فرقُ الحقول `NONE` ومجموعةُ المفاتيح متطابقة**)، **وبلا `+1` صفر صفقة**. وسندُه: `while i < n-fwd` (`Super_stock.py:14175`) و`fut = df.iloc[i:i+fwd]` (`:14206`) لا تقرأ الشمعة الزائدة، والقرارُ على `df.iloc[:i]` (`:14185`) |
| **«محرّكا الحسم متفائلان»** | الوقفُ يُفحَص **أوّلًا** كل شمعة و`t1_from = filled+1` في الثلاثة: `_resolve_arm` (`:13585-13594`) · `_arm_a_exit_bar` (`:13628-13633`) · `_max_gain_before_stop` (`:13652-13657`) |
| **«`_pit_split_factor` يسرّب»** | يستعمل التقسيمات **بعد** الشمعة حصرًا (`dd > a` — `:1306`) **لإلغاء** تعديلٍ لا لقرار، وأثرُه دائمًا في اتجاه **الرفض** فلا يصنع ربحًا |
| **«`replay10` لم تُصلح `P0-01`»** | `outcome_of(c)` يُنادى **بعد** حجز الخانة (`replay10.py:160` مقابل `:156`) |
| **«`score=45.00`/`best_spike=100` دليلُ إرخاءٍ لم ينفَّذ»** | `P100` يعطي ‏10 و‏79.5 (`catalog_envelope_result.md:81`،`:83`) وهما **تحت** `SCORE_MIN=45` (`Super_stock.py:212`) و`PRIOR_SPIKE_FLOOR=60` (`:130`) ⇒ الإرخاءُ نفَّذ والتساوي **مصادفةٌ مئينيّة** |
| **«`eligible_at` تسرّبٌ يحيز المقارنة»** | مشتركٌ بين الذراعين بنفس الجسر ⇒ **لا يحيز الفرق**؛ سقط كـ«تسرّبٍ تفريقيّ» وبقي **قيدًا** — وقد **أُصلح** بجعل `eligible_at` المِرساةَ الحاكمة (‏§③-و) |
| **«الإرخاء قد يبدّل الخطّة فتنهار المقارنة»** *(شبهةٌ لم يُثِرها أحد وفحصتُها لأنها تُبطل كلَّ شيء)* | 【مقيسٌ منّي】: الخطّة **متطابقة** تحت قيمٍ متطرّفة مختلفة لكلّ مفاتيح الإرخاء الثلاثة عشر ⇒ الإرخاء يبدّل **العضوية** وحدها. **والاستثناء الوحيد `n_soft`** وقد صار قيدًا مُعلَنًا (‏§①) |

---

## ⑫ انحرافاتٌ مُعلَنة عن `PIVOT_V2_PLAN.md §م-و` — تُكتب الآن لا بعدًا

| # | نصّ الخطّة | الانحراف | السبب |
|---|---|---|---|
| **ن1** | «المحفظة بـ`replay10` بسعة 10» (`:162`) ⇒ المقياس محفظيّ | **المقياس الأساسيّ صار التوقّع/إشارة على البِركة**، والمحفظيّ ثانويّ | `MDE` المحفظيّ (‏0.23-0.27R) **يتجاوز** حدَّ الأثر المسجَّل 0.15R (‏§③-ج) ⇒ لا يمكن أن يَبين |
| **ن2** | «كونٌ يستبعد **الواحد والثلاثين**» (`:163`) | الاستبعاد بـ**الخمسة والثلاثين** | مجموعةٌ فائقة ⇒ **أشدُّ تحفّظًا**، وهويّةُ الثلاثين غير مدوَّنة |
| **ن3** | «ذراعان إلزاميًّا» (`:163`) | **ستُّ أذرع** ‏+ ذراع `P100` | ذراعُ منع الملاحقة (‏D11) وذراعُ المئينيّة كلتاهما **شبه مجّانيتين** ومطلوبتان بالطعون |
| **ن4** | البوّابة الخماسية (`:165`) | **سبعةٌ** (‏+ امتناع/تسليم · + تركيز ‏≤20%) | حارسُ «فخّ الامتناع» وسابقةُ `event_exec_prereg.md:115` |
| **ن5** | لم تنصّ على حكمٍ سلبيّ | **حُظِرت «فشلت» و«يُغلَق المحور»** وأُضيف `TOST` بحدّ 0.35R | القوّة المحسوبة قبل الأرقام + قاعدة `country_magnitude_result.md:51` + سابقة `fails_result.md:4` |
| **ن6** | «الوسم الصالح وحده `mg_pre_stop`» (`:164`) | **مطبَّقٌ حرفيًّا** — و`exploded` حُذف من الأداة القائمة أيضًا | الخطّة تحرّمه و`replay10_arms.py:71` يقرأه |

---

## ⑬ ما يحتاج **قرار المالك** — قبل الدفع

1. 🔴 **الحواف (حاجب):** إعادةُ إصدارها آليًّا بتشغيلةٍ واحدة (‏`catalog_envelope.py` على نفس اللقطة، ‏≈حرفيًّا صفر كود) واشتراطُ `source="مُخرَجٌ آليّ"` + بصمة `575dc1855c51`؛ **أو** المضيُّ بها بوسمٍ صريح «الحوافُّ منقولةٌ بالعين ⇒ التجربة تختبر **قرارَ الصيّاد المنشور** لا **ظرفَ الأداة المقيس**».
2. **عضويةُ الفئة السالبة في المعايرة** (`YYAI`/`XHLD`/`GWAV` — `catalog_envelope.py:44-47` مقابل وسم `faisal_negative` في `ctb_harvest.py:15`): استبعادٌ بنفس منطق `HTZ` (يستلزم إعادة معايرة) أم تصريحٌ بأن الظرف مُعايَرٌ على «أيّ ‏+100% في رمزٍ ذكره فيصل»؟
3. **منعُ الملاحقة (D11):** أقبل أن تبقى `E1` ذراعًا تشخيصية بلا الحاجز، مع كون `E1-C` هي **النافذة**؟
4. **إزاحةُ نافذة المعايرة:** أقبل `K=0` الحاليّ مع قيدٍ مُعلَن، أم نعيد المعايرة بمرساةٍ عند **قاع القاعدة** ونُصدر ظرفًا ثانيًا؟
5. **ظرفٌ سببيّ سنويًّا** (لكلّ سنة حوافُّ من مراسي ما قبل ‏1 يناير): تشغيلةُ معايرةٍ إضافية لكلّ سنة — أنفعلها أم نكتفي بقاعدة إخراج السنة الملوَّثة؟
6. **الميزانية:** ثلاث تشغيلات ‏× **‏≈172 دقيقة/سنة** 【حسابٌ منّي من قياسي: 59.2 مث/نداء ‏× 3383 رمزًا ‏× 50 زيارة = 168 دقيقة قياسًا ‏+ ‏≈4 دقائق بناءَ صفقات】 بمهلة 330 دقيقة، أو تجزئةٌ حتمية.
7. **صنفُ الحكم:** أقبل أن يكون الأقصى **اقتراحًا** و`A` **فرضيةً للحصاد الأماميّ** لا قرارَ منتج؟
8. **الفجوة عن الحيّ:** أقبل أن `E1` أوسعُ من الصيّاد الحيّ بـ‏9.2% (‏M14) ومختلفةٌ عنه في عتبة طول التاريخ، فالحكم لا ينتقل ‏1:1؟

---

## 🔴 تعديل ⑦ (‏2026-08-05، **بعد** دفع التسجيل و**قبل** أيّ رقمِ ذراع) — خطأُ إجراءٍ منّي، ومعه عيبان في الكود

> ### أ) ما وقع بالضبط
>
> التسجيل جمَّد في §⑨ وV3 ظرفًا ببصمة **`575dc1855c51`** و**30 رمزًا** و
> `excluded={HTZ}`، **ونصَّ في §⑫-1 على العلاج المطلوب حرفيًّا**: «إعادةُ إصدارها
> آليًّا … واشتراطُ `source="مُخرَجٌ آليّ"` **‏+ بصمة `575dc1855c51`**».
>
> **وأنا فعلتُ الأوّل وخالفتُ الثاني في الحركة نفسها:** أعدتُ الإصدار آليًّا
> (‏تشغيلة `31028258442`) **وفي الوقت ذاته** غيّرتُ **مجتمعَ المعايرة** (استبعاد
> `YYAI` و`XHLD` بنفس منطق `HTZ`) ⇒ صار الظرفُ **`bbc3cfde3e92`** بـ**28 رمزًا**.
> **ولم أُعدّل التسجيل معه** ⇒ أطلقتُ تشغيلةً (‏`31031808776`) على ظرفٍ **لا
> يُصرِّح التسجيلُ به**.
>
> ### ب) ولماذا لم يمنعني شيء — عيبٌ ثانٍ مستقلّ
>
> **`V3` كان موصوفًا في التسجيل وغيرَ منفَّذٍ في الكود إطلاقًا** (‏لا `return 5` في
> `envelope_bt.py`): الأداة كانت **تطبع** البصمة وعددَ الرموز والمُستبعَدين
> **وتمضي**. ⇒ الحارسُ الذي كُتب لهذي الحالة بعينها **لم يكن موجودًا حيث يلزم**.
> 🔒 **أُصلح:** `check_v3` + `return 5`، وثوابتُه `V3_FINGERPRINT`/`V3_N_SYMBOLS`/
> `V3_EXCLUDED` **مربوطةٌ بهذا التعديل** فأيُّ تبديلٍ لاحقٍ للظرف يُوقِف التشغيل
> حتى يُعدَّل التسجيل معه.
>
> ### ج) ولا هو تدويرٌ للنتائج — والبرهان في السجلّ
>
> ⚖️ **قرارُ الاستبعاد سبق أيَّ رقم:** دُفع في `7e815b1` (‏17:03) والحوافُّ في
> `78c5944` (‏17:47)، **وأوّلُ وآخرُ تشغيلةٍ لـ`T-ENVELOPE` هي `31031808776`
> (‏17:49) وقد أُلغيت عند الخطوة 7 قبل طَبْعِ أيّ رقمِ ذراع** (‏`Run T-ENVELOPE`
> = `cancelled`) ⇒ **لا وجودَ لأيّ رقمِ ربحيةٍ في المشروع حتى لحظة كتابة هذا
> التعديل**، فالتغييرُ لا يمكن أن يكون مُعايَرًا على نتيجة.
> 📌 **ومبرّرُ الاستبعاد مكتوبٌ من قبل** (تعديل ⑥): `YYAI` هي «الفئة السالبة
> الحقيقية» بنصّ المستودع (فيصل «علميا لا» ثم نزل ‏−68%) و`XHLD` «سهم خبر انضخّ،
> ليس ارتكازًا» ⇒ معايرةُ الظرف عليهما تُوسّعه نحو فئةٍ **يرفضها فيصل نفسه**، وهو
> عينُ منطق `HTZ` المقبول سلفًا. **و`GWAV` لم يُستبعَد** (لغتُه مختلطة ⇒ لا استبعادَ
> بشكّ).
>
> ### د) والأرقامُ المرجعية تُحدَّث معه (‏28 رمزًا · تشغيلة `31028258442`)
>
> | | التقاط `LOO` | التمييز | الإثراء | مطابق/يوم |
> |---|---|---|---|---|
> | **`P90`** | ~~66.7% (20/30)~~ **‏67.9% (19/28)** | ~~7.2%~~ **‏7.6%** | ~~×9.20~~ **‏×8.95** | ~~≈105~~ **‏≈107** |
>
> ⇒ **حدُّ القبول «‏≥3× يمضي» (المُعلَن قبل الرقم) ما زال مستوفًى بفارقٍ ثلاثة أضعاف.**
>
> ### هـ) وعيبان آخران في الكود كُشفا بالطعن الخصوميّ وأُصلحا هنا
>
> **‏1) `V4` (وحدةُ المقام) كان موصوفًا وغيرَ منفَّذ — والانتهاكُ بنيويّ:**
> `candidates_from_trades` تبني فهرسَ الجلسات **من الصفقات المُمرَّرة إليها وحدها**
> (`replay10.py:249`) و`held` يُقاس بخطوات ذلك الفهرس (`:260`) ⇒ نداؤها **لكل ذراعٍ
> على حدة** يجعل الذراعَ الكثيفة تحجز الخانةَ مدّةً أطول للصفقة نفسها ⇒ **المزاحمةُ
> على السعة تُقاس بشدّةٍ مختلفة لسببٍ هو كثافةُ الإشارات لا قاعدةُ التداول**. 🔒
> **أُصلح بنيويًّا** بـ`shared_axis`: يُبنى الجسرُ **مرّةً واحدة** على اتّحاد كلّ
> الأذرع ثم تُرشَّح المرشّحات ⇒ `idx` و`outcome_of` مشتركان **بالبناء** فيستحيل
> المقامُ المتفرّق. **وبصفر منطقٍ مستنسَخ**، والترتيبُ داخل الذراع **غير متلوّث**
> (الجسرُ يرتّب بـ(التاريخ، الرمز) والترشيحُ يحفظ الترتيبَ النسبيّ).
> **⚠️ ويبقى حدُّ الصدق المسجَّل في §③-ج قائمًا:** `MDE` مقياسِ المحفظة
> (‏0.23-0.27R) **يتجاوز** حدَّ الأثر 0.15R ⇒ `M-D` **يُنشَر ولا يحكم**.
>
> **‏2) المِرساةُ الحاكمة `eligible_at` لم تكن مقروءةً إطلاقًا:** §③-و ينصّ أنها
> الحاكمة و§⑫ يقول «**أُصلح**» — و`envelope_bt.py` فيه **صفر ذكرٍ** للحقل، والجسرُ
> يُرسي على `date` (`replay10.py:252`) ⇒ الخانةُ تُحجَز **جلسةً قبل** أن تكون الخطّة
> قابلة للتنفيذ = ادّعاءٌ لا يفي به الكود (صنفُ الانسحابات المدوَّنة في `CLAUDE.md`).
> 🔒 **أُصلح** بـ`anchor_eligible`: يُبدَّل `date` بـ`eligible_at` في **نسخةٍ** ثم
> تُمرَّر لجسر `replay10` نفسِه ⇒ `session` و`held` مرسَيان على أوّل جلسةٍ قابلة
> للتنفيذ **بمحرّكٍ واحد بلا منطقٍ موازٍ**، والأصلُ محفوظٌ في `signal_date`،
> **وصفقةٌ بلا الحقل تُسقَط ويُعلَن عددُها** لا تُمرَّر بمِرساةٍ صامتة.
>
> **‏3) `rejected_cap` كان يُقرأ بمفتاحٍ لا وجود له** (`rejected_capacity` بينما
> `replay10:172` يُرجع `rejected_cap`) ⇒ **عدّادُ القصّ بالسعة `None` دائمًا** وهو
> رقمٌ رئيسيّ في الأساس المسجَّل (§①: «السعة تقصّ 27% · 30% · 48%»). أُصلح.
>
> ### و) 🧭 والدرسُ المُعمَّم
>
> **حارسُ صلاحيةٍ مكتوبٌ في التسجيل وغيرُ منفَّذٍ في الكود ليس حارسًا — هو نيّة.**
> ثلاثةٌ من حرّاس هذا التسجيل (`V3` · `V4` · مِرساة `eligible_at`) كانت كذلك،
> **وأوّلُ من انتهك `V3` هو كاتبُه**. ⇒ **قاعدةٌ لاحقة: كلُّ صفٍّ في جدول البطلان
> يلزمه سطرُ كودٍ يُوقِف، وقفلٌ في السويّة يُثبت أنه يُوقِف فعلًا.**

---

## 🔴🔴 تعديل ⑧ (‏2026-08-05) — **عيبٌ `P0` في المعايرة نفسها: الظرفُ كان مُقاسًا داخل الانفجار**

> ### أ) العيب
>
> `walk_symbol` كانت تُرسي على `explosion_index` = **أحدثُ** جلسةٍ تبلغ ‏+100% فوق
> أدنى قاعٍ في العشرين السابقة. وفي صعودٍ ممتدّ **تستوفي ذلك جلساتٌ كثيرة** فتقع
> المِرساةُ **عميقًا داخل الصعود** ⇒ «العشرون جلسةً **قبل** الانفجار» تصير عشرين
> جلسةً **من** الانفجار.
>
> 🧪 **مقيسٌ لا مُستنتَج** (عيّنةٌ صناعية: قاعدةٌ هادئة 40ج عند 1.00 ثم صعودٌ 30ج
> إلى 5.00): `pick="last"` ⇒ نافذةُ القياس ‏[51…70] = **‏20 من 20 داخل الانفجار**،
> والسعرُ فيها **‏2.60→5.00** بدل القاعِ 1.00. و`pick="first"` ⇒ **‏6 من 20** داخله
> ⇒ **الطرفان معطوبان، والعلاجُ تعريفُ البدء لا تبديلُ الطرف.**
>
> ### ب) ولماذا هو أخطرُ من كلّ ما سبقه
>
> عيوبُ تعديل ⑦ كانت في **أداة الحكم** (`envelope_bt`)، وهذا في **المعايرة** ⇒
> يُبطل **الظرفَ نفسَه** الذي تختبره التجربة كلُّها، ومعه أرقامُ الالتقاط والتمييز
> والإثراء المنشورة (‏×5.96 · ×9.20 · ×8.95) وحكمَ «‏🟢 يمضي».
>
> 🔑 **وهو يفسّر «المفاجآت» التي قرأتُها اكتشافًا:** «أسهمُه لم تكن متشبّعة»
> (قاع `RSI` 54) · `RSI` الآن ‏≤66 · الجاهزية ‏≥15 · أرضيةُ الهبوط 76-79% —
> **قراءاتُ سهمٍ وسطَ حركته لا عند قاعه.** ⇒ ووصفي «إعادةُ تشكيلٍ لا تخفيف» كان
> **أثرَ العيب** جزئيًّا لا خاصيّةً لأسهم فيصل.
>
> ### ج) الإصلاح
>
> `explosion_onset`: **نزولٌ متكرّرٌ** إلى القاع الحقيقيّ — قاعُ النافذة السابقة
> يصير مِرساةً جديدة **ما دام أدنى ماديًّا** (‏`ONSET_EPS`)، فحين لا يوجد قاعٌ أدنى
> فقد بلغنا القاع. وخطوةٌ واحدة **لا تكفي** (مقيس: تُبقي 11 من 20 داخله).
> ✅ **المقيس بعد الإصلاح: ‏0 من 20 داخل الانفجار في ثلاثة أشكال** (صعودٌ ممتدّ ·
> قفزةُ يومٍ واحد · هبوطٌ ثم قاعٌ ثم صعود) · وحارسُ «قاعٌ مساوٍ ⇒ توقّف» يمنع
> الزحفَ في قاعدةٍ مسطّحة · **وشاهدُ ضبطٍ في السويّة يُثبت أن المِرساة القديمة
> تُلوّث 20 من 20 فعلًا** فلا يكون القفلُ عدميًّا.
>
> ### د) الأثر على التجربة
>
> ⛔ **`T-ENVELOPE` لا تُشغَّل** حتى تُعاد المعايرة ⇒ بصمةٌ جديدة ⇒ **تُحدَّث ثوابتُ
> `V3`** معها بتعديلٍ مؤرَّخ (وهو ما سيمنعه `V3` المنفَّذ الآن تلقائيًّا).
> ⚖️ **ولم يُنشَر أيُّ رقمِ ربحية:** التشغيلةُ الوحيدة (`31031808776`) **أُلغيت عند
> خطوتها السابعة قبل طَبْعِ أيّ رقمِ ذراع** ⇒ السحبُ يمسّ **المعايرة** لا حكمًا.
> ⚖️ **ولا هو تدويرٌ للنتائج:** الإصلاحُ صلاحيةٌ بحتة، **مُبرهَنٌ باستقلالٍ تامّ عن
> أيّ مُخرَج** (عيّنةٌ صناعية بلا سوقٍ ولا كاتالوج).
>
> ### هـ) 🧭 الدرس
>
> **مِرساةُ الحدث ليست الحدث.** «أوّلُ/آخرُ جلسةٍ تستوفي شرطًا» ≠ «بدءُ الظاهرة»،
> وفي ظاهرةٍ ممتدّة **يُقاس ما بعدها على أنه ما قبلها**. وكلُّ تجربةٍ تُرسي على حدثٍ
> ممتدّ يلزمها **عدّادٌ صريح: كم جلسةً من نافذة القياس تقع داخل الحدث؟** — ولو كان
> هذا العدّادُ مطبوعًا من أوّل يوم لانكشف العيبُ في التشغيلة الأولى لا في السابعة.


---

## 📌 تعديل ⑩ — بصمةُ الظرف بعد المعايرة النظيفة (‏2026-08-06)

> يُكتب **بعد** صدور مُخرَج المعايرة `31070365317` ويُصرَّح بذلك. **ولا يمسّ أيّ معيارٍ
> أو عتبة** — يُحدّث **ثابتَ الهويّة** الذي يحرسه `V3`، كما ينصّ `V3` نفسُه على أن
> تبديل الظرف يستلزم تعديلًا مؤرَّخًا.

🔴 **وعيبٌ قائمٌ يُصحَّح معه:** كان `envelope_bt.py` مثبَّتًا على **`2dc8c7afb0db`**
بينما هذي الوثيقة تُعلن **`bbc3cfde3e92`** ⇒ **الحارسُ يحرس رقمًا لا وجود له في
الوثيقة، والوثيقةُ تصف رقمًا ليس في الكود** — أي أن `V3` كان **بلا مرجعٍ واحد**.
وهذا مثالٌ آخر على القاعدة المدوَّنة: **حارسٌ مكتوبٌ في وثيقةٍ وغيرُ مطابقٍ للكود ليس
حارسًا — هو نيّة.**

**الظرفُ النافذ الآن:**

| البند | القيمة |
|---|---|
| البصمة | **`a454cd33c848`** (كانت `2dc8c7afb0db` في الكود · `bbc3cfde3e92` في الوثيقة) |
| تشغيلة المعايرة | `31070365317` |
| اللقطة | `31070013412` · as-of **2026-08-06** · 3385 رمزًا |
| `n_symbols` | **28** (لم يتغيّر) · `excluded` = {HTZ, YYAI, XHLD} (لم يتغيّر) |
| المِرساة | **`explosion_onset`** المُصلَحة (تصحيح ⑧) — **وهذي أوّلُ معايرةٍ نظيفةٍ تُجمَّد** |

**والحوافُّ:** `price 1.70` · `drop_pct [83.22 , 99.69]` · `best_spike 91.98` ·
`base_range 472.03` · `dollar_vol 45,218` · `rsi_min 45` · `rsi_now 49.78` ·
`n_soft 6` · `readiness 10` · `score 35` · `rr 1.20`.

⚖️ **وحدُّ إسنادٍ يُقال مع كل مقارنة:** التشغيلتان (الملوَّثة `31038415188` والنظيفة)
تختلفان في **شيئين معًا** — المِرساة **واللقطة** — فلا يصحّ نسبُ الفارق (أو ثباتِ ستٍّ
من إحدى عشرة حافّة) إلى إصلاح المِرساة وحده.

🔴 **وتصحيحٌ ثانٍ يُلحق بأرقام الكلفة كلِّها:** مسارُ الكلفة كان يحسب الخطوة
`len(universe) // cap` ⇒ بـ600 على 3357 تنتهي العيّنة عند الفهرس **2995 = تغطية
‏89.2%**، **والكونُ أبجديّ فذيلُ الأبجدية مهمَلٌ بالكامل**. صُحّح إلى `ceil` (تغطية
100%) ⇒ **كلُّ رقم كلفةٍ نُشر قبل اليوم — بما فيه «‏≈93 مطابق/يوم» — أرضيّةٌ لا سقف.**
