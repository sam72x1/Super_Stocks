# 🔇 LIQ-NOISE — تسجيلٌ مسبق: **كم إشعارًا يقصّ كلُّ طبقة؟ وهل تُفقِد متحرّكًا؟**

> **مدفوعٌ قبل أيّ رقم.** أمرُ المالك 2026-08-17: «لا يرسل لي الا الاسهم اللي سيولتها
> في فريم الدقيقة **فوق ٣٠ الف دولار** سيولة **داخله مب خارجة** و **التحديث في حال
> كانت تزيد** … كمية الاشعارات اللي توصلني الان كل دقيقة **مقرفه جدااا و عشوايه** ·
> **سو عصف ذهني و اوصل لافضل نتيجة … لو فيه شي افضل من اللي قلته لك الان مباشرة
> اعتمده بعد ما تسوي مجموعة اختبارات و تجارب علي الفكرة**».

## ① العيبُ مقيسٌ بنيويًّا — والزنادُ كان نسبيًّا وحده

`liq_stage_events` كانت تُرسي على **قفزةٍ نسبيّةٍ فقط** (‏`vol_x ≥ IGNITION_VOL_MULT`
= 3× متوسّطَ ما قبلها) **بلا أرضيةٍ مطلقةٍ ولا اتجاه** ⇒ سهمٌ يتداول 500 سهمًا/دقيقة
عند $1 يستوفي الزناد على **‏$1,500** — وهي ليست «سيولةً دخلت» بأيّ معنًى. ثم `Mu`
يُرسَل مع **كلّ دقيقةٍ مكتملةٍ جديدة** حتى `LIQ_UPDATE_CAP`=6 **ولو خبَت الحركة** ⇒
ستُّ رسائلَ لسهمٍ يهبط. **‏× كونٍ من ‏≈60-319 اسمًا × دورةٍ كلَّ دقيقة = الإغراقُ
الذي وصفه المالك، وسببُه تصميمي لا صدفة.**

## ② الأذرعُ الثمانية — مثبَّتةٌ الآن، ولا تُضاف ذراعٌ بعد الأرقام

كلُّها إعاداتٌ للمنطق **بلا نداءٍ شبكيٍّ إضافيّ** (شموعُ الدقيقة نفسُها).

| الذراع | التعريف |
|---|---|
| **`V0`** | الأساسُ الذي أغرقه: `vol_x ≥ 3` للمِرساة · `Mu` بكلّ دقيقةٍ جديدة |
| **`V1`** | `V0` ‏+ أرضيةُ **‏$30,000** على المِرساة **وحدها** |
| **`V2`** | `V1` ‏+ الأرضيةُ على التحديثات أيضًا |
| **`V3`** | `V2` ‏+ **اتجاهٌ داخل** (خضراءُ **وإغلاقٌ في النصف الأعلى**) |
| **`V4`** | `V3` ‏+ **تحديثٌ عند تجاوز القمّة** (high-water) ⇐ **المُعتمَدُ الآن بأمره** |
| **`V5`** | `V4` ‏+ **دقيقتان متتاليتان** مستوفيتان قبل أوّل إشعار (كلفتُه دقيقةُ تأخّر) |
| **`V6`** | `V4` لكنّ الأرضيةَ `max($30,000, 5× وسيطَ دقائق الجلسة لهذا السهم)` |
| **`V7`** | `V4` ‏+ إغلاقُ الدقيقة **فوق افتتاح اليوم** (اليومُ صاعدٌ لا هابط) |

⛔ **وذراعُ تأكيد المضارب (`operator_flow`) خارج المِجَسّ عمدًا**: تلزمها صفقاتٌ
تاريخيّةٌ بحدودٍ زمنيّة، وكلفتُها = **عددُ إشعارات `V4`** وهو مخرَجُ المِجَسّ نفسِه
⇒ يُقرأ منه بلا تشغيلٍ ثانٍ.

## ③ المقاييسُ — مثبَّتةٌ قبل الأرقام

**أ) الضجيج:** إجماليُّ الإشعارات · عددُ الأسهم المُنبَّه عليها · وسيطُ وأقصى
إشعاراتٍ للسهم الواحد.
**ب) الاسترجاعُ والأسبقيّة على «المتحرّكين»:** المتحرّكُ **يُعرَّف من الشموع نفسِها**
(‏`أعلى الجلسة ÷ افتتاحها − 1 ≥ 30%`) — **لا من قائمةٍ مختارةٍ بعد الحدث**. ولكلّ
متحرّك: هل أطلقت الذراعُ `M1` **قبل** دقيقةِ القمّة؟ و**الأسبقيّةُ** = الدقائقُ
بينهما. ثم **فقدُ الأسبقيّة** = وسيطُ `lead(V0) − lead(Vk)` على مَن أطلقتهما الذراعان.

## ④ قاعدةُ القرار — مكتوبةٌ قبل أيّ رقم

`V4` **مُعتمَدٌ بأمر المالك مهما كانت الأرقام**. ولا تُبدَّل به ذراعٌ إلّا باستيفاء
الثلاثة معًا:
1. **إشعاراتُها ليست أكثرَ** من `V4`.
2. **ولا تُفقِد متحرّكًا واحدًا** أطلقته `V4` (الاسترجاعُ لا ينقص).
3. **ووسيطُ فقدِ الأسبقيّة ‏≤ دقيقةٍ واحدة.**

وإن لم تستوفِ ذراعٌ الثلاثة ⇒ **تُنشَر ولا تُعتمَد**، وأقول صريحًا «لا شيءَ أفضلُ
ممّا أمر به المالك» — فالجوابُ الصادقُ على «لو فيه شي افضل» قد يكون **لا**.
🔴 **وحالةٌ واحدةٌ تُوجب الرجوعَ إليه فورًا:** أن تُفقِد `V4` متحرّكًا كانت `V0`
ستمسكه ⇒ فأرضيتُه تكتم ما جاءت لتُمسكه، **ويُبلَّغ بالرقم لا يُطوى**.

## ⑤ سقفُ النجاح — مثبَّتٌ سلفًا

**المسموح:** تعديلُ ثوابتِ بوّابةِ الضجيج (`LIQ_MIN_USD`/`LIQ_CLOSE_POS_MIN`/
`LIQ_UPDATE_CAP`) واستبدالُ منطق الطبقة الرابعة **داخل `liq_stage_events` وحدها** ·
وأسطرُ عرض. ⛔ **الممنوع:** أيُّ مسٍّ بعتبات فيصل (`IGNITION_*`) · بالفرز أو باختيار
القائمة · بـ`rank_key`/`select_top`/`analyze_ticker`/`scan_ignition`/درعِ المقسّم ·
وأيُّ كتمٍ في **قناةٍ أخرى** (رادارُ الانطلاق و«هنا الدخول» لا يُمَسّان).

## ⑥ تنبّؤاتي — قابلةٌ للكسر

- **`H1`: `V4` تقصّ ‏≥80% من إشعارات `V0`.** وإن قصَّت أقلَّ فتشخيصي («الأرضيةُ
  الغائبةُ هي سببُ الإغراق») **مكذَّبٌ ويُنشَر**.
- **`H2`: الأرضيةُ المطلقةُ وحدها تفعل معظمَ القصّ**، والاتجاهُ والقمّةُ يضيفان
  ‏≤20 نقطةً مئويّةً إضافيّة.
- **`H3`: `V4` تُبقي كلَّ متحرّكٍ أمسكته `V0`** — لأن المتحرّكَ بطبعه ذو دقائقَ
  دولاريّةٍ كبيرة. وكسرُه **حرجٌ** يُبلَّغ فورًا (‏§④).
- **`H4`: `V5` تقصّ أكثرَ وتفقد ‏≥دقيقةٍ من الأسبقيّة على معظم المتحرّكين** ⇒
  تسقط بالقاعدة (3).
- **`H5`: أكثرُ الإشعارات صنفُه `group` بمقياس فيصل الدولاريّ** حتى بعد الأرضية
  (‏$30,000 دون `IGNITION_USD_GROUP`=50,000) ⇒ **والوسمُ يبقى صادقًا: «دخلت سيولة»
  ليست «دخل المضارب»**.

## ⑦ حدودُ صدقٍ تُقرأ مع النتيجة لا بعدها

1. **جلسةٌ واحدة ⇒ مِجَسُّ كلفةٍ لا تجربةَ حكم.** لا يُشتقّ منه عائدٌ ولا دقّة.
2. **«إشعارٌ على غير متحرّكٍ» ليس إنذارًا كاذبًا بالضرورة** — قد تدخل سيولةٌ حقيقيّةٌ
   ولا يتحرّك السهمُ 30%. فالمقياسُ **بديلُ دقّةٍ** لا دقّة.
3. **والمتحرّكون قلّةٌ في اليوم** ⇒ أيُّ حكمِ استرجاعٍ بأقلَّ من **‏5 متحرّكين**
   يُعلَن «لا حكم» ولا يُبنى عليه.
4. **الجلسةُ قد تكون جاريةً وقتَ التشغيل** ⇒ «قمّةُ الجلسة» هي القمّةُ **حتى الآن**،
   والأسبقيّةُ تُقاس عليها.
5. **والإعادةُ تقريبٌ لا محاكاةٌ تامّة:** المُشغِّلُ الحيُّ يلفّ كلَّ ‏≈60 ثانية
   ويقرأ نافذةَ 65 دقيقة؛ والمِجَسُّ يُطعِم دقيقةً مكتملةً واحدةً في كلّ خطوة
   **وهو ما يفعله الحيُّ فعلًا** — لكنّ سقوطَ نداءٍ حيًّا أو تأخّرَ دورةٍ يجعل الحيَّ
   **أقلَّ** إشعارًا لا أكثر ⇒ أرقامُ المِجَسّ **سقفٌ** للضجيج.
