# `T-PIVOT-BOTTOM` — «سجّل وقف القاع للارتكاز» (تسجيلٌ مسبقٌ قبل أيّ رقم)

> **أمرُ المالك (2026-08-27):** «… بعدها **سجّل وقف القاع للارتكاز**». هذا عقدُ
> الشقّ الثالث. **وأمرُه «سجّل» لا «اعتمد»** ⇒ سقفُ النجاح **قياسٌ وتوصية**،
> والاعتمادُ كلمةٌ منه (‏§⑦).

---

## ⓪ توتّرٌ يُعلَن قبل السؤال — لأنّي أنا مَن كتب القاعدةَ التي تمنعه

في مادّة فيصل **أربعةُ أوقافٍ لكلٍّ نظامُه، وقاعدتُنا المدوَّنة: «لا تخلطها»:**

| النظام | الوقف | الوسم |
|---|---|---|
| **الارتكاز (الفارز)** | **‏7% تحت الدعم** (`STOP_BELOW_LOW_PCT`) | `engineering` — قرارُنا |
| صيّاد المقسّم | **القاعُ نفسُه** | `faisal_verbatim` |
| النهج العلميّ | الدخول ناقص 6% | `faisal_verbatim` |
| فلترةُ التقسيم | ذيلُ شمعة القاع | `faisal_verbatim` |
| **قناةُ الضغط (P1)** | **القاعُ نفسُه** — اعتُمد 2026-08-27 | `faisal_verbatim` |

⇒ **نقلُ وقفِ القاع إلى الفارز هو بعينه «استيرادُ نظامٍ آخر»** الذي حذّرت منه
قاعدتي بعد حادثة `trigger_state`. ⚖️ **والذي يجعله مشروعًا شيئان لا ثالث:**
① **أمرُ المالك الصريح** ② **وأنه يُقاس معزولًا** لا يُشحن بالرأي.

🔑 **ولماذا الآن:** الاعتمادُ في قناة الضغط جاء بسلسلةٍ ثلاثيّة (`T-GOLD-ENTRY`
‏+0.689R ⟶ `T-SLIP` ‏+0.566R بشموع الدقيقة ⟶ `T-SLIP-NBBO` ‏+0.576R على العرض
القائم) — **وكلُّها على مجتمع حلقات الضغط، ولم تُقَس على مرشّحي الفارز قطّ.**
🔴 **و`T-STOP` قاست الاتّجاه المعاكس** (‏13%/15% = وقفٌ **أوسع**) **ففشلت**
(‏−0.1 · +0.1 · 0.0 وتنقلب) ⇒ **الأضيقُ لم يُجرَّب على الفارز إطلاقًا.**

---

## ① السؤال

> على مرشّحي الفارز، هل الوقفُ **عند مِرساة الدخول نفسِها** (‏`pivot` =
> المستوى المُختبَر بعد اعتماد `T-ANCHOR`) أفضلُ من **‏7% تحتها** — بوحدةِ
> مخاطرةٍ **واحدةٍ للطرفين**؟

---

## ② المجتمعُ والمحرّك — صفرُ مسٍّ بالإنتاج

- **المجتمع:** لقطاتُ PIT الثلاث (‏2023 `31574165523` · 2024 `31199493957` ·
  2025 `31191327731`) بإعدادات الإنتاج (‏`FAISAL_ONLY=1`).
- **المحرّك:** صفقاتُ `backtest_symbol` الإنتاجية بعلمَي الإلحاق
  (`BT_REPLAY10`/`BT_ENVVALS`) — والصفقةُ تحمل `pivot` و`entry` و`stop` و`t1`
  أصلًا ⇒ **الذراعُ تُحسَب من الحقول المخزَّنة بلا مِشيةٍ ثانية.**
- **الحسم:** `_resolve_arm` **الإنتاجيّة بالاسم** على شموع النافذة نفسِها.
- 🔒 **`Super_stock.py` لا يُمَسّ بحرف** — ولا عتبةٌ ولا بوّابة.

---

## ③ الأذرعُ ثلاثٌ ولا رابعة

| الذراع | الوقف | بوّابةُ `rr` | الدور |
|---|---|---|---|
| **`B0`** | الإنتاج (‏`stop[0]`) | الإنتاج | المرجع |
| **`B1`** 🥇 | **`pivot` نفسُه** | **الإنتاج** (المجتمعُ ثابت) | **الحاكمة — عزلُ الوقف** |
| **`B2`** | `pivot` نفسُه | **مُعادُ الحساب** بالوقف الجديد | كلفةُ النشر (العضويّة تتغيّر) |

⚖️ **ولماذا `B1` هي الحاكمة:** تغييرُ الوقف يرفع `rr` (البسطُ ثابتٌ والمقامُ
يصغر) فيُدخل إشاراتٍ كانت ترسب على `MIN_RR_T1` ⇒ **`B2` يخلط أثرَ الوقف بأثرِ
تبدّل المجتمع**. `B1` يجيب «هل الوقفُ أفضل؟» و`B2` يجيب «ماذا يقع لو شُحن؟» —
**وكلاهما يُنشَر**، والحكمُ على `B1` وحدها.

---

## ④ المقياسُ — وحدةُ مخاطرةٍ **واحدة** للطرفين

🔴 **الفخُّ المقيس سابقًا** (‏`T-RECLAIM-INTRADAY` §التسجيل): ذراعٌ تُقاس
**بوحدتها هي** تبدو أفضلَ ممّا تعنيه للمحفظة. ⇒ **الوحدةُ الحاكمة هنا:**

> **`R₀ = entry − stop(B0)`** — وحدةُ مخاطرة **الإنتاج**، تُقسَم عليها عوائدُ
> الأذرع الثلاث. فوقفُ القاع **لا يُكافأ** بصِغَر مقامه.

- **المقياسُ الحاكم:** متوسّطُ `R₀` لكلّ صفقة.
- **المساندُ (يُنشَر ولا يحكم):** نسبةُ الربح · متوسّطُ العائد المحقَّق ٪ ·
  عددُ الإشارات · و`R` بوحدة كلّ ذراعٍ **مُعلَنًا أنه غيرُ قابلٍ للمقارنة**.

**تُوصى `B1` إن استوفت الأربعةَ معًا:**
1. `B1 − B0` **بـ`R₀`** يبلغ **‏+0.15** فأكثر مجمَّعًا.
2. **موجبٌ في السنوات الثلاث** بلا انقلاب.
3. الفاصلُ 95% للفرق المجمَّع **لا يلمس الصفر** (بوتستراب عنقودُه **الرمز**
   — `replay10.cluster_bootstrap_ci` بالاسم).
4. **الأرضية:** ‏100 صفقةٍ محسومةٍ فأكثر في كلّ سنة (رقمٌ **مُعادٌ** من
   `T-EXIT` لا مخترَع).

---

## ⑤ بوّاباتُ الصلاحية — منفَّذةٌ لا موصوفة

| البوّابة | الشرط | عند السقوط |
|---|---|---|
| **`BV0`** | إعادةُ حسم `B0` بـ`_resolve_arm` تُطابق `outcome`/`ret_a` المخزَّنَين **بت-بت** | **خروج 3** |
| **`BV1`** | `B1` **يتفرّق** عن `B0` في كلّ سنة (وإلّا `no-op`) | **خروج 4** |
| **`BV2`** | `pivot` حاضرٌ في **‏95%** من الصفقات فأكثر (وإلّا العلمُ خامل) | **خروج 3** |
| **`BV3`** | `entry` أكبرُ من `pivot` في كلّ صفقةٍ تدخل الحكم (وإلّا الوقفُ فوق الدخول = مخاطرةٌ سالبة) — والمخالفُ **يُعَدّ ويُستبعَد ويُعلَن** | يُطبَع |
| **`BV4`** | الأرقامُ الأساسية تُعيد إنتاج المنشور لسنةٍ واحدة على الأقلّ | يُطبَع |

---

## ⑥ التنبّؤات

- **`BP1`** (الأثقل): أرجّح أن `B1` **ترفع** متوسّطَ `R₀` وتُنقص نسبةَ الربح
  بشدّة (نمطُ `T-GOLD-ENTRY`: ‏19.1% ⟶ 10.3%) — **فإن ارتفعت النسبةُ أيضًا
  فذاك مؤشّرُ عطبٍ لا نجاح**.
- **`BP2`**: `B2` يزيد الإشارات **‏20%** فأكثر (‏`rr` يرتفع فيُدخل المرفوض).
- **`BP3`**: أرجّح أن `B1` تعبر ① و② وتسقط في **③** (الفاصلُ يلمس الصفر) —
  لأن مسافةَ 7% صغيرةٌ نسبةً إلى تقلّب فئتنا فالفرقُ يبتلعه الضجيج.
- **`BP4`**: نسبةُ الصفقات التي `entry` فيها ليس أكبرَ من `pivot` **دون 2%**.
- **`BP5`**: أثرُ `B1` **أضعفُ** من أثرِه في قناة الضغط (‏+0.689R) — لأن دخولَ
  الفارز **متوسّطُ دفعاتٍ فوق المِرساة** لا لمسةٌ عندها.

---

## ⑦ سقفُ النجاح — مثبَّتٌ سلفًا

**أمرُه «سجّل» ⇒ المُخرَجُ قياسٌ وتوصيةٌ مكتوبة.** وإن استوفت `B1` المعاييرَ
الأربعة **تُعرَض عليه بالرقمين (‏`B1` معزولًا و`B2` منشورًا)** ولا تُشحن إلّا
بكلمةٍ منه — لأنها تمسّ **الوقفَ والعضويّة معًا** (`LOGIC_VERSION` +
`force_renew`). ⛔ **وممنوعٌ مهما كانت الأرقام:** مسُّ `M1`-`M14` أو الأهداف أو
الدفعات أو أوقافِ الأنظمة الأربعة الأخرى.

---

## ⑧ حدودُ صدقٍ مكتوبةٌ قبل أيّ رقم

1. 🔴 **الوقفُ عند المِرساة يُضرَب بلمسةِ ذيلٍ واحدة** — و«مسحُ السيولة» بلفظ
   فيصل **‏7-13% تحت الدعم** ⇒ الوقفُ الجديد **داخل منطقة الحصاد بالتعريف**.
   وهذا **جوهرُ المقايضة** لا حاشية: نشتري مخاطرةً أصغر بتكرارِ خروجٍ أعلى.
2. الحسمُ بلمسةِ ذيلٍ داخل الشمعة **بلا انزلاق** — والانزلاقُ قِيس في `T-SLIP`
   على **قناةٍ أخرى** ولا يُنقَل.
3. `t1` **لا يتغيّر** ⇒ المقيسُ الوقفُ وحده لا سياسةُ الخروج (‏`T-EXIT` أعدمت
   الأهدافَ الأبعد · و`T-MANAGE-25` أعدمت التمتيع بوصفة 75/25).
4. انحيازُ البقاء مخفَّفٌ باللقطة **وغيرُ منفيّ** · وتشويهُ التقسيمات قائم ·
   وعمى الجلسة الممتدّة قائم (`T-AH`).
5. **والدائريّةُ منتفية هنا** (الوقفُ ليس مشتقًّا من نتيجة) — وهذا أنظفُ من
   `T-RANK-DENSE`.

---

## ⑨ ملحقٌ مؤرَّخ 2026-08-27 — **قبل أيّ رقم**: `B2` غيرُ قابلٍ للتقييم

عند بناء الأداة انكشف قيدٌ لم أرَه وقت الكتابة، **فيُعلَن قبل التشغيل لا بعده**:

`rr` في `analyze_ticker` ليس بوّابةَ رفضٍ صلبة بل **نقصًا لينًا** (‏`:3921`
«العائد مقابل المخاطرة منخفض») يُرفَض صاحبُه فقط إن تجاوز مجموعُ النواقص
`WATCH_MAX_FAILS`. ⇒ الإشاراتُ التي **سيُدخلها** وقفُ القاع ليست «مَن `rr`
الجديد يعبر عندهم» فحسب، بل تعتمد على **عدّاد النواقص كلِّه** ⇒ **لا يمكن
استخراجُها من صفقات الإنتاج المخزَّنة**، ولا تُحسب إلّا بحقنِ الوقف داخل
`analyze_ticker` نفسِها = **مسٌّ بالجذر لأجل ذراعٍ غيرِ حاكمة**.

⇒ **`B2` تُعلَن «غيرَ قابلةٍ للتقييم» بهذي الأداة**، ويُنشَر بدلًا منها ما
يُقاس فعلًا بلا مسٍّ: **اتّجاهُ `rr` ومقدارُه على صفقات الإنتاج** (‏كم ترتفع
نسبةُ العائد/المخاطرة، وكم صفقةً كان `rr` القديم يَسِمها نقصًا فيرفعه الجديد
فوق الحدّ) — **وهو حدٌّ أدنى لتبدّل العضويّة لا قياسٌ له.**

⛔ **ولا يُغيَّر شيءٌ في `B0`/`B1` ولا في المعايير الأربعة** — الحاكمةُ كما
سُجِّلت، والتنبّؤ `BP2` **يُوسَم «لم يُقَس»** لا محقَّقًا ولا مكذَّبًا.
