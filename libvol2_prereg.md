# 🔓💧🛑 `T-LIBVOL-2` — تسجيلٌ مسبق: **الكسرُ بسيولةٍ بوقفٍ على قاع شمعة الكسر**

> **أمرُ المالك (2026-09-02): «سجّل وقف الكسر».** مدفوعٌ **قبل سطرِ كودِ أداةٍ واحد**،
> ولا يُمَسّ بعد أوّل رقم. أيُّ تعديلٍ لاحق = إبطالٌ يُعلَن.

## ⓪ لماذا محورٌ جديدٌ لا إعادةُ فتحٍ ممنوعة

`T-LIBVOL` (‏`libvol_result.md`، 2026-08-28) أُغلقت على **الكسر بسيولة زنادَ دخولٍ بوقفِ
الأساس**: ‏`V1 − V0` = **‏−0.879R** والسيولةُ تختار الأسوأ. **والعلّةُ المقيسة هناك أن
الدخولَ ارتفع والوقفَ بقي ثابتًا** ⇒ مخاطرةٌ أوسعُ بمكافأةٍ لم تتّسع. **وشرطُ الفتح المدوَّن
في `§⑦` نصًّا:** «الكسرُ بسيولةٍ **مع وقفٍ يُعاد ضبطُه على قاع شمعة الكسر**». ⇒ المتغيّرُ
**الوقفُ وحدَه**، والدخولُ والحاجزُ والسيولةُ والنافذةُ **كما قِيست حرفيًّا**. ويتّسق مع
`T-GOLD-ENTRY` (الوقفُ وحدَه كان الحافّة ‏+0.689R) و`T-PIVOT-BOTTOM-2` (وقفُ القاع ‏+0.063R).

⛔ **وما لا يُحرَّك:** `VOL_SPIKE_MULT` · `BT_LIB_WAIT`=20 · تعريفُ الحاجز `L1` · إغلاقٌ لا ذيل.

## ① التعريفُ الإجرائيّ — مثبَّتٌ

- **الحاجز** = `L1` = `_liberation_levels(r)[0]` (الرقمُ الحرج من `build_interpretation`) على
  `r` المحسوب بـ`analyze_ticker` عند فهرس الإشارة نفسِه (`df.iloc[:i]`) ⇒ صفرُ تسريب.
- **الكسر** = أوّلُ **إغلاقٍ** فوق الحاجز خلال `BT_LIB_WAIT` جلسة **بحجمٍ ≥ `VOL_SPIKE_MULT` ×
  متوسّطِ عشرين سابقة** — بدالّة `_libvol_break` **بالاسم** (‏`vol_prev` = أحجامُ ما قبل الأمام).
- **الدخول** = إغلاقُ شمعة الكسر · نملك من فتح التالية (`_resolve_arm(..., idx+1, entry_intrabar=False)`).
- **الوقفُ الجديد (`W1`)** = **قاعُ شمعة الكسر نفسُها** `lo[idx]` **بلا هامشٍ ولا تدوير**
  (رقمٌ صفريُّ المعالم — لا 5% ولا 7% ولا سنت). سندُه نصُّ فيصل على الهمر «مهمّ ما يكسر ذيلها»
  وقاعدةُ «وقفه قاعه» — **إسنادٌ استنتاجيّ (`inferred`) لا حرفيّ**، يُوسَم كذلك.
- **الحسم** = `_resolve_arm` الإنتاجيّة (الوقفُ أوّلًا بلمسة الذيل · الهدفُ `t1` الأساس بلا تغيير).
- **الوقفُ فوق الدخول أو يساويه** (‏`lo[idx] ≥ close[idx]` — شمعةٌ تُغلق على قاعها) ⇒
  **يُستبعَد من `W1` ويُعَدّ ويُعلَن** (سابقةُ `BV3`).

## ② الأذرع — ثلاثٌ لا رابعة

| الذراع | التعريف |
|---|---|
| **`W0`** | الأساس (تعبئةٌ عند متوسّط الدفعات · وقفُ الأساس) — **مقصورٌ على نفس الصفقات المُعبَّأة في `W1`** (مقترن) |
| **`V1`** | الكسرُ بسيولة **بوقف الأساس** = ذراعُ `T-LIBVOL` **حرفيًّا** (بوّابةُ إعادة الإنتاج) |
| **`W1`** | الكسرُ بسيولة **بوقفٍ = قاع شمعة الكسر** — **الحاكمة** |

## ③ المقياسُ الحاكم — وحدةٌ واحدة

**`R₀` = العائدُ المحقَّق ÷ مخاطرةِ الذراع بوقف الأساس** `(entry_arm − stop_base)/entry_arm`
— **لكلّ الأذرع** (سابقةُ `T-PIVOT-BOTTOM`/`T-RECLAIM-INTRADAY`: وقفٌ أضيق لا يُكافأ بصِغَر
مقامه). `W1` و`V1` تتشاركان الدخولَ والمقام ⇒ **`W1 − V1` يقيس أثرَ الوقف وحدَه**.
**والمساند (وصفيّ):** نسبةُ الربح · العائدُ المحقَّق % · نصيبُ الصفقات التي قاعُ شمعتها **دون**
وقف الأساس (وقفٌ أرخى لا أضيق) · وصفقاتُ الاستبعاد.

## ④ المعايير — الأربعةُ تلزم معًا (على **`W1 − W0` المقترن** بـ`R₀`)

1. ‏`W1 − W0` **‏≥ +0.15R₀** مجمَّعًا (عتبةٌ مُعادة من `T-STOP`/`T-LIBERATION`/`T-LIBVOL`)
2. موجبٌ في السنوات الثلاث بلا انقلاب
3. فاصلُ 95% **المقترن** (متوسّطُ فرق كلّ صفقةٍ ± 1.96·SE) لا يلمس الصفر
4. أرضية **‏150 محسومةً مجمَّعًا و30 لكلّ سنة**

**وسؤالٌ ثانٍ يُقاس ولا يحكم:** `W1 − V1` (أثرُ الوقف داخل الذراع) — يُنشَر بفاصله.

## ⑤ بوّاباتُ الصلاحية — **منفَّذةٌ رمزَ خروجٍ لا سطرًا** (درسُ `LV0`)

| البوّابة | الشرط | الإخفاق |
|---|---|---|
| **`LW0`** | `V1` يُعيد `libvol_result.md` **بت-بت**: المحسومةُ ‏201 · 260 · 241 والمتوسّطُ ‏−0.395 · −0.392 · −0.402 (‏±0.001، بـ`_arm_stats` نفسِها) — **على اللقطات نفسِها** (‏2023=`31574165523` · 2024=`31199493957` · 2025=`31191327731`) وبـ`PIVOT_STOP_AT_LOW=False` قسرًا (سابقةُ `CAP15`: المنشورُ قِيس قبل اعتماد وقف القاع) | خروج **3** |
| **`LW1`** | `W1` تفترق عن `V1` في صفقةٍ واحدةٍ على الأقلّ (وإلّا الوقفُ الجديد لم ينفُذ = `no-op`) | خروج **4** |
| **`LW2`** | سنةُ اللقطة = سنةُ القياس (‏`asof[:4]`) | خروج **4** |
| **`LW3`** | الأرضية §④-4 | خروج **4** — «لا حكم» |
| **`LW4`** | قفلُ سويّة (AST): `_resolve_arm` · `_libvol_break` · `_liberation_levels` · `analyze_ticker` · `backtest_symbol` **بالاسم** · والوقفُ الجديد يُقرأ من `lo[idx]` · وصفرُ استرجاعٍ بقسمة | سويّة |
| **`LW5`** | قفلٌ سلوكيّ بعيّنةٍ **تفرّق**: شمعةُ كسرٍ قاعُها فوق وقف الأساس ثم شمعةٌ تلمس ذلك القاع ولا تلمس وقفَ الأساس ⇒ `W1` خسارة و`V1` تستمرّ | سويّة |

## ⑥ التنبّؤات — قبل أيّ رقم

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| `LW-P1` | `W1 − V1` **موجبٌ في الثلاث و≥ +0.15R₀** مجمَّعًا (الوقفُ الأضيق يقصّ الخسارة) | سالبٌ أو دون العتبة |
| `LW-P2` | `W1 − W0` **يبقى سالبًا في الثلاث** — علاوةُ الدخول عند الكسر لا تُستردّ بالوقف وحدَه ⇒ **الحاكمةُ تفشل** | موجبٌ في الثلاث |
| `LW-P3` | نسبةُ ربح `W1` **أدنى** من `V1` بعشر نقاطٍ فأكثر في كلّ سنة (وقفٌ أضيق يُضرب أكثر) | أعلى أو الفرقُ دون عشر |
| `LW-P4` | قاعُ شمعة الكسر **دون** وقف الأساس في **أقلّ من 10%** من المُعبَّأة | 10% فأكثر |
| `LW-P5` | استبعادُ «الوقفُ عند الدخول» **دون 2%** | 2% فأكثر |

## ⑦ سقفُ النجاح

**اقتراحٌ للمالك + سطرُ عرضٍ** — ⛔ ولا زنادَ دخولٍ آليّ · ولا مسَّ بـ`activation_state` ·
ولا بالوقف الإنتاجيّ (`PIVOT_STOP_AT_LOW`) · ولا بالأهداف · ولا `LOGIC_VERSION`.

## ⑧ حدودُ صدق — قبل النتيجة

1. **الحسمُ بلمسة ذيلٍ بلا انزلاق — وهو يحابي الوقفَ الأضيق:** `T-SLIP` قاست أن الوقفَ الأضيق
   ينزلق **أقلّ** لا أكثر (قاعٌ فيه طلبات)، لكن أيَّ اعتمادٍ يلزمه قياسُ انزلاقٍ مسجَّلٌ جديد.
2. **`VOL_SPIKE_MULT` عتبةٌ لغرضٍ آخر** و«بسيوله» بلفظ فيصل غيرُ مكمَّم — تُورَث كما هي.
3. **`W0` هنا وقفُه 7% تحت المِرساة** (لإعادة إنتاج المنشور) **والإنتاجُ اليوم وقفُ القاع**
   (‏08-29) ⇒ الأساسُ المقارَن **أساسُ يوم القياس المنشور** لا أساسُ اليوم — يُعلَن.
4. الشرطُ **لا يُقاس إلّا على مَن كُسر حاجزُه بسيولة** (‏≈700 صفقة في الثلاث) — مجتمعٌ جزئيّ.
5. الذراعُ **تُعرِّف** الوقفَ بقاع الشمعة **بلا هامش** — وأيُّ هامشٍ لاحق (‏1-3%) ذراعٌ
   جديدة لا تحريكُ رقم؛ **لا تُضاف بعد الأرقام**.
6. **الجذورُ لا تُمَسّ**: الأداةُ مستقلّة (`libvol2_arms.py`) و`Super_stock.py` **بت-بت**.

**التوقيع:** مدفوعٌ قبل أيّ رقمٍ وقبل أيّ سطرِ أداة.

## ⑨ ملحقٌ مؤرَّخ (2026-09-02، **قبل أيّ رقمِ ذراع** — أثناء بناء قفل `LW5`)

🔴 **عيبٌ حقيقيٌّ في أداة `T-LIBVOL` المنشورة اكتشفه فِكستشرُ `LW5` لا القراءة:** `_libvol_break` تبدأ بـ
`(vol_prev or [])` داخل `try/except (TypeError, ValueError)`، والإنتاجُ يمرّر `vol_prev` **مصفوفةَ numpy**
(‏`df["Volume"].iloc[i-20:i].values`) ⇒ `or` على مصفوفةٍ بأكثر من عنصرٍ **يرمي `ValueError`** فيُبتلَع
ويصير `prev = []` ⇒ **مرجعُ العشرين السابقة كان ميّتًا في كلّ تشغيلات `T-LIBVOL`**: المرجعُ الفعليّ =
شموعُ الأمام `vol[:k]` وحدَها (فارغةٌ عند `k=0` ⇒ `no_volref` = الـ295 المنشورة · وعند `k≥1` متوسّطُ
`k` شمعةٍ لا عشرين). **وقفلُ `LBV4` كان أخضرَ لأن فِكستشره يمرّر قائمةً** = «الفِكستشرُ الذي يكذب».
**مُثبَتٌ تفريقيًّا:** نفسُ المدخلات بـ`np.array([100]*20)` ⇒ `no_volref`، وبـ`[100]*20` ⇒ `filled`.

**القرارُ — مثبَّتٌ قبل أيّ رقم:**
1. `V1` و`W1` **تُمرَّر لهما المصفوفةُ كما في الإنتاج حرفيًّا** (وإلّا سقطت بوّابةُ `LW0` بحقّ) ⇒ الحاكمةُ
   تُقاس على **المرجع الذي قِيس به المنشور**، ويُعلَن أن «سيولة 5× متوسّطَ عشرين» **لم تكن قطّ ما نُفِّذ**.
2. تُضاف ذراعان **وصفيّتان لا تحكمان** بالمرجع المصحَّح (`list(vol_prev)`): **`V1c`** بوقف الأساس
   و**`W1c`** بوقف قاع شمعة الكسر — تُطبَعان بفاصليهما ولا تُرقَّيان إلى الحكم مهما كانت أرقامُهما
   (ترقيةُ ذراعٍ بعد الأرقام تحريكُ هدف). وإن أراد المالك حكمًا عليهما فتسجيلٌ جديد.
3. **`Super_stock.py` لا يُصلَح في هذي التجربة** (المصدرُ مجمَّدٌ بعد نشر أرقامه — سابقةُ `CAP15`)؛
   الإصلاحُ الإنتاجيّ لـ`_libvol_break` **قرارُ مالكٍ مستقلّ**، وقفلُ `LW8` في السويّة يوثّق العيبَ تفريقيًّا
   حتى يُحسَم. ويُلحَق بانرٌ مؤرَّخ بـ`libvol_result.md`.

## ⑩ ملحقٌ مؤرَّخ (2026-09-02 · بعد تشغيلاتِ `d46d8132`/`1a37963f` · **قبل أيّ رقمِ ذراعٍ لسنة 2025**)

`LW0` أدّت عملَها مرّتين ولم يُحرَّك بارٌ:

1. **مجتمعُ الإعادة** — `backtest_libvol_compare` تُقصر على صفقات الأساس
   **المحسومة** (`outcome in (win, loss)`)، وأداتي كانت تُعيد `V1` على الصفقات
   كلِّها ⇒ سقطت الثلاثُ (‏2024: ‏342/−0.371 مقابل ‏260/−0.392). أُصلح في
   `1a37963f`: `base` = المحسومُ في الأساس، و`LW0` والأذرعُ عليه، والكسورُ خارجَه
   **تُعَدّ وتُعلَن** ولا تدخل الأذرع.
2. **فلترُ طول التاريخ** — `run_backtest` يمشي على كلّ رمزٍ طولُه
   `MIN_BARS + BACKTEST_FORWARD_DAYS` (‏160) وأداتي كانت تشترط `MIN_BARS + 60`
   (‏180) ⇒ رمزٌ بين الحدّين مشى عليه الإنتاجُ وأسقطته أداتي ⇒ 2025 سقطت **بصفقةٍ
   واحدة** (‏`V1` 240/−0.403 مقابل المنشور 241/−0.402) بينما عبرت 2023 و2024
   بت-بت. أُصلح إلى فلتر الإنتاج حرفيًّا.

⚖️ **حدُّ صدق:** أرقامُ ذراعَي 2023 و2024 **رُئيت** قبل الإصلاح الثاني (الحكمُ
هناك «فشلت» بفارقٍ يزيد على ‏0.6R₀ عن البار). الإصلاحُ يمسّ **عضويةَ المجتمع** لا
تعريفَ ذراعٍ ولا بارًا ولا معيارًا، ويُعاد تشغيلُ السنوات الثلاث على النسخة نفسِها
(ميزانيةٌ واحدة). وسببُ الصفقة الواحدة **استنتاجٌ قويّ من الكود لا قياسٌ مباشر**
(الـartifacts محجوبةٌ عن بيئة التطوير) — وتعبورُ `LW0` بعد الإصلاح هو التحقّق.

