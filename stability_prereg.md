# 🧱🕯️ `T-STABILITY` — «ثبات الدعم 3 جلسات» بوّابةَ رفض (تسجيلٌ مسبق)

> **يُكتَب ويُدفَع قبل أيّ رقم.** أمرُ المالك 2026-08-13: **«سجّل الثبات وقِسه»**
> بعد مسكةِ `BBLG`. **سقفُ النجاح مثبَّتٌ سلفًا: منحنى كلفةٍ + توصية — ولا تغييرَ
> إنتاجيَّ إلّا بأمرِه الصريح بعد رؤية الأرقام.**

---

## ① العيبُ الذي وُلدت منه — مقيسٌ لا مُفترَض

`BBLG` رُشِّح **في اليوم الذي صنع فيه قاعَه**: هبوطُ اليوم **‏−27.6%** (‏0.9088 ⟶
0.658) · الإغلاقُ عند **‏34%** من مدى اليوم · و**`bars_after` = صفر** ⇒ `held=False`
و`ready=False`. وسمّاه البوتُ «تجميعًا عند قاعٍ متشبّع».

**والسببُ مُثبَتٌ من الكود:** `pivot_stability` تُنتج `held`/`ready` وتُستعمل في
الفارز في **موضعٍ واحد** — تُضيف **‏15 نقطةً** وسطرَ عرض، وتعليقُها نفسُه يقول
«**مكوّن، لا قرار جاهزية**». **وأسبابُ الرفض الثلاثة عشر لا تحوي الثبات إطلاقًا.**
⇒ **قاعدةُ فيصل المنصوصة غيرُ مُنفَّذة كبوّابة**، والدفترُ يَسِمها «🔴 بوّابة (D4)»
**وهو خطأٌ يُصحَّح**.

---

## ② ما يُقاس وما **لا** يُقاس — فصلٌ مكتوبٌ قبل الأرقام

| المفتاح | النصّ | وسمُ الدفتر | يُقاس؟ |
|---|---|---|---|
| **`STABILITY_MIN=3`** | «ضرب قاع … **حافظ ع قاعه لمدة 3 جلسات**» (`IMG_0151`) | 🥇 **`faisal_verbatim`** | ✅ **هو موضوعُ التجربة** |
| `STABILITY_MAX=8` | «5 + سماحية بسيطة» — **تعليقُ كودنا** | ⚙️ `engineering` (نطاقُه المنقول 3-5) | ⛔ **لا يُفعَّل ولا كذراع** |
| `STABILITY_TOL_PCT=2` | — | ⚙️ `engineering` | ⛔ لا يُمَسّ (يبقى 2%) |

🔴 **ولماذا يُمنَع السقفُ قطعًا:** تفعيلُه يرفض سهمًا **ثبت 10 جلسات** — وهو أفضلُ
حالًا من الذي ثبت ثلاثًا. فإنفاذُ رقمٍ من عندنا في اتّجاهٍ يخالف المنطق **زيادةٌ منّا
لا التزامٌ بفيصل** — وهو عينُ ما أمر المالك بإيقافه. **مقفولٌ بـ`ST5`.**

---

## ③ الأذرع — **ثلاثةٌ ولا رابعة** · في تشغيلةٍ واحدة بميزانيةٍ ثابتة

| الذراع | الشرط | الغرض |
|---|---|---|
| **`S0`** | الأساس (الإنتاج: يُحسب ولا يرفض) | المرجع |
| **`S1`** | يُرفَض إن `bars_after < 3` | **الزمنُ وحده** — كم من الكلفة يأتي من مجرّد «مضت ثلاث جلسات» |
| **`S2`** | يُرفَض إن **ليس** (`held` **و** `bars_after ≥ 3`) | 🥇 **الذراعُ الحاكمة** — قراءةُ نصّ فيصل كاملةً («**حافظ** ع قاعه **لمدة 3 جلسات**») |

⚖️ **و`S2` هي الحاكمة لا `S1`**: «حافظ» فعلٌ في النصّ، وإسقاطُه يجعل الشرطَ «مضى
وقتٌ» لا «صمد القاع». و`S1` تُقاس **لتفكيك الكلفة** لا لتُعتمَد.
🔒 **وميزانيةٌ ثابتة** (درسُ `T-CLIFF`): نفسُ اللقطة المجمَّدة · نفسُ السنوات · نفسُ
المُرتِّب · نفسُ السعة الحيّة · نفسُ سقوف الجلب ⇒ الفرقُ **الشرطُ وحده**.

---

## ④ المقياس — مثبَّتٌ قبل الأرقام

**الحاكم: `d50`** = المنفجرون **المُسلَّمون** (بلغوا `EXPLOSION_PCT`=50% قبل وقفهم)
**داخل خانات السعة الحيّة** — بأداة `replay10` الأمينة، نفسِ مقياس `T-BASE-2` و
`T-BASE-475` و`T-CAP`. **لأنه «ما يصل المالك» لا «ما يقبله الفارز».**
**ويُنشَر معه ولا يحكم:** `d100` · الإشارات · المحسومة · الدقة · **`R`/صفقة** ·
`rejected_cap`.

---

## ⑤ حرّاسُ الاعتماد — أربعةٌ، ولكلٍّ قراءةٌ **محدَّدةٌ سلفًا**

| # | الحارس | الحدّ |
|---|---|---|
| **(أ)** | **`d50` لا ينقص مجمَّعًا** (‏`S2` ≥ `S0`) | شرطٌ لازم |
| **(ب)** | ولا سنةً واحدةً بأكثر من **‏20%** | نفسُ حدّ `base475_prereg` |
| **(ج)** | **`R`/صفقة لا يسوء — والقراءةُ «مجمَّعًا» مكتوبةٌ الآن** | ‏≥ −0.02R |
| **(د)** | **يرفض `BBLG` فعلًا** بـ`_diagnose_symbol` الإنتاجيّة | شرطٌ لازم |

⚖️ **و(ج) تُقرأ مجمَّعًا صراحةً** — تصحيحًا لدرس `T-BASE-475` حيث تركتُ الصياغةَ
مبهمةً («سنويًّا أم مجمَّعًا؟») فصار الحكمُ «غير حاسم» بقصورٍ منّي. **ولا تُختار
القراءةُ بعد رؤية الأرقام.**
⇒ **سقوطُ (أ) أو (د) ⇒ لا يُعتمَد.** وسقوطُ (ب) أو (ج) وحدهما ⇒ **مقايضةٌ تُعرَض
على المالك** لا رفضٌ ولا اعتماد.

---

## ⑥ حرّاسُ الصلاحية — **ولكلٍّ سطرُ كودٍ يُوقِف** (درسُ `envelope_prereg §⑦`)

| # | الحارس | الخروج |
|---|---|---|
| `V1` | القيمُ النافذةُ للأذرع الثلاث **متمايزةٌ ومطبوعة** | 3 |
| `V2` | **عدّادُ البوّابة يشتعل تدرّجًا**: `S0`=0 · `S1`>0 · `S2` ≥ `S1` ⇒ **لا no-op** | 3 |
| `V3` | **فحصُ تكامل**: `S0` = ذراعُ `B120` في `base475_result.md §①` **عند سعة 15** (‏2024:25 · 2025:26 · 2026:15) | **يُعلَن ولا يُسقط** |
| `V4` | `child_env()` يرفع أعلامَ الباكتيست (درسُ `T-CHASE`: بلاها `d50`=None و«‏+0» يُقرأ «لا فرق») | 2 |
| `V5` | **`STABILITY_MAX` صفرُ ذِكرٍ في كتلة البوّابة** | قفل |
| `V6` | **العلمُ مطفأٌ ⇒ المُخرَجُ بت-بت** (قفلٌ سلوكيّ) | قفل |

🔴 **و`V3` تُقرأ بثلاثِ حالاتٍ لا حالتين — يُحسَم الآن قبل أيّ رقم:** مطابقةٌ بت-بت
⇒ ✅ · **وأيُّ خرقٍ أو سنةٍ غير مرجعية ⇒ ℹ️ «مقامٌ جديد» يُعلَن بصوتٍ عالٍ ويُوسَم به
سطرُ النتيجة — ولا يُسقط التجربة.**
⚖️ **وسندُه منطقيٌّ لا تسهيلًا:** صلاحيةُ الحكم قائمةٌ على المقارنة **الداخلية**
(‏§③: نفسُ اللقطة والسنة والمُرتِّب والسعة في **تشغيلةٍ واحدة**)، والمِرساةُ المنشورة
**فحصُ محرّكٍ خارجيّ** ⇒ سقوطُها يُبطل **المقارنةَ بالمنشور** لا المقارنةَ بين
الأذرع. **وهذا يُكتَب صريحًا هنا** تصحيحًا لقصور `T-BASE-475` حيث تركتُ قراءةَ حارسٍ
مبهمةً فصار الحكمُ «غير حاسم» بسببي — **ولا تُختار القراءةُ بعد رؤية الأرقام.**

---

## ⑦ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر سقطت أو صمدت)

- **`P1`** `S2` يقصّ **‏≥30%** من الإشارات (الثبات قيدٌ حقيقيّ لا زينة).
- **`P2`** **`d50` ينقص** — لأن بعض القيعان الحقيقية تُلتقَط مبكّرًا بـ`bars_after`
  صغير، فالثباتُ يشتري أمانًا بثمن.
- **`P3`** `S1` و`S2` **يفترقان ماديًّا** (‏≥10% في الإشارات) ⇒ `held` يضيف فوق مجرّد
  مرورِ ثلاث جلسات. وإن تقاربا فالمعنى أن «حافظ» مستوفًى تلقائيًّا بعد 3 جلسات.
- **`P4`** **`R`/صفقة يتحسّن** (رفضُ الدخول وسطَ انهيارٍ نشط ينفع العائد).
- **`P5`** **`BBLG` يُرفَض في `S1` و`S2` كلتيهما** (‏`bars_after`=0).

---

## ⑧ ما لا يُدَّعى — يُكتَب قبل الأرقام

1. **ليس حكمًا على `T-CORE5`** (ذاك قاس **إسقاطَ** الحواجز المشتقّة، وهذا يقيس
   **إضافةَ** شرطٍ فيصليّ) — سؤالان، ولا يُنقَض حكمٌ بقياسٍ لسؤالٍ آخر.
2. **ولا يُدَّعى أنه يمنع خسارةَ `BBLG`** — المقيسُ **منطقُ الترشيح** لا مصيرُ السهم،
   **ولا تنبّؤَ بسعره** في أيّ موضع.
3. **وانحيازُ البقاء وتشويهُ التقسيمات وعمى الافتر** قائمةٌ كما في كلّ باكتيستاتنا،
   **لكنها مشتركةٌ بين الأذرع فتُطرَح من الفرق**.
4. **والسعةُ 15**: أيُّ مقارنةٍ بأرقامٍ منشورةٍ عند سعة 10 **باطلة** ⇒ المِرساةُ
   تُقاس **في نفس التشغيلة** (`V3`).
5. ⚠️ **وتعارضٌ يُعلَن سلفًا:** تشديدُ البوّابات **يقلّل المرشّحين**، والمالك رفع
   السعةَ إلى 15 **ليزيدهم** ⇒ **المقايضةُ قرارُه** بعد الأرقام.

---

## ⑨ إعلانُ مسٍّ بجذرٍ مقفول

تنفيذُ الأذرع يستلزم خطّافًا في **`analyze_ticker`** — جذرٌ مقفول. **بأمر المالك
المباشر** وبنفس سابقة `BT_SPLIT_AWARE_M4`/`BT_M4_POST_SPLIT`/`BT_LIBERATION`:
**علمٌ مطفأٌ افتراضيًّا** فالإنتاجُ **بت-بت** (`V6`)، وبصمتُه تُحدَّث **بإقرارٍ
مؤرَّخ**. **وبقيّةُ الجذور لا تُمَسّ.**

## ⑩ رموزُ الخروج

**‏0** = قِيس والحرّاسُ استُوفوا · **‏2** = أعلامٌ غائبة · **‏3** = بوّابةُ صلاحيةٍ
ساقطة ⇒ **لا رقمَ يُنشَر ولا يُفسَّر** · **‏4** = لقطةٌ مفقودة.
