#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""🛡️ **حارسُ التغطية المشترك للمِجَسّات** — `coverage_bad(...)` (خطة 035).

ستُّ أدواتِ مِجَسٍّ حيّة (`gate_probe` · `m0_probe` · `liq_move_probe` ·
`liq_noise_probe` · `cumrise_probe` · `alert_filter_check`) تجلب كونًا من
~300+ رمزٍ (شموعُ يومٍ/دقيقةٍ من Polygon)، وتطبع عددَ مَن تعذّر جلبُه
(`تعذّر {fails}`) — **ولا تُوقِف** إلا عند الصفر المطلق
(`if not data: return 2`). لو خنق Polygon الجلبَ وأسقط، مثلًا، 150 من 314
رمزًا، تُحسَب الجداولُ على الناجين المتحيّزين ويمضي `main()` إلى `return 0`
= **تشغيلةٌ خضراءُ تُقرأ «نتيجةً حقيقية»** — وهي مرشِّحاتُ قراراتٍ حيّة
للمالك (‏«اطفي M0» · «شغّل c1/P5» · حكمُ `G5`).

🔒 **نظيرُها المطبَّق سلفًا:** `exit_stop_arms.coverage_bad` (‏`V4`، يقيس
أيامَ تداولٍ مفقودة على مدى **سنة**). وهذي **مقياسٌ مختلف الدلالة** (كسرٌ
ضدّ **كونِ رموزٍ** في **جلسةٍ واحدة** لا ضدّ أيامٍ عبر سنة) فلا تُنسَخ صيغتُه
حرفيًّا — تُماثَل بنيتُها فقط (كسرٌ على حدٍّ مُسمّى · طباعةُ الحكم · عودةٌ
غيرُ صفريّة بسببٍ مُعلَن). لا تُستعمل `exit_stop_arms.coverage_bad` هنا
(توقيعُها يوميّ ويسحب معه آلة S3).

نقيّةٌ لتُقفَل سلوكيًّا (`test_bot.py`، أقفال `PC*`) — طفرةُ قلبِ `>` أو
حذفِ الحارس يجب أن تُسقط أقفالَ التخوم لا أن تنجو.
"""

# engineering — عتبةُ اجتهادٍ لا رقمَ فيصل: جلسةُ يومٍ واحد تتحمّل نسبةَ فشلٍ
#   عابرٍ لرموزٍ مفردة أوسعَ من التي يتحمّلها مقياسٌ سنويّ (نظيرُ
#   `exit_stop_arms.MAX_MISSING_DAYS`، لكن على مقامٍ مختلف تمامًا).
MAX_MISS_FRAC = 0.20


def coverage_bad(n_fetched: int, n_universe: int, max_miss_frac: float) -> bool:
    """صحيحٌ حين يتعذّر جلبُ حصّةٍ من الكون تُبطل الثقةَ بالجداول المحسوبة.

    `n_universe<=0` ⇒ **زائف** (الصفرُ المطلق تحرسه بوّابةُ
    `if not data`/`if not rows` في كلّ أداةٍ **قبل** هذا الاستدعاء — لا
    ازدواجَ حكمٍ على المجتمع الفارغ)."""
    if n_universe <= 0:
        return False
    return (n_universe - n_fetched) / n_universe > max_miss_frac
