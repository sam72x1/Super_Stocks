# `T-COUNT` — «قس عدد الصفقات» (تسجيلٌ مسبقٌ مدفوعٌ قبل أيّ سطرِ كودِ أداة)

> **أمرُ المالك (2026-08-27):** «**قس عدد الصفقات** شتغل على قناة الضغط».
> هذا عقدُ الشقّ الأوّل — وهو **تنفيذٌ لشرطِ الفتح الذي كتبتُه بنفسي** في
> `ranker_dense_result §⑧`: «أقواهما مرشَّحًا قياسُ عددِ الصفقات متغيّرًا
> مستقلًّا، وتسجيلُ `taken` للعشوائيّ».

---

## ⓪ لماذا — والسؤالُ وُلد من قصورٍ في أداتي أنا

`T-RANK-DENSE` قاست عند الكثافة الحيّة فوجدت **المُرتِّبَ الإنتاجيَّ أسوأَ من
العشوائيّ في السنوات الثلاث** (‏−0.2334 مقابل −0.0893). **ثم قيّدت القراءةَ
بنفسها:**

| الذراع | صافي `R` كلّيّ | مأخوذ | **`R` لكلّ صفقة** |
|---|---|---|---|
| `Q0` الإنتاج | ‏−203.8 | 1,329 | **‏−0.153** |
| `Q1` | ‏−168.0 | 1,196 | **‏−0.140** |
| `Q2` | ‏−246.6 | 1,455 | **‏−0.169** |
| `Q4` فيفو | ‏−131.1 | 866 | **‏−0.151** |

🔑 **`R` لكلّ صفقة شبهُ ثابت (‏−0.140 إلى −0.169) والمأخوذون 866-1,455** ⇒
**الأذرعُ تفترق في العدد لا في جودة الاختيار**، **والمقياسُ يكافئ التداولَ
الأقلَّ حين يكون التوقّعُ سالبًا** ⇒ تفوّقُ العشوائيّ **قد يكون أثرَ عددٍ لا
جودة**. 🔴 **ولم أستطع حسمَه لأن `taken` للعشوائيّ لم يُسجَّل** — وهو الرقمُ
اللازمُ بعينه.

⇒ **هذي التجربةُ تفصل المتغيّرين.**

---

## ① السؤال

> هل تفوّقُ المُرتِّب العشوائيّ على مُرتِّب الإنتاج **أثرُ عددِ الصفقات** (توقّعٌ
> سالبٌ ⇒ مَن يتداول أقلَّ يخسر أقلّ)، أم **أثرُ جودةِ اختيار**؟

---

## ② المجتمعُ والمحرّك — **نفسُ الصفوف بت-بت، لا إعادةَ مشي**

- **المُدخَل:** ملفّاتُ `ranker_rows.jsonl` **المرفوعةُ من تشغيلات
  `T-RANK-DENSE` نفسِها**: 2023 `33087529806` · 2024 `33087537982` · 2025
  `33087546312` — تُنزَّل بـ`actions/download-artifact` بـ`run-id` (نمطُ
  `frozen-dataset` القائم).
- ⚖️ **وهذي أقوى صورةٍ من قاعدة الميزانية الثابتة:** المجتمعُ ليس «مطابقًا» بل
  **هو هو حرفيًّا** (نفسُ الملفّ) ⇒ يستحيل أن يتفرّق ذراعان بأثر الكون أو
  النافذة أو السقوف — العيبُ المقيس في `T-CLIFF`.
- **المحرّك:** `replay10.candidates_from_trades` و`replay10.replay` و
  `replay10.net_r_per_day` **بالاسم بلا تعديلِ حرف** — والمتغيّرُ الوحيدُ
  **`capacity`** وهو وسيطٌ قائمٌ في `replay` أصلًا.
- 🔒 **صفرُ مسٍّ بـ`Super_stock.py` وصفرُ مسٍّ بـ`replay10.py`** — الأداةُ تقرأ
  وتستدعي فقط.

---

## ③ الأذرع — سلّمُ سعةٍ × ثلاثةُ مرتِّبات، ولا رابع

| المُرتِّب | الرمز | الدور |
|---|---|---|
| `replay10.rank_live` | **`L`** | مُرتِّبُ الإنتاج (المرجع) |
| `make_rank_random` (‏200 بذرة · **الوسيط**) | **`R`** | شاهدُ الصدفة — **ومعه `taken` مسجَّلًا هذي المرّة** |
| `replay10.rank_fifo` | **`F`** | شاهدُ ضبطٍ ثانٍ بلا معلومة |

**وسلّمُ السعة مثبَّتٌ سلفًا:** `15 · 12 · 10 · 8 · 6 · 4 · 3 · 2 · 1`
(‏15 هي السعةُ الحيّة `WATCHLIST_SIZE`، والباقي نزولًا حتى الواحد).

⛔ **ولا تُضاف سعةٌ ولا مُرتِّبٌ بعد رؤية الأرقام.**

**ولكلّ خليّةٍ (مُرتِّب، سعة) يُسجَّل:** `net_r_day` · **`taken`** · `expl` ·
`slot_days` · `rejected_cap`.

---

## ④ المقياسُ والتحليل — تفكيكٌ مثبَّتٌ قبل أيّ رقم

**المقياسُ الحاكم:** `replay10.net_r_per_day` — **مُعادٌ حرفيًّا من
`T-RANK-DENSE` بلا تحريك.**

**والتفكيك:**

1. `Δ_raw = R(cap15) − L(cap15)` — الأفضليّةُ المرصودة (‏≈+0.144 مجمَّعًا).
2. `T_R = taken` للعشوائيّ عند السعة 15 · `T_L = taken` للإنتاج عندها.
3. **`L*` = صافي `R` لليوم للإنتاج عند `taken = T_R`** — يُستخرَج من منحنى
   `L` بـ**استيفاءٍ خطّيّ** بين أقرب سعتين تحيطان بـ`T_R`.
4. `Δ_matched = R(cap15) − L*` — الأفضليّةُ **عند عددِ صفقاتٍ متساوٍ**.
5. **`share_count = 1 − Δ_matched / Δ_raw`** = حصّةُ **العدد** من التفسير.

**والحكمُ ثلاثيٌّ مثبَّتٌ سلفًا:**

| الحكم | الشرط |
|---|---|
| **«العددُ هو التفسير»** | `share_count` **‏0.70 فأكثر في السنوات الثلاث** |
| **«المُرتِّبُ يضرّ حقًّا»** | `share_count` **‏0.30 أو أقلّ في السنوات الثلاث** |
| **«مختلط»** | ما عدا ذلك — **يُنشَر مختلطًا ولا يُوحَّد بعد الأرقام** |

⚖️ **و`Δ_raw` موجبٌ بالبناء** (العشوائيُّ أعلى في الثلاث، مقيسٌ ومنشور) فالقسمةُ
معرَّفة. **وإن انقلبت إشارتُه في سنةٍ فالسنةُ تُعلَن «غيرَ قابلةٍ للتفكيك»** ولا
يُحسَب لها `share_count`.

---

## ⑤ بوّاباتُ الصلاحية — تُنفَّذ في الكود لا تُوصَف

> درسُ `ranker_dense_result §⓪`: **«‏`RV4` كُتبت في العقد ولم تُكتَب في الكود»**
> — ووقعتُ فيه أمس، فكلُّ صفٍّ هنا **يلزمه سطرُ كودٍ يُوقِف وقفلٌ يُثبت أنه يُوقِف**.

| البوّابة | الشرط | عند السقوط |
|---|---|---|
| **`CV0`** | `L` عند السعة 15 يُعيد أرقامَ `Q0` المنشورة **الأربعة** (`net_r_day` · `taken` · `expl` · `rejected_cap`) **بت-بت** | **خروج 3** — عطبُ أداة |
| **`CV1`** | عددُ صفوف الملفّ يطابق المنشور (‏21257 · 21745 · 21470) | **خروج 3** |
| **`CV2`** | `taken` **رتيبٌ غيرُ متناقص** مع ارتفاع السعة لكلّ مُرتِّب | **خروج 3** — محرّكٌ مكسور |
| **`CV3`** | `T_R` **أقلُّ فعلًا** من `T_L` | **خروج 5** — السؤالُ منتفٍ: الأفضليّةُ ليست عددًا، ويُعلَن |
| **`CV4`** | `T_R` يقع **داخل** مدى `taken` لسلّم `L` (وإلّا الاستيفاءُ استقراءٌ) | **خروج 5** — لا تفكيك |

🔒 **وحارسُ العلم الميّت:** إن خرجت خلايا السلّم **متطابقةً بت-بت** عبر السعات
فهي بصمةُ `no-op` ⇒ **يُعلَن ولا يُفسَّر** (سابقةُ `BT_CANDLE`).

---

## ⑥ التنبّؤات — قابلةٌ للكسر، تُنشَر مكذَّبةً كما تُنشَر محقَّقة

- **`CN-P1`** (الأثقل — وهو رهانٌ على فرضيّتي أنا في `§④` من الأمس):
  **`share_count` ‏0.70 فأكثر** ⇒ العددُ يفسّر معظمَ الأفضليّة.
- **`CN-P2`**: `T_R` **دون ‏0.85 × `T_L`** (العشوائيُّ يتداول أقلَّ ماديًّا).
- **`CN-P3`**: منحنى `net_r_day` **يتحسّن رتيبًا كلّما قلّت السعة** لكلّ
  مُرتِّب — لأن التوقّعَ سالب.
- **`CN-P4`**: عند السعة **‏1** تكون الأذرعُ الثلاثُ **أقربَ إلى الصفر** من أيّ
  سعةٍ أعلى.
- **`CN-P5`**: `taken` للعشوائيّ و`taken` لفيفو **متقاربان** (‏داخل 25% من
  بعضهما) — كلاهما بلا معلومة.
- **`CN-P6`**: `expl` (المنفجرُ المُسلَّم) **ينخفض** مع انخفاض السعة لكلّ
  مُرتِّب ⇒ **تقليلُ التداول يشتري `R` ويبيع منفجرين** — وهو جوهرُ مقايضة
  المالك.

---

## ⑦ سقفُ النجاح — مثبَّتٌ سلفًا، ولا يمسّ الإنتاج

🔒 **المُخرَجُ جوابٌ وتقريرٌ فقط.** ⛔ **وممنوعٌ مهما كانت الأرقام:** مسُّ
`rank_key` · أو **`WATCHLIST_SIZE`** · أو أيٍّ من `M1`-`M14` أو عتباتِ الظرف أو
الدخول/الوقف/الأهداف.

🔴🔴 **وتحذيرٌ يُكتب قبل الأرقام لئلّا يُقرأ الجدولُ توصية:** مقايضةُ السعة
**مقيسةٌ ومحسومةٌ سلفًا** — `T-CAP` (‏2026-08-12) وجدت أن السعة 15 تُسلّم
**منفجرين أكثرَ بـ29%** وأن **حارسَ العائد يسقط** (‏−0.075R/صفقة، سالبٌ في كلّ
سنة)، **واختار المالكُ 15 وهو يرى الرقمَين**. ⇒ **إن أظهر المنحنى أن الأقلَّ
أفضلُ في `R` فهو إعادةُ اشتقاقٍ لتوتّرٍ معلوم لا اكتشافٌ جديد، وقرارُ المالك
قائمٌ ولا يُعاد فتحُه من هنا.**

---

## ⑧ حدودُ صدقٍ مكتوبةٌ قبل أيّ رقم

1. 🔴 **السعةُ وكيلٌ لعدد الصفقات لا العددُ نفسُه.** خفضُ السعة يقلّل التداولَ
   **ويغيّر أيضًا أيَّ المرشّحين يُؤخَذ** (الأعلى رتبةً فقط) ⇒ التفكيكُ
   **تقريبيٌّ لا تامّ**، وهذا أثقلُ حدودِه.
2. **المجتمعُ صفوفُ `T-RANK-DENSE`** ⇒ يرث **كلَّ** حدودِها: فريمٌ يوميٌّ حصرًا ·
   «منفجرٌ مُسلَّم» لمسُ سعرٍ لا صفقةٌ منفَّذة · تعبئةٌ بلا انزلاق · انحيازُ
   بقاءٍ مخفَّفٌ لا منفيّ · تشويهُ تقسيمات · عمى الجلسة الممتدّة.
3. **العشوائيُّ وسيطُ 200 بذرة**، و`taken` له يُسجَّل **وسيطًا** كذلك — فقد لا
   تكون البذرةُ صاحبةُ الوسيط في `R` هي صاحبتُه في `taken`. ⇒ **يُطبَع الاثنان
   مع مداهما** ولا يُدَّعى أنهما لبذرةٍ واحدة.
4. **والتوقّعُ سالبٌ في كلّ خليّة** ⇒ هذا **تفكيكُ خسارةٍ لا بحثٌ عن ربح**، ولا
   يُقرأ منه أن أيَّ ذراعٍ «تربح».
