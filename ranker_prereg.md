# 🥇 T-RANKER — تسجيلٌ مسبق (يُدفَع **قبل** أيّ رقم · 2026-07-31)

> **السؤال:** بسعةٍ محدودة، هل يمكن ترتيبٌ يُوصِل **منفجرين أكثر** من المُرتِّب
> الحاليّ — ومن العشوائيّ؟
> 🔒 بحث/قياس · **صفر مسّ إنتاج** · العلم مطفأ افتراضيًّا · لا `LOGIC_VERSION`.
> ⚠️ **كلُّ ما دون هو مثبَّتٌ قبل رؤية أيّ رقم؛ وأيّ تعديلٍ بعده = `p-hacking` صريح.**

---

## ① لماذا هذي التجربة بالذات؟

مقيسٌ في `T-REPLAY10`: المُرتِّب الحاليّ **لم يُثبِت تفوّقًا على العشوائيّ ولا تكافؤًا**
(‏+0.012 · −0.074 · +0.086 ⇒ ينقلب · و`p` «دالّة» في سنتين **باتّجاهين متعاكسين**)،
**وقيمة الترتيب كلُّها +5.0R في ثلاث سنوات** بينما تأرجحُها السنويّ **+1.3 · −8.0 ·
+11.8R**. ومع ذلك **هو مَن يقرّر ما يصل المالك**: السعة تقصّ **27% · 30% · 48%**.

⇒ **هو الحلقة الوحيدة التي (أ) تقرّر التسليم فعلًا · (ب) مقيسةٌ عند الصدفة فمجال
التحسين مُثبَتٌ مفتوح · (ج) ولا تفتح بوّابةً واحدة ⇒ صفر خطر `C3`.**

## ② التوقّع المُعلَن مسبقًا (لئلّا يُسوَّق النفي مفاجأةً)

**الأرجح عندي: نتيجةٌ عدميّة ثالثة عشرة.** لأن **لا سمةَ اختيارٍ ميّزت المنفجر قطّ**
في هذا المشروع (الجاهزية · النواقص · البصمة · FSTO · القطاع · الدولة · الشورت ·
السعر — كلُّها سقطت). **والفرق الوحيد المشروع** أن تلك اختُبرت **بوّابةً** أو
**بمقياس نسبة نجاح**، بينما هنا تُختبَر **رُتبةً داخل بِركةٍ مؤهَّلة** وبمقياس
**التسليم**. فإن سقطت، فالنتيجة: **لا يوجد ما يُرتَّب به — والحدّ ليس البوابات ولا
الترتيب بل قِلّةُ المنفجرين في المجتمع أصلًا**، وهذا **حكمٌ نافع** يوجّه الجهد كلَّه
إلى **دفتر الوصفات** (الصيّاد) بدل الفارز.

## ③ التصميم — **مقارنةٌ مزدوجة داخل الجلسة** (وهو مربط الفرس)

كلُّ التجارب التي سقطت قارنت **سنةً بسنة** فابتلع ضجيجُ السوق كلَّ أثر. هنا:
**كلُّ ذراعٍ تُرتِّب البِركة نفسها في الجلسة نفسها** (نفس الإشارات · نفس السعة ·
نفس المحرّك) ⇒ الفرق **لا يحمل ضجيج السوق** لأن الذراعين تعيشان اليوم ذاته.
وهذا هو التصميم الذي أعطى ثباتًا في `T-NEARMISS` و`T-MANAGE-25`.

**السعة = `WATCHLIST_SIZE` الحيّ** (لا رقمٌ مُخترَع) · والمحرّك `replay10.py`
(الأمين: بلا نظرٍ مستقبليّ · تحريرُ الخانة بالجلسات · لا `no_fill` مُستبعَد مسبقًا).

## ④ الأذرع — **مثبَّتةٌ الآن، ولا تُضاف ولا تُحذَف بعد الأرقام**

| الذراع | التعريف | لماذا هي بالذات |
|---|---|---|
| **K0** | `rank_key` الإنتاجيّ | خطُّ الأساس — ما يصلك اليوم |
| **K1** | عشوائيّ (‏10,000 خلطة) | **أرضية الضجيج** — بلا تجاوزها لا معنى لشيء |
| **K2** | `rr` تنازليًّا | **مكوّنٌ قائمٌ غير مُعاير** (رابع مفاتيح `rank_key`) ⇒ يعزل: هل تحمل نسبةُ العائد/المخاطرة معلومةَ ترتيب؟ |
| **K3** | **تنويعٌ أوّلًا** | يستهدف **فشلًا مقيسًا**: بوّابة التركيز ⑤ سقطت في الثلاث (‏24-32% من الربح في رمزٍ واحد) ⇒ سقفُ رمزٍ واحد لكل قطاع/يوم ثم `K0` لكسر التعادل |

🔒 **ولا ذراعَ رابعة تُبتكَر بعد رؤية النتائج.** وK2/K3 **بلا أيّ معامل يُضبَط**
(‏`rr` كما هو · والتنويع سقفُه **واحد** لا رقمٌ يُجرَّب).

## ⑤ المقاييس — **مثبَّتة قبل الأرقام**

- **الأساسيّ: عدد المنفجرين المُسلَّمين** = المأخوذون الذين بلغوا **‏+100%** قبل
  وقفهم (‏`mg_pre_stop ≥ 100`). **وهو ما يسأل عنه المالك حرفيًّا** («كم منفجر يوصلني»)
  — لا نسبة النجاح (سقطت مرارًا) ولا العائد وحده.
- **المساند: صافي R لكلّ جلسة**، **مزدوجًا داخل الجلسة**، بفاصل bootstrap عنقوديّ
  (العنقود = **الجلسة**).
- **الوصفيّ (خارج الحكم):** نسبة المحفظة · التركيز · عدد المأخوذين.

## ⑥ معيار النجاح — **الخمسة معًا، وإلّا فشلت**

1. **العيّنة:** ‏≥100 جلسة في كلّ سنة، والسنوات الثلاث كاملة.
2. **الاتجاه:** الذراع المرشَّحة تتفوّق على **K0** في **المنفجرين المُسلَّمين** في
   **الثلاث سنوات بلا انقلاب**.
3. **الدلالة:** الحدّ السفليّ 95% للفرق المزدوج (صافي R/جلسة) **فوق الصفر** مجمَّعًا.
4. **تتجاوز الضجيج:** تتفوّق على **K1** بالمعيارين ①و② نفسيهما.
5. **لا يحملها اسمٌ واحد:** أكبر رمزٍ يسهم بأقلّ من **30%** من الفارق.

⚠️ **وتصحيحٌ لثلاث مقارنات** (‏K2 · K3 مقابل K0، ثم مقابل K1) — لا تُقرأ `p` مفردةً.

## ⑦ سقف النجاح — **مُعلَنٌ سلفًا**

**أقصى ما يترتّب: اقتراحٌ للمالك.** ولا تغيير إنتاجيّ إلا بإذنه الصريح، ومعه
**`LOGIC_VERSION` مرفوع + `force_renew=1` مرّة** (يمسّ العضوية). **ولا يمسّ**
`M1-M14` ولا الدخول ولا الوقف ولا الأهداف ولا `M13`/`M14` (خارج البحث بقراره).

## ⑧ حدود صدقٍ مكتوبةٌ قبل النتيجة

انحياز البقاء **غير مقيس** · تشويه التقسيمات · عمى الافتر · و`backtest_symbol` ينادي
`analyze_ticker` وحده (‏`M13`/`M14` في `scan_market` فقط) · وكسرُ التعادل داخل اليوم
**أبجديّ**. ⚖️ **ولا يُبطل أيٌّ منها المقارنة**: الذراعان **تتقاسمانها بالبناء** —
وهو بالضبط سببُ اختيار التصميم المزدوج.

## ⑨ ما يُكذِّب كلَّ ذراع (يُكتَب الآن)

- **K2 مكذَّبة** لو لم يتفوّق `rr` على K0 في المنفجرين المُسلَّمين في أيّ سنتين.
- **K3 مكذَّبة** لو خفّض التنويعُ التركيزَ **ولم** يرفع المنفجرين المُسلَّمين ⇒
  يكون قد اشترى «توازنًا» بثمن التسليم.
- **والعدميّة الشاملة** لو لم تتجاوز أيُّ ذراعٍ **K1** ⇒ **لا يوجد ما يُرتَّب به**،
  ويُغلَق محور الترتيب كما أُغلق محور الاختيار.
