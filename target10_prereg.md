# 🎯 T-TARGET10 — «تجربةُ الهدف»: هل هدفُ ربحٍ ‏+10% يُحسّن الحصيلةَ المحقَّقة؟

**تسجيلٌ مسبق — يُدفَع قبل أيّ رقم، ولا معيارَ يُحرَّك بعده.**
أمرُ المالك (2026-08-26): «**سجل تجربة الهدف** و في الوقت اللي تنتظر فيه النتايج
سو لي عصف ذهني».

**وأصلُ الرقم من قياسٍ سابق لا من اختراع:** في قياس «القوي هل فيه خاسرة؟»
(‏2026-08-25، جلسات 08-18→08-22) ظهر أن قاعدة «أمسِك بلا هدفٍ حتى الخروج
البنيويّ» تُخسر 86% من صفقات «قوي»، وأن **هدفَ ‏+10% كان أكبرَ رافعةٍ مقيسة**
(‏+8.5 نقطة على «قوي») — لكنّ ذلك القياس **أسبوعٌ واحد**. هذي التجربة تحكمه
على **ثلاث سنوات** قبل أن يُبنى عليه شيء.

---

## ⓪ السؤالان

- **§A (الحاكم):** لمركزٍ يُفتح عند **سعر كرت M5** ويُدار بالخروج البنيويّ
  القائم (أوّلُ إغلاقِ دقيقةٍ دون قاع شمعة المِرساة)، هل إضافةُ **هدفِ بيعٍ
  ‏+10%** تُحسّن التوقّع بوحدة المخاطرة `R`؟
- **§B (وصفيٌّ مرافق — مادّةُ العصف الذهنيّ):** المتحرّكون الكبار (‏+30% فأكثر
  بعد كرت M5) — كم نسبةُ مَن يَسِمُهم التصنيفُ الحاليّ «قوي»؟ وأيُّ محورٍ بديل
  (‏J1 · أقوى خليّة · فجوة فوق 75%) يلتقط منهم أكثر؟ (حالة `$DAIC` ‏+583%
  بتصنيفٍ غير قوي هي الشاهدُ الحيّ الذي أطلق السؤال.)

---

## ① البياناتُ والكون — مقياسٌ واحدٌ لا اثنان

- المصدر: ملفّات Polygon المجمّعة `minute_aggs` (تشمل المشطوبين = PIT بالبناء).
- **السنواتُ الثلاث: 2023 · 2024 · 2025** — والحكمُ عبرها لا عبر سنة.
- الأنبوبةُ كلُّها من `kasih_scan` **بالاسم** (‏`parse_day` · `prescreen` ·
  `first_anchor` [= `liq_stage_events` الإنتاجية بالنمط القانونيّ] · `resolve` ·
  `f2_usd5` · `GAP_CAP`=500 · كون [`PRICE_LO`,`PRICE_HI`] · حارسُ التغطية
  `coverage_verdict`) — **صفرُ منطقٍ مكرّر**، فتطابقُ عددِ المراسي مع المنشور في
  `kasih_result.md` لكلّ سنةٍ **بت-بت** بوّابةُ تكامُلٍ لا رفاهية.
- سعرُ الدخول `e5` بدلالة `tier_days_report.true_e5` **بالاسم** (سعرُ كرت M5
  حرفيًّا، **بلا قطعٍ عند الخروج البنيويّ** — علّةُ `entry_view` مدوَّنةٌ هناك).
- الخصائصُ (‏§B) من `kasih2_scan.k2_features` والفئةُ من `Super_stock.liq_tier`
  **حرفيًّا** والتوليفةُ من `Super_stock.kasih_j1` — نمطُ `tier_days_report` نفسُه.

## ② تعريفاتُ الأذرع — أربعُ أذرعٍ ولا خامسة

**الدخول (واحدٌ للجميع):** إغلاقُ الدقيقة الخامسة `e5` (كرتُ M5 — أوّلُ لحظةٍ
يصل فيها التصنيفُ للمالك). **شمعةُ الدخول نفسُها مستبعَدةٌ من التقييم**
(سابقة «رأسُ شمعة التعبئة مُستبعَد») ⇒ تُقيَّم الشموعُ التي بدايتُها
`anchor_ms + 5د` فصاعدًا حصرًا.

- **T0 (الأساس):** الخروجُ البنيويّ وحده — أوّلُ إغلاقٍ دون `anchor_low`؛
  وإن لم يقع فإغلاقُ آخر شمعةٍ في اليوم (`eod`).
- **T10 (الحاكمة — رقمُ المالك):** أمرُ بيعٍ حدّيّ عند `e5 × 1.10`. في كلّ
  شمعة **يُفحَص الوقفُ أوّلًا** (إغلاقٌ دون `anchor_low` ⇒ خروجٌ بالإغلاق ولو
  لمس رأسُها الهدف — قراءةٌ متحفّظة لغموض ترتيب اللمس داخل الدقيقة، سابقة
  `_max_gain_before_stop`)، ثم إن بلغ رأسُ الشمعة الهدفَ ⇒ خروجٌ **بسعر الهدف**
  (افتراضُ تعبئة أمر الحدّ — يُعلَن في حدود الصدق). وإلّا `eod`.
- **T20 · T30:** كالحاكمة بهدف ‏+20% و+30% — **سلّمٌ وصفيّ** يُثبَّت الآن، ولا
  ذراعَ تُضاف بعد الأرقام.

**الاستبعاداتُ المُعلَنة (تُعَدّ وتُسمّى ولا تُطوى):**
1. صفوفٌ كُسر قاعُها **قبل** اكتمال الخمس (`broke5`) — المقارنةُ بين سياسات
   خروجٍ لمركزٍ فُتح قبل أيّ إشارةِ خروج، وهذي دخولُها ورقيٌّ بعد خروجٍ واقع.
2. صفوفٌ بلا شمعةٍ بعد نافذة الخمس (لا `e5` مجمَّدًا أو لا شموعَ تقييم).
3. صفوفُ `R` غيرِ المُعرَّف (‏`e5 − anchor_low` ليس موجبًا) — تُستبعَد من
   تجميع `R` وحده وتُعَدّ.

**وحدةُ الحكم:** `R = (سعرُ الخروج − e5) / (e5 − anchor_low)` — المقياسُ الذي
لا يتأثّر بحجم التحرّك (أنقذ `T-STOP` و`T-LIBERATION` من قراءاتٍ مقلوبة).

## ③ المقاييس

لكلّ ذراعٍ في كلّ سنة: العدد · **متوسّط `R` (الحاكم)** · وسيطُ النسبة% ·
نسبةُ الصفوف الموجبة · نسبةُ تعبئة الهدف. وتفصيلٌ **وصفيّ** لكلّ فئةِ تصنيف
(قوي/متوسط/ضعيف بالتعريف النافذ `LIQ_TIER_STRONG_MIN=3`) — يُنشَر ولا يحكم.

## ④ معاييرُ الحكم (§A) — تُقرأ كما كُتبت

**بوّاباتُ الصلاحية (قبل أيّ تفسير):**
- `V4`: حارسُ تغطية `kasih_scan.coverage_verdict` أخضرُ في كلّ سنة — وإلّا
  «عطبُ أداةٍ لا نتيجة» لتلك السنة.
- `V0`: للصفوف المؤهَّلة، وقوعُ كسرِ `T0` وسعرُه يطابقان `resolve` **بت-بت**
  (لصفوفٍ غيرِ `broke5` التعريفان متكافئان رياضيًّا) — أيُّ تفرّقٍ يُعَدّ
  وتفرّقٌ واحدٌ فأكثر ⇒ خروجٌ غيرُ صفريّ.
- تكامُل: عددُ المراسي الخامّ يطابق المنشورَ في `kasih_result.md` للسنة.
- أرضيّة: 150 صفًّا مؤهَّلًا للأذرع لكلّ سنةٍ فأقلّ منها «لا حكم».

**النجاح (كلُّها معًا):**
1. `متوسّط R(T10) − متوسّط R(T0)` **موجبٌ في السنوات الثلاث** (لا انقلاب).
2. الفرقُ مجمَّعًا **‏+0.10R فأكثر**.

استيفاءُ ①② ⇒ «استوفت». سقوطُ أحدهما ⇒ **فشلت** (وتُنشَر الأرقام كما هي).
السلّم `T20/T30` **وصفيٌّ لا يدخل الحكم** مهما كانت أرقامُه.

## ⑤ سقفُ النجاح — مُثبَّتٌ سلفًا

إن استوفت: **اقتراحٌ للمالك + سطرُ عرضٍ مقترَح على كرت M5** («🎯 هدف +10% =
$X») — **ولا أتمتةَ خروجٍ ولا أمرَ بيعٍ آليّ ولا مساسَ بأيّ عتبةِ إطلاقٍ أو
كتمٍ أو رقمٍ من أرقام المالك** (‏5% · $30 ألفًا · 3× · النبض 10%). وأيُّ
تحويلٍ لاحقٍ إلى قاعدةٍ منفَّذة = قرارُ مالكٍ صريح.

## ⑥ §B — جدولُ التقاط المتحرّكين (وصفيٌّ بالإعلان، مادّةُ العصف)

لكلّ صفٍّ مؤهَّل تُحسَب: الفئة (`liq_tier`) وعدُّ الخضراء · `J1` وأقوى خليّة
(`kasih_j1`) · سلّةُ الفجوة (`f5_bucket`) · صنفُ الخمس (`f2`) · **ومقياسا
الحركة كلاهما** (درسُ `HUIZ` المدوَّن): `mg5` **المقصوصُ** عند الخروج البنيويّ
(المقياسُ المنشور) و`mgday` **غيرُ المقصوص** (أقصى رأسٍ بعد الدخول حتى نهاية
اليوم مهما وقع) — والفرقُ بينهما يُطبَع لا يُدفَن.

«المتحرّك» = `mgday ≥ 30%` (و‏≥50% وصفيًّا). يُطبَع لكلّ سنة: توزيعُ الفئات
داخل المتحرّكين (**recall**) ونسبةُ المتحرّكين داخل كلّ فئةٍ ومحور
(**precision**) — بأرضية 40/سلة وإلّا «لا حكم» لتلك السلّة.

🔴 **دائريّةٌ مُعلَنة (نصُّ `tier_days_prereg` §⓪ يسري هنا):** الفئةُ تُعاد
بناؤها بأثرٍ رجعيّ بالتعريف النافذ اليوم، والمحاورُ محسوبةٌ من نافذة المِرساة
نفسِها ⇒ §B **وصفُ تغطيةٍ لا إثباتُ تنبّؤ**، وأيُّ تغييرِ تصنيفٍ يُبنى عليه =
تسجيلٌ مسبقٌ جديد + قرارُ مالك.

## ⑦ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر نتائجُها كما تقع)

- **TP1:** `T10` تتفوّق على `T0` في السنوات الثلاث، والفرقُ المجمَّع بين
  ‏+0.05 و+0.30R. (يُكذّبه أن يلتهم قصُّ الرابحين الكبار المكسبَ.)
- **TP2:** نسبةُ تعبئة هدف ‏+10% بين 20% و40% من الصفوف المؤهَّلة.
- **TP3:** `متوسّط R(T30)` أدنى من `متوسّط R(T10)` مجمَّعًا (سابقة `T-EXIT`:
  الهدفُ الأبعد يهدم).
- **TP4 (§B):** حصّةُ «قوي» من المتحرّكين ‏+30% **دون 40% في كلّ سنة**
  (ظاهرةُ `DAIC` — والأخواتُ أعطت 25.6% على أسبوع).
- **TP5 (§B):** التقاطُ `J1` للمتحرّكين أعلى من التقاط «قوي» في كلّ سنة.

## ⑧ حدودُ صدقٍ تُقرأ مع الأرقام لا بعدها

1. «تعبئةُ الهدف» افتراضُ أمرِ حدٍّ يُملأ عند لمس الرأس — **بلا انزلاقٍ ولا
   حجمٍ** ⇒ أرقامُ أذرع الهدف **سقفٌ متفائل**؛ و`T0` تُقارَن بالافتراض نفسِه
   (الإغلاقُ سعرَ خروجٍ) فالمقارنةُ متّسقة.
2. غموضُ ترتيب اللمس داخل الدقيقة محسومٌ **ضدّ** أذرع الهدف (الوقفُ أوّلًا).
3. مِرساةٌ واحدة/رمز/يوم · يومُ المِرساة وحده · بلا ما بعد 20:00 نيويورك.
4. الفئةُ المُعادُ بناؤها **ليست ما رآه المالك موسومًا** (‏`k2` الحيّ شُحن
   2026-08-20).
5. ✂️ تنويهُ القصّ الإلزاميّ (`MEASURE_CUT_NOTE`) يسري على `mg5`؛ ولأجله
   يُحسَب `mgday` جنبَه في §B.
6. الحكمُ على **هذي الرافعة وحدها** (هدفٌ فوق الوقف البنيويّ القائم) — لا على
   «أفضل سياسة خروجٍ مطلقًا» (الوقفُ المتحرّك والخروجُ الجزئيّ خارج التسجيل).

## ⑨ الأداةُ والتشغيل

`target10_arms.py` + `.github/workflows/target10.yml` (يدويٌّ حصرًا · بلا كرون
· قراءةٌ فقط: لا تلغرام ولا كتابةَ حالةٍ والإنتاجُ لا يستورده). الأذرعُ الأربع
تُحسَب على **الصفوف نفسِها في تمريرةٍ واحدة** ⇒ ميزانيةٌ واحدةٌ بالبناء
(قاعدةُ `faisal-gates §⑤`). المخرَج: `target10_rows_{السنة}.jsonl` + جداول.
