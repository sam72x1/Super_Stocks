# 🥇⑦ T-RANKER-TIE — تسجيلٌ مسبق (يُدفَع **قبل** أيّ رقم)

> **أمرُ المالك (2026-08-06):** «⑦ فعله لو تشوفه ايجابي».
> **وهذا الملفُّ مدفوعٌ قبل حساب أيّ ترتيبٍ بديل** — أيُّ رقمٍ يظهر بعده يُقرأ على
> معياره المكتوب هنا، لا على معيارٍ يُختار بعد رؤية النتيجة.

---

## ① لماذا الآن — والهدفُ محدَّدٌ لا عامّ

`T-REPLAY10` قاست المُرتِّب الحاليّ **عند مستوى الصدفة** (③ غير حاسم · وفي 2024 أسوأُ
من العشوائيّ وFIFO يتفوّق). وكان ذلك حكمًا عامًّا بلا موضعٍ محدَّد.

**والقياسُ الحيّ (`faisal_only_live.md`، جلسة 2026-08-05) حدّد الموضع بدقّة:**
من 109 مرشّحًا يقبلهم ظرفُ فيصل، **‏16 سهمًا يتعادلون عند جاهزية 70**، والسعةُ 10 ⇒
**حدُّ القطع يقع داخل التعادل**: ستّةٌ يصلون وعشرةٌ يُقصَّون، والفاصلُ **النقاطُ ثم
`rr`**. أي أن **عشرةَ أسهمٍ من فئةٍ واحدة يقرّر مصيرَها كاسرُ تعادلٍ لم يُختبَر قطّ.**

⇒ **الهدفُ هنا ليس «تحسينَ المُرتِّب» عمومًا — بل كاسرُ التعادل وحده.**

## ② الذراعُ المُقترَح — ومن أين جاء (لا اختراع)

🔒 **قاعدةُ المالك: الرقمُ من الكاتالوج لا من عندي.** ولذلك **لا أُدخل معيارًا جديدًا**
ولا وزنًا من رأسي. الذراعُ الوحيد المسموح:

**`R-ENV` — الأقربُ إلى وسط ظرف فيصل أوّلًا.**
لكلِّ معيارٍ من الأحد عشر، موقعُ السهم **داخل حدود الظرف نفسه** (‏`envelope_p90.json`،
بصمة `b4e5372075c1`) مُطبَّعًا إلى `[0,1]`، ثم **الوسيطُ** عبر المعايير المتاحة.
- الحدُّ من نوع `lo` (أرضية): الأبعدُ فوق الأرضية = أعمقُ داخل الظرف.
- الحدُّ من نوع `hi` (سقف): الأبعدُ تحت السقف = أعمقُ داخل الظرف.
- الحدُّ `both` (نطاق): المسافةُ إلى **منتصف** النطاق (الأقربُ أعمق).
- معيارٌ غائب ⇒ **يُتخطّى** (لا يُخمَّن) · وأقلُّ من 6 معايير متاحة ⇒ **الذراعُ يمتنع**
  عن ذلك السهم ويُبقيه بترتيبه الحاليّ.

**ومصدرُ كلِّ رقمٍ فيه حوافُّ فيصل المُعايَرة من أسهمه** — صفرُ ثابتٍ من عندي.

## ③ أين يُطبَّق بالضبط — ولا يُطبَّق في غيره

**كاسرُ تعادلٍ فقط**: يُستعمل **حصريًّا** حين تتساوى `readiness` بين سهمين، وقبل
`score`/`rr`. **ولا يمسّ:**
- ترتيبَ من يختلفون في `readiness` (المفتاحُ الأوّل كما هو).
- العضويةَ في البِركة (البوّاباتُ لا تُمَسّ).
- الدخولَ ولا الوقفَ ولا الأهداف.

⇒ أسوأُ حالةٍ ممكنة: **إعادةُ ترتيبٍ داخل فئةٍ واحدة** — لا إدخالَ سهمٍ مرفوض.

## ④ المعيار — مثبَّتٌ قبل الأرقام

المقياسُ **`R` المحقَّق** بأداة `replay10` الأمينة (سعةٌ من `WATCHLIST_SIZE` الحيّ ·
بلا نظرٍ مستقبليّ · تحرير الخانة بالجلسات).

**يُعتمَد `R-ENV` إن استوفى الأربعة معًا:**
1. **الفرقُ المزدوج داخل الجلسة** (نفسُ البِركة · نفسُ اليوم · نفسُ السعة) موجبٌ
   في **السنوات الثلاث** بلا انقلاب.
2. الفرقُ المجمَّع **‏≥ +0.10R** لكلّ صفقة.
3. الفاصلُ 95% (‏block bootstrap عنقوديٌّ بالرمز) **لا يلمس الصفر**.
4. **‏≥ 30 صفقةً متأثّرة** (أي وقع فيها تعادلٌ فعليّ) في كلّ سنة.

**وإلّا: لا يُعتمَد.** و«لا حكم» تُعلَن إن سقط الشرط 4 وحده.

## ⑤ شواهدُ الضبط — إلزامية

- **`R-0`**: المُرتِّب الحاليّ (‏score ثم rr).
- **`R-RAND`**: كاسرُ تعادلٍ **عشوائيٌّ حتميّ** بالبذرة (200 خلطة). ⇒ إن لم يتفوّق
  `R-ENV` على العشوائيّ فهو **زينة**.
- **`R-FIFO`**: الأقدمُ أوّلًا (شاهدٌ بسيط تفوّق على المُرتِّب في 2024).

## ⑥ ⚖️ الميزانيةُ الثابتة — شرطُ صلاحيةٍ لا تفصيل

قاعدةٌ أُضيفت اليوم من `karpathy/autoresearch` بعد أن أفسد فقدُها نتيجةَ `T-CLIFF`:
**كلُّ الأذرع تعمل على نفس اللقطة · نفس الكون · نفس السعة · نفس النافذة · نفس
سقوف الجلب** — وتُطبَع هذي القيودُ في مُخرَج كلّ ذراع. **أيُّ ذراعٍ يخالفها تُلغى
نتيجتُه** ولا تُفسَّر.

## ⑦ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر ولو كُذِّبت)

- **`P1`** التعادلُ عند `readiness` شائعٌ لا نادر: **‏≥ 20%** من الأيام يقع فيها
  حدُّ القطع داخل تعادل. *(لو كُذِّب فالذراعُ بلا مادّة، وتُغلَق التجربة.)*
- **`P2`** `R-ENV` يتفوّق على `R-RAND` — وإلّا فلا معنى لـ«القرب من ظرف فيصل».
- **`P3`** الفرقُ **أصغرُ من 0.10R** (أتوقّع سقوطَ الشرط 2). سببي: `T-REPLAY10` قاست
  قيمةَ **الترتيب كلِّه** بـ‏+5.0R في ثلاث سنوات، وكاسرُ التعادل **جزءٌ منه** فأثرُه
  أصغر بالبناء.
- **`P4`** عددُ الصفقات المتأثّرة **يستوفي** الثلاثين في كلّ سنة (المرشّحون كثيرون
  والجاهزيةُ مضاعفاتُ 5 فالتعادلُ متكرّر).

## ⑧ سقفُ النجاح — محدَّدٌ سلفًا

نجاحٌ كامل ⇒ **اقتراحٌ للمالك** بتبديل كاسر التعادل، مع `LOGIC_VERSION` مرفوع.
**ولا شيءَ يُغيَّر تلقائيًّا.** وفشلٌ أو «لا حكم» ⇒ يبقى الحالُ، ويُنشَر الرقم.

## ⑨ حدودُ صدقٍ تُقرأ مع أيّ نتيجة

انحيازُ البقاء غيرُ مقيس · تشويهُ التقسيمات · بلا افتر · و`h4_confirm`=0 حيًّا ⇒
المفتاحُ الثاني في `rank_key` **خاملٌ فعليًّا** فالتعادلُ يُحسَم اليوم بـ`score`/`rr`
وحدهما · والظرفُ **مُعايَرٌ على 25 رمزًا** فقربُ سهمٍ من وسطه ليس دليلَ جودةٍ مستقلًّا.
