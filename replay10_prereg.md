# 🔁 T-REPLAY10 — التسجيل المسبق (يُكتب ويُدفع **قبل أي رقم من الأذرع**)

> **الدافع:** المراجعة الخصومية المستقلة أثبتت أن **محاكي المحفظة الحالي فيه نظرٌ
> مستقبليّ** (يستبعد `no_fill` قبل السعة) و**سعتُه 15 بينما الإنتاج 10** و**يحرّر الخانة
> بأيامٍ تقويمية لا جلسات**. ⇒ **كلُّ استنتاجٍ محفظيّ ورُتبيّ مُعلَّق** حتى تُبنى أداةٌ
> أمينة. **هذي هي.** (‏`CLAUDE_REPLY_TO_CODEX.md`)
>
> 🔒 **بحث/قياس — صفر مسّ إنتاج · لا `LOGIC_VERSION` · سقف النجاح: أرقامٌ واقتراح.**
> **الحالة عند الكتابة: صفر رقمٍ من أيّ ذراع.**

---

## ① ما عاينتُه قبل الكتابة — **يُعلَن للشفافية**

عاينتُ **توفّر البيانات** فقط (لا نتائج أذرع)، ووجدتُ:
- **‏57 لقطة** لـ`weekly_watchlist.json` في git · **‏13 يومًا مميّزًا** (‏2026-07-15 → 07-31)
  · **‏12 تغيّر عضوية · 22 إضافة · 17 إزالة**.
- 🔴 **وحجم القائمة الحيّة يبلغ 11 و12 و13 اسمًا** رغم أن `WATCHLIST_SIZE = 10`.
  ⇒ **يؤكّد ملاحظة المراجِع:** العشرة **سقفُ اختيارٍ جديد** لا حجمُ الحالة الكليّ —
  و**المحمولون** (`continues`/`exited`) يرفعون الإجمالي فوقه. **وهذي دلالةٌ تدخل
  التصميم أدناه، لا نتيجةً للتجربة.**

---

## ② الأداة — **مواصفات مثبَّتة الآن**

**آلة الحالة `replay10` تُبنى نقيّةً (بلا I/O) وتلتزم بالآتي حرفيًّا:**

| القاعدة | التنفيذ |
|---|---|
| **لا استبعاد مسبق** | **كلُّ مرشّحٍ مختار يحجز خانةً من تاريخ إشارته** — سواء تعبّأ لاحقًا أم لا. **هذا إصلاح `P0-01`.** |
| **السعة** | **‏10 = سقف الاختيار الجديد** لكل يوم · **المحمولون لا يُحتسبون ضدّها** (مطابقةً للحيّ) |
| **تحرير الخانة** | **الوقف ⇒ يُزال** · **بلوغ الهدف ⇒ يحرّر الخانة ويبقى الاسم متابَعًا** (قاعدة الإنتاج المنصوصة: «الهدف المُحقَّق لا يُنهي المتابعة ولا يحجز خانة») · انتهاء النافذة ⇒ يُزال |
| **الزمن** | **جلسات تداول** من فهرس اللقطة — **لا `ordinals` تقويمية.** إصلاح `P0-02` |
| **المسح** | **يوميّ** (كل جلسة)، لا قفزٌ بـ`BACKTEST_STEP` |
| **لا تكرار** | لا خانتان لرمزٍ واحد متزامنًا |
| **المُرتِّب** | **الفعليّ:** `readiness → h4_confirm → score → rr`. ⚠️ **و`h4_confirm` يساوي صفرًا تاريخيًّا وحيًّا** (يُحسب في `enrich` بعد `select_top` — موثَّق منذ 2026-06-24) ⇒ **يُعلَن، ولا يُدَّعى تمثيلُه** |
| **العوائد** | **من محرّك التنفيذ نفسه** (`ret_a` بذراع الوقف الأساسية) — **لا إعادة بناءٍ اسميّة.** إصلاح `P0-05` |
| **M13/M14** | 🔴 **غير مطبَّقتين تاريخيًّا** (لا تُستدعيان في `backtest_symbol`) ⇒ **تُسمّى العيّنة صراحةً «مرشّحون تاريخيون من `analyze_ticker` — قيود النشر غير ممثَّلة»** |

---

## ③ 🚦 بوّابة الصلاحية — **إلزامية، وتسبق قراءة أيّ ذراع**

**تُفكَّك إلى بوّابتين، لأن خلطهما هو الخطأ نفسه الذي وقعنا فيه:**

### **البوّابة أ — أمانة آلة الحالة** (حاكمة)
تُغذّى الآلة بـ**تيّار الأحداث المرصود من التاريخ الحيّ نفسه** (الإضافات/الإزالات/الأهداف
كما وقعت في لقطات git)، ثم **يجب أن تُعيد إنتاج العضوية اليومية المرصودة**.
**العتبة المسجَّلة: تطابقُ عضويةٍ يوميّ (‏Jaccard) ‏≥0.95 متوسطًا، وصفر مخالفةٍ في سبب
الإزالة.** ⇒ **إن سقطت، تُصلَح الآلة ولا تُقرأ أيّ نتيجة.**

### **البوّابة ب — أمانة الفارز** (تُبلَّغ، لا تحكم)
هل تُعيد إعادة الفرز على اللقطة المجمَّدة إنتاج اختيارات الأيام المتداخلة؟
🔴 **ولا تُجعَل حاكمة، لسببٍ مُعلَن:** اللقطة `as-of 2026-07-22` والتاريخ الحيّ يمتدّ إلى
‏07-31 ⇒ **التداخل ‏≈6 جلسات فقط** · وبيانات ياهو تُراجَع بأثرٍ رجعيّ. ⇒ **فشلُها قد
يعني اختلاف بيانات لا اختلاف منطق.** تُنشَر نسبتُها كما هي.

### ⚠️ **وحدُّ قوّةٍ يُعلَن الآن لا بعد النتيجة**
‏**13 يومًا و22 إضافة و17 إزالة عيّنةٌ صغيرة.** البوّابة أ تكشف **خطأً فادحًا** في آلة
الحالة، **ولا تُصادق على القواعد الدقيقة** (الحمل · التجديد الأسبوعي · حالات الحدود).
⇒ **عبورُها شرطٌ لازم لا كافٍ**، ويُكتب ذلك في النتيجة.

---

## ④ الأذرع — مثبَّتة الآن، لا خامسة لها

| الذراع | التعريف |
|---|---|
| **R0** | **المُرتِّب الفعليّ** (‏readiness → h4(=0) → score → rr) |
| **R1** | **FIFO** داخل اليوم (الأقدم إشارةً أولًا) |
| **R2** | **عشوائيّ** — **‏10,000 بذرة ثابتة**، والنتيجة توزيعُها لا عيّنةٌ منه |
| **R3** | **مؤجَّلة** — «سلامة التنفيذ فقط» تحتاج بيانات NBBO تاريخية point-in-time لم تُبنَ بعد. **تُعلَن مؤجَّلة، ولا تُضاف بعد رؤية الأرقام.** |

**السنوات: 2023 · 2024 · 2025** · **اللقطة: `30598885456`.**

---

## ⑤ المقياس — مثبَّت الآن

- **الأساسيّ: صافي R لليوم عند إجمالي مخاطرةٍ ثابت**، محسوبًا من **عوائد التنفيذ
  الفعلية للمحرّك** (`ret_a`). **وليس `win_rate` ولا `mg_pre_stop`.**
- **الثانويّ (يُنشَر ولا يحكم):** `mg_pre_stop` · إشغال الخانات · المرفوض بالسعة ·
  أقصى تراجع · CVaR · عدد المنفجرين المُسلَّمين.

## ⑥ الاستدلال — مثبَّت الآن

- **`block bootstrap`** بالكتلة الشهرية · **`cluster` بالرمز** · **‏10,000 عيّنة**.
- **`randomization inference`** داخل مجموعة متنافسي اليوم لمقارنة R0 بـR2.
- **لا اختبار بواسون ولا Bernoulli مستقلّ** (الأذرع تتشارك الأيام والرموز).

## ⑦ المعيار — **ثلاثي، ومعه هامش تكافؤٍ مسجَّل**

1. **تفوّق:** الحدّ السفليّ ‏95% لفرق «صافي R/يوم» بين **R0 وR2** ‏**> 0**،
   **وموجبٌ في السنوات الثلاث**.
2. **تكافؤ:** فاصل ‏90% للفرق **داخل ‏±0.05R/يوم** ⇒ **يُعلَن أن الترتيب لا يضيف**،
   وتُوقَف بحوث `score` بوصفه مُنتقيًا.
3. **غير حاسم:** ما عدا ذلك ⇒ **لا ادّعاء في أيّ اتجاه.**

🔑 **و`R1` (FIFO) شاهدٌ ثانٍ:** إن تفوّق FIFO على R0 فذلك **إشارةُ عطبٍ في المُرتِّب**
لا مجرّد غياب حافة.

---

## ⑧ التنبّؤات القابلة للكسر

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| **V1** | **البوّابة أ ستسقط من أول تشغيل** — قواعد الحمل/التجديد أعقد ممّا وثّقناه | عبورُها من أول مرّة ⇒ الوثائق أدقّ ممّا ظننت |
| **V2** | **R0 لا يتفوّق على R2** بالمعيار (‏سندُه: `rank_key` قِيس متذبذبًا حول بِركته) | تفوّقه ⇒ **أوّل قيمةٍ مُثبَتة للترتيب** |
| **V3** | **التكافؤ لن يُعلَن أيضًا** — الفاصل أوسع من ‏±0.05R بهذا الحجم ⇒ «غير حاسم» | ضيقُ الفاصل ⇒ الحجم كافٍ فعلًا |
| **V4** | **إصلاح `no_fill` يُنقص الأداء المقيس** للجميع (المزاحمون الفاشلون يشغلون خانات) | ارتفاعُه ⇒ أثرٌ عكسيّ يستحقّ تفسيرًا |
| **V5** | **حجم القائمة الحيّة سيتجاوز 10** في الإعادة أيضًا (بسبب الحمل) | ألّا يتجاوز ⇒ فهمي لقاعدة الحمل خاطئ |

🔴 **سجلّي اليوم: سحبتُ خمس صياغات وثلاثة ادّعاءات.** فاحتمال كسر هذي عالٍ.

---

## ⑨ حدود الصدق (تُعلَن قبل النتيجة)

1. **انحياز البقاء** · **تشويه التقسيمات** · **الشمعة اليومية بلا افتر**.
2. **البوّابة أ ضعيفة القوّة** (‏13 يومًا) — شرطٌ لازم لا كافٍ.
3. **`M13`/`M14` غير ممثَّلتين تاريخيًّا** ⇒ العيّنة أوسع من قائمة الإنتاج.
4. **`h4_confirm` = 0** ⇒ المُرتِّب المُعاد ثلاثيّ الفعل لا رباعيّ.
5. **‏`ret_a` يفترض تعبئةً كاملة عند متوسط الدفعات** — عيبٌ معروف (`P1-08`) **لا يُصلَح
   في هذي التجربة**، ويُعلَن أنه مشترَكٌ بين كل الأذرع فلا يحيز المقارنة بينها.
6. **لا تُقارَن أرقام هذي التجربة بأرقام ما قبلها** — الأداة مختلفة، والقديم مُعلَّق.

## ⑩ سقف النجاح

**أرقامٌ + اقتراحٌ واحد للمالك.** **لا تغيير إنتاجيّ · لا مسّ بالجذور التسع · لا
`LOGIC_VERSION`.** وأيّ تعديلٍ لاحق يمسّ العضوية = **قرار مالكٍ حصريّ**.
