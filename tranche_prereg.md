# `T-TRANCHE` — «سجّل تجربة الدفعات» (تسجيلٌ مسبقٌ **قبل أيّ سطرِ كودِ أداة**)

> **أمرُ المالك (2026-08-29):** «سجل تجربة الدفعات».
> **وأمرُه «سجّل» لا «اعتمد»** ⇒ سقفُ النجاح **قياسٌ وتوصية**، والشحنُ كلمةٌ منه (§⑧).

---

## ⓪ لماذا هذي التجربة مشروعةٌ وليست «استيرادَ نظامٍ آخر»

قاعدتُنا المدوَّنة تمنع نقلَ قاعدةٍ من نظامٍ إلى نظام (حادثة `trigger_state`).
**وهنا لا نقلَ إطلاقًا** — الجملةُ مصدرُ القرار **واحدةٌ بحرفها**:

> «‏1.50 قاع **دخوله طلبات من 1.60 ل 1.70** · **وقفه قاعه**» (`faisal_verbatim`)

🔴 **وقد شحنّا نصفَها يوم 2026-08-29 وتركنا نصفَها:** «وقفه قاعه» نافذٌ الآن،
و«طلبات فوق القاع» ليست. **والحالةُ الناتجة أسوأُ من الطرفين:** الدفعاتُ تبدأ
**عند المِرساة بالضبط** والوقفُ **عند المِرساة بالضبط** ⇒ **أدنى دفعةٍ = الوقف**
⇒ أمرٌ يُعبَّأ عندها يكون في لحظة وقفه.

⇒ **السؤالُ ليس «هل نستورد؟» بل «هل نُكمل الجملةَ التي شحنّا نصفَها؟»**
⚖️ **ومع ذلك لا يُشحن بالرأي:** مقدارُ الإزاحة **ليس رقمًا فيصليًّا واحدًا**
(§⑦-1) ⇒ يُقاس.

---

## ① السؤال

> على مرشّحي الفارز وبالوقف المعتمَد (‏= المِرساة)، هل **رفعُ سلّم الدفعات فوق
> المِرساة** أفضلُ من بدئه **عندها** — بوحدةِ مخاطرةٍ **واحدةٍ للأذرع كلِّها**؟

---

## ② المجتمعُ والمحرّك — صفرُ مسٍّ بالإنتاج

- **المجتمع:** لقطاتُ PIT الثلاث (‏2023 `31574165523` · 2024 `31199493957` ·
  2025 `31191327731`) بإعدادات الإنتاج (`FAISAL_ONLY=1`).
- **المحرّك:** صفقاتُ `backtest_symbol` الإنتاجية، ثم تُعاد خطّةُ كلّ صفقةٍ
  بنداء `analyze_ticker` **عند فهرسها نفسِه** (لا من الحقول المدوَّرة) —
  نمطُ `pivot_stop_arms.plan_at` حرفيًّا.
- **الحسم:** `_resolve_arm` **الإنتاجيّة بالاسم** على شموع النافذة نفسِها،
  والتعبئةُ بقاعدة الإنتاج (`lo[k] <= entry` حيث `entry` = **متوسّطُ** الدفعات).
- 🔒 **`Super_stock.py` لا يُمَسّ بحرف** — ولا علمَ جديد ولا عتبة. الإزاحةُ
  تُطبَّق **داخل الأداة** على مُخرَج `analyze_ticker` بصيغة الإنتاج نفسِها:
  `[round(anchor * (1 + off + step*i), 2) for i in range(n_tr)]`
  بـ`ENTRY_TRANCHES`=3 و`ENTRY_STEP_PCT`=3.0 **كما هما**.
- ⛔ **`ENTRY_STEP_PCT` لا يُمَسّ** (المتغيّرُ **بدايةُ** السلّم لا خطوتُه —
  وخطوتُنا 3.0 تقع بين خطوتَي مثالَي فيصل: ‏2.17% و3.33%).

---

## ③ الأذرعُ أربعٌ ولا خامسة (مثبَّتةٌ الآن — إضافةُ ذراعٍ بعد الأرقام ممنوعة)

| الذراع | إزاحةُ بداية السلّم | الدفعات على مِرساةٍ 1.50 | الدور |
|---|---|---|---|
| **`P0`** | **‏0.0%** (الإنتاج) | 1.50 · 1.54 · 1.59 | المرجع |
| **`P1`** 🥇 | **‏+2.0%** | 1.53 · 1.58 · 1.62 | **الحاكمة** |
| **`P2`** | ‏+5.0% | 1.58 · 1.62 · 1.67 | وصفيّة |
| **`P3`** | ‏+7.0% | **1.60 · 1.65 · 1.70** | وصفيّة |

🔎 **وملاحظةٌ تُدوَّن (تصحيحُ حسابٍ في المثال التوضيحيّ لا تغييرَ ذراع — أمسكه قفلُ `TRN2` قبل أيّ رقم):** بمِرساةِ 1.50 يُنتج `P3` **‏1.60 · 1.65 · 1.70** = **مثالُ فيصل المنصوص حرفيًّا** «‏دخوله طلبات من 1.60 ل 1.70». ⛔ **ولا يُرقّى `P3` بذلك إلى الحكم** — الحاكمةُ `P1` كما سُجِّلت قبل أيّ رقم، وترقيتُه الآن اختيارٌ بعد رؤية سببٍ يحابيه.

⚖️ **ولماذا `P1` هي الحاكمة — سببٌ يُكتَب قبل الأرقام لا يُختار بعدها:**
**«أصغرُ حلٍّ يحلّ المشكلة فعلًا أولى»** (معيارُ التشغيل). المشكلةُ المشحونة
أن أدنى دفعةٍ **تساوي** الوقف؛ و‏2.0% أصغرُ إزاحةٍ **تفصلهما فصلًا ينجو من
التدوير بخانتين عند أرضية سعرنا** (‏$1.65 ⇒ 3 سنتات) **وتقع فوق إزاحةِ مثال
`AMIX` المنصوص (‏+1.09%)**. و`P2`/`P3` تحصران بقيّةَ مداه المنصوص (‏+6.67%)
**ولا تُرقّى إلى الحكم مهما كانت أرقامُها** (سابقةُ `T-CLIFF` §⑦).

---

## ④ المقياسُ — وحدةُ مخاطرةٍ **واحدة**، ومعها قراءةٌ ثانية تُنشَر ولا تحكم

🔴 **الفخُّ المقيس** (`T-RECLAIM-INTRADAY`): ذراعٌ تُقاس **بوحدتها هي** تبدو
أفضلَ ممّا تعنيه للمحفظة. ⇒ **الوحدةُ الحاكمة:**

> **`R₀ = entry(P0) − stop`** — وحدةُ مخاطرة **الإنتاج**، تُقسَم عليها عوائدُ
> الأذرع الأربع.

- **المقياسُ الحاكم:** متوسّطُ `R₀` لكلّ صفقة.
- **المساندُ (يُنشَر ولا يحكم):** نسبةُ الربح · العائدُ المحقَّق ٪ · **نسبةُ
  التعبئة** · و**`R` بوحدة كلّ ذراعٍ** موسومًا **«غيرُ قابلٍ للمقارنة»**.

🔴🔴 **وحدُّ صدقٍ يعلو المقياس ويُكتَب قبل أيّ رقم:** الوحدةُ الثابتة **تعاقب
الإزاحةَ الأوسع بنيويًّا** (مخاطرةُ `P3` بالسهم ‏≈3.3× مخاطرةَ `P0` فوقفُها
يكلّف ‏≈3.3R₀ بينما يكلّف بوحدتها هي ‏−1R دائمًا). ⇒ **إن سقطت الأذرعُ الواسعة
فقد يكون ذلك أثرَ الوحدة**، ولذلك:
- **تُنشَر القراءتان معًا** (ثابتةٌ + بوحدة كلّ ذراع).
- **وإن اختلف حكمُهما فالنتيجةُ تُنشَر «مختلطة» ولا تُوحَّد** (سابقةُ `T-COUNT`).
- **والحكمُ الرسميّ على الثابتة وحدَها** كما سُجِّل هنا.
⚖️ **وليست التجربةُ فارغةً بهذا العقاب:** رفعُ السلّم **يزيد التعبئة** (الأمرُ
أقربُ للسعر) و`no_fill` = صفرُ عائدٍ في `P0` ⇒ السؤالُ الحقيقيّ: **هل تُعوّض
الصفقاتُ الإضافية غلاءَ الدخول وكِبَر المخاطرة؟**

**تُوصى `P1` إن استوفت الأربعةَ معًا:**
1. `P1 − P0` بـ`R₀` يبلغ **‏+0.15** فأكثر مجمَّعًا (**رقمٌ مُعادٌ حرفيًّا** من
   `T-STOP`/`T-LIBERATION`/`T-RECLAIM-INTRADAY`/`T-PIVOT-BOTTOM` — **ولا
   يُحرَّك بعد رؤية الأرقام مهما قارَبه**).
2. **موجبٌ في السنوات الثلاث** بلا انقلاب.
3. الفاصلُ 95% للفرق المجمَّع **لا يلمس الصفر** (بوتستراب عنقودُه **الرمز** —
   `replay10.cluster_bootstrap_ci` بالاسم).
4. **الأرضية:** ‏100 صفقةٍ محسومةٍ فأكثر لكلّ ذراعٍ في كلّ سنة (‏رقمٌ **مُعادٌ**
   من `T-EXIT`).

---

## ⑤ بوّاباتُ الصلاحية — **منفَّذةٌ في الكود** لا موصوفة

| البوّابة | الشرط | عند السقوط |
|---|---|---|
| **`TV0`** | إعادةُ حسم `P0` بـ`_resolve_arm` تُطابق `outcome`/`ret_a` المخزَّنَين **بت-بت** | **خروج 3** |
| **`TV1`** | سلّمُ `P0` **يطابق دفعاتِ الإنتاج بت-بت** (وإلّا فالمقارنةُ ضدّ أساسٍ مفبرَك) | **خروج 3** |
| **`TV2`** | كلُّ ذراعٍ **تتفرّق** عن `P0` في كلّ سنة (وإلّا `no-op`) | **خروج 4** |
| **`TV3`** | المِرساة (`pivot`) حاضرةٌ في **‏95%** من الصفقات فأكثر | **خروج 3** |
| **`TV4`** | **الوقفُ أدنى من أدنى دفعةٍ في كلّ ذراع** — والمخالفُ يُعَدّ ويُستبعَد ويُعلَن (وفي `P0` هو **مساوٍ** بالبناء ⇒ يُعَدّ ويُعلَن ولا يُستبعَد) | يُطبَع |
| **`TV5`** | الأرقامُ الأساسية تُعيد إنتاج المنشور (`pivot_stop_result §①`: `B1` ‏−0.0906/−0.0621/−0.0715) لسنةٍ واحدة على الأقلّ | يُطبَع |
| **`TV6`** | **صفرُ كتابةٍ للحالة وصفرُ إرسال** (بالـAST) · والإنتاجُ لا يستوردها | **خروج 3** |

---

## ⑥ التنبّؤات (قابلةٌ للكسر — وأوّلُها يراهن **ضدّ** التغيير)

- **`TP1`** (الأثقل): أرجّح أن `P1 − P0` بـ`R₀` **دون +0.15** وربّما **سالب** —
  لأن `t1` ثابتٌ فرفعُ الدخول **يُنقص المكافأة حتمًا** ويزيد المخاطرة، والوحدةُ
  الثابتة لا تكافئ ذلك. ⇒ **إن عبرت فآليّتي التي بنيتُ عليها خاطئة.**
- **`TP2`**: نسبةُ التعبئة **ترتفع رتيبًا** مع الإزاحة (`P0` < `P1` < `P2` < `P3`)
  — شبهُ ميكانيكيّة، **تُعلَن كذلك ولا تُقرأ نجاحًا**.
- **`TP3`**: نسبةُ الربح **ترتفع** مع الإزاحة بينما `R₀` **لا يرتفع معها** =
  نمطُ `T-STOP`/`T-LIBERATION`/`T-PIVOT-BOTTOM` (نسبةُ الربح مقياسٌ يخدع).
  ⇒ **إن لم ترتفع نسبةُ الربح فنموذجي الميكانيكيّ مكذَّب.**
- **`TP4`**: `no_fill` تحت `P0` **فوق 25%** من الصفقات.
- **`TP5`**: القراءتان (ثابتة/بوحدة كلّ ذراع) **تختلفان في الإشارة** لـ`P3`.
- **`TP6`**: عددُ الصفقات التي ينزل `rr` فيها تحت `MIN_RR_T1` تحت `P3`
  **‏20% فأكثر** ⇒ شحنُ `P3` يُبدّل العضويّة ماديًّا.

---

## ⑦ حدودُ صدقٍ مكتوبةٌ قبل أيّ رقم

1. 🔴 **مقدارُ الإزاحة ليس رقمًا فيصليًّا واحدًا:** مثالاه المنصوصان يعطيان
   **‏+1.09%** (`AMIX`: وقف 4.60 · دفعات 4.65/4.75/4.85) و**‏+6.67%**
   (‏1.50 ⟶ 1.60) ⇒ **الأذرعُ الثلاث `engineering` تحصر مداه**، والمنصوصُ عنده
   **«الطلباتُ فوق القاع والوقفُ القاع»** لا المقدار. يُوسَم في دفتر المصادر.
2. **`rr` والعضويّةُ غيرُ قابلتين للتقييم بهذي الأداة** (نفسُ `pivot_stop_prereg`
   §⑨): `rr` **نقصٌ لينٌ** لا رفضٌ صلب، فتبدُّلُ العضويّة يعتمد عدّادَ النواقص
   كلَّه ولا يُستخرَج من صفقاتٍ مخزَّنة. ⇒ **يُوصَف حدُّه الأدنى** (كم صفقةً
   ينزل `rr` فيها تحت الحدّ) **ولا يُقاس**، و`TP6` يُقرأ وصفًا لا حكمًا.
3. **أثرُ التسليم غيرُ مقيس:** رفعُ الدفعات يرفع سقفَ نطاق الدخول
   (`in_entry_band` يقرأ `max(tranches)`) ⇒ **يزيد مَن يُوسَم «🟢 جاهز»** —
   وهذا **تغييرُ إشعارٍ** لا يقيسه الباكتيست إطلاقًا.
4. **التعبئةُ والوقفُ قد يقعان في الشمعة نفسِها** (الوقفُ الآن عند المِرساة
   داخل منطقة حصاد السيولة 7-13% بلفظ فيصل) ⇒ يُعَدّ ويُعلَن لكلّ ذراع.
5. الحسمُ **بلمسةِ ذيلٍ بلا انزلاق** ⇒ الأذرعُ **كلُّها متفائلة**، والأوسعُ
   إزاحةً أقلُّ تفاؤلًا (وقفُها أبعدُ عن دخولها) ⇒ **الانحيازُ يحابي `P0`**
   ⇒ قراءةٌ متحفّظة تجاه التغيير.
6. `t1` **لا يتغيّر** ⇒ المقيسُ **مكانُ الدخول وحده**، لا سياسةُ الخروج
   (`T-EXIT` أعدمت الأهدافَ الأبعد · `T-MANAGE-25` أعدمت التمتيع بوصفة 75/25).
7. انحيازُ البقاء مخفَّفٌ باللقطة **وغيرُ منفيّ** · تشويهُ التقسيمات قائم ·
   وعمى الجلسة الممتدّة قائم (`T-AH`).
8. **الدائريّةُ منتفية** (الإزاحةُ ليست مشتقّةً من نتيجة).
9. 🔴 **والأثقلُ عمليًّا: النظامُ سالبُ التوقّع في الطرفين** (`B1` المشحونة
   ‏≈−0.09R). ⇒ **حتى لو عبرت `P1` فالمقيسُ «خسارةٌ أصغر» لا ربح**، ولا يُقرأ
   الجدولُ وعدَ عائد.

---

## ⑧ سقفُ النجاح — مثبَّتٌ سلفًا

**أمرُه «سجّل» ⇒ المُخرَجُ قياسٌ وتوصيةٌ مكتوبة.** وإن استوفت `P1` المعاييرَ
الأربعة **تُعرَض عليه بالقراءتين** ولا تُشحن إلّا بكلمةٍ منه — لأنها تمسّ
**خطّةَ الدخول والعضويّة والتسليمَ معًا** (`LOGIC_VERSION` + `force_renew`).

⛔ **وممنوعٌ مهما كانت الأرقام:** مسُّ `M1`-`M14` · أو الأهداف (`D5`) · أو
`ENTRY_TRANCHES`/`ENTRY_STEP_PCT` · أو الوقفِ المعتمَد · أو أوقافِ الأنظمة
الأربعة الأخرى · أو `ENTRY_READY_BAND_TOL_PCT` · أو ترقيةِ `P2`/`P3` إلى الحكم.

---

## ⑨ ملحقٌ مؤرَّخ 2026-08-29 — **قبل أيّ رقم**: عيبُ وصلٍ في أداةِ `T-PIVOT-BOTTOM`

عند وصل هذي الأداة انكشف قيدٌ **لم أرَه وقتَ كتابة العقد السابق**، فيُعلَن هنا
**قبل التشغيل لا بعده**:

**`r["pivot"]` ليس مِرساةَ الدخول.** في `analyze_ticker`:
- `pivot = min(lows[-PIVOT_LOOKBACK:])` بنافذة **‏25** (‏`pivot_stability`).
- **ومِرساةُ الدفعات والوقف `_anchor` = `tested_level(df)["level"]` بنافذة ‏30**
  (الوضعُ النافذ `tested_strict`).

⇒ **‏`tested ≤ pivot` دائمًا** (أدنى ثلاثين ≤ أدنى خمسٍ وعشرين)، والمساواةُ
حين يقع قاعُ الثلاثين داخل الخمس والعشرين.

🔴 **وأثرُه على المنشور:** `pivot_stop_prereg §①` نصَّ أن الذراعَ `B1` وقفُها
**«مِرساةُ الدخول نفسُها»** — **وأداتُها نفّذت `r["pivot"]`** ظنًّا أنه يحملها
(صنفُ «المفتاح المتخيَّل» في `wire-check §①`). ⇒ **`B1` قاست وقفًا عند قاعِ
الخمس والعشرين، والمشحونُ حيًّا وقفٌ عند مِرساة الدفعات (‏= `tranches[0]`).**

⚖️ **والاتّجاه معلومٌ بالبناء ويُقال صريحًا:**
`stop(B0) = _anchor×0.93` **<** `stop(المشحون) = round(_anchor,2)` **≤**
`stop(B1) = pivot` ⇒ **المشحونُ بين الذراعين وأقربُ إلى `B1`** ⇒ **مكسبُ
‏+0.0644R حدٌّ أعلى لِما شُحن لا وصفٌ له.**

🔒 **والمشحونُ هو الصحيح بالعقد لا الأداة:** وقفٌ عند `pivot` (وهو **فوق**
المِرساة في حالات التفاوت) كان سيضع **الوقفَ فوق أدنى دفعة** = الكارثةُ التي
يحرسها الضمانُ الذهبيّ. فالخللُ في **أداةِ القياس** لا في الشحن.

**⇒ ثلاثةُ تعديلاتٍ على هذا العقد، تُكتَب قبل أيّ رقم:**

1. **`P0` = خطّةُ الإنتاج المشحونة كما هي** (‏`analyze_ticker` بعلمِ وقف القاع
   **مُشغَّلًا**): دفعاتُه ووقفُه كما يراهما المالك اليوم. **فالمرجعُ واقعٌ لا
   إعادةُ بناء.**
2. **`TV5` يُستبدَل** (كان: «يُعيد أرقام `B1` المنشورة»): صار **يُعيد أرقام
   `B0`** بإجبار العلم مُطفأً — وهي **قابلةٌ للإعادة بت-بت** (‏−0.2010 ·
   −0.1243 · −0.0912) وتبقى فحصَ تكاملٍ حقيقيًّا للمحرّك.
3. **عدّادٌ جديدٌ إلزاميّ يُطبَع:** نسبةُ الصفقات التي **`pivot ≠ _anchor`**
   فيها، ووسيطُ الفجوة ٪ — **وهو ما يُحجّم انحرافَ `T-PIVOT-BOTTOM` بالرقم**
   بدل أن يبقى وصفًا. **ويُنشَر في `tranche_result.md` وفي بانرٍ مؤرَّخ على
   `pivot_stop_result.md`.**

⛔ **ولا يُغيَّر شيءٌ آخر:** الأذرعُ الأربعُ كما سُجِّلت · والوحدةُ الحاكمة ·
والحدُّ **‏+0.15R** · والأرضية · والتنبّؤاتُ الستّ. **ولا يُعاد تشغيل
`T-PIVOT-BOTTOM`** — حكمُها («لا تُوصى» ‏3 من 4) **لا يتغيّر بهذا** لأن الانحراف
يجعل المقيسَ **أفضلَ** من المشحون، والحكمُ كان سلبيًّا أصلًا.

---

## ⑩ ملحقٌ مؤرَّخ (2026-08-29) — عطبٌ في **أداتي** أوقف التشغيلات الثلاث قبل أيّ رقمِ ذراع

🔴 **الوقائع:** التشغيلاتُ الثلاث خرجت **بخروج 3** على بوّابة `TV1` وحدَها:

```
🔒 `TV1`: سلّمُ `P0` يطابق دفعاتِ الإنتاج في 1614 من 1620
⛔ `TV1` تفرّق — ADTH/2023-01-05: [1.5, 1.55, 1.59] مقابل [1.5, 1.54, 1.59]
```

⇒ **صفرُ رقمِ ذراعٍ صدر ولا رُئي** ⇒ إصلاحُ الأداة الآن **ليس تحريكَ بار** (لا نتيجةَ
بعدُ تُحابى)، والعقدُ §①-§⑨ **لم يُمَسّ بحرف**.

### السبب — مقيسٌ لا مُخمَّن

كنتُ أسترجع المِرساةَ **بالقسمة**: `anchor = rr_stop / (1 − s_hi/100)`.
والاسترجاعُ يُزيح القيمةَ **بوحدةِ ULP واحدة**، وحين تقع `anchor × 1.03` على حدّ
التدوير `.xx5` **بالضبط** تعبره الإزاحةُ فينقلب الرقم:

```
a₀ = 1.5                    ⇒ [1.5, 1.54, 1.59]   (الإنتاج)
a₁ = (1.5×0.93)/0.93        ⇒ [1.5, 1.55, 1.59]   (الاسترجاع)
a₀ == a₁ ⟵ False   ·   a₁ = 1.5000000000000002
```

**وهي حالةُ `ADTH/2023-01-05` حرفيًّا** — أُعيد إنتاجُها بسطرين.

### العلاج — الدالّةُ التي أنتجت المِرساةَ تُنادى بالاسم

`anchor_at(S, df_slice, pl)` تنسخ فرعَ الإنتاج (`Super_stock.py:3733-3742`):
`_anchor_mode` ⟵ فإن كان نافذًا فـ`tested_level` على **الشريحة نفسِها**، وإلّا
`pivot`. **وصفرُ قسمةٍ في الأداة كلِّها** (مقفولٌ بالـAST).

### 🧭 الدرسُ المُعمَّم (يُدوَّن لأنه يتكرّر)

> **لا تسترجع كمّيةً بالقسمة إن كانت الدالّةُ التي أنتجتها متاحةً بالاسم.**
> والاسترجاعُ **إعادةُ بناء** لا مصدر — وأثرُه يبقى مخفيًّا حتى يلامس حدَّ تدوير،
> فيصير فرقُ ULP **قرارًا**. (توسيعٌ لقاعدة «كلُّ رقمين يُقارَنان يُدوَّران بالدقّة
> نفسِها» — هنا الرقمان **واحدٌ** ومسارُ حسابه اثنان.)

### 🐞 وقفلي أنا كان أعمى بالصنف نفسِه

`TRN1` كان يستعمل **نفسَ الاسترجاع المعطوب** ليبني مرجعَه ⇒ **يستحيل عليه بنيويًّا أن
يمسك العيب** = «عيّنةٌ لا تفرّق» **داخل القفل** لا في الفِكستشر. وأثقلُ منه أن العيّنةَ
الصناعية `synth_pivot` **لا تنتج مِرساةً على حدّ التدوير إطلاقًا** ⇒ الطرفان يعطيان
الجوابَ نفسَه في 38 بذرة. ⇒ ثلاثةُ أقفالٍ بدل واحد:

| القفل | ما يحرسه |
|---|---|
| `TRN1` | المِرساةُ من **دالّة الأداة** التي تنادي الإنتاج — لا بالقسمة |
| `TRN1ب` | **بنيويّ (AST):** `anchor_at` تنادي `_anchor_mode` و`tested_level` · **صفرُ قسمةٍ على `rr_stop`** في الأداة |
| `TRN1ج` | **عيّنةٌ تفرّق:** حالةُ 1.50 الحيّة — سليمٌ 1.54 · مسترجَعٌ 1.55 |

✅ **ومُتحقَّقٌ أن العلاج يعمل في الفرعين:** `ANCHOR_MODE=""` ⇒ 10 من 10 مطابقة ·
`tested_strict` ⇒ 2 من 2 (والباقي مرفوضٌ بـ`M_لا_مستوى_مختبر` = سلوكُ الإنتاج).
