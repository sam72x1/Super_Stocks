# 🎚️📉 `T-TRAIL` — تسجيلٌ مسبق: **الوقفُ المتحرّك بدل سقفِ الربح**

> **مدفوعٌ ومدموجٌ قبل أيّ سطرِ أداةٍ وقبل أيّ رقم.** ولا تُضاف ذراعٌ ولا يُحرَّك حدٌّ
> ولا تُوسَّع عيّنةٌ بعد رؤية الأرقام. أمرُ المالك «سجّل الوقف المتحرّك» (‏2026-09-17)
> بعد حكم `T-OPTRADE` (‏`P0` = ‏−0.7000R · خروجٌ على ‏+10% بعد ‏13 دقيقةً وسيطًا).

---

## ⓪ إعلانُ التلوّث والسابقة — ما أعرفه قبل أن أقيس

**⓪-أ التلوّثُ مُعلَنٌ صريحًا:** رأيتُ حالتَي `AEMD` (‏2026-09-17) و`RETO` (‏2026-09-15)
بأرقامهما: `AEMD` مِرساةٌ ‏06:31 عند ‏$3.15 وأعلى إغلاقٍ بعدها **‏$9.22 (‏+192.7%)**
وسياسةُ `P0` تخرج على ‏+10% بإغلاق الدقيقة 12 · و`RETO` ‏+10% في دقيقةٍ واحدة
والإغلاقُ النظاميّ **‏+658%** والممتدّ **‏+1679%**. ⇒ **أعرف اتّجاهَ النتيجة على مجتمع
المراسي ولا أعرف مقدارَها** ⇒ **مجتمعُ المراسي `E-OP` لا يحكم وحدَه ولا يُنتج الفرعَ 1**،
والحاكمُ هو المجتمعُ الذي **لم أرَ فيه رقمَ وقفٍ متحرّكٍ قطّ** (`E-BT`).

**⓪-ب السابقةُ مقيسةٌ وكلُّها فشل — تُكتب قبل أيّ رقمٍ جديد:**

| التجربة | ما قاست | الحكم المنشور |
|---|---|---|
| `T-EXIT` | أهدافٌ ثابتة ‏+20% و+100% | فشلت · ‏+100% ⇒ **‏−0.62/−0.80/−0.90R** |
| **`T-EXITMGMT`** | خروجٌ جزئيٌّ عند `mid` و+10% ‏+ **وقفٌ ينتقل للتعادل مرّةً** | فشلت (‏1 من 4) · `X1−X0` = **‏−0.0023R [−0.0277, +0.0222]** = **أضيقُ فاصلٍ في تاريخ المشروع** · **و`X3−X0` = ‏−0.0207 سالبٌ في الثلاث** |
| `T-OPHOLD-2` | **إلغاءُ السقف** بوقفٍ ثابت (حملٌ لإغلاق النظاميّة) | فشلت · `HN` = **‏−2.1032R** · **و‏45.2% ممّن بلغوا ‏+10% انقلبوا وقفًا** |
| `T-TARGET10` | هدفُ ‏+10% | **استوفت** ⇒ السقفُ الحاليّ ليس اعتباطًا |
| `T-T1MOVE` · `T-MANAGE-25` | الهدفُ يتحرّك · إدارةُ 25% | فشلتا · الأثر ‏±0.01R |

**⓪-ج ولماذا هذا محورٌ جديدٌ لا إعادةَ فتح — بالبناء لا بالتسمية:** المقيسُ سابقًا
**طرفان**: سقفٌ قائمٌ مع وقفٍ يخطو مرّةً (`X3`) ⇒ ‏−0.0207 · وسقفٌ مُلغًى مع وقفٍ
**ثابت** (`HN`/`T-EXIT+100%`) ⇒ ‏−2.10R. **والوسطُ لم يُقَس قطّ: سقفٌ مُلغًى مع وقفٍ
يترقّى مع كلّ قمّةٍ جديدة.** وهو المرشَّحُ الوحيد الذي يفسّر الطرفين معًا (السقفُ يقصّ
الذيلَ · والوقفُ الثابت يُعيد الرابحَ خاسرًا).

**⓪-د وشرطُ الفتح منصوصٌ في الأرشيف ويُسمّي هذا المحورَ بلفظه — مرّتين:**
`T-RECLAIM-INTRADAY` (‏`DECISIONS_ARCHIVE.md:4416`): «شرطُ الفتح: محورٌ جديدٌ حقيقيّ
(هدفُ ربحٍ مقترنٌ بالخروج · **وقفٌ متحرّك** · خروجٌ جزئيّ) بتسجيلٍ مسبقٍ وإذنٍ صريح» ·
و`T-T1MOVE` (`:2549`): «أقربُه إدارةُ الخروج بعد الدخول (**وقفٌ متحرّك** · خروجٌ
جزئيّ · هدفٌ زمنيّ)». 🔴 **وتصحيحٌ يُقال:** ذيلُ الثاني «ولم يُقَس أيٌّ منها على
مجتمع الفارز اليوميّ قطّ» **بائتٌ جزئيًّا** — `T-EXITMGMT` (‏2026-09-04) قاست
**الخروجَ الجزئيّ والخطوةَ الواحدة** على ذلك المجتمع بعينه. **الباقي غيرَ مقيسٍ هو
الترقّي المستمرّ وحدَه**، وهو موضوعُ هذا العقد. والإذنُ الصريح: أمرُ المالك أعلاه.

---

## ① السؤالُ القابل للتكذيب

**هل استبدالُ سقفِ الربح بوقفٍ يترقّى مع القمّة يُحسّن التوقّعَ على نفس الصفقات —
بنفس الدخول ونفس الوقف الابتدائيّ — أم لا؟**

وسؤالٌ ثانٍ **لا ينفصل عنه**: **وهل الترقّي هو الذي يُحسّن، أم مجرّدُ إلغاء السقف؟**
(‏الأوّلُ وحدَه بلا الثاني حكمٌ بلا معنى — ودرسُ `C-MOM` في `T-PMGATE` وقع على هذا.)

---

## ② المجتمعان — **مُغلَقان** وأحدُهما وحدَه يحكم

| | التعريف | الدور |
|---|---|---|
| **`E-BT`** 🥇 | مجتمعُ `T-EXITMGMT` بعينه: لقطاتُ PIT الثلاث · **‏4,817 زوجًا · 1,375 رمزًا** · شموعٌ يوميّة | **الحاكم** — ثلاثُ سنوات · **ولم أرَ فيه رقمَ وقفٍ متحرّكٍ قطّ** |
| `E-OP` | مجتمعُ `T-OPCURVE`/`T-OPTRADE` بعينه: `opcurve_rows.tsv` **‏481 صفًّا** (‏390 قابلًا للتداول) · شموعُ دقيقة | **وصفيٌّ ملزِمُ النشر** — **مُلوَّثٌ بـ§⓪-أ فلا يُنتج الفرعَ 1 وحدَه** |

**والهُويّةُ تُثبَت لا تُدَّعى** — بوّاباتٌ توقف قبل أيّ رقم:
- **`V-T1`** على `E-BT`: `Y0` يجب أن يُعيد أساسَ `T-TRANCHE`/`X0` المنشور
  **‏−0.2602 · −0.1807 · −0.2172**، والتعبئةُ **‏1399/1386/1370**، والصفوفُ
  **‏1620/1591/1606** — أيُّ فرقٍ ⇒ **خروج 5**.
- **`V-T2`** على `E-OP`: مُتعدَّدُ `(date, sym)` مطابقٌ بت-بت لـ**‏481** صفَّ
  `opcurve_rows.tsv`، و`Y0` يُعيد `P0` المنشور **‏−0.7000R على 390 صفًّا** — وإلّا **خروج 5**.
- **`V-T3`** على `E-OP`: `C-CAPn` يجب أن يُعيد `HN` المنشور في `ophold2_result.md`
  **‏−2.1032R على 385 صفًّا** — **شاهدُ هُويّةٍ ثالثٌ ضدّ قياسٍ منشورٍ سابق** ⇒ وإلّا **خروج 5**.

---

## ③ التعريفاتُ — بالأسماء لا بالشبيه

| العنصر | التعريف | المصدر بالاسم |
|---|---|---|
| مسافةُ الترقّي `d` | **‏10%** (والحسّاسيّةُ 7% و13%) | `CONFIG["SPLIT_SWEEP_MID/MIN/MAX_PCT"]` — **لا رقمَ مكتوبٌ بيد** |
| الوقفُ المترقّي | `stop_t = max(stop_0, peak_t × (1 − d/100))` — **يشدّ ولا يُرخي أبدًا** | تعريفٌ واحدٌ بلا عتبةِ تسليح |
| `peak_t` | أعلى قمّةٍ حتى الشمعة `t` ضمنًا | — |
| **ترتيبُ الشمعة** | **الوقفُ يُفحَص أوّلًا** · ثمّ تُحدَّث القمّة · **والوقفُ المترقّي يسري من الشمعة التالية** | قاعدةُ `T-EXITMGMT §②` بحرفها — **صفرُ اصطلاحٍ جديد** |
| `R₀` | `entry − stop_0` — **واحدةٌ لكلّ الأذرع** (الدخولُ والوقفُ الابتدائيُّ لم يتغيّرا) | `tranche_arms.r_fixed` |
| الفاصل | بوتستراب عنقوديٌّ بالرمز ببذرةٍ مثبَّتة | `exitmgmt_arms.boot_ci` |
| التكلفةُ على `E-OP` | `k(C-MED)` ‏+ شبكةُ `c × s` تُطبَع | `fcost_arms.k_of` / `ret_at` |
| أفقُ `E-OP` | **‏120 دقيقة** (`h*` المنشور) · و**أوّلُ إغلاقٍ نظاميّ** (أفقُ `HN`) | `opcurve_result.md` · `ophold2_prereg.md §③` |

---

## ④ الأذرعُ والشواهد — قائمةٌ **مُغلَقة**

**على `E-BT` (الحاكم):**

| الذراع | السقف | الوقف | الدور |
|---|---|---|---|
| **`Y0`** | `t1` (الإنتاج) | ثابت | **بوّابةُ تكامل** (`V-T1`) |
| **`Y1`** 🥇 | **مُلغًى** | **مترقٍّ `d=10`** | **الحاكمة** |
| **`C-CAP`** 🥇 | **مُلغًى** | **ثابت** | **الشاهدُ الحاكم** — يعزل «الترقّي» عن «إلغاء السقف» |
| `Y4` | `t1` باقٍ | مترقٍّ | وصفيّة — تعميمُ `X3` المستمرّ |
| `Y1@7` · `Y1@13` | مُلغًى | مترقٍّ ‏7% · 13% | وصفيّتان — حدّا نطاق فيصل |

**على `E-OP` (وصفيّ ملزِمُ النشر):** `Y0` (= `P0`) · `Y1` (‏120د) · **`Y1n`** (لأوّل
إغلاقٍ نظاميّ) · `C-CAP` (‏120د · ثابت) · **`C-CAPn`** (= `HN` · `V-T3`) · `Y4`.

⛔ **ولا ذراعَ سادسةٍ على `E-BT` ولا سابعةٍ على `E-OP`**، ولا قيمةَ `d` غيرَ الثلاث،
ولا أفقَ غيرَ المنصوصَين.

---

## ⑤ المعاييرُ والفروع — مكتوبةٌ قبل أوّل صفّ

| # | المعيار (على `E-BT` حصرًا) |
|---|---|
| **`TR1`** 🥇 | `Y1 − Y0` مقترنًا **موجبٌ في السنوات الثلاث** وفاصلُ المجمَّع **لا يلمس الصفر** |
| **`TR2`** 🥇 | `Y1 − C-CAP` مقترنًا **موجبٌ في الثلاث** وفاصلُ المجمَّع **لا يلمس الصفر** |
| **`TR3`** 🥇 | **ماديّةٌ:** `Y1 − Y0` المجمَّع **‏≥ +0.05R** (‏وسمٌ `engineering` — ‏≈2× أضيقِ فاصلٍ قِيس عندنا `±0.025`) |
| **`TR4`** | **أرضيّة:** ‏**≥150** زوجًا لكلّ سنةٍ ولكلّ ذراع · **وسقوطُها يمنع الفرعَ 1** |

**الفروعُ ثلاثةٌ لا رابع:**

1. **«تُوصى»** — `TR1` **و**`TR2` **و**`TR3` **و**`TR4` معًا ⇒ **اقتراحٌ للمالك بلا شحن**.
2. **«لا تُوصى»** — سقط معيارٌ حاكمٌ واحدٌ **ولو في سنةٍ واحدة** ⇒ لا شحنَ ولا رفعَ عيّنة.
   (‏**سقوطُ سنةٍ واحدةٍ ليس «لا حكم» بل الفرعُ 2** — درسُ `T-PMGATE` و`T-RSI-RANK`.)
3. **«لا حكم»** — سقطت **الأرضيّةُ `TR4`** ⇒ **خروج 9** · ويُنشَر العددُ المقيس.

**ورمزا الخروج:** **‏5** لسقوط حارسِ هُويّة (`V-T1`/`V-T2`/`V-T3`) · **‏9** لـ«لا حكم».

🔓 **وشرطُ إغلاقٍ مكتوبٌ قبل النتيجة:** **سقوطُ `TR2` يُغلِق محورَ «إدارةُ الخروج بعد
الدخول» إغلاقًا مُنفَّذًا لا مكتوبًا** (رابعةُ تجربةٍ تسقط فيه) — بحارسٍ يخرج قبل أيّ
عملية، وبشرطِ فتحٍ ثلاثيّ: مصدرٌ جديدٌ للدعوى · عقدٌ جديد · وإذنُ المالك.

---

## ⑥ التنبّؤاتُ العمياء — تُنشَر مؤكَّدةً أو مكذَّبة

| # | التنبّؤ |
|---|---|
| `TP1` | `Y1 − Y0` على `E-BT` **موجبٌ ودون ‏+0.05R** ⇒ **`TR3` هو الساقط** (أثرُ `T-EXITMGMT`) |
| `TP2` | `C-CAP` **سالبٌ بعنف** على `E-BT` (صدى `T-EXIT` ‏+100% = ‏−0.62..−0.90R) |
| `TP3` | `Y1 − C-CAP` **موجبٌ وكبير (فوق ‏+0.30R)** ⇒ **الترقّي هو ما يجعل إلغاءَ السقف محتمَلًا** |
| `TP4` | `Y4 − Y0` **سالبٌ وقريبٌ من الصفر** (تعميمُ `X3 − X0` = ‏−0.0207) |
| `TP5` | `Y1@7` **أسوأُ** من `Y1@10` — ‏7% داخلَ نطاق سحب فيصل نفسِه فيُكنَس |
| `TP6` | على `E-OP`: `Y1n − Y0` **موجب** (الذيلُ هناك) — **ومُعلَنٌ أنه متوقَّعٌ بتلوّث §⓪-أ فلا يُقرأ تأكيدًا** |

---

## ⑦ حدودُ الصدق — تُطبَع في كلّ تقرير

1. **التلوّثُ (§⓪-أ):** اتّجاهُ `E-OP` معروفٌ لي ⇒ **وصفيٌّ لا حاكم**.
2. **السابقةُ فشلٌ متكرّر** (§⓪-ب) ⇒ **الاحتمالُ الأسبقُ الفشل**، و`±0.025R` تعني أن
   أثرًا حقيقيًّا صغيرًا **قد يكون غيرَ قابلٍ للكشف أصلًا**.
3. **`X3 − X0` سالبٌ في الثلاث** ⇒ صيغةُ الترقّي بخطوةٍ واحدةٍ **مع** السقف **مقيسةٌ
   سالبة** — فلا يُقرأ `Y4` محورًا جديدًا بل تعميمًا.
4. 🔴 **نقلُ إطارٍ مُعلَن (قاعدةُ `faisal-gates §②` إلزاميّة):** ‏7/10/13 **رقمُ فيصل
   حرفيًّا** (`faisal_verbatim` · `IMG_0297` · CCHH «سحب السيوله متعارف عليه من 7٪ ل 13٪»)
   **لكنّ نظامَه «عمقُ سحبِ سيولةٍ قبل المواصلة» لا «مسافةُ وقفٍ متحرّك»** ⇒ الاستعمالُ
   هنا **`faisal_adopted` نقلُ إطارٍ** لا استشهادٌ حرفيّ · **ولا يُقرأ الحكمُ حكمًا على
   قاعدة فيصل**، ولا تُشحَن `d` عتبةً بهذي التجربة.
5. **`+0.05R` ماديّةٌ `engineering`** — لا سندَ فيصليًّا لها، وتُعلَن كذلك.
6. **لمسٌ لا تنفيذ:** القمّةُ لمسةٌ (متفائلة) والوقفُ باصطلاح الإنتاج ⇒ **والانحيازُ
   هنا في اتّجاه الذراع**: قمّةٌ تُلمَس ولا تُنفَّذ **ترقّي الوقفَ مع ذلك** ⇒ **الرقمُ
   سقفُ أداءٍ للذراع لا أرضية.**
7. **ترتيبُ الشمعة مجهول** ⇒ الاصطلاحُ المحافظ (§③) بحرف `T-EXITMGMT`.
8. **التكلفة:** كلُّ ذراعٍ **خروجٌ واحد** ⇒ التكلفةُ **تُلغى في الفرق المقترن** على
   `E-BT` (يُثبَت بعدّاد الأرجل) · وعلى `E-OP` تُطبَّق `k(C-MED)` وتُطبَع الشبكة.
9. **`E-OP` ‏≈4 أسابيعَ من سوقٍ واحد** · وانحيازُ البقاء قائم · **والمكتومُ ببوّابة
   المضارب ليس في المجتمع**.
10. **‏64% من الانفجارات بلا مِرساةٍ أصلًا** ⇒ **سقفُ أيّ سياسةٍ ‏≈36% من الفرص**
    (منقولٌ من `T-OPTRADE §⑩` ويبقى قائمًا).

---

## ⑧ سقفُ النجاح — والمحظورات

- **سقفُ النجاح: اقتراحُ سياسةِ خروجٍ للمالك — بلا شحنٍ ولا تنفيذِ أوامرَ ولا وسيطِ تداول.**
- ⛔ **صفرُ مسٍّ بجذر** · **لا `LOGIC_VERSION`** · **الأداتان قراءةٌ فقط** بحارسٍ مقفول.
- ⛔ **`STOP_BELOW_LOW_PCT` و`ENTRY_*` ووقفُ القاع لا تتحرّك بهذي التجربة.**
- ⛔ **وإعادةُ الدخول داخل اليوم مُغلَقةٌ** (`T-RECLAIM-INTRADAY`) فلا تُدخَل من هذا الباب.
- ⛔ **ولا تُضاف ذراعٌ ولا تُحرَّك `d` ولا `+0.05R` ولا أفقٌ بعد رؤية أوّل رقم.**

---

## ⑨ الأداتان — مواصفةٌ ملزِمة (تُبنيان **بعد** دمج هذا العقد)

- **`trail_arms.py`** (`trail.yml` يدويٌّ بلا كرون · **بلا سرِّ تلغرام**) — `E-BT`
  الحاكم · يُعيد استعمال `tranche_arms.plan_at`/`r_fixed` و`exitmgmt_arms.boot_ci`
  **بالاسم** · **باكتيستٌ واحدٌ لكلّ سنة ومجموعةُ صفقاتٍ واحدةٌ تُعاد عليها الأذرع**
  ⇒ الميزانيةُ والكونُ والنافذةُ **متطابقةٌ بالبناء لا بالدعوى**.
- **`trail_op_arms.py`** (`trail_op.yml` يدويّ) — `E-OP` · يُعيد استعمال
  `optrade_arms.simulate`/`trade_r` و`fcost_arms.k_of`/`ret_at` **بالاسم**.
- **وضعُ جدوى `TRAIL_DRY=1`:** يطبع **ثلاثةَ أعدادٍ فقط** (الصفوف · القابلُ للتداول ·
  التغطية) **ويعود قبل أيّ `R` وأيّ فرقٍ وأيّ فاصل** — مقفولٌ بعدّاد نداءات.
  ⚠️ **ودرسُ `T-PMGATE`: وضعُ الجدوى يُجيز ما يمرّ به وحدَه فلا يُقرأ إذنًا للمسار كلِّه.**
- **`read_verdict` دالّةٌ نقيّةٌ تطبّق §⑤ بحرفه** (‏سقوطُ سنةٍ ⇒ الفرعُ 2) — بجدول حقيقة.
- **والحتميّة:** المدخلاتُ نفسُها ⇒ مُخرَجٌ بت-بت · والنتيجةُ في `trail_result.md`
  بجانب هذا العقد لا فوقه.

---

## ⑩ الأقفال (تُكتب في السويّة مع هذا العقد، وكلٌّ يسقط بطفرةٍ مسمّاة)

- **`TRK0`** — بنيويّ: فروعُ §⑤ **ثلاثةٌ كلٌّ في رقمه** · «لا حكم» في الثالث وحدَه ·
  ورمزا الخروج **‏5** و**‏9** منصوصان · والأرضيّةُ `TR4` **تمنع الفرعَ 1** نصًّا ·
  والماديّةُ `TR3` مسمّاةٌ برقمها.
- **`TRK1`** — §④/§②: الأذرعُ قائمةٌ مُغلَقة · **`C-CAP` شاهدٌ حاكم** · وحرّاسُ الهُويّة
  الثلاثةُ يحملون أرقامَهم المنشورة (‏−0.2602/−0.1807/−0.2172 · −0.7000 · −2.1032).
- **`TRK2`** — §⓪/§⑦/§⑧: التلوّثُ مُعلَنٌ بـ`AEMD`/`RETO` · والسوابقُ الأربعُ مسمّاة ·
  و**نقلُ الإطار** مُعلَنٌ `faisal_adopted` · و`+0.05R` موسومةٌ `engineering` ·
  و§⑧ يمنع الشحنَ ويحمي `STOP_BELOW_LOW_PCT`.
- **`TRK3`** — **سلوكيٌّ من الكود الحيّ لا من نصّ العقد:** الدوالُّ الستُّ المُعادُ
  استعمالها **موجودةٌ بأسمائها** في وحداتها، و`SPLIT_SWEEP_MIN/MID/MAX_PCT` في `CONFIG`
  تساوي **‏7.0/10.0/13.0** — فإن تحرّك أحدُها **يسقط القفل** ولا يُقرأ العقدُ نافذًا.
