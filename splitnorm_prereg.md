# ⚖️🧱 `T-SPLITNORM` — تسجيلٌ مسبق: **تسويةُ التقسيم على مستويات فيصل**

> **أمرُ المالك (2026-09-19): «سجل التسوية».**
> **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · ولا يُشحَن شيءٌ مهما كانت النتيجة.**
> **مدفوعٌ ومدموجٌ قبل أيّ سطرِ أداةٍ وقبل أيّ رقمٍ جديد.**
>
> 🔒 **والعقودُ السابقةُ مدموجةٌ لا تُعدَّل:** `support_def_prereg.md` وملاحقُه
> `§⑨`-`§⑫` تبقى بحرفها — وهذا **عقدٌ مستقلٌّ جديد** لأن المُقاس هنا
> **تغييرُ تعريفٍ في المقياس** لا إعادةُ تشغيلٍ بمعاييره (`support_def_result.md §⑥`-2:
> «تسويةُ التقسيم على مستويات فيصل — وهي **تغييرُ تعريفٍ يلزمه تسجيلٌ مسبقٌ جديد**
> لا إصلاحٌ صامت»).

---

## §⓪ الدعوى ومصدرُها — **ليست دعوى فيصل** بل دعوى منهجِ قياسٍ عندنا

فيصل يرسم مستوياتِه **على شارتٍ بمقياس يومِه**. ومصدرُ بياناتنا
(`download_history`) **يُعدِّل التاريخَ رجعيًّا لكلّ تقسيم**. ⇒ إذا وقع تقسيمٌ
**بين تاريخ الشارت واليوم** فالطرفان **مقياسان مختلفان**، والمقارنةُ بينهما
**ليست قياسًا بل أثرَ مقياس**.

**والدليلُ منشورٌ سلفًا لا مُفترَض** (`support_def_result.md §③`، التشغيلة
`35455722208`):

| الرمز | مستوى فيصل | عاملُ التقسيم عند تاريخ الشارت | خطأُ `A0` الخام |
|---|---|---|---|
| `NEXR` | ‏1.712 | **‏0.0909091** (‏1:11) | **‏+1005%** |
| `NXTT` | ‏5.830 | **‏0.01** (‏1:100) | **‏+9849%** |

⇒ **صفّان من تسعةٍ في المجتمع الحاكم غيرُ قابلَين للمقارنة.**

⚠️ **ومقيسٌ اليومَ أن التسوية ليست تفسيرًا عامًّا** (‏`weekly_support_result.md`
ملحق `§⑦` · التشغيلة `35457956045` · **الفرعُ 2**): `PPBT` له ثلاثةُ تقسيماتٍ
**وصفرٌ منها بعد تاريخ الشارت**، والعاملُ المطلوبُ لمصالحة فجوته ‏≈1.46 **ليس
نسبةَ تقسيمٍ أصلًا**. ⇒ **هذا العقدُ لا يدّعي أن التسوية تُغلق فجوةَ تعريف
الدعم** — يدّعي فقط أنها **تُصلح المقياسَ حيث يوجد تقسيم**.

**واستيفاءُ شروط الفتح الثلاثة** (`support_def_result.md §⑩`): ① التسويةُ
مسمّاةٌ نصًّا في `§⑥`-2 ② معيارٌ مسجَّلٌ **جديد** ليس تخفيضًا لحدٍّ رأيتُ قيمتَه
(العتباتُ ‏80% و`p ≤ 0.10` والأرضيّةُ **لا تُمَسّ**) ③ **أمرُ المالك.**

---

## §① ما هو قائمٌ وما هو غائب — مقيسٌ لا مُدَّعًى

- **قائم:** `Super_stock._split_scale_factor(splits, since_iso)` دالّةُ إنتاجٍ
  نقيّةٌ مُختبَرة · و`_fetch_splits` جالبُها · **والمِجَسُّ يحسبهما ويطبعهما
  سلفًا** بقاعدةٍ مدموجة: «**يُطبَع ولا يُستعمَل**» (`§⑩`).
- **غائب:** **استعمالُها** — أي تسويةُ مستوى فيصل قبل المقارنة. صفرُ سطرٍ في
  المستودع يفعل ذلك اليوم.

---

## §② التعريفُ المُقاس — واحدٌ، ومكتوبٌ بحرفه

```
norm(level, sf) = level / sf
```

**والاتّجاهُ ليس اختياري:** دوكسترنغُ دالّة الإنتاج ينصّ حرفيًّا أن **«القسمة
عليه تحوّل المستوى المخزّن لمقياس البيانات الحالية»**. ⇒ القسمةُ لا الضرب.

**وأربعةُ قيودٍ في التطبيق — تُكتَب الآن لأنها تغيّر النتيجة:**

1. **تُطبَّق على كلّ صفٍّ حاكمٍ بلا استثناء** — و`sf = 1` تجعلها **هُويّةً**
   بالبناء. (‏فلا يُختار من تُسوّى بعد رؤية الأرقام.)
2. **`refpx` لا يُسوَّى إطلاقًا** — فهو مأخوذٌ من بيانات المزوّد المعدَّلة أصلًا.
3. **الترتيب: التسويةُ ⟶ `drop_marker` ⟶ `pick_level`.** وهذا يغيّر **أيَّ
   مستوًى يُختار** و**هل ينجو الصفُّ أصلًا** — فيُعلَن قبل الرقم.
4. **عاملُ التقسيم يُحسَب عند `asof` الصفِّ نفسِه** بدالّتَي الإنتاج
   `_fetch_splits` و`_split_scale_factor` **بالاسم** — لا نسخةَ ولا إعادةَ كتابة.

---

## §③ 🔴🔴 إقرارُ التلوّث — **كاملًا وقبل أيّ تشغيل**

**مُخرَجُ `SD1` على مجتمع اليوم مشتقٌّ بيدي من الجدول المنشور، فهذا ليس اختبارًا
أعمى.** والاشتقاقُ يُكتَب هنا كاملًا ليُدقَّق عليّ لا ليُصدَّق:

من `e0 = (a0 − ref)/ref × 100` المنشورة:

| الرمز | `ref` خام | `e0` منشور | ⇒ `a0` مشتقّ | `sf` | ⇒ `ref` مُسوًّى | الإشارة بعد التسوية |
|---|---|---|---|---|---|---|
| `NEXR` | ‏1.712 | ‏+1005% | **‏≈18.92** | ‏0.0909091 | **‏≈18.832** | `a0` **أعلى** ⇒ ليس أدنى |
| `NXTT` | ‏5.830 | ‏+9849% | **‏≈580.03** | ‏0.01 | **‏583.0** | `a0` **أدنى** ⇒ أدنى ✅ |

والمنشورُ أن الخام **‏6 من 9 أدنى** والمخالفون الثلاثة هم `NEXR` و`NXTT` و`GRI`.
⇒ بعد التسوية ينقلب `NXTT` وحدَه ⇒ **‏7 من 9 = ‏77.78%** و
`sign_test_p(7, 9) = **0.1797**`.

⇒ **الحدُّ ‏80% لا يُبلَغ و`p` فوق ‏0.10** ⇒ **`SD1` لا يعبر.**
⇒ **الفرعُ 1 غيرُ قابلٍ للبلوغ على مجتمع اليوم — يُعلَن قبل التشغيل**
(سابقةُ `§⑫-ⓑ` في العقد السابق).
⇒ **ولا يُحسَب لي تنبّؤٌ على هذا المُخرَج** (`§⑫-ⓓ`: صحيحٌ بالبناء ⇒ لا إصابة).

**وحدُّ دقّةٍ على اشتقاقي نفسِه:** `e0` منشورةٌ مقرَّبة، فـ`a0` مشتقٌّ **تقريبيّ**.
والهامشُ في `NEXR` **‏0.47% فقط** (‏18.92 مقابل 18.832) ⇒ **رقيق**. لذلك:
**المِجَسُّ يحسبها بدقّةٍ كاملةً ويطبع الاشتقاقَ بجانبها، وأيُّ تعارضٍ يوقف
التشغيلَ بخروجٍ مميَّز** (`V-N4`) — فإن كذّبني الحسابُ **فالخطأُ خطئي ويُنشَر**.

🔑 **وما يُبطل هذا التلوّثَ جزئيًّا:** إن أدخلت التسويةُ **صفوفًا جديدة** (`§④`-2)
فالمجتمعُ الموسَّع **لم أشتقّه ولا أعرفه** ⇒ **`SD1` عليه أعمى**، ويُقرأ حكمًا.

---

## §④ ما يبقى أعمى فعلًا — **وهو موضوعُ هذا العقد**

1. **`SN1` الحياد (حاكم):** كلُّ صفٍّ عاملُه `sf = 1` يجب أن يُعطي `ref` و`a0`
   و`e0` و«أدنى؟» **مطابقةً بت-بت** لما أعطته التشغيلة `35455722208`.
   **قابلٌ للتكذيب:** لو غيّرت التسويةُ ترتيبَ `pick_level` أو أسقطت صفًّا
   محايدًا سقط `SN1`.
2. **`SN2` توسُّعُ المجتمع (حاكم · أعمى):** التشغيلةُ الخام طبعت «طُلبت ‏16 ·
   نجحت ‏11 · **استُبعدت ‏5**» — **ولم أقرأ أسماءَ المستبعَدين ولا أسبابَهم.**
   وبعضُ الاستبعاد سببُه «كلُّ المستويات فوق سعرِ المرجع»، **وهو حكمٌ على
   المقياس الخام** قد ينقلب بالتسوية. ⇒ **يُطبَع العددُ والأسماءُ والسبب**،
   **وكلُّ صفٍّ يدخل يجعل `SD1` الموسَّعة أعمى.**
3. **`SN3` صفوفُ الكاتالوج (حاكم · أعمى):** الصفوفُ الحاكمةُ من `CASES`
   (‏`origin = cat`) **لم يُطبَع عاملُ تقسيمها قطّ** ولا أعرفه. **وتواريخُها
   أقدم** فاحتمالُ التقسيم أعلى. ⇒ إن كان لأيٍّ منها `sf ≠ 1` فمعناه أن
   **`SD1 = 6/9` المنشورة كانت هي نفسُها ملوَّثةً بأثر مقياس** — وهذا **اكتشافٌ
   عن الحكم السابق** لا عن التعريف.
4. **وصفيٌّ يُطبَع ولا يحكم:** `SD1`/`SD2`/`SD3` على المجتمع المُسوّى، **وكلُّ
   رقمٍ منها موسومٌ «مشتقٌّ سلفًا» أو «أعمى»** بحسب `§③`.

---

## §⑤ فروعُ الحكم — مكتوبةٌ قبل الرقم · ولا فرعَ رابع

1. **الفرعُ 1 — «التسويةُ تُعتمَد مقياسًا لهذا المحور»:** `SN1` ✅ **و** كلُّ صفٍّ
   `sf ≠ 1` يصير خطؤُه المُسوّى **داخلَ مدى أخطاء صفوف `sf = 1`** (فلا يبقى
   شاذٌّ بمرتبةٍ عشريّة). ⇒ المقياسُ المُسوّى يصير **مقياسَ هذا المحور وحدَه**
   — **ولا يُشحَن في الإنتاج شيء.**
2. **الفرعُ 2 — «التسويةُ ليست حياديّة»:** `SN1` 🔴 ⇒ **تُرفَض** ولا يُعتمَد
   شيء · ويُنشَر **أيُّ صفٍّ اختلف ولماذا**.
3. **الفرعُ 3 — «لا قياس»:** تعذّرُ الجلب · أو سقوطُ حارسٍ · أو عدمُ بلوغ
   أرضيّة `SD0` المنشورة (‏≥5 ثلاثيّاتٍ من ≥3 رموز) — **تُعلَن بعددها ولا تُفسَّر.**

**ورموزُ الخروج مميَّزة** ولا تصادم رموزَ المِجَسّ القائمة
(‏0/2/3 حكمًا · 5/6/7 حرّاسًا · **‏8 إغلاقًا**).

---

## §⑥ حدودُ صدقٍ تُقال قبل النتيجة

1. **الفرعُ 1 لا يُثبت انحيازَ تعريف الدعم** ولا يُقرَّب منه — يُصحّح **مقياسًا**،
   و`SD1` تبقى ساقطةً كما اشتُقّ في `§③`.
2. **العيّنةُ صغيرة:** ‏9 صفوفٍ من ‏9 رموز. و`sign_test_p(9,9) = 0.0039`
   ⇒ عند `n = 9` **الإجماعُ وحدَه يعبر** — **وهذا وصفيٌّ** ولا يُغري بمطاردته.
3. **التسويةُ لا تُصلح ما لا تقسيمَ فيه** — مقيسٌ اليومَ على `PPBT` (‏`§⓪`).
4. **العتباتُ المنشورة لا تُخفَّض** (‏80% · `p ≤ 0.10` · الأرضيّة) — **مقفولٌ.**
5. **الترتيبُ في `§②`-3 قرارُ تصميمٍ لي**، وترتيبٌ آخر قد يعطي صفوفًا أخرى —
   يُعلَن ولا يُجرَّب بديلُه بعد رؤية النتيجة (**مطاردةُ نتيجة**).
6. **اشتقاقي في `§③` قد يكون خاطئًا** — و`V-N4` يوقف التشغيلَ إن كان، ويُنشَر.

---

## §⑦ تنبّؤاتي — تُنشَر مكذَّبةً أو مؤكَّدة

| # | التنبّؤ | ثقتي |
|---|---|---|
| `PN1` | `SN1` الحياد **يعبر** (القسمةُ على 1 هُويّةٌ بالبناء) | **عالية** |
| `PN2` | `SN2` = **صفرُ صفٍّ جديد** يدخل | متوسّطة |
| `PN3` | `SN3` = **صفرُ صفِّ كاتالوجٍ** بعاملٍ ‏≠ 1 | **منخفضة** |
| `PN4` | الوصفيّ `SD1` = ‏7/9 · `p = 0.1797` | **مشتقٌّ — لا يُحسَب** |
| `PN5` | وسيطُ `SD2` **ينخفض** عن ‏31.03% | **مشتقٌّ جزئيًّا — لا يُحسَب** |

---

## §⑧ حرّاسٌ تُطبَع في كلّ تقرير — والسقوطُ يوقف

- **`V-N1`** العلمُ **مطفأ** ⇒ المُخرَجُ الحاكمُ يطابق `35455722208` بت-بت
  (‏`SD1` ‏6/9 · `p = 0.5078` · `SD2` ‏31.03% · `SD3` ‏50.83%).
- **`V-N2`** العلمُ **مُشعَل** ⇒ كلُّ صفٍّ `sf = 1` يطابق نظيرَه الخامَ بت-بت.
- **`V-N3`** `norm(x, 1.0) == x` بالضبط (لا تقريبَ ولا انحراف).
- **`V-N4`** المِجَسُّ يطبع **اشتقاقَ `§③`** بجانب المحسوب لـ`NEXR` و`NXTT`،
  **وأيُّ تعارضٍ خارج ‏1% يوقف** بخروجٍ مميَّز — **فلا يمرّ خطؤُه ولا خطئي.**
- **`V-N5`** الترتيبُ `§②`-3 مُثبَتٌ **سلوكيًّا** لا نصًّا.
- **`V-N6`** `_split_scale_factor` و`_fetch_splits` الإنتاجيّتان **بالاسم**.
- **`V-N7`** `SUPDEF_NORM` **مطفأٌ افتراضًا** ⇒ المسارُ المنشورُ بت-بت،
  **وحارسُ الإغلاق `SUPDEF_REOPEN` يبقى فوقه** (التسويةُ لا ترفعه).

---

## §⑨ ما لا يُمَسّ بهذي التجربة

- ⛔ **`pivot_stability` لا تُمَسّ بأيّ حال** — ولا أيٌّ من الجذور الاثني عشر.
- ⛔ **لا `LOGIC_VERSION`** · **ولا شحنَ حقلٍ أو مفتاحٍ أو سطرِ عرضٍ** مهما كانت
  النتيجة (‏`support_def_prereg.md §⑥`-6 قائمٌ ونافذ).
- ⛔ **العقودُ المدموجةُ لا تُعدَّل** — وأيُّ إضافةٍ تذهب إلى **ملحقٍ مؤرَّخٍ جديد**.
- ⛔ **ولا تُعاير عتبةٌ ولا نافذةٌ لتعبر** — مطاردةُ النتيجة مرفوضةٌ بنصّ العقد.
