# 🕵️📈 `T-OP23` — سلوكُ المضارب قبل الانفجار على 23 سنة (تسجيلٌ مسبق + مواصفةُ بناءٍ لأوبيس)

**التاريخ:** 2026-08-13 · **الأمر:** «شغّل باكتيست على 23 سنة نستهدف الأسهم اللي
انفجرت وتطابق شروط فيصل فقط، ونحاول نطلع منها سلوك المضارب قبل الانفجار».
**الحالة:** تسجيلٌ مسبق يُدفَع **قبل أيّ رقم** — والأرقام من تشغيلات الـworkflow حصرًا.

---

## ⓪ تصحيحُ إطارٍ يُقرأ أوّلًا (صدقٌ مع المالك)

- «باكتيست 23 سنة» **ليس** باكتيست yfinance القائم (سقفُه ‏≈3 سنوات وكونُه ناجٍ) —
  هو **خطُّ أنابيب Polygon** الذي فتحته مفاتيح S3: التاريخ من **2003**، والكونُ
  **يشمل المشطوبين** ⇒ 🥇 **أوّلُ دراسةٍ في المشروع بلا انحياز بقاء.**
- وسوابقُ الصدق قائمة: خمسُ تجارب «سلوكٍ يميّز المنفجر» فشلت (`Q5`-family) —
  **الجديد هنا ثلاثة لم تجتمع قطّ:** نافذةُ ‏[−2،−1] قبل **بدء** الانفجار (لا
  نافذة القاع) · عيّنةٌ ضخمة (عقدان ⇒ مئات/آلاف الأحداث بدل ‏7-35) · **ومجموعةُ
  تمييزٍ حقيقية** (منفجرون مقابل مؤهَّلين لم ينفجروا — سؤالُ المالك بصيغته
  القابلة للاختبار).

---

## ⓪-أ 🔴🔴 ملحقٌ مؤرَّخ 2026-08-13 **بعد أوّل تشغيلة** — الأداةُ لا تبلغ 23 سنة بعد

**أوّل تشغيلة `discover` (‏`31747874970`) نجحت ميكانيكيًّا** (‏49 حدثًا خامًّا ·
**‏8 مستبعَدةٌ بحارس التقسيم = 16.3%** ⇒ **`W2` تحقّق** [الحدّ ‏≥15%] · **‏26
مطابقًا لشروط فيصل من 41**) — **لكنّ توزيع الحقب كشف عيبًا في أداتي:**
**‏2003-2012 = صفر · 2013-2019 = صفر · 2020-2026 = 26.**

🔴 **السبب مُثبَتٌ من الكود لا مستنتَج:** `phase_discover` يجلب بـ
`S.download_history` (‏yfinance بسقف `HISTORY_DAYS`=800 يومًا ‏≈2.2 سنة) ⇒
**كلُّ المراسي تقع في 2026 بالضرورة**. فالعنوانُ «23 سنة» **دعوًى لم تُنفَّذ
بعد**، والمنفَّذُ **سنتان بمصدر الإنتاج**.

⚖️ **ويُقال صريحًا:** ما تحقّق هو **صلاحيةُ الآلة** (حارسُ التقسيم يعمل بنسبةٍ
ماديّة · الفلترُ الإنتاجيّ يعمل · الكلفةُ تافهة) — **لا** الدراسةُ التاريخية.
**والإصلاحُ محدَّد:** تبديلُ الجالب إلى `pit_history.polygon_daily` بـ
`start="2003-01-01"` لكلّ رمز (المنفذُ مُتحقَّقٌ سلفًا في `P2` من `pit_prereg`)
— **ولا يُشغَّل طورُ `trades` قبله**، لأن قياسَ «سلوكِ المضارب عبر ثلاث حقب»
على حقبةٍ واحدة **يخالف §② حرفيًّا** (التقسيمُ الحقبيُّ إلزاميّ).

## ① المراحل الثلاث — مثبَّتة قبل التشغيل

**‏P1 — اكتشافُ الأحداث (‏2003-01 ⟶ 2026-08):** شموعٌ يومية للسوق كاملًا
(‏`day_aggs` من الملفّات المجمَّعة أو `‏/v2/aggs/grouped` — أيّهما أرخص، ويُعلَن) ⟶
حدثُ انفجار = بلوغُ **‏+100% فوق أدنى قاع العشرين جلسةً السابقة**، والمِرساة =
**`explosion_onset` المصحَّحة بالاسم** (⛔ قفلٌ نحويّ يمنع `explosion_index`
المعطوبة — عيبُ P0 الموثَّق). 🔴 **وحارسُ التقسيم الوهميّ إلزاميّ:** تقسيمٌ عكسيّ
داخل ‏±5 جلسات من الحدث (من `/v3/reference/splits` الكامل) ⇒ **يُستبعَد ويُعَدّ
بعدّادٍ مطبوع** — وإلّا صارت «الانفجارات» صناعةَ تقسيماتٍ (الدرسُ المقيس في
`T-METHOD`: ‏192 من المرشّحين كانوا تقسيمات).

**‏P2 — فلترُ «شروط فيصل فقط»:** لكل حدثٍ ناجٍ من الحارس: تُجلَب سلسلتُه اليومية
**المعدَّلة** (‏`/v2/aggs` adjusted — مقياسٌ واحد، درسُ `V9`) ويُنادى
**`analyze_ticker` الإنتاجيّ نفسُه** على الشريحة المنتهية **قبل** المِرساة
(بأرقام `FAISAL_ONLY` النافذة حرفيًّا) ⇒ يبقى **«مرشّحٌ عند قاعه»** فقط — نفسُ
تعريف فئة `BOXL`/الـ24، مُعمَّمًا على عقدين. **صفرُ منطقٍ موازٍ: الحكمُ حكمُ
الفارز الحيّ بالحرف.**

**‏P3 — سلوكُ المضارب:** لكل حدثٍ مطابق، صفقاتُ `/v3/trades` للنوافذ الأربع:
الحدث ‏[−2،−1] · شاهدُ السهم نفسِه ‏[−12،−11] (‏`case-crossover`) — والمقاييس
**دوالُّ الإنتاج بأسمائها** (‏`acc_components` · `_operator_blocks` ·
`uniform_prints` + العدّ/القيمة$) + طبقةُ الشموع (‏`activity_features` — ملحق
`preexp_probe_prereg §⑥`).

**ومجموعةُ التمييز `G-NOEXP`:** مؤهَّلون بنفس الفلتر (‏P2) **لم يبلغوا ‏+50%
خلال 40 جلسةً بعد التأهّل** — يُسحَبون **حتميًّا** بـ`sha256("سنة:رمز")` بنسبة
‏1:1 مع الأحداث في كلّ حقبة (نمطُ `control_panel` المعتمَد)، وتُقاس نوافذُهم
بنفس الآلة. ⇒ **السؤال الأساسيّ المسجَّل: هل يفترق سلوكُ ‏[−2،−1] بين مَن انفجر
ومَن تأهّل ولم ينفجر؟**

---

## ② القراءة الإحصائية — مثبَّتة الآن (لا تُختار بعد الأرقام)

**التقسيمُ الحقبيّ إلزاميّ** (البُنى الدقيقة تغيّرت): **‏2003-2012 · 2013-2019 ·
2020-2026** — ⚠️ وما قبل 2013-12 **بلا كسور اللوت في شريط SIP** (يُضعِف مقاييسَ
الطبعات الصغيرة/الموحّدة هناك — يُعلَن ولا يُرقَّع).

**مقياسٌ «مرشَّحُ تمييز»** إذا استوفى الثلاثة معًا:
1. اتجاهُ فرقِ (منفجرين − غيرِ منفجرين) **واحدٌ في الحِقَب الثلاث**؛
2. فاصلا Wilson (أو فاصلُ وسيطٍ بالـbootstrap للمستمرّ) **منفصلان في حقبتين من
   ثلاث على الأقلّ**؛
3. والاتجاهُ نفسُه يظهر في مقارنة الحدث بشاهد السهم نفسِه (‏`crossover`).

⇒ **وسقفُ النجاح مثبَّت:** «مرشَّحُ تمييز» **ليس قاعدةً** — مخرجُه الوحيد
اقتراحُ **اختبارٍ أماميّ مسجَّلٍ جديد** على قوائم البوت الحيّة. **صفرُ بوّابة
وصفرُ وزنِ ترتيبٍ وصفرُ سطرِ عرضٍ من هذي الدراسة مباشرةً.** ولا شيءَ يستوفي
الثلاثة ⇒ يُعلَن «لا مميِّز» بحجم عيّنته — **وهو جوابٌ نهائيّ ذو وزن** بعد
عقدين بلا انحياز بقاء.

---

## ③ حرّاس الصلاحية — لكلٍّ سطرُ كودٍ يُوقِف

| # | الحارس | الخروج |
|---|---|---|
| `O1` | صفرُ تسريب: كلُّ جلبٍ بـ`timestamp.lt` عند فتح جلسة المِرساة · وقفلٌ يبدّل ما بعدها فلا يتغيّر شيء | 3 |
| `O2` | حارسُ التقسيم الوهميّ فعّال — عدّادُه يُطبَع، وعيّنةُ اختبارٍ بتقسيمٍ مزروع **تُستبعَد** | 3 |
| `O3` | مِرساةُ `explosion_onset` بالاسم (نحويّ) | قفل |
| `O4` | فلترُ P2 = `analyze_ticker` الحيّ بالاسم — لا نسخةَ مبسَّطة | قفل |
| `O5` | شاهدُ ضبطٍ لكل حقبة: يومُ `AAPL` واحد (عدُّ صفقاتٍ وحقولٌ معقولة) — «الصفرُ عطبُ أداةٍ حتى يُنفى» | 3 |
| `O6` | الاقتطاعُ مُعلَنٌ لكل جلب (المجلوب/الحدّ · ترتيبٌ تنازليّ — درسُ اقتطاع `T-ACC`) | قفل |
| `O7` | تعذّرُ الجلب **يُعَدّ ولا يُصنَّف** (عطبُ شبكةٍ ليس «هدوءًا») | يُعلَن |
| `O8` | أرضيّاتُ العيّنة: ‏≥30 حدثًا مطابقًا **لكل حقبة** وإلّا وُسمت «لا تكفي» وتُنشَر خامًا | يُعلَن |
| `O9` | **كلُّ إسقاطِ كلفةٍ من عيّنةٍ يُطبَع معه معامِلُ التوسيع** (الدرسُ المقيس) | قفل |
| `O10` | CSV تراكميّ قابلٌ للاستئناف + **تجزئةٌ بالسنة/الحقبة** (سقفُ الجوب 6 ساعات — درسُ `flatfiles`) | بنيويّ |

---

## ④ تنبّؤات قابلة للكسر (تُنشَر سقطت أو صمدت)

- **`W1`** عددُ الأحداث المطابقة (‏P2) بمئاتٍ على الأقلّ عبر العقدين — وإن خرج
  بعشراتٍ فمقامُ الدراسة انهار ويُعلَن قبل أيّ تفسير.
- **`W2`** حارسُ التقسيم يستبعد **جزءًا ماديًّا** (‏≥15%) من أحداث P1 الخام —
  (سابقة `T-METHOD`).
- **`W3`** طبقةُ الشموع لا تميّز (امتدادُ خمس سقطات) — **وإن ميّزت فالمفاجأة
  تُعلَن مفاجأةً.**
- **`W4`** 🔴 الأمانة: إن ظهر «مرشَّحُ تمييز» في الصفقات فالأرجحُ من عائلة
  الكتل/التوحيد لا من نِسَب الشراء (‏T-ACC أسقطت النِّسَبَ عند القاع) — تخمينٌ
  مكتوبٌ ليُكسَر.

## ⑤ ما لا يُدَّعى

1. العيّنةُ الحدثية مختارةٌ على النتيجة ⇒ التوصيفُ لا يُترجَم عائدًا.
2. الصفقاتُ تشمل الجلسةَ الممتدّة (يُعلَن ولا يُفصَل هنا).
3. لا مساسَ بأيّ بوّابة/مُرتِّب/عرض — والدراسةُ خارج `Super_stock` (أداةٌ
   مستقلّة لا تُستورَد في الإنتاج، قفلُ عزل).
4. `MIN_BARS` وطولُ السلسلة قد يُقصيان أحداثَ أسهمٍ حديثة الإدراج — يُعَدّون.

## ⑥ مواصفةُ البناء لأوبيس (رابعةً في الطابور — بعد الحزم الثلاث)

1. **`op23_scan.py`** (أداةٌ مستقلّة): مراحلُ P1/P2/P3 خلف أوضاعٍ
   (`OP23_PHASE=discover|filter|trades|report`) · مدخلات سنة/حقبة للتجزئة ·
   CSV تراكميّ لكل مرحلة يُرفَع artifact ويُستأنف · كلُّ الحرّاس §③ بسطر كودٍ
   يُوقِف · رموز خروج: 0/2 (مفاتيح غائبة)/3 (صلاحية)/8 (مدخل مدًى غير صالح).
2. **`op23.yml`**: يدويّ فقط · `POLYGON_API_KEY` (+`POLYGON_S3_KEY/SECRET` إن
   استُعملت الملفّات المجمَّعة) من Secrets حصرًا ولا تُطبَع قيمُها · كلُّ مدخلٍ
   موصولٌ بـ`env` (قاعدة P1).
3. **تقديرُ الكلفة يُطبَع قبل التنفيذ في وضع `discover`** (‏O9) — والتشغيلُ
   الكامل على مراحل بأمر المالك مرحلةً مرحلة (‏discover أوّلًا ⇒ نرى W1/W2
   قبل حرق ساعات الصفقات).
4. أقفالُ `test_bot.py`: عزلٌ (لا استيراد في الإنتاج) · `O1`-`O3` نحويًّا/سلوكيًّا
   · وطفرةٌ لكلٍّ تُسقطه.

---

## ⓪-ب ملحقٌ مؤرَّخ 2026-08-13 — نتيجةُ الطور `discover` بعد إصلاح المصدر
### وعيبان آخران: مسارُ رفعٍ مغروس · وسقفٌ بنيويّ للحقب

**تشغيلة `31749977789`** (على `a26cd58`، أي بعد إصلاح ملحق ⓪-أ):

- **التاريخ من 2003 يعمل فعلًا:** ‏50 من 51 رمزًا جُلبت · **تعذّر 1** (`AIFA`)
  **بسببه المُسمّى** — «الصفرُ عطبُ أداةٍ حتى يُنفى» منفَّذًا لا موعودًا.
- **P1:** خامٌّ 50 · **مستبعَدٌ بحارس التقسيم 7 (‏14.0%)** ⇒ تنبّؤ `W2` (‏≥15%)
  **قريبٌ ولم يتحقّق حرفيًّا** — يُسجَّل مكذَّبًا بفارقٍ ضئيل لا «تقريبًا صحيح».
- **P2:** مطابقٌ لشروط فيصل **‏23 من 43**.
- **الحقب:** ‏2003-2012 = **0** · 2013-2019 = **0** · 2020-2026 = **23**.

🔴🔴 **والحقبُ ما زالت فارغةً — والسببُ الآن مختلفٌ تمامًا عمّا أصلحتُه، ويُصحَّح
إعلاني السابق:** ملحق ⓪-أ نسب الفراغَ إلى سقف `HISTORY_DAYS`=800، وكان **عيبًا
حقيقيًّا وأُصلح فعلًا**؛ لكنّ الفراغ **بقي** لأن `explosion_index` افتراضُها
`pick="last"` (‏«أحدثُ انفجار» — قرارٌ مدوَّنٌ في `catalog_envelope` تعديل ②) ⇒
**مِرساةٌ واحدةٌ لكلّ رمز وهي الأحدث** ⇒ سقفُ الأحداث = **عددُ الرموز (51)**
وتوزيعُ الحقب **أثرُ اختيار المِرساة لا شحُّ تاريخ**. ⇒ 🔴 **تنبّؤ `W1` («مئاتٌ
على الأقلّ عبر العقدين») مكذَّبٌ بنيويًّا لا تجريبيًّا: كان مستحيلَ التحقّق بهذا
التصميم**، وكتابتُه في العقد خطأٌ منّي في التصميم لا في القياس.
⚙️ **والسطرُ صار يُطبَع مع الحقب** (لا في تقريرٍ لاحق) فلا تُقرأ الأصفارُ نتيجةً —
مقفولٌ `O14ب`. ⏸️ **وملءُ الحقب يلزمه مسحٌ متعدّدُ المراسي على كونٍ أوسع
(‏`pit_universe`) — تصميمٌ آخر بتسجيلٍ جديد وإذن المالك، لا تعديلُ عتبةٍ هنا.**

🔴 **وعيبٌ ثانٍ كشفه سجلُّ التشغيلة:** خطوةُ الرفع كانت `path: op23_events.jsonl`
**مغروسًا** بينما اسمُ الملفّ **مدخل**؛ فالتشغيلةُ كتبت **‏43 حدثًا** في
`op23_events_polygon.jsonl` وطبع الرافعُ «‏No files were found» ⇒ **الأحداثُ لا
تُرفَع ولا تُستأنَف**. أُصلح إلى `${{ inputs.csv }}` — مقفولٌ `O14أ`.
🧭 **الدرس: «نتيجةٌ محبوسةٌ في ملفّ = نتيجةٌ غيرُ مقروءة» بصيغتها الأشدّ — ملفٌّ
لا يُرفَع أصلًا؛ ولولا أنّي أقرأ السجلَّ لا الأداة لضاعت التشغيلة كلُّها.**

📌 **وما يبقى صالحًا من التشغيلة:** المنفذُ والتاريخُ والحارسُ والفلترُ كلُّها
تعمل، **و‏23 من 43 من مِرساة كتالوج فيصل يجتازون `analyze_ticker` الإنتاجيّ عند
قاعهم** — رقمٌ يُنشَر ولا يُبنى عليه (العيّنةُ مختارةٌ على النتيجة، وهو **نفسُ
تعريف فئة `BOXL` والـ24** معمَّمًا).
