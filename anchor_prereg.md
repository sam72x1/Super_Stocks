# 🎯📌 `T-ANCHOR` — الإرساء على المستوى المُختبَر (تسجيلٌ مسبق — يُدفَع قبل أيّ رقم)

**التاريخ:** 2026-08-13 · **الأمر:** «سجل ب» بعد `entry_points_audit.md` §④-ب.
**الحالة:** تسجيلٌ مسبق — **لا يوجد رقمٌ واحدٌ بعد**، وكلُّ قراءةٍ أدناه ثُبِّتت قبل التشغيل.

---

## ① السؤال والسند

**السؤال:** هل إرساءُ دفعات الدخول والوقف على **مستوى مُختبَر صامد** (قاعٌ لُمِس
مرّتين مستقلّتين فأكثر — `tested_level` القائمة) بدل **أدنى قاع النافذة المتحرّك**
(`pivot` الحالي) يحسّن التسليم؟

**السند من الكاتالوج (حرفيّ):** `IMG_0451` «**1.75 ضربها مرّتين ولا كسرها** ·
الدخول 1.80←1.90 · الوقف 1.70» — الدخول **فوق** المستوى المُختبَر والوقف **تحته**.
**والعيب المقيس في الإرساء الحالي:** أيُّ هبوطٍ جديد يعيد تعريف `pivot`
(`stability_result.md`) — شاهده الحي `BBLG`: رُشِّح في اليوم الذي صنع فيه قاعه
ودفعاتُه على قاع اليوم نفسه.

**أرقامٌ صفرٌ من عندي:** `tested_level(lookback=30, tol=0.015, min_touches=2)`
**كما هي في الكود** (موثَّقة سلفًا · اللمستان مستقلّتان بالعناقيد) — لا تُعاير هنا.
⛔ **وشرط «الصمود 3 جلسات» مُستبعَدٌ عمدًا** — قِيس في `T-STABILITY` وفشل
(‏−13 منفجرًا/3 سنوات)، و«لا كسرها» مستوفًى بنيويًّا في `tested_level`.
**متغيّرٌ واحد فقط: المِرساة.**

---

## ② الأذرع — ثلاثٌ ولا رابعة

| ذراع | `BT_ANCHOR` | المعنى |
|---|---|---|
| `A0` | `""` | الأساس: `pivot` الحالي — **بت-بت مع الإنتاج** |
| `B1` | `tested` | مستوى مُختبَر إن وُجد، **وإلا `pivot`** (ارتداد — يحفظ عدد الإشارات) |
| `B2` | `tested_strict` | مستوى مُختبَر **أو رفض** باسمٍ مُسمًّى `M_لا_مستوى_مختبر` |

المِرساةُ تُبدَّل **حيث يرسو `pivot` للدخول والوقف حصرًا** (الدفعات الثلاث +
صيغة الوقف 5-7% + حارس «الوقف تحت أدنى دفعة») — **والأهداف والبوّابات
والنقاط لا تُمَسّ بحرف.**

---

## ③ المقياس — مثبَّت قبل الأرقام

**الحاكم: `d50`** = المنفجرون المُسلَّمون داخل خانات السعة الحيّة (15) بأداة
`replay10` — نفس مقياس `T-STABILITY`/`T-RANKER3`. **ويُنشَر ولا يحكم:** `d100` ·
الإشارات · المحسومة · `R`/صفقة · **عدّاد التغطية** (كم إشارة وُجد لها مستوى
مُختبَر) · عدّاد `M_لا_مستوى_مختبر`.

---

## ④ حرّاس الاعتماد — بقراءةٍ محدَّدة سلفًا

| # | الحارس | الحدّ |
|---|---|---|
| (أ) | `d50` مجمَّعًا لا ينقص · **ولا سنةَ بأكثر من 20%** | شرطٌ لازم |
| (ب) | `R`/صفقة **مجمَّعًا** (القراءة مثبَّتة الآن — درس `T-BASE-475`) | ‏≥ −0.02R |
| (ج) | عيّنة الفرق كافية: **‏≥30 صفقةً تختلف مِرساتُها فعلًا** بين الذراع والأساس مجمَّعًا | وإلا «لا حكم» |
| (د) | شاهد `BBLG`: ذراع `B2` **ترفض** يومَ ترشيحه (قاعُ يومه لمسةٌ واحدة ⇒ لا مستوى مُختبَر) — بمِجَسّ `_diagnose_symbol` الإنتاجيّ | شرطٌ لازم لـ`B2` وحدها |

⇒ سقوط (أ) ⇒ **لا يُعتمَد**. سقوط (ب) وحده ⇒ مقايضةٌ تُعرَض على المالك.
والذراعُ المُعتمَدةُ المحتملة تُختار **بالحرّاس لا بأفضل رقمٍ بعديًّا**: إن عبرت
الاثنتان تُفضَّل `B1` (أقلُّ قصًّا) — **مكتوبٌ الآن قبل الأرقام**.

---

## ⑤ حرّاس الصلاحية — لكلٍّ سطرُ كودٍ يُوقِف (درس `envelope_prereg §⑦`)

| # | الحارس | الخروج |
|---|---|---|
| `V1` | **صفر نظرٍ مستقبليّ**: `tested_level` تُنادى على الشريحة حتى يوم الترتيب حصرًا — قفلٌ يبدّل المستقبل فلا تتغيّر المِرساة | 3 |
| `V2` | **لا no-op**: مِرساة `B1` تخالف `A0` في ‏≥1% من الإشارات — وعدّاد التغطية يُطبَع | 3 |
| `V3` | تكامل: `A0` يعيد مِرساة `B120` (‏2024:25 · 2025:26 · 2026:15 بسعة 15) — ثلاثيّ الحالة: عبَر ✅ / خرقٌ يُعلَن ℹ️ «مقامٌ جديد» ولا يُسقِط. 🔴 **تصريحٌ مؤرَّخ 2026-08-13 (قبل أوّل تشغيلة):** بعد دمج `rrtruth` (حزمة صدق الدخول — تغيّر عضويةَ بوّابة `rr`) **الخرقُ متوقَّعٌ بالبناء** ⇒ ℹ️ ستظهر وهي **نتيجةٌ مُعلَنة سلفًا لا شذوذ**، والرقمان يُطبَعان؛ وصلاحيةُ المقارنة الداخلية بين الأذرع قائمة (نفسُ المحرّك في نفس التشغيلة) | ℹ️ |
| `V4` | `child_env()` يرفع أعلام الباكتيست (`BT_REPLAY10`+`BT_ENVVALS`+`BT_POTENTIAL`) | 2 |
| `V5` | ميزانيةٌ ثابتة: نفس اللقطة والسنة والسعة لكل الأذرع — تشغيلةٌ واحدة لكل سنة تقيس الثلاث | 3 |
| `V6` | **العلم مطفأ ⇒ الفارز بت-بت** (قفلٌ سلوكيّ في السويّة) | قفل |
| `V7` | لقطةٌ مفقودة ⇒ توقّف | 4 |

---

## ⑥ تنبّؤات قابلة للكسر (تُنشَر سقطت أو صمدت)

- **`P1`** التغطية ماديّة: مستوى مُختبَر يوجد في **‏≥40%** من الإشارات (وإلا
  `B2` تجويعٌ لا ذراع).
- **`P2`** `B2` تقصّ الإشارات ماديًّا (‏≥15%) — وقصُّها يلتقط فئة `BBLG`
  (المُرشَّح يومَ قاعه الطازج).
- **`P3`** الحارس (د) يمرّ.
- **`P4`** 🔴 الأمانة الإحصائية: العدد صحيحٌ صغير (‏±3 داخل الضجيج) وسوابقُ
  عائلة «تحسين الاختيار» خمسُ سقطات ⇒ **الأرجح ألا يُحسَم اعتمادٌ صريح** —
  يُكتَب الآن فلا يُقرأ الفشل مفاجأةً.

---

## ⑦ ما لا يُدَّعى — قبل الأرقام

1. البوّابات `M1`-`M14` وأرقام الظرف والأهداف **لا تُمَسّ**.
2. `entry_ref` المخزَّن للتتبّع **لا يُمَسّ** (قرار المالك 2026-06-24).
3. انحياز البقاء وتشويه التقسيمات وعمى الافتر مشتركة بين الأذرع فتُطرَح من الفرق.
4. لا يُدَّعى أن المستوى المُختبَر «يجهّزه المضارب» — المقيس أثر المِرساة على
  التسليم فقط.
5. أيُّ اعتمادٍ يمسّ الدخول/الوقف ⇒ `LOGIC_VERSION` + `force_renew=1` —
  **وقراره للمالك وحده.**

## ⑧ رموز الخروج

**0** = قِيس والحرّاس طُبعوا · **2** = أعلامٌ غائبة · **3** = صلاحيةٌ ساقطة ⇒
لا رقمَ يُنشَر ولا يُفسَّر · **4** = لقطةٌ مفقودة.

**الأداة:** يبنيها أوبيس من `entry_truth_package.md §ب` (علم `BT_ANCHOR` مطفأ ·
`anchor_arms.py` · `anchor.yml`) — **ولا تُشغَّل قبل دفع هذا التسجيل، والأرقام
من تشغيلات الـworkflow حصرًا.**

---

## ⑨ نتيجةُ 2026 (تشغيلة `31747844515` — تُقرأ ولا يُحكَم بها وحدها)

**بوّابةُ الصلاحية خضراء كاملةً (خروج 0):** `V1` القيمُ الثلاث متمايزة ·
`V2` عدّادُ الرفض يشتعل في `B2` وحدَها (‏A0=0 · B1=0 · **B2=9,898**) ·
`V2ب` الإشاراتُ رتيبة (‏1587 ≥ 1587 ≥ 736) · `V4` المقياسُ محسوب ·
**و`V3` بت-بت (‏A0 مقيس=15 · منشور=15).**

| ذراع | إشارات | محسومة | دقة | مأخوذة | **d50** | d100 | R/صفقة |
|---|---|---|---|---|---|---|---|
| `A0` الأساس | 1587 | 1211 | 26.1% | 126 | **15** | 6 | −0.1161 |
| `B1` 🥇 الحاكمة | 1587 | 1210 | 26.0% | 126 | **15** | 6 | −0.1156 |
| `B2` الصارمة | 736 | 631 | 28.5% | 132 | **15** | **9** | −0.1707 |

🔴 **و`V3` لم يُخرَق — والتصريحُ المؤرَّخ أعلاه (§⑤) مكذَّب:** توقّعتُ أن دمج
`rrtruth` يكسر مِرساةَ `B120` **بالبناء**، والمقيس **‏15 = 15 بت-بت**.
**والسببُ بنيويّ مُثبَتٌ من الأرقام:** تحت ظرف فيصل `MIN_RR_T1`=**0.5** و`rr`
**نقصٌ لينٌ لا رفض** (حدُّ النواقص 8) ⇒ تشديدُ `rr` **نادرًا ما يقلب عضوية**.
⇒ **أثرُ أ-1 عمليًّا: صدقُ العرض وعدُّ النواقص، لا قصٌّ واسع** — يُقال صريحًا
ولا يُدَّعى أكثر.

🎯 **والحارس (د) مرّ:** المِجَسُّ الإنتاجيّ يُظهر `BBLG` **مقبولًا** في `A0`
و`B1` (درجة 60) و**مرفوضًا بـ`M_لا_مستوى_مختبر` في `B2`** (‏`tested_level`
تُرجع `None` له) — و«مسارُ الارتداد ❌ يُرفض» أيضًا.

📌 **وملاحظتان وصفيّتان (لا حكم):** `B1` ≡ الأساس عمليًّا في هذي السنة (‏d50
متساوٍ · `R` ‏+0.001) ⇒ **المستوى المُختبَر نادرٌ في مجتمعنا** · و`B2` تقصّ
**‏53.6%** من الإشارات وترفع `d100` من 6 إلى 9 و**الدقة +2.4 نقطة** لكنها
تُسيء `R` بـ‏−0.055. **والحكمُ يلزمه السنواتُ الثلاث** (‏2024/2025 تشغيلتان
قيد العمل) والحرّاسَ الأربعة مجتمعةً.

## ⑩ نتيجةُ 2024 (تشغيلة `31747852779`) — **الذراعُ الصارمة تقفز**

`V1`✅ · `V2`✅ (‏A0=0 · B1=0 · **B2=19,826**) · `V2ب`✅ (‏3177 ≥ 3177 ≥ 1591) ·
`V4`✅ · **`V3` ℹ️ خرقٌ مُعلَن**: `A0` مقيس **21** مقابل منشور 25 ⇒ «مقامٌ جديد»
(المقارنةُ بالمنشور باطلة · وبين الأذرع قائمة).

| ذراع | إشارات | **d50** |
|---|---|---|
| `A0` | 3177 | **21** |
| `B1` | 3177 | **20** |
| `B2` | 1591 | **32** |

🥇 **`B2` = ‏32 مقابل 21 = ‏+52%** بنصف الإشارات — أوّلُ فرقٍ ماديّ في التجربة.
⚠️ **ولا يُحكَم بسنةٍ واحدة** (الحرّاسُ أربعةٌ والسنواتُ ثلاث · و`B1` الحاكمةُ
المسجَّلة **أدنى** من الأساس هنا).
🔴 **وخرقُ `V3` هنا يُثبت أن `rrtruth` غيّر الأساسَ في 2024 (‏25 ⟶ 21) ولم
يغيّره في 2026 (‏15=15)** ⇒ **الأثرُ يتفاوت بالسنة** — وتصريحي «الخرقُ متوقَّعٌ
بالبناء» صحيحٌ لسنةٍ وباطلٌ لأخرى، فيُقرأ كذلك لا كقاعدة.

## ⑪ نتيجةُ 2025 (تشغيلة `31747851053`) — واكتمالُ السنوات الثلاث

`V1`✅ · `V2`✅ (‏B2=22,054) · `V2ب`✅ (‏3415 ≥ 3415 ≥ 1607) · `V4`✅ ·
**`V3` ✅ بت-بت** (‏A0 مقيس 26 = منشور 26).

### 📊 الحصيلةُ الثلاثيّة — `d50` (المنفجرون المُسلَّمون)

| سنة | `A0` الأساس | `B1` الحاكمة | **`B2` الصارمة** | إشارات (‏A0 ⟶ B2) |
|---|---|---|---|---|
| 2024 | 21 | 20 | **32** | 3177 ⟶ 1591 |
| 2025 | 26 | 28 | **41** | 3415 ⟶ 1607 |
| 2026 | 15 | 15 | **15** | 1587 ⟶ 736 |
| **مجمَّعًا** | **62** | **63** | **88** | ‏−52% إشارة |

🥇 **`B2` أعلى أو مساوٍ في السنوات الثلاث بلا انقلابٍ واحد** — ‏+52% ·
‏+58% · 0% ⇒ **مجمَّعًا ‏88 مقابل 62 = ‏+42%** بنصف الإشارات.
⚠️ **و`B1` (الحاكمةُ المسجَّلة) ‏63 مقابل 62 = فرقٌ داخل الضجيج** ⇒ **ترشيحُ
`B1` المكتوب قبل الأرقام (§④ «إن عبرت الاثنتان تُفضَّل الأقلُّ قصًّا») سقط
عمليًّا: المستوى المُختبَر نادرٌ فالارتدادُ يجعلها ≈ الأساس.**

🚧 **ولا اعتمادَ يُعلَن هنا — والسببُ مكتوبٌ في العقد قبل الأرقام:**
① الحارس (ب) `R`/صفقة **لم يُجمَّع بعد** عبر الثلاث (‏2026 وحدَها ‏−0.171
مقابل −0.116 = **أسوأُ من الحدّ ‏−0.02R**) ⇒ **مقايضةٌ تُعرَض على المالك لا
اعتمادٌ آليّ** · ② والحارس (ج) «‏≥30 صفقةً تختلف مِرساتُها» **لم يُقَس صراحةً**
(العدّادُ المطبوع رفضٌ لا مِرساةً مختلفة) · ③ **والقصُّ ‏52%** يعني نصفَ
الإشارات تختفي — وهو ما تنبّأ به `P2` (‏≥15%) **وتحقّق بأربعة أضعاف**.
🎯 **والتنبّؤات:** `P2` ✅ · `P3` ✅ (الحارس د مرّ في الثلاث) · `P1` 🔴 **مكذَّب**
(التغطيةُ أقلُّ من 40% بكثير — الدليلُ أن `B1`≈`A0`) · `P4` **صمد جزئيًّا**:
توقّعتُ ألّا يُحسَم اعتماد — ولم يُحسَم، **لكن لسببٍ آخر**: الفرقُ ماديٌّ
وواضح، والمانعُ حارسُ العائد لا ضعفُ الإشارة.
