# الجولة 3 — حكم أول تشغيل حي بعد إصلاحَي F-01/F-02 (لأوبيس)

> التاريخ: 2026-07-11 (صباح السبت بعد تجديد الجمعة 22:07 UTC)
> المصدر: تقرير مساعد التطوير + 3 ملفات CSV (trades/signals/missed بتاريخ 2026-07-10)
> + فحص `weekly_watchlist.json` من `origin/main` (commit `c802505`).
> الطبيعة: تحقق/تحليل فقط — **صفر تغيير كود مطلوب فورًا**. البنود المعلّمة 🟡 اختيارية لأوبيس.

## 1) ✅ F-01 مُثبَت حيًّا — قائمة الارتداد رجعت للحياة
أول تجديد بعد الإصلاح (2026-07-10 22:07) بنى قائمة الارتداد بـ**15 سهمًا**
(GVH·NA·VRAX·CAMP·CRVO·KC·ANNA·GEOS·RENX·AEC·AZ·GITS·ILLR·NBP·AAPG)
وكاشف الانفجارات راكم **441 متحرّكًا**. قبل الإصلاح كانت القائمة تُبنى **فارغة كل
جمعة بصمت** (تظليل `hist`). الإصلاح يعمل بالإنتاج — الملف مُغلق.

## 2) ⏳ F-02 — لا حالة تقسيم بعد (سلوك سابق حرفيًا حتى إشعار)
لم يقع تقسيم عكسي على أسهم القائمة منذ الدمج، فمسار `_split_scale_factor` لم
يُستدعَ بحالة حقيقية بعد (فاشل-آمن → عامل 1.0). **يُراقَب**: أول سهم قائمة يعلن
تقسيمًا عكسيًا = اختبار الإنتاج الأول؛ السبب المعروض سيحمل وسم «بعد تسوية تقسيم».

## 3) ℹ️ نقص بيانات 11/17 صفقة في trades CSV — بنيوي تاريخي، ليس خللًا (محسوم)
الصفوف الفارغة (SOGP/DJT/INSM/EZRA/NBP/CTMX/VSTM/UPB/NERV/OLMA/EVTV) كلها
سجلات تنبيه **قبل إصلاح A3 (2026-07-03)** — `record_new_alerts` (سطر ~8978) لم
يكن يخزّن tier/sector/rsi/float/rr وقتها، فالحقول **غير موجودة فيزيائيًا** في
`alerts_history.json`. الـmapping في `export_weekly_csvs` (سطر ~4549) سليم،
والتنبيهات ≥2026-07-03 كاملة (تأكّد بـ ACON/FEMY/GEOS). **يشفى ذاتيًا** مع دوران
الصفقات — لا إصلاح ممكن لبيانات ماضية لم تُخزَّن. **لا فعل.**

## 4) 📊 لقطة الأداء (17 صفقة محسومة)
- **71% نجاح (12✅/5🛑)** · أرضية Wilson 47% · توقّع **+0.5R** · متوسط أقصى ربح +14% (وسيط +13%).
- **اعتماد الذيل صحّي**: أعلى صفقتين = 29% من مجمّع الأرباح فقط → حافة عريضة لا يانصيب.
- أسبوع-مقابل-أسبوع: 100%→67% (فارق −33 نقطة) لكن العيّنة 6 صفقات — **اتجاهي لا حاسم**،
  والتقرير نفسه يمنع الضبط قبل 20 محسومة. **لا فعل.**
- ملاحظة تُراقَب: متوسط قوة الخاسرين (77) أعلى من الرابحين (68) — القوة ليست تنبؤية بعد
  (متّسق مع حكم §0-ز: لا مميِّز عند القاع).

## 5) 🚦 إشارتان ضعيفتان تحت المراقبة (لا تعديل أوزان الآن)
«فجوات متعددة الفريمات» 44% (9) و«معيد إجرام» 38% (8) — الأضعف بفارق واضح عن
البقية (67-100%). قاعدة التقرير: **ثبات النمط أسبوعين قبل أي تعديل أوزان**. أعِد
الفحص بتقرير الجمعة القادمة (2026-07-17)؛ لو ثبتا تحت 50% على عيّنة أكبر → اقتراح
ضبط بموافقة المستخدم.

## 6) 🟡 اختياري لأوبيس: تسوية التقسيم في أرباح «الفائتة/الانفجارات» (طبقة تقارير)
15 صفًا في missed CSV موسومة `suspect_split=True` بأرقام وهمية (INLF +11333% ·
NVVE +4254%...) — التقرير **يستبعدها بصدق** (سلوك صحيح). التحسين الممكن:
تمرير `_split_scale_factor` (موجودة، نقية، من F-02) على حساب `gain_10d` في
الفائتة/الانفجارات بدل الاستبعاد → أرقام حقيقية بدل فقد 15 عيّنة. **طبقة تقارير
فقط · لا LOGIC_VERSION · أولوية منخفضة** — نداء splits إضافي لكل مشبوه (سقف
15-20/أسبوع). قرار أوبيس/المستخدم.

## 7) 🔒 بوابات الرفض — الأقفال تصمد، لا تغيير
- **M4_base_واسعة** أكثر بوابة فوّتت (69 من 96 «كان ارتكازًا») لكن نسبة الفائتة/المقام
  2.8% فقط، وتجربة A/B (2026-07-03) حسمت أن تخفيفها **يُسقط النجاح 50%→40%** — قفل B3 يبقى.
- **M1_سعر**: 26/2077 = 1.3% — كفاءة عالية، لا مساس.
- **KELYB** (M5_سيولة، +33%) الوحيد الموسوم «ارتكاز تحرّك» — M5 هو الدرع المحسوم
  (تحقيق أسهم التغريدة الأربعة)، لا مساس.

## 8) ℹ️ KSCP −18.8% مقابل تصميم الوقف 5-7%
ليس خللًا: الوقف يُحسب 5-7% تحت **الدعم** وقت الترشيح، والنسبة المعروضة تُقاس من
**سعر التنبيه** (`entry_ref` = سعر الترشيح، قرار المستخدم المحسوم 2026-06-24) —
السهم رُشّح فوق دعمه فالمسافة الكلية للوقف أعمق من 7%. سلوك موثّق. **لا فعل.**

---
**الخلاصة لأوبيس:** الإصلاحان يعملان (F-01 مُثبَت · F-02 ينتظر أول حالة حقيقية).
لا عيب تشغيلي جديد. البند الوحيد القابل للتنفيذ = §6 (اختياري، تقارير فقط).
بندا مراقبة للجمعة القادمة: §2 (أول تقسيم) و§5 (الإشارتان الضعيفتان).
