# `T-TOPK` — «كم اسمًا يصل المالك؟» تسجيلٌ مسبق (مدفوعٌ قبل أيّ رقم)

**أمرُ المالك 2026-09-03:** «سجّل توب 15» — **ومعه تصحيحٌ يقلب اتّجاه السؤال:**

> «لو رفعنا هل منطقي اني بدخل في 15 سهم قبل يفتح السوق؟ **مب منطقي أصلا**.
> المفروض يكون المرشح **3 أسهم فقط بالكثير** وهي الأسهم اللي عليها الكلام، لأن
> عدد أسهم الارتكاز اللي تنفجر في اليوم **سهمين إلى 3 بالكثير** وفي أحسن
> الأحوال. فمب منطقي — المفروض يوصلني 3 أسهم وهي اللي تعتبر **توب التوب**.»

🔴 **وهو محقّ، والخطأُ خطئي:** عرضتُ «ارفع إلى 15» **⭐ أفضلَ خيار** وأنا أقيس
**التغطية** (‏كم منفجرًا نلتقط) وهو يقيس **التنفيذ** (‏كم اسمًا يستطيع دخوله
فعلًا) — **والتنفيذُ يعلو**، فقائمةٌ لا تُنفَّذ ليست قائمة.

---

## ① السؤال

**بأيّ شكلِ قطعٍ تُسلَّم «توب التوب»، وما ثمنُه بالرقم؟**
سؤالٌ عن **شكل التسليم** لا عن حكمِ التجربة.

## ② ما لا يُمَسّ (مثبَّتٌ قبل أيّ رقم)

- 🔒 **الحكمُ الرسميّ «فشلت» (‏`presession_prereg §⑥`) لا يتغيّر** — قِيس عند
  `TOPK=10` المسجَّل قبل الأرقام، وأيُّ رقمٍ هنا **لا يُرقّى حكمًا**.
- 🔒 **المفتاحُ المشحون لا يُبدَّل:** `post_hi_ret` للبريماركت · `usd_day`
  للافتر. تبديلُه بعد رؤية أرقام هذا الملفّ **`p-hacking` بحرفه**.
- 🔒 **النافذةُ الحاكمة الجلسةُ كاملةً** (تصحيحُ المالك 2026-09-03).
- 🔒 **بلا مسحٍ جديد:** الصفوفُ هي المرفوعةُ سلفًا (‏`33724354384` ·
  `33724361819` · `33724367680` · `33777092830`) ⇒ **الميزانيةُ ثابتةٌ بالبناء**
  والأذرعُ تُقاس على **نفس الكون ونفس الأيام ونفس المفتاح**، والمتغيّرُ
  **شكلُ القطع وحدَه**.
- 🔒 **صفرُ مسٍّ بالإنتاج:** لا `LOGIC_VERSION` · لا تغييرَ عتبةٍ حيّة ·
  و`PRESESSION_SEND: PM` وشكلُ الرسالة كما هما حتى يأمر المالك.

## ③ الأذرعُ — مثبَّتةٌ كاملةً قبل أوّل رقم (ولا تُضاف ذراعٌ بعدها)

**عائلةُ الرتبة (‏أمرُ المالك الحرفيّ «توب 15» داخلها):**

`K1` · `K2` · **`K3`** · `K5` · **`K10` (الأساس المنشور)** · **`K15`** · `K20`

**عائلةُ الأرضية المطلقة** — «توب التوب» بمعناه الحرفيّ: لا عددٌ ثابتٌ يُرسَل
كلَّ ليلة، بل **قراءةٌ متطرّفةٌ فقط**؛ فليلةٌ بلا متطرّفٍ **تُسلَّم صفرًا**:

`F1` · **`F3`** · `F5` — أرضيةٌ على قيمة المفتاح **مُعايَرةٌ على سنتَي المعايرة
(‏2023-2024) وحدهما** بحيث يكون **متوسّطُ الأسماء لكلّ قرار** 1 · 3 · 5 على
الترتيب، **ثم تُطبَّق كما هي حرفيًّا** على 2025 (خارج العيّنة) وعلى 2026
(وصفيّة). ⚖️ **والمعايرةُ على المعايرة وحدَها** — لا على سنة التقييم — بنفس
القاعدة التي اختير بها المفتاح.

**المركَّبة:** `C3` = `K3` ∩ `F3` (رتبةٌ ثلاثةٌ فأعلى **و** فوق الأرضية معًا).

## ④ المقاييسُ لكلّ ذراع (‏2025 خارج العيّنة · والبقيّةُ للسياق)

1. **أسماء/قرار:** المتوسّط · الوسيط · **ونسبةُ القرارات بصفر اسم**
2. **إصابات** (‏انفجرَ ‏+80% في نافذة الجلسة) · `P` · `R` · `lift`
3. **فاصلُ Wilson 95% لـ`P`** — إلزاميّ، لأن المقامات تصغر بسرعة عند `k`=1..3
4. **سقفُ العرّاف:** متوسّطُ المنفجرين لكلّ قرار — ليُقرأ رقمُ المالك («‏2-3
   باليوم») مقابل ما يقوله السوق فعلًا

## ⑤ سقفُ النجاح — **منحنى كلفةٍ واقتراحٌ واحد، لا شحن**

هذي **قرارُ تسليمٍ لا فرضيةَ حافّة** (سابقتا `T-AH-YEAR` و`T-CLIFF`): يُسلَّم
جدولٌ كاملٌ للمالك ومعه **اقتراحٌ واحدٌ مُعلَّل** — **ولا يُشحَن شيءٌ بلا أمره
الصريح**، ولا تُمَسّ عتبةٌ ولا رسالةٌ حيّة.

## ⑥ دعاوى قابلةٌ للتكذيب (تُنشَر محقَّقةً أو مكذَّبة)

- **`T1` (الحاسمة):** **الترتيبُ مميِّزٌ داخل العشرة** ⇒ `P@3 ≥ 1.5 × P@10`
  على 2025. 🔴 **وإن سقطت فالمعنى ثقيل:** رأسُ القائمة ليس أفضلَ من ذيلها،
  والقطعُ إلى ثلاثةٍ **يقصّ الإصابات بنسبتها بلا مكسبِ دقّة** ⇒ **الحلُّ ليس في
  العدد بل في المفتاح.**
- **`T2`:** تحت `F3` تكون **نسبةُ القرارات بصفر اسم ‏≥ 40%** (أغلبُ الليالي لا
  ينفجر فيها شيء).
- **`T3`:** `hits@3 ÷ hits@10 ‏≤ 0.5` (القطعُ يفقد نصفَ الإصابات على الأقلّ).
- **`T4`:** رتابةٌ نازلة `P@1 > P@3 > P@10`. **وانكسارُها يعني أن رأسَ الترتيب
  ضجيج.**

## ⑦ حدودُ صدقٍ تُكتَب قبل الأرقام

- **مقاماتٌ صغيرة:** عند `k`=1 المقامُ ‏≈250 قرارًا/سنة ⇒ فاصلُ Wilson عريض ⇒
  **الاتّجاهُ يُقرأ والرقمُ لا يُدقَّق.**
- **«انفجر» = لمسُ ‏+80%** بأرضية تنفيذٍ ‏$20,000 — **لا عائدَ مقيسًا** ⇒ سقفٌ
  متفائلٌ في كلّ الأذرع بالتساوي.
- **سنةُ تقييمٍ واحدة** (‏2025) · و2026 **وصفيّةٌ خارج العقد**.
- **الأرضيةُ مُعايَرةٌ على مستوى القيمة لا على مستوى القرار** ⇒ عددُ الأسماء
  يتفاوت بين الليالي **بالتصميم**، وهذا هو المقصود لا عيبٌ فيه.
- **ولا يُقاس هنا أثرُ التسليم على المالك** (هل يدخل فعلًا؟ بأيّ حجم؟) — خارج
  ما تستطيعه البيانات.

## ⑧ الأقفال

`PS30` سلّمُ الرتبة **رتيبٌ بالبناء** ومن `group_ranks` نفسِها (‏AST) ·
`PS31` المعايرةُ على قناع المعايرة **وحدَه** وتقلب اتّجاهَها للمفاتيح الصاعدة ·
`PS32` `C3` **تقاطعٌ** لا اتّحاد. وكلٌّ منها تُسقطه طفرةٌ مسمّاة.
