# 🧭 T-M2REF — تسجيل مسبق: «مرجع الهبوط الواعي بالتقسيم» (علاج المصيبة C1)

> **مدفوع قبل أيّ رقم.** أمر المالك 2026-08-08: «**قسها**» — بعد أن أظهر مِجَسّ TDIC
> الحيّ (تشغيلة `31239914262`) أول حالة حيّة تهمّه يحجبها سقف M2 بقراءة هبوطٍ
> **100%** بينما هبوطه من قمة دورته الفعلية ($59 في مايو) ≈ **95.4%** = داخل نطاق
> فيصل [89.6–99.7] — والفارق كلُّه من قياس الهبوط على سلسلةٍ معدَّلة بالتقسيمات
> (المصيبة C1 الموثّقة في `OPUS_EXECUTION_PACKAGE.md`؛ نمط NUWE 908.25 لسهم $20.65).

---

## ① السؤال المسجَّل

هل استبدال مرجع M2 (قمة 52 أسبوعًا **المعدَّلة**) بـ**قمة ما بعد آخر تقسيم عكسي**
(`_post_split_high` — الشموع بعده حقيقية غير معدَّلة، بلا نظر مستقبلي: تقسيمات
معروفة حتى تاريخ الشمعة فقط) يرفع **المنفجرين المُسلَّمين لخانات السعة** دون أن
يفتح بوابة ضجيج أو يهدم العائد؟

**وسؤال الآلية المصاحب:** كم رمزًا في السوق يحجبه السقف زورًا بالمرجع المعدَّل
وينفتح بالمرجع الواعي؟ (حجم فئة C1 — يُقاس لأول مرة على السوق الكامل.)

## ② ما نعرفه سلفًا (يُدوَّن قبل الأرقام حتى لا يُعاد اكتشافه انتقائيًّا)

- **T-JEM (2026-07-23):** الدالة تستخرج مرجع فيصل الحرفي بدقة (JEM 6.90 · WORX
  2.33 · EHGO 2.80) — الأداة صحيحة ميكانيكيًّا.
- **T-GATES §② (2026-07-31):** على لقطة `30598885456` المرجعُ الواعي **نقل الجدار
  ولا يفتحه** (`فوق_97` 177⟶0 لكن `تحت_40` 112⟶195) بل **وأضرّ** (EHGO مرّ بـ92%
  فصار يسقط بـ40%) — ⚠️ لكن ذلك قيس على **عتبات الإنتاج القديمة** (أرضية 40) لا
  على نطاق فيصل الحالي [89.6–99.7]، وعلى لقطةٍ تقسيماتُها من ياهو.
- **NUWE 908.25:** قراءة `_post_split_high` الملوّثة على تلك اللقطة — سببها
  المرجَّح **نقص سجلّ التقسيمات** لا عيب المعادلة: النسبُ داخل النافذة **ثابتة
  تجاه التقسيمات المستقبلية** (كل الأسعار قبل تقسيمٍ لاحق تُضرب بنفس المعامل ⇒
  حاصل price/hi52 لا يتغيّر)، فالتلوّث الوحيد الممكن = تقسيمٌ **داخل** النافذة
  غائبٌ من السياق. **لقطات PIT الثلاث تقسيماتُها من مرجع Polygon** (`pit_snapshot`
  · قفل PITS2) = أكمل من ياهو ⇒ الاعتراض القديم «لا يُتحقّق بلا سعرٍ خام» يُختبر
  هنا لا يُسلَّم به.

## ③ الأذرع (مثبَّتة — ولا ثالثة)

| ذراع | الوصف |
|---|---|
| **R0** | الإنتاج كما هو (معايير فيصل العشرون · مرجع M2 = قمة 52أ المعدَّلة) |
| **R1** | نفسه حرفيًّا + `BT_SPLIT_REF_M2=1` (المرجع = قمة ما بعد آخر تقسيم عكسي معروف حتى تاريخ الشمعة؛ لا تقسيم ⇒ يرتدّ للمرجع المعدَّل = R0 لذلك الرمز) |

- **عزل تامّ:** كل ذراع في **عملية منفصلة** (subprocess) بيئتُها تُضبط قبل
  الاستيراد — درس بصمة الـno-op (`BT_CANDLE`).
- نفس اللقطة · نفس الكون · نفس النافذة · نفس المُرتِّب (`rank_live`) · نفس
  السعة (10) · **بلا `free_of`** (سياسة الخانات متعامدة مع سؤال العضوية —
  تثبيتها على «بلا» يطابق عرف T-RANKER2/T-SLOT الأساس).

## ④ البيانات

لقطات PIT الثلاث (نفس لقطات أذرع المحفظة/T-RANKER2/T-SLOT/T-CAP):
**2024=`31199493957` · 2025=`31191327731` · 2026=`31199510868`** — كلٌّ عبر
`load_frozen_dataset` الإنتاجية، والتقسيمات من اللقطة نفسها (Polygon).

## ⑤ المقاييس (تُقفَل الآن)

1. **الأساسي:** المنفجرون المُسلَّمون d50 (مأخوذٌ بلغ `EXPLOSION_PCT`=50% قبل
   وقفه · غير المُعبَّأ لا يُعدّ) لكل سنة + المجمَّع — R1 مقابل R0. (وd100 يُنشر
   وصفيًّا.)
2. **حارس العائد:** متوسط R للصفقة (r_unit على المأخوذين): فرق R1−R0 المجمَّع
   **≥ −0.05R** — نفس حارس T-CAP.
3. **الآلية (يقين وصفي يُنشر بالأرقام):** مسحٌ نقيّ على لقطة كل سنة عند آخر شمعة:
   - `blocked_adj` = هبوطه المعدَّل > 99.72 (محجوب بالسقف اليوم)
   - `opened` = محجوبٌ **وله** مرجعٌ واعٍ يضعه **داخل** النطاق [89.6، 99.7]
   - `shifted_low` = محجوبٌ ومرجعُه الواعي يُنزله **تحت** الأرضية (نمط EHGO —
     الجدار انتقل ولم ينفتح)
   - `still_blocked` = محجوبٌ ويبقى فوق السقف حتى بالمرجع الواعي
4. **بوابات الصلاحية (أيّ سقوطٍ فيها = لا حكم، تُصلَح الأداة وتُعاد):**
   - **V1:** R0 يُعيد أرقام K-LIVE/S-LIVE الموثّقة **بت-بت**: d50 = **22 (2024)
     · 14 (2025) · 6 (2026)**.
   - **V2:** سطر «العلم فعّال: قرأ لقطة splits لـN رمزًا» في R1 بـ**N > 0**
     (وإلا فالذراع no-op — المصيبة 7).
   - **V3 (مِرساتا شفافية):** تُطبع قيم المرجع الواعي لـ**TDIC وNUWE** من لقطة
     2026 (تاريخ آخر تقسيم عكسي · المرجع · الهبوط الناتج) — وغيابُ أيٍّ منهما من
     اللقطة **يُعلَن** لا يُتخطّى بصمت. قراءة NUWE بمئات الدولارات مجدّدًا =
     سجلّ تقسيمات ناقص ⇒ **R1 باطلة**.
5. **TDIC (وصفي):** هل ينقلب جدار M2 عنده في ذراع R1 بلقطة 2026؟ (لا يدخل الحكم
   — سؤال المالك الأصلي يُجاب بالاسم.)

## ⑥ قاعدة القرار (تُقفَل الآن)

- **يُقترح الاعتماد على المالك** فقط إذا: d50(R1) ≥ d50(R0) في **كل** سنة ·
  **و**المجمَّع أكبر **بأكثر من 1** · **و**حارس العائد مستوفى · **و**بوابات
  الصلاحية الثلاث خضراء.
- فرق المجمَّع ∈ {−1، 0، +1} ⇒ **«لا فرق مادي»** — لا اقتراح تحسين، **وتُنشر
  أرقام الآلية** (كم محجوبًا زورًا) ليقرّر المالك على أساس «تصحيح صدق القياس»
  لا «تحسين عائد» — والتمييز بين الأساسين **يُكتب في النتيجة صراحةً**.
- أيّ سنة أسوأ أو حارس مكسور ⇒ **C1 تبقى مغلقة إنتاجيًّا** وتُنشر الأرقام.
- **أيّ اعتمادٍ لاحق = قرار مالكٍ حصريّ** (تغيير عضوية ⇒ `LOGIC_VERSION` +
  `force_renew=1`) — هذه التجربة نفسها **صفر مسّ إنتاج** (العلم مطفأ افتراضيًّا).

## ⑦ حدود الصدق (تُدوَّن قبل النتائج)

- 2026 سنة جزئية · الأعداد صغيرة فالمعيار **ثبات الاتجاه** لا p-values.
- المرجع الواعي يعتمد اكتمال سجلّ تقسيمات اللقطة — V3 هي الكاشف، ولا نملك سعرًا
  خامًا مستقلًّا للتحكيم عند تعارضٍ غير محسوم.
- حصّة البقاء: كون اللقطة هو الكون (مشترك بين الذراعين فلا يلوّث **المقارنة**،
  لكنه يقيّد التعميم).
- «انفتاح» الرمز لا يعني ربحيّته — d50 هو الحكم على ذلك، لا عدّاد `opened`.

## ⑧ ما الذي يكذّب ماذا (تنبّؤات قابلة للكسر، تُكتب الآن)

- **P1:** فئة C1 موجودة وليست هامشية: `opened` المجمَّع عبر السنوات > 10 رموز.
- **P2:** جزء معتبر من المحجوبين يُعيد نمط EHGO (`shifted_low` > 0) — المرجع
  ينقل بعض الجدران لا يفتحها.
- **P3:** TDIC ينقلب في 2026 (drop ≈ 95% داخل النطاق) — إن لم ينقلب فتشخيصي
  للمِجَسّ الحيّ كان ناقصًا ويُصحَّح علنًا.
- **P4:** d50 لا يقفز قفزًا كبيرًا (الفتحُ يورّد مرشّحين للمُرتِّب والسعة، وهما
  عنق الزجاجة المقيس) — تحسّن متواضع إن وُجد.

---
*التشغيل: `m2ref.yml` (سنة + frozen_run_id) — 3 تشغيلات. النتيجة تُكتب في
`m2ref_result.md` بعد اكتمال الثلاث ولا تُفسَّر تشغيلة قبل أختها.*
