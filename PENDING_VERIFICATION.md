# بنود قيد التحقّق — آخر تحديث 2026-07-04 (خطة الإغلاق OPUS_NEXT_PLAN)

> ✅ **لا بنود تطوير معلّقة فعّالة.** كل الشكوك حُسمت (بتجارب/أدلة أو قرارات موثّقة).
> البند الوحيد المتبقي = **مراقبة إنتاجية** لتشغيل جمعة 2026-07-10 (لا كود مطلوب).
> ما حُسم مُثبت أدناه حتى لا نعيد فتحه بلا دليل جديد.

---

## أ) حُسم في تدقيق 2026-06-30

| # | البند | الحكم | الدليل |
|---|---|---|---|
| 1 | وصول إصلاح `was_pivot` إلى main | **حُسم: وصل ويعمل إنتاجيًا** | `weekly_watchlist.json` فيه 141 انفجارًا؛ لم تعد كلها `was_pivot=false`: ظهر `UPC` كـ`was_pivot=true` (+311%). |
| 2 | تصنيف `M4_base` كهوية/بنية لا كفرصة فائتة | **حُسم جزؤه المؤكد: لا رجوع لضجيج M4_base** | ضمن الانفجارات الحالية: `M4_base_واسعة` = 59، وكلها `was_pivot=false`; لا دليل إنتاجي على أن `M4_base` صار يُفبرك فرصًا فائتة. |
| 3 | `signals` CSV و`short_pct` | **حُسم: يعمل** | توليد CSV التدقيق أظهر عمود `short_pct` وقيمًا فعلية للقائمة الحالية: OPRX=10.3، NEGG=26.4، KSCP=16.7. |
| 4 | «صفقات محسومة = 0» | **حُسم كخطأ تقريري وأُصلح** | `weekly_watchlist` وحده يعطي 0، لكن `alerts_history.json` فيه 6 صفقات محسومة (5 أهداف، 1 ستوب). أُصلح مساعد التطوير و`trades` CSV ليقبلا `alerts_history` صراحةً. |
| 5 | المصدر المحلّل كان قديمًا | **حُسم** | تدقيق 2026-06-30 تم على main الحالي (`HEAD` 107555a) و`weekly_watchlist.json` المحلي الحالي (`week_start=2026-06-26`). |
| 6 | تلوّث الأرشيف: هل زاد بعد 2026-06-26؟ | **حُسم جزئيًا: لم تظهر أرشيفات وهمية جديدة بعد 2026-06-26** | مقارنة commits البيانات: history بقي 15 من 9e1cf44/fa07ac7/5bf685d إلى HEAD؛ لم يزد بعد ذلك. قرار التنظيف/الحارس ما زال معلّقًا أدناه. |

اختبارات بعد الإصلاح الآمن: **183/183**. `pyflakes Super_stock.py dev_assistant.py test_bot.py` نظيف.
لم تُمسّ جذور الفرز/الدخول/الوقف/الأهداف/المستويات؛ التعديل في طبقة تقرير/CSV واختبارات فقط، لذلك لا رفع `LOGIC_VERSION`.

---

## ب) حُسم في تدقيق 2026-07-01

| # | البند | الحكم | الدليل |
|---|---|---|---|
| 1 | تحقق إنتاجي من إصلاح `alerts_history` في مساعد التطوير | **حُسم: يعمل على main الحالي** | `_collect_closed_alerts` + `_dedup_closed` تحتسب 6 صفقات من `alerts_history.json`: DJT stopped، INSM t1، NBP t3، CTMX t2، UPB t1، NERV t3. `build_dev_assistant_report` يعرض `صفقات محسومة متراكمة: 6`. |
| 2 | عدم إرسال تقرير Cline قديم عند فشل تقرير اليوم | **حُسم كخطأ عرض وأُصلح** | `cline_notify.py` صار يقبل تقرير اليوم فقط إلا عبر `CLINE_REPORT_PATH` صريح؛ أُضيف اختباران في `test_bot.py` يثبتان الحارس والتجاوز الصريح. |

اختبارات بعد الإصلاح الآمن: **185/185**.
لم تُمسّ جذور الفرز/الدخول/الوقف/الأهداف/المستويات؛ التعديل في طبقة إشعار Cline واختباراتها فقط.

---

## ج) البنود التاريخية — **كلها محسومة ✅** (للسجل، لا تُعاد إلا بدليل جديد)

### 1) تواتر ضعف التغطية — **حُسم عمليًا (2026-07-03, A4)** ✅
- عيّنة تشغيل 2026-07-03 (سجل Actions): تغطية **94.6% (3366/3559)** بلا أي تحذير،
  وإعادة المحاولة استرجعت 193 رمزًا (الفاشلان الوحيدان delisted فعلًا).
- **والأهم: القياس أُتمت** — `record_reject_stats` (A1) يخزّن `universe/valid` يوميًا في
  `weekly_watchlist.json` → تقرير الجمعة يعرض التواتر بالأرقام تلقائيًا. لا فحص يدوي بعد اليوم.

### 2) ثغرة «RSI لم يتشبّع» — **حُسمت (N1، 2026-07-04)** ✅ يبقى مرفوضًا
الموضع: `Super_stock.py:1504-1508` — قاع RSI أعلى من 32 يضيف «لم يصل التشبّع بعد»
بمسار الارتداد لكن لا يضبط `risen=True` فلا يدخل المراقبة.
- **القرار (الخيار أ، توصية مؤكّدة):** يبقى كما هو. فيصل (تغريدة 8057): «قبل ينفجر
  RSI بين 23-27» — القاع غير المتشبّع = **نموذج ارتكاز غير مكتمل**، وإدخاله يحشو
  قائمة الارتداد (سقف 15) بحالات أضعف على حساب المكتملة. لا تغيير في جذر القبول/الرفض.
- الخيار (ب) المرفوض: إدخاله المراقبة بـ`risen=True` وأولوية أدنى — متاح لو أراده
  المستخدم لاحقًا (موافقة صريحة + اختبار)، لكن لا دليل يبرّره الآن.

### 3) عتبة M4 الحقيقية — **حُسمت بتجربة A/B (2026-07-03)** ✅ تبقى 40%
- **المقام (سجل 2026-07-03):** M4_base_واسعة = 309/يوم → فائتة واقعية 11.7% · انفجارات ارتكازية 0.65%.
- **التجربة (باكتيست 132 رمزًا، نفس العيّنة والتاريخ):**
  A (40/40 الأصلية): محسومة=4 · نجاح **50%** (2✅/2🛑) · B (50/35 التخفيف): محسومة=5 ·
  نجاح **40%** (2✅/3🛑) — التخفيف أضاف **خسارة وصفر أرباح** رغم أن العيّنة منحازة
  لصالحه (كلها صعدت ≥30% فعليًا). معيار القرار المسجّل مسبقًا فشل.
- **القرار المحسوم:** `BASE_RANGE_MAX_PCT=40` و`MIN_DROP_FLOOR=40` تبقيان — **قفل
  اختبار جديد (B3)** يحرس القرار. إعادة الفتح تحتاج دليلًا جديدًا (عيّنة أكبر بكثير).

### 4) حارس الأرشفة + تنظيف التاريخ المكرَّر — **نُفّذ (N2، 2026-07-04)** ✅
كان `history` فيه 16 إدخالًا، منها `2026-06-21` مكرَّر ×9 (تشغيلات متعددة بلا حارس).
- **الحل:** `_dedup_history` يبقي أحدث إدخال لكل `week_start` — مطبَّق في **حارس الأرشفة**
  (`run_weekly_renewal`: append ثم dedup) و**`save_watchlist`** (ينظّف على كل حفظ = ذاتي
  الشفاء، لا يحتاج رفع نسخة). تحقّق حي: 16 → **5 أسابيع فريدة**. قفلان في `test_bot.py`.
- الملف الحي يُنظَّف تلقائيًا أول تشغيل إنتاجي (لم يُلمس يدويًا تفاديًا لتضارب البيانات).

### 5) `except Exception: return None` في `analyze_ticker` — *كامن لا فعّال*
ما زال يمسك أي استثناء داخل جذر الفرز ويرجّع `None` صامتًا.
- **اقتراح آمن لاحق:** تسجيل السبب/الرمز قبل `return None` بدون تغيير نتيجة الفرز.
- يحتاج موافقة لأنه داخل مسار التحليل الأساسي.

### 6) حالة `UPC` — **حُسمت مع تجربة A/B (2026-07-03)** ✅ أرضية M2 تبقى 40%
- **المقام:** M2_هبوط_تحت_40 = 1867/يوم → انفجار ارتكازي واحد = **0.05%** (شذوذ إحصائي).
- **التجربة:** UPC/TC/CLRO كانت ضمن عيّنة A/B — تخفيف الأرضية لـ35 (تشغيل B) لم يضف
  أي ربح (خسارة إضافية فقط، النجاح 50%→40%). البوابة تعمل كما صُممت.
- **القرار:** لا تغيير في M2. (UPC نفسه موسوم 🔍 قفزة خارقة قد تكون أثر تقسيم.)

### 7) تنبيه Cline الحقيقي في GitHub Actions — *تحقق نشر/أسرار*
الإصلاح المحلي يمنع إرسال تقرير قديم، لكن يلزم تحقق إنتاجي من تشغيل Actions.
- **افحص التشغيل القادم:** إذا فشل إنشاء `reports/cline_weekly_<اليوم>.md` يجب أن تصل رسالة تحذير «لم يُعثر على تقرير هذا الأسبوع» لا ملخص تقرير قديم.
- **افحص الأسرار:** `TELEGRAM_BOT_TOKEN` و`TELEGRAM_CHAT_ID` موجودة وتسمح بالإرسال.

---

## ملخّص القرارات — **كلها محسومة ✅ (لا معلّق فعّال)**
1. ~~مراقبة أسهم RSI غير المتشبّعة؟~~ → **حُسمت (N1)**: تبقى مرفوضة (فيصل 23-27).
2. ~~تنظيف الأرشيف + حارس أرشفة؟~~ → **نُفّذ (N2)**: `_dedup_history` + قفلان.
3. ~~تسجيل استثناء `analyze_ticker`~~ → **نُفّذ (C4)**: تسجيل تشخيصي + قفل.
4. ~~تصدير مقام الرفض~~ → **نُفّذ (A1)**: `record_reject_stats` يوميًا.
5. ~~مراجعة UPC~~ → **حُسمت (تجربة A/B)**: لا تغيير في M2.
6. **تحقق مراقبة فقط (جمعة 2026-07-10):** تنبيه Cline يصل + كل إضافات التقرير الجديدة
   تعمل إنتاجيًا (تشيك-ليست N4 في `OPUS_NEXT_PLAN.md`). آخر بند مراقبة — لا كود.

> **الحالة:** المشروع دخل وضع «تشغيل ومراقبة» — لا بنود تطوير معلّقة. التطوير القادم
> يقوده تراكم البيانات الحية (تقرير الجمعة + تدقيق Cline)، لا الفحص اليدوي.
7. ~~نتائج الباكتيست A/B~~ → **حُسمت (2026-07-03)**: التخفيف فشل (نجاح 50%→40%) —
   العتبتان تبقيان 40/40 بقفل اختبار B3. (بند 3 و6 أعلاه بالتفصيل.)
