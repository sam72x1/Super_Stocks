# 🪞 `T-MIRROR` — تسجيلٌ مسبق: **المرآةُ ×2**

> **أمرُ المالك (2026-08-28): «اعتماد ١ ٢ ٣ ٤».** العقدُ الثاني — **مدفوعٌ قبل
> سطرِ كودِ أداةٍ واحد** ولا يُمَسّ بعد أوّل رقم.

## ⓪ المصدرُ ومقياسُه — والاكتشافُ الذي وَلَّده

**النصُّ الحرفيّ** (صور 2026-08-28، `faisal_images/_incoming_20260828/`):
سلالمُ فيصل الأربعة بأرقامها كما رسمها بيده على `CUPR` و`PPCB` (يوميّ و4س)
و`HCWB`. **وفكّكتُها حسابيًّا فظهر أن كلّ دعمٍ عنده له مستوًى عند ‏×2 تمامًا:**

| السهم | المرايا المرصودة |
|---|---|
| `HCWB` | ‏2.050⟶4.186 (×2.04) · 2.591⟶5.190 (×2.00) · 2.897⟶5.819 (×2.01) · 3.262⟶6.462 (×1.98) |
| `PPCB` يوميّ | ‏0.950⟶1.969 (×2.07) · 1.969⟶4.088 (×2.08) · 4.986⟶10.120 (×2.03) |
| `PPCB` 4س | ‏0.9473⟶1.945 · 1.511⟶3.023 · 2.260⟶4.440 |
| `CUPR` | ‏2.502⟶4.974 · 2.854⟶5.782 |

**‏12 زوجًا من 121 زوجًا ممكنًا. وشاهدا ضبطٍ شُغّلا قبل هذا العقد:**
- شاهدٌ موحَّد (‏20,000 محاكاة): وسيطُ الصدفة **6** ⇒ **p = 0.028**
- 🔴 **وشككتُ في شاهدي**: سهمٌ هبط النصفَ يصنع مرايا ×2 **بالبناء** ⇒ بنيتُ
  شاهدًا **بعنقودَي دعمٍ ومقاومةٍ بنفس تباعد كلّ سلّم** ⇒ **يصمد: p = 0.036**
- **ويشتدّ بتضييق التسامح**: عند 2% ⟶ 8 مقابل 3 ⇒ **p = 0.014**
- **و‏×2 وحدَه يبرز**: 1.5 (p=0.42) · φ (0.40) · √2 (0.42) · 2.5 (0.12) ·
  3 (0.47) ⇒ **سلالمُه ليست فيبوناتشي**

⚖️ **واختيارُ ×2 نظريٌّ لا منقَّب**: قاعدةُ «÷2» موثَّقةٌ بلفظ فيصل منذ
`IMG_0150`/`IMG_0153`. **والجديدُ الاتجاه**: كلُّ ÷2 في كودنا **نازلة**
(`_split_day_value` · `SPLIT_RADAR_BAND_LOW` · و`half_down_target`
**متقاعدة**)، ولا موضعَ واحدٌ يستعملها **صاعدة**.

## ① التعريفُ الإجرائيّ — مثبَّتٌ ولا يُحرَّك

عند نقطة الإشارة (‏بيانات ما قبلها حصرًا):
- **الدعوم** = `key_levels` الإنتاجيّة (‏أساسيّ + فرعيّ) و`pivot`.
- **المقاومات** = `resistance_levels` الإنتاجيّة.
- **مرآةٌ نظيفة** = وجودُ مقاومةٍ `Rj` ودعمٍ `Si` بحيث `|Rj/Si − 2| / 2 ≤ 0.04`.
- **`mirror_n`** = عددُ المرايا · **`mirror_best`** = أقربُ نسبةٍ إلى 2 · و
  **`mirror_ok`** = `mirror_n ≥ 1`.

⚖️ **والتسامحُ 4% مثبَّتٌ من القياس أعلاه** (‏هو التسامحُ الذي عليه `p=0.036`)
⛔ **ولا يُحرَّك إلى 2% أو 6% بعد رؤية جدول الأذرع.**

## ② المجتمعُ والأذرع

المجتمع: **إشاراتُ الفارز** في لقطات PIT الثلاث — الميزانيةُ الثابتة نفسُها.

| الذراع | التعريف |
|---|---|
| **`M0`** | كلُّ الإشارات (الأساس) |
| **`M1`** | الإشاراتُ التي `mirror_ok` (**الحاكمة**) |
| **`M̄1`** | مُكمِّلُها — **شاهدُ التكذيب الإلزاميّ** |

## ③ المقياسُ الحاكم

**`R` المحقَّق لكلّ صفقة** بمحرّك الحسم الإنتاجيّ. والمساند (وصفيّ):
معدّلُ بلوغ ‏+50% قبل الوقف (`mg_pre_stop`).

🔴 **وهذا مقياسُ انتقاءٍ لا خروج** — «هل تميّز المرآةُ السهمَ الذي ينفجر؟»
⛔ **ولا يُقاس ×2 هدفًا للخروج**: `T-EXIT` قاست هدفًا ثابتًا ‏+100% فأعطى
**‏−0.62/−0.80/−0.90R** (يُبلَغ 1-3% فقط) ⇒ **الباب مُغلَق ولا يُعاد فتحُه هنا.**

## ④ المعايير — الأربعةُ تلزم معًا

1. `M1 − M̄1` **‏≥ +0.15R** مجمَّعًا (عتبةٌ مُعادة)
2. موجبٌ في السنوات الثلاث بلا انقلاب
3. فاصلُ 95% لا يلمس الصفر
4. أرضية **‏150 محسومةً مجمَّعًا و30/سنة لكلّ ذراع**

## ⑤ بوّاباتُ الصلاحية

| البوّابة | الشرط | الإخفاق |
|---|---|---|
| **`MV0`** | `M0` يُعيد رقمًا **منشورًا** للأساس بت-بت | خروج **3** |
| **`MV1`** | `M1` و`M̄1` **تفترقان** عضويّةً · و`M1` ليست فارغةً ولا الكلَّ | خروج **4** |
| **`MV2`** | **حصّةُ `mirror_ok` بين ‏5% و60%** — دونها نادرةٌ بلا معنًى وفوقها لا تميّز | يُعلَن ويُقاس (لا يوقف) |
| **`MV3`** | إعادةُ إنتاج: الأداةُ تُخرج **‏12 مرآةً** على سلالم فيصل الأربعة | خروج **3** |
| **`MV4`** | الدعومُ والمقاوماتُ من **دوالّ الإنتاج بالاسم** (AST) لا نسخةٍ منها | قفلُ سويّة |

## ⑥ التنبّؤات

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| `MR-P1` | **لا تبلغ +0.15R** — أرجّح الفشل (‏سبعُ سقطاتٍ لعائلة «سلوكٌ يميّز المنفجر») | تبلغها |
| `MR-P2` | حصّةُ `mirror_ok` **‏20-50%** من الإشارات | خارجه |
| `MR-P3` | `MV3` يعبر — الأداةُ تُعيد الاثنتَي عشرة | يسقط ⇒ عطبُ أداة |
| `MR-P4` | `mirror_n ≥ 2` **لا يتفوّق** على `mirror_n = 1` (لا سلّم) | يتفوّق رتيبًا |
| `MR-P5` | تشديدُ التسامح إلى 2% **يُنقص العيّنةَ دون الأرضية** | يبقى فوقها |

## ⑦ سقفُ النجاح

**اقتراحٌ للمالك + سطرُ عرضٍ («‏🪞 مرآة ×2 عند $X»)** — ⛔ **ولا بوّابةَ رفضٍ
ولا وزنَ ترتيبٍ ولا هدفَ خروجٍ** مهما كانت الأرقام.

## ⑧ حدودُ صدق

1. **أربعُ سلالمَ فقط** وَلَّدت الفرضية — والقياسُ على السوق هو الاختبار.
2. **الدائريّةُ منتفيةٌ بالبناء** (السلالمُ من صورٍ لأسهمٍ خارج عيّنة القياس).
3. **مستوياتُنا ليست مستوياتِه**: `key_levels`/`resistance_levels` حسابُنا،
   وفيصل «يحطّ لاين عرضي بيده» ⇒ **نقيس بنيةً شبيهةً لا بنيتَه.**
4. **`p = 0.036` على 4 سلالم** — إشارةٌ لا برهان، والاختبارُ هذا هو الحكم.

## ⑨ ملحقٌ مؤرَّخ (2026-08-28) — تعريفُ فِكستشر `MV3` · **قبل أيّ رقمِ ذراع**

🔴 **الدافع:** أوّلُ فحصِ دخانٍ لـ`mirror_pairs` على السلالم الأربعة أعطى **‏8**
لا 12، **فوقفتُ قبل الحكم** — فإمّا العقدُ يصف ما لا تفعله الأداة، وإمّا الفِكستشر
يكذب. **والسببُ الفِكستشر:** قسّمتُ كلَّ سلّمٍ بيدي إلى «دعومٍ ومقاومات» بينما
`§⓪` حسبَ **كلَّ الأزواج داخل السلّم** — ودليلُه في `§⓪` نفسِه: **«‏12 زوجًا من
‏121 زوجًا ممكنة»**، و‏121 = ‏36+28+21+36 = مجموعُ `C(n,2)` للسلالم الأربعة
**غيرَ مقسَّمة**. ⇒ **الرقمُ لم يُحرَّك ولا يجوز تحريكُه؛ المُصحَّحُ نصُّ الفِكستشر.**

**التعريفُ المُثبَّت لـ`MV3`:** يُمرَّر **كلُّ سلّمٍ قائمةً واحدة** إلى طرفَي
`mirror_pairs` (‏`mirror_pairs(L, L)`) ⇒ المتوقَّع **‏12 مرآةً** موزَّعةً
**‏CUPR 2 · PPCB يوميّ 3 · PPCB 4س 3 · HCWB 4**، ومجموعُ الأزواج الممكنة **‏121**
— **والعددان يُقفلان معًا** فلا يُرضي القفلَ مجموعٌ صحيحٌ بتوزيعٍ خاطئ.
**ومُتحقَّقٌ أن الأداة تُعيدهما بت-بت** قبل كتابة هذا الملحق.

⚠️ **وحدُّ صدقٍ يُنشَر مع الرقم لا بعده (يلحق `§⑧`):** الإنتاج
(`_mirror_augment`) **يقسّم** الدعومَ عن المقاومات بالبناء (‏`key_levels` و`pivot`
مقابل `resistance_levels`) ⇒ **لا يرى الزوجَ الذي طرفاه في الجهة نفسها**، وهو
بالضبط ما أسقط ‏4 من 12 في فحص الدخان (‏`PPCB` يوميّ: `0.950⟶1.969` و
`1.969⟶4.088` كلاهما تحت السعر). ⇒ **حصّةُ `mirror_ok` الحيّة أرضيّةٌ لا سقف**،
و`MV2` يُقرأ على هذا الأساس. ⛔ **ولا يُبدَّل تقسيمُ الإنتاج ليرفع العدّاد** —
تبديلُه بعد رؤية 8 هو تحريكُ هدفٍ بعينه.
