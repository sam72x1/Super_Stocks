# 🕰️🥇 `T-HOLD-KEY` — «الأطولُ حفظًا أوّلًا» عند الكثافة الحيّة وبمفاتيحَ محياة

> **أمرُ المالك (2026-08-27):** «‏**سجل مفتاح الحفظ. «قس عند الكثافة الحية احي
> المفاتيح الخامدة**» — الأبوابُ الثلاثةُ التي فتحها `press_rank_result §⑨`.
> **هذا عقدُها الواحد**، مدفوعٌ **قبل أيّ سطرِ كودِ أداة**.

---

## ⓪ 🔴🔴 حدُّ الصدق الأثقل يُكتب أوّلًا لا آخرًا: **هذي تجربةُ تثبيتٍ لا اكتشاف**

`hold_sessions` **اختير بعد رؤية أرقام `T-PRESS-RANK`** (ذراعُ `A3` تفوّقت في
السنوات الثلاث بـ‏+0.817R/جلسة)، **والسنواتُ الثلاثُ هي هي**. ⇒ **دائريّةٌ
مُعلَنةٌ صريحة**، وهي بعينها العلّةُ التي قتلت تصنيفَ `A/B` و«رابط Technology
2025». ⇒ 🔴 **مهما كانت النتيجةُ لا تُوصَف بأنها «خارج العيّنة»**، والتأكيدُ
الحقيقيُّ الوحيد **أماميّ** (سابقةُ `T-TIE-FWD`).

⚖️ **وما يجعلها اختبارًا لا تحصيلَ حاصل — ثلاثةُ فروقٍ حقيقية:** ① المجتمعُ
الحاكم **يتغيّر** (كثافةٌ حيّة لا كونٌ كامل) ② والمرجعُ `B0` **يشتدّ** (‏`prev_q`
يُحيا فيصير الإنتاجُ أقوى ممّا قِيس ضدّه) ③ و`B1` **ليست `A3`**: تُبقي مفاتيحَ
الإنتاج فاصلَ تعادلٍ بدل الرمز. ⇒ **يمكن أن تسقط، وهذا هو المقصود.**

---

## ① الأبوابُ الثلاثة وكيف تندمج

| الباب | أمرُ المالك | تحقيقُه هنا |
|---|---|---|
| ① | «سجل مفتاح الحفظ» | **`B1` الذراعُ الحاكمة** — `hold_sessions` مفتاحًا أوّلَ |
| ② | «قس عند الكثافة الحية» | **`D_live` المجتمعُ الحاكم** — بِركةٌ مُعادُ بناؤها بحجمٍ حيّ |
| ③ | «احي المفاتيح الخامدة» | **`prev_q` يُحيا في `B0`** — بعد أن قِيست كلفتُه فسقط عذرُ الاستحالة |

---

## ② 🔴 تصحيحٌ ذاتيٌّ يسبق التصميم: كلفةُ `prev_q` في `§⑨-2` كانت خطأً

كتبتُ في `press_rank_prereg §⑨-2` أن `prev_qualified` **«مقيسُ الاستحالة»**
(‏≈8.6 ساعة/سنة) — والحسابُ ضرب كلفةَ المسار الكامل في 24 إزاحة **بلا حسابِ
الخروج المبكّر ولا الرفض المبكّر، وكلاهما في الكود**.

**والمقيسُ الآن على هذي البيئة** (إطاراتٌ مُصطنَعة — حدُّ صدقٍ في §⑧):

| الكمّية | المقيس |
|---|---|
| `analyze_ticker` مسارًا كاملًا | **‏107ms** |
| `analyze_ticker` رفضًا مبكّرًا عند بوّابةٍ صلبة | **‏0.2-1.2ms** |
| `prev_qualified` بلا تأهّلٍ إطلاقًا (‏24 إزاحةً كاملة) | **‏12.6ms** |
| `prev_qualified` مع تأهّل (خروجٌ مبكّر) | **‏112ms** |

🔑 **والسببُ بنيويّ: الكلفةُ ذاتيّةُ التحديد** — الشريحةُ التي تمضي إلى آخر
المسار هي التي **تتأهّل** فتُنهي الحلقة، وغيرُ المتأهّلة **تُرفَض عند أوّل
بوّابةٍ صلبة**. ⇒ **الحدُّ الأعلى النظريُّ (‏48 × 107ms = 5.1s) مستحيلُ الوقوع.**

⇒ **البابُ ③ مفتوحٌ فعلًا، وعذرُ الاستحالة ساقط.** ⚠️ **ويبقى `plan` خامدًا**
لسببٍ مختلفٍ **يُعلَن ولا يُنسَب للكلفة**: `build_pool` يأخذه من
`wl["pullback"]` (خططُ قائمة الارتداد) وهي **حالةٌ حيّة** يلزمها محاكاةُ دورةِ
القائمة كاملةً — **خارج نطاق هذا العقد**.

---

## ③ المجتمعان — والحاكمُ `D_live`

**المِشيةُ والمحرّكُ كما في `T-PRESS-RANK` حرفيًّا** (خطوة 1 بلا قفزة `WAIT` ·
`press_read(w=ALERT_W)` · `wake_read` · `mirror_plan` · `resolve_episode` ·
`should_alert` · `r_win_value()` — كلُّها **بالاسم**)، والمتغيّرُ **المجتمع**:

- **`D_full`** — كلُّ صفٍّ جاهزٍ في الكون (**مجتمعُ `T-PRESS-RANK` نفسُه**) ⇒
  يُستعمل لبوّابة الجسر `HV0` ولنشر الأرقام قابلةً للمقارنة.
- 🥇 **`D_live`** (الحاكم) — الصفوفُ الجاهزةُ **المقصورةُ على بِركةٍ مُعادِ
  بنائها** من مصادر `press_radar.build_pool` **القابلةِ للإرجاع تاريخيًّا وحدَها**:
  1. **الترشيحُ الحيّ** — رمزٌ اجتازه `analyze_ticker` (فرزًا أو ارتدادًا) خلال
     `MEMORY_DAYS` الماضية (من مصفوفة `Q` نفسِها التي يحتاجها `prev_q`).
  2. **متحرّكٌ حديث** — من `Super_stock.scan_explosions` بالاسم، خلال `MEMORY_DAYS`.
  3. **ذاكرةُ الرادار** — كلُّ اسمٍ ظهر في ①/② خلال `MEMORY_DAYS` يبقى.
  ثم **سقفُ `press_radar.POOL_CAP`** بالاسم.

🔴 **ومصدران من الخمسة لا يُعادان ويُعلَنان:** خططُ قائمة الارتداد
(`wl["pullback"]`) و«شُطب حديثًا» (`wl["removed"]`) — **حالةٌ حيّةٌ بحتة** ⇒
`D_live` **ليست البِركةَ الحيّة بل بِركةٌ مطابقةُ الحجم والمصدرِ الغالب**.

⚖️ **ولا دائريّةَ في بناء `D_live`:** مصادرُها **لا تعرف `alert_rank` ولا
`hold_sessions` ولا أيَّ ذراع** — البِركةُ تُبنى قبل الترتيب وتُغذّي الأذرعَ الأربع
**بالتساوي**.

---

## ④ الأذرع — أربعٌ ولا خامسة · والحاكمةُ واحدة

| الذراع | الترتيب | الدور |
|---|---|---|
| **`B0`** | `alert_rank` **و`prev_q` محيًى** ثم المستيقظُ أوّلًا ثم **قاعدةُ عددِ كروت الإنتاج** | **المرجع** (لا يُرقّى) |
| **`B1`** 🥇 | **`hold_sessions` تنازليًّا مفتاحًا أوّلَ** ثم ترتيبُ `B0` حرفيًّا | **الحاكمةُ الوحيدة** |
| **`B2`** | عشوائيّ (‏200 بذرة · الوسيط والمئين 95) | شاهدُ الصدفة |
| **`B3`** | `hold_sessions` تنازليًّا **والرمزُ فاصلَ تعادل** (‏= `A3` القديمة) | وصفيٌّ: هل تُضيف مفاتيحُ الإنتاج شيئًا؟ |

🔒 **و`B0` يُصلَح عيبُ أمانةٍ أعلنتُه في `press_rank_result §⑧-2`:** الإنتاج
`if fire: shown = fire[:ALERT_CAP]` **ولا يُكمل من الهادئين**
(`press_radar.py:618`) — وأداةُ الأمس كانت تُكمل. **هنا تُنقَل قاعدةُ العدد
حرفيًّا إلى الأذرع الأربع** فتبقى الميزانيةُ واحدةً بينها **وتصير أمينةً للإنتاج
معًا**.

⛔ **ولا تُضاف ذراعٌ بعد الأرقام** · ولا يُبدَّل مفتاحُ `B1` · ولا تُحرَّك
`ALERT_CAP` (‏8) ولا `READY_HOLD` (‏3) ولا `REALERT_DAYS` (‏5) ولا `ALERT_W`
(‏40) ولا `POOL_CAP` ولا `MEMORY_DAYS`.

---

## ⑤ المقياسُ والمعايير — **مُعادةٌ من `press_rank_prereg §④` بلا تحريكِ رقم**

**المقياسُ الحاكم:** صافي `R` المُسلَّم لكلّ جلسة، **على `D_live`**.
(‏`R`: فائزةٌ ‏+`r_win_value()` · خاسرةٌ ‏−1 · و`no_fill`/`open` صفرٌ **وتستهلك
كرتًا**.) والمساندُ يُنشَر ولا يحكم: عددُ الكروت · نسبةُ البلوغ بفاصل Wilson ·
والمقصوصون بالسقف.

**تُعتمَد `B1` إن استوفت الأربعةَ معًا على `D_live`:**

1. **`B1 − B0` موجبٌ في السنوات الثلاث** بلا انقلاب.
2. **الفاصلُ 95% للفرق المجمَّع لا يلمس الصفر** (بوتستراب **عنقودُه الجلسة** ·
   ‏10,000 سحبة · بذرة **54321** — نفسُ الإجراء حرفيًّا).
3. **`B1` فوق المئين 95 لشاهد `B2` في سنتين من ثلاث.**
4. **الأرضية:** أربعون جلسةً فأكثر **و150 كرتًا مُسلَّمًا** فأكثر في كلّ سنة.

**والحكمُ يُكتب مرّةً واحدة** عند اكتمال السنوات الثلاث. ⛔ ولا تُعاد تشغيلةٌ
بمعيارٍ مبدَّل، ولا يُنقَل الحكمُ إلى `D_full` إن سقط على `D_live`.

---

## ⑥ بوّاباتُ الصلاحية — **تُنفَّذ في الكود لا تُوصَف**

| البوّابة | الشرط | عند السقوط |
|---|---|---|
| **`HV0`** جسرٌ | على `D_full` وبقاعدةِ عددِ كروتِ الأمس، ذراعُ `A0` تُعيد `press_rank_result §①`: **‏+0.3102 · +0.7353 · +1.9946** | **خروج 3** |
| **`HV1`** تكامل | `HOLD3` محسومةً **‏1660/1857/1854** **وفائزةً 351/333/341** | **خروج 3** |
| **`HV2`** الإحياء | نصيبُ الصفوف بـ`prev_q` صادقٍ **بين 0% و100% حصرًا** (وإلّا الإحياءُ `no-op` أو منحلّ) · **وتُطبَع الكلفةُ المقيسة** | **خروج 4** |
| **`HV3`** الميزانية | الإسقاطُ الزمنيُّ من عيّنةٍ مقيسةٍ **قبل** المشي الكامل · تجاوزُ السقف ⇒ يقف بسببٍ مُسمًّى | **خروج 6** |
| **`HV4`** الكثافة | وسيطُ الجاهزين/جلسة في `D_live` **بين 8 و40** (وإلّا فليست كثافةً حيّة) | **خروج 4** |
| **`HV5`** التفرّق | `B1` تُسلّم كرتًا لا تُسلّمه `B0` في كلّ سنة | **خروج 4** |
| **`HV6`** الدِدوب | صفرُ رمزٍ يُسلَّم مرّتين داخل `REALERT_DAYS` في أيّ ذراع | **خروج 3** |
| **`HV7`** التغطية | ‏95% فأعلى · والمفقودُ يُسمّى بعدده | **خروج 3** |

🔒 **وحارسُ العلم الميّت:** جدولُ أذرعٍ متطابقٌ بت-بت ⇒ بصمةُ `no-op` **يُعلَن
ولا يُفسَّر** (سابقةُ `BT_CANDLE`).

---

## ⑦ التنبّؤات — قابلةٌ للكسر، تُنشَر مكذَّبةً كما تُنشَر محقَّقة

- **`HK-P1`** (الأثقل، ويراهن **ضدّ** الحاكمة): أرجّح أن تعبر `B1` المعيارَ ①
  **وتسقط في ②** على `D_live` — لأن السقفَ يبنّد أقلَّ عند الكثافة الحيّة
  فيصير للترتيب أثرٌ أصغر، فيتّسع الفاصلُ نسبةً إلى الفرق.
- **`HK-P2`**: إحياءُ `prev_q` **يرفع** `B0` على `D_full` فوق `A0` المنشورة
  (‏+0.3102/+0.7353/+1.9946) في سنتين على الأقلّ — فهو مفتاحُ ترتيبٍ بُني لغرضه.
- **`HK-P3`**: على `D_live` يبنّد السقفُ في **‏30-70%** من الجلسات (لا 100%).
- **`HK-P4`**: `B3` (حفظٌ صرف) **لا يقلّ** عن `B1` مجمَّعًا — أي أن مفاتيحَ
  الإنتاج لا تضيف فاصلَ تعادلٍ نافعًا.
- **`HK-P5`**: نصيبُ `prev_q` الصادق بين **‏10% و60%** من الصفوف الجاهزة.
- **`HK-P6`**: `B0` على `D_live` **فوق** وسيط `B2` — بعكس ما وقع على `D_full`،
  لأن الاختيارَ من نصفٍ لا من عشرين ضعفًا يقلّل أثرَ سوء الترتيب.

---

## ⑧ سقفُ النجاح وحدودُ الصدق

🥇 **إن عبرت `B1` الأربعةَ على `D_live` فهي تُقترَح على المالك** — **مفتاحًا
أوّلَ في `alert_rank` بلا حذفِ مفتاحٍ قائم** — **وشحنُها قرارُه هو**. ⚖️ وهي
**ترتيبُ عرضٍ لا بوّابة** (لا اسمَ يُسقَط — درسُ `WETO`) ⇒ **لا `LOGIC_VERSION`
ولا `force_renew`.** 🔴 **وإن لم تعبر: `alert_rank` يبقى كما هو ويُقال صراحةً
«لا ترتيبَ أفضلَ مقيسًا».**

⛔ **وممنوعٌ مهما كانت الأرقام:** مسُّ أيٍّ من الثوابت الستّة في §④ · أو
`M1`-`M14` أو الدخول/الوقف/الأهداف · أو ترقيةُ `B2`/`B3` · أو تحويلُ
`hold_sessions` إلى **فلتر** · أو نقلُ حكمٍ من `D_full` إلى `D_live` أو العكس.

**وحدودُ صدقٍ مكتوبةٌ قبل أيّ رقم:**
1. 🔴🔴 **الدائريّةُ (§⓪) هي الأثقل** — تجربةُ تثبيتٍ على السنوات نفسِها.
2. 🔴 **`D_live` ليست البِركةَ الحيّة** — مصدران من خمسةٍ غيرُ قابلَين للإرجاع.
3. **`plan` يبقى خامدًا** ⇒ في `B0` **إضعافٌ واحدٌ باقٍ** (كان ثلاثةً) ⇒ الفرقُ
   `B1−B0` ما زال **سقفًا لـ`B1`** لا أرضيّة، **لكنه أضيقُ ممّا كان.**
4. **كلفةُ §② مقيسةٌ على إطاراتٍ مُصطنَعة** لا على بيانات حقيقية ⇒ **`HV3` هو
   الحارس**، وأيُّ تجاوزٍ يوقف بسببٍ مُسمًّى لا يُخمَّن.
5. **«فائزة» لمسُ هدفٍ لا صفقةٌ منفَّذة** ⇒ سقفٌ متفائلٌ للأذرع **بالتساوي**.
6. **الوقفُ وقفُ `mirror_plan`** (‏7% تحت المِرساة) لا وقفُ القاع المعتمَد ⇒
   قابلٌ للمقارنة مع `§⑬` و`T-PRESS-RANK` وغيرُ قابلٍ للمقارنة مع `T-GOLD-ENTRY`.
7. **انحيازُ البقاء** مخفَّفٌ باللقطة وغيرُ منفيّ · **وتشويهُ التقسيمات** قائم ·
   **وعمى الجلسة الممتدّة** قائم (الصحوةُ بلا الافتر).
8. **و`B3` سيُنشَر بفاصلِ ثقةٍ هذي المرّة** — تصحيحًا لقصور `T-PRESS-RANK` الذي
   صدّر فرقَ الحاكمة وحدَه فخرج `A3−A0` وصفيًّا بلا فاصل.

---

## ⑨ ملحقٌ مؤرَّخ (2026-08-27) — يُكتب **بعد بناء الأداة وقبل أيّ رقم**

تنفيذًا لقاعدة «حارسٌ مكتوبٌ وغيرُ منفَّذٍ ليس حارسًا» ولقاعدة «لا يُحرَّك شيءٌ
بعد رؤية الأرقام»:

**⑨-1 «متحرّكٌ حديث» يُبنى بالعتبة لا بالدالّة — انحرافٌ عن حرف §③ يُعلَن.**
نصَّ §③ على `Super_stock.scan_explosions` **بالاسم**، وقراءةُ الكود بعده أظهرت
أنها **ماسحُ «اليوم»**: تقرأ آخر `EXPLOSION_LOOKBACK` جلسةً من نهاية كلّ إطار
**وتستدعي `analyze_ticker` داخلها** لحساب `was_pivot`/`base_reason`. ⇒ نداؤها
لكلّ جلسةٍ من 250 يضرب الكلفةَ مئاتِ المرّات لحسابٍ **لا نحتاجه**. ⇒ يُستعمل
**`CONFIG["EXPLOSION_PCT"]` بالاسم** (نفسُ العتبة التي تقرؤها الدالّة) على قفزة
اليوم الواحد مباشرةً. ⚖️ **صفرُ رقمٍ مخترَع، والمُسقَطُ تصنيفٌ لا عتبة.**

**⑨-2 `prev_q` وعضويّةُ البِركة من نداءٍ واحد.** `prev_qualified` تمسح الإزاحاتِ
من الأقرب فالأبعد وتُرجع **أحدثَ يومِ تأهّلٍ مُعايَن** ⇒ النداءُ الواحد يعطي
المفتاحَ (`prev_q` = وُجد يومٌ) **وعضويّةَ «ترشيحٌ حيّ»** (اليومُ داخل
`MEMORY_DAYS`) معًا. ⚠️ **وحدُّ صدقٍ موروثٌ من الدالّة نفسِها:** معاينةُ كلّ
خمس جلسات ⇒ العضويّةُ **أرضيّةٌ لا حصر** (نافذةُ تأهّلٍ قصيرة قد تقع بين
العيّنات) — وهو نصُّ `docstring` الإنتاج.

**⑨-3 حارسُ الميزانية `HV3` مُنفَّذ:** تُقاس كلفةُ `prev_qualified` على **عيّنةٍ
حقيقيّة من الصفوف الجاهزة** ثم تُسقَط على عددها الكامل، ويقف بخروج **6** بسببٍ
مُسمًّى قبل حرق التشغيلة. ⇒ **أرقامُ §② المُصطنَعة لا تُبنى عليها تشغيلة.**

**⑨-4 `B0` بلا تجزئةٍ داخلَ الترتيب:** الأذرعُ تُرجع **الترتيبَ الكامل**، و
**`deliver_prod` وحدَها** تُطبّق قاعدةَ الإنتاج (المستيقظون أوّلًا وإن وُجدوا
فهم الكروتُ حصرًا بلا إكمالٍ من الهادئين) ⇒ القاعدةُ **واحدةٌ للأذرع الأربع**.

**⑨-5 الفاصلُ يُصدَّر لـ`B1−B0` و`B3−B0` معًا** — تصحيحًا لقصور
`T-PRESS-RANK` الذي صدّر فرقَ الحاكمة وحدَه فخرج `A3−A0` بلا فاصل (‏§⑧-8).

**⑨-6 `HV0` يعمل على صفوفٍ `prev_q=None`** (شرطُ الأمس) **وقبل الإحياء** —
وإلّا استحال أن يُعيد أرقامَ الأمس، فتكون البوّابةُ خامدةً بالبناء.

**⑨-7 تعديلٌ مؤرَّخ على ⑨-2 (قبل أيّ رقم) — شبكةٌ عامّة بدل نداءٍ لكلّ صفّ.**
كشف البناءُ أن ⑨-2 **لا يكفي**: عضويّةُ البِركة تلزم معرفةَ التأهّل **لكلّ رمز**
لا للجاهزين وحدهم (رمزٌ خارج البِركة يُسقط صفَّه)، و`prev_qualified` لكلّ
(رمز، جلسة) = ‏947 ألف نداءٍ ‏≈13 ساعة ⇒ **مستحيل**. ⇒ تُبنى **شبكةُ تأهّلٍ
عامّة `Q5`**: يُنادى **`Super_stock.analyze_ticker` بالاسم** (فرزًا وارتدادًا)
لكلّ رمزٍ **كلَّ خمس جلسات** — **نفسُ خطوة `prev_qualified` (‏`step=5`)** — فتُشتقّ
منها الكمّيتان:
- **`prev_q`** = وُجد تأهّلٌ داخل `max_back`=120 جلسةً قبل المِرساة.
- **«ترشيحٌ حيّ»** = وُجد تأهّلٌ داخل `MEMORY_DAYS`.

⚖️ **والفرقُ عن `prev_qualified` محاذاةُ الشبكة لا الخطوة** (شبكةٌ عامّةٌ ثابتة
بدل شبكةٍ نسبيّةٍ لكلّ مِرساة) — **وهي أكثرُ اتّساقًا** لأن مِرساتين متجاورتين
تريان الأساسَ نفسَه. 🔒 **وتُقفَل مطابقتُها ببوّابةٍ منفَّذة:**

| **`HV8`** | على عيّنةٍ عشوائيّةٍ حتميّة من الصفوف، توافقُ `prev_q` المشتقِّ من `Q5` مع `press_radar.prev_qualified` **الحقيقيّة** لا يقلّ عن **‏90%** · والتوافقُ يُطبَع | **خروج 3** |

⇒ **ولا يُقرأ رقمٌ من التشغيلة إن سقطت `HV8`** — فالبديلُ يُثبِت أنه بديلٌ أو يسقط.
