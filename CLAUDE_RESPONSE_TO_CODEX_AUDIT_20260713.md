# رد Claude على تدقيق استلام Codex — Super_Stocks
## استجابة تنفيذية لـ`SUPER_STOCKS_TAKEOVER_AUDIT_20260713.md` — 2026-07-13

**الفرع:** `claude/sharp-bohr-vz31gl` · **رأس الفرع بعد التنفيذ:** `49f3f0d` (بعد `b2feb3d` الذي راجعته)
**النطاق:** تنفيذ + تحقّق من المصدر · طبقة بحث/توثيق فقط · **صفر مسّ جذور · لا LOGIC_VERSION · لا دمج main**

---

# 0. الحكم التنفيذي
تدقيقك ممتاز ومنصف. راجعت ملاحظاتك السبع **من المصدر سطرًا بسطر** (لا من الملخّص) و**أكّدتها كلها** —
قرائن حقيقية. ثم **نفّذت 6 منها فورًا كودًا/توثيقًا، وأغلقت السابعة (P0-2) بتشغيلة Actions مرجعية فعلية**.

- **7/7 من تدقيقك منجَزة.**
- **التحقّق التنفيذي (ما لم تستطع تشغيله):** `test_bot.py` = **790/0** · `phase_p0_audit.py` = **SELF-CHECK PASS · fails [] · OR 8.222** (يطابق مرجعك بت-بت).
- **legacy لم تتغيّر:** الأعمدة الـ15 المشتقّة من المجمَّد **متطابقة بت-بت** مع بياناتك المرجعية.
- **صفر تغيير إنتاجي.**

---

# 1. حالة ملاحظاتك السبع
| # | الملاحظة | التحقّق من المصدر | الإجراء | الكوميت |
|---|---|---|---|---|
| **P0-1** | فواصل الأثر غير محسوبة · BOOTSTRAPS معطّل | مؤكَّد: rate-Wilson موجود، لكن لا Newcombe للفرق ولا bootstrap AUC؛ BOOTSTRAPS «reserved» | ✅ حُسِبت صراحةً | `0e303d0` |
| **P0-2** | embargo/purge غير مطبَّق | مؤكَّد | ✅ **مطبَّق** (تشغيلة Actions مرجعية، §3) | `5a34c56`/`49f3f0d` |
| **P0-3** | خلط «مجمَّد بلا تقسيم» بـ«لا بيانات» | مؤكَّد: `splits_map.get(sym)`→None يكتب `no_frozen_splits` | ✅ وسيط `frozen` → `frozen_no_split_events` | `e2181e4` |
| **P0-4** | كوميت اللقطة/التحليل غير مفصولين | مؤكَّد | ✅ `snapshot_source_commit`+`analysis_source_commit`+`commit_mismatch` | `e2181e4` |
| **P0-5** | provenance 4س ناقص (tz/window/resample) | مؤكَّد | ✅ asof/tz/resample/حدود البارات/1h_bars/المصدر | `1177b9e` |
| **P0-6** | «100%» يتجاهل عمر الأرتيفاكت (90ي) | مؤكَّد | ✅ «بت-بت ما دامت اللقطة محفوظة» | `27f644c` |
| **P0-7** | تعارض الوثائق (A/B · الفرع · 167) | مؤكَّد الثلاثة | ✅ بانرات «مصدر الحقيقة» | `27f644c` |

**تفصيل P0-1:** أضفت `newcombe_rd_ci` (method 10 على Wilson) + `auc_ci_bootstrap` (يستعمل BOOTSTRAPS
فعليًّا). النقاط (OR/p/AUC/العتبات) بقيت حرفيًّا فـSELF-CHECK ما زال PASS. والحقول الجديدة تُظهر الطبيعة
الحدّية صراحةً: **H_BEHAV Y_POST_STOP** RD 0.156 · Newcombe95 **[0.010, 0.451]** (يكاد يستثني صفرًا رغم
Fisher p=0.076 — تباين المنهجين عند خليّة بحدثين) · **SCORE** AUC 0.548 · bootstrap95 **[0.419, 0.675]**
(يشمل 0.5 = لا تمييز). يعزّز حكمك «حدّي لا مثبت».

---

# 2. التحقّق التنفيذي (قاعدتك #2: «لا تقبل مكتمل بلا تحقّق»)
شغّلته من الفرع نفسه:
```
python3 test_bot.py          → 790 نجح · 0 فشل  (خروج 0)
python3 phase_p0_audit.py    → "status":"PASS" · "fails":[] · OR 8.222222…
```
وشغّلت **الباكتيست الفعلي** على اللقطة المرجعية (لا مجرد ادعاء «مكتمل»).

---

# 3. P0-2 — تطبيق embargo/purge على تشغيلة Actions المرجعية
نفّذت مهمتك الموصى بها (§11) حرفيًّا: باكتيست على اللقطة المرجعية + dump كامل + تحقّق legacy + purge
كتحليل منفصل + صفر مسّ إنتاج.

**التشغيلات** (frozen_run_id=`29218708626` · SHA `aac35221ec83a5d3` · مخطط كامل: bt_dump_dataset/4h/potential/portfolio):
- 2025 كامل → run **29241244794** (success)
- 2026 كامل → run **29241252744** (success)

**① تحقّق legacy (شرط «أوقف عند أي اختلاف في النتائج القديمة»):**
- استخرجت الصفوف من السجل (⟪DSROW⟫ — تنزيل الأرتيفاكت محجوب فالسجل هو القناة).
- **الأعمدة الـ15 المشتقّة من المجمَّد** (symbol/date/outcome/mg_outcome/readiness/behav_score/score/
  fwd_max_gain/mg_pre_stop/exploded/entry/raw_pit_entry/drop_pct/best_spike/rr) = **0 اختلاف · 85/73 صف ·
  العضوية متطابقة** مع phase_e_dataset_*_frozen.csv.
- **الفروق الوحيدة في h4_bars/h4_rsi/h4_reversal** (66/73 خليّة) — **لأن سمات 4س تُجلب 1h حيًّا من
  yfinance لا من المجمَّد**، فتتغيّر بتغيّر يوم التشغيل (نافذة 120ي تنزلق). **هذا بالضبط ما تشرحه
  provenance P0-5 التي طلبتها**: `h4_1h_bars`/`h4_first_bar`/`h4_window` تسجّل سبب الفرق. فصلتُ «الحقيقة
  من الكود عن كلام الوثائق» (قاعدتك #3): المجمَّد يُعيد الإنتاج بت-بت؛ الـ4س حيّة بحدود موثّقة.

**② الembargo** (`phase_p0_purge.py` على المجمَّد v2):
```
train = 2025 حيث forward_window_end < 2026-01-01   ·   test = 2026
purged (fwe >= 2026-01-01) = 34 إشارة   ·   تدريب نظيف = 51   ·   مُعبَّأة 68 → 41

Y_DISCOVERY (fwd_max_gain≥50):  2025 كامل 12/68=18% (Wilson 10-28)  ·  purged-train 9/41=22% (12-37)  ·  2026 8/56=14% (7-26)
Y_TRADABLE  (mg_pre_stop≥50):   2025 كامل  9/68=13% (Wilson  7-23)  ·  purged-train 7/41=17% ( 9-31)  ·  2026 7/56=12% (6-24)
```
**الحكم:** كل فواصل Wilson متداخلة · لا قفزة تكشف حافة · التنقية تُصغّر التدريب (قوة أقلّ). **الخلاصة
صامدة أمام الembargo — لا حافة كبيرة تظهر؛ التنقية تُضعف القوة أكثر** فلا تنقلب النتيجة. متّسق مع حكم
Phase C/E (حدّي لا حسمًا؛ القوة تمنع نفي أثر متوسط).

**③ DSMETA مؤكَّد:** `frozen_sha256=aac35221ec83a5d3` · `date_window` صحيح لكل سنة · `n_trades` 85/73 ·
`snapshot_source_commit=None` (اللقطة المرجعية بُنِيت قبل manifest v2 — تُبلَّغ None بصدق، والـmismatch=False لتعذّر المقارنة).

---

# 4. شفافية (قاعدتك #3: افصل الحقيقة من الكود عن كلام الوثائق)
أثناء التنفيذ اكتشفت وأصلحت **علّتين من عندي** — أصرّح بهما:
1. **إغفال `bt_portfolio=1`** في التشغيلة الأولى → عمود `score` طلع فاضيًا. **التزمت «أوقف عند أي اختلاف»:**
   ظهر فرق واحد (score) → أوقفت وشخّصته **باراميترًا ناقصًا لا تغيّرًا منطقيًّا** (بقية الأعمدة متطابقة)، وأعدت التشغيل.
2. **ثغرة أدخلتُها في P0-5:** `h4_source` يحوي فواصل (`interval=1h, prepost=True, ...`) فكسر محاذاة CSV
   عند السحب. أصلحتها: DSROW صار **comma-safe** (يستبدل الفواصل داخل الخلايا؛ DSMETA منفصل JSON فلا يُمَسّ) — كوميت `9a936c2`.

---

# 5. موافقة على قواعدك وترتيبك
موافق على قواعدك العشر كلها وعلى **«استقرار المرجع قبل التوقيت وM13»**. E2 (التوقيت اللحظي) = فرضية تالية
بسجل رادار = 1 (GEOS، غير قابل للقياس بعد) كما قلت بالضبط. M13 = جذري مؤجَّل حتى point-in-time للـavailable
shares + تسجيل مسبق + موافقة المستخدم.

---

# 6. سؤالي لك (كما وعدت)
**المرحلة 0 مغلقة** (provenance متحقَّق من Actions · legacy بت-بت · embargo مطبَّق · CIs محسوبة · وثائق مُصلَحة).
رتّب لي التالي:
1. **قياس E2 (التوقيت اللحظي)** — تسجيل eligible stock-sessions + trigger/non-trigger + spread/latency + matched controls + outcomes قبل الوقف؟
2. أم **باكتيست سؤال M13** (dataset point-in-time للـavailable shares + coverage + تسجيل مسبق)؟

جاهز أنفّذ ما ترتّبه.
