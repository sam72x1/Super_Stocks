# 💥🔗② `T-LINK100-2` — تسجيلٌ مسبق: **«قبل ما يدبل يُضغط لأدنى قاع»**

> **الحالة:** مسجَّلٌ **قبل أيّ سطرِ أداةٍ وقبل أيّ رقم**.
> **النطاق:** **قراءةٌ/بحثٌ فقط** — صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` ·
> صفرُ إرسال · **ولا يُشحَن شيءٌ بهذا العقد مهما كانت النتيجة.**
> **ولا يُعدَّل `link100_prereg.md`** (مدموجٌ ومقفولٌ بنصّ `§⓪`-3 فيه) — هذا عقدٌ
> **مستقلٌّ** يرث منه المجتمعَ والشاهدين والمعيارَ **بالاسم**.

---

## §⓪ الدعوى ومصدرُها — حرفيًّا لا مُستنتَجًا

**الكاتالوج `FAISAL_IMAGES_CATALOG.md` سطر 713** (‏ضمن تغريدة `KUST`):

> **«دائمًا السهم قبل يدبل يُضغط لأدنى قاع تحضيرًا وسحب سيولة المشترين بالأعلى».**

**الوسم: `faisal_verbatim`** — نصٌّ منقولٌ بحرفه، وهو **قاعدةُ سلوكٍ قبل الانفجار**
(«يدبل» = يتضاعف = ‏+100%) لا قاعدةَ دخولٍ ولا هدفًا ولا وقفًا.

### والمحورُ جديدٌ بالبحث لا بالتسمية

`T-LINK100` قاس **‏13 ميزةً** قبل الحدث، **وليس فيها حداثةُ القاع**:
`dd52` تقيس **البعدَ عن قمّة 52 أسبوعًا** — وهي تقول «قريبٌ من قاعه» **ولا تقول
«سجّل قاعَه للتوّ»**. سهمٌ يقبع عند ‏−95% من قمّته منذ سنةٍ كاملةٍ يُعطي `dd52`
نفسَها التي يُعطيها سهمٌ كسر قاعَه **أمس** — والدعوى عن **الثاني** لا الأوّل.
**ومثلُها `quiet`** (حداثةُ شمعةٍ مداها ‏≥50%) — مدًى لا قاع.
⇒ **الميزةُ الرابعةَ عشرة غائبةٌ بالقياس لا بالظنّ** (‏`FEATURES` في
`link100_probe.py` مطبوعةٌ في `§①` أدناه).

---

## §① ما هو قائمٌ وما هو غائب — مقيسٌ لا مُدَّعًى

**القائم:** `link100_probe.py` يقيس على **السوق الأمريكيّ كلِّه بالمشطوبين**
(`grouped` يوميّة `adjusted=false`) ثلاثَ عشرةَ ميزة:
`vol_x` · `ret5` · `range_c` · `gap1` · `dd52` · `sma_pos` · `rsi14` · `quiet` ·
`px` · `usd1` · `rsplit180` · `pm_gap` · `pm_usd` — كلُّها من `[..d−1]` حصرًا،
بشاهدَين `CX` (أسهمٌ أخرى **نفسَ اليوم**) و`CC` (السهمُ نفسُه **أيّامًا أخرى**)
وبمعيار `LK1` وبونفيروني.

**الغائب:** أيُّ ميزةٍ تقرأ **متى** سُجِّل القاعُ الأدنى. بحثُ المستودع كلِّه عن
`newlow` أو «أدنى قاعٍ في نافذة» داخل طبقة الميزات: **صفرُ مطابقة**.

---

## §② التعريفُ المُقاس — واحدٌ ومكتوبٌ بحرفه

الميزةُ الرابعةَ عشرة **`newlow60_5`** ثنائيّةُ القيمة (`نعم`/`لا`)، وتُحسب من
`p = hist[:i]` (**‏`[..d−1]` حصرًا ⇒ صفرُ تسريب**) بهذي الخطوات بالضبط:

1. `lows = [b[3] for b in p]` — قيعانُ الجلسات السابقة للحدث.
2. إن كان `len(lows) < W` فالقيمةُ **`None`** (تخرج من مقام هذي الميزة وحدَها،
   كما تفعل كلُّ ميزةٍ قائمة) — **ولا تُملأ بقيمةٍ مفترَضة**.
3. `w = min(lows[-W:])` · `k = min(lows[-K:])`.
4. **`نعم`** إن كان `k <= w` (أي أن أدنى قاعٍ في النافذة `W` وقع داخلَ آخر `K`
   جلسة) · وإلّا **`لا`**. **والمقارنةُ `<=` لا `==`** تفاديًا لفخّ مساواة
   العائمات (‏`k` أحدُ عناصر `w` بالبناء فلا يمكن أن يكون أصغر).

**والقيمُ الحاكمة `W = 60` و`K = 5`** — **ووسمُهما `engineering` مُعلَن**: فيصل
قال «أدنى قاع» **بلا نافذةٍ ولا مهلة**، والرقمان اختيارُنا نحن ⇒ **لا يُشحَنان
عتبةً بأيّ حكم**، وتُطبَع الشبكةُ `W ∈ {20, 60, 120} × K ∈ {3, 5, 10}`
**وصفيًّا** (تسعُ خلايا) **ولا تدخل معيارَ الحكم ولا عدَّ بونفيروني** — وهي
سابقةُ `C-TIME` في `T-PMGATE` بحرفها.

🔴 **وما لا تقيسه هذي الميزة ويُقال الآن:** الدعوى فيها شقٌّ ثانٍ — **«وسحب سيولة
المشترين بالأعلى»** — وهو **نيّةٌ لا تُقاس من `grouped` اليوميّة** ⇒ **يُقاس
الشقُّ الأوّل وحدَه**، والحكمُ لا يُعمَّم على الشقّ الثاني.

---

## §③ المجتمع — عينُ مجتمع `T-LINK100` بحارسِ هُويّة

المجتمعُ والشاهدان والطيُّ (‏20 جلسة) وحارسُ التقسيم (‏±3 أيّام) **كلُّها
بالبناء لا بالدعوى**: الميزةُ تُضاف داخلَ `feats(...)` نفسِها، فتمرّ على الصفوف
نفسِها في الترتيب نفسِه.

**و`V-N1` حارسُ هُويّةٍ يوقف قبل أيّ تفسير** — يُقارَن بالمنشور في
`link100_result.md` **نصًّا**:

| السنة | أحداثٌ تُقاس | `vol_x ≥ 3` (حصّةُ الأحداث) |
|---|---|---|
| 2023 | **‏481** | **‏38.7%** |
| 2024 | **‏655** | **‏47.3%** |
| 2025 | **‏773** | **‏48.6%** |

- **الأحداثُ تُطابَق بت-بت** · **والحصّةُ بتسامح ‏0.1 نقطة** (تقريبُ الطباعة).
- أيُّ اختلافٍ ⇒ **خروج `7`** ولا يُفسَّر رقمٌ واحد.
- **ولا يُعتمَد `rsplit180` ولا `pm_usd` مرساةً** — الأوّلُ يقرأ تقسيماتٍ حيّةً
  قد تتغيّر، والثاني شموعَ دقيقة ⇒ **المرساةُ من `grouped` الخام وحدَها.**

---

## §④ الشاهدان والمعيار — موروثان بالاسم

- **`CX`** مقطعيّ · **`CC`** ذاتيّ — بدوالّهما القائمة (`cell_pass` · `wilson` ·
  `z_bonf`) **تُنادى بالاسم ولا تُنسَخ**.
- **`LK1`** كما في العقد الأصل: الخليّةُ تعبر إذا كانت **‏≥2×** مقابل `CX`
  **و**`CC` **معًا** · وفاصلا Wilson **منفصلان** لكلٍّ منهما · و`n ≥ 50` في
  الحدث · **وفي كلّ سنةٍ من الثلاث**.
- **`z` بونفيروني:** تُحسب من **عدد الخلايا الفعليّ** بعد إضافة الميزة، ويُؤخَذ
  **`z = max(z_bonf(الخلايا), 3.222)`** ⇒ **مُثبَتٌ أنه لا يكون أرخى من
  التشغيلة المنشورة أبدًا** (زيادةُ الخلايا ترفع `z`، والحدُّ الأدنى يمنع أيّ
  انخفاضٍ لو تغيّر التقريب). **والقيمتان تُطبَعان معًا.**
  ⚠️ **والحساسيّةُ الوصفيّةُ لا تدخل العدّ** (‏`§②`) — وإلّا رفعت `z` على
  الحاكمة بخلايا لا تُحكَم بها.

---

## §⑤ فروعُ الحكم — ثلاثةٌ · ولا فرعَ رابع

1. **الفرعُ 1 — «سِمةٌ ثالثة»:** `newlow60_5 = نعم` تعبر `LK1` **في السنوات
   الثلاث** ⇒ تُقرأ **مع** `vol_x ≥ 3` و`ret5 > +20%` ‏**إثراءً قبل الحدث لا
   بوّابةَ فرز**، وأيُّ استعمالٍ يلزمه **تسجيلٌ أماميٌّ جديدٌ وإذنُ المالك**.
2. **الفرعُ 2 — «لا تعبر»:** تسقط على `CX` أو على `CC` أو على سنة — **ويُنشَر
   على أيّهما سقطت**، فالفرقُ بين السقوطين معلومةٌ بذاته (سابقةُ `rsplit180`:
   `CX` تعبر و`CC` تسقط ⇒ **«أيُّ الأسهم تنفجر» لا «أيُّ الأيّام»**).
3. **الفرعُ 3 — «لا حكم»:** سقوطُ `V-N1` أو حارسِ تغطيةٍ أو `n < 50` في سلّة
   الحدث في أيّ سنة — **يُعلَن بعدده ولا يُفسَّر**.

---

## §⑥ حرّاسٌ تُطبَع في كلّ تقرير — والسقوطُ يوقف

- **`V-N1`** هُويّةُ المجتمع ضدّ المنشور (‏`§③`) — **قبل أيّ نسبةٍ جديدة**.
- **`V-N2`** **المسارُ القائم بت-بت والعلمُ مطفأ افتراضًا**: بلا
  `LINK100_NEWLOW=1` **لا تُحسَب الميزةُ ولا تدخل `FEATURES` ولا عدَّ بونفيروني**
  ⇒ إعادةُ التشغيلة المنشورة تُعطي **الخلايا نفسَها**. **مقفولٌ سلوكيًّا.**
- **`V-N3`** **صفرُ تسريب**: الميزةُ تُحسَب من `hist[:i]` وحدَها — يُثبَت بشاهدِ
  ضبطٍ يقلب شمعةَ **يوم الحدث** ولا تتغيّر القيمة.
- **`V-N4`** `z` المستعمَلة **‏≥ 3.222** ويُطبَع الرقمان.
- **`V-N5`** الأداةُ **لا ترسل ولا تكتب حالةً** (‏`selfcheck_readonly` بالـAST
  المُشدَّد) **وبلا سرِّ تلغرام في الـworkflow**.
- **`V-N6`** الجذورُ الاثنا عشر **بصمتُها مطابقة** قبل التشغيل وبعده.

---

## §⑦ حدودُ صدقٍ تُقال **قبل** النتيجة

1. **الشقُّ الثاني من الدعوى لا يُقاس** (‏`§②`) — «سحبُ سيولة المشترين» نيّةٌ لا
   عمودٌ في `grouped`.
2. **`W` و`K` من عندنا** — ولو عبرت الميزةُ فالعابرُ **الظاهرةُ** لا الرقمان.
3. **تعدّدُ المقارنات يتراكم:** هذي الميزةُ الرابعةَ عشرةَ بعد ثلاثَ عشرة، وبونفيروني
   يضبط **داخل التشغيلة** لا **عبر التجارب** ⇒ **عبورُها لا يُقرأ اكتشافًا مستقلًّا**
   بل مرشَّحًا يلزمه تأكيدٌ أماميّ.
4. **الارتباطُ بالمقيس سلفًا:** «قاعٌ جديدٌ قُبيل الحدث» قد يتقاطع مع `ret5 < −20%`
   و`dd52 < −90%` ⇒ **يُطبَع تقاطعُها الوصفيّ** فلا تُقرأ السِّمةُ جديدةً وهي ظلٌّ
   لمقيسٍ ساقط.
5. **العيّنةُ ثلاثُ سنواتٍ من سوقٍ واحد** — والحافّةُ في `T-PMUSD` تآكلت عبر
   السنوات ⇒ **الثباتُ عبر الثلاث شرطٌ لا زينة.**

---

## §⑧ تنبّؤي المسبق — يُنشَر مكذَّبًا أو مؤكَّدًا

- **`NP1`** تعبر مقابل **`CX`** في الثلاث (المنفجرون قريبون من قيعانهم — `dd52`
  تقول ذلك سلفًا).
- **`NP2`** 🥇 **وتسقط على `CC`** كما سقطت `rsplit180` ⇒ **الفرعُ 2**.
- **`NP3`** حصّةُ `نعم` بين الأحداث **بين ‏15% و45%** في كلّ سنة.
- **`NP4`** الشبكةُ الوصفيّةُ **رتيبةٌ في `K`** (‏`K` أكبر ⇒ حصّةُ `نعم` أكبر)
  **وغيرُ رتيبةٍ بالضرورة في `W`**.
- **`NP5`** التقاطعُ مع `ret5 < −20%` **دون ‏50%** ⇒ السِّمتان ليستا واحدة.

---

## §⑨ ما لا يُمَسّ بهذي التجربة

الجذورُ الاثنا عشر · `rank_key`/`select_top` · أيُّ بوّابةِ فرز · `CONFIG` ·
`link100_prereg.md` (مدموجٌ مقفول) · **وأرقامُ `link100_result.md` المنشورة**
(تُقرأ مرساةً ولا تُعاد كتابتُها).
**ولا تُضاف الميزةُ إلى الفارز ولا إلى أيّ رسالةٍ تُرسَل** — ولو عبرت.

---

## §⑬ ملحقٌ مؤرَّخ ‏2026-09-20 — **مرساةٌ فوق المرجع الحيّ** (`V-N1-b`)

> **العقدُ أعلاه مدموجٌ ولا يُعدَّل** — وهذا ملحقٌ مؤرَّخٌ يُقرأ معه، كما في
> `T-SUPDEF §⑩`-`§⑫` و`T-PMUSD §⑫`. **والنتيجةُ الأولى منشورةٌ قبله**
> (`link100_2_result.md` — الفرعُ 3 بسقوط `V-N1`)، فلم يُكتَب هذا الملحقُ
> ليُنقذ تشغيلةً بل بعد أن صدر حكمُها.

### ⓐ الواقعةُ المقيسة
التشغيلة `35487721499` أعطت **‏480** حدثًا في 2023 والمنشورُ **‏481** ⇒
`V-N1` أوقفها بخروج ‏7 **قبل أيّ رقمِ ميزة**. والتشخيصُ مُغلَقٌ حسابًا:
كلُّ ما **فوق** فلتر التقسيم مطابقٌ بت-بت (‏250 · 250 · **‏869** · **‏700**)
و`مُستبعَدو التقسيم` وحدَهم تحرّكوا (‏160 ⟶ **161**)، **وقانونُ البقاء
يُغلقه**: ‏700 − 161 − 59 = 480 و700 − 160 − 59 = 481.
⇒ **المتغيّرُ مرجعُ `splits_of` الحيُّ عند المزوّد، لا الآلة.**

### ⓑ 🔴 العيبُ في المرساة الأصليّة — يُقال صراحةً
`§③` رفض `rsplit180` مرساةً **لأنه يقرأ تقسيماتٍ حيّةً قد تتغيّر**، ثمّ
اعتمد **عددَ الأحداث** وهو يمرّ عبر **الفلتر نفسِه** ⇒ **العلّةُ سُمّيت ثمّ
وقع فيها العقدُ في فقرتها.** ولهذا **تُستبدَل المرساةُ لا يُرخى الحدّ**.

### ⓒ `V-N1-b` — ثلاثةُ أشقٍّ، والسقوطُ يوقف بخروج ‏7 كما كان

1. **المرساةُ فوق المرجع الحيّ — بت-بت ولا تسامح:**
   لكلّ سنة `(أيّامُ تداول · جُلبت · أحداثٌ خام · بعد الطيّ)` تساوي المنشورَ
   في `link100_result.md §①` **تمامًا**:
   **‏2023 = (250, 250, 869, 700)** · **‏2024 = (252, 252, 1088, 912)** ·
   **‏2025 = (250, 250, 1248, 1087)**.
   وهذي كلُّها مُخرَجُ `year_days`/`grouped_day`/`day_events`/`fold_events`
   — **وصفرُ نداءٍ لمرجعٍ حيٍّ بينها**.
2. **قانونُ البقاء:** `بعد الطيّ − مُستبعَدو التقسيم − بلا تاريخٍ كافٍ =
   عددُ الأحداث المُقاسة` **بالضبط** في كلّ سنة — فلا يُسقَط صفٌّ بسببٍ
   ثالثٍ غيرِ مُعلَن.
3. **الانزياحُ يُطبَع ولا يُطوى:** `مُستبعَدو التقسيم` وفرقُه عن المنشور،
   وعددُ الأحداث وفرقُه — **في كلّ سنة وفي كلّ تقرير**.

**ولا تسامحَ عدديٌّ يُختار:** لا سقفَ نسبةٍ ولا `±n` — **لأنّ أيَّ رقمٍ
أختاره الآن يكون بعد رؤية الانزياح**. الضمانةُ هي ① و② و③ مجتمعةً.

### ⓓ وما يسدّ الثغرةَ التي يفتحها ② — `LNA7` تثبيتُ الثوابت
قانونُ البقاء وحدَه يمرّ لو تحرّك `SPLIT_GUARD` نفسُه (‏يتضخّم
`مُستبعَدو التقسيم` ويبقى الميزانُ صحيحًا) ⇒ **تُثبَّت ثوابتُ المجتمع
والمعيار في السويّة**: `SPLIT_GUARD = 3` · `FOLD_DAYS = 20` ·
`EXPL_X = 2.0` · `PRICE_HI = 20.0` · `CTRL_MAX_MOVE = 0.30` ·
`CC_BACK = 21` · `CC_CLEAR = 41` · `MIN_BUCKET_N = 50` ·
`RATIO_MIN = 2.0` · `ALPHA = 0.05` · `MIN_DAYS_COVER = 0.95` ·
`MIN_MATCH = 0.90` · `Z_FLOOR = 3.222`.
⇒ **صافي الأثر تشديدٌ لا إرخاء:** الحرّيّةُ الوحيدةُ الباقيةُ هي مرجعُ
التقسيمات الحيُّ **وأثرُه مطبوعٌ برقمه**.

### ⓔ محاسبةٌ صادقة لما خسرناه
المرساةُ القديمة كانت تدّعي «‏481 بالضبط»؛ والجديدةُ تدّعي «‏700 بالضبط
‏+ ميزانٌ مُغلَق ‏+ انزياحٌ مطبوع» ⇒ **أضعفُ بمقدار ما يستطيع مرجعُ
التقسيمات تحريكَه** (‏المرصودُ صفٌّ واحد = ‏0.21%)، **وأقوى في كلّ ما
عداه**. ⇒ **والنسبُ المنشورة (‏38.7% · 47.3% · 48.6%) تصير وصفيّةً لا
مرساةً** — صفٌّ واحدٌ يحرّكها ‏0.1 نقطة.

### ⓕ ما **لا** يغيّره هذا الملحق
⛔ لا معيارَ (`LK1` بحاله: ‏≥2× مقابل الشاهدَين معًا · فاصلا ويلسون
منفصلان · `n ≥ 50` · بونفيروني) · ⛔ ولا `z` (‏`max(z_bonf, 3.222)`) ·
⛔ ولا تعريفَ الميزة (`W = 60` · `K = 5`) · ⛔ ولا الفروعَ الثلاثة ·
⛔ **ولا بندَ «صفرُ شحنٍ ولو عبرت»** · ⛔ ولا `LOGIC_VERSION`.

### ⓖ قاعدةٌ تُعمَّم على المستودع
**أيُّ مرساةِ هُويّةٍ توضَع «فوق» كلّ نداءٍ لمرجعٍ حيّ** (تقسيمات ·
اقتراض · أرباح · فلوت) — لا تحتَه. **وينطبق أثرًا رجعيًّا على `U-RAW`
في `T-PMUSD`** (‏محسوبةٌ على `ev` بعد الفلتر نفسِه) ⇒ **معرَّضةٌ
للانزياح ذاته**، عبرت يوم 09-17 **ولا ضمانَ أن تعبر غدًا**. **لم تُمَسّ
هنا — تُدوَّن للعلم.**
