# 🌅⏱️ `T-PMGATE` — بوّابةُ «فتحِ البري + 30 دقيقة» · تسجيلٌ مسبق

> **يُدفَع ويُدمَج في `main` قبل أيّ سطرٍ من الأداة وقبل أيّ رقم.**
> أمرُ المالك: **«سجّل البوّابة»** (‏2026-09-11).
> **قراءةٌ/بحثٌ فقط — صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · لا `force_renew`.**

---

## ⓪ الدعوى ومصدرُها — وحدودُ ما أعرفه اليوم

**المصدر:** الملفُّ الصوتيُّ لدليل طريقة فيصل (‏35.8 دقيقة) بتفريغٍ **آليٍّ أرسله
المالك**. 🔒 **لا يُدفَع الصوتُ ولا التفريغُ** (عملُ صديق فيصل والمستودعُ عامّ) —
**والمدوَّنُ منه خلاصتُه وحكمُه لا نصُّه**، والمقتطعُ أدناه **الحدُّ الأدنى الذي
تُعرَّف به الفرضيةُ** فلا تصير الدعوى مطّاطة.

**الدعوى بلفظها المُقتطَع:** «الساعة 11 ونص ما صعد السهم … وهذه من النوادر تحدث ·
غالبا صعود السهم بعد 11 مباشرة … اللي طالع بالافتر · ما ضرب 11 ونص · **اذا ما تجاوز
اعلى شمعة وصلها ما يروح بعيد** · اسهم بالبريء ما يروح بعيد».

**وتفكيكُها إلى ما يُقاس:** للسهم الذي **صعد في الافتر**، يُنظَر عند **11:30 بتوقيت
المتحدّث** — وهي بتقويمه الصيفيّ **‏08:30 UTC = 04:30 نيويورك = فتحُ البريماركت
(‏04:00) + 30 دقيقة**: **هل تجاوز أعلى شمعةٍ بلغها في الافتر؟** فإن **لم** يتجاوزها
عند تلك اللحظة ⇒ **«ما يروح بعيد»** في بقيّة البري.

⚠️ **وسمُ المصدر `faisal_adopted` لا `faisal_verbatim`** — التفريغُ **آليٌّ مشوَّشٌ
في مواضعَ مقيسة**، ✳️ **والرقمان (‏11:30 · «ما يروح بعيد») يُقاسان ولا يُشحنان عتبةً.**

### ⓪-ب هذا **ليس** إعادةَ فتحٍ لمحورٍ مُغلَق — والفرقُ يُكتب قبل الأرقام

`T-AHEXT-2` أُغلقت أمسِ بالفرع 2، **ومحورُها «سقفُ الامتداد ‏100-120%»** أي **كم
يرتفع** المشتعلُ في الافتر. **وهذا المحورُ مختلف: متى يرتفع، وهل لحظةٌ بعينها تفصل.**
والكمّيّةُ المقيسة هنا (**تجاوزُ قمّة الافتر قبل 04:30** ثمّ **الحركةُ التالية**)
**لم تُحسَب في أيّ نسخةٍ من `T-AHEXT`** ولا في أيّ أداةٍ بالمستودع — والبحثُ في
`DECISIONS_ARCHIVE.md` و`HANDOFF_ARCHIVE.md` عن هذا المحور **صفرُ مطابقة**.
⇒ فشرطُ إعادة الفتح المؤرَّخ في `T-AHEXT-2` **لا موضوعَ له هنا**، والقيدُ المُلزِم
الباقي: **لا يُقرأ أيُّ رقمٍ من هنا حكمًا على السقف**، ولا العكس.

### ⓪-ج ما أعرفه اليوم وقد يلوّن قراءتي — يُعلَن ولا يُخفى

**هذي التجربةُ عمياءُ على كمّيّتها الحاكمة**: لم يُحسب عندي قطّ تجاوزُ قمّة الافتر
قبل 04:30 ولا الحركةُ الأمامية بعده. **والذي أعرفه ولا يُنبئ بها مباشرةً:** أن
`E-AH` (‏صاعدُ الافتر ‏≥20%) يتجاوز ‏+120% في البري **أكثرَ** من الطازج (‏−7.77 ·
−10.58 · −7.56 نقطة) — وهو حكمٌ على **المستوى** لا على **التوقيت**. ⇒ **التجربةُ
تُوسَم عمياءَ**، خلافًا لـ`T-AHEXT-2` التي وُسمت «تأكيديّةً مشروطة».

---

## ① التعاريف — بساعةِ نيويورك حصرًا

كلُّ لحظةٍ تُبنى بـ`ahext_arms.et_ms` (‏`ZoneInfo("America/New_York")`) **فتتصيّف
وتتشتّى آليًّا** — و**UTC المثبَّت عطبٌ مقيسٌ عندنا مرّتين** فلا يُستعمل.

| الرمز | التعريف |
|---|---|
| `AH` | نافذةُ الافتر: `D-1 16:00 → 20:00` |
| `ah_hi` | **أعلى قمّةٍ** في `AH` (‏«أعلى شمعة وصلها») |
| `PM-a` | البري المبكّر: `D 04:00 → 04:30` **حصرًا** (‏`[frm, to)`) |
| `PM-b` | البري المتأخّر: `D 04:30 → 09:30` **حصرًا** |
| `px_gate` | **آخرُ إغلاقٍ في `PM-a`** = سعرُ لحظة القرار |
| `hi_a` | أعلى قمّةٍ في `PM-a` |
| `hi_b` | أعلى قمّةٍ في `PM-b` |

**المجتمع:** صفوفُ `E-AH` وحدَها — أي `classify_arm(ah_rise) == "E-AH"`
(‏`ah_rise ≥ AH_MIN`)، **بالتعريف المنشور نفسِه مستوردًا بالاسم**.

**البوّابةُ عند 04:30:**

- **`G-YES`** ⇔ `hi_a > ah_hi` (تجاوز قمّةَ الافتر قبل 04:30)
- **`G-NO`** ⇔ `hi_a ≤ ah_hi` مع وجود شمعةٍ واحدةٍ على الأقلّ في `PM-a`
- **`G-NONE`** ⇔ **لا شمعةَ في `PM-a`** إطلاقًا (لم يتداول بعد)

🔴 **و`G-NONE` تُعلَن ولا تُطوى:** الدعوى تفترض سهمًا **يُرى** عند تلك اللحظة،
وسعرُ القرار عنده غيرُ معرَّف. ⇒ **تخرج من المقارنة الحاكمة وعددُها وحصّتُها
تُطبَعان**، **وتُقرأ ثانيةً في حساسيّةٍ مُعلَنة** (§③-ب).

## ①-ب المقياسُ الحاكم — ولماذا **ليس** مرجعَ إغلاق الأمس

- **الحاكم `FWD`** = `hi_b / px_gate − 1` ⇒ **الحركةُ الأمامية من لحظة القرار**.
- **الوصفيّ `PMP`** = `pm_peak` المنشور نفسُه (‏قمّةُ البري كلِّها ÷ إغلاق الأمس).

🔑 **والسببُ مكتوبٌ قبل الأرقام:** `G-YES` تجاوزت قمّةَ الافتر بالتعريف ⇒ بمرجع
إغلاق الأمس **تبدأ أعلى ميكانيكيًّا**، فيصير الفرقُ حسابَ تعريفٍ لا اكتشافًا —
وهو **بعينه** الاعتراضُ الذي أضعف `AX1` في `T-AHEXT-2`. **و`FWD` يُحصّنه:** كلا
الذراعين يُقاس من **سعره هو** عند اللحظة نفسِها.

**وعتبةُ «يروح بعيد»:** `FWD ≥ PM_MIN` — **و`PM_MIN = 20.0` رقمٌ قائمٌ عندنا
مستوردٌ بالاسم من `ahext_arms`، لا رقمٌ يُخترَع لهذي التجربة.**

## ② المجتمعُ غيرُ المنحاز — يُستورَد ولا يُعاد بناؤه

**العيبُ نفسُه قائم:** غربالُ الفجوة اليوميّة يُسقط الذائبين، وقد قاسه `AX5`
بـ**‏63.7%**. ⇒ تُستورَد بالاسم من `ahext2_arms` آلةُ الطبقات الثلاث:
`strata` (‏`H ⊎ L ⊎ Z` تستوعب الكونَ) · `plan_stratum` (معاينةٌ حتميّة ووزن `N/n`) ·
`kish` · `w_rate` · `w_pctile` · `NEFF_MIN` · `H_CAP`/`L_N`/`Z_N` · `SEED_TAG`.

🔒 **و`SEED_TAG` يبقى `T-AHEXT2-20260910` بلا تبديل** ⇒ **العيّنةُ هي عيّنةُ
`T-AHEXT-2` بت-بت** ⇒ (أ) المجتمعان قابلان للمقارنة مباشرةً (ب) يمكن **إثباتُ**
إعادةِ الإنتاج بقفلٍ سلوكيّ بدل الوعد.

**السنواتُ واللقطات (‏نفسُها بلا تبديل):** 2023 `31574165523` · 2024 `31199493957` ·
2025 `31191327731`.

## ③ الحرّاس وحدُّ العيّنة

- **`V-P1` القراءةُ فقط:** قفلُ AST على مصدر الأداة — صفرُ إرسالٍ وصفرُ كتابةِ حالة.
- **`V-P2` صفرُ نظرٍ مستقبليّ:** كلُّ نافذةٍ `[frm, to)` حصرًا · و`PM-a` و`PM-b`
  **لا تتقاطعان** · و`hi_b` لا يقرأ شمعةَ 09:30.
- **`V-P3` لا تسرّبَ بين الجلستين:** `ah_hi` من `AH` وحدَها.
- **`V-P4` حدُّ الدقّة:** `n_eff ≥ NEFF_MIN` (‏150) **لكلّ دلوٍ في كلّ سنة** —
  وسنةٌ لا تستوفيه في دلوٍ **لا تدخل `PG1`/`PG2`**. (‏**والنطاقُ منصوصٌ في البند
  الذي يستعمله** — درسُ ثغرة `T-UNIVERSE`.)
- **`V-P5` صحّةُ الوزن:** مجموعُ أوزان الطبقات = حجمُ الكون بهامشٍ دون 1%،
  **يُقرأ سلوكيًّا** لا بالثقة.
- **`V-P6` التغطية:** الجلبُ الناجح **≥`COVER_MIN`** (‏80%) في **كلّ طبقةٍ على
  حدة**، وطبقةٌ دونها ⇒ **لا حكم** (‏وليس «تُهمَل»: إهمالُها يُعيد الانحياز).
- **`V-P7` إعادةُ المنشور بت-بت:** على الصفوف نفسِها، `ah_rise` و`pm_peak`
  المحسوبان هنا **يطابقان `ahext_arms.measure` بت-بت** ⇒ الأداةُ لم تنحرف عن
  الأصل. **والأصلُ لا يُمَسّ بحرف.**
- **`V-P8` الميزانيةُ واحدة:** السقوفُ والكونُ والنافذةُ والخيوطُ **مثبَّتةٌ
  للأذرع كلِّها وتُطبَع في كلّ تقرير** (درسُ `T-CLIFF`: ميزانيةٌ مختلفة = فرقٌ
  مصطنع).

### ③-ب قراءةُ الحساسيّة المُعلَنة (لا تحكم)

تُعاد المقارنةُ مرّةً ثانية و`G-NONE` **مضمومةٌ إلى `G-NO`** بسعرِ قرارٍ بديلٍ =
**آخرُ إغلاقٍ في الافتر**. تُطبَع ولا تحكم — **وإن قلبت الإشارةَ فذلك يُنشَر بارزًا.**

## ④ ضبطان يعزلان «الساعة» عن «الزخم» — وصفيّان بنصّ العقد

الفصلُ بين *قراءةٍ صحيحة* و*تحصيلِ حاصل* لا يُترَك للرأي:

- **`C-TIME` نوعيّةُ اللحظة:** تُعاد البوّابةُ نفسُها عند **04:15 · 04:45 · 05:00**
  ⇒ هل 04:30 **لحظةٌ مميَّزة** أم نقطةٌ على منحنًى رتيب؟
- **`C-MOM` نوعيّةُ المرجع:** بوّابةٌ بديلةٌ **عند 04:30 نفسِها** مرجعُها **زخمٌ
  صرف** (‏`px_gate / prev_close` في النصف الأعلى من صفوف اليوم) بدل «تجاوزِ قمّة
  الافتر» ⇒ هل **المرجعُ الفيصليّ** يضيف على الزخم المجرَّد؟

⚖️ **وكلاهما وصفيٌّ صراحةً ولا يُسقط الحكمَ** — لأن رتابةَ المنحنى **متوقَّعة**
(معلومةٌ أكثر بمرور الوقت)، وسقوطُ دعوًى نافعةٍ على تقنيّةٍ خطأٌ كسقوطها على ضجيج.
**لكنّ نتيجتَهما تُنشَر مع الحكم لا بعده**، فلا يُباع «الزخمُ» على أنه «ساعةُ فيصل».

## ⑤ قاعدةُ القراءة — ثلاثةُ فروعٍ بشكلها المعتاد

ليكن **`Δ = hit(G-YES) − hit(G-NO)`** حيث `hit` = الحصّةُ **الموزونة** التي بلغ
`FWD` عندها `PM_MIN` فأكثر.

- **الفرعُ 1 — تثبت:** **`PG1`** `Δ` موجبٌ في السنوات الثلاث وفاصلُ البوتستراب
  **المجمَّع** لا يلمس الصفر · **و`PG2`** `Δ ≥ 10` نقاطٍ في **سنتين** فأكثر
  (ماديّةٌ لا دلالةٌ فقط) ⇒ **اقتراحٌ للمالك، بلا شحنٍ حتى يأمر.**
- **الفرعُ 2 — تفشل:** يسقط أحدُ المعيارين **أو تنقلب إشارةُ `Δ`** بين السنوات
  الداخلة ⇒ **يُغلَق المحورُ بشرطِ فتحٍ مؤرَّخ.**
- **الفرعُ 3 — لا حكم:** `V-P4` غيرُ مستوفًى في **سنتين فأكثر** · **أو** `V-P6`
  يسقط في أيّ طبقة · **أو** `V-P5` يسقط · **أو** `V-P7` يسقط.

**والبوتستراب عنقوديٌّ بالرمز** ببذرةٍ مثبَّتةٍ تُطبَع، على نمط `boot_diff_r`
المُستعمَل عندنا — فالصفوفُ تتكرّر بالرمز ولا تُعامَل مستقلّة.

## ⑥ تنبّؤاتي — تُكتب الآن وتُنشَر مكذَّبةً إن كُذِّبت

- **`Q1`** — `Δ` **موجب** في الثلاث (الزخمُ وحدَه يكفي لذلك).
- **`Q2`** — الفصلُ **رتيبٌ مع الزمن** (‏04:15 < 04:30 < 04:45 < 05:00) ⇒ **‏04:30
  ليست لحظةً مميَّزة**، والرقمُ 30 دقيقةً وصفٌ لا ميكانيكا.
- **`Q3`** — `G-NO` **أقلّيّة** بين صفوف `E-AH` (يطابق «غالبا صعود السهم بعد 11
  مباشرة»).
- **`Q4`** — `G-NONE` حصّةٌ **معتبرة (‏10% فأكثر)** من المجتمع.
- **`Q5`** — الأثرُ بمقياس `FWD` **أصغرُ** منه بمقياس `PMP` (الأخيرُ يعدّ الصعودَ
  المبكّر مرّتين).

## ⑦ حدودُ صدقٍ مُعلَنةٌ قبل الأرقام

1. **ليست حافّةَ تداول:** لا وقفَ ولا خروجَ ولا انزلاق ⇒ «يروح أبعد» ≠ «يُربَح منه».
2. **الزخمُ ملازمٌ للدعوى:** `G-YES` أظهرت قوّةً بالتعريف ⇒ الفصلُ **قد يكون زخمًا
   صرفًا**؛ وهو **نافعٌ للقرار** ولا يُسوَّق «ساعةً سحرية» — و`C-MOM` هو الحكم.
3. **انحيازُ البقاء قائم** (كونُ اللقطة) — درسُ `T-UNIVERSE`.
4. **الوزنُ يصحّح الانحيازَ لا الدقّة** — وطبقةٌ صغيرةُ العيّنة كبيرةُ الوزن تُدخل
   ضجيجًا، و`n_eff` هو الذي يُظهره.
5. **سيولةُ البريماركت رقيقة** ⇒ `px_gate` قد يكون على حجمٍ ضئيل؛ العددُ الوسيطُ
   لشموع `PM-a` **يُطبَع** ولا يُخفى.
6. **المصدرُ تفريغٌ آليّ** (‏§⓪) ⇒ الصياغةُ قد تحمل خطأَ نسخٍ في الساعة نفسِها —
   و`C-TIME` يكشف ذلك جزئيًّا.
7. **ولا شيءَ من هذي التجربة يمسّ الإنتاج** — لا عتبةَ ولا `LOGIC_VERSION`.

## ⑧ العواقب

| الفرع | ما يقع |
|---|---|
| 1 | **اقتراحٌ للمالك** (وسمُ تسليمٍ أو ترتيبٍ على صاعدِ الافتر) · **ولا شحنَ حتى يأمر** |
| 2 | **إغلاقُ المحور** بشرطِ فتحٍ مؤرَّخ: مصدرٌ ثانٍ للدعوى غيرُ التفريغ الآليّ · **أو** تعريفٌ مختلفٌ مسجَّلٌ مسبقًا للبوّابة (مرجعٌ غيرُ قمّة الافتر، أو نافذةٌ غيرُ 04:30) |
| 3 | **لا حكم** · ويُعلَن الحارسُ الساقطُ باسمه ورقمِه، ولا يُقرأ رقمٌ حكمًا |

## ⑨ ما لا يقع مهما كانت الأرقام

- **لا يُعدَّل هذا العقدُ بعد دمجه ليمرَّ قفلٌ أو ليطابق نتيجة** — يُصحَّح القفل.
- **لا تتحرّك ساعةُ البوّابة ولا `PM_MIN` ولا `AH_MIN` بعد رؤية رقم** (‏مطاردةُ
  نتيجة): الثوابتُ **تُستورَد بالاسم** من `ahext_arms` فلا تنحرف ولو أردتُ.
- **لا تُشغَّل سنةٌ رابعة لترجيح كفّة** — الثلاثُ مُعلَنةٌ هنا، وأيُّ سنةٍ إضافية
  **وصفيّةٌ تُعلَن ولا تدخل الحكم**.
- **ولا يُمَسّ `ahext_arms.py` ولا `ahext2_arms.py` بحرف.**
