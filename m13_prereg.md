# 🕵️ T-M13 — حدُّ الشورت غير المسنود على بِركة الإنتاج الجديدة (يُدفَع **قبل** أيّ رقم)

> **أمرُ المالك (2026-08-07 ليلًا):** «بعدها ٢ مباشرة» — البند ②: «حدّ الشورت
> M13 (‏40 ألف) بلا سند من فيصل (عتبته الوحيدة بلسانه: تحت 20 ألف، وعلى مقياس
> مختلف). قابل للقياس تاريخيًّا لو أمرت». **وهذا الملف مدفوع قبل أي تشغيلة.**

## ① ما تغيّر عن T-SHORT الأولى — ولماذا تُعاد على مجتمعٍ جديد

`T-SHORT` (2026-07-27) قاست الأداة نفسها على **بِركة البوّابات القديمة ولقطة
الناجين** وحكمت: ‏+10 · +29 · −7 ⇒ انقلبت بالسنة الثالثة ⇒ لا تغيير على M13.
**المجتمع تبدّل مرّتين منذها:** بوّاباتُ الإنتاج صارت العشرين (‏`FAISAL_ONLY=1`)
واللقطاتُ صارت أكوان PIT الشاملة (انحياز البقاء ساقط). فالسؤال يُعاد على
**بِركة الإنتاج الفعلية**: هل الشورتُ المنخفض عند الإشارة يميّز المنفجر داخلها؟

## ② الأداة — المسجَّلة القائمة بلا أيّ تعديل

`_bt_short_enrich` (‏FINRA اليوميّ المؤرَّخ **يوم الإشارة أو قبله حصرًا** ·
الغائب = مجهولٌ يُعرَض ولا يدخل المقارنة) + `backtest_short_thread` (شرائحُ
فيصل الثابتة + فاصلا Wilson + **مقارنةٌ مسجَّلة واحدة**: «‏10 آلاف أو أقل»
مقابل «من 20 إلى 40 ألفًا»). **صفر كود تحليلٍ جديد** — سائقٌ ناقلٌ فقط
(`short_arms.py`) لأن مدخل `bt_short` أُزيل من الـworkflow بسقف الـ25.

## ③ قاعدة القرار (قبل الأرقام) — وقفل C3 فوقها

- **لا يُقترَح أيُّ مسٍّ بـM13 إلا إذا** أظهرت المقارنةُ المسجَّلة فرقًا
  **بإشارةٍ واحدة في اللقطات الثلاث بلا انقلاب** وبفاصلَي ثقةٍ منفصلين في
  سنتين على الأقلّ — وهي عتبة درس T-SHORT نفسِه («سنتان لا تكفيان»).
- إن انقلبت الإشارة أو تداخلت الفواصل ⇒ الحكم: **العتبة لا تُميّز على هذي
  البِركة أيضًا ⇒ M13 تبقى 40 ألفًا كما هي** (قرارٌ هندسيّ مقفول C3، وتغييره
  قرارُ مالكٍ حصريّ بكل الأحوال — القياسُ يُنير لا يُقرّر).

## ④ حدودُ صدقٍ مسجَّلة (تُقرأ مع أيّ رقم)

**المقيسُ حجمُ FINRA اليوميّ لا «المتاح للاقتراض»** الذي ينطق به فيصل (لا تاريخ
له إطلاقًا) ⇒ لا تعميم إثباتًا ولا نفيًا — سبيلُ «المتاح» حصادُ `borrow_hist`
الأماميّ · شريحةُ «مجهول» تُعرَض بحجمها (تغطية FINRA على المغمورة ناقصة) ·
2026 جزئية · 2025/2026 داخل-العيّنة للبوّابات.

## ⑤ تنبّؤات (تُنشَر ولو كُذِّبت)

- **N1:** نمطُ الانقلاب يتكرّر (لا تمييزَ متّسقًا — اتّساقًا مع خمسة عشر
  تأكيدًا لـ«الحافة = التوقيت لا الاختيار»).
- **N2:** شريحةُ «مجهول» مادّية (فوق ربع المُعبَّآت) — تغطيةُ FINRA للمغمورة
  التي تصطادها العشرون أضعف من بِركة البوّابات القديمة.
