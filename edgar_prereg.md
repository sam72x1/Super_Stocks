# 📰🏛️ `T-PRE-EDGAR` — تسجيلٌ مسبق: الحافزُ الخبريّ إيداعَ SEC ليلةَ الانفجار

> **مدفوعٌ قبل أيّ سطرِ كودِ أداةٍ وقبل أيّ رقم.** كلُّ ما يلي مكتوبٌ ومُثبَّتٌ قبل رؤية أيّ
> مُخرَج — ولا يُحرَّك حدٌّ ولا نافذةٌ ولا مقياسٌ بعد ظهور الأرقام.
> أمرُ المالك: **«جميع ما سبق»** (‏2026-09-06) — وفيها «سجّل EDGAR».

---

## ⓪ لماذا الآن — ومن أين جاءت

`T-PRE-CEIL` أخرجت رقمين يحكمان هذي التجربة:

1. **السقفُ ليس بنيويًّا:** عرّافٌ يختار لكلّ صفٍّ أفضلَ مفتاحٍ من الثمانية والعشرين بأثرٍ
   رجعيّ يبلغ `R@60` = **‏67.7%** والمشحونُ **‏23.9%** ⇒ **المعلومةُ موجودةٌ والفجوةُ ×2.8.**
2. **ولا دمجَ يقترب منه:** أنظفُ دمج ‏+0.6 نقطة، **ودمجُ الثمانية والعشرين كلِّها أسوأُ من
   المفتاح المنفرد في ‏9 من 9 خلايا** ⇒ **الفجوةُ فجوةُ ميزاتٍ لا فجوةُ دمج**، والثمانيةُ
   والعشرون **مُغلَقةٌ بنصّ الحكم** (‏«لا يُعاد بخامسٍ ولا بصيغةِ دمجٍ ثانية»).

⇒ **الطريقُ الوحيد المفتوح هو ميزةٌ جديدةٌ خارج الثمانية والعشرين**، وأقربُها المسجَّلُ سلفًا
`E3` — الحافزُ الخبريّ. وهو **ليس مشتقًّا من السعر ولا من الحجم** فلا يرتبط بأيٍّ من الثمانية
والعشرين بالبناء، وهذا سببُ ترشيحه بعينه.

⚖️ **وحدُّ دائريّةٍ يُكتَب قبل الرقم:** فرضيّةُ «خبرٌ يسبق الانفجار» **لم تُشتقَّ من جدولٍ رأيناه**
— هي مسجَّلةٌ نصًّا في `presession_ceiling_prereg.md §⑤` **قبل تشغيل `E1`/`E2`** (‏`H6` في §②).
⇒ **ليست انتقاءً بعديًّا.** ولا يُعفينا ذلك من الشواهد: الشاهدُ الحتميّ إلزاميّ (‏§③-3).

---

## ① الشرطُ المسجَّل سلفًا — يُنقَل حرفيًّا ولا يُغيَّر

> «لكلّ منفجرِ ‏+100% في 2025 (من صفوف E1) مقابل شاهدٍ حتميّ من غير المنفجرين: هل يوجد إيداعُ
> SEC (‏`8-K`/`424B`/`S-1`) **قُبل بين 16:00 أمس و03:50** اليوم؟ المصدر
> `data.sec.gov/submissions` (‏`acceptanceDateTime`) · المعيار: الفرقُ **‏≥ 15 نقطةً بفاصلَي
> Wilson منفصلَين**. **تُبنى بعد E1/E2 وبأمرٍ صريح** — لأن جدوى المصدر (سقوفُ النداء · تغطيةُ
> رموزنا بـCIK) غيرُ مقيسة.»
> — `presession_ceiling_prereg.md §⑤` (مدفوعٌ `bd79d3f`)

🔒 **المعيارُ ‏(15 نقطة + فاصلان منفصلان) لا يُمَسّ.** وكلُّ ما يُضاف في هذا العقد **يُشدِّد
ولا يُرخي** (أرضيّاتُ عيّنةٍ وبوّاباتُ صلاحية) — ويُعلَن أنه إضافة، وأثرُه الوحيدُ الممكن
**زيادةُ احتمال «لا حكم»** لا زيادةُ احتمال النجاح.

---

## ② المرحلة صفر — مِجَسُّ الجدوى (**بلا أيّ رقمِ دراسة**)

نصُّ §⑤ نفسُه يشترطها: «جدوى المصدر … **غيرُ مقيسة**». وسابقتُها في المستودع
`flatfiles_probe` و`nbbo_history_verdict` و`otc_uplist_verdict`: **مِجَسُّ جدوى لا تجربةَ حكم**
⇒ سقفُ نجاحه صفر، ولا يُنشَر منه رقمُ فرقٍ ولا نسبةُ إصابة.

**يُشغَّل على عيّنةٍ حتميّةٍ من رموز 2025 (‏`sha256("edgar-probe:" + رمز)` أدنى 60 رمزًا) ويطبع
الخمسة، وكلُّ واحدةٍ بوّابةُ توقُّفٍ برمزِ خروجٍ غيرِ صفريّ:**

| # | البوّابة | الحدّ | الخروج عند السقوط |
|---|---|---|---|
| **F1** | تغطيةُ CIK: نسبةُ رموزِ العيّنة الموجودة في `company_tickers.json` | ‏≥ 70% | **‏5** «غيرُ قابلةٍ للقياس — تغطيةٌ ناقصة» |
| **F2** | حضورُ `acceptanceDateTime` في `filings.recent` لكلّ CIK مُختبَر | ‏= 100% | **‏5** |
| **F3** | العمقُ التاريخيّ: هل يبلغ `recent` تاريخَ 2025-01-02؟ وإلّا هل تُقرأ `filings.files[]`؟ | ‏≥ 90% من العيّنة تبلغ 2025-01-02 بإحدى الطريقتين | **‏5** |
| **F4** | سقفُ النداء: معدّلٌ فعليٌّ ‏≥ 5 طلبات/ثانية بلا `403`/`429` على 60 نداءً متتاليًا بـ`SEC_UA` | ‏≥ 5/ث · صفرُ حجب | **‏5** |
| **F5** | خطرُ تبدّل الرمز: نسبةُ رموز العيّنة التي **تغيب** اليوم عن الخريطة (‏= بُدّلت/شُطبت) | **يُقاس ويُعلَن — بلا حدّ** | — |

🔴 **و`F5` لا بوّابةَ له عمدًا** — لأنه **حدُّ صدقٍ لا عطبُ أداة**: الخريطةُ الرسميّة **بلا
تاريخ** (‏`company_tickers.json` لقطةُ اليوم)، فرمزٌ تبدّل بين 2025 واليوم **يُنسَب لشركةٍ
أخرى**. وسابقتُه مقيسةٌ عندنا بالاسم: **`BTOG` صار `SGRX`** يومَ ‏+200% (‏2026-09-04). ⇒
**يُعَدُّ ويُنشَر مع كلّ رقمٍ لاحق**، ويُستبعَد من الدراسة كلُّ رمزٍ يغيب اليوم عن الخريطة
(‏= **قصٌّ مُعلَنٌ بعدده** لا صامت).

⛔ **وإن سقطت أيُّ بوّابةٍ من `F1`-`F4` ⇒ تُنشَر «غيرُ قابلةٍ للقياس» بسببها المُسمّى وتنتهي
التجربة** — ولا يُخفَّض حدٌّ منها بعد رؤية رقمه.

---

## ②-ب حقائقُ المستودع المُتحقَّقة — قبل أيّ كود

**قُرئت من الكود بعيني ويُنشَر مصدرُها بالسطر** (لا تُخمَّن ولا تُنسَب لذاكرة):

1. 🔴 **`sec_recent_filings` غيرُ صالحةٍ لسنةٍ تاريخية بنيويًّا:** حلقتُها تكسر عند
   `fdate < pcut` حيث `pcut = today − PROXY_LOOKBACK_DAYS(75)` (‏`Super_stock.py:4788-4791`)
   ⇒ **لا ترى أبعدَ من 75 يومًا** مهما فعلنا. ⇒ **لا تُستعمَل هنا إطلاقًا** (مقفولٌ `V-E7`).
2. ✅ **والنمطُ الصحيح موجودٌ ومُختبَرٌ سلفًا:** `method_scan.parse_dated_offerings` **دالّةٌ
   نقيّةٌ تقرأ كلَّ `filings.recent` بلا قصٍّ زمنيّ** — و`docstring`ها يسمّي ذلك «الفرق الحاسم
   عن `sec_recent_filings`» — ومعها `offering_before` («‏**بلا نظرٍ مستقبليّ**: لا تُقرأ نشرةٌ
   بعد `when`»). ⇒ **يُنسَخ منها الشكلُ لا الجسم**، لأنها تقرأ `filingDate` **وهو ممنوعٌ هنا
   بنصّ §④** ⇒ تُكتَب دالّةٌ نقيّةٌ أختٌ تقرأ **`acceptanceDateTime`** وحده.
3. 🔴 **`acceptanceDateTime` غيرُ مقروءٍ في المستودع كلِّه** (‏صفرُ ورودٍ في أيّ `.py`) ⇒ حقلٌ
   جديدٌ كليًّا، **وحضورُه ليس مضمونًا** ⇒ `F2` بوّابةُ توقُّفٍ لا تحصيلَ حاصل.
4. 🔴 **بيئةُ التطوير تحجب SEC** (‏`403` على نفق `CONNECT`) ⇒ **المِجَسُّ يُشغَّل في Actions
   حصرًا**، ولا يُقرأ سقوطُه محلّيًّا حكمًا على المصدر (درسُ `flatfiles_probe`).
5. ✅ **والوصولُ الحيُّ من Actions مُثبَتٌ سلفًا بالأرقام** (تحقّقُ 2026-07-27): ‏`CMTL` 520
   إيداعًا · `AAPL` 589 · `APVO` 223 · `TYGO` 128 · `NMRA` 95 · `NEGG` 25 · `PONY` 21 —
   **وكلُّها دون سقف `recent`** ⇒ `F3` أرجحُ عبورًا، **ولا يُعفيها ذلك من كونها بوّابة.**
6. **سقفُ SEC المُعلَن 10 طلبات/ثانية**، والمشحونُ عندنا `time.sleep(0.15)` ‏≈ **6.7/ث**
   (‏`Super_stock.py:4942` و`5257`) ⇒ حدُّ `F4` (‏≥5/ث) **دون المشحون**، فسقوطُه يعني عطبًا
   حقيقيًّا لا تشدّدًا.
7. 🥇 **و`sec_offering_index.json` بُني ولم يُملأ قطّ:** `method_run.py:86` يطبع صراحةً
   «‏**غير مُختبَر** (لا فهرس SEC مؤرَّخ) — لا يُدَّعى استيفاؤه» ⇒ **شرطُ الطرح في صيّاد
   النهج العلميّ يعمل بأفضل-جهدٍ بلا فهرس**. ⇒ مِجَسُّ `E3` **يملأ الفهرسَ عرَضًا**
   فتستفيد `T-METHOD` مجّانًا. ⚠️ **ولا يُحتسَب نجاحًا لـ`T-PRE-EDGAR`** — أثرٌ جانبيٌّ
   يُعلَن، وسقفُ النجاح كما هو في §⑨.
8. **`SEC_CONTACT` مُصرَّحٌ به في خمسة workflows فقط** (‏`daily_screener` · `envelope_hunter`
   · `method_hunter` · `split_filter` · `split_hunter`) ⇒ **workflow المِجَسّ يلزمه تصريحُه**
   وإلّا صار `SEC_UA` بلا بريدٍ فتحجب SEC (‏عطبُ إعدادٍ يُقرأ «المصدر لا يعمل» — درسُ
   `BT_CANDLE`). مقفولٌ `V-E8`.
9. 🔴 **`presession_feats.py` نقيّةٌ بلا شبكة** (مقفولةٌ `PS16`/`PS19`) ⇒ **الميزةُ تُحسَب في
   `presession_scan.py` لا فيها**، وتُضاف إلى القائمة البيضاء باسمٍ **لا يصطدم** بمفاتيح
   الوسم (‏`max*` · `usd*` · `hit80_*` · `maxs` · `usds` · `t80_10`).
10. 🔴 **وصفوفُ `E1` المرفوعة لا تحمل الميزة** (‏`nan` بالبناء) ⇒ **لا يُعاد مسحُ السوق**
    (ميزانيةٌ ثابتة) بل تُبنى **أداةُ وصلٍ** تقرأ الصفوف المرفوعة وتُلحق الميزةَ بمفتاح
    **(‏رمز، يوم)**، وأيُّ صفٍّ لا يجد قرينَه **يُعَدّ ويُعلَن** لا يُسقَط صامتًا.

---

## ③ المجتمعُ والحالاتُ والشاهد (‏**لا يُبنى إلّا إن عبرت المرحلة صفر**)

1. **المصدرُ صفوفُ `E1`** (‏`presession_scan` · 2025 · `PM` والجلسة) — **هي هي بت-بت**، لا
   يُعاد مسحُ السوق (ميزانيةٌ ثابتة بالبناء).
2. **الحالات (‏`CASE`):** منفجرو ‏+100% في 2025 من صفوف `E1` — **العددُ المنشور 702**
   (‏`presession_ceiling_result.md §①`). يُطبَع العددُ الفعليُّ ويُقارَن بـ702؛ اختلافُه ‏>2%
   **عطبُ أداةٍ لا نتيجة** (‏`V-E1`).
3. **الشاهد (‏`CTRL`):** من **الأيام نفسِها والكون نفسِه** — غيرُ منفجرين — بسحبٍ **حتميّ**
   `sha256(f"{يوم}:{رمز}")` ثم أدنى `N` لكلّ يوم، بحيث `|CTRL| ≈ |CASE|` (‏نسبةُ 1:1).
   🔒 **حتميٌّ بالبناء** (نمطُ `control_panel` في `ctb_harvest.py`) ⇒ إعادةُ التشغيل تُعيد
   اللوحةَ نفسَها بت-بت، وصفرُ انتقاءٍ بعديّ.
4. **قصٌّ مُعلَن:** كلُّ رمزٍ بلا CIK اليومَ يُستبعَد **من الطرفين معًا** ويُعَدّ ويُطبَع.

---

## ④ الميزةُ وتعريفُ النافذة — بالدقيقة

`edgar_hit(رمز، يوم)` = **True** إذا وُجد إيداعٌ لِـCIK ذلك الرمز نموذجُه ضمن:

```
FORMS = 8-K · 8-K/A · 424B1 · 424B2 · 424B3 · 424B4 · 424B5 · 424B7 · S-1 · S-1/A · S-3 · S-3/A
```
(‏= اتحادُ `8-K` و`424B*` و`S-1` بنصّ §⑤، ومعها `S-3` لأن أغلبَ تخفيفِ فئتنا يمرّ به —
**وهي الإضافةُ الوحيدة على قائمة §⑤ وتُعلَن**.)

**والنافذةُ `acceptanceDateTime` داخل `[16:00 نيويورك من يوم التداول السابق ، 03:50 نيويورك من
اليوم]`** — طرفاها مُعلَنان: `16:00` إغلاقُ الجلسة النظاميّة · `03:50` لحظةُ قرار الرادار
(‏`DECISION_LEAD` المشحونة).

🔴 **و`filingDate` ممنوعٌ منعًا باتًّا** — SEC تُسنِد `filingDate` إلى **يوم العمل التالي**
لكلّ ما يُقبَل بعد 17:30 نيويورك، فهو يتقدّم ويتأخّر عن الحقيقة معًا ⇒ **يُسرّب ويُخفي في
الاتّجاهين**. `V-E3` يقفل ألّا يُقرأ `filingDate` في القرار إطلاقًا (بالـAST).

⚠️ **ومنطقةُ التوقيت تُحسَب لا تُفترَض:** `acceptanceDateTime` بتوقيت شرق أمريكا مع الإزاحة،
ويُحوَّل إلى `America/New_York` بـ`market_calendar` القائم (‏لا حسابَ إزاحةٍ يدويّ — درسُ
«التقويم غيرُ المُتحقَّق يخلط ثلاثَ ساعات»).

---

## ⑤ المعيارُ والأرضيات

**الحاكمُ (منقولٌ حرفيًّا من §⑤ — لا يُمَسّ):**

```
EG1 = نسبةُ CASE التي فيها إيداعٌ داخل النافذة
EG2 = نسبةُ CTRL التي فيها إيداعٌ داخل النافذة
النجاح ⟺ (EG1 − EG2) ≥ 15.0 نقطةً مئويّة  ∧  فاصلا Wilson 95% منفصلان تمامًا
```

**والأرضيّاتُ المضافة (تُشدِّد ولا تُرخي — تُعلَن إضافةً):**

| # | الشرط | الحدّ | عند السقوط |
|---|---|---|---|
| **EG3** | عيّنةُ الحالات بعد قصّ CIK | ‏≥ 100 | **«لا حكم»** ويُطبَع العدد |
| **EG4** | عيّنةُ الشاهد بعد قصّ CIK | ‏≥ 100 | **«لا حكم»** |
| **EG5** | نسبةُ القصّ (بلا CIK) في أيٍّ من الطرفين | ‏≤ 40% | **«لا حكم»** — مجتمعٌ مشوَّه |

⛔ **«لا حكم» ليست فشلًا ولا نجاحًا** — تُنشَر باسمها ولا يُبنى عليها قرار.

---

## ⑥ بوّاباتُ الصلاحية — **منفَّذةٌ في الكود برمزِ خروجٍ غيرِ صفريّ** لا موصوفة

> «حارسٌ مكتوبٌ وغيرُ منفَّذٍ في الكود ليس حارسًا — هو نيّة» (‏`envelope_prereg §⑦`).

| # | البوّابة | الخروج |
|---|---|---|
| **V-E0** | المرحلةُ صفر عبَرت (‏`F1`-`F4`) وإلّا لا يُشغَّل شيء | **‏5** |
| **V-E1** | عددُ `CASE` قبل القصّ يطابق المنشور **‏702 ‏±2%** | **‏3** «عطبُ أداةٍ لا نتيجة» |
| **V-E2** | `CASE ∩ CTRL = ∅` (لا رمز-يومَ في الطرفين) | **‏3** |
| **V-E3** | **بالـAST:** صفرُ ورودٍ لـ`filingDate` في مسار القرار · و`acceptanceDateTime` مقروءٌ فعلًا | **‏3** |
| **V-E4** | حتميّةُ الشاهد: تشغيلتان على المدخلات نفسِها ⇒ **لوحةٌ متطابقةٌ بت-بت** | **‏3** |
| **V-E5** | حارسُ الـ`no-op`: `EG1 > 0` **أو** `EG2 > 0` — صفرٌ في الطرفين معًا = أنبوبةٌ صامتة | **‏4** |
| **V-E6** | صفرُ نداءٍ لـ`Super_stock.send_telegram` وصفرُ كتابةِ حالة (بالـAST) | **‏3** |
| **V-E7** | **بالـAST:** صفرُ نداءٍ لـ`Super_stock.sec_recent_filings` في الأداة (‏قصُّها 75 يومًا يجعلها عمياءَ عن 2025 — §②-ب-1) | **‏3** |
| **V-E8** | `SEC_CONTACT` مُصرَّحٌ به في `env:` بملفّ الـworkflow (فحصُ YAML) — وإلّا صار `SEC_UA` بلا بريدٍ فتحجب SEC ويُقرأ الحجبُ «المصدرُ لا يعمل» | **‏5** |

---

## ⑦ بطاقةُ التنبّؤ (قابلةٌ للكسر · تُقيَّم حرفيًّا)

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| **EG-P1** | **المرحلةُ صفر تعبر** — التغطيةُ ‏≥70% والسقفُ ‏≥5/ث | سقوطُ أيّ بوّابةٍ من `F1`-`F4` |
| **EG-P2** | `F5` (الرموزُ الغائبة اليوم) بين **‏5% و20%** — لا ‏≤2% ولا ‏≥35% | خروجُه عن النطاق |
| **EG-P3** | 🔴 **الحاكمة تسقط:** الفرقُ يقع في **‏[‏+3 · +12] نقطة** ⇒ حقيقيٌّ ودون الحدّ | فرقٌ ‏≥15 (نجاح) أو ‏≤0 (لا أثر) |
| **EG-P4** | `EG2` (الشاهد) **‏≥ 8%** — الإيداعُ الليليُّ **شائعٌ** لا نادر | `EG2 < 8%` |
| **EG-P5** | `424B*` وحدَها أقوى فصلًا من `8-K` وحدَها (التخفيفُ يسبق الحركة في فئتنا) | العكس أو تساوٍ |

🔴 **و`EG-P3` يراهن ضدّ التجربة نفسِها** — كما في كلّ عقودنا: **التنبّؤُ الحاكم يراهن على
السقوط**، فلو نجحت كان النجاحُ رغم حدسي لا بفضله.

---

## ⑧ حدودُ صدقٍ — تُكتَب الآن وتُنشَر مع أيّ رقم

1. **الهويّةُ تُحلّ بخريطة اليوم** ⇒ الرمزُ المتبدّل يُنسَب خطأً (`F5` يقيس حجمه · `BTOG⟶SGRX`
   السابقة الحيّة). **غيرُ قابلٍ للإصلاح بمصدرٍ مجانيّ.**
2. **المُصدِرُ الأجنبيّ يودع `6-K` لا `8-K`** — وفئتُنا فيها صينيّاتٌ كثيرة ⇒ **الحالاتُ
   الأجنبيّة تُقرأ «بلا إيداع» بالبناء** فتُنقص `EG1` ⇒ **قراءةٌ متحفّظة تجاه الفرضية**
   (‏تشدّ الحكمَ نحو الفشل لا نحوه). و`6-K` **لا يُضاف بعد رؤية الأرقام**.
3. **«وُجد إيداع» ليس «الخبرُ حافز»** — نقيس **حضورَ إيداعٍ في نافذة** لا **مضمونَه**.
4. **الحالاتُ مختارةٌ على النتيجة** (منفجرون) ⇒ الشاهدُ الحتميُّ هو ما يجعل الرقمَ ذا معنى،
   ومع ذلك **لا ربحيّةَ تُقاس هنا إطلاقًا** — تمييزٌ لا عائد.
5. **سنةُ تقييمٍ واحدة (2025)** بلا شاهدِ سنةٍ ثانية.
6. **النافذةُ ليلةً واحدة**؛ خبرٌ قُبل قبل 16:00 أمسِ **يُقرأ «بلا إيداع»** بالتعريف.
7. **سقفُ النداء يُقاس لا يُفترَض** — و`F4` يقيسه على 60 نداءً فقط (‏ليس على المجتمع كلِّه).

---

## ⑨ سقفُ النجاح — مثبَّتٌ قبل أيّ رقم

**أقصى ما يُنتجه هذا العقد مهما كانت الأرقام: اقتراحٌ للمالك + سطرُ عرضٍ تشخيصيّ.**

⛔ **ولا يُفعَل مهما كانت النتيجة:** لا مفتاحَ ترتيبٍ جديد · لا بوّابةَ إطلاقٍ ولا كتم · لا مسَّ
بـ`FLOOR_BY_SLOT` ولا `PREFILTER_CAP` ولا `PREFILTER_KEY` ولا `TOPK` ولا `MIN_DAY_USD` ·
لا مسَّ بـ`M1`-`M14` ولا بالدخول/الوقف/الأهداف · **لا `LOGIC_VERSION`** ·
**ولا يُضاف `6-K` ولا نموذجٌ ولا نافذةٌ ثانية بعد رؤية الجدول.**

---

## ⑩ ترتيبُ التنفيذ

1. **هذا العقدُ يُدفَع أوّلًا** ·
2. **المرحلةُ صفر** (‏`edgar_probe.py` + workflow يدويّ · قراءةٌ فقط) ⟶ تُنشَر نتيجتُها
   **بلا رقمِ دراسة** ·
3. **إن عبرت:** أداةُ الدراسة + الأقفال + جولةُ طفرات ⟶ السويّة (خروج 0 · صفر فشل) ⟶ دفع ⟶
   تشغيل ·
4. النتيجةُ في `edgar_result.md` **بجانب هذا العقد لا فوقه** ·
5. **لا تغييرَ إنتاجيّ بلا «نفّذ» صريح.**
