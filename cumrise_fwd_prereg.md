# 📈➡️ T-CUMRISE-FWD — الحصادُ الأماميّ للرفعة التراكميّة (تسجيلٌ مسبق)

> **يُدفَع قبل أوّل صفٍّ يتراكم.** وُلد من حالتَي `$SAGT` (‏2026-08-18، زحفٌ ‏+24%
> في 44 دقيقةً بلا دقيقةٍ تبلغ 5% حتى 10:14) و`$PFSA` (‏2026-08-17): بوّابةُ
> الدقيقة الواحدة بنيويًّا متأخّرةٌ على الزاحف، و`T-CUMRISE` (جلسةٌ واحدة) رفض
> الأذرعَ الثلاث لكن حدودُ صدقه الأولى: **«جلسةٌ واحدة»**. هذا العقدُ يكبّر
> المقامَ **أمامِيًّا** — جلساتٌ حيّةٌ تتراكم، لا إعادةُ تدوير لقطات.

## ① السؤال
على **خمسِ جلساتٍ حيّةٍ فأكثر** تتراكم تلقائيًّا، هل تستوفي `R1` (تراكميُّ
`LIQ_CUM_MINUTES`=3) معاييرَ `T-GATE` المنقولةَ حرفيًّا مقابل `R0` (الإنتاج)
**على المجمَّع**؟ (و`R2`/`R3` تُسجَّلان للسياق بلا حكم.)

## ② الآلة — نفسُ المِجَسّ بلا ذراعٍ جديدة
`cumrise_probe.py` نفسُه (أذرعُه الأربعُ المقفولة `CR1`-`CR7` بلا حرفِ تغييرٍ
في القياس) يكتسب **وضعَ سجلٍّ** (`CUMRISE_LEDGER=1`): بعد جدول الجلسة يُلحق
**صفًّا واحدًا** بـ`cumrise_fwd_ledger.jsonl` — حقائقُ خامّةٌ لا مقاييسُ مشتقّة
(نمطُ `hunter_ledger`): اليوم · ولكلّ ذراع: عددُ المُنبَّهين · الإشعارات ·
قائمةُ التأخّر · مقامُ الإثمار ونسبتُه · غيرُ المحسوم · المتحرّكون المصادون.
**دِدوبٌ باليوم** (جلسةٌ مسجَّلةٌ لا تُعاد — الكرونان يمرّان بلا تكرار).
كرونان بعد إغلاق الافتر بالفصلين: `41 0` و`41 1 * * 2-6` (مُزاحان عن
الصيّادين `13`/`51`).

## ③ المعايير — منقولةٌ من `gate_prereg.md §⑥` بلا تحريك (على المجمَّع)
تُعتمَد `R1` **اقتراحًا للمالك** إن استوفت الأربعة معًا:
1. وسيطُ التأخّر المجمَّع أفضلُ من `R0` بخمسِ نقاطٍ فأكثر.
2. الإثمارُ المجمَّع لا يسوء بأكثر من `JITTER_PP`=3.6 نقطة.
3. الإشعاراتُ المجمَّعة لا تزيد فوق 50%.
4. **صفرُ متحرّكٍ يصيده `R0` وتفقده `R1`** عبر كلّ الجلسات.

## ④ الأرضيّات (دونها ⇒ «لا حكم» يُعلَن ولا يُطوى)
- جلساتٌ مسجَّلة **‏5 فأكثر**.
- مقامُ الإثمار المجمَّع **‏20 فأكثر لكلّ ذراع**.
- مراسي `R1` المجمَّعة **‏30 فأكثر**.

## ⑤ سقفُ النجاح — مثبَّتٌ سلفًا
**اقتراحٌ للمالك حصرًا.** ‏5% رقمُه **ولا يُمَسّ ولا يُقترَح تغييرُه** — المتغيّرُ
الوحيدُ المطروح نافذةُ قياسِ رقمِه (دقيقة ⟶ ثلاث). ولا تغييرَ آليًّا لأيّ عتبةٍ
مهما كانت الأرقام. **ولا ذراعَ تُضاف بعد الأرقام.**

## ⑥ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر ولو كُذّبت)
- **K-F1:** إثمارُ `R1` المجمَّع يسوء فوق 3.6 نقطة (امتدادُ جلسة 08-17) ⇒ تسقط
  بالمعيار ② — **وهذا التنبّؤ ضدُّ الذراع فإن صمدت `R1` كان أثمن.**
- **K-F2:** مكسبُ التأخّر يثبت بين ‏+4 و+9 نقاط.
- **K-F3:** جلسةٌ واحدةٌ على الأقلّ من الخمس تخرج `R1 ≡ R0` (صفرُ مِرساةٍ إضافية).

## ⑦ حدودُ صدقٍ مكتوبةٌ قبل أوّل رقم
1. «أثمر» **لمسُ ‏+3% خلال 15 دقيقة** لا تنفيذٌ ولا ربح.
2. دائريّةٌ جزئيّة (الذراعُ تنتقي الأقربَ للحركة) — لا تُنفى بهذا التصميم.
3. الإعادةُ ترى كلَّ دقيقةٍ والحيُّ يلفّ ‏≈60ث ⇒ الأعدادُ سقف.
4. المجمَّعُ يخلط جلساتٍ مختلفةَ المزاج — يُقرأ معه التفاوتُ بين الجلسات.
5. التأخّرُ يبتلع فجوةَ الافتتاح.
6. **الفلترُ المشحون خارج القياس** — يُقاس الزنادُ لا التسليم.
