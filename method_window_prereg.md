# 📜 T-METHOD-WINDOW — تحديثا «النهج العلمي» (تسجيلٌ مسبقٌ قبل أيّ رقم)

**التاريخ:** 2026-08-15 · **الآمر:** المالك («نفّذ الحزمة») ·
**العقد الأمّ:** `OPUS_METHOD_UPDATES_PACKAGE.md` (‏§③) — وهذا تنفيذُه حرفيًّا.

## ① السؤال المسجَّل

في منشور فيصل 2026-08-15 («كيف تعرف انه بيدبل») تحديثان **منصوصان** يخالفان ما
ينفّذه صيّادُ «النهج العلمي» اليوم:

| البند | النافذ اليوم | سنده | النصّ الجديد |
|---|---|---|---|
| نافذة الحدث المؤسِّس | **20-30 جلسة** | `IMG_0489` «تنتظر هبوطه فترة زمنيه · **20 < 30 يوم**» | **«من 10 ايام لشهر»** ⇒ ‏10-30 |
| RSI | **لا شيء** (أُسقط 2026-08-01) | «ليس في الصور الستّ» | **«‏+ RSI تحت 30»** |

**السؤال:** كم يُدخل التوسيعُ (‏10-30) وكم يُخرج شرطُ RSI تحت 30 — **من قمع
الصيّاد نفسِه**؟

## ② ما لا يُدَّعى (يُكتب قبل الأرقام)

**هذا قياسُ قمعٍ وكلفة — لا عائد ولا ربحية ولا حافّة.** سببُه مُثبَتٌ بالقراءة:
أداةُ العائد الوحيدة (`method_scan.py`) **بائتةٌ بأربعة أوجه** — تقرأ
`trigger_state` (القراءة المنسوخة التي صحّحها المالك) · و`trigger_state` **تحصر
RSI في ‏[20،30] أصلًا** فإضافةُ «تحت 30» عليها **no-op بنيويّ** · وتُعيد تطبيق
الحدث المؤسِّس بنسخةٍ لا تقرأ `CONFIG` · **ولا تقرأ لقطة PIT إطلاقًا** (‏`method.yml`
ينزّلها ويضبط `BT_FROZEN_PATH` و`method_run.py` لا يقرؤه = بصمةُ `BT_CANDLE`).
⇒ **يُمنَع استعمالُها هنا**، ولا يُقارَن رقمٌ من هذي التجربة بأرقام `T-METHOD`.

## ③ الأذرع الأربع (مثبَّتةٌ الآن — لا تُضاف خامسة بعد الأرقام)

| الذراع | النافذة | شرط RSI |
|---|---|---|
| `A0` (الأساس = الإنتاج اليوم) | 20-30 | لا شيء |
| `A1` | **10-30** | لا شيء |
| `A2` | 20-30 | **تحت 30** |
| `A3` | **10-30** | **تحت 30** |

## ④ التصميم الملزِم (ميزانيةٌ ثابتةٌ **بالبناء** لا بالوعد)

- **تمريرةٌ واحدة والأذرعُ مشتقّةٌ منها:** لكلّ (رمز، جلسة) يُنادى
  `method_founding(df_i, dmin=0, dmax=30)` **مرّةً واحدة** ثم تُشتقّ عضويةُ
  الأذرع من `bars_since_peak` ومن `rsi` المحسوب مرّةً ⇒ **يستحيل بنيويًّا** أن
  تختلف العيّنةُ أو الميزانية بين الأذرع (القاعدة المستخرَجة من إفساد `T-CLIFF`).
- **`dmax=30` في الأربع** ⇒ شريحةُ `df.tail(win_bars + dmax + 5)` **متطابقة**
  والمختلفُ مقارنةُ `dmin` وحدها ⇒ الأذرعُ **متداخلة** (`A0 ⊆ A1`).
- **دوالُّ الإنتاج بأسمائها:** `method_founding` · `method_sequence`
  (بنداء الإنتاج حرفيًّا `win = bars_since_peak + 2`) · `falling_gap_candle` ·
  `rsi` — **صفرُ منطقٍ مكرّر**.
- **الأذرعُ تمرّر عتباتِها صراحةً ولا تقرأ `CONFIG`** لنافذة الهبوط ولا لحدّ RSI
  ⇒ **أرقامُ هذي التجربة تبقى قابلةً لإعادة الإنتاج بت-بت بعد التطبيق**.
- **مشيٌ سببيّ:** كلُّ نداءٍ على `df.iloc[:i]` حصرًا · **خطوةُ 5 جلسات** مُعلَنة
  وواحدةٌ للأربع · وأرضيةُ 60 بارًا كما يشترط الإنتاج.
- **‏RSI على الفريم اليوميّ حصرًا** (قاعدةُ المالك «من الفريم الكبير إلى الصغير»
  + `IMG_0446`) · **و«تعذّرٌ ≠ مخالفة»**: `rsi()` تنتهي بـ`fillna(50)` فالمحشوّ
  يُقرأ 50 و«تحت 30» كانت سترفضه ⇒ يُميَّز المحسوبُ من المحشوّ صراحةً ويمرّ.
- **‏4 ساعات خارج القياس التاريخيّ** (الأداةُ بلا شبكة) ⇒ الأعدادُ **أرضيّةٌ**
  مقابل الحيّ الذي يقرأ الفريمين، **والمقارنةُ بين الأذرع لا بينها وبين الحيّ**.

🔗 **واقترانٌ يُعلَن قبل الأرقام:** نافذةُ التسلسل مشتقّةٌ من الحدث
(`bars_since_peak + 2`) ⇒ توسيعُ `dmin` **يُقصِّرها** للصفوف الجديدة (‏12-21 بارًا
بدل 22-32) ⇒ **`founding` يرتفع و`seq` قد لا يرتفع بنسبته** — ويُطبَع العدّادان
معًا فلا يُقرأ ارتفاعُ `founding` وحده مكسبًا.

## ⑤ العدّادات (مثبَّتةٌ الآن)

لكلّ ذراعٍ ولقطة، على نفس أزواج (رمز، جلسة) حرفيًّا: `price` · `window_ok` ·
`founding` · `seq` · `entry_zone` · `gap_ok` · `rsi_pass` · **`match`** ·
**والرموزُ الداخلة/الخارجة بأسمائها** · **وتوزيعُ `bars_since_peak` في سلّتين
‏10-19 و20-30** · وشريحةُ الكتالوج **وصفيًّا لا حكمًا**.

## ⑥ تنبّؤاتٌ قابلةٌ للكسر

سندُها ظرفُ الكاتالوج النافذ (بصمة `019c983e1691`): وسيطُ قاع RSI في نوافذ أسهم
فيصل **`RSI_OVERSOLD=33.0`** ووسيطُ RSI الحاليّ **`RSI_MAX_NOW=35.78`** ⇒
**«تحت 30» يقع دون وسيط كتالوجه نفسِه**.

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| `MP1` | سلّةُ ‏10-19 **غيرُ فارغة** في اللقطات الثلاث ⇒ التوسيعُ يضيف فعلًا | سلّةٌ فارغةٌ في أيّ لقطة |
| `MP2` | شرطُ RSI **يقصّ أكثر من نصف** المطابقين (`match(A2) < 0.5 × match(A0)`) مجمَّعًا | قصٌّ أقلُّ من النصف |
| `MP3` | `A3` **لا يُصفَّر** (`match(A3) ≥ 1` مجمَّعًا على الثلاث) | صفرٌ في الثلاث |

## ⑦ قاعدةُ التطبيق — **مثبَّتةٌ الآن قبل أيّ رقم**

- **`U1` (النافذة ‏10-30) يُطبَّق بلا شرط**: نصٌّ حرفيّ و**توسيعٌ رتيبٌ محض**
  (‏`10-30 ⊇ 20-30`) لا يُخرج مرشّحًا كان يمرّ.
- **`U2` (RSI تحت 30) يُطبَّق بوّابةَ رفضٍ بلا شرط أيضًا** (قاعدةُ «لا تتفلسف»:
  حين يكون نصُّه صريحًا يُنفَّذ) — **والاستثناءُ الوحيد المُعلَن سلفًا**: إن
  **صفَّر الأداة** (‏`match(A2)=match(A3)=0` في **اللقطات الثلاث** *و* صفرٌ على
  البِركة الحيّة في تشغيلةٍ واحدة) ⇒ يُشحَن **وسمَ عرضٍ وعدّادًا** ويُعلَن للمالك
  بكلمةِ تفعيلٍ واحدة. **ولا معيارَ ثالثٌ يُخترَع بعد الأرقام.**

## ⑧ بوّاباتُ الـno-op ورموزُ الخروج

| الرمز | الحالة |
|---|---|
| `2` | اللقطة مفقودة (`BT_FROZEN_PATH`) |
| `4` | **صفرُ أزواجٍ مقيسة** — بصمةُ الـno-op |
| `5` | `A0 ⊄ A1` (تداخلٌ منكسر) = **عطبُ أداةٍ لا نتيجة** |
| `6` | `rsi_pass` = كلُّ الصفوف أو صفرُها ⇒ المقياسُ لم يفرّق (يُطبَع بوصفه كذلك) |
| `0` | قياسٌ سليم |

⚠️ **و«`A1 == A0`» ليست عطلًا** — قد تكون نتيجةً حقيقية؛ ويميّزها **توزيعُ
السلّتين** المطبوع.

## ⑨ حدودُ صدقٍ تُكتب قبل الأرقام

1. **وحدةُ «يوم»** جلسةٌ عندنا وقد تكون تقويميّةً عنده — مُعلَنة، غيرُ محسومة،
   ولا تُغيَّر في هذي التجربة.
2. القياسُ **قمعٌ لا عائد** — لا «أفضل» ولا «أسوأ»، بل «يُدخل كذا ويُخرج كذا».
3. الأداةُ **يوميّةُ الفريم** والحيُّ يقرأ الفريمين ⇒ أعدادُها **أرضيّة**.
4. الخطوة 5 جلسات ⇒ **عيّنةٌ متّسقة** بين الأذرع لا حصرٌ كامل.
5. لقطاتُ PIT تشمل المشطوبين ⇒ انحيازُ البقاء **مخفَّفٌ لا ممحوّ** · والسلاسل
   **معدَّلةٌ بالتقسيمات** فقد يكون «الحدث المؤسِّس» أثرَ تعديل (الحارسُ المبنيّ
   في `method_scan.split_in_window` **غيرُ مبنيٍّ في الإنتاج**) — يُعلَن.
6. شريحةُ الكتالوج **مختارةٌ على النتيجة** ⇒ وصفيّةٌ مهما جاءت.
7. الوصفةُ كلُّها **«قيد الإثبات الأماميّ»** — وهذي التجربةُ **لا تغيّر ذلك**.
