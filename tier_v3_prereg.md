# 🥇③ تسجيلٌ مسبق: `T-TIER-V3` — «سجل تصنيف v3» (تصنيفٌ يضمّ `J1`)

> **الأمر:** «**سجل تصنيف v3**» (المالك 2026-08-26) — الاقتراحُ الثاني من سقف
> نجاح `T-TARGET10` (‏`target10_result.md`)، بعد أن قاس §B أن **`J1` تتفوّق على
> «قوي» التقاطًا ودقّةً في السنوات الثلاث** وأن «قوي» تلتقط ‏≈خُمس متحرّكي
> ‏+30% فقط — وحالةُ `$DAIC` (‏متوسط/ضعيف في يومَي ‏+583% و+84% و`J1` تلتقطه
> في اليومين) هي النمطُ لا الشذوذ.
> **يُدفَع هذا الملفُّ قبل أيّ رقم** — ولا ذراعَ تُضاف بعده.
> **قياسٌ فقط:** صفرُ كتم · صفرُ بوّابة · صفرُ مسٍّ بأرقام المالك ·
> **`liq_tier` لا تُمَسّ بحرف** · ولا `LOGIC_VERSION`.

---

## ⓪ لماذا هذي التجربةُ الآن — وما الذي تغيّر منذ `T-STRONG-2`

`T-STRONG-2` (‏2026-08-22) اعتمدت `S2` (أخضر ‏3 فأكثر، **بلا** `J1`) بأمر
المالك، و`J1` خرجت من التصنيف كلِّه فصارت شارةً مستقلّة. **ثم استجدّ دليلان:**
① **‏`T-TARGET10 §B` (على 44,200 مِرساة · 3 سنوات):** `J1` تتفوّق على «قوي»
في **المحورين معًا وفي كلّ سنة** — التقاطُ متحرّكي ‏+30%: ‏33.7/34.7/32.8%
مقابل 19.3/24.0/21.8 · **والدقّة**: ‏26.0/28.6/29.3 مقابل 22.0/26.5/23.1.
② **وكلفةُ التصنيف صارت كلفةَ تسليم:** بعد شحن `update_tier=["قوي"]`
(أمرُه الثالث 2026-08-26) التحديثاتُ لا تصل إلّا لحامل «قوي» ⇒ خطأُ الوسم
**يمنع النبضَ** عن مثل `DAIC`.

⇒ **السؤال:** هل يوجد تعريفٌ ثلاثيٌّ (قوي/متوسط/ضعيف) **يضمّ `J1`** يفصل
كالإنتاج أو أحسن **ويسترجع رابحين أكثر** — أم يبقى `S2` كما هو؟

---

## ① الأذرعُ الأربع — مثبَّتةٌ الآن ولا خامسة

كلُّها **تعريفاتٌ ثلاثيّةٌ كاملة** تُقرأ عند كرت `M5` من حقولٍ موجودةٍ في
صفوف `kasih2_scan` أصلًا (`_green` من `c3/c4/v2/v3` · `_j1` من سلّة `j1` ·
و`j1_top` = `J1` وصنفُ الخمس `strong` وفجوةٌ 75 فأكثر — **بقاعدة
`kasih_j1` الإنتاجية**). والحدُّ `SMIN` **يُستورَد من
`Super_stock.LIQ_TIER_STRONG_MIN` بالاسم** (رقمُ المالك «ارفع الحد 3» —
**لا يُمَسّ ولا يُحرَّك**):

| الذراع | 🥇 قوي | 🥈 متوسط | 🥉 ضعيف | لماذا هي بالذات |
|---|---|---|---|---|
| **`V0`** | أخضر ‏`SMIN` فأكثر | أخضر `SMIN−1` | الباقي | **الأساس** = الإنتاج `S2` حرفيًّا (بوّابةُ التكامل عليه) |
| **`V1`** 🎯 | أخضر ‏`SMIN` فأكثر **أو** `J1` | أخضر `SMIN−1` | الباقي | **الحاكمة — الاتحاد**: أصغرُ توسعةٍ تجيب حالةَ `DAIC` بلا نزع أحدٍ من «قوي» الحالية |
| **`V2`** | `J1` وحدَها | أخضر ‏`SMIN` فأكثر (بلا `J1`) | الباقي | **الإحلال**: هل `J1` وحدَها «قوي» أفضل؟ (اختبارُ §B على مقياس هذا الخطّ) |
| **`V3`** | أخضر ‏`SMIN` فأكثر **أو** `j1_top` | أخضر `SMIN−1` **أو** `J1` (غير top) | الباقي | **الاتحادُ القنّاص**: يضيف أدقَّ خليّةٍ مقيسة وحدَها (‏≈3× الأساس في §B) وبقيّةُ `J1` تجلس وسطًا لا قاعًا |

⛔ **ولا تُضاف ذراعٌ ولا تُحرَّك عتبةٌ بعد رؤية أيّ رقم.**

---

## ② المقياسُ الحاكم — والرفيقُ شرطٌ لا زينة

**الحاكم: «كاسح30 من سعر كرت الخمس» = حقل `kasih30_from5` المكتوب في
الصفوف** (صفرُ إعادةِ حساب). **وسندُ اختياره بنيويٌّ مكتوبٌ قبل الأرقام:**
- التصنيفُ يصل المالكَ **على كرت `M5`** وسعرُه القابلُ للفعل هو `e5` —
  و`kasih30` يُقاس من إغلاق دقيقة المِرساة، وهو سعرٌ **لا يملكه المالك**
  لحظةَ الكرت (عينُ خلط `HUIZ` الموثَّق في `MEASURE_CUT_NOTE`).
- ⚖️ **وإقرارُ صدقٍ لا يُطوى:** اختيارُ هذا الحاكم **مُطَّلعٌ على §B**
  (حيث تتفوّق `J1` على مقياسٍ من عائلة `e5`) ⇒ لكيلا يكون تسوّقَ مقياس،
  **يُشترَط الفصلُ على الرفيق أيضًا** (المعيار ⑥ أدناه): ذراعٌ لا تفصل
  على `kasih30` **لا تعبر** ولو تألّقت على الحاكم.

**المقامُ على الحاكم = الصفوفُ التي `kasih30_from5` فيها محسوبٌ** (= مَن
بلغ الخمسَ دقائق) — وهو مقامُ التسليم الفعليّ: مَن كُسر قاعُه قبل الخمس
**لا كرتَ له أصلًا** فلا تصنيف. والرفيقُ `kasih30` يُحسَب على كلّ الصفوف.

**المجتمع:** صفوفُ `kasih2_rows_*.jsonl` — **الملفّاتُ الثلاثةَ عشر
بمعرِّفاتها الكاملة** (لا تُشتقّ من مدًى — درسُ `impurity §⑧`):
السنواتُ الثلاث `32330346732` · `32330353463` · `32330360191` · والجلساتُ
العشر `32330168045` · `32343667021` · `32343673576` · `32345880211` ·
`32345882313` · `32345884227` · `32345886026` · `32345888162` ·
`32345889879` · `32345891385`.

---

## ③ المعيار — ستّةٌ تلزم معًا (الأربعةُ الأُوَل مستورَدةٌ من `strong2_scan` بالاسم)

تُقيَّم فئةُ **«قوي»** لكلّ ذراعٍ مقابل مكمِّلها (كلُّ ما ليس قوي) —
بثوابت `strong2_scan` نفسِها (`SEP_MULT=2` · `MIN_COVER=10` ·
`MIN_RECALL=25` · `TRAIN/HOLDOUT`):

1. **الفصل على الحاكم:** نسبةُ «قوي» ضِعفُ مكمِّلها فأكثر **و**فاصلا
   ويلسون منفصلان — في كلٍّ من 2023 و2024 و2025.
2. **التغطية:** «قوي» ‏10% فأكثر من مقام الحاكم.
3. **خارج العيّنة:** الشرطُ ① يصمد على 2025 (الهولد-آوت نفسُه).
4. **الاسترجاع:** «قوي» تحوي ‏25% فأكثر من كاسحي الحاكم كلِّهم.
5. **الرتابة (سلّمُ الفئات الثلاث):** نسبةُ قوي أعلى من متوسط، ومتوسط
   أعلى من ضعيف — **على الحاكم في كلّ سنةٍ من الثلاث** (نِقاطًا، بلا شرط
   ويلسون بين الدرجات) · وأيُّ فئةٍ دون 30 صفًّا في سنةٍ ⇒ الذراعُ تسقط
   على ⑤ (سلّمٌ لا يُقرأ لا يُعتمَد).
6. **الفصل على الرفيق:** الشرطُ ① نفسُه على `kasih30` في السنوات الثلاث —
   **حارسُ تسوّق المقياس** المعلَن في ②.

🔒 **وإن عبرت أكثرُ من ذراعٍ مرشَّحة (`V1`/`V2`/`V3`): يُختار الأعلى
استرجاعًا على الحاكم (④)** — قاعدةُ `strong2 §③` المثبَّتة، تُثبَّت هنا
قبل الأرقام. **وإن لم تعبر واحدةٌ: يبقى `S2` كما هو ولا اقتراح.**
⚖️ **و`V0` تُقاس وتُطبَع للمقارنة ولا تُرشَّح** — هي النافذُ أصلًا.

---

## ④ بوّاباتُ الصلاحية — قبل أيّ حكم

- **`G1` تكاملُ الأساس:** أرقامُ `V0` على الرفيق `kasih30` تُعيد أرقامَ
  `S2` المنشورة في `strong2_result.md` **بت-بت** (دقّةُ السنوات
  ‏34.0/41.5/38.5 · تغطية ‏10.2% · استرجاع ‏35.7%) — وإلّا **عطبُ أداةٍ
  لا نتيجة ⇒ خروج 3**.
- **`G2` تكافؤُ `j1_top`:** دالّةُ الأداة تطابق `Super_stock.kasih_j1`
  (العنصرَ الثاني) على أربع حالاتٍ حدّية (‏strong/فجوة 75.0 · strong/74.9
  · operator/80 · group) — وإلّا خروج 3.
- **`G3` السنواتُ الثلاث حاضرة** وإلّا خروج 2 (المعيار ① يشترطها).
- **`G4`** الأذرعُ الأربع **تتفرّق** على عيّنةٍ اصطناعية (لكلّ زوجٍ صفٌّ
  يفصله) — ذراعان لا تفترقان = بوّابةٌ عمياء (درسُ `V2` في
  `intraday_reclaim`) ⇒ خروج 3.

---

## ⑤ سقفُ النجاح — وما هو ممنوعٌ مهما كانت الأرقام

✅ **إن عبرت ذراعٌ: اقتراحٌ للمالك بأرقامها الستّة ومقارنتها بـ`V0`** —
وإن أمر يُبدَّل التعريفُ في `liq_tier` (**عرضٌ/تسليم ⇒ لا `LOGIC_VERSION`**)
**مع تسمية أثر التسليم صراحةً في الاقتراح**: `update_tier=["قوي"]` مشحونٌ
⇒ تغييرُ تعريف «قوي» يغيّر **مَن يستلم التحديثات** — يُقال له بالعدد
المقيس (كم صفًّا ينتقل). 🥇 **والسجلُّ الأماميُّ لا يضيع:**
`tier_fwd_ledger.jsonl` يخزّن `green`/`j1`/`j1_top` خامًّا ⇒ كلُّ صفٍّ
يُعاد اشتقاقُه بالتعريف الجديد بأثرٍ رجعيّ (`tier_of` تعيد الاشتقاق —
مقفول `TFW11`/`STR6`).

⛔ **وممنوعٌ في الحالتين:**
- مسُّ `liq_tier` أو أيّ إنتاجٍ **قبل أمره الصريح**.
- تحويلُ التصنيف إلى فلترِ إطلاقٍ أو كتمٍ جديد (‏`TIER3` قائم).
- إضافةُ ذراعٍ أو تحريكُ عتبةٍ أو تغييرُ قاعدة ③ بعد الأرقام.
- مسُّ `LIQ_TIER_STRONG_MIN`=3 (رقمُ المالك) أو أرقامِه (‏5% · $30 ألفًا
  · 3× · 10%) أو أيّ بوّابةِ إطلاق.
- ولا فئةَ رابعة (البنيةُ ثلاثيّةٌ بأمره).

---

## ⑥ تنبّؤاتٌ قابلةٌ للكسر — تُنشَر ولو كُذّبت

- **`TV-P1`** 🎯 **المخاطرةُ ضدّ الحاكمة:** `V1` (الاتحاد) تسقط أو تكاد على
  الفصل المزدوج — ضمُّ `J1` كلِّها يخفّف الدقّةَ نحو حدّ الضِعف، وأرجّح
  سقوطَ ⑥ (الرفيق) في سنةٍ على الأقلّ (‏`J1` على `kasih30` ‏≈20-23% مقابل
  ‏34-41% للعدّاد).
- **`TV-P2`:** `V2` (الإحلال) تسقط على الرتابة ⑤ — «متوسطُها» (أخضر ‏3
  فأكثر بلا `J1`) أعلى نسبةً من «قويّها» (`J1`) على الرفيق في سنتين على
  الأقلّ ⇒ سلّمٌ مقلوب.
- **`TV-P3`:** `V3` (القنّاص) تعبر الفصلَ المزدوج بأوسع هامشٍ بين
  المرشَّحات الثلاث، واسترجاعُها دون `V1` ودون 45%.
- **`TV-P4`:** لا ذراعَ مرشَّحة تعبر الستّةَ كاملةً ⇒ يبقى `S2`
  (سجلُّ المستودع: أغلبُ التجارب تسقط — وسقوطُ الجميع نتيجةٌ تُنشَر).
- **`TV-P5`:** من عشرة 08-19 (سؤال المالك الأصليّ): `V1` تُصيّر **5 أو 6**
  منهم «قوي» (‏`S2` أعطت 4 و`J1` تضيف `RDAC` — الرابحَ الحقيقيَّ الوحيد —
  على الأقلّ).

📌 **ومطابقةٌ وصفيّةٌ محسوبةٌ سلفًا (ليست تنبّؤًا — صفوفُ `DAIC` في
`tier_fwd_ledger.jsonl` معلومة):** `V1`/`V2` تسمان `DAIC` **قوي في يوميه**
(‏08-24: أخضر 2 + `J1` · 08-25: أخضر 0 + `j1_top`) · و`V3` تسمه **متوسط
يوم 08-24** (‏`j1_top=false` فيه) **وقوي يوم 08-25** — والإنتاجُ وسمه
متوسط/ضعيف. تُطبَع من السجلّ للتوثيق.

---

## ⑦ حدودُ صدقٍ تُقرأ **مع** الأرقام لا بعدها

1. 🔴🔴 **دائريّةٌ مضاعفة:** المكوّناتُ اختيرت في `T-KASIH-2` على هذي
   الصفوف بعينها، **واختيارُ الحاكم `from5` مُطَّلعٌ على §B** — المعيار ⑥
   يخفّف الثانية ولا يُلغي الأولى. **والاختبارُ النظيفُ الوحيد السجلُّ
   الأماميّ** (وهو يخزّن المكوّناتِ فيُعاد اشتقاقُه بأيّ تعريف).
2. **«كاسح» لمسُ ‏+30% لا صفقةٌ منفَّذة** — سقفٌ متفائلٌ للأذرع بالتساوي.
3. **المجتمعُ مراسي بوّابةِ السيولة** لا كونُ السوق، ومقامُ الحاكم «مَن
   بلغ الخمس» أصغرُ من مقام الرفيق — **والفرقُ يُطبَع لا يُخفى**.
4. **والعبورُ لا يُثبت ربحية** — يُثبت أن الفئةَ أغنى كاسحين من مكمِّلها.
5. صفوفُ الجلسات العشر أحدثُ عهدًا وأصغرُ مقامًا — تدخل التغطيةَ
   والاسترجاعَ المجمَّعين ولا تدخل المعيار ① (سنويٌّ حصرًا).
