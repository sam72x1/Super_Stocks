# 🔥🩸 `T-FUSE` «سجّل الفتيل» — التسجيل المسبق (يُدفع قبل أيّ رقم وقبل أيّ سطرِ أداة)

> **الأمر:** المالك 2026-08-27 «أبدا في تسجيل الفتيل» — بعد تقرير
> `faisal_presignal_report.md` (هدفُه الحرفيّ: «ادخل السهم و خلال اسبوع
> واهو فعلا بينفجر») وبعد دفعة صور القناة التعليمية `EDU_0827_*`
> (‏§ثالث وعشرون في `FAISAL_IMAGES_CATALOG.md`).
> **بحث/قياس حصرًا · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION`.**

---

## ⓪ لماذا هذي التجربة — وما المقيس سلفًا

- **الطرفُ الموجب مقيس:** ‏91% من كتالوج فيصل عشيّةَ انفجاره كان «حافظًا
  ‏≥3ج ثم كُنس قاعُه» (‏`press_prereg §⑮-ب` · 21/23 + 9/9 من أسماكنا)،
  و**‏79%** قاعُه طازجٌ 0-2 جلسة (‏§⑮). **والطرفُ السالب مجهول**: وسمُ
  «🩸 كنسٌ طازج بعد حفظ» يُعرض في الرادار اليوم وكُتب عند بنائه حرفيًّا
  «الإنذارُ الكاذب يُقاس أماميًّا» — **ولم يُقَس تاريخيًّا قطّ.** هذي
  التجربة تقيسه على ثلاث سنواتٍ كاملة.
- **وصورُ اليوم أضافت ساعةَ نضج:** «متوسطات الحركه … احسب بالايام من وين
  هابط · **20 يوم او 30 او 50**» و«**صعد قبل متوسطات الحركه = رزق لم
  يكتب · الاصل علمي ماصعد**» (‏`EDU_0827_school_two_models_ma_ladder` ·
  `EDU_0827_ma_rizq_rule_sle`) — وحالةُ `WNW` الحيّة (هبوط 21/7 ⟶ «حقق
  متوسط 30» ⟶ اختبر القاعَ ولا كسره وثبت ⟶ ‏+52% بعد الإغلاق في اليوم
  نفسِه). ⇒ **عمرُ الهبوط** بُعدٌ مسجَّلٌ في التجربة.
- 🔴 **وما لا تكرّره:** ليست قياسَ تّيب/صفقات (سقط 6 مرّات) ولا قياسَ
  «مَن هو السهم» (سقط في T-PRESS-Q وغيرها) — تقيس **«متى داخل حياة
  الجالس المضغوط»**، وهو المحور الذي لم يُفرَد بتجربةِ حكمٍ قطّ.

## ① السؤال

بين **حلقات الجلوس المضغوط** (قراءةُ التنبيه الإنتاجية `press_read` بنافذة
`ALERT_W=40`)، هل حالةُ **«الفتيل»** — قاعٌ **طازجٌ** (‏`hold_sessions`
‏0 إلى 2) جاء **كنسًا بعد حفظٍ** (‏`swept_hold=True`) — ترفع التوقّعَ
`R` ونسبةَ بلوغ الهدف فوق بقيّة الحلقات؟

## ② المجتمع والمحرّك — إعادةُ استعمالٍ بالاسم، صفرُ منطقٍ منسوخ

- **اللقطات:** `PIT` الثلاث المجمَّدة — 2023=`31574165523` ·
  2024=`31199493957` · 2025=`31191327731` (نفسُ لقطات `T-GOLD-ENTRY`).
- **المحرّك:** مِشيةُ `press_wake_arms.walk_symbol_wake` **نمطًا حرفيًّا**
  (لا تُعدَّل الدالّة — أرقامُها منشورةٌ عبر `gold_entry_result` ⇒ مجمَّدة
  `CAP15`): لكل جلسةٍ `press_read(df.iloc[:i+1], w=40)` بالاسم · عند
  المطابقة خطّةُ `rebound_arms.mirror_plan(press_low)` وحسمُ
  `rebound_arms.resolve_episode` (‏win/loss/no_fill/pending · القفزة
  `WAIT=40`) — **نفسُ آلة §⑬ التي نشرت ‏5371 · 19.08% · +0.174R.**
- **الحقول الإضافية للحلقة** (قراءةٌ من مُخرَج `press_read` نفسِه أو من
  الشريحة ذاتها — صفرُ نداءٍ جديد): `hold` (‏`hold_sessions`) · `swept`
  (‏`swept_hold` — دالّةُ الإنتاج `swept_after_hold`) · `prev_hold` ·
  `tl` (‏`tested_level` من مُخرَج `press_read`) · **`age`** = ‏`i − j_star`
  (جلساتٌ منذ قمّة النافذة = «من وين هابط») تُحسب **بسطر `press_read`
  الحرفيّ نفسه** (‏argmax على `hi[i-w+1:i+1]`) ويُثبت قفلٌ أن
  `hi[j_star] == r["high_w"]` بت-بت · و`rev`/`vol` من
  `press_radar.wake_read(sl)` كما في §⑯ (بلا افتر = أرضيّة).

## ③ الخلايا — مثبَّتةٌ الآن ولا خليّةَ تُضاف بعد الأرقام

تقسيمُ كلِّ الحلقات على محورَي (‏`swept` × طزاجة القاع):

| الخليّة | التعريف | الدور |
|---|---|---|
| `F-ALL` | كلُّ الحلقات | الأساس |
| `F-H3` | `hold ≥ 3` | **مِرساةُ التكامل** (يجب أن تعيد §⑬ مجمَّعةً بت-بت) |
| **`F1`** | **`swept=True` و`hold ≤ 2`** | **الحاكمة — «الفتيل»: كُنس قاعٌ محفوظٌ والقاعُ الجديد طازج** |
| `F2` | `swept=True` و`hold ≥ 3` | كنسٌ قديمٌ ثم عاد فحفظ (خليّة §⑯ 🩸) |
| `F3` | `swept=False` و`hold ≤ 2` | قاعٌ طازجٌ **بلا** بنية حفظ-فكنس (سكّينٌ خام) — **شاهدُ العزل**: الفرقُ `F1−F3` يعزل أثرَ البنية عن أثر الطزاجة |
| `F4` | `swept=False` و`hold ≥ 3` | جلوسٌ هادئٌ خالص |
| حلقاتُ `hold` غير المحسوب أو خارج الخلايا | تُعَدّ وتُسمّى | لا قصَّ صامتًا |

**واستكشافيّاتٌ تُنشَر ولا تحكم** (داخل `F1` و`F-H3` حصرًا — ولا تُرقّى
واحدةٌ إلا بكامل معايير §④ على نفسها، سابقةُ `T-KASIH-2`):
- **`age ≥ 20`** («صعد قبل المتوسطات = رزق لم يكتب» — الرقمُ 20 من سلّم
  فيصل المنصوص 20/30/50، `faisal_verbatim` من صور اليوم) مقابل `age < 20`.
- شمعةٌ انعكاسيةٌ يومَ القراءة (`rev` من `wake_read`).
- قفزةُ حجمٍ يومَ القراءة (`vol`).
- `tested_level` حاضرٌ (قاعٌ مزدوجٌ مُختبَر — زنادُ `WNW` «اختبرها ولا
  كسرها») مقابل غائب.

## ④ المعايير — الحكمُ على `F1` مقابل مُكمِّلها (كلُّ الحلقات عداها)

1. **فرقُ التوقّع** `E(F1) − E(F̄1)` **‏+0.15R فأكثر مجمَّعًا** (الحدُّ
   المُعاد من `T-STOP`/`T-LIBERATION`/`T-SLIP` بلا تحريك).
2. **موجبٌ في السنوات الثلاث** (لا انقلاب).
3. **فاصلا Wilson لنسبة بلوغ الهدف منفصلان مجمَّعًا** (‏`F1` مقابل `F̄1`).
4. **الأرضية:** ‏40 محسومةً في `F1` لكلّ سنة و150 مجمَّعًا — دونها
   **«لا حكم»** ويُطبع العدّاد.

**والحكم مكتوبٌ بطرفيه الآن:** عبرت الأربعة ⇒ «الفتيلُ يميّز» ⇒ اقتراحٌ
للمالك (ترقيةُ وسم 🩸 الطازج إلى مقدَّم ترتيبٍ/تنبيهٍ باسم «لحظة فيصل») —
**عرضٌ/ترتيبُ انتباهٍ لا بوّابة**. سقط أيٌّ منها ⇒ الوسمُ يبقى عرضًا كما
هو، ويُدوَّن أن توقيعة الكتالوج (‏91%) **التقاطٌ بلا تمييزٍ عكسيّ** —
نتيجةٌ تُنشَر لا تُدفَن.

## ⑤ بوّابات الصلاحية

- **`FV0` تكاملٌ مزدوج:** (أ) مِشيتي تُعيد حلقات `walk_symbol_wake`
  **بت-بت** في الحقول المشتركة (‏i · hold · oc · swept) على كلّ رموز
  اللقطة — أيُّ تفرّقٍ ⇒ خروج 3 (عطبُ أداةٍ لا نتيجة). (ب) `F-H3`
  مجمَّعةً عبر الثلاث تعيد §⑬: **‏5371 محسومة · 19.08% · +0.174R**.
- **`FV1`** حارسُ الـ`no-op`: صفرُ حلقاتٍ أو خليّةٌ حاكمةٌ فارغة ⇒ خروج 4.
- **`FV2`** `hi[j_star] == r["high_w"]` بت-بت لكلّ حلقة (قفلُ `age`).
- **`FV3`** عدّادُ الحلقات خارج الخلايا يُطبع (لا قصَّ صامتًا).
- **`FV4`** أقفالُ السويّة `FU*` + جولةُ طفراتٍ كاملة (كلُّ قفلٍ يسقط
  مرّةً عمدًا · وطفرةٌ تنهار لا تسقط = قفلٌ يُصلَّب) · وpreflight يرفض
  البدء عند مُشغِّلٍ آخرَ حيّ (`ps -eo pid,args`).

## ⑥ تنبّؤاتي — تُنشَر ولو كُذِّبت

- **`FU-P1`** *(الأخطر عليّ)*: `F1` تعبر ①③ لكنّ نسبتها المطلقة لبلوغ
  الهدف تبقى **دون 30%** — «الأغلب لا ينفجر حتى بعد الكنسة».
- **`FU-P2`**: `F1 − F3` موجبٌ في الثلاث (البنيةُ لا الطزاجةُ وحدها) —
  وإن تساويا فالكنسةُ بعد حفظٍ **لا تضيف** فوق أيّ قاعٍ طازج.
- **`FU-P3`**: `age ≥ 20` يرفع النسبة داخل `F1` («رزق لم يكتب») بفارقٍ
  مرئيٍّ وإن لم يبلغ الدلالة.
- **`FU-P4`**: `F2` (كنسٌ قديمٌ ثم عاد فحفظ) بين `F1` و`F4`.
- **`FU-P5`**: الشمعةُ الانعكاسية يومَ القراءة **لا تضيف** فوق `F1`
  (فواصل متداخلة) — امتدادُ سقوط أنماط الشموع كمميِّز.
- **`FU-P6`**: `no_fill` في `F1` **أدنى** من `F4` (القاعُ الطازج قريبٌ
  من الدفعات فيتعبّأ أكثر — نظيرُ اكتشاف `T-REBOUND-2`).

## ⑦ سقفُ النجاح — مثبَّتٌ سلفًا وبلا تغيير

مهما كانت الأرقام: **اقتراحٌ للمالك + وسمُ عرضٍ/ترتيبِ انتباهٍ فقط.**
لا بوّابةَ رفضٍ ولا كتمَ ولا مسَّ `press_read`/`swept_after_hold`/
`walk_symbol_wake`/`mirror_plan`/`resolve_episode` (منشورةُ الأرقام —
`CAP15`) ولا أرقامَ المالك ولا أيَّ جذرٍ (بصمة AST قبل كلّ دفعة).

## ⑧ حدودُ صدقٍ تُكتب قبل الأرقام

1. «بلغ الهدف» **لمسٌ لا تنفيذ** (سقفٌ متفائل — سابقة كل التجارب).
2. `E[R]` بخطّة المرآة (دفعاتٌ من قاع الضغط · هدف ×1.5) — **ليست ربحيةَ
   البوت** ولا خطةَ فيصل الكاملة.
3. انحيازُ بقاءٍ مخفَّفٌ لا معدوم (لقطات PIT تشمل من شُطب لاحقًا لكن
   مُدرَجي أثناء السنة غائبون) · بلا افتر (`wake_read` أرضيّة).
4. `w=40` قراءةُ التنبيه المعتمدة — النتائجُ لا تُعمَّم على `w=20`.
5. الاستكشافياتُ خمسٌ على نفس البيانات ⇒ خطرُ تعدّد المقارنات — لذلك
   حاكمةٌ واحدة (`F1`) والبقيةُ لا تُرقّى إلا بكامل المعايير.
6. توقيعةُ الكتالوج (‏91%) قِيست على **مِرساة الانفجار المصحَّحة**
   وعيّنةٍ مختارةٍ على النتيجة — هذي التجربةُ هي اختبارُها العكسيّ، ولا
   يُستنتج منها شيءٌ قبل صدورها.
