# ⛰️② T-CLIFF-2 — تسجيلٌ مسبق (يُدفَع **قبل** أوّل تشغيلة)

> **أمرُ المالك (2026-08-06):** «‏4 — لو متأكد من شغلك ودقيق فسوّها، المهمّ لا تتفلسف».
> **ومصدرُ الفرضية حكمُ `T-CLIFF` نفسه** (‏`cliff_result.md`)، حرفيًّا:
> «العلّة **مفتاحُ الترتيب والسقف، لا العتبة**» — لأن `c3` تُدخل `EHGO` البِركةَ،
> والترتيبُ بعمق **كليف اليوم الواحد** يضعه بذيلها، والسقفُ `PROBE_CAP`=80 يقصّه.
> **و`EHGO` ضحلُ الكليف بالتعريف وهو سببُ وجود `c3` أصلًا.**

---

## ① السؤال — واحدٌ لا يتوسّع

> هل يلتقط الصيّادُ سهمًا إضافيًّا من الستة **بتغيير مفتاح الترتيب و/أو رفع سقف
> الفحص وحدهما** — **بلا مسّ عتبة `SPLIT_CLIFF_PCT` ولا أيٍّ من شروط فيصل الستّة**؟

**ولا يُوسَّع:** لا عتبةَ تُخفَّض · ولا شرطَ يُلغى · ولا تُمَسّ `scan_split_hunter`.

## ② الأذرعُ الأربعة — مثبَّتةٌ الآن، لا خامسةَ لها

| الذراع | المُرشّح | الترتيب قبل القصّ | سقف `probe` |
|---|---|---|---|
| **A0** (الأساس الإنتاجيّ) | `c0` = كليف يومٍ واحد ‏≥30% | `cliff` | 80 |
| **A1** (ذراعُ `T-CLIFF`) | `c3` = تراكميّ ‏≥40% خلال ≤20ج | `cliff` | 80 |
| **A2** (الترتيبُ وحده) | `c3` | **`cum`** | 80 |
| **A3** (الترتيب + السقف) | `c3` | **`cum`** | **240** |

**والعزلُ مقصود:** `A1→A2` يعزل **الترتيب** · `A2→A3` يعزل **السقف**. فإن نجح
`A3` وحده عرفنا أن الثمن سقفٌ أعلى لا ترتيبٌ أذكى.

## ③ المقياسان — كلاهما إلزاميّ (نفسُ `cliff_prereg.md` §③ حرفيًّا)

1. **الاسترجاع:** كم من الستة (‏AZI·DSY·EHGO·ZCMD·JZ·SPRC) يحقّق **≥1 يوم مطابقةٍ
   كاملة** — بحكم `scan_split_hunter` **الإنتاجيّة نفسِها**.
2. **الكلفة:** معدّل المطابقين الكاملين/يوم على **السوق الكامل**، ومعه عددُ الداخلين
   لمرحلة `probe`.

🔴🔴 **وتصحيحٌ منهجيّ إلزاميّ يخصّ هذي التجربة بالذات:** الاسترجاعُ يُؤخَذ من
**نفس تشغيلة السوق الكامل** (‏`cliff_scan.py` يسجّل رموزَ المطابقين) — **ولا يُؤخَذ
من `hunter_six_check.py`**. السبب مقيس: تلك الأداة تمشي **رمزًا واحدًا** فلا يُقيَّد
بـ`PROBE_CAP` **ولا يعمل مفتاحُ الترتيب أصلًا** (ترتيبُ عنصرٍ واحد بلا معنى) ⇒ لكانت
أذرعُ `A2`/`A3` تُقاس في ظرفٍ **يُلغي بالضبط ما تختبره**. **وهذا هو عينُ العيب الذي
أفسد `T-CLIFF`** (‏`EHGO` ظهر بالاسترجاع وغاب عن الكلفة) — فلا يُكرَّر.

## ④ ⚖️ الميزانيةُ الثابتة — والأساسُ يُؤخَذ من تشغيلةٍ واحدة بعينها

اللقطة **`30597787554`** (نفسُ لقطة `T-CLIFF`) · نفسُ الكون · نفسُ النافذة · نفسُ
سقوف الجلب · `float_mode=yahoo` · `float_budget=1500`.

🔴 **وقيدٌ دقيق:** `cliff_scan.yml` يُضيف `c0` إلزاميًّا في كلّ تشغيلة — لكن في
تشغيلتَي `cum` يكون ذلك `c0` **مرتَّبًا تراكميًّا** فهو **ليس الأساس الإنتاجيّ**.
⇒ **الأساسُ المُلزِم للنسبة (‏×) هو `A0` من التشغيلة الأولى وحدها**، و`c0` في
التشغيلتين الأخريين **يُهمَل صراحةً** ولا يُقارَن به.

**التشغيلات الثلاث:** ① `arms=c3 · order=cliff · probe_cap=""` ⟶ `A0`+`A1` ·
② `arms=c3 · order=cum · probe_cap=""` ⟶ `A2` · ③ `arms=c3 · order=cum ·
probe_cap=240` ⟶ `A3`.

## ⑤ المعيار — **منقولٌ حرفيًّا** من `cliff_prereg.md` §④ بلا تعديل

ذراعٌ **«مرشّحة للعرض على المالك»** ⟺ استوفت الشرطين معًا:

- **(أ)** تلتقط **‏≥1 سهمًا إضافيًّا** من الستة **قبل انفجاره** (لا يُحتسب NUWE ولا JEM).
- **(ب)** معدّل المطابقين اليوميّ **‏≤3× معدّل `A0`** المقيس.

**وما لا يستوفيهما يُكتب «فشل» صراحةً** ولا يُعاد تدويره بمعيارٍ جديد.
**و«لا حكم»** حين لا تكفي العيّنة — وهي **ليست فشلًا**.
⚖️ **ونقلُ المعيار بلا تعديلٍ مقصود:** أيُّ تليينٍ له الآن — بعد رؤية أرقام
`T-CLIFF` — هو اختيارُ عتبةٍ تُناسب النتيجة.

## ⑥ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر ولو كُذِّبت)

- **`G1`** `A2` (الترتيبُ وحده) **يلتقط `EHGO`** — الفرضيةُ الأساسية من حكم `T-CLIFF`.
  *(يكذّبه غيابُه عن مطابقي `A2` ⇒ العلّةُ السقفُ لا الترتيب.)*
- **`G2`** كلفةُ `A2` **≈ كلفة `A1`** (‏±20%): الترتيبُ **لا يغيّر عدد** الداخلين
  للفحص، بل **أيَّهم** ⇒ عددُ المطابقين قد يتغيّر، وعددُ `probe` **لا**.
- **`G3`** `A3` يتجاوز سقفَ `3×` في شرط (ب) — لأن السقف 240 يُدخل **ثلاثة أضعاف**
  المفحوصين. *(لو لم يتجاوزه فالسقفُ خيارٌ عمليّ لا ترفٌ.)*
- **`G4` (فحصُ صلاحية):** `A1` يُعيد إنتاج رقم `T-CLIFF` (‏≈150 مطابقًا مقابل 145
  لـ`A0`). 🔴 **وإن لم يُعِده فالتشغيلةُ باطلةٌ كلُّها** ولا تُفسَّر أيُّ ذراعٍ فيها.

## ⑦ سقفُ النجاح — محدَّدٌ سلفًا

نجاحٌ كامل ⇒ **اقتراحٌ للمالك** فقط. `SPLIT_RADAR_ORDER` و`SPLIT_RADAR_PROBE_CAP`
**يبقيان على قيمتَي الإنتاج** (`cliff` · 80) ولا يُغيَّران تلقائيًّا.
🔒 **وصيّادُ المقسّم تحت حماية المالك** — يُبلَّغ ولا يُمَسّ.

## ⑧ حدودُ صدقٍ تُقرأ مع أيّ نتيجة

انحيازُ البقاء · الفلوت **ليس point-in-time** (ياهو اليوم على تاريخٍ ماضٍ — وهو
حدٌّ **مشترَكٌ بين الأذرع** فلا يحيز المقارنة) · بلا افتر · واللقطةُ كونُ ناسداك
**اليوم** لا كونَ تلك الأيام · و**ثبوتُ الالتقاط ليس ثبوتَ الربحية** (المقيس «هل
يظهر السهمُ مطابقًا قبل انفجاره؟» لا «هل يربح؟»).
