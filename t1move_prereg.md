# 🎯🪜 `T-T1MOVE` — «سجّل t1 متحرّك» (تسجيلٌ مسبق)

> **أمرُ المالك 2026-09-04:** «‏… · «سجّل t1 متحرّك» · …»
> **مدفوعٌ قبل أيّ رقم.** ولا تُضاف ذراعٌ ولا يُحرَّك حدٌّ بعد رؤية الأرقام.

---

## ⓪ لماذا هذي التجربة مشروعة — وهي **البابُ الذي تركته تجربتان مُغلَقتان**

نصُّ `T-TRANCHE` عند إغلاقها (‏2026-09-01): «🔓 **وشرطُ الفتح:** محورٌ جديد،
وأقواه مرشَّحًا **`t1` متحرّكٌ مع الدخول** (فالمقيسُ هنا رفعُ الدخول **بهدفٍ
ثابت** وهو **نصفُ التغيير لا كلُّه**)».
ونصُّ `T-LIBVOL-2` (‏2026-09-02): «🔓 **وشرطُ الفتح:** **`t1` متحرّكٌ مع الدخول**
(نصفُ التغيير الذي لم يُقَس في الثلاث تجارب) — **المرشَّحُ نفسُه الذي أغلقت عليه
`T-TRANCHE`**».

⇒ **ثلاثُ تجاربِ دخولٍ متتالية سقطت وكلُّها تشترك في عيبٍ واحدٍ مُعلَن:**

| التجربة | ما غيّرته | الهدف | الحصيلة |
|---|---|---|---|
| `T-TRANCHE` | رفعُ سلّم الدفعات (‏+2%) | **ثابت** | ‏−0.199R · فاصلٌ كلُّه تحت الصفر |
| `T-LIBVOL` | دخولٌ على كسرٍ بسيولة | **ثابت** | ‏−0.879R |
| `T-LIBVOL-2` | الكسرُ نفسُه بوقفٍ على قاع الكسر | **ثابت** | ‏−0.820R |

**والميكانيكا مقيسةٌ لا مُفترَضة** (‏`tranche_result.md`): «`t1` **ثابتٌ** فرفعُ
الدخول **يقصّ المكافأةَ حتمًا** ويوسّع المخاطرة» ⇒ كلُّ ذراعِ دخولٍ رفعت السعرَ
كانت تُقاس **بهندسةٍ عقابيّة بالبناء**. هذي التجربة تسأل السؤال الناقص:
**هل يعود التوقّعُ إن تحرّك الهدفُ مع الدخول فحُفظت الهندسة؟**

⚠️ **وإن خرجت مطابقةً لسابقاتها فهي تكرارٌ يُعلَن لا اكتشاف** — والقيمةُ حينها أن
«الهدفُ الثابت» يسقط تفسيرًا، فيبقى **الدخولُ الأغلى** وحده مسؤولًا.

---

## ① المجتمع والأداة

- **المجتمع:** صفقاتُ الباكتيست الإنتاجيّة نفسُها على لقطات PIT الثلاث
  (‏2023 · 2024 · 2025) — **نفسُ ما مشت عليه `T-TRANCHE`** فتكون النتيجتان
  قابلتين للمقارنة مباشرةً.
- **الأداة:** `t1move_arms.py` + `t1move.yml` (يدويّ · بلا كرون · قراءةٌ فقط).
  الحسمُ بدوالّ الإنتاج **بالاسم** — `analyze_ticker` لبناء الخطّة و`_resolve_arm`
  (أو مكافئُها الإنتاجيّ) للحسم. **صفرُ منطقٍ مكرَّر.**
- **الوقفُ لكلّ الأذرع = وقفُ الإنتاج المشحون** (‏`PIVOT_STOP_AT_LOW=True` ⇒
  الوقف = المِرساة) — **لا يُمَسّ**، فعزلُ أثر الهدف عن أثر الوقف شرطُ صلاحية
  (درسُ `T1b`/`T-STOP`/`T-LIBVOL-2`).

## ② الأذرعُ الخمس — **مثبَّتةٌ سلفًا ولا سادسة**

| الذراع | الدخول | الهدف `t1` |
|---|---|---|
| **`A0`** | الإنتاج (الدفعاتُ من المِرساة) | الإنتاج | ← **بوّابةُ تكامل** |
| **`M1`** | `T-TRANCHE P1` (‏+2% فوق المِرساة) | **ثابتٌ** (هدفُ الإنتاج) | ← يُعيد إنتاج `P1` |
| **`M2`** | `T-TRANCHE P1` (‏+2%) | **متحرّكٌ بالـR** ← 🥇 **الحاكمة** |
| **`M3`** | سلّمُ فيصل الحرفيّ (`P3` ‏+7%) | **متحرّكٌ بالـR** |
| **`M4`** | `T-TRANCHE P1` (‏+2%) | **متحرّكٌ بالنسبة** |

**وتعريفُ «متحرّكٌ بالـR» (الحاكم):**
`k = (t1_A0 − entry_A0) ÷ (entry_A0 − stop)` ثم `t1' = entry' + k × (entry' − stop)`
⇒ **نسبةُ العائد إلى المخاطرة محفوظةٌ بالضبط** وهو المعنى الحرفيّ لـ«الهدف يتحرّك
مع الدخول». **و«متحرّكٌ بالنسبة»:** `t1' = t1_A0 × (entry' ÷ entry_A0)`.

🔒 **ووحدةُ المخاطرة واحدةٌ لكلّ الأذرع: `R₀ = entry(A0) − stop`** — الدرسُ
المدوَّن في `T-TRANCHE` و`T-RECLAIM-INTRADAY`: ذراعٌ تُقاس بوحدتها هي تبدو أكبرَ
ممّا تعنيه للمحفظة.

## ③ المقياسُ الحاكم

**متوسّطُ `R` المحقَّق لكلّ صفقة** (لا نسبةُ الربح — خدعت أربع مرّاتٍ مقيسة:
`T-STOP` · `T-LIBERATION` · `T-RECLAIM-INTRADAY` · `T-TRANCHE`).
والحكمُ على **`M2 − A0` مقترنًا** (نفسُ الصفقات) لا على المتوسّطين منفصلَين.

## ④ المعاييرُ الأربعة — **تلزم معًا**

① `M2 − A0` **‏≥ +0.15R** مجمَّعًا — **رقمٌ مُعادٌ حرفيًّا** من `T-STOP` ·
`T-LIBERATION` · `T-RECLAIM-INTRADAY` · `T-PIVOT-BOTTOM` **ولا يُحرَّك بعد الأرقام**.
② **موجبُ الإشارة في السنوات الثلاث** بلا انقلاب.
③ فاصلُ ثقةٍ 95% (بوتستراب عنقودُه **الرمز**) **لا يلمس الصفر**.
④ أرضيةُ عيّنة: **‏150 صفقةً محسومةً مجمَّعًا و30 لكلّ سنة**.

⛔ **وسقفُ النجاح مثبَّتٌ سلفًا: اقتراحٌ للمالك — بلا شحن.** تحريكُ `t1` يمسّ
`analyze_ticker` (جذرٌ) **وبوّابةَ `MIN_RR_T1`** ⇒ يمسّ العضوية ⇒ أمرٌ صريح ثم
`LOGIC_VERSION` ثم `force_renew`.

## ⑤ بوّاباتُ الصلاحية — رمزُ خروجٍ حقيقيٌّ لكلٍّ

- **`V0`** `A0` يُعيد أرقامَ الأساس المنشورة **بت-بت** ⇒ وإلّا **خروج 3**
  («عطبُ أداةٍ لا نتيجة»).
- **`V1`** `M1` يُعيد `T-TRANCHE P1` (‏−0.4620 · −0.4056 · −0.3889) **بت-بت**
  ⇒ وإلّا **خروج 3**. (بوّابتان لا واحدة: الأولى تُصادق المحرّك والثانية تُصادق
  **ذراعَ الدخول** التي نبني عليها.)
- **`V2`** الأذرعُ الخمسُ **تتفرّق** فعلًا (‏`M1 ≠ M2` و`M2 ≠ M4`) ⇒ وإلّا **خروج 4**
  (بصمةُ `no-op`).
- **`V3`** **نسبةُ التعبئة متطابقةٌ بين `M1` و`M2`** — الهدفُ لا يؤثّر في التعبئة
  بالبناء ⇒ اختلافُها **عطبُ أداة** ⇒ **خروج 3**.
- **`V4`** تغطيةُ الأيام ‏≥95% والمفقودُ يُسمّى بتواريخه.
- **`V5`** السويّةُ خضراء.

## ⑥ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر مكذَّبةً كانت أو محقَّقة)

- **`T1-P1`** 🥇 **الأخطرُ عليّ:** أرجّح `M2 − A0` في **‏[+0.00 · +0.15]** —
  أي أن تحريكَ الهدف **يستردّ أكثرَ خسارة `T-TRANCHE` (‏−0.199R) ولا يبلغ البار**.
  السند: `T-EXIT` قاست أن **كلَّ هدفٍ أبعدَ من المرجع يهدم التوقّع** (‏+20% ⇒
  ‏−0.05..−0.25R · +100% ⇒ ‏−0.62..−0.90R) — وتحريكُ `t1` صعودًا **هو ذلك بعينه**.
- **`T1-P2`** نسبةُ الربح تحت `M2` **أدنى** منها تحت `M1` (الهدفُ الثابت أقرب)
  **والتوقّعُ أعلى** ⇒ **المرّةُ الخامسة** التي تخدع فيها نسبةُ الربح.
- **`T1-P3`** `M3` (سلّمُ فيصل الحرفيّ) يبقى **سالبًا** مقابل `A0` حتى بهدفٍ
  متحرّك — عرضُ السلّم عقوبةٌ بنيويّة لا أثرُ هدف.
- **`T1-P4`** `|M4 − M2|` **‏≤ 0.03R** (الصيغتان تتقاربان عند أسعارنا).
- **`T1-P5`** أرضيةُ العيّنة **تُستوفى بوفرة** (‏`T-TRANCHE` مشت 4,155 زوجًا).

## ⑦ حدودُ صدقٍ مكتوبةٌ **قبل** الأرقام

- **الحسمُ بلمسة ذيلٍ بلا انزلاق** ⇒ **يحابي الهدفَ الأقرب** ⇒ `M1` مُجمَّلة
  ⇒ القراءةُ **متحفّظةٌ تجاه `M2`** (تشدّ الحكمَ نحو الرفض لا القبول).
- **العضويّةُ مثبَّتةٌ عمدًا:** `rr` يُحسب في الإنتاج من `t1` الثابت، فتحريكُه
  يرفع `rr` ⇒ **تتبدّل العضويّة** وهو **غيرُ قابلٍ للتقييم** بهذي الأداة
  (سابقةُ `B2` في `T-PIVOT-BOTTOM`) ⇒ **يُعلَن ولا يُقاس**، والمقيسُ **الخروجُ
  وحده**.
- **النظامُ سالبُ التوقّع في الطرفين** ⇒ الجدولُ يقارن خسائرَ لا أرباحًا،
  **ولا دعوى ربحية**.
- انحيازُ بقاءٍ وتشويهُ تقسيماتٍ وعمى افترٍ — كما في كلّ باكتيستاتنا.

## ⑧ ما لا يُمَسّ مهما كانت الأرقام

`M1`-`M14` · أرقامُ المالك · الوقفُ المشحون · صيغةُ الدفعات · `WATCHLIST_SIZE` ·
`kasih_scan.resolve` · درعُ صيّاد المقسّم · وقنواتُ التنبيه كلُّها.
