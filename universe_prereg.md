# 🧭 `T-UNIVERSE` — «اعزل الكون»: تسجيلٌ مسبقٌ مدفوعٌ **قبل أيّ سطرِ أداةٍ وقبل أيّ رقم**

> أمرُ المالك (‏2026-09-10): «**اعزل الكون**» — بعد أن أثبت تفكيكُ المصدر
> (`survivorship_result.md §⑥/§⑦`) أن الفرقَ المنشور بين الأساس ولقطة `PIT`
> **ليس أثرَ كونٍ صافيًا** لأن الطرفين خطّا إنتاجٍ مختلفان (‏مصدرُ أسعارٍ **و**
> إصدارُ كود)، وأن التصميمَ **لا يملك دقّةَ الفصل**.
>
> 🔑 **والبندُ المفتوحُ بنصّه في `§⑤`:** «وعزلُ الكون فعليًّا يحتاج تشغيلةً
> واحدة: **نفسُ المصدر ونفسُ الكود على لقطة `PIT` مقصورةً على الناجين** —
> لم تُشغَّل، ولا تُدَّعى.» **وهذا العقدُ يُنفّذها.**

**تاريخُ الدفع:** 2026-09-10 · **قبل** كتابةِ سطرٍ واحدٍ من `universe_arms.py`
وقبلَ فتحِ سجلِّ أيّ تشغيلة. 🔒 **بحثٌ/قياسٌ فقط:** صفرُ مسٍّ بالإنتاج · لا
`LOGIC_VERSION` · لا `force_renew` · الجذورُ الاثنا عشر تبقى بت-بت · الأداةُ
**تقرأ ولا تكتب حالةً ولا تُرسل تلغرام**.

---

## ⓪ السؤالُ بالضبط — وما لا يقيسه هذا العقد

**السؤال:** لو ثبّتنا **مصدرَ الأسعار** و**إصدارَ الكود** و**يومَ التشغيل**،
وغيّرنا **عضويةَ الكون وحدَها**، كم يتحرّك الحكم؟

**آليّةُ العزل:** لقطةُ `PIT` واحدةٌ لسنةٍ واحدة تحمل **الناجين والمشطوبين معًا**.
تُمشى **مرّةً واحدة**، ثم تُقسَّم صفقاتُها بعضويةِ الرمز في **كون اليوم**:

| الذراع | تعريفُها | ملاحظة |
|---|---|---|
| **`U-ALL`** | كلُّ رموز اللقطة | = تشغيلةُ `PIT` المنشورة نفسُها |
| **`U-SURV`** 🥇 | رموزُ اللقطة **الحاضرةُ** في كون اليوم | «باكتيستٌ ساذجٌ يُشغَّل اليوم» |
| **`U-DEL`** | رموزُ اللقطة **الغائبةُ** عن كون اليوم | المشطوبون/المندمجون/مغيّرو الرمز |

⇒ **`U-ALL` = `U-SURV` ⊎ `U-DEL`** (اتحادٌ مُفكَّك) — والفرقُ بين الذراعين
**عضويةُ الكون وحدَها**، لأن المصدرَ واحدٌ والكودَ واحدٌ والشموعَ هي هي.

**ما لا يقيسه هذا العقد** (يُقال قبل الأرقام لا بعدها):
- **لا يُصالح** الفرقَ المنشور (‏`PIT` مقابل الأساس) — ذاك خلطُ مصدرٍ وكودٍ وكون،
  وقد حُكم عليه بأنه **غيرُ قابلٍ للفصل** بذلك التصميم. هذا عقدٌ **مستقلّ**.
- **لا يقيس منهجيةَ فيصل** — بل **فارزَنا الآليّ** على بوّاباتنا القديمة.
- **لا يقيس سياسةَ خروجٍ** غيرَ `t1` (‏`_bt_realized_r`).

---

## ① الأداةُ والتشغيل (يُثبَّت قبل أيّ تشغيلة)

- `universe_arms.py` — أداةٌ مستقلّة تُستورَد فيها `Super_stock` وتُعيد استعمال
  **`backtest_symbol` و`load_frozen_dataset` و`get_universe` و`_bt_realized_r`
  بأسمائها**. لا نسخَ منطق، ولا إعادةَ كتابةِ مشيٍ.
- `universe.yml` — يدويٌّ بلا كرون · `actions: read` لتنزيل اللقطة · مدخلاتُه
  `snapshot_run_id` و`year`.
- **اللقطاتُ الثلاث** (‏من `pit_prereg §ملحق ③`، لا تُبنى جديدة):
  ‏2023 = `31574165523` · 2024 = `31199493957` · 2025 = `31191327731`.
- **الإعدادات مثبَّتةٌ على تشغيلات `PIT` المنشورة حرفيًّا** (‏`pit_prereg §④`):
  **`faisal_only=0`** (بوّاباتنا القديمة) · **`bt_potential=1`** · سوقٌ كامل ·
  سنةٌ كاملة. أيُّ انحرافٍ عنها يُبطل `V-U1` فيُبطل التشغيلة.
- **ميزانيةٌ واحدةٌ للأذرع الثلاث بحكم البناء:** مشيةٌ واحدةٌ تُقسَّم، فلا يمكن
  أن يختلف سقفٌ أو كونٌ أو نافذةٌ بين ذراعٍ وأخرى (‏درسُ `T-CLIFF`).

---

## ② تعريفُ «الناجي» — يُثبَّت الآن ويُعلَن حدُّه

**الناجي = رمزٌ يُرجعه `get_universe()` الإنتاجيّة يومَ التشغيل** (ملفّا ناسداك
المُدرَجين، وهو **بعينه** الكونُ الذي يمسحه الفارزُ الحيّ).

⚠️ **وحدُّ صدقٍ يُقال قبل الأرقام:** هذا **كونُ اليوم** لا كونُ يوم التشغيلة
المنشورة (‏2026-07-27) ⇒ رمزٌ شُطب بين التاريخين يُعدّ هنا مشطوبًا وكان ناجيًا
هناك. **الأثرُ في اتّجاهٍ واحدٍ معلوم** (يزيد `U-DEL` قليلًا) ولا يُخفى.
⚠️ **ولا يُستعمل `pit_universe`-`gone`** (كونُ Polygon/XNAS) بديلًا: تعريفانِ
مختلفان، والمُلزِمُ هو الكونُ الذي **يمسحه فارزُنا فعلًا**.

---

## ③ المقاييس (تُطبَع لكلّ ذراعٍ كاملةً، بلا انتقاء)

`n_signals` · `n_decided` · `wins` · `losses` · `open` · `win_rate` ·
**`R` = متوسّط `_bt_realized_r` على المحسومة** (نفسُ ما يطبعه
`backtest_honest_summary` حرفيًّا) · وفاصلُ Wilson للنسبة.

---

## ④ قاعدةُ الحكم — **مكتوبةٌ قبل أيّ رقم**

**الفرضيّةُ المُختبَرة:** انحيازُ البقاء حقيقيٌّ وقابلٌ للقياس ⇒ **`U-DEL` أسوأُ
من `U-SURV`**.

**والاختبارُ الحاكمُ على العيّنتين المُفكَّكتين `U-DEL` مقابل `U-SURV`** — لا على
`U-ALL` مقابل `U-SURV` (‏عيّنتان متداخلتان، فرقُهما مكبوحٌ بحصّةٍ لا بأثر):

- **`G1` (حاكمة):** `U-SURV − U-DEL` في نسبة النجاح **و** في `R`.
- **`G2` (وصفيّةٌ لا تحكم):** `U-ALL − U-SURV` = **الأثرُ التشغيليّ** على العنوان
  المنشور (كم يُجمّل حذفُ المشطوبين الرقمَ الذي نعلنه).

**فروعُ القراءة (يُختار الفرعُ بالأرقام، لا تُختار الأرقامُ بالفرع):**
1. **الانحيازُ مُثبَتٌ ومقيس** ⟸ `U-SURV − U-DEL` **موجبٌ في السنوات الثلاث**
   **و** يتجاوز ضجيجَه في سنتين على الأقلّ.
2. **لا انحيازَ قابلًا للكشف عند هذي العيّنة** ⟸ الإشارةُ **تنقلب** بين السنوات
   **أو** كلُّ سنةٍ داخل ضجيجها ⇒ يُعلَن **الحدُّ الأعلى** لا «صفر».
3. **لا حكم** ⟸ سقط قفلُ صحّةٍ من `§⑤`، أو `n_decided` في `U-DEL` **أقلُّ من 30**
   في أكثرَ من سنة.

**الضجيج:** خطأٌ معياريٌّ لفرق نسبتين مستقلّتين
`SE = √(p₁(1−p₁)/n₁ + p₂(1−p₂)/n₂)`، و**بوتستراب عنقوديٌّ بالرمز** (‏5,000 عيّنة ·
بذرة `20260910` مثبّتة هنا) لفرق `R` — العنقوديُّ لأن صفقات الرمز الواحد ليست
مستقلّة (‏درسٌ من `T-WAIT-23W`).

**حدُّ العيّنة:** سنةٌ فيها `n_decided(U-DEL) < 30` **لا تدخل `G1`** وتُطبَع وصفيّةً.

---

## ⑤ أقفالُ الصحّة — سقوطُ أيٍّ منها يُبطل التشغيلة

| القفل | ما يُثبته | لماذا |
|---|---|---|
| **`V-U1`** | `U-ALL` **يُعيد إنتاجَ مجاميع `PIT` المنشورة بت-بت** (‏عددُ الإشارات · الرابح · الخاسر · النسبة) | لو لم تُعِدها فالأداةُ تمشي غيرَ ما مشته التشغيلةُ المنشورة ⇒ لا معنى للتقسيم |
| **`V-U2`** | `U-SURV` + `U-DEL` = `U-ALL` **صفقةً صفقة** (تقاطعٌ خالٍ · صفرُ صفقةٍ ضائعة) | التقسيمُ **يُتحقَّق منه سلوكيًّا** ولا يُفترَض من «استقلال الرمز» |
| **`V-U3`** | الناجون والمشطوبون **كلاهما غيرُ صفريّ** ولا يبتلع أحدُهما اللقطةَ كلَّها | ذراعٌ فارغةٌ = خطأُ مطابقةِ رموزٍ يُقرأ «نتيجة» |
| **`V-U4`** | `get_universe()` **نجح** (‏عددٌ في حدود الآلاف) وإلّا **خروجٌ غيرُ صفريّ** | فشلُ الجلب يُعيد كونًا فارغًا ⇒ **الجميعُ «مشطوب»** = أخطرُ عطبٍ صامت |
| **`V-U5`** | الإعداداتُ المطبوعة = `faisal_only=0` ‏+ `bt_potential=1` | انحرافُ إعدادٍ يُبطل المقارنةَ مع المنشور |

**وكلُّ قفلٍ منها يُطبَع في المُخرَج بقيمته**، لا بكلمة «نجح».

---

## ⑥ تنبّؤاتٌ قابلةٌ للتكذيب — **تُنشَر مكذَّبةً ولا تُحذَف**

- **`P1`:** `U-SURV` نسبةُ نجاحه **أعلى** من `U-DEL` في السنوات الثلاث.
- **`P2`:** الأثرُ التشغيليُّ `U-ALL − U-SURV` **أقلُّ من ‏2 نقطة** في كلّ سنة
  (لأن المشطوبين أقليّةٌ من الإشارات).
- **`P3`:** حصّةُ `U-DEL` من **الإشارات** **أعلى** من حصّته من **الرموز** —
  فارزُنا يطلب منهارًا ‏≥50%، وهي بعينها فئةُ المرشَّح للشطب.
- **`P4`:** فرقُ `R` بين الذراعين **أصغرُ** من فرق النسبة بالمعيار المعياريّ
  (‏الوقفُ يقصّ الذيلَ الأسوأ فيتقارب `R` حيث تتباعد النسبة).
- **`P5`:** `V-U1` يعبر من أوّل تشغيلة.

---

## ⑦ حدودُ صدقٍ تُعلَن **الآن** (لا تُكتشَف بعد الأرقام)

1. **تغطيةُ أسعار المشطوبين ‏≈90% لا 100%** (‏مقيسٌ في مرحلة PIT): مَن لا شموعَ
   له غائبٌ عن `U-ALL` أصلًا ⇒ **الأثرُ المقيسُ حدٌّ أدنى** للانحياز الحقيقيّ.
2. **المُدرَجون أثناء السنة غائبون** عن اللقطة (كونُها أوّلُ يومٍ من السنة).
3. **«غائبٌ عن كون اليوم» ليس «مات»** — اندماجٌ وتغييرُ رمزٍ داخله (سقفٌ خام).
4. **`get_universe()` يومَ التشغيل** ≠ كونُ يوم التشغيلة المنشورة (‏§②).
5. المقيسُ **نسبةُ النجاح و`R` بخروج `t1`** — لا سياسةَ إدارةٍ ولا أهدافَ أعلى.
6. **‏2023 لم يكن مفكَّكًا في `§⑥`** بسبب قصّ الطباعة عند 200 صفقة — **وهنا لا
   قصّ**: الأداةُ تقسّم في الذاكرة وتطبع المجاميع، فالسنواتُ الثلاثُ متساوية.

---

## ⑧ ما يترتّب على كلّ فرع (يُثبَّت الآن فلا يُفصَّل الحكمُ على المقاس)

- **فرع 1** ⇒ يُنشَر **مقدارُ** الانحياز، ويُلحَق بكلّ رقمِ باكتيستٍ مستقبليٍّ
  بوصفه تصحيحًا معلومًا. **ولا يتغيّر سطرُ كودٍ إنتاجيّ** (المقياسُ تشخيصيّ).
- **فرع 2** ⇒ يُنشَر **الحدُّ الأعلى** المقيس، ويُغلَق محورُ «انحياز البقاء»
  بشرطِ فتحٍ مؤرَّخ: مجتمعٌ أوسعُ أو تعريفُ ناجٍ مختلف.
- **فرع 3** ⇒ يُعلَن «لا حكم» بسببه المسمّى، **ولا يُعاد تفسيرُ الأرقام**.

**وفي الفروع الثلاثة: صفرُ مسٍّ بالإنتاج.** هذي تجربةُ قياسٍ لا اقتراحُ تغيير.
