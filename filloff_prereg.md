# `T-FILLOFF` — «إزاحةُ التعبئة»: هل تُبطل التكلفةُ المقيسةُ حكمَ `PR-SWEEP` المنشور؟

> **تسجيلٌ مسبقٌ مدفوعٌ ومدموجٌ في `main` قبل أيّ سطرِ أداةٍ وقبل أيّ رقم.**
> **الأمر:** المالك — «سجّل الإزاحة» (‏2026-09-15)، بعد أن أعلن `btcost_prereg.md §⑧-3`
> أن **أيَّ حكمٍ جديدٍ على الأزواج يلزمه عقدٌ مستقلٌّ ‏+ إذنُ المالك**. **هذا هو ذاك
> العقد، وأمرُه هو ذاك الإذن.**
> **التاريخ:** 2026-09-15 · **النوع:** بحثٌ/قراءةٌ فقط · **صفرُ مسٍّ بالإنتاج** ·
> لا `LOGIC_VERSION` · **وصفرُ مشيةِ باكتيستٍ جديدة.**

---

## ⓪ التلوّثُ المُعلَن — ما رأيتُه قبل كتابة هذا العقد

🔴 **هذا العقدُ مكتوبٌ بعد أن رأيتُ أرقامًا كثيرة، وأُسمّيها كلَّها بلا استثناء** —
فمن قرأ الحكمَ لاحقًا يعرف أين يقف الاحتياط:

**‏① من `fcost_result.md` (`T-FCOST`، التشغيلة `34751099730`):**
- `Δ(أ)` **المحسومةُ وحدَها** لـ`PR-SWEEP` عند `c = 0`: ‏**−0.8075 · −0.3411 · −0.9547**
  (‏2023 · 2024 · 2025) — سالبةٌ في الثلاث.
- `Δ(ب)` **صفرُ التعبئة** عند `c = 0`: ‏**+0.4248 · +0.3812 · +0.3492** — موجبةٌ في الثلاث.
- **حصصُ التعبئة:** ذراعُ المسح ‏**0.228 · 0.259 · 0.235** · الأساسُ ‏**0.852 · 0.857 · 0.843**.
- **إزاحةُ التعبئة** عند سبريدٍ ‏1.75%: ‏**1.0827 · 1.0381 · 1.0536** ⇒ **‏×2.55 · ×2.72 ·
  ×3.02** الدلتا ⇒ **سقطت `FC3`**.
- المجتمع: ‏**1620 · 1591 · 1607** إشارة · و**‏1381 · 1364 · 1354** محسومةً للأساس.

**‏② من `btcost_result.md` (`T-BTCOST`، التشغيلتان `34942262597` و`34952563381`):**
- **`C-MED` = ‏1.8018%** [1.7241, 1.8868] · `C-P25`/`C-P75` = ‏0.8584%/3.5831% ·
  `C-ENT` ‏1.9293% · `C-EXI` ‏1.6807% · وبالسنة ‏1.8692 · 2.0183 · 1.5152.
- **`S-MED` = ‏−0.4587%** · سالبٌ في ‏78.7% من ‏2,533 ساقِ خسارة.
- `E_A(0,0)` = ‏−0.976 · −0.785 · −0.945 ⟶ `E_A(C-MED, 0)` = ‏**−2.7443 · −2.5567 ·
  −2.7138** (حسمٌ ‏≈1.77 نقطة).
- **و`Δ` الأزواجِ عند القياس** (‏§⑨ الوصفيّ): `PR-SWEEP` (أ) ‏−0.8053 · −0.3537 · −0.9479
  · و(ب) ‏**+1.2880 · +1.2099 · +1.1891**.

**‏③ من `MARKET_BACKTEST_FINDINGS.md §0-و/§0-ل` (حكمُ `T1b` المنشور، مجتمعٌ آخر):**
- المُعبَّأةُ في الطرفين: ‏2025 **+3.8** (سفليّ ‏−0.4) · 2026 **+9.9** (سفليّ ‏+2.1) —
  وبعد إصلاح `F-L1`: ‏+3.7 (‏−0.2) · +9.1 (‏+1.1).
- التعبئة ‏16-24% · التفادي يسحق التحويل · **يُرفض ويُقفَل**.

🔑 **وما لم يُحسَب قطّ — وهو موضوعُ هذي التجربة وحدَه:**
**قراءةُ «المُعبَّأة في الطرفين» (المقامُ الزوجيّ) على مجتمع `T-FCOST`، عند التكلفة
المقيسة.** `T-FCOST` حسب قراءتَي (أ) و(ب) ولم يحسب الثالثة، **و§⑧ الوصفيُّ لم يحسبها
أيضًا** — وهي **بالضبط** المقامُ الذي جرى عليه الحكمُ المنشور بنصّ توثيق
`backtest_sweep_compare`. ⇒ **العددُ الحاكمُ في هذي التجربة عددٌ لم تره عيني ولا عينُ
أحد.**

---

## ① السؤالُ الحاكم — والمحورُ الجديد

> **هل تُغيّر تكلفةُ التنفيذ المقيسة (`C-MED`, `S-MED`) حكمَ `PR-SWEEP` على المقام
> الذي حُكم عليه فعلًا — أم يصمد الرفضُ كما هو؟**

**ولماذا هو محورٌ جديدٌ لا إعادةَ فتح:**

1. **`T-FCOST` أعلن هذي الفجوةَ بنفسه ولم يسدَّها** — نصُّه: «حكمُ `T1b` المنشورُ **لم
   يجرِ على القراءة (ب)** بل على **المُعبَّأة في الطرفين** = مقامٌ ثالثٌ محصَّنٌ من أثر
   العدد بالبناء ⇒ سقوطُ `FC3` **يُقيّد** الاستنتاجَ من (ب) **ولا يُبطل الحكمَ المنشور**».
   **فالتقييدُ مُعلَنٌ والتحقّقُ غائب.**
2. **والبندُ 2 من `PENDING_VERIFICATION.md` مفتوحٌ لهذا الزوج وحدَه بنصّه** — أُغلق
   لـ`PR-STOP` و`PR-LEGACY` وبقي مفتوحًا لـ`PR-SWEEP`.
3. **وموضعٌ ثالثٌ للإزاحة لم ينتبه له أحد:** التفكيكُ الذي يحكم رِجلَ «التحويل ≥ التفادي»
   يستعمل **اتّفاقيةَ صفرِ التعبئة داخل نفسه** (`br = 0` لغير المُعبَّأ) ⇒ **الإزاحةُ
   تعيش في رِجلٍ من رِجال المعيار المنشور نفسِه** — لا في قراءةٍ جانبيّةٍ فقط.

⛔ **وهذا ليس `T1c`:** الملفُّ مُغلَقٌ بنصّ `§0-ل` («لا `T1c` بلا محورٍ جديد ‏+ تسجيلٍ
مسبق ‏+ موافقة»). **وهذي التجربةُ لا تقترح نموذجَ دخولٍ ولا تتبنّى شيئًا** — هي
**تدقيقُ تكلفةٍ لحكمٍ قائم**. والثلاثةُ التي يشترطها الإغلاقُ مستوفاةٌ على أيّ حال:
محورٌ جديد (‏المقامُ الزوجيُّ عند التكلفة المقيسة — بِكرٌ بالدليل أعلاه) ‏+ هذا العقد
‏+ أمرُ المالك «سجّل الإزاحة».

---

## ② الجبرُ — أين تعيش الإزاحةُ وأين تختفي

الهُويّةُ المُغلَقة من `T-FCOST` (‏`fcost_arms.ret_at`):

```
ret(r₀, o, c, s) = 100·[(1 + r₀/100)·f_s·k − 1]     ·     k = (1 − c/2)/(1 + c/2)
f_s = (1 − s)  للخاسر  ·  f_s = 1  لغيره
```

**‏① في المقام الزوجيّ (المُعبَّأة في الطرفين) تختفي الإزاحةُ بالبناء:**
الصفَّان من **الصفقة نفسِها** فالمقامُ واحد ⇒ لا حدَّ `100(k−1)(f_X − f_Y)`. والفرقُ:

```
Δ_pair = 100·k·[ (1 + r_sw/100)·f_sw − (1 + r_a/100)·f_a ]
```

- **زوجٌ متماثلُ النتيجة** (ربحان أو خسارتان): `f_sw = f_a` ⇒ **انكماشٌ ضربيٌّ صرفٌ
  بـ`k·f`** ⇒ **الإشارةُ لا تنقلب** جبريًّا.
- **زوجٌ مختلطُ النتيجة**: `f` يختلف بين الساقين ⇒ **الأثرُ غيرُ متماثل**، و`s` السالبُ
  **يُخفّف الخاسرَ** أيًّا كان صاحبُه ⇒ **هنا وحدَه يمكن أن تتحرّك الإشارة** ⇒ **يُحسَب
  ولا يُفترَض.**

**‏② وفي التفكيك تبقى الإزاحةُ حيّة:** `d = sr − br` مع `0` لغير المُعبَّأ ⇒
- **التفادي** (خسارةُ أساسٍ · لا دخولَ بالمسح): `d = −ret(r_a, "loss", c, s)` ⇒ **رصيدُ
  الامتناع يتحرّك مع التكلفة** — بـ`k < 1` يكبر وبـ`s < 0` يصغر، **والمحصّلةُ تُقاس**.
- **التحويل** (خسارةٌ ⟶ ربح): الساقان مُعبَّأتان ⇒ انكماشٌ ضربيّ.
⇒ **رِجلُ «التحويل ≥ التفادي» هي الرِّجلُ التي تلمسها الإزاحةُ فعلًا.**

**‏③ ورِجلُ التعبئة (`n_sf/n_bf ≥ 40%`) لا تتأثّر بالتكلفة إطلاقًا** — لأن التكلفةَ
تُطبَّق **بعديًّا على العوائد** ولا تغيّر وقوعَ التعبئة. **وهذا يُعلَن سلفًا لا يُكتشَف
بعد الأرقام.**

---

## ③ المصدرُ وإعادةُ الاستعمال **بالاسم** — وصفرُ مشية

- **صفوفُ الصفقات:** `fcost_trades_2023.json` · `_2024` · `_2025` من أرتيفكت التشغيلة
  **`34751099730`** (`fcost-34751099730`) — وهي **نفسُ الصفوف** التي بُنيت عليها أرقامُ
  `T-FCOST` المنشورة، وتحمل `entry_model` · `outcome(_b)` · `ret_a`/`ret_b` ·
  `outcome_sweep(_b)` · `ret_sweep_a`/`ret_sweep_b` · `fill_reason_sweep` · `symbol` ·
  `date`.
- **التكلفة:** `btcost_legs.jsonl` من أرتيفكت التشغيلة **`34942262597`** —
  **و`C-MED`/`S-MED` يُستخرجان من السيقان حسابًا لا يُكتبان بيدي** (سابقةُ §⑧).
- **الآلة:** **`Super_stock.backtest_sweep_compare` تُنادى بالاسم حرفيًّا** على صفوفٍ
  أُعيدت كتابةُ عوائدها عند `(c, s)` ⇒ **المعيارُ السداسيُّ لا يُعاد كتابتُه فلا يزيغ**.
  ومعها `fcost_arms.ret_at` و`k_of` **بالاسم**.
- ⇒ **صفرُ نداءٍ للمزوّد · صفرُ مشيةِ باكتيست · ولا مفتاحَ `POLYGON_API_KEY` في
  الـworkflow أصلًا** (فالجلبُ مستحيلٌ بنيويًّا لا ممنوعٌ بالنيّة). **ولا سرَّ تلغرام**
  فالإرسالُ مستحيلٌ كذلك.

**نقطتا القياس اثنتان فقط:** `(0, 0)` و`(C-MED, S-MED)` — **ولا شبكةَ ولا مسحَ عتبات**،
لأن المطلوبَ **مقارنةُ حكمَين** لا رسمُ منحنًى (وشبكةُ التكلفة محسوبةٌ سلفًا في `T-FCOST`).

**والتقييمُ الحاكم على المجتمع المجمَّع** (اتّحادُ السنوات الثلاث في نداءٍ واحد) لأن
رِجلَ «موجبٌ كلَّ سنة» لا تعمل إلّا هناك · **وتُطبَع الثلاثُ منفردةً وصفيًّا**.

---

## ④ المعاييرُ المسجَّلة — والفروعُ الثلاثة

| # | المعيار | الشرط |
|---|---|---|
| **`FO1`** | **ثباتُ الحكم** | حكمُ `backtest_sweep_compare` عند `(C-MED, S-MED)` **هو نفسُه** عند `(0,0)` على المجتمع المجمَّع |
| **`FO2`** | **ثباتُ الرِّجل الساقطة** | **مجموعةُ** الرِّجال الساقطة من الستّ **متطابقةٌ** بين نقطتَي التكلفة |
| **`FO3`** | **ثباتُ إشارة الفرق الزوجيّ** | `Δ_pair` (ذراع A) **لا تنقلب إشارتُه** عند التكلفة المقيسة — لا مجمَّعًا ولا في أيّ سنةٍ من الثلاث |

**الرِّجالُ الستُّ** (‏من `backtest_sweep_compare` حرفيًّا): ① `la > 0` (الحدُّ السفليُّ
للفرق الزوجيّ A) ② `mb > 0` (ذراع B) ③ `cv_sum ≥ av_sum` ④ `cv_sum > 0`
⑤ `n_sf/n_bf ≥ 0.40` ⑥ موجبٌ كلَّ سنةٍ لها ‏3 صفقاتٍ فأكثر.

- **الفرعُ 1 — «الحكمُ يصمد أمام التكلفة المقيسة»:** `FO1` ✅ **و**`FO2` ✅ **و**`FO3` ✅
  ⇒ **يُغلَق البندُ 2 من `PENDING_VERIFICATION.md` لـ`PR-SWEEP`**، ويُرفَع بانرُ التقييد
  في `MARKET_BACKTEST_FINDINGS.md §0-ل` إلى **«قِيس وصمد»** بدل «مُقيَّد».
- **الفرعُ 2 — «الحكمُ يتزحزح»:** سقوطُ أيٍّ من الثلاثة ⇒ **لا إغلاق** · تُنشَر النتيجةُ
  بحرفها · **ويُرفَع الأمرُ للمالك** — **ولا تبنّي ولا تغييرَ إنتاجيّ تحت أيّ حال.**
- **الفرعُ 3 — «لا حكم»:** سقوطُ بوّابةِ صلاحيةٍ (‏§⑤) ⇒ خروجٌ **9** بلا حكم.

---

## ⑤ بوّاباتُ الصلاحية — تُطبَع كلُّها في كلّ تقرير

| # | البوّابة | الشرط |
|---|---|---|
| `V-O1` | **الهُويّةُ بت-بت** | مُخرَجُ `backtest_sweep_compare` على الصفوف **المُعادِ كتابتُها عند `(0,0)`** مطابقٌ **حرفًا بحرف** لمُخرَجه على الصفوف الخام ⇒ **مخالفات = 0** |
| `V-O2` | **لا انزياحَ في المعيار** | تفكيكي المحلّيُّ للرِّجال الستّ **يوافق حكمَ الدالّة الإنتاجيّة** عند نقطتَي التكلفة وفي الأربع تقييمات (مجمَّع ‏+ ثلاث سنوات) — وإلّا **خروج 6** |
| `V-O3` | **التكلفةُ محسوبةٌ لا مكتوبة** | `C-MED`/`S-MED` مستخرجان من `btcost_legs.jsonl` ويُطبعان، ويجب أن يطابقا المنشور (‏1.8018% · −0.4587%) ضمن ‏1e-4 |
| `V-O4` | **أرضيةُ المقام الزوجيّ** | `both` ‏**≥ 60** في كلّ سنةٍ **و≥ 250** مجمَّعًا — وإلّا **خروج 7** *(المتوقَّعُ ‏≈360/سنة بحساب حصص التعبئة المنشورة — فالأرضيةُ متحفّظة)* |
| `V-O5` | **صفرُ مشيةٍ وصفرُ جلب** | لا `run_backtest` ولا `requests` ولا `yfinance` ولا مفتاحَ مزوّدٍ في الأداة — **بالـAST** ‏+ غيابُ السرَّين من الـworkflow |
| `V-O6` | **الجذورُ لم تُمَسّ** | بصمةُ الاثني عشر ‏+ `_resolve_arm` ‏+ **`backtest_sweep_compare`** مطابقةٌ لـ`origin/main` |
| `V-O7` | **صفرُ إسنادٍ إلى `CONFIG`** | الأداةُ لا تُسنِد `CONFIG` في أيّ موضع (‏سابقةُ `V-F9`) · وقراءتا `BT_SWEEP_PCT`/`BT_SWEEP_STOP` داخل الدالّة الإنتاجيّة **تغذّيان سطرَ عرضٍ ولا تدخلان الحكم** — **ويُثبَت ذلك بنيويًّا** |
| `V-O8` | **التكلفةُ على العوائد وحدَها** | الحقولُ غيرُ العائدية (‏`outcome*` · `entry_model` · `fill_reason_sweep` · `date` · `symbol`) **متطابقةٌ بت-بت** بين الخام والمُعادِ كتابتُه · **ويُطبَع عددُ الصفوف التي تحمل عائدًا بنتيجةٍ ليست `win`/`loss`** (لا تُكتب فوقها، وتُعلَن) |
| `V-O9` | **الحتميّة** | إعادةُ التشغيل بالمدخلات نفسِها تُعطي المُخرَجَ نفسَه بت-بت |

---

## ⑥ التنبّؤاتُ — مكتوبةٌ **قبل** أيّ رقم، وتُنشَر مكذَّبةً كما تُنشَر مؤكَّدة

| # | التنبّؤ | الثقة |
|---|---|---|
| `OP1` | **`FO1` يصمد** — الحكمُ يبقى «يُرفض» عند التكلفة المقيسة | **عالية** — لأن رِجلَ التعبئة (‏23-26% ≪ 40%) **لا تتأثّر بالتكلفة بالبناء** فالرفضُ مضمونٌ برِجلٍ واحدةٍ على الأقلّ |
| `OP2` | **`FO2` يصمد** — الرِّجالُ الساقطةُ هي هي، ومنها التعبئةُ والتحويل≥التفادي | متوسّطة |
| `OP3` | **رصيدُ التفادي يكبر** بالتكلفة (‏`k < 1` يغلب `s < 0`) ⇒ رِجلُ «التحويل ≥ التفادي» **تزداد سقوطًا لا تتعافى** | متوسّطة |
| `OP4` | **`Δ_pair` ينكمش ‏≈2% ولا ينقلب** (‏`k ≈ 0.9822` عند ‏1.80%) ⇒ `FO3` يصمد | **عالية** — جبريّةٌ للأزواج متماثلة النتيجة |
| `OP5` | 🔴 **`Δ_pair` عند `c = 0` على هذا المجتمع سيكون سالبًا أو قريبًا من الصفر** — خلافًا لـ`+3.8`/`+9.9` المنشورَين على المجتمع القديم | **منخفضة — وهو أخطرُ تنبّؤاتي**: أبنيه على أن `Δ(أ)` سالبةٌ في الثلاث، **وهو استدلالٌ من مقامٍ آخر** (‏مجموعتان مختلفتان لا تقاطُعُهما) فقد يكذب |
| `OP6` | **حسمُ التكلفة في `Δ_pair` أصغرُ من حسمها في `E_A`** (‏1.77 نقطة) بمرتبة، لأن الأخيرَ يحمل حدَّ `100(k−1)` كاملًا والأوّلَ لا يحمله | متوسّطة |

---

## ⑦ حدودُ صدقٍ — تُكتب **قبل** الأرقام

1. 🔴 **المجتمعُ غيرُ مجتمعِ الحكم المنشور** — `T1b` حُكم عليه على **2025 و2026** بكونٍ
   **قبل بوّابات فيصل** (2026-08-06) **وقبل وقف القاع** (2026-08-29)، وهذا المجتمعُ
   **2023-2025 ببوّابات فيصل ووقف القاع**. ⇒ **هذي التجربةُ تُعيد حكمَ المعيار، ولا
   تُعيد إنتاجَ التشغيلة المنشورة** · **ومستوياتُ الأرقام لا تُقارَن بـ`+3.8`/`+9.9`.**
   ⚖️ **والتصميمُ يعزل أثرَ التكلفة رغم ذلك**، لأن نقطتَي التكلفة تُقاسان **على الصفوف
   نفسِها** ⇒ الفرقُ بينهما **تكلفةٌ صرفة**.
2. **`C-MED`/`S-MED` وسيطان للمجتمع يُطبَّقان بالتساوي** — لا تكلفةَ لكلّ صفقة. والتكلفةُ
   الحقيقيّةُ تختلف بالرمز والوقت (‏`C-P25` ‏0.86% و`C-P75` ‏3.58%).
3. **`NBBO` قمّةُ الدفتر لا عمقُه** ⇒ `C-MED` **أرضيّةٌ للتكلفة لا سقف**.
4. **«اللمسُ ليس تعبئة» على الهدف غيرُ منمذَج** ⇒ المحرّكُ يبقى متفائلًا ⇒ **سقفٌ للأداء
   لا أرضية** — اتّجاهٌ معاكسٌ للحدّ ③، **والاثنان يُقالان معًا**.
5. **`s` مقيسٌ على ساقِ الخسارة وحدَها** ويُطبَّق كذلك · **وسالبيّتُه تعريفيّةٌ جزئيًّا**
   (مرجعُ النموذج `stop×(1−c/2)` يقع تحت الوقف) ⇒ تُقرأ «ساقُ الخسارة متحفّظةٌ لا متفائلة».
6. **التكلفةُ لا تُحرّك لحظةَ الزناد** — سبريدٌ حقيقيٌّ كان سيُقدّم/يؤخّر ضربَ الوقف
   والهدف. غيرُ منمذَج، ويمسّ **الذراعين معًا** بالاتّجاه نفسِه تقريبًا.
7. **انحيازُ البقاء قائمٌ** كما في كلّ باكتيستاتنا على لقطات `PIT`.
8. **العيّنةُ الزوجيّةُ ليست عيّنةً عشوائية** — «عبّأ الطرفان» شرطٌ يعتمد على مسار السعر
   ⇒ **مُصفًّى بالنتيجة جزئيًّا** (وهو عيبٌ في المعيار المنشور نفسِه، نرثه ولا نصلحه هنا).
9. **`_mean_lo95` تقريبٌ طبيعيٌّ لا بوتستراب** — نستعمله **كما هو** لأن المطلوبَ حكمُ
   المعيار المنشور لا حكمٌ أفضلُ منه.
10. **التلوّثُ في §⓪** — رأيتُ قراءتَي (أ) و(ب) بكلّ أرقامهما قبل كتابة هذا العقد.

---

## ⑧ سقفُ النجاح — أعلى ما يمكن أن تُنتجه هذي التجربة

**رقمٌ واحدٌ وشرطٌ مستوفًى، لا أكثر:**
- **الفرعُ 1:** «‏`PR-SWEEP` قِيس أمام تكلفةٍ مقيسةٍ على مقامه الصحيح فصمد حكمُه» ⇒
  **يُغلَق البندُ 2** ويُحدَّث بانرُ التقييد.
- **الفرعُ 2:** **نتيجةٌ تُنشَر وتُرفَع للمالك** — ولا شيءَ غيرها.

⛔ **وما لا تفعله تحت أيّ فرع:**
1. **لا تتبنّى `T1b` ولا `T1c` ولا أيَّ نموذجِ دخول** — الملفُّ يبقى مُغلَقًا بنصّ `§0-ل`.
2. **لا تمسّ جذرًا ولا عتبةً ولا `CONFIG`** · ولا `LOGIC_VERSION` · ولا `force_renew`.
3. **لا تُعدّل حكمًا منشورًا**: `fcost_result.md` و`btcost_result.md` **لا يُمَسّان ولا
   يُعاد تشغيلُهما** — يُكتب `filloff_result.md` **بجانبهما لا فوقهما**.
4. **لا تُغيّر معيارَ `backtest_sweep_compare`** — يُنادى ولا يُعدَّل.
5. **ولا تُرفَع العيّنةُ ولا تُبدَّل نقطةُ التكلفة بحثًا عن نتيجة** — النقطتان مثبَّتتان
   في هذا العقد، والعقدُ **مدموجٌ لا يُعدَّل**.

---

## ⑨ الإغلاقُ والفتح

**على الفرع 1** يُغلَق المحورُ **إغلاقًا مُنفَّذًا لا مكتوبًا**: تخرج الأداةُ بـ**‏8**
**قبل** أن تقرأ مدخلًا أو تفتح أرتيفكتًا أو تُطلق عملية — فلا يُعاد التشغيلُ بحثًا عن
جوابٍ آخر. **ورمزُ ‏8 متمايزٌ عمدًا** عن رموز القياس (‏2 مدخل · 3 فحصٌ ذاتيٌّ/جذور ·
4 أرتيفكت · 5 `V-O1` · 6 `V-O2` · 7 `V-O4` · **9 «لا حكم»**) فلا يُقرأ الإغلاقُ خللًا.

**وشرطُ الفتح ثلاثةٌ معًا:**
1. **محورٌ جديدٌ لم يُقَس** — وإعادةُ نقطةِ تكلفةٍ أو سنةٍ أو عتبةٍ **ليست محورًا** ·
   **أو** مصدرُ تكلفةٍ أدقُّ من `NBBO` (عمقُ دفترٍ حقيقيّ) · **أو** نمذجةُ «اللمسُ ليس
   تعبئة» مسجَّلةً مسبقًا.
2. **تسجيلٌ مسبقٌ جديد** — هذا العقدُ **مدموجٌ لا يُعدَّل**.
3. **إذنُ المالك.**

والمفتاحُ `FILLOFF_REOPEN` **إقرارٌ لا التفاف**، ويُحرَس **من نقطتَي الدخول معًا**
(`main` **والطفل إن وُجد**) — سابقةُ `RKA16`/`RSK9ب` التي كشفت التفافًا حقيقيًّا.

**وعلى الفرع 2 لا إغلاق** — البندُ 2 يبقى مفتوحًا لـ`PR-SWEEP`.

---

## ⑩ الأقفالُ المطلوبة قبل الدفع

`FLK0` العقدُ يُسمّي `FO1` و`FO2` و`FO3` والفروعَ الثلاثةَ بنيويًّا (ثلاثةُ أسطرٍ مرقّمة،
كلُّ فرعٍ في سطره) · `FLK1` حرّاسُ `V-O1`-`V-O9` مذكورةٌ كلُّها · `FLK2` **الآليّةُ
مُثبَتةٌ من الكود الحيّ لا من نصّ العقد**: `backtest_sweep_compare` موجودةٌ وتُنادى
بالاسم، و`fcost_arms.ret_at`/`k_of` مستورَدتان بالاسم، و`ret_at(r, o, 0, 0) == r`
**بت-بت** · `FLK3` العقدُ يُعلن التلوّثَ في §⓪ بأرقامه.

**وكلُّ قفلٍ يلزمه طفرةٌ تُسقطه** — وإلّا فهو زينة.
