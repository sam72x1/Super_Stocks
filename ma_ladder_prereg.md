# 📏🪜 `T-MA-LADDER` — تسجيلٌ مسبق: **«احسب بالأيام من وين هابط · 20 أو 30 أو 50»**

> **أمرُ المالك (2026-09-19): «نفذ جميع النواقص معك الصلاحية كاملة».**
> **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · ولا يُشحَن شيءٌ
> مهما كانت النتيجة.** مدفوعٌ ومدموجٌ **قبل أيّ سطرِ أداةٍ وقبل أيّ رقم**.

---

## §⓪ الدعوى ومصدرُها — حرفيًّا لا مُستنتَجًا

**الكاتالوج `L1173`** (‏`EDU_20260827_school_two_models_ma_ladder.png` — على القرص
ومقروءةٌ بالعين):
> «**متوسطات الحركه تبين لك توجه السهم · احسب بالايام من وين هابط · 20 يوم او 30
> او 50**»

ومعها **`L1184`**: «**صعد قبل المتوسطات = رزق لم يكتب**».

الوسم **`faisal_verbatim`** للنصّ · **والقراءةُ التشغيليّةُ `faisal_inferred`**
ومكتوبةٌ هنا صراحةً كي لا تُقرأ لاحقًا وكأنها نصُّ فيصل:
1. **فترةُ المتوسط تُختار بعدد جلسات الهبوط** — لا برقمٍ ثابت.
2. **«النضج» = بلوغُ السعر ذلك المتوسط** بعد الهبوط.
3. **والصعودُ قبله لا يُعتدّ به** (`L1184`).

**النظام:** «سهمٌ هابطٌ من أعلى» = **مجتمعُ الارتكاز، نظامُنا** ⇒ **لا بوّابةَ
سياقٍ لازمة** (قاعدةُ `faisal-gates §②` مستوفاةٌ بالنظام لا بالاستثناء).

### والمحورُ جديدٌ بالبحث لا بالتسمية
`grep -c "T-MA-LADDER"` في الأرشيفين = **‏1 · 1**، وكلاهما **وصفُه مقترَحًا** في
حزمة 2026-09-18 — **صفرُ حكمٍ منشور**. و`split_ma_maturity` القائمةُ تطبّق
«حقّق متوسط `N` من **تاريخ الحدث**» **لوصفة المقسَّم وحدَها** (‏`SPLIT_MA_PERIODS`
‏20/30/50 · سندُها `LABT`/`JEM`) ⇒ **لم تُقَس قطُّ على مجتمع الارتكاز**، ولا
على **فترةٍ مُشتقّةٍ من طول الهبوط**.

### 🔒 وهو **خارجَ** المحاور الخمسة المُغلَقة — ويُثبَت واحدًا واحدًا
| المُغلَق | موضوعُه | لماذا هذا خارجَه |
|---|---|---|
| `T-RSI40` | `RSI` **بوّابةَ رفض** | لا `RSI` هنا إطلاقًا |
| `T-RSI-RANK` | `RSI` **مفتاحَ أولويّة** | المقيسُ **نضجُ متوسّطٍ مُرشِّحًا**، ولا يُستعمل `RSI` في أيّ ذراع |
| `T-TRAIL` | **إدارةُ الخروج** بعد الدخول | هذا **قبل الدخول** (اختيارُ الإشارة)، والخروجُ يبقى خروجَ الإنتاج بت-بت |
| `T-PMGATE` | **بوّابةُ توقيتٍ** في البريماركت | لا جلسةَ ولا ساعةَ هنا |
| `T-SUPDEF` | **تعريفُ الدعم** | `pivot_stability` **لا تُمَسّ** ولا يُقرأ تعريفُ دعمٍ بديل |

⚠️ **و`rsi_rank_arms.py` مُغلَقٌ بخروج ‏8** — فإن استُوردت منه دالّةٌ نقيّةٌ
**بالاسم** فـ**لا يُنادى `main` ولا `run_child` ولا يُضبَط `RSIRANK_REOPEN`
إطلاقًا**، ويُقفَل ذلك **سلوكيًّا** (عدّادُ نداءٍ على الثلاثة = صفر).

---

## §① المجتمع — عينُ مجتمع الباكتيست على اللقطات الثلاث

صفوفُ إشارات `backtest_symbol` على **اللقطات المجمَّدة الثلاث** (‏PIT) نفسِها
التي تستعملها `T-TRAIL`/`T-HEADCUT` — **ومعرّفاتُها تُقرأ من `trail.yml` لا
تُكتَب بيدٍ**، وأيُّ اختلافٍ ⇒ **خروج 5 قبل أيّ رقم** (`V-M1`).

---

## §② التعريفُ المُقاس — واحدٌ ومكتوبٌ بحرفه

لكلّ صفِّ إشارةٍ بتاريخِه، ومن **الشموع السابقة للإشارة حصرًا** (‏صفرُ تسريب):

1. `pd = peak_and_decline(high[:i])` بالاسم ⇒ `bars_since_peak`.
2. **`n = clamp(bars_since_peak, 20, 50)`** ثمّ يُقرَّب إلى **أقرب** عضوٍ من
   `SPLIT_MA_PERIODS` = ‏(20, 30, 50) — **وهي أرقامُ فيصل الثلاثة بحرفها**،
   والتقريبُ `engineering` مُعلَن.
3. **`matured`** = بلغ **الإغلاقُ** قيمةَ `ema(close, n)` (بالاسم من الإنتاج)
   **في إحدى آخر `W = 5` جلساتٍ قبل الإشارة** — **و`W = 5` `engineering`**
   وتُطبَع حساسيّتُه `{3, 5, 10}` **وصفيًّا خارجَ عدّ المعايير**.
4. **`clamp` و`W` لا يُعايَران بعد رؤية أيّ رقم** (‏`§⑦`).

🔴 **واكتشافٌ عن `clamp` يُقال قبل أيّ رقم — كشفته طفرةٌ نجت على قفلي:**
حدّا القصّ **هما طرفا `PERIODS`** (‏20 و50) ⇒ التقريبُ إلى **الأقرب** يعطي
الجوابَ نفسَه **بقصٍّ وبلا قصّ** — مُثبَتٌ بالحساب **لكلّ `b` في [0, 1000]:
صفرُ قيمةٍ تختلف**. ⇒ **`clamp` خاملٌ بنيويًّا، لا يغيّر رقمًا واحدًا.**
⚖️ **ولا يُحذَف** لأن ‏20 و50 **رقما فيصل المنصوصان** في `L1173` وبقاؤهما
يجعل التعريفَ يقرأ كنصّه، **ويصير عاملًا لو تبدّلت `PERIODS` يومًا** —
لكنّ **`MLA2` يقفل التكافؤَ** فلا يبقى تعبيرٌ ميّتٌ يوهم بأثرٍ ليس له.

🔴 **وحدُّ صدقٍ في قلب التعريف يُقال الآن:** `peak_and_decline` تقرأ **القمّة**
لا **«بدءَ الهبوط»** الذي يقصده فيصل (قد يسبق القمّةَ أو يتأخّر عنها جلسات) ⇒
**نقلُ تعريفٍ مُعلَن**، ويُطبَع **وصفيًّا** تعريفٌ بديل `alt` (أوّلُ إغلاقٍ تحت
`ema(close, 20)` بعد القمّة) **ولا يحكم**.

---

## §③ الأذرع — بميزانيّةٍ ثابتةٍ واحدة

| الذراع | التعريف |
|---|---|
| `L0` | **كلُّ الإشارات** = الإنتاجُ نفسُه (مرجع) |
| **`L1` 🥇** | **الناضجةُ وحدَها** (`matured`) |
| `L2` | **مُكمِّلُها** (غيرُ الناضجة) — يُطبَع ولا يحكم |
| **`C-MOM`** 🥇 | ضبطٌ **حاكم**: إشاراتٌ `ret5 > 0` **بحجم `L1` نفسِه** |
| **`C-RAND`** 🥇 | ضبطٌ **حاكم**: عيّنةٌ عشوائيّةٌ **حتميّةٌ** بحجم `L1` نفسِه |

**والميزانيّةُ واحدةٌ للجميع** (`WATCHLIST_SIZE` عبر `replay10.replay` بمُرتِّبٍ
**محقون**) ⇒ **`rank_key` لا تُمَسّ بحرف**، والكونُ والنافذةُ والسقوفُ مثبَّتةٌ
وتُطبَع (قاعدةُ `faisal-gates §⑤`).

⚠️ **والضبطان `C-MOM` و`C-RAND` حاكمان لا وصفيّان — بدرسِ `T-PMGATE` بحرفه:** «النضج» يعني أن السعرَ
**صعد** إلى متوسّطه ⇒ **زخمٌ صرفٌ قد يفسّره كلَّه**، والسؤالُ النافعُ الوحيد:
**هل يضيف «المتوسّطُ المُشتقُّ من طول الهبوط» على زخمٍ مجرَّد؟**

---

## §④ المقياسُ والمعايير — مسجَّلةٌ قبل أيّ رقم

- المقياسُ الحاكم **`R` للصفقة** (`tranche_arms.r_fixed` بالاسم) · والفاصلُ
  **`exitmgmt_arms.boot_ci`** (عنقوديٌّ بالرمز) · وعدُّ المُسلَّمين `d100` يُطبَع.
- **`ML1`** ‏`R(L1) − R(L0)` **موجبٌ في السنوات الثلاث** والفاصلُ المجمَّع **لا
  يلمس الصفر**.
- **`ML2`** ‏`R(L1) − R(C-MOM)` **موجبٌ** ومجمَّعُه لا يلمس الصفر.
- **`ML3`** ‏`R(L1) − R(C-RAND)` كذلك.
- **الماديّة `ML4`:** المجمَّعُ **‏≥ +0.05R** — وسمُها `engineering` مُعلَن.

## §⑤ فروعُ الحكم — ثلاثةٌ · ولا فرعَ رابع
1. **الفرعُ 1 — «تُوصى»:** `ML1`-`ML4` **كلُّها** ✅ ⇒ **اقتراحٌ على المالك لا
   تنفيذ**، **ولا يُشحَن شيءٌ بهذا العقد**.
2. **الفرعُ 2 — «لا تُوصى»:** سقوطُ **أيٍّ** منها.
3. **الفرعُ 3 — «لا حكم»:** **‏`L1` دون ‏100 صفٍّ في أيّ سنة** · أو سقوطُ حارس ·
   أو تعذّرُ لقطة — **تُعلَن بعددها ويُطبَع معدّلُ النضج**، ولا تُفسَّر.

---

## §⑥ حرّاسٌ تُطبَع في كلّ تقرير — والسقوطُ يوقف

- **`V-M1`** اللقطاتُ **عينُ لقطات `trail.yml`** (تُقرأ منه) ⇒ وإلّا **خروج 5**.
- **`V-M2`** `L0` يُعيد رقمَ الإنتاج المنشورَ **بت-بت** — وإلّا **خروج 6**.
- **`V-M3`** حجمُ `C-MOM` و`C-RAND` = حجمُ `L1` **بالضبط** (‏وإلّا فالمقارنةُ
  ليست تحت الميزانيّة نفسِها).
- **`V-M4`** معدّلُ النضج يُطبَع **قبل** أيّ `Δ`، ومعه **إعلانُ الانحلال** إن كان
  ‏0% أو ‏100%.
- **`V-M5`** الأداةُ **لا ترسل ولا تكتب حالةً** (‏`selfcheck_readonly` المُشدَّد)
  · **وبلا سرِّ تلغرام في الـworkflow**.
- **`V-M6`** الجذورُ الاثنا عشر **بصمتُها مطابقة** · **و`rsi_rank_arms.main`/
  `run_child` صفرُ نداء** · **و`RSIRANK_REOPEN` غيرُ مضبوط**.

---

## §⑦ ما لا يُمَسّ بهذي التجربة

- ⛔ **الجذورُ الاثنا عشر** · **و`rank_key` لا تُمَسّ** · **و`pivot_stability` لا
  تُمَسّ** · و`SPLIT_MA_PERIODS` و`MA_GATE_MAX_ABOVE_PCT` **لا تتحرّكان**.
- ⛔ **لا `LOGIC_VERSION`** · **ولا شحنَ حقلٍ أو مفتاحٍ أو سطرِ عرض** مهما كانت
  النتيجة — **والفرعُ 1 اقتراحٌ لا تنفيذ**.
- ⛔ **ولا يُعايَر `clamp` ولا `W` ولا التقريبُ بعد رؤية النتيجة** — الحاكمةُ
  مكتوبةٌ هنا، والحساسيّةُ **وصفيّةٌ تُطبَع كاملةً ولا يُنتقى منها الأمرح**.

---

## §⑧ حدودُ صدقٍ تُقال قبل النتيجة

1. **`peak_and_decline` تقرأ القمّةَ لا بدءَ الهبوط** — نقلُ تعريفٍ مُعلَن
   (‏`§②`)، والبديلُ `alt` يُطبَع وصفيًّا.
2. **الباكتيست ينفّذ بلا انزلاق** — وأثرُ التكلفة يُقرأ من `T-BTCOST` وصفيًّا.
3. **سجلُّ محورِ الاختيار سالبٌ بامتياز — والأسماءُ مقروءةٌ من الأرشيف لا
   مذكورةٌ من الذاكرة:** **ستٌّ** حكمُها المنشورُ «فشلت» أو «لا ذراعَ تُتبنّى»
   (`T-RANKER` ‏1 من 5 · `T-RANKER2` · `T-RANKER3` · `T-PRERANK` ‏3 أذرعٍ تسقط ·
   `T-PRERANK-2` لا خليّةَ من عشرين · `T-RANK-DENSE` ‏2 من 4) · و**`T-RANKER-TIE`
   لم يُتبنَّ منها كاسرُ تعادلٍ** وصار حصادًا أماميًّا (`T-TIE-FWD`) بقرار المالك ·
   **و`T-RSI-RANK` مُغلَقةٌ بخروج ‏8** ⇒ **الاحتمالُ الأسبقُ الفشل**، **والحافّةُ
   المُثبَتةُ مرارًا هي التوقيتُ لا الاختيار.**
4. **`d100` صغيرٌ بالبناء** (‏8-16 للسنة في `T-RSI-RANK`) ⇒ فروقُ المُسلَّمين
   **أعدادٌ صحيحةٌ صغيرة** و«لم يُثبَت» ليست «أُثبت عدمُه».

---

## §⑨ تنبّؤاتي — تُنشَر مكذَّبةً أو مؤكَّدة

| # | التنبّؤ | ثقتي |
|---|---|---|
| `MP1` | `ML1` يعبر (الناضجُ أفضلُ من كلّ الإشارات) | متوسّطة |
| `MP2` | **`ML2` يسقط** — «الناضج» و«ذو الزخم» مجتمعٌ واحدٌ تقريبًا ⇒ **الفرعُ 2** | **عالية** |
| `MP3` | معدّلُ النضج بين ‏25% و75% (لا انحلال) | متوسّطة |
| `MP4` | `n = 20` هي الأغلب (الهبوطُ عند الإشارة قصيرٌ غالبًا) | منخفضة |
| `MP5` | `ML4` الماديّة تسقط ولو عبر `ML1` | عالية |
