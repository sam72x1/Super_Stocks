# 🚦📈 `T-C-TRIGGER` — تسجيلٌ مسبق: زنادُ التسليم الموازي · وعزلُ شرط ‏+20% وقفزةِ الحجم

> **مدفوعٌ قبل أيّ سطرِ كودِ أداةٍ وقبل أيّ رقمٍ من 2023/2024.** لا يُحرَّك حدٌّ ولا مقياسٌ ولا
> سنةٌ بعد ظهور الأرقام. أمرُ المالك **«جميع ما سبق»** (‏2026-09-06) وفيها «سجّل T-C».

---

## ⓪ لماذا — ومن أين جاءت · وحدُّ الدائريّة قبل كلّ شيء

`T-PM-CURVE` أخرجت ملاحظةً وصفيّةً ثقيلة على 2025: **`T-C` تفير أقلَّ من الإنتاج وتلتقط أكثر**.

| المقياس (2025 · منشور) | `R1` (الإنتاج) | `T-C` |
|---|---|---|
| وسيطُ الرسائل/يوم | **‏22.5** | **‏18.0** (‏×0.80) |
| إجماليُّ الرسائل | 6,084 | 4,826 |
| الإثمار (‏+30% خلال 15د) | 12.5% | **‏19.4%** |
| الالتقاط ‏+30 / +50 / +100 | 18.1 / 38.4 / 54.8 | **‏22.3 / 44.8 / 64.0** |
| «قبل ‏+30%» على منفجري ‏+100% | **‏7.5%** | 5.8% |

🔴🔴 **وحدُّ الدائريّة يسبق الجدول:** `T-C` **خرجت بعد رؤية أرقام 2025**، ووُسمت في ملفّ
النتيجة نفسِه «ملاحظةٌ وصفيّةٌ ثقيلة … ⚠️ **ولا تُقرأ تفوّقًا**». ⇒ **إعادةُ قياسها على 2025
تثبيتٌ لا اكتشاف** (سابقةُ `hold_sessions`: اختير بعد رؤية `A3` وعلى السنوات نفسِها **وسقط
رغم أن العيّنة تحابيه**).

⇒ 🔒 **الحكمُ على 2023 و2024 حصرًا** (‏250 و252 يومَ تداول · خارج العيّنة تمامًا) ·
**و2025 شاهدُ ضبطٍ ومعايرةٍ فقط** · **و2026 الجزئيّة (‏168 يومًا) وصفيّةٌ خارج الحكم**.
⚠️ **وسنتا حكمٍ دون حدّ الثلاث سنوات المدوَّن عندنا** — **يُعلَن الآن ولا يُبرَّر لاحقًا**،
والسببُ أن 2025 محروقةٌ بالدائريّة و2026 ناقصة.

---

## ① الفارقُ بين `T-C` و`R1` **سبعةُ محاورَ لا محورٌ واحد** — وهذا أهمُّ سطرٍ في العقد

مقروءٌ من الكود لا مُفترَض (‏`pm_curve_scan.py:55-67` مقابل `Super_stock.py:13358-13372`
و`13390-13400`):

| # | المحور | `T-C` | `R1` (الإنتاج) |
|---|---|---|---|
| 1 | مقامُ الدولار | **تراكمٌ من فتح البريماركت** | نافذةُ **‏3 دقائق** (`LIQ_CUM_MINUTES`) |
| 2 | عتبةُ الدولار | **$100,000** (`IGNITION_USD_OPERATOR`) | **$30,000** (`LIQ_MIN_USD`) |
| 3 | مرجعُ الارتفاع | **إغلاقُ الأمس** | **فتحُ الشمعة نفسِها** |
| 4 | مقدارُ الارتفاع | **‏+20%** | **‏+5%** (`LIQ_MIN_MOVE_PCT`) |
| 5 | قفزةُ الحجم ‏3× | **غائبة** | **لازمة** (`IGNITION_VOL_MULT`) |
| 6 | موضعُ الإغلاق في النصف الأعلى | **غائب** | **لازم** (`_inflow`) |
| 7 | الشمعةُ الأخيرة | **يفير عليها** | **يستحيل** (‏`closed = rows[:-1]`) |

🔴 **⇒ يُمنَع منعًا باتًّا نسبةُ فرقِ `T-C` عن `R1` إلى شرط ‏+20% وحدَه** — وهو بالضبط ما
يُغري به جدولُ 2025. **والعزلُ يقع بأذرعٍ مخصَّصة (‏§③) لا بالمقارنة المباشرة.**

🔴 **والمحور 7 عدمُ تناظرٍ بنيويّ يمنح `T-C` أسبقيّةً مصطنعة:** `liq_stage_events` تُسقط آخرَ
صفٍّ دائمًا، **و`replay_anchor` تبدأ من `i0+1`** فحتى شمعةُ دخول المرشِّح لا تصلح مِرساة ⇒
**عدّادٌ إلزاميّ يطبع كم صفًّا فار فيه الزنادُ على الشمعة الأخيرة** (‏`V-T7`) — **يُعلَن ولا
يُصحَّح صامتًا.**

---

## ② المعماريّة — ملفٌّ مستقلٌّ لأن قفلًا قائمًا يمنع غيرَه

🔒 **`PC6` في `test_bot.py:41878-41886` يشترط `_PC.ARMS == ("R1","T-A","T-B","T-C","T-D")`
مساواةً تامّة** ⇒ **إضافةُ ذراعٍ داخل `pm_curve_scan.py` تكسر قفلًا قائمًا** ⇒

- **`tc_arms.py` ملفٌّ مستقلّ** يستورد `pm_curve_scan` (‏`PC`) و`pm_radar_scan` (‏`PMR`)
  **بالاسم** ويستعمل دوالَّهما — **صفرُ منطقٍ منسوخ ومقياسٌ واحدٌ لا اثنان**.
- **`pm_curve_scan.py` يبقى بت-بت** (‏`PC6` أخضر · لا ذراعَ تُضاف فيه).
- **`pm_radar_scan.replay_anchor` يكتسب وسيطًا اختياريًّا `vol_mult=None` وحدَه** — والافتراضُ
  **يُعيد السلوكَ المنشور بت-بت** (سابقةُ `CAP15` و`now_ms`)، و`PMR4` يبقى أخضر (‏عددُ نداءات
  `S.liq_stage_events` **واحدٌ كما هو**).
- **`Super_stock.py` لا يُمَسّ بحرف** — `liq_stage_events` تقبل `vol_mult` **أصلًا** منذ
  تعريفها (‏`13251` · `13309`) فلا سطرَ إنتاجيًّا يتغيّر.

---

## ③ الأذرعُ — سبعٌ ولا ثامنة (مثبَّتةٌ الآن · إضافةُ ذراعٍ بعد الأرقام ممنوعة)

| الذراع | التعريف | الدور |
|---|---|---|
| **`R1`** | `PC.r1_anchor` — الإنتاجُ حرفيًّا | الأساس |
| **`T-C`** | `PC.trigger_ms(…, MOVER_USD, 20.0)` | **الحاكمة** |
| **`T-B`** | `PC.trigger_ms(…, MOVER_USD, 10.0)` | عزلُ **مستوى** شرط الارتفاع |
| **`T-E`** | `R1` بـ**`vol_mult=0.0`** | عزلُ **قفزة الحجم ‏3×** وحدَها |
| **`R1∪T-C`** | الأبكرُ من مِرساة `R1` وزناد `T-C` | **الطبقةُ الموازية** |
| **`C-0`** ⛔ | `pct = 0` عند `MOVER_USD` (‏= إغلاقٌ فوق الأمس) | ضابطة — **ممنوعةُ الشحن** |
| **`C-NULL`** ⛔ | بلا شرطِ سعرٍ إطلاقًا عند `MOVER_USD` | ضابطة — **ممنوعةُ الشحن** |

🔒 **`C-0` و`C-NULL` ممنوعتا الشحن بقفلٍ في السويّة** (سوابقُ `K-ORACLE` · `E-PSEUDO` ·
`R2`/`R3`): تُقاسان **وصفًا** ولا تُقترحان طبقةَ تسليمٍ أبدًا، لئلّا يُقرآ «شكلًا خامسًا»
ينقض ما أغلقه `T-PRE-CEIL §⑦` نصًّا.

🔑 **وتمييزٌ يُكتَب قبل الأرقام:** `C-0` **ليست إلغاءَ الشرط** بل **أدنى مستوًى له** (‏`lvl =
prev_close`)؛ والإلغاءُ التامّ هو `C-NULL` وحدَه. ⇒ **`T-B` مقابل `T-C` يعزل المستوى ·
و`T-C` مقابل `C-NULL` يعزل الوجود.**

📒 **وأرقامُ المستويات مُعادةٌ لا مخترَعة (تصحيحُ إسناد):** **‏+20% = `SPLIT_ROSE_MAX_PCT`**
(‏`Super_stock.py:640` · وسمُ الدفتر **`faisal_verbatim`** من `IMG_0153`) **بدلالةٍ مختلفةٍ
مُعلَنة** · **‏+10% = `LIQ_PULSE_PCT`** (‏`12875` · قرارُ مالك 2026-08-19).
🔴 **ولا مستوًى ثالثٌ يُضاف** · **و‏+30% ممنوعٌ بنيويًّا** لأنه يساوي سلّمَ الوسم فيصير
**دائريّةً صريحة**.

---

## ④ المقاييسُ — والمقترنُ إلزاميّ

الأذرعُ **متداخلةٌ رتيبًا بالبناء** (‏`T-C ⇒ T-B ⇒ T-A ⇒ T-D` — `chain_ok`) ⇒
🔴 **كلُّ مقارنةٍ حاكمة تُحسَب فرقًا مقترنًا داخل الصفّ الواحد** (نفسُ الرمز-اليوم)، **لا بين
مجموعتين**. ومعها **العدّادُ الحدّيّ**: كم صفًّا يفيره أحدُهما ولا يفيره الآخر، وماذا حلّ به.

| المقياس | التعريف | الدور |
|---|---|---|
| `msgs_median` | وسيطُ عددِ الإطلاقات في اليوم | كلفة |
| `cap_pct` | نسبةُ منفجري السلّم الذين فار الزنادُ عليهم **قبل** بلوغهم السلّم | التقاط |
| `fruit30_pct` | نسبةُ الإطلاقات التي بلغ سعرُها ‏+30% خلال 15 دقيقة | جودة |
| `delay_median` | ⚠️ **وصفيٌّ فقط** | — |

🔴 **و`delay_median` ممنوعٌ حاكمًا:** يُحسب على **الملتقَطين وحدَهم** ⇒ يُقاس على مجموعتين
مختلفتين — **وهو عينُ عيب `G2` الذي وقع فعلًا في `T-GATE`** وسُجِّل في `premarket_radar_result
§⑦`.

---

## ⑤ الحكمان — منفصلان ولا يُدمَجان

**(أ) التكرارُ خارج العيّنة — الثلاثةُ تلزم في 2023 ‏و‏ 2024 معًا:**

```
TC1  الكلفة   : msgs_median(T-C) ÷ msgs_median(R1) ≤ 1.00
TC2  الالتقاط : cap_pct(T-C) ≥ cap_pct(R1) في السلالم الثلاثة (+30 · +50 · +100)
TC3  الإثمار  : fruit30_pct(T-C) ≥ fruit30_pct(R1)
```

**(ب) الطبقةُ الموازية — الاثنان يلزمان في 2023 ‏و‏ 2024 معًا:**

```
TC5  كلفةُ الاتّحاد : msgs_median(R1∪T-C) ÷ msgs_median(R1) ≤ 1.50
TC6  مكسبُ الاتّحاد : cap_pct(R1∪T-C) − cap_pct(R1) عند +100% ≥ +5.0 نقاط
```

**الأرضية (مُعادةٌ من `pm_curve_scan` لا مخترَعة):**

```
TC4  ≥ MIN_MOVERS (=100) متحرّكًا لكلّ سلّمٍ لكلّ سنة · وتغطية ≥ COVERAGE_MIN (=95.0)
     وإلّا «لا حكم» — تُطبَع الأعدادُ ولا يُطبَع فرق
```

⛔ **ولا يُنتقى حكمٌ بعد الأرقام:** سقوطُ (أ) ونجاحُ (ب) — أو العكس — **يُنشَران كما هما**،
ولا يُقال «نصفُ نجاح». **و«لا حكم» ليست فشلًا ولا نجاحًا.**

---

## ⑥ عزلٌ وصفيٌّ (‏**خارج الحكم** · يُنشَر ولا يُبنى عليه قرار)

1. **`T-B − T-C`** = أثرُ **مستوى** شرط الارتفاع (‏+10 مقابل +20) عند نفس الأرضية.
2. **`T-C − C-NULL`** = أثرُ **وجود** شرط الارتفاع أصلًا · و**`C-0`** أدنى مستوًى له.
3. **`R1 − T-E`** = أثرُ **قفزة الحجم ‏3×** وحدَها.
   🥇 وهي أنظفُ اختبارٍ لهذا المحور عندنا: `T-VOLBASE` بدّلت **مقامَ** القفزة ولم تُطفئها،
   وهنا تُطفَأ **وحدَها** وكلُّ ما عداها ثابتٌ بالبناء.
   ⛔ **ولا يُقترَح إطفاؤها مهما كانت الأرقام** — رقمُ المالك الثالث، والذراعُ **سقفٌ تشخيصيّ**
   من صنف `K-ORACLE` لا طبقةُ تسليم.
4. **عدّادُ الشمعة الأخيرة** (المحور 7 في §①).
5. `delay_median` لكلّ ذراع.

---

## ⑦ بوّاباتُ الصلاحية — **منفَّذةٌ بخروجٍ غيرِ صفريّ** لا موصوفة

> «حارسٌ مكتوبٌ وغيرُ منفَّذٍ في الكود ليس حارسًا — هو نيّة» (‏`envelope_prereg §⑦`) ·
> وسابقتان مقيستان: `premarket_radar_result §⑦` و`base475`/`m4_iso` (‏`V3` غيرُ منفَّذ).

| # | البوّابة | الخروج |
|---|---|---|
| **V-T0** | **شاهدُ الضبط بت-بت على 2025:** `R1` ‏22.5 / 6,084 / 12.5% · `T-C` ‏18.0 / 4,826 / 19.4% · `cap100` ‏54.8 و64.0 | **‏3** «عطبُ أداةٍ لا نتيجة» |
| **V-T1** | مفاتيحُ S3 حاضرة | **‏2** |
| **V-T2** | صفرُ صفوفٍ في أيّ سنةٍ مقيسة | **‏4** |
| **V-T3** | تغطيةُ الأيام ‏≥ `COVERAGE_MIN` والمفقودُ يُسمّى بتواريخه | **‏3** |
| **V-T4** | `chain_ok` سليمة (‏التداخلُ الرتيب) | **‏3** |
| **V-T5** | **`T-E ⊇ R1` بنيويًّا** (إطفاءُ بوّابةٍ لا يُنقص مِرساة) | **‏3** |
| **V-T6** | **`R1∪T-C`** لكلّ صفّ = الأبكرُ من الاثنين بالضبط | **‏3** |
| **V-T7** | عدّادُ الشمعة الأخيرة **مطبوعٌ فعلًا** (بالـAST داخل نداء الطباعة) | **‏3** |
| **V-T8** | **حارسُ الـ`no-op`:** كلُّ ذراعٍ ضابطةٍ (`C-0` · `C-NULL` · `T-E`) **تفترق عدديًّا** عن `T-B`/`T-C`/`R1` — وإلّا فهي غيابُ قياسٍ لا نتيجة | **‏4** |
| **V-T9** | `pm_curve_scan.py` **بت-بت** · و`pm_radar_scan.replay_anchor` بلا الوسيط **يُعيد المنشور بت-بت** · وصفرُ مسٍّ بـ`Super_stock.py` | **‏3** |

---

## ⑧ بطاقةُ التنبّؤ (قابلةٌ للكسر · تُقيَّم حرفيًّا)

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| **TC-P1** | 🔴 **(أ) يسقط:** ملاحظةُ 2025 **لا تتكرّر كاملةً** في السنتين — أرجّح سقوطَ `TC2` في سلّمٍ واحدٍ على الأقلّ | تحقُّقُ `TC1`+`TC2`+`TC3` في السنتين |
| **TC-P2** | **(ب) يسقط بالكلفة لا بالالتقاط:** `TC6` يتحقّق و`TC5` لا (الاتّحادُ يرفع الرسائل فوق ‏1.50×) | العكس أو تحقُّقهما معًا |
| **TC-P3** | **`R1 − T-E` سالبٌ ماديًّا** (‏`T-E` يُطلق أضعافًا) — إطفاءُ ‏3× يفتح فيضًا | فرقٌ ‏≤ 20% في عدد الإطلاقات |
| **TC-P4** | **الشمعةُ الأخيرة تفسّر ‏≤ 5%** من إطلاقات `T-C` ⇒ عدمُ التناظر حقيقيٌّ وصغير | ‏> 5% ⇒ **الأسبقيّةُ المنشورة مصنوعةٌ جزئيًّا** |
| **TC-P5** | **`C-NULL` أقربُ إلى `T-C` من `C-0`** ⇒ الحاكمُ هو **الأرضيةُ الدولاريّة $100k لا شرطُ الارتفاع** | `C-0 ≈ T-C` وكلاهما بعيدٌ عن `C-NULL` |

🔴 **والحاكمُ `TC-P1` يراهن ضدّ التجربة** — فلو نجحت كانت رغم حدسي لا بفضله.

---

## ⑨ حدودُ صدقٍ — تُكتَب الآن وتُنشَر مع كلّ رقم

1. **سبعةُ محاورَ لا محور** (‏§①) ⇒ لا يُنسَب فرقٌ إلى `+20%` وحدَه.
2. **عدمُ تناظر الشمعة الأخيرة** يمنح `T-C` أسبقيّةً مصطنعةً غيرَ مقيسةٍ قبل `V-T7`.
3. **لمسٌ لا تنفيذ:** `cap_pct`/`fruit30_pct` بلوغُ سعرٍ بلا انزلاق ⇒ **سقفٌ متفائلٌ للجميع**.
4. **ولا ربحيّةَ تُقاس إطلاقًا** — لا في `T-PM-CURVE` ولا هنا. (البابُ المفتوح: `kasih_scan.resolve`
   بالاسم على `(ms, close)` الذي يُرجعه الزناد — **بتسجيلٍ مسبقٍ لاحقٍ وأمرٍ صريح، لا في هذي الدفعة**.)
5. **مقامٌ مشروط:** صفوفُ الأداة تُكتَب فقط إن فار سلّمٌ أو ذراع ⇒ **مَن لم يتحرّك ولم يُفِر
   غائبٌ عن الملفّ** ⇒ يُطبَع عددُ الصفوف الكلّيّ وعددُ أيام المسح مع أيّ وسيط.
6. **مرجعٌ بأثرٍ رجعيّ:** حدّا السعر ‏[0.40 · 10.00] من ظرف الكاتالوج **الحاليّ** يُطبَّقان على
   2023/2024 ⇒ **قياسُ اتّساقِ أداةٍ لا `point-in-time`**، ولا يُدَّعى «ماذا كان الإنتاجُ يفعل حينها».
7. **رسائلُ لا تسليم:** `msgs` عددُ إطلاقاتٍ لا كروتٍ وصلت المالك (الفلترُ وسقفُ العرض بعدها).
8. **سنتا حكمٍ** دون حدّ الثلاث سنوات (‏§⓪) · **و`T-E` سقفٌ تشخيصيٌّ لا يُشحَن.**
9. **الرادارُ الحيُّ يحتاج عاملًا من 04:00** — وهو قيدٌ تشغيليٌّ قائمٌ على أيّ طبقةٍ موازية.

---

## ⑩ سقفُ النجاح — مثبَّتٌ قبل أيّ رقم

**أقصى ما يُنتجه هذا العقد: اقتراحُ طبقةِ تسليمٍ موازية للمالك + سطرُ عرضٍ تشخيصيّ.**

⛔ **ولا يُفعَل مهما كانت الأرقام:** لا مسَّ ببوّابة المِرساة ولا بأرقام المالك الثلاثة
(‏‏5% · $30,000 · ‏3×) · لا تبديلَ لـ`LIQ_MIN_USD` ولا `LIQ_MIN_MOVE_PCT` ولا `LIQ_CUM_MINUTES`
ولا `IGNITION_VOL_MULT` ولا `IGNITION_USD_OPERATOR` · لا مسَّ بـ`FLOOR_BY_SLOT` ولا
`PREFILTER_CAP` ولا `PREFILTER_KEY` ولا `TOPK` · **لا `LOGIC_VERSION`** · **ولا تُشحَن `T-E`
ولا `C-0` ولا `C-NULL` بحال** · **ولا تُضاف ذراعٌ ثامنة ولا مستوًى ثالثٌ بعد رؤية الجدول.**

---

## ⑪ ترتيبُ التنفيذ

1. **هذا العقدُ يُدفَع أوّلًا** ·
2. `tc_arms.py` + `tc_arms.yml` (يدويّ · `contents: read` · قراءةٌ فقط) + الوسيطُ الاختياريّ
   في `pm_radar_scan.replay_anchor` ·
3. الأقفال + **جولةُ طفراتٍ** (كلُّ بوّابةٍ في `§⑦` تُسقطها طفرةٌ مسمّاة) ·
4. السويّة (خروج 0 · صفر فشل · سطرُ الملخّص حاضر · `$?` يُقرأ مباشرةً بلا أنبوبة) ⇒ دفع ·
5. **شاهدُ الضبط 2025 أوّلًا** (‏`V-T0`) ⇒ ثم 2024 ⇒ ثم 2023 ⇒ ثم 2026 وصفًا ·
6. النتيجةُ في `tc_trigger_result.md` **بجانب هذا العقد لا فوقه** ·
7. **لا تغييرَ إنتاجيّ بلا «نفّذ» صريح.**
