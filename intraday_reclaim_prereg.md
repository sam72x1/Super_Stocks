# ⛏️🕐 `T-RECLAIM-INTRADAY` — «سجّل الاستعادة داخل اليوم»

> **أمرُ المالك 2026-08-22:** «سجّل الاستعادة داخل اليوم».
> 🔴 **يُدفَع هذا الملفُّ قبل كتابة سطرِ كودٍ واحدٍ من الأداة، وقبل أيّ رقم.**
> ولا يُعدَّل بعد رؤية أيّ نتيجة — والتعديلُ الوحيد المسموح **ملحقٌ مؤرَّخٌ
> يُدفَع قبل التشغيلة التي يخصّها**.

---

## ⓪ 🔑 لماذا الآن — وما الذي يجعلها **غيرَ مكرّرة**

`T-SWEEP-RECLAIM` (‏2026-08-19) قاست الاستعادةَ **عبر الجلسات** فوجدت أن
‏36.9% من ستوباتنا تستعيد خلال خمس جلسات، **وأن إعادةَ الدخول الآليّة سالبةُ
التوقّع** (‏−0.041R عند W5 · −0.059R عند W10) — والميكانيكا: الدخولُ عند
إغلاق الاستعادة **أغلى** والوقفُ على قاع المسح **أعمق** ⇒ مكافأةٌ أصغرُ من
المخاطرة **بنيويًّا**.

🔴 **ونصُّ حكمها لم يشمل هذا السؤال إطلاقًا:** لم تُقَس **الاستعادةُ داخل
اليوم نفسِه** ولا مرّة. وبلاغُ `HUIZ` (‏2026-08-22، `exit_cut_result.md`)
أخرج الحالةَ بالأرقام: خروجٌ بنيويٌّ **05:09 بسعر 1.64** ثم قمّةُ **4.18 عند
08:24** = **‏+155% من سعر خروجنا في اليوم نفسِه**، وليست مفردة (‏27 من 148
مِرساةً في جلستين قمّتُها بعد الخروج = ‏18.2%).

⚖️ **والفرقُ عن سابقتها مكتوبٌ سلفًا لئلّا يُدَّعى جِدَّةً وهو تكرار:**
① الأفقُ **دقائقُ لا جلسات** ② والخروجُ المقيس **الخروجُ البنيويّ**
(‏`resolve`: إغلاقُ دقيقةٍ دون قاع شمعة المِرساة) لا وقفَ الإنتاج 7%
③ والمجتمعُ **مراسي بوّابة السيولة** لا مشطوبي قائمة الترشيح.
🔴 **وإن خرجت النتيجةُ مطابقةً لسابقتها فهي تكرارٌ يُعلَن**، لا اكتشاف.

---

## ① 🧭 السؤال — بصيغةٍ قابلةٍ للتكذيب

**هل قاعدةُ الخروج البنيويّ تُكلّفنا داخلَ اليوم — وهل ثمّة قاعدةٌ بديلةٌ
تُحسّن التوقّعَ بوحدة المخاطرة؟**

⛔ **وليس السؤال:** «هل يستعيد السهمُ؟» — ذاك **وصفيٌّ** وقد قِيس (‏18.2%
قمّتُها بعد الخروج على جلستين). السؤالُ **هل يُربَح من الاستعادة**.

---

## ② 🎯 الأذرعُ — **خمسٌ مثبَّتةٌ ولا سادسة**

كلُّها تبدأ من **المِرساة نفسِها** بسعرِ دخولٍ واحد (`entry` = إغلاقُ شمعة
المِرساة) فلا يختلف الاختيارُ بين ذراعٍ وأخرى — **المتغيّرُ قاعدةُ الخروج
وحدَها**.

| الذراع | التعريف |
|---|---|
| **`X0`** | **الأساس (الإنتاج/المقياس المنشور):** خروجٌ عند أوّل إغلاقِ دقيقةٍ **دون `anchor_low`** · وإلّا إغلاقُ آخر شمعةٍ في اليوم. |
| **`X1`** 🥇 | **الحاكمة — إعادةُ الدخول عند الاستعادة:** `X0` كما هو، ثم **أوّلُ إغلاقِ دقيقةٍ فوق `anchor_low`** بعد الخروج ⇒ دخولٌ ثانٍ عند ذلك الإغلاق · وقفُه **أدنى قاعٍ بين الخروج والاستعادة** (قاعُ المسح) · وخروجُه أوّلُ إغلاقٍ دون ذلك القاع وإلّا إغلاقُ اليوم. **مرّةً واحدةً لكلّ مِرساة — لا تهرام.** |
| `X2` | **وقفٌ واعٍ بالمسح (‏13%):** لا خروجَ إلّا بإغلاقٍ دون `anchor_low × (1 − SPLIT_SWEEP_MAX_PCT/100)` · وإلّا إغلاقُ اليوم. |
| `X3` | نفسُها بـ`SPLIT_SWEEP_MID_PCT` (‏10%). |
| `X4` | **بلا وقفٍ إطلاقًا** — إغلاقُ اليوم دائمًا (الحدُّ الأعلى لما يشتريه الإرخاء **وثمنُه**). |

🔒 **وصفرُ رقمٍ مخترَع:** `SPLIT_SWEEP_MAX_PCT`=13 و`SPLIT_SWEEP_MID_PCT`=10
**قائمتان في `CONFIG`** وسندُهما نصُّ فيصل «سحب السيوله … من 7٪ ل 13٪»
(‏`faisal_verbatim`) · و`anchor_low` تعريفُ `resolve` نفسِه.

⚖️ **والحاكمةُ `X1` مثبَّتةٌ سلفًا** لأنها نصُّ أمر المالك. و`X2`-`X4` **سلّمُ
كلفةٍ يُنشَر** ويُقاس **بالمعايير نفسِها**؛ فإن عبرت إحداها فهي **اقتراحٌ
مستقلٌّ للمالك** — **وحكمُ التجربة (نجحت/فشلت) على `X1` وحدَها.**

---

## ③ 📐 المقياسُ الحاكم — **وحدةُ مخاطرةٍ واحدةٌ لكلّ الأذرع**

**`R₀ = entry − anchor_low`** — وقفُ المقياس المنشور نفسُه.
**وكلُّ ذراعٍ تُقاس بـ`R₀` نفسِها** حتى تكون قابلةً للمقارنة.

🔴 **وهذا فخٌّ حقيقيٌّ أُعلنه:** `T-SWEEP-RECLAIM` قاست إعادةَ الدخول
**بوحدةِ مخاطرتها هي** (‏قاعُ المسح) فبدت الأرقامُ أصغرَ ممّا تعنيه للمحفظة.
هنا **`X1` = محصّلةُ الصفقتين معًا مقسومةً على `R₀`**.

**المقياسُ الحاكم = `E` (متوسّطُ `R` لكلّ مِرساة).**
⚖️ **ومحقَّقٌ لا لمسُ قمّة** — لا `MFE` ولا `mg_after` في الحكم إطلاقًا
(وهو بعينه الخلطُ الذي أخرجه بلاغُ `HUIZ`). و`MFE` يُطبَع **وصفيًّا** فقط.

**والمساندان (يُنشَران ولا يحكمان):** نسبةُ الصفقات الموجبة · ووسيطُ `R`.

---

## ④ ⚖️ معاييرُ النجاح — **أربعةٌ تلزم معًا**

| | الشرط | الحدّ |
|---|---|---|
| ① | **تحسّنُ التوقّع** `E(X1) − E(X0)` | **‏+0.15R فأكثر** — مُعادٌ من `T-STOP` و`T-LIBERATION` بلا تحريك |
| ② | **ثباتُ الإشارة** | موجبٌ في **السنوات الثلاث** بلا انقلاب |
| ③ | **الدلالة** | فاصلُ ثقةٍ 95% للفرق **لا يلمس الصفر** مجمَّعًا |
| ④ | **الأرضية** | **‏≥150 مِرساةً تُطلق `X1`** مجمَّعًا **و‏≥30 في كلّ سنة** |

🔴 **وسقوطُ أيٍّ منها ⇒ فشلت.** ولا يُحرَّك حدٌّ بعد رؤية رقم · ولا تُضاف
ذراعٌ سادسة · ولا يُبدَّل المقياسُ الحاكم.
⏳ **وإن سقطت ④ وحدَها ⇒ «لا حكم»** ويُطبَع العدّادُ عاريًا بلا نسبة.

---

## ⑤ 🔒 بوّاباتُ الصلاحية — تُشغَّل **قبل** أيّ رقمٍ وتُوقِف بخروجٍ غير صفريّ

| | البوّابة | الفشل |
|---|---|---|
| `V0` | **`X0` يُعيد `kasih_scan.resolve` بت-بت** (لحظةُ الخروج وسعرُه لكلّ مِرساة) — تفرّقٌ واحدٌ ⇒ **عطبُ أداةٍ لا نتيجة** | خروج 3 |
| `V1` | **`X1` تُطلق فعلًا** — صفرُ إطلاقٍ = `no-op` | خروج 4 |
| `V2` | **الأذرعُ الخمسُ تتفرّق** على عيّنةٍ مصطنعةٍ واحدةٍ على الأقلّ (لا ذراعَ مكرّرة) | خروج 3 |
| `V3` | **تغطيةُ الأيام ‏≥95%** واليومُ المفقود **يُسمَّى بتاريخه** (درسُ `AH-HD1`) | خروج 3 |
| `V4` | **`resolve` و`first_anchor` byte-identical** مع `origin/main` | خروج 3 |

---

## ⑥ 🎯 تنبّؤاتٌ قابلةٌ للكسر — تُنشَر مكذَّبةً كما تُنشَر متحقّقة

| | التنبّؤ | ما يكذّبه |
|---|---|---|
| `RC-P1` 🥇 | **`X1` تفشل** — التحسّنُ دون ‏+0.15R (وأرجّحه ‏≤+0.05R) للميكانيكا نفسِها التي أسقطت `T-SWEEP-RECLAIM`: دخولٌ أغلى ووقفٌ أعمق | تحسّنٌ ‏+0.15R فأكثر بثباتٍ سنويّ |
| `RC-P2` | **نسبةُ الصفقات الموجبة ترتفع في `X1`** بينما التوقّعُ لا يتحرّك — نمطُ `T-STOP` (‏34⟶54% بتوقّعٍ ثابت) و`T-LIBERATION` (‏87% بتوقّعٍ سالب) | النسبةُ لا ترتفع، أو ترتفع ومعها التوقّع |
| `RC-P3` | **`X4` (بلا وقف) أسوأُ الخمسة توقّعًا** رغم أعلى `MFE` | `X4` ليست الأسوأ |
| `RC-P4` | **تُطلق `X1` في ‏25-45%** من المراسي التي وقع لها خروجٌ بنيويّ (صدًى لـ‏36.9% عبر الجلسات) | خارج النطاق |
| `RC-P5` | **التحسّنُ (إن وُجد) يتركّز في «ضعيف»** لأن القصَّ يعضّه أشدّ (‏22.4% قمّتُها بعد الخروج مقابل 18.0% لقوي) | التركّزُ في «قوي» أو لا فرقَ فئويّ |
| `RC-P6` | **`X2`/`X3` سلّمٌ رتيب** (‏13% أفضلُ من 10% أو العكس بلا تذبذب) | لا رتابة |

🧭 **وحدسي أخطأ في `T-STRONG-2` في أربعةٍ من خمسة** — فالتنبّؤُ هنا **مِسطرةٌ
على نفسي** لا ادّعاءُ معرفة.

---

## ⑦ ⛔ سقفُ النجاح — **ما يجوز فعلُه مهما كانت الأرقام**

✅ **المسموح:** **اقتراحٌ للمالك بالرقم** · وسطرُ عرضٍ في الأدوات التشخيصية.
⛔ **الممنوعُ صراحةً — ولا يُفتَح بحسن نيّة:**
1. 🔴 **`kasih_scan.resolve` يبقى byte-identical إلى الأبد** — كلُّ رقمٍ
   منشورٍ في المستودع محسوبٌ عليه (‏44,200 مِرساة · الأساس 11% · التوليفة
   19.7-22.6% · الأحمر 3.4-4.0%)، وتبديلُه **يُبطل المقارنةَ مع كلِّ ما
   نُشر**. أيُّ قاعدةٍ جديدة **تُنشَر بجانبه لا بدلًا منه** (سابقةُ `CAP15`).
2. **لا مسَّ بأرقام المالك** (‏5%/دقيقة · $30 ألفًا · ‏3× · `LIQ_PULSE_PCT`).
3. **لا مسَّ بتعريف التصنيف** ولا بـ`LIQ_TIER_STRONG_MIN`.
4. **لا مسَّ بـ`STOP_BELOW_LOW_PCT`** (وقفُ الارتكاز 7% **نظامٌ آخر** —
   قاعدةُ «أربعةُ أوقافٍ لا تُخلَط»).
5. **لا بوّابةَ إطلاقٍ ولا كتمٍ ولا فلترَ تسليم.**
6. **ولا `LOGIC_VERSION`** — التجربةُ قياسٌ خارج الإنتاج بالكامل.

---

## ⑧ ⚠️ حدودُ صدقٍ **تُكتب الآن** لا بعد الأرقام

1. **المجتمعُ كونُ `kasih_scan` التاريخيّ** (إغلاقُ الأمس **‏0.40-10.00$**)
   ⇒ **فئةُ `RITR` (‏$0.0763) خارجُه تمامًا** وهي فئةٌ رابحةٌ مُثبَتة.
2. **لمسٌ لا تنفيذ:** الدخولُ والخروجُ عند **إغلاقات دقائق** بلا انزلاقٍ ولا
   حجم ⇒ **سقفٌ متفائلٌ لكلّ الأذرع بالتساوي** فلا يحابي واحدة.
3. **الشموعُ تشمل البريماركت والأفتر** (‏04:00-20:00 نيويورك) — و`HUIZ`
   نفسُه مِرساتُه فجريّة ⇒ جزءٌ من الأرقام **سيولتُه رقيقةٌ فعلًا**.
4. **إغلاقُ اليوم مخرجٌ اصطلاحيّ** لِمَن لم يُضرَب وقفُه — **اختيارٌ مُعلَنٌ
   لا موصًى به**، وهو نفسُه في الأذرع الخمس (لا يحابي).
5. **صفرُ نمذجةٍ لتكلفة الاقتراض/الرسوم** — وهي حقيقيّةٌ في هذي الفئة.
6. **الدائريّةُ منتفيةٌ هنا** (الأذرعُ قواعدُ خروجٍ لا انتقاءَ عيّنة) —
   ⚠️ **لكنّ اختيارَ الأذرع مُطَّلعٌ على بلاغ `HUIZ`** ⇒ ليست خمسًا عشوائية،
   والمعيار ② (ثباتُ ثلاث سنوات) يخفّف ولا يُلغي.
7. **التقسيمُ العكسيّ:** الشموعُ خامّة و`GAP_CAP`=500 يُسقط التشويهَ الفاحش
   **ولا يُسقط ما دونه** — وهو حدٌّ قائمٌ في كلّ أرقامنا لا جديدٌ هنا.

---

## ⑨ 🛠️ التنفيذ

**الأداة:** `reclaim_arms.py` (تستورد `kasih_scan` **بالاسم**: `parse_day`
· `prescreen` · `first_anchor` · `resolve` · `weekdays` · `PRICE_LO/HI` ·
`GAP_CAP` — **صفرُ منطقٍ مكرّر**) · **الـworkflow** `reclaim.yml` يدويٌّ
بلا كرون · قراءةٌ فقط · **والإنتاجُ لا يستوردها** (قفلٌ بالـAST).
**التشغيل:** ثلاثُ تشغيلاتٍ (‏2023 · 2024 · 2025) — **والحكمُ بعد الثلاث
مجتمعةً**، ولا يُقرأ رقمُ سنةٍ منفردًا حكمًا.
**النتيجة:** `intraday_reclaim_result.md` ومعها **بطاقةُ التنبّؤات الستّة
بحالة كلٍّ** (متحقّق/مكذَّب/غير مُقيَّم).
