# 📦 T-CAP — سعةُ القائمة: ماذا يتغيّر لو صارت الخانات 15 بدل 10؟ (يُدفَع **قبل** أيّ رقم)

> **أمرُ المالك (2026-08-07 ليلًا):** «نفذ ١» — والبند ① من خيارات الختام:
> «سعة القائمة نفسها (10 خانات)… ما قسنا قط: لو صارت 15 وش يتغير؟».
> **وهذا الملفُّ مدفوعٌ قبل تشغيل أيّ ذراع.** ورقمُ السعة **قرارُ مالكٍ حصريّ**
> (هندسيّ — عددُ مراكزه المتزامنة وقدرتُه على متابعتها)؛ القياسُ يُنير قرارَه فقط.

## ① الأداة والأذرع

- نفسُ عدّة T-SLOT حرفيًّا (`replay10` · لقطات PIT الثلاث · `FAISAL_ONLY=1` ·
  المُرتِّب `rank_live`) **مع سياسة NF8 مفعَّلةً في الذراعين** — لأنها صارت
  الإنتاجَ الفعليّ الليلة، والسؤال «سعة النظام القائم» لا نظامٍ قديم. عتبةُ NF8
  تُقرأ من `CONFIG["SLOT_UNFILLED_FREE_SESSIONS"]` (مصدرٌ واحد).
- **ذراعان لا ثالثة:** `C10` (السعة الحيّة) · `C15` (الرقمُ الوحيد ذو السابقة —
  سعةُ أداة المحفظة المنشورة `BT_PORT_SIZE` وكلُّ أرقام «top-15» المنشورة).
- 🧪 **بوّابةُ صلاحيةٍ حاكمة:** `C10` يجب أن يُعيد أرقام T-SLOT لذراع `S-NF8`
  **حرفيًّا** (خام/معدَّل: ‏2024 = 35/26 · 2025 = 30/19 · 2026 = 15/11) وإلّا
  فالأداة معطوبة ولا تُقرأ التجربة.

## ② المقاييس (الأساسيّ أولًا) وقاعدة القراءة

1. **المنفجرون المُسلَّمون** `d50` و**المعدَّل الصادق** `d50_adj` (نفس تعريف
   T-SLOT — خصمُ منفجري التعبئة المتأخّرة) + بالغو +100% ومعدَّلُهم.
2. حارسُ العائد: `R`/مأخوذة لـ`C15` لا يقلّ عن `C10` بأكثر من 0.05 مجمَّعًا.
3. عددُ المأخوذات ومرفوضو السعة (كم إشارة تُنقذها الخانات الخمس؟).

**قاعدة القراءة:** أُوصي بـ15 فقط إن كان `d50_adj(C15)` أعلى من `C10` **في كلّ
سنة** وبالغو +100% المعدَّلون لا ينقصون مجمَّعًا وحارسُ العائد مستوفًى — وإلّا
أُبلغ الأرقام بلا توصية. **وبكلتا الحالتين لا يُغيَّر `WATCHLIST_SIZE` إلا بأمر
المالك الصريح** (يمسّ عدد مراكزه الحقيقية لا خوارزميةً فقط).

## ③ تنبّؤات (تُنشَر ولو كُذِّبت)

- **P1:** `C15` يُسلّم منفجرين أكثر خامًا في الثلاث (سعةٌ أكبر لا تحجب).
- **P2:** الزيادةُ المعدَّلة أصغر بكثير من نسبة الزيادة في الخانات (‏50% خانات
  زيادة لا تعطي 50% منفجرين — تناقصُ الغلّة، لأن NF8 حرّر أصلًا جزءًا من الضغط).
- **P3:** حارسُ العائد يصمد (الهامشيّون ليسوا أسوأ جوهريًّا — الترتيب غير مميِّز).

## ④ حدودُ صدق

نفسُ حدود T-SLOT (تفاؤل «عدم إعادة الشحن» مُقيَّد بالتعديل · مِشيةٌ متفرّقة ·
2026 جزئية · 2025/2026 داخل-العيّنة مشتركة) · و**15 مركزًا حيًّا عبءُ متابعةٍ
ومخاطرةٍ أعلى** — بُعدٌ لا يقيسه الباكتيست أصلًا ويُترك لحكم المالك.
