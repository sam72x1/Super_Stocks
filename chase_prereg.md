# 🚧 T-CHASE — تسجيل مسبق: «هل إرجاعُ سقف منع الملاحقة يُكلّف شيئًا مقيسًا؟»

> **مدفوعٌ قبل أيّ رقم.** أمر المالك 2026-08-08: قال «**1**» = «قسها» بعد مسكته
> «لحظة هذي المفروض ما تعتبر من ضمن الأسهم المنفجرة».

## ① الخلفية — الرقمُ مقيسٌ والمشكلةُ في **جنس** الإحصاء لا في صحّته

`gain5` = تغيُّرُ السعر في 5 جلسات. يغذّي بوّابةَ منع الملاحقة `RECENT_RISE_BLOCK_PCT`
(‏D11): مرشّحٌ صعد أكثر من الحدّ في آخر 5 جلسات ⇒ **يُرفَض** (`M4_انفجر_فعلاً`).

المقيسُ من كاتالوج فيصل (تشغيلة `31265004124` · 25 رمزًا × 20 جلسة = **500 نافذة**):

| الإحصاء | القيمة |
|---|---|
| **وسيط** `gain5` | **‏−8.26%** |
| `P90` | ‏+8.16% |
| **`P100` = أقصى نافذة (المُطبَّق حاليًّا)** | **‏+214.73%** |

⇒ أسهمُ فيصل في الخمس جلسات قبل انفجارها **هابطة**، **ونافذةٌ واحدة من 500** ضبطت
البوّابة. وبـ214.73 البوّابةُ **معطَّلةٌ عمليًّا**، ومعها **سطرُ «انفجر X% خلال
أسبوع — النموذج اشتغل»** في المتابعة اليومية (`Super_stock.py:10680`) **يصمت**.

🔑 **والتمييزُ الحاكم:** مفهومُ المالك «الحدّ الأدنى» = **لا تُقصِ أسهمي** ⇒ `P100`
صحيحٌ للأرضيات. لكن `gain5` **سقفٌ** لا أرضية، و«الأوسع» في السقف = **لا سقف**.
⇒ هذي التجربةُ **ليست نقضًا لمفهومه** بل تمييزُ فئةٍ داخله.

## ② الذراعان (مثبَّتتان — ولا ثالثة)

| ذراع | `RECENT_RISE_BLOCK_PCT` | الوصف |
|---|---|---|
| **G214** | **214.7286870417835** | الإنتاج اليوم حرفيًّا (الأساس) |
| **G35** | **35.0** | رقمُ المالك الموثّق (‏D11 «تبقى كما هي») |

**وكلُّ شيءٍ آخر P100 بلا تغيير** (نفسُ ملفّ الحوافّ · نفسُ اللقطة · نفسُ المُرتِّب
`rank_live` · نفسُ السعة 10 · نفسُ النافذة). التبديلُ في **سائقٍ بحثيّ** بعد
الاستيراد — **صفر مسٍّ بالإنتاج**، والقيمةُ النافذة تُطبَع لكلّ ذراع.

⚠️ **ولا ذراعَ لـ`P90`=8.16 في هذي التجربة**: إضافتُها بعد رؤية أرقام G35 = اختيارٌ
بعديّ. إن أراد المالك قياسَها فهي **تجربةٌ مسجَّلةٌ جديدة**.

## ③ البيانات

لقطات PIT الثلاث: **2024=`31199493957` · 2025=`31191327731` · 2026=`31199510868`**
— نفسُها المستعملة في `T-MINFLOOR` فتُقارَن الأرقام مباشرةً.

## ④ بوّابةُ صلاحيةٍ حاكمة (تُقرأ **قبل** أيّ تفسير)

الحكمُ **لا يُفسَّر** إن سقطت أيٌّ من الثلاث — بصمةُ الـ`no-op` الموثّقة:
1. **القيمةُ النافذة تختلف**: ‏214.73 مقابل 35.0 (تُطبَع من `CONFIG` بعد التطبيق).
2. **العدّادُ يشتعل**: `M4_انفجر_فعلاً` في توزيع رفض G35 **أكبر من صفر**، وفي
   G214 ‏≈0. يُقرأ من **عدّاد الإنتاج نفسِه** (`reject_distribution_lines`).
3. **الإشاراتُ تنقص**: `signals(G35) < signals(G214)` — البوّابةُ تُزيل ولا تُضيف.
   تساوٍ تامّ ⇒ **no-op ⇒ لا تُفسَّر**.

## ⑤ المقاييس (تُقفَل الآن — بهذا الترتيب)

1. **المنفجرون المُسلَّمون** `d50` بسعة الإنتاج (المقياسُ الحاكم في كلّ تجاربنا)
   + `d100`.
2. **حارسُ العائد**: متوسط `R`/صفقة.
3. **نسبةُ الدقة** بمقامها (‏محسومة رابحة ÷ محسومة) — لكل سنةٍ والمجمَّع.
4. **الحجم**: الإشارات · المحسومة · غير المُعبَّأة · المرفوض بالسعة.
5. **عدّادُ البوّابة**: كم رفضةَ `M4_انفجر_فعلاً` (كلفةُ الحماية بالأرقام).

كلُّ رقمٍ يُنشَر **مع مقامه** — لا نسبةَ بلا عدد.

## ⑥ قاعدةُ القرار — **تجربةُ كلفةٍ لا تجربةُ حافّة**

🔴 **الفرضيةُ الصفرية هنا معاكسةٌ للمعتاد، وهذا مقصود:** أنا **لا أدّعي** أن
إرجاعَ السقف يرفع العائد. المُختبَرُ: **هل يُكلّف شيئًا مقيسًا؟** لأن قيمةَ الحماية
(منعُ الملاحقة + عودةُ سطر «فات القطار») **لا يقيسها الباكتيست أصلًا**: يُعبّئ عند
متوسط الدفعات مهما كان السعرُ مرتفعًا، فلا يرى ثمنَ الشراء بعد ركضة.

**يُوصى بالاعتماد** إذا استُوفيت الثلاث:
- **(أ)** `d50` المجمَّع لم ينقص أكثر من **10% نسبيًّا** عن G214.
- **(ب)** `R`/صفقة المجمَّع لم يسُؤ أكثر من **0.05R** (نفسُ حارس `T-CAP`).
- **(ج)** لا انقلابَ صارخ: لا سنةٌ تسوء بأكثر من **20%** في `d50` بينما أخرى تتحسّن.

**لا يُوصى** إن سقط أيٌّ منها — ويُبلَّغ الرقمُ كما هو، **والقرارُ للمالك في
الحالتين** (البوّابةُ تمسّ العضوية و`LOGIC_VERSION`).

⚖️ **وحدٌّ صريح:** «لم يُكلّف» **ليس** «ربّح». وإن جاءت النتيجةُ محايدةً فالمكسبُ
المُدَّعى هو **الحمايةُ والإشعار** لا العائد — ويُقال كذلك بلا تجميل.

## ⑦ تنبّؤات قابلة للكسر (قبل أيّ رقم)

- **C1:** الإشاراتُ تنقص **بين 5% و25%** في كل سنة (البوّابةُ تشتعل ولا تكتسح).
- **C2:** نسبةُ الدقة **تتغيّر ‏±2 نقطة فقط** — لأن المُستبعَدين «فاتهم القطار»
  والباكتيستُ يُعبّئهم عند متوسط الدفعات فلا يرى غلاءَ سعرهم.
- **C3:** `d50` **ينقص قليلًا** — بعضُ مَن انفجر كان صاعدًا سلفًا. **هذا هو الخطرُ
  الحقيقيّ للتجربة**، وهو ما يقيسه الحارس (أ).
- **C4:** `R`/صفقة **يتحسّن أو يثبت** (إزالةُ دخولاتٍ ملاحِقة).
- **C5:** عدّادُ `M4_انفجر_فعلاً` في G214 = **0 أو قريبٌ منه** (تأكيدُ أن البوّابة
  معطَّلةٌ اليوم فعلًا لا بالظنّ).

## ⑧ حدود صدق (قائمةٌ مهما كانت النتيجة)

انحيازُ بقاء · تشويهُ تقسيمات · بلا افتر · المُرتِّبُ مقيسٌ عند الصدفة (‏T-REPLAY10)
فالسعةُ تقصّ ما لا نتحكّم به · والباكتيستُ **أعمى عن ثمن الملاحقة** بنيويًّا (وهو
سببُ كون هذي تجربةَ كلفة) · وسنواتُ 2025/2026 داخلَ العيّنة في معايرة الظرف.

---
*التنفيذ: `chase_arms.py` (سائقٌ بحثيّ، صفر مسٍّ بالإنتاج) + `chase.yml`.
النتيجة: `chase_result.md`.*
