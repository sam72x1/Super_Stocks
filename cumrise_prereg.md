# 📈🧮 `T-CUMRISE` — «قِس الرفعة التراكميّة» (تسجيلٌ مسبقٌ مدفوعٌ **قبل أيّ رقم**)

> **أمرُ المالك 2026-08-18:** «ارفع السقف و **قِس الرفعة التراكمية**».
> **وهذا الملفُّ يُدفَع قبل أن يُفتَح سجلُّ أوّلِ تشغيلة.** أيُّ رقمٍ يظهر بعده
> يُقرأ بمعاييره المكتوبة هنا — ولا يُحرَّك حدٌّ ولا تُضاف ذراعٌ بعد رؤية الأرقام.

---

## ① السؤالُ بنصّه

بوّابةُ المِرساة اليوم تشترط **رفعةً في دقيقةٍ واحدة** لا تقلّ عن
`LIQ_MIN_MOVE_PCT` = **5%** (‏`_inflow` في `liq_stage_events` · تُقاس **فتحًا ⟶
إغلاقًا** لا إلى القمّة). **والسؤال: هل قياسُ الرفعة نفسِها على النافذة
التراكميّة `LIQ_CUM_MINUTES` = 3 دقائق — بدل دقيقةٍ واحدة — يُقلّل التأخّر بلا
أن يفقد إثمارًا ولا يُغرق؟**

🔑 **ولماذا هذا السؤالُ بالذات الآن:** سهمٌ يرتفع ‏3% ثم 3% ثم 4% **لا يبلغ 5% في
أيّ دقيقةٍ منفردة** فلا تُطلِق البوّابةُ أصلًا، **وهو قد صعد 10%**. وحكمُ
`T-GATE` (‏2026-08-18) قاس أن وسيطَ التأخّر عند أوّل تنبيه **‏+17.1%**، وأن
**‏14 من 30 متحرّكًا يتحسّنون بإرخاء شرط الرفعة** — وأن **الرفعةَ هي الرافعة**
(إرخاءُ الحجم يحسّن 3 فقط). فهذي الذراعُ تسأل: هل يُدرَك ذلك التحسّنُ **بلا
إرخاءِ رقم المالك**؟

## ② ⚖️ صفرُ رقمٍ مخترَع — وهذا شرطُ مشروعيّتها

- **‏5% رقمُ المالك ولا يُمَسّ** (`LIQ_MIN_MOVE_PCT`). المتغيّرُ **النافذةُ التي
  تُقاس عليها** لا قيمتُها.
- **و‏3 دقائقَ ليست رقمًا جديدًا:** هي `LIQ_CUM_MINUTES` النافذةُ **النافذةُ في
  الإنتاج اليوم** للأرضية الدولاريّة (اعتُمدت 2026-08-17 بعقدٍ مسجَّل).
  ⇒ الذراعُ تُوحّد النافذتين: **الأرضيةُ والرفعةُ على المدى نفسِه.**
- والنافذةُ الخماسيّة في `R2` منقولةٌ من `G3` في `gate_prereg §④` — **رقمٌ
  مُعادٌ لا مخترَع**، ووسمُه `engineering` صريحًا.

## ③ التعريفُ الحسابيُّ للذراع (مثبَّتٌ قبل التشغيل)

لنافذةٍ من `n` دقيقةً مغلقةً آخرُها `last`:

```
cum_rise = (close(last) − open(first_of_window)) / open(first_of_window) × 100
```

و**بقيّةُ الشروط الأربعة تبقى حرفيًّا كما هي**: قفزةُ الحجم `IGNITION_VOL_MULT`
=3× · الأرضيةُ التراكميّة `LIQ_MIN_USD`=$30,000 على `LIQ_CUM_MINUTES`=3 ·
«خضراء» · وموضعُ الإغلاق `LIQ_CLOSE_POS_MIN`=0.5.
🔒 **ولا يُمَسّ شرطُ الحياة:** الشرطان الأخيران يُقاسان على **الدقيقة الأخيرة**
كما في الإنتاج ⇒ **مجموعٌ ضخمٌ بدقيقةٍ ميّتةٍ لا يُطلِق** (سابقةُ `T-LIQ-MOVE`).

## ④ الأذرعُ الأربع — مثبَّتةٌ، ولا تُزاد ذراعٌ بعد الأرقام

| الذراع | التعريف | الوسم |
|---|---|---|
| **`R0`** | الإنتاجُ كما هو: رفعةُ **دقيقةٍ واحدة** لا تقلّ عن 5% | الأساس |
| **`R1`** | رفعةٌ تراكميّةٌ على **3 دقائق** لا تقلّ عن 5% | الذراعُ الحاكمة |
| **`R2`** | رفعةٌ تراكميّةٌ على **5 دقائق** لا تقلّ عن 5% | حساسيّةُ النافذة |
| **`R3`** | **أيٌّ منهما**: دقيقةٌ منفردةٌ 5% **أو** تراكميُّ 3 دقائقَ 5% | الحدُّ الأعلى |

⚠️ **و`R1` ليست مجموعةً فوق `R0`:** دقيقةٌ ترتفع 6% بعد دقيقتين هابطتين قد يكون
تراكميُّها سالبًا ⇒ **`R1` قد تكتم ما يُطلقه الإنتاج**. ولذلك `R3` (الاتّحاد)
مثبَّتةٌ سلفًا: هي **رتيبةٌ لا تكتم شيئًا يُطلِقه اليوم**، وثمنُها الإشعارات.

## ⑤ المقاييسُ الأربعة وحدودُها — **منقولةٌ من `T-GATE` بلا تحريك**

1. **التأخّر (الحاكم):** وسيطُ `(سعرُ المِرساة ÷ إغلاقِ الأمس − 1) × 100` عبر
   الأسهم المُنبَّهة. **الحدُّ: تحسّنٌ ‏5 نقاطٍ فأكثر** (حدُّ `T-GATE` نفسُه).
2. **الإثمار:** نصيبُ الإشعارات التي يبلغ سعرُها **‏+3%** خلال **15 دقيقة**
   بعدها (‏3% لا 5% عمدًا — قياسُ 5% بعد شرطِ 5% **دائريّة**).
   **الحدُّ: لا يسوء بأكثرَ من ‏3.6 نقطة** (`JITTER_PP` المقيس، لا رقمٌ جديد).
3. **الإشعارات:** عددُ الدقائق العابرة. **الحدُّ: زيادةٌ لا تتجاوز ‏+50%**
   (`engineering` منّي — يُوسَم كذلك).
4. **صفرُ متحرّكٍ مفقود:** كلُّ سهمٍ صعد **‏≥30%** (`MOVER_PCT`، منقولٌ حرفيًّا)
   ويُنبَّه في `R0` **يجب** أن يبقى مُنبَّهًا. **فقدُ واحدٍ ⇒ الذراعُ ساقطة.**

**والحكم:** تُعتمَد ذراعٌ **فقط** إن استوفت الأربعة. واستيفاءُ ثلاثةٍ ⇒ «لا
حكم» لا «تُعتمَد». وأيُّ فرقٍ دون **‏3.6 نقطة** يُقرأ **داخل الضجيج** ولا يُدَّعى.

## ⑥ الأرضيّاتُ ومتى نقول «لا حكم» قبل النظر

- **‏≥20 إشعارًا** في مقام الإثمار لكلّ ذراع، وإلّا **لا يُقرأ إثمارُها**.
- **‏≥8 أسهمٍ مُنبَّهة** لحساب وسيط التأخّر، وإلّا **لا يُطبَع وسيط**.
- **‏≥5 متحرّكين** (‏≥30%) في الجلسة، وإلّا **المعيار ④ يُعلَن «غيرُ مقيس»**
  ولا يُعَدّ مستوفًى.
- تعذّرُ جلبِ إغلاقِ الأمس ⇒ السهمُ **يُعَدّ ويُستبعَد ويُسمّى** — لا يُخمَّن.

## ⑦ سقفُ النجاح — مثبَّتٌ سلفًا

**اقتراحٌ للمالك مع رقمِه، وسطرٌ في تقرير القياس. لا تغييرَ إنتاجيًّا آليًّا
مهما كانت الأرقام.** والذراعُ تمسّ **زنادَ الإشعار** ⇒ اعتمادُها **قرارُ مالكٍ
صريح**. ⛔ وممنوعٌ بنصّ هذا العقد: مسُّ `M1`-`M14` أو الجذور أو الدخول/الوقف/
الأهداف أو قيمةِ `LIQ_MIN_MOVE_PCT` أو `LIQ_MIN_USD`.

## ⑧ تنبّؤاتٌ قابلةٌ للكسر (تُنشَر ولو كُذّبت)

- **`K1`:** `R1` تُقلّل وسيطَ التأخّر بـ**‏5 نقاطٍ فأكثر** عن `R0`.
- **`K2`:** `R1` **تكتم** سهمًا واحدًا على الأقلّ يُطلقه `R0` (ليست مجموعةً فوقه).
- **`K3`:** `R3` أقلُّ تأخّرًا من `R1` **وأكثرُ إشعارًا منها**.
- **`K4`:** `R2` (خمس دقائق) **لا تتفوّق** على `R1` في التأخّر بفارقٍ ماديّ
  (‏≥3.6 نقطة) — أي أن الاتّساع وحده ليس رافعة.
- **`K5`:** الإثمارُ **ينخفض** في كلّ ذراعٍ عن `R0` (لأن التبكير يشتري إشارةً
  أضعف) — **وإن ارتفع فهو مكسبٌ مزدوجٌ يُنشَر ولا يُبنى عليه من جلسةٍ واحدة**.

## ⑨ حرّاسُ الصلاحية — منفَّذةٌ في الكود لا موصوفةٌ هنا

- **`V1`** `R0` يطابق `liq_stage_events` **سلوكيًّا** على عيّناتٍ مفرِّقة
  (عابرةٌ · وساقطةٌ بكلّ شرطٍ على حدة) — تفرّقٌ واحدٌ ⇒ **خروج 3 ولا يُنشَر رقم**.
- **`V2`** كلُّ ذراعٍ **تفرّق فعلًا** عن `R0` على عيّنةٍ مبنيّة — وإلّا فهي
  `no-op` تُقرأ «لا فرق» كذبًا (بصمةُ `BT_CANDLE`).
- **`V3`** الجلسةُ تُحسَم **مرّةً واحدةً للكلّ** (`resolve_day`) فلا تُخلَط أيّام.
- **`V4`** إغلاقُ الأمس بقطعٍ **قبل** يوم القياس (عيبُ `/prev` الموثَّق).
- **`V5`** **الميزانيةُ واحدة**: الأذرعُ الأربع تُقاس على **نفس الشموع المجلوبة
  مرّةً واحدة** ونفسِ الكون ونفسِ اليوم — وتُطبَع القيود.
- **`V6`** صفرُ حدثٍ في الجلسة ⇒ **خروج 5 مُعلَن** (لا جدولَ أصفارٍ يُقرأ نتيجة).

## ⑩ حدودُ صدقٍ تُقرأ **مع** النتيجة لا بعدها

1. **جلسةٌ واحدة** ⇒ اتّجاهٌ لا حكمُ ربحيّة، و**لا صفقةَ تُقاس**.
2. **«أثمر» = لمسُ ‏+3% لا ربحٌ محقَّق**، ولا تنفيذَ ولا سيولةَ تُفترَض.
3. **دائريّةٌ جزئيّةٌ لا تُنفى:** الذراعُ تنتقي إشعاراتٍ أقربَ للحركة — وهذا
   **انتقاءٌ لا تنبّؤ**.
4. **الإعادةُ ترى كلَّ دقيقة** والحيُّ يلفّ كلَّ ‏≈60 ثانية ⇒ أعدادُ الإشعارات
   **سقفٌ**، والنِّسَبُ أدقُّ من الأعداد.
5. **التأخّرُ يبتلع فجوةَ الافتتاح** (حدُّ `T-GATE` المدوَّن): جزءٌ منه لا تسبقه
   بوّابةٌ لحظيّةٌ أصلًا ⇒ **لا يُنسَب كلُّه للبوّابة**.
6. **والفلترُ المشحون (`C1`) خارج هذا القياس** — يُقاس الزنادُ لا التسليم.

## ⑪ حالتان مسمّاتان **قبل** الأرقام (فلا يُقال «اختِيرتا بعدها»)

- **`$PFSA`** (‏2026-08-18): مِرساتُه الحيّة 04:09 نيويورك · `vol_x` 4.4 ·
  ذروةُ سيولةٍ ‏$2,284,431 — **يُطبَع تأخّرُه في كلّ ذراع**.
- **`$WFF`** (‏2026-08-17): بلاغُ المالك أن الإشعار وصله والسهمُ فوق 20%.

**وليستا معيارًا** — المعاييرُ في §⑤ وحدها؛ هما **شاهدان يُعرَضان بالاسم**.
