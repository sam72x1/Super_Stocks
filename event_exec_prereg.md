# ⚡ T-EVENT-EXEC — التسجيل المسبق (يُكتب ويُدفع **قبل أيّ رقم**)

> **السؤال (بصياغة المراجِع الحرفية):** هل حدثُ **اشتعالٍ** (`ignition`) أو
> **مسح-واستعادة** يضيف **توقّعًا قابلًا للتنفيذ** فوق كون **ARMED** (الأسهم التي
> كانت على القائمة أصلًا)؟
>
> 🔴 **والرهان مسجَّلٌ سلفًا وثقيل:** في `CLAUDE_REPLY_TO_CODEX.md` §④-12 ثبّتُّ
> معيارَ إغلاقٍ **قبل** أيّ من التجربتين: «إذا عبرت `T-REPLAY10` ثم **(أ)** لم يتفوّق
> المُرتِّب بهامش ‏≥0.05R/يوم بحدٍّ سفليّ 95% فوق الصفر، **و(ب)** خرج `T-EVENT-EXEC`
> بحدٍّ سفليّ 95% للمتوسط **دون** الصفر بعد التكاليف — **عندها يُغلق مسار الاختيار عند
> القاع بوصفه مصدر حافة**، وتبقى الأدوات تشخيصية فقط».
> **والشرط (أ) تحقّق فعلًا** (`replay10_result.md`: ③ غير حاسم · التفوّق ساقط).
> ⇒ **هذي التجربة تقرّر مصير الملفّ كلّه، والمعيار مكتوبٌ قبل الأرقام.**
>
> 🔒 **بحث/قياس — صفر مسّ إنتاج · لا `LOGIC_VERSION` · سقف النجاح: أرقامٌ واقتراح.**
> **الحالة عند الكتابة: صفر رقمٍ من أيّ ذراع.**

---

## ① ما عاينتُه قبل الكتابة — **يُعلَن للشفافية**

عاينتُ **إمكان التنفيذ** فقط (لا نتائج):
- **`/v3/quotes` (‏NBBO التاريخيّ) متاحٌ لاشتراكنا ويعود ‏≥1200 يومًا** — مُثبَتٌ بمِجَسّ
  ‏2026-07-30 (`nbbo_history_verdict.md`) على **مغمورٍ من نوع أسهمنا (APVO)** كما على
  السائل، بصفر ‏401/403. ⇒ **قاعدة «الدخول بأوّل `ask` عمره ‏≤5ث» قابلة للتنفيذ تاريخيًّا.**
- **`/v3/trades` التاريخيّ مستعمَلٌ عندنا منذ `T-ACC`** (‏`polygon_base_trades` بـ
  `timestamp.lt`) ⇒ بوّابة المضارب قابلة للقياس تاريخيًّا.
- 🔴 **وما لا أعرفه بعد ويُعلَن الآن:** **عمق تاريخ شموع الدقيقة** (`/v2/aggs`) لم
  يُفحَص قطّ. ⇒ **الأداة تبدأ بفحص تغطيةٍ إلزاميّ**، وإن قصُر التاريخ عن سنةٍ **تُعلَن
  تلك السنة «غير مقيسة» ولا تُستبدَل بسنةٍ أقرب** بعد رؤية النتائج.

---

## ② مجتمع الدراسة — **كون ARMED**

**‏ARMED = ما كان على القائمة فعلًا**، لا ما رشّحه الفارز. ويُشتقّ من **أداة الإعادة
الأمينة نفسها** (`T-REPLAY10`، الذراع `R0` = المُرتِّب الإنتاجيّ): كلُّ صفقةٍ **مأخوذة**
تُنتج نافذة `[تاريخ الدخول، تاريخ الخروج]`، وكلُّ جلسة تداولٍ داخلها = **(رمز، جلسة)
مسلَّحة**.

⚖️ **ولماذا هذا المجتمع لا «كل الإشارات»:** رادار الانطلاق الحيّ يمسح **القائمة
المؤهَّلة** لا السوق ⇒ قياسُه على غيرها يقيس شيئًا لا يقع. **والسعة تقصّ ‏27-48% من
الإشارات** (مقيسٌ في `T-REPLAY10`) فالفرق جوهريّ لا تجميليّ.

**السنوات: 2023 · 2024 · 2025** · **لقطة كون ARMED: `30598885456`** (نفس لقطة
`T-REPLAY10` — فالمجتمعان متطابقان بالبناء).

---

## ③ الزناد — **مُجمَّدٌ على قيم الإنتاج · صفر ضبط**

**تُستدعى دوالّ الإنتاج نفسها بلا نسخٍ ولا إعادة كتابة:**
`_ignition_break_level` (الرقم الحرج، وإلا الأرضية ×1.05) · `_ignition_signal`
(**قفزة حجم الدقيقة ‏≥`IGNITION_VOL_MULT`=3× متوسط السابقة** ‏+ كسر المستوى صاعدًا
‏+ اتجاهٌ صاعد داخل النافذة) · `_ignition_candle_class` · `_operator_blocks`
(‏`OPERATOR_MIN_SHARES`=1000).

🔒 **ولا عتبة تُلمَس:** أيّ ضبطٍ للعتبات بعد رؤية النتائج = `p-hacking`، والعتبتان
(‏3× و1000 سهم) **شاركتا في توليد الفرضية** ⇒ ولهذا السبب بالذات **ذراع المضارب
ثانويّةٌ لا أساسية** (‏§⑤).

---

## ④ التنفيذ — **قواعد صارمة تُثبَّت الآن**

| القاعدة | التنفيذ |
|---|---|
| **الحدث الأساسيّ** | **أوّل حدثٍ لكل (رمز، جلسة)** — لا أفضلُها ولا آخرُها |
| **المقام** | **كلُّ حدثٍ خام** — بما فيه **المكبوت** (بوّابة المضارب) و**غير المنفَّذ** (بلا `ask` صالح). لا يُحذف من المقام شيء |
| **سعر الدخول** | **أوّل `ask` من ‏NBBO عمره ‏≤5 ثوانٍ** بعد إغلاق شمعة الزناد |
| **لا `ask` صالح** | يُسجَّل **`non_executable`** — **ولا يُستبدَل بسعرٍ مريح** (لا إغلاق الدقيقة ولا الفتح التالي) |
| **الوقف والهدف** | **من الخطة نفسها** (`stop`/`t1` المخزَّنان بالصفقة) — التجربة تقيس **التوقيت** لا سياسة خروجٍ جديدة |
| **الحسم** | **`_resolve_arm` الإنتاجيّ نفسه** على الشموع اليومية من يوم الزناد ⇒ **الوقف أوّلًا عند التعادل** |
| **حساسية** | ذراع **الهدف أوّلًا** تُنشَر بجانبها (لا تحلّ محلّها) |
| **التكاليف صريحة** | الدخول عند **`ask` حقيقيّ** (نصفُ السبريد مدفوعٌ واقعًا) ‏+ الخروج بخصم **نصف السبريد المقيس عند الدخول** |
| **مقياس R** | `(سعر الخروج − سعر الدخول) ÷ (سعر الدخول − الوقف)`، **والوقف = −1R بالبناء** |

---

## ⑤ الأذرع — **مثبَّتة الآن، لا خامسة لها**

| الذراع | التعريف | الدور |
|---|---|---|
| **E-REAL** | الاشتعال الحقيقيّ بالزناد المجمَّد | **أساسيّة** |
| **E-PSEUDO** | **حدثٌ زائف مُطابَق**: **نفس الرمز** · أقرب جلسة **ARMED بلا زناد** · **نفس دقيقة الساعة** من الحدث الحقيقيّ | **أساسيّة** (‏`case-crossover`) |
| **E-CROSS** | **عبورٌ بسيط** للمستوى صاعدًا **بلا** شرط الحجم وبلا اتجاه — أوّل دقيقةٍ تعبر | **أساسيّة** (تعزل «هل الحجم يضيف؟») |
| **E-OPERATOR** | `E-REAL` مُصفّاةً بـ`has_operator` | 🔴 **ثانويّة فقط** — عتبتها شاركت في توليد الفرضية |

🔑 **و`E-PSEUDO` هي فكرة المراجِع «خارج الصندوق» وأثمن ما في التصميم:** مقارنة الحدث
بيومٍ زائفٍ **لنفس السهم** تضبط تلقائيًّا الهوية والفلوت والتاريخ والقطاع ⇒ **تعزل قيمة
التوقيت وحدها**. ورمزٌ بلا يومٍ مُطابَقٍ **يُستبعَد من المقارنة المزدوجة ويُعدّ ويُعلَن**.

---

## ⑥ المقاييس — مثبَّتة الآن

- **الأساسيّ:** **متوسط صافي R بعد التكاليف** على `E-REAL` · **والفرق المزدوج**
  `E-REAL − E-PSEUDO` داخل الرمز.
- **الثانوية (تُنشَر ولا تحكم):** نسبة التنبيه القابل للتنفيذ · معدّل الالتقاط ·
  الصعود المتبقّي بعد الدخول · السبريد · **مقام الإنذار الكاذب** · توزيع المساهمة.

## ⑦ الاستدلال — مثبَّت الآن

**‏`cluster bootstrap` بالرمز** · **‏`block bootstrap` بالكتلة الشهرية** ·
**‏10,000 عيّنة** · و**‏`randomization inference` داخل مجموعة أيام الرمز نفسه** لمقارنة
`E-REAL` بـ`E-PSEUDO`. **لا اختبار مستقلّ يفترض استقلال الصفقات.**

## ⑧ بوّابة القرار — **حرفيًّا كما اقترحها المراجِع**

1. **‏≥150 صفقة محسومة إجمالًا · و≥30 في كلّ سنة** إن أمكن تاريخيًّا.
2. **الحدّ السفليّ ‏95% لمتوسط صافي R فوق الصفر** بعد التكاليف.
3. **نتيجة موجبة في كلّ سنة.**
4. **نسبة التنفيذ ‏≥80%.**
5. **لا رمزٌ ولا جلسةٌ تصنع أكثر من ‏20% من الربح.**

⇒ **الخمسة معًا.** وسقوط أيّها = **لا اعتماد**، ويُكتب السبب صراحةً.

🔴 **وقاعدة الإغلاق المسجَّلة (‏§12):** الشرط (أ) **تحقّق**؛ فإن خرج **الحدّ السفليّ
‏95% دون الصفر بعد التكاليف** ⇒ **يُغلق «الاختيار عند القاع» بوصفه مصدر حافة**،
وتبقى الأدوات **تشخيصية**. ⚠️ **وهذا ليس حكمًا على منهجية فيصل** — بل على **نسختنا
الآلية منها بلا يدٍ تدير**؛ التمييز مكتوبٌ الآن لا بعد النتيجة.

---

## ⑨ التنبّؤات القابلة للكسر

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| **W1** | **فحص التغطية سيُسقط سنةً كاملة على الأقلّ** (عمق شموع الدقيقة أقصر من 3 سنوات) | تغطيةٌ كاملة للثلاث ⇒ العمق أطول ممّا ظننت |
| **W2** | **نسبة التنفيذ ستسقط دون ‏80%** — المغمورة بلا `ask` صالحٍ في كثيرٍ من الدقائق | بلوغها ⇒ الدفتر أكثف ممّا ظننت |
| **W3** | **`E-REAL` لن يتفوّق على `E-PSEUDO`** بفارقٍ يصمد — بعد أربعة عشر تأكيدًا أن الحافة ليست في الاختيار، **وهذا أوّل اختبارٍ مباشرٍ لدعوى «الحافة = التوقيت»** | تفوّقه ⇒ **أوّل حافةٍ مُثبَتة في المشروع** |
| **W4** | **`E-CROSS` ≈ `E-REAL`** ⇒ شرط الحجم (‏3×) لا يضيف | فرقٌ واضح ⇒ الحجم يضيف فعلًا |
| **W5** | **بوّابة «لا رمز يصنع ‏>20% من الربح» ستسقط** — كل توزيعاتنا يحملها ذيل قِلّة | صمودُها ⇒ توزيعٌ أعرض ممّا قِيس سابقًا |

🔴 **سجلّي:** في `T-REPLAY10` تحقّقت 4 من 5 وبقي واحدٌ **غير مُختبَر** — فأعلنتُه ولم
أدّعِه. وأتوقّع هنا كسرًا مماثلًا.

---

## ⑩ حدود الصدق (تُعلَن قبل النتيجة)

1. **كون ARMED مُجمَّد `as-of 2026-07-22`** بينما **بيانات الدقيقة تُجلَب اليوم حيًّا**
   ⇒ **اختلاف مقياس تعديلٍ محتمل** لو وقع تقسيمٌ بينهما. **حارسٌ إلزاميّ:** يُقارَن مدى
   شموع دقيقة يوم الزناد بالشمعة اليومية المجمَّدة، وتجاوزُ الانحراف حدًّا مُعلَنًا ⇒
   **`scale_mismatch` يُستبعَد ويُعدّ** (لا يُصحَّح بتقدير).
2. **انحياز البقاء** (كون ناسداك اليوم) · **تشويه التقسيمات** — موروثان من اللقطة.
3. **‏`E-PSEUDO` يضبط الهوية لا حالة السوق العامّة** في ذلك اليوم.
4. **‏NBBO = قمّة الدفتر لا عمقه** ⇒ «قابل للتنفيذ» تعني «وُجد `ask` معلَن»، **لا** أن
   الحجم المطلوب كان متاحًا عنده. **حدٌّ لا يُخفَّف.**
5. **الحسم على شموع يومية** بعد الدخول اللحظيّ ⇒ **الوقف يُفحَص بلمسة الذيل**
   (محافظ)، ولا يُقاس وقفٌ متحرّك ولا خروجٌ جزئيّ (ذاك `T-MANAGE-25`).
6. **لا تُقارَن أرقام هذي التجربة بأرقام ما قبلها** — المجتمع والمحرّك مختلفان.

## ⑪ سقف النجاح

**أرقامٌ + اقتراحٌ واحد للمالك.** **لا تغيير إنتاجيّ · لا مسّ بالجذور · لا
`LOGIC_VERSION` · ولا تعديل عتبةٍ على الرادار الحيّ.** وأيّ تفعيلٍ لاحق = **قرار مالكٍ
حصريّ**.
