# 🎯 T-FAISAL-ONLY — تسجيلٌ مسبق

> **مدفوعٌ قبل أيّ رقمٍ من المعايرة الجديدة.** أيُّ تعديلٍ بعد الدفع يكون **ملحقًا
> مؤرَّخًا** أسفلَ الملفّ، والأصلُ يبقى مشطوبًا لا ممحوًّا.
>
> **التاريخ:** 2026-08-06 · **الحالة عند الدفع:** **لا رقمَ واحدًا من المعايرة الجديدة**
> — لا حوافّ ولا التقاط ولا كلفة.
>
> **اللقطةُ المجمَّدة (جاهزة):** تشغيلة **`31070013412`** · as-of **2026-08-06** ·
> **3385 رمزًا** من 3564 (‏95%) · 667 بتقسيمات · ‏SHA `63951aedf081` · نافذةٌ من 2024-05-28
> ⇒ **تغطّي مجموعة التحقّق كاملةً** (‏2026-06-12 → 08-05).
>
> **تشغيلةُ المعايرة:** أُطلقت **بعد** كتابة هذا الملفّ وقبل دفعه، ومعرّفُها يُلحق أدناه
> فورَ صدوره. **ولم أرَ مُخرَجها عند الكتابة.**
>
> ⏳ **ومراجعةٌ خصوميّة على هذا التصميم تعمل الآن** — كلُّ ما تُخرجه يصير **ملحقًا
> مؤرَّخًا** أسفلَ الملفّ، ويُصرَّح فيه بأنه كُتب **قبل رؤية أرقام المعايرة** أو بعدها.

---

## ⓪ أمرُ المالك والدافعُ المقيس

**أمرُ المالك (2026-08-06، حرفيًّا):** «لازم نلتزم بمعايير فيصل فقط، وهذي نجيبها عن طريق
الكتالوج».

**والدافعُ ليس رأيًا — هو قياسٌ حيٌّ من سجلّ البوت نفسه** (`weekly_watchlist.json["explosions"]`،
‏2026-06-12 → 2026-08-05):

| | العدد |
|---|---|
| متحرّكون رصدهم البوت (‏قفزة ≥50% أو تجمّع ≥70%) | **580** |
| منهم بلا شبهة تقسيم | **499 حدثًا · 209 رمزًا** |
| منهم موسومٌ «كان ارتكازًا» (اجتاز هوية M1-M3) | **197 حدثًا · 83 رمزًا** |
| **منهم اجتاز الفارزَ عند قاعه فرشّحناه** | **صفر** |

**وجدارُهم الأوّل** (‏`base_reason` = البوّابة التي رفضتهم **عند قاعهم قبل الركض**):

| الجدار | الأحداث | النسبة | وسمُ المصدر في `FAISAL_SOURCE_LEDGER.md` |
|---|---:|---:|---|
| `M1_سعر` | 186 | 37.3% | `engineering` — أرضيتُنا **$1.5** وأرضيةُ فيصل المنصوصة **$1** |
| `M4_base_واسعة` | 123 | 24.6% | **`engineering`** — بلا سندٍ من فيصل |
| `M2_هبوط_تحت_40` | 68 | 13.6% | `engineering` |
| `M10_RSI_ما_تشبّع` | 45 | 9.0% | `inferred` |
| `M2_هبوط_فوق_97` | 34 | 6.8% | `engineering` |
| `M5_سيولة` | 15 | 3.0% | `engineering` — **وله شاهدُ حمايةٍ مقيس** (‏C3 · YYAI) |
| `M3_انفجار_تحت_60` | 12 | 2.4% | `engineering` |
| `نواقص_فوق_3` | 11 | 2.2% | `engineering` — وقد سقط سندُها في `T-FAILS` |

📌 **وملاحظةٌ وصفيّة تُسجَّل الآن لأنها تُقرأ لاحقًا:** وسيطُ القفزة **متطابق** بين
الموسوم «كان ارتكازًا» (‏91%) وغيرِ الموسوم (‏91%) ⇒ **وسمُ الهوية لا يميّز حجمَ الحركة
إطلاقًا**؛ هو يخبرنا مَن اجتاز M1-M3 لا مَن تحرّك أكثر.

---

## ① السؤال

> لو استبدلنا عتباتِنا الهندسية بعتباتٍ **مقيسةٍ من كاتالوج فيصل نفسه**، هل نلتقط
> المتحرّكين الذين نفوّتهم اليوم؟ **وبأيّ كلفةٍ يومية؟**

⚠️ **وما لا يسأله:** لا يسأل «هل نربح أكثر؟». الربحيةُ **غيرُ قابلةٍ للقياس** على هذي
اللقطة (‏سببُه في §⑦) ⇒ كلُّ ما تُنتجه هذي التجربة **التقاطٌ وكلفة**، ومَن اشتقّ منها
معدَّلَ ربحٍ فقد أخطأ.

---

## ② المجموعاتُ الثلاث — منفصلةٌ بالبناء

### A — المعايرة (كاتالوج فيصل)
الخمسةُ والثلاثون في `catalog_envelope.py:47` ناقص `EXCLUDED_BY_OWNER` الثلاثة
(‏`HTZ` · `YYAI` · `XHLD`) ⇒ **32 مرشَّحًا**. لكلٍّ: مِرساةُ الحدث بـ`explosion_onset`
**المُصلَحة**، ثم قياسُ المعايير الأحد عشر في **العشرين جلسةً السابقة** لها، ثم `P90`.
**لا تدخل الحكمَ إطلاقًا.**

### B — التحقّق (‏مستقلّة — وهذا هو التصحيح)
**كلُّ المتحرّكين المرصودين حيًّا ناقص شبهة التقسيم = 499 حدثًا · 209 رمزًا.**

🔴🔴 **تصحيحٌ ذاتيّ سبق الدفع، ويُسجَّل لأنه كان سيُبطل النتيجة:** كان تصميمي الأوّل
يجعل B هي **الـ197 الموسومة `was_pivot=True`**. وهذا **فاسدٌ بالبناء**: `was_pivot` =
`_had_pivot_identity` (‏`Super_stock.py:9168`) **يقرأ `MIN_PRICE` و`MIN_DROP_FLOOR` و
`MAX_DROP_PCT` و`PRIOR_SPIKE_FLOOR`** — أي **البوّاباتِ الأربع التي سيُعيد الظرفُ
ضبطَها**. فشرطُ دخولِ مجموعة التحقّق كان سيكون **اجتيازَ عتباتِنا الحالية**، والالتقاطُ
يخرج منفوخًا بالبناء. ✅ **والكشفُ نفسُه بريء**: `scan_explosions` (‏`:9222`) يلتقط من
**حركة السعر وحدها** (‏`EXPLOSION_PCT` · `EXPLOSION_RUN_PCT`) بلا أيّ بوّابة ⇒ المجموعة
الصحيحة هي **كلُّ المتحرّكين**، و`was_pivot` **طبقةٌ وصفيّة تُعرَض ولا تُشترَط**.

**شرطا الدخول الوحيدان:** ① رصدَه كاشفُ المتحرّكين ② `suspect_split=False`.
**وشرطُ الخروج الوحيد:** أيُّ رمزٍ يقع في A **يُحذف من B ويُعلَن** (‏تُقاس نسبةُ
التقاطع وتُنشَر مهما كانت).

### C — الكلفة
الكونُ الكامل من اللقطة المجمَّدة، على **نفس شبكة الجلسات** المستعمَلة في B.

---

## ③ المقاييس — بمقامٍ موحَّد

🔴 **درسٌ مدوَّن يُطبَّق هنا:** رقمُ «إثراء ×8.6» في `catalog_envelope_result.md` **سُحب**
لأن الالتقاط قيس على 20 جلسة/رمز والتمييز على 5 ⇒ **مقامان مختلفان والقسمةُ بلا معنى**.
فهنا **الوحدةُ واحدةٌ في الطرفين: «رمز-جلسة»**.

| # | المقياس | التعريف الحرفيّ |
|---|---|---|
| **M1** | **الالتقاط على B** | نسبةُ رموز B التي يقبلها الظرفُ في **جلسةٍ واحدة على الأقلّ** من العشرين السابقة لمِرساة حدثها |
| **M2** | **الكلفة على C** | وسيطُ عدد الرموز المقبولة/يوم على الكون الكامل |
| **M3** | **معدّلُ القبول الخام** | نسبةُ «رمز-جلسة» مقبولة على C — **نفس وحدة M1** |
| **M4** | **الإثراء** | `M1 ÷ M3` — ولا يُنشَر إلّا إن كان المقامان متطابقَي الوحدة |
| **M5** | **الشاهد** | الفارزُ الحاليّ على B = **صفرٌ مقيسٌ سلفًا**؛ يُعاد التحقّق منه بالأداة نفسها |

**نافذةُ العشرين جلسة** مأخوذةٌ من `catalog_envelope.py` القائم (`GRID_SESSIONS=20`)
**ولا تُغيَّر بعد رؤية الأرقام**. ويُنشَر معها **فحصُ حساسيةٍ عند 10 و40 جلسة** — وصفيًّا،
**والحكمُ يبقى على العشرين** مهما قالت الحساسية.

---

## ④ المعيار — مثبَّتٌ قبل أيّ رقم

| الحكم | الشرط |
|---|---|
| 🟢 **يمضي** | `M1 ≥ 50%` **و** `M2 ≤ 50` مطابق/يوم **و** `M4 ≥ 3×` |
| 🟠 **يلزمه مُرتِّب** | `M1 ≥ 50%` و `M2` بين 50 و150 |
| 🔴 **يقف** | `M1 < 50%` **أو** `M2 > 150` |

عتباتُ الكلفة (‏50 · 150) **موروثةٌ من `catalog_envelope_design.md` §D6** لا مخترَعةٌ
هنا. وعتبةُ الالتقاط 50% و`3×` **اختيارُنا** — تُوسَم `engineering` صراحةً، وتُثبَّت
الآن قبل الأرقام فلا تُحرَّك بعدها.

**سقفُ النجاح:** **اقتراحٌ للمالك بأرقامه** — ولا تغييرَ إنتاجيّ إلّا بإذنه الصريح، ومعه
`LOGIC_VERSION` مرفوع و`force_renew=1` مرّة.

---

## ⑤ ما لا يُمَسّ مهما كانت النتيجة

- **`M13` (الشورت) و`M14` (الفلوت)** — قرارُ مالكٍ صريح (2026-07-29)، تبقيان فلترًا نهائيًّا.
- **الجذورُ التسعة** — `rank_key` · `select_top` · `classify_tier` · `entry_status` ·
  `analyze_ticker` · `apply_short_gate` · `apply_float_gate` · `scan_market` ·
  `backtest_symbol`. تُقفَل ببصمة AST قبل العمل وبعده.
- **صيّادُ المقسّم** — تحت حماية المالك الصريحة.
- **الوقفُ والأهدافُ والدخول** — خارج نطاق هذي التجربة تمامًا.

---

## ⑥ تنبّؤاتٌ قابلة للكسر (‏قبل الأرقام)

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| **F1** | `M1_سعر` و`M4_base_واسعة` معًا يفسّران **≥55%** من الجدران الأولى في B | نسبتُهما أقلّ |
| **F2** | الالتقاط على B **≥50%** | أقلّ |
| **F3** | الكلفة **>50/يوم** ⇒ الحكمُ الأرجح «يلزمه مُرتِّب» لا «يمضي» | ‏≤50 |
| **F4** | تقاطعُ A مع B **≤3 رموز** | أكثر |
| **F5** | الظرفُ المُصلَح **أضيقُ في `rsi_now`** (المِرساةُ الملوَّثة قرأت 49.78 وسط الصعود) و**أوسعُ في `base_range`** | العكس |
| **F6** | فتحُ بوّابةٍ واحدة لا يكفي: وسيطُ عدد الجدران لرموز B **≥2** | ‏<2 |

---

## ⑦ حدودُ صدقٍ — مُعلَنةٌ قبل النتيجة

1. **B مختارةٌ على النتيجة** (كلُّها تحرّكت) ⇒ تقيس **الالتقاط** لا **الربحية**، ولا
   يُشتقّ منها معدَّلُ ربحٍ ولا توقّعُ `R`.
2. **الربحيةُ ممتنعةٌ بنيويًّا هنا:** آخرُ مِرساةٍ في الكاتالوج **2026-07-27**، وكلُّ سنةٍ
   سابقة تحوي مراسٍ داخلها ⇒ لا مجموعةَ «سببيّةٌ ونظيفة» (‏`catalog_envelope_result.md`).
3. **B ثمانيةُ أسابيع فقط** (‏سقفُ سجلّ `explosions` = 600 صفًّا، ونحن عند 580) ⇒ نظامُ
   سوقٍ واحد، ولا تُعمَّم.
4. **انحيازُ البقاء** قائمٌ في C (‏كونُ ناسداك اليوم — المشطوبةُ غائبة).
5. **تشويهُ التقسيمات**: خُفِّف بإسقاط `suspect_split` ولم يُلغَ.
6. **الظرفُ يتمدّد بطبعه**: 32 رمزًا على 11 معيارًا ⇒ كلُّ معيارٍ يأخذ أقصى ما رآه.
7. **عمى الافتر**: القياسُ على الشموع اليومية النظامية (‏`L1` مقيسٌ عند 3.4% فأثرُه صغير
   لكنه ليس صفرًا).
8. 🔴 **والأثقل:** حتى لو مضى الظرف — **السعةُ 10-15 خانة والمُرتِّبُ مقيسٌ عند مستوى
   الصدفة** (‏`replay10_result.md`) ⇒ توسيعُ البِركة **قد لا يغيّر ما يصل المالك**.
   وهذا **حدٌّ مُعلَنٌ قبل التشغيل** لا عذرٌ يُقال بعده.

---

## ⑧ أقفالٌ إلزامية

- بصمةُ AST للجذور التسعة **قبل العمل وبعده** — أيُّ اختلافٍ يُوقف كلَّ شيء.
- **لا قفلٍ نصّيّ** (‏`getsource` + إبرة): سقطنا في هذا الفخّ **أربع مرّات** لأن الإبرة
  تُوجد في تعليقٍ أو `docstring`. كلُّ قفلٍ هنا **سلوكيّ أو AST**، ولكلٍّ **طفرةٌ تُثبت
  أنه يسقط**.
- الأداةُ الجديدة `faisal_only_check.py` **تستورد** دوالّ الإنتاج ولا تُعيد كتابة منطق
  بوّابة (‏صفرُ منطقٍ مكرّر — وإلّا تفرّق المنطقان فكذب التشخيص).
- **حارسُ عدم النظر المستقبليّ:** القصُّ عند مِرساة الحدث حصرًا. ⚠️ والمميِّزُ `hasattr(x,
  "iloc")` **لا** `hasattr(x, "index")` (‏`list.index` دالّة! ⇒ قائمةٌ تدخل مسار pandas
  فيُبتلَع الاستثناء وتُرجَع السلسلةُ **بلا قصّ** = تسريبٌ صامت — عيبٌ حقيقيّ وقع عندنا).
- **عدّادُ الاختبارات قبل وبعد** يجب أن يتحرّك، وإلّا فكتلةُ الأقفال ميّتة.
- **إثباتُ أن العلم فعّال**: سطرٌ صريح في السجلّ («قرأ ظرفًا ببصمة X لـN رمزًا») — وعدّاداتٌ
  متطابقةٌ بت-بت = **`no-op` لا تُفسَّر نتيجتُها**.

---

## ⑨ ترتيبُ التنفيذ

1. لقطةٌ مجمَّدة جديدة تغطّي حتى 2026-08-05 (‏قيد البناء عند الدفع).
2. **معايرةُ A** بالمِرساة المُصلَحة ⇒ `envelope_p90.json` **ببصمةٍ جديدة**.
3. `faisal_only_check.py` + أقفالُها + طفراتُها.
4. قياسُ **M1** على B و**M2/M3** على C من اللقطة نفسها.
5. `wall_stack` على B (‏لـ**F6**).
6. النتيجة في `faisal_only_result.md` — **ومعها سجلُّ التشغيلات كاملًا** بما فيه الباطل.

---

*لا رقمَ في هذا الملف من المعايرة الجديدة. كلُّ رقمٍ ورد أعلاه إمّا من سجلّ البوت الحيّ
(‏§⓪) أو من نتيجةٍ منشورةٍ سابقًا وموسومةٍ بمصدرها.*

---

## 📌 ملحق ① — ذراعُ السعة (‏2026-08-06)

> 🔒 **مكتوبٌ ومدفوعٌ قبل قراءة مُخرَج المعايرة.** التشغيلة `31070365317` اكتملت
> ‏04:24:22 UTC، **ولم أفتح سجلَّها عند كتابة هذا الملحق** — لا حوافّ ولا التقاط ولا كلفة.
> السبب: مسحُ المستودعات الخارجية أخرج اعتراضًا **يمسّ المعيار نفسَه**، فتأجيلُه إلى ما
> بعد الأرقام كان سيجعله «معايرةً بعد النظر».

### الاعتراض

بسعةٍ قاطعةٍ ومُرتِّبٍ **مقيسٍ عند مستوى الصدفة**، كلُّ تعديلِ بوّابةٍ **يُقاس داخل ضجيجه**
— وهذا **مقيسٌ مرّتين عندنا**: `M4-LADDER` (‏فرقٌ مجمَّع ‏+1.76 نقطة بـ`z=0.72` · `p≈0.47`)
و`T-CORE5` (‏الخام نجح والمحفظة انقلبت في السنة الثالثة). و`T-FAISAL-ONLY` **توسيعُ بِركةٍ
بطبعه** ⇒ لو قِيست على البِركة وحدها لأنتجت «لا حكم» بعد أسابيع — وهي النتيجةُ نفسُها التي
دفعنا ثمنها ثلاث مرّات.

### والرقمُ الذي يحسمه — مقيسٌ اليومَ من القائمة الحيّة

| | القيمة |
|---|---|
| معدّلُ وصول المرشّحين الآن | **0.71 اسم/يوم** (‏12 اسمًا في 17 يومًا) |
| `WATCHLIST_SIZE` | **10** |
| ⇒ الحالة | **تجويعٌ لا ازدحام — السعةُ لا تقطع اليوم أصلًا** |
| وما يفعله الظرفُ المتوقَّع | يرفع الوصول إلى **عشراتٍ يوميًّا** ضدّ 10 خانات |

🔴 **فالنتيجةُ البنيويّة: `T-FAISAL-ONLY` لا يحلّ مشكلةً — بل يُبدّلها.** ينقلنا من
**تجويعٍ** (‏0.71/يوم · التقاطٌ صفر من 209 متحرّكًا) إلى **ازدحامٍ** يصير فيه المُرتِّبُ —
وقد قِيس عند الصدفة — **هو مَن يقرّر ‏~99% ممّا يصل المالك**. وقياسُ «الالتقاط على البِركة»
حينئذٍ **يقيس ما لا يصل**.

### التعديل على §③ و§④

1. **مقياسٌ حاكمٌ جديد `M6` = الالتقاط على المُسلَّم** — نسبةُ رموز B التي **تدخل القائمة
   فعليًّا** بعد `rank_key` والسعة، لا التي يقبلها الظرف. **و`M6` هو الحكم؛ و`M1` (الالتقاط
   على البِركة) يبقى وصفيًّا يُنشَر ولا يُحكَم به.**
2. **ذراعان في تسجيلٍ واحد** — الحوافُّ المقيسة من الكاتالوج **×** السعة:
   **`S0` = السعة الحالية (10)** · **`S1` = سعةٌ مرفوعة** (القيمةُ **قرارُ مالك**، وتُثبَّت
   **قبل** التشغيل لا بعده). ويُنشَر `M6` لكلٍّ.
3. **المعيارُ في §④ يُقرأ على `M6` لا على `M1`.** وعتباتُه كما هي بلا تحريك.
4. **وشرطُ صدقٍ مضاف:** إن كان `M6` عند `S0` **صفرًا أو قريبًا منه** بينما `M1` عالٍ،
   فالحكمُ الصريح **«الظرفُ يعمل والتسليمُ لا»** — ويُنشَر بهذي الصيغة، ولا يُقدَّم نجاحًا.

### وحدُّ صدقٍ يُضاف إلى §⑦

**‏(9)** رفعُ السعة **ليس مجّانيًّا ولم يُقَس**: القائمةُ الثابتة تُشطب بالوقف فقط، فرفعُ
السعة يُطيل ذيلَ الأسماء المحمولة ويزيد ضجيجَ الرسالة اليومية. **ولا يُنفَّذ في الإنتاج**
بهذي التجربة — يُقاس فيها **محاكاةً** فقط.

### 🌐 وسياقٌ من خارج المستودع (يُسجَّل ولا يدخل المعيار)

مسحُ اثني عشر مستودعًا خارجيًّا (‏بأمر المالك 2026-08-06) انتهى إلى **صفر مرشَّحٍ يمسّ
اختناقاتنا**: عشرةٌ منها أدواتُ وكلاءَ وقوائمُ موارد، و`qlib` **سقطت الصلةُ** بأسبابٍ تقنيّة
(‏لا `learning-to-rank` حقيقيّ — `loss not in {"mse","binary"} → NotImplementedError` أي
انحدارٌ ثم فرزٌ بالدرجة = ما نفعله · و`TopkDropoutStrategy` غيرُ قابلةٍ للعزل · و`n_drop`
يخالف قرارَ المالك «تُشطب بالستوب فقط» · و`Rank IC` بلا معنًى على مقطعٍ يوميٍّ بحجم 1.4 اسم).
**و`Kronos` لم يُفحَص** (سقط وكيلُه بعطل تقنيّ) ⇒ **لا حكمَ له، ويُعاد فحصُه** — ولا يُبنى
عليه شيءٌ في هذي التجربة.

---

## 🔴 ملحق ② — تصحيحُ تشخيصٍ في ملحق ① (‏2026-08-06)

> يُكتب **بعد** قراءة مُخرَج المعايرة `31070365317` — ويُصرَّح بذلك. **ولا يمسّ المعيار
> في §④ ولا العتبات**؛ يُضيف **شرطَ صلاحية** ويُصحّح رقمًا كتبتُه أنا.

### ما قلتُه وما هو صحيح

كتبتُ في ملحق ①: «**الاختناقُ اليومَ تجويعٌ لا ازدحام**» بدليل معدّل وصولٍ **0.71 اسم/يوم**.
🔴 **والتشخيص خاطئ.** ‏0.71 هو **ناتجُ الانسداد لا مقياسُ العرض**:

| المقيس (‏2026-08-06 · من القائمة الحيّة وسجلّ فرز 08-05) | القيمة |
|---|---|
| كونٌ صالح | 3380 |
| **اجتاز البوّابات الأربع عشرة** | **10** |
| ‏M14 (فلوت) تحذف | 7 |
| ⇒ **مؤهَّلٌ فعليّ/يوم** | **3** |
| `_slot_holders` = النشطون **بلا `hit`** | **10** |
| `space = WATCHLIST_SIZE(10) − 10` | **صفر** |
| قائمة الارتداد | **15 / 15** ⇒ `space` = **صفر** |
| ⇒ **المُضاف اليوم** | **صفر** |

⇒ **العرضُ ثلاثةٌ يوميًّا والبابُ مغلق.** والصفةُ الصحيحة **انسدادٌ (blockage)** لا تجويع.

### والأثقل: ‏7 من العشرة «خرجوا من النموذج»

سبعةٌ من حاملي الخانات العشرة وسمُهم **`exited`** — `KLXE · NEGG · TRON · PSTV · LENZ ·
APVO · QNTM` — أي **لم يعودوا ارتكازًا**، ويُحمَلون عمدًا لمتابعة مركز المالك (قرار
2026-07-21). **لكنّهم يحجزون 70% من السعة**، وبعضُهم **لا يمكن أن يعود للترشيح أبدًا**
بعد أن صارت M14 بوّابةً صلبة (قرار 2026-07-29).

### شرطُ الصلاحية المُضاف (‏V-SPACE)

🔴 **بينما `space = 0` فإن `M6` — المقياسُ الحاكم — يساوي صفرًا بنيويًّا مهما بلغ الظرف.**
فالبِركةُ تتّسع إلى ‏≈93/يوم والمُسلَّمُ يبقى **صفرًا**، وقياسُه حينئذٍ **قياسُ الانسداد
لا قياسُ الظرف**.

⇒ **لا يُنشَر أيُّ رقمِ `M6` ما لم يُصرَّح معه بقيمة `space` التي قِيس عندها.** وذراعُ
السعة في ملحق ① تُقرأ الآن على أنها **ذراعُ فتحِ الباب** لا مجرّد رفعِ رقم:
- **`S0`** = الحالة الراهنة (‏`space=0` ⇒ `M6=0` **متوقَّعٌ سلفًا، ولا يُقرأ فشلًا للظرف**).
- **`S1`** = الباب مفتوح — بأيٍّ من طريقين تُثبَّت **قبل** التشغيل: رفعُ `WATCHLIST_SIZE`،
  أو **عدمُ حجزِ المحمولين (`exited`) للخانات**. **والاختيارُ بينهما قرارُ مالك.**

### وتصحيحٌ لجهةٍ أخرى — لئلّا يُنقَل عيبًا وليس عيبًا

رُصد في الجرد أن سهمًا في القائمة كسر وقفَه وسيُسجَّل «خروجًا رابحًا». **وهذا سلوكٌ
مقصودٌ وموثَّق في الكود نفسه** (`Super_stock.py:9976-9979`): صيانةُ القائمة تشطب بالوقف
حتى بعد الهدف، **وسجلُّ الأداء يتجاهل الوقفَ بعد الهدف ويسجّله ربحًا لأن المتداول جنى عند
`t1`**. ⇒ **ليس عطلًا.** لكنّه **يرفع نسبةَ النجاح المُعلَنة عن قراءةِ «اشترِ واحتفظ»**،
وهو **نفسُ بابِ افتراضِ التنفيذ** الذي يجعل `R` يتأرجح بين ‏+0.06 و‏+1.11 حسب مرجع الدخول
⇒ يُذكر في §⑦ حدًّا لا يُحذف.

---

## 🔴 ملحق ③ — `F4` مكذَّب · ودائريّةٌ في عيّنة المعايرة (‏2026-08-06)

### ‏① `F4` مكذَّب — بحقيقةٍ مستقلّةٍ عن الأرقام

توقّعتُ في §⑥ أن **تقاطع A مع B ≤ 3 رموز**. **والمقيس 11:**
`AZI · DSY · EHGO · GWAV · INLF · JEM · KUST · LNAI · NUWE · SPRC · ZCMD`.
⇒ **B تصير 198 رمزًا** بعد الاستبعاد (كان 209).

⚖️ وهذي **حقيقةُ عضويّةٍ** تُحسب من قائمتين مخزَّنتين — **لا تعتمد على أيّ رقمٍ من
المعايرة** ⇒ تسجيلُها الآن تصحيحٌ لا معايرةٌ بعديّة. **والتنبّؤ يبقى مكتوبًا مكذَّبًا**
ولا يُحذف. 📌 وسببُه مفهوم: كاتالوجُ فيصل أسهمٌ **انفجرت**، وB **كلُّ من انفجر** في
النافذة ⇒ التقاطعُ متوقَّعٌ بنيويًّا، وتقديري كان ساذجًا.

🔒 **وشرطُ الاستبعاد يبقى إلزاميًّا**: أيُّ رمزٍ في A يُحذف من B ويُعلَن — وإلّا قِيس
الظرفُ على ما عُوير عليه.

### ‏② دائريّةٌ في عيّنة المعايرة — تُعرَض ولا تُحسَم ذاتيًّا

ثلاثةٌ من الـ28 المُعايَر عليها — **`APVO` · `CMTL` · `KLXE`** — موسومةٌ في
`borrow_labelled_set.md` بـ**`bot_selected`** أي **اختارها فارزُنا** لا فيصل، وليس لها
ذكرٌ في ملفّات مصادره. ⇒ **دائريّة**: نُعاير «معايير فيصل» على أسهمٍ اختارها نظامُنا،
فنقيس أنفسنا جزئيًّا. وهي **من صنف `HTZ`** الذي استُبعد لسببٍ مماثل.

⏸️ **ولا أستبعدها ذاتيًّا** — الاستبعادُ بعد رؤية الأرقام قرارٌ يُغيّر النتيجة، **وقرارُ
العضوية للمالك**. تُنشَر النتيجةُ **بالحالتين** (‏28 و25) إن أذن، وإلّا فبـ28 مع **إعلان
الدائريّة في كل رقم**.

### ‏③ ثلاثةُ عيوبٍ في أداة القياس — تُصلَح قبل قياس `M6`

1. **الحوافُّ الجديدة لم تُلتقَط:** `envelope_p90.json` المدفوع ما زال على
   `run_id=31038415188` بينما المعايرة النظيفة `31070365317`. **تُجمَّد ببصمةٍ جديدة.**
2. **حارسُ `V3` يحرس رقمًا لا وجود له:** `envelope_bt.py:51` = `2dc8c7afb0db`
   و`envelope_prereg.md` يُعلن `bbc3cfde3e92` ⇒ **الكودُ والوثيقةُ مختلفان أصلًا**.
   يُوحَّدان على البصمة الجديدة بتعديلٍ مؤرَّخ.
3. **مسارُ الكلفة يغطّي 89.2% لا 100%:** `_step = len(universe) // cap` ⇒ بـ600 و3357
   ينتهي عند الفهرس 2995، **والكونُ أبجديّ** ⇒ **ذيلُ الأبجدية مهمَل** والتعليقُ فوقه
   يدّعي مسحَ الكون. الصحيح `ceil`. ⇒ رقمُ **‏≈93 مطابق/يوم** المنشور **أرضيّةٌ لا سقف**.
4. **شاهدُ الضبط ملوَّث:** يستبعد الكاتالوج وحده ⇒ **B داخله**. يُستبعَد `A ∪ B`.
5. **الشبكةُ الزمنية مختلفة:** الالتقاطُ نافذةٌ شخصيّة قبل مِرساة كل رمز، والكلفةُ آخرُ
   20 جلسةً للجميع ⇒ يلزم شاهدٌ **مطابَقُ التاريخ**.
6. **`explosion_onset` بلا حدِّ مسافة** ⇒ قاعدةٌ بقيعانٍ صاعدة قد تُرجع مِرساةً أبعدَ
   بكثير. **يُعلَن حدًّا ولا يُصلَح الآن** (إصلاحُه يبدّل الحوافّ مرّةً ثالثة)، ويُضاف
   **عدّادُ «كم جلسةً من النافذة داخل الحركة؟»** لكشفه وصفيًّا.

### ‏④ وذراعُ السعة `S1` **مثبَّتةٌ الآن بقرار المالك**

**‏`S1` = «المحمولون (`exited`) لا يحجزون خانة»** — لا رفعُ `WATCHLIST_SIZE`.
الأثرُ المحسوب على قائمة اليوم: `space` يصير **7** بدل صفر. **وهي ذراعُ محاكاةٍ في
التجربة؛ وتطبيقُها في الإنتاج قرارٌ منفصل** يستوجب `LOGIC_VERSION` و`force_renew=1`.

---

## 🔒 ملحق ④ — قرارُ المالك: **‏25 فقط** (‏2026-08-06)

> **نصُّ القرار:** «لا خلّنا على 25 فقط، **ما عندي مجال للمخاطرة أبدًا**».
>
> ⚖️ **واتُّخذ قبل رؤية أيّ رقمٍ من ذراع الـ25** — التشغيلةُ كانت جارية ومُخرَجُها
> لم يُقرأ، لا منّي ولا منه. ⇒ **ليس معايرةً بعد النظر**، وهو أنظفُ شكلٍ ممكنٍ لحسم
> عضويةِ عيّنة.

### ما يُنفَّذ

`APVO` · `CMTL` · `KLXE` تُضاف إلى `EXCLUDED_BY_OWNER` ⇒ الكاتالوج **‏29 مرشَّحًا**
(‏35 ناقص ستّة) ومنه تُعاير الحوافُّ النهائية.

**السند — فحصٌ مباشر لا نقلٌ عن وكيل:** الثلاثة موسومون في `borrow_labelled_set.md`
بـ**`bot_selected`** (اختارهم فارزُنا)، و**صفرُ ذكرٍ لهم في كلّ ملفّات مصادر فيصل**
مقابل `JZ` **‏37** إشارة و`ONCO` **‏18**.

### ولماذا **بنيويًّا** لا بوسيط تشغيل

لو بقي الاستبعادُ في `ENV_SYMBOLS` لعاد الكاتالوجُ إلى 28 **صامتًا** في أوّل تشغيلةٍ
تُنسى فيها الوسيطة. ⇒ **يُقتَل الصنفُ لا الحالة** — وهو نفسُ مبدأ `FO11`.

### وما يسقط بهذا القرار

- **لا تُنشَر ذراعُ الـ28 حكمًا ولا فحصَ حساسية.** كنتُ اقترحتُ نشرَ الاثنتين؛
  **المالك رفض**، والسببُ وجيهٌ ويُسجَّل: نشرُ رقمين متجاورين يفتح بابَ اختيارِ
  الأنسب لاحقًا. **والرقمُ النافذ واحد.**
- **الحوافُّ `a454cd33c848` (‏28 رمزًا) مسحوبةٌ من موضع القرار** — تبقى في التاريخ
  موسومةً «مُعايَرة على عيّنةٍ فيها دائريّة»، ولا يُبنى عليها.
- **تشغيلةُ `T-FAISAL-ONLY` الجارية (`31077713855`) تقيس الظرفَ القديم** ⇒ نتيجتُها
  **لا تُعتمَد**، وتُعاد على الظرف الجديد. (‏وهي **تُسمّي بصمتَها في ترويستها** فلا
  تُخلَط بغيرها — وهذا بالضبط ما بُنيت الترويسةُ له.)
