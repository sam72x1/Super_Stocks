# 📏🔊 `T-VOLBASE` — «سجل مرجع الحجم» (تسجيلٌ مسبقٌ · أمرُ المالك 2026-08-29)

> 🔴🔴 **يُدفَع قبل سطرِ أداةٍ واحد.** ولا يُقرأ رقمٌ ولا يُفتَح سجلُّ تشغيلةٍ
> قبل أن يكون هذا الملفُّ على `origin/main`.

---

## ⓪ الدافع — عطبٌ **مقروءٌ من الكود** لا مُفترَض

بوّابةُ المِرساة تحسب قفزةَ الحجم هكذا (‏`Super_stock.liq_stage_events` ومرآتُها
`gate_probe.gate_trace`):

```
win    = bars[i + 2 - W : i + 2]        # W = LIQ_WINDOW_MIN = 65
closed = win[:-1]
prior  = [b["v"] for b in closed[:-1]]  # ‏≤63 شمعةً سابقة
avg    = sum(prior) / len(prior)
vx     = last["v"] / avg                # الشرط: vx ≥ 3.0
```

⇒ **المرجعُ نافذةٌ متدحرجة تدخلها شمعاتُ الركضة نفسُها** ⇒ كلَّما استمرّت
الحركةُ ارتفع مقامُها فانهار `vx`. **نموذجٌ بسيط** (هادئ 1000 · حارّ 5000 ·
‏63 شمعةً في المرجع):

| دقيقةُ الركض | ‏0 | ‏5 | ‏10 | **‏11** | ‏20 |
|---|---|---|---|---|---|
| `vx` | ‏5.00 | ‏3.79 | ‏3.06 | **‏2.95 ❌** | ‏2.20 |

⇒ **بعد ‏≈11 دقيقةً من الركض يستحيل بنيويًّا عبورُ بوّابة الحجم** مهما بلغت
السيولة الداخلة. ⚠️ **وهذا نموذجٌ حسابيٌّ لا قياسُ سوق** — والتجربةُ تقيس **كم
مرّةً يقع فعلًا**، ولا يُقدَّم النموذجُ نتيجةً.

🔗 **وصلتُه ببلاغ المالك ③** (‏2026-08-28): «معطيني تنبية اول دقيقة وانه مرتفع
‏70٪ … توه يعتبره توها الان تبدا تدخل السيولة» — الزاحفُ الهادئ لا يُطلق دقيقةً
عنيفة، **ومتى أطلقها كان مرجعُ حجمه قد ابتُلع أصلًا**.

## ⓪-ب ولماذا ليست تكرارًا لـ`T-CUMRISE`

`T-CUMRISE` وسّعت **نافذةَ الرفعة** (‏`rise` على 3 أو 5 دقائق) و**فشلت** (‏`R1`
تأخّرٌ ‏−6.7 · إثمار **‏−9.7** نقطة · إشعاراتٌ **‏+80%**). وهذي تُغيّر **مرجعَ
الحجم** — محورٌ **لم يُمَسّ في أيٍّ من 32 تسجيلًا مسبقًا في المستودع**.
🔑 **وفرقٌ بنيويٌّ يُعلَن قبل الأرقام: `T-CUMRISE` كانت «تخفيفًا رتيبًا»**
(نافذةٌ أوسع ⇒ مرشّحون أكثرُ حتمًا)، **وهذي «إعادةُ إسناد» قد تشدّ أو ترخي بحسب
شكل النافذة** — واتّجاهُها **يُقاس ولا يُفترَض**.

⛔ **وإن خرجت بالنمط نفسِه (تأخّرٌ أقلّ · إثمارٌ أسوأ · ضجيجٌ أكثر) فهي تكرارٌ
يُعلَن لا اكتشاف** — بنصّ سابقة `T-RECLAIM-INTRADAY §⓪`.

---

## ① السؤال

هل **مرجعُ حجمٍ لا تبتلعه الحركةُ نفسُها** يُقدّم المِرساةَ ماديًّا **بلا** أن
يهدم جودةَ الإشعار ولا أن يُغرق القناة؟

## ② الأذرعُ الأربع — **ولا خامسة** (‏`p-hacking`)

| ذراع | المرجعُ (المقام) | الوسم |
|---|---|---|
| **`B0`** | `mean(prior)` — **الإنتاج كما هو** | الأساس |
| **`B1`** | `median(prior)` — **نفسُ النافذة، إحصاءٌ صامد** | 🟢 قابلٌ للتوصية |
| **`B2`** | `prev_day_volume / 390` — **جلسةٌ مكتملةٌ سابقة** | 🟢 قابلٌ للتوصية |
| **`B3`** | `min(B0, B2)` — **الأدنى منهما** | ⛔ حدٌّ أعلى **لا اقتراح** |

⚖️ **وصفرُ رقمٍ مخترَع:** المضاعفُ يبقى **‏3.0** (‏`IGNITION_VOL_MULT` — رقمُ
المالك) · والنافذةُ تبقى **‏65** · و`B1` **إحصاءٌ آخرُ على البيانات نفسِها**
(صفرُ نداءٍ إضافيّ) · و`B2` مقامُه **‏390 = طولُ الجلسة النظاميّة** (حقيقةُ
سوقٍ لا عتبةٌ مُعايَرة · وسمُه `engineering` في دفتر المصادر) **وحجمُ الأمس
مجلوبٌ سلفًا** في `prev_close` ⇒ صفرُ نداءٍ إضافيّ · و`B3` **اتّحادٌ رتيب** لا
يكتم ما يُطلقه اليوم (نمطُ `R3` في `T-CUMRISE` و`G6` في `T-GATE`).

🔴 **واتّجاهُ `B1` ليس مضمونًا:** في نافذةٍ أغلبُها هادئٌ بقفزاتٍ قليلة يكون
الوسيطُ **أدنى** من المتوسّط (فيرخي)، وفي نافذةٍ أغلبُها حارٌّ بدقائقَ ميّتة
يكون **أعلى** (فيشدّ). **يُقاس ويُطبَع، ولا يُدَّعى قبل القياس.**

🔴 **واتّجاهُ `B2` مُعلَنٌ مشكوكًا فيه:** مراسينا **أغلبُها بريماركت**
(‏`T-KASIH`: ‏19-23% لبريماركت مبكر مقابل 8-9% لما بعد الظهر)، ودقائقُ
البريماركت أرقُّ بمراتب من متوسّط الجلسة النظاميّة ⇒ **`B2` قد يكون أشدَّ
هناك فيفقد متحرّكين** — وهو تنبّؤٌ مسجَّلٌ يُكذَّب أو يتحقّق.

## ③ المقاييس — **مُعادةٌ حرفيًّا** من `T-GATE`/`T-CUMRISE`

1. **التأخّر** (الحاكم) = وسيطُ ارتفاع السهم عن **إغلاق الأمس** لحظةَ المِرساة.
2. **الإثمار** = نسبةُ الإشعارات التي بلغ سعرُها **‏+3%** خلال 15 دقيقةً بعدها.
   (‏3% لا 5% **عمدًا** — البوّابةُ تشترط 5% **قبل** الإشعار فقياسُها بعده دائريّة.)
3. **الإشعارات** = عددُ الدقائق العابرة (كلفةُ الضجيج).
4. **المتحرّكون المفقودون** = مَن صعد ‏≥`MOVER_PCT`=30% والتقطه `B0` ولم تلتقطه الذراع.

## ④ المعايير الأربعة — **مُعادةٌ بلا تحريك**

ذراعٌ **تُقترَح** إن استوفت **الأربعة معًا**:

| # | الشرط | الحدّ |
|---|---|---|
| ① | التأخّرُ ينزل | **‏5 نقاطٍ فأكثر** |
| ② | الإثمارُ لا يسوء | فوق `JITTER_PP` = **‏3.6 نقطة** ⇒ سقوط |
| ③ | الإشعاراتُ لا تزيد | فوق **‏+50%** ⇒ سقوط |
| ④ | **صفرُ متحرّكٍ مفقود** | فقدُ واحدٍ ⇒ سقوط |

🔒 **ولا يُحرَّك حدٌّ بعد رؤية رقم** — والحدودُ الأربعةُ منقولةٌ من
`cumrise_prereg.md §④` حرفيًّا لتكون النتيجةُ **قابلةً للمقارنة** بها.

## ⑤ بوّاباتُ الصلاحية — تسبق أيَّ تفسير

| بوّابة | الشرط | عند السقوط |
|---|---|---|
| **`V0`** | `B0` يُعيد صفَّ `R0` من **`cumrise_result.md §①`** **بت-بت**: ‏29 سهمًا مُنبَّهًا · تأخّر **‏+20.5%** · إثمار **‏44.8%** (مقام 29) · **‏80** إشعارًا · **‏13/15** متحرّكًا · وتغطية **‏307 من ‏314** | **خروج 3** — «عطبُ أداةٍ لا نتيجة» |
| **`V1`** | الأذرعُ الأربعُ **تتفرّق** (عدّاداتٌ متطابقةٌ بت-بت = بصمةُ `no-op`) | **خروج 4** |
| **`V2`** | التغطية: عددُ الرموز يطابق التشغيلةَ المرجعيّة (‏±1) | يُعلَن بعدده |
| **`V3`** | `gate_trace` **بلا** `vol_ref` مطابقةٌ للسلوك الحاليّ ⇒ `G0`-`G6` قابلةٌ لإعادة الإنتاج | قفلٌ **سلوكيّ** في السويّة |

🔴 **ومرجعُ `V0` هو `cumrise_result.md` لا `gate_result.md` — والسببُ تعارضٌ
مقيسٌ يُدوَّن:** الجلسةُ واحدة (‏2026-08-17) **والكونُ مختلف** — `T-GATE` مشى
‏313 سهمًا و**‏16** متحرّكًا فأعطى `G0` تأخّرًا **‏+17.1%**، و`T-CUMRISE` مشى
‏314 و**‏15** فأعطى `R0` **‏+20.5%**. **والرقمان صحيحان كلٌّ لمجموعته**، وخلطُهما
هو بعينه عيبُ «مجموعتان مختلفتان» الموثَّق في `T-GATE §⑨`. ⇒ **الشاسيه المعتمَد
`cumrise_probe` وحدَه** لأنه صاحبُ المعايير الأربعة، **والمقارنةُ تكون به لا
بغيره.**

⚖️ **والميزانيةُ ثابتةٌ ومطبوعة** (قاعدةُ `karpathy/autoresearch` · وعيبُ
`T-CLIFF` المقيس): **الجلسةُ نفسُها (‏2026-08-17)** والكونُ نفسُه والنافذةُ
نفسُها والسقوفُ نفسُها للأذرع الأربع — فالفرقُ أثرُ الذراع لا أثرُ الميزانية.

## ⑥ سقفُ النجاح — **مثبَّتٌ سلفًا**

- **اقتراحٌ للمالك ومنحنى كلفة — لا تغييرَ آليٌّ لأيّ عتبة.**
- ⛔ **ولا مسَّ بأرقامه:** ‏5% (`LIQ_MIN_MOVE_PCT`) · ‏$30,000 (`LIQ_MIN_USD`) ·
  ‏3× (`IGNITION_VOL_MULT`) · ولا بالنافذة 65 ولا بالتصنيف ولا ببوّابة كتم.
- 🔴 **وعبورُ الأربعة ليس اعتمادًا:** هو **مرشَّحٌ لقياس ناتجٍ على ثلاث سنوات**
  بأنبوبة `kasih_scan` (‏`resolve` بت-بت) — **ولا اعتمادَ قبله** بنصّ قاعدة
  «الحدُّ الأدنى ثلاثُ سنوات».

## ⑦ تنبّؤاتٌ قابلةٌ للكسر — تُنشَر مكذَّبةً كانت أو محقَّقة

| # | التنبّؤ | ما يُكذّبه |
|---|---|---|
| **`VB-P1`** | 🔴 **الأخطرُ عليّ:** لا ذراعَ تعبر الأربعة — النمطُ نفسُه (تأخّرٌ أقلّ · إثمارٌ أسوأ · ضجيجٌ أكثر) | عبورُ `B1` أو `B2` |
| `VB-P2` | `B1` **يفير أكثر** من `B0` مجمَّعًا · **ولا يفير أكثرَ في كلّ نافذة** | تطابقُ الاتّجاه في 100% من النوافذ |
| `VB-P3` | `B2` **يفقد متحرّكًا** (أشدُّ في البريماركت) ⇒ ④ يسقط | صفرُ مفقود |
| `VB-P4` | `B3` صفرُ مفقودٍ **بالبناء** لكنه يتجاوز **‏+50%** إشعارًا ⇒ ③ يسقط | إشعاراتُه دون +50% |
| `VB-P5` | انخفاضُ تأخّر `B1` بين **‏3 و8 نقاط** (فقد لا يبلغ حدَّ الخمس أصلًا) | خارج النطاق |

## ⑧ عدّاداتٌ وصفيّةٌ تُطبَع ولا تحكم

- **حجمُ الظاهرة:** كم دقيقةً عبرت **كلَّ** الشروط **عدا** قفزةَ الحجم · وكم
  منها `vx` بين **‏2.0 و3.0** («كادت») ⇒ يقيس الابتلاعَ مباشرةً.
- **تفكيكُ الجلسة:** التأخّرُ والإشعاراتُ **بريماركت مقابل نظاميّ** لكلّ ذراع
  (اختبارُ `VB-P3` بعينه).
- **اتّجاهُ `B1`:** نسبةُ النوافذ التي كان فيها الوسيطُ **أعلى** من المتوسّط.

## ⑨ حدودُ صدقٍ — تُقرأ **مع** النتيجة لا بعدها

1. **جلسةٌ واحدة** (‏2026-08-17) — أُعيدت عمدًا لتكون قابلةً للمقارنة بـ`T-GATE`
   و`T-CUMRISE` تحت الميزانية نفسِها. **ولا تُعمَّم على السوق.**
2. **«أثمر» لمسُ ‏+3% لا ربحٌ محقَّق** ⇒ سقفٌ متفائل.
3. 🔴 **وسيطُ التأخّر يُقاس على مجموعتين مختلفتين** (عيبٌ موثَّقٌ في `T-GATE`):
   يتحرّك بدخولِ سهمٍ جديدٍ لا بتحسّنِ قائم.
4. **انحيازُ بقاء** (كونُ ناسداك اليوم) · **وتشويهُ التقسيمات** (‏`LATE_SANE_MAX`).
5. **‏390 طولُ الجلسة النظاميّة**، ومراسينا كثيرًا **خارجَها** ⇒ `B2` ليس
   «متوسّطَ دقيقةٍ مقارِنة» تمامًا — يُعلَن ولا يُجمَّل.
6. **مقامُ الإثمار صغير** (‏20-42 إشعارًا في `T-GATE`) ⇒ **اتّجاهٌ لا رقمٌ دقيق**.
7. 🔴 **وهذا قياسُ اكتشافٍ لا قياسُ ربحية** — يجيب «متى نراه؟» لا «هل نربح منه؟».

## ⑩ ما لا يُمَسّ مهما كانت الأرقام

`Super_stock.liq_stage_events` وبوّاباتُ `M1`-`M14` والجذورُ التسعة و
`kasih_scan.resolve` ودرعُ صيّاد المقسّم — **بت-بت**. والتجربةُ كلُّها في
`volbase_arms.py` (قراءةٌ فقط · بلا كرون · الإنتاجُ لا يستوردها).

---

## ⑪ ملحقٌ مؤرَّخ 2026-08-29 — **`V0` كما سُجِّل غيرُ قابلٍ للاستيفاء بنيويًّا**

🔴🔴 **يُكتَب قبل رؤية أيّ رقمِ ذراع.** التشغيلةُ `33241951710` خرجت عند `V0`
**قبل الجدول** فلم يُقرَأ `B1`/`B2`/`B3` إطلاقًا — والسجلُّ شاهدٌ على ذلك.

**والسببُ مقروءٌ من الكود لا مُستنتَج:** `operator_entry_live._load_universe`
يبني الكونَ من **حالةٍ حيّةٍ يعيد البوتُ كتابتها كلَّ دقائق** (‏`weekly_watchlist`
· `near_watch` · `press_radar_state` · `hunter_watchlist` · `op_entry_state`) ⇒
**إعادةُ تشغيلِ الجلسة نفسِها لا تُعيد الكونَ نفسَه**: ‏**382** سهمًا اليوم مقابل
**‏314** في التشغيلة المرجعيّة · و**‏21** متحرّكًا مقابل **‏15** · و374 ببيانات
مقابل 307.

⇒ **`V0` مستحيلٌ استيفاؤه لأيّ تشغيلةٍ بعد يوم المرجع** — وهو صنفُ «حارسٌ لا
يمكن أن يمرّ» الموثَّق (بوّابةُ `E2`)، **وعيبُ تصميمٍ منّي يُعلَن لا يُطوى.**

### ✅ البديل `V0-ب` — يُثبَّت الآن **قبل أيّ رقم**

`B0` يطابق **دالّةَ الإنتاج `Super_stock.liq_stage_events` نفسَها** على
**بياناتِ السوق الحقيقيّة لكلّ رمزٍ في الكون** — مِرساةً بمِرساة، **وتفرّقٌ
واحدٌ ⇒ خروج 3** («عطبُ أداةٍ لا نتيجة»).

⚖️ **وهو أقوى في نوعه** من مطابقة رقمٍ منشور: يقارن على **مئات الرموز الحقيقيّة**
لا على ستّ عيّناتٍ مصطنعة، ويُثبت أن إضافة `vol_ref` **لم تُزحزح الإنتاج بحرف**.
🔴 **وأضعفُ في نوعٍ آخر**: لا يربط النتيجةَ برقمٍ منشورٍ سابق — **والطرفان
يُعلَنان ولا يُختار أحدُهما للتجميل.**

### 🔴 وتُسحَب دعوى المقارنة الخارجية من `§⑤`

قولي «الجلسةُ نفسُها ⇒ الأرقامُ قابلةٌ للمقارنة بـ`T-CUMRISE`» **باطلٌ** لأن
الكونَ اختلف ⇒ ⛔ **لا تُقارَن أرقامُ هذي التجربة عدديًّا بـ`cumrise_result.md`.**
✅ **والمقارنةُ الداخليّة سليمةٌ كما هي** (‏`B1`/`B2`/`B3` مقابل `B0` في
التشغيلة نفسِها بميزانيةٍ واحدة) — **وهي بعينها ما تقيسه المعاييرُ الأربعة**،
فالتجربةُ لم تُصَب في جوهرها.

### 📌 وأثرٌ رجعيٌّ يُدوَّن ولا يُطوى

أرقامُ `T-GATE` و`T-CUMRISE` **غيرُ قابلةٍ لإعادة الإنتاج** بالسبب نفسِه —
كونُهما حالةٌ حيّةٌ زالت. **تبقى صالحةً مقارنةً داخليّة** (وهو ما قام عليه
حكمُهما فعلًا) **ولا تصلح مِرساةً لتشغيلةٍ لاحقة** — وأيُّ تجربةٍ قادمةٍ تبني
حارسَها على **دالّة الإنتاج** لا على رقمٍ منشور.

### ⛔ وما لم يتغيّر

المعاييرُ الأربعةُ وحدودُها · والأذرعُ الأربع · وسقفُ النجاح · والتنبّؤاتُ
الخمسة — **كلُّها بحرفها**، ولم يُقرأ رقمُ ذراعٍ واحدٍ قبل كتابة هذا الملحق.
