# 🌙 `T-AH` — قياسُ «عمى الافتر» من الملفّات المجمَّعة (تسجيلٌ مسبق)

> **يُكتَب ويُدفَع قبل أيّ رقم.** أمرُ المالك 2026-08-12: **«شغّل الافتر»** — أوّلُ
> استعمالٍ حقيقيّ لمفاتيح S3 بعد أن أثبت المِجَسُّ الوصولَ (‏`31565975248`).
> **مِجَسُّ جدوى وقياسُ أساسٍ — لا تجربةَ حكم:** لا قاعدةَ قرارٍ على الطاولة، ولا
> يُعتمَد منه شيءٌ ولا يُقترَح. وأيُّ تعديلِ عتبةٍ لاحقٍ **قرارُ مالكٍ بتسجيلٍ جديد**.

## ① العيبُ الذي وُلد منه (مُثبَتٌ حيًّا لا مُفترَض)

الفرزُ والصيّادون يقرأون **الشمعةَ اليومية** من ياهو، **وهي لا تشمل الجلسةَ الممتدّة**
— حدُّ صدقٍ مدوَّنٌ في `CLAUDE.md` منذ 2026-07-27 **بقي سطرًا حتى انفجر عليه سهم**:
`NUWE` ارتفع **‏+100% في الافتر**، **فأعاد صيّادُ المقسّم ترشيحَه في صباح اليوم التالي**
(تشغيلة `30513556819`) لأن فرزَه على الإغلاق الرسميّ **أعمى عمّا حدث بعده**.
والعلاجُ القائم `ah_guard` **حارسٌ حيٌّ فاشلٌ-آمنٌ مفتوح** بعتبة `SPLIT_ROSE_MAX_PCT`
‏= 20% — **وعتبتُه لم تُقَس قطّ**، ومدوَّنٌ صريحًا: «⏸️ تعديلُ العتبة قرارُ مالك».

⇒ **ما تضيفه الملفّاتُ حصرًا: التاريخ.** `us_stocks_sip/minute_aggs_v1` يشمل الجلسةَ
الممتدّة ‏23 سنة ⇒ يصير الحارسُ **قابلًا للمعايرة برقمٍ مقيس** بدل التخمين.

## ② الأسئلةُ المسجَّلة (تُقاس · ولا يُضاف سؤالٌ بعد الأرقام)

- **`Q1` كم يقع خارج النظاميّ؟** لكلّ رمز-يوم: `post_high / reg_close − 1` ⇒ نصيبُ
  الرموز-الأيام التي تتجاوز **‏10% · 20% · 50% · 100%**، مُبوَّبًا: الكونُ كلُّه مقابل
  **فئتنا** (سعرٌ دون `AH_PRICE_MAX`=$10).
- **`Q2` معايرةُ `ah_guard`:** معدَّلُ الأساس عند عتبة **‏20%** = **كم مرّةً كان
  الحارسُ سيُفعَّل**. (⚠️ **يقيس معدَّلَ الظاهرة لا دقّةَ الحارس** — مرشّحو الصيّاد
  التاريخيون غيرُ محفوظين، وسجلُّه عمرُه يومٌ واحد ⇒ **مُصرَّحٌ به قبل الرقم**.)
- **`Q3` نصيبُ الحجم الممتدّ** من حجم اليوم (وصفيّ).
- **`Q4` الجدوى:** حجمُ يومٍ · زمنُ الاختزال · إسقاطُ سنةٍ **بمعامِلِ توسيعٍ مطبوع**.

## ③ حرّاسُ الصلاحية (بلا استيفائها **لا يُنشَر رقم**)

| # | الحارس | لماذا |
|---|---|---|
| `V1` | **شاهدُ ضبطٍ إلزاميّ** `AAPL`: حجمٌ ممتدٌّ **‏>0 و<20%** من النظاميّ | «الصفرُ عطبُ أداةٍ حتى يُنفى» — درسُ PIT (‏`pip` سحب numpy فقُرئ 0/40 وكاد يُدفَن المشروع) |
| `V2` | **أسماءُ الأعمدة من الترويسة** لا مفترَضة | درسُ «قرأتُ `available` والحقلُ `shares_available` فخرجت 0 من 170» |
| `V3` | **حدُّ الجلسة من `market_calendar.session_info`** لا ‏16:00 ثابتة | يومُ الإغلاق المبكّر (‏13:00) يجعل 13:00-16:00 **ممتدًّا**؛ وتثبيتُ 16:00 يَسِمه «نظاميًّا» = مقياسٌ ثانٍ |
| `V4` | **يتّفق مع `EX.ny_session_key` الإنتاجية** في اليوم النظاميّ | إعادةُ استعمالٍ لا بناء — وإلّا صار عندنا تعريفان للجلسة |
| `V5` | **‏`prev_close` لا يُفترَض:** حركةُ ما قبل السوق **لا تُقاس** إلّا إذا كان اليومُ السابق في العيّنة | مقياسٌ مفبركٌ أسوأُ من غيابِ مقياس |
| `V6` | كلُّ إسقاطٍ من عيّنةٍ **يطبع معامِلَ التوسيع** | درسُ المِجَسّ: قسمُ الكلفة أسقط رقمَ العيّنة كأنه يومٌ كامل فأخطأ ‏12× |

## ④ ما لا يُدَّعى — يُكتَب قبل الأرقام

1. **ليس حكمًا على `T-LABEL-AUDIT`** (قاس تغيُّرَ **وسمِ النتيجة** فوجده ‏3.4% وكذّب
   `L1`). هذا يقيس **الظاهرةَ ومعدَّلَها**، وذاك قاس **أثرَها على الوسم** — سؤالان.
   ⇒ **ولا يُنقَض حكمٌ بقياسٍ لسؤالٍ آخر.**
2. **ولا حافّةَ ولا ربحية:** `post_high` **سعرٌ لُمِس** لا سعرٌ نُفِّذ (سيولةُ الافتر
   رقيقة) ⇒ كلُّ رقمٍ هنا **سقفٌ متفائل**.
3. **ولا مسٌّ بالإنتاج:** صفرُ استيرادٍ في `Super_stock` · صفرُ كتابةِ حالة · صفرُ
   تلغرام · ولا يُغيَّر `SPLIT_ROSE_MAX_PCT` ولا `ah_guard` ولا أيُّ عتبة.
4. **والعيّنةُ يومٌ واحد في التشغيلة الأولى** ⇒ **«لا حكم» بالتصريح**؛ الأرقامُ
   وصفيّةٌ لجدوى، وأيُّ معايرةٍ تلزمها سنةٌ كاملة **بتسجيلٍ جديد**.

## ⑤ رموزُ الخروج (‏«أخضرُ» لا يُقرأ إذنًا)

**‏0** = قِيس والحرّاسُ استُوفوا · **‏2** = تعذّرُ تهيئة (مفتاحان/‏aws) · **‏3** = شاهدُ
الضبط سقط ⇒ **لا رقمَ يُنشَر** · **‏4** = ملفُّ اليوم غيرُ موجود · **‏5** = ترويسةٌ
ناقصةُ الأعمدة.
