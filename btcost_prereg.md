# 🩸📡 `T-BTCOST` — تكلفةُ التنفيذ **مقيسةً على مجتمع المحرّك نفسِه** (تسجيلٌ مسبق)

> **الأمر:** «سجل التكلفة على المجتمع» (‏2026-09-13).
> **الموضع:** `T-FCOST` انتهى إلى **الفرع 2 — لا إغلاق** (سقطت `FC3`)، والبندُ 2 في
> `PENDING_VERIFICATION.md` **باقٍ مفتوحًا للزوج `PR-SWEEP` وحدَه**. و`fcost_prereg.md §⑨`
> سمّى المصدرَ الذي **يُعتَدّ به**: «مصدرُ تكلفةٍ مقيسٌ على هذا المجتمع (‏`NBBO` على
> صفقات الباكتيست نفسِها) — **وإعادةُ عتبةٍ من السلّم ليست مصدرًا**». **وهذا العقدُ
> يُنتج ذلك المصدرَ بعينه** — ولا يدّعي أكثر.
> **الحالة:** بحثٌ/قراءةٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · ولا تتحرّك
> `BT_SPREAD_PCT` ولا `STOP_BELOW_LOW_PCT` ولا أيُّ جذر.
> **هذا الملفُّ مدفوعٌ ومدموجٌ في `main` قبل أيّ سطرٍ من الأداة وقبل أيّ رقم.**

---

## ⓪ ما رأيتُه **قبل** كتابة هذا العقد — إقرارُ تلوّثٍ صريح

**من `T-SLIP` (مجتمعُ المِرساة/الاشتعال):** `P0` انزلاقٌ متوسّطُه ‏1.08-1.28% و`P1`
‏0.72-0.81% (‏≈1.5×) · الحافّةُ ‏+0.689R ⟶ **‏+0.566R** · «الوقفُ الضيّق ينزلق أقلّ».

**من `T-SLIP-NBBO` (المجتمعُ نفسُه، بالعرض القائم):** **‏+0.576R** · **وسيطُ السبريد
‏1.32-1.75%** (وهو بعينه `c*` **المستعار** في `T-FCOST`) · `NBB` عند الوقف أو دونه في
**‏95.6-97.5%** · العامليّةُ **العرضُ القائم ‏−0.114R · التوجيهُ ‏−0.066R · ذيلُ الدقيقة
‏−0.152R** · تغطيةٌ **‏75.8%** · ‏14,052 حدثَ وقف · `scale_mismatch` ‏1360 ·
و**`bid_size` بالـلوتات لا الأسهم** (توثيقُ Polygon).

**من `T-FCOST` (مجتمعُ المحرّك — وهو مجتمعُ هذي التجربة):** `ρ` = **‏0.029126** ثابتًا
هندسيًّا (`1 − 1/1.03`) ⇒ معاملُ 1% = **‏0.3433R** · `E_A(0,0)` = **‏−0.976 · −0.785 ·
−0.945** نقطة · إشاراتٌ ‏1,620/1,591/1,607 ومحسومةٌ **‏1,381/1,364/1,354** · تعبئةُ `A`
‏0.852/0.857/0.843 و`SW` ‏0.228/0.259/0.235 · **و`FC3` سقطت** بإزاحةٍ ‏×2.55/×2.72/×3.02.

🔴 **وأعلنُ الحدَّ الحاسم: صفرُ رقمِ تكلفةٍ مقيسٍ على هذا المجتمع رأيتُه — لأنه لا
يوجد.** كلُّ ما سبق إمّا مجتمعٌ آخر (‏`T-SLIP*`) أو **استعارةٌ منه** (`T-FCOST`).
وهذا بالضبط ما يجعل التجربةَ **عمياءَ لا تأكيديّة**.

---

## ① السؤال المسجَّل

على **صفقات محرّك الباكتيست نفسِها**، وباستعمال `NBBO` التاريخيّ عند **لحظة التعبئة
الفعليّة ولحظة الخروج الفعليّة**:

1. ما **السبريدُ المقيس** `c` (‏= `(ask − bid) / mid`) الذي يُسعّره المحرّكُ نصفَين؟
2. وما **الانزلاقُ المتبقّي** `s` على ساق الخسارة **بعد** خصمِ نصف السبريد (فلا
   يُحتسَب مرّتين)؟

**والمطلوبُ رقمٌ لهذا المجتمع، لا حكمٌ على زوجٍ ولا سياسةُ تداول.**

---

## ② الميكانيكا — والمادّةُ تُستورَد ولا يُعاد بناؤها

**الكونُ مطابقٌ لـ`T-FCOST` بالبناء:** اللقطاتُ الثلاثُ المجمَّدة `31574165523` ·
`31199493957` · `31191327731` (‏2023 · 2024 · 2025) والأعلامُ نفسُها.

**استرجاعُ اللحظتين — لكلّ صفقةٍ محسومة:**

| الخطوة | المصدرُ المستورَد **بالاسم** |
|---|---|
| فهرسُ التعبئة `filled` | نفسُ تعبير `backtest_symbol` حرفيًّا: أوّلُ شمعةٍ `low ≤ entry` |
| فهرسُ الخروج `k` | **`_arm_a_exit_bar`** من `Super_stock` — **مقفولةٌ بتطابقٍ خاصّيّ مع `_resolve_arm`** (بُنيت لـ`T-REPLAY10`) |
| دقيقةُ اللمس | **`trigger_ms`** من `slip_nbbo_arms` |
| الاقتباسُ السائد | **`fetch_quotes`** ‏+ **`prevailing`** من `slip_nbbo_arms` |
| حارسُ التقسيم | **`raw_factor`** من `slip_nbbo_arms` (‏`raw_scale_unverified`) |

⚠️ **وخطرُ إعادة الاشتقاق يُحوَّل إلى قفلٍ لا إلى وعد:** إعادةُ اشتقاق `filled`/`k`
من شموعٍ تُجلَب ثانيةً **قد تفترق** عن مشية المحرّك. ⇒ **`V-B2`** يشترط أن تُطابق
`outcome` المُعادةُ اشتقاقًا `outcome` المخزَّنةَ في الصفقة **على ‏99% فأكثر** من
المحسومة، **والمفترقُ يُستبعَد ويُعَدّ باسمه `walk_mismatch`** (سابقةُ
`scale_mismatch`). دون ‏99% ⇒ **خروجٌ ولا رقم.**

**اللحظتان:**
- **الدخول:** أوّلُ دقيقةٍ في يوم التعبئة `low ≤ entry`.
- **الخروج:** `loss` ⟶ أوّلُ دقيقةٍ `low ≤ stop` · `win` ⟶ أوّلُ دقيقةٍ `high ≥ t1` ·
  `open` ⟶ آخرُ دقيقةٍ في النافذة.

**المقياسان — بصيغةٍ تمنع الحسابَ المزدوج:**

```
c_i      = (ask − bid) / ((ask + bid) / 2)                    ← ساقٌ واحدة
P_model  = exit_model × (1 − c_i/2)                           ← ما يُسعّره المحرّك
s_i      = 1 − P_real / P_model        حيث P_real = NBB السائدُ عند اللمس
```

🔒 **و`s_i` لا يُقصّ ولا يُحدّ:** القيمةُ السالبة (تنفيذٌ أفضلُ من النموذج) **تُعَدّ
وتُنشَر** (‏`V-B8`) — فقصُّها يصنع رقمًا متشائمًا كذبًا.

**`s` تُقاس على ساق الخسارة وحدَها** — لأن المحرّكَ يطبّقها هناك حصرًا
(`f = (1 − s) if outcome == "loss" else 1.0`).

---

## ③ المُخرَجاتُ المقيسة

| الرمز | المعنى |
|---|---|
| **`C-MED`** | وسيطُ `c` على **كلّ السيقان** (دخولًا وخروجًا)، مجمَّعًا ولكلّ سنة |
| `C-P25` · `C-P75` | الرُّبيعان — **يُنشَران دائمًا** فلا يُقرأ الوسيطُ وحدَه |
| `C-ENT` · `C-EXI` | وسيطُ السبريد لكلّ ساقٍ على حدة (هل الخروجُ أغلى؟) |
| **`S-MED`** | وسيطُ `s` على سيقان الخسارة · ومعه `S-P25`/`S-P75` وحصّةُ السالب |
| `COV` | التغطية = السيقانُ المقيسة ÷ المطلوبة، **وأسبابُ الإسقاط مسمّاةٌ ومعدودة** |

---

## ④ معاييرُ الحكم — ثلاثةٌ **حاكمة** تلزم كلُّها معًا

**`BC1` — التغطية.** `COV` **‏≥60%** في **كلٍّ** من السنوات الثلاث.
*(سابقةُ `T-SLIP-NBBO`: ‏75.8% — والحدُّ أدنى منها عمدًا لأن مجتمعَنا أرقُّ دفترًا.)*

**`BC2` — الأرضية.** **‏≥300 ساقًا مقيسة لكلّ سنة** و**‏≥1,200 مجمَّعًا**.

**`BC3` — الدقّة.** فاصلُ بوتستراب ‏95% لوسيط `c` المجمَّع **أضيقُ من ‏0.50 نقطة
مئويّة**. 🔑 **ومعناه بلفظٍ واحد:** رقمٌ فاصلُه أوسعُ من ذلك **لا يُحسّن** على
المستعار (‏1.32-1.75% مداها 0.43 نقطة) ⇒ **قياسٌ لا يشتري شيئًا ليس مصدرًا.**

---

## ⑤ الفروعُ الثلاثة — مكتوبةٌ قبل أيّ رقم

1. **الثلاثةُ تعبر ⇒** `C-MED` و`S-MED` **هما تكلفةُ هذا المجتمع**، **والمصدرُ الذي
   سمّاه `fcost_prereg.md §⑨` صار موجودًا** — ويُسلَّم الرقمُ للمالك.
2. **`BC3` وحدَها تسقط (‏`BC1`+`BC2` تعبران) ⇒** يُنشَر الرقمُ **مدًى لا نقطةً**،
   **ولا يُعَدّ «مصدرًا»** بنصّ `§⑨` (قياسٌ غيرُ دقيقٍ لا يُغني عن المستعار).
3. **`BC1` أو `BC2` تسقط ⇒ «لا حكم»** — ويُكتب في ملفّ النتيجة **تاريخُ إعادة قراءةٍ
   صريح** لا وعدٌ بلا موعد (درسُ «حدُّ صدقٍ يَعِد ثم لا يُعاد يتحوّل إلى سطرٍ يكذب»).

---

## ⑥ بوّاباتُ الصلاحية

- **`V-B1` — بوّابةُ التكامل.** المشيةُ تُعيد أعدادَ `T-FCOST` المنشورة **بت-بت** على
  اللقطات نفسِها: إشاراتٌ **‏1,620 · 1,591 · 1,607** ومحسومةٌ **‏1,381 · 1,364 ·
  1,354**. أيُّ تفرّقٍ ⇒ **عطبُ أداةٍ لا نتيجة ⇒ خروجٌ ولا يُقرأ رقم.**
- **`V-B2` — تطابقُ إعادة الاشتقاق** (‏§②): ‏≥99% وإلّا خروج · والمفترقُ يُعَدّ باسمه.
- **`V-B3` — الجذورُ الاثنا عشر ‏+ `_resolve_arm` و`_arm_a_exit_bar`** ببصمة AST مطابقة
  لـ`origin/main`. ولا تُعدَّل `slip_nbbo_arms.py` بحرف.
- **`V-B4` — قراءةٌ فقط:** الـworkflow **يدويٌّ بلا كرون** · **بلا سرِّ تلغرام** ·
  `permissions` الأعلى **حصرًا** `{contents: read, actions: read}` ولا جوبَ يُوسّعها ·
  وصفرُ كتابةٍ لأيّ ملفِّ حالةٍ إنتاجيّ.
- **`V-B5` — صفرُ إسنادٍ إلى `CONFIG`** (‏AST) — الأعلامُ عبر **بيئة الطفل** حصرًا.
- **`V-B6` — إعادةُ الاستعمال بالاسم** (‏§② الجدول) لا إعادةُ بناء — مقفولٌ بنيويًّا.
- **`V-B7` — لا إسقاطَ صامت:** كلُّ ساقٍ غيرِ مقيسة تُنسَب إلى **سببٍ مسمًّى**
  (`no_quotes` · `no_minutes` · `raw_scale_unverified` · `walk_mismatch` · `stale_quote`)
  ومجموعُ الأسباب **يساوي** الفرقَ بالضبط.
- **`V-B8` — `s` بلا قصّ** (‏§②).
- **`V-B9` — الحتميّة:** شريحةٌ مُعادةٌ بالمدخلات نفسِها تُعطي الأرقامَ نفسَها بت-بت.

---

## ⑦ تنبّؤاتي المسجَّلة — تُنشَر ولو كُذِّبت

- **`BP1`:** `C-MED` **أعلى** من ‏1.75% (الطرفِ الأعلى المستعار) — لأن مجتمعَنا
  **قيعانٌ هادئةٌ لمغمورين** بأرضيةِ سيولةٍ ‏$200K/يوم، ومجتمعَ المِرساة أسهمٌ
  **في حركة**. تقديرٌ: **‏2.0-3.5%**.
- **`BP2`:** `S-MED` **أصغر** من انزلاق `P0` المنشور (‏1.08-1.28%) — لأن وقفَ القاع
  يجعل وحدةَ المخاطرة ‏2.91%، و«الوقفُ الضيّق ينزلق أقلّ» **مؤكَّدٌ مرّتين بمصدرين**.
- **`BP3`:** **`BC3` هي الأقربُ للسقوط** — سبريدُ المغمورين ذيلُه ثقيل.
- **`BP4`:** بالرقم المقيس **يزداد حكمُ `T-FCOST` رسوخًا لا ينقلب**: الحسمُ في `E_A`
  يتجاوز **‏2.1 نقطة** المحسوبةَ عند ‏1.75%.
- **`BP5`:** `COV` تقع **‏70-85%**.
- **`BP6`:** `C-EXI` **أوسع** من `C-ENT` (الخروجُ عند الوقف = لحظةُ ضغط).

---

## ⑧ إعادةُ قراءةٍ **وصفيّةٌ مشروطة** — ولا تفتح مُغلَقًا ولا تُغلق مفتوحًا

إن عبرت المعاييرُ الثلاثة، يُطبَع **وصفيًّا**: `E_A` و`Δ` الأزواجِ الثلاثة عند
(`C-MED`, `S-MED`) محسوبةً **بالهُويّة المُغلَقة** من `fcost_rows.jsonl` القائم
(صفرُ مشيةٍ جديدة).

🔒 **وثلاثةُ قيودٍ صريحة:**
1. **وصفيٌّ لا حاكم** — لا يُقرأ حكمًا على `FC1`/`FC2`/`FC3`.
2. **ولا يُعدَّل شيءٌ من `T-FCOST`:** لا `fcost_result.md` ولا بانرُ التقييد في
   `MARKET_BACKTEST_FINDINGS.md §0-ل` ولا `fcost_arms.py` — **ولا يُعاد تشغيلُها**.
   *(‏وللدقّة: `T-FCOST` **لم يُغلَق** — حكمُه الفرعُ 2، فلا رمزَ إغلاقٍ في أداته.)*
3. **وأيُّ حكمٍ جديدٍ على الأزواج يلزمه عقدٌ مستقلٌّ ‏+ إذنُ المالك** — وهذا العقدُ
   **لا يدّعي أنه ذاك العقد**.

---

## ⑨ حدودُ صدقٍ تُكتب **قبل** الأرقام

1. **`NBBO` قمّةُ الدفتر لا عمقُه** — وأمرٌ أكبرُ من القمّة لا يُسعَّر ⇒ **`C-MED`
   أرضيّةٌ للتكلفة لا سقفٌ لها.**
2. **«اللمسُ ليس تعبئة» على الهدف غيرُ منمذَجٍ** ⇒ المحرّكُ يبقى متفائلًا ⇒ أرقامُنا
   **سقفٌ للأداء لا أرضية** (وهو اتّجاهٌ معاكسٌ للأوّل — والاثنان يُقالان معًا).
3. **ترتيبُ اللمس داخل الدقيقة مجهول** — نأخذ الاقتباسَ السائد عند أوّل لمسة.
4. **لحظةُ القرار ≠ لحظةُ اللمس** — المتداولُ الحقيقيّ قد يقرّر قبلها أو بعدها.
5. **لا نمذجةَ طابورٍ ولا تعبئةٍ جزئيّة ولا رسوم.**
6. **`bid_size` بالـلوتات** (‏موثَّق) ⇒ لا يُقرأ عمقًا بالأسهم.
7. **انحيازُ البقاء قائمٌ** كما في كلّ باكتيستاتنا على لقطات `PIT`.
8. **الكونُ هو كونُ `T-FCOST`** (بوّاباتُ فيصل منذ 2026-08-06) ⇒ لا يُقارَن برأسيّات
   ما قبلها.

---

## ⑩ سقفُ النجاح — مثبَّتٌ سلفًا

**أقصى ما تُنتجه هذي التجربةُ رقمٌ وشرطٌ مستوفًى.** لا تُغيّر `BT_SPREAD_PCT`
الافتراضيّة · ولا تمسّ جذرًا · ولا تُعدّل حكمًا منشورًا · ولا تعتمد سياسةَ تداول.
**واعتمادُ أيّ تكلفةٍ في الإنتاج قرارُ مالكٍ بعقدٍ مستقلّ.**

---

## ⑪ شرطُ الإغلاق — مكتوبٌ قبل النتيجة

**سقوطُ الفرع 3 (‏«لا حكم») مرّتين على هذا التصميم ⇒ يُغلَق محورُ «قياسُ التكلفة على
مجتمع المحرّك»** إغلاقًا **مُنفَّذًا لا مكتوبًا** (خروجٌ برمزٍ مميَّزٍ **قبل** أيّ
عملية · **وحارسان لا حارس** إن تفرّعت نقطةُ الدخول — درسُ `rsi40_arms`)، ولا يُرفَع
إلّا **بإقرارِ بيئةٍ صريح ‏+ إذنِ المالك ‏+ عقدٍ جديد**.

---

## ⑫ وضعُ الجدوى `BTCOST_DRY=1` — قبل إنفاق الرنر

يطبع **التغطيةَ والأعدادَ وأسبابَ الإسقاط فقط** — **صفرُ `c` وصفرُ `s` وصفرُ فاصلٍ
وصفرُ حكم** — وسؤالُه الوحيد: **هل تعبر `BC1` و`BC2`؟**

⚠️ **ودرسُ `T-PMGATE` يُكتب هنا قبل أن يقع: وضعُ الجدوى يُجيز ما يمرّ به وحدَه** —
فلا يُقرأ إذنًا للمسار كلِّه، وكتلةُ `c`/`s` لا تعمل فيه أصلًا.

---

## ⑬ الأداةُ والحرّاس — تُبنى **بعد** دمج هذا العقد

`btcost_arms.py` ‏+ `.github/workflows/btcost.yml` · أقفالٌ في `test_bot.py` ·
وبطّاريّةُ طفراتٍ تُسقط كلَّ قفلٍ بطفرةٍ مسمّاة. **ولا سطرَ أداةٍ قبل أن يُدمَج
هذا الملفُّ في `main`.**
