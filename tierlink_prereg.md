# 🔗🔬 تسجيلٌ مسبق — `T-TIERLINK`: «الرابطُ المشترك» للمنفجرين منذ أداة المتابعة والتصنيف

> **مدفوعٌ قبل أيّ سطرِ كودِ أداةٍ وقبل أيّ رقم.** أمرُ المالك 2026-09-02:
> «سو عصف ذهني وتجارب واختبارات للجميع الأسهم اللي انفجرت من بداية صنع أداة المتابعة
> والتصنيف اللي قوي وضعيف ومتوسط لأن أكيد فيه رابط مشترك وواضح جدًّا — وأنت هنا
> المطلوب منك أنك تطلعه».
> ⚖️ **والجوابُ يُقاس لا يُفترَض:** إن وُجد رابطٌ يعبر المعيار أدناه يُنشَر باسمه، وإن
> لم يوجد يُنشَر «لا رابط بهذا المعيار» — **ولا يُخفَّف المعيارُ بعد رؤية الأرقام.**

## ① السؤال بصيغةٍ قابلةٍ للتكذيب

بين **كلّ** مِرساةِ سيولةٍ حيّةٍ رصدتها الأداة منذ ولادتها (‏2026-08-18) — لا بين
المنفجرين وحدهم (اختيارٌ على النتيجة = دائريّة) — **هل توجد ميزةٌ معلومةٌ لحظةَ الكرت**
تفصل مَن انفجر عمّن لم ينفجر، فصلًا يستوفي المعيار §④؟

## ② المجتمع (ثلاثُ طبقاتٍ مُسمّاة، لا انتقاء)

- **A — المراسي الحيّة:** كلُّ `LIQ:*` بـ`anchor_ms` من **آخر لقطةِ `op_entry_state.json`
  لكلّ يوم في تاريخ git** (‏2026-08-18 ⟶ 2026-09-01). **‏338 مِرساة** (عُدّت قبل الدفع).
  ⚠️ حقولُ `k2` تبدأ 08-20 (‏231 مِرساة تحملها) · و`anchor_price`/`anchor_low` من 08-19.
- **B — كروتُ `M5` المُسلَّمة:** `tier_fwd_ledger.jsonl` (‏08-24 ⟶ 09-01، **‏190 صفًّا
  فريدًا** بأوّل صفٍّ لكلّ (تاريخ، رمز)) — الطبقةُ الوحيدة التي تحمل الميزاتِ الكاملة
  (`tier` · `green` · `c3/c4/v2/v3` · `j1` · `cls` · `usd` · `vol_x` · `e5` · `prev_close`).
- **C — سجلُّ الانفجارات** (`weekly_watchlist.explosions` · `expl_date ≥ 2026-08-18`):
  **‏78 صفًّا منها 31 موسومةٌ `suspect_split`** — يُستعمل **للتغطية فقط** (كم منفجرًا لم
  تره الأداة أصلًا) لا للحكم.

## ③ النتيجة (Polygon · `adjusted=false` إلزامًا · صفرُ نظرٍ مستقبليّ في الميزات)

الأساسُ = **سعرُ كرت `M5`** (‏`e5` من B، وإلّا إغلاقُ الدقيقة الخامسة بعد المِرساة من
شموع اليوم) — لا سعرُ المِرساة (درسُ `HUIZ`: سعرُ المِرساة لا يملكه المالك).

| رمز | التعريف | مصدرُ الحساب |
|---|---|---|
| **`mg_day`** (الحاكم) | أقصى `high` بعد الكرت **حتى إغلاق اليوم بلا قطع** ÷ الأساس − 1 | `sym_day_probe.full_day_max` بالاسم |
| `mg_cut` | الأقصى **قبل** الخروج البنيويّ (أوّلُ إغلاقِ دقيقةٍ دون `anchor_low`) | `sym_day_probe.exit_point` بالاسم |
| `close_ret` | إغلاقُ اليوم ÷ الأساس − 1 (مَن أمسك للإغلاق) | مباشر |
| `mg_5d` | أقصى `high` يوميّ في **الخمس جلساتٍ التالية** ÷ الأساس − 1 | `/v2/aggs` يوميّ |

**الوسمُ الحاكم `exploded50` = `mg_day ≥ 50%`** (كلمةُ المالك «انفجرت») · والرفيقُ
`exploded100` = `mg_day ≥ 100%` · و`kasih30_cut` = `mg_cut ≥ 30%` (المقياسُ المنشور).

## ④ المعيار — يُثبَّت الآن ولا يتحرّك

الميزةُ تُسمّى **«رابطًا»** فقط إن استوفت **الأربعة معًا** على `exploded50`:
1. نسبةُ الانفجار في سلّتها الأعلى **‏≥ ضِعفَي** نسبةِ مُكمِّلها.
2. فاصلا Wilson ‏95% للسلّتين **لا يتداخلان**.
3. **‏n ≥ 20** في كلٍّ من السلّتين.
4. **التكرارُ الزمنيّ:** الاتّجاهُ نفسُه في نصفَي الفترة (‏H1 ‏≤ 08-25 · H2 ‏≥ 08-26)
   بـ‏n ≥ 10 في السلّة الأعلى في كلٍّ منهما.

- تستوفي 1-3 وتسقط 4 ⇒ **«فصلٌ غيرُ متكرّر»** (يُنشَر ولا يُعتمَد).
- تسقط 3 ⇒ **«لا حكم»** لا «لا رابط».
- ولا تُضاف ميزةٌ ولا سلّةٌ ولا تركيبةٌ **بعد** رؤية الأرقام — والقائمةُ في §⑤ مُغلَقة.

## ⑤ الميزاتُ المُختبَرة (مُغلَقةٌ · كلُّها معلومةٌ لحظةَ الكرت)

`tier` (قوي/متوسط/ضعيف) · `green` (‏0-4) · `c3` · `c4` · `v2` · `v3` · `j1` · `cls`
(strong/operator/mid/group) · `vol_x` (‏<10 · 10-30 · ≥30) · `usd` (‏<100 ألف · 100-300 ·
≥300) · **الفجوة** = سعرُ المِرساة ÷ إغلاقُ الأمس − 1 (‏<10% · 10-30 · ≥30) · **سعرُ
الأمس** (‏<1$ · 1-3 · 3-10 · >10) · **وقتُ المِرساة** (بريماركت / نظاميّ / أفتر بساعة
نيويورك) · **عمقُ ذيل المِرساة** = (سعر − قاع) ÷ سعر (‏<5% · 5-15 · ≥15) · **مضغوطٌ
سابقًا** = الرمزُ في `press_radar_ledger` بالجلسة السابقة (ومعه `hold_sessions ≥ 3`).
**وثلاثُ تركيباتٍ مسبقة فقط:** `V1` (الإنتاج: ‏`green≥3` أو `j1`) · `green≥3` وحدَها ·
`j1` وحدَها.

## ⑥ التنبّؤاتُ القابلة للكسر (قبل الأرقام)

- **`TL-P1`** لا ميزةَ مفردة تستوفي الأربعة — الأرجح سقوطُ 3 أو 4 (السجلُّ الأماميّ
  ‏190 صفًّا ونسبةُ الأساس ‏≈10-15% ⇒ سلالُ الأقليّة صغيرة).
- **`TL-P2`** أوسعُ فصلٍ لـ`j1` أو `V1` (سندُ `T-TIER-V3`: استرجاع 44% وفصل ‏2.4-2.7×
  تاريخيًّا) — لكنه يسقط على 3 أو 4.
- **`TL-P3`** مراسي **البريماركت** أعلى انفجارًا من النظاميّ (سندُ `kasih_result F3`).
- **`TL-P4`** التغطية: **أقلُّ من نصف** الانفجارات النظيفة (غير `suspect_split`) في C
  كان لها مِرساةٌ في يومها — لأن كونَ الأداة قوائمُنا لا السوق.
- **`TL-P5`** الترتيبُ قوي ≥ متوسط ≥ ضعيف يصمد **اتّجاهًا** ويسقط فصلًا.

## ⑦ حدودُ صدقٍ مكتوبةٌ قبل النتيجة

- **لمسٌ لا تنفيذ**: `mg_day` قمّةٌ لُمست بلا انزلاق (سقفٌ متفائل) · و`close_ret` رفيقُه
  الأمين.
- فترةٌ **قصيرة** (‏≈11 جلسة تداول) وسوقٌ واحد ⇒ حتى «رابطٌ» عابرٌ للمعيار **قيدُ
  الإثبات الأماميّ** لا حكمٌ نهائيّ.
- الميزاتُ من **آخر لقطةٍ لليوم**؛ `k2` يُكتَب عند `M5` ولا يتغيّر بعدها (بالبناء) فلا
  تسريب — لكن `vol_x`/`peak_usd` في A **قد تُحدَّث بعد الكرت** ⇒ تُقرأ من B حين تتوفّر
  ومن A **موسومةً** حين لا.
- 08-27/08-28 كرونات ميّتة (مقيسة) ⇒ مراسٍ أقلّ فيهما — عيبُ تغطيةٍ لا سوق.
- `exploded50` **لا يقيس ربحيّة** (لا وقفَ ولا هدف) — سؤالُ المالك «مَن ينفجر» لا «كم
  أربح».

## ⑧ سقفُ النجاح

**تقريرٌ واقتراحٌ للمالك** — لا بوّابةَ ولا فلترَ ولا تغييرَ عتبةٍ ولا `LOGIC_VERSION`،
مهما كانت الأرقام. والأداةُ قراءةٌ فقط (صفرُ إرسال · صفرُ كتابةِ حالة).
