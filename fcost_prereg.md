# 🩸⚙️ `T-FCOST` — تكلفةُ التنفيذ على **محرّك الباكتيست نفسِه** (تسجيلٌ مسبق)

> **الأمر:** المالك 2026-09-13 **«سجل الانزلاق»** — بعد أن عرضتُ عليه أن هذا
> **البندُ الوحيد غير المنتظِر** من المعلَّق، ونصُّه في `PENDING_VERIFICATION.md`
> بندًا ثانيًا: «**تكاليفُ التنفيذ/السبريد على `backtest_symbol` نفسِه** — قِيست
> على مسار **الاشتعال والمِرساة** (`T-SLIP` · `T-SLIP-NBBO`) **لا على محرّك
> الباكتيست** الذي تُبنى عليه الأحكامُ المقفولة ⇒ الشقُّ باقٍ بحرفه.»
>
> 🔒 **الدفع:** هذا الملفُّ على `origin/main` **قبل سطرِ كودٍ واحد** من الأداة،
> وقبل أيّ تشغيلة، وقبل أيّ رقم.
> **قراءةٌ/بحثٌ فقط · صفرُ مسٍّ بالإنتاج · لا `LOGIC_VERSION` · لا `force_renew`.**
> **ولا تتحرّك قيمةُ `BT_SPREAD_PCT` الافتراضية (‏0.0) بهذي التجربة إطلاقًا.**

---

## ⓪ ما رأيتُه **قبل** كتابة هذا العقد — إقرارُ تلوّثٍ صريح

**قرأتُ قبل الكتابة، وأعلنُه كلَّه:**

1. **`PENDING_VERIFICATION.md`** البند 2 بنصّه أعلاه.
2. **`DECISIONS_ARCHIVE.md:3380` (`T-SLIP`)** — الحافّة ‏+0.689R ⟶ **+0.566R**
   (انكماشُ 17.9%) · معاملا الانزلاق **`P0` ‏0.093R** و**`P1` ‏0.335R** لكلّ 1% ·
   والعامليّةُ **الفجوة ‏−0.076R · داخلُ الدقيقة ‏−0.047R · ذيلُها ‏−0.110R**.
3. **`DECISIONS_ARCHIVE.md:3321` (`T-SLIP-NBBO`)** — ‏**+0.576R** · متوسّطُ
   انزلاق `P0` **‏1.28%** و`P1` **‏0.81%** · ووسيطاهما **‏≈0.5%** و**‏≈0.15%** ·
   **وسبريدٌ وسيطُه ‏1.32-1.75%**.
4. **`backtest_2025_live_winrate.md`** — ‏199 إشارة · 41 غيرَ مُعبَّأة · **‏147
   محسومة** · 38✅/109🛑 · **‏25.9%** [‏19-33%] · العائدُ المحقَّق **متوسّط ‏−2%
   ووسيط ‏−10%** · **توقّع ‏−0.2R** · والوقفُ الزوجيُّ **‏A ‏−1.7% مقابل B ‏−3.1%
   ⇒ `B−A` = ‏−1.4 نقطة**. ومن `CLAUDE.md`: النجاحُ **‏27.4% (2023) · 33.6%
   (2024) · 25.9% (2025)**.
5. **الكودَ نفسَه:** `_resolve_arm` يُسعّر الخروجَ بـ`buy = entry·(1+c/2)` و
   `sell = exit·(1−c/2)` حيث `c = BT_SPREAD_PCT` (**‏0.0 افتراضًا**)، ويُسعّر
   الوقفَ **`min(stop, open)`** = **واعٍ بالفجوة على مستوى الشمعة اليوميّة**،
   ويُسعّر الهدفَ **عند `t1` بالضبط عند اللمس**.

🔴 **والحقيقةُ التي تُثبت أن المحورَ بِكرٌ لا إعادةَ فتح — من سجلّنا لا من رأيي:**
`HANDOFF_ARCHIVE.md:5438` يقول عن `F-COST` يومَ بنائه (‏2026-07-12):
«**⏳ المتبقي 3-ب: تشغيل Actions … لإعادة حكم T1b وطباعة B3/C3 على المحرّك
المُصلَح**» — **ولم يُشغَّل قطُّ منذ ذلك اليوم**. فالآلةُ موجودةٌ منذ أربعةَ عشرَ
شهرًا **بصفر تشغيلةٍ وصفر رقم**، وبحثُ `BT_SPREAD_PCT` في الأرشيفين يُخرج **سطرَ
البناء وحدَه**. ⇒ **صفرُ رقمِ حصيلةٍ رأيتُه على هذا المحور.**

⚠️ **وما رأيتُه أعلاه كلُّه من مجتمعٍ آخر** (‏مِرساةُ الذهب/الاشتعال) أو من
**مستوًى** لا من **دلتا** — ولذلك صُمّمت هذي التجربةُ **سلّمَ تكلفةٍ ونقطةَ
تعادل** لا رقمًا واحدًا مستعارًا (‏§③).

---

## ① السؤال المسجَّل

**كلُّ حكمٍ محرّكيٍّ مقفولٍ عندنا صدر من محرّكٍ يُسعّر التنفيذَ بصفر.**
فالسؤال: **هل ينقلب أيُّ حكمٍ محرّكيٍّ مقفول حين يُسعَّر التنفيذُ عند المستوى
المقيس؟ وعند أيّ تكلفةٍ ينقلب لو انقلب؟**

⛔ **وما ليس سؤالًا هنا:** «هل تقلّ العوائدُ بالتكلفة؟» — **جوابُه نعم بالحساب
لا بالقياس** (‏§②)، وطرحُه سؤالًا يُنتج رقمًا صحيحًا بلا معنى.

---

## ② الميكانيكا — والهُويّةُ الجبريّة التي تحكم القراءة كلَّها

بتعريف `_resolve_arm`: `ret(c)/100 = (1 + r₀/100)·k(c) − 1` حيث
**`k(c) = (1 − c/2) ÷ (1 + c/2)`**.
⇒ **`ret(c) = k·r₀ + 100(k−1)`** — تحويلٌ **أفينيّ** للعائد الخام، مستقلٌّ عن
السعر والوقف والهدف والنتيجة.

**ويترتّب عليه ثلاثةُ لوازمَ تُكتب الآن قبل أيّ رقم:**

1. **متوسّطُ ذراعٍ على مجموعة صفقاتٍ:** `E(c) = k·E(0) + 100(k−1)`.
2. **الفرقُ بين ذراعين يُحسَب على «المحسومة وحدَها» لكلٍّ:**
   `E_X(c) − E_Y(c) = k·[E_X(0) − E_Y(0)]` — **الإزاحةُ تُلغى، والإشارةُ لا
   تنقلب أبدًا** مهما بلغت التكلفة. (‏`k > 0` دائمًا.)
3. 🔑 **والفرقُ حين يُحتسَب غيرُ المُعبَّأ صفرًا** (‏اتّفاقيةُ أذرع الدخول
   عندنا): `E_X(c) − E_Y(c) = k·Δ(0) + 100(k−1)·(f_X − f_Y)` حيث `f` نسبةُ
   التعبئة ⇒ **الإزاحةُ تبقى وإشارتُها سالبةٌ دائمًا** (`k < 1`) ⇒ **التكلفةُ
   تحابي الذراعَ التي تتداول أقلّ**، بمقدارٍ **يُحسَب سلفًا**.

📐 **وجدولُ الثوابت مثبَّتٌ الآن** (‏يُطبَع في المُخرَج ويُقارَن):

| `c` | `k(c)` | `100(k−1)` |
|---|---|---|
| 0% | 1.000000 | 0.0000 |
| 1% | 0.990050 | −0.9950 |
| **1.32%** | 0.986887 | **−1.3113** |
| **1.75%** | 0.982652 | **−1.7348** |
| 2% | 0.980198 | −1.9802 |
| 4% | 0.960784 | −3.9216 |
| 8% | 0.923077 | −7.6923 |

🔗 **وفحصُ اتّساقٍ يُكتب قبل القياس:** بوحدة المخاطرة `ΔR ≈ −c ÷ ρ` حيث
`ρ = (الدخول − الوقف) ÷ الدخول`. وبـ`ρ ≈ 0.107` يعطي **‏0.093R لكلّ 1%** —
وهو **بالضبط** معاملُ `P0` المنشور في `T-SLIP`. ⇒ **جبري مُصادَقٌ على قياسٍ
مستقلٍّ سابق قبل أن أقيس شيئًا** (‏وهو `V-F6` أدناه).

---

## ③ أذرعُ التكلفة — شبكةٌ ثنائيّةٌ تُثبَّت الآن ولا تتحرّك

**العائلةُ `S` (سبريدٌ متماثل، ذهابًا وإيابًا):** `c ∈ {0 · 1% · 1.32% · 1.75%
· 2% · 4% · 8%}` — **وهي ما يُنفّذه `BT_SPREAD_PCT` حرفيًّا.**

**العائلةُ `L` (انزلاقٌ على الخاسر وحدَه):** `s ∈ {0 · 0.5% · 1% · 2%}` —
تُطبَّق على صفقات `loss` فقط: `exit′ = exit·(1−s)`.
🔑 **ولماذا عائلةٌ ثانية:** الهدفُ **أمرُ حدٍّ** يُنفَّذ عند سعره أو أفضل،
والوقفُ **أمرُ سوقٍ** ينزلق — فالتكلفةُ **غيرُ متماثلة**، و`BT_SPREAD_PCT`
وحدَه لا يعبّر عنها.

**والخليّةُ المقيسة (الحاكمة) `(c*, s*)` تُشتقّ من أرقامٍ منشورة لا من عندي:**
- `c*` = **‏1.32% و1.75%** — طرفا وسيط السبريد في `T-SLIP-NBBO`. **الطرفان
  يُنشَران معًا ولا يُختلَق متوسّطٌ بينهما.**
- `s*` = **‏0.5%** — من عامليّة `T-SLIP`: المحرّكُ **يُسعّر الفجوةَ سلفًا**
  (‏`min(stop, open)`) فالجزءُ **غيرُ المنمذَج** هو «داخلُ الدقيقة» ‏**−0.047R**،
  و`s = ΔR·ρ ≈ 0.047 × 0.107 ≈ 0.5%`. **و`s_hi` = ‏1.7%** من الحدّ الأعلى
  (`داخلُ الدقيقة + ذيلُها` = ‏−0.157R).
  ⚠️ **وحدُّ دقّةٍ يُقال:** العامليّةُ المنشورة **لا تُسمّي ذراعَها** ⇒ قرأتُها
  مجمَّعةً، **والسلّمُ يُحيط بالقراءتين** فلا يتوقّف الحكمُ على تأويلي.

**الخليّةُ `(0, 0)` = المحرّكُ الإنتاجيُّ اليوم = بوّابةُ التكامل.**

---

## ④ الأزواجُ المُعادُ قياسُها — من **مشيةٍ واحدةٍ** لكلّ سنة

| الزوج | الوصف | التعبئة | الحكمُ المنشور المُعادُ اختبارُه |
|---|---|---|---|
| **`PR-STOP`** | `E_B − E_A` (وقفُ الإغلاق مقابل وقف الذيل) | **متساوية** | `B−A` = ‏−1.4 نقطة ⇒ «لا يُتبنّى» · «الوقفُ ليس الرافعة» |
| **`PR-LEGACY`** | `E_legacy − E_A` (تفاؤلُ شمعة التعبئة ‏F-L1) | **متساوية** | التضخّمُ منشورٌ بالنجاح لا بالعائد |
| **`PR-SWEEP`** | `E_sweep − E_A` (‏T1b دخولُ المسح‑الاستعادة) | 🔑 **متفاوتة** | «الوقفُ ليس الرافعة · الرافعةُ توقيتُ الدخول» |
| **`PR-HEAD`** | مستوى `E_A` ونسبةُ النجاح والعددُ (‏لا زوج) | — | ‏27.4 / 33.6 / 25.9% · ‏−0.2R |

**والمقياسُ:** `E` = **متوسّطُ العائد المحقَّق بالنسبة المئوية لكلّ صفقةٍ
محسومة** — نفسُ مقياس `backtest_2025_live_winrate.md` حرفيًّا.

🔑 **و`PR-SWEEP` يُحسَب بقراءتين تُنشَران معًا:**
- **(أ) المحسومةُ وحدَها** — مقامان مختلفان، واللازمُ ② ينطبق ⇒ **لا انقلاب.**
- **(ب) غيرُ المُعبَّأ صفرًا** — اتّفاقيةُ أذرع الدخول عندنا، واللازمُ ③ ينطبق
  ⇒ **الإزاحةُ تبقى**. **و(ب) هي الحاكمةُ في `FC3`** لأنها الاتّفاقيةُ التي
  نُشرت تحتها أحكامُ الدخول.

**ومشيةٌ واحدةٌ لكلّ سنةٍ تكفي للشبكة كلِّها** — لأن التكلفةَ **لا تغيّر نتيجةَ
أيّ صفقة** (‏`outcome` مستقلٌّ عن `c` و`s`) بل حسابَ عائدها فقط.

---

## ⑤ معاييرُ الحكم — ثلاثةٌ **حاكمة** تلزم كلُّها معًا

**`FC1` — ثباتُ الإشارة.** كلُّ زوجٍ من الثلاثة يحتفظ بإشارته عند **كلّ خليّةٍ
داخل المدى المقيس** (`c ≤ 1.75%` مع `s ≤ 0.5%`) **وفي السنوات الثلاث**.

**`FC2` — هامشُ التعادل.** لكلّ زوجٍ ذي إشارة: أصغرُ `c` (عند `s = s*`) تقلب
إشارتَه يجب أن تكون **‏3.5% فأكثر** (= ‏2× الطرفِ الأعلى المقيس)، **أو لا
تعادلَ منتهيًا أصلًا**.

**`FC3` — سقفُ إزاحة التعبئة.** للأزواج المتفاوتة التعبئة بقراءة (ب):
`|100(k−1) · (f_X − f_Y)|` عند `c = 1.75%` يجب أن يكون **أصغرَ من `|Δ(0)|`**
لذلك الزوج، **في السنوات الثلاث**.
🔑 **ومعناه بلفظٍ واحد:** هل تستطيع التكلفةُ وحدَها أن **تصنع** حكمًا أو
**تمحوَه**؟ فإن استطاعت فالحكمُ المنشورُ **أثرُ عدّادِ صفقاتٍ لا حافّة** — وهو
درسُ `T-WAIT-23W` بعينه في ثوبِ تكلفة.

**والأرضية `V-F4`:** ‏**100 محسومةً لكلّ ذراعٍ لكلّ سنة** و**‏350 مجمَّعًا**
— **سابقةُ `T-EXIT`** المنصوصة («‏≥100 محسومة/سنة»، استُوفيت 147/109/168)،
ولا تُرخى لبلوغها.

---

## ⑥ الفروعُ الثلاثة — مكتوبةٌ قبل أيّ رقم

1. **الثلاثةُ تعبر ⇒** الأحكامُ المحرّكيّةُ **تصمد أمام تكلفة التنفيذ بهامشٍ
   مقيس** ⇒ يُغلَق البندُ 2 من `PENDING_VERIFICATION.md` **بدليله**، إغلاقًا
   **مُنفَّذًا لا مكتوبًا** (‏§⑨).
2. **سقط أيٌّ منها ⇒** **لا إغلاق**؛ والزوجُ الساقطُ وحدَه يُوسَم **ببانرِ
   تقييدٍ في ملفّ نتيجته** (سابقةُ `gold_entry_result.md`)، والبندُ يبقى
   مفتوحًا **له وحدَه** لا للمحور كلِّه.
3. **`V-F4` يسقط في سنةٍ أو أكثر ⇒** **«لا حكم»** — ويُكتب في ملفّ النتيجة
   **تاريخُ إعادة قراءةٍ صريح**، لا وعدٌ بلا موعد (درسُ «حدُّ صدقٍ يَعِد بإعادة
   قياسٍ ثم لا يُعاد يتحوّل إلى سطرٍ يكذب»).

---

## ⑦ بوّاباتُ الصلاحية

- **`V-F1` — بوّابةُ التكامل.** الخليّةُ `(0,0)` المحسوبةُ في الأداة تساوي
  **مُخرَجَ المحرّك نفسِه** (`ret_a`) **بت-بت**. وأيُّ تفرّقٍ ⇒ **عطبُ أداةٍ لا
  نتيجة ⇒ خروجٌ ولا يُقرأ رقم.**
  ⚠️ **ونسبةُ النجاح تُقارَن بالمنشور نطاقًا لا بت-بت** — لأن الكونَ لقطةٌ
  مجمَّدةٌ وليس تشغيلةَ 2026-07 الحيّة (‏§⑩-8).
- **`V-F2` — مصادقةُ التحويل على المحرّك.** تشغيلةٌ إضافيّةٌ **على مجموعةِ
  رموزٍ صغيرةٍ مثبَّتةٍ سلفًا** بـ`BT_SPREAD_PCT = 0.04` يجب أن تطابق التحويلَ
  البعديَّ عند `c = 4%` بفارقٍ **دون 0.05 نقطة** على المتوسّط. **وإلّا خروجٌ ولا
  حكم.** (‏هذي هي التي تجعل الحسابَ البعديَّ **مصادَقًا** لا مُدَّعًى.)
- **`V-F3` — حارسُ الـ`no-op`.** تشغيلُ الأعلام الملحِقة (`BT_SWEEP_ENTRY` ·
  `BT_POTENTIAL` · `BT_ENVVALS`) يجب أن يترك `outcome`/`ret_a` **مطابقًا** لتشغيلةٍ
  بلا أعلامٍ على المجموعة نفسِها. وإلّا خروجٌ ولا حكم. **وعددُ الصفقات التي
  يتغيّر عائدُها عند `c = 1%` يُطبَع** — **صفرُ تغيّرٍ ⇒ خروج** (بصمةُ `BT_CANDLE`).
- **`V-F4` — الأرضية** (‏§⑤). دونها ⇒ الفرعُ 3.
- **`V-F5` — لا-تأثيرُ التكلفة على النتيجة.** نسبةُ النجاح وعددُ المحسومة
  **متطابقةٌ في كلّ خلايا الشبكة** (التكلفةُ تُسعّر ولا تحسم). أيُّ حركةٍ ⇒
  **عطبُ أداةٍ ⇒ خروج.**
- **`V-F6` — المصادقةُ الجبريّة المستقلّة.** المعاملُ المشتقُّ `1/ρ` المقيسُ من
  صفقات الكون يجب أن يُعيد **‏0.09-0.11R لكلّ 1%** = معاملَ `P0` المنشور في
  `T-SLIP`. خارجَه ⇒ **يُطبَع تحذيرٌ صريح** وتُقرأ النتيجةُ مقيَّدة (‏لا خروج —
  فالمجتمعان مختلفان بالتصميم).
- **`V-F7` — قراءةٌ فقط.** الـworkflow **يدويٌّ بلا كرون** · **بلا سرِّ تلغرام**
  (فالإرسالُ مستحيلٌ بنيويًّا) · `permissions` الأعلى **حصرًا** `{contents: read,
  actions: read}` (‏`actions: read` لازمٌ لتنزيل اللقطة) ولا جوبَ يُوسّعها ·
  وصفرُ كتابةٍ لأيّ ملفّ حالةٍ إنتاجيّ.
- **`V-F8` — الجذورُ الاثنا عشر ببصمة AST مطابقة** لـ`origin/main` قبل كلّ دفعة.
  **ولا تُعدَّل `_resolve_arm` ولا `backtest_symbol` بحرف.**
- **`V-F9` — حصرُ ضبط الإعدادات.** لا إسنادَ إلى `CONFIG` خارج القائمة البيضاء
  المعلَنة `{BT_SPREAD_PCT · BT_SWEEP_ENTRY · BT_SWEEP_STOP · BT_POTENTIAL ·
  BT_ENVVALS}` **وفي وضع `BACKTEST` حصرًا** (‏قفلُ `B1` القائم يحرس الإنتاج).

---

## ⑧ تنبّؤاتي المسجَّلة — تُنشَر ولو كُذِّبت

- **`FP1`:** الهُويّةُ الأفينيّة تصدق **عدديًّا** على `PR-STOP` و`PR-LEGACY`:
  `Δ(c) = k·Δ(0)` بفارقٍ دون **‏0.01 نقطة** ⇒ **لا انقلابَ ممكن** لهما.
  **وكذبُها يعني عطبَ أداةٍ لا اكتشافًا.**
- **`FP2`:** الحسمُ في `E_A` عند `(1.75%, 0.5%)` يقع بين **‏1.8 و2.5 نقطة**
  (‏`k·E(0) + 100(k−1)` ‏+ حصّةُ الخاسرين). *(مشتقٌّ من ‏−2% المنشور و‏74%
  خاسرين.)*
- **`FP3`:** **لا زوجَ ينقلب** داخل المدى المقيس في أيّ سنة ⇒ `FC1` تعبر.
- **`FP4`:** **`PR-SWEEP` وحدَه** يُظهر حركةً معتبرةً بقراءة (ب)، **وفي اتّجاه
  الذراع التي تتعبّأ أقلّ**. وإزاحتُه عند `c = 1.75%` بفارق تعبئةٍ ‏0.30 تقع
  حول **‏−0.52 نقطة**.
- **`FP5`:** `FC3` **هي المعيارُ الأقربُ للسقوط** من الثلاثة.
- **`FP6`:** نسبةُ النجاح **لا تتحرّك بمقدار رقمٍ واحد** عبر الشبكة كلِّها
  (‏= `V-F5` مقروءًا تنبّؤًا).

---

## ⑨ شرطُ الإغلاق — مكتوبٌ **قبل** النتيجة

**عبورُ `FC1` و`FC3` معًا ⇒ يُغلَق محورُ «تكاليفُ التنفيذ على المحرّك»**
إغلاقًا **مُنفَّذًا لا مكتوبًا**: الأداةُ تخرج برمزٍ **مميَّزٍ عن كلّ رموز
قياسها** **قبل** أن تقرأ مدخلًا أو تفتح لقطةً أو تُطلق عمليةً واحدة، ولا يُرفع
الحارسُ إلّا **بإقرارِ بيئةٍ صريح** (`FCOST_REOPEN`) — **إقرارٌ لا التفاف**.
**وحارسان لا حارس** إن تفرّعت نقطةُ الدخول (درسُ `rsi40_arms`).

🔓 **وشرطُ الفتح ثلاثةٌ معًا:**
1. **مصدرُ تكلفةٍ مقيسٌ على هذا المجتمع** (‏NBBO على صفقات الباكتيست نفسِها)
   لا مستعارٌ من مجتمع المِرساة — **وإعادةُ عتبةٍ من السلّم ليست مصدرًا.**
2. **أو نموذجٌ لـ«اللمسُ ليس تعبئة» على الهدف** (‏§⑩-2) مسجَّلٌ مسبقًا.
3. **وإذنُ المالك.** والعقدُ **مدموجٌ لا يُعدَّل** — أيُّ فتحٍ يلزمه عقدٌ جديد.

**وسقوطُ أيٍّ منها ⇒ لا إغلاق** (الفرعُ 2)، والبندُ يبقى مفتوحًا للزوج الساقط.

---

## ⑩ حدودُ صدقٍ تُكتب **قبل** الأرقام

1. **استعارةُ مجتمع:** `c*` و`s*` مقيسان على مجتمع **مِرساةِ الذهب/الاشتعال**
   لا على مجتمع `backtest_symbol`. السيولةُ قد تختلف ⇒ **ولذلك الحكمُ على
   السلّم ونقطةِ التعادل لا على رقمٍ واحد**، ولا يُؤكَّد أنّ التكلفةَ الفعليّة
   لهذا المجتمع هي تلك.
2. 🔴 **«اللمسُ ليس تعبئة» على الهدف غيرُ منمذَج** — المحرّكُ يملأ `t1` عند
   لمسه. **والاتّجاهُ يُقال:** يجعل المحرّكَ **متفائلًا** ⇒ **أرقامُنا سقفٌ لا
   أرضية**. وهو خارجَ نطاق هذا العقد صراحةً (‏`T-SLIP §⑨-4` أعلنه ولم يقسه)،
   **ويضرّ الأذرعَ ضيّقةَ المخاطرة أكثر.**
3. **لا نمذجةَ طابورٍ ولا تعبئةٍ جزئية** — يُفترَض خروجُ المركز كلِّه بسعرٍ
   واحد ⇒ **أرضيةُ ضررٍ لا سقفه** لمركزٍ كبيرٍ في سهمٍ رقيق.
4. **انزلاقُ الدخول غيرُ منمذَج** — الدفعاتُ أوامرُ حدٍّ **تحت** السوق، وعيبُها
   الحقيقيُّ «اللمسُ ليس تعبئة» وهو يُنقص **عددَ** الصفقات لا يُعمّق الخسارة
   (‏`T-SLIP §⑨-3` حرفيًّا).
5. **الفجوةُ مُسعَّرةٌ سلفًا على الشمعة اليوميّة** (`min(stop, open)`) ⇒ العائلةُ
   `L` تقيس **المتبقّي غيرَ المنمذَج فقط**، ولا تُضاف إلى فجوةٍ مسعَّرةٍ مرّتين.
6. **تقريبُ المحرّك:** `ret_a` يُخزَّن بخانةٍ عشريّةٍ واحدة ⇒ التحويلُ البعديُّ
   يرث خطأً **‏≤0.05 نقطة للصفقة**، وأثرُه على متوسّطِ `N` صفقةٍ **‏≤0.029/√N
   نقطة** (‏≈0.001 عند ‏1000). **مذكورٌ ومحدود، و`V-F2` يصادق التحويلَ على
   المحرّك نفسِه.**
7. **انحيازُ البقاء** في اللقطات المجمَّدة **مُعلَنٌ ولا يُصحَّح.**
8. **الكونُ لقطةٌ مجمَّدة** لا تشغيلةُ 2026-07 الحيّة ⇒ **الأرقامُ الرأسيّةُ
   ستختلف عن ‏25.9/27.4/33.6% وهذا متوقَّعٌ لا عطب**؛ **والمقارنةُ داخليّةٌ على
   المشية نفسِها** فأثرُ الكون يُطرَح.
9. **الاحتمالُ الأسبقُ مُعلَن:** هذي **إعادةُ قياسِ صلاحيةِ أحكامٍ قائمة** لا
   بحثٌ عن حافّةٍ جديدة، **واللازمُ ② يجعل `FC1` مرجَّحةَ العبور بالجبر** ⇒
   **القيمةُ الحقيقيّةُ في `FC3` وحدَه**، وأُعلنه الآن حتى لا يُقرأ عبورُ
   `FC1` إنجازًا.

---

## ⑪ سقفُ النجاح — مثبَّتٌ سلفًا

مهما كانت الأرقام: **بيانٌ مقيسٌ في ملفّ نتيجةٍ ‏+ سطرُ عرضٍ تشخيصيٍّ اختياريّ.**
- **لا تغييرَ لقيمة `BT_SPREAD_PCT` الافتراضية (تبقى ‏0.0).**
- لا مسَّ عتبةٍ ولا بوّابةٍ ولا جذرٍ ولا `LOGIC_VERSION` ولا `force_renew`.
- لا مسَّ `_resolve_arm` ولا `backtest_symbol` ولا أرقامِ المالك (`LIQ_*`) ولا
  صيّادِ المقسّم.
- **وأقصى ما يُنتَج عند السقوط: بانرُ تقييدٍ في ملفّ نتيجةٍ بحثيّ** — توثيقٌ لا كود.

---

## ⑫ وضعُ الجدوى `FCOST_DRY=1` — قبل إنفاق الرنر

يُشغَّل **سنةً واحدة**، ويطبع **العددَ ونسبةَ التعبئة ونسبةَ النجاح فقط**:
**صفرُ `Δ` وصفرُ نقطةِ تعادلٍ وصفرُ حكم**. سؤالُه الوحيد: **هل تعبر `V-F4`؟**

⚠️ **ودرسُ `T-PMGATE` مكتوبٌ هنا قبل أن يقع: وضعُ الجدوى يُجيز ما يمرّ به
وحدَه** — فمرورُه لا يُجيز المسارَ الكامل، والعيبُ الذي لا تلمسه الجدوى يبقى
حيًّا حتى التشغيلة الكاملة.

---

## ⑬ الأداةُ والحرّاس — تُبنى **بعد** دمج هذا العقد

`fcost_arms.py` ‏+ `.github/workflows/fcost.yml` (يدويٌّ بلا كرون).
والأقفالُ `FCK*` في السويّة على **هذا العقد**، و`FCA*` على الأداة حين تُبنى —
**كلُّ قفلٍ تُسقطه طفرةٌ مرّةً عمدًا، والطفرةُ التي تنهار بلاغُ عيبٍ في القفل
لا نجاةٌ للكود.**
