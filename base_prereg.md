# 🧱 T-BASE — تسجيل مسبق: «كم يُكلّف إرجاعُ سقف عرض القاعدة (‏M4) إلى 40؟»

> **مدفوعٌ قبل أيّ رقم.** أمر المالك 2026-08-08: قال «**1**» = «قس M4» بعد أن **رصد
> بنفسه** أن `LGHL` — وهو في القائمة التي بُنيت اليوم — «**امس مرتفع 80٪ ورجع يهبط**»
> وقال: «هذي بحد ذاتها كارثه وتبين لك ان فيه مشكله موجوده عندنا وانت ما تدري عنها».
> **وكان محقًّا: الثغرةُ في بوّابةٍ غير التي كنتُ أقيسها.**

## ① الخلفية — الحالةُ الحيّة والميكانيكا

من سجلّ البوت لـ`LGHL` (‏`weekly_watchlist.json`، جلسة 2026-08-08):
`best_spike`=101.75% · `drop_pct`=99.72% · السعر 0.90 · القاع 0.865 ·
`entry_mode`=**`sweep_confirmed`** «مسح تحت الدعم ثم استعادة» · `setup_type`=
`liquidity_sweep` ⇒ **وُسِم «🟢 جاهز للدخول الآن»**.

🔑 **البوّابةُ المسؤولة `M4` لا `gain5`:** `M4` يحسب
`أعلى(15 جلسة) ÷ أدنى(15 جلسة) − 1`، **والنافذةُ تضمّ ضخّةَ أمس** ⇒ حركةُ ‏+80%
تجعل النسبة **‏≥80% حسابًا مُلزِمًا** (مستوى ما قبل الضخّة وقمّتُها كلاهما داخل
النافذة). حدُّنا الموثّق **40** ⇒ كان يُرفَض `M4_base_واسعة`. المُطبَّق:
**‏23,684.631623040408** ⇒ يمرّ.

🔴 **و`gain5` (‏T-CHASE) قد لا يمسكه أصلًا**: إغلاقٌ مقابل إغلاق على 5 جلسات،
فضخّةٌ ارتدّ جزؤها **تتلاشى في الطرح**. ⇒ التجربتان **مستقلّتان** ولا تنسخ إحداهما
الأخرى. ✅ **وتصحيحٌ لفرضيةٍ فُحصت ورُدَّت:** `M3` يستعمل
`spike_info(exclude_last=BASE_WINDOW=15)` ⇒ ضخّةُ أمس **ليست** «الانفجار السابق»؛
الـ101.75% أقدمُ من 15 جلسة، فمادّةُ الارتكاز حقيقية والعطبُ في **قراءة القاع الآن**.

## ② الدافعُ الأعمق — قرارٌ مقيسٌ نُسِخ صامتًا

`BASE_RANGE_MAX_PCT`=**40** ليس رقمًا عشوائيًّا: **أبقته `M4-LADDER`** (2026-07-31)
بعد أن قاست فتحَه فوجدت **‏+1.76 نقطة عند `z=0.72 · p≈0.47` = داخل الضجيج**،
وحُكمُها المكتوب «‏`BASE_RANGE_MAX_PCT = 40` يبقى — لا تغيير على الأساس».
⇒ إحلالُ `P100` **نسخ حكمًا مقيسًا بلا تجربةٍ جديدة**. وهذي التجربةُ تُعيد السؤال
**تحت النظام الجديد** لأن أرقام `M4-LADDER` قِيست ببوّابات ما قبل الظرف فلا تنتقل.

## ③ الذراعان (مثبَّتتان — ولا ثالثة)

| ذراع | `BASE_RANGE_MAX_PCT` | الوصف |
|---|---|---|
| **B23685** | **23684.631623040408** | الإنتاج اليوم حرفيًّا (الأساس) |
| **B40** | **40.0** | حكمُ `M4-LADDER` الموثّق |

**وكلُّ شيءٍ آخر P100 بلا تغيير** (نفسُ ملفّ الحوافّ · اللقطة · المُرتِّب `rank_live`
· السعة 10 · النافذة). التبديلُ في **سائقٍ بحثيّ** بعد الاستيراد — **صفر مسٍّ
بالإنتاج** — والقيمةُ النافذة تُطبَع لكلّ ذراع.

⚠️ **ولا ذراعَ لقيمةٍ وسطى** (‏60/80/100 التي جرّبتها `M4-LADDER`): إضافتُها بعد
رؤية أرقام B40 = اختيارٌ بعديّ. إن أرادها المالك فتجربةٌ مسجَّلةٌ جديدة.

## ④ البيانات

لقطات PIT الثلاث: **2024=`31199493957` · 2025=`31191327731` · 2026=`31199510868`**
— نفسُها في `T-MINFLOOR` و`T-CHASE` فتُقارَن الأرقام مباشرةً.

🔴 **وحدٌّ يُكتب قبل الأرقام: الباكتيست لا يستطيع إعادةَ اختبار `LGHL` نفسِه** —
حدثُه (‏2026-08-07) **بعد** as-of لقطة 2026. ⇒ `LGHL` **حالةٌ تشخيصية** لا حالةَ
اختبار، والتجربةُ تقيس **كلفةَ السكّان** لا هذا السهم.

## ⑤ بوّابةُ صلاحيةٍ حاكمة (تُقرأ **قبل** أيّ تفسير)

الحكمُ **لا يُفسَّر** إن سقطت أيٌّ من الثلاث (بصمةُ الـ`no-op` الموثّقة):
1. **القيمةُ النافذة تختلف**: ‏23,684.63 مقابل 40.0 (تُطبَع من `CONFIG` بعد التطبيق).
2. **العدّادُ يشتعل**: `M4_base_واسعة` في توزيع رفض B40 **أكبر من صفر**، وفي
   B23685 ‏≈0 — يُقرأ من **عدّاد الإنتاج نفسِه** (`reject_distribution_lines`).
3. **الإشاراتُ تنقص**: `signals(B40) < signals(B23685)`. تساوٍ ⇒ **no-op**.

## ⑥ المقاييس (تُقفَل الآن — بهذا الترتيب)

1. **المنفجرون المُسلَّمون** `d50` بسعة الإنتاج + `d100`.
2. **حارسُ العائد**: متوسط `R`/صفقة.
3. **نسبةُ الدقة** بمقامها، لكل سنةٍ والمجمَّع.
4. **الحجم**: الإشارات · المحسومة · غير المُعبَّأة · المرفوض بالسعة.
5. **كلفةُ الحماية**: عددُ رفضات `M4_base_واسعة`.

## ⑦ قاعدةُ القرار — **تجربةُ كلفةٍ لا تجربةُ حافّة**

`B40` **إرجاعُ حكمٍ مقيسٍ سابق** لا اقتراحٌ جديد، والباكتيستُ **أعمى بنيويًّا** عن
ثمن الشراء بعد ركضة (يُعبّئ عند متوسط الدفعات مهما ارتفع السعر) وعن الوسم الكاذب
«🟢 جاهز». ⇒ المُختبَر: **هل يُكلّف؟**

**يُوصى بالاعتماد** إذا استُوفيت الثلاث:
- **(أ)** `d50` المجمَّع لم ينقص أكثر من **10% نسبيًّا**.
- **(ب)** `R`/صفقة المجمَّع لم يسُؤ أكثر من **0.05R** (حارس `T-CAP`).
- **(ج)** لا انقلابَ صارخ: لا سنةٌ تسوء بأكثر من **20%** في `d50` وأخرى تتحسّن.

**لا يُوصى** إن سقط أيٌّ منها — ويُبلَّغ الرقمُ كما هو، **والقرارُ للمالك في
الحالتين** (يمسّ العضوية و`LOGIC_VERSION`).

⚖️ **وحدٌّ صريح:** «لم يُكلّف» ليست «ربّح». والمكسبُ المُدَّعى عند الحياد =
**استعادةُ حكمٍ مقيسٍ + سدُّ وسمِ «جاهز» الكاذب** لا العائد.

## ⑧ مِجَسٌّ تشخيصيّ **خارج الحكم** (مُعلَنٌ سلفًا)

`BASE_PROBE_SYMS` (افتراضُه `LGHL`) يشغّل `_diagnose_symbol` **الإنتاجيّ** على
الرمز بكلا السقفين ويطبع `base_range` المقيس وحكمَ كلٍّ. **تشخيصٌ لا يدخل أيَّ
مقياسٍ من ⑥** — غرضُه سؤالُ المالك الحرفيّ: «هل كان 40 سيرفض LGHL؟».

## ⑨ تنبّؤات قابلة للكسر (قبل أيّ رقم)

- **B1:** المِجَسّ يُظهر `base_range(LGHL) ≥ 80%` ⇒ **B40 يرفضه** و**B23685 يقبله**.
- **B2:** الإشاراتُ تنقص **بين 15% و60%** في كل سنة (‏M4 كان أعلى الجدران الأولى
  تاريخيًّا: ‏45-49%).
- **B3:** `d50` **ينقص** — و`M4-ISO` قاست الاتجاهَ المعاكس (فتحُ M4 رفع التسليم
  ‏23⟶35 مطلقًا) ⇒ **إغلاقُه يُنقصه**. وهذا **الخطرُ الحقيقيّ** للتجربة وهو ما
  يقيسه الحارس (أ). 🔴 **وإن سقط الحارسُ فالنتيجةُ تُنشَر كما هي ولا تُجمَّل.**
- **B4:** الدقةُ تتغيّر ‏±3 نقاط (لا اتجاهَ متوقَّع).
- **B5:** عدّادُ `M4_base_واسعة` في B23685 = **0 أو قريبٌ منه**.

## ⑩ حدود صدق

انحيازُ بقاء · تشويهُ تقسيمات (‏M4 يقيس مدًى على سلسلةٍ معدَّلة) · بلا افتر ·
المُرتِّبُ عند الصدفة فالسعةُ تقصّ ما لا نتحكّم به · و`LGHL` غيرُ قابلٍ لإعادة
الاختبار (‏§④) · وأرقامُ `M4-LADDER` لا تنتقل للنظام الجديد.

---
*التنفيذ: `base_arms.py` + `base.yml`. النتيجة: `base_result.md`.*
