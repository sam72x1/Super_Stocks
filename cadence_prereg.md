# ⏱️ `T-CADENCE` — تسجيلٌ مسبق: **إيقاعُ السهم**

> **أمرُ المالك (2026-08-28): «اعتماد ١ ٢ ٣ ٤».** العقدُ الثالث — **مدفوعٌ قبل
> سطرِ كودِ أداةٍ واحد** ولا يُمَسّ بعد أوّل رقم.

## ⓪ المصدر — وسطرٌ في كودنا يحسب الجوابَ ثم يرميه

**نصُّ فيصل مرّتين برقمه** (صور 2026-08-28):
- `PPCB`: «**متوسط حركه بعد هبوطه من 1.60 · 20 يوم تقريبا**»
- `CUPR`: «هبط للدعم 7-31 في 2.50 · **متوسط 30 يوم تقريبا**»

🔑 **وقرأتُ `spike_history` سطرًا سطرًا:** تحسب `if i - last >= 20` لتفصل
العناقيد — **ثم تُرجع `mags` وحدَها**. أي أن **الفاصلَ الزمنيَّ محسوبٌ في
الذاكرة ويُهمَل**. وصفرُ مقياسٍ للإيقاع في المستودع كلِّه (‏`trading_days_between`
و`_iso_days_between` مساعدا تقادُمٍ لا إيقاع).

📌 و`bars_to_50` موجودة (بُنيت لـ`T-CANDLE`) — **لكنها تقيس زمنَ المجتمع من
دخولنا، لا إيقاعَ السهم نفسِه**، وأرقامُها **غيرُ منشورة** في `candle_result.md`.

⇒ **وهذا أقربُ محورٍ إلى هدف المالك الحرفيّ**: «ادخل السهم و **خلال اسبوع**
واهو فعلا بينفجر».

## ① التعريفُ الإجرائيّ — مثبَّتٌ ولا يُحرَّك

عند نقطة الإشارة (‏بيانات ما قبلها حصرًا):
- **رفعاتُ السهم** = عناقيدُ `spike_history` نفسُها بتعريفها القائم حرفيًّا
  (عتبةُ `EXPLOSION_PCT` · نافذةُ `PRIOR_SPIKE_WINDOW` · فجوةُ 20 جلسةً تفصل).
- **`gaps`** = الفواصلُ **بالجلسات** بين نهايات العناقيد المتتالية.
- **`cadence`** = **وسيطُ** `gaps` (‏لا المتوسّط — الوسيطُ يقاوم الذيل).
- **`since_last`** = جلساتٌ منذ نهاية آخر عنقود.
- **`due`** = `since_last ≥ cadence` (‏«حان إيقاعُه»).

**ومَن له أقلُّ من رفعتين ⇒ `cadence = None`** ولا يدخل الذراعَ الحاكمة.

## ② المجتمعُ والأذرع

المجتمع: **إشاراتُ الفارز** في لقطات PIT الثلاث — الميزانيةُ الثابتة نفسُها.

| الذراع | التعريف |
|---|---|
| **`C0`** | كلُّ الإشارات (الأساس) |
| **`C1`** | `due` صحيح (**الحاكمة**) |
| **`C̄1`** | `due` خطأ — **شاهدُ التكذيب** |
| **`C2`** | `cadence` معلومٌ **وقصير** (‏≤10 جلسات) — وصفيٌّ لا يحكم |

## ③ المقياسُ الحاكم

**`R` المحقَّق لكلّ صفقة.** والمساندُ **الأهمّ عمليًّا وهو وصفيّ**:
**وسيطُ `bars_to_50`** — الجواب المباشر لسؤال «خلال أسبوع؟».

## ④ المعايير — الأربعةُ تلزم معًا

1. `C1 − C̄1` **‏≥ +0.15R** مجمَّعًا (عتبةٌ مُعادة)
2. موجبٌ في السنوات الثلاث بلا انقلاب
3. فاصلُ 95% لا يلمس الصفر
4. أرضية **‏150 محسومةً مجمَّعًا و30/سنة لكلّ ذراع**

## ⑤ بوّاباتُ الصلاحية

| البوّابة | الشرط | الإخفاق |
|---|---|---|
| **`CV0`** | `C0` يُعيد رقمًا **منشورًا** للأساس بت-بت | خروج **3** |
| **`CV1`** | `C1` و`C̄1` **تفترقان** · وليست إحداهما فارغةً | خروج **4** |
| **`CV2`** | العناقيدُ من `spike_history` **بالاسم** (AST) — لا مِشيةَ منسوخة | قفلُ سويّة |
| **`CV3`** | نصيبُ مَن له `cadence` معلومٌ **‏≥30%** من الإشارات — وإلّا المقياسُ خامدٌ ويُعلَن | يُعلَن ويُقاس |
| **`CV4`** | لا نظرَ مستقبليّ: العناقيدُ محسوبةٌ على `df.iloc[:i]` حصرًا | قفلٌ سلوكيّ |

## ⑥ التنبّؤات

| # | التنبّؤ | ما يكذّبه |
|---|---|---|
| `CD-P1` | **لا تبلغ +0.15R** — أرجّح الفشل | تبلغها |
| `CD-P2` | **وسيطُ الإيقاع ‏15-40 جلسة** (فيصل قال 20 و30) | خارجه |
| `CD-P3` | **وسيطُ `bars_to_50` فوق 5 جلسات** ⇒ «خلال أسبوع» **متفائل** | ‏≤5 |
| `CD-P4` | نصيبُ `cadence` المعلوم **‏≥50%** (أغلبُ مرشّحينا لهم رفعتان فأكثر) | دونه |
| `CD-P5` | `C2` (الإيقاعُ القصير) **لا يتفوّق** — القِصَرُ ليس جودة | يتفوّق |

## ⑦ سقفُ النجاح

**اقتراحٌ للمالك + سطرُ عرضٍ («‏⏱️ إيقاعُه ‏N جلسة · مضى M»)** — ⛔ **ولا
بوّابةَ رفضٍ ولا وزنَ ترتيبٍ ولا تغييرَ نافذةٍ زمنية** مهما كانت الأرقام.

## ⑧ حدودُ صدق

1. **الإيقاعُ يُقاس بتعريفنا للرفعة** (‏`EXPLOSION_PCT`) لا بتعريف فيصل — وهو
   **يعدّ بالورقة** («قلم وورقه … سهم كذا صعد نسبة كذا بيوم كذا»).
2. «‏20 يوم» و«‏30 يوم» عندهما **يومٌ تقويميّ أم جلسة؟ غيرُ محسوم** — نقيس
   بالجلسات ونُعلن الفارق.
3. **وسيطُ عيّنةٍ صغيرة** (رفعتان ⇒ فاصلٌ واحد) هشّ — يُعَدّ ويُعلَن.
4. `bars_to_50` **لمسٌ لا تنفيذ** ⇒ سقفٌ متفائل.
